"""Golden-runner (CONTRACT-03) — the walking-skeleton equivalence loop.

Spawns the fixture-grade .NET toy converter over the A-model CLI boundary, normalizes both the
converter output and the approved ``golden/`` baseline through the shared §4-5 core, and diffs.
``/golden-approve`` ships here in minimal form: a ``.received``/``.verified`` split whose promotion
no agent can self-bless (P9).
"""
