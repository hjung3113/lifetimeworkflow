# AGENTS.md — .NET package (nearest-wins)

> Scope: `libs/dotnet/` (and `components/` .NET code). Refines the root `AGENTS.md` and
> **wins nearest** for .NET work. This file is **self-sufficient**: it restates the repo
> non-negotiables below because nested-`AGENTS.md` merge semantics differ per runtime
> (Codex replaces, others concat) — never rely on inheritance (P11).

## .NET-local rules

- **Toolchain:** .NET 10 SDK (`dotnet *` scope). Build/test with `dotnet build` /
  `dotnet test`. NOTE: the SDK is egress-blocked in this ephemeral container (BOOT-01) —
  .NET execution is deferred; author code + tests, but run them once the SDK is installed.
- **Tests:** xunit.v3 (Microsoft.Testing.Platform built in) via `dotnet test`.
- **Golden / approval:** Verify.XunitV3 — `.received`/`.verified` snapshot workflow; promotion
  is human review at the PR (the `/examples/*/golden/` CODEOWNERS entry), never an agent
  self-bless.
- **Schema validation:** JsonSchema.Net (System.Text.Json, Draft 2020-12) — no Newtonsoft.
- **Normalization core:** the §4.3–4.6 canonicalization .NET impl lives here
  (`libs/dotnet/Normalize`), cross-validated against `libs/python` via the shared
  `libs/normalize-fixtures` corpus. Emit UTF-8 without BOM, force LF, InvariantCulture.
- **Boundary:** CLI-spawn / file / DB per the A-model — the parser/converter talks to the
  Python side only across process/file/DB, never in-process object passing.

## Non-negotiables (restated per-package — P11 backstop)

These are **not** inherited-only. They hold with full force inside this package:

1. **Contract-first.** `contracts/` is the single source of truth. Code that disagrees with
   the contract is wrong — fix the code, not the contract. Contract changes carry a
   golden / contract-drift gate (`bash tools/contract_drift/check.sh`).

2. **§4.3–4.6 polyglot boundary invariants.** Cross-language equivalence is checked only
   **after** the shared canonicalization core, never a raw byte-diff: UTF-8 with **BOM
   stripped**, forced **LF**, InvariantCulture `.` decimals (`.NET ToString` is
   locale-dependent — always InvariantCulture), tolerance-aware float compare, deterministic
   key/row ordering, **UTC** ISO-8601 timestamps (`DateTime.Kind` discipline), explicit TSV
   escape + null-vs-empty token. Language boundary = process/file/DB only (A-model).

3. **Constitution plane is gated (machines gate, humans ratify).** Do **not** write to
   `contracts/`, `docs/adr/`, or `golden/` from .NET code or agents. Golden baselines are
   human-promoted; ADRs are append-only. Runtime enforcement is Phase-4 hooks.

4. **Derived plane is not hand-edited.** `.memory/derived/` (repo-map, contracts-index) is
   regenerated by `tools/memory_regen`; delete + rerun reproduces it byte-identically.

See root `AGENTS.md` for the monorepo map and the full golden-path command table.
