# Repo Agents

This is the target repository's own AGENTS.md, authored locally before adoption. It has no
harness-managed markers yet — the adoption apply cycle must append the managed block exactly once.
