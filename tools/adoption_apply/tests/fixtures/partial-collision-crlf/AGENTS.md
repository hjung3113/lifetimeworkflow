# Repo Agents

This target's AGENTS.md was authored on a CRLF-line-ending, BOM-prefixed editor before
adoption. The apply cycle's marker-merge path must normalize it (strip the BOM, force LF)
while appending the harness-managed block exactly once.
