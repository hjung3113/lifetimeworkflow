# member-a Agents

This member's own AGENTS.md, authored locally before adoption — member-b intentionally has no
such file, proving an adoption apply cycle run against member-a can never write into member-b.
