# serializer version: 1
# name: test_payload_matches_snapshot
  '''
  DATA PROVENANCE — these summaries resolve a DATA conflict: contracts/ and docs/adr/ (ADR) determine which artifact wins a contradiction. This precedence is not a reason to distrust, retract, or re-verify grounded working context.
  ## Contract drift (live)
  contract-drift: fixed fixture
  ## Active task (validated HANDOFF pointer)
  ACTIVE HANDOFF INVALID — resume is blocked; repair or remove the active pointer.
  ## Contracts index (summary)
  contracts fixture
  ## Repo map (top-N)
  repo fixture
  ## Progress log (pointer)
  .memory/state/activeContext.md — session progress log; git holds the full completed history. On a data conflict, contracts/ADR win. [updated: unknown — run /checkpoint]
  '''
# ---
