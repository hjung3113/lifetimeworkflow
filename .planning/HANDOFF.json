{
  "version": "1.0",
  "timestamp": "2026-07-18T00:00:00Z",
  "phase": "milestone-boundary",
  "phase_name": "v2.1 shipped; next milestone scoping deferred to next session",
  "status": "paused",
  "completed_this_session": [
    {"id": 1, "name": "v2.1 (MEM2) milestone audit — refreshed to status: passed, 7/7 requirements"},
    {"id": 2, "name": "Phase 16 (Local Memory Web UI, MEM2-07) — discuss(--auto) → UI-spec → research → pattern-map → plan(6 plans/5 waves) → execute → code-review(CR-01 + 3 warnings fixed) → verify. Browser round-trip 5/5 verified live via Orca CLI browser.", "status": "complete"},
    {"id": 3, "name": "Phases 12/13/14 goal-verification — generated the missing VERIFICATION.md (12: 4/4, 13: 4/4, 14: 6/6, all passed). Closed the recurring 'has SUMMARY != verified' gap.", "status": "complete"},
    {"id": 4, "name": "Nyquist backfill — Phases 13 & 14 VALIDATION.md finalized (status: final, nyquist_compliant: true). 13 got 2 new test files (7 tests); 14 was already covered (5/5).", "status": "complete"},
    {"id": 5, "name": "Quick task 260716-16x (6 broken GSD hooks) — confirmed already fixed + committed (085064c); cleared from audit-open (stale global gsd-sdk needed a bare SUMMARY.md alias).", "status": "complete"},
    {"id": 6, "name": "tools/memory_ui/README.md written", "status": "complete"},
    {"id": 7, "name": "Milestone v2.1 archived (milestones/v2.1-{ROADMAP,REQUIREMENTS,MILESTONE-AUDIT}.md + v2.1-phases/12..16; REQUIREMENTS.md deleted; ROADMAP collapsed; MILESTONES/PROJECT/STATE updated). Commit 3b8a39e.", "status": "complete"},
    {"id": 8, "name": "git tag v2.1 created + pushed; branch claude/data-pipeline-harness-8aypct pushed to origin (github.com/hjung3113/lifetimeworkflow).", "status": "complete"}
  ],
  "milestone_state": {
    "shipped": "v2.1 MEM2 — Process Memory & Provenance Reframe (2026-07-18)",
    "tag": "v2.1 (pushed to origin)",
    "full_suite": "690 passed / 0 failed",
    "archived_milestones": ["v1.0 (phases 1-8, not formally milestone-closed)", "v2.0 (phases 9-11)", "v2.1 (phases 12-16)"],
    "live_phases": ["17-constitution-gate-dev-enforce-decoupling (infra, complete, independent of MEM2)", "01-08 (v1.0, never formally archived)"]
  },
  "next_action": {
    "command": "/gsd:new-milestone",
    "version": "v2.2 (minor) or v3.0 (major) — user picks",
    "theme": "harness hardening / gate-tightening PLUS additional features the user will specify next session",
    "user_directive": "1번(하네스 하드닝) 포함 + 다른 기능도 추가 예정 — 다음 세션에서 스코핑",
    "note": "new-milestone was entered this session but intentionally paused at the 'what to build next' gate; NO new-milestone artifacts (requirements/roadmap) were written. Re-run /gsd:new-milestone fresh next session and answer its questioning with the theme below + the user's additional features."
  },
  "candidate_requirements_for_hardening_theme": [
    "CI-gate the provenance lint: there is NO .pre-commit-config.yaml and no dedicated CI step running `python -m tools.harness_lint.provenance` against the live .memory/agreements/ dir. tools/harness_lint/provenance.py is unit-tested + wired into /lint, but a malformed agreement committed without running /lint is not caught automatically. (v2.1 integration warning 1)",
    "Fix check_agreement() body-shape gap: it validates frontmatter only (status/added/provenance). A valid-frontmatter/empty-body agreement passes lint but _render_agreement() silently returns None and drops the entry — fails silently. Add a body check (# Title line + non-empty rule line). (v2.1 integration warning 2)",
    "Verify/harden the CI emit-drift gate against untracked files: 15-REVIEW.md CR-01 flagged bare `git diff --exit-code` being blind to untracked emitted artifacts. Phase 16-04 verified emit via a CI replica using `git add -A` + `git diff --cached`; confirm the ACTUAL .github/workflows/ci.yml emit-drift leg uses the untracked-safe idiom (the fix idiom already exists at ci.yml:206).",
    "Reword the EXPECTED_COMMANDS mis-wording: MEM2-04 requirement text + ROADMAP Phase 15 SC1 name a constant `EXPECTED_COMMANDS` that has never existed in source (0 repo hits). Substance is satisfied by glob-driven test_commands.py discovery. Reword the docs so a future phase doesn't rediscover the ghost. (D-11)",
    "(deferred, optional) MEM2-F1: per-instance agreement overlays for examples/* (like project.toml/ADR-0003 overlays) — v2.1 explicitly deferred; MVP was one global core channel.",
    "+ ADDITIONAL FEATURES: the user will specify these next session — do NOT assume; ask in new-milestone questioning."
  ],
  "required_reading_next_session": [
    ".planning/PROJECT.md — Current State + Next Milestone (theme note)",
    ".planning/MILESTONES.md — v2.1 accomplishments",
    ".planning/milestones/v2.1-MILESTONE-AUDIT.md — the passed audit + tech_debt list (source of the hardening candidates)",
    ".planning/milestones/v2.1-REQUIREMENTS.md — the v2 Requirements / Out of Scope (MEM2-F1)"
  ],
  "infrastructure_notes": [
    ".claude/settings.local.json HARNESS_DEV_BYPASS may be set for the dev session (Phase-17 posture); constitution-plane writes (contracts/adr/golden) can land without the human token in-session — CODEOWNERS still gates PR merge. Phase 16 touched none of the constitution plane.",
    "Untracked .mcp.json at repo root is pre-existing and intentionally left alone.",
    "The running `gsd-sdk` is a stale npx-cached global (~/.npm/_npx/.../get-shit-done-cc); the repo's own .claude/get-shit-done is newer. Quirk: audit-open needs a bare SUMMARY.md (not just ${id}-SUMMARY.md) for quick tasks."
  ]
}
