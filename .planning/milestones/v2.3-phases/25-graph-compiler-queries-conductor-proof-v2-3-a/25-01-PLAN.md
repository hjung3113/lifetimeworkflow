---
phase: 25-graph-compiler-queries-conductor-proof-v2-3-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/contract_graph/pyproject.toml
  - tools/contract_graph/__init__.py
  - tools/contract_graph/compile.py
  - tools/contract_graph/tests/test_compile.py
  - tools/harness_lint/tests/test_contract_graph_config.py
  - tools/harness_config/loader.py
  - tools/harness_config/tests/test_topology_relationships.py
autonomous: true
requirements: [TOPO-04]

must_haves:
  truths:
    - "D-02: a malformed relationship (unresolved authority, dangling dependent, or an authority that doesn't own a tracked contract) is reported as a stable, grep-able descriptive slug — unresolved-authority / dangling-endpoint / unknown-contract — not a numbered code and not a bare crash."
    - "A relationship graph containing fan-in, fan-out, a disconnected component, or a canonical (legal) cycle compiles with EMPTY diagnostics — these shapes are structurally legal, never flagged."
    - "The core default project config and the default workspace config both compile with zero diagnostics via the new gate, mirroring test_pipeline_config.py's structural-scan idiom."
    - "effective_relationships() no longer crashes with a bare KeyError on a relationship record missing a required key (WR-02 closed) — it raises a ValueError naming the offending record, and its signature/return shape are unchanged."
  artifacts:
    - path: "tools/contract_graph/compile.py"
      provides: "compile_graph(cfg=None) -> {relationships, adjacency, diagnostics} — validates endpoints + authority-owned-contract resolution on top of effective_relationships()"
      exports: ["compile_graph"]
    - path: "tools/contract_graph/pyproject.toml"
      provides: "new uv workspace member (package=false, zero deps), auto-discovered by the root `tools/*` glob"
    - path: "tools/harness_lint/tests/test_contract_graph_config.py"
      provides: "the TOPO-04 consistency gate — zero-diagnostics assertion on core/instance/workspace configs + one test per diagnostic slug"
  key_links:
    - from: "tools/contract_graph/compile.py"
      to: "tools/harness_config/loader.py::effective_relationships"
      via: "compile_graph imports and calls effective_relationships(cfg) unchanged — never re-lowers/re-unions"
      pattern: "effective_relationships\\("
    - from: "tools/contract_graph/compile.py"
      to: "contract existence check"
      via: "reuses the schema-glob existence idiom already used 3x in this repo (rglob *.schema.json)"
      pattern: "rglob\\(.\\*\\.schema\\.json.\\)"
---

<objective>
Build the domain-neutral graph compiler — a validation/resolution layer that sits ON TOP of Phase 24's
`effective_relationships()` — plus the `harness_lint` consistency gate that proves it stays green on the
core/instance/workspace defaults. Close WR-02 (bare `KeyError` on malformed input) as a small, sanctioned
guard addition to `loader.py`.

Purpose: TOPO-04 requires a single deterministic implementation that emits stably-ordered, repo-confined
graph data and stable diagnostic slugs, validating endpoint declarations and authority-owned-contract
existence while explicitly ACCEPTING fan-in, fan-out, disconnected components, and canonical cycles as
legal shapes. This is the foundation Plan 02 (queries), Plan 03 (conductor), and Plan 04 (proof fixtures)
all build on.
Output: `tools/contract_graph/` package (new uv workspace member) with `compile_graph()`; a new
`harness_lint` gate test file; a WR-02 guard in `tools/harness_config/loader.py`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-CONTEXT.md
@.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-RESEARCH.md
@.planning/phases/24-contract-relationship-vocabulary-compatibility-v2-3-a/24-REVIEW.md

<interfaces>
From `tools/harness_config/loader.py` (read verbatim; DO NOT change signatures/return shapes):
- `effective_relationships(cfg: dict | None = None) -> list[dict]` — returns stable-sorted
  `{id, contract, authority, dependents}` records; raises `ValueError` on duplicate id / duplicate
  semantic edge / contradiction. `authority` is a scalar string; `dependents` is a non-empty list of
  strings. Lowered legacy ids are `pipeline/<contract>/<from>-><to>`.
- `components(cfg=None) -> list[dict]` — each dict has `id`, `stage`, `language`, `produces`, `consumes`.
- `languages(cfg=None) -> list[dict]`, `load_project(path=...) -> dict`.
- `contract_graph_relationships(cfg=None) -> list[dict]` — raw passthrough, DO NOT re-derive from this;
  `effective_relationships` already unions it.

From `tools/workspace_config/loader.py` (read verbatim):
- `load_workspace(path=...) -> dict`, `members(cfg=None) -> list[dict]` (each has `id`, `root`),
  `edges(cfg=None) -> list[dict]`, `contract_graph_relationships(cfg=None) -> list[dict]`.
- `split_endpoint(endpoint: str) -> tuple[str | None, str]` — `"repo:stage"` -> `("repo","stage")`;
  bare `"stage"` -> `(None, "stage")`. Reuse this directly for ALL endpoint parsing (project and
  workspace cfg alike) — do not write a second parser.

From `tools/contract_drift/drift.py` (existence-glob idiom, read at lines ~281-284, mirrored at
`tools/harness_lint/tests/test_pipeline_config.py:82` and `test_workspace_config.py:97-99`):
- The pattern `{p.name.removesuffix(".schema.json") for p in dir.rglob("*.schema.json")}` is the
  established "does a tracked schema exist for this contract id" check. Reuse this exact idiom against
  `contracts/` (project) or `<producer_member_root>/contracts/` (cross-repo authority).
</interfaces>

<gate_idiom>
From `tools/harness_lint/tests/test_pipeline_config.py` (mirror this file's shape exactly, substituting
`effective_relationships`/`compile_graph` for `components`/`pipeline`):
- Repo root via `Path(__file__).resolve().parents[3]`.
- Load real config through the shared loader (no mocking).
- Iterate/assert/fail-loud with an f-string naming the offending record.
- `_CONTRACTS_DIR = _REPO_ROOT / "contracts"`.
</gate_idiom>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold `tools/contract_graph/` + implement `compile_graph()` accepting fan-in/fan-out/disconnected/cycle</name>
  <files>tools/contract_graph/pyproject.toml, tools/contract_graph/__init__.py, tools/contract_graph/compile.py, tools/contract_graph/tests/test_compile.py</files>
  <read_first>tools/harness_config/pyproject.toml (mirror package=false shape verbatim), tools/harness_config/__init__.py (mirror PEP 562 lazy re-export), tools/harness_config/loader.py (effective_relationships, components, languages), tools/workspace_config/loader.py (members, split_endpoint), tools/contract_drift/drift.py lines ~250-300 (existence-glob idiom)</read_first>
  <behavior>
    - Test: a cfg whose `effective_relationships()` includes one authority with 3 dependents (fan-out) compiles with `diagnostics == []` and `adjacency[authority]` sorted-equal to the 3 dependents.
    - Test: a cfg whose `effective_relationships()` has 3 relationships sharing one dependent id from 3 different authorities (fan-in) compiles with `diagnostics == []`.
    - Test: a cfg with a relationship whose dependent never appears as anyone's authority (disconnected component) compiles with `diagnostics == []`.
    - Test: a cfg containing a legal two-node cycle (A depends on B, B depends on A, both declared, both contracts tracked) compiles with `diagnostics == []` — cycles are never flagged by the compiler.
    - Test: the unedited core default (`load_project()`) and the unedited default workspace (`load_workspace()`, adapted via a project-shaped wrapper) both compile with `diagnostics == []`.
  </behavior>
  <action>
    Create `tools/contract_graph/pyproject.toml` mirroring `tools/harness_config/pyproject.toml` verbatim in
    shape: `name = "logparser-contract-graph"`, `requires-python = ">=3.11"`, `dependencies = []`,
    `[tool.uv] package = false`. It is auto-discovered by the root `[tool.uv.workspace] members = ["libs/python", "tools/*"]` glob — do not edit root `pyproject.toml`.

    Create `tools/contract_graph/__init__.py` with PEP 562 lazy re-export (`__all__ = ["compile_graph"]`,
    `__getattr__` delegating to `tools.contract_graph.compile`), mirroring `tools/harness_config/__init__.py`
    exactly (avoids the conftest-collection ordering hazard documented there).

    Create `tools/contract_graph/compile.py` with `compile_graph(cfg: dict | None = None) -> dict`. Steps:
    (1) call `effective_relationships(cfg)` unchanged — this is the ONLY source of relationship records,
    never re-derive from raw `[pipeline].edges`/`[[contract_graph.relationships]]`; (2) build the endpoint
    declaration set by calling `components(cfg)` for project-shaped cfg (fallback to an empty set when the
    key is absent) and, when a `members` key is present in cfg (workspace-shaped), also collect declared
    member ids; (3) for every relationship's `authority` and each `dependents[i]`, resolve via
    `split_endpoint`: if the repo half is non-None it must be a declared member id; if the repo half is
    None the bare stage must be a declared component id OR a declared member id; (4) build
    `adjacency: dict[str, list[str]]` — `authority -> sorted(dependents)` across ALL relationships whose
    authority resolved successfully (a legal cycle is just two entries pointing at each other; no special
    casing); (5) return `{"relationships": relationships, "adjacency": {k: adjacency[k] for k in sorted(adjacency)}, "diagnostics": []}`
    for now (diagnostic emission is Task 2). Diagnostics are ALWAYS returned empty-or-populated as a
    sorted `list[str]`, never raised as exceptions (mirrors `contract_drift.run_gate`'s result-dict
    pattern) — the only exception that propagates is `effective_relationships()`'s own `ValueError`.
  </action>
  <acceptance_criteria>
    `uv run pytest tools/contract_graph/tests/test_compile.py -q` exits 0, all 5 behavior tests pass.
    `python -c "from tools.contract_graph import compile_graph; print(compile_graph()['diagnostics'])"`
    prints `[]` for the core default.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/contract_graph/tests/test_compile.py -q</automated>
  </verify>
  <done>`tools/contract_graph/` is a working uv member; `compile_graph()` accepts fan-in/fan-out/disconnected/cycle with empty diagnostics on all fixtures and the unedited core/workspace defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Diagnostic slugs — unresolved-authority / dangling-endpoint / unknown-contract</name>
  <files>tools/contract_graph/compile.py, tools/contract_graph/tests/test_compile.py</files>
  <read_first>contracts/harness/topology/relationship.schema.json (field shape), tools/harness_lint/tests/test_pipeline_config.py (test_edge_contracts_have_a_tracked_schema — the produces/consumes ownership check to mirror for the project case), tools/harness_lint/tests/test_workspace_config.py (test_edge_contracts_tracked_in_producer — the producer-member ownership check to mirror for the cross-repo case)</read_first>
  <behavior>
    - Test: a relationship whose `authority` does not resolve to any declared component/member produces
      exactly one diagnostic string starting with `"unresolved-authority: "` naming the relationship id
      and the unresolved authority string.
    - Test: a relationship whose `dependents` contains one unresolved endpoint produces exactly one
      diagnostic string starting with `"dangling-endpoint: "` naming the relationship id and the
      unresolved dependent string; the OTHER (resolved) dependents are still present in `adjacency`.
    - Test: a relationship whose authority resolves to a declared component that does NOT list the
      relationship's `contract` in its `produces`, OR whose contract has no tracked
      `contracts/**/<contract>.schema.json` when the authority has no `produces` field, produces exactly
      one diagnostic string starting with `"unknown-contract: "`.
    - Test: `diagnostics` is always returned `sorted()` (two runs on the same malformed cfg produce
      byte-identical diagnostic lists in the same order).
  </behavior>
  <action>
    Extend `compile_graph` (from Task 1) to append diagnostic strings during endpoint/contract
    resolution instead of silently dropping unresolved relationships: an unresolved `authority` appends
    `f"unresolved-authority: relationship {rel['id']} authority {authority!r} is not a declared component or member"`
    and excludes that relationship's authority row from `adjacency`; an unresolved dependent appends
    `f"dangling-endpoint: relationship {rel['id']} dependent {dep!r} is not a declared component or member"`
    and excludes only that dependent from the authority's adjacency list (siblings still included). For
    contract ownership: when the authority resolves to a component/member declaration carrying a
    `produces` field, require `rel['contract'] in produces`, else append
    `f"unknown-contract: relationship {rel['id']} contract {rel['contract']!r} not owned by authority {authority!r}"`;
    when the authority declaration carries no `produces` field (an opaque logical authority with no
    component backing, or a cross-repo `repo:stage` authority), fall back to existence-only — reuse the
    schema-glob idiom against `contracts/` (project) or `<producer_member_root>/contracts/` (cross-repo,
    resolved via the authority's repo half) — appending
    `f"unknown-contract: relationship {rel['id']} contract {rel['contract']!r} has no tracked schema"`
    when absent. Sort `diagnostics` before returning.
  </action>
  <acceptance_criteria>`uv run pytest tools/contract_graph/tests/test_compile.py -q` exits 0 with all Task-1 + Task-2 tests passing (9+ tests total).</acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/contract_graph/tests/test_compile.py -q</automated>
  </verify>
  <done>All three D-02 slugs fire exactly once per violated constraint, are stable-sorted, and the core/instance/workspace defaults still compile with `diagnostics == []`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `harness_lint` consistency gate + WR-02 closure (bare KeyError -&gt; ValueError)</name>
  <files>tools/harness_lint/tests/test_contract_graph_config.py, tools/harness_config/loader.py, tools/harness_config/tests/test_topology_relationships.py</files>
  <read_first>tools/harness_lint/tests/test_pipeline_config.py (structural-scan idiom to mirror verbatim: parents[3] repo root, load via shared loader, iterate/assert/fail-loud), tools/harness_config/loader.py lines 90-176 (effective_relationships — the four `rel[...]` / `edge[...]` dict indexes to guard), .planning/phases/24-.../24-REVIEW.md WR-02 section (the exact fix suggestion)</read_first>
  <behavior>
    - Test: `uv run pytest tools/harness_lint/tests/test_contract_graph_config.py -q` — the core default
      project config compiles with `diagnostics == []`.
    - Test: the default workspace config compiles with `diagnostics == []`.
    - Test: a synthetic relationship record missing the `authority` key raises `ValueError` (not
      `KeyError`) naming the missing key and the offending record, from `effective_relationships`
      directly (WR-02 regression).
  </behavior>
  <action>
    Create `tools/harness_lint/tests/test_contract_graph_config.py` mirroring
    `test_pipeline_config.py`'s idiom exactly: import `compile_graph` from `tools.contract_graph` and
    `load_project`/`load_workspace` from their respective loaders; assert
    `compile_graph(load_project())["diagnostics"] == []` and
    `compile_graph(load_workspace())["diagnostics"] == []` — these are the two "must stay green" anchors
    the gate protects. Add WR-02 closure to `tools/harness_config/loader.py::effective_relationships`:
    before the lowering comprehension and the dedup loops, guard that every relationship record and
    every raw edge carries `{"id", "contract", "authority", "dependents"}` (for records) or
    `{"from", "to", "contract"}` (for raw edges), raising
    `ValueError(f"effective_relationships: relationship record missing key(s) {sorted(missing)}: {rel!r}")`
    naming the offending record — never let a bare `KeyError` escape. Do NOT change the function's
    signature or return shape (Pitfall 2) — this is an additive guard only. Add the WR-02 regression test
    to `tools/harness_config/tests/test_topology_relationships.py`, following the Phase-24 GEN-04-safe
    precedent (non-contiguous `Path` segments for any instance reference; this test needs none since it
    is a synthetic in-memory cfg dict).
  </action>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint/tests/test_contract_graph_config.py -q` exits 0.
    `uv run pytest tools/harness_config/tests/test_topology_relationships.py -q` exits 0.
    `uv run pytest -q` (full suite) exits 0 with no new failures.
    `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0 (GEN-04 unaffected).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_contract_graph_config.py tools/harness_config/tests/test_topology_relationships.py -q</automated>
  </verify>
  <done>The new gate is wired and green on both default configs; WR-02 is closed (ValueError with an actionable message replaces the bare KeyError, signature/shape unchanged); full suite + GEN-04 stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML config -> compiler | `harness/project.toml` / `workspace.toml` / an instance overlay are human/instance-authored but parsed as untrusted-shape input (V5) — a malformed or hostile record must not crash ungracefully or silently misresolve. |
| Compiler -> filesystem | The existence-glob idiom constructs a path from a config-derived contract id joined under a `contracts/` root; this must stay repo-confined. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `tools/contract_graph/compile.py` | mitigate | Every unresolved endpoint/contract yields a diagnostic slug, never a crash; the only propagating exception is `effective_relationships()`'s own `ValueError`, now closed against bare `KeyError` (WR-02, Task 3). |
| T-25-02 | Information Disclosure / Tampering | schema-existence glob | accept | Contract ids are already trusted-shape strings validated by `relationship.schema.json` (`minLength:1`); the glob only reads filenames under a fixed `contracts/` root, never writes, and the root is not derived from user input beyond a repo-relative id string — consistent with the existing accepted risk posture in `test_pipeline_config.py`/`test_workspace_config.py`. |
</threat_model>

<verification>
`uv run pytest tools/contract_graph tools/harness_lint/tests/test_contract_graph_config.py tools/harness_config/tests/test_topology_relationships.py -q` exits 0.
`uv run pytest -q` (full suite) exits 0.
`uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0.
</verification>

<success_criteria>
`compile_graph()` exists, is deterministic, accepts fan-in/fan-out/disconnected/cycle with empty diagnostics, emits the three D-02 slugs on the corresponding violations, and is wired into a green `harness_lint` gate on both default configs. WR-02 is closed without touching `effective_relationships()`'s signature or return shape.
</success_criteria>

<output>
Create `.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-01-SUMMARY.md` when done
</output>
