---
phase: 25-graph-compiler-queries-conductor-proof-v2-3-a
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - harness/commands/pipeline.md
  - harness/skills/pipeline-map/SKILL.md
  - harness/agents/orchestrator.md
  - tools/harness_lint/tests/test_conductor_graph_render.py
  - .opencode
  - .claude
autonomous: true
requirements: [TOPO-06]

must_haves:
  truths:
    - "D-01: /pipeline and pipeline-map render a non-linear graph as an indented tree rooted at authority endpoints, with a legal cycle rendered as an explicit `(cycle -> <node>)` terminal marker instead of recursing."
    - "D-01/TOPO-06: the EXISTING linear render text (the two-stage 'stage N: ... consumes=... produces=...' block and the 'from -> to (contract)' block) is byte-identical to what shipped before this plan — proven by an automated literal-text regression test, not by inspection."
    - "TOPO-06: no new command and no new persona were introduced — orchestrator stays the one mode:primary persona named 'orchestrator'; the total emitted command count is unchanged."
    - "TOPO-06: harness/ round-trips to .opencode/ and .claude/ byte-identical after the conductor edits, with zero model identifiers in the projected trees."
  artifacts:
    - path: "harness/commands/pipeline.md"
      provides: "the existing linear render PLUS an additional D-01 indented-tree section for the general (branching/cyclic) case"
    - path: "harness/skills/pipeline-map/SKILL.md"
      provides: "the same D-01 indented-tree addition, consistent with pipeline.md"
    - path: "tools/harness_lint/tests/test_conductor_graph_render.py"
      provides: "the byte-identity regression for the linear-case text blocks + the indented-tree/cycle-marker token presence check"
  key_links:
    - from: "harness/agents/orchestrator.md"
      to: "tools/contract_graph"
      via: "the 'Trace the topology' intake step references direct/reverse/transitive for affected-set routing when the graph is non-linear"
      pattern: "contract_graph"
    - from: "tools/harness_emit"
      to: ".opencode/ + .claude/"
      via: "re-run with zero emitter code change, glob discovery picks up the edited harness/ sources"
      pattern: "harness_emit"
---

<objective>
Generalize the three EXISTING conductor surfaces — `/pipeline`, `pipeline-map`, and `orchestrator.md`
— to consume the compiled graph (Plan 01) and affected-set queries (Plan 02), rendering non-linear
topologies as D-01's indented tree while keeping the current linear render byte-identical. Round-trip
the edited `harness/` source through the existing emitter to both runtimes.

Purpose: TOPO-06 requires the graph become usable through the EXISTING user-facing surface — no
second interpreter, no new command, no new persona — with the hard invariant that today's linear
topology output must not change for anyone still on a linear config.
Output: edited `pipeline.md` / `pipeline-map/SKILL.md` / `orchestrator.md`; a new byte-identity +
prose-token regression test; a re-emitted `.opencode/` + `.claude/`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-CONTEXT.md
@.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-RESEARCH.md

<interfaces>
Existing linear-case text this plan MUST preserve byte-identical (source: `harness/commands/pipeline.md`
lines 44-53, 63-66; `harness/skills/pipeline-map/SKILL.md` mirrors the same concept in prose, no
literal example block there today):
```
stage 1: source (python) consumes=[] produces=[greeting]
stage 2: sink (python) consumes=[greeting] produces=[]
```
```
source -> sink (greeting)
```

`orchestrator.md` current "Trace the topology" intake step (step 4, read verbatim — extend, do not
replace): reads `[[components]]`/`[pipeline]` via `tools.harness_config` (`components()`/`pipeline()`)
to identify which stage/component a request touches and its upstream/downstream edge contracts.

Plan 01/02 outputs to consume (read the finished files, do not re-implement):
- `tools.contract_graph.compile_graph(cfg=None) -> {"relationships", "adjacency", "diagnostics"}`.
- `tools.contract_graph.direct/reverse/transitive(graph, node) -> {"ids": [...], "paths": [[...]]}`.

Anti-sprawl gates this plan MUST NOT trip (read to confirm counts before/after):
- `tools/harness_lint/tests/test_agents.py` — `EXPECTED_PERSONAS` (stays the current count; no new
  persona).
- `tools/harness_emit/tests/test_coexist.py` — asserts exactly N opencode commands and N Claude
  commands (read the current N; this plan's edits must not change it — no new command file).
- `tools/harness_lint/tests/test_orchestrator_topology.py` — existing "Trace the topology" +
  stage/component routing structural gate; extend, don't fork.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the D-01 indented-tree render (additional section) + graph-aware routing note</name>
  <files>harness/commands/pipeline.md, harness/skills/pipeline-map/SKILL.md, harness/agents/orchestrator.md</files>
  <read_first>harness/commands/pipeline.md (full file, read verbatim — identify exactly which lines are the linear example blocks to leave untouched), harness/skills/pipeline-map/SKILL.md (full file), harness/agents/orchestrator.md (full file — the "Trace the topology" step and routing table), tools/contract_graph/compile.py and query.py (Plan 01/02 finished files)</read_first>
  <action>
    In `harness/commands/pipeline.md`: ADD a new numbered step after the existing "Print the edge
    chain" step (do not renumber or edit the existing steps 1-4; append as a new step, e.g. "5.") that
    describes rendering the general (branching/cyclic) case: call `tools.contract_graph.compile_graph`
    then render `graph["adjacency"]` as an indented tree rooted at each authority with no incoming
    edge (or, for a fully cyclic graph with no root, at the lexicographically-first authority),
    indenting one level per hop, and printing a dependent already seen on the current root-to-node path
    as a terminal `(cycle -> <node>)` marker instead of recursing into it again — reusing the exact
    visited-set-before-recurse discipline from `tools.contract_graph.query`. State explicitly that this
    is an ADDITIONAL rendering mode for non-linear graphs; the existing linear stage-list/edge-chain
    steps 2-3 remain the render for a graph that IS a single chain (every stage fan-out/fan-in = 1) and
    their example text blocks are UNCHANGED. Apply the identical addition to
    `harness/skills/pipeline-map/SKILL.md` (a new "## Rendering non-linear graphs" section after
    "Tracing a request across stages", same tree/cycle-marker description, referencing `/pipeline` as
    the render entry point per its existing "Related" section) — do not edit any of that skill's
    existing prose sections.

    In `harness/agents/orchestrator.md`: extend (do not replace) the "Trace the topology" intake step
    (step 4) with one added sentence noting that when the compiled graph is non-linear, the affected
    set for a change is computed via `tools.contract_graph`'s `direct`/`reverse`/`transitive` queries
    (ids + paths) rather than a hand-walk of `pipeline()["edges"]`, so routing to the owning
    component/language engineer accounts for branch/cycle topologies too. Do NOT add a new routing-table
    row, a new persona, or a new command reference — this is prose enrichment of the existing step and
    existing `/pipeline` routing rows only.
  </action>
  <acceptance_criteria>
    `git diff --unified=0 harness/commands/pipeline.md` shows ONLY added lines (no `-` removed lines)
    in the numbered-step region — i.e. the existing steps 1-4 text is untouched.
    `git diff --unified=0 harness/skills/pipeline-map/SKILL.md` shows ONLY added lines.
    `grep -c "cycle" harness/commands/pipeline.md` returns a nonzero count (the new section mentions the
    cycle marker).
  </acceptance_criteria>
  <verify>
    <automated>git diff --unified=0 harness/commands/pipeline.md harness/skills/pipeline-map/SKILL.md | grep -c '^-[^-]' | grep -qx 0</automated>
  </verify>
  <done>Both conductor surfaces describe the D-01 indented-tree/cycle-marker render as an additional section; orchestrator.md's intake step references affected-set queries for non-linear routing; no existing linear-case text line was removed or edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Byte-identity regression for the linear render + prose-token/anti-sprawl gate</name>
  <files>tools/harness_lint/tests/test_conductor_graph_render.py</files>
  <read_first>tools/harness_lint/tests/test_orchestrator_topology.py (parse_frontmatter idiom to mirror), tools/harness_lint/__init__.py::parse_frontmatter, harness/commands/pipeline.md and harness/skills/pipeline-map/SKILL.md (the finished Task-1 files), tools/harness_lint/tests/test_agents.py (EXPECTED_PERSONAS current value), tools/harness_emit/tests/test_coexist.py (current command count)</read_first>
  <behavior>
    - Test: `harness/commands/pipeline.md` contains the exact literal lines
      `"stage 1: source (python) consumes=[] produces=[greeting]"` and
      `"stage 2: sink (python) consumes=[greeting] produces=[]"` (the linear-case byte-identity anchor
      — this is the concrete artifact proving TOPO-06's "existing linear output stays byte-identical"
      requirement, since these prose files carry no runnable render function today).
    - Test: `harness/commands/pipeline.md` contains the exact literal line
      `"source -> sink (greeting)"` (the second linear-case anchor).
    - Test: both `pipeline.md` and `pipeline-map/SKILL.md` contain a `cycle` token and an `indent`-or-`tree`
      token (the D-01 non-linear render addition is actually present).
    - Test: `harness/agents/orchestrator.md` still parses to `name == "orchestrator"` and
      `mode == "primary"` (no second primary introduced) via `parse_frontmatter`.
  </behavior>
  <action>
    Create `tools/harness_lint/tests/test_conductor_graph_render.py` mirroring
    `test_orchestrator_topology.py`'s frontmatter-parsing idiom (repo root via `parents[3]`,
    `parse_frontmatter` for `.md` files). This file is the CONCRETE proof artifact for the TOPO-06
    byte-identity requirement (research Pitfall 1 / Assumption A2): a hardcoded literal-string
    assertion against the exact linear example text in `pipeline.md`, which fails loud the moment any
    future edit rewrites those lines. Add the D-01 token-presence checks for the non-linear addition.
    Reuse `EXPECTED_PERSONAS` from `tools.harness_lint` (import, don't hardcode a new count) to assert
    the persona set is UNCHANGED from before this plan — no new persona was added.
  </action>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint/tests/test_conductor_graph_render.py -q` exits 0.
    `uv run pytest tools/harness_lint/tests/test_orchestrator_topology.py tools/harness_lint/tests/test_agents.py -q` exits 0 (existing gates unaffected).
    `uv run pytest tools/harness_emit/tests/test_coexist.py -q` exits 0 (command count unchanged — no new command was added).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_conductor_graph_render.py -q</automated>
  </verify>
  <done>The linear-render byte-identity claim is now an automated, falsifiable test (not a manual diff), and anti-sprawl gates (persona count, command count) confirm no new surface was created.</done>
</task>

<task type="auto">
  <name>Task 3: Re-emit `harness/` to both runtimes; verify byte-identical round-trip, no model id</name>
  <files>.opencode, .claude</files>
  <read_first>tools/harness_emit/generate.py (glob discovery — confirm zero code change needed), tools/harness_emit/tests/test_emit_determinism.py (the projected-tree snapshot this plan must not `--snapshot-update` before committing the re-emit)</read_first>
  <action>
    Run `python -m tools.harness_emit` to project the Task-1 edits (`pipeline.md`, `pipeline-map/SKILL.md`,
    `orchestrator.md`) into `.opencode/{command,skill,agent}` and `.claude/{commands,skills,agents}`.
    Commit the re-emitted trees BEFORE touching any `.ambr` determinism snapshot (the established
    sequence per STATE.md's Phase-13/14/15 "gate-theft" lesson: source edit -> emit -> commit projected
    trees -> only then regenerate/verify determinism snapshots — never `--snapshot-update` first). Scan
    the newly emitted `orchestrator.md`/`pipeline.md`/`pipeline-map`/`SKILL.md` copies for any real
    model identifier string (there should be none — this phase never introduces one).
  </action>
  <acceptance_criteria>
    `git diff --stat -- .opencode .claude` shows changes confined to the three edited surfaces'
    projected copies (agent/command/skill for orchestrator/pipeline/pipeline-map) — no unrelated file
    changed.
    `python -m tools.harness_emit && git diff --exit-code -- .opencode .claude` exits 0 (idempotent
    re-emit — the emit-drift CI check passes).
    `uv run pytest -q` (full suite) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python -m tools.harness_emit && git diff --exit-code -- .opencode .claude</automated>
  </verify>
  <done>`.opencode/` and `.claude/` carry the generalized conductor surfaces byte-identically re-emittable, with no model identifier, and the emit-drift gate is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime-neutral `harness/` source -> projected runtime trees | An un-re-emitted edit leaves `.opencode/`/`.claude/` stale relative to source — a documentation/agent-instruction integrity gap, not a code-execution one, but it is exactly the "gate-theft" failure class this repo has hit three times before (Phase 13/14/15). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering | `harness/commands/pipeline.md`, `pipeline-map/SKILL.md` | mitigate | Task 2's literal-text regression test makes "the linear render changed" a CI-visible, automated failure instead of a silent prose drift — the concrete proof artifact the phase requires. |
| T-25-06 | Repudiation (stale projected tree) | `tools/harness_emit` round-trip | mitigate | Task 3 re-runs the emitter and asserts `git diff --exit-code` after a second re-emit, proving the committed trees are the CURRENT deterministic projection, not a stale prior one. |
</threat_model>

<verification>
`uv run pytest tools/harness_lint/tests/test_conductor_graph_render.py tools/harness_lint/tests/test_orchestrator_topology.py tools/harness_emit/tests/test_coexist.py -q` exits 0.
`python -m tools.harness_emit && git diff --exit-code -- .opencode .claude` exits 0.
`uv run pytest -q` (full suite) exits 0.
</verification>

<success_criteria>
`/pipeline`, `pipeline-map`, and `orchestrator.md` consume the compiled graph and affected-set queries,
render non-linear topologies per D-01, keep the linear case provably byte-identical, introduce no new
command/persona, and round-trip byte-identically to both runtimes with no model identifier.
</success_criteria>

<output>
Create `.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-03-SUMMARY.md` when done
</output>
