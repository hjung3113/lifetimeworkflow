---
phase: 29-docs-drive-loop-adoption-integration-closeout-v2-3-c
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - harness/commands/docs-update.md
  - harness/skills/docs-upkeep/SKILL.md
  - tools/harness_lint/caps.py
  - tools/harness_lint/tests/test_docs_update_wiring.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - tools/harness_emit/emit-manifest.json
  - .opencode/command/docs-update.md
  - .opencode/skill/docs-upkeep/SKILL.md
  - .claude/commands/docs-update.md
  - .claude/skills/docs-upkeep/SKILL.md
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements: [DOCSUP-06]

must_haves:
  truths:
    - "SC-1: `/docs-update` and `docs-upkeep` emit to BOTH runtimes and a second emit is a byte no-op"
    - "D-04: the command's data source is `python -m tools.docs_guard` with fixed argv — never the gitignored `.memory/derived/docs-staleness.md`, whose absence on a fresh clone would read as a false green"
    - "D-05: exit 0 stops, exit 1 is the only working state (draft a bounded edit OR an exact disposition, then route to /review + /verify-work), exit 3 refuses to draft anything and sends the operator to the registry"
    - "D-06: the command body carries ZERO hand-typed glob literals — it names `tools.docs_guard` and the `exclusion_reason` classes; the control is plan 29-01's function, the prose merely documents it"
    - "D-02: EXPECTED_SKILLS 12->13 and the command count 24->25 move in the SAME change; the command description literally contains 'use' or 'when'; the skill description is distinct from all twelve others"
    - "D-03: the round-trip is verified with `git status --porcelain` (sees UNTRACKED files) — `emit-drift`'s bare `git diff` is blind to the four new emitted files"
    - "The loop never acts on STALE_ADVISORY or UNCOVERED, never re-runs contract_drift, and never writes the derived queue — those are what make it bounded"
  artifacts:
    - path: "harness/commands/docs-update.md"
      provides: "the thin fixed-argv macro over tools.docs_guard with the 0/1/3 routing table"
      contains: "tools.docs_guard"
    - path: "harness/skills/docs-upkeep/SKILL.md"
      provides: "the read-the-guard / choose-edit-or-disposition / record-the-review runbook, plus the skill-creator Step 0 anti-sprawl justification"
      contains: "docs-upkeep"
    - path: "tools/harness_lint/tests/test_docs_update_wiring.py"
      provides: "the wiring lint: the command names the module, names no glob literal, and names no `.memory/` path"
      contains: "docs_guard"
  key_links:
    - from: "harness/commands/docs-update.md"
      to: "tools/docs_guard/cli.py"
      via: "fixed argv `python -m tools.docs_guard`, $ARGUMENTS positional"
      pattern: "tools.docs_guard"
    - from: "harness/skills/docs-upkeep/SKILL.md"
      to: "tools/docs_guard/exclusions.py"
      via: "names exclusion_reason and its three reason strings as the enforcing control"
      pattern: "exclusion_reason"
    - from: "tools/harness_lint/caps.py"
      to: "harness/skills/docs-upkeep/"
      via: "EXPECTED_SKILLS membership — the emitter and the lint share one constant"
      pattern: "docs-upkeep"
---

<objective>
Add the DOCSUP-06 surface: one thin `/docs-update` command and one `docs-upkeep` skill, authored
runtime-neutrally under `harness/` and emitted byte-identically to `.opencode/` and `.claude/`.

Purpose: this is the drive loop's entry point. Its correctness is mostly about what it does NOT do
— it does not read the gitignored queue, does not carry its own copy of the exclusion globs, does
not chase advisory work, and does not restate contract drift.

Output: two source files, four emitted files, three moved counters, one wiring lint.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-CONTEXT.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-RESEARCH.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-06-SUMMARY.md
@harness/commands/adopt.md
@harness/skills/brownfield-adoption/SKILL.md
@tools/docs_guard/cli.py
@tools/harness_lint/caps.py
@tools/harness_lint/tests/test_context_budget_wiring.py

<interfaces>
<!-- VERIFIED this session. -->

Gate constraints the new frontmatter must satisfy:
| Constraint | Enforced at |
|---|---|
| command `description` non-empty, <=1024 folded chars, contains "use" or "when" | tools/harness_emit/validate.py:114-121; tools/harness_lint/tests/test_commands.py:34 |
| command `agent` is a slug resolving to a real `harness/agents/<slug>.md` | validate.py:123-128; test_agent_referential_integrity.py |
| skill `name` == directory name, <=64, no "anthropic"/"claude", no `<`/`>` | validate.py:142-155; caps.py:117,120 |
| skill `description` <=1024, contains "use"/"when", DISTINCT from every other skill | validate.py:157-164; test_skills.py:108 |
| the emitted skill set == `EXPECTED_SKILLS` exactly | caps.py:129-144; validate.check_skill_set; test_emit_determinism.py:99-105 |

Existing agents (`harness/agents/`): code-reviewer, curator, explorer, orchestrator,
python-engineer. Use `python-engineer` (D-01 / research §1.3): the command runs one module and
drafts a bounded file edit. `curator` is WRONG — it is the derived-plane owner, and
`tools/harness_lint/tests/test_derived_freshness.py:32` pins
`_ALLOWED_TOOL_MODULES = frozenset({"memory_regen", "docs_sync"})` for `curator.md` +
`refresh-memory.md` only. Naming `tools.docs_guard` in either of those two files turns that test RED.

`tools/harness_lint/caps.py:129-144` currently holds exactly 12 names, ending `brownfield-adoption`.
`tools/harness_emit/tests/test_coexist.py` holds THREE literals to move: the `def` name at :39
(`test_all_24_commands_emit_to_both_trees`) and the two `assert len(...) == 24` at :64-65.
`AGENTS.md:104-105` carry the auto-merged command and skill lists (spliced by
`generate.py:441-449`); `CLAUDE.md` has the same fence.

From tools/docs_guard/cli.py — the routing table the command documents:
0 clean (STALE_ADVISORY may be on stderr and never changes the code) · 1 BROKEN /
STALE_REQUIRED / failing coherence finding / uncovered- or binding-count regression · 3 registry or
ledger INVALID · 2 argparse usage, never produced deliberately. CLI flags: `--registry`,
`--ledger`, `--root`.

From plan 29-01: `tools.docs_guard.exclusion_reason(target) -> "constitution-plane" |
"derived-plane" | "accepted-adr" | None`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the wiring lint and record its RED run</name>
  <files>tools/harness_lint/tests/test_docs_update_wiring.py</files>
  <behavior>
    Modelled on `test_context_budget_wiring.py` (parse via the shared `parse_frontmatter`, never
    hand-sliced `---` fences):
    - the command file exists, its frontmatter `agent` is `python-engineer`, `subtask` is true, and
      its description contains a routing trigger;
    - the body names `tools.docs_guard`;
    - the body names NO hand-typed glob literal — assert absence of `contracts/**`,
      `docs/reference/**`, `.memory/derived/**`, `golden/**` as substrings (D-06: the command must
      not carry its own copy of the exclusion set);
    - the body names no `.memory/` path at all (D-04 / P3: a command that reads the gitignored queue
      is a fresh-clone false green);
    - the body names all three exit codes 0, 1 and 3;
    - the skill exists, its `name` is `docs-upkeep`, and its body names `exclusion_reason`;
    - `docs-upkeep` is a member of `caps.EXPECTED_SKILLS` (so a half-widening fails HERE with a
      legible message, not only deep inside the emitter).
  </behavior>
  <action>Write the test file only. Run the RED selection PLAIN:
  `uv run pytest tools/harness_lint/tests/test_docs_update_wiring.py -q` — never `! uv run pytest`,
  which exits 0 on a collection error and masquerades as RED. Record the verbatim first failure.
  Commit with `git commit -- tools/harness_lint/tests/test_docs_update_wiring.py` after inspecting
  `git diff --cached --name-only`.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_docs_update_wiring.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>The lint exists and fails for the right reason (missing command/skill files), with the
  message recorded verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Author the command and skill, and move all three counters in the same change</name>
  <files>harness/commands/docs-update.md, harness/skills/docs-upkeep/SKILL.md, tools/harness_lint/caps.py, tools/harness_emit/tests/test_coexist.py</files>
  <action>Author `harness/commands/docs-update.md` in the shape of `harness/commands/adopt.md`:
  frontmatter `description` (a `>-` block; must contain "use"/"when" and state that the command
  never edits an accepted ADR, a derived reference page, a contract or a golden), `agent:
  python-engineer`, `subtask: true`. Body: a one-line statement that this is a thin macro over the
  already-coded `tools.docs_guard` and re-implements neither classification, digesting nor the
  exclusion set; the single fixed-argv invocation `python -m tools.docs_guard $ARGUMENTS` with the
  `$ARGUMENTS`-is-positional-never-interpolated-into-a-shell-string invariant restated
  (`adopt.md:83-86`); the 0/1/3 routing table from `<interfaces>`; and an explicit statement that
  contract-drift and golden are LEADING (the command never re-runs `contract_drift.run_gate()` and
  never restates its findings). No glob literal, no `.memory/` path — the queue is a SessionStart
  pointer regenerated by `/refresh-memory`, not this command's input.

  Author `harness/skills/docs-upkeep/SKILL.md` in the shape of
  `harness/skills/brownfield-adoption/SKILL.md`: `name: docs-upkeep`, a description that is
  DISTINCT from all twelve existing skill descriptions (test_skills.py:108) and contains
  "use"/"when". Body opens with the skill-creator Step 0 anti-sprawl justification — none of the
  twelve owns human-doc review obligation; `data-contracts` authors/checks contracts,
  `two-plane-memory` owns the derived half this skill excludes, `gate-model` is the general
  refuse-by-default pattern this instantiates. Then the runbook: read the guard; on exit 1 pick
  EITHER a bounded edit to the bound target document OR the exact disposition; route to `/review` +
  `/verify-work`; the ledger row is authored by a HUMAN, never by the agent (ADR-0010 clause 3b),
  and `tools.docs_guard.exclusion_reason` — named explicitly, with its three reason strings — is
  what enforces the exclusions, with `contract_guard` and `path_deny_globs` as the runtime write-side
  backstops. State boundedness as four properties: acts only on the guard's enumerated
  BROKEN/STALE_REQUIRED set; never expands to UNCOVERED (raising `uncovered_max` is a human edit);
  never chases STALE_ADVISORY; one pass, and re-running is idempotent because the guard is read-only.
  Add a `## Related` section citing `gate-model` and `two-plane-memory`.

  Body length target under ~150 lines (>500 warns but still emits). No `model:` field anywhere — no
  model identifier may appear in any repo artifact.

  In the SAME change: widen `tools/harness_lint/caps.py` `EXPECTED_SKILLS` 12 -> 13 by adding
  `"docs-upkeep"` (and extend the comment block above it with the Phase-29 sentence, matching how
  Phase 8/10 annotated theirs), and bump `tools/harness_emit/tests/test_coexist.py` 24 -> 25 at all
  THREE literals (the `def` name at :39 and both `assert len(...) == 24` at :64-65), extending its
  docstring with the Phase-29 line. A half-widening fails three tests in the other direction (P5).

  Do NOT touch `harness/commands/refresh-memory.md` or `harness/agents/curator.md`. Do NOT edit
  `harness/skills/gate-model/SKILL.md` or `harness/skills/brownfield-adoption/SKILL.md` — both are
  seeded binding TARGETS whose digests plan 29-04 asks a human to sign.

  Run the narrow selection: `uv run pytest tools/harness_lint tools/harness_emit -q`. Note that
  `test_projected_tree_matches_committed_snapshot` and the emit determinism snapshot are expected to
  fail until task 3 re-emits — record which failures are the expected pre-emit ones rather than
  declaring the task done on a red suite. Commit with an explicit four-path pathspec.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_docs_update_wiring.py -q && grep -c 'docs-upkeep' tools/harness_lint/caps.py</automated>
  </verify>
  <done>The wiring lint is green; `EXPECTED_SKILLS` has 13 entries; all three `test_coexist.py`
  literals read 25; the only remaining failures in the narrow selection are the emit-projection ones
  task 3 closes.</done>
</task>

<task type="auto">
  <name>Task 3: Emit to both runtimes, track the new files, and prove the second emit is a no-op</name>
  <files>.opencode/command/docs-update.md, .opencode/skill/docs-upkeep/SKILL.md, .claude/commands/docs-update.md, .claude/skills/docs-upkeep/SKILL.md, tools/harness_emit/emit-manifest.json, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, AGENTS.md, CLAUDE.md</files>
  <action>Run `uv run python -m tools.harness_emit`. It writes both trees, re-merges the
  HARNESS-MANAGED block in `AGENTS.md` (:104-105 command and skill lists) and `CLAUDE.md`, and
  prune-then-writes `tools/harness_emit/emit-manifest.json` (machine-owned, must be committed).
  Regenerate the syrupy snapshot: `uv run pytest tools/harness_emit -q --snapshot-update`, then
  re-run WITHOUT the flag and confirm green.

  Verify the round-trip with a check that SEES UNTRACKED FILES (D-03 — `emit-drift` at `ci.yml`
  uses bare `git diff` and would pass green with four new files untracked):
  `git status --porcelain -- .opencode .claude opencode.json AGENTS.md CLAUDE.md tools/harness_emit/emit-manifest.json`
  must list exactly the expected paths and nothing unexpected; every `??` entry must be
  `git add`ed explicitly.

  Then the byte-identity proof: run the emitter a SECOND time and assert
  `git status --porcelain -- .opencode .claude` is EMPTY and
  `git diff --exit-code -- .opencode .claude opencode.json AGENTS.md CLAUDE.md` exits 0.

  Confirm no model identifier leaked: `uv run pytest tools/harness_lint/tests/test_agents.py tools/harness_emit/tests/test_opencode_config.py -q`.
  Confirm `git diff --check` is clean (byte hygiene: no trailing whitespace, LF only, no BOM).

  Commit with an explicit pathspec listing all eight paths; inspect `git diff --cached --name-only`
  first and confirm nothing else from the shared index rides along.</action>
  <verify>
    <automated>uv run python -m tools.harness_emit && git status --porcelain -- .opencode .claude opencode.json AGENTS.md CLAUDE.md && uv run pytest tools/harness_emit tools/harness_lint -q</automated>
  </verify>
  <done>Four emitted files are tracked and committed; a second emit produces zero porcelain output;
  `tools/harness_emit` + `tools/harness_lint` are fully green; `git diff --check` is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime-neutral source -> two emitted trees | a hand-edit or a missed `git add` desynchronizes the runtimes |
| guard stdout -> agent action | the command decides what to edit from tool output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Repudiation (false assurance) | new emitted files land untracked; `emit-drift` passes anyway | mitigate | verify with `git status --porcelain`, explicit `git add`, second-emit no-op proof (D-03) |
| T-29-10 | Injection | `$ARGUMENTS` woven into a constructed shell string | mitigate | fixed `python -m tools.docs_guard $ARGUMENTS` argv, positional only; invariant restated in the body |
| T-29-11 | Tampering | the command grows its own copy of the exclusion globs | mitigate | wiring lint asserts absence of every glob literal; the control is 29-01's function |
| T-29-12 | Spoofing (false green) | the loop reads the gitignored derived queue | mitigate | wiring lint asserts the body names no `.memory/` path (D-04, fresh-clone case) |
| T-29-13 | Information disclosure (policy) | a model identifier in a new artifact | mitigate | no `model:` field; `validate.py` placeholder regex + `test_agents.py` / `test_opencode_config.py` re-run in task 3 |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | none in this plan — zero new dependencies |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint tools/harness_emit -q` green.
- Second emit: `git status --porcelain -- .opencode .claude` empty.
- `git diff --check` clean.
- `harness/skills/gate-model/SKILL.md` and `harness/skills/brownfield-adoption/SKILL.md` unmodified.
</verification>

<success_criteria>
`/docs-update` and `docs-upkeep` exist in `harness/` and in both emitted trees byte-identically, all
three enumerated counters moved together, and the wiring lint proves the command names the module,
no glob, no queue path, and all three exit codes.
</success_criteria>

<output>
Create `.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-03-SUMMARY.md` when done.
</output>
