---
phase: 26.2-secret-pattern-semantics-ignorecase-collapse-digit-less-fals
plan: 01
subsystem: contracts
tags: [secret-scan, regex, gate-registry, contract-hash, contract-drift, adoption-scan, evidence-capture, pytest]

# Dependency graph
requires:
  - phase: 26.1-secret-pattern-precision-sc-4-false-positive-closure
    provides: "Tightened secret_patterns[1] (min-20 + unconditional 3-way charset AND), the checkpoint:human-verify constitution-plane precedent, and 26.1-REVIEW.md's four findings (CR-01, WR-01, IN-01, IN-02) this phase closes"
provides:
  - "secret_patterns[1] rewritten: (?-i:...)-scoped case lookaheads (closes CR-01) so the documented upper/lower diversity requirement survives the consumers' re.IGNORECASE compile"
  - "Length-floor + 2-of-3-charset disjunction (closes the mixed-case slice of WR-01 per D-01) — evidence-redaction path no longer silently drops mixed-case digit-less secret shapes"
  - "Data-based test suite calling scan._secret_pattern() and capture._sensitive_pattern() directly (never a standalone re.compile) proving CR-01/WR-01/SC-2/SC-3, closing IN-01"
  - "Recorded disposition for all four 26.1-REVIEW.md findings (CR-01, WR-01, IN-01, IN-02) — none silently dropped"
affects: [phase-27-task-local-adoption-workflow]

# Tech tracking
tech-stack:
  added: []
  patterns: ["(?-i:...) scoped inline-flag group to keep specific charset-class lookaheads immune to an outer re.IGNORECASE compile", "length-floor + N-of-M charset-class disjunction as a gitleaks-style relaxed secret-shape heuristic"]

key-files:
  created:
    - .planning/phases/26.2-secret-pattern-semantics-ignorecase-collapse-digit-less-fals/26.2-01-SUMMARY.md
  modified:
    - contracts/harness/task-control/gate-registry.json
    - contracts/.hashes/manifest.json
    - .memory/derived/contracts-index.md
    - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
    - tools/adoption_scan/tests/test_scan_exclusions.py
    - tools/evidence/tests/test_capture.py

key-decisions:
  - "D-01 (human-decided pre-execution, applied this plan): relax WR-01's digit requirement to length >= 20 plus any 2-of-3 charset classes (upper, lower, digit), rather than restoring strict 3-of-3 diversity — the evidence-redaction plaintext-leak harm outranks the marginal false-positive risk, and SC-1's ci.yml red-green proof was re-verified to confirm the relaxation does not reopen 26.1's original false positive."
  - "Ratified the constitution-plane edit (commit 08b5d68) via a blocking checkpoint:human-verify gate — the human operator reviewed the diff, the four-row disposition table, the manifest rebaseline, and the derived-plane regeneration, then explicitly confirmed ratification in-session on 2026-07-20. This is distinct from and not conflated with the HARNESS_DEV_BYPASS dev-session setting used only to permit the mechanical write."
  - "Left tools/hooks/secret_scan.py:44-47 untouched (D-07, out of scope) — it hardcodes its own PATTERNS list independent of gate-registry.json's secret_patterns array; documented as a standing seam, not converged in this phase."

requirements-completed: [ADOPT-01]

# Metrics
duration: 22min
completed: 2026-07-20
---

# Phase 26.2 Plan 01: Secret-Pattern Semantics — IGNORECASE Collapse + Digit-less False-Negative Closure Summary

**Rewrote `secret_patterns[1]` in `gate-registry.json` with `(?-i:...)`-scoped case lookaheads and a length-floor + 2-of-3-charset-class disjunction, closing 26.1-REVIEW.md's CR-01 (case-diversity requirement inert under `re.IGNORECASE`) and the mixed-case slice of WR-01 (digit-less secret shapes unconditionally missed), proven against both live consumer functions and human-ratified via a blocking checkpoint.**

## Performance

- **Duration:** 22 min
- **Tasks:** 3/3 complete
- **Files modified:** 6

## Accomplishments

- Closed CR-01: `(?-i:[A-Z])`/`(?-i:[a-z])` scoped inline-flag groups make the case-diversity lookaheads immune to the outer `re.IGNORECASE` both `scan._secret_pattern()` and `capture._sensitive_pattern()` apply — a single-case, digit-less, 20+ char value is proven to stay unmatched in both consumers.
- Closed the mixed-case slice of WR-01 per D-01: relaxed the digit requirement from an unconditional 3-way AND to length >= 20 plus any 2-of-3 charset classes — mixed-case digit-less values (e.g. a 20-char alphabetic passphrase with upper+lower) now match in both consumers, closing the evidence-redaction plaintext-leak risk WR-01 identified.
- Re-verified SC-1: `test_ci_yml_false_positive_closed` stays green after the relaxation — the 26.1 false positive on this repo's own `.github/workflows/ci.yml` does not reopen.
- Closed IN-01: all new assertions call the live consumer functions directly (`scan._secret_pattern()`, `capture_module._sensitive_pattern()`), never a standalone `re.compile`, with a dedicated branch-attribution test pinning the match to registry index 1 (SC-3).
- Recorded a disposition for all four 26.1-REVIEW.md findings — none silently dropped (SC-4, table below).
- Landed the contract edit, hash rebaseline, and derived-plane regeneration in one atomic commit (`08b5d68`), human-ratified via the plan's blocking `checkpoint:human-verify` gate (SC-5).

## Task Commits

Each task was committed atomically:

1. **Task 1: Author data-based CR-01/WR-01/SC-3 tests against both live consumer functions** — `cdc23f0` (test)
2. **Task 2: Land the CR-01/WR-01 pattern fix, rebaseline hash, verify drift, regenerate derived plane** — `08b5d68` (fix)
3. **Task 3: Checkpoint — Human ratification of the constitution-plane secret_patterns[1] semantics fix** — no code commit (checkpoint task); human operator reviewed the diff, disposition table, manifest rebaseline, and derived-plane regeneration, then explicitly ratified in-session on 2026-07-20.

**Plan metadata:** this commit (docs: complete plan)

## Files Created/Modified

- `contracts/harness/task-control/gate-registry.json` — `secret_patterns[1]` rewritten from the unconditional 3-way-AND form to `(?-i:...)`-scoped, length >= 20 + 2-of-3-charset-class disjunction. Indices 0 and 2-7, and the `gates` object, byte-identical.
- `contracts/.hashes/manifest.json` — rebaselined via `tools.contract_hash.hash --write`; exactly one entry changed (`gate-registry.json`).
- `.memory/derived/contracts-index.md` — regenerated via `tools.memory_regen.contracts_index`; the `gate-registry.json` row's `hash_prefix` changed (`1d212f2f56d8` → `dac0b5df21dd`).
- `tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr` — committed render-determinism snapshot updated to match the new `hash_prefix` (collateral of the derived-plane regen, mirroring 26.1's own two-`.ambr`-update precedent).
- `tools/adoption_scan/tests/test_scan_exclusions.py` — new `test_secret_patterns_1_case_diversity_survives_ignorecase` (CR-01), `test_secret_patterns_1_two_of_three_classes_matches` (WR-01/SC-2, parametrized), `test_secret_patterns_1_single_class_digit_less_remains_excluded` (SC-2 residual-gap disposition, parametrized), `test_secret_patterns_1_branch_attribution` (SC-3); IN-02 provenance comment added above `old_pattern` in `test_ci_yml_false_positive_closed`.
- `tools/evidence/tests/test_capture.py` — mirrored `test_sensitive_pattern_case_diversity_survives_ignorecase`, `test_sensitive_pattern_two_of_three_classes_matches`, `test_sensitive_pattern_single_class_digit_less_remains_excluded` calling `capture_module._sensitive_pattern()` directly (SC-1's "as compiled by the consumers" requirement covers both consumers).

## Decisions Made

- **D-01: relax WR-01 to 2-of-3 charset classes, not strict 3-of-3** — the evidence-redaction plaintext-leak harm (a real secret reaching a committed artifact) outranks the marginal false-positive cost; SC-1's `ci.yml` proof was re-run and stays green under the relaxation.
- **Human ratification recorded as a genuine, explicit in-session confirmation at the plan's blocking checkpoint** — distinct from and not conflated with the `HARNESS_DEV_BYPASS` dev-session setting used only to permit the mechanical write of `08b5d68`.
- **`tools/hooks/secret_scan.py:44-47` left untouched (D-07)** — documented, out-of-scope seam; it hardcodes its own `PATTERNS` list independent of `gate-registry.json`'s `secret_patterns` array. Flagged as a follow-up candidate for a future phase/ADR to converge the write-time detector with the contract-driven definition.

## Finding Disposition Table (SC-4 — copied verbatim from the Task 3 checkpoint)

| Finding | Disposition | Evidence |
|---------|-------------|----------|
| CR-01 (IGNORECASE collapses case lookaheads) | fixed | `(?-i:[A-Z])`/`(?-i:[a-z])` scoped groups in the new `secret_patterns[1]`; `test_secret_patterns_1_case_diversity_survives_ignorecase` proves a single-case digit-less value stays unmatched under `re.IGNORECASE` in both consumers |
| WR-01 (digit-less values unconditionally missed) | fixed (relaxed, per D-01) | Length-floor + 2-of-3 charset disjunction; mixed-case digit-less values now match (`test_secret_patterns_1_two_of_three_classes_matches`); single-case-only and all-numeric digit-less values remain an accepted residual gap (`test_secret_patterns_1_single_class_digit_less_remains_excluded`) — rationale: closing that residual gap would require dropping the length+diversity heuristic to a bare 20-char run, which reopens SC-1's `ci.yml` false positive (re-verified green in Task 2) |
| IN-01 (SC-2's 7 fixtures don't exercise the changed branch) | fixed | New tests call `scan._secret_pattern()`/`capture_module._sensitive_pattern()` directly against fixtures specifically targeting `secret_patterns[1]`, with `test_secret_patterns_1_branch_attribution` pinning the match to registry index 1 |
| IN-02 (`old_pattern` is a hand-duplicated literal with no history link) | accepted, low severity, comment added | One-line provenance comment added above `old_pattern` in `test_ci_yml_false_positive_closed` pointing at commit `d6e9054` |

**Accepted residual gap (recorded, not silently dropped):** single-case-only (all-uppercase or all-lowercase) and all-numeric digit-less values still do not match `secret_patterns[1]` in either consumer, both before and after this plan. This is an intentional, documented tradeoff (D-01) — closing it fully would require relaxing the pattern to a bare length-only check, which would reopen SC-1's `ci.yml` false positive (the exact defect 26.1 closed). Proven by `test_secret_patterns_1_single_class_digit_less_remains_excluded` / `test_sensitive_pattern_single_class_digit_less_remains_excluded`.

**Out-of-scope seam (recorded, not converged):** `tools/hooks/secret_scan.py:44-47` is a write-time PreToolUse detector with its own hardcoded `PATTERNS` list; it does not read `gate-registry.json`'s `secret_patterns` array, so this plan's CR-01/WR-01 fix does not propagate to it (D-07, carried forward unchanged from 26.1's own follow-up note).

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

- `uv run pytest tools/evidence/tests/test_capture.py -k "sensitive_pattern" -x` fails to collect in isolation with `ModuleNotFoundError: No module named 'tools'` — reproduced identically on the unmodified pre-plan tree via `git stash`, confirming this is a pre-existing environment/collection quirk unrelated to this plan's changes, not a regression. Worked around by using the full-suite invocation (`uv run pytest -q -k "sensitive_pattern"` / `uv run pytest -q`) for verification, which collects and runs the module correctly — this is the same invocation the plan's own `<verification>` block and Wave-2 sampling rate specify for the full suite.
- The derived-plane regen collaterally required updating `tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr` (a committed render-determinism snapshot whose content includes the changed `hash_prefix`) — the same class of collateral 26.1 already needed for its own two `.ambr` snapshots; updated via `--snapshot-update` on just that one test, then the full suite re-confirmed green (1054 passed).

## Verification Evidence

- `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k "secret_patterns_1" -x` (before Task 2's fix): 7 passed, 1 failed (`mixed_case_digit_less-red_before_task2-WR01_relaxation_proof`) — the genuine red state proving WR-01's relaxation was not already accidentally true.
- `uv run pytest -q -k "sensitive_pattern"` (before Task 2's fix): 6 passed, 1 failed (same red-state row, mirrored on `capture._sensitive_pattern()`).
- After Task 2: `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py tools/evidence/tests/test_capture.py -x -q` → 56 passed.
- `uv run python -m tools.contract_drift.drift` → exit 0 (clean), both before and after the full derived-plane regen.
- `git diff --stat contracts/.hashes/manifest.json` (commit `08b5d68`) → exactly one entry changed.
- `docs/reference` confirmed a no-op (`tools.docs_sync` globs `*.schema.json` only; `gate-registry.json` is a plain data contract).
- Full suite: `uv run pytest -q` → 1054 passed.
- SC-4/SC-5: human ratification obtained at the Task 3 checkpoint via an explicit in-session "approved" confirmation of the diff and disposition table, recorded 2026-07-20 — distinct from and not conflated with the `HARNESS_DEV_BYPASS` write setting. The orchestrator additionally independently re-verified the pattern against both live consumer compiles (mixed-case digit-less matches; upper-only/lower-only/all-numeric digit-less do not; `ci.yml` does not match; `tools.contract_drift.drift` exits 0).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 27 (Task-Local Adoption Workflow + Safe Application) can now build on a `secret_patterns` value matcher whose documented charset semantics match its actual runtime behavior under both consumers' `re.IGNORECASE` compile, with the digit-less evidence-redaction risk narrowed to a small, explicitly recorded residual gap.
- The `tools/hooks/secret_scan.py` convergence follow-up (carried from 26.1) remains open and untouched; not a blocker for Phase 27 but should stay tracked.

---
*Phase: 26.2-secret-pattern-semantics-ignorecase-collapse-digit-less-fals*
*Completed: 2026-07-20*

## Self-Check: PASSED
- FOUND: .planning/phases/26.2-secret-pattern-semantics-ignorecase-collapse-digit-less-fals/26.2-01-SUMMARY.md
- FOUND: cdc23f0 (test)
- FOUND: 08b5d68 (fix)
