---
phase: 26.2-secret-pattern-semantics-ignorecase-collapse-digit-less-fals
verified: 2026-07-20T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 26.2: Secret-Pattern Semantics — IGNORECASE Collapse + Digit-less False Negatives Verification Report

**Phase Goal:** `secret_patterns[1]` behaves as its contract documents, and the branch this repo actually edits is actually tested.
**Verified:** 2026-07-20T00:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1: documented charset semantics == runtime behavior under `re.IGNORECASE`, proven against both live consumers | ✓ VERIFIED | Independently re-derived `secret_patterns[1]` from the committed `gate-registry.json`, compiled it exactly as `scan._secret_pattern()`/`capture._sensitive_pattern()` do, and probed it directly (not the SUMMARY's claim). Confirmed live: all-upper digit-less 20ch → no match, all-lower digit-less 20ch → no match, mixed-case digit-less → match, upper+digit / lower+digit → match, 19-char (below floor) → no match, 20-char (at floor) → match, in BOTH `scan._secret_pattern()` and `capture_module._sensitive_pattern()`. `.github/workflows/ci.yml` still does not match (SC-1's original false-positive proof re-verified independently, not just re-run). Also independently reproduced the CR-01 collapse: compiling a deliberately un-scoped variant (without `(?-i:...)`) DOES match the all-upper digit-less fixture, proving the scoping is load-bearing, not decorative. |
| 2 | SC-2: digit-presence requirement has an explicit, recorded disposition; `password:` + long digit-less passphrase has an intentional, recorded outcome | ✓ VERIFIED | `test_secret_patterns_1_single_class_digit_less_remains_excluded` in `tools/adoption_scan/tests/test_scan_exclusions.py:200` and its mirror in `tools/evidence/tests/test_capture.py:152` directly parametrize `"".join(["correcthorsebattery", "staple"])` (an all-lowercase digit-less 25-char passphrase) prefixed with `"pass"+"word"+": "` — this is the exact roadmap example — and assert no match, both live and after the fix (no transition — an accepted residual gap). `test_secret_patterns_1_two_of_three_classes_matches` proves the mixed-case digit-less slice of the same shape now DOES match (D-01's relaxation). Rationale recorded in SUMMARY.md's disposition table and in the checkpoint output: relaxing further would reopen SC-1's `ci.yml` false positive (independently re-verified above, not just trusted). |
| 3 | SC-3: new tests exercise `secret_patterns[1]` directly and are load-bearing, not decorative | ✓ VERIFIED | Checked out commit `cdc23f0` (Task 1 — tests added, contract NOT yet fixed) into a scratch worktree and ran `test_secret_patterns_1_two_of_three_classes_matches` against the pre-fix (26.1) pattern: it genuinely FAILS on the mixed-case-digit-less row (`AssertionError`), proving the test is not a tautology. `test_secret_patterns_1_branch_attribution` (`test_scan_exclusions.py:207`) compiles `secret_patterns[1]` alone and asserts it — not the other 7 branches — produces the match, pinning branch attribution as required. |
| 4 | SC-4: `26.1-REVIEW.md` findings CR-01, WR-01, IN-01, IN-02 each reach a recorded disposition, none silently dropped | ✓ VERIFIED | `26.2-01-SUMMARY.md`'s four-row disposition table (lines 92-97) maps CR-01→fixed, WR-01→fixed (relaxed, D-01), IN-01→fixed, IN-02→accepted-with-rationale, each with concrete evidence (test names / line references). Cross-checked each against the actual 26.1-REVIEW.md finding text — all four correctly represented, none conflated or dropped. `STATE.md:235` independently corroborates the same disposition summary. |
| 5 | SC-5: contract edit + hash rebaseline + derived-plane regen land in one atomic commit, human-ratified, drift clean at that commit | ✓ VERIFIED (see caveat below) | `git show --stat 08b5d68` confirms exactly 4 files changed in one commit: `gate-registry.json`, `contracts/.hashes/manifest.json`, `.memory/derived/contracts-index.md`, and the committed `.ambr` snapshot — no other files. Ran `uv run python -m tools.contract_drift.drift` myself at HEAD: exit 0, "OK — live manifest matches the committed baseline." Human ratification: recorded in SUMMARY.md and independently corroborated in `STATE.md:235` as an explicit in-session "approved" at the plan's blocking `checkpoint:human-verify` gate, distinct from the `HARNESS_DEV_BYPASS` write-permission setting used only for the mechanical write. This was NOT a CODEOWNERS PR review — but per `.github/CODEOWNERS`'s own documented caveat and `STATE.md:252`, CODEOWNERS PR review is structurally unreachable on this repo (default branch = working branch, sole owner cannot self-approve their own PR), and the in-session blocking-checkpoint ratification is the established substitute this repo has used consistently since Phase 26 (see `STATE.md:222, 232, 234`). This same standard was accepted as satisfying SC-4/SC-5 in 26.1's own prior passed verification (`26.1-VERIFICATION.md` line 31). Judged against that established precedent, the recorded evidence satisfies SC-5's intent. |

**Score:** 5/5 truths verified

### Out-of-Scope Fence

`tools/hooks/secret_scan.py` — confirmed NOT modified. `git diff --stat 727bab6~1 08b5d68` (full phase span) touches only `.memory/derived/contracts-index.md`, `.planning/STATE.md`, phase docs, `contracts/.hashes/manifest.json`, `contracts/harness/task-control/gate-registry.json`, the two test files, and one `.ambr` snapshot. `git log --all -- tools/hooks/secret_scan.py` shows its last change (`7d71c7e`) predates this phase entirely.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `contracts/harness/task-control/gate-registry.json` (`secret_patterns[1]`) | `(?-i:...)`-scoped case lookaheads + length-floor + 2-of-3-charset disjunction | ✓ VERIFIED | Read live value from the file, byte-matches the plan's specified JSON-escaped string exactly; indices 0/2-7 confirmed byte-identical via `git show --stat` (only 2 lines changed in the whole file). |
| `tools/adoption_scan/tests/test_scan_exclusions.py` | New tests calling `scan._secret_pattern()` directly | ✓ VERIFIED | `test_secret_patterns_1_case_diversity_survives_ignorecase`, `test_secret_patterns_1_two_of_three_classes_matches`, `test_secret_patterns_1_single_class_digit_less_remains_excluded`, `test_secret_patterns_1_branch_attribution` all present and confirmed calling the live consumer function (grepped, read source). |
| `tools/evidence/tests/test_capture.py` | Mirrored cases calling `capture_module._sensitive_pattern()` | ✓ VERIFIED | `test_sensitive_pattern_case_diversity_survives_ignorecase`, `test_sensitive_pattern_two_of_three_classes_matches`, `test_sensitive_pattern_single_class_digit_less_remains_excluded` present, confirmed calling `_sensitive_pattern()` directly. |
| `contracts/.hashes/manifest.json` | Rebaselined, exactly one entry changed | ✓ VERIFIED | `git show --stat 08b5d68` shows 2 lines changed (one entry); `tools.contract_drift.drift` clean at HEAD. |
| `.memory/derived/contracts-index.md` | Regenerated, `gate-registry.json` row hash_prefix changed | ✓ VERIFIED | Present in the same atomic commit; SUMMARY documents old/new hash_prefix values. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `gate-registry.json` | `scan.py::_secret_pattern` | `json.loads` read, no code change | ✓ WIRED | Confirmed live: imported `scan` module, called `scan._secret_pattern()`, its output reflects the edited pattern (independently probed above). |
| `gate-registry.json` | `capture.py::_sensitive_pattern` | `json.loads` read, no code change | ✓ WIRED | Confirmed live: imported `capture` module, called `_sensitive_pattern()`, output reflects the edited pattern. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Live consumer probe matrix (8 cases) | inline `python3` script importing `scan`/`capture` and probing both compiled patterns | All 8 cases matched expected outcome | ✓ PASS |
| `ci.yml` false-positive stays closed | `pat_scan.search(ci.yml contents)` | `None` | ✓ PASS |
| CR-01 guard is load-bearing | compiled a deliberately un-scoped variant, probed all-upper digit-less fixture | Matched (proves the guard fires on regression) | ✓ PASS |
| SC-3 test is load-bearing | ran `test_secret_patterns_1_two_of_three_classes_matches` against commit `cdc23f0` (pre-fix pattern) in a scratch worktree | Genuinely FAILED (`AssertionError`) on the mixed-case-digit-less row | ✓ PASS |
| Full test scope | `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py tools/evidence/tests/test_capture.py -x -q` | 56 passed | ✓ PASS |
| Contract drift gate | `uv run python -m tools.contract_drift.drift` | exit 0, "OK — live manifest matches the committed baseline." | ✓ PASS |
| Full suite | `uv run pytest -q` | 1054 passed | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ADOPT-01 | 26.2-01-PLAN.md | Secret-pattern classification correctness | ✓ SATISFIED | SC-1 through SC-5 all independently verified above. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tools/adoption_scan/tests/test_scan_exclusions.py` | 158, 166, 197 | `"".join([single_literal])` — does not actually break literal contiguity, violating the plan's own stated fixture-obfuscation convention and Task 1's explicit acceptance criteria ("no contiguous secret-shaped literal") | ⚠️ WARNING | Already caught and documented by `26.2-REVIEW.md` (WR-01), still unfixed at HEAD as of this verification (no follow-up commit after `08b5d68`). No live-secret or functional risk (values are single-case/digit-less, not real secret shapes, and `tests/` is allowlisted by `secret_scan.py`), but it is a genuine, currently-open gap between the plan's stated acceptance criteria and what shipped. Does not block any of SC-1 through SC-5 — none of the roadmap success criteria concern test-fixture literal hygiene — so it does not change the phase's pass/fail status, but it should be tracked for cleanup (e.g. in a follow-up commit or the next phase touching this file). |
| `tools/evidence/tests/test_capture.py` | 113, 121, 149 | Same `"".join([single_literal])` pattern, mirrored | ⚠️ WARNING | Same as above — mirrored instance. |

### Human Verification Required

None. All observable truths were independently re-derived against the live codebase (not the SUMMARY's claims), all automated gates were re-run directly by this verification, and the human-ratification evidence trail (SUMMARY.md + STATE.md + established repo precedent) is sufficient to judge SC-5 without further human action at this time.

### Gaps Summary

No blocking gaps. All five roadmap success criteria (SC-1 through SC-5) are independently verified against live code, not SUMMARY claims: the pattern was re-derived from the actual contract file and probed directly against both live consumer functions; the SC-3 test suite was proven load-bearing by reverting to the pre-fix commit and observing a genuine failure; the atomic-commit and drift-clean claims for SC-5 were independently re-run rather than trusted; and the out-of-scope fence (`tools/hooks/secret_scan.py`) was confirmed untouched.

One non-blocking WARNING carries forward from `26.2-REVIEW.md` and remains open at HEAD: six new test fixture literals use `"".join([single_string])`, which is a no-op and does not achieve the literal-obfuscation the plan's own acceptance criteria required. This is cosmetic/process debt, not a functional or security defect, and does not map to any of SC-1 through SC-5 — it is surfaced here for visibility and follow-up, not as a phase blocker.

---

_Verified: 2026-07-20T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
