---
phase: 27-task-local-adoption-workflow-safe-application-v2-3-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/harness/adoption/approval.schema.json
  - contracts/.hashes/manifest.json
  - docs/reference/approval.md
  - .memory/derived/contracts-index.md
  - tools/docs_sync/tests/test_docs_sync_determinism.py
autonomous: false
requirements: [ADOPT-06]

must_haves:
  truths:
    - "A new, self-contained (no cross-file $ref, D-11) Draft 2020-12 approval.schema.json exists at contracts/harness/adoption/, binding a decision list to an exact (draft_hash, task_revision, git_ref) tuple — never extending attestation.schema.json's unrelated constraint-source binding shape"
    - "The contract-hash manifest is rebaselined via tools.contract_hash.hash --write ONLY (never hand-edited) and tools.contract_drift.drift reports clean"
    - "The committed-derived plane (docs/reference/approval.md + .memory/derived/contracts-index.md) is regenerated in the same commit as the contract, never hand-authored"
    - "The constitution-plane write is human-ratified via a blocking checkpoint — never agent self-approved, never a fabricated GOLDEN_APPROVE_HUMAN token"
  artifacts:
    - path: "contracts/harness/adoption/approval.schema.json"
      provides: "The ADOPT-06 approval record contract: batch_id, draft_hash, task_revision, git_ref, decisions[] (kind in {contract,golden,adr,relationship-authority,conflict,unknown}), approved_at"
      contains: "draft_hash"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined JCS SHA-256 manifest with exactly one new entry for approval.schema.json"
    - path: "docs/reference/approval.md"
      provides: "Regenerated reference page for the new schema"
  key_links:
    - from: "contracts/harness/adoption/approval.schema.json"
      to: "tools/adoption_apply/approval.py"
      via: "jsonschema.Draft202012Validator validation before every approval.json write (Plan 27-04)"
      pattern: "approval.schema.json"
---

<objective>
Author the ADOPT-06 constitution-plane contract `contracts/harness/adoption/approval.schema.json` —
a NEW, self-contained schema (per D-11's "no cross-file `$ref`, duplicate the shape if reused"
convention) binding a human's promotion decision on proposed contract/golden/ADR/relationship-
authority/conflict/unknown items to an exact `(draft_hash, task_revision, git_ref)` tuple, so that
ANY change to the draft content, the task's CAS revision, or the git ref invalidates the approval.
Per RESEARCH's Alternatives Considered (Assumption A1, MEDIUM confidence, flagged explicitly): this
is deliberately a NEW schema, not an extension of `attestation.schema.json` — that schema's
`constraints[]` shape is about constraint-source binding at CLARIFY/SPEC time, a structurally
different vocabulary from a promotion-decision binding.

Purpose: gives Plan 27-04's `approval.py` a ratified target to validate against before any
`approval.json` is written, and closes the "no valid `approval.schema.json` exists yet" gap
27-VALIDATION.md's Wave 0 flags.
Output: the ratified schema, a rebaselined hash manifest, a regenerated committed-derived plane, all
landed in one atomic, human-ratified commit — mirroring 26.2-01-PLAN.md's checkpoint ritual exactly.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-RESEARCH.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-PATTERNS.md
@.planning/phases/26.2-secret-pattern-semantics-ignorecase-collapse-digit-less-fals/26.2-01-PLAN.md
@contracts/harness/task-control/attestation.schema.json
@contracts/harness/adoption/manifest.schema.json
@contracts/harness/adoption/plan.schema.json
@tools/contract_hash/hash.py
@tools/contract_drift/drift.py
@tools/docs_sync/generate.py
@tools/docs_sync/tests/test_docs_sync_determinism.py
@.github/CODEOWNERS

<interfaces>
<!-- Sibling-shape analog (NOT $ref'd) — contracts/harness/task-control/attestation.schema.json,
     full 24-line file, read this session. Copy the ENVELOPE shape only, never the constraints[]
     vocabulary. -->
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://harness.local/contracts/harness/task-control/attestation.schema.json",
  "title": "Task context attestation contract",
  "type": "object",
  "additionalProperties": false,
  "required": ["constraints"],
  "properties": { "constraints": { "type": "array", "items": { "...": "..." } } }
}
```

The 6-value `dispositionEnum` in `contracts/harness/adoption/manifest.schema.json` is the exact
peer-schema formatting convention (a named `$defs` enum + a `$defs` record type) to mirror for
`approval.schema.json`'s own `decisionKindEnum`/`decisionRecord`.

`tools/contract_hash/hash.py` — `DATA_CONTRACT_PATHS` + `SCHEMA_GLOB` (globs
`contracts/**/*.schema.json`, confined to the contracts subtree) — `approval.schema.json` is
auto-discovered by this glob; `--write` rebaselines with exactly one new manifest entry, no manual
key edit.

`tools/docs_sync/generate.py::iter_schemas()` — globs the same `contracts/**/*.schema.json` set;
`write()` prune-then-writes `docs/reference/<name>.md` per current schema. Adding
`approval.schema.json` means `docs/reference/approval.md` is auto-generated on the next
`python -m tools.docs_sync` run — never hand-author it.

`tools/docs_sync/tests/test_docs_sync_determinism.py:27-40` — `EXPECTED_PAGES` frozenset (currently
11 names: `attestation, evidence, format-conventions, greeting, handoff, inventory, manifest, plan,
relationship, state, task`) — MUST gain `"approval"` or `test_render_is_deterministic_over_real_tree`
And the page-set assertions (`names == EXPECTED_PAGES`) fail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author approval.schema.json + rebaseline hash + regenerate derived plane</name>
  <files>
    contracts/harness/adoption/approval.schema.json,
    contracts/.hashes/manifest.json,
    docs/reference/approval.md,
    .memory/derived/contracts-index.md,
    tools/docs_sync/tests/test_docs_sync_determinism.py
  </files>
  <read_first>
    contracts/harness/task-control/attestation.schema.json (full — envelope shape to clone),
    contracts/harness/adoption/manifest.schema.json (full — `$defs` enum + record convention,
      `additionalProperties: false` discipline, D-11 self-containment),
    contracts/harness/adoption/plan.schema.json's `relationshipCandidate` `$defs` entry (already
      quoted in this plan's `<interfaces>` block via 27-PATTERNS.md — the vocabulary
      "contract/golden/adr/relationship-authority/conflict/unknown" comes from ADOPT-06's own text,
      not from plan.schema.json directly, but the record-with-kind-enum shape is the same pattern),
    tools/contract_hash/hash.py (`DATA_CONTRACT_PATHS`, `build_manifest`, `main` — the `--write` CLI
      flag),
    tools/contract_drift/drift.py (`run_gate` — exit-0-iff-clean CLI),
    tools/docs_sync/generate.py `iter_schemas`/`write` (confirms auto-discovery, no manual page
      authoring),
    tools/docs_sync/tests/test_docs_sync_determinism.py lines 27-40 (`EXPECTED_PAGES` — the set this
      task must widen by exactly one entry)
  </read_first>
  <action>
    Write `contracts/harness/adoption/approval.schema.json`: `$schema`
    `https://json-schema.org/draft/2020-12/schema`, `$id`
    `https://harness.local/contracts/harness/adoption/approval.schema.json`, `title` "Brownfield
    adoption promotion approval record", `type: object`, `additionalProperties: false`, `required:
    [batch_id, draft_hash, task_revision, git_ref, decisions, approved_at]`. Properties: `batch_id`
    (string, minLength 1), `draft_hash` (string, pattern `^[0-9a-f]{64}$`), `task_revision` (integer,
    minimum 0), `git_ref` (string, pattern `^[0-9a-f]{40}$`), `decisions` (array of
    `$defs/decisionRecord`, minItems 1 — a promotion with zero decisions is meaningless), `approved_at`
    (string, ISO-8601 UTC — mirror the exact pattern used elsewhere in this repo's schemas for a
    timestamp field, e.g. `evidence.schema.json`'s `started_at`/`ended_at` shape). `$defs`:
    `decisionKindEnum` — exactly the 6 ADOPT-06 kinds `["contract", "golden", "adr",
    "relationship-authority", "conflict", "unknown"]`, no more no fewer (mirrors manifest.schema.json's
    `dispositionEnum` "exactly these N values" discipline); `decisionRecord` — `additionalProperties:
    false`, `required: [item_id, kind, disposition]`, properties `item_id` (string, minLength 1 — a
    stable id pointing back into the batch's `plan.json` proposal/question/relationshipCandidate),
    `kind` (`$ref: #/$defs/decisionKindEnum`), `disposition` (enum `["approve", "reject", "defer"]`),
    optional `note` (string). This is a self-contained schema — zero `$ref` outside its own file
    (D-11), duplicating the enum vocabulary rather than pointing at `manifest.schema.json` or
    `plan.schema.json`.

    Writing under `contracts/**` requires a human-authorized `GOLDEN_APPROVE_HUMAN` token or an
    equivalent human-present write — do not fabricate it; if the write is denied, pause and request
    human authorization (identical posture to every prior constitution-plane task in this repo's
    history).

    Then, in this strict order: `uv run python -m tools.contract_hash.hash --write` (rebaselines
    `contracts/.hashes/manifest.json` — exactly one new entry, `contracts/harness/adoption/
    approval.schema.json`); `uv run python -m tools.contract_drift.drift` (confirm exit 0); add
    `"approval"` to `EXPECTED_PAGES` in `tools/docs_sync/tests/test_docs_sync_determinism.py`; `uv
    run python -m tools.docs_sync` (confirm `docs/reference/approval.md` is created, and no other
    page changes); `uv run python -m tools.memory_regen.contracts_index` (confirm
    `.memory/derived/contracts-index.md` gains an `approval.schema.json` row). Finally: `uv run
    pytest tools/docs_sync -q` (the widened `EXPECTED_PAGES` set must be green, including any
    syrupy snapshot regenerated for the new page — use `--snapshot-update` ONLY for the new
    `approval` page's own snapshot entry, never touch an existing page's snapshot).
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift && uv run pytest tools/docs_sync -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0. `git diff --stat
    contracts/.hashes/manifest.json` shows exactly one new entry
    (`contracts/harness/adoption/approval.schema.json`). `git diff --stat -- docs/reference` shows
    exactly one new file (`approval.md`), no existing page's content changed. `uv run pytest
    tools/docs_sync -q` is green. `.memory/derived/contracts-index.md` gains exactly one new row for
    `approval.schema.json`. `jsonschema.Draft202012Validator.check_schema()` on the new file raises
    no `SchemaError` (verify inline: `uv run python -c "import json;from jsonschema import
    Draft202012Validator; Draft202012Validator.check_schema(json.load(open('contracts/harness/
    adoption/approval.schema.json')))"`).
  </acceptance_criteria>
  <done>approval.schema.json exists, self-contained, matches ADOPT-06's binding tuple; drift is
  clean; the committed-derived plane (docs/reference/approval.md, contracts-index.md) is
  regenerated; docs_sync's own test suite is green with the widened EXPECTED_PAGES.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human ratification of the new approval.schema.json constitution-plane write</name>
  <files>
    contracts/harness/adoption/approval.schema.json,
    contracts/.hashes/manifest.json,
    docs/reference/approval.md,
    .memory/derived/contracts-index.md
  </files>
  <read_first>
    .github/CODEOWNERS (the /contracts/ routing + advisory-enforcement + solo-owner
      self-approval caveats),
    .planning/phases/26.2-secret-pattern-semantics-ignorecase-collapse-digit-less-fals/26.2-01-PLAN.md
      lines 302-372 (the checkpoint task shape this one mirrors verbatim)
  </read_first>
  <action>
    BLOCKING human checkpoint — the agent does NOT self-ratify. Present for review, in this order:
    1. The full new file `contracts/harness/adoption/approval.schema.json` (it is new, not a diff of
       an existing file — confirm it is additive, touching no other schema).
    2. The rebaselined `contracts/.hashes/manifest.json` diff (exactly one new entry).
    3. `uv run python -m tools.contract_drift.drift` output (must report clean).
    4. The regenerated derived plane diff (`docs/reference/approval.md` new,
       `.memory/derived/contracts-index.md` gains one row, no other page/row changed).
    5. Confirmation that RESEARCH's Assumption A1 (new schema vs. extending
       `attestation.schema.json`) was followed as recommended — record this disposition explicitly
       in the SUMMARY: "A1: followed — new self-contained approval.schema.json, no
       attestation.schema.json extension."

    Then PAUSE. Ratify via CODEOWNERS PR review or an explicit human-supplied write — never a
    fabricated `GOLDEN_APPROVE_HUMAN` token or `HARNESS_DEV_BYPASS` used to self-land the
    ratification.
  </action>
  <what-built>
    A new, self-contained `contracts/harness/adoption/approval.schema.json` binding a promotion
    decision to `(draft_hash, task_revision, git_ref)`, plus a rebaselined hash manifest and
    regenerated committed-derived plane, ready to land atomically.
  </what-built>
  <how-to-verify>
    1. Confirm `uv run python -m tools.contract_drift.drift` reports clean.
    2. Review the new schema file end to end — confirm `additionalProperties: false` everywhere,
       the 6-value `decisionKindEnum` matches ADOPT-06's text exactly, and no `$ref` points outside
       this file.
    3. Confirm `git diff --stat contracts/.hashes/manifest.json` shows exactly one new entry.
    4. Confirm `uv run pytest tools/docs_sync -q` is green.
    5. Confirm no agent self-approved this change (no fabricated `GOLDEN_APPROVE_HUMAN` token
       committed anywhere).
  </how-to-verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0; `git diff --stat
    contracts/.hashes/manifest.json` shows exactly one new entry; `uv run pytest tools/docs_sync -q`
    is green; the SUMMARY records the A1 disposition verbatim.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: uv run python -m tools.contract_drift.drift</human-check>
  </verify>
  <resume-signal>Type "approved" once the new schema and derived-plane diff have been reviewed, or describe required changes.</resume-signal>
  <done>The new approval.schema.json, rebaselined manifest, and regenerated derived plane are
  human-ratified via the standard constitution-plane path, not agent self-approval — ready for
  Plan 27-04's approval.py to validate against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent session → `contracts/harness/adoption/approval.schema.json` | Constitution-plane write; denied by `contract_guard` unless human-authorized (`GOLDEN_APPROVE_HUMAN`) or dev-bypassed — never agent self-ratified. |
| `contract_hash --write` output → `contracts/.hashes/manifest.json` | Machine-computed baseline; must never be hand-edited. |
| `docs_sync`/`contracts_index` output → committed-derived plane | Machine-regenerated; CI's `stale-derived` job is the non-bypassable verify side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Elevation of Privilege | `contracts/harness/adoption/approval.schema.json` write path | accept (out-of-band) | Unchanged existing controls: `contract_guard` PreToolUse deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS `/contracts/`. This plan adds no new bypass; Task 2's `checkpoint:human-verify` is the explicit ratification surface. |
| T-27-02-02 | Tampering | `contracts/.hashes/manifest.json` | mitigate | Rebaseline ONLY via `tools.contract_hash.hash --write` — never hand-edited; verified by `git diff --stat` showing exactly one changed entry. |
| T-27-02-03 | Repudiation | Approval schema's own binding vocabulary being loose enough that a stale approval could be replayed | mitigate | Schema enforces exact-match required fields (`draft_hash` pattern-anchored 64-hex, `git_ref` pattern-anchored 40-hex, `task_revision` integer) — the STRICT equality re-check itself is Plan 27-04's job (approval.py), this schema only ensures the fields exist in the right shape to be checked. |
| T-27-02-SC | Tampering (supply chain) | N/A | accept | Zero new package installs (schema authoring + existing `tools.contract_hash`/`tools.contract_drift`/`tools.docs_sync`/`tools.memory_regen` CLIs only); no slopcheck required. |
</threat_model>

<verification>
- `uv run python -m tools.contract_drift.drift` exits 0.
- `uv run pytest tools/docs_sync -q` exits 0.
- `git diff --stat contracts/.hashes/manifest.json` shows exactly one changed entry.
- `git diff --stat -- docs/reference` shows exactly one new file (`approval.md`).
</verification>

<success_criteria>
- ADOPT-06 (contract half): `approval.schema.json` exists, self-contained, human-ratified, binding
  exactly `(draft_hash, task_revision, git_ref)` + a decision list over the 6 ADOPT-06 item kinds.
- Contract-drift clean; committed-derived plane fresh; landed in one atomic, human-ratified commit.
</success_criteria>

<output>
Create `.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-02-SUMMARY.md`
when done. Copy the A1 disposition line from the checkpoint task verbatim into the SUMMARY.
</output>
