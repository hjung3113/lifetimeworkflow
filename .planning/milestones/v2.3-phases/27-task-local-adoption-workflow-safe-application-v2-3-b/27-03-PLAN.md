---
phase: 27-task-local-adoption-workflow-safe-application-v2-3-b
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - tools/adoption_apply/apply.py
  - tools/adoption_apply/tests/test_atomic_apply.py
  - tools/adoption_apply/tests/test_constitution_refusal.py
autonomous: true
requirements: [ADOPT-05]

must_haves:
  truths:
    - "apply.py refuses every contracts/·docs/adr/·golden/ destination BEFORE any open()/os.link()/os.replace() call — proven by a spy asserting ZERO filesystem-write calls for a refused destination, independent of whether a Claude PreToolUse hook is in the loop at all"
    - "Discovery/draft mode (ADOPT-05 clause 1) writes are structurally confined to the task artifact root — refuse_if_outside_root() rejects any resolved destination outside the given root BEFORE a write, proven by test_draft_confined_to_artifact_root's escape-attempt refusal, independent of the apply-mode constitution check above"
    - "A create disposition is atomic and collision-safe: os.link-based publish, raising on an existing target, never os.replace (which silently overwrites)"
    - "A second apply against an unchanged target is a true no-op — target bytes identical before and after"
    - "A create disposition whose target changed after draft-time (concurrent drift) is refused, not silently applied — re-hash happens immediately before the write, not trusted from draft time"
    - "Marker-merge for the 3 MARKER_CAPABLE destinations (AGENTS.md, CLAUDE.md, .claude/settings.json) reuses tools.harness_emit.merge verbatim — no second fence/marker scheme"
    - "apply.py iterates manifest[\"dispositions\"] only and fails loud on any destination absent from both dispositions[] and excluded[] — never a silent .get(destination, \"create\") default"
  artifacts:
    - path: "tools/adoption_apply/apply.py"
      provides: "refuse_if_constitution(), refuse_if_outside_root(), atomic_create(), apply_disposition(), apply_manifest() — the ADOPT-05 writer (apply-mode constitution refusal AND draft-mode artifact-root confinement)"
      contains: "def refuse_if_constitution"
    - path: "tools/adoption_apply/tests/test_constitution_refusal.py"
      provides: "test_refuses_before_mutation — the zero-filesystem-call spy proof"
      contains: "def test_refuses_before_mutation"
    - path: "tools/adoption_apply/tests/test_atomic_apply.py"
      provides: "idempotent-reapply, concurrent-drift-refused, marker-merge-idempotent, no-arbitrary-command-execution, draft-confined-to-artifact-root, SC-2 full-cycle tests"
      contains: "def test_draft_confined_to_artifact_root"
  key_links:
    - from: "tools/adoption_apply/apply.py"
      to: "tools.hooks.contract_guard.CONSTITUTION_GLOBS"
      via: "direct import, never re-derived — same import direction tools/adoption_scan/destinations.py already establishes"
      pattern: "from tools.hooks.contract_guard import CONSTITUTION_GLOBS"
    - from: "tools/adoption_apply/apply.py"
      to: "tools.harness_emit.merge.splice_managed_block / merge_settings"
      via: "verbatim reuse for the 3 MARKER_CAPABLE destinations"
      pattern: "from tools.harness_emit.merge import"
    - from: "tools/adoption_apply/apply.py"
      to: "tools.adoption_scan.destinations.MARKER_CAPABLE / DISPOSITION_ENUM / _existing_hash"
      via: "import, never re-list the 3 marker-capable strings or reimplement the hash helper"
      pattern: "from tools.adoption_scan.destinations import"
---

<objective>
Implement `tools/adoption_apply/apply.py`: the atomic, collision-safe, idempotent manifest-apply
writer ADOPT-05 requires, with a structural constitution-plane refusal that is independently safe
whether or not a Claude `PreToolUse` hook is anywhere in the loop. RESEARCH's single highest-risk
finding drives this plan: `tools/hooks/contract_guard.py` only fires on Claude's own Write/Edit tool
— a bare Python `os.replace()`/`os.link()` call from `apply.py` never triggers it. `apply.py` must
therefore duplicate the constitution-plane check as an explicit precondition, reusing
`tools.hooks.contract_guard.CONSTITUTION_GLOBS` as DATA (never re-derived), called at the START of
every apply-mode file operation — not cached once per batch, so a single malformed/edited manifest
row can never slip a constitution write through.

Purpose: this is the module every later plan's fixtures (27-05) and `/adopt apply` sub-verb (27-06)
call — its correctness is the entire safety property ADOPT-05/SC-2 name.
Output: `apply.py` + its full unit test suite (constitution refusal, atomic create, idempotent
reapply, concurrent-drift refusal, marker-merge, no-arbitrary-command-execution, and the SC-2
integration proof), all green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-RESEARCH.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-PATTERNS.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-VALIDATION.md
@tools/task_control/manager.py
@tools/handoff/handoff.py
@tools/hooks/contract_guard.py
@tools/harness_perms/resolver.py
@tools/harness_emit/merge.py
@tools/adoption_scan/destinations.py
@contracts/harness/adoption/manifest.schema.json

<interfaces>
<!-- Every symbol apply.py imports, with the exact signature/behavior verified live this session. -->

`tools/hooks/contract_guard.py:43` — `CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**",
"golden/**"]`. `tools/adoption_scan/destinations.py:87` already imports this exact constant across
the same package-boundary shape (`from tools.hooks.contract_guard import CONSTITUTION_GLOBS  # noqa:
F401 (re-exported for callers)`) — confirming `adoption_apply -> tools.hooks.contract_guard` is an
established, non-circular import direction.

`tools/harness_perms/resolver.py:47-49` — `resolve_path(deny_globs, path) -> "deny"|"allow"`: `"deny"
if any(fnmatchcase(path, glob) for glob in deny_globs) else "allow"`.

`tools/adoption_scan/destinations.py:101` — `MARKER_CAPABLE: frozenset[str] =
frozenset({"AGENTS.md", "CLAUDE.md", ".claude/settings.json"})`.

`tools/adoption_scan/destinations.py:117-124` — `DISPOSITION_ENUM: tuple[str, ...] = ("create",
"preserve", "conflict", "marker-merge", "derived-regenerate", "human-ratification-required")` — the
total 6-value switch `apply.py` must cover, refusing (not defaulting) on any other value.

`tools/adoption_scan/destinations.py:277` (approx) — `_existing_hash(path: Path) -> str`: sha256 over
the file in 64KiB chunks. Import directly (accept the private-name coupling — RESEARCH names this
acceptable) rather than reimplementing.

`tools/harness_emit/merge.py` (full, 238 lines, already read) — `splice_managed_block(existing_text,
block_body) -> str` (idempotent: markers present → replace only the fenced region; absent → append
once); `merge_settings(existing: dict, ...) -> dict` (order-preserving JSON merge, returns a deep
copy, never mutates the input).

`tools/task_control/manager.py:150-173` (`_atomic_create`) + `tools/handoff/handoff.py:130-161`
(`_atomic_write_once`) — the exact `tempfile.mkstemp` (same dir as target) → write/flush/fsync →
`os.link(tmp, path)` (raises `FileExistsError` on collision) → directory fsync → `finally:
os.unlink(tmp)` sequence, confirmed two independently-audited times in this repo. `os.link` never
silently overwrites; `os.replace` DOES (confirmed live in RESEARCH's own session) — `apply.py`'s
`create` disposition must use the `os.link` idiom, never `os.replace`.

`contracts/harness/adoption/manifest.schema.json` — `dispositions[]` (`$ref: dispositionRecord`) and
`excluded[]` (`$ref: excludedDestinationRecord`) are TWO SEPARATE top-level arrays; an `excluded`
item is NEVER also in `dispositions`. `apply.py` iterates `dispositions[]` only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Structural constitution refusal + atomic create/marker-merge core</name>
  <files>
    tools/adoption_apply/apply.py,
    tools/adoption_apply/tests/test_constitution_refusal.py
  </files>
  <read_first>
    tools/hooks/contract_guard.py lines 1-80 (full `decide()`/`_on_constitution_plane` — confirms
      the PreToolUse-only scope this task's structural refusal must NOT rely on),
    tools/harness_perms/resolver.py (full, 55 lines — `resolve_path`),
    tools/adoption_scan/destinations.py lines 87, 101, 117-124, 277-286 (the four imported symbols),
    tools/task_control/manager.py lines 150-173 (`_atomic_create`, the idiom to mirror — own copy,
      not a cross-package private import, per this plan's `<interfaces>` and 27-PATTERNS.md's
      explicit "copy the exact sequence" instruction),
    tools/harness_emit/merge.py lines 1-25 and the full `splice_managed_block`/`merge_settings`
      bodies (already quoted in this plan's `<interfaces>` block),
    27-RESEARCH.md "Pattern 3: Structural Constitution Refusal" and "Pitfall 1" (the exact failure
      mode this task closes: a test suite that only exercises `apply.py` via a simulated Claude
      tool-call event, never a bare CLI/subprocess invocation)
  </read_first>
  <behavior>
    `ConstitutionRefusal(ValueError)`: raised by `refuse_if_constitution(destination: str) -> None`
    when `resolve_path(CONSTITUTION_GLOBS, destination) == "deny"`.

    `CollisionError(ValueError)`: raised by `atomic_create(path: Path, payload: bytes) -> None` when
    `os.link` hits an existing target.

    `PathEscapeError(ValueError)`: raised by `refuse_if_outside_root(path: Path, root: Path) -> None`
    when `Path(path).resolve()` is not equal to, or is not a descendant of, `Path(root).resolve()`
    (a plain `os.path.commonpath`/`is_relative_to`-style containment check — no symlink-following
    assumptions beyond `Path.resolve()`'s own). This is ADOPT-05 clause 1's structural control for
    DISCOVERY/DRAFT mode ("discovery/draft 모드는 task artifact root 안에서만 쓰고"): the `/adopt draft`
    sub-verb (Plan 27-06's `cli.py`) calls this BEFORE writing `inventory.json`/`plan.json`/
    `manifest.json` into a batch's `artifacts/adoption/<batch-id>/` directory, confining every draft
    write to that directory regardless of `batch_id`/`target_ref` content. This is a peer control to
    `refuse_if_constitution` — same "raise before any write" shape, different boundary (an inclusion
    root, not an exclusion glob set).

    Test matrix (`test_constitution_refusal.py`):
    - `test_refuses_before_mutation`: monkeypatch/spy `os.open`, `os.link`, and `os.replace` at
      module scope (or patch `apply.atomic_create`/`apply._write_bytes` directly with a `Mock` that
      records calls) BEFORE calling `apply.apply_disposition({"destination": "contracts/widget.
      schema.json", "disposition": "create"}, ...)` for a target under `contracts/**` — asserts
      `ConstitutionRefusal` is raised AND the spy/mock records ZERO calls. Repeat parametrized over
      `docs/adr/x.md` and `golden/y/baseline.verified.tsv`.
    - `test_refuses_bare_cli_invocation`: call `refuse_if_constitution` directly with NO Claude
      tool-call event object anywhere in the call chain (a bare function call) — proves the check is
      independent of the Claude hook (closes RESEARCH Pitfall 1's exact warning sign).
    - `test_non_constitution_destination_allowed`: `refuse_if_constitution("src/widget.py")` does
      NOT raise.
  </behavior>
  <action>
    Implement `apply.py`'s import block (`from tools.hooks.contract_guard import
    CONSTITUTION_GLOBS`, `from tools.harness_perms import resolve_path`, `from
    tools.harness_emit.merge import splice_managed_block, merge_settings`, `from
    tools.adoption_scan.destinations import MARKER_CAPABLE, DISPOSITION_ENUM, _existing_hash`),
    `ConstitutionRefusal`, `CollisionError`, `PathEscapeError`, `refuse_if_constitution`,
    `refuse_if_outside_root`, and `atomic_create` (own copy
    of the `tempfile.mkstemp`/`os.link`/fsync/`finally: os.unlink` sequence — mirror
    `manager.py::_atomic_create`'s shape exactly, operating on raw `bytes` rather than a dict, since
    `apply.py` writes arbitrary target files, not JSON state). Add a private
    `_apply_marker_merge(destination: str, target_root: Path, block_body_or_settings) -> None`
    calling `splice_managed_block`/`merge_settings` per the destination's file type (`.json` →
    `merge_settings`, else → `splice_managed_block`), then publishing via an internal
    `_atomic_replace` (own copy, `os.replace`-based, mirroring `manager.py::_atomic_replace` — a
    marker-merge target already exists by definition, so `os.replace` is correct here, unlike
    `create`). Call `refuse_if_constitution(destination)` at the START of the single dispatch
    function `apply_disposition(record: dict, target_root: Path, ...)` — every branch, before any
    `open()`/`os.link()`/`os.replace()`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py -x</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py::test_refuses_before_mutation -x`
    passes and the test body asserts a spy call-count of exactly zero (not merely that an exception
    was raised). `test_refuses_bare_cli_invocation` passes with no Claude event object constructed
    anywhere in the test. `grep -n "CONSTITUTION_GLOBS" tools/adoption_apply/apply.py` shows an
    IMPORT, not a re-listed literal `["contracts/**", ...]`.
  </acceptance_criteria>
  <done>apply.py's constitution refusal is structural, checked first, and proven independent of any
  Claude hook; atomic_create and marker-merge helpers exist and are unit-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Disposition-total apply_manifest, drift refusal, idempotence, SC-2 integration</name>
  <files>
    tools/adoption_apply/apply.py,
    tools/adoption_apply/tests/test_atomic_apply.py
  </files>
  <read_first>
    tools/adoption_scan/destinations.py lines 117-124 (`DISPOSITION_ENUM`) and the module docstring's
      "Pitfall 3" paragraph (`dispositions[]` vs `excluded[]` — never `.get(destination, "create")`),
    contracts/harness/adoption/manifest.schema.json (full — confirms the two-array shape),
    27-RESEARCH.md "Pitfall 3" and "Pitfall 4" (never build a second glob-resolution engine for the
      drift re-check — reuse `_existing_hash` directly),
    27-VALIDATION.md rows for `test_idempotent_reapply`, `test_concurrent_drift_refused`,
      `test_marker_merge_idempotent`, `test_no_arbitrary_command_execution`,
      `test_draft_confined_to_artifact_root`, `test_sc2_full_apply_cycle` (the exact behaviors this
      task's tests must prove; `test_draft_confined_to_artifact_root` exercises Task 1's
      `refuse_if_outside_root`, not `apply_manifest` itself — ADOPT-05 clause 1's draft-mode half)
  </read_first>
  <behavior>
    `apply_manifest(manifest: dict, target_root: Path, *, batch_dir: Path) -> dict`: iterates
    `manifest["dispositions"]` ONLY (never synthesizes a disposition for an `excluded[]` entry or an
    unlisted destination — fails loud with a typed `UnknownDispositionError(ValueError)` on any
    `record["disposition"]` not in `DISPOSITION_ENUM`). For each record:
    - `create`: re-hash the CURRENT target (if it now exists) via `_existing_hash` and compare
      against the `existing_sha`/hash recorded in the manifest's disposition `reason`/evidence at
      draft time (the drift re-check) — if a target now exists where the manifest said none did
      (concurrent drift), raise `ConcurrentDriftError(ValueError)` rather than silently calling
      `atomic_create` (which would itself raise `CollisionError` — but the point is to raise the
      MORE SPECIFIC drift error first, with a message naming "concurrent target drift"). If still
      absent, `atomic_create`.
    - `preserve`/`conflict`: no-op (never written by `apply_manifest` — a disposition-level
      idempotence guarantee; `preserve` means hash-equal already, `conflict` means human decides,
      neither is auto-applied).
    - `marker-merge`: dispatch to `_apply_marker_merge`.
    - `derived-regenerate`: no-op (owned by `tools.docs_sync`/`tools.memory_regen`/`tools.harness_emit`
      — `apply.py` never regenerates a derived artifact itself).
    - `human-ratification-required`: `refuse_if_constitution` already raised for these before this
      switch is reached (belt-and-suspenders — assert unreachable if somehow not).
    Returns a summary dict `{"applied": [...], "skipped": [...], "refused": [...]}`.

    Never call `subprocess.run` with an argv built from `record`/`manifest` content anywhere in
    `apply.py` — every subprocess call in this module (if any) uses a FIXED argv shape.

    Test matrix (`test_atomic_apply.py`):
    - `test_idempotent_reapply`: apply a `create` disposition once, capture target bytes; call
      `apply_manifest` a SECOND time with the SAME manifest (now correctly reporting `preserve` for
      that destination, since a re-drafted manifest would) — asserts target bytes unchanged and no
      exception.
    - `test_concurrent_drift_refused`: a manifest says `create` for a destination; before calling
      `apply_manifest`, write SOME file at that target path out-of-band (simulating a human editing
      the target between draft and apply) — asserts `ConcurrentDriftError` is raised and the
      out-of-band file's bytes are UNCHANGED (not silently overwritten).
    - `test_marker_merge_idempotent`: apply a `marker-merge` disposition against a target `AGENTS.md`
      carrying the BEGIN/END HARNESS-MANAGED markers twice in a row — asserts the second apply is
      byte-identical to the first (mirrors `splice_managed_block`'s own documented idempotence).
    - `test_no_arbitrary_command_execution`: `grep`-style structural assertion (parse `apply.py`'s
      own source, or a targeted `unittest.mock.patch("subprocess.run")` spy across the full
      `apply_manifest` call) proving zero `subprocess.run` calls occur during a full
      create+marker-merge+preserve+conflict apply cycle.
    - `test_draft_confined_to_artifact_root` (ADOPT-05 clause 1): construct a task artifact root
      `root = tmp_path / "artifacts" / "adoption" / "<batch-id>"`; assert
      `refuse_if_outside_root(root / "inventory.json", root)` does NOT raise (a legitimate in-root
      write); then assert `refuse_if_outside_root(root.parent.parent / "escape.json", root)` DOES
      raise `PathEscapeError` (an attempted write one artifact-kind level up, i.e. outside the batch
      directory) AND, parametrized, `refuse_if_outside_root(root / ".." / ".." / ".." / "etc" /
      "passwd", root)` also raises `PathEscapeError` (a `..`-traversal escape attempt) — proving the
      refusal is resolved-path-based, not a naive string-prefix check.
    - `test_sc2_full_apply_cycle` (SC-2 integration): a manifest with ONE of each of the 6
      dispositions (including a `human-ratification-required` row) run through `apply_manifest` in
      one call — asserts: the constitution-plane row is refused before any write (spy proves zero
      calls for it specifically), the `create` row lands atomically, the `marker-merge` row is
      idempotent on a second pass, `preserve`/`conflict`/`derived-regenerate` are all no-ops, and the
      full cycle's summary dict correctly buckets all 6 rows.
  </behavior>
  <action>
    Implement `apply_manifest`, `UnknownDispositionError`, `ConcurrentDriftError` per the
    `<behavior>` block. Wire `apply_disposition` (Task 1) as the per-record dispatch
    `apply_manifest` calls in a loop over `manifest["dispositions"]`, sorted by `destination` for
    deterministic output ordering. Author `test_draft_confined_to_artifact_root` in this file
    exercising Task 1's `refuse_if_outside_root`/`PathEscapeError` directly (no `apply_manifest`
    involvement — this is a standalone unit proof of the draft-mode confinement primitive).
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_atomic_apply.py -x</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_atomic_apply.py -x` exits 0, covering all six
    named tests (including `test_draft_confined_to_artifact_root`). `uv run pytest
    tools/adoption_apply -q` (the full package so far) is green. `grep -n "subprocess"
    tools/adoption_apply/apply.py` shows either no match or only a fixed-argv git-plumbing call,
    never a call built from `record`/`manifest` content.
  </acceptance_criteria>
  <done>apply_manifest is total over DISPOSITION_ENUM, refuses concurrent drift, is idempotent on
  create+marker-merge, executes no arbitrary command, draft-mode writes are structurally confined to
  the task artifact root, and the SC-2 full-cycle proof is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `apply.py` invocation → arbitrary caller (Claude, bare CLI, CI, non-Claude-Code automation) | Must be safe with NO Claude PreToolUse hook in the loop at all. |
| Manifest content (drafted at time T0) → target filesystem (written at time T1 > T0) | TOCTOU window; the target may have changed between draft and apply. |
| `dispositions[]` vs `excluded[]` | Structurally disjoint arrays; a destination absent from both must fail loud, never default to `create`. |
| Discovery/draft write (batch-id/target-ref derived path) → task artifact root | A malformed/adversarial `batch_id`/`target_ref` must never let a draft write escape `artifacts/adoption/<batch-id>/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | TOCTOU between draft-time hash and apply-time write | mitigate | `_existing_hash` re-check immediately before every `create` write inside the same `apply_manifest` call — `test_concurrent_drift_refused` proves refusal, not silent overwrite. |
| T-27-03-02 | Tampering | Silent overwrite of a human-authored constitution file | mitigate | `refuse_if_constitution` called first in `apply_disposition`, independent of any Claude hook — `test_refuses_before_mutation`'s zero-call spy is the proof. |
| T-27-03-03 | Elevation of Privilege | Arbitrary command execution via manifest/draft content reaching a `subprocess` call | mitigate | `apply.py` never builds a `subprocess` argv from record/manifest content — `test_no_arbitrary_command_execution` is a structural + spy proof. |
| T-27-03-04 | Repudiation | A destination silently defaulting to `create` when absent from both `dispositions[]` and `excluded[]` | mitigate | `UnknownDispositionError` fails loud on any value outside `DISPOSITION_ENUM`; no `.get(x, "create")` default anywhere in the dispatch. |
| T-27-03-05 | Tampering | Draft-mode artifact write escaping the task artifact root | mitigate | `refuse_if_outside_root` checked before every draft write (Plan 27-06's `cli.py draft` sub-verb calls it); `test_draft_confined_to_artifact_root` proves both a direct out-of-root path and a `..`-traversal attempt are refused. |
| T-27-03-SC | Tampering (supply chain) | N/A | accept | Zero new package installs (stdlib + existing `tools.hooks`/`tools.harness_perms`/`tools.harness_emit`/`tools.adoption_scan` imports only); no slopcheck required. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_apply -q` — full package green.
- `uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py::test_refuses_before_mutation -x` — zero-call spy proof.
- `uv run pytest tools/adoption_apply/tests/test_atomic_apply.py::test_sc2_full_apply_cycle -x` — SC-2 integration proof.
</verification>

<success_criteria>
- ADOPT-05 fully satisfied: discovery/draft mode writes are structurally confined to the task
  artifact root via `refuse_if_outside_root` (this plan), on top of Plan 27-01's batch layout
  (target root never touched by discover) and the D-11 `--out` confinement `tools.adoption_scan.cli`
  already enforces; apply mode is atomic/collision-safe/idempotent create + marker-merge, refuses
  silent overwrite and concurrent target drift, refuses constitution destinations before mutation.
- SC-2 (ROADMAP): constitution destinations refused before mutation; non-constitution apply
  atomic/collision-safe/idempotent — proven end to end.
</success_criteria>

<output>
Create `.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-03-SUMMARY.md`
when done.
</output>
