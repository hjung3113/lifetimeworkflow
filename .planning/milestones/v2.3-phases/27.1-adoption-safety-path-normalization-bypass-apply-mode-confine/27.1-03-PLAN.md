---
phase: 27.1-adoption-safety-path-normalization-bypass-apply-mode-confine
plan: 03
type: execute
wave: 3
depends_on: ["27.1-01", "27.1-02"]
files_modified:
  - contracts/harness/adoption/manifest.schema.json
  - contracts/.hashes/manifest.json
  - .memory/derived/contracts-index.md
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  - tools/adoption_apply/tests/test_manifest_schema_conformance.py
autonomous: false
requirements: [ADOPT-05, ADOPT-06]

must_haves:
  truths:
    - "SC-4: `manifest.schema.json` constrains `destination`'s (and `evidenceRef.path`'s) shape — no absolute paths, no `..` path segments — proven by a data-based case per adversarial/legitimate destination string"
    - "SC-4: the schema edit, hash rebaseline, and derived-plane regeneration (BOTH `.memory/derived/contracts-index.md` AND `tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr`) land together, human-ratified, not agent self-approved"
    - "SC-5: CR-01, CR-02, CR-03, WR-01, WR-02, WR-03, WR-04 each carry a recorded disposition (fixed / accepted-with-rationale) in the SUMMARY"
    - "`tools.contract_drift.drift` is clean at the commit containing the schema edit"
    - "`brownfield-adoption/SKILL.md`'s Stage-5 claim ('promotion ratifies those decisions, and only then is the batch safely applied to the target') is verified TRUE post-27.1-02, and the harness emitter round-trip is a clean no-op (no source doc changed by this phase, so the emitted trees must diff-clean)"
  artifacts:
    - path: "contracts/harness/adoption/manifest.schema.json"
      provides: "`dispositionRecord.destination`, `excludedDestinationRecord.destination`, and `evidenceRef.path` each gain a `pattern` rejecting a leading `/` and any `..` path segment (WR-03)"
      contains: "(?!/)(?!.*(?:^|/)\\\\.\\\\.(?:/|$))"
    - path: "tools/adoption_apply/tests/test_manifest_schema_conformance.py"
      provides: "New schema-validation test file (mirrors `tools/adoption_scan/tests/test_schema_conformance.py`'s house convention) asserting `Draft202012Validator` rejects the adversarial destination set and accepts the legitimate set for `manifest.schema.json`'s `destination`/`path` fields"
      contains: "def test_manifest_schema_rejects_unsafe_destination"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined JCS SHA-256 manifest reflecting the re-edited `manifest.schema.json` — exactly one changed entry"
    - path: ".memory/derived/contracts-index.md"
      provides: "Regenerated contracts index whose `manifest.schema.json` row hash_prefix reflects the new content"
    - path: "tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr"
      provides: "Regenerated snapshot matching the new contracts-index content — the Pitfall-2 companion artifact that must move in the SAME commit"
  key_links:
    - from: "contracts/harness/adoption/manifest.schema.json"
      to: "tools/adoption_apply/apply.py::refuse_unsafe_destination"
      via: "defense-in-depth: the schema constrains a manifest's shape upstream of the runtime check that already refuses the same adversarial shapes at apply time (27.1-01)"
      pattern: "pattern"
    - from: "contracts/harness/adoption/manifest.schema.json"
      to: "contracts/.hashes/manifest.json"
      via: "tools.contract_hash.hash --write rebaseline, never hand-edited"
      pattern: "manifest.schema.json"
---

<objective>
Close WR-03 from `27-REVIEW.md`: `manifest.schema.json`'s `destination`/`path` fields have no
path-shape constraint today, so an absolute or `..`-containing destination validates successfully —
the upstream contract gap that let CR-01/CR-02's manifest content pass schema validation
undetected. Add the `pattern` constraint RESEARCH verified live discriminates correctly, as a
constitution-plane edit with the full ritual: hash rebaseline, drift check, BOTH derived-plane
artifacts regenerated together (Pitfall 2 — 27-02's own regression, not to be repeated), and a
blocking human-ratification checkpoint. Close out the phase with SC-5's mandatory disposition table
for all seven `27-REVIEW.md` findings.

Purpose: WR-03 and CR-01/CR-02 (27.1-01) are causally linked, not redundant — the schema catches a
malformed manifest even if the runtime checks were somehow bypassed or a manifest is hand-edited
between draft and apply, and WR-04's re-validation (27.1-02) only matters once the schema itself
constrains the shape.
Output: a human-ratified `manifest.schema.json` edit constraining `destination`/`path` shape, a
clean drift gate, both derived-plane artifacts current, and a complete SC-5 disposition table ready
for the phase SUMMARY.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-RESEARCH.md
@.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-VALIDATION.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-REVIEW.md
@.planning/phases/26.1-secret-pattern-precision-sc-4-false-positive-closure/26.1-01-PLAN.md
@contracts/harness/adoption/manifest.schema.json
@tools/adoption_scan/tests/test_schema_conformance.py
@harness/skills/brownfield-adoption/SKILL.md

<interfaces>
<!-- Exact current schema content and the verified-live pattern value — no re-derivation needed. -->

`contracts/harness/adoption/manifest.schema.json`'s three fields to edit, each currently
`{"type": "string", "minLength": 1, ...}` with no `pattern`:
`$defs.evidenceRef.properties.path` (line 30-34), `$defs.dispositionRecord.properties.destination`
(line 56-60), `$defs.excludedDestinationRecord.properties.destination` (line 79). Add
`"pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"` (the JSON-escaped form; the non-JSON regex is
`^(?!/)(?!.*(?:^|/)\.\.(?:/|$)).+$`) to each of these three property objects, alongside their
existing `type`/`minLength`/`description` keys. Nothing else in the file changes.

RESEARCH verified this pattern live: it matches `"contracts/widget.schema.json"` and
`"foo/bar..json"` and `"a/..b/c"` (legitimate `..` as a substring within a filename/segment, not a
bare `..` segment); it does NOT match `"/etc/passwd"` (leading `/`), `"a/../contracts/x.json"`
(a `..` segment mid-path), or `"../etc/passwd"` (leading `..` segment).

`tools/contract_hash/hash.py::build_manifest` / CLI `--write` rebaselines
`contracts/.hashes/manifest.json` — invoke via `uv run python -m tools.contract_hash.hash --write`,
never hand-edit. `tools/contract_drift/drift.py` — `uv run python -m tools.contract_drift.drift`
exits 0 iff the live tree matches the baseline. `tools/memory_regen/contracts_index.py` regenerates
`.memory/derived/contracts-index.md`; its own test
`tools/memory_regen/tests/test_contracts_index.py` asserts byte-parity against
`tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr` — BOTH files must be current in
the SAME commit (26.2/27.1-RESEARCH Pitfall 2 — 27-02's own regression, not to be repeated).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — schema-conformance test for the new `destination`/`path` pattern constraint</name>
  <files>tools/adoption_apply/tests/test_manifest_schema_conformance.py</files>
  <read_first>
    contracts/harness/adoption/manifest.schema.json (full, current — unedited),
    tools/adoption_scan/tests/test_schema_conformance.py (full — the house convention for schema
      tests in this repo: `Draft202012Validator(schema).iter_errors(document)`, a `_schema(name)`
      loader reading `contracts/harness/adoption/<name>.schema.json`),
    27.1-RESEARCH.md "Code Examples" section (the exact pattern value and its verified match/no-match
      set)
  </read_first>
  <behavior>
    New file, mirroring `test_schema_conformance.py`'s loader style but scoped to
    `manifest.schema.json`'s `destination`/`path` shape specifically (not the whole-document
    end-to-end validation `adoption_scan`'s test already covers). Build a minimal valid manifest
    document skeleton (`{"target_ref": "unknown", "dispositions": [...], "excluded": []}`) and, for
    each candidate `destination` string, substitute it into a single `dispositionRecord`
    (`{"destination": <candidate>, "disposition": "create"}`) and assert
    `Draft202012Validator(schema).iter_errors(document)` yields zero errors for legitimate
    destinations and at least one error for adversarial ones. Parametrize
    `test_manifest_schema_rejects_unsafe_destination` over: `"/etc/passwd"` (absolute),
    `"../etc/passwd"` (leading traversal), `"a/../contracts/x.json"` (mid-path traversal) — each
    asserted to produce a validation error. Parametrize
    `test_manifest_schema_accepts_safe_destination` over: `"contracts/widget.schema.json"`,
    `"src/widget.py"`, `"foo/bar..json"` (a literal `..` substring inside a filename, NOT a path
    segment — RESEARCH's verified non-match case proving the pattern doesn't over-refuse) — each
    asserted to produce zero errors. Add one more case,
    `test_manifest_schema_excluded_destination_and_evidence_path_also_constrained`, doing the same
    absolute/traversal negative check against `excludedDestinationRecord.destination` and
    `evidenceRef.path` (nested under a `dispositionRecord.evidence[0]`), proving all three fields
    share the constraint, not just the primary one.
  </behavior>
  <action>
    Create `tools/adoption_apply/tests/test_manifest_schema_conformance.py` with the four test
    functions described above, following `test_schema_conformance.py`'s exact
    loader/`Draft202012Validator` idiom.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_manifest_schema_conformance.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_manifest_schema_conformance.py -q` reports the
    three `test_manifest_schema_rejects_unsafe_destination` parametrize rows and the
    `excluded_destination_and_evidence_path` case as FAILING (the current, unedited schema has no
    `pattern`, so these adversarial strings currently validate with zero errors when the test
    expects at least one) — the required RED state. The
    `test_manifest_schema_accepts_safe_destination` rows already pass (no `pattern` yet means
    nothing is rejected, so legitimate strings trivially pass both before and after).
  </acceptance_criteria>
  <done>The new schema-conformance test file exists with all four test functions; the negative (adversarial) cases are red against the current, unedited `manifest.schema.json`.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — land the `destination`/`path` pattern constraint, rebaseline hash, verify drift, regenerate BOTH derived-plane artifacts</name>
  <files>
    contracts/harness/adoption/manifest.schema.json,
    contracts/.hashes/manifest.json,
    .memory/derived/contracts-index.md,
    tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  </files>
  <read_first>
    contracts/harness/adoption/manifest.schema.json (full, current state after Task 1 — no schema
      edit landed yet),
    tools/contract_hash/hash.py (`build_manifest`, `--write` CLI),
    tools/contract_drift/drift.py (`run_gate`, exit-0-iff-clean CLI),
    tools/memory_regen/contracts_index.py and tools/memory_regen/tests/test_contracts_index.py (the
      generator and its own snapshot test — confirms both derived artifacts are coupled),
    this plan's `<interfaces>` block (the exact JSON-escaped pattern value, already verified live)
  </read_first>
  <action>
    Add `"pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"` to exactly three property objects in
    `contracts/harness/adoption/manifest.schema.json`: `$defs.evidenceRef.properties.path`,
    `$defs.dispositionRecord.properties.destination`, and
    `$defs.excludedDestinationRecord.properties.destination`. Change nothing else in the file.
    Writing under `contracts/**` requires a human-authorized `GOLDEN_APPROVE_HUMAN` token or an
    equivalent human-present write — do not fabricate the token; if the write is denied, pause and
    request human authorization.

    Then run, in this strict order: `uv run python -m tools.contract_hash.hash --write`
    (rebaselines `contracts/.hashes/manifest.json` — only the `manifest.schema.json` entry's hash
    should change); `uv run python -m tools.contract_drift.drift` (confirm exit 0); `uv run python -m
    tools.docs_sync` (confirm `docs/reference/manifest.md` regenerates cleanly — `manifest.schema.json`
    IS globbed by `iter_schemas()`, unlike 26.1/26.2's plain-JSON gate-registry edits, so expect a
    real diff here, not a no-op); `uv run python -m tools.memory_regen.contracts_index` (confirms the
    `manifest.schema.json` row's hash_prefix in `.memory/derived/contracts-index.md` changes). Then
    re-run `uv run pytest tools/memory_regen -q`; if `test_contracts_index.py` is red on a snapshot
    mismatch, update the snapshot via its documented `--snapshot-update` mechanism (the new content
    is correct — this is the Pitfall-2 companion artifact that must move in the SAME commit as the
    schema edit and the `.memory/derived/contracts-index.md` regen, never left for a later plan to
    inherit red).

    Finally re-run every relevant test scope and confirm green: `uv run pytest
    tools/adoption_apply/tests/test_manifest_schema_conformance.py -x` (Task 1's tests, now fully
    green including the previously-red adversarial rows); `uv run pytest tools/adoption_apply -q`
    (no regression from the two Wave-1 plans' changes); `uv run pytest -q` (full workspace suite).
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift && uv run pytest tools/adoption_apply/tests/test_manifest_schema_conformance.py -x && uv run pytest tools/memory_regen -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0. `git diff --stat
    contracts/.hashes/manifest.json` shows exactly one changed entry (`manifest.schema.json`).
    `uv run pytest tools/adoption_apply/tests/test_manifest_schema_conformance.py -x` exits 0 (all
    four tests, including the three previously-red adversarial rows). `uv run pytest
    tools/memory_regen -q` exits 0 (`test_contracts_index.py`'s snapshot matches the regenerated
    content). `uv run pytest -q` (full suite) is green.
  </acceptance_criteria>
  <done>The `destination`/`path` pattern constraint is landed in `manifest.schema.json`; the hash manifest is rebaselined; drift is clean; docs/reference and BOTH derived-plane artifacts (`contracts-index.md` AND the `memory_regen` `.ambr` snapshot) are regenerated and current, ready to land in one atomic, human-ratified commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human ratification of the constitution-plane manifest.schema.json edit, plus the SC-5 seven-finding disposition table</name>
  <files>
    contracts/harness/adoption/manifest.schema.json,
    contracts/.hashes/manifest.json,
    .memory/derived/contracts-index.md,
    tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  </files>
  <read_first>
    .github/CODEOWNERS (the /contracts/ routing + advisory-enforcement + solo-owner self-approval
      caveats already recorded in STATE.md),
    .planning/phases/26.1-secret-pattern-precision-sc-4-false-positive-closure/26.1-01-PLAN.md
      lines 302-372 (the checkpoint task this one mirrors in shape),
    27-REVIEW.md (all seven findings this checkpoint's disposition table must dispose of),
    harness/skills/brownfield-adoption/SKILL.md (the Stage-5 claim this phase makes true)
  </read_first>
  <action>
    BLOCKING human checkpoint — the agent does NOT self-ratify. Present for review, in this order:

    1. The exact diff of `contracts/harness/adoption/manifest.schema.json` (the three added
       `pattern` keys, nothing else changed).
    2. The rebaselined `contracts/.hashes/manifest.json` diff (exactly one changed entry).
    3. Confirmation that BOTH `.memory/derived/contracts-index.md` AND
       `tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr` moved together (Pitfall 2
       — 27-02's own regression, not repeated here).
    4. Confirmation that `harness/skills/brownfield-adoption/SKILL.md`'s Stage-5 claim ("promotion
       ratifies those decisions, and only then is the batch safely applied to the target") is now
       literally true given Plan 27.1-02's `check_valid` wiring — no doc edit is required if the
       claim already reads as true; if it does not, correct it in this task before proceeding
       (constitution-adjacent but not itself `contracts/`-gated, so an ordinary edit suffices).
    5. Confirmation that the emitted runtime trees are still a clean no-op for this phase:
       `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude`
       (this phase touches no `harness/` source doc, so this MUST be a no-op; a diff here would mean
       something in this phase accidentally drifted the emitted surface).
    6. The following disposition table for all seven `27-REVIEW.md` findings — copy this table
       verbatim into the phase's closing SUMMARY.md:

       | Finding | Disposition | Evidence |
       |---------|-------------|----------|
       | CR-01 (constitution refusal bypassed by `..`/case-variant destination) | fixed | `refuse_unsafe_destination` (27.1-01) resolves, confines, and case-insensitively classifies the destination before any write; `test_apply_disposition_refuses_hostile_destinations_end_to_end` proves it against the full `HOSTILE_DESTINATIONS` table |
       | CR-02 (no destination confinement in apply mode) | fixed | `refuse_unsafe_destination` calls the existing `refuse_if_outside_root` for every apply-mode write, not only draft (27.1-01); `test_absolute_destination_refused_zero_writes`/`test_traversal_destination_refused_zero_writes` prove zero filesystem calls occur |
       | CR-03 (`approval.check_valid` never consulted by apply) | fixed | `_cmd_apply` now calls `check_valid` as a hard precondition, refusing with exit 4 (27.1-02); `test_cli_apply_refuses_without_approval`/`test_cli_apply_refuses_on_stale_approval` prove it |
       | WR-01 (unlocked `_apply_marker_merge` read-modify-write) | fixed | `fcntl.flock`-guarded critical section mirroring `batch.py::update_status` (27.1-01); `test_concurrent_marker_merge_does_not_lose_writes` proves it |
       | WR-02 (`check_valid` raises instead of returning `False`) | fixed | `try/except (FileNotFoundError, OSError): return False` around the draft-hash recomputation (27.1-02); `test_valid_false_on_missing_draft_artifact` proves it |
       | WR-03 (no path-shape constraint on `destination`/`path` in the schema) | fixed | `manifest.schema.json`'s `destination`/`path` fields gain the `pattern` constraint (this plan); `test_manifest_schema_rejects_unsafe_destination` proves it |
       | WR-04 (`_cmd_apply` never re-validates the manifest against its schema) | fixed | `_cmd_apply` calls the existing `_validate("manifest", manifest)` helper before dispatching to `apply_manifest` (27.1-02); `test_cli_apply_refuses_on_malformed_manifest` proves it |

       Additionally note, for the record (not one of the seven required dispositions but found
       during this phase's research): a symlink-read information-leak in `_apply_marker_merge` was
       identified and closed via an `O_NOFOLLOW` read primitive (27.1-01,
       `test_marker_merge_refuses_symlink_read`) — new scope beyond `27-REVIEW.md`'s literal
       findings, disposed of in the same phase since it shares the exact module/test files already
       being edited.

    Then PAUSE. Ratify via CODEOWNERS PR review or an explicit human-supplied write — never a
    fabricated `GOLDEN_APPROVE_HUMAN` token or `HARNESS_DEV_BYPASS` used to self-land the
    ratification. Note for the record (do not act on it): `tools/hooks/secret_scan.py:44-47` remains
    a documented, out-of-scope seam carried forward unchanged from 26.2's fence (D-07).
  </action>
  <what-built>
    A human-ratified `manifest.schema.json` `destination`/`path` shape constraint (WR-03), landed
    with a rebaselined hash manifest and both current derived-plane artifacts, plus a complete
    seven-finding disposition table closing out Phase 27.1's SC-5.
  </what-built>
  <how-to-verify>
    1. Confirm `uv run python -m tools.contract_drift.drift` reports clean.
    2. Review the diff under `contracts/harness/adoption/manifest.schema.json` and
       `contracts/.hashes/manifest.json` — this is the CODEOWNERS-gated constitution-plane change.
    3. Confirm `uv run pytest tools/adoption_apply -q` and `uv run pytest tools/memory_regen -q` are
       both green.
    4. Confirm the emit-drift check (`tools.harness_emit.generate` + `git diff --exit-code`) is a
       clean no-op.
    5. Confirm the seven-row disposition table above is complete and will be copied into the phase
       SUMMARY.
    6. Confirm no agent self-approved this change.
  </how-to-verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0; `git diff --stat
    contracts/.hashes/manifest.json` shows exactly one changed entry
    (`contracts/harness/adoption/manifest.schema.json`); `uv run pytest tools/adoption_apply -q` and
    `uv run pytest tools/memory_regen -q` are both green; `uv run python -m
    tools.harness_emit.generate && git diff --exit-code -- .opencode .claude` is a no-op; the
    seven-row disposition table is present in the checkpoint's action output and gets copied into
    the phase SUMMARY verbatim; `uv run pytest -q` (full workspace suite) is green.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: uv run python -m tools.contract_drift.drift && uv run pytest -q</human-check>
  </verify>
  <resume-signal>Type "approved" once the constitution-plane diff and the seven-finding disposition table have been reviewed, or describe required changes.</resume-signal>
  <done>The schema edit, rebaselined manifest, and both regenerated derived-plane artifacts are human-ratified via the standard constitution-plane path, not agent self-approval; all seven 27-REVIEW.md findings carry a recorded disposition in the SUMMARY; the full workspace suite is green; the phase's SC-1..SC-5 are all satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent session → `contracts/harness/adoption/manifest.schema.json` | Constitution-plane write; denied by `contract_guard` unless human-authorized (`GOLDEN_APPROVE_HUMAN`) or dev-bypassed — never agent self-ratified. |
| Drafted/hand-edited `manifest.json` → schema validation | The schema is now itself a defense-in-depth layer against a manifest whose `destination`/`path` shape is unsafe, independent of the runtime checks in `apply.py`. |
| `contract_hash --write` output → `contracts/.hashes/manifest.json` | Machine-computed baseline; must never be hand-edited. |
| `docs_sync`/`memory_regen` output → committed-derived plane | Machine-regenerated; CI's `stale-derived` job is the non-bypassable verify side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-08 | Tampering | `manifest.schema.json`'s `destination`/`path` fields (upstream of CR-01/CR-02's runtime checks) | mitigate | New `pattern` constraint rejects absolute paths and `..` path segments at the schema layer — defense-in-depth alongside (not instead of) 27.1-01's runtime `refuse_unsafe_destination` — closes WR-03. |
| T-27.1-09 | Tampering | `contracts/.hashes/manifest.json` | mitigate | Rebaseline ONLY via `tools.contract_hash.hash --write` — never hand-edited. Verified by `git diff --stat` showing exactly one changed entry. |
| T-27.1-10 | Elevation of Privilege | `contracts/harness/adoption/manifest.schema.json` write path | accept (out-of-band) | Unchanged existing controls: `contract_guard` PreToolUse deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS `/contracts/`. This plan adds no new bypass; the checkpoint task is the explicit ratification surface. Residual risk (CODEOWNERS advisory-only unless branch protection enabled, solo-owner self-approval nuance) is pre-accepted per the Phase-26/26.1/26.2 precedent. |
| T-27.1-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan; existing `tools.contract_hash`/`tools.contract_drift`/`tools.docs_sync`/`tools.memory_regen`/`tools.harness_emit` CLIs only; no slopcheck required. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_apply/tests/test_manifest_schema_conformance.py -x` exits 0.
- `uv run python -m tools.contract_drift.drift` exits 0.
- `uv run pytest tools/memory_regen -q` exits 0 (both derived artifacts current together).
- `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude` is a no-op.
- `uv run pytest -q` (full workspace suite) green before phase close.
</verification>

<success_criteria>
- SC-4: `manifest.schema.json` constrains `destination`/`path` shape; the edit, hash rebaseline, and BOTH derived-plane artifacts land together, human-ratified; drift is clean.
- SC-5: all seven `27-REVIEW.md` findings carry a recorded disposition in the SUMMARY, none silently dropped.
- Phase 27.1's SC-1..SC-3 (closed by 27.1-01/27.1-02) are re-confirmed green as part of this plan's full-suite gate.
</success_criteria>

<output>
Create `.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-03-SUMMARY.md` when done. Copy the seven-row disposition table from the checkpoint task verbatim into the SUMMARY.
</output>
