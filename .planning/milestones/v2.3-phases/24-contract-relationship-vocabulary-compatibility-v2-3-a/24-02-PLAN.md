---
phase: 24-contract-relationship-vocabulary-compatibility-v2-3-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/project.toml
  - workspace.toml
  - tools/harness_config/loader.py
  - tools/harness_config/__init__.py
  - tools/workspace_config/loader.py
  - tools/workspace_config/__init__.py
  - tools/harness_config/tests/test_topology_relationships.py
autonomous: true
requirements: [TOPO-02, TOPO-03]

must_haves:
  truths:
    - "harness/project.toml and workspace.toml each still parse successfully with zero edits to any pre-existing line after the change"
    - "contract_graph_relationships(cfg) returns the raw list unchanged for both project and workspace configs, and returns [] on the current linear configs (no explicit records yet)"
    - "effective_relationships() lowers the current source→sink/greeting edge deterministically to a namespaced authority/dependent relationship with zero config edits required"
    - "effective_relationships() raises deterministically on duplicate id, duplicate semantic edge, and contradiction inputs"
    - "existing loader signatures (load_project/languages/components/pipeline/language_bash_scopes/load_workspace/members/edges/split_endpoint) are unchanged"
    - "D-03: [[contract_graph.relationships]] mirrors the schema fields 1:1; contract_graph_relationships() returns raw list[dict] passthrough with zero validation/traversal/policy"
    - "D-04: legacy edges lower with authority=from, dependents=[to], and a namespaced id pipeline/<contract>/<from>-><to>, then union with explicit records"
    - "D-05: effective_relationships() fails on duplicate id, duplicate semantic edge (authority,contract,dependent), and contradiction (one contract claimed by two authorities)"
  artifacts:
    - path: "tools/harness_config/loader.py"
      provides: "contract_graph_relationships() + effective_relationships() additive accessors"
      exports: ["contract_graph_relationships", "effective_relationships"]
    - path: "tools/workspace_config/loader.py"
      provides: "contract_graph_relationships() accessor mirrored for workspace configs"
    - path: "tools/harness_config/tests/test_topology_relationships.py"
      provides: "lowering-determinism, accessor-passthrough, and 3-failure-mode test coverage"
    - path: "harness/project.toml"
      provides: "additive [contract_graph] slot"
      contains: "contract_graph"
    - path: "workspace.toml"
      provides: "additive [contract_graph] slot"
      contains: "contract_graph"
  key_links:
    - from: "tools/harness_config/loader.py"
      to: "harness/project.toml"
      via: "cfg.get(\"contract_graph\", {}).get(\"relationships\", [])"
      pattern: "contract_graph"
    - from: "tools/harness_config/tests/test_topology_relationships.py"
      to: "tools/harness_config/loader.py"
      via: "effective_relationships(...)"
      pattern: "effective_relationships"
---

<objective>
Add the additive `[[contract_graph.relationships]]` TOML slot to both `harness/project.toml` and
`workspace.toml` (TOPO-02), a raw-passthrough `contract_graph_relationships()` accessor in both
loaders, and the single deterministic `effective_relationships()` lowering+union path (TOPO-03) that
turns every legacy `[pipeline].edges` entry into an authority/dependent relationship, unions it with
any explicit records, and fails on duplicate-id / duplicate-semantic-edge / contradiction — while
leaving the current linear configs byte-unchanged.

Purpose: this is the coexistence seam between the legacy linear pipeline model and the general
relationship vocabulary Phase 25's compiler will consume — additive only, zero enforcement beyond
the three named failure modes, existing loader signatures untouched.
Output: `tools/harness_config/loader.py` gains `contract_graph_relationships()` +
`effective_relationships()`; `tools/workspace_config/loader.py` gains the mirrored accessor; both
TOML files gain an empty `[contract_graph]` data slot; new tests prove passthrough, determinism, and
the three failure modes.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-contract-relationship-vocabulary-compatibility-v2-3-a/24-CONTEXT.md
@.planning/phases/24-contract-relationship-vocabulary-compatibility-v2-3-a/24-RESEARCH.md
@tools/harness_config/loader.py
@tools/harness_config/__init__.py
@tools/workspace_config/loader.py
@tools/workspace_config/__init__.py
@harness/project.toml
@workspace.toml

<interfaces>
<!-- Existing raw-passthrough accessor pattern to near-copy (tools/harness_config/loader.py:53-74) -->
```python
def components(cfg: dict | None = None) -> list[dict]:
    if cfg is None:
        cfg = load_project()
    return list(cfg.get("components", []))

def pipeline(cfg: dict | None = None) -> dict:
    if cfg is None:
        cfg = load_project()
    return dict(cfg.get("pipeline", {}))
```
<!-- Workspace two-level .get idiom to mirror (tools/workspace_config/loader.py:55-64) -->
```python
def edges(cfg: dict | None = None) -> list[dict]:
    if cfg is None:
        cfg = load_workspace()
    return list(cfg.get("pipeline", {}).get("edges", []))
```
<!-- PEP 562 lazy re-export __all__ pattern (tools/harness_config/__init__.py, tools/workspace_config/__init__.py) -->
```python
__all__ = ["components", "language_bash_scopes", "languages", "load_project", "pipeline"]

def __getattr__(name: str):
    if name in __all__:
        from tools.harness_config import loader
        return getattr(loader, name)
    raise AttributeError(...)
```
<!-- BEGIN/END marker + "Pure DATA, no enforcement / Consumers:" doc convention (harness/project.toml:47-81) -->
```toml
# BEGIN PIPE-01 topology
# ... Pure DATA, no enforcement ...
# Consumers:
#   * tools/harness_config/loader.py — components() + pipeline() passthrough helpers.
[[components]]
...
# END PIPE-01 topology
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive [contract_graph] TOML slot + raw-passthrough accessors (both configs)</name>
  <files>
    harness/project.toml, workspace.toml, tools/harness_config/loader.py,
    tools/harness_config/__init__.py, tools/workspace_config/loader.py,
    tools/workspace_config/__init__.py
  </files>
  <read_first>
    harness/project.toml (BEGIN/END PIPE-01 marker convention, lines 47-81),
    tools/harness_config/loader.py (components()/pipeline() idiom + "Keep the signatures stable" docstring),
    tools/harness_config/__init__.py (PEP 562 __all__ re-export),
    tools/workspace_config/loader.py (edges() two-level .get idiom),
    tools/workspace_config/__init__.py,
    .planning/phases/24-.../24-CONTEXT.md D-03
  </read_first>
  <action>
    Append a NEW `# BEGIN CONTRACT-GRAPH topology-relationships` / `# END CONTRACT-GRAPH
    topology-relationships` marker block to `harness/project.toml`, placed AFTER the existing `#
    END PIPE-01 topology` line — leave every existing line byte-untouched (append-only, per
    Pitfall 4). Inside the new block, declare an empty `[contract_graph]` table with a
    "Pure DATA, no enforcement / Consumers:" doc-comment naming
    `tools/harness_config/loader.py — contract_graph_relationships()/effective_relationships()
    passthrough+lowering helpers` — do NOT add any `[[contract_graph.relationships]]` entries (the
    linear default stays byte-unchanged; explicit records are opt-in for a downstream project to
    add). Mirror the identical marker + empty `[contract_graph]` table + doc-comment in
    `workspace.toml` (append after its `[pipeline]` block). Add `contract_graph_relationships(cfg:
    dict | None = None) -> list[dict]` to `tools/harness_config/loader.py` as a near-copy of
    `components()`: default-load via `load_project()` if `cfg is None`, `return
    list(cfg.get("contract_graph", {}).get("relationships", []))` — zero validation/traversal/
    policy (D-03). Add the identically-named accessor to `tools/workspace_config/loader.py`
    (default-loads via `load_workspace()`), same two-level `.get` body. Add
    `"contract_graph_relationships"` to both `__init__.py` `__all__` lists (PEP 562 lazy re-export,
    `__getattr__` logic unchanged). Do NOT change the signature of `load_project`, `load_workspace`,
    `languages`, `components`, `pipeline`, `members`, `edges`, or `split_endpoint`.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_config/tests/test_loader.py tools/harness_lint/tests/test_pipeline_config.py tools/harness_lint/tests/test_workspace_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    `git diff --unified=0 harness/project.toml` shows only added (`+`) lines, zero removed (`-`)
    lines from existing content; same for `git diff --unified=0 workspace.toml`; `uv run python -c
    "from tools.harness_config import contract_graph_relationships as f; assert f() == []"` exits 0;
    `uv run python -c "from tools.workspace_config import contract_graph_relationships as f; assert
    f() == []"` exits 0; `uv run pytest tools/harness_config/tests/test_loader.py
    tools/harness_lint/tests/test_pipeline_config.py tools/harness_lint/tests/test_workspace_config.py
    -q` still passes unmodified (existing consistency gates untouched by the additive slot).
  </acceptance_criteria>
  <done>Both TOML files carry the new empty [contract_graph] slot with zero edits to pre-existing content; both loaders expose the raw-passthrough accessor; all pre-existing loader/config tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: effective_relationships() — deterministic lowering + union + 3 failure modes</name>
  <files>tools/harness_config/loader.py, tools/harness_config/tests/test_topology_relationships.py</files>
  <read_first>
    .planning/phases/24-.../24-CONTEXT.md D-04 and D-05,
    .planning/phases/24-.../24-RESEARCH.md "Deterministic lowering + union skeleton" + Pitfall 6 (non-determinism) + Pitfall 7 (workspace repo:stage passthrough),
    harness/project.toml (the unedited source→sink/greeting edge),
    workspace.toml (the unedited member-a:emit→member-b:ingest/greeting edge),
    tools/harness_config/loader.py pipeline()/contract_graph_relationships() (just added in Task 1)
  </read_first>
  <behavior>
    - Test: `effective_relationships(load_project())` on the unedited generic default returns
      exactly one relationship: `id == "pipeline/greeting/source->sink"`, `contract == "greeting"`,
      `authority == "source"`, `dependents == ["sink"]` — lowering-only, zero config edits needed.
    - Test: calling `effective_relationships` twice on the same cfg dict yields byte-identical
      (`==`) output (determinism).
    - Test: the returned list is sorted by `id` (stable-sort proof — construct a cfg with 2+
      lowered/explicit entries out of alphabetical order and assert the output is sorted).
    - Test: given a cfg whose `contract_graph.relationships` holds one explicit record with an id
      distinct from any lowered id, the union output contains BOTH the lowered and the explicit
      entry.
    - Test: dup-id failure — two records (any combination of lowered/explicit) sharing the same
      `id` raises a deterministic exception.
    - Test: dup-semantic-edge failure — two records with an identical `(authority, contract,
      dependent)` triple raise (construct via a multi-dependent record expanding to 2 triples where
      one collides with another record's single triple).
    - Test: contradiction failure — the same `contract` claimed by two different `authority`
      values raises.
    - Test: workspace lowering passthrough — `effective_relationships(load_workspace())` on the
      unedited `member-a:emit`→`member-b:ingest`/`greeting` edge produces `authority ==
      "member-a:emit"` and `dependents == ["member-b:ingest"]` VERBATIM (raw `repo:stage` strings,
      no `split_endpoint` interpretation — Pitfall 7).
  </behavior>
  <action>
    Implement `effective_relationships(cfg: dict | None = None) -> list[dict]` in
    `tools/harness_config/loader.py`: default-load via `load_project()` if `cfg is None`; lower
    each entry of `pipeline(cfg).get("edges", [])` to `{"id": f"pipeline/{contract}/{from_}->
    {to_}", "contract": contract, "authority": from_, "dependents": [to_]}` (namespaced id,
    delimiter fixed as `pipeline/<contract>/<from>-><to>` per D-04) — treat `from`/`to` as OPAQUE
    strings, never calling `split_endpoint` or interpreting a `repo:` half (Pitfall 7, keeps the
    function cfg-agnostic across `load_project()` and `load_workspace()` output since both plain
    dicts expose `pipeline.edges` + `contract_graph.relationships`). Union the lowered list with
    `contract_graph_relationships(cfg)` (explicit records, unmodified passthrough). Detect and raise
    a `ValueError` with a deterministic, stable-sorted diagnostic message on: (a) duplicate `id`
    across the merged list; (b) duplicate `(authority, contract, dependent)` triple — expand every
    record's `dependents` list into one triple per dependent before comparing; (c) contradiction —
    the same `contract` key resolves to more than one distinct `authority` value across the merged
    list. On success, return the merged list stable-sorted by `id` (`sorted(merged, key=lambda r:
    r["id"])`). Keep the function pure — no I/O beyond the passed/loaded `cfg` dict, no wall-clock,
    no `set` in the output-construction path (only for internal duplicate-detection lookups).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_config/tests/test_topology_relationships.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/harness_config/tests/test_topology_relationships.py -q` exits 0 with at
    least 8 collected tests; a test asserts `effective_relationships(load_project())[0]["id"] ==
    "pipeline/greeting/source->sink"`; three separate tests each wrap a call in
    `pytest.raises(ValueError)` for hand-built dup-id / dup-semantic-edge / contradiction cfg dicts
    using domain-neutral names (e.g. authorities `"a"`/`"b"`, contract `"widget"`) — not real
    repo files — to stay GEN-04-clean; one test asserts the workspace-lowering case preserves the
    raw `member-a:emit` / `member-b:ingest` strings verbatim.
  </acceptance_criteria>
  <done>effective_relationships() lands with lowering+union+3 fail-modes+stable-sort, all test-proven against both the unedited project.toml and workspace.toml plus hand-built failure fixtures.</done>
</task>

<task type="auto">
  <name>Task 3: Byte-unchanged regression + full existing-gate closeout</name>
  <files>tools/harness_config/tests/test_topology_relationships.py</files>
  <read_first>
    .planning/phases/24-.../24-RESEARCH.md Pitfall 4 (byte-drift in linear fixtures) and Validation Architecture sampling rate,
    examples/log-parser/project.toml,
    tools/harness_lint/tests/test_pipeline_config.py,
    tools/harness_lint/tests/test_workspace_config.py,
    tools/harness_lint/tests/test_core_no_example_dep.py (the GEN-04 guard — note its exact
      substring scan: ANY tracked line under tools/, harness/, libs/ containing the literal
      contiguous substring "examples/" is flagged; only the harness/project.toml
      root=/persona=/test_paths= pointer lines are exempt — `tools/harness_config/tests/` is
      itself a scanned core path with no exemption)
  </read_first>
  <action>
    Extend `test_topology_relationships.py` with a regression test that loads the log-parser
    instance config and asserts `contract_graph_relationships(cfg) == []` (the instance declares no
    explicit records — zero required edits) while `pipeline(cfg)`/`components(cfg)` are read
    unaffected by the new accessor. GEN-04 SAFETY (mandatory): this test file lives under
    `tools/harness_config/tests/`, a path scanned by the GEN-04 guard
    (`tools/harness_lint/tests/test_core_no_example_dep.py`) for the literal contiguous substring
    `"examples/"` — a plain string literal such as `"examples/log-parser/project.toml"` would trip
    a NEW GEN-04 violation with no exemption available here. Build the instance config path via
    NON-CONTIGUOUS `Path` segments instead — e.g. `_REPO_ROOT.joinpath("examples", "log-parser",
    "project.toml")` or `Path(__file__).resolve().parents[3] / "examples" / "log-parser" /
    "project.toml"` constructed with separate segment arguments (not one joined string), and keep
    any surrounding docstring/comment prose free of the same contiguous substring too. Pass the
    constructed `Path` to `tools.harness_config.load_project(path=...)`. Do NOT edit
    `examples/log-parser/project.toml`. DEVIATION NOTE: if
    `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` still fails after this
    edit, a line in the new test (code, docstring, or comment) contains the contiguous `examples/`
    substring somewhere — reconstruct that line via separate `Path(...)`/`joinpath(...)` segments
    (or rephrase the prose) rather than a single string literal, then re-run the guard before
    moving on. Then run the full verification sweep named in the Validation Architecture
    (24-VALIDATION.md) sampling rate and report each command's exit status as this task's
    deliverable: `uv run pytest tools/harness_config tools/harness_lint/tests/test_pipeline_config.py
    tools/harness_lint/tests/test_workspace_config.py
    tools/harness_lint/tests/test_core_no_example_dep.py -q`, `uv run pytest -q` (full suite),
    `uv run python -m tools.contract_drift.drift`, `git diff --check` (byte/whitespace hygiene).
  </action>
  <verify>
    <automated>uv run pytest -q && uv run python -m tools.contract_drift.drift && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    All four commands above exit 0; `uv run pytest
    tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0 when re-run AFTER the new
    regression test is added (proves the non-contiguous path construction introduced no new GEN-04
    violation); `git diff --stat harness/project.toml workspace.toml
    examples/log-parser/project.toml` shows `examples/log-parser/project.toml` with ZERO diff lines
    (fully untouched) while `harness/project.toml`/`workspace.toml` show only the appended
    `[contract_graph]` block as new lines.
  </acceptance_criteria>
  <done>Full existing gate suite (contract-drift, GEN-04, topology/workspace consistency tests, full pytest, git diff hygiene) is green with the Phase-24 additions; all three linear configs (core project.toml, workspace.toml, log-parser instance) remain valid with zero required edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `harness/project.toml` / `workspace.toml` (developer-authored TOML) → `tomllib.load` → `effective_relationships()` | Config text is human-authored, not attacker-controlled, but a malformed or hand-edited relationship record (bad shape, missing `from`/`to`, non-string endpoint) must fail closed with a clean diagnostic, not crash the loader. |
| Opaque endpoint strings (`authority`/`dependents`, and lowered `from`/`to`) | Phase 24 stores these as pass-through strings only — no filesystem/path resolution happens here (that is explicitly Phase 25's job). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | `effective_relationships()` lowering/union | mitigate | Defensive `dict.get`/`KeyError`-safe access on `cfg.get("pipeline", {}).get("edges", [])`; raise a typed `ValueError` with a deterministic message on any of the 3 named failure modes rather than propagating a raw `KeyError`/`IndexError`. |
| T-24-05 | Tampering | Path/endpoint strings in `authority`/`dependents` | accept (documented boundary) | Phase 24 passes `authority`/`dependents`/lowered `from`/`to` through as OPAQUE strings — no filesystem resolution, no `split_endpoint` interpretation, in this phase (Pitfall 7). Endpoint resolution — and any traversal-hardening it needs — is explicitly Phase 25 scope; do not implement resolution here. |
| T-24-06 | Repudiation | Non-deterministic union output | mitigate | Stable-sort the returned list by `id`; expand `dependents` deterministically for triple-comparison; no `set` iteration order or wall-clock in the output-construction path (Pitfall 6). |
| T-24-07 | Tampering | Byte-drift in `harness/project.toml`/`workspace.toml` | mitigate | Append-only `BEGIN/END CONTRACT-GRAPH` marker block; Task 1/3 acceptance criteria require a diff showing only added lines; Task 3's regression proves `examples/log-parser/project.toml` needs zero edits. |
| T-24-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan; no slopcheck required. |
</threat_model>

<verification>
- `uv run pytest tools/harness_config/tests/test_topology_relationships.py -q` — accessor/lowering/union/failure-mode tests green.
- `uv run pytest tools/harness_lint/tests/test_pipeline_config.py tools/harness_lint/tests/test_workspace_config.py tools/harness_lint/tests/test_core_no_example_dep.py -q` — existing topology/workspace/GEN-04 gates stay green.
- `uv run pytest -q && uv run python -m tools.contract_drift.drift` — full suite + drift green.
- `git diff --check` — no byte/whitespace hygiene violations.
</verification>

<success_criteria>
- `[contract_graph]` slot is accepted additively by both `harness/project.toml` and
  `workspace.toml` with zero edits to pre-existing content.
- `contract_graph_relationships(cfg)` is a pure raw-list passthrough in both loaders; existing
  loader signatures are unchanged.
- `effective_relationships()` deterministically lowers legacy edges, unions with explicit records,
  and fails on dup-id / dup-semantic-edge / contradiction — proven by test.
- The current linear `harness/project.toml`, `workspace.toml`, and
  `examples/log-parser/project.toml` all remain valid with zero required edits.
</success_criteria>

<output>
Create `.planning/phases/24-contract-relationship-vocabulary-compatibility-v2-3-a/24-02-SUMMARY.md` when done
</output>
