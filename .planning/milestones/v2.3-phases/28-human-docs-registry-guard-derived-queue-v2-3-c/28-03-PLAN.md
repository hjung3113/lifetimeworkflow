---
phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - tools/docs_guard/registry.py
  - tools/docs_guard/tests/test_registry.py
autonomous: true
requirements: [DOCSUP-01]

must_haves:
  truths:
    - "SC-1: `load_registry` rejects EVERY row of the adversarial table — path escape (absolute, `..` segment, symlink-escape), duplicate id, empty `sources` on a `severity = \"required\"` row, a `target` matching `DERIVED_GLOBS`, and an `docs/adr/**` target whose `dispositions` are anything other than exactly `[\"REVIEWED_STILL_CURRENT\", \"SUPERSEDING_ADR_REQUIRED\"]`"
    - "SC-1: the adversarial table was authored and RED-confirmed against a validator that performs SHAPE validation ONLY, with each row's failure message recorded, BEFORE the semantic rules landed"
    - "Path escape is refused BEFORE any filesystem call — a structural pre-check on the raw selector spelling, mirroring `apply.py:109-125`; the resolve-based confinement is the backstop, not the primary control"
    - "`DERIVED_GLOBS` is IMPORTED from `tools.adoption_scan.destinations`, never retyped — a second copy could silently disagree with the destination catalog (destinations.py:105-114 is the single authoritative home)"
    - "`refuse_unsafe_destination` is deliberately NOT reused: it also refuses `contracts/**`, and registry SOURCES legitimately include contract schemas. The narrower `refuse_if_outside_root` / `_confine` idiom is used instead (research 'Deliberately NOT used')"
    - "A `docs/adr/**` target whose ADR is `Status: proposed` is rejected at validation — a mid-ratification document's content is expected to change, so binding to it is a defect (research Q6)"
    - "Every diagnostic is deterministically sorted, mirroring `loader.py:149-160`'s duplicate-id block — a gate whose message ordering varies across runs is not reviewable"
    - "`load_registry(path=...)` takes an EXPLICIT path (D-14, the `load_project(path=...)` seam) so an instance-local overlay is possible later; only the core registry ships in Phase 28"
    - "A MISSING registry file is not an error: it yields zero bindings, so plans 28-05/28-06 can land before the seed data in plan 28-07. Missing != invalid — only invalid maps to exit 3"
  artifacts:
    - path: "tools/docs_guard/registry.py"
      provides: "`load_registry(path=..., root=...)` -> sorted list[Binding]; `RegistryError` (ValueError subclass) carrying the sorted diagnostic list; schema validation via Draft202012Validator against the constitution-plane schema, then the five DOCSUP-01 semantic rejections"
      contains: "DERIVED_GLOBS"
    - path: "tools/docs_guard/tests/test_registry.py"
      provides: "`REJECTION_CASES` — one parametrized row per DOCSUP-01 rejection class plus its spelling variants — and a paired negative-control table of registries that must stay VALID"
      contains: "REJECTION_CASES"
  key_links:
    - from: "tools/docs_guard/registry.py"
      to: "contracts/harness/docs/doc-dependencies.schema.json"
      via: "jsonschema.Draft202012Validator over the tomllib-parsed dict — the constitution-plane shape gate"
      pattern: "Draft202012Validator"
    - from: "tools/docs_guard/registry.py"
      to: "tools/adoption_scan/destinations.py::DERIVED_GLOBS"
      via: "import, never retyped"
      pattern: "from tools.adoption_scan.destinations import"
---

<objective>
Implement DOCSUP-01: the registry loader and its five rejections, with the adversarial-input table
authored and RED-confirmed first.

Purpose: `/contract-check` step 1 pairs `<name>.schema.json` with a sibling `.yaml`/`.yml`/`.json`
instance (ci.yml:108-127). The registry instance is `.toml` and lives in `docs/`, so CI silently
skips it. Per D-16/A7 this module is the registry's ONLY validator — which is exactly why its
rejections must be proven by an adversarial table rather than a smoke test.

Output: `tools/docs_guard/registry.py` + `REJECTION_CASES`, with RED evidence recorded.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-CONTEXT.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-RESEARCH.md
@contracts/harness/docs/doc-dependencies.schema.json
@tools/adoption_scan/destinations.py
@tools/harness_config/loader.py
@tools/adoption_apply/apply.py

<interfaces>
<!-- Verified against source in this session. -->

`tools/adoption_scan/destinations.py:105-114` — IMPORT this, never retype it:

    DERIVED_GLOBS: list[str] = [
        "docs/reference/**", ".memory/derived/**", ".memory/state/**", ".opencode/**",
        ".claude/agents/**", ".claude/commands/**", ".claude/skills/**", "opencode.json",
    ]

`tools/hooks/contract_guard.py:44` — also import rather than retype, for the ADR-target test:

    CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**", "golden/**"]

`tools/harness_config/loader.py:149-160` — the deterministic duplicate-id diagnostic idiom to copy:

    id_seen: set[str] = set(); dup_ids: set[str] = set()
    for rel in merged:
        rid = rel["id"]
        if rid in id_seen: dup_ids.add(rid)
        id_seen.add(rid)
    if dup_ids:
        raise ValueError("...: duplicate relationship id(s): " + ", ".join(sorted(dup_ids)))

`tools/adoption_apply/apply.py:94` — the narrow confinement helper this module MAY reuse:

    def refuse_if_outside_root(path: str | Path, root: str | Path) -> None   # raises PathEscapeError

`tools/adoption_apply/apply.py:109-125` — the STRUCTURAL pre-check shape to copy (reject an absolute
spelling or a literal `..` path segment BEFORE any `Path.resolve()` or filesystem call):

    segments = destination.replace("\\", "/").split("/")

Do NOT reuse `apply.py`'s `refuse_unsafe_destination` — it also refuses `contracts/**`, and registry
sources legitimately include contract schemas.

`tools/harness_config/loader.py:18,32-39` — the thin-loader idiom: `tomllib.load(fh)` on a binary
handle, explicit-path parameter, missing file degrades rather than raising.

`tools/adoption_apply/approval.py:38` — the in-process validation precedent:
`jsonschema.Draft202012Validator`.

The constitution-plane schema (plan 28-01) constrains SHAPE only — `id` pattern, `severity` enum,
`dispositions` enum membership, `additionalProperties: false`. It CANNOT express: cross-row id
uniqueness, glob-set membership of `target`, the conditional `sources` non-emptiness, path escape,
or the ADR cross-field policy. Those five are this module's job.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — author the DOCSUP-01 rejection table and record its pre-fix failures</name>
  <files>tools/docs_guard/tests/test_registry.py</files>
  <read_first>
    tools/adoption_apply/tests/test_constitution_refusal.py (the `HOSTILE_DESTINATIONS` /
      `PATH_ESCAPE_KINDS` parametrized-table idiom and its sentinel-resolution helper — mirror it,
      do not invent a new one),
    tools/adoption_scan/destinations.py lines 100-115 (`DERIVED_GLOBS`),
    contracts/harness/docs/doc-dependencies.schema.json (plan 28-01's output — the SHAPE half),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    `REJECTION_CASES` — a module-level list of `(case_name, registry_toml_text, expected_reason)`
    rows, GROUPED BY REJECTION CLASS so a reader can see which control each exercises. Every row
    must be shape-VALID against the constitution-plane schema, so that it can only be caught by a
    semantic rule — a row the schema already rejects proves nothing about this module.

    Class A — path escape (refused before any filesystem call):
      - `source_absolute`: a `sources` entry that is an absolute path (synthesized from `tmp_path`,
        never a literal system path).
      - `source_dotdot`: `sources = ["docs/../../etc/passwd"]` — a literal `..` SEGMENT, not a
        substring (`foo/bar..md` must stay valid; include it in the negative-control table).
      - `target_absolute`, `target_dotdot`: the same two spellings on `target`.
      - `source_symlink_escape`: a real symlink inside the tree pointing outside it. Resolve-then-
        confine must refuse it, not follow it.

    Class B — duplicate id:
      - `duplicate_id`: two `[[binding]]` rows with the same `id`. The diagnostic must NAME the
        duplicated id and be deterministically sorted (assert the exact message on a two-duplicate
        registry so ordering is actually pinned, not incidentally stable).

    Class C — empty required selector:
      - `required_empty_sources`: `severity = "required"`, `sources = []` -> reject.
      - Negative control (must stay VALID): `severity = "advisory"`, `sources = []`. This is why the
        schema deliberately omits `minItems` (plan 28-01) — the rule is conditional, and a test that
        rejects both classes would silently make the advisory tier unrepresentable.

    Class D — derived/reference target:
      - One row per `DERIVED_GLOBS` entry that can name a file: `docs/reference/x.md`,
        `.memory/derived/x.md`, `.memory/state/x.md`, `.opencode/x.md`, `.claude/agents/x.md`,
        `.claude/commands/x.md`, `.claude/skills/x.md`, `opencode.json`. Build the rows BY ITERATING
        the imported `DERIVED_GLOBS` so the table cannot silently fall out of date if that list
        grows — a hardcoded copy is exactly the defect this class is meant to prevent.

    Class E — accepted-ADR edit policy:
      - `adr_target_with_updated`: target `docs/adr/0009-*.md`, `dispositions` including `updated`
        -> reject.
      - `adr_target_default_dispositions`: target under `docs/adr/**` with
        `["updated", "reviewed-no-change"]` -> reject.
      - `adr_target_partial`: only `["REVIEWED_STILL_CURRENT"]` -> reject (the pair is FORCED, not a
        subset — D-09).
      - `adr_target_extra`: the correct pair PLUS `updated` -> reject.
      - `adr_target_proposed_status`: a target ADR whose `- **Status:**` line reads `proposed` ->
        reject even with the correct disposition pair (research Q6: a mid-ratification document's
        content is expected to change).
      - Negative control (must stay VALID): an `accepted` ADR target with exactly
        `["REVIEWED_STILL_CURRENT", "SUPERSEDING_ADR_REQUIRED"]`.

    Negative-control table `VALID_CASES` (each must load without error — the guard must not
    over-refuse): a plain `docs/how-to/*.md` target with `["updated", "reviewed-no-change"]`; a
    `sources` entry naming `contracts/**` (legitimately a source — this is the row that proves
    `refuse_unsafe_destination` was NOT reused); a `sources` entry containing `..` as a SUBSTRING
    but not a segment (`libs/python/a..b.py`); an `advisory` row with empty `sources`; a registry
    file that does not exist at all (-> zero bindings, no error).

    Assertions: each rejection row raises `RegistryError` (a `ValueError` subclass) whose message
    contains `expected_reason`; the message NEVER contains file CONTENT (a hostile registry may
    carry arbitrary bytes — mirror the `SymlinkRefusal` no-content-leak precedent); and no
    filesystem read of the escaping path occurred, proven for Class A with an `open`/`Path.read_bytes`
    spy at zero calls on the escaping path.
  </behavior>
  <action>
    Write `tools/docs_guard/tests/test_registry.py` with both tables and their parametrized tests.
    Then create a THROWAWAY `registry.py` that performs SHAPE validation ONLY — `tomllib.load` +
    `Draft202012Validator` against the constitution-plane schema, returning the parsed bindings —
    and run the RED confirmation:

        ! uv run pytest tools/docs_guard/tests/test_registry.py -k rejection -q

    The inverted form exits 0 only when the selection FAILS. Never pipe pytest into `head`/`tail`.

    Confirm each row fails FOR THE STATED REASON — "DID NOT RAISE RegistryError", i.e. the
    shape-only validator ACCEPTED a registry it must refuse — and not because of a collection,
    import, fixture, or schema-not-found error. If any row is already red for a schema reason, the
    row is not shape-valid and must be rewritten: a row the schema catches proves nothing about this
    module's controls.

    Run `uv run pytest tools/docs_guard/tests/test_registry.py -k valid -q` and confirm the
    negative-control table is GREEN even against the shape-only validator — that pins the baseline
    for "did not over-refuse" before any semantic rule lands.

    Paste both verbatim outputs into the SUMMARY under `## RED evidence (DOCSUP-01)`.

    Run ONLY this file's selections. Plan 28-04 is concurrently creating
    `tools/docs_guard/ledger.py` and `tests/test_ledger.py` in the same wave; a failure there is the
    other wave-2 plan in flight — do not modify it and do not treat it as this plan's regression.
  </action>
  <verify>
    <automated>! uv run pytest tools/docs_guard/tests/test_registry.py -k rejection -q</automated>
  </verify>
  <acceptance_criteria>
    Every `REJECTION_CASES` row is RED against the shape-only validator, each failing with "DID NOT
    RAISE `RegistryError`" and not with a collection/import/fixture/schema-not-found error. Every
    `VALID_CASES` row is GREEN against that same validator. The Class D rows were generated by
    ITERATING the imported `DERIVED_GLOBS`, not hardcoded. Both verbatim outputs are captured for
    the SUMMARY.
  </acceptance_criteria>
  <done>The DOCSUP-01 adversarial table and its negative-control table exist, and both have been run against a deliberately incomplete validator with the results recorded — before any semantic rule was written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — the five DOCSUP-01 rejections on top of schema validation</name>
  <files>tools/docs_guard/registry.py</files>
  <read_first>
    tools/harness_config/loader.py lines 15-45 and 145-165 (thin-loader idiom + the deterministic
      duplicate-id diagnostic block to copy verbatim),
    tools/adoption_apply/apply.py lines 88-130 (`refuse_if_outside_root` and the structural
      pre-check shape),
    tools/adoption_scan/destinations.py lines 25-55 and 100-115 (the never-retype rule and
      `DERIVED_GLOBS`),
    Task 1's `REJECTION_CASES` / `VALID_CASES`,
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    Every `REJECTION_CASES` row raises `RegistryError` with its stated reason. Every `VALID_CASES`
    row loads cleanly. A missing registry file returns an empty binding list without raising.
    `load_registry` is pure: it performs read-only filesystem access and writes nothing.
  </behavior>
  <action>
    Replace the throwaway with the real `registry.py`, in this ORDER (order is load-bearing —
    a structural check must precede any filesystem call, exactly as in `apply.py:109-125`):

      1. `load_registry(path=DEFAULT_REGISTRY_PATH, root=REPO_ROOT)`. If `path` does not exist,
         return `[]` — missing is not invalid (only invalid maps to exit 3 in plan 28-05). Parse
         with `tomllib.load` on a binary handle (loader.py:18,38). A `tomllib.TOMLDecodeError`
         becomes `RegistryError`, never a raw traceback.
      2. SHAPE: validate the parsed dict with `jsonschema.Draft202012Validator` against
         `contracts/harness/docs/doc-dependencies.schema.json`. Collect `iter_errors()` and emit ALL
         of them, sorted by `json_path`, in one `RegistryError` — not just the first. Locate the
         schema by repo-relative path resolved from `__file__`, never by CWD.
      3. STRUCTURAL path escape, before any `Path.resolve()` or filesystem call, over every
         `sources[i]` and `target`: split on `/` after normalizing `\\`, reject an absolute spelling
         or any literal `..` SEGMENT (never a substring — `a..b.md` is legal). Copy the
         `apply.py:109-125` shape; do not invent a sixth `_confine` spelling.
      4. RESOLVE-THEN-CONFINE backstop: `refuse_if_outside_root(resolved, root)` per path, which
         also catches the symlink-escape row. Reuse `apply.refuse_if_outside_root` — the NARROW
         helper — and add a comment stating why `refuse_unsafe_destination` is deliberately NOT used
         (it refuses `contracts/**`, which are legitimate registry SOURCES).
      5. DUPLICATE ID: copy `loader.py:149-160`'s sorted-diagnostic block verbatim in shape.
      6. EMPTY REQUIRED SELECTOR: `severity == "required" and not sources` -> reject. An `advisory`
         row with empty `sources` stays valid.
      7. DERIVED/REFERENCE TARGET: import `DERIVED_GLOBS` from `tools.adoption_scan.destinations`
         and match `target` against it using the repo's existing glob resolver
         (`tools.harness_perms.resolve_path`, the same one `contract_guard._on_constitution_plane`
         uses) rather than a new matcher. Never retype the glob list.
      8. ADR POLICY (D-09): if `target` matches `docs/adr/**` (via the imported
         `CONSTITUTION_GLOBS`-derived test, not a retyped string), then `dispositions` must equal
         exactly `{"REVIEWED_STILL_CURRENT", "SUPERSEDING_ADR_REQUIRED"}` — the pair is FORCED, so
         a subset, a superset, and `updated` in any position all reject. Additionally parse the
         target ADR's `- **Status:**` line; a `proposed` (or absent) status rejects, because a
         mid-ratification document's content is expected to change. A NON-ADR target must NOT be
         allowed to declare the two upper-case ADR dispositions — reject that too, so the vocabulary
         stays structurally partitioned for Phase 29's `/docs-update` filter.

    Return a sorted-by-`id` list of binding records (a `NamedTuple` or frozen dataclass, not a raw
    dict, so downstream plans get attribute access and a stable field set). Every diagnostic list is
    `sorted()` before it reaches a message.

    Two comments are mandatory:
      - At the module docstring: this module is the registry's ONLY validator, because
        `/contract-check` step 1 pairs a schema with a sibling `.yaml`/`.yml`/`.json` instance and
        the registry is `.toml` in `docs/` — CI silently skips it (D-16/A7). Nobody should assume
        double validation.
      - At the `refuse_if_outside_root` call: why `refuse_unsafe_destination` is not reused.

    `load_registry` NEVER writes. Add no CLI here — plan 28-05 owns `cli.py`.
  </action>
  <verify>
    <automated>uv run pytest tools/docs_guard/tests/test_registry.py tools/docs_guard/tests/test_digest.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/docs_guard/tests/test_registry.py -q` exits 0 — every rejection row now
    raises and every negative-control row still loads (the guard did not over-refuse). Plan 28-02's
    `test_digest.py` still passes unchanged. `grep -n 'DERIVED_GLOBS\s*=' tools/docs_guard/registry.py`
    returns nothing (imported, not retyped) and `grep -n 'from tools.adoption_scan.destinations
    import' tools/docs_guard/registry.py` matches. `grep -n 'refuse_unsafe_destination'
    tools/docs_guard/registry.py` appears only inside the explanatory comment. `git status
    --porcelain tools/docs_guard/ledger.py` is empty — plan 28-04 owns that file in this same wave.
    Do NOT run the full suite in flight.
  </acceptance_criteria>
  <done>`registry.py` validates shape against the constitution-plane schema and enforces all five DOCSUP-01 rejections in an order that refuses path escape before any filesystem call, with `DERIVED_GLOBS` imported and the non-reuse of `refuse_unsafe_destination` documented in code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/doc-dependencies.toml` → `load_registry` | Untrusted: hand-edited today, agent-PROPOSED from Phase 29 (DOCSUP-07). Every selector and target is attacker-influenceable input. |
| Registry selector → filesystem | The structural pre-check is the primary control; resolve-then-confine is the backstop. Neither may follow an escaping symlink. |
| Registry `dispositions` → the report's instruction to a human | A registry that could encode `updated` for an accepted ADR would make the gate teach the wrong action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-10 | Tampering / Information Disclosure | a registry selector escaping the repo (`../../etc/...`, absolute, symlink) | mitigate | Structural absolute/`..`-segment pre-check BEFORE any filesystem call (apply.py:109-125 shape), then `refuse_if_outside_root` as the resolve-level backstop; proven by Class A rows with a read-spy at zero calls. |
| T-28-11 | Tampering | the report instructing a human to edit an accepted ADR | mitigate | D-09: `docs/adr/**` targets are FORCED to exactly `{REVIEWED_STILL_CURRENT, SUPERSEDING_ADR_REQUIRED}`; subset, superset, and `updated` all reject, and a `proposed`-status ADR cannot be a target at all. |
| T-28-12 | Spoofing (false assurance) | a binding targeting a DERIVED file, making a machine-regenerated artifact look human-reviewed | mitigate | `target` matched against the IMPORTED `DERIVED_GLOBS`; the test table is generated by iterating that list so it cannot fall out of date. |
| T-28-13 | Repudiation | duplicate binding ids making the ledger ambiguous about which row was reviewed | mitigate | Duplicate-id rejection using `loader.py:149-160`'s deterministic sorted diagnostic. |
| T-28-14 | Denial of Service | a malformed TOML or a hostile registry crashing the gate with a traceback | mitigate | `tomllib.TOMLDecodeError` and every validation failure become `RegistryError`; the message names the offending id/path only, never file content. |
| T-28-15 | Elevation of Privilege | a registry that "validates itself" by bypassing the constitution-plane schema | accept | The schema is hash-gated (plan 28-01) and loaded by repo-relative path resolved from `__file__`, never by CWD. Tampering with the schema is a constitution-plane write, already gated by `contract_guard` + `drift`. |
| T-28-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new external packages — `tomllib` (stdlib) and the already-pinned `jsonschema==4.26.0`. |
</threat_model>

<verification>
- `uv run pytest tools/docs_guard/tests/test_registry.py tools/docs_guard/tests/test_digest.py -q` exits 0.
- `git status --porcelain tools/docs_guard/ledger.py tools/docs_guard/tests/test_ledger.py contracts` is empty.

Do NOT run the full suite in flight: plan 28-04 is concurrently adding `ledger.py` in this wave.
</verification>

<success_criteria>
- SC-1 (ROADMAP): registry validation rejects path escape, duplicate id, empty required selector,
  derived/reference target, and the accepted-ADR edit policy — proven per-class, not by one smoke test.
- The adversarial table was RED against a shape-only validator FIRST, with failure messages recorded.
- `DERIVED_GLOBS` and the constitution-plane globs are imported, never retyped.
- Nothing legitimate is over-refused: the `VALID_CASES` table (including a `contracts/**` SOURCE and
  an `advisory` row with empty `sources`) stays green.
</success_criteria>

<output>
Create `.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-03-SUMMARY.md` when
done, with a `## RED evidence (DOCSUP-01)` section carrying the verbatim pre-fix output for both the
rejection and the negative-control selections.
</output>
