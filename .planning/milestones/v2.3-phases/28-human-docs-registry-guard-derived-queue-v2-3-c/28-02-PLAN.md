---
phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/docs_guard/__init__.py
  - tools/docs_guard/__main__.py
  - tools/docs_guard/pyproject.toml
  - tools/docs_guard/digest.py
  - tools/docs_guard/tests/__init__.py
  - tools/docs_guard/tests/conftest.py
  - tools/docs_guard/tests/test_digest.py
  - uv.lock
autonomous: true
requirements: [DOCSUP-02]

must_haves:
  truths:
    - "`tools/docs_guard/` is a uv workspace member (`package = false`, zero declared dependencies) picked up by the existing `members = [\"libs/python\", \"tools/*\"]` glob — the root `pyproject.toml` needs NO edit (D-17)"
    - "`tools/docs_guard/__init__.py` declares the COMPLETE public surface for the whole phase up front (interface-first), so no later plan in this phase has to edit it — plans 28-03/04/05 add submodules only"
    - "`digest.compute(paths)` interleaves each POSIX path AND that file's own hex digest, deliberately diverging from `approval.py:57-63`'s raw-byte concatenation, and a code comment records WHY so a reviewer does not 'fix' it back toward the precedent (D-03)"
    - "The digest is order-INSENSITIVE to selector spelling (the resolved set is sorted) and order-SENSITIVE to a byte moving between two files and to the addition of an empty file — the two ambiguities raw concatenation cannot see"
    - "No §4.3-4.6 normalization runs before hashing: the digest must agree with what a human sees in `git diff` (D-03 tradeoff, recorded in the module docstring)"
    - "A resolved path that does not exist is NEVER hashed as empty — `compute` raises a typed `MissingSourceError` so the caller can classify it `BROKEN` (research Q3)"
    - "A hermetic `git init` tmp-repo fixture exists in `tools/docs_guard/tests/conftest.py` so plan 28-04's git-history path is EXERCISED, not mocked"
  artifacts:
    - path: "tools/docs_guard/pyproject.toml"
      provides: "virtual uv member declaration mirroring tools/adoption_scan/pyproject.toml — package = false, dependencies = []"
      contains: "package = false"
    - path: "tools/docs_guard/__init__.py"
      provides: "PEP 562 lazy re-export declaring the phase's complete public API surface; submodules land across plans 28-02..28-05"
      contains: "__getattr__"
    - path: "tools/docs_guard/digest.py"
      provides: "`resolve(selectors, root)` -> sorted unique paths, `compute(paths)` -> 64-hex digest, `MissingSourceError`"
      contains: "MissingSourceError"
    - path: "tools/docs_guard/tests/conftest.py"
      provides: "`docs_repo` fixture — a real `git init` tmp tree with an initial commit, reusable by every docs_guard test module"
      contains: "git"
    - path: "tools/docs_guard/tests/test_digest.py"
      provides: "determinism, selector-spelling insensitivity, byte-move sensitivity, empty-file sensitivity, and missing-path refusal cases"
      contains: "byte_move"
  key_links:
    - from: "tools/docs_guard/digest.py::compute"
      to: "tools/docs_guard/guard.py"
      via: "single digest implementation imported by both the guard and the queue renderer — never a second impl (contracts_index.py:4-8 standing rule)"
      pattern: "from tools.docs_guard.digest import"
    - from: "tools/docs_guard/pyproject.toml"
      to: "uv.lock"
      via: "`uv sync --all-packages` registers the new virtual member"
      pattern: "logparser-docs-guard"
---

<objective>
Stand up `tools/docs_guard/` as the phase's single new uv member and land its foundational,
dependency-free module: the digest.

Purpose: the digest is the one genuinely new algorithm in Phase 28 (research "Key insight": the
phase's real work is composition plus three new decisions, and this is the first). Every later
plan — registry, ledger, guard, queue — consumes it, so it lands first and alone, with the
divergence from the `approval.py` precedent recorded in code. This plan also authors the package's
COMPLETE public surface up front so no later plan in the phase needs to edit `__init__.py`, which
would force a same-wave file conflict.

Output: a working uv member, a `__init__.py` interface contract for the whole phase, `digest.py`,
and a hermetic real-git test fixture that plan 28-04 will reuse.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-CONTEXT.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-RESEARCH.md
@tools/adoption_scan/pyproject.toml
@tools/harness_config/__init__.py
@tools/adoption_apply/approval.py

<interfaces>
<!-- Verified against source in this session. -->

`tools/adoption_scan/pyproject.toml` — the exact virtual-member shape to mirror:

    [project]
    name = "logparser-adoption-scan"
    version = "0.0.0"
    description = "..."
    requires-python = ">=3.11"
    dependencies = []

    [tool.uv]
    package = false

Root `pyproject.toml:33` already globs `members = ["libs/python", "tools/*"]` with
`exclude = ["tools/bootstrap"]`, so a new `tools/docs_guard/pyproject.toml` is picked up with no
root edit (D-17). `uv.lock` gains a `virtual` source entry, exactly as
`source = { virtual = "tools/adoption_scan" }` at uv.lock:254.

`tools/harness_config/__init__.py` — the PEP 562 lazy re-export idiom to mirror verbatim
(`tools` is a namespace package; an eager top-level import breaks pytest conftest bootstrap):

    __all__ = [...]
    def __getattr__(name: str):
        if name in __all__:
            from tools.harness_config import loader
            return getattr(loader, name)
        raise AttributeError(f"module {__name__!r} has no attribute {name!r}")

`tools/adoption_apply/approval.py:47,57-63` — the precedent this plan DELIBERATELY diverges from:

    _DRAFT_FILES = (... fixed 3-element tuple ...)

    def _recompute_draft_hash(batch_dir):
        h = hashlib.sha256()
        for name in _DRAFT_FILES:
            h.update((batch_dir / name).read_bytes())   # raw bytes, no separator, no path
        return h.hexdigest()

Safe only because the file list is fixed and 3 elements long. A registry selector expands to a
VARIABLE set, where this is ambiguous.

Repo conventions this module must follow: no `datetime`/`time` anywhere in the docs_guard package
(the phase-wide determinism rule); sort everything (`set` iteration must never leak into output);
`Path.glob` order is not guaranteed, so always wrap in `sorted()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the uv member and author the phase's complete public interface contract</name>
  <files>
    tools/docs_guard/pyproject.toml,
    tools/docs_guard/__init__.py,
    tools/docs_guard/__main__.py,
    tools/docs_guard/tests/__init__.py,
    tools/docs_guard/tests/conftest.py,
    uv.lock
  </files>
  <read_first>
    tools/adoption_scan/pyproject.toml (full),
    tools/harness_config/__init__.py (full — the PEP 562 idiom),
    tools/adoption_scan/__main__.py (the `python -m` entry convention),
    tools/harness_lint/tests/test_ci_stale_derived.py (the real-`git init` tmp-repo negative-control
      idiom this task's fixture copies)
  </read_first>
  <action>
    Create `tools/docs_guard/pyproject.toml` mirroring `tools/adoption_scan/pyproject.toml`
    field-for-field: `name = "logparser-docs-guard"`, `version = "0.0.0"`, `requires-python =
    ">=3.11"`, `dependencies = []`, `[tool.uv] package = false`. The `description` states the
    DOCSUP-01..05 scope and that the package declares zero external dependencies — `jsonschema` and
    `pytest` come from the workspace root runtime/dev groups, so `uv sync --all-packages` must not
    resolve anything new. Do NOT edit the root `pyproject.toml` (D-17).

    Create `tools/docs_guard/__init__.py` using `tools/harness_config/__init__.py`'s PEP 562 lazy
    re-export verbatim in shape. This is the INTERFACE-FIRST step: declare the package's COMPLETE
    public surface for the whole phase now, mapping each exported name to the submodule that will
    hold it, so plans 28-03/04/05 add files only and never edit this one (which would be a same-wave
    conflict). The map:

      digest.py    (this plan)  -> compute, resolve, MissingSourceError
      registry.py  (plan 28-03) -> load_registry, RegistryError
      ledger.py    (plan 28-04) -> load_ledger, previous_ledger, check_coherence, LedgerError
      guard.py     (plan 28-05) -> classify, STATES
      impact.py    (plan 28-05) -> impact_ids

    Implement `__getattr__` as a name -> submodule dict lookup, then `importlib`-free
    `from tools.docs_guard import <submodule>` inside the function body (mirroring harness_config).
    An unknown name raises `AttributeError`. Add a module docstring recording that a name whose
    submodule has not landed yet raises `ModuleNotFoundError` on access — a transient, within-phase
    condition, not a bug — and that the surface is frozen here precisely to keep the wave structure
    conflict-free.

    Create `tools/docs_guard/__main__.py` as a thin `sys.exit(cli.main())` delegate, following
    `tools/adoption_scan/__main__.py`'s convention. `cli.py` lands in plan 28-05, so import it
    lazily INSIDE `main()` (not at module top) so `python -m tools.docs_guard` fails with a clear
    message rather than the package being unimportable this wave.

    Create `tools/docs_guard/tests/__init__.py` (empty, matching the sibling packages' test-package
    convention) and `tools/docs_guard/tests/conftest.py` with a `docs_repo` fixture:
    `tmp_path`-rooted, `git init` + `git -c user.email=... -c user.name=... commit` of a small
    seed tree, returning the repo root `Path`. Use fixed argv `subprocess.run([...], shell=False)`
    and configure identity via `-c` flags on each invocation so no global git config is read or
    written. The seed tree must contain a `docs/` subtree and at least two source files, so plan
    28-04 can commit a ledger, mutate it, and exercise `git show HEAD:./<path>` for real rather
    than mocking it. If the `git` binary is unavailable, the fixture `pytest.skip`s with an explicit
    reason — matching the repo's degrade-honestly posture, never a silent pass.

    Then run `uv sync --all-packages` and confirm `uv.lock` gains exactly one new virtual member
    entry for `logparser-docs-guard` and no other package's resolution moved.
  </action>
  <verify>
    <automated>uv sync --all-packages && uv run python -c "import tools.docs_guard as d; print(sorted(d.__all__))"</automated>
  </verify>
  <acceptance_criteria>
    `uv sync --all-packages` succeeds. `git diff uv.lock` shows exactly one added member (the
    `logparser-docs-guard` virtual entry plus its members-list line) and no changed version pin for
    any existing package. `uv run python -c "import tools.docs_guard"` succeeds. `git status
    --porcelain pyproject.toml` is empty (root pyproject unchanged, D-17). `git status --porcelain
    contracts docs/reference .memory/derived` is empty — plan 28-01 owns those in this same wave, and
    a change there is the other wave-1 plan in flight, not this plan's regression.
  </acceptance_criteria>
  <done>`tools/docs_guard/` is a registered uv member with a frozen, phase-complete public interface and a real-git hermetic test fixture; the root pyproject is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED then GREEN — the path+per-file-digest algorithm and its ambiguity table</name>
  <files>tools/docs_guard/digest.py, tools/docs_guard/tests/test_digest.py</files>
  <read_first>
    tools/adoption_apply/approval.py lines 40-70 (`_DRAFT_FILES` + `_recompute_draft_hash` — the
      precedent this module diverges from and must cite),
    tools/contract_hash/hash.py lines 36-66 (the symlink defense to mirror),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    Author `AMBIGUITY_CASES` — the adversarial table that proves the divergence from
    `approval.py:57-63` is load-bearing — BEFORE writing `digest.py`. Each row is a pair of file
    trees that raw-byte concatenation would hash IDENTICALLY, and that this implementation must
    distinguish:

      - `byte_move`: tree A = {"a.md": b"xy", "b.md": b"z"}; tree B = {"a.md": b"x", "b.md": b"yz"}.
        Same concatenated bytes, different digest required.
      - `empty_file_added`: tree A = {"a.md": b"x"}; tree B = {"a.md": b"x", "b.md": b""}.
        Same concatenated bytes, different digest required.
      - `rename_only`: tree A = {"a.md": b"x"}; tree B = {"b.md": b"x"}. Same bytes, different
        digest required — the path is part of the hash input.
      - `split_across_files`: tree A = {"a.md": b"", "b.md": b"xy"}; tree B = {"a.md": b"xy",
        "b.md": b""}. Same concatenation, different digest required.

    Plus the properties that must HOLD (the negative controls — a digest that distinguishes
    everything is as useless as one that distinguishes nothing):

      - `test_digest_is_deterministic`: two `compute()` calls over the same tree are equal.
      - `test_digest_ignores_selector_spelling`: `resolve(["docs/**"], root)` and
        `resolve(["docs/a.md", "docs/**"], root)` produce the same sorted unique path list, hence the
        same digest — selector spelling and ordering must not change the result.
      - `test_digest_ignores_glob_iteration_order`: `compute()` over a shuffled input path list
        equals `compute()` over the sorted one (proving the sort is inside `compute`, not left to
        the caller).
      - `test_digest_no_normalization_applied`: a file whose bytes contain `\r\n` hashes DIFFERENTLY
        from the same file with `\n` — proving no §4.3-4.6 normalization runs before hashing (D-03).
      - `test_compute_raises_on_missing_path`: a resolved path that does not exist raises
        `MissingSourceError`, and the message names the path. It must NEVER hash as empty — that is
        what lets the guard classify `BROKEN` instead of silently reporting `FRESH` (research Q3).
      - `test_resolve_refuses_escape`: a selector resolving outside `root` raises — resolve-then-
        confine, mirroring `hash.py:60-63`'s symlink defense. A symlink inside the tree pointing
        outside it is refused, not followed.

    RED confirmation is required and must be recorded: run the new selection against a
    `digest.py` that implements the PRECEDENT algorithm (raw-byte concatenation, no path, no
    separator) and confirm every `AMBIGUITY_CASES` row FAILS for the stated reason — "digests are
    equal but must differ". Record the verbatim failure output. This is the phase's anti-pattern
    fence: a control tested only against the spelling it already handles is not coverage
    (research P5, ROADMAP.md:670-673).
  </behavior>
  <action>
    Write `tools/docs_guard/tests/test_digest.py` FIRST with the full `AMBIGUITY_CASES` table and
    every property test above. Then write a THROWAWAY precedent-shaped `digest.py` whose `compute`
    is `approval.py:57-63` verbatim (raw `h.update(path.read_bytes())` in a loop, no path, no
    separator) and run:

        ! uv run pytest tools/docs_guard/tests/test_digest.py -k ambiguity -q

    The inverted form exits 0 only when the selection FAILS. Never pipe pytest into `head`/`tail` —
    a pipeline reports the LAST command's status, which makes the gate unfailable. Confirm each row
    fails because the two digests are EQUAL, not because of a collection, import, or fixture error.
    Paste the verbatim output into the SUMMARY under `## RED evidence`.

    Then replace `compute` with the real algorithm:

      resolved = sorted(unique(expand(selector) for selector in selectors), key=posix path)
      h = sha256()
      for path in resolved:
          h.update(path.as_posix().encode("utf-8")); h.update(b"\n")
          h.update(sha256(path.read_bytes()).hexdigest().encode("ascii")); h.update(b"\n")

    Split the module into `resolve(selectors, root) -> list[Path]` (glob expansion, dedupe,
    resolve-then-confine, sorted by POSIX path — the sort lives HERE and in `compute`, never left to
    a caller) and `compute(paths) -> str` (the loop above; it re-sorts defensively so a caller
    passing an unsorted list still gets the canonical digest). `MissingSourceError` is a
    `ValueError` subclass raised by `compute` when a path does not exist.

    Two comments are mandatory and are the point of this task:

      1. Above the loop, state that this DELIBERATELY diverges from
         `tools/adoption_apply/approval.py:57-63`, that the precedent's raw concatenation is safe
         ONLY for its fixed 3-element `_DRAFT_FILES` tuple, and that on a variable selector-expanded
         set it makes a byte moving between two files and the addition of an empty file invisible.
         Name `AMBIGUITY_CASES` in `tests/test_digest.py` as the proof. Written so a reviewer does
         not "fix" it back toward the precedent (D-03).
      2. Above `compute`, state that NO §4.3-4.6 normalization runs before hashing, and why: the
         digest must agree with what a human sees in `git diff`, `format-on-write` + `polyglot_lint`
         already keep the tree LF/no-BOM so a CRLF-only re-save should not be silently absorbed, and
         normalizing would make the digest disagree with the raw-byte model the ledger's reviewer
         used.

    Use `hashlib` and `pathlib` only. No `datetime`, no `time`, no `float`, no `set` iteration
    reaching output.
  </action>
  <verify>
    <automated>uv run pytest tools/docs_guard/tests/test_digest.py -q</automated>
  </verify>
  <acceptance_criteria>
    Every `AMBIGUITY_CASES` row was RED against the precedent-shaped implementation, failing with
    "digests are equal but must differ" (verbatim output recorded in the SUMMARY), and is GREEN
    against the real one. `uv run pytest tools/docs_guard -q` exits 0. Every property/negative-
    control test passes — in particular `test_digest_ignores_selector_spelling` and
    `test_digest_ignores_glob_iteration_order`, which prove the implementation did not simply become
    sensitive to everything. `grep -n 'datetime\|time\.\|float(' tools/docs_guard/digest.py` returns
    nothing. Both mandatory comments are present in `digest.py`. Scope every run to
    `tools/docs_guard` — plan 28-01 is concurrently staging a constitution-plane commit in the same
    wave.
  </acceptance_criteria>
  <done>`digest.py` implements the interleaved path+per-file-digest algorithm with its divergence and no-normalization rationale recorded in code, proven by an adversarial table that was RED against the precedent algorithm first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry-controlled selector string → `digest.resolve` | Untrusted: a selector may be hand-edited or, from Phase 29, agent-proposed. It must never expand outside the repo root. |
| Working-tree bytes → `digest.compute` | Read-only. Content is hashed, never executed, never echoed into a message. |
| New uv member → `uv.lock` | A member addition must not silently move any existing package's resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06 | Tampering | `digest.compute` ambiguity — a byte moved between files, or an empty file added, producing an unchanged digest | mitigate | Interleave the POSIX path and the per-file hex digest with explicit `\n` separators; proven by the `AMBIGUITY_CASES` table, RED-confirmed against the precedent algorithm first. |
| T-28-07 | Information Disclosure | a selector or symlink resolving outside the repo root | mitigate | `resolve()` resolves-then-confines against `root` and refuses an escaping path, mirroring the `hash.py:60-63` symlink defense; refused before any `read_bytes()`. |
| T-28-08 | Spoofing (false assurance) | a missing source file hashed as empty, making a `BROKEN` binding look `FRESH` | mitigate | `compute` raises `MissingSourceError`; never hashes `b""` for an absent path (research Q3). |
| T-28-09 | Tampering | `uv.lock` resolution drift riding on a member addition | mitigate | `dependencies = []` + `package = false`; acceptance criteria require `git diff uv.lock` to show only the new virtual entry. |
| T-28-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new external packages — stdlib `hashlib`/`pathlib`/`subprocess` only. 28-RESEARCH.md records the Package Legitimacy Gate as not applicable to this phase. |
</threat_model>

<verification>
- `uv sync --all-packages` succeeds; `git diff uv.lock` shows only the new virtual member.
- `uv run pytest tools/docs_guard -q` exits 0.
- `git status --porcelain pyproject.toml contracts docs/reference .memory/derived` is empty.

Do NOT run the full suite as an in-flight gate — plan 28-01 is staging a constitution-plane commit
in this same wave. The fan-in belongs to plan 28-08.
</verification>

<success_criteria>
- `tools/docs_guard` is a working uv member with a frozen, phase-complete `__init__.py` surface.
- `digest.compute` distinguishes every `AMBIGUITY_CASES` row that raw concatenation cannot, and is
  invariant to selector spelling and input ordering.
- The divergence from `approval.py:57-63` and the no-normalization decision are both recorded as
  code comments, not only in the plan.
- RED evidence for the ambiguity table is recorded verbatim in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-02-SUMMARY.md` when
done, with a `## RED evidence` section containing the verbatim pre-fix failure output for the
`AMBIGUITY_CASES` table.
</output>
