---
phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
plan: 06
type: execute
wave: 4
depends_on: ["28-05"]
files_modified:
  - tools/memory_regen/docs_staleness.py
  - tools/memory_regen/inject.py
  - tools/memory_regen/tests/test_docs_staleness.py
  - tools/memory_regen/tests/__snapshots__/test_docs_staleness.ambr
  - tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr
  - harness/commands/refresh-memory.md
  - .opencode/command/refresh-memory.md
  - .claude/commands/refresh-memory.md
autonomous: true
requirements: [DOCSUP-04, DOCSUP-05]

must_haves:
  truths:
    - "SC-4: the queue regenerates byte-identically — proven by a generate-twice SHA-256 equality test AND a committed syrupy `.ambr` over a hermetic fixture, because the target is GITIGNORED and `git diff` cannot prove it (contracts_index.py:13-14 / research Pitfall 2)"
    - "SC-4: `assemble()` stays byte-identical and the ~4000-char budget test stays green; the ZERO-ITEM case is BYTE-IDENTICAL to today's payload, because an empty section string is skipped at inject.py:181"
    - "D-11: the pointer is EXACTLY ONE LINE by construction — a header plus a single `f\"{n} human doc(s) need review — see .memory/derived/docs-staleness.md\"`. `_read_head` is NOT used: it returns 20 lines (`_HEAD_LINES = 20`, inject.py:24) and would break the one-line rule"
    - "D-11: the never-drop tuple at `inject.py:184` — `(\"agreements\", \"banner\", \"drift\", \"task\")` — is NEVER widened. The docs pointer is the least important section in the payload and must be droppable"
    - "D-11: the new reader takes `derived_dir` as a PARAMETER, mirroring `_contracts_summary(derived_dir=...)` at inject.py:63. A module-constant read would bypass the fixture dirs and pin live repo state into the committed snapshot (research Pitfall 3)"
    - "D-11: the pointer READS the rendered derived file; it never recomputes the guard. Recomputing would put a `git` subprocess and a full doc-corpus walk on the session-start hot path and make the payload depend on live state the tests cannot fixture"
    - "P4: no `datetime`/`time` token — even an UNUSED import — enters `inject.py`. `test_inject_module_has_no_wallclock` is a static TEXT scan (test_inject_determinism.py:70-85) with a live negative control"
    - "D-10: the queue lives at `tools/memory_regen/docs_staleness.py`, not in `tools/docs_guard/`, because `test_derived_freshness.py:32` pins `_ALLOWED_TOOL_MODULES = {memory_regen, docs_sync}` for the curator / `/refresh-memory` surface. That gate scans those markdown files' TEXT, so importing `tools.docs_guard` from the generator is fine; NAMING it in `refresh-memory.md` is not"
    - "D-10: the queue stays GITIGNORED — `.gitignore:23` (`.memory/derived/*`) already covers it, ZERO .gitignore change — and it does NOT join the `stale-derived` job's path list (ci.yml:236-237), or every ordinary source commit would red CI"
    - "The default budget constant `budget_chars: int = 4000` (inject.py:158) is NOT changed"
  artifacts:
    - path: "tools/memory_regen/docs_staleness.py"
      provides: "the DERIVED generator quartet — `rows()` / `render()` / `write()` / `main()` — writing `.memory/derived/docs-staleness.md`; pointer-only rows, no prose excerpt, no diff body"
      contains: "DERIVED — do not hand-edit (tools/memory_regen/docs_staleness.py)"
    - path: "tools/memory_regen/inject.py"
      provides: "`_docs_staleness_pointer(derived_dir=DERIVED_DIR)` plus ONE new droppable `(\"docs\", ...)` section placed after `(\"contracts\", ...)` and before `(\"repomap\", ...)`"
      contains: "_docs_staleness_pointer"
    - path: "tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr"
      provides: "regenerated payload snapshot; must not contain any tmp_path"
    - path: "harness/commands/refresh-memory.md"
      provides: "+1 invocation of `tools.memory_regen.docs_staleness` in step 1 (the gitignored, session-derived half — NOT step 2's committed-derived set)"
      contains: "docs_staleness"
  key_links:
    - from: "tools/memory_regen/docs_staleness.py"
      to: "tools/docs_guard/guard.py::classify"
      via: "import — one digest/classification implementation, never a second (contracts_index.py:4-8 standing rule)"
      pattern: "from tools.docs_guard"
    - from: "tools/memory_regen/inject.py::_docs_staleness_pointer"
      to: ".memory/derived/docs-staleness.md"
      via: "reads the RENDERED derived file under the derived_dir PARAMETER; never recomputes the guard"
      pattern: "derived_dir"
    - from: "harness/commands/refresh-memory.md"
      to: ".opencode/command/refresh-memory.md and .claude/commands/refresh-memory.md"
      via: "tools.harness_emit round-trip — required or the emit-drift CI job (ci.yml:203) fails"
      pattern: "docs_staleness"
---

<objective>
Implement DOCSUP-04: the gitignored derived staleness queue and the conditional one-line SessionStart
pointer.

Purpose: this is the highest-risk plan in the phase. The injector carries a byte-identity guarantee,
a static wall-clock scan, a ~4000-char budget, and a committed snapshot — 28-RESEARCH.md names the
SIX exact tests this change can break with path:line, and this plan runs them by name rather than
leaning on the full suite.

Output: `docs_staleness.py`, one droppable conditional section in `inject.py`, the `/refresh-memory`
invocation, and the emitter round-trip.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-CONTEXT.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-RESEARCH.md
@tools/memory_regen/contracts_index.py
@tools/memory_regen/inject.py
@tools/memory_regen/tests/test_inject_determinism.py
@tools/memory_regen/tests/test_inject_assembler.py
@harness/commands/refresh-memory.md
@tools/docs_guard/guard.py

<interfaces>
<!-- Verified against source in this session. Do not re-derive. -->

`tools/memory_regen/inject.py:157-188` — `assemble()` verbatim, the seam this plan edits:

    def assemble(budget_chars: int = 4000, derived_dir: Path = DERIVED_DIR,
                 state_dir: Path = STATE_DIR, agreements_dir: Path = AGREEMENTS_DIR) -> str:
        task = _active_task_pointer(state_dir)
        sections = [
            ("agreements", _agreements_block(agreements_dir)),
            ("banner", BANNER),
            ("drift", _drift_summary()),
            ("task", task),
            ("contracts", _contracts_summary(derived_dir)),
            ("repomap", _repo_map_topN(derived_dir)),
            ("active", _active_context_pointer(state_dir)),
        ]
        out: list[str] = []; used = 0
        for name, text in sections:
            if not text:                                       # <-- 181: empty text is SKIPPED
                continue
            addition = len(text) + (1 if out else 0)
            if name not in ("agreements", "banner", "drift", "task") and used + addition > budget_chars:
                continue                                       # <-- 184: the never-drop tuple
            out.append(text); used += addition
        return "\n".join(out)

`inject.py:63-72` — the parameterized-reader pattern the new pointer must mirror EXACTLY:

    def _contracts_summary(derived_dir: Path = DERIVED_DIR) -> str:
        head = _read_head(Path(derived_dir) / "contracts-index.md")
        ...

`inject.py:24` — `_HEAD_LINES = 20`. `_read_head` returns TWENTY lines. Do NOT use it for the
pointer.

`tools/memory_regen/contracts_index.py:33,51,83,105,117` — the DERIVED generator quartet to clone:

    DERIVED_HEADER = "DERIVED — do not hand-edit (tools/memory_regen/contracts_index.py)"
    def index_rows(...) -> list[tuple[...]]      # sorted, content-derived only
    def render(rows) -> str                      # header + stable table + trailing "\n"
    def write(path=...) -> Path                  # mkdir parents, write_text(encoding="utf-8")
    def main(argv=None) -> int                   # `python -m ...`

`contracts_index.py:11-14` bans `datetime` and raw `float` in the rendered output and records that
the determinism proof is a snapshot, not `git diff`, because the target is gitignored.

**The six tests this change can break — run these BY NAME:**

| Test | Path:line | What breaks it |
|---|---|---|
| `test_default_payload_within_budget` | `tests/test_inject_assembler.py:32-33` | Runs `assemble()` against the REAL tree with the 4000 default; a multi-line queue block blows it |
| `test_budget_holds_with_full_agreements_block` | `tests/test_inject_assembler.py:152-157` | Widening the never-drop tuple |
| `test_assemble_is_byte_identical` | `tests/test_inject_determinism.py:29-41` | Any live/nondeterministic read |
| `test_assemble_delete_regenerate_is_byte_identical` | `tests/test_inject_determinism.py:44-54` | Same |
| `test_payload_matches_snapshot` | `tests/test_inject_determinism.py:57-67` | Snapshot must be regenerated; also fails if the payload embeds `tmp_path` (it asserts `str(tmp_path) not in payload`) |
| `test_inject_module_has_no_wallclock` | `tests/test_inject_determinism.py:77-85` | A `datetime`/`time` token in the file TEXT — even an unused import |

`.gitignore:23-24` — already covers the queue; ZERO change:

    .memory/derived/*
    !.memory/derived/contracts-index.md

`harness/commands/refresh-memory.md` — step 1 is the session-derived GITIGNORED half
(`repo_map`, `pointer_index`); step 2 is the committed-derived gated set (`docs_sync`,
`contracts_index`). The queue belongs in STEP 1.

`tools/harness_lint/tests/test_derived_freshness.py:29-36` — `_ALLOWED_TOOL_MODULES =
frozenset({"memory_regen", "docs_sync"})`, applied by regex `tools\.([a-z0-9_]+)` over the TEXT of
`harness/agents/curator.md` and `harness/commands/refresh-memory.md`. So `refresh-memory.md` may say
`tools.memory_regen.docs_staleness` and must NOT say `tools.docs_guard`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The derived queue generator</name>
  <files>
    tools/memory_regen/docs_staleness.py,
    tools/memory_regen/tests/test_docs_staleness.py,
    tools/memory_regen/tests/__snapshots__/test_docs_staleness.ambr
  </files>
  <read_first>
    tools/memory_regen/contracts_index.py (full — the quartet, the header, the no-datetime/float
      rule, and the never-fabricate rule),
    tools/memory_regen/tests/test_contracts_index.py (the generate-twice + syrupy pairing to mirror),
    tools/docs_guard/guard.py (plan 28-05 — `classify`'s result dict),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    - `render(rows)` is a stable table whose first line is exactly
      `DERIVED — do not hand-edit (tools/memory_regen/docs_staleness.py)`, ending with a trailing
      newline.
    - Columns, fixed order (DOCSUP-05 grouping, pointer-only): binding id | target path | state |
      severity | required disposition | graph impact ids.
    - Rows are sorted by binding id. Only `BROKEN`, `STALE_REQUIRED`, `STALE_ADVISORY`, and
      `SUPERSEDING_ADR_REQUIRED` obligations appear — `FRESH` and `SUPPRESSED` bindings are omitted,
      because a queue of things needing nothing is not a queue.
    - `test_queue_contains_no_prose_or_diff`: no row carries a document excerpt, a diff hunk, or any
      text copied out of a target file. Assert by seeding a target with a distinctive sentinel
      sentence and asserting it is ABSENT from the rendered queue.
    - `test_queue_generate_twice_identical`: `render()` twice over the same fixture -> equal
      SHA-256.
    - `test_queue_delete_regenerate_identical`: `write()`, hash, delete, `write()`, hash -> equal.
    - `test_queue_matches_snapshot`: a committed syrupy `.ambr` over a HERMETIC fixture; the rendered
      text must not contain any `tmp_path`.
    - `test_queue_has_no_wallclock_or_float`: a static text scan of `docs_staleness.py` for
      `datetime`, `date.today`, `.now()`, `time.time`, `time.monotonic`, and `float(`, with a live
      planted-token negative control so the scan is known-live.
    - `test_queue_empty_when_nothing_stale`: zero qualifying bindings renders the header plus an
      explicit empty marker — a stable, non-empty file (so `main()` is idempotent), while the
      injector's zero-item path keys off the ROW COUNT, not file emptiness.
  </behavior>
  <action>
    Write `tools/memory_regen/docs_staleness.py` as a faithful clone of `contracts_index.py`'s
    quartet — `rows()` / `render()` / `write()` / `main()` — with
    `DERIVED_HEADER = "DERIVED — do not hand-edit (tools/memory_regen/docs_staleness.py)"` and
    `QUEUE_PATH = DERIVED_DIR / "docs-staleness.md"`.

    `rows()` calls `tools.docs_guard.guard.classify(...)` and `tools.docs_guard.impact.impact_ids`,
    and reuses `tools.docs_guard.digest` transitively. It NEVER re-implements classification or
    hashing — a second implementation could silently disagree with the gate (the standing rule at
    contracts_index.py:4-8). Accept optional explicit `registry_path` / `ledger_path` / `root`
    parameters so the tests are hermetic and the D-14 overlay seam stays open.

    Add a module docstring recording, in this order: (a) why this module lives under
    `tools/memory_regen/` and not `tools/docs_guard/` — `test_derived_freshness.py:32` pins
    `_ALLOWED_TOOL_MODULES = {memory_regen, docs_sync}` for the `/refresh-memory` + curator surface,
    and that gate scans those markdown files' TEXT, so importing `tools.docs_guard` here is fine
    while naming it in `refresh-memory.md` is not; (b) that the output is GITIGNORED
    (`.gitignore:23` already covers it — zero .gitignore change) and must NOT be added to the
    `stale-derived` job's path list, because its content is a function of the files being edited and
    committing it would red every ordinary source commit; (c) that determinism is therefore proven
    by generate-twice SHA-256 plus a committed syrupy snapshot, not by `git diff`.

    No `datetime`, no `time`, no `float`, no `set` iteration reaching output. Pointer-only rows:
    never a prose excerpt, never a diff body.

    Add the tests and generate the `.ambr` with `--snapshot-update`, then re-run without the flag.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_docs_staleness.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/memory_regen/tests/test_docs_staleness.py -q` exits 0, including the
    generate-twice, delete-regenerate, snapshot, and no-prose assertions. The wall-clock/float scan
    has a live planted-token negative control. `git status --porcelain .gitignore` is empty (zero
    change, D-10). `git check-ignore -q .memory/derived/docs-staleness.md` succeeds, confirming the
    queue is ignored. `grep -n 'docs-staleness' .github/workflows/ci.yml` returns nothing at this
    point (the queue must never join `stale-derived`). `uv run python -m tools.memory_regen.docs_staleness`
    exits 0 against the live tree.
  </acceptance_criteria>
  <done>The queue generator clones the DERIVED quartet, reuses the guard rather than re-implementing it, renders pointer-only sorted rows deterministically, and stays gitignored and out of `stale-derived`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The conditional one-line SessionStart pointer</name>
  <files>
    tools/memory_regen/inject.py,
    tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr
  </files>
  <read_first>
    tools/memory_regen/inject.py lines 18-30, 56-75, and 157-190 (the constants, the parameterized
      reader pattern, and `assemble()`),
    tools/memory_regen/tests/test_inject_determinism.py lines 20-95 (the four determinism tests and
      the static wall-clock scan with its negative control),
    tools/memory_regen/tests/test_inject_assembler.py lines 30-35 and 145-160 (the two budget tests),
    this plan's `<interfaces>` block — especially the six-test table
  </read_first>
  <behavior>
    - `_docs_staleness_pointer(derived_dir=DERIVED_DIR)` returns `""` when the queue file is absent
      OR reports zero qualifying rows. An empty string is SKIPPED at inject.py:181, so the payload
      is BYTE-IDENTICAL to today's. Assert exactly that: `assemble()` with no queue file equals the
      pre-change payload for the same fixture.
    - With N > 0 rows it returns EXACTLY TWO lines — a header line plus one summary line — and never
      more, regardless of N. Assert `text.count("\n") <= 1` over a fixture with 50 queue rows. This
      is the assertion that keeps the budget test safe as the queue grows.
    - The section name is `"docs"` and it is placed AFTER `("contracts", ...)` and BEFORE
      `("repomap", ...)`.
    - `test_docs_pointer_is_droppable`: with the budget squeezed, the `"docs"` section is dropped
      while `agreements`/`banner`/`drift`/`task` survive. Assert the never-drop tuple at inject.py:184
      is UNCHANGED by comparing it to the exact literal `("agreements", "banner", "drift", "task")`.
    - `test_docs_pointer_uses_derived_dir_parameter`: pass a fixture `derived_dir` containing a queue
      and assert the pointer reflects IT, not the live repo. Then assert `str(tmp_path) not in
      assemble(derived_dir=fixture)` — the Pitfall-3 guard.
    - `test_docs_pointer_does_not_recompute_guard`: monkeypatch `tools.docs_guard.guard.classify` to
      raise, then call `assemble()` and assert it still succeeds. The pointer reads the rendered
      file only; a guard call on the session-start hot path would surface here.
    - `test_docs_pointer_mentions_no_drift`: the pointer text contains no contract-drift wording —
      the payload already carries a never-dropped `drift` section (inject.py:170) and a third
      surfacing would be double-reporting.
  </behavior>
  <action>
    Add `_docs_staleness_pointer(derived_dir: Path = DERIVED_DIR) -> str` to `inject.py`, mirroring
    `_contracts_summary`'s signature and its `Path(derived_dir) / "<file>"` read exactly. Do NOT use
    `_read_head` — it returns `_HEAD_LINES = 20` lines (inject.py:24) and would break the one-line
    rule. Read the rendered `docs-staleness.md`, count its qualifying data rows by parsing the stable
    table the generator emits, and build the string as a `DOCS_HEADER` constant plus a single
    `f"{n} human doc(s) need review — see .memory/derived/docs-staleness.md"`. Return `""` on
    `OSError` and on `n == 0`.

    Insert `("docs", _docs_staleness_pointer(derived_dir))` into the `sections` list between
    `("contracts", ...)` and `("repomap", ...)`. Do NOT add `"docs"` to the never-drop tuple at
    inject.py:184 — that tuple is the reason `test_budget_holds_with_full_agreements_block` can still
    hold, and this is the least important section in the payload. Do NOT change
    `budget_chars: int = 4000` (inject.py:158).

    Import NOTHING new into `inject.py` beyond `pathlib` (already present). In particular no
    `datetime`, no `time`, and no import of `tools.docs_guard` — `test_inject_module_has_no_wallclock`
    is a static TEXT scan and a guard import would put a `git` subprocess on the hot path.

    Then run the six named tests EXPLICITLY, one selection, before anything else:

        uv run pytest \
          tools/memory_regen/tests/test_inject_assembler.py::test_default_payload_within_budget \
          tools/memory_regen/tests/test_inject_assembler.py::test_budget_holds_with_full_agreements_block \
          tools/memory_regen/tests/test_inject_determinism.py::test_assemble_is_byte_identical \
          tools/memory_regen/tests/test_inject_determinism.py::test_assemble_delete_regenerate_is_byte_identical \
          tools/memory_regen/tests/test_inject_determinism.py::test_payload_matches_snapshot \
          tools/memory_regen/tests/test_inject_determinism.py::test_inject_module_has_no_wallclock \
          -q

    Record the result verbatim in the SUMMARY under `## Six named injector tests (D-11)`.

    `test_payload_matches_snapshot` will fail if and only if the fixture payload actually changed.
    Because the fixture has no queue file, the pointer returns `""` and the payload SHOULD be
    unchanged — so a snapshot failure here is a REAL defect (most likely a module-constant read
    instead of the parameter, Pitfall 3), NOT a snapshot that needs updating. Investigate before
    running `--snapshot-update`. Only regenerate the `.ambr` if you have added a fixture that
    deliberately carries a queue, and record that reasoning in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_regen -q</automated>
  </verify>
  <acceptance_criteria>
    All SIX named tests pass, run as an explicit selection with the output recorded. `uv run pytest
    tools/memory_regen -q` exits 0. `grep -n 'agreements", "banner", "drift", "task"'
    tools/memory_regen/inject.py` still matches the unchanged four-element tuple. `grep -n
    'budget_chars: int = 4000' tools/memory_regen/inject.py` still matches. `grep -nE
    'datetime|time\.|docs_guard' tools/memory_regen/inject.py` returns nothing. The zero-item
    payload is byte-identical to the pre-change payload for the same fixture, asserted explicitly.
  </acceptance_criteria>
  <done>`inject.py` carries one droppable, conditional, at-most-two-line docs section that reads the rendered queue under the `derived_dir` parameter, with the never-drop tuple, the budget constant, and the byte-identity guarantee all intact.</done>
</task>

<task type="auto">
  <name>Task 3: Wire /refresh-memory and round-trip the emitter</name>
  <files>
    harness/commands/refresh-memory.md,
    .opencode/command/refresh-memory.md,
    .claude/commands/refresh-memory.md
  </files>
  <read_first>
    harness/commands/refresh-memory.md (full — the step-1 session-derived vs step-2 committed-derived
      split),
    tools/harness_lint/tests/test_derived_freshness.py (the `_ALLOWED_TOOL_MODULES` text scan)
  </read_first>
  <action>
    Add `tools.memory_regen.docs_staleness` to `harness/commands/refresh-memory.md` STEP 1 — the
    session-derived, gitignored half alongside `repo_map` and `pointer_index`. It must NOT go into
    step 2: step 2 is the committed-derived set the `stale-derived` CI gate guards, and the queue is
    deliberately not in that set (D-10).

    Extend step 1's existing `!` invocation line rather than adding a second one, and extend the
    prose to name the queue as session-ephemeral, never committed, never gated — matching how the
    repo-map is already described.

    Spell the module ONLY as `tools.memory_regen.docs_staleness`. Never write `tools.docs_guard`
    anywhere in this file or in `harness/agents/curator.md`:
    `test_derived_freshness.py:32` restricts that surface to `{memory_regen, docs_sync}` via a regex
    over the file TEXT, and a `tools.docs_guard` token would red it. Do not touch `curator.md` at all.

    Then re-emit both runtimes:

        uv run python -m tools.harness_emit.generate
        git diff --stat -- .opencode .claude

    The diff must show exactly the two emitted `refresh-memory.md` files and nothing else. Never
    hand-edit an emitted file — if the emitted content is wrong, fix the `harness/` source and
    re-emit. Confirm the round-trip is closed:

        uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude

    is a clean no-op on the SECOND run.
  </action>
  <verify>
    <automated>uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude && uv run pytest tools/harness_lint/tests/test_derived_freshness.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint/tests/test_derived_freshness.py -q` exits 0 (the
    `_ALLOWED_TOOL_MODULES` scan still passes). A second `uv run python -m tools.harness_emit.generate`
    leaves `git diff --exit-code -- .opencode .claude` clean, so the `emit-drift` CI job (ci.yml:203)
    would pass. `grep -rn 'docs_guard' harness/commands/refresh-memory.md harness/agents/curator.md`
    returns nothing. `git status --porcelain harness/agents/curator.md` is empty. `grep -n
    'docs_staleness' harness/commands/refresh-memory.md` shows it under step 1, not step 2.
    `uv run pytest tools/memory_regen tools/docs_guard -q` exits 0.
  </acceptance_criteria>
  <done>`/refresh-memory` regenerates the queue as part of its session-derived step, both runtime artifacts are re-emitted byte-identically from the neutral source, and the derived-freshness allowlist gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered queue file → `inject.py` | The injector reads a machine-generated file. It must not recompute the guard, and must read under the `derived_dir` parameter so tests can fixture it. |
| Queue content → SessionStart payload | The payload has a hard ~4000-char budget and a byte-identity guarantee. An unbounded or nondeterministic section breaks both. |
| `harness/` source → emitted runtime trees | One-way. Emitted files are never hand-edited; `emit-drift` (ci.yml:203) is the verify side. |
| Queue file → git | Must stay OUTSIDE git. Committing it would put it in `stale-derived` and red every ordinary source commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-31 | Denial of Service | a multi-line pointer blowing the ~4000-char budget | mitigate | At most two lines BY CONSTRUCTION (header + one summary), asserted over a 50-row fixture; `_read_head` (20 lines) deliberately not used; the budget constant untouched. |
| T-28-32 | Tampering | widening the never-drop tuple to protect the docs pointer | mitigate | The tuple is asserted equal to the exact literal `("agreements", "banner", "drift", "task")`; the docs section is proven droppable under a squeezed budget. |
| T-28-33 | Repudiation | the committed injector snapshot silently absorbing live repo state | mitigate | The pointer takes `derived_dir` as a PARAMETER (Pitfall 3), and the snapshot test's `str(tmp_path) not in payload` assertion is re-run explicitly; a snapshot failure on the no-queue fixture is treated as a real defect, not a snapshot to update. |
| T-28-34 | Repudiation | a wall-clock token entering `inject.py` | mitigate | No new imports beyond what is present; `test_inject_module_has_no_wallclock` is a static TEXT scan with a live negative control and is run by name. |
| T-28-35 | Denial of Service | the queue joining `stale-derived` and redding every source commit | mitigate | Queue stays gitignored via the existing `.gitignore:23` (zero change), asserted by `git check-ignore`; `ci.yml` is asserted to contain no `docs-staleness` reference. |
| T-28-36 | Denial of Service | a `git` subprocess + full corpus walk on the session-start hot path | mitigate | The pointer reads the rendered file only; proven by monkeypatching `classify` to raise and asserting `assemble()` still succeeds. |
| T-28-37 | Information Disclosure | document prose or a diff hunk copied into the derived queue | mitigate | Pointer-only rows; asserted by a sentinel sentence seeded into a target and proven absent from the render. |
| T-28-38 | Tampering | `tools.docs_guard` named in the `/refresh-memory` or curator surface | mitigate | `test_derived_freshness.py:32`'s `_ALLOWED_TOOL_MODULES` text scan; the generator lives under `memory_regen` precisely so the invocation can be spelled legally, and `curator.md` is not touched at all. |
| T-28-39 | Tampering | emitted runtime trees diverging from the neutral source | mitigate | Re-emit + `git diff --exit-code -- .opencode .claude` as a clean no-op on the second run; emitted files never hand-edited. |
| T-28-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new external packages. |
</threat_model>

<verification>
- The SIX named injector tests pass as an explicit selection, output recorded.
- `uv run pytest tools/memory_regen tools/docs_guard tools/harness_lint/tests/test_derived_freshness.py -q` exits 0.
- `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude` is a clean no-op on the second run.
- `git status --porcelain .gitignore harness/agents/curator.md` is empty.
- `git check-ignore -q .memory/derived/docs-staleness.md` succeeds.

Do NOT run the full suite in flight: plan 28-07 is concurrently editing `.github/workflows/ci.yml`
and seeding `docs/*.toml` in this same wave. The fan-in belongs to plan 28-08.
</verification>

<success_criteria>
- SC-4 (ROADMAP): the derived queue regenerates deterministically; the injector byte-identity and
  ~4000-char budget tests are green; `/docs-sync`, `/refresh-memory`, and `stale-derived` semantics
  are unchanged.
- The zero-item payload is byte-identical to today's.
- The never-drop tuple, the 4000 default, and `.gitignore` are all unchanged.
- The emitter round-trip is closed for both runtimes.
</success_criteria>

<output>
Create `.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-06-SUMMARY.md` when
done, with a `## Six named injector tests (D-11)` section carrying the verbatim result of the
explicit six-test selection, and a statement of whether `test_inject_determinism.ambr` was
regenerated and exactly why.
</output>
