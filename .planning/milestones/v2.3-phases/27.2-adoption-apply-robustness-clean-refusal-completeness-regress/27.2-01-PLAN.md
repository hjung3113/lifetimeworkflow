---
phase: 27.2-adoption-apply-robustness-clean-refusal-completeness-regress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_apply/apply.py
  - tools/adoption_apply/approval.py
  - tools/adoption_apply/cli.py
  - tools/adoption_apply/tests/test_constitution_refusal.py
  - tools/adoption_apply/tests/test_approval_invalidation.py
  - tools/adoption_apply/tests/test_cli.py
autonomous: true
requirements: [ADOPT-05, ADOPT-06]

must_haves:
  truths:
    - "SC-1 (WR-05): a directory-shaped `destination` — `\".\"`, `\"./\"`, `\"\"`, a TRAILING-SLASH spelling whose target does not exist yet (`\"a/\"`, `\"newdir/\"`), a trailing-dot spelling (`\"a/b/.\"`), an existing directory path, and `\"./<existing-dir>\"` — is refused by `refuse_unsafe_destination` BEFORE any filesystem write, and surfaces at the CLI as exit 1 with a diagnostic naming the destination and no `Traceback` on stderr"
    - "SC-1: the trailing-slash class is caught by a STRUCTURAL pre-check on the raw destination spelling, before resolution — neither the resolve-equality check nor `is_dir()` can catch `\"newdir/\"`, because it resolves to a non-existent, non-root path"
    - "SC-1: a legitimate file destination inside an existing directory, and a destination whose parent chain does not exist yet, both stay ALLOWED (D-03 — the guard must not over-refuse)"
    - "SC-2 (WR-06): `check_valid` returns `False` — never raises — for every row of the corruption table: invalid JSON, valid JSON that is not an object (list / string / number), an object missing each required key in turn (`draft_hash`, `task_revision`, `git_ref`), and each required key present with the wrong type"
    - "SC-4: both adversarial-input tables are authored and RED-confirmed against pre-fix code in Task 1, with the observed failure message recorded, BEFORE either fix lands"
    - "D-08 holds: no file under `contracts/`, `docs/adr/`, or `golden/` is edited by this plan — the schema still admits `\".\"`/`\"./\"`/`\"a/\"`, so the refusal is deliberately runtime-only and that residual is recorded as carried forward"
    - "Wave-1 co-tenancy: this plan's in-flight gates run ONLY its own test selections; the full-suite / drift / emit / GEN-04 fan-in belongs to the phase-closing commit, because plan 27.2-02 is editing `tests/test_atomic_apply.py` concurrently"
  artifacts:
    - path: "tools/adoption_apply/apply.py"
      provides: "`refuse_unsafe_destination` additionally refuses a destination whose raw last segment is empty or `.` (structural, pre-resolution), one that resolves to `target_root` itself, and one that resolves to an existing directory (WR-05) — all three raising the existing `PathEscapeError` (D-02)"
      contains: "resolves to target_root itself"
    - path: "tools/adoption_apply/approval.py"
      provides: "`check_valid`'s guarded region widened to cover `json.loads` and every `stored[...]` subscript, catching `json.JSONDecodeError`, `KeyError`, and `TypeError` alongside the existing `FileNotFoundError`/`OSError` (WR-06)"
      contains: "json.JSONDecodeError"
    - path: "tools/adoption_apply/cli.py"
      provides: "`_cmd_apply`'s `except` tuple widened as defense-in-depth (D-01) to also name `IsADirectoryError` and `apply_module.CollisionError`"
      contains: "IsADirectoryError"
    - path: "tools/adoption_apply/tests/test_constitution_refusal.py"
      provides: "`DIRECTORY_SHAPED_DESTINATIONS` adversarial table (including the trailing-slash and trailing-dot rows) + a parametrized refusal test and a paired negative-control test proving legitimate destinations are still allowed"
      contains: "DIRECTORY_SHAPED_DESTINATIONS"
    - path: "tools/adoption_apply/tests/test_approval_invalidation.py"
      provides: "`CORRUPT_APPROVAL_CASES` parametrized table asserting `check_valid` returns `False` and does not raise for every corruption row"
      contains: "CORRUPT_APPROVAL_CASES"
    - path: "tools/adoption_apply/tests/test_cli.py"
      provides: "CLI-boundary case proving `destination: \".\"` exits 1 with a clean stderr message and no `Traceback`"
      contains: "directory_shaped"
  key_links:
    - from: "tools/adoption_apply/apply.py::refuse_unsafe_destination"
      to: "tools/adoption_apply/apply.py::apply_disposition"
      via: "the single 27.1 choke point already called first in every branch — WR-05 is fixed there, never at the individual write branches (D-01)"
      pattern: "refuse_unsafe_destination"
    - from: "tools/adoption_apply/apply.py::PathEscapeError"
      to: "tools/adoption_apply/cli.py::_cmd_apply"
      via: "already-listed exception in the except tuple -> exit 1 (D-02: reuse the existing refusal exit code, no new code)"
      pattern: "PathEscapeError"
    - from: "tools/adoption_apply/approval.py::check_valid"
      to: "tools/adoption_apply/cli.py::_cmd_apply"
      via: "boolean gate -> exit 4; callers unchanged because the return shape is unchanged (D-04)"
      pattern: "check_valid"
---

<objective>
Close WR-05 and WR-06 from `27.1-REVIEW.md` — the two unhandled-exception paths on the adoption
apply write path. Both fail closed (no write happens), so neither is a security bypass, but each
contradicts a property Phase 27.1 explicitly claimed: "a hostile or malformed manifest is refused
cleanly, never an unhandled traceback" (WR-05) and `check_valid`'s own docstring promise "no partial
credit — any single mismatch returns `False`" (WR-06).

Purpose: Phase 29 wires `/adopt` into a reviewed seeding loop; the docs-supervision layer inherits
whatever confidence this write path has. A raw traceback where a documented exit code was promised
is exactly the kind of defect that erodes that confidence.

Output: `refuse_unsafe_destination` refuses directory-shaped destinations at the 27.1 choke point;
`check_valid` never raises on a corrupted `approval.json`; both fixes are preceded by an
adversarial-input table that was RED-confirmed against pre-fix code with the failure message
recorded (SC-4).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.2-adoption-apply-robustness-clean-refusal-completeness-regress/27.2-CONTEXT.md
@.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-REVIEW.md
@tools/adoption_apply/apply.py
@tools/adoption_apply/approval.py
@tools/adoption_apply/cli.py
@tools/adoption_apply/tests/test_constitution_refusal.py
@tools/adoption_apply/tests/test_approval_invalidation.py

<interfaces>
<!-- Current contracts the executor needs. No codebase exploration required. -->

`tools/adoption_apply/apply.py` — the 27.1 choke point, unchanged as of this plan:

    def refuse_unsafe_destination(destination: str, target_root: str | Path) -> Path

Its current body, in order: (1) a structural pre-check rejecting an absolute `destination` or one
containing a literal `..` path segment (`segments = destination.replace("\\", "/").split("/")`);
(2) `target_path = (Path(target_root) / destination).resolve(strict=False)`; (3)
`refuse_if_outside_root(target_path, target_root)`; (4)
`relative = target_path.relative_to(Path(target_root).resolve()).as_posix()` then
`refuse_if_constitution(relative.lower())`; (5) `return target_path`.

Exception classes defined in the same module: `ConstitutionRefusal`, `PathEscapeError`,
`CollisionError`, `UnknownDispositionError`, `ConcurrentDriftError`, `SymlinkRefusal` — all
`ValueError` subclasses.

`apply_disposition` calls `refuse_unsafe_destination(destination, target_root)` as its third
statement, before any branch. Its `create` branch then does
`if target_path.exists(): current_hash = _existing_hash(target_path); raise ConcurrentDriftError(...)`
— and `_existing_hash` (`tools/adoption_scan/destinations.py`) does `path.open("rb")`, which is what
raises `IsADirectoryError` on a directory today.

`apply_manifest` buckets ONLY `ConstitutionRefusal` into `summary["refused"]`; every other exception
propagates to the caller.

`tools/adoption_apply/cli.py::_cmd_apply` currently catches exactly:

    except (
        apply_module.ConstitutionRefusal,
        apply_module.ConcurrentDriftError,
        apply_module.UnknownDispositionError,
        apply_module.PathEscapeError,
        apply_module.SymlinkRefusal,
    ) as exc:
        print(f"tools.adoption_apply apply: {exc}", file=sys.stderr)
        return 1

Note that `CollisionError` is NOT in that tuple today, and it is raised by `atomic_create`.

`tools/adoption_apply/approval.py::check_valid` (lines 172-193), current body:

    path = _approval_path(task_dir, batch_id)
    if not path.is_file():
        return False
    stored = json.loads(path.read_bytes())          # <-- outside the try (WR-06)
    batch_dir = _batch_dir(task_dir, batch_id)
    try:
        draft_hash = _recompute_draft_hash(batch_dir)
    except (FileNotFoundError, OSError):
        return False
    return (
        stored["draft_hash"] == draft_hash          # <-- outside the try (WR-06)
        and stored["task_revision"] == _current_task_revision(task_dir)
        and stored["git_ref"] == _current_git_ref(repo_root)
    )

The three required stored keys are exactly `draft_hash` (str), `task_revision` (int), `git_ref`
(str). `approval.py` imports `json` already; it does NOT currently import `sys`.

Existing test-table conventions to mirror (do not restructure them):
- `tools/adoption_apply/tests/test_constitution_refusal.py` holds `HOSTILE_DESTINATIONS` (line 21)
  and `PATH_ESCAPE_KINDS` (line 91), consumed by
  `@pytest.mark.parametrize("kind", HOSTILE_DESTINATIONS + PATH_ESCAPE_KINDS)` at line 105 and by
  end-to-end `apply_disposition` tests at lines 124-175. `_ESCAPE_ABSOLUTE`/`_ESCAPE_TRAVERSAL` are
  sentinel kinds resolved to real strings by a helper at line 95 (an absolute destination must be a
  synthetic `tmp_path`-derived string, never a literal system path).
- `tools/adoption_apply/tests/test_approval_invalidation.py` holds fixtures at lines 38/74 and a
  `_seed_batch(task_dir, git_repo, *, plan_seed="p1")` helper at line 89;
  `test_valid_false_on_missing_draft_artifact` (line 211) is the WR-02 precedent this plan's
  corruption table extends.
- `tools/adoption_apply/tests/test_cli.py::test_cli_apply_refuses_hostile_destination_cleanly`
  (line 465) is the CLI-boundary convention: seed a manifest via `_seed_batch_with_manifest`,
  `_promote(...)`, then `subprocess.run([sys.executable, "-m", "tools.adoption_apply", "apply", ...],
  cwd=repo_root)` and assert `returncode == 1`, `"Traceback" not in result.stderr`, and
  `result.stderr.strip()` non-empty.

Project rule (27.1 D-01, still binding): Phase 27's existing tests are append-only — never weaken,
rename, or delete an existing test function in these files. Every change here is a pure addition
except the two production-source edits.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — author both adversarial-input tables and record their pre-fix failure messages</name>
  <files>
    tools/adoption_apply/tests/test_constitution_refusal.py,
    tools/adoption_apply/tests/test_approval_invalidation.py,
    tools/adoption_apply/tests/test_cli.py
  </files>
  <read_first>
    tools/adoption_apply/tests/test_constitution_refusal.py (full — the HOSTILE_DESTINATIONS /
      PATH_ESCAPE_KINDS table idiom and the sentinel-resolution helper),
    tools/adoption_apply/tests/test_approval_invalidation.py (full — the fixtures, `_seed_batch`,
      and `test_valid_false_on_missing_draft_artifact` as the WR-02 precedent),
    tools/adoption_apply/tests/test_cli.py lines 440-585 (the hostile-destination CLI-boundary
      convention this task extends),
    this plan's `<interfaces>` block (current `refuse_unsafe_destination` and `check_valid` bodies —
      no re-derivation needed)
  </read_first>
  <behavior>
    WR-05 table, in `test_constitution_refusal.py` — a new module-level
    `DIRECTORY_SHAPED_DESTINATIONS` list of `(case_name, destination)` rows covering ALL THREE
    directory-shaped classes CONTEXT D-03 names, not just the resolvable ones:

      (a) resolves to the root itself — `"."`, `"./"`, `""` (empty string);
      (b) TRAILING-SLASH / TRAILING-DOT spellings whose target does NOT exist yet — `"a/"`,
          `"newdir/"`, `"a/b/."`. These are the rows the resolve-based checks cannot see:
          `(root / "newdir/").resolve()` is `root/newdir`, which is neither equal to the root nor
          an existing directory, so such a manifest today passes the schema `pattern` and silently
          creates a FILE named `newdir`. This class is why a structural pre-check is required;
      (c) an EXISTING directory — `"src"` after `(tmp_path / "src").mkdir()`, the `"./"`-prefixed
          spelling of that same directory (`"./src"`), and `"src/"`.

    Parametrized test `test_refuse_unsafe_destination_rejects_directory_shaped` asserts each row
    raises `apply.PathEscapeError` (D-02 — the existing refusal exception, not a new one) and that
    the message names the destination. Group the rows by class in the table so a future reader can
    see which check each one exercises. A paired negative control
    `test_refuse_unsafe_destination_still_allows_file_destinations` asserts these stay ALLOWED
    (D-03, guard must not over-refuse): `"src/widget.py"` inside an existing `src/` directory,
    `"a/b/c.txt"` whose parent chain does not exist yet, and `"AGENTS.md"` at the root. An
    end-to-end row `test_apply_disposition_refuses_directory_shaped_destination` runs
    `apply.apply_disposition({"destination": ".", "disposition": "create"}, tmp_path, payload=b"x")`
    and asserts `PathEscapeError` — NOT `IsADirectoryError` — with `os.open`/`os.link`/`os.replace`
    spies at zero calls, mirroring the existing zero-write-spy proof at lines 124-175. Add a second
    end-to-end row for `destination: "newdir/"` asserting the same — that class is the one that
    would otherwise silently create a file, so it must be proven at the `apply_disposition`
    boundary, not only at the guard.

    WR-05 CLI row, in `test_cli.py` — a new `test_cli_apply_refuses_directory_shaped_destination`
    following `test_cli_apply_refuses_hostile_destination_cleanly`'s exact seed/promote/subprocess
    shape (locate it BY NAME, not by line number), with `destination: "."` AND a second case
    `destination: "newdir/"` in separate manifests, each asserting `returncode == 1`, `"Traceback"
    not in result.stderr`, a non-empty stderr naming the destination, and — for the `"newdir/"`
    case — that no file named `newdir` was created under the apply target. Do not modify the
    existing hostile-destination test.

    WR-06 table, in `test_approval_invalidation.py` — a new module-level `CORRUPT_APPROVAL_CASES`
    list of `(case_name, raw_bytes_or_document)` rows covering, per D-05: invalid JSON
    (`b"{not valid json"`); valid JSON that is not an object (a list, a bare string, a number);
    an object missing each required key IN TURN (three rows — one dropping `draft_hash`, one
    `task_revision`, one `git_ref`); and each required key present with the WRONG TYPE (three rows —
    `draft_hash` as an int, `task_revision` as a string, `git_ref` as a list). Parametrized test
    `test_check_valid_never_raises_on_corrupt_approval` seeds a promoted batch via the existing
    fixtures/`_seed_batch`, overwrites `approval.json` with the row's bytes, then asserts
    `check_valid(...) is False` — and, critically, that the call does not raise, expressed as a
    direct assertion on the return value rather than a `pytest.raises` inversion.

    Note on the wrong-type rows: they may already return `False` today by simple inequality without
    raising. That is expected and is not a defect — they are table-completeness rows per D-05, and
    the RED evidence for WR-06 comes from the invalid-JSON, non-object, and missing-key rows.
  </behavior>
  <action>
    Add the three tables and their tests as PURE ADDITIONS to the three existing test files (27.1
    D-01: never weaken, rename, or delete an existing test function). Follow each file's established
    parametrize/fixture idiom exactly rather than introducing a new one.

    Then run the RED confirmation and RECORD it. Run each new test selection against the
    still-unfixed production code and capture the verbatim failure output:

      uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py -k directory_shaped -q
      uv run pytest tools/adoption_apply/tests/test_cli.py -k directory_shaped -q
      uv run pytest tools/adoption_apply/tests/test_approval_invalidation.py -k corrupt_approval -q

    Run ONLY these selections at this point — never the full suite. This task deliberately leaves
    ~15 tests RED, and plan 27.2-02 is editing `tools/adoption_apply/tests/test_atomic_apply.py`
    concurrently in the same wave. A failure in a file this plan does not own is the other wave-1
    plan in flight — do not modify it, and do not treat it as this plan's regression.

    Paste the exact failure lines into the plan SUMMARY under a `## RED evidence (SC-4)` heading.
    The failures MUST be for the stated reason — confirm each one explicitly:
      - `refuse_unsafe_destination(".", ...)` fails with `DID NOT RAISE PathEscapeError` (the guard
        currently accepts it), NOT an import/collection error.
      - `apply_disposition` with `destination: "."` fails with `IsADirectoryError: [Errno 21] Is a
        directory`, which is the exact WR-05 symptom.
      - `check_valid` on invalid JSON fails with `json.decoder.JSONDecodeError`, and on a
        missing-key row with `KeyError: 'draft_hash'` (or `TypeError` for the non-object rows) —
        the exact WR-06 symptoms.

    If any row passes when it was expected to fail, STOP and investigate before writing any fix:
    a test that cannot be shown failing is not accepted as evidence (SC-4).
  </action>
  <verify>
    <automated>! uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py -k directory_shaped -q</automated>
  </verify>
  <acceptance_criteria>
    The RED gate is INVERTED on purpose: `! uv run pytest ...` exits 0 only when the selection
    FAILS. Never pipe the pytest invocation into `tail`/`head` — a pipeline reports the LAST
    command's status, which would make the gate unfailable. Run the same inverted form manually for
    the other two selections (`test_cli.py -k directory_shaped`,
    `test_approval_invalidation.py -k corrupt_approval`) and record each. All three new selections
    are RED against unmodified `apply.py`/`approval.py`/`cli.py`, each failing for the WR-05/WR-06
    reason named above and not for a collection, import, or fixture error — in particular the
    `"newdir/"` rows must fail with `DID NOT RAISE PathEscapeError`, since neither resolve-based
    check can see them. Every pre-existing test in the three files THIS plan owns still passes
    unchanged (D-01). The verbatim failure output is captured for the SUMMARY's
    `## RED evidence (SC-4)` section.
  </acceptance_criteria>
  <done>Both adversarial-input tables exist as pure additions, the RED run is complete, and its failure messages are recorded — before any production-source line has been edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — refuse directory-shaped destinations at the choke point (WR-05)</name>
  <files>tools/adoption_apply/apply.py, tools/adoption_apply/cli.py</files>
  <read_first>
    tools/adoption_apply/apply.py (full — `refuse_unsafe_destination` at 109-143 and
      `apply_disposition` at 264-313),
    tools/adoption_apply/cli.py lines 142-206 (`_cmd_apply` and its except tuple),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    - `refuse_unsafe_destination(".", root)` raises `PathEscapeError` naming `"."`.
    - Same for `"./"` and `""`.
    - `refuse_unsafe_destination("newdir/", root)` raises `PathEscapeError` EVEN THOUGH `root/newdir`
      does not exist — the trailing-slash class, caught structurally before resolution.
    - Same for `"a/"`, `"src/"`, and `"a/b/."`.
    - `refuse_unsafe_destination("src", root)` where `root/src` is an existing directory raises
      `PathEscapeError`.
    - `refuse_unsafe_destination("src/widget.py", root)` where `root/src` exists returns the
      resolved path unchanged (no refusal).
    - `refuse_unsafe_destination("a/b/c.txt", root)` where `a/` does not exist returns the resolved
      path unchanged (D-03 — a not-yet-existing path is not directory-shaped).
    - `apply_disposition({"destination": ".", ...})` raises `PathEscapeError`, never
      `IsADirectoryError`, and performs zero writes.
  </behavior>
  <action>
    Add THREE refusals to `refuse_unsafe_destination`, all sharing the existing `PathEscapeError`
    (D-02 — reuse the refusal exit path callers already branch on; do NOT introduce a new exception
    class or a new exit code). One is structural and belongs at the TOP; two are resolve-based and go
    after `refuse_if_outside_root`:

    1. STRUCTURAL, extending the function's EXISTING pre-check block — the one that already computes
       `segments = destination.replace("\\", "/").split("/")` and rejects an absolute path or a `..`
       segment. In that same block, BEFORE any `Path.resolve()` or filesystem call, add:
       `if segments[-1] in ("", "."): raise PathEscapeError(...)`. This is the ONLY check that can
       catch the trailing-slash class: `"a/"` splits to `["a", ""]`, `"newdir/"` to `["newdir", ""]`,
       `"./"` to `[".", ""]`, `""` to `[""]`, and `"a/b/."` to `["a", "b", "."]` — every one ends in
       an empty or `"."` segment, which is exactly what "names a directory, not a file" means in a
       raw path spelling. `"src/widget.py"` ends in `"widget.py"` and is untouched. Reusing the
       already-computed `segments` keeps this a two-line addition to an existing block, with no
       second path-parsing idiom in the module.
    2. If `target_path == Path(target_root).resolve()`, raise `PathEscapeError` stating that the
       destination resolves to `target_root` itself and that a destination must be a proper
       descendant. Keep this even though check 1 already covers `"."`/`"./"`/`""` by spelling — it is
       the resolution-level backstop for any spelling that collapses onto the root some other way.
    3. If `target_path.is_dir()`, raise `PathEscapeError` stating that the destination resolves to
       an existing directory. This covers a bare existing-directory name and its `"./"`-prefixed
       spelling. Use `is_dir()` (false for a non-existent path) so D-03 holds: a normal file
       destination, existing or not, stays allowed.

    All three messages must include the raw `destination` string so the CLI diagnostic names it.
    Place checks 2 and 3 before `relative = target_path.relative_to(...)` — the `relative_to` call on
    an equal path yields `"."`, which is precisely the value that classifies against no constitution
    glob and lets `"."` through today. Update the function's docstring numbered list to include the
    new steps, and note WR-05 alongside the existing CR-01/CR-02 references.

    Record in the SUMMARY (not in code): `manifest.schema.json`'s `pattern` still ADMITS
    `"."`/`"./"`/`"a/"` — per D-08 this phase closes WR-05 in code only, so the schema-level
    admission is a recorded, carried-forward residual, not an oversight.

    Then, as defense-in-depth only (D-01 — this is NOT the primary fix), widen `_cmd_apply`'s
    `except` tuple in `cli.py` to additionally name `IsADirectoryError` and
    `apply_module.CollisionError`. `CollisionError` is raised by `atomic_create` and is absent from
    the tuple today, so it is the same class of gap; both map to the existing exit 1 and the same
    `tools.adoption_apply apply: {exc}` stderr line — no new exit code, no new message format. Add a
    short comment naming WR-05 and stating that the tuple is a backstop, not the guard.

    Change nothing else in either file. Do not touch `contracts/` (D-08).
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py tools/adoption_apply/tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py -k directory_shaped -q`
    exits 0 (previously RED), including every trailing-slash and trailing-dot row. `uv run pytest
    tools/adoption_apply/tests/test_cli.py -k directory_shaped -q` exits 0. Both files together are
    green with no pre-existing test regressed — in particular
    `test_refuse_unsafe_destination_allows_legitimate_input` and
    `test_non_constitution_destination_allowed` must still pass, proving the guard did not
    over-refuse. Scope every run to the files THIS plan owns: plan 27.2-02 is concurrently editing
    `tests/test_atomic_apply.py`, so a failure there is the other wave-1 plan in flight — do not
    modify it. `git status --porcelain contracts docs/adr golden` is empty.
  </acceptance_criteria>
  <done>`refuse_unsafe_destination` refuses directory-shaped destinations in all three classes — trailing-slash/dot spellings structurally, target-root-equal and existing-directory paths after resolution — with `PathEscapeError`; the CLI except tuple carries `IsADirectoryError` + `CollisionError` as a backstop; the WR-05 tables are green and nothing legitimate is over-refused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — check_valid never raises on a corrupted approval.json (WR-06)</name>
  <files>tools/adoption_apply/approval.py</files>
  <read_first>
    tools/adoption_apply/approval.py (full — `check_valid` at 172-193 and the WR-02 fix it extends),
    27.1-REVIEW.md WR-06 section (the two verified-live reproductions),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    - `check_valid` on a batch whose `approval.json` is `b"{not valid json"` returns `False`.
    - Same for `approval.json` containing a JSON list, a bare JSON string, and a JSON number.
    - Same for an object missing `draft_hash`, missing `task_revision`, or missing `git_ref`.
    - Same for `draft_hash` as an int, `task_revision` as a string, `git_ref` as a list.
    - The happy path is unchanged: a current, matching approval still returns `True`, and the
      existing stale/mismatch/missing-draft-artifact tests still return `False`.
  </behavior>
  <action>
    Widen the existing guarded region in `check_valid` so it spans the whole read-and-compare, per
    D-04 — same return shape (`bool`), never a new failure channel, callers unchanged. Move
    `stored = json.loads(path.read_bytes())` and the final three-way boolean comparison INSIDE the
    `try`, and widen the `except` tuple to
    `(FileNotFoundError, OSError, json.JSONDecodeError, KeyError, TypeError)`.

    Each added exception must be justified in the comment, not added defensively at random:
    `json.JSONDecodeError` for malformed bytes (it subclasses `ValueError`, not `OSError`, so it is
    genuinely not covered today); `KeyError` for a missing required key; `TypeError` for valid JSON
    that is not an object, where `stored["draft_hash"]` raises rather than returning a mismatch.
    Keep `FileNotFoundError`/`OSError` — they are WR-02's original coverage for a missing draft
    artifact and must not be dropped.

    Per D-04, bind a distinct reason string to the corruption case rather than swallowing it
    silently: in the `except` handler, print a one-line diagnostic to `sys.stderr` naming the
    approval path and the exception class (e.g. `approval.json unusable for batch '<id>':
    <ExcClass>` ) before `return False`. Import `sys` at the top of the module alongside the
    existing stdlib imports. Do NOT include the file's contents or the exception's full message body
    in the diagnostic — a corrupted approval may contain arbitrary bytes, and the existing
    `test_marker_merge_refuses_symlink_read` precedent in this package establishes that raised/
    printed strings must not echo target content.

    Update `check_valid`'s docstring to state that it never raises: any unreadable, malformed, or
    structurally-wrong stored approval is treated as invalid, extending WR-02's guarantee from the
    draft artifacts to the approval document itself.

    Change nothing else. `promote`'s schema validation path is untouched — a corrupted
    `approval.json` is a read-side concern only.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_approval_invalidation.py tools/adoption_apply/tests/test_constitution_refusal.py tools/adoption_apply/tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_approval_invalidation.py -k corrupt_approval -q`
    exits 0 (previously RED with `JSONDecodeError`/`KeyError`/`TypeError`). Every pre-existing test
    in that file — `test_valid_when_nothing_changed`, `test_invalidated_on_draft_change`,
    `test_invalidated_on_revision_change`, `test_invalidated_on_ref_change`,
    `test_valid_false_on_missing_draft_artifact`, `test_sc1_full_resume_cycle` — still passes
    unchanged (D-01). All three test files THIS plan owns are green together. The full-suite,
    contract-drift, emit round-trip, and GEN-04 gates are deliberately NOT run here — they are the
    phase-closing fan-in (SC-5's own "at the closing commit" wording), because plan 27.2-02 is
    concurrently editing `tests/test_atomic_apply.py` and its in-flight state is not this plan's
    regression. `git status --porcelain contracts docs/adr golden` is empty (D-08).
  </acceptance_criteria>
  <done>`check_valid` returns `False` for every row of the corruption table and never raises; the happy path and all existing invalidation semantics are unchanged; all three test files this plan owns are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest content (`destination` string) → `apply.py` write path | Untrusted: a manifest may be hand-edited, tampered with, or drafted against a hostile tree between draft and apply. `refuse_unsafe_destination` is the sole choke point. |
| On-disk `approval.json` → `check_valid` | Untrusted: the file may be truncated by a crash, hand-edited, or replaced. It must never be able to crash the gate. |
| `apply.py` exceptions → `cli.py` exit codes | The CLI's `except` tuple is the contract boundary between an internal fault and a documented exit code. |
| Agent session → `contracts/`, `docs/adr/`, `golden/` | Not crossed by this plan (D-08). `contract_guard` remains the enforcement layer if it ever is. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.2-01 | Denial of Service | `apply_disposition`'s `create` branch via `_existing_hash` on a directory | mitigate | `refuse_unsafe_destination` refuses a target-root-equal or existing-directory destination before any filesystem write, raising the existing `PathEscapeError` → documented exit 1 (WR-05). |
| T-27.2-01b | Tampering | `apply_disposition`'s `create` branch on a trailing-slash destination (`"newdir/"`) | mitigate | Structural pre-check on the raw spelling (`segments[-1] in ("", ".")`) refuses before resolution — without it the path resolves to a non-existent, non-root location and a FILE named `newdir` is silently created where the manifest asked for a directory. Neither resolve-equality nor `is_dir()` can catch this class. |
| T-27.2-02 | Denial of Service | `check_valid` on a corrupted `approval.json` | mitigate | Guarded region widened to the whole read-and-compare, catching `json.JSONDecodeError`/`KeyError`/`TypeError` in addition to `FileNotFoundError`/`OSError`; returns `False` (WR-06). Fails closed — no write occurs on the `False` branch. |
| T-27.2-03 | Information Disclosure | the new corruption diagnostic on stderr | mitigate | The diagnostic names the approval path and exception class only — never the file's contents or the exception's full message body, mirroring the `SymlinkRefusal` no-content-leak precedent. |
| T-27.2-04 | Tampering | `refuse_unsafe_destination` over-refusing and breaking the happy path | mitigate | Paired negative-control test (`test_refuse_unsafe_destination_still_allows_file_destinations`) plus the untouched SC-2 full-apply-cycle integration test both gate Task 2 (D-03). |
| T-27.2-05 | Elevation of Privilege | constitution-plane write | accept | Not crossed: this plan edits no file under `contracts/`, `docs/adr/`, or `golden/` (D-08); verified by an empty `git status --porcelain` over those paths in Tasks 2 and 3. |
| T-27.2-11 | Tampering | `manifest.schema.json` still admits `"."`/`"./"`/`"a/"` | accept | Per D-08 this phase closes WR-05 in code only; the schema-level admission is a deliberate, recorded, carried-forward residual — the runtime choke point is the enforcing layer. Tightening the `pattern` would require a hash rebaseline, derived regeneration, and human ratification, which this phase explicitly does not open. |
| T-27.2-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new package installs — stdlib `json`/`sys` and existing in-repo modules only. No `RESEARCH.md` Package Legitimacy Audit required. |
</threat_model>

<verification>

**In-flight (this plan's own gates — run these while plan 27.2-02 may still be executing):**

- `uv run pytest tools/adoption_apply/tests/test_constitution_refusal.py tools/adoption_apply/tests/test_approval_invalidation.py tools/adoption_apply/tests/test_cli.py -q` exits 0.
- `git status --porcelain contracts docs/adr golden` is empty.
- `git diff --stat tools/adoption_apply/tests/test_atomic_apply.py` is empty — this plan does not own that file.

Do NOT run the full suite as an in-flight gate. Plan 27.2-02 is editing `tests/test_atomic_apply.py`
in the same wave, and this plan's own Task 1 deliberately leaves ~15 tests RED until Tasks 2-3 land.
A failure in a file this plan does not own is the other wave-1 plan in flight — do not modify it.

**Phase-closing fan-in (after BOTH wave-1 plans land — SC-5's "at the closing commit"):**

- `uv run pytest -q` (full non-example suite) exits 0.
- `uv run python -m tools.contract_drift.drift` exits 0 (must be a no-op — this plan edits no contract).
- `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude` is a clean no-op (no `harness/` source doc touched).
- `uv run pytest tools/harness_lint -q` exits 0 (GEN-04 core→example independence).
</verification>

<success_criteria>
- SC-1: every directory-shaped destination in `DIRECTORY_SHAPED_DESTINATIONS` — all three classes, including the trailing-slash rows no resolve-based check can see — refuses cleanly with the documented exit code and a diagnostic naming the destination; no `Traceback` at the CLI boundary; legitimate file destinations are still allowed.
- SC-2: `check_valid` returns `False` and never raises for every row of `CORRUPT_APPROVAL_CASES`.
- SC-4: both tables were authored and RED-confirmed in Task 1 before either fix landed, with verbatim failure messages recorded in the SUMMARY.
- SC-5 (this plan's share): this plan's own three test files green in flight; no constitution-plane edit. The full-suite / drift / emit / GEN-04 fan-in is the phase-closing commit's gate, run once after both wave-1 plans land.
</success_criteria>

<output>
Create `.planning/phases/27.2-adoption-apply-robustness-clean-refusal-completeness-regress/27.2-01-SUMMARY.md` when done. It MUST contain:

- a `## RED evidence (SC-4)` section with the verbatim pre-fix failure output for both the WR-05 and
  the WR-06 tables, including a trailing-slash row explicitly;
- a `## Carried-forward residuals` section recording, unchanged and NOT acted on:
  `manifest.schema.json` still admits `"."`/`"./"`/`"a/"` (refusal is runtime-only per D-08);
  27.1-REVIEW.md WR-08 (parent-directory symlink-swap TOCTOU); 27.1-REVIEW.md IN-02 (`fcntl.flock`
  NFS advisory-lock caveat); and `tools/hooks/secret_scan.py:44-47` pattern-list hardcoding.
</output>
