---
phase: 27.2-adoption-apply-robustness-clean-refusal-completeness-regress
verified: 2026-07-21T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: null
---

# Phase 27.2: Adoption Apply Robustness — Verification Report

**Phase Goal:** Close the three findings left open by `27.1-REVIEW.md` — clean refusal for
directory-shaped destinations (WR-05), `check_valid` never raises on a corrupted `approval.json`
(WR-06), and a concurrency test that actually fails when `fcntl.flock()` is removed (WR-07).
**Verified:** 2026-07-21
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|---|---|---|
| SC-1 | WR-05 closed — every directory-shaped destination refuses cleanly, no traceback | ✓ VERIFIED | `apply.py:147-196` — three new refusals at the `refuse_unsafe_destination` choke point, all raising the existing `PathEscapeError` (D-02). `DIRECTORY_SHAPED_DESTINATIONS` (`test_constitution_refusal.py:190-204`) covers all D-03 classes — `"."`, `"./"`, `""`, `"a/"`, `"newdir/"`, `"a/b/."`, `"src/"`, `"src"`, `"./src"` — plus `symlink_to_root`, and each row pins `expected_guard` so a row cannot drift onto a different check. The review-added ancestor-is-a-file shape is a separate table (`NON_DIRECTORY_ANCESTOR_DESTINATIONS`) closed by the `target_path.parents` walk at `apply.py:184-191`. CLI boundary asserted traceback-free at `test_cli.py:599-657`. |
| SC-2 | WR-06 closed — `check_valid` returns structured invalid, never raises, across the full corruption table | ✓ VERIFIED | `approval.py:206-223` — the guarded region now spans `path.read_bytes().decode("utf-8")`, `json.loads`, and the three subscripts read eagerly into one tuple; catch tuple `(OSError, json.JSONDecodeError, UnicodeDecodeError, KeyError, TypeError)`. `CORRUPT_APPROVAL_CASES` has 12 rows: invalid JSON, `invalid_utf8`, `valid_document_utf16`, three non-object rows, three missing-key-in-turn rows, three wrong-type rows. **Try scope is narrow:** `_recompute_draft_hash` keeps its own `except OSError`, and `_current_task_revision`/`_current_git_ref` sit outside every `try` — `test_check_valid_lets_a_task_control_defect_surface` pins that a CAS-layer `KeyError` still propagates rather than being misreported as a stale approval, and `test_check_valid_propagates_an_environment_fault` pins `AdoptionApprovalRefused`. |
| SC-3 | WR-07 closed — the concurrency test detects removal of the control | ✓ VERIFIED | **Independently re-proven by the verifier, not accepted from the SUMMARY.** Ran the *positive* test with `fcntl.flock` neutered process-wide via an external pytest plugin (no repo edit, no barrier, no in-test patch): `test_concurrent_marker_merge_does_not_lose_writes` **FAILED** with `max_concurrent=2 (expected 1); events=[('enter','## Block B'),('enter','## Block A'),('exit','## Block B'),('exit','## Block A')]`. Unpatched it passes. `_assert_mutual_exclusion` (`test_atomic_apply.py:324-341`) is the sole judgement in the positive test (line 355) and in the negative control under `pytest.raises(AssertionError)` (line 383). The `.lock`-existence assertion is deleted; `grep lock_path.is_file` returns nothing, and a section comment at lines 267-270 forbids restoring it. `test_marker_merge_acquires_exclusive_flock` adds a timing-independent `LOCK_EX` spy. |
| SC-4 | Adversarial-input table authored BEFORE each control-shaped fix, RED run recorded | ✓ VERIFIED | Commit order is structural evidence: `fa5b392 test(27.2-01): add RED adversarial-input tables` precedes `2f4d1a1` (WR-05 fix) and `970f5b1` (WR-06 fix). **Two claimed RED runs reconciled against pre-fix code:** (a) `git show 85948fc:tools/adoption_apply/approval.py` has `json.loads` at line **181** outside the `try`, with `stored["draft_hash"]` at **190** and `stored["git_ref"]` at **192** — exactly the line numbers and exception types in the claimed `KeyError: 'draft_hash'` / `KeyError: 'git_ref'` / `TypeError: list indices must be integers` output. (b) pre-fix `refuse_unsafe_destination` contained only the absolute/`..` structural check and went straight to `relative_to` — no last-segment, root-equality, `is_dir()` or ancestor check — so `DID NOT RAISE` for all nine rows is the only possible result, and the pre-fix `cli.py` `except` tuple (`ConstitutionRefusal, ConcurrentDriftError, UnknownDispositionError, PathEscapeError, SymlinkRefusal`) does not name `IsADirectoryError`, so the CLI traceback claim holds. Both claims are real. The review-fix pass in `27.2-FIXES.md` extends the same discipline with mutation evidence per finding (`if target_path == ...` → `if False:`; dropping `KeyError` from the tuple → 3 rows red; appending `: {exc}` to the diagnostic → 9 of 12 rows red). |
| SC-5 | Full gate green at the closing commit | ✓ VERIFIED | All four gates run by the verifier — see Behavioral Spot-Checks below. No constitution-plane edit: `git diff --name-only 85948fc..HEAD` touches only `.planning/` and `tools/adoption_apply/`. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `tools/adoption_apply/apply.py` | directory-shaped + ancestor refusals at the choke point | ✓ VERIFIED | 3 refusal branches + parents walk, all `PathEscapeError`; docstring steps 1-6 updated |
| `tools/adoption_apply/approval.py` | `check_valid` never raises on a corrupt document | ✓ VERIFIED | widened `try`, explicit UTF-8 decode, no-content-leak diagnostic |
| `tools/adoption_apply/cli.py` | backstop `except` tuple | ✓ VERIFIED | adds `IsADirectoryError`, `CollisionError`, `FileExistsError`/`NotADirectoryError`, commented as backstop-not-guard |
| `tools/adoption_apply/tests/test_constitution_refusal.py` | 10-row directory-shaped table + ancestor table | ✓ VERIFIED | each row carries `expected_guard` |
| `tools/adoption_apply/tests/test_approval_invalidation.py` | 12-row corruption table + scope tests | ✓ VERIFIED | includes `invalid_utf8`, `valid_document_utf16` |
| `tools/adoption_apply/tests/test_cli.py` | CLI-boundary no-traceback rows | ✓ VERIFIED | parametrized, asserts guard text not bare destination substring |
| `tools/adoption_apply/tests/test_atomic_apply.py` | mutual-exclusion observer + negative control | ✓ VERIFIED | production `apply.py` untouched by plan 02 |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `cli.py::_cmd_apply` | `apply.refuse_unsafe_destination` | `apply_manifest` → `apply_disposition` choke point | WIRED — CLI subprocess test observes exit 1 + guard text |
| `_assert_mutual_exclusion` | positive test AND negative control | shared helper, both call sites verified | WIRED (`grep` → 4 hits: def, positive call, docstring, `pytest.raises` call) |
| `check_valid` corruption handler | `cli.py` exit 4 | returns `False`, no new failure channel (D-04) | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full non-example suite | `uv run pytest -q` | **1163 passed**, 7 snapshots passed, exit 0 | ✓ PASS |
| Contract drift | `uv run python -m tools.contract_drift.drift` | `contract-drift: OK — live manifest matches the committed baseline`, exit 0 | ✓ PASS |
| Harness lint / GEN-04 | `uv run pytest tools/harness_lint -q` | **300 passed** | ✓ PASS |
| Emit round-trip | `uv run python -m tools.harness_emit` then `git status --porcelain .opencode .claude opencode.json` | 95 artifacts emitted, **no diff** | ✓ PASS |
| Adoption suite | `uv run pytest tools/adoption_apply -q` | **100 passed** | ✓ PASS |
| SC-3 mutation (verifier-run) | positive test with `fcntl.flock` neutered via external plugin | **1 failed** on `max_concurrent=2` | ✓ PASS (test correctly goes red) |
| Constitution plane | `git diff --name-only 85948fc..HEAD` | only `.planning/`, `tools/adoption_apply/` | ✓ PASS |
| Model identifier | `git log 85948fc..HEAD --format=%B` + code diff scan | no matches | ✓ PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | no `TODO`/`FIXME`/`XXX`/`TBD`/`HACK`/`PLACEHOLDER` added anywhere in the phase diff | — | none |
| `tools/adoption_apply/cli.py` | 29 | ruff `I001` unsorted import block | ℹ️ Info | **Pre-existing** — import block verified byte-identical to `85948fc` (diff of the import lines is empty). Recorded in both `27.2-01-SUMMARY.md` and `27.2-FIXES.md`. Not introduced here. |

### Residual Tracking

Every carried-forward residual named in the task brief is recorded durably:

| Residual | Recorded in |
|---|---|
| AD-01 — `BrokenBarrierError` legibility under CI load | `27.2-FIXES.md` Residuals |
| AD-02 — `monkeypatch` on the singleton `fcntl` module (xdist hazard) | `27.2-FIXES.md` Residuals |
| D-04 deviation — `check_valid` returns bare `bool`, not a reason string | `27.2-FIXES.md` AD-03 section, documented as an accepted deviation with rationale (`check_valid` was always `-> bool`, so D-04's premise was factually wrong) |
| Pre-existing `I001` | `27.2-01-SUMMARY.md` + `27.2-FIXES.md`, byte-identity re-confirmed here |
| 27.1 IN-02 — advisory flock over NFS | both SUMMARYs + `27.2-FIXES.md` |
| 27.1 WR-08 — parent-dir symlink-swap TOCTOU | both SUMMARYs + `27.2-FIXES.md` |
| `tools/hooks/secret_scan.py:44-47` hardcoded patterns | `27.2-CONTEXT.md` `<deferred>`, both SUMMARYs, `27.2-FIXES.md` |
| `manifest.schema.json` still admits `"."`/`"./"`/`"a/"` (runtime-only closure per D-08) | both SUMMARYs + `27.2-FIXES.md` |

### Human Verification Required

None. Every success criterion resolved by executing code, and no PLAN carried a deferred
`<human-check>` block.

### Gaps Summary

No gaps. All five success criteria are verified against the codebase, not against the summaries.
The two independent checks that mattered most both held: the concurrency test genuinely goes red
when `flock` is neutered (re-run by the verifier without the negative control's barrier, so the
positive test's own detection power — not just the helper's — is proven), and both spot-checked
SC-4 RED claims reconcile exactly with the pre-fix source at `85948fc`, down to the line numbers.

---

_Verified: 2026-07-21_
_Verifier: gsd-verifier_
