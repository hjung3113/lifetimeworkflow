---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/harness/adoption/inventory.schema.json
  - contracts/.hashes/manifest.json
  - docs/reference/inventory.md
  - .memory/derived/contracts-index.md
autonomous: false
requirements: [ADOPT-01]
gap_closure: true

must_haves:
  truths:
    - "26-VERIFICATION.md gap 1 (ADOPT-01): inventory.schema.json has a record kind for schema surfaces and a record kind for CODEOWNERS surfaces — of the six ADOPT-01/26-CONTEXT.md surface categories (schema, spec, doc, ADR, AGENTS, CODEOWNERS, CI) the shipped schema only had a slot for four (doc/ADR/AGENTS via documentation_surfaces, CI via ci_surfaces); this plan adds the missing two slots so detect.py (Plan 26-05) has somewhere to write"
    - "Revision fix (checker BLOCKER 1): schema_surfaces/codeowners_surfaces are added as OPTIONAL properties in THIS plan (present in `properties`, absent from top-level `required`) — never required until Plan 26-05's Task 1 promotes them in the SAME task that first wires scan.py to populate them, so there is no wave where `required` demands a key `build_inventory()` does not yet produce. The full repo test suite (`uv run pytest -q`, including `tools/adoption_scan/tests/test_detect.py::test_inventory_validates_against_schema`) stays GREEN immediately after this plan lands, before Plan 26-05 ever runs — proven as an explicit acceptance criterion below, per 26-VALIDATION.md's per-wave sampling contract"
    - "The contract-drift gate is green with inventory.schema.json's new hash tracked in the rebaselined contracts/.hashes/manifest.json — no other schema's hash changes"
    - "The committed-derived plane (docs/reference/inventory.md, .memory/derived/contracts-index.md) is regenerated in this SAME wave so stale-derived does not go red on a later, unrelated commit"
  artifacts:
    - path: "contracts/harness/adoption/inventory.schema.json"
      provides: "schema_surfaces and codeowners_surfaces top-level array properties (both $ref: #/$defs/surfaceRecord), OPTIONAL in this plan — NOT added to the top-level required list here (that happens in 26-05)"
      contains: "schema_surfaces"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined JCS SHA-256 for inventory.schema.json only"
      contains: "contracts/harness/adoption/inventory.schema.json"
    - path: "docs/reference/inventory.md"
      provides: "Regenerated Diataxis reference page reflecting the two new (optional) properties"
  key_links: []
---

<objective>
Close 26-VERIFICATION.md gap 1's schema half: extend the human-ratified
`contracts/harness/adoption/inventory.schema.json` with two missing surface-record slots —
`schema_surfaces` and `codeowners_surfaces` — so ADOPT-01's literal six-category list (schema,
spec, doc, ADR, AGENTS, CODEOWNERS, CI) has somewhere to land, rebaseline the contract-hash
manifest, and regenerate the committed-derived plane in the same wave. Per the checker's revision
feedback, both properties land as OPTIONAL here — NOT required — so the live test suite (which
already strictly schema-validates `scan.build_inventory()`'s current, unmodified output) does not
go red in the window between this wave and Plan 26-05's implementation wave.

Purpose: this is purely the constitution-plane contract change, staged to never break the suite.
Plan 26-05 (wave 2, depends on this plan) implements `detect.py`'s
`detect_schema_surfaces()`/`detect_codeowners_surfaces()`, wires them into `scan.py`, AND — in that
SAME task — promotes both properties to `required`, so "optional-but-present" and
"scan.py-populates-them" land atomically. This plan alone only makes the properties nameable; it
does not yet require them.
Output: `contracts/harness/adoption/inventory.schema.json` (extended, both new properties
optional), a rebaselined `contracts/.hashes/manifest.json`, and a fresh
`docs/reference/inventory.md` + `.memory/derived/contracts-index.md`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VERIFICATION.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VALIDATION.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-01-PLAN.md
@contracts/harness/adoption/inventory.schema.json
@tools/adoption_scan/tests/test_detect.py
@tools/contract_hash/hash.py
@tools/contract_drift/drift.py
@tools/docs_sync/generate.py
@tools/memory_regen/contracts_index.py

<interfaces>
<!-- The exact surfaceRecord $def already in inventory.schema.json — the new properties reuse it
verbatim via $ref, they do NOT define a new record shape. -->
```json
"surfaceRecord": {
  "type": "object",
  "additionalProperties": false,
  "required": ["target", "classification", "evidence"],
  "properties": {
    "target": { "type": "string", "minLength": 1 },
    "classification": { "$ref": "#/$defs/classification" },
    "evidence": { "type": "array", "minItems": 0, "items": { "$ref": "#/$defs/evidenceRef" } },
    "rationale": { "type": "string" }
  }
}
```
<!-- The exact test this plan must NOT break — tools/adoption_scan/tests/test_detect.py:50-55,
already committed, runs on every `uv run pytest`. It validates the LIVE build_inventory() output,
which after this plan still does NOT contain schema_surfaces/codeowners_surfaces keys (scan.py is
untouched here) — Draft202012Validator only rejects a MISSING key when it is in `required`, so
leaving both new properties out of `required` is what keeps this test green: -->
```python
def test_inventory_validates_against_schema(tmp_minirepo: Path, repo_root: Path) -> None:
    schema = json.loads((repo_root / "contracts/harness/adoption/inventory.schema.json").read_text())
    inventory = scan.build_inventory(tmp_minirepo)
    errors = list(Draft202012Validator(schema).iter_errors(inventory))
    assert not errors, [error.message for error in errors]
```
Rebaseline: `write_manifest(manifest_path=MANIFEST_PATH, contracts_dir=CONTRACTS_DIR) -> Path` in
`tools/contract_hash/hash.py`; CLI `uv run python -m tools.contract_hash.hash --write`. Verify:
`uv run python -m tools.contract_drift.drift` (exit 0 iff clean). Derived regen:
`uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema_surfaces + codeowners_surfaces (OPTIONAL) to inventory.schema.json, rebaseline</name>
  <files>contracts/harness/adoption/inventory.schema.json, contracts/.hashes/manifest.json</files>
  <read_first>
    contracts/harness/adoption/inventory.schema.json (the exact file being extended — read in full,
      note the existing `documentation_surfaces`/`ci_surfaces`/`test_surfaces`/
      `candidate_process_boundaries` properties as the pattern to copy, and note the top-level
      `required` array this task must NOT add either new key to),
    tools/adoption_scan/tests/test_detect.py (`test_inventory_validates_against_schema` — the
      existing test this task must keep green by construction),
    26-VERIFICATION.md gap 1 (`gaps[0]`) — the exact artifacts/missing list this task closes,
    26-RESEARCH.md "Schema style template" and "Pitfall 2: Drift gate is RED between authoring and
      rebaselining"
  </read_first>
  <action>
    Edit `contracts/harness/adoption/inventory.schema.json`: add two new top-level `properties`
    entries, `schema_surfaces` and `codeowners_surfaces`, each `{"type": "array", "items": {"$ref":
    "#/$defs/surfaceRecord"}}` — identical shape to the existing `ci_surfaces`/`test_surfaces`
    properties, reusing the already-defined `surfaceRecord` $def (do NOT define a new $def). Do
    **NOT** add either name to the top-level `required` array — leave `required` exactly as it is
    today (this is the revision fix for checker BLOCKER 1: promoting to `required` happens in Plan
    26-05's Task 1, in the SAME task that first makes `scan.py` populate both keys, so there is
    never a wave where `required` demands a key nothing yet produces). Update the schema's top-level
    `description` string to mention `schema_surfaces` and `codeowners_surfaces` alongside the
    existing surface categories it already lists, and note inline that both are staged optional
    pending Plan 26-05's promotion. Keep 2-space indent, LF line endings, no BOM. Writing to
    `contracts/**` is a constitution-plane write `contract_guard` denies unless the
    human-authorized `GOLDEN_APPROVE_HUMAN` token is present or this is an equivalent human-present
    write — do NOT fabricate the token; if denied, pause and request human authorization (same
    posture as 26-01-PLAN.md Task 1). Then run `uv run python -m tools.contract_hash.hash --write`
    to rebaseline `contracts/.hashes/manifest.json` — the `**/*.schema.json` glob picks up the
    modified file automatically; never hand-edit the hash JSON. Then run
    `uv run python -m tools.contract_drift.drift` and confirm exit 0. Finally run
    `uv run pytest -q` (the FULL repo suite, not just `tools/adoption_scan`) and confirm it is
    green — this is the direct proof that adding the two properties as optional did not regress
    `test_inventory_validates_against_schema` or any other schema-conformance test.
  </action>
  <verify>
    <automated>python -m json.tool contracts/harness/adoption/inventory.schema.json > /dev/null && uv run python -m tools.contract_drift.drift && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    `contracts/harness/adoption/inventory.schema.json` is valid JSON; its `properties` object
    contains both `"schema_surfaces"` and `"codeowners_surfaces"` (both `items` = `{"$ref":
    "#/$defs/surfaceRecord"}`); its top-level `required` array does **NOT** contain either name
    (grep `"required"` block confirms the same member list as before this task — no new entries).
    `uv run python -m tools.contract_drift.drift` exits 0. `git diff --stat contracts/.hashes/manifest.json`
    shows exactly one changed line/key: `contracts/harness/adoption/inventory.schema.json` — no
    other schema's hash changes. `uv run pytest -q` exits 0 (full suite, including
    `tools/adoption_scan/tests/test_detect.py::test_inventory_validates_against_schema`) —
    proving the optional-only addition is non-breaking.
  </acceptance_criteria>
  <done>inventory.schema.json has schema_surfaces + codeowners_surfaces properties (both OPTIONAL, both surfaceRecord arrays); contract-drift is green with only inventory.schema.json's hash rebaselined; the full test suite stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human ratification of the constitution-plane schema extension</name>
  <files>contracts/harness/adoption/inventory.schema.json, contracts/.hashes/manifest.json</files>
  <read_first>.github/CODEOWNERS (the /contracts/ routing + advisory-CODEOWNERS + solo-approval caveats), .planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-01-PLAN.md (Task 2's checkpoint, the precedent this checkpoint mirrors)</read_first>
  <action>
    This is a BLOCKING human checkpoint — the agent does NOT self-ratify. The agent presents the
    `inventory.schema.json` diff (two new OPTIONAL properties, neither added to `required`) plus the
    rebaselined `contracts/.hashes/manifest.json` diff for human review, then PAUSES. A human
    confirms the two new properties correctly close 26-VERIFICATION.md gap 1's inventory-schema
    half, that NEITHER is required yet (by design — Plan 26-05 promotes them), and that the
    rebaseline touches ONLY `inventory.schema.json`'s hash, then ratifies via the standard
    constitution-plane path (CODEOWNERS PR review or an explicit human-supplied write — never agent
    self-approval or `HARNESS_DEV_BYPASS` used to self-land the ratification).
  </action>
  <what-built>
    `inventory.schema.json` extended with `schema_surfaces`/`codeowners_surfaces` (both OPTIONAL
    `surfaceRecord` arrays, staged for Plan 26-05 to promote to required) plus a rebaselined
    `contracts/.hashes/manifest.json`.
  </what-built>
  <how-to-verify>
    1. Confirm `uv run python -m tools.contract_drift.drift` reports OK.
    2. Confirm `uv run pytest -q` is green (full suite) — the staged-optional design should not
       have broken anything.
    3. Review the diff under `contracts/harness/adoption/inventory.schema.json` and
       `contracts/.hashes/manifest.json` — CODEOWNERS-gated (`.github/CODEOWNERS` routes
       `/contracts/` to the repo owner), ratified at PR merge, same as 26-01/ADR-0004/0005/0009.
    4. Confirm no agent self-approved this change (no fabricated `GOLDEN_APPROVE_HUMAN` token
       committed anywhere).
  </how-to-verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0; `uv run pytest -q` exits 0; `git diff --stat contracts/.hashes/manifest.json` shows exactly one changed schema hash.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: uv run python -m tools.contract_drift.drift && uv run pytest -q</human-check>
  </verify>
  <resume-signal>Type "approved" once the constitution-plane diff has been reviewed, or describe required changes.</resume-signal>
  <done>The extended inventory.schema.json (properties optional) and the rebaselined manifest are human-ratified via the standard constitution-plane path, not agent self-approval.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the committed-derived plane</name>
  <files>docs/reference/inventory.md, .memory/derived/contracts-index.md</files>
  <read_first>
    tools/docs_sync/generate.py (iter_schemas glob — fully dynamic, picks up the modified schema
      with zero code change), tools/memory_regen/contracts_index.py,
    .github/workflows/ci.yml (the `stale-derived` job commands this task must reproduce locally),
    26-RESEARCH.md "Pitfall 1: The stale-derived CI job REDs after adding the schemas"
  </read_first>
  <action>
    Run `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`.
    `docs_sync` regenerates `docs/reference/inventory.md` from the modified schema (the file already
    exists from 26-01; this run overwrites it with the two new, optional properties reflected).
    Commit the regenerated files in this same wave — never leave this for a later commit. Finish
    with a final full-suite check: `uv run pytest -q`.
  </action>
  <verify>
    <automated>uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git add -A -- docs/reference .memory/derived/contracts-index.md && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    The exact `stale-derived` CI command sequence exits 0 (no diff after regeneration).
    `docs/reference/inventory.md` mentions both `schema_surfaces` and `codeowners_surfaces`.
    `uv run pytest -q` exits 0 (full suite green at the end of this wave).
  </acceptance_criteria>
  <done>docs/reference/inventory.md and .memory/derived/contracts-index.md are regenerated and committed; the stale-derived CI mirror is clean; the full test suite is green at the end of Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent session → `contracts/harness/adoption/inventory.schema.json` | Constitution-plane write; denied by `contract_guard` unless human-authorized — never agent self-ratified. |
| `contract_hash --write` output → `contracts/.hashes/manifest.json` | Machine-computed baseline; must never be hand-edited. |
| `docs_sync`/`contracts_index` output → committed-derived plane | Machine-regenerated; CI's `stale-derived` job is the non-bypassable verify side. |
| This plan's schema edit ↔ the live test suite | The properties MUST land optional-only so `test_inventory_validates_against_schema` (already committed) does not go red before Plan 26-05 exists — the boundary this plan's whole staging design protects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Elevation of Privilege | `contracts/harness/adoption/inventory.schema.json` write path | accept (out-of-band) | Unchanged existing controls: `contract_guard` PreToolUse deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS `/contracts/`. Task 2 of this plan is an explicit `checkpoint:human-verify`. Residual risk pre-accepted per Phase-24's `T-24-02` precedent. |
| T-26-04-02 | Tampering | `contracts/.hashes/manifest.json` | mitigate | Rebaseline ONLY via `tools.contract_hash.hash --write` — never hand-edited; `git diff --stat` scoped to exactly one changed key is an explicit acceptance criterion. |
| T-26-04-03 | Repudiation (regression) | Live test suite between this plan and Plan 26-05 | mitigate | Properties staged OPTIONAL, never `required`, until Plan 26-05's Task 1 promotes them in the same task that populates them — `uv run pytest -q` is an explicit acceptance criterion on every task in this plan, closing checker BLOCKER 1. |
| T-26-04-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan. |
</threat_model>

<verification>
- `uv run python -m tools.contract_drift.drift` — green, exactly one schema hash changed.
- `uv run pytest -q` — full repo suite green (explicit per-task acceptance criterion; closes checker BLOCKER 1).
- `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git diff --exit-code -- docs/reference .memory/derived/contracts-index.md` — clean.
</verification>

<success_criteria>
- `inventory.schema.json` has `schema_surfaces` and `codeowners_surfaces` as OPTIONAL `surfaceRecord` arrays (not required).
- Contract-drift is green; derived plane is fresh; the human-ratification checkpoint is not bypassed.
- The full repo test suite is green throughout this wave — no red window between this plan and Plan 26-05.
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-04-SUMMARY.md` when done
</output>
