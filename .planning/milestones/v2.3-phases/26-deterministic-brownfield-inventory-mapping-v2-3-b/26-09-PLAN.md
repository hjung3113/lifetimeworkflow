---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 09
type: execute
wave: 3
depends_on: ["26-08"]
files_modified:
  - tools/adoption_scan/plan.py
  - tools/adoption_scan/detect.py
  - tools/adoption_scan/tests/test_plan_classification.py
  - tools/adoption_scan/tests/test_detect.py
  - tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr
autonomous: true
requirements: [ADOPT-01, ADOPT-02]
gap_closure: true

must_haves:
  truths:
    - "26-VERIFICATION.md's remaining ADOPT-02 gap (WR-05), closed: plan.py::classify() walks inventory['schema_surfaces'] and emits one 'contract-candidate' proposal per schema-file evidence pointer; generate_questions() then emits a real 'contract-candidate' question — the question/proposal kind that was permanently dead code (identical defect class to the codeowners gap 26-05 already closed) now fires on a real scan of a repo with *.schema.json files under contracts/"
    - "WR-06, folded in: detect_codeowners_surfaces() recognizes all three GitHub-honored CODEOWNERS locations (CODEOWNERS, .github/CODEOWNERS, docs/CODEOWNERS), each producing its own surfaceRecord when present — a brownfield target using the root or docs/ location no longer silently reports zero ownership surfaces and skips the blocking codeowners-ownership question"
    - "A real self-scan of this harness's own contracts/ directory (11+ real *.schema.json files) produces one contract-candidate proposal + one contract-candidate question PER schema file (not one lumped record for the whole directory) — proven by a real-repo-scan test, not only the synthetic fixture"
  artifacts:
    - path: "tools/adoption_scan/plan.py"
      provides: "classify() gains a schema_surfaces walk loop emitting per-evidence-pointer 'contract-candidate' proposals; _QUESTION_KIND_BY_PROPOSAL_KIND already maps 'contract-candidate'->'contract-candidate' (no new dict entry needed, only the classify()-side walk was missing)"
      contains: "schema_surfaces"
    - path: "tools/adoption_scan/detect.py"
      provides: "detect_codeowners_surfaces() matches CODEOWNERS / .github/CODEOWNERS / docs/CODEOWNERS, one surfaceRecord per found path"
  key_links:
    - from: "tools/adoption_scan/plan.py::classify"
      to: "tools/adoption_scan/scan.py::build_inventory (via inventory['schema_surfaces'])"
      via: "direct dict walk over inventory.get('schema_surfaces', []), mirroring the existing codeowners_surfaces walk"
      pattern: "schema_surfaces"
    - from: "tools/adoption_scan/detect.py::detect_codeowners_surfaces"
      to: "_CODEOWNERS_PATHS frozenset"
      via: "path membership check against all three GitHub-honored locations"
      pattern: "CODEOWNERS"
---

<objective>
Close the last two open findings from 26-VERIFICATION.md/26-REVIEW.md's gap-closure round: WR-05
(`schema_surfaces` detected by `detect.py`, required by the inventory schema, but never consumed by
`plan.py::classify()` — the `contract-candidate` proposal/question kind stays permanently
unreachable, the exact defect class Plan 26-05 already fixed for `codeowners_surfaces`) and WR-06
(`detect_codeowners_surfaces` only recognizes `.github/CODEOWNERS`, missing the two other locations
GitHub honors: root `CODEOWNERS` and `docs/CODEOWNERS`).

Purpose: ADOPT-02 requires every proposed contract candidate to reach a classified proposal/question
— a detected-but-unconsumed inventory signal is a silent gap identical in shape to the one this
phase's prior gap-closure round already fixed once (codeowners); leaving `schema_surfaces` unwired
means the phase's own precedent for "close this defect class" is inconsistently applied. WR-06 is
folded in because a missed CODEOWNERS location removes a BLOCKING pre-apply question — the failure
direction that actually matters (silently reporting "no ownership surface" for a repo that has one).
Output: `plan.py`'s `classify()` gains the `schema_surfaces` walk; `detect.py`'s
`detect_codeowners_surfaces()` recognizes all three locations; new tests in both `test_detect.py`
and `test_plan_classification.py`; a refreshed committed snapshot (plan section only).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VERIFICATION.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-REVIEW.md
@tools/adoption_scan/plan.py
@tools/adoption_scan/detect.py
@tools/adoption_scan/scan.py
@tools/adoption_scan/tests/test_plan_classification.py
@tools/adoption_scan/tests/test_detect.py

<interfaces>
<!-- plan.py's existing codeowners_surfaces walk (Plan 26-05's precedent) — mirror this SHAPE
exactly for schema_surfaces, verbatim from tools/adoption_scan/plan.py's classify(): -->
```python
for entry in inventory.get("codeowners_surfaces", []):
    proposals.append(
        {
            "id": f"codeowners/{entry['target']}",
            "kind": "codeowners",
            "classification": "unknown",
            "target": entry["target"],
            "evidence": entry["evidence"],
        }
    )
```
`_QUESTION_KIND_BY_PROPOSAL_KIND` already contains `"contract-candidate": "contract-candidate"`
(line ~39) and `_GROUP_BY_QUESTION_KIND["contract-candidate"] = "topology"` (line ~50) — both
already wired from Plan 26-01/03; `"contract-candidate"` is NOT in `_BLOCKING_KINDS` (deliberately
non-blocking, unlike `codeowners-ownership`) — do not add it there. `_question_text()`'s
`templates` dict already has a `"contract-candidate"` entry (line ~86). The ONLY missing link is the
`classify()`-side walk emitting the proposal in the first place — mirror the shape above, but note
`detect_schema_surfaces()` returns AT MOST ONE `surfaceRecord` per repo (target =
`"contracts/**/*.schema.json"`, one record whose `evidence` list has one entry per matching schema
file) rather than one record per file — so `classify()` must walk `entry["evidence"]` (not the
outer `schema_surfaces` list) to emit ONE proposal PER SCHEMA FILE, per 26-REVIEW.md WR-05's
suggested fix:
```python
for entry in inventory.get("schema_surfaces", []):
    for ref in entry["evidence"]:
        proposals.append({
            "id": f"contract-candidate/{ref['path']}",
            "kind": "contract-candidate",
            "classification": "unknown",
            "target": ref["path"],
            "evidence": [ref],
        })
```

<!-- detect.py's existing single-location CODEOWNERS matcher — the function this task widens: -->
```python
def detect_codeowners_surfaces(included: list[dict]) -> list[dict]:
    codeowners_entries = [
        entry for entry in included
        if PurePosixPath(entry["path"]) == PurePosixPath(".github/CODEOWNERS")
    ]
    if not codeowners_entries:
        return []
    return [_surface(".github/CODEOWNERS", codeowners_entries, "observed")]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: plan.py schema_surfaces wiring (WR-05) + detect.py multi-path CODEOWNERS (WR-06)</name>
  <files>tools/adoption_scan/plan.py, tools/adoption_scan/detect.py, tools/adoption_scan/tests/test_plan_classification.py, tools/adoption_scan/tests/test_detect.py</files>
  <read_first>
    tools/adoption_scan/plan.py `classify()` (full function — the `codeowners_surfaces` walk loop
      to mirror, per `<interfaces>` above),
    tools/adoption_scan/detect.py `detect_schema_surfaces()` and `detect_codeowners_surfaces()`
      (both full functions — the former's per-repo-not-per-file record shape is why `classify()`
      must walk `entry["evidence"]`; the latter is this task's WR-06 edit target),
    26-VERIFICATION.md gap list / 26-REVIEW.md WR-05 + WR-06 sections (exact defect descriptions and
      suggested fixes this task implements),
    tools/adoption_scan/tests/test_plan_classification.py `test_codeowners_ownership_question_fires`
      (the exact test shape to mirror for the new `contract-candidate` test)
  </read_first>
  <behavior>
    - `classify()` in `plan.py` gains a new walk loop, placed immediately after the existing
      `codeowners_surfaces` loop: for each `entry` in `inventory.get("schema_surfaces", [])`, for
      each `ref` in `entry["evidence"]`, append `{"id": f"contract-candidate/{ref['path']}", "kind":
      "contract-candidate", "classification": "unknown", "target": ref["path"], "evidence":
      [ref]}` — `classification` is ALWAYS `"unknown"` (whether a schema is a TRACKED contract is a
      human call, per D-02). No changes to `_QUESTION_KIND_BY_PROPOSAL_KIND`,
      `_GROUP_BY_QUESTION_KIND`, `_BLOCKING_KINDS`, or `_question_text()` — all already carry the
      needed `"contract-candidate"` entries from a prior plan.
    - `detect_codeowners_surfaces()` in `detect.py`: replace the single-path equality check with a
      membership check against a new module-level constant
      `_CODEOWNERS_PATHS: frozenset[str] = frozenset({"CODEOWNERS", ".github/CODEOWNERS",
      "docs/CODEOWNERS"})`. For each `entry` in `included` whose `PurePosixPath(entry["path"]).as_posix()`
      (or the raw `entry["path"]` string, since these are already repo-relative POSIX per the
      inventory contract) is a member of `_CODEOWNERS_PATHS`, emit ONE `_surface(entry["path"],
      [entry], "observed")` per matching path found (mirrors `detect_documentation_surfaces()`'s
      per-nested-AGENTS.md per-file precedent — one record per distinct CODEOWNERS location, not one
      lumped record). Sort the returned list by `target` for determinism (mirror
      `detect_documentation_surfaces()`'s `sorted(records, key=lambda record: record["target"])`
      pattern). Update the function's docstring to state all three recognized locations.
    - Test (`test_plan_classification.py`): add `test_contract_candidate_question_fires` — builds a
      minimal inventory dict with one `schema_surfaces` entry whose `evidence` list has exactly ONE
      `evidenceRef` (e.g. `{"path": "contracts/widget.schema.json", "sha256": "0"*64, "size": 10}`),
      calls `plan.build_plan(inventory)`, and asserts exactly one proposal with `kind ==
      "contract-candidate"` and `classification == "unknown"` and `target ==
      "contracts/widget.schema.json"` exists, AND exactly one question with `kind ==
      "contract-candidate"` exists in `built["questions"]` with `blocking == False` (non-blocking,
      per the `<interfaces>` note that `contract-candidate` is deliberately NOT in
      `_BLOCKING_KINDS`).
    - Test (`test_plan_classification.py`): add `test_contract_candidate_proposal_per_schema_file` —
      builds an inventory with one `schema_surfaces` entry whose `evidence` list has TWO
      `evidenceRef`s (two distinct schema file paths), calls `plan.build_plan(inventory)`, and
      asserts exactly TWO `contract-candidate` proposals exist (one per evidence pointer, not one
      lumped proposal for the whole `schema_surfaces` entry) — the specific per-file granularity
      claim this task's must-have makes.
    - Test (`test_plan_classification.py`): add a real-repo-scan assertion (not only the synthetic
      fixture) — using the `repo_root` fixture from `tools/adoption_scan/tests/conftest.py`, call
      `scan.build_inventory(repo_root)` then `plan.build_plan(inventory)` against THIS harness
      checkout's own real `contracts/` tree, and assert `len([p for p in built["proposals"] if
      p["kind"] == "contract-candidate"])` equals the real, live count of
      `contracts/**/*.schema.json` files (computed via `sorted((repo_root /
      "contracts").rglob("*.schema.json"))`, mirroring `test_catalog_covers_real_contract_schemas`'s
      live-count pattern in `test_dispositions.py` — never a hardcoded literal).
    - Test (`test_detect.py`): add `test_codeowners_surface_root_location` and
      `test_codeowners_surface_docs_location` — each constructs a synthetic `included` list with one
      entry at `"CODEOWNERS"` (root) or `"docs/CODEOWNERS"` respectively (fabricated `sha256`/`size`,
      no fixture-tree dependency needed), calls `detect.detect_codeowners_surfaces(included)`
      directly, and asserts a `surfaceRecord` with that exact `target` is returned.
    - Test (`test_detect.py`): the existing `test_codeowners_surface_observed` (driven by
      `tmp_minirepo`'s `.github/CODEOWNERS` case (q)) continues to pass unchanged — proving the
      widened matcher still recognizes the `.github/CODEOWNERS` location the fixture already covers.
    - Refresh the committed snapshot: run `uv run pytest tools/adoption_scan/tests/test_snapshots.py
      -q --snapshot-update` and inspect the diff — it must show ONLY additions inside the
      `===== plan =====` section (a new `contract-candidate` proposal + question for
      `tmp_minirepo`'s existing `contracts/widget.schema.json` fixture case (p), previously
      unconsumed by `classify()`); the `===== inventory =====` section is untouched (neither
      `detect.py`'s `schema_surfaces` detection logic nor `scan.py` change in this task — only the
      CODEOWNERS matcher's *scope* changes, and `tmp_minirepo` has no root/docs CODEOWNERS fixture
      case to exercise that widening, so the inventory's `codeowners_surfaces` content is unchanged
      too); the `===== manifest =====` section must be untouched (Plan 26-07 already decoupled it
      from live/fixture content via the injected fixed catalog).
  </behavior>
  <action>
    Add the `schema_surfaces` walk loop to `classify()` exactly as specified. Widen
    `detect_codeowners_surfaces()` to the three-location `_CODEOWNERS_PATHS` set. Add all five new
    tests across the two test files. Refresh the committed snapshot and inspect the diff per the
    behavior notes above.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan -q && uv run python -m tools.contract_drift.drift && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q && uv sync --all-packages && git diff --exit-code uv.lock</automated>
  </verify>
  <acceptance_criteria>
    `tools/adoption_scan` suite passes, including all five new tests. A real scan of this repo's own
    `contracts/` tree produces exactly one `contract-candidate` proposal per real `*.schema.json`
    file under `contracts/` (live-count-matched, not hardcoded). `detect_codeowners_surfaces()`
    recognizes `CODEOWNERS`, `.github/CODEOWNERS`, and `docs/CODEOWNERS` as three independently
    testable locations. The refreshed `test_snapshots.ambr` diff touches only the `plan` section.
    `uv run python -m tools.contract_drift.drift` exits 0 (no contract touched by this plan). The
    GEN-04 guard stays green. `uv.lock` is unchanged.
  </acceptance_criteria>
  <done>plan.py::classify() emits a contract-candidate proposal per schema-file evidence pointer, closing the last permanently-dead-code question kind from this phase's gap-closure round; detect_codeowners_surfaces() recognizes all three GitHub-honored CODEOWNERS locations; full suite/drift/GEN-04/uv.lock all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `schema_surfaces` evidence -> `contract-candidate` proposal | Must never be promoted past `"unknown"` classification — whether a schema is a tracked, ratified contract is a human/CODEOWNERS-gated decision, never inferred from file existence alone. `classify()` hardcodes `classification: "unknown"` for every `contract-candidate` proposal, mirroring the same bright-line rule already enforced for `codeowners`/`agents-boundary`/`docs-destination`. |
| Widened `detect_codeowners_surfaces()` path set | Only EXISTENCE and repo-relative path of a CODEOWNERS file are recorded (D-02 observed); its ownership-mapping content is never parsed or interpreted as authority, at any of the three locations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09-01 | Elevation of Privilege | A `contract-candidate` proposal being promoted into an authoritative "tracked contract" record | mitigate (structural) | `classify()` hardcodes `classification: "unknown"` for every `contract-candidate` proposal emitted from `schema_surfaces`; no code path reads the schema file's own content to derive tracked/untracked status. `contract-candidate` is deliberately excluded from `_BLOCKING_KINDS` (non-blocking) — apply is never gated on answering it, only informed by it. |
| T-26-09-02 | Information Disclosure | A missed CODEOWNERS location silently reporting "no ownership surface" (WR-06) | mitigate | `_CODEOWNERS_PATHS` now covers all three GitHub-honored locations; `codeowners-ownership` stays in `_BLOCKING_KINDS`, so a real ownership surface at any of the three locations now correctly blocks apply until answered. |
| T-26-09-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_scan -q` — full suite green, including the five new tests and the real-repo-scan assertion.
- `uv run python -m tools.contract_drift.drift` — green (no contract touched by this plan).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` — GEN-04 guard green.
- `uv sync --all-packages && git diff --exit-code uv.lock` — lockfile unchanged.
</verification>

<success_criteria>
- `schema_surfaces` reaches a classified `contract-candidate` proposal/question on a real scan — the last permanently-dead-code defect class from this phase's gap-closure round is closed.
- `detect_codeowners_surfaces()` recognizes all three GitHub-honored CODEOWNERS locations.
- Full suite, contract-drift, GEN-04 guard, and `uv.lock` all stay green.
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-09-SUMMARY.md` when done
</output>
