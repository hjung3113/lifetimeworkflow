---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 08
type: execute
wave: 2
depends_on: ["26-07"]
files_modified:
  - contracts/harness/adoption/inventory.schema.json
  - contracts/.hashes/manifest.json
  - docs/reference/inventory.md
  - .memory/derived/contracts-index.md
  - tools/adoption_scan/tests/test_plan_classification.py
autonomous: false
requirements: [ADOPT-01, ADOPT-02]
gap_closure: true

must_haves:
  truths:
    - "26-VERIFICATION.md gap 2 (CR-03, ADOPT-01/ADOPT-02 internal consistency), closed: contracts/harness/adoption/inventory.schema.json's surfaceRecord.evidence requires minItems:1 (was 0), matching plan.schema.json's proposalRecord.evidence/questionRecord.evidence — no schema-valid inventory can construct a surfaceRecord with empty evidence, so build_plan() can never produce a proposal/question that fails plan.schema.json's own cardinality requirement"
    - "A schema-valid inventory constructed with evidence:[] on any surfaceRecord-shaped field now FAILS inventory.schema.json validation directly (the contradiction is closed at the source, not papered over downstream) — proven by a new regression test in test_plan_classification.py"
    - "The regression test asserts build_plan(inventory) validates against plan.schema.json for every surface-array shape inventory.schema.json permits (documentation_surfaces/ci_surfaces/test_surfaces/candidate_process_boundaries/schema_surfaces/codeowners_surfaces all populated with minimally-valid, non-empty-evidence entries) — the exact regression class 26-REVIEW.md CR-03 asked for"
    - "The schema, hash rebaseline, docs_sync regen, and memory_regen contracts-index regen land in ONE commit (checker WR-10 precedent from the prior review round — no intermediate red-suite window)"
  artifacts:
    - path: "contracts/harness/adoption/inventory.schema.json"
      provides: "surfaceRecord.evidence promoted from minItems:0 to minItems:1, description updated to state a surface with no evidence is never emitted (matching actual detect.py producer behavior)"
      contains: "\"minItems\": 1"
    - path: "tools/adoption_scan/tests/test_plan_classification.py"
      provides: "test_build_plan_validates_for_every_inventory_surface_shape (or equivalently named) — schema-validates build_plan(inventory) output against plan.schema.json for a maximally-populated, schema-conformant synthetic inventory"
  key_links:
    - from: "contracts/harness/adoption/inventory.schema.json"
      to: "contracts/harness/adoption/plan.schema.json"
      via: "surfaceRecord.evidence minItems:1 now matches proposalRecord.evidence/questionRecord.evidence minItems:1 — the same conceptual evidence-cardinality invariant, no longer contradicted across the two contracts"
      pattern: "minItems.*1"
---

<objective>
Close 26-VERIFICATION.md gap 2 (CR-03): reconcile `inventory.schema.json`'s `surfaceRecord.evidence`
(`minItems: 0`, "may be empty") with `plan.schema.json`'s `proposalRecord.evidence`/
`questionRecord.evidence` (both `minItems: 1`) by rebaselining the inventory contract to
`minItems: 1` — the reading that matches actual producer behavior, since no `detect.py` function
ever emits a surface record with empty evidence (every `_surface()` call site only fires when at
least one matching `included` entry exists). Land the schema edit, hash rebaseline, and derived-plane
regen atomically, human-ratified, with a regression test that proves `build_plan()` output validates
against `plan.schema.json` for every surface-array shape the (now-tightened) inventory schema
permits.

Purpose: a schema-valid inventory could previously crash `cli.main()` with exit 1 and zero artifacts
written — directly contradicting the phase goal's "agent-free, fully CI-testable" and "complete...
manifest" language. This plan closes the contradiction at its source (the contract), not by adding
defensive code downstream.
Output: a rebaselined `inventory.schema.json` + `contracts/.hashes/manifest.json` + refreshed
`docs/reference/inventory.md` + `.memory/derived/contracts-index.md`, plus a new cross-schema
regression test.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VERIFICATION.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-REVIEW.md
@contracts/harness/adoption/inventory.schema.json
@contracts/harness/adoption/plan.schema.json
@tools/adoption_scan/detect.py
@tools/adoption_scan/plan.py
@tools/adoption_scan/tests/test_plan_classification.py
@tools/contract_hash/hash.py
@tools/contract_drift/drift.py
@tools/docs_sync/generate.py
@tools/memory_regen/contracts_index.py

<interfaces>
<!-- The exact CR-03 repro from 26-REVIEW.md — the shape this plan's regression test generalizes
into a full-coverage assertion, verbatim: -->
```python
inv = {"target_ref": "x", "codeowners_surfaces":
       [{"target": ".github/CODEOWNERS", "classification": "observed", "evidence": []}]}
Draft202012Validator(plan_schema).iter_errors(plan.build_plan(inv))
# -> ['[] should be non-empty', '[] should be non-empty']
```
After this plan's fix, an inventory carrying `evidence: []` on ANY surfaceRecord-shaped field is
itself schema-INVALID against `inventory.schema.json` (fails at the source, one validation gate
earlier than before) — the regression test below proves the FORWARD direction instead: every
schema-valid inventory (i.e. one where every surface's evidence is non-empty, which is now the ONLY
schema-valid shape) produces a `build_plan()` output that itself validates against `plan.schema.json`.

<!-- Rebaseline / regen commands, reused verbatim from Plan 26-04/26-05 (same interfaces, not
retyped): -->
Rebaseline: `uv run python -m tools.contract_hash.hash --write`.
Verify: `uv run python -m tools.contract_drift.drift`.
Derived regen: `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`.

<!-- Existing schema-validation helper already used in this test module — reuse it, do not
reimplement Draft202012Validator wiring: -->
`tools/adoption_scan/tests/test_plan_classification.py` already imports `jsonschema` machinery for
`test_classify_over_fixture_validates_shape` (or the sibling schema-validation test in this file) —
read that test first and mirror its schema-loading pattern (which schema file path, which validator
class) rather than reinventing schema-loading boilerplate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebaseline surfaceRecord.evidence to minItems:1 + derived-plane regen + cross-schema regression test</name>
  <files>contracts/harness/adoption/inventory.schema.json, contracts/.hashes/manifest.json, docs/reference/inventory.md, .memory/derived/contracts-index.md, tools/adoption_scan/tests/test_plan_classification.py</files>
  <read_first>
    contracts/harness/adoption/inventory.schema.json `$defs.surfaceRecord` (the exact `evidence`
      sub-schema this task edits — currently `minItems: 0` with description "May be empty for an
      unknown/absent surface"),
    contracts/harness/adoption/plan.schema.json `$defs.proposalRecord.properties.evidence` and
      `$defs.questionRecord.properties.evidence` (both already `minItems: 1` — the target this task
      makes `inventory.schema.json` match),
    tools/adoption_scan/detect.py (every `_surface(...)` call site — confirms no detector ever
      constructs a surfaceRecord with empty evidence; this is why `minItems: 1` is the correct,
      non-breaking rebaseline direction rather than loosening `plan.schema.json` instead),
    26-VERIFICATION.md gap 2 / 26-REVIEW.md CR-03 (the exact reproduction and fix recommendation this
      task implements),
    tools/adoption_scan/tests/test_plan_classification.py (full — find the existing
      schema-validation test's `jsonschema`/`Draft202012Validator` loading pattern to mirror)
  </read_first>
  <behavior>
    - Edit `contracts/harness/adoption/inventory.schema.json`'s `$defs.surfaceRecord.properties.evidence`:
      change `"minItems": 0` to `"minItems": 1`, and update the `"description"` from "May be empty
      for an unknown/absent surface." to something stating the actual invariant, e.g. "A surface
      with no evidence is never emitted by any detector; evidence is always non-empty." Do not touch
      any other property in `surfaceRecord` or any other `$def`.
    - Do NOT touch the module-level top-level `"description"` field's Plan-26-04/26-05 changelog
      prose (that is WR-08, explicitly out of this plan's gap-closure scope per the orchestrator's
      brief — leave it as-is unless it is the one line this task's own edit directly falsifies; if
      it references the evidence cardinality at all, correct only that specific inaccuracy, nothing
      else).
    - Writing to `contracts/**` is a constitution-plane write `contract_guard` denies unless the
      human-authorized `GOLDEN_APPROVE_HUMAN` token is present or this is an equivalent human-present
      write — do NOT fabricate the token; if denied, pause and request human authorization (the
      checkpoint task below is the explicit gate for this).
    - Run `uv run python -m tools.contract_hash.hash --write` to rebaseline
      `contracts/.hashes/manifest.json`.
    - Run `uv run python -m tools.contract_drift.drift` and confirm exit 0.
    - Run `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`
      to refresh `docs/reference/inventory.md` and `.memory/derived/contracts-index.md` — this
      schema edit changes generated reference text (the `evidence` field's rendered description/
      cardinality), so both derived artifacts MUST be regenerated in this same task to avoid a
      `stale-derived` red later.
    - Test: add a regression test to `test_plan_classification.py` (mirror the existing
      schema-validation test's loading pattern for `inventory.schema.json`/`plan.schema.json` and
      `jsonschema.Draft202012Validator`) that constructs ONE synthetic, maximally-populated inventory
      dict — non-empty `documentation_surfaces`, `ci_surfaces`, `test_surfaces`,
      `candidate_process_boundaries`, `schema_surfaces`, `codeowners_surfaces`, each with exactly one
      minimally-shaped entry carrying non-empty `evidence` (one `evidenceRef` each) — validates that
      synthetic inventory against `inventory.schema.json` (must pass, proving the fixture itself is
      schema-conformant under the NEW `minItems: 1` rule), then calls `plan.build_plan(inventory)`
      and validates the result against `plan.schema.json`, asserting ZERO validation errors
      (`list(Draft202012Validator(plan_schema).iter_errors(built)) == []`). This is the forward-
      direction proof requested by 26-REVIEW.md CR-03: every schema-valid inventory (under the
      tightened contract) produces a schema-valid plan.
    - Test: add a second, negative-control assertion in the same or an adjacent test — constructing
      an inventory dict with a surfaceRecord-shaped entry carrying `evidence: []` and asserting
      `jsonschema.Draft202012Validator(inventory_schema).iter_errors(that_inventory)` yields at least
      one error (`"should be non-empty"` or `minItems` in the message) — proving the CR-03
      contradiction is now caught at the inventory-schema gate itself, one step earlier than the
      previous `build_plan()`-time failure.
  </behavior>
  <action>
    Edit `inventory.schema.json`'s `surfaceRecord.evidence` per the behavior above. Rebaseline the
    contract hash, verify drift, regenerate the derived plane. Add both regression tests to
    `test_plan_classification.py`. All of this lands in this one task/commit per the atomicity
    requirement.
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift && uv run pytest tools/adoption_scan/tests/test_plan_classification.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    `contracts/harness/adoption/inventory.schema.json`'s `surfaceRecord.evidence` now reads
    `"minItems": 1`. `uv run python -m tools.contract_drift.drift` exits 0. The two new tests in
    `test_plan_classification.py` pass: the forward-direction test shows zero
    `Draft202012Validator` errors for `build_plan()` output over a maximally-populated schema-valid
    inventory; the negative-control test shows at least one `inventory.schema.json` validation error
    for an `evidence: []` surfaceRecord. `docs/reference/inventory.md` and
    `.memory/derived/contracts-index.md` are regenerated and committed in this same task (no
    `git diff` against them after regen). The full repo suite (`uv run pytest -q`) passes.
  </acceptance_criteria>
  <done>inventory.schema.json's surfaceRecord.evidence requires minItems:1, matching plan.schema.json; the contract-hash/derived-plane regen landed atomically; a regression test proves build_plan() output is always plan.schema.json-valid for every schema-valid inventory shape; full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human ratification of the evidence-cardinality rebaseline</name>
  <files>contracts/harness/adoption/inventory.schema.json, contracts/.hashes/manifest.json</files>
  <read_first>.github/CODEOWNERS, .planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-05-PLAN.md (Task 1's checkpoint, the precedent this checkpoint mirrors)</read_first>
  <action>
    This is a BLOCKING human checkpoint — the agent does NOT self-ratify. The agent presents the
    small `inventory.schema.json` diff (`surfaceRecord.evidence`'s `minItems` changed 0 -> 1, plus
    the corrected description) and the rebaselined `contracts/.hashes/manifest.json` diff, then
    PAUSES. A human confirms the rebaseline correctly matches actual producer behavior (no detector
    ever emits empty evidence — Task 1's `detect.py` read-through is the evidence for this) before
    ratifying the stricter contract, via the standard constitution-plane path (CODEOWNERS PR review
    or an explicit human-supplied write — never agent self-approval or `HARNESS_DEV_BYPASS` used to
    self-land the ratification).
  </action>
  <what-built>
    `inventory.schema.json`'s `surfaceRecord.evidence` tightened from `minItems: 0` to `minItems: 1`,
    reconciling it with `plan.schema.json`'s existing `minItems: 1` on the same conceptual field,
    plus a rebaselined `contracts/.hashes/manifest.json`.
  </what-built>
  <how-to-verify>
    1. Confirm `uv run python -m tools.contract_drift.drift` reports OK.
    2. Confirm `uv run pytest -q` is green (full suite).
    3. Review the diff — CODEOWNERS-gated, ratified at PR merge, same as 26-01/26-04/26-05.
    4. Confirm no agent self-approved this change.
  </how-to-verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: uv run python -m tools.contract_drift.drift && uv run pytest -q</human-check>
  </verify>
  <resume-signal>Type "approved" once the diff has been reviewed, or describe required changes.</resume-signal>
  <done>The evidence-cardinality rebaseline is human-ratified via the standard constitution-plane path, not agent self-approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `contracts/harness/adoption/inventory.schema.json` write path | Constitution-plane write, unchanged existing controls: `contract_guard` PreToolUse deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS `/contracts/`. This plan's Task 2 is an explicit blocking checkpoint. |
| The evidence-cardinality contract itself | Governs what a schema-valid inventory can claim; tightening it closes an input-validation gap that previously let a schema-valid document crash the CLI downstream — moving the failure to the earliest possible gate (inventory validation) rather than leaving it latent until plan-building. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08-01 | Denial of Service | A schema-valid inventory crashing `cli.main()` with zero artifacts written (CR-03) | mitigate | `inventory.schema.json`'s `surfaceRecord.evidence` now requires `minItems: 1`, matching `plan.schema.json` — the contradiction that permitted this input shape is closed at the source. |
| T-26-08-02 | Elevation of Privilege | `contracts/harness/adoption/inventory.schema.json` write path | accept (out-of-band) | Unchanged existing controls: `contract_guard` deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS. Task 2 is an explicit `checkpoint:human-verify`. |
| T-26-08-03 | Repudiation (regression) | A future contract edit re-introducing an evidence-cardinality mismatch between the two schemas | mitigate | The new cross-schema regression test (`build_plan()` output always validates against `plan.schema.json` for every schema-valid inventory) fails loud if the two contracts ever diverge again. |
| T-26-08-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan. |
</threat_model>

<verification>
- `uv run python -m tools.contract_drift.drift` — green after the rebaseline.
- `uv run pytest tools/adoption_scan/tests/test_plan_classification.py -q` — the two new regression tests pass.
- `uv run pytest -q` — full repo suite green.
- `git diff --exit-code -- docs/reference .memory/derived/contracts-index.md` after regen — clean (derived plane matches the new contract).
</verification>

<success_criteria>
- `inventory.schema.json` and `plan.schema.json` agree on evidence cardinality (`minItems: 1` on both).
- No schema-valid inventory can produce a `build_plan()` output that fails `plan.schema.json` validation.
- The rebaseline is human-ratified via the standard constitution-plane path.
- Contract-hash, docs_sync, and memory_regen all land atomically with the schema edit.
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-08-SUMMARY.md` when done
</output>
