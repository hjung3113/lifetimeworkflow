---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 05
type: execute
wave: 2
depends_on: ["26-04", "26-06"]
files_modified:
  - tools/adoption_scan/detect.py
  - tools/adoption_scan/scan.py
  - tools/adoption_scan/plan.py
  - tools/adoption_scan/tests/conftest.py
  - tools/adoption_scan/tests/test_detect.py
  - tools/adoption_scan/tests/test_plan_classification.py
  - tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr
  - contracts/harness/adoption/inventory.schema.json
  - contracts/.hashes/manifest.json
  - docs/reference/inventory.md
  - .memory/derived/contracts-index.md
autonomous: false
requirements: [ADOPT-01, ADOPT-02]
gap_closure: true

must_haves:
  truths:
    - "26-VERIFICATION.md gap 1 (ADOPT-01), fully closed: detect.py has detect_schema_surfaces() and detect_codeowners_surfaces(); scan.py's build_inventory() populates inventory['schema_surfaces']/inventory['codeowners_surfaces'] from them; a real self-scan of a repo with *.schema.json files under contracts/ and a .github/CODEOWNERS produces non-empty records for both, not zero"
    - "26-VERIFICATION.md gap 1's plan.py half: plan.py::classify() walks inventory['codeowners_surfaces'] and emits a 'codeowners' proposal (classification always unknown, per D-02 — CODEOWNERS entries are ownership claims); generate_questions() then emits a real 'codeowners-ownership' question — the question kind that was previously permanently dead code now fires on a real scan with a CODEOWNERS file present"
    - "Revision fix (checker BLOCKER 1): Task 1 of THIS plan — the same task that wires scan.py to populate schema_surfaces/codeowners_surfaces — also promotes both properties from optional (26-04) to required in inventory.schema.json, rebaselines contract-hash, and verifies drift green, so 'required' and 'actually populated' land atomically with no red-suite window in between"
    - "Revision fix (checker WARNING 2): detect_schema_surfaces() matches ONLY files whose first path segment is 'contracts' AND whose name ends '.schema.json' — not every *.schema.json anywhere in the tree. This repo has 22 real *.schema.json files but only 11 under contracts/ (the rest under harness/, tools/, examples/log-parser/, .claude/skills/, tests/fixtures/); a negative-match fixture case + assertion proves the narrower rule, not just the positive case"
  artifacts:
    - path: "tools/adoption_scan/detect.py"
      provides: "detect_schema_surfaces(included) and detect_codeowners_surfaces(included), both observed-classification surfaceRecord emitters mirroring detect_ci_surfaces' shape; detect_schema_surfaces is scoped to contracts/**/*.schema.json only"
      min_lines: 40
    - path: "tools/adoption_scan/scan.py"
      provides: "build_inventory() wires the two new detect.py functions into the returned dict under schema_surfaces/codeowners_surfaces keys"
    - path: "tools/adoption_scan/plan.py"
      provides: "classify() emits a 'codeowners' kind proposal per codeowners_surfaces entry; _QUESTION_KIND_BY_PROPOSAL_KIND maps it to the existing 'codeowners-ownership' question kind (already wired into _GROUP_BY_QUESTION_KIND/_BLOCKING_KINDS/_question_text — only the classify()-side proposal emission and the kind-mapping entry were missing)"
    - path: "contracts/harness/adoption/inventory.schema.json"
      provides: "schema_surfaces/codeowners_surfaces promoted from optional (26-04) to required, landed in the SAME task that makes scan.py populate them"
      contains: "schema_surfaces"
  key_links:
    - from: "tools/adoption_scan/scan.py"
      to: "tools/adoption_scan/detect.py"
      via: "build_inventory() calling detect.detect_schema_surfaces(included) / detect.detect_codeowners_surfaces(included)"
      pattern: "detect_schema_surfaces|detect_codeowners_surfaces"
    - from: "tools/adoption_scan/plan.py"
      to: "contracts/harness/adoption/inventory.schema.json"
      via: "classify() reading inventory['codeowners_surfaces'] (the property Plan 26-04 added, this plan promotes to required)"
      pattern: "codeowners_surfaces"
---

<objective>
Close the code half of 26-VERIFICATION.md gap 1: implement `detect_schema_surfaces()` (scoped to
`contracts/**/*.schema.json` only — checker WARNING 2 fix) and `detect_codeowners_surfaces()` in
`detect.py`, wire both into `scan.py::build_inventory()`, and — in the SAME task, per checker
BLOCKER 1's recommended fix — promote `inventory.schema.json`'s `schema_surfaces`/
`codeowners_surfaces` properties from optional (26-04) to required, so the schema's strictness and
`scan.py`'s actual production of those keys land atomically with no window where the suite could go
red. Finally wire `inventory["codeowners_surfaces"]` into `plan.py::classify()` so the
`codeowners-ownership` question kind — structurally present in `plan.py` since Plan 26-03 but
permanently unreachable because no inventory field ever fed it — actually fires on a real scan.

Purpose: ADOPT-01's literal text names six surface categories; four were detected, two were not.
This plan closes the remaining two (schema, CODEOWNERS) end-to-end: detection code, the
required-promotion that makes the schema actually enforce their presence, and — for CODEOWNERS
specifically — the question that makes "unresolved ownership stays a question" (ADOPT-02) true for
CODEOWNERS paths instead of vacuously true.
Output: `detect.py`/`scan.py`/`plan.py` changes, an extended `tmp_minirepo` fixture (cases
(p)/(q)/(r)), a promoted-to-required `inventory.schema.json` + rebaselined contract-hash + refreshed
derived plane, new `test_detect.py`/`test_plan_classification.py` assertions, and a refreshed
committed snapshot.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VERIFICATION.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VALIDATION.md
@contracts/harness/adoption/inventory.schema.json
@tools/adoption_scan/detect.py
@tools/adoption_scan/scan.py
@tools/adoption_scan/plan.py
@tools/adoption_scan/tests/conftest.py
@tools/adoption_scan/tests/test_detect.py
@tools/adoption_scan/tests/test_plan_classification.py
@tools/contract_hash/hash.py
@tools/contract_drift/drift.py
@tools/docs_sync/generate.py
@tools/memory_regen/contracts_index.py

<interfaces>
<!-- detect.py's existing surface-detection shape (detect_ci_surfaces), the exact pattern the two
new functions must mirror — verbatim from tools/adoption_scan/detect.py, already in this repo. -->
```python
def detect_ci_surfaces(included: list[dict]) -> list[dict]:
    ci_entries = [
        entry for entry in included
        if PurePosixPath(entry["path"]).parts[:2] == (".github", "workflows")
    ]
    if not ci_entries:
        return []
    return [_surface(".github/workflows", ci_entries, "observed")]
```
`_surface(target, entries, classification, rationale=None) -> dict` and
`_evidence(entries) -> list[dict]` are the two private helpers already in `detect.py` — reuse both,
do not redefine.

<!-- plan.py's existing dead-code wiring points this plan lights up (already present, do not
retype): -->
`_QUESTION_KIND_BY_PROPOSAL_KIND: dict[str, str]` — add one new entry.
`_GROUP_BY_QUESTION_KIND["codeowners-ownership"] = "ownership"` — already present.
`_BLOCKING_KINDS` already contains `"codeowners-ownership"` — already present.
`_question_text()`'s `templates` dict already has a `"codeowners-ownership"` entry — already present.

<!-- Contract-plane interfaces this plan's Task 1 also uses (same as 26-04's, reused not retyped). -->
Rebaseline: `uv run python -m tools.contract_hash.hash --write`. Verify:
`uv run python -m tools.contract_drift.drift`. Derived regen:
`uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detect_schema_surfaces (contracts/-scoped) / detect_codeowners_surfaces + scan.py wiring + promote schema to required</name>
  <files>tools/adoption_scan/detect.py, tools/adoption_scan/scan.py, tools/adoption_scan/tests/conftest.py, tools/adoption_scan/tests/test_detect.py, contracts/harness/adoption/inventory.schema.json, contracts/.hashes/manifest.json</files>
  <read_first>
    tools/adoption_scan/detect.py (full — `detect_ci_surfaces`/`detect_documentation_surfaces` as
      the two patterns to mirror; `_surface`/`_evidence` helpers),
    tools/adoption_scan/scan.py `build_inventory()` (the dict literal this task adds two keys to),
    tools/adoption_scan/tests/conftest.py `tmp_minirepo` (the ONE fixture tree — D-06 — this task
      extends with cases (p)/(q)/(r), never a second fixture),
    contracts/harness/adoption/inventory.schema.json (Plan 26-04's staged-optional
      `schema_surfaces`/`codeowners_surfaces` properties — this task promotes both to `required`),
    26-VERIFICATION.md gap 1 `artifacts[0]` (`tools/adoption_scan/detect.py` issue text)
  </read_first>
  <behavior>
    - `detect_schema_surfaces(included: list[dict]) -> list[dict]`: one `surfaceRecord` per file in
      `included` whose repo-relative path satisfies BOTH conditions — `PurePosixPath(entry["path"]).parts`
      is non-empty and `parts[0] == "contracts"`, AND the path ends with `.schema.json`
      (`PurePosixPath(entry["path"]).name.endswith(".schema.json")`) — i.e. `contracts/**/*.schema.json`
      specifically, NOT any `*.schema.json` anywhere in the tree (checker WARNING 2: this repo alone
      has 22 real `*.schema.json` files but only 11 live under `contracts/`; the rest live under
      `harness/`, `tools/`, `examples/log-parser/`, `.claude/skills/`, `tests/fixtures/` and must NOT
      match). `classification: "observed"` (direct file-path+extension evidence per D-02). `target`
      = the literal string `"contracts/**/*.schema.json"` when >=1 match exists (mirrors the
      coarse-grained, one-record-per-category style of `detect_ci_surfaces`/`detect_test_surfaces` —
      schema surfaces are not per-directory-boundary-sensitive the way AGENTS.md is, so WR-01's
      per-file argument does not apply here). Returns `[]` when no matching file is present.
      Evidence = every matching entry, via `_evidence()`.
    - `detect_codeowners_surfaces(included: list[dict]) -> list[dict]`: one `surfaceRecord` when a
      `.github/CODEOWNERS` file is present in `included` (`PurePosixPath(entry["path"]) ==
      PurePosixPath(".github/CODEOWNERS")`), `target = ".github/CODEOWNERS"`,
      `classification: "observed"` (literal file existence, D-02). Returns `[]` when absent.
    - `scan.py::build_inventory()`'s returned dict literal gains two new keys:
      `"schema_surfaces": detect.detect_schema_surfaces(included)` and
      `"codeowners_surfaces": detect.detect_codeowners_surfaces(included)`, placed alongside the
      existing `"ci_surfaces"`/`"test_surfaces"` keys.
    - `tests/conftest.py`'s `tmp_minirepo` fixture gains three new embedded cases (extending the SAME
      tree, per D-06 — never a second fixture): (p) a schema surface UNDER contracts/ —
      `root / "contracts" / "widget.schema.json"` containing minimal valid JSON (e.g.
      `'{"type": "object"}\n'`); (q) a CODEOWNERS surface — `root / ".github" / "CODEOWNERS"`
      containing a minimal line (e.g. `"* @nobody\n"`); (r) a schema-LOOKING file OUTSIDE contracts/
      (the WARNING 2 negative-match case) — `root / "tools" / "widget_tool.schema.json"` containing
      the same minimal valid JSON as case (p), proving `detect_schema_surfaces` does NOT match it.
    - Test: `test_detect.py` gains `test_schema_surface_observed` (asserts
      `inventory["schema_surfaces"]` is non-empty and `classification == "observed"`, AND that its
      combined evidence includes case (p)'s path `"contracts/widget.schema.json"`),
      `test_schema_surface_excludes_files_outside_contracts` (asserts case (r)'s path
      `"tools/widget_tool.schema.json"` does NOT appear in any `schema_surfaces` evidence pointer —
      the explicit negative-match regression test for WARNING 2), and
      `test_codeowners_surface_observed` (same shape as the schema test, for `codeowners_surfaces`).
    - Test: `test_inventory_validates_against_schema` (already present) continues to pass — it now
      exercises the two new REQUIRED properties structurally, proving `build_inventory()`'s output
      satisfies the promoted schema with zero manual schema-mocking.
  </behavior>
  <action>
    Implement `detect_schema_surfaces`/`detect_codeowners_surfaces` in `detect.py` per the behavior
    above (contracts/-scoped match for the former), reusing `_surface`/`_evidence`. Add both calls
    to `scan.py::build_inventory()`'s return dict. Extend `tests/conftest.py::tmp_minirepo` with
    cases (p)/(q)/(r) exactly as specified — do not create a second fixture function. Add the three
    new tests to `test_detect.py`. Then, in this SAME task (checker BLOCKER 1's recommended fix):
    edit `contracts/harness/adoption/inventory.schema.json` to add `"schema_surfaces"` and
    `"codeowners_surfaces"` to the top-level `required` array (both properties already exist,
    optional, from Plan 26-04 — do not touch their `properties` shape). Writing to `contracts/**` is
    a constitution-plane write `contract_guard` denies unless the human-authorized
    `GOLDEN_APPROVE_HUMAN` token is present or this is an equivalent human-present write — do NOT
    fabricate the token; if denied, pause and request human authorization. Run
    `uv run python -m tools.contract_hash.hash --write` to rebaseline
    `contracts/.hashes/manifest.json`, then `uv run python -m tools.contract_drift.drift` and
    confirm exit 0. Finally run `uv run pytest -q` (full repo suite) and confirm green — by this
    point `scan.py` already populates both keys (implemented earlier in this same task), so
    promoting them to `required` closes the loop with zero red window.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_detect.py -q && uv run python -m tools.contract_drift.drift && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    `test_detect.py` passes, including all three new tests. `scan.build_inventory(tmp_minirepo)["schema_surfaces"]`
    and `["codeowners_surfaces"]` are both non-empty lists over the extended fixture, and
    `schema_surfaces`'s evidence never includes `"tools/widget_tool.schema.json"` (case (r)).
    `inventory.schema.json`'s top-level `required` array now contains both `"schema_surfaces"` and
    `"codeowners_surfaces"`. `uv run python -m tools.contract_drift.drift` exits 0.
    `uv run pytest -q` exits 0 (full suite green — the promotion and the population land in the
    same task, no red window).
  </acceptance_criteria>
  <done>detect.py detects schema (contracts/-scoped) and CODEOWNERS surfaces; scan.py's build_inventory() wires both into the inventory document; inventory.schema.json now REQUIRES both keys; contract-drift is green; the full suite stays green throughout.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human ratification of the required-promotion</name>
  <files>contracts/harness/adoption/inventory.schema.json, contracts/.hashes/manifest.json</files>
  <read_first>.github/CODEOWNERS, .planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-04-PLAN.md (Task 2's checkpoint, the precedent this checkpoint mirrors)</read_first>
  <action>
    This is a BLOCKING human checkpoint — the agent does NOT self-ratify. The agent presents the
    small `inventory.schema.json` diff (two property names moved from absent-in-`required` to
    present-in-`required` — no shape change) plus the rebaselined `contracts/.hashes/manifest.json`
    diff, then PAUSES. A human confirms `scan.py` genuinely populates both keys on every run (Task
    1's tests are the proof) before ratifying the strictness increase, via the standard
    constitution-plane path (CODEOWNERS PR review or an explicit human-supplied write — never agent
    self-approval or `HARNESS_DEV_BYPASS` used to self-land the ratification).
  </action>
  <what-built>
    `inventory.schema.json`'s `schema_surfaces`/`codeowners_surfaces` promoted from optional to
    required, plus a rebaselined `contracts/.hashes/manifest.json`.
  </what-built>
  <how-to-verify>
    1. Confirm `uv run python -m tools.contract_drift.drift` reports OK.
    2. Confirm `uv run pytest -q` is green (full suite).
    3. Review the diff — CODEOWNERS-gated, ratified at PR merge, same as 26-01/26-04.
    4. Confirm no agent self-approved this change.
  </how-to-verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0; `uv run pytest -q` exits 0.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: uv run python -m tools.contract_drift.drift && uv run pytest -q</human-check>
  </verify>
  <resume-signal>Type "approved" once the diff has been reviewed, or describe required changes.</resume-signal>
  <done>The required-promotion is human-ratified via the standard constitution-plane path, not agent self-approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: plan.py codeowners-ownership wiring + derived-plane regen + snapshot refresh + full verify</name>
  <files>tools/adoption_scan/plan.py, tools/adoption_scan/tests/test_plan_classification.py, tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr, docs/reference/inventory.md, .memory/derived/contracts-index.md</files>
  <read_first>
    tools/adoption_scan/plan.py (full — `classify()`'s existing per-array walk loops, the
      `_QUESTION_KIND_BY_PROPOSAL_KIND`/`_GROUP_BY_QUESTION_KIND`/`_BLOCKING_KINDS` module-level
      dicts, and `_question_text()`'s `templates` dict — all already carry a `codeowners-ownership`
      entry EXCEPT `_QUESTION_KIND_BY_PROPOSAL_KIND`, which has no `"codeowners"` key at all),
    26-VERIFICATION.md gap 1 `artifacts[2]` (`tools/adoption_scan/plan.py:54,66,89` issue text —
      the exact dead-code description this task resolves),
    tools/docs_sync/generate.py, tools/memory_regen/contracts_index.py (the derived-plane regen this
      task must run since Task 1's schema promotion changed inventory.schema.json's REQUIRED shape)
  </read_first>
  <behavior>
    - `classify()` gains a new walk loop over `inventory.get("codeowners_surfaces", [])`, mirroring
      the existing `ci_surfaces`/`test_surfaces` loop shape: for each entry, append
      `{"id": f"codeowners/{entry['target']}", "kind": "codeowners", "classification": "unknown",
      "target": entry["target"], "evidence": entry["evidence"]}` — `classification` is ALWAYS
      `"unknown"`, never a restatement of the source's `"observed"`, because WHO owns a CODEOWNERS
      path is an ownership/authority claim that D-02 reserves for a question by construction (same
      rule already applied to `agents-boundary`/`docs-destination`/`test-command` proposals).
    - `_QUESTION_KIND_BY_PROPOSAL_KIND` gains one new entry: `"codeowners": "codeowners-ownership"`.
      No other module-level dict needs a new entry — `_GROUP_BY_QUESTION_KIND`, `_BLOCKING_KINDS`,
      and `_question_text()`'s `templates` already carry `"codeowners-ownership"` (26-03's dead-code
      wiring; this is the ONE missing link).
    - Test: `test_plan_classification.py` gains `test_codeowners_ownership_question_fires` — builds
      a minimal inventory dict with one `codeowners_surfaces` entry (target=".github/CODEOWNERS",
      classification="observed", evidence=[...]), calls `plan.build_plan(inventory)`, and asserts
      exactly one proposal with `kind == "codeowners"` and `classification == "unknown"` exists, AND
      exactly one question with `kind == "codeowners-ownership"` exists in `built["questions"]` —
      the concrete, no-longer-hypothetical proof that this question kind fires on a real signal.
    - Test: `test_classify_over_fixture_validates_shape` (already present) continues to pass over
      the Task-1-extended fixture — `plan.build_plan(scan.build_inventory(tmp_minirepo))` now
      includes a `codeowners` proposal and a `codeowners-ownership` question because the fixture's
      `.github/CODEOWNERS` (case (q)) flows all the way through.
  </behavior>
  <action>
    Add the `codeowners_surfaces` walk loop to `classify()` and the one missing
    `_QUESTION_KIND_BY_PROPOSAL_KIND` entry, exactly as specified. Add the new test. Then run
    `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index` — Task
    1's schema promotion changed `inventory.schema.json`'s `required` shape, so `docs/reference/inventory.md`
    must be regenerated in THIS wave to avoid a later `stale-derived` red. Then refresh the committed
    syrupy snapshot: `uv run pytest tools/adoption_scan/tests/test_snapshots.py -q --snapshot-update`,
    and inspect the diff — it must show ONLY additions inside the `===== inventory =====`/
    `===== plan =====` sections corresponding to `schema_surfaces`, `codeowners_surfaces`, the new
    `codeowners` proposal, and the new `codeowners-ownership` question; the `===== manifest =====`
    section must be untouched by THIS task (Plan 26-06 owns manifest-section snapshot changes,
    sequenced into the same file in an earlier wave).
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_plan_classification.py -q && uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git add -A -- docs/reference .memory/derived/contracts-index.md && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md && uv run pytest -q && uv run python -m tools.contract_drift.drift && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q && uv sync --all-packages && git diff --exit-code uv.lock</automated>
  </verify>
  <acceptance_criteria>
    `test_plan_classification.py` passes including `test_codeowners_ownership_question_fires`. The
    `stale-derived` command sequence exits 0 (no diff). The full repo suite (`uv run pytest -q`)
    passes. `uv run python -m tools.contract_drift.drift` exits 0. The GEN-04 guard stays green.
    `uv.lock` is unchanged. The refreshed `test_snapshots.ambr` diff touches only the inventory/plan
    sections, not the manifest section.
  </acceptance_criteria>
  <done>plan.py::classify() emits a codeowners proposal per codeowners_surfaces entry; the codeowners-ownership question kind fires on a real scan; the derived plane and committed snapshot both reflect Task 1's schema promotion and the new proposal/question; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scanned target's `.github/CODEOWNERS` content | Untrusted input; only its EXISTENCE and repo-relative path are recorded (D-02 observed), never its ownership-mapping content interpreted or trusted as authority. |
| `plan.py`'s new `codeowners` proposal → `generate_relationship_candidates()` | Must never flow into a relationship candidate — `classify()` only ever sets `kind: "codeowners"` on this proposal, and `generate_relationship_candidates()` already filters on `kind == "relationship"`, so a `codeowners` proposal is structurally inert there (no code change needed to prove this — cite it in the SUMMARY). |
| `detect_schema_surfaces()`'s contracts/-only match ↔ every other `*.schema.json` in the repo | Must never over-match into `harness/`, `tools/`, `examples/`, `.claude/skills/`, or `tests/fixtures/` — the WARNING 2 fix's exact boundary, proven by a negative-match regression test. |
| Task 1 → Task 2 (same plan, sequential) | The schema promotion (Task 1) and the derived-plane regen (Task 2) must land in the same wave — an intervening commit with a stale `docs/reference/inventory.md` would red `stale-derived`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Elevation of Privilege | A CODEOWNERS proposal being promoted into an authoritative ownership record | mitigate (structural) | `classify()` hardcodes `classification: "unknown"` for every `codeowners` proposal, never a restatement of `observed` — the same bright-line rule already enforced for `agents-boundary`/`docs-destination`. No code path anywhere reads a `codeowners` proposal's `.github/CODEOWNERS` content to derive an authority. |
| T-26-05-02 | Information Disclosure | `.github/CODEOWNERS` content leaking into the inventory beyond its path | accept | `detect_codeowners_surfaces()` only ever emits `{target, classification, evidence}` — `evidence` is the already-hashed `evidenceRef` shape (path+sha256+size), never the file's parsed ownership rules. Same non-leak posture as every other surface detector in this module. |
| T-26-05-03 | Elevation of Privilege | `contracts/harness/adoption/inventory.schema.json` write path (Task 1's required-promotion) | accept (out-of-band) | Unchanged existing controls: `contract_guard` PreToolUse deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS `/contracts/`. Task 2 (checkpoint) of this plan is an explicit `checkpoint:human-verify`. |
| T-26-05-04 | Repudiation (regression) | Live test suite across Task 1's required-promotion | mitigate | Detection code and the required-promotion land in the SAME task (checker BLOCKER 1 fix); `uv run pytest -q` is an explicit acceptance criterion on Task 1. |
| T-26-05-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan. |
</threat_model>

<verification>
- `uv run pytest -q` — full repo suite green throughout (both tasks assert this explicitly).
- `uv run python -m tools.contract_drift.drift` — green after Task 1's rebaseline.
- `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git diff --exit-code -- docs/reference .memory/derived/contracts-index.md` — clean after Task 2.
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` — GEN-04 guard green.
- `uv sync --all-packages && git diff --exit-code uv.lock` — lockfile unchanged.
</verification>

<success_criteria>
- A real scan of a repo containing `*.schema.json` files under `contracts/` and a `.github/CODEOWNERS` produces non-empty `schema_surfaces`/`codeowners_surfaces` inventory records; schema files OUTSIDE `contracts/` are never matched.
- `schema_surfaces`/`codeowners_surfaces` are `required` in `inventory.schema.json`, promoted in the same task that makes `scan.py` populate them — no red-suite window.
- `codeowners-ownership` is a reachable question kind on a real scan, not permanently dead code.
- Full test suite, contract-drift, derived plane, GEN-04 guard, and `uv.lock` all stay green.
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-05-SUMMARY.md` when done
</output>
