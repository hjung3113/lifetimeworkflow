---
phase: 03-agents-commands-skills
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/opencode.json
  - harness/opencode.config.schema.json
  - tools/harness_lint/__init__.py
  - tools/harness_lint/frontmatter.py
  - tools/harness_lint/pyproject.toml
  - tools/harness_lint/tests/__init__.py
  - tools/harness_lint/tests/test_opencode_json.py
autonomous: true
requirements: [CONFIG-01]
must_haves:
  truths:
    - "opencode.json defines model tiering (cheap explorer / expensive implementer), instructions glob, formatter, and MCP wiring"
    - "opencode.json is JSON-Schema valid against a hermetic vendored subset schema (no network)"
    - "A single shared YAML frontmatter parser exists for all downstream structural validators"
  artifacts:
    - path: "harness/opencode.json"
      provides: "CONFIG-01 runtime config: model/small_model, instructions[], formatter, mcp, default permission block, plugin wiring"
      contains: "small_model"
    - path: "harness/opencode.config.schema.json"
      provides: "vendored Draft 2020-12 subset schema for the hermetic structural gate"
      contains: "properties"
    - path: "tools/harness_lint/frontmatter.py"
      provides: "shared markdown YAML-frontmatter parser (ruamel.yaml from workspace env)"
      exports: ["parse_frontmatter"]
    - path: "tools/harness_lint/tests/test_opencode_json.py"
      provides: "jsonschema validation of opencode.json + key presence assertions"
  key_links:
    - from: "tools/harness_lint/tests/test_opencode_json.py"
      to: "harness/opencode.config.schema.json"
      via: "jsonschema.validate(load(opencode.json), vendored_schema)"
      pattern: "jsonschema.validate"
    - from: "harness/opencode.json"
      to: "harness/plugins/session-inject.ts"
      via: "plugin registration wiring (authored; runtime deferred)"
      pattern: "session-inject"
---

<objective>
## Phase Goal

**As a** developer/agent in this monorepo, **I want to** have the opencode runtime config authored as validated single-source data plus a shared frontmatter parser, **so that** the whole harness surface can be structurally gated hermetically even though no opencode runtime exists in the container (D-02).

Author `harness/opencode.json` (CONFIG-01: model tiering, instructions glob, formatter, MCP wiring, default permission block, session-inject plugin registration), vendor a hermetic subset JSON-Schema to validate it, and stand up the `tools/harness_lint/` package with the shared YAML frontmatter parser that Plans 03/04/05 reuse.

Purpose: proves the config half of success criterion 1 without a runtime, and lays the shared validation foundation every later structural test depends on.
Output: `harness/opencode.json`, `harness/opencode.config.schema.json`, `tools/harness_lint/` uv member (frontmatter parser + opencode.json test).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agents-commands-skills/03-CONTEXT.md
@.planning/phases/03-agents-commands-skills/03-RESEARCH.md

<interfaces>
<!-- Shared parser downstream plans (03/04/05) import; author this signature exactly. -->
parse_frontmatter(md_text: str) -> tuple[dict, str]
    # returns (frontmatter_dict, body_text); frontmatter parsed from the leading --- ... --- block
    # uses ruamel.yaml (already resolved in the workspace env via check-jsonschema; DO NOT add pyyaml — absent)

opencode.json shape to author (per RESEARCH §opencode.json Schema):
  $schema, model, small_model, instructions[], formatter{}, permission{}, mcp{}, plugin[]
Model tiers are placeholder tokens (provider/implementer-tier, provider/explorer-tier) — NOT real model IDs
(model-identity constraint + A1 key-name deferred to Phase 6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author opencode.json + vendored subset schema</name>
  <files>harness/opencode.json, harness/opencode.config.schema.json</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"opencode.json Schema (CONFIG-01)" (top-level keys table) and §"Code Examples → opencode.json skeleton"
    - harness/plugins/session-inject.ts (its RESUME NOTE flags the Phase-3 wiring point + hook-name MEDIUM confidence)
    - CLAUDE.md §Technology Stack (ruff/dotnet format formatter, instructions = AGENTS.md pointers not contracts/** payloads)
  </read_first>
  <action>
    Create harness/opencode.json pinned to "$schema":"https://opencode.ai/config.json" with: model = "provider/implementer-tier" (expensive default) and small_model = "provider/explorer-tier" (cheap tier) — use these PLACEHOLDER tokens, never real model IDs (model-identity constraint; exact key name is A1-deferred to Phase 6). instructions = ["AGENTS.md","**/AGENTS.md"] — pointers only, do NOT glob contracts/** payloads (Pitfall P2). formatter = { ruff: {command:["ruff","format","$FILE"], extensions:[".py"]}, dotnet: {command:["dotnet","format"], extensions:[".cs"]} }. permission = the coarse project default block: edit "ask", bash object mirroring the last-wins ordering from harness/permission-matrix.json (* first ask, dotnet */uv */pytest * allow, git push* ask, rm -rf* deny), webfetch "ask", external_directory "ask". mcp = {} (config-only wiring, D-03). plugin = ["harness/plugins/session-inject.ts"] to register the Phase-2 injector (authored wiring only; re-verify chat.system.transform vs experimental. prefix + session.created event name at Phase 6 per A6 — add a JSON _note field flagging this, no code comment).
    Then hand-author harness/opencode.config.schema.json as a Draft 2020-12 subset schema (a validation AID, not the runtime's schema) that requires the CONFIG-01 keys: model, small_model, instructions (array of strings), formatter (object), mcp (object), permission (object with a bash property). Mark additionalProperties true so unknown opencode keys don't fail. Add a schema description noting it is a hermetic vendored subset for the structural gate.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import json; c=json.load(open('harness/opencode.json')); assert c['model'] and c['small_model'] and isinstance(c['instructions'],list) and c['formatter'] and 'mcp' in c and c['permission']['bash']"` exits 0
    - opencode.json contains no real model identifier (only placeholder tier tokens)
    - `harness/opencode.config.schema.json` parses as JSON and lists model/small_model/instructions/formatter/mcp/permission in its properties
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import json; c=json.load(open('harness/opencode.json')); s=json.load(open('harness/opencode.config.schema.json')); assert all(k in s['properties'] for k in ['model','small_model','instructions','formatter','mcp','permission'])"</automated>
  </verify>
  <done>opencode.json carries model tiering, instructions pointers, ruff+dotnet formatter, empty mcp, default last-wins permission block, and session-inject plugin registration; the vendored subset schema enumerates the CONFIG-01 keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: harness_lint foundation — shared frontmatter parser + opencode.json structural test</name>
  <files>tools/harness_lint/__init__.py, tools/harness_lint/frontmatter.py, tools/harness_lint/pyproject.toml, tools/harness_lint/tests/__init__.py, tools/harness_lint/tests/test_opencode_json.py</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Don't Hand-Roll" (one shared frontmatter parser; jsonschema.validate not manual key checks) and §"opencode.json Schema → Structural validation approach"
    - tools/memory_regen/pyproject.toml (virtual member template) and tools/memory_regen/tests/test_contracts_index.py (test discipline)
    - harness/opencode.json + harness/opencode.config.schema.json (Task 1 outputs)
  </read_first>
  <behavior>
    - parse_frontmatter("---\\nname: x\\ndescription: y\\n---\\nBODY") == ({"name":"x","description":"y"}, "BODY")
    - parse_frontmatter of a nested block (permission:\n  edit: deny) yields a nested dict under "permission"
    - parse_frontmatter of text with no --- fence returns ({}, original_text)
    - test_opencode_json: jsonschema.validate(load("harness/opencode.json"), load(schema)) does NOT raise
    - test_opencode_json: asserts model, small_model, instructions(list), formatter, mcp keys present
  </behavior>
  <action>
    Create tools/harness_lint/ as a virtual uv member (pyproject name logparser-harness-lint, requires-python >=3.11, `[tool.uv] package = false`, NO unique external deps — jsonschema and ruamel.yaml both resolve from the shared workspace env, same as other members import shared deps; DO NOT add pyyaml, it is absent). frontmatter.py exports parse_frontmatter(md_text) using ruamel.yaml (round-trip or safe loader) to parse the leading fenced --- ... --- block, returning (dict, body); when no fence is present return ({}, md_text). __init__.py re-exports parse_frontmatter. tests/test_opencode_json.py loads harness/opencode.json and harness/opencode.config.schema.json via stdlib json and calls jsonschema.validate (import jsonschema) — asserting no ValidationError plus explicit presence of model/small_model/instructions/formatter/mcp (CONFIG-01). Run `uv sync --all-packages` to register the member.
  </action>
  <acceptance_criteria>
    - `uv sync --all-packages` succeeds; uv.lock unchanged (no new packages — ruamel/jsonschema already resolved)
    - `uv run pytest tools/harness_lint/tests/test_opencode_json.py -x -q` exits 0
    - `uv run python -c "from tools.harness_lint.frontmatter import parse_frontmatter; fm,b=parse_frontmatter('---\nname: x\n---\nB'); assert fm=={'name':'x'} and b.strip()=='B'"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_opencode_json.py -x -q</automated>
  </verify>
  <done>parse_frontmatter round-trips flat + nested frontmatter and no-fence text; test_opencode_json.py proves opencode.json validates against the vendored schema and carries all CONFIG-01 keys; `uv sync --all-packages` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored config → structural validator | untrusted/typo'd JSON validated before it is trusted as the harness config |
| markdown source → frontmatter parser | YAML frontmatter parsed for later access-control assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | opencode.json drift/typo | mitigate | jsonschema.validate against a vendored hermetic subset schema; test asserts CONFIG-01 keys present |
| T-03-06 | Elevation of Privilege | default permission block too broad | mitigate | permission.bash mirrors the `*`-first last-wins ordering; no trailing broad allow (P3) |
| T-03-07 | Information Disclosure | instructions glob pulls full contract bodies | mitigate | instructions limited to AGENTS.md pointers, not contracts/** payloads (P2) |
| T-03-08 | Tampering | non-hermetic schema fetch at test time | mitigate | schema is vendored under harness/; test never fetches opencode.ai (403 + drift) |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (ruamel.yaml/jsonschema already resolved); `uv sync` must not mutate uv.lock |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_opencode_json.py -x -q` green.
- `uv run pytest` full suite green (no regression).
- opencode.json contains only placeholder model tier tokens (no real model ID) — model-identity constraint honored.
</verification>

<success_criteria>
CONFIG-01 satisfied: opencode.json defines model tiering, instructions glob, formatter, and MCP wiring, is JSON-Schema valid against a hermetic vendored schema, and the shared frontmatter parser is available for downstream agent/command/skill validators.
</success_criteria>

<output>
Create `.planning/phases/03-agents-commands-skills/03-02-SUMMARY.md` when done.
</output>
