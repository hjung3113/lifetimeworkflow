---
phase: 03-agents-commands-skills
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - harness/agents/orchestrator.md
  - harness/agents/dotnet-engineer.md
  - harness/agents/python-engineer.md
  - harness/agents/code-reviewer.md
  - harness/agents/explorer.md
  - tools/harness_lint/tests/test_agents.py
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05]
must_haves:
  truths:
    - "Five agent personas exist in neutral source with routing-signal descriptions and model/permission scoping"
    - "code-reviewer is read-only in BOTH representations (opencode permission deny + Claude tools allowlist has no Write/Bash/Edit)"
    - "explorer runs on the cheap model tier and cannot edit"
  artifacts:
    - path: "harness/agents/orchestrator.md"
      provides: "AGENT-01 primary orchestrator persona"
      contains: "description"
    - path: "harness/agents/code-reviewer.md"
      provides: "AGENT-04 read-only reviewer (dual representation)"
      contains: "Read, Grep, Glob"
    - path: "tools/harness_lint/tests/test_agents.py"
      provides: "structural validation of all agent frontmatter + reviewer read-only invariant"
  key_links:
    - from: "tools/harness_lint/tests/test_agents.py"
      to: "tools/harness_lint/frontmatter.py"
      via: "parse_frontmatter over each harness/agents/*.md"
      pattern: "parse_frontmatter"
    - from: "harness/agents/dotnet-engineer.md"
      to: "harness/permission-matrix.json"
      via: "permission.bash scope {dotnet *: allow} consistent with the matrix"
      pattern: "dotnet"
---

<objective>
## Phase Goal

**As a** developer/agent in this monorepo, **I want to** invoke five scoped personas (orchestrator, dotnet-engineer, python-engineer, read-only code-reviewer, explorer) whose least-privilege access is expressed in single-source frontmatter, **so that** the right specialist is routed by description and no persona holds more privilege than its job needs.

Author the 5 agent personas (AGENT-01..05) as neutral `harness/agents/*.md` — authoring BOTH the opencode `permission:` intent and the Claude `tools:` allowlist where the runtimes diverge (D-04) — and add the structural validator that enforces the read-only-reviewer invariant.

Purpose: proves success criterion 2 (five personas load scoped by permission + model tier) structurally, without a runtime.
Output: 5 agent markdown files + `test_agents.py`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agents-commands-skills/03-CONTEXT.md
@.planning/phases/03-agents-commands-skills/03-RESEARCH.md

<interfaces>
<!-- From Plan 02 (already merged): -->
from tools.harness_lint.frontmatter import parse_frontmatter  # (dict, body)

Per-runtime persona mapping to author (RESEARCH §Agent Format table):
  orchestrator     opencode mode: primary, broad permission | Claude delegation tools
  dotnet-engineer  opencode permission.bash {"*":"ask","dotnet *":"allow"} | Claude tools: Read, Edit, Bash, Grep, Glob
  python-engineer  opencode permission.bash {"*":"ask","uv *":"allow","pytest *":"allow"} | Claude tools: Read, Edit, Bash, Grep, Glob
  code-reviewer    opencode permission {edit:deny, bash:deny, write:deny} | Claude tools: Read, Grep, Glob  (NO Write/Bash/Edit)
  explorer         opencode model: cheap tier + permission {edit:deny} | Claude tools: Read, Grep, Glob + cheap model
The 15 valid permission keys: read, edit, bash, glob, grep, list, task, external_directory,
  todowrite, question, webfetch, websearch, lsp, skill, doom_loop.
Model tiers are placeholder tokens (provider/explorer-tier) — NOT real model IDs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 5 agent personas (dual-representation, routing-signal descriptions)</name>
  <files>harness/agents/orchestrator.md, harness/agents/dotnet-engineer.md, harness/agents/python-engineer.md, harness/agents/code-reviewer.md, harness/agents/explorer.md</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Agent Format (AGENT-01..05)" (opencode vs Claude fields, the per-runtime mapping table) and §"Pitfall P7" (description-as-label) + §"Pitfall P-perm"
    - AGENTS.md + libs/python/AGENTS.md (rules personas reference)
    - .claude/agents/*.md (repo's own Claude agent frontmatter shape — name/description/tools)
  </read_first>
  <action>
    Create one markdown file per persona with neutral frontmatter that carries BOTH runtime intents (the Phase-6 emitter will split them; author both now per D-01/D-04). Each frontmatter includes: name; description = a 3rd-person, verb-first routing signal ("Use when… + what it does" with concrete triggers like "a contract changes", "a golden goes red", "a migration step is requested") — never a bare noun-phrase label (P7); a mode field (orchestrator: primary; others: subagent); a model field ONLY on explorer (placeholder provider/explorer-tier cheap tier) — others omit to inherit; a permission block (opencode least-privilege) AND a tools list (Claude allowlist). Scope exactly: orchestrator = primary + broad delegation (task/todowrite), decomposes and delegates, no direct heavy edits; dotnet-engineer = permission.bash {"*":"ask","dotnet *":"allow"} + Claude tools Read, Edit, Bash, Grep, Glob; python-engineer = permission.bash {"*":"ask","uv *":"allow","pytest *":"allow"} + same Claude tools; code-reviewer = permission {edit:"deny", bash:"deny", write:"deny", read:"allow"} + Claude tools EXACTLY "Read, Grep, Glob" (NO Write/Bash/Edit — the read-only invariant, P-perm); explorer = model provider/explorer-tier + permission {edit:"deny"} + Claude tools Read, Grep, Glob, returns file paths. The markdown body is the persona system prompt (concise). Keep descriptions disjoint across personas.
  </action>
  <acceptance_criteria>
    - Five files exist under harness/agents/ with non-empty description frontmatter
    - code-reviewer.md frontmatter has NO edit/bash/write allow in the permission block AND its Claude tools list is exactly Read, Grep, Glob (no Write/Bash/Edit)
    - dotnet-engineer bash scope allows `dotnet *`; python-engineer allows `uv *` and `pytest *`
    - No file contains a real model identifier (explorer uses the placeholder tier token)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from tools.harness_lint.frontmatter import parse_frontmatter; import pathlib; fm,_=parse_frontmatter(pathlib.Path('harness/agents/code-reviewer.md').read_text()); p=fm.get('permission',{}); assert str(p.get('edit'))=='deny' and str(p.get('bash'))=='deny'; t=str(fm.get('tools','')); assert 'Write' not in t and 'Bash' not in t and 'Edit' not in t"</automated>
  </verify>
  <done>All 5 personas authored with routing-signal descriptions and correct least-privilege scoping; code-reviewer read-only in both representations; explorer on cheap tier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural validator for agent frontmatter (test_agents.py)</name>
  <files>tools/harness_lint/tests/test_agents.py</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Agent Format → Structural validation (D-04)" (the exact assertions) and §"Validation Architecture" AGENT rows
    - tools/harness_lint/frontmatter.py (Plan 02 — parse_frontmatter)
    - harness/agents/*.md (Task 1 outputs)
  </read_first>
  <behavior>
    - Globs harness/agents/*.md; for each: description is present and non-empty
    - Each description contains a routing trigger token (case-insensitive "use"/"when") — P7 guard
    - Any `mode` present is in {primary, subagent, all}
    - Any permission keys are a subset of the 15 valid keys
    - code-reviewer AND explorer: no edit/bash/write allow in permission AND (if a Claude tools list present) no Write/Bash/Edit token — asserted for BOTH representations
    - Exactly the five expected persona names are present (no sprawl beyond AGENT-01..05)
  </behavior>
  <action>
    Create tools/harness_lint/tests/test_agents.py that globs harness/agents/*.md, parses each via parse_frontmatter, and asserts the behavior cases above. Encode the 15-key allowlist as a module constant reused from the matrix concept. Implement a helper is_read_only(fm) that checks BOTH the opencode permission block (edit/bash/write not "allow") AND the Claude tools string (no Write/Bash/Edit) and assert it for code-reviewer and explorer. Fail loud with the offending persona name in the assertion message.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_agents.py -x -q` exits 0
    - Temporarily granting code-reviewer `edit: allow` makes the test FAIL (invariant is real, not vacuous) — verify by reasoning/inspection, revert if tried
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_agents.py -x -q</automated>
  </verify>
  <done>test_agents.py structurally validates all 5 personas — descriptions carry routing triggers, permission keys valid, reviewer+explorer read-only in both representations — and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator → delegated persona | task delegation must not escalate privilege beyond the callee's scope |
| reviewer persona → repo | a reviewer must never mutate the repo or run shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Elevation of Privilege | code-reviewer/explorer write affordance | mitigate | opencode permission deny + Claude tools omit Write/Bash/Edit; test_agents asserts read-only in BOTH reps (P-perm) |
| T-03-10 | Spoofing/routing | description-as-label | mitigate | verb-first "Use when…" descriptions; test asserts a routing trigger token per persona (P7) |
| T-03-11 | Elevation of Privilege | invalid/over-broad permission keys | mitigate | test asserts permission keys ⊆ the 15; engineer bash scoped to dotnet/uv/pytest |
| T-03-12 | Tampering | persona sprawl | accept | test pins exactly the five enumerated personas; no extra agents authored (P1/P8) |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_agents.py -x -q` green.
- `uv run pytest tools/harness_lint -x -q` green (opencode.json + agents).
- Manual read: code-reviewer.md has zero write/bash affordance in either representation.
</verification>

<success_criteria>
AGENT-01..05 satisfied: five scoped personas exist in neutral source with routing-signal descriptions, correct model tiering, and the read-only-reviewer invariant proven structurally in both runtime representations.
</success_criteria>

<output>
Create `.planning/phases/03-agents-commands-skills/03-03-SUMMARY.md` when done.
</output>
