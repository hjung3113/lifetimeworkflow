---
phase: 03-agents-commands-skills
plan: 05
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - harness/skills/dotnet-conventions/SKILL.md
  - harness/skills/python-conventions/SKILL.md
  - harness/skills/golden-testing/SKILL.md
  - harness/skills/data-contracts/SKILL.md
  - harness/skills/skill-creator/SKILL.md
  - harness/skills/normalization-catalog/SKILL.md
  - harness/skills/pipeline-patterns/SKILL.md
  - tools/harness_lint/tests/test_skills.py
autonomous: true
requirements: [SKILL-01, SKILL-02]
must_haves:
  truths:
    - "Four core skills (dotnet-conventions, python-conventions, golden-testing, data-contracts) exist with progressive-disclosure frontmatter"
    - "skill-creator (meta) + two domain skills (normalization-catalog, pipeline-patterns) exist with disjoint routing descriptions"
    - "Every SKILL.md respects the shared caps: name ≤64 + regex + dir-match, description non-empty ≤1024, body <~500 lines"
  artifacts:
    - path: "harness/skills/golden-testing/SKILL.md"
      provides: "SKILL-01 golden/approval-testing skill"
      contains: "name"
    - path: "harness/skills/skill-creator/SKILL.md"
      provides: "SKILL-02 meta skill demanding 'why not an existing skill?'"
      contains: "name"
    - path: "tools/harness_lint/tests/test_skills.py"
      provides: "structural cap + regex + routing-trigger validation of every SKILL.md"
  key_links:
    - from: "tools/harness_lint/tests/test_skills.py"
      to: "harness/skills"
      via: "glob harness/skills/*/SKILL.md + parse_frontmatter + cap checks"
      pattern: "SKILL.md"
    - from: "harness/skills/*/SKILL.md"
      to: "directory name"
      via: "frontmatter name must equal parent dir name"
      pattern: "name"
---

<objective>
## Phase Goal

**As a** developer/agent in this monorepo, **I want to** progressively-disclosed skills (conventions, golden-testing, data-contracts, domain catalogs, a skill-creator meta) whose frontmatter stays within both runtimes' size caps, **so that** the "how we work here" knowledge is loadable on demand without blowing the context budget or being rejected by a runtime.

Author the 7 skills (SKILL-01 core + SKILL-02 meta/domain) as `harness/skills/<name>/SKILL.md` with disjoint routing descriptions and progressive disclosure, plus the structural cap validator.

Purpose: proves success criterion 5 (skills load with progressive disclosure under each runtime's size cap) — the caps are identical for both runtimes (name ≤64, description ≤1024; body <~500 lines is a warn), per the RESEARCH 200-vs-1024 correction.
Output: 7 SKILL.md files + `test_skills.py`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agents-commands-skills/03-CONTEXT.md
@.planning/phases/03-agents-commands-skills/03-RESEARCH.md

<interfaces>
<!-- From Plan 02: -->
from tools.harness_lint.frontmatter import parse_frontmatter  # (dict, body)

Shared caps (BOTH runtimes — RESEARCH §"Skill Format + Caps", the 200-vs-1024 correction):
  name        : 1–64 chars, regex ^[a-z0-9]+(-[a-z0-9]+)*$, MUST equal parent dir name, no "anthropic"/"claude", no XML tags
  description : non-empty, ≤1024 chars, states what + WHEN (routing trigger), no XML tags
  body        : <~500 lines RECOMMENDED (warn, not fail)
The 7 skills (D-07): core = dotnet-conventions, python-conventions, golden-testing, data-contracts;
  meta = skill-creator; domain = normalization-catalog, pipeline-patterns (carryover + scenarios).
Descriptions MUST be disjoint (anti-sprawl P7/P8).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 4 core skills</name>
  <files>harness/skills/dotnet-conventions/SKILL.md, harness/skills/python-conventions/SKILL.md, harness/skills/golden-testing/SKILL.md, harness/skills/data-contracts/SKILL.md</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Skill Format + Caps (SKILL-01/02, D-07)"
    - libs/python/AGENTS.md + libs/dotnet/AGENTS.md (per-package conventions the skills distill)
    - contracts/**/*.schema.json + tools/golden_runner/ (source facts for data-contracts + golden-testing skills)
    - CLAUDE.md §"Golden-Equivalence Comparator" (§4.3–4.6 conventions for golden-testing/data-contracts)
  </read_first>
  <action>
    Create four SKILL.md files, each in a directory whose name equals the frontmatter name. Frontmatter: name (lowercase-hyphen, matches dir, ≤64), description (verb-first "Use when… + does…", ≤1024, disjoint from the others). Bodies use progressive disclosure — the SKILL.md body is concise (well under ~500 lines) and points to references/ for depth rather than inlining everything. Content: dotnet-conventions = .NET 10 / xunit.v3 / Verify.XunitV3 patterns + InvariantCulture/LF/no-BOM emit rules (§4.3–4.6); python-conventions = uv workspace / ruff / pyright / pytest patterns + module-path tool invocation; golden-testing = the golden runner + Verify/syrupy approval workflow + /golden-approve human gate + normalized-equivalence (no byte-diff) discipline; data-contracts = contract-first rule, Draft 2020-12 schema shape, RFC 8785 hash/drift gate, contracts/ as constitution plane. Keep descriptions non-overlapping so routing is unambiguous (P7/P8).
  </action>
  <acceptance_criteria>
    - Four dirs exist, each with SKILL.md whose frontmatter name == dir name
    - Each description is non-empty and ≤1024 chars and contains a "use"/"when" trigger token
    - No SKILL.md name contains "anthropic"/"claude" or XML tags
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from tools.harness_lint.frontmatter import parse_frontmatter; import pathlib,re; [ (lambda fm: (lambda n,d: (_ for _ in ()).throw(AssertionError(p)) if not(n==p.parent.name and 0<len(d)<=1024 and re.match(r'^[a-z0-9]+(-[a-z0-9]+)*$',n)) else None)(fm.get('name',''), fm.get('description','')))(parse_frontmatter(p.read_text())[0]) for p in [pathlib.Path('harness/skills',x,'SKILL.md') for x in ['dotnet-conventions','python-conventions','golden-testing','data-contracts']] ]"</automated>
  </verify>
  <done>Four core skills authored within caps, dir-name-matched, with disjoint routing descriptions and progressive-disclosure bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Author skill-creator (meta) + 2 domain skills</name>
  <files>harness/skills/skill-creator/SKILL.md, harness/skills/normalization-catalog/SKILL.md, harness/skills/pipeline-patterns/SKILL.md</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Skill Format + Caps" and §"Anti-Patterns → Skill sprawl"
    - contracts/normalization/correction-rules.schema.json + contracts/state/equipment-progress.schema.json (domain source for normalization-catalog + pipeline-patterns carryover)
  </read_first>
  <action>
    Create three SKILL.md files (dir name == name). skill-creator = the meta skill that guides authoring a new skill AND demands the anti-sprawl question "why not an existing skill?" before creating one (P8); its description routes on "when creating/adding a skill". normalization-catalog = domain skill cataloguing the normalization correction-rule kinds from the contracts; description routes on "when adding/looking up a normalization rule". pipeline-patterns = domain skill covering carryover/state and pipeline scenario patterns from the state contract; description routes on "when reasoning about carryover/pipeline scenarios". All three within caps, disjoint descriptions, progressive-disclosure bodies. Do NOT exceed the enumerated 7 skills (P1 — no sprawl).
  </action>
  <acceptance_criteria>
    - Three dirs exist, each SKILL.md name == dir name, description ≤1024 with a trigger token
    - Exactly 7 skill directories total under harness/skills/ (no more, no fewer)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pathlib; d=sorted(p.name for p in pathlib.Path('harness/skills').iterdir() if p.is_dir()); assert d==['data-contracts','dotnet-conventions','golden-testing','normalization-catalog','pipeline-patterns','python-conventions','skill-creator'], d"</automated>
  </verify>
  <done>skill-creator + normalization-catalog + pipeline-patterns authored within caps; exactly 7 skills total, descriptions disjoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Structural cap validator (test_skills.py)</name>
  <files>tools/harness_lint/tests/test_skills.py</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Structural cap validation (D-07)" (exact assertions) and §"Pitfall P5/P6" (name ≤64 / desc ≤1024 real numbers)
    - tools/harness_lint/frontmatter.py (Plan 02)
  </read_first>
  <behavior>
    - Globs harness/skills/*/SKILL.md
    - name: matches ^[a-z0-9]+(-[a-z0-9]+)*$, len ≤64, equals parent dir name, contains no "anthropic"/"claude", no "<"/">"
    - description: non-empty, len ≤1024, no XML tags, contains a routing trigger token ("use"/"when")
    - body line count >500 emits a warning (pytest warning or recorded), does NOT fail
    - exactly the 7 expected skill names present (anti-sprawl)
  </behavior>
  <action>
    Create tools/harness_lint/tests/test_skills.py globbing harness/skills/*/SKILL.md, parsing via parse_frontmatter, asserting the cap/regex/dir-match/trigger rules as HARD failures and the >500-line body as a warning (use warnings.warn or pytest.warns-compatible recording — never a hard fail, per D-07). Assert the set of skill dirs equals the 7 enumerated names. Fail loud with the offending skill path.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_skills.py -x -q` exits 0
    - Setting a description to 1025 chars would FAIL; a 600-line body only WARNS (verify by reasoning)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_skills.py -x -q</automated>
  </verify>
  <done>test_skills.py enforces the shared caps (name ≤64+regex+dir-match, desc ≤1024, body warn>500) across all 7 skills and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored skill → runtime loader | oversized/malformed skill could be silently truncated or rejected by a runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Tampering/DoS | skill over-cap (silent truncation) | mitigate | author-time cap lint: name ≤64, desc ≤1024 hard-fail; body >500 warns (P5/P6 real numbers) |
| T-03-18 | Spoofing/routing | skill sprawl / overlapping descriptions | mitigate | exactly 7 enumerated skills; disjoint verb-first descriptions; skill-creator demands "why not existing?" (P8) |
| T-03-19 | Tampering | reserved words / XML tags in name/desc | mitigate | test rejects "anthropic"/"claude" and `<`/`>` in name/description |
| T-03-20 | Tampering | emit-drift (opencode vs Claude skill shape) | accept | caps identical for both runtimes (RESEARCH correction); Phase-6 emitter reconciles remaining shape diffs |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_skills.py -x -q` green.
- `uv run pytest tools/harness_lint -x -q` green (opencode.json + agents + commands + skills).
</verification>

<success_criteria>
SKILL-01/02 satisfied: four core + one meta + two domain skills exist with progressive disclosure, disjoint routing descriptions, dir-name-matched names, all within the shared name/description caps proven by test_skills.py.
</success_criteria>

<output>
Create `.planning/phases/03-agents-commands-skills/03-05-SUMMARY.md` when done.
</output>
