---
phase: 04-plugins-hooks
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/polyglot_lint/__init__.py
  - tools/polyglot_lint/lint.py
  - tools/polyglot_lint/pyproject.toml
  - tools/polyglot_lint/tests/__init__.py
  - tools/polyglot_lint/tests/test_lint.py
  - tools/polyglot_lint/tests/test_corpus_parity.py
autonomous: true
requirements: [POLY-01]

must_haves:
  truths:
    - "A BOM/CRLF raw byte blob produces a violation; clean UTF-8/LF produces none."
    - "A non-canonical decimal/datetime/null cell produces a violation; a canonical one does not."
    - "A TSV with uneven tab counts across rows produces a column-shift violation."
    - "The linter's notion of canonical is identical to normalize.core on the shared fixture corpus (no second normalizer)."
    - "The CLI exits 1 (fail loud) on any violation, 0 when clean."
  artifacts:
    - path: "tools/polyglot_lint/lint.py"
      provides: "lint_bytes / lint_tsv / lint_file -> [Violation]; main(argv) exit 0/1"
      contains: "def lint_file"
    - path: "tools/polyglot_lint/pyproject.toml"
      provides: "uv workspace member (package=false), zero deps"
      contains: "package = false"
    - path: "tools/polyglot_lint/tests/test_corpus_parity.py"
      provides: "proof the linter shares normalize.core (D-02/D-03 built-once)"
      contains: "normalize"
  key_links:
    - from: "tools/polyglot_lint/lint.py"
      to: "libs/python/normalize/core.py"
      via: "sys.path shim import (mirror golden_runner)"
      pattern: "from normalize.core import"
---

## Phase Goal

**As a** developer or coding agent working in this polyglot harness, **I want to** have the Phase 1-3 contract, §4.3-4.6 boundary, and permission rules enforced by non-bypassable runtime hooks, **so that** a violation is blocked at write/commit time instead of relying on advisory prose.

> NOTE (planner): the ROADMAP Phase 4 `**Goal:**` line is a rationale sentence, not an `As a / I want to / so that` user story. The story above is a planner-derived provisional; run `/gsd mvp-phase 4` to ratify or correct it.

<objective>
Build the POLY-01 polyglot-boundary linter: `tools/polyglot_lint/lint.py`, which encodes the `integration_contracts §4.3-4.6` checklist (BOM · CRLF/LF · TSV-escape/column-shift · decimal-locale · timezone/datetime · null-vs-empty) as executable rules. This is the shared §4.3-4.6 rule engine that HOOK-04 (on-write) and HOOK-03 (commit-gate) both call.

Purpose: turn the §4.3-4.6 convention prose into a fail-loud detector, WITHOUT re-implementing normalization — the linter detects a violation by normalizing a copy via `normalize.core` and diffing against the raw (Pattern 5). This satisfies success criterion 2 ("shares Phase 1's normalization core, built once, not re-implemented").
Output: a stdlib-only uv workspace member with unit tests proving each rule catches its violation and a corpus-parity test proving zero drift from `normalize.core`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugins-hooks/04-CONTEXT.md
@.planning/phases/04-plugins-hooks/04-RESEARCH.md
@CLAUDE.md
@libs/python/normalize/core.py
@libs/python/normalize/../../normalize-spec.md
@tools/golden_runner/runner.py
@tools/golden_runner/pyproject.toml

<interfaces>
<!-- Reused verbatim — DO NOT re-implement (D-02/D-03). Extracted from libs/python/normalize/core.py -->
From libs/python/normalize/core.py:
```python
DEFAULT_NULL_TOKEN = "\\N"     # R6 agreed null token
NULL_SENTINEL = "<NULL>"        # what a null maps to ("" stays "", "" != null)
def normalize_cell(value: str, kind: str, null_token: str = DEFAULT_NULL_TOKEN) -> str
    # kind in {"decimal","datetime","string"}; R6 null-check first, then decimal/datetime canon
def normalize_tsv(raw: bytes) -> str
    # R1 strip BOM (utf-8-sig) + R2 CRLF/CR -> LF + R8 ordinal row sort; drops trailing empty line
```
sys.path shim pattern (mirror golden_runner.runner lines 31-36): REPO_ROOT = Path(__file__).resolve().parents[2]; insert REPO_ROOT/"libs"/"python" onto sys.path; then `from normalize.core import ...` with `# noqa: E402`.

Fixture corpus (for corpus-parity test): libs/normalize-fixtures/{bom_crlf,decimal_locale,null_vs_empty,tz_iso8601}.json
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing rule tests + corpus-parity test (RED)</name>
  <files>tools/polyglot_lint/tests/__init__.py, tools/polyglot_lint/tests/test_lint.py, tools/polyglot_lint/tests/test_corpus_parity.py</files>
  <read_first>
    - libs/python/normalize/core.py (the canonical target — normalize_tsv / normalize_cell)
    - libs/normalize-spec.md (R1-R8 rule numbering the Violation.rule codes mirror)
    - tools/golden_runner/tests/ pattern and tools/harness_perms/tests/test_resolver.py (in-repo test idiom, real-fixture loading)
    - libs/normalize-fixtures/*.json (the shared corpus; each has raw input + expected canonical)
  </read_first>
  <behavior>
    - test_lint: `lint_bytes(b"\xef\xbb\xbfa\tb\n")` yields an R1-BOM violation; `lint_bytes(b"a\tb\r\nc\td\n")` yields R2-CRLF; clean `b"a\tb\nc\td\n"` yields [].
    - test_lint: `lint_tsv("1,5\t2024-01-01 00:00", kinds=["decimal","datetime"])` yields non-canonical decimal + datetime violations; a canonical row yields [].
    - test_lint: a TSV whose rows have differing tab counts yields an R7-tsv column-shift violation.
    - test_lint: `main([path])` returns 1 for a file with any violation, 0 for a clean file; the reason is printed to stderr.
    - test_corpus_parity: for each libs/normalize-fixtures/*.json, the linter's canonical target equals `normalize_tsv`/`normalize_cell` output (assert the linter imports and calls normalize.core, never a second implementation).
  </behavior>
  <action>Author the test files describing the behavior above. Load fixtures with stdlib json (mirror test_resolver real-matrix loading). These tests MUST fail (or error on missing module) before Task 2 — run and confirm RED. Commit: `test(04-01): failing POLY-01 linter + corpus-parity tests`.</action>
  <verify>
    <automated>uv run pytest tools/polyglot_lint/tests -x -q 2>&1 | grep -qiE "error|failed|no tests ran|ModuleNotFoundError"</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/polyglot_lint/tests -x -q` fails/errors (module absent). Test files reference `normalize.core` for the parity target.</acceptance_criteria>
  <done>Two test files exist encoding every rule + parity check; suite is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lint.py detection-by-normalization + uv member (GREEN)</name>
  <files>tools/polyglot_lint/lint.py, tools/polyglot_lint/__init__.py, tools/polyglot_lint/pyproject.toml</files>
  <read_first>
    - tools/golden_runner/runner.py lines 30-36 (exact sys.path shim to import normalize.core from a tools/ member)
    - tools/harness_perms/__init__.py (PEP 562 lazy re-export idiom for a namespace-package member)
    - tools/golden_runner/pyproject.toml (package=false uv member template to copy)
    - 04-RESEARCH.md §"POLY-01: detection by normalization" and Pattern 5 (the diff-raw-vs-canonical trick)
  </read_first>
  <action>Implement `lint_bytes(raw:bytes)`, `lint_tsv(text:str, kinds:list[str]|None)`, `lint_file(path, kinds)` returning `list[Violation]` (frozen dataclass with `rule` code and `detail`), plus `main(argv)->int` (argparse: path + `--kinds`; exit 0 clean / 1 on any violation, reasons to stderr). Detect BOM via `raw.startswith(b"\xef\xbb\xbf")` (R1-BOM), CR via `b"\r" in raw` (R2-CRLF), column-shift via distinct tab counts across non-empty rows (R7-tsv), and decimal/datetime/null non-canonicality by comparing each cell to `normalize_cell(cell, kind, DEFAULT_NULL_TOKEN)` (R3/R5/R6). Import normalize.core via the golden_runner sys.path shim — NEVER re-implement BOM/decimal/TZ logic (D-02/D-03 built-once; Anti-Pattern in RESEARCH). `__init__.py` lazily re-exports lint_file/lint_bytes/lint_tsv (PEP 562, mirror harness_perms). Add `pyproject.toml` (`package=false`, `dependencies=[]`) copying golden_runner's template. Guard with `if __name__=="__main__": raise SystemExit(main())`. Run `uv sync --all-packages` so the new member is registered.</action>
  <verify>
    <automated>uv run pytest tools/polyglot_lint/tests -x -q</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/polyglot_lint/tests -x -q` passes. `printf '\xef\xbb\xbfa\tb\n' > /tmp/p.tsv && uv run python -m tools.polyglot_lint.lint /tmp/p.tsv; echo $?` prints an R1-BOM FAIL and exits 1. `grep -c "def _norm_decimal\|utf-8-sig" tools/polyglot_lint/lint.py` is 0 (no re-implemented normalizer).</acceptance_criteria>
  <done>All POLY-01 tests green; CLI fails loud (exit 1) on a BOM/CRLF file; corpus-parity proves shared core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file/DB byte stream -> normalized comparator | Untrusted TSV bytes (BOM/CRLF/locale) cross the .NET<->Python polyglot boundary; a representation drift here silently corrupts golden equivalence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | linter's canonicalization diverging from normalize.core | mitigate | Import normalize.core; detect by diff. `test_corpus_parity.py` asserts the linter's canonical == `normalize_tsv`/`normalize_cell` on the shared fixture corpus (Pitfall 2). |
| T-04-02 | Repudiation | a §4.3-4.6 violation passing silently | mitigate | `main` exits 1 and writes the failing rule code + detail to stderr (fail loud); unit-tested per rule. |
| T-04-SC | Tampering | dependency supply chain | accept | Zero new external packages — stdlib + in-repo normalize.core only; `pyproject.toml dependencies=[]`; `uv.lock` unchanged (no install surface). |
</threat_model>

<verification>
- `uv run pytest tools/polyglot_lint/tests -x -q` green.
- `uv run pytest` (full suite) stays green — no Phase 1-3 regression.
- Manual demo: a TSV with a BOM and CRLF fails the linter loud (exit 1).
</verification>

<success_criteria>
POLY-01 linter detects every §4.3-4.6 violation class, fails loud (exit 1), and provably shares normalize.core (corpus-parity green). Satisfies Phase 4 success criterion 2's "built once, not re-implemented".
</success_criteria>

<output>
Create `.planning/phases/04-plugins-hooks/04-01-SUMMARY.md` when done.
</output>
