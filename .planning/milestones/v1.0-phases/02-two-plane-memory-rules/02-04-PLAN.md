---
phase: 02-two-plane-memory-rules
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tools/memory_regen/queries.py
  - tools/memory_regen/repo_map.py
  - tools/memory_regen/tests/test_parse.py
  - tools/memory_regen/tests/test_repo_map_determinism.py
  - tools/memory_regen/tests/__snapshots__/test_repo_map_determinism.ambr
autonomous: true
requirements: [MEM-03]
user_setup: []

must_haves:
  truths:
    - "tree-sitter parses .py/.cs/.sh source into non-empty def/ref captures using the tree-sitter 0.25 Query + QueryCursor API."
    - "A networkx PageRank over the symbol graph ranks files; the top-N elided defs render into a token-bounded repo-map."
    - "repo-map regenerates deterministically — delete + rerun reproduces it byte-identically (no timestamps, no raw floats, sorted iteration)."
  artifacts:
    - path: "tools/memory_regen/queries.py"
      provides: "per-language def/ref tags queries (python / c_sharp / bash)"
    - path: "tools/memory_regen/repo_map.py"
      provides: "parse → nx.DiGraph → PageRank → top-N → .memory/derived/repo-map.md (deterministic)"
      exports: ["build_graph", "render", "main"]
      min_lines: 60
    - path: "tools/memory_regen/tests/test_repo_map_determinism.py"
      provides: "generate-twice byte-identical + syrupy snapshot"
    - path: "tools/memory_regen/tests/test_parse.py"
      provides: "def/ref captures non-empty for .py/.cs/.sh"
  key_links:
    - from: "tools/memory_regen/repo_map.py"
      to: "tree_sitter.Query + QueryCursor"
      via: "0.25 API (NOT lang.query().captures())"
      pattern: "QueryCursor"
    - from: "tools/memory_regen/repo_map.py"
      to: "networkx.pagerank"
      via: "sorted DiGraph → ranked top-N"
      pattern: "pagerank"
---

<objective>
Build the repo-map generator — the one genuinely new engine this phase: tree-sitter parses the monorepo (.py/.cs/.sh) into def/ref symbols, a networkx directed graph is ranked by PageRank, and the top-N files render with elided signatures into a token-bounded `.memory/derived/repo-map.md`. It replaces the empty repo-map section the injector reads.

Purpose: Give agents an importance-ranked code map from reference topology (not size/mtime), regenerated every session and proven delete+rerun-identical (success criterion 2) — using the validated tree-sitter 0.25 QueryCursor API and deterministic PageRank ranking.
Output: `queries.py` + `repo_map.py` (entrypoint `python -m tools.memory_regen.repo_map`) + parse/determinism tests + committed syrupy snapshot.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-two-plane-memory-rules/02-CONTEXT.md
@.planning/phases/02-two-plane-memory-rules/02-RESEARCH.md

<interfaces>
<!-- tree-sitter 0.25 API (VERIFIED in-session — training-data lang.query().captures() THROWS): -->
  lang = tree_sitter.Language(tree_sitter_python.language())
  parser = tree_sitter.Parser(lang)
  tree = parser.parse(src_bytes)
  query = tree_sitter.Query(lang, "(function_definition name: (identifier) @def) (call function: (identifier) @ref)")
  caps = tree_sitter.QueryCursor(query).captures(tree.root_node)   # -> {"def":[Node,...], "ref":[Node,...]}
<!-- Determinism (all required): sorted node/edge insertion; top-N by key=lambda kv:(-score, path);
     do NOT print raw floats (rank-only or round fixed dp); NO timestamp/generated-at in the body. -->
  ranked = sorted(nx.pagerank(g, weight="weight").items(), key=lambda kv:(-kv[1], kv[0]))
Grammar wheels (installed in 02-01): tree_sitter_python, tree_sitter_c_sharp, tree_sitter_bash.
Source scope to map: libs/python, libs/dotnet, tools/**, components/** (confine walks to repo subtrees).
Output target: .memory/derived/repo-map.md  (gitignored — created at generate time; header "DERIVED — do not hand-edit").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-language tags queries + tree-sitter parse to def/ref captures (0.25 API)</name>
  <files>tools/memory_regen/queries.py, tools/memory_regen/tests/test_parse.py</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (Pattern 1 the exact validated 0.25 snippet, Pitfall 3 API drift, §Standard Stack)
    - tools/memory_regen/tests/conftest.py (tmp_source_tree fixture with tiny .py/.cs/.sh from 02-01)
  </read_first>
  <behavior>
    - queries.py exposes a mapping from language name (python/c_sharp/bash) to (grammar module, tags query string) with def+ref captures.
    - A parse helper loads the grammar via `tree_sitter.Language(mod.language())`, parses bytes, and runs `tree_sitter.QueryCursor(tree_sitter.Query(lang, q)).captures(root)` — the 0.25 API, NOT lang.query().captures().
    - Parsing the tmp_source_tree .py/.cs/.sh files yields non-empty "def" captures (and refs where present).
    - Uses the individual grammar wheels only (no tree-sitter-language-pack runtime download).
  </behavior>
  <action>
    Create tools/memory_regen/queries.py with a LANGUAGES table keyed by language name → (imported grammar module, file-extensions, tags query string capturing @def and @ref). Python: function_definition/class_definition name @def, call function identifier @ref. C#: method/class/interface declaration name @def, invocation @ref. Bash: function_definition name @def, command_name @ref (best-effort — keep queries minimal and robust). Provide `parse_symbols(path, lang_name) -> {"def": [...names], "ref": [...names]}` using EXACTLY the 0.25 `tree_sitter.Query(lang, q)` + `tree_sitter.QueryCursor(query).captures(root)` API from RESEARCH Pattern 1 (extract capture node text via node.text.decode). Write test_parse.py asserting def captures are non-empty for each of the tmp_source_tree .py/.cs/.sh fixtures, and (Pitfall 3 guard) that the code path does not call the removed `Language.query`/`Query.captures` API.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_parse.py -x` exits 0.
    - Parsing a .py, a .cs, and a .sh fixture each returns a non-empty "def" list.
    - No `AttributeError` / DeprecationWarning from the tree-sitter query path (0.25 API used).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_parse.py -x -q</automated>
  </verify>
  <done>tree-sitter 0.25 parse yields non-empty def/ref captures for all three languages via QueryCursor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PageRank ranking + deterministic token-bounded repo-map render</name>
  <files>tools/memory_regen/repo_map.py, tools/memory_regen/tests/test_repo_map_determinism.py, tools/memory_regen/tests/__snapshots__/test_repo_map_determinism.ambr</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-CONTEXT.md (D-04 DERIVED/no-hand-edit, D-05 tree-sitter+PageRank+token budget, D-06 code-only, D-07 discretion on ~1k budget)
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (Pattern 2 deterministic PageRank, Pitfall 1 floats/timestamps, Pitfall 2 git-diff trap, §".memory/ Layout")
    - tools/memory_regen/queries.py (parse_symbols from Task 1)
    - tools/memory_regen/tests/conftest.py (fixtures)
  </read_first>
  <behavior>
    - build_graph(source_roots) walks the confined repo subtrees, parses each file via parse_symbols, and builds an nx.DiGraph with edge A→B (weight = ref count) when file A references a symbol defined in file B; nodes and edges inserted in sorted order (Pattern 2 determinism).
    - Ranking = sorted(nx.pagerank(g, weight="weight").items(), key=lambda kv:(-kv[1], kv[0])) — score desc, path asc tie-break; raw floats are NOT printed.
    - render() emits a "DERIVED — do not hand-edit (tools/memory_regen/repo_map.py)" header + top-N files with elided def signatures, within a ~1k-token (~4000-char) budget (char/4 heuristic), NO timestamp.
    - Generating twice is byte-identical (determinism); a committed syrupy snapshot pins the render.
  </behavior>
  <action>
    Implement tools/memory_regen/repo_map.py: `build_graph(source_roots=[libs/python, libs/dotnet, tools, components])` — confine each walk to its repo subtree (resolve + subtree-check like contract_hash; skip symlinks escaping the tree — T-02-10), collect def/ref per file, add sorted nodes then sorted edges (weight=ref count). Rank with `nx.pagerank(g, weight="weight")`, select top-N by `(-score, path)`. `render(ranked, budget_chars=4000)` — DERIVED header + top-N files each with a short elided def list, cap by char budget (drop lowest-ranked files first), NO raw floats (rank-only), NO timestamp (Pitfall 1). `write()` → `.memory/derived/repo-map.md` (mkdir parents). `main()` CLI (`python -m tools.memory_regen.repo_map`). Write test_repo_map_determinism.py: (a) generate-twice byte-identical over the tmp_source_tree fixture (hash-compare, NOT git-diff — Pitfall 2); (b) top-N ordering is stable across runs; (c) render is under the char budget; (d) a committed syrupy snapshot (__snapshots__/test_repo_map_determinism.ambr) over the tmp fixture as the determinism reference.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_repo_map_determinism.py -x` exits 0 (including snapshot).
    - `uv run python -m tools.memory_regen.repo_map` writes `.memory/derived/repo-map.md` beginning with the DERIVED header, ≤ ~4000 chars.
    - Delete `.memory/derived/repo-map.md`, rerun, `sha256sum` of the two outputs match (determinism; encoded as the pytest, not a shell one-off).
    - No timestamp and no raw PageRank float in the output body.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_repo_map_determinism.py -x -q</automated>
  </verify>
  <done>repo-map ranks by PageRank and renders a token-bounded, DERIVED-marked map that regenerates byte-identically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree walk → generator | globs walk libs/, tools/, components/ subtrees |
| grammar wheels → parser | compiled grammars loaded from pinned wheels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering/Info-disclosure (path traversal) | source-tree walk in build_graph | mitigate | Resolve + subtree-confine each source root (copy contract_hash `root not in resolved.parents` pattern); never follow symlinks escaping the repo |
| T-02-11 | Repudiation/Integrity (P12 rot / non-determinism) | repo-map.md | mitigate | DERIVED header + no timestamp + no raw float + sorted iteration + stable (-score,path) tie-break; determinism test (generate-twice byte-identical) |
| T-02-SC | Tampering | tree-sitter grammar wheels | accept | Installed + pinned + audited [OK] in 02-01; individual wheels only (no runtime binary download) |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen/tests/test_parse.py tools/memory_regen/tests/test_repo_map_determinism.py -x` green.
- Generate → hash → delete → regenerate → identical hash.
- Full `uv run pytest` green.
</verification>

<success_criteria>
repo-map (tree-sitter + PageRank) regenerates from code on demand, delete+rerun byte-identical, token-bounded, DERIVED-marked, using the validated 0.25 API. (ROADMAP Phase-2 criterion 2; MEM-03; D-04/D-05/D-06.)
</success_criteria>

<output>
Create `.planning/phases/02-two-plane-memory-rules/02-04-SUMMARY.md` when done.
</output>
