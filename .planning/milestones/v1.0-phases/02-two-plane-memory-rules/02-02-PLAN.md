---
phase: 02-two-plane-memory-rules
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tools/memory_regen/inject.py
  - tools/memory_regen/tests/test_inject_assembler.py
  - tools/memory_regen/tests/test_hook_wiring.py
  - .claude/hooks/memory-inject.sh
  - .claude/settings.json
  - harness/plugins/session-inject.ts
autonomous: true
requirements: [HOOK-05]
user_setup: []

must_haves:
  truths:
    - "At session start the agent receives a capped (~1k-token) payload that is banner-first, drift-aware, and pointer-only — never full contract bodies."
    - "The payload always carries the provisional banner even when lower-priority sections are dropped."
    - "Over budget triggers priority-truncation of whole sections (repo-map dropped before banner/drift), not blind mid-line cutting."
    - "The injector hook is the 4th SessionStart slot in .claude/settings.json — the 3 existing hooks all survive (coexist, not overwrite)."
    - "A single injection contract (inject.assemble) is documented for both runtimes; the opencode adapter is authored-but-deferred."
  artifacts:
    - path: "tools/memory_regen/inject.py"
      provides: "assemble(budget_chars=4000) — the shared injection-contract payload assembler + CLI"
      exports: ["assemble", "main"]
      min_lines: 40
    - path: ".claude/hooks/memory-inject.sh"
      provides: "Claude SessionStart wrapper: regen (best-effort) + assemble + node-encode envelope"
    - path: ".claude/settings.json"
      provides: "4th SessionStart slot wiring the injector (3 existing preserved)"
    - path: "harness/plugins/session-inject.ts"
      provides: "Authored-deferred opencode adapter stub calling the SAME assembler + RESUME note"
    - path: "tools/memory_regen/tests/test_inject_assembler.py"
      provides: "cap/banner/priority-truncate/pointer-not-body assertions"
    - path: "tools/memory_regen/tests/test_hook_wiring.py"
      provides: "structural assertion of 4 SessionStart slots"
  key_links:
    - from: ".claude/hooks/memory-inject.sh"
      to: "tools/memory_regen/inject.py"
      via: "python -m tools.memory_regen.inject"
      pattern: "tools\\.memory_regen\\.inject"
    - from: "tools/memory_regen/inject.py"
      to: "tools.contract_drift.run_gate"
      via: "live drift status (reuse Phase-1)"
      pattern: "run_gate"
    - from: ".claude/settings.json"
      to: ".claude/hooks/memory-inject.sh"
      via: "SessionStart 4th slot"
      pattern: "memory-inject\\.sh"
---

<objective>
Deliver the end-to-end session-start injection slice: a non-ignorable, ~1k-token, provisional, banner-first payload assembled by ONE shared function (`inject.assemble`) and wired as the 4th Claude SessionStart hook. This is the phase's user-observable capability — an agent opening a session immediately orients from derived memory without reading full contracts, and is told the memory is provisional (ADR/contract wins).

Purpose: Fix the single injection contract first (D-01) so the Claude adapter runs now and the opencode adapter (authored, deferred) wraps the identical assembler later.
Output: `inject.py` (assembler + CLI), the Claude hook wrapper coexisting as slot 4, an authored-deferred opencode plugin stub, and the assembler + hook-wiring tests.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-two-plane-memory-rules/02-CONTEXT.md
@.planning/phases/02-two-plane-memory-rules/02-RESEARCH.md
@.claude/settings.json
@.claude/hooks/gsd-session-state.sh
@tools/contract_drift/drift.py

<interfaces>
<!-- The assembler reads derived files IF PRESENT (produced by 02-03/02-04) and degrades
     gracefully when absent — so this plan has NO hard dependency on those generators and
     never edits their files. -->
From tools/contract_drift/drift.py:
  run_gate(contracts_dir=CONTRACTS_DIR, baseline_path=MANIFEST_PATH) -> {"ok": bool, "drifted": [(rel, kind, cls), ...]}
Derived files the assembler READS if present (never writes):
  .memory/derived/contracts-index.md   (02-03 output)
  .memory/derived/repo-map.md          (02-04 output)
State pointer (never body):
  .memory/state/activeContext.md
Claude SessionStart envelope (mirror gsd-session-state.sh):
  {"hookSpecificOutput":{"hookEventName":"SessionStart","additionalContext": <stdout>}}
Existing SessionStart slots (must all survive): gsd-check-update.js, gsd-session-state.sh, tools/bootstrap/install.sh
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inject.assemble() — the shared, capped, priority-truncated injection contract</name>
  <files>tools/memory_regen/inject.py, tools/memory_regen/tests/test_inject_assembler.py</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-CONTEXT.md (D-02 non-ignorable + banner, D-07 ~1k cap / priority-truncate / no full contracts)
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (§"Assembler skeleton", Pattern 4, Pitfall 4, Pitfall 6/P13)
    - tools/contract_drift/drift.py (run_gate signature — reuse for the live drift section)
    - tools/memory_regen/tests/conftest.py (fixtures written in 02-01)
  </read_first>
  <behavior>
    - assemble(budget_chars=4000) returns a string whose length is at most budget_chars.
    - Section priority order: (0) provisional banner, (1) live drift summary via run_gate, (2) contracts-index summary (read .memory/derived/contracts-index.md head if present, else a short "(index pending)" line), (3) repo-map top-N (read .memory/derived/repo-map.md if present, else omitted), (4) activeContext POINTER only (path + one-line note, never the file body).
    - Banner text asserts "provisional" and that ADR/contract overrides .memory/ on conflict.
    - When assembled content would exceed budget, whole low-priority sections are dropped in reverse-priority order; banner (0) and drift (1) are NEVER dropped.
    - The payload contains NO full contract schema body (no `"$schema"` / full JSON schema text leaks in).
    - activeContext appears as a pointer path, not its file contents (P13).
  </behavior>
  <action>
    Implement `assemble(budget_chars: int = 4000) -> str` in tools/memory_regen/inject.py per RESEARCH §"Assembler skeleton": a static BANNER constant (provisional; ADR/contracts always override .memory/), a `_drift_summary()` calling `tools.contract_drift.drift.run_gate()` and rendering a compact ok/drifted line-count (never full schema text), a `_contracts_summary()` reading the head of `.memory/derived/contracts-index.md` if it exists else "(contracts-index pending — run tools.memory_regen.contracts_index)", a `_repo_map_topN()` reading `.memory/derived/repo-map.md` if present else empty, and `_active_context_pointer()` emitting only the path `.memory/state/activeContext.md` with a one-line "volatile — confirm against contracts/ADR" note. Assemble sections in priority order; skip any non-(banner,drift) section whose inclusion would exceed budget_chars (priority-truncate whole sections, never mid-line). Add a `main()` CLI printing `assemble()` to stdout (`python -m tools.memory_regen.inject`). No timestamps, no secrets. Write test_inject_assembler.py covering every bullet in <behavior>, including a forced-over-budget case (tiny budget) asserting banner+drift survive and repo-map is dropped, plus a pointer-not-body assertion.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py -x` exits 0.
    - `uv run python -m tools.memory_regen.inject` prints a payload whose first line is the provisional banner and whose length is at most 4000 chars.
    - With `budget_chars` set tiny, output still contains the banner and drift line and omits the repo-map section.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_inject_assembler.py -x -q</automated>
  </verify>
  <done>Shared assembler enforces cap + banner + priority-truncate + pointer-only; unit tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Claude SessionStart hook (4th slot, coexist) + authored-deferred opencode adapter stub</name>
  <files>.claude/hooks/memory-inject.sh, .claude/settings.json, harness/plugins/session-inject.ts, tools/memory_regen/tests/test_hook_wiring.py</files>
  <read_first>
    - .claude/settings.json (existing 3 SessionStart slots — APPEND a 4th, never overwrite)
    - .claude/hooks/gsd-session-state.sh (node-encode `{hookSpecificOutput:{additionalContext}}` envelope pattern to mirror)
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (§"Claude SessionStart wrapper", §"opencode Injection Contract (Deferred)", D-01)
    - .planning/phases/02-two-plane-memory-rules/02-CONTEXT.md (D-01 opencode authored-only/deferred)
  </read_first>
  <action>
    Create `.claude/hooks/memory-inject.sh` mirroring gsd-session-state.sh discipline: `set -euo pipefail`, `cd "$CLAUDE_PROJECT_DIR"`, best-effort regenerate derived plane (`uv run python -m tools.memory_regen.repo_map` and `... contracts_index`, each suffixed `>/dev/null 2>&1 || true` so a missing generator during Wave-2 never breaks the hook), capture `PAYLOAD="$(uv run python -m tools.memory_regen.inject 2>/dev/null || echo '')"`, then node-encode the `{hookSpecificOutput:{hookEventName:"SessionStart",additionalContext:PAYLOAD}}` envelope passing PAYLOAD via argv (NOT string interpolation — command-injection defense T-02-04). Append a 4th SessionStart entry to `.claude/settings.json` running `bash "$CLAUDE_PROJECT_DIR"/.claude/hooks/memory-inject.sh` — preserve the 3 existing groups byte-for-byte (coexist). Create the authored-deferred opencode adapter stub `harness/plugins/session-inject.ts`: export a plugin whose `event` handler on `session.created` triggers regen and whose `chat.system.transform` prepends `$(python -m tools.memory_regen.inject)` to the system prompt — with a top-of-file RESUME note that opencode runtime is absent in this container so EXECUTION validation is deferred (mirror the .NET deferral pattern), and that it consumes the SAME `inject.assemble` contract (D-01). Write test_hook_wiring.py: parse `.claude/settings.json`, assert exactly 4 SessionStart groups, assert the 3 existing commands (gsd-check-update.js, gsd-session-state.sh, tools/bootstrap/install.sh) are all still present, and assert the 4th references memory-inject.sh.
  </action>
  <acceptance_criteria>
    - `.claude/settings.json` SessionStart array has 4 groups; the 3 original commands are all present; the new one references `memory-inject.sh`.
    - `uv run pytest tools/memory_regen/tests/test_hook_wiring.py -x` exits 0.
    - `bash .claude/hooks/memory-inject.sh` emits valid JSON with `hookSpecificOutput.additionalContext` whose first line is the provisional banner.
    - `harness/plugins/session-inject.ts` exists and contains a deferred-execution RESUME note + references `tools.memory_regen.inject`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_hook_wiring.py -x -q</automated>
  </verify>
  <done>Injector is the 4th SessionStart slot (3 existing preserved); hook emits the banner-first envelope; opencode stub authored/deferred; wiring test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .memory/ derived+state → injected agent context | volatile memory crosses non-ignorably into every session's prompt |
| SessionStart hook array → runtime config | a 4th hook is appended alongside 3 trusted existing hooks |
| shell wrapper → node/uv subprocesses | the wrapper spawns generators + assembler and encodes their stdout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Spoofing/Tampering (P13) | injected activeContext/derived memory | mitigate | Provisional banner (priority 0, never dropped) declares memory is a hint and ADR/contracts override on conflict; inject a POINTER to activeContext, never its body; decisions live in append-only ADR not .memory/ |
| T-02-04 | Tampering (command injection) | memory-inject.sh | mitigate | `set -euo pipefail`; pass PAYLOAD via node argv (no string+shell interpolation); mirror bootstrap/install.sh + gsd-session-state.sh discipline |
| T-02-05 | Tampering (hook coexistence race) | .claude/settings.json SessionStart | mitigate | Append 4th slot, never overwrite; test_hook_wiring.py asserts all 4 groups survive and the 3 existing commands remain |
| T-02-06 | Information disclosure | injected payload | mitigate | Assembler caps ≤4000 chars and injects index summaries/pointers only — no full contract bodies; unit test asserts no `"$schema"` leaks |
| T-02-SC | Tampering | supply chain (deps installed in 02-01) | accept | No new package installs in this plan; pins established and audited [OK] in 02-01 |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen/tests/test_inject_assembler.py tools/memory_regen/tests/test_hook_wiring.py -x` green.
- `.claude/settings.json` has 4 SessionStart groups; 3 originals intact.
- `bash .claude/hooks/memory-inject.sh` → valid JSON, additionalContext starts with the provisional banner.
- Full `uv run pytest` green.
</verification>

<success_criteria>
Session-start injection injects pointers/indexes (not full contracts), capped ~1k tokens, banner-marks volatile state provisional so ADR always overrides; the 3 existing SessionStart hooks coexist; opencode adapter authored/deferred against the same contract. (ROADMAP Phase-2 criterion 4; HOOK-05; D-01/D-02/D-07.)
</success_criteria>

<output>
Create `.planning/phases/02-two-plane-memory-rules/02-02-SUMMARY.md` when done.
</output>
