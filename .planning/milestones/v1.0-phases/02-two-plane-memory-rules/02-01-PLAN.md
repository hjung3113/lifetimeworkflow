---
phase: 02-two-plane-memory-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .memory/README.md
  - .memory/state/activeContext.md
  - .memory/state/progress.md
  - .gitignore
  - tools/memory_regen/pyproject.toml
  - tools/memory_regen/__init__.py
  - tools/memory_regen/tests/__init__.py
  - tools/memory_regen/tests/conftest.py
  - tools/memory_regen/tests/test_layout.py
autonomous: true
requirements: [MEM-01, MEM-02]
user_setup: []

must_haves:
  truths:
    - "The constitution plane (contracts/, docs/adr/, glossary, golden/) is declared human-owned and immutable-to-agents in a committed marker."
    - "`.memory/derived/` is gitignored (regenerated); `.memory/state/` is committed (survives the ephemeral container)."
    - "`tools/memory_regen/` resolves as a uv-workspace member with the pinned tree-sitter + networkx deps installed."
    - "A structural test proves the two-plane layout and gitignore boundary without touching git-diff on a gitignored path."
  artifacts:
    - path: ".memory/README.md"
      provides: "Two-plane declaration + constitution-immutable marker + derived-regenerated rule"
      contains: "DERIVED"
    - path: ".memory/state/activeContext.md"
      provides: "Committed volatile-state stub (survives sessions), no secrets/PII"
    - path: ".gitignore"
      provides: "`.memory/derived/` ignore rule (state/ stays tracked)"
      contains: ".memory/derived/"
    - path: "tools/memory_regen/pyproject.toml"
      provides: "uv virtual member with tree-sitter==0.25.2 + grammar wheels + networkx==3.6.1 pins"
      contains: "tree-sitter==0.25.2"
    - path: "tools/memory_regen/tests/conftest.py"
      provides: "repo-root sys.path wiring + tmp source-tree/contracts-tree fixtures"
    - path: "tools/memory_regen/tests/test_layout.py"
      provides: "Structural assertion of two-plane layout + gitignore + member import"
  key_links:
    - from: ".gitignore"
      to: ".memory/derived/"
      via: "git check-ignore match"
      pattern: "\\.memory/derived/"
    - from: "tools/memory_regen/tests/conftest.py"
      to: "repo root sys.path"
      via: "parents[3] insert (mirror golden_runner conftest)"
      pattern: "parents\\[3\\]"
---

<objective>
Lay the two-plane memory skeleton and the `tools/memory_regen/` uv-workspace member that every other Phase-2 plan builds on: the constitution/derived/state split on disk, the refined gitignore boundary (D-03), and the pinned tree-sitter + networkx toolchain (installed once here so Wave-2 plans never touch `uv.lock`).

Purpose: A thin, honest skeleton — no generators yet — so the injector (02-02), contracts-index (02-03), repo-map (02-04), and AGENTS.md rules (02-05) all land against a stable layout and a resolved environment.
Output: `.memory/` two-plane tree (state committed, derived gitignored), constitution-immutability marker, the `memory_regen` member + test harness (conftest + `test_layout.py`).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-two-plane-memory-rules/02-CONTEXT.md
@.planning/phases/02-two-plane-memory-rules/02-RESEARCH.md
@.gitignore
@contracts/README.md
@tools/contract_hash/pyproject.toml
@tools/golden_runner/tests/conftest.py

<interfaces>
<!-- The member is a *virtual* uv workspace member (package=false), invoked by module path
     `python -m tools.memory_regen.<entry>` from repo root. `tools` is a namespace package
     (no tools/__init__.py) — golden_runner/conftest.py already imports `tools.golden_runner.*`
     by inserting the repo root (parents[3]) onto sys.path. Mirror that wiring. -->
Root workspace (pyproject.toml): members = ["libs/python", "tools/*"] already globs tools/memory_regen — NO root pyproject edit needed for membership.
Dev deps already present: pytest>=8.4,<9, syrupy==5.2.0 (do not re-add).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-plane .memory/ layout + gitignore boundary + constitution-immutability marker</name>
  <files>.memory/README.md, .memory/state/activeContext.md, .memory/state/progress.md, .gitignore</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-CONTEXT.md (D-03 derived-gitignored/state-committed, D-04 derived-regenerated)
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (§".memory/ Layout + Gitignore", Pitfall 2)
    - .gitignore (append target — do NOT overwrite existing rules)
    - contracts/README.md (constitution-plane prose to align the immutability marker with)
  </read_first>
  <action>
    Create the two-plane derived/volatile root. `.memory/README.md` (committed) states the three planes: (1) CONSTITUTION plane — contracts/, docs/adr/, docs/glossary.md, golden/ — human-owned, CODEOWNERS-gated, immutable-to-agents per MEM-01, with a note that runtime agent-write enforcement is Phase-4 contract-guard (NOT built in this phase — this is a declaration/marker only); (2) DERIVED plane — `.memory/derived/` — auto-regenerated by tools/memory_regen, gitignored, "DERIVED — do not hand-edit" per D-04; (3) STATE plane — `.memory/state/` — small committed volatile state that survives the ephemeral container per D-03. Include the literal token "DERIVED" in the README so tests can assert the marker. Create `.memory/state/activeContext.md` and `.memory/state/progress.md` as committed stubs (brief placeholders, NO secrets/PII/tokens — the injector will only ever inject a pointer to activeContext, never its body, per P13). Do NOT create any tracked file under `.memory/derived/` (it is gitignored). Append exactly one line `.memory/derived/` to `.gitignore` under a clear comment; keep `.memory/state/**` tracked.
  </action>
  <acceptance_criteria>
    - `.memory/README.md` names all four constitution members (contracts/, docs/adr/, glossary, golden/) AND contains the token "DERIVED".
    - `.gitignore` contains a `.memory/derived/` line; `git check-ignore .memory/derived/repo-map.md` prints a match (rc 0).
    - `git check-ignore .memory/state/activeContext.md` returns non-zero (state is TRACKED).
    - No secret/token pattern in `.memory/state/*` (e.g. `grep -riE 'secret|token|password|api[_-]?key' .memory/state` returns nothing meaningful).
  </acceptance_criteria>
  <verify>
    <automated>git check-ignore .memory/derived/repo-map.md &amp;&amp; ! git check-ignore .memory/state/activeContext.md &amp;&amp; grep -q 'DERIVED' .memory/README.md</automated>
  </verify>
  <done>Two-plane `.memory/` tree exists; derived gitignored, state committed; constitution-immutability marker present.</done>
</task>

<task type="auto">
  <name>Task 2: memory_regen uv-workspace member + Wave-0 test harness (conftest + layout test)</name>
  <files>tools/memory_regen/pyproject.toml, tools/memory_regen/__init__.py, tools/memory_regen/tests/__init__.py, tools/memory_regen/tests/conftest.py, tools/memory_regen/tests/test_layout.py</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (§Standard Stack pins + §Package Legitimacy Audit — all five packages [OK]; §Recommended Project Structure)
    - tools/contract_hash/pyproject.toml (virtual-member pyproject shape: package=false, no build-system)
    - tools/golden_runner/tests/conftest.py (repo-root sys.path wiring via parents[3]; fixture style)
    - pyproject.toml (root — confirm members glob `tools/*` already includes memory_regen; testpaths covers `tools`)
  </read_first>
  <action>
    Create `tools/memory_regen/pyproject.toml`: project name `logparser-memory-regen`, version 0.0.0, requires-python ">=3.11", `[tool.uv] package = false` (virtual member, invoked as `python -m tools.memory_regen.<entry>`). Pin `dependencies` EXACTLY (supply-chain T-02-SC — no floating ranges): tree-sitter==0.25.2, tree-sitter-python==0.25.0, tree-sitter-c-sharp==0.23.5, tree-sitter-bash==0.25.1, networkx==3.6.1 (all verdict [OK] per RESEARCH §Package Legitimacy Audit — no [ASSUMED]/[SUS], so no legitimacy checkpoint). These are declared here (not in 02-04) so `uv.lock` is resolved ONCE in Wave 1 and Wave-2 plans never contend on the lock. Add empty `tools/memory_regen/__init__.py` and `tools/memory_regen/tests/__init__.py`. Create `tests/conftest.py` mirroring golden_runner/conftest.py: insert repo root (`Path(__file__).resolve().parents[3]`) and `libs/python` onto sys.path; provide a `repo_root` fixture, a `tmp_source_tree` fixture (writes tiny `.py`/`.cs`/`.sh` files with a def+ref each, for later parse/repo-map tests), and a `tmp_contracts_tree` fixture (copies a couple of `contracts/**/*.schema.json` into tmp for later contracts-index tests). Create `tests/test_layout.py` — structural assertions only: `.memory/derived/` is gitignored while `.memory/state/` is tracked (shell out to `git check-ignore`, do NOT assert on git-diff of a gitignored path — Pitfall 2), `.memory/README.md` contains "DERIVED" and names the constitution members, and `import tree_sitter, tree_sitter_python, tree_sitter_c_sharp, tree_sitter_bash, networkx` all succeed. Run `uv sync` so the pins resolve into the workspace lock.
  </action>
  <acceptance_criteria>
    - `uv sync` exits 0 and `uv.lock` gains the five pinned packages.
    - `uv run python -c "import tree_sitter, tree_sitter_python, tree_sitter_c_sharp, tree_sitter_bash, networkx"` exits 0.
    - `uv run pytest tools/memory_regen/tests/test_layout.py -x` exits 0.
    - `uv run pytest` (full workspace) stays green — Phase-1 drift/golden suites unaffected by the new member.
  </acceptance_criteria>
  <verify>
    <automated>uv sync &amp;&amp; uv run pytest tools/memory_regen/tests/test_layout.py -x -q</automated>
  </verify>
  <done>memory_regen member resolves with pinned deps; layout test green; full suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → build environment | tree-sitter/grammar/networkx wheels enter the workspace via `uv add`/`uv sync` |
| filesystem → committed repo | `.memory/state/` is committed and could leak secrets if authored carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | tree-sitter*, networkx installs | mitigate | Pin exact versions (0.25.2 / 0.25.0 / 0.23.5 / 0.25.1 / 3.6.1); all [OK] in RESEARCH §Package Legitimacy Audit (slopcheck + functional import); individual grammar wheels only — NOT tree-sitter-language-pack (runtime binary download) |
| T-02-01 | Information disclosure | `.memory/state/*` (committed) | mitigate | Author stubs with no secrets/PII/tokens; injector injects only a pointer to activeContext (never body); grep gate in acceptance |
| T-02-02 | Tampering | `.gitignore` derived boundary | mitigate | Append `.memory/derived/` (never overwrite); assert via `git check-ignore` that derived is ignored and state is tracked |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen/tests/test_layout.py -x` green.
- `git check-ignore .memory/derived/x` matches; `.memory/state/activeContext.md` tracked.
- Full `uv run pytest` green (no regression to Phase-1 suites).
</verification>

<success_criteria>
Constitution plane declared immutable-to-agents; `.memory/derived/` gitignored and `.memory/state/` committed (D-03); memory_regen member installed with pinned tree-sitter+networkx; structural layout test green. (ROADMAP Phase-2 criterion 1; MEM-01, MEM-02.)
</success_criteria>

<output>
Create `.planning/phases/02-two-plane-memory-rules/02-01-SUMMARY.md` when done.
</output>
