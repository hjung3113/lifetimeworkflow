---
phase: 05-despecialization
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - examples/log-parser/contracts/log-specs/
  - examples/log-parser/contracts/reference-data/
  - examples/log-parser/contracts/state/
  - examples/log-parser/contracts/normalization/
  - examples/log-parser/components/toy-converter/
  - examples/log-parser/libs/dotnet/
  - examples/log-parser/golden/repr-only/
  - examples/log-parser/golden/value-regression/
  - examples/log-parser/tests/
  - examples/log-parser/contracts/.hashes/manifest.json
  - contracts/.hashes/manifest.json
  - tools/golden_runner/runner.py
  - tools/golden_runner/tests/conftest.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr
  - tools/memory_regen/tests/test_agents_md.py
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  - tools/memory_regen/tests/__snapshots__/test_repo_map_determinism.ambr
  - tools/memory_regen/contracts_index.py
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "The semiconductor domain seed (contracts/{log-specs,reference-data,state} + contracts/normalization/correction-rules.* + components/toy-converter + libs/dotnet/Normalize + libs/dotnet/Normalize.Tests + golden/{repr-only,value-regression}) is relocated under examples/log-parser/ via history-preserving git mv."
    - "The language-NEUTRAL harness tooling stays in core: libs/python/normalize (the §4.3-4.6 canonicalizer that core polyglot_lint/golden_runner import — a uv member) and libs/normalize-fixtures (referenced by the core corpus-parity test). The .NET §4.3-4.6 impl (libs/dotnet/Normalize*) MOVES with the example because it is the example's language-side implementation — no core Python tool imports it, it is not a uv member, and 'core is language-neutral' (ADR-0002) forbids .NET code in core. contracts/normalization/format-conventions.schema.json stays at root as generic convention material."
    - "The root contract-hash manifest is rebaselined to the generic instance only (format-conventions + sample/greeting); the live contract-drift gate reads clean AFTER the move."
    - "The example gets its own rebaselined manifest (examples/log-parser/contracts/.hashes/manifest.json); moved example tests/csproj that reference libs/normalize-fixtures resolve example->core (allowed one-direction)."
    - "The core golden_runner no longer names a relocated domain artifact: its TOY_CONVERTER_PROJECT default AND the dead conftest toy_converter_project fixture are removed (no dangling components/toy-converter reference). The domain golden_runner tests move WITH their cases to examples/; every core test the move invalidates is repointed to surviving material (docs-sync EXPECTED_PAGES + test_rows_are_sorted_and_typed target + test_agents_md PER_PACKAGE_AGENTS/root-map assertions dropping libs/dotnet as a core plane) and the docs-sync .ambr + two memory .ambr snapshots are regenerated; the non-example pytest suite stays green."
  artifacts:
    - path: "examples/log-parser/contracts/log-specs/standard-log.schema.json"
      provides: "relocated domain contract (git-mv, history preserved)"
      contains: "properties"
    - path: "contracts/.hashes/manifest.json"
      provides: "rebaselined root manifest — generic instance only (format-conventions + sample/greeting)"
      contains: "sample/greeting"
    - path: "examples/log-parser/contracts/.hashes/manifest.json"
      provides: "the example instance's own drift baseline"
      contains: "log-specs"
  key_links:
    - from: "git mv (verbatim rename)"
      to: "commit_gate.staged_files --diff-filter=ACM"
      via: "renames are R, excluded from ACM -> moved BOM/CRLF golden seeds are NOT re-linted"
      pattern: "diff-filter=ACM"
    - from: "contracts/ (post-move working tree)"
      to: "contracts/.hashes/manifest.json"
      via: "contract_hash --write rebaseline -> drift clean"
      pattern: "contract_hash"
    - from: "examples/log-parser/tests/"
      to: "tools/golden_runner/runner.py"
      via: "example->core import allowed (one direction); golden_dir override points at examples golden"
      pattern: "run_golden_case"
---

## Phase Goal

**As a** developer or coding agent turning this log-parser harness into a reusable template, **I want to** relocate the semiconductor domain seed into `examples/log-parser/`, slot the language/toolchain assumption into config, and prove the core depends on no example, **so that** the repo can be cloned as a domain-neutral template while every contract/§4.3-4.6/gate invariant stays enforced.

> NOTE (planner): ROADMAP Phase 5 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived; ratify via `/gsd mvp-phase 5` if a canonical story is required.

<objective>
Perform the **domain move** (GEN-01): `git mv` the semiconductor seed + the example's language-side .NET impl under `examples/log-parser/` (preserving tree + history), rebaseline the root manifest to the generic instance only so the live drift gate reads clean, build the example's own manifest, move the domain golden_runner tests WITH their cases, de-pin the core golden_runner's toy-converter default (checker W-1), and regenerate the FOUR stale Phase 1-4 snapshots the move invalidates (docs-sync EXPECTED_PAGES + docs-sync `.ambr` + two memory `.ambr`). Only the language-NEUTRAL harness tooling stays in core: `libs/python/normalize` (the §4.3-4.6 canonicalizer that core `polyglot_lint`/`golden_runner` import — a uv member; moving it breaks the workspace + GEN-04) and `libs/normalize-fixtures` (core corpus-parity test references it). The **.NET §4.3-4.6 impl `libs/dotnet/Normalize*` MOVES** with the example — it is the example's language-side implementation (no core Python tool imports it, not a uv member) and "core is language-neutral" (ADR-0002) forbids .NET code in core. `contracts/normalization/format-conventions.schema.json` stays at root as generic §4.3-4.6 convention material (RESEARCH Q4).

Purpose: cleanly separate the durable core from its one domain instance so the repo is a template. The move is a pure relocation — no algorithm edits (Pitfall P2: do NOT rewrite the intentionally-dirty BOM/CRLF golden seeds; a content edit reclassifies the rename A/M and trips polyglot).
Output: `examples/log-parser/**`, a rebaselined root + example manifest, moved domain tests, regenerated snapshots.

**Precondition (token + gate mechanics):** the ADR/contract Writes happen in 05-05; here the constitution-plane changes are the golden/contract RELOCATIONS (Bash `git mv`, NOT Write/Edit — so `contract_guard`'s Write|Edit matcher never fires on them) and the manifest rebaseline (Bash). The commit is landed through the LIVE commit-gate with `GOLDEN_APPROVE_HUMAN` exported in the Claude Code process env (05-RESEARCH Q3: export before the session, or a gitignored `.claude/settings.local.json` `env` block removed after). 05-01 makes the drift component ratifiable; the rebaseline keeps drift clean anyway. Verbatim renames are status R → excluded from `--diff-filter=ACM` → the moved BOM/CRLF seeds are NOT re-linted (RESEARCH Q2/Q3). **FORBIDDEN:** `git commit --no-verify`, editing `.claude/settings.json` to drop the hook, or any bash-bypass of the gate.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-despecialization/05-CONTEXT.md
@.planning/phases/05-despecialization/05-RESEARCH.md
@CLAUDE.md
@tools/contract_hash/hash.py
@tools/golden_runner/runner.py
@tools/golden_runner/tests/test_repr_only.py
@tools/memory_regen/contracts_index.py
@tools/docs_sync/tests/test_docs_sync_determinism.py

<interfaces>
<!-- Move set (DOMAIN + example's language-side .NET impl) — verbatim git mv, preserve tree under examples/log-parser/ -->
contracts/log-specs/                     -> examples/log-parser/contracts/log-specs/
contracts/reference-data/                -> examples/log-parser/contracts/reference-data/
contracts/state/                         -> examples/log-parser/contracts/state/
contracts/normalization/correction-rules.catalog.yaml  -> examples/log-parser/contracts/normalization/correction-rules.catalog.yaml
contracts/normalization/correction-rules.schema.json   -> examples/log-parser/contracts/normalization/correction-rules.schema.json
components/toy-converter/                 -> examples/log-parser/components/toy-converter/
libs/dotnet/                              -> examples/log-parser/libs/dotnet/                 # WHOLESALE: Normalize/ + Normalize.Tests/ + AGENTS.md (the example's .NET language side; not a uv member, no core Python importer)
golden/repr-only/                        -> examples/log-parser/golden/repr-only/
golden/value-regression/                 -> examples/log-parser/golden/value-regression/
tools/golden_runner/tests/{test_repr_only,test_value_regression,test_compare_recorded}.py + tests/recorded/*  -> examples/log-parser/tests/

<!-- STAYS IN CORE (do NOT move) — language-NEUTRAL harness tooling only -->
libs/python/normalize/       # §4.3-4.6 canonicalizer; core polyglot_lint/golden_runner import it; uv member
libs/normalize-fixtures/     # corpus referenced by the core corpus-parity test (moved .NET tests may reference it: example->core OK)
contracts/normalization/format-conventions.schema.json      # generic §4.3-4.6 convention -> stays at root
tools/golden_runner/tests/{test_sample_loop,test_identity_converter,test_approve_gate}.py + conftest.py  # generic -> stay
# NOTE: libs/dotnet/** MOVES (above) — 'core is language-neutral' per ADR-0002; no core Python tool imports the .NET assembly, it is not a uv member.

<!-- Core reference to a relocated artifact — must be de-pinned (checker W-1) -->
tools/golden_runner/runner.py  TOY_CONVERTER_PROJECT default (REPO_ROOT/components/toy-converter/ToyConverter.csproj) -> DROP the domain default / require the converter via run_golden_case(converter=...) so core names no relocated path.

<!-- Snapshots invalidated by the move (RESEARCH Pitfall P3 + checker Blocker-1) -->
tools/docs_sync/tests/test_docs_sync_determinism.py  EXPECTED_PAGES -> {format-conventions, greeting}
tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr     -> regenerate (render() over the post-move root schema set)
tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr        -> regenerate (pytest --snapshot-update)
tools/memory_regen/tests/__snapshots__/test_repo_map_determinism.ambr   -> regenerate (components/toy-converter + libs/dotnet left the source dirs)

<!-- Rebaseline commands (RESEARCH Code Examples) -->
uv run python -m tools.contract_hash.hash --write          # root manifest -> generic only
# build the example's own manifest by pointing the hasher at the example contracts root (see action)
uv run python -m tools.contract_drift.drift                # -> clean
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv the domain seed to examples/log-parser/ + rebaseline both manifests</name>
  <files>examples/log-parser/contracts/, examples/log-parser/components/toy-converter/, examples/log-parser/golden/, examples/log-parser/tests/, examples/log-parser/contracts/.hashes/manifest.json, contracts/.hashes/manifest.json</files>
  <read_first>
    - 05-RESEARCH.md §Code Examples (verified git mv sequence) + §Q4 (path-keyed manifest → rebaseline mandatory) + §Pitfall P2 (verbatim only — never edit the dirty seeds).
    - tools/contract_hash/hash.py — `build_manifest(contracts_dir)` is parametrized; `write_manifest(manifest_path)` writes; `--write` targets the root. For the example manifest, call `build_manifest(<repo>/examples/log-parser/contracts)` and write it to `examples/log-parser/contracts/.hashes/manifest.json` (a one-off `python -c` or the `--write` path with an explicit root — see action).
    - tools/golden_runner/tests/test_repr_only.py — a domain test that moves; it imports from `tools.golden_runner.runner` (example→core, allowed) and must resolve its golden case under the example root via the `golden_dir` override added in 05-02.
  </read_first>
  <action>Via Bash `git mv` (verbatim — NO content edits, preserving the tree): move each path in the Interfaces "Move set" to its `examples/log-parser/...` destination (create parent dirs as needed; move directories whole where listed) — this INCLUDES **`libs/dotnet/` WHOLESALE** (`git mv libs/dotnet examples/log-parser/libs/dotnet` — Normalize/ + Normalize.Tests/ + `libs/dotnet/AGENTS.md`; the example's .NET §4.3-4.6 impl; verbatim move — .NET is egress-deferred so any relative csproj ref fixups are out of this env's scope and do NOT affect the pytest suite). **Remove the dead core fixture (checker W-2):** delete the `toy_converter_project` fixture (and any `TOY_CONVERTER_PROJECT`-style default it mirrors) from `tools/golden_runner/tests/conftest.py` — it points at the moved `components/toy-converter`; the domain tests that used it have moved to `examples/`. Move the three domain golden_runner tests + `tests/recorded/*` to `examples/log-parser/tests/`, and update ONLY their import-relative case resolution to pass `golden_dir=<examples>/log-parser/golden` into `run_golden_case`/`compare` (the 05-02 override) — do NOT touch the §4.3-4.6 assertions. Add a minimal `examples/log-parser/tests/conftest.py` (copy the generic `require_dotnet`/`golden_out` fixtures) so the moved .NET-gated tests still SKIP cleanly. **De-pin the core (checker W-1):** in `tools/golden_runner/runner.py` remove the domain `TOY_CONVERTER_PROJECT = REPO_ROOT/"components/toy-converter/..."` default so the core no longer names a relocated artifact — the converter must be supplied via `run_golden_case(converter=...)` (05-02's parametrization); the moved example tests pass the example path. (Keep the `DEFAULT_SOURCE_DIRS`/`components` cleanup for repo_map optional — cosmetic, W-2.) Rebaseline: `uv run python -m tools.contract_hash.hash --write` (root manifest → now only `contracts/normalization/format-conventions.schema.json` + `contracts/sample/greeting.schema.json`). Build the example manifest by running the hasher over the example contracts root and writing `examples/log-parser/contracts/.hashes/manifest.json` (e.g. `uv run python -c "from pathlib import Path; from tools.contract_hash.hash import build_manifest; import json,os; root=Path('examples/log-parser/contracts'); out=root/'.hashes'/'manifest.json'; out.parent.mkdir(parents=True,exist_ok=True); out.write_text(json.dumps(build_manifest(root),indent=2,sort_keys=True)+chr(10))`). Do NOT move `libs/python/normalize`, `libs/normalize-fixtures`, or `format-conventions.schema.json`. Do NOT commit yet (Task 2 regenerates snapshots first, then one commit).</action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift && test -f examples/log-parser/contracts/log-specs/standard-log.schema.json && test -f examples/log-parser/contracts/.hashes/manifest.json && test ! -e contracts/log-specs && git -C . diff --cached --name-status | grep -q '^R' </automated>
  </verify>
  <acceptance_criteria>`git status --short` shows the moves as `R` (renames, history preserved); `git log --follow examples/log-parser/contracts/log-specs/standard-log.schema.json` shows pre-move history. `uv run python -m tools.contract_drift.drift` reads clean against the rebaselined root manifest (which now lists only format-conventions + sample/greeting). The example manifest exists and lists the moved domain schemas. `libs/python/normalize`, `libs/normalize-fixtures`, and `contracts/normalization/format-conventions.schema.json` are UNMOVED.</acceptance_criteria>
  <done>Domain seed + .NET impl relocated via history-preserving renames; root + example manifests rebaselined; drift clean; Python normalize core (libs/python/normalize + libs/normalize-fixtures) untouched; core golden_runner no longer names the relocated toy-converter.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the stale snapshots + land the move through the live gate</name>
  <files>tools/docs_sync/tests/test_docs_sync_determinism.py, tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr, tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr, tools/memory_regen/tests/__snapshots__/test_repo_map_determinism.ambr, tools/memory_regen/contracts_index.py</files>
  <read_first>
    - 05-RESEARCH.md §Pitfall P3 — the three snapshots that break and how (EXPECTED_PAGES hardcodes the 5 domain names; the two .ambr encode domain paths / the toy-converter graph node).
    - tools/docs_sync/tests/test_docs_sync_determinism.py:23-31 — EXPECTED_PAGES set to reduce to {format-conventions, greeting} (05-02 already added greeting; remove the 5 domain names since only correction-rules/standard-log/equipment-* moved and format-conventions stays).
    - tools/memory_regen/contracts_index.py:36-41 — the KIND map has domain families; a `"sample"` entry is optional (the generic sample falls through to "other" — verify the regenerated snapshot reflects the post-move root).
  </read_first>
  <action>Update `EXPECTED_PAGES` in `test_docs_sync_determinism.py` to exactly `{"format-conventions", "greeting"}` (the post-move root contract page set). **Repoint `test_rows_are_sorted_and_typed` (checker W-1):** it does `next((n,s) for n,s in iter_schemas() if n == "standard-log")` with no default → `StopIteration` after the move; change the target to a schema that STAYS (`"format-conventions"` or `"greeting"`) so `rows()` sort/type coverage is preserved, NOT deleted. **Update the core `test_agents_md.py` (checker Blocker-1):** `tools/memory_regen/tests/test_agents_md.py` hardcodes `libs/dotnet/AGENTS.md` at root + the literal `libs/dotnet` in the root-map assertion. Since `libs/dotnet/` (incl. its `AGENTS.md`) moved to `examples/log-parser/`, update `PER_PACKAGE_AGENTS` to drop `libs/dotnet/AGENTS.md` as a CORE per-package plane (the .NET per-package rules now live under `examples/log-parser/libs/dotnet/AGENTS.md`), and update the root-map member assertion to the post-move core set — reconcile with 05-05's root `AGENTS.md` recast so the asserted members match the recast text (05-05 depends on 05-03, so 05-03 sets the test to expect the template shape 05-05 will write; if timing makes this awkward, 05-05 owns the final reconciliation — but 05-03 must at minimum stop asserting `libs/dotnet/AGENTS.md` exists at root). **Catch-all:** after the move, run `uv run pytest` and repoint/regenerate ANY other core test the relocation invalidates (a core test encoding a moved domain path) so the non-example suite is green — do NOT weaken a test's intent, repoint it to surviving material. **Regenerate ALL invalidated snapshots** (checker Blocker-1: the docs-sync module's OWN `.ambr` — `test_docs_sync_determinism` renders every schema from `iter_schemas()` which now yields only {format-conventions, greeting} — plus the two memory snapshots): `uv run pytest tools/docs_sync/tests/test_docs_sync_determinism.py tools/memory_regen/tests/test_contracts_index.py tools/memory_regen/tests/test_repo_map_determinism.py --snapshot-update -q`, then re-run WITHOUT `--snapshot-update` to prove determinism. Optionally add a `"sample": "sample"` (or leave "other" fallback) entry to `tools/memory_regen/contracts_index.py` KIND so the generic sample reads cleanly — regenerate the contracts-index snapshot accordingly. Then land ONE commit for the whole move through the LIVE commit-gate with `GOLDEN_APPROVE_HUMAN` exported (per the precondition): `feat(05-03): relocate log-parser domain seed to examples/log-parser/; rebaseline manifests; regen snapshots (GEN-01)`. Do NOT use `--no-verify`. If the gate reports residual drift, it WARN/passes via the 05-01 approval path (token set) — but the rebaseline should already be clean.</action>
  <verify>
    <automated>uv run pytest tools/docs_sync tools/memory_regen -x -q && uv run pytest</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/docs_sync tools/memory_regen -x -q` green (EXPECTED_PAGES matches; both .ambr snapshots deterministic). `uv run pytest` full non-example suite green (the moved domain tests are under `examples/` which is OUTSIDE `testpaths=[libs/python,tools]`, so they are not collected — core suite stays green). The move commit landed WITHOUT `--no-verify` (git log shows the commit; the pre-commit gate ran).</acceptance_criteria>
  <done>The three invalidated snapshots are regenerated deterministically; the move is committed through the live gate via the sanctioned approval path; the non-example suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git mv (rename) -> commit_gate staged view | Verbatim renames are R, excluded from `--diff-filter=ACM`; a stray content edit would reclassify A/M and re-lint the dirty seeds. |
| moved working tree -> rebaselined manifest | Drift = (working-tree contracts) vs (working-tree manifest); rebaseline before commit makes it clean without HEAD comparison. |
| human token -> live commit-gate | The move is ratified via `GOLDEN_APPROVE_HUMAN` in the process env; never `--no-verify`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | editor rewrites the intentionally-dirty BOM/CRLF golden seeds during move (P2) | mitigate | `git mv` verbatim only; NO content edits; renames stay R (excluded from ACM) so seeds are not re-linted; acceptance checks `git status` shows R. |
| T-05-10 | Repudiation / Tampering | move landed by bash-bypassing the gate (`--no-verify`) | mitigate | Explicitly forbidden; commit lands through the live pre-commit gate with the human token; ADR (05-05) records the change. |
| T-05-11 | Denial of Service | uv workspace breaks (member `libs/python` moved) | mitigate | `libs/python/normalize` + `libs/normalize-fixtures` STAY (uv members / core-imported); only `libs/dotnet/**` (not a uv member, no Python importer) moves with the example; `uv run pytest` green proves the workspace resolves. |
| T-05-12 | Tampering | orphaned drift baseline (manifest still lists moved paths) | mitigate | Mandatory rebaseline (path-keyed manifest, RESEARCH Q4); drift-clean assertion in Task 1 verify. |
| T-05-SC | Tampering | dependency supply chain | accept | Zero new packages — `git mv` + in-repo hasher + snapshot regen; `uv.lock` unchanged. |
</threat_model>

<verification>
- `uv run python -m tools.contract_drift.drift` clean after rebaseline.
- `git status --short` shows moves as `R`; `git log --follow` on a moved schema shows history.
- `uv run pytest tools/docs_sync tools/memory_regen -x -q` green (snapshots regenerated).
- `uv run pytest` full non-example suite green; `examples/` tests not collected by core testpaths.
- Commit landed without `--no-verify` (gate ran).
</verification>

<success_criteria>
The log-parser domain seed is relocated under `examples/log-parser/` via history-preserving renames behind a re-baselined manifest, the live contract-drift gate reads clean after the move, and the non-example suite stays green — Phase 5 success criterion 1 (ADR itself lands in 05-05).
</success_criteria>

<output>
Create `.planning/phases/05-despecialization/05-03-SUMMARY.md` when done.
</output>
