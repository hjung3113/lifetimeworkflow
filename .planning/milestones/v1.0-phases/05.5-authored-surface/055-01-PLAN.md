---
phase: 05.5-authored-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/log-parser/skills/normalization-catalog/SKILL.md
  - examples/log-parser/skills/pipeline-patterns/SKILL.md
  - examples/log-parser/skills/dotnet-conventions/SKILL.md
  - examples/log-parser/agents/dotnet-engineer.md
  - tools/harness_lint/tests/test_skills.py
  - tools/harness_lint/tests/test_agents.py
  - harness/commands/strangler-step.md
  - harness/project.toml
autonomous: true
requirements: [GEN-05]

must_haves:
  truths:
    - "The domain skills normalization-catalog and pipeline-patterns and the instance-language skill dotnet-conventions no longer exist under harness/skills/ — they live under examples/log-parser/skills/."
    - "The instance-language persona dotnet-engineer no longer exists under harness/agents/ — it lives at examples/log-parser/agents/dotnet-engineer.md."
    - "The moves preserve git history (git mv rename, status R)."
    - "harness_lint anti-sprawl pins name exactly the 4 core skills and 4 core personas that remain; the referential-integrity test finds no command dangling to a moved persona; project.toml's dotnet.persona resolves on disk."
    - "The non-example pytest suite (uv run pytest) is green immediately after this plan."
  artifacts:
    - path: "examples/log-parser/agents/dotnet-engineer.md"
      provides: "The moved instance-language persona (history-preserving)"
      contains: "dotnet"
    - path: "tools/harness_lint/tests/test_skills.py"
      provides: "EXPECTED_SKILLS reduced to the 4 core skills (anti-sprawl pin)"
      contains: "EXPECTED_SKILLS"
    - path: "tools/harness_lint/tests/test_agents.py"
      provides: "EXPECTED_PERSONAS reduced to the 4 core personas (anti-sprawl pin)"
      contains: "EXPECTED_PERSONAS"
    - path: "harness/project.toml"
      provides: "dotnet.persona repointed to the moved examples/ persona path"
      contains: "examples/log-parser/agents/dotnet-engineer.md"
  key_links:
    - from: "harness/commands/strangler-step.md"
      to: "harness/agents/orchestrator.md"
      via: "agent: field repointed off the moved dotnet-engineer to a core persona"
      pattern: "agent:\\s*orchestrator"
    - from: "harness/project.toml"
      to: "examples/log-parser/agents/dotnet-engineer.md"
      via: "dotnet.persona instance-slot pointer (instance data may name an instance asset)"
      pattern: "examples/log-parser/agents/dotnet-engineer.md"
    - from: "tools/harness_lint/tests/test_skills.py"
      to: "harness/skills/*/SKILL.md glob"
      via: "EXPECTED_SKILLS frozenset equals the discovered set after the move"
      pattern: "EXPECTED_SKILLS"
---

## Phase Goal

**As a** developer or coding agent turning this log-parser harness into a reusable template, **I want to** relocate the domain and instance-language *authored surface* (domain skills, the .NET skill, the .NET persona) into `examples/log-parser/` and repair every hardcoded expectation the move invalidates, **so that** the core `harness/` carries only domain-neutral authored assets while the non-example test suite stays green.

<objective>
Execute the history-preserving MOVE (Phase 5.5 wave 1) and, in the SAME change, repair the four `harness_lint` expectations the move breaks — so the non-example suite is green the instant this plan lands (per D-01, D-03, D-05; RESEARCH §Summary "one wave, ordered").

Concretely: `git mv` three skills (`normalization-catalog`, `pipeline-patterns`, `dotnet-conventions`) into `examples/log-parser/skills/<name>/` and the `dotnet-engineer` persona into `examples/log-parser/agents/`; reduce `EXPECTED_SKILLS` (7 to 4) and `EXPECTED_PERSONAS` (5 to 4); repoint `strangler-step.md`'s `agent:` off the moved persona to a core persona (and genericize its one line of `.NET parser/converter` prose while the file is open); repoint `project.toml`'s `dotnet.persona` to the new `examples/` path.

Purpose: land the move atomically with its consequences so the suite never RED-flags between steps. This is the load-bearing wave — the prose sweep (055-02) and the guard extension (055-03) depend on the move being complete and the suite green.
Output: 4 relocated authored files, 2 corrected anti-sprawl pins, 1 repointed command agent, 1 repointed persona pointer.

**Plane note:** every file this plan touches is `harness/`, `tools/`, or `examples/` — NONE are constitution plane (`contracts/`, `adr/`, `golden/`). No `GOLDEN_APPROVE_HUMAN` token is required; the commit lands through the ordinary gate.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-authored-surface/055-CONTEXT.md
@.planning/phases/05.5-authored-surface/055-RESEARCH.md
@.planning/phases/05-despecialization/05-05-PLAN.md
@tools/harness_lint/tests/test_skills.py
@tools/harness_lint/tests/test_agents.py
@harness/commands/strangler-step.md
@harness/project.toml

<interfaces>
<!-- The exact edits — each traced to a RED test in RESEARCH Common Pitfalls P1. -->

MOVE SET (git mv, history-preserving; destinations mirror the instance's existing layout):
  harness/skills/normalization-catalog/  -> examples/log-parser/skills/normalization-catalog/
  harness/skills/pipeline-patterns/      -> examples/log-parser/skills/pipeline-patterns/
  harness/skills/dotnet-conventions/     -> examples/log-parser/skills/dotnet-conventions/
  harness/agents/dotnet-engineer.md      -> examples/log-parser/agents/dotnet-engineer.md

FOUR RED TESTS + THEIR FIX (must land WITH the move):
  1. test_skills.py::test_expected_skills_present_no_sprawl
       glob finds 4, EXPECTED_SKILLS pins 7 -> reduce to:
       EXPECTED_SKILLS = {"python-conventions","golden-testing","data-contracts","skill-creator"}
  2. test_agents.py::test_expected_personas_present_no_sprawl
       glob finds 4, EXPECTED_PERSONAS pins 5 -> reduce to:
       EXPECTED_PERSONAS = {"orchestrator","python-engineer","code-reviewer","explorer"}
  3. test_agent_referential_integrity.py::test_command_agent_resolves_to_real_persona[strangler-step]
       strangler-step.md line 6 `agent: dotnet-engineer` no longer resolves -> repoint `agent: orchestrator`
  4. test_language_config.py::test_each_configured_persona_exists
       project.toml line 27 `persona = "harness/agents/dotnet-engineer.md"` file gone ->
       `persona = "examples/log-parser/agents/dotnet-engineer.md"`

NOT affected (do NOT edit — verified in RESEARCH P1 "Not affected"):
  - test_commands.py — no exact-set pin (extras allowed); new-normalization-rule/strangler-step/docs-sync stay valid.
  - test_language_config.py::test_matrix_language_scopes_equal_config — scope equality unaffected by a persona-FILE move.
  - python.persona stays "harness/agents/python-engineer.md" (core, unmoved).

strangler-step.md prose (line 5, done here since the file is open):
  "for one incremental migration step on the .NET parser/converter side; requires /golden parity green."
  -> language-neutral: "for one incremental migration step on the instance's language side; requires /golden parity green."
  (Drops the domain prose per RESEARCH forced-genericization; bare .NET/parser/converter are NOT guard tokens,
   but the repoint + reword belong together in the atomic strangler-step edit.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the 4 domain/instance-language authored assets + fix the two anti-sprawl pins</name>
  <files>examples/log-parser/skills/normalization-catalog/SKILL.md, examples/log-parser/skills/pipeline-patterns/SKILL.md, examples/log-parser/skills/dotnet-conventions/SKILL.md, examples/log-parser/agents/dotnet-engineer.md, tools/harness_lint/tests/test_skills.py, tools/harness_lint/tests/test_agents.py</files>
  <read_first>
    - tools/harness_lint/tests/test_skills.py — `_SKILLS_DIR / "*/SKILL.md"` glob discovery + the `EXPECTED_SKILLS` frozenset (currently 7) asserted equal to the discovered set (anti-sprawl).
    - tools/harness_lint/tests/test_agents.py — `_AGENTS_DIR.glob("*.md")` discovery + the `EXPECTED_PERSONAS` frozenset (currently 5) asserted equal to the discovered set.
    - 055-RESEARCH.md Architecture Patterns "Pattern 1" — glob-based discovery means moved assets simply vanish from the scanned set; the only edit is the expected frozenset.
    - .planning/phases/05-despecialization/05-05-PLAN.md — the Phase-5 `git mv` history-preserving move precedent.
  </read_first>
  <behavior>
    - After the move, `harness/skills/` contains exactly `data-contracts`, `golden-testing`, `python-conventions`, `skill-creator`; `harness/agents/` contains exactly `code-reviewer.md`, `explorer.md`, `orchestrator.md`, `python-engineer.md`.
    - `examples/log-parser/skills/` gains `normalization-catalog/`, `pipeline-patterns/`, `dotnet-conventions/` (each with its `SKILL.md`); `examples/log-parser/agents/` gains `dotnet-engineer.md`.
    - `git status --short` shows the moves as renames (`R`), preserving history.
    - `EXPECTED_SKILLS` equals the 4 remaining core skills; `EXPECTED_PERSONAS` equals the 4 remaining core personas — both anti-sprawl tests pass with no "got […], expected […]" mismatch.
  </behavior>
  <action>Create the destination parents if absent (`examples/log-parser/skills/`, `examples/log-parser/agents/`), then move with `git mv`: `git mv harness/skills/normalization-catalog examples/log-parser/skills/normalization-catalog` and likewise for `pipeline-patterns` and `dotnet-conventions`, and `git mv harness/agents/dotnet-engineer.md examples/log-parser/agents/dotnet-engineer.md`. Do NOT edit the moved files' bodies in this plan — their internal domain prose is legitimate under `examples/` (the guard only scans core). Then edit `tools/harness_lint/tests/test_skills.py`: set `EXPECTED_SKILLS = frozenset({"python-conventions","golden-testing","data-contracts","skill-creator"})` (drop the three moved slugs) and update the module docstring's "seven enumerated skills" phrasing to "four". Edit `tools/harness_lint/tests/test_agents.py`: set `EXPECTED_PERSONAS = frozenset({"orchestrator","python-engineer","code-reviewer","explorer"})` (drop `dotnet-engineer`); update any "five personas" docstring phrasing to "four". Do NOT touch `test_commands.py` or the matrix-scope test (RESEARCH P1 "Not affected").</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_skills.py tools/harness_lint/tests/test_agents.py -x -q && test -f examples/log-parser/agents/dotnet-engineer.md && test ! -e harness/agents/dotnet-engineer.md && git status --short | grep -q '^R'</automated>
  </verify>
  <acceptance_criteria>The 4 assets exist under `examples/log-parser/{skills,agents}/` and are gone from `harness/`; `git status --short` shows renames (`R`); `EXPECTED_SKILLS` = {4 core}, `EXPECTED_PERSONAS` = {4 core}; the two anti-sprawl tests pass.</acceptance_criteria>
  <done>The domain + instance-language authored assets are relocated history-preservingly and the two harness_lint anti-sprawl pins name exactly the remaining core set.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the two references that dangle after the move (command agent + project.toml persona)</name>
  <files>harness/commands/strangler-step.md, harness/project.toml</files>
  <read_first>
    - harness/commands/strangler-step.md — frontmatter `agent: dotnet-engineer` (line 6) + the `.NET parser/converter` prose (line 5) that must both change.
    - tools/harness_lint/tests/test_agent_referential_integrity.py — asserts each command's `agent:` resolves to an existing `harness/agents/<agent>.md`; `orchestrator` is a valid core persona.
    - tools/harness_lint/tests/test_language_config.py::test_each_configured_persona_exists — resolves `_REPO_ROOT / lang["persona"]` and asserts the file exists.
    - harness/project.toml — the `[[languages]]` dotnet block, `persona = "harness/agents/dotnet-engineer.md"` (line 27); the instance-data file is the sanctioned place a core file may name an instance asset (ADR-0002 (c)).
    - 055-RESEARCH.md Common Pitfalls P1 (#3, #4) + Assumptions A1 (orchestrator is the recommended repoint target).
  </read_first>
  <behavior>
    - `strangler-step.md` frontmatter `agent:` resolves to an existing core persona file (`harness/agents/orchestrator.md`) — referential-integrity test green.
    - `strangler-step.md`'s summary prose no longer names ".NET parser/converter" — it reads language-neutral (the instance's language side).
    - `project.toml` `dotnet.persona` points at `examples/log-parser/agents/dotnet-engineer.md`, which exists on disk — persona-existence test green.
    - `python.persona` is unchanged (`harness/agents/python-engineer.md`); the matrix-scope equality test is unaffected.
  </behavior>
  <action>In `harness/commands/strangler-step.md`: change frontmatter `agent: dotnet-engineer` to `agent: orchestrator` (the delegating core persona; A1). In the same file, reword the line-5 summary from ".NET parser/converter side" to "the instance's language side" so no domain prose remains (keep the rest of the command body — its strangler-fig methodology, gates, and ordering — verbatim; logic unchanged). In `harness/project.toml`: change the dotnet block's `persona = "harness/agents/dotnet-engineer.md"` to `persona = "examples/log-parser/agents/dotnet-engineer.md"`. Leave the `python` block, all `bash_scope`/`test`/`format` values, and the `[instance] root` untouched.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_agent_referential_integrity.py tools/harness_lint/tests/test_language_config.py -x -q && grep -q 'examples/log-parser/agents/dotnet-engineer.md' harness/project.toml && grep -q 'agent: orchestrator' harness/commands/strangler-step.md</automated>
  </verify>
  <acceptance_criteria>The referential-integrity + persona-existence tests pass; `project.toml` `dotnet.persona` points at the moved `examples/` path; `strangler-step.md` `agent:` is `orchestrator` and its summary line carries no `.NET parser/converter` prose.</acceptance_criteria>
  <done>No command dangles to a moved persona and no `project.toml` pointer is broken; the two references that the move invalidated are repaired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core `harness/` -> instance `examples/log-parser/` | Authored assets cross from core to instance; the anti-sprawl pins + (in 055-03) the prose guard are the tamper-evidence that the core set stays exactly the intended 4+4. |
| instance-data `project.toml` -> instance persona | The one sanctioned place a core-plane file names an instance asset (ADR-0002 (c)); exempted from the guard in 055-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-055-01 | Tampering | a stray un-moved copy or a re-added domain skill/persona in core | mitigate | `EXPECTED_SKILLS`/`EXPECTED_PERSONAS` pin the exact core set; a re-added or leftover asset fails the anti-sprawl tests. |
| T-055-02 | Repudiation | the move loses git history (cp+rm instead of git mv) | mitigate | `git mv` used; `git status --short` rename (`R`) asserted in verify; `git log --follow` recoverable. |
| T-055-03 | Denial of Service | a dangling command→persona or project.toml pointer RED-flags the suite | mitigate | strangler-step `agent:` + project.toml `dotnet.persona` repointed in the SAME plan as the move (atomic). |
| T-055-SC | Tampering | dependency supply chain | accept | Zero new packages — markdown moves + two test-frozenset edits; `uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint -x -q` green (skills/agents anti-sprawl + referential-integrity + language-config all pass on the post-move tree).
- `uv run pytest` full non-example suite green.
- `git status --short` shows 4 renames (`R`) — history preserved.
- `harness/skills/` = {data-contracts, golden-testing, python-conventions, skill-creator}; `harness/agents/` = {code-reviewer, explorer, orchestrator, python-engineer}.
- `harness/project.toml` `dotnet.persona` = `examples/log-parser/agents/dotnet-engineer.md`; `strangler-step.md` `agent: orchestrator`.
</verification>

<success_criteria>
Success-criterion demo (1) from 055-CONTEXT: `normalization-catalog`, `pipeline-patterns`, `dotnet-conventions`, and `dotnet-engineer` are moved under `examples/log-parser/` and gone from core `harness/`, with the non-example pytest suite (harness_lint expected-lists updated) green — the load-bearing precondition for the 055-02 sweep and 055-03 guard.
</success_criteria>

<output>
Create `.planning/phases/05.5-authored-surface/055-01-SUMMARY.md` when done.
</output>
