---
phase: 5.7
plan: "01"
slug: must-have-inner-loop
wave: 1
status: ready
requirements: [LIFE-01, LIFE-02, LIFE-03, LIFE-04, LIFE-05]
created: 2026-07-09
---

# Phase 5.7 · Plan 01 — MUST-HAVE (inner loop: contract→implement→test→debug)

Fixes the broken inner loop the audit found. Every task updates the relevant `harness_lint`
expected-set **in the same wave** (Phase 5.5 lesson).

## Tasks

### T1 — LIFE-01 `/contract-check` command (resolves the dangling ref)
- **Add** `harness/commands/contract-check.md`. Frontmatter: `description` (with `use`/`when`),
  `agent: python-engineer`, `subtask: true`.
- Body = thin macro: (a) `check-jsonschema --schemafile` each `contracts/**/*.schema.json` instance
  pair; (b) `uv run python -m tools.contract_drift.drift` for the RFC-8785 hash gate. Presence-safe.
- **Verify:** `test_commands` + `test_agent_referential_integrity` green; the `new-contract-rule` +
  `CLAUDE.md` references now resolve.

### T2 — LIFE-02 `golden-debug` skill (the biggest gap)
- **Add** `harness/skills/golden-debug/SKILL.md` + `references/canonicalization-axes.md`.
- Body = decision tree over the 7 axes (RESEARCH §"7 canonicalization axes"): per axis a
  discriminator + which-side-to-fix; ends on the red-golden rule (never edit `.verified`; regressed
  code → fix code; intentional → data case + human approval + ADR). Reuses `tools.golden_runner`
  `.received`/`.verified` paths. Body <500 lines; depth in `references/`.
- **Add** `golden-debug` to `EXPECTED_SKILLS` in `test_skills.py`.
- **Verify:** `test_skills` green (caps, disjoint desc, name==dir).

### T3 — LIFE-03 `polyglot-boundary` skill (Core Value: tribal → skill)
- **Add** `harness/skills/polyglot-boundary/SKILL.md` + `references/canonicalization-table.md`
  (folds the CLAUDE.md table). Single §4.3–4.6 source of truth; points at `libs/normalize-spec.md`
  as the canonical spec. Domain-neutral (no `_PROSE_TOKENS`; `libs/dotnet` forbidden — say "an
  instance's language-side twin").
- **Add** `polyglot-boundary` to `EXPECTED_SKILLS`.
- **Verify:** `test_skills` green; prose guard clean.

### T4 — LIFE-04 neutral engineer template + `/add-language` scaffold
- **Add** `harness/agents/templates/engineer.md` — a GENERIC engineer persona template (subagent
  mode; scope + contract-first + §4.3–4.6 boundary + golden-gate boilerplate; `<LANG>`/`<TOOLCHAIN>`
  placeholders). Lives in `templates/` → OUTSIDE the non-recursive `harness/agents/*.md` glob, so
  `EXPECTED_PERSONAS` stays 4. Note `python-engineer` as the reference instantiation.
- **Add** `harness/commands/add-language.md` (`agent: python-engineer`, `subtask: true`): scaffolds a
  new instance language — derive a persona from the template into `examples/<instance>/agents/`,
  append a `[[languages]]` table to `project.toml`, and (reminder) add the matching `bash_scope` to
  `permission-matrix.json` so the GEN-03 gate stays consistent.
- **Verify:** `test_agents` (personas still exactly 4), `test_commands`, referential-integrity green;
  prose guard clean (template is generic).

### T5 — LIFE-05 de-domain `/new-normalization-rule` → `/new-contract-rule`
- **`git mv`** `harness/commands/new-normalization-rule.md` → `harness/commands/new-contract-rule.md`.
- Neutralize naming: title/description/body "normalization or correction rule" → "contract rule";
  keep the order-enforced contract-first scaffold intact; keep `agent: python-engineer`.
- **Update** the `CLAUDE.md` reference (`new-normalization-rule` mention) to the new name.
- **Verify:** `test_commands` + referential-integrity green; no dangling `new-normalization-rule`.

## Wave 1 gate
`uv run pytest` (non-example) GREEN; `EXPECTED_SKILLS` == {python-conventions, golden-testing,
data-contracts, skill-creator, golden-debug, polyglot-boundary}; no dangling command refs.
