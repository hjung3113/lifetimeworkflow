---
phase: 08-pipeline-topology-conductor-per-component-agents
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - harness/agents/orchestrator.md
  - tools/harness_lint/tests/test_orchestrator_topology.py
autonomous: true
requirements: [PIPE-02]
must_haves:
  truths:
    - "The primary orchestrator reads the declared topology and routes by pipeline stage/component, not only by language"
    - "The orchestrator stays the single mode:primary persona — EXPECTED_PERSONAS stays exactly 4"
    - "The orchestrator's intake procedure includes a 'Trace the topology' step referencing tools.harness_config"
  artifacts:
    - path: "harness/agents/orchestrator.md"
      provides: "Topology-aware conductor: routing table + intake with stage/component routing"
      contains: "topology"
    - path: "tools/harness_lint/tests/test_orchestrator_topology.py"
      provides: "Structural gate asserting the conductor carries stage/component + topology routing signal"
      contains: "def test_"
  key_links:
    - from: "harness/agents/orchestrator.md"
      to: "tools.harness_config components/pipeline"
      via: "intake 'Trace the topology' step"
      pattern: "topology"
    - from: "tools/harness_lint/tests/test_orchestrator_topology.py"
      to: "harness/agents/orchestrator.md"
      via: "read + grep routing/intake for stage/component tokens"
      pattern: "orchestrator.md"
---

<objective>
Evolve the existing primary `orchestrator` persona IN PLACE into a topology-aware conductor: add a
"Trace the topology" intake step and stage/component routing rows to its decision table, keeping it the
SINGLE `mode: primary` persona (conductor IS the evolved orchestrator, not a new role). Add a small
structural gate that pins the topology/stage/component routing signal so the evolution can't silently regress.

Purpose: PIPE-02 — the conductor that reads the Plan 01 topology slot and routes by stage/component.
Output: evolved orchestrator.md + a topology-routing structural gate; EXPECTED_PERSONAS unchanged (4).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-RESEARCH.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-PATTERNS.md

<interfaces>
harness/agents/orchestrator.md current shape (evolve, do NOT recreate):
  frontmatter `name: orchestrator`, `mode: primary` — MUST stay unchanged.
  "## Intake → decompose (minimal procedure)" — numbered 1..5 (Orient / Classify / Decompose / Delegate / Verify).
  "## Routing decision table (work shape → persona / command)" — Markdown 3-col `| Work shape | Route to | Entry command / skill |`.

Topology read API (from Plan 01):
  from tools.harness_config import components, pipeline  # components()->list[dict], pipeline()->dict with .edges

EXPECTED_PERSONAS invariant (tools/harness_lint/tests/test_agents.py):
  frozenset({"orchestrator", "python-engineer", "code-reviewer", "explorer"})  # stays 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve orchestrator into the topology-aware conductor</name>
  <files>harness/agents/orchestrator.md</files>
  <read_first>
    - harness/agents/orchestrator.md (its current description, `mode: primary` frontmatter, the Intake numbered procedure, and the Routing decision table — clone the existing Markdown table shape and the `use`/`when` description-trigger idiom)
    - harness/agents/python-engineer.md (how an instance/language engineer is described, so the new routing rows reference real specialists)
  </read_first>
  <action>
    Edit `harness/agents/orchestrator.md` IN PLACE (keep `name: orchestrator` and `mode: primary` — do NOT add a second
    primary or a `conductor.md`). In the `description`, add that it "routes by pipeline stage/component using the declared
    topology" alongside the existing language-boundary language; preserve the `use`/`when` routing-trigger tokens
    (`test_description_is_routing_signal` greps for them). In "## Intake → decompose", insert a new numbered step
    "**Trace the topology** — read `[[components]]`/`[pipeline]` via `tools.harness_config` (components()/pipeline());
    identify which stage/component the request touches and its upstream/downstream edge contracts" before the Delegate step.
    In the "## Routing decision table", add a **Component** dimension: rows mapping a pipeline stage/component change to the
    instance's component agent (parser/converter/scheduler/collector when declared), with an explicit fallback to the
    language engineer when no component agent is declared; add `/pipeline` as the entry for "Which component owns this stage?".
    Keep all edits domain-neutral prose — the bare words `parser`/`converter`/`scheduler`/`collector` are NOT GEN-04-flagged,
    but do NOT name domain contracts (`standard-log`/`equipment`).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_agents.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `harness/agents/orchestrator.md` frontmatter still has `name: orchestrator` and `mode: primary` (unchanged).
    - `uv run pytest tools/harness_lint/tests/test_agents.py::test_expected_personas_present_no_sprawl -x` exits 0 (EXPECTED_PERSONAS stays 4).
    - The file contains a "Trace the topology" intake step and at least one routing-table row keyed on stage/component + a `/pipeline` entry.
    - `grep -v '^#' harness/agents/orchestrator.md` shows no `standard-log`/`equipment`/`libs/dotnet` tokens (GEN-04 clean).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Structural gate pinning the conductor's topology routing signal</name>
  <files>tools/harness_lint/tests/test_orchestrator_topology.py</files>
  <read_first>
    - tools/harness_lint/tests/test_agents.py (the `parse_frontmatter` import, `_REPO_ROOT = parents[3]` anchoring, and how an existing structural gate reads an agent file and asserts on its content)
    - harness/agents/orchestrator.md (the file this gate reads — after Task 1)
  </read_first>
  <action>
    Create NEW `tools/harness_lint/tests/test_orchestrator_topology.py`. Anchor `_REPO_ROOT = Path(__file__).resolve().parents[3]`.
    Read `harness/agents/orchestrator.md` text and assert: (a) `test_orchestrator_stays_single_primary` — frontmatter
    (via `parse_frontmatter` from `tools.harness_lint`) has `mode == "primary"` and `name == "orchestrator"`;
    (b) `test_conductor_has_topology_intake` — the body contains a case-insensitive "trace the topology" step and the token
    `topology`; (c) `test_conductor_routes_by_stage_component` — the routing section mentions both `stage` and `component`
    (or the component role words) AND references `/pipeline`. Keep the file domain-neutral (no `examples/`, no domain contract tokens).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_orchestrator_topology.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_orchestrator_topology.py -x` exits 0 with 3 tests collected.
    - The gate imports `parse_frontmatter` from `tools.harness_lint` (no hand-sliced `---` fences).
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` exits 0 (new core test file carries no example dependency).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persona set → anti-sprawl gate | A new "conductor" role could smuggle in a second primary |
| agent model: field → policy | Model-identity placeholder-only policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-04 | Info Disclosure (policy) | orchestrator.md model/frontmatter fields | mitigate | No `model:` identifier added; `test_no_real_model_identifier` (in test_agents.py) stays green — only `provider/*-tier` placeholders permitted |
| T-8-05 | Spoofing/EoP (second primary) | persona set | mitigate | Evolve orchestrator in place; `test_expected_personas_present_no_sprawl` asserts EXPECTED_PERSONAS stays exactly 4; new `test_orchestrator_stays_single_primary` pins mode:primary |
| T-8-SC | Supply chain | package installs | accept | N/A — zero package installs this phase |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_agents.py tools/harness_lint/tests/test_orchestrator_topology.py -x` green.
- EXPECTED_PERSONAS unchanged (4); orchestrator stays single primary.
</verification>

<success_criteria>
PIPE-02 satisfied: orchestrator is topology-aware (reads declared topology, routes by stage/component), stays the one primary.
</success_criteria>

<output>
Create `.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-02-SUMMARY.md` when done.
</output>
