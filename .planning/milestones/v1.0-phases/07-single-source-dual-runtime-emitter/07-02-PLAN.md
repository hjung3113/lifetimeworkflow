---
phase: 07-single-source-dual-runtime-emitter
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - tools/harness_emit/project_command.py
  - tools/harness_emit/project_skill.py
  - tools/harness_emit/generate.py
  - tools/harness_emit/validate.py
  - tools/harness_emit/tests/test_mapping.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/
  - .opencode/command/
  - .opencode/skill/
  - .claude/commands/
  - .claude/skills/
autonomous: true
requirements: [EMIT-01, EMIT-02]

must_haves:
  truths:
    - "python -m tools.harness_emit writes the 17 harness commands to .opencode/command/ and .claude/commands/"
    - "The Claude command projection keeps description and drops agent/subtask (no Claude equivalent)"
    - "python -m tools.harness_emit writes the 9 harness skills to .opencode/skill/ and .claude/skills/, copying references/ byte-for-byte"
    - "Harness .claude/commands/*.md do not collide with GSD's .claude/commands/gsd/ subtree"
    - "Skill name/description caps are enforced identically for both runtimes; over-cap aborts writing nothing"
  artifacts:
    - path: "tools/harness_emit/project_command.py"
      provides: "Command projection: opencode keeps agent/subtask, Claude keeps description only"
    - path: "tools/harness_emit/project_skill.py"
      provides: "Skill projection + references/ byte-copy to both trees"
    - path: "tools/harness_emit/tests/test_coexist.py"
      provides: "GSD non-collision assertions for commands"
  key_links:
    - from: "tools/harness_emit/project_skill.py"
      to: "harness/skills/<name>/references/"
      via: "byte-for-byte copy to both .opencode/skill and .claude/skills"
      pattern: "references"
    - from: "tools/harness_emit/validate.py"
      to: "tools.harness_lint.caps"
      via: "skill _NAME_MAX/_DESC_MAX/_BODY_WARN_LINES enforcement"
      pattern: "_DESC_MAX"
---

## Phase Goal

**As a** harness maintainer, **I want to** compile the single runtime-neutral `harness/` source into both opencode and Claude Code artifact trees with loud-fail validators and a CI drift gate, **so that** the runtimes stay byte-faithful to one source. This plan widens the proven pipeline from agents to commands + skills.

<objective>
Widen the Wave-1 walking slice to the two remaining Markdown artifact types: 17 commands and 9 skills (2 with `references/` subtrees). Each adds a projector branch + a validate case + a main() wiring line + a committed emitted surface — no new machinery.

Purpose: Commands and skills are the low-risk widening steps before the higher-risk plugin/config surfaces. Skills exercise the recursive `references/` byte-copy; commands exercise GSD non-collision (`.claude/commands/*.md` vs `.claude/commands/gsd/`).

Output: `project_command.py`, `project_skill.py`, committed `.opencode/{command,skill}/**` + `.claude/{commands,skills}/**` harness slices, and `test_coexist.py` proving GSD command non-collision.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-source-dual-runtime-emitter/07-RESEARCH.md
@.planning/phases/07-single-source-dual-runtime-emitter/07-PATTERNS.md
@tools/harness_emit/generate.py
@tools/harness_emit/project_agent.py
@tools/harness_emit/validate.py

<interfaces>
<!-- Established by Plan 01 — reuse, do not re-invent. -->
tools.harness_emit.generate: REPO_ROOT, HARNESS_DIR, OPENCODE_DIR, CLAUDE_DIR, HarnessEmitError, _confine, main()
tools.harness_emit.project_agent: to_opencode / to_claude pattern to mirror
tools.harness_emit.validate: loud-fail gate; import caps from tools.harness_lint.caps
tools.harness_lint.parse_frontmatter(md_text) -> (dict, body)
tools.harness_lint.caps: _NAME_MAX=64, _DESC_MAX=1024, _BODY_WARN_LINES=500, _NAME_RE, _RESERVED_WORDS, _XML_CHARS, EXPECTED_SKILLS

Command frontmatter (harness/commands/*.md, 17 files): description, agent (slug), subtask (bool when present). Body has !`shell` + $ARGUMENTS (shared by both runtimes).
Skill layout (harness/skills/<name>/SKILL.md, 9 dirs): frontmatter name (== dir) + description. golden-debug/ and polyglot-boundary/ each have a references/ subtree.
Coexistence facts: .claude/commands/ holds only the gsd/ subdir; harness emits top-level .claude/commands/*.md → no collision.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Command projector + Claude non-collision coexist test</name>
  <read_first>
    - harness/commands/build.md and harness/commands/golden.md (frontmatter description/agent/subtask + !`shell` body)
    - tools/harness_emit/project_agent.py (the to_opencode/to_claude pattern to mirror)
    - tools/harness_emit/generate.py (main() wiring + iter_* discovery to extend)
    - tools/harness_emit/validate.py (where to add the command case)
    - tools/harness_lint/tests/test_commands.py (agent-slug well-formed + subtask-boolean rules to mirror in validate)
    - tools/memory_regen/tests/test_hook_wiring.py (coexist-assertion idiom for test_coexist.py)
  </read_first>
  <behavior>
    - project_command.to_opencode(fm) keeps description/agent/subtask; body passed through unchanged
    - project_command.to_claude(fm) keeps description; drops agent + subtask; body passed through unchanged
    - validate.check_command raises HarnessEmitError when agent is not a well-formed slug or subtask is non-boolean
    - all 17 commands emit to both trees; re-emit byte-identical
    - test_coexist: harness .claude/commands/*.md never overlap the .claude/commands/gsd/ subtree
  </behavior>
  <action>Create `tools/harness_emit/project_command.py` mirroring `project_agent.py`: read via `parse_frontmatter`, re-serialize frontmatter from a FIXED ordered template. opencode target `.opencode/command/<n>.md` keeps `description, agent, subtask`; Claude target `.claude/commands/<n>.md` keeps `description` only (drop `agent` + `subtask` — no Claude equivalent). The `` !`shell` `` + `$ARGUMENTS` body is shared verbatim by both. Extend `generate.py` `main()` + discovery to iterate `harness/commands/*.md` (17 files) through the confined write path. Add a command case to `validate.py` mirroring `test_commands.py` (agent well-formed slug; subtask boolean when present) — HARD-fail via HarnessEmitError. Create `tools/harness_emit/tests/test_coexist.py` asserting the emitted harness command set is top-level `.claude/commands/*.md` and is DISJOINT from `.claude/commands/gsd/**` (seed a fixture gsd command and assert it is byte-unchanged and not in the manifest). Extend `test_mapping.py` with a command-shape case. No model identifiers.</action>
  <verify>
    <automated>uv run pytest tools/harness_emit/tests/test_mapping.py tools/harness_emit/tests/test_coexist.py -x</automated>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- .opencode/command .claude/commands</automated>
  </verify>
  <acceptance_criteria>17 commands emit to `.opencode/command/*.md` (with agent/subtask) and `.claude/commands/*.md` (description only, no agent/subtask); test_coexist proves no gsd/ collision; re-emit is byte-identical.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Skill projector + references/ byte-copy + snapshot; commit command+skill surface</name>
  <read_first>
    - harness/skills/golden-debug/SKILL.md and harness/skills/golden-debug/references/ (skill with a references subtree)
    - harness/skills/polyglot-boundary/references/ (the second references subtree)
    - harness/skills/data-contracts/SKILL.md (skill without references — the common case)
    - tools/docs_sync/generate.py write()/_confine (confined copy target)
    - tools/harness_lint/tests/test_skills.py (EXPECTED_SKILLS set + the caps now in caps.py)
    - tools/harness_emit/tests/test_emit_determinism.py (snapshot idiom to extend for the widened tree)
  </read_first>
  <behavior>
    - project_skill emits harness/skills/<name>/SKILL.md to BOTH .opencode/skill/<name>/SKILL.md and .claude/skills/<name>/SKILL.md, keeping name+description identically (divergence None)
    - references/** is copied byte-for-byte to BOTH targets under <name>/references/
    - validate: skill name ≤64 + slug + == dir; description ≤1024 + no <>/reserved word; body >500 lines WARNS (warnings.warn) and still emits
    - an over-cap (>1024) skill description raises HarnessEmitError and writes nothing
    - the emitted set == EXPECTED_SKILLS (9); anti-drift
  </behavior>
  <action>Create `tools/harness_emit/project_skill.py`: discover `harness/skills/<name>/SKILL.md` (9 dirs, pin against `EXPECTED_SKILLS` from `tools.harness_lint.caps`), keep `name, description` identically for both runtimes (Mapping Table divergence = None), and copy any `references/**` byte-for-byte to `.opencode/skill/<name>/references/` AND `.claude/skills/<name>/references/` through `_confine`d write targets. Add the skill validate case to `validate.py` importing `_NAME_MAX`, `_DESC_MAX`, `_BODY_WARN_LINES`, `_NAME_RE`, `_RESERVED_WORDS`, `_XML_CHARS` from `caps.py`: name/description are HARD caps (raise HarnessEmitError, never truncate — resolved to 1024, not 200); body >500 lines is `warnings.warn` ONLY (D-07, still emits). Wire `generate.py` `main()` to emit skills. Extend the committed syrupy `.ambr` snapshot in `tools/harness_emit/tests/__snapshots__/` to cover the widened command + skill projected tree. Commit the emitted `.opencode/{command,skill}/**` + `.claude/{commands,skills}/**` harness slices + updated manifest.</action>
  <verify>
    <automated>uv run pytest tools/harness_emit -x</automated>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- .opencode/skill .claude/skills</automated>
    <automated>test -f .opencode/skill/golden-debug/references/$(ls harness/skills/golden-debug/references | head -1) && test -f .claude/skills/golden-debug/references/$(ls harness/skills/golden-debug/references | head -1)</automated>
  </verify>
  <acceptance_criteria>9 skills emit to both trees with references/ byte-copied; over-cap description raises and writes nothing; a 600-line body warns but still emits; emitted skill set == EXPECTED_SKILLS; command+skill surface committed; re-emit clean.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/skills/<name>/references/ → emitted trees | recursive copy could escape target dir |
| emitter → .claude/commands (GSD gsd/ subtree coexists) | top-level writes must not touch gsd/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | references/ recursive copy path | mitigate | route every copy target through `_confine`; sorted-glob discovery skips symlinks-outside-subtree (docs_sync iter_* defense) |
| T-07-05 | Tampering | over-cap skill description truncation | mitigate | validate HARD-fails on >1024 (raise, write nothing); body>500 warns only (D-07); no slice/truncate |
| T-07-02 | Tampering / DoS-of-workflow | emitted command clobbers gsd/ command | mitigate | harness commands are top-level `.claude/commands/*.md`, disjoint from `.claude/commands/gsd/**`; test_coexist asserts gsd fixture byte-unchanged |
| T-07-SC | Tampering | package installs | accept | zero new deps; uv.lock unchanged |
</threat_model>

<verification>
- `uv run pytest tools/harness_emit -x` green
- 17 commands + 9 skills present in both trees; references/ mirrored
- `git diff --exit-code` clean after re-emit on command/skill paths
- gsd/ command subtree untouched (test_coexist)
</verification>

<success_criteria>
Commands and skills join agents in both runtime trees with correct per-runtime shape, references/ byte-copied, skill caps loud-fail (never truncate), GSD command subtree provably untouched, and re-emit byte-identical.
</success_criteria>

<output>
Create `.planning/phases/07-single-source-dual-runtime-emitter/07-02-SUMMARY.md` when done.
</output>
