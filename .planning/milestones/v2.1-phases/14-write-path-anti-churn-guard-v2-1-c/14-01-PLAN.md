---
phase: 14-write-path-anti-churn-guard-v2-1-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/harness_lint/agreements.py
  - tools/harness_lint/tests/conftest.py
  - tools/harness_lint/tests/test_agreements_predicate.py
  - tools/memory_regen/inject.py
  - tools/memory_regen/tests/conftest.py
  - tools/memory_regen/tests/test_inject_determinism.py
autonomous: true
requirements: [MEM2-04]

must_haves:
  truths:
    - "D-05/D-18: the agreement file-selection predicate exists exactly ONCE, in tools/harness_lint/agreements.py, and tools/memory_regen/inject.py consumes it — no second hand-kept copy exists."
    - "D-14: the shared predicate shares LAYERS L1-L4 ONLY (sorted glob, _/README/symlink exclusion, confinement, fail-closed parse). It does NOT share L5 (status == 'active'), which stays in inject.py as the injector's render policy — so a status: pending typo and every status: retired entry remain reachable by the Phase-14 lint."
    - "D-17: the no-wall-clock static gate is WIDENED to cover tools/harness_lint/agreements.py in the same commit as the extraction, and a negative control proves the widened gate bites (a planted clock token FAILS)."
    - "D-18: the import edge is memory_regen -> harness_lint (the direction inject.py:15 already uses); the submodule is imported directly, __init__.py's __all__/__getattr__ is untouched, and no import cycle exists."
    - "D-06: reads stay confined — non-recursive sorted glob('*.md') (never rglob), no symlink follow, nothing outside agreements_dir."
    - "inject._agreements_block keeps its name and signature; all 7 tests that call it directly stay green (D-17 second clause)."
    - "Phase 13's byte-identity determinism and the ~4000-char budget survive the extraction unchanged."
  artifacts:
    - path: "tools/harness_lint/agreements.py"
      provides: "The single L1-L4 agreement file-selection predicate: iter_agreement_files + load_agreement"
      exports: ["iter_agreement_files", "load_agreement"]
      min_lines: 30
    - path: "tools/harness_lint/tests/test_agreements_predicate.py"
      provides: "Sort/symlink/exclusion/confinement/empty-dir coverage + the D-05 injector-parity test"
      min_lines: 40
    - path: "tools/harness_lint/tests/conftest.py"
      provides: "The single tmp_agreements_tree corpus builder, shared by both consumers"
      contains: "tmp_agreements_tree"
  key_links:
    - from: "tools/memory_regen/inject.py"
      to: "tools/harness_lint/agreements.py"
      via: "from tools.harness_lint.agreements import iter_agreement_files, load_agreement"
      pattern: "from tools\\.harness_lint\\.agreements import"
    - from: "tools/memory_regen/tests/test_inject_determinism.py"
      to: "tools/harness_lint/agreements.py"
      via: "the widened no-wall-clock text scan"
      pattern: "harness_lint/agreements\\.py"
    - from: "tools/memory_regen/tests/conftest.py"
      to: "tools/harness_lint/tests/conftest.py"
      via: "re-export of tmp_agreements_tree + _AGREEMENTS_CREATION_ORDER"
      pattern: "from tools\\.harness_lint\\.tests\\.conftest import"
---

<objective>
Extract the `.memory/agreements/` file-selection predicate out of `inject.py` into one importable
module both the injector and the (Plan 02) provenance lint consume — and widen the no-wall-clock
static gate to cover the new module **in the same commit**, so the extraction does not silently move
code out from under a live Phase-13 guarantee.

Purpose: D-05's "single rule, reused". Two copies of this predicate would drift, and a drift means
the lint and the injector disagree about what an agreement *is*. D-18 settles the direction
(`memory_regen` → `harness_lint`, an edge that already exists at `inject.py:15`); D-14 settles the
scope (L1-L4 only, never L5).
Output: `tools/harness_lint/agreements.py`, a rewired `_agreements_block`, a widened wall-clock gate
with a negative control, one shared fixture corpus, and the D-05 parity test.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-CONTEXT.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-VALIDATION.md
@AGENTS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

The existing edge that makes D-18 free — tools/memory_regen/inject.py:15:
```python
from tools.harness_lint import parse_frontmatter
```

The parser to delegate to (tools/harness_lint/frontmatter.py) — raises ValueError when the
frontmatter block does not parse to a mapping; returns ({}, md_text) when no fence is present:
```python
def parse_frontmatter(md_text: str) -> tuple[dict, str]: ...
```

The predicate to extract, currently INLINE in inject.py:90-115 (not a named function).
Lines 91-95 set up `resolved_base`; 97-104 are L1-L4; **line 105 is L5 and MUST NOT move**:
```python
def _agreements_block(agreements_dir: Path = AGREEMENTS_DIR) -> str:
    base = Path(agreements_dir)
    try:
        resolved_base = base.resolve()
    except OSError:
        return ""
    entries: list[str] = []
    for path in sorted(base.glob("*.md")):                                        # L1
        if path.name.startswith("_") or path.name == "README.md" or path.is_symlink():  # L2
            continue
        try:
            path.resolve().relative_to(resolved_base)                             # L3
            frontmatter, body = parse_frontmatter(path.read_text(encoding="utf-8"))
        except (OSError, ValueError):                                             # L4
            continue
        if str(frontmatter.get("status", "")).strip() != "active":                # L5 — STAYS
            continue
        rendered = _render_agreement(body)
        ...
```

The target shape for the new module (`tools/harness_lint/agreements.py`):
```python
def iter_agreement_files(agreements_dir: Path) -> list[Path]: ...   # L1-L3; [] on OSError
def load_agreement(path: Path) -> tuple[dict, str] | None: ...      # L4; None on OSError/ValueError
```

The live gate that the extraction hollows out — tools/memory_regen/tests/test_inject_determinism.py:70-75:
```python
def test_inject_module_has_no_wallclock(repo_root: Path) -> None:
    text = (repo_root / "tools/memory_regen/inject.py").read_text(encoding="utf-8")
    for token in ("datetime", "date.today", ".now()", "time.time", "time.monotonic"):
        assert token not in text
```

The in-repo negative-control pattern to mirror — tools/harness_lint/tests/test_core_no_workspace_member_dep.py:141-147:
```python
def test_negative_control_flags_synthetic_member_ref() -> None:
    """Scan is live: a crafted core line naming a real member root IS flagged (cannot silently no-op)."""
    marker = sorted(_member_roots())[0]
    hits = _scan_lines("tools/fake_core_module.py", f'x = "{marker}/foo"')
    assert hits, "negative control failed: scan did not flag a synthetic workspace-member reference"
```

The corpus to relocate — tools/memory_regen/tests/conftest.py:82-145 defines
`_AGREEMENTS_CREATION_ORDER` (a deliberately NON-alphabetical 5-tuple) and the `tmp_agreements_tree`
fixture: active x2 (`alpha-ground.md`, `zeta-proceed.md`), retired x1 (`middle-retired.md`),
plus `_TEMPLATE.md` and `README.md`. It is imported by name at test_inject_determinism.py:15.

The sys.path shim already present at tools/harness_lint/tests/conftest.py:15-17 (parents[3] = repo root).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the L1-L4 predicate and widen the no-wall-clock gate in the same commit</name>

  <files>tools/harness_lint/agreements.py, tools/memory_regen/inject.py, tools/memory_regen/tests/test_inject_determinism.py</files>

  <read_first>
    - `tools/memory_regen/inject.py` — in full. The predicate is at 90-115, inline in `_agreements_block`; the `harness_lint` import is at :15; `_AGREEMENTS_MAX_ENTRIES`/`_AGREEMENTS_MAX_CHARS` at :22-23 and the budget loop at :110-115 must stay put.
    - `tools/memory_regen/tests/test_inject_determinism.py` — in full. Lines 70-75 are the gate being widened; 77-87 (`test_hook_wrappers_have_no_wallclock`) is the in-file style precedent for scanning multiple files.
    - `tools/harness_lint/tests/test_core_no_workspace_member_dep.py:135-150` — the negative-control pattern ("prove the scan is live and cannot silently no-op").
    - `tools/harness_lint/frontmatter.py` — `parse_frontmatter`'s contract (raises `ValueError`, returns `({}, md_text)` on no fence).
    - `tools/harness_lint/__init__.py` — the PEP-562 lazy `__getattr__`. Do NOT modify it.
  </read_first>

  <behavior>
    - `iter_agreement_files(dir)` returns a **sorted** list; the corpus's non-alphabetical creation order must not survive into the result.
    - `iter_agreement_files` excludes `_TEMPLATE.md` (leading `_`), `README.md`, and any symlink.
    - `iter_agreement_files` returns `[]` for a nonexistent/unresolvable dir (OSError) and for an empty dir — never raises.
    - `iter_agreement_files` still returns `middle-retired.md` — status is NOT its concern (D-14).
    - `load_agreement(path)` returns `(frontmatter, body)`, or `None` on OSError/ValueError.
    - `inject.assemble(...)` output is byte-identical to before the extraction.
    - The widened gate FAILS when a clock token is planted in `agreements.py` (negative control).
  </behavior>

  <action>
Create `tools/harness_lint/agreements.py` with `from __future__ import annotations`, importing the
parser as `from tools.harness_lint.frontmatter import parse_frontmatter` — the submodule directly,
per D-18. Do NOT add to `tools/harness_lint/__init__.py`'s `__all__` or `__getattr__`: a top-level
eager import there breaks pytest's conftest-collection bootstrap (the hazard the lazy re-export
docstring names).

Export exactly two functions:
- `iter_agreement_files(agreements_dir: Path) -> list[Path]` — lift lines 91-101 of `inject.py`
  verbatim in behavior: `base.resolve()` inside `try/except OSError` returning `[]`; iterate
  `sorted(base.glob("*.md"))` (**`sorted` is load-bearing** — a bare `glob()` returns filesystem
  order and breaks byte-identity determinism; never `rglob`, per D-06); skip
  `path.name.startswith("_") or path.name == "README.md" or path.is_symlink()`; confine via
  `path.resolve().relative_to(resolved_base)` inside `try/except (OSError, ValueError): continue`.
- `load_agreement(path: Path) -> tuple[dict, str] | None` — `parse_frontmatter(Path(path).read_text(encoding="utf-8"))`
  inside `try/except (OSError, ValueError): return None`.

**Do NOT move `inject.py:105`'s `status != "active"` check (L5).** Per D-14 that is the injector's
render policy, not the definition of an agreement file. Sharing it would make D-01's own
`status ∈ {active, retired}` rule unenforceable (a `status: pending` typo would be filtered out
before the Plan-02 lint could flag it) and would leave every retired entry unlinted forever (D-09
keeps them forever). `iter_agreement_files` MUST NOT take a `status` parameter.

**This module must never contain the substring `datetime`, `date.today`, `.now()`, `time.time`, or
`time.monotonic`** — it is about to come under the widened wall-clock gate. Its module docstring must
say so, and must state that L5 is deliberately excluded and why (D-14).

Rewire `inject.py::_agreements_block` to consume both helpers. **Preserve its name and its
`(agreements_dir: Path = AGREEMENTS_DIR) -> str` signature verbatim** — 7 live tests call it directly
as a private (`test_inject_assembler.py:38, 46, 55, 59, 64, 76, 156`). Keep L5, `_render_agreement`,
the empty→`""` return, and the `_AGREEMENTS_MAX_ENTRIES`/`_AGREEMENTS_MAX_CHARS` → `AGREEMENTS_POINTER`
budget branch exactly where they are. Add
`from tools.harness_lint.agreements import iter_agreement_files, load_agreement` alongside the
existing `:15` import; the `parse_frontmatter` import at `:15` stays (it is still used by
`_active_context_pointer`).

Widen `test_inject_determinism.py::test_inject_module_has_no_wallclock` (currently 70-75) to scan
**both** `tools/memory_regen/inject.py` and `tools/harness_lint/agreements.py`. Factor the token
tuple and the scan into a helper taking text so the negative control can reuse it. Then add
`test_negative_control_wallclock_scan_flags_planted_token` mirroring
`test_core_no_workspace_member_dep.py:141-147`: feed the scan a synthetic source string containing a
clock token and assert it is flagged. Without this control a widened-but-broken gate looks green.
Update the module/gate docstring to name both covered files.
  </action>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/memory_regen tools/harness_lint -q` → **0 failed** (baseline before this plan: 313 passed).
    - `grep -n "from tools.harness_lint.agreements import" tools/memory_regen/inject.py` → matches.
    - `grep -c "sorted(" tools/harness_lint/agreements.py` → ≥ 1; `grep -c "rglob" tools/harness_lint/agreements.py` → 0.
    - `grep -Ec "datetime|date\.today|\.now\(\)|time\.time|time\.monotonic" tools/harness_lint/agreements.py` → 0.
    - `grep -n "status" tools/harness_lint/agreements.py | grep -v '^\s*#' | grep -vi "deliberately\|render policy\|D-14"` → no functional status check (D-14: only docstring/comment mentions allowed).
    - `grep -n "status" tools/memory_regen/inject.py` → the `!= "active"` check is still present in `_agreements_block`.
    - `grep -n "def _agreements_block(agreements_dir: Path = AGREEMENTS_DIR) -> str:" tools/memory_regen/inject.py` → matches (name + signature preserved).
    - `grep -n "harness_lint/agreements.py" tools/memory_regen/tests/test_inject_determinism.py` → matches (gate widened).
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q` → all pass, and the run includes a negative-control test whose name contains `negative_control`.
    - **Negative control bites (run manually, then revert):** append `# datetime` to `tools/harness_lint/agreements.py`, run `uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q` → **must FAIL**; revert and re-run → passes. A widened gate that stays green with a planted token is a broken gate.
    - `ruff check tools/harness_lint/agreements.py tools/memory_regen/inject.py && ruff format --check tools/harness_lint/agreements.py tools/memory_regen/inject.py` → clean.
  </acceptance_criteria>

  <done>`agreements.py` holds the only copy of the L1-L4 predicate; `inject.py` consumes it with name/signature/L5/budget intact; the wall-clock gate covers both modules and a planted token provably fails it.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the tmp_agreements_tree corpus to the shared harness_lint conftest</name>

  <files>tools/harness_lint/tests/conftest.py, tools/memory_regen/tests/conftest.py</files>

  <read_first>
    - `tools/memory_regen/tests/conftest.py` — in full. `_AGREEMENTS_CREATION_ORDER` at 82-88 and `tmp_agreements_tree` at 91-145 are what move; the sys.path shim at 26-33 and the other fixtures stay.
    - `tools/harness_lint/tests/conftest.py` — in full (17 lines; has the `parents[3]` sys.path shim, no fixtures yet).
    - `tools/memory_regen/tests/test_inject_determinism.py:15` — `from tools.memory_regen.tests.conftest import _AGREEMENTS_CREATION_ORDER`; this import must keep resolving.
    - `tools/memory_regen/tests/test_inject_determinism.py:89-105` — `test_tmp_agreements_tree_fixture_shape` asserts the corpus shape and the non-alphabetical order; it must stay green.
  </read_first>

  <action>
pytest conftest scope is directory-based: `tmp_agreements_tree` currently lives in
`tools/memory_regen/tests/conftest.py` and is therefore **invisible** to `tools/harness_lint/tests/`.
Task 3's parity test needs both consumers measured against **one** corpus — duplicating the fixture
would reintroduce exactly the drift D-05 exists to eliminate.

Move `_AGREEMENTS_CREATION_ORDER` and the `tmp_agreements_tree` fixture **verbatim** (contents
dict unchanged, including the quoted `added: "2026-01-02"` stamps and the non-alphabetical write
loop) from `tools/memory_regen/tests/conftest.py` into `tools/harness_lint/tests/conftest.py`.
Direction matches D-18 (`memory_regen` → `harness_lint`); the reverse would invert the edge this
phase just settled.

In `tools/memory_regen/tests/conftest.py`, replace the moved code with a re-export:
`from tools.harness_lint.tests.conftest import _AGREEMENTS_CREATION_ORDER, tmp_agreements_tree  # noqa: F401`.
The `noqa: F401` is required (ruff `F` is enabled). This keeps `test_inject_determinism.py:15`'s
import resolving and keeps the fixture injectable into every existing memory_regen test that
requests it. Do NOT touch the sys.path shim, `repo_root`, `tmp_source_tree`, or
`tmp_contracts_tree`.

Update both conftest module docstrings: harness_lint's gains the corpus description (single builder,
shared by both consumers, deliberately non-alphabetical so the sort assertion is falsifiable);
memory_regen's notes the re-export and why the builder moved (D-05 single corpus, D-18 direction).
  </action>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/memory_regen tools/harness_lint -q` → **0 failed**.
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_tmp_agreements_tree_fixture_shape -q` → passes (proves the corpus survived the move intact).
    - `grep -n "def tmp_agreements_tree" tools/harness_lint/tests/conftest.py` → matches.
    - `grep -c "def tmp_agreements_tree" tools/memory_regen/tests/conftest.py` → 0 (moved, not copied — one builder only).
    - `grep -n "from tools.harness_lint.tests.conftest import" tools/memory_regen/tests/conftest.py` → matches and carries `noqa: F401`.
    - `grep -n 'added: "2026-01-02"' tools/harness_lint/tests/conftest.py` → matches (stamps stayed quoted).
    - `ruff check tools/harness_lint/tests/conftest.py tools/memory_regen/tests/conftest.py` → clean.
  </acceptance_criteria>

  <done>One corpus builder exists, in `tools/harness_lint/tests/conftest.py`, injectable from both members; every pre-existing memory_regen test stays green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Assert the predicate's guarantees and the D-05 injector parity</name>

  <files>tools/harness_lint/tests/test_agreements_predicate.py</files>

  <read_first>
    - `tools/harness_lint/agreements.py` — the module under test (Task 1).
    - `tools/harness_lint/tests/conftest.py` — the `tmp_agreements_tree` corpus (Task 2) and its exact file names.
    - `tools/memory_regen/tests/test_inject_assembler.py:72-76` — `test_agreements_reads_are_confined`, the symlink-escape pattern to mirror (creates `outside.md`, symlinks `escape.md` to it, asserts the content is not read).
    - `tools/memory_regen/inject.py` — `_agreements_block`'s L5 + render behavior, for the parity assertions.
    - `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-VALIDATION.md` — Per-Task Verification Map rows 10, 11, 12, 14, 15 are this task's spec.
  </read_first>

  <behavior>
    - Row 14 — **sorted, not filesystem order:** `iter_agreement_files(tmp_agreements_tree)` returns names in sorted order; assert against `_AGREEMENTS_CREATION_ORDER` being non-alphabetical so the test is falsifiable, not vacuous.
    - Row 10 — **exclusion:** `_TEMPLATE.md` and `README.md` are absent from the result (excluded, not flagged).
    - Row 11 — **symlink not followed:** an `escape.md` symlink pointing outside the dir is not returned.
    - Row 12 — **presence-safe:** an empty dir → `[]`; a nonexistent dir → `[]`; neither raises.
    - Row 9/D-14 — `middle-retired.md` **IS** returned by the predicate (status is not a selection concern).
    - Row 15 — **D-05 parity:** the set the predicate selects, filtered by the injector's own L5, matches exactly what `inject._agreements_block` renders; the retired entry is selected but not rendered.
    - `load_agreement` on a malformed/unreadable file returns `None`, not an exception.
  </behavior>

  <action>
Create `tools/harness_lint/tests/test_agreements_predicate.py` with `from __future__ import annotations`,
covering each behavior above as its own test. Import the module under test directly
(`from tools.harness_lint.agreements import iter_agreement_files, load_agreement`) and, for the
parity test only, `from tools.memory_regen import inject`.

The parity test is D-05's invariant turned into an assertion — the thing that makes "the lint and the
injector never disagree about what an agreement IS" checkable rather than a prose claim. Assert
`{p.name for p in iter_agreement_files(tmp_agreements_tree)} == {"alpha-ground.md", "middle-retired.md", "zeta-proceed.md"}`,
then that the L5-filtered subset is `{"alpha-ground.md", "zeta-proceed.md"}`, then that each active
entry's `# ` title appears in `inject._agreements_block(tmp_agreements_tree)` and that the retired
entry's title (`Retired rule`) does **not**. That last assertion is what pins D-14: selected by the
predicate, excluded by the injector's own render policy.

For the sort test, import `_AGREEMENTS_CREATION_ORDER` from the conftest and assert it differs from
its own sorted form **in the test**, so a future fixture that accidentally becomes alphabetical fails
loudly instead of making the sort assertion vacuous (the pattern
`test_inject_determinism.py:105` already establishes).

Never write to the real `.memory/agreements/` — `tmp_path` and the `tmp_agreements_tree` fixture only.
  </action>

  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_agreements_predicate.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_agreements_predicate.py -x -q` → all pass, **≥ 7 tests collected**.
    - The file contains a test whose name matches `parity` and which calls both `iter_agreement_files` and `inject._agreements_block`: `grep -n "parity" tools/harness_lint/tests/test_agreements_predicate.py` → matches.
    - `grep -n "Retired rule" tools/harness_lint/tests/test_agreements_predicate.py` → matches (the D-14 selected-but-not-rendered assertion exists).
    - `grep -n "symlink_to" tools/harness_lint/tests/test_agreements_predicate.py` → matches (row 11).
    - `grep -rn "\.memory/agreements" tools/harness_lint/tests/test_agreements_predicate.py` → **no match** (no real-dir writes).
    - `uv run pytest tools/memory_regen tools/harness_lint -q` → **0 failed**.
    - `uv run pytest tools/harness_emit -q` → **exactly 1 failed / 46 passed** (unchanged; that failure is Phase 15's debt).
    - `git status --porcelain .opencode .claude tools/harness_emit/tests/__snapshots__` → **empty**.
    - `ruff check . && ruff format --check .` → clean.
  </acceptance_criteria>

  <done>Every predicate guarantee (sort, exclusion, symlink, confinement, presence-safety, retired-is-selected) is asserted, and D-05's parity invariant is a live test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.memory/agreements/*.md` → `inject.assemble()` → SessionStart payload | Committed-but-writable files become priority-0 agent directives. This is the T-13-01 surface; this plan preserves its file-selection defenses while relocating them. |
| `tools/harness_lint/agreements.py` → future edits | Extraction moves code out from under a live static gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 (inherited) | Elevation of Privilege | `.memory/agreements/*` → priority-0 directives | mitigate (partial, by design per D-03) | This plan is the enabling refactor: it makes the selection predicate shared and testable so Plan 02's lint can reach **every** agreement file. Durable mitigation lands in Plan 02; shape-not-truth remains the accepted residual (D-03). |
| T-13-04 (inherited) | Tampering | symlink escape out of `agreements_dir` | mitigate | `is_symlink()` skip + `resolve().relative_to(resolved_base)` confinement move verbatim into `iter_agreement_files` (D-06); asserted by the row-11 symlink test. |
| T-13-06 (inherited) | Tampering | filesystem-order nondeterminism | mitigate | `sorted()` preserved verbatim on the glob; asserted against a deliberately non-alphabetical corpus so the test cannot go vacuous. |
| T-14-01 | Tampering | `tools/harness_lint/agreements.py` escaping the no-wall-clock gate | mitigate | D-17: widen the gate to the new module **in the same commit** as the extraction, plus a negative control proving a planted clock token FAILS. |
| T-14-02 | Repudiation | retired / malformed-status entries becoming permanently unlinted | mitigate | D-14: L5 is **not** shared. The predicate returns retired entries; only the injector filters them. Asserted by the parity test's "selected but not rendered" pair. |
| T-14-03 | Tampering | predicate drift between the two consumers | mitigate | Single module + the D-05 parity test + one shared corpus (Task 2) rather than two conftest copies. |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen tools/harness_lint -q` → **0 failed** (baseline 313 passed; this plan adds tests, never subtracts).
- `uv run pytest tools/harness_emit -q` → **exactly 1 failed / 46 passed**. **`0 failed` is a FAILURE CONDITION** — it means the snapshot was updated, which steals Phase 15's gate. Never run `--snapshot-update`.
- `uv run pytest -q` → **exactly 1 failed / 620 passed**.
- `git status --porcelain .opencode .claude tools/harness_emit/tests/__snapshots__` → empty (D-10: source-only).
- `ruff check . && ruff format --check .` → clean.
- Negative-control drill: plant a clock token in `agreements.py` → determinism suite must go RED → revert → green.
</verification>

<success_criteria>
- `tools/harness_lint/agreements.py` is the single home of the L1-L4 predicate; `grep -rn "glob(\"\*.md\")" tools/` shows no second copy in `inject.py`.
- `inject._agreements_block` keeps name + signature; all 7 direct callers green.
- L5 (`status == "active"`) still lives in `inject.py` and nowhere in `agreements.py` (D-14).
- The no-wall-clock gate names both `inject.py` and `agreements.py`, and a planted token fails it (D-17).
- One corpus builder, in `tools/harness_lint/tests/conftest.py`, feeding both members.
- The D-05 parity invariant is an assertion, not prose.
</success_criteria>

<output>
Create `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-01-SUMMARY.md` when done.
</output>

