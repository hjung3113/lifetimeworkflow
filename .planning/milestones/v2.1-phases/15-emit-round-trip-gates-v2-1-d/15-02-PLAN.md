---
phase: 15-emit-round-trip-gates-v2-1-d
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - tools/harness_emit/tests/test_coexist.py
autonomous: true
requirements: [MEM2-06]

must_haves:
  truths:
    - "No model identifier appears anywhere in the emitted trees — placeholder tiers only, verified including command/skill bodies which no gate covers"
    - "GEN-04 core→example independence is green against the regenerated snapshot"
    - "The full non-example suite passes (0 failed / 659 passed)"
    - "The test_coexist module docstring states the correct command count (20), matching the function it documents"
    - "SC1's reference to EXPECTED_COMMANDS is recorded as mis-worded, and the constant is provably not created"
  artifacts:
    - path: "tools/harness_emit/tests/test_coexist.py"
      provides: "Command coexistence gate with a module docstring consistent with its 20-command assertion"
      contains: "20"
  key_links:
    - from: "tools/harness_lint/tests/test_core_no_example_dep.py"
      to: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      via: "git ls-files scan over _CORE_ROOTS (tools, harness, libs)"
      pattern: "tools"
---

<objective>
Prove the phase's gates — no model id anywhere in the emitted trees (including the body coverage gap
no automated gate closes), GEN-04 green against the regenerated snapshot, full suite green — then fix
the one genuine cosmetic staleness Phase 14 left behind and record SC1's mis-wording.

Purpose: Plan 01 does the emit; this plan proves it is safe to ship and unblocks PR #3. SC2 and SC3
are asserted here rather than assumed. It also holds the line on `EXPECTED_COMMANDS`: the criterion
names a constant that does not exist, and the correct outcome is to record the mis-wording, not to
invent the constant.

Output: a one-line docstring correction, a recorded criterion deviation, and executed gate evidence.
No functional change.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-emit-round-trip-gates-v2-1-d/15-RESEARCH.md
@.planning/phases/15-emit-round-trip-gates-v2-1-d/15-VALIDATION.md
@.planning/phases/15-emit-round-trip-gates-v2-1-d/15-01-SUMMARY.md
</context>

<critical_constraints>
**Do NOT create `EXPECTED_COMMANDS`.** This is the plan that formally records why. `caps.py` defines
`EXPECTED_PERSONAS`, `EXPECTED_SKILLS`, `EXPECTED_TEMPLATES` — there is deliberately no commands
equivalent, because `test_commands.py` is glob-driven (which is exactly why `agree.md` was auto-covered
in Phase 14 with zero test edits). Inventing the frozenset would add the anti-sprawl maintenance
burden the glob design avoids. Phase 14 ruled this way already (D-11); Phase 15 holds the same line.

**Do NOT "fix" the command count.** `test_all_20_commands_emit_to_both_trees` was already bumped 19→20
by Phase 14 (`fa1aea8`) and already passes, because `_emit(tmp_path)` counts the runtime-neutral
SOURCE, not the committed trees. Only the module DOCSTRING is stale. No assertion changes.

**Out of scope — do not let these contaminate the phase:**
- Adding a local test that reads the committed `.opencode/`/`.claude/` trees (real structural gap, but new
  test surface beyond MEM2-06 in a change-no-code phase — it is carried as a follow-up candidate).
- The `ruff check` CI gate and the ~57 pre-existing E-codes in `tools/` plus 2 format failures. Pre-existing
  debt, unrelated to MEM2-06. Only ensure this phase adds no NEW lint debt.
- `/agree` lacking `subtask: true` in its opencode projection. That is an authored Phase-14 SOURCE decision;
  the emitter faithfully projects what is authored. Changing it means editing `harness/commands/agree.md` — a
  source change, out of scope. Flag only if a reviewer raises it.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Prove the model-id and GEN-04 gates against the emitted trees</name>

  <files>(verification only — no files modified)</files>

  <read_first>
    - `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-01-SUMMARY.md` — confirm the emit landed and read the ACTUAL measured delta before asserting anything about the trees
    - `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-RESEARCH.md` — § Gate Inventory (esp. the SC2 "coverage nuance": no gate greps emitted BODIES for a model id) and § GEN-04
    - `tools/harness_emit/validate.py` — read `check_agent` (~lines 84-88) and `check_opencode_config` (~lines 216-220) to see exactly what the model-id gates DO and DO NOT scan
    - `tools/harness_lint/caps.py` — confirm `PLACEHOLDER_MODEL` / the placeholder-tier regex, and confirm no `EXPECTED_COMMANDS` symbol exists
    - `tools/harness_lint/tests/test_core_no_example_dep.py` — read `_CORE_ROOTS` and the forbidden-token list so your manual scan uses the real tokens, not invented ones
  </read_first>

  <action>
    Precondition: Plan 01 must be complete and committed. Verify with
    `git ls-files --error-unmatch .opencode/command/agree.md`. If it fails, stop — this plan proves
    Plan 01's output and has nothing to prove without it.

    Step 1 — Run the automated model-id gates:
    `uv run pytest tools/harness_lint/tests/test_opencode_json.py tools/harness_lint/tests/test_agents.py -k model`.
    These cover the `model` frontmatter key on agents and the `model`/`*_model` values in
    `opencode.json` against `^provider/[a-z0-9-]+-tier$`.

    Step 2 — Close the coverage gap MANUALLY, because SC2 says "no model id appears ANYWHERE in the
    emitted trees" and the automated gates only read frontmatter keys and `opencode.json` values —
    **no gate greps emitted command/skill bodies**. It is covered only transitively (bodies are byte
    copies of `harness/` source, and source agents are scanned), so a model id sitting in a command or
    skill BODY would ship unnoticed. Grep the emitted trees (`.opencode/`, `.claude/commands`,
    `.claude/skills`, `opencode.json`) case-insensitively for real provider identifiers — the families
    the researcher used: `claude-` followed by opus/sonnet/haiku, `gpt-` followed by a digit, `gemini-`
    followed by a digit, and `o1-`/`o3-mini`. Expect ZERO hits. The only model values legitimately
    present are the placeholder tiers `provider/explorer-tier` and `provider/implementer-tier`. Do NOT
    add a new gate for this — one grep in verification is the agreed scope; report the result instead.

    Step 3 — Run GEN-04 AFTER Plan 01's `.ambr` regen (which is where the hazard lives — the snapshot
    is a tracked core-plane file under `tools/` that embeds every body verbatim, and has leaked domain
    tokens twice before): `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py`.

    Step 4 — Run the full non-example suite: `uv run pytest`. Expect `0 failed / 659 passed`.

    Step 5 — Prove `EXPECTED_COMMANDS` was NOT invented: `grep -rn "EXPECTED_COMMANDS" tools/ harness/ libs/`
    must return zero hits. Every hit anywhere in the repo should remain confined to `.planning/` prose
    documenting its non-existence.

    Step 6 — Re-run the emit-drift replica as the final integrity check:
    `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json`.

    This task modifies nothing. Record the evidence for the summary.
  </action>

  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_opencode_json.py tools/harness_lint/tests/test_agents.py -k model</automated>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py</automated>
    <automated>uv run pytest</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_opencode_json.py tools/harness_lint/tests/test_agents.py -k model` passes with no failures.
    - `grep -rniE 'claude-[0-9.]*-?(opus|sonnet|haiku)|gpt-[0-9]|gemini-[0-9]|o1-|o3-mini' .opencode .claude/commands .claude/skills opencode.json` returns NO matches (exit 1).
    - `grep -rn 'provider/' .opencode/agent 2>/dev/null | grep -v 'provider/[a-z0-9-]*-tier'` returns no matches — every model value is a placeholder tier.
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py` passes.
    - `uv run pytest` reports `0 failed` and `659 passed`.
    - `grep -rn "EXPECTED_COMMANDS" tools/ harness/ libs/` returns zero hits (exit 1).
    - `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` exits 0.
    - `git status --porcelain` shows no tracked-file changes from this task (verification-only).
  </acceptance_criteria>

  <done>SC2 and SC3 are proven by execution: zero model identifiers anywhere in the emitted trees including bodies, GEN-04 green against the regenerated snapshot, full suite green, emit-drift replica clean, and EXPECTED_COMMANDS provably not created.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale test_coexist module docstring and record the SC1 mis-wording</name>

  <files>tools/harness_emit/tests/test_coexist.py</files>

  <read_first>
    - `tools/harness_emit/tests/test_coexist.py` — read the full file, especially line 3's module docstring ("The emitter writes its 19 harness commands…") and `test_all_20_commands_emit_to_both_trees` (~line 39) plus its own docstring, which Phase 14 already bumped. The module docstring is the only stale artifact.
    - `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-RESEARCH.md` — § Mis-Worded Criteria in This Phase's Own Source (both the `EXPECTED_COMMANDS` ruling and the "counts already updated" finding)
    - `harness/commands/` — confirm the live source count is 20 by listing it, rather than trusting the number in this plan
  </read_first>

  <action>
    Step 1 — Confirm the count from the source of truth, not from prose: list `harness/commands/*.md`
    and confirm there are exactly 20. The emitter's `iter_commands` is a non-recursive `glob("*.md")`
    over that directory, so the directory listing IS the count.

    Step 2 — Fix ONLY the module docstring in `tools/harness_emit/tests/test_coexist.py`. Line 3
    currently reads "The emitter writes its 19 harness commands as TOP-LEVEL ``.claude/commands/*.md``
    (Claude) and ``.opencode/command/*.md`` (opencode)." Change the number 19 to 20 so the module
    docstring agrees with `test_all_20_commands_emit_to_both_trees`, which Phase 14 already bumped
    (`fa1aea8`) and which already passes. Phase 14 updated the function and the function's docstring
    but missed the module docstring — this is the entire fix.

    Change NOTHING else in this file: no assertion, no constant, no test name, no new test. This is a
    comment-level correction; no gate reads the module docstring.

    Step 3 — Confirm no other stale "19" survives in the emitter test surface. Grep `tools/harness_emit/`
    for a standalone `19` in a commands context and confirm the docstring you just fixed was the only
    one. Do not chase `19` occurrences that are unrelated (line numbers, other counts).

    Step 4 — Run `uv run pytest tools/harness_emit -q` and then the full suite to confirm the
    docstring edit changed no behavior.

    Step 5 — Record the SC1 deviation for the summary (this is a documentation act, not a code change):
    Success Criterion 1 states that "`EXPECTED_COMMANDS` are updated to match." The constant does not
    exist in any source file and was deliberately never created — `test_commands.py` is glob-driven by
    design, and Phase 14 ruled the same way (D-11). SC1's SUBSTANCE is fully satisfied by the emit plus
    the `.ambr` regen in Plan 01. The criterion is MIS-WORDED, not unmet — this is the third such
    instance in this milestone's own source. Do not fail the phase on SC1's literal wording, and do not
    invent the constant to satisfy it.

    Step 6 — Commit. No model identifier in the message.
  </action>

  <verify>
    <automated>uv run pytest tools/harness_emit -q</automated>
    <automated>uv run pytest</automated>
  </verify>

  <acceptance_criteria>
    - `sed -n '3p' tools/harness_emit/tests/test_coexist.py | grep -q '20'` succeeds and the same line no longer contains `19`.
    - `ls harness/commands/*.md | wc -l` returns 20, matching the corrected docstring.
    - `git diff HEAD~1 HEAD -- tools/harness_emit/tests/test_coexist.py | grep -c '^[+-][^+-]'` equals 2 (exactly one line changed — docstring only).
    - `uv run pytest tools/harness_emit/tests/test_coexist.py` passes, including `test_all_20_commands_emit_to_both_trees`.
    - `uv run pytest` reports `0 failed` and `659 passed`.
    - `grep -rn "EXPECTED_COMMANDS" tools/ harness/ libs/` still returns zero hits.
    - `git diff HEAD~1 HEAD --name-only` lists only `tools/harness_emit/tests/test_coexist.py`.
  </acceptance_criteria>

  <done>The test_coexist module docstring states 20, matching both the live source count and the assertion it documents; no behavior changed; and SC1's EXPECTED_COMMANDS reference is recorded as mis-worded rather than satisfied by inventing the constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Emitted trees → distributed harness consumers | Content that ships. A model identifier or domain token crossing here escapes the repo and violates CLAUDE.md's 모델 아이덴티티 constraint. |
| Core plane (`tools/`, `harness/`, `libs/`) → example instance (`examples/`) | GEN-04's one-way independence boundary. The regenerated `.ambr` sits on the core side while embedding every body. |
| Success criteria (`.planning/` prose) → repo source | A mis-worded criterion can pressure an executor into fabricating source to match prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03b | Information disclosure | Emitted command/skill BODIES — model-id leak outside gate coverage | mitigate | The automated gates (`check_agent`, `check_opencode_config`, `test_opencode_json.py:51`) scan only the `model` frontmatter key and `opencode.json` values. **No gate greps bodies.** Coverage is transitive only (bodies are byte-copies of `harness/` source). Closed here by an explicit Task-1 grep across `.opencode/`, `.claude/commands`, `.claude/skills`, `opencode.json` for `claude-*opus\|sonnet\|haiku`, `gpt-N`, `gemini-N`, `o1-`/`o3-mini` → must be 0 hits. Deliberately verification-only, not a new gate (change-no-code phase). |
| T-15-04b | Information disclosure | Regenerated `.ambr` — GEN-04 domain-token leak into the core plane | mitigate | Verified by running `test_core_no_example_dep.py` AFTER Plan 01's regen. `_CORE_ROOTS = ("tools", "harness", "libs")` enumerated via `git ls-files`, so the tracked `.ambr` is in scope and embeds every body verbatim. Two in-repo precedents (08-01, 10-01 prose leaks). Pre-verified 0 hits across all 12 tokens — verified anyway, not assumed. |
| T-15-08 | Tampering | Test surface — fabricating `EXPECTED_COMMANDS` to satisfy mis-worded SC1 | mitigate | Inventing a constant to make prose true is source tampering driven by a documentation defect, and it would duplicate the glob while adding drift surface. Mitigated by an explicit plan prohibition, the Phase-14 D-11 precedent, and the acceptance criterion `grep -rn "EXPECTED_COMMANDS" tools/ harness/ libs/` → 0 hits. The correct action is recording the mis-wording. |
| T-15-09 | Tampering | `tools/harness_emit/tests/test_coexist.py` — scope creep from a docstring fix into assertion changes | mitigate | The count assertion is ALREADY correct (Phase 14 bumped 19→20; `_emit(tmp_path)` counts SOURCE, not the trees — the D-10 error that survived planner and checker). Only the module docstring is stale. Bounded by the acceptance criterion of exactly one changed line and by re-running the suite to prove zero behavior change. |
| T-15-10 | Repudiation | Model identifier in commit messages | mitigate | No gate scans commit messages; the model-id gates read frontmatter and `opencode.json` only. Process-only: Task 2 requires a model-id-free message. Verified by human read at review. |
| T-15-SC | Tampering | Supply chain — package installs | accept | **No installs.** This plan changes one docstring and runs existing tests. `uv.lock` must not change; no `## Package Legitimacy Audit` is required because nothing is installed; no `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<verification>
1. `uv run pytest tools/harness_lint/tests/test_opencode_json.py tools/harness_lint/tests/test_agents.py -k model` → green (SC2, automated portion)
2. `grep -rniE 'claude-[0-9.]*-?(opus|sonnet|haiku)|gpt-[0-9]|gemini-[0-9]|o1-|o3-mini' .opencode .claude/commands .claude/skills opencode.json` → 0 hits (SC2, body coverage gap)
3. `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py` → green (SC3, GEN-04)
4. `uv run pytest` → 0 failed / 659 passed (SC3, full suite)
5. `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` → exits 0 (SC2, emit-drift)
6. `grep -rn "EXPECTED_COMMANDS" tools/ harness/ libs/` → 0 hits
7. `sed -n '3p' tools/harness_emit/tests/test_coexist.py` → says 20, not 19
</verification>

<success_criteria>
- Zero model identifiers anywhere in the emitted trees, bodies included; placeholder tiers only.
- GEN-04 green after the `.ambr` regen; full non-example suite 0 failed / 659 passed.
- Emit-drift replica clean over the documented 8-path set.
- `test_coexist` module docstring says 20, matching the live source count and its own assertion.
- `EXPECTED_COMMANDS` still does not exist; SC1's reference to it is recorded as mis-worded.
- No new lint debt; `uv.lock` unchanged.
</success_criteria>

<output>
Create `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-02-SUMMARY.md` when done.

Record: the model-id grep result across the emitted trees (including the body-coverage gap that no
automated gate closes); the GEN-04 verdict post-regen; the final suite numbers; the **SC1 deviation**
— `EXPECTED_COMMANDS` does not exist, was deliberately not created (Phase-14 D-11 precedent), and
SC1's substance is met by the emit plus the `.ambr` regen — flagged as the third mis-worded criterion
this milestone; and the **downstream payoff**: PR #3's two red jobs (`core-suite`, `emit-drift`) were
both this one re-emit debt, so settling it unblocks ratification of ADR-0004/0005/0006/0007, which
`gate.needs` was blocking.

Carry forward as follow-up candidates (deliberately out of scope): a local test guarding the committed
trees (the structural gate-theft hole); whether `ruff check` should become a CI gate (~57 pre-existing
E-codes); and whether `/agree` needs `subtask: true` in its opencode projection (a source decision).
</output>
</content>
