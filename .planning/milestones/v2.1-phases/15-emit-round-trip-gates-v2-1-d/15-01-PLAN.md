---
phase: 15-emit-round-trip-gates-v2-1-d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/command/agree.md
  - .claude/commands/agree.md
  - .opencode/command/orient.md
  - .claude/commands/orient.md
  - .opencode/command/checkpoint.md
  - .claude/commands/checkpoint.md
  - .opencode/command/lint.md
  - .claude/commands/lint.md
  - .opencode/skill/two-plane-memory/SKILL.md
  - .claude/skills/two-plane-memory/SKILL.md
  - .opencode/plugin/session-inject.ts
  - AGENTS.md
  - tools/harness_emit/emit-manifest.json
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
autonomous: true
requirements: [MEM2-06]

must_haves:
  truths:
    - "Both committed runtime trees carry /agree — .opencode/command/agree.md and .claude/commands/agree.md exist and are tracked"
    - "The committed trees carry the Phase-13/14 body edits to orient, checkpoint, lint, and the two-plane-memory skill"
    - "The AGENTS.md HARNESS-MANAGED command index lists agree"
    - "A fresh re-emit produces zero diff over the documented emit-drift path set"
    - "The projected-tree snapshot fixture matches the harness/ source (test_projected_tree_matches_committed_snapshot is green)"
  artifacts:
    - path: ".opencode/command/agree.md"
      provides: "opencode projection of /agree"
      contains: "agent: orchestrator"
    - path: ".claude/commands/agree.md"
      provides: "Claude projection of /agree (description-only frontmatter)"
    - path: "tools/harness_emit/emit-manifest.json"
      provides: "Regime A ownership manifest including both agree.md paths"
      contains: ".opencode/command/agree.md"
    - path: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      provides: "Regenerated projected-tree snapshot matching current harness/ source"
  key_links:
    - from: "harness/commands/agree.md"
      to: ".opencode/command/agree.md"
      via: "tools.harness_emit glob discovery (iter_commands) + project_command"
      pattern: "agree"
    - from: "harness/commands/agree.md"
      to: "AGENTS.md HARNESS-MANAGED block"
      via: "merge.splice_managed_block deterministic command index"
      pattern: "agree"
---

<objective>
Run the Phase-7 emitter and commit exactly what it writes, so the committed `.opencode/` and
`.claude/` trees stop lagging the `harness/` source, then regenerate the one stale snapshot fixture.

Purpose: Phase 13 edited three command bodies, one skill body, and one plugin; Phase 14 authored
`harness/commands/agree.md` — all source-only. None were ever projected. This is the accumulated
re-emit debt that reds BOTH failing CI jobs on PR #3 (`core-suite` + `emit-drift`), which
`gate.needs` blocks on, which in turn blocks ratification of ADR-0004/0005/0006/0007.

Output: 2 new emitted files, 8 changed emitted files, 1 spliced `AGENTS.md` line, +2 manifest
entries, 1 regenerated `.ambr`. **Zero code changes** — the emitter is glob-driven and already
discovers `/agree`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-emit-round-trip-gates-v2-1-d/15-RESEARCH.md
@.planning/phases/15-emit-round-trip-gates-v2-1-d/15-VALIDATION.md
</context>

<critical_constraints>
**This phase changes no code.** `tools/harness_emit/**.py` must not be edited. If you believe the
emitter needs a change to pick up `/agree`, you are wrong — `iter_commands` is a non-recursive
`glob("*.md")` over `harness/commands/` and the researcher proved by execution that it already
emits 84 artifacts including both `agree.md` projections.

**Every file this plan touches is machine-owned (DERIVED).** Do not hand-write a single byte into
`.opencode/`, `.claude/`, `AGENTS.md`'s managed block, `emit-manifest.json`, or the `.ambr`. Hand
edits are precisely what the `emit-drift` gate exists to catch.

**Do NOT create `EXPECTED_COMMANDS`.** Success Criterion 1 names it; it does not exist in any source
file (verified: 0 hits across `tools/`, `harness/`, `libs/`). `test_commands.py` is glob-driven by
design. Phase 14 already ruled against inventing it (D-11). SC1 is mis-worded — Plan 02 records that.

**Do NOT expect `opencode.json` or `.claude/settings.json` to change.** The researcher diffed both
against a real isolated emit: both come out byte-identical. The emitter rewrites them to the same
bytes. If either shows a diff after the emit, STOP and report — that is an unexpected finding.

**Ordering is mandatory: emit FIRST, snapshot-regen SECOND.** See `<gate_theft_guard>` below.
</critical_constraints>

<gate_theft_guard>
The single most important constraint in this phase.

`test_projected_tree_matches_committed_snapshot` renders from **`harness/` source** and compares to
the `.ambr`. It **never opens `.opencode/` or `.claude/`**. No local test anywhere reads the
committed runtime trees. Therefore:

> Running `--snapshot-update` **alone** yields `0 failed / 659 passed` while the committed trees are
> still missing `/agree`. A totally green local suite over a broken repo.

Regenerating the `.ambr` IS legitimate and IS required here (it is the "fixtures updated" half of
SC1 — the snapshot is genuinely stale versus source). **The theft is regenerating it INSTEAD OF
emitting.** A green `pytest` is NOT proof this phase succeeded.

The only proof is the local replica of the CI `emit-drift` gate (`.github/workflows/ci.yml:197`):

`uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json`

Task order in this plan (emit → verify non-empty `git status` → regen → suite) is not a suggestion.
</gate_theft_guard>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the delta, run the emitter, commit its output</name>

  <files>.opencode/command/agree.md, .claude/commands/agree.md, .opencode/command/orient.md, .claude/commands/orient.md, .opencode/command/checkpoint.md, .claude/commands/checkpoint.md, .opencode/command/lint.md, .claude/commands/lint.md, .opencode/skill/two-plane-memory/SKILL.md, .claude/skills/two-plane-memory/SKILL.md, .opencode/plugin/session-inject.ts, AGENTS.md, tools/harness_emit/emit-manifest.json</files>

  <read_first>
    - `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-RESEARCH.md` — § Measured Emit Delta (the expected file set), § The Gate-Theft Trap, § Common Pitfalls (esp. Pitfall 4: bare `git diff` misses untracked files)
    - `.github/workflows/ci.yml` lines 185-200 — the `emit-drift` job; line 197 is the verbatim documented path set you must replicate locally
    - `tools/harness_emit/generate.py` — read `emit()` and the `iter_commands`/`iter_skills`/`iter_agents`/`iter_plugins` glob discovery and `main()` (line ~463) to confirm the entrypoint takes NO flags before running it
    - `harness/commands/agree.md` — the Phase-14 source being projected for the first time
    - `AGENTS.md` — see the current HARNESS-MANAGED marker fence and the Commands index line, so you recognize the one-line splice when it happens
  </read_first>

  <action>
    Re-measure before trusting anything. The research delta is a snapshot of the researcher's working
    tree (Assumption A2) — confirm it against the live tree rather than assuming file counts.

    Step 1 — Establish a clean baseline. Run `git status --porcelain`. Expect it to show no
    modifications to tracked files (an untracked `.mcp.json` is pre-existing and unrelated — leave it
    alone, never stage it). If tracked files are already dirty, STOP and report: an unexpected
    working-tree state invalidates the measured delta.

    Step 2 — Record the pre-emit baseline count. Run `uv run pytest` and confirm the baseline is
    exactly `1 failed / 658 passed`, the single red being
    `tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot`.
    That red is SANCTIONED and is the gate this phase settles by emitting. If the suite is already
    `0 failed`, STOP and report — someone has already updated the `.ambr` and the gate is stolen.

    Step 3 — Run the emitter: `uv run python -m tools.harness_emit`. It takes no flags. It writes
    both runtime trees, rewrites `opencode.json` and the manifest, splices the `AGENTS.md` and
    `CLAUDE.md` managed blocks, and merges `.claude/settings.json`.

    Step 4 — Assert the emit actually did something: `git status --porcelain`. It MUST be non-empty.
    Expect, per the measured delta: 2 NEW untracked files (`.opencode/command/agree.md`,
    `.claude/commands/agree.md`); 8 modified files (`orient.md`, `checkpoint.md`, `lint.md` in both
    `.opencode/command/` and `.claude/commands/`; `two-plane-memory/SKILL.md` in both `.opencode/skill/`
    and `.claude/skills/`; `.opencode/plugin/session-inject.ts`); modified `AGENTS.md`; modified
    `tools/harness_emit/emit-manifest.json`. If `git status` is EMPTY, STOP — the emit was a no-op and
    the phase premise is broken.

    Step 5 — Verify the explicitly-unchanged set. `opencode.json` and `.claude/settings.json` must NOT
    appear in `git status`, and no file under `.claude/agents/` or `.opencode/agent/` may appear (no
    agent changed this milestone; the `gsd-*.md` files are GSD-owned and must stay untouched). If any
    of them appear as modified, STOP and report rather than committing.

    Step 6 — Inspect the diff for correctness, not just presence. Confirm `AGENTS.md` changed on
    exactly ONE line — the Commands index inside the HARNESS-MANAGED fence gaining `agree` in sorted
    position; nothing outside the fence may move (Regime B splice-only). Confirm
    `emit-manifest.json` gained exactly two entries: `.claude/commands/agree.md` and
    `.opencode/command/agree.md`, with nothing pruned.

    Step 7 — Stage everything including the untracked new files with `git add -A -- .opencode .claude AGENTS.md tools/harness_emit/emit-manifest.json`
    (`git add -A`, not bare `git add` on tracked paths — Pitfall 4: the two `agree.md` files are
    untracked and a bare `git diff` would miss them entirely, producing a false pass).

    Step 8 — Commit the emit output on its own, before any snapshot work. Message body must describe
    the re-emit and must contain NO model identifier (CLAUDE.md 모델 아이덴티티 constraint; no gate
    scans commit messages, so this is agent discipline).
  </action>

  <verify>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json</automated>
    <automated>uv run pytest tools/harness_emit/tests/test_coexist.py::test_all_20_commands_emit_to_both_trees</automated>
    <automated>test -f .opencode/command/agree.md && test -f .claude/commands/agree.md && git ls-files --error-unmatch .opencode/command/agree.md .claude/commands/agree.md</automated>
  </verify>

  <acceptance_criteria>
    - `.opencode/command/agree.md` and `.claude/commands/agree.md` both exist AND are tracked by git (`git ls-files --error-unmatch` exits 0 for both).
    - After the commit, a fresh `uv run python -m tools.harness_emit` followed by `git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` exits 0 (empty diff).
    - `git show --stat HEAD` lists `AGENTS.md` and `tools/harness_emit/emit-manifest.json` but does NOT list `opencode.json` or `.claude/settings.json`.
    - `git diff HEAD~1 HEAD -- AGENTS.md | grep -c '^[+-][^+-]'` equals 2 (exactly one line replaced inside the managed fence).
    - `grep -c 'agree' tools/harness_emit/emit-manifest.json` returns 2.
    - `uv run pytest tools/harness_emit/tests/test_coexist.py::test_all_20_commands_emit_to_both_trees` passes.
    - `git diff HEAD~1 HEAD --name-only | grep -E '^\.claude/agents/|^\.opencode/agent/'` returns no matches (exit 1).
    - `uv run pytest tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot` still FAILS at this point — the snapshot regen is Task 2 and must not have been done early.
    - `git diff HEAD~1 HEAD -- uv.lock` is empty (no dependency change; this phase installs nothing).
  </acceptance_criteria>

  <done>Both runtime trees carry /agree and the Phase-13/14 body edits; AGENTS.md's managed command index lists agree; the manifest owns both new paths; a re-emit over the documented path set produces an empty diff; opencode.json and settings.json are provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the projected-tree snapshot and confirm the suite goes green</name>

  <files>tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>

  <read_first>
    - `tools/harness_emit/tests/test_emit_determinism.py` — read `test_projected_tree_matches_committed_snapshot` (lines ~55-81) and confirm for yourself that it renders from `_AGENTS_DIR`/`_COMMANDS_DIR`/`_SKILLS_DIR` (i.e. `harness/` source) and never opens `.opencode/` or `.claude/`. This is why Task 1 had to run first.
    - `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` — the current stale snapshot; confirm it holds exactly ONE snapshot before regenerating
    - `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-RESEARCH.md` — § The Gate-Theft Trap and Pitfall 5 (unscoped `--snapshot-update`)
    - `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-VALIDATION.md` — the gate-theft guard note and the expected end state
  </read_first>

  <action>
    Precondition gate — do not start this task until Task 1's emit is COMMITTED. Verify with
    `git log --oneline -1` and `git ls-files --error-unmatch .opencode/command/agree.md`. If
    `agree.md` is not tracked, go back to Task 1. Regenerating this snapshot before the emit is
    committed is the gate theft described in `<gate_theft_guard>` and must not happen.

    Step 1 — Regenerate the snapshot, SCOPED to the one test file:
    `uv run pytest --snapshot-update tools/harness_emit/tests/test_emit_determinism.py`.
    The scope is not optional (Pitfall 5): a repo-wide `--snapshot-update` would touch the other four
    `.ambr` files (one under `tools/docs_sync/`, three under `tools/memory_regen/`). This `.ambr`
    holds exactly one snapshot (`test_projected_tree_matches_committed_snapshot`), so a scoped run is
    verified safe.

    Step 2 — Confirm the blast radius. Run `git status --porcelain -- 'tools/**/__snapshots__'` and
    confirm the ONLY changed snapshot file is
    `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`. If any `docs_sync` or
    `memory_regen` snapshot moved, revert them — the scope leaked.

    Step 3 — Sanity-check the regenerated content: the `.ambr` embeds the full body of every agent,
    command, and skill, so it should now contain `agree`. Confirm the diff is additive/updating in
    line with the Phase-13/14 body edits, not a wholesale rewrite.

    Step 4 — GEN-04 hazard check. The `.ambr` is a TRACKED file under `tools/` and is therefore in
    GEN-04's scan scope (`_CORE_ROOTS = ("tools", "harness", "libs")`, enumerated via `git ls-files`),
    and it embeds every body verbatim — so a regen can import a forbidden domain token into a
    core-plane file. This has bitten twice before (the 08-01 and 10-01 prose leaks). Run
    `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py` AFTER the regen, not before.
    The researcher pre-verified 0 hits against the projected snapshot, so this should pass — but
    verify, do not assume.

    Step 5 — Run the full suite: `uv run pytest`. Expect exactly `0 failed / 659 passed` (the 658
    passing baseline plus the one previously-red snapshot test now green). A different number means
    something unexpected moved — investigate before committing.

    Step 6 — Commit the regenerated snapshot. No model identifier in the message.

    Step 7 — Final proof, and the one that actually matters: re-run the local emit-drift replica
    `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json`
    and confirm a clean exit. A green pytest alone does NOT prove this phase succeeded.
  </action>

  <verify>
    <automated>uv run pytest tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot</automated>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py</automated>
    <automated>uv run pytest</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot` passes.
    - `uv run pytest tools/harness_emit/tests/test_emit_determinism.py::test_emit_twice_byte_identical` passes.
    - `uv run pytest` reports `0 failed` and `659 passed`.
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py` passes (GEN-04 green after the regen).
    - `git status --porcelain -- 'tools/**/__snapshots__'` is empty after commit, and `git show --stat HEAD` lists `test_emit_determinism.ambr` as the only `.ambr` touched.
    - `grep -c 'agree' tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` returns a non-zero count.
    - The commit that regenerated the `.ambr` is strictly LATER in `git log` than the commit that emitted the trees (`git log --oneline -2` shows emit before regen).
    - `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` exits 0.
  </acceptance_criteria>

  <done>The projected-tree snapshot matches current harness/ source, the full non-example suite is 0 failed / 659 passed, GEN-04 is green after the regen, and the emit-drift replica is clean — with the emit provably committed before the regen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `harness/` source → emitted `.opencode/` + `.claude/` trees | The generation boundary. Anything crossing it is machine-projected; a hand-authored byte on the far side is an integrity violation. |
| Local working tree → committed repo → distributed harness | Emitted trees ship to consumers. Content leaked here (model ids, domain tokens) escapes the repo. |
| Test fixture (`.ambr`) → CI gate verdict | The fixture is the gate's oracle. Whoever can edit the fixture can dictate the verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `test_emit_determinism.ambr` — **gate theft** | mitigate | The phase-defining risk. `--snapshot-update` alone greens the suite while the trees stay stale (no local test reads `.opencode/`). Mitigated by PROCESS, as no automated control exists: mandatory task ordering (emit → assert `git status` non-empty → regen), the Task-2 precondition gate (`git ls-files --error-unmatch .opencode/command/agree.md`), the acceptance criterion that the emit commit precedes the regen commit in `git log`, and verification via the CI-replica `re-emit && git diff --exit-code` rather than a green pytest. |
| T-15-02 | Tampering | `.opencode/` · `.claude/` · `AGENTS.md` managed block — derived-plane hand-edit | mitigate | All are DERIVED (Regime A owned + manifest-tracked; `AGENTS.md` Regime B splice-only) and carry `DERIVED_MARKER`. Plan forbids hand-writing any byte; the only permitted writer is `python -m tools.harness_emit`. Detected by the local emit-drift replica over `ci.yml:197`'s path set and by the CI `emit-drift` job in non-bypassable `gate.needs`. |
| T-15-03 | Information disclosure | Emitted trees — real model identifier leak | mitigate | `validate.check_agent` pins `model == "provider/explorer-tier"`; `check_opencode_config` enforces `^provider/[a-z0-9-]+-tier$`; both are validate-then-write (abort having written nothing). Researcher scanned the full emitted tmp tree for `claude-*opus\|sonnet\|haiku`, `gpt-N`, `gemini-N`, `o1/o3-mini` → 0 hits; only placeholder tiers present. **Known coverage gap:** no gate greps emitted command/skill BODIES for a model id (covered only transitively, since bodies are byte-copies of `harness/` source). Plan 02 closes this with an explicit body grep in verification rather than by adding a new gate. |
| T-15-04 | Information disclosure | Regenerated `.ambr` — GEN-04 domain-token leak into the core plane | mitigate | The `.ambr` is tracked under `tools/`, is inside GEN-04's `git ls-files tools harness libs` scan, and embeds every body verbatim — so a regen can import `examples/`, `equipment`, `설비`, `wafer`, `dotnet-engineer`, etc. into a core-plane file. Two in-repo precedents (08-01, 10-01). Pre-verified 0 hits across all 12 tokens, but `test_core_no_example_dep.py` is re-run AFTER the regen (Task 2 Step 4), never before. |
| T-15-05 | Elevation of privilege | `.opencode/plugin/session-inject.ts` — emitter handling executable plugin source | accept | Pre-existing control T-07-04 remains intact and unmodified: `iter_plugins` uses `read_bytes`/`write_bytes` only (`generate.py:422-434`) — plugin source is byte-copied and NEVER parsed, imported, or executed by the emitter. This phase changes no emitter code, so the control is untouched. Accepted: no new exposure. |
| T-15-06 | Tampering | GSD-owned files (`.claude/commands/gsd/**`, `gsd-*.md`) clobbered by the emitter | accept | Pre-existing control T-07-02 intact: Regime B splice + `gsd-*` manifest exclusion, proven by `test_coexist`'s seeded-GSD tests. Measured emit confirmed the GSD subtree untouched. Accepted; asserted anyway via the Task-1 criterion that no `.claude/agents/` path appears in the diff. |
| T-15-SC | Tampering | Supply chain — package installs | accept | **No installs in this phase.** `tools/harness_emit/pyproject.toml` declares `dependencies = []`; no `## Package Legitimacy Audit` is required because nothing is installed. Enforced by the Task-1 acceptance criterion that `uv.lock` shows an empty diff. No `[ASSUMED]`/`[SUS]` package exists to gate. |
| T-15-07 | Repudiation | Model identifier in this phase's own commit messages | mitigate | CLAUDE.md's 모델 아이덴티티 constraint covers commits and PRs, and **no gate scans commit messages** — the model-id gates read only frontmatter and `opencode.json`. Process-only: both tasks explicitly require model-id-free commit messages. Verified by human read at review. |
</threat_model>

<verification>
Run in order. Steps 1-2 are the phase's real proof; a green suite alone is insufficient (T-15-01).

1. `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` → exits 0
2. `git ls-files --error-unmatch .opencode/command/agree.md .claude/commands/agree.md` → exits 0
3. `uv run pytest` → 0 failed / 659 passed
4. `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py` → green (GEN-04, run AFTER the regen)
5. `grep -rn "EXPECTED_COMMANDS" tools/ harness/ libs/` → 0 hits (the constant was correctly NOT invented)
6. `git log --oneline -2` → the emit commit precedes the snapshot-regen commit
</verification>

<success_criteria>
- Both runtime trees carry `/agree` plus every Phase-13/14 body edit, all machine-generated.
- `AGENTS.md`'s managed command index lists `agree`; content outside the fence is byte-identical.
- The emit-drift replica over the documented 8-path set exits clean.
- Full non-example suite: 0 failed / 659 passed.
- GEN-04 green after the `.ambr` regen.
- No emitter code changed; `uv.lock` unchanged; `EXPECTED_COMMANDS` not created.
- Emit committed strictly before the snapshot regen.
</success_criteria>

<output>
Create `.planning/phases/15-emit-round-trip-gates-v2-1-d/15-01-SUMMARY.md` when done.

Record: the ACTUAL measured delta (`git status` at execution) versus the research's predicted 2-new /
8-changed / 1-AGENTS-line / +2-manifest / 1-`.ambr`, noting any divergence (Assumption A2); the
confirmed no-op status of `opencode.json` and `.claude/settings.json`; and the commit SHAs in
emit-then-regen order.
</output>
</content>
