---
phase: 12-model-adr-doc-reframe-v2-1-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .memory/state/activeContext.md
  - .memory/state/progress.md
  - AGENTS.md
autonomous: true
requirements: [MEM2-03]

must_haves:
  truths:
    - "No session-start state surface tells an agent its own grounded work is 'a hint, not truth' or to distrust it"
    - "Each reworded surface STILL states the data-authority rule (contracts/ + docs/adr/ win a DATA conflict)"
    - "The AGENTS.md reword lands OUTSIDE the HARNESS-MANAGED block (line 97+) — a plain hand edit, no re-emit needed"
  artifacts:
    - path: ".memory/state/activeContext.md"
      provides: "Data-authority blockquote (was 'a hint, not truth')"
      contains: "docs/adr/"
    - path: ".memory/state/progress.md"
      provides: "Data-authority blockquote (was 'a hint, not truth')"
      contains: "docs/adr/"
    - path: "AGENTS.md"
      provides: "Data-authority lazy-load prose at lines 86-88 (outside the managed block)"
      contains: "docs/adr/"
  key_links:
    - from: ".memory/state/activeContext.md"
      to: "contracts/ + docs/adr/"
      via: "data-authority banner"
      pattern: "docs/adr/"
    - from: "AGENTS.md"
      to: "contracts/ + docs/adr/"
      via: "lazy-load rule prose"
      pattern: "docs/adr/"
---

<objective>
Reword the distrust framing to data-authority (SC2 / MEM2-03) in the THREE surfaces that plan 12-01
does not own: `.memory/state/activeContext.md`, `.memory/state/progress.md`, and `AGENTS.md`. Zero
file overlap with plan 12-01 → both run in wave 1 in parallel.

Purpose: after this phase, no session-start surface tells an agent to distrust its own grounded work
("a hint, not truth" / bare "provisional") — the framing becomes "which artifact wins a DATA
conflict", preserving the real rule (contracts/ + docs/adr/ override `.memory/state/`).

Output: three prose-only edits. The AGENTS.md edit is a plain hand edit OUTSIDE the HARNESS-MANAGED
marker block (Pitfall 2 avoided — no emitter round-trip). Do NOT touch `inject.py` (its banner
reword is Phase 13), the emitter, or any generated `.opencode/`/`.claude/` tree.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-RESEARCH.md
@.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data-authority reword of the two state blockquotes (SC2)</name>
  <files>.memory/state/activeContext.md, .memory/state/progress.md</files>
  <read_first>
    - 12-RESEARCH.md §SC2 "Surface 2" (activeContext L3-5 verbatim) + "Surface 3" (progress L3-4
      verbatim) + "Verify SC2".
    - Current state: `.memory/state/activeContext.md:1` title "(COMMITTED, PROVISIONAL)" + `:3-5`
      blockquote "PROVISIONAL — this file is a hint, not truth. `contracts/` and `docs/adr/` always
      override `.memory/state/` on conflict. No secrets... injects only a *pointer*...".
    - Current state: `.memory/state/progress.md:1` title "(COMMITTED, PROVISIONAL)" + `:3-4`
      blockquote "PROVISIONAL — a hint, not truth. `contracts/` and `docs/adr/` always win on
      conflict. No secrets/PII. Durable decisions go in append-only `docs/adr/`, not here."
  </read_first>
  <action>
    In `.memory/state/activeContext.md`: retitle the H1 label "(COMMITTED, PROVISIONAL)" →
    "(COMMITTED)"; reword the blockquote — replace "PROVISIONAL — this file is a hint, not truth."
    with a data-authority line, e.g. "DATA AUTHORITY — on a data conflict, `contracts/` and
    `docs/adr/` win over `.memory/state/`. This is a session progress log, not a reason to re-verify
    grounded work." KEEP the "No secrets, tokens, credentials, or PII here." clause and the
    "injects only a *pointer* to this file, never its body." clause verbatim.
    In `.memory/state/progress.md`: retitle the H1 label "(COMMITTED, PROVISIONAL)" →
    "(COMMITTED)"; reword the blockquote — replace "PROVISIONAL — a hint, not truth." with the same
    data-authority framing; KEEP "No secrets/PII." and "Durable decisions go in append-only
    `docs/adr/`, not here." verbatim.
    Do NOT change any content under "## In flight" / "## Next" / the progress bullets — prose banner
    only.
  </action>
  <acceptance_criteria>
    - `grep -riqE 'provisional|hint, not truth|confirm before trusting' .memory/state/activeContext.md .memory/state/progress.md` exits non-zero (all epistemic-distrust phrasing removed).
    - `grep -q 'docs/adr/' .memory/state/activeContext.md` and `grep -q 'docs/adr/' .memory/state/progress.md` both exit 0 (data-authority rule preserved).
    - `grep -qi 'secrets' .memory/state/activeContext.md` exits 0 (secrets clause retained).
    - `grep -qi 'pointer' .memory/state/activeContext.md` exits 0 (pointer clause retained).
    - `grep -qi 'append-only' .memory/state/progress.md` exits 0 (durable-decisions→ADR clause retained).
  </acceptance_criteria>
  <verify>
    <automated>! grep -riqE 'provisional|hint, not truth|confirm before trusting' .memory/state/activeContext.md .memory/state/progress.md && grep -q 'docs/adr/' .memory/state/activeContext.md && grep -q 'docs/adr/' .memory/state/progress.md && grep -qi 'secrets' .memory/state/activeContext.md && grep -qi 'append-only' .memory/state/progress.md</automated>
  </verify>
  <done>Both state blockquotes read as data-authority (zero standalone "provisional"/"hint, not truth"); secrets, pointer, and durable-decisions clauses retained; the data-authority rule preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Data-authority reword of AGENTS.md lazy-load prose, OUTSIDE the managed block (SC2)</name>
  <files>AGENTS.md</files>
  <read_first>
    - 12-RESEARCH.md §SC2 "Surface 5" (AGENTS.md L87-88 verbatim + the CRITICAL managed-block
      analysis) + the "Source-vs-generated tension summary" table.
    - Current state: `AGENTS.md:86-88` — "...(~1k-token cap, pointer-only, provisional-banner-first).
      Volatile `.memory/state/` is **provisional** — contracts/ and docs/adr/ always override it on
      conflict." The HARNESS-MANAGED block begins at `AGENTS.md:97` (`<!-- BEGIN HARNESS-MANAGED -->`)
      and ends at `:106` — the target lines 86-88 are OUTSIDE it (preserved-verbatim human region).
  </read_first>
  <action>
    Edit only lines ~86-88 of `AGENTS.md` (rule 5, "Lazy-load rule"): reword "provisional-banner-first"
    → "data-authority-banner-first", and reword "Volatile `.memory/state/` is **provisional** —
    contracts/ and docs/adr/ always override it on conflict." → a data-authority statement, e.g.
    "On a **data** conflict, `contracts/` and `docs/adr/` are authoritative over volatile
    `.memory/state/` — this is which artifact wins a contradiction, not a reason to distrust
    grounded work." Keep the rest of rule 5 (the ~1k-token cap / pointer-only / lazy-load mechanic)
    intact. Do NOT edit anything at or below line 97 (the `<!-- BEGIN HARNESS-MANAGED -->` block);
    verify the edit lands strictly above that marker so NO emitter re-emit is needed.
  </action>
  <acceptance_criteria>
    - `grep -niE 'provisional|hint, not truth|confirm before trusting' AGENTS.md` exits non-zero (no distrust phrasing anywhere in AGENTS.md).
    - `grep -q 'docs/adr/' AGENTS.md` exits 0 (data-authority rule preserved).
    - `grep -qi 'data-authority-banner-first\|data conflict' AGENTS.md` exits 0 (data-authority reword present).
    - The edit is outside the managed block: `awk '/BEGIN HARNESS-MANAGED/{exit} /provisional/{found=1} END{exit found}' AGENTS.md` — no "provisional" occurs before the BEGIN marker (managed block untouched, edit above it).
    - `git diff -U0 AGENTS.md | grep -E '^[+-]' | grep -qE 'HARNESS-MANAGED|Harness-Emitted Runtime Surface'` exits non-zero (no line inside/on the managed block changed).
  </acceptance_criteria>
  <verify>
    <automated>! grep -qiE 'provisional|hint, not truth|confirm before trusting' AGENTS.md && grep -q 'docs/adr/' AGENTS.md && ! (git diff -U0 AGENTS.md | grep -E '^[+-]' | grep -qE 'HARNESS-MANAGED|Harness-Emitted Runtime Surface')</automated>
  </verify>
  <done>AGENTS.md lazy-load prose reads as data-authority; zero distrust phrasing; the change sits entirely above the HARNESS-MANAGED marker (line 97) so no emitter round-trip is required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitter → `AGENTS.md` | The emitter owns ONLY the fenced HARNESS-MANAGED region (lines 97-106); everything outside is preserved-verbatim human content. Editing lines 86-88 crosses no emitter boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | accidental edit inside the AGENTS.md HARNESS-MANAGED block (would be overwritten / drift) | mitigate | Acceptance asserts the diff touches no line inside the managed block; the reword is confined to lines 86-88 above the BEGIN marker. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (prose-only edits). |
</threat_model>

<verification>
- `grep -riE 'provisional|hint, not truth|confirm before trusting' .memory/state/activeContext.md .memory/state/progress.md AGENTS.md` returns nothing.
- Each surface still contains `docs/adr/` (data-authority rule intact).
- `git diff` for AGENTS.md touches no HARNESS-MANAGED line.
- `git diff --quiet tools/memory_regen/inject.py` (unchanged).
</verification>

<success_criteria>
- SC2 (state + AGENTS.md portion): the three surfaces are reworded to data-authority; no distrust phrasing remains; the contracts/+adr-win-on-conflict rule is preserved; the AGENTS.md managed block is untouched.
</success_criteria>

<output>
Create `.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-02-SUMMARY.md` when done. Note that
combined with 12-01, all 5 SC2 surfaces are now reworded (phase-level SC2 grep over all 5 files
should be clean).
</output>
