---
phase: 13-injector-reframe-channel-wiring-v2-1-b
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - tools/memory_regen/inject.py
  - tools/memory_regen/tests/test_inject_assembler.py
  - tools/memory_regen/tests/test_inject_determinism.py
  - tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr
autonomous: true
requirements: [MEM2-02, MEM2-05]

must_haves:
  truths:
    - "assemble() emits a full-body working-agreements directive at priority-0, composed from the active .memory/agreements/* set"
    - "The agreements block is capped (6 entries / 700 chars) and degrades whole-block to a pointer on overflow"
    - "The banner reads as data-provenance ('which artifact wins a data conflict'), never as distrust of grounded work"
    - "The activeContext pointer reads as a progress-log pointer and carries the updated: stamp verbatim"
    - "agreements + banner + drift are never dropped, even at budget_chars=1"
    - "delete+regen stays byte-identical and the payload stays <= 4000 chars with a full-cap agreements block"
    - "The repo-map still survives a full-cap agreements block"
  artifacts:
    - path: "tools/memory_regen/inject.py"
      provides: "agreements composer, cap constants, reworded banner/pointer, verbatim stamp"
      contains: "_AGREEMENTS_MAX_CHARS"
      exports: ["assemble", "BANNER", "ACTIVE_HEADER", "AGREEMENTS_HEADER"]
    - path: "tools/memory_regen/tests/test_inject_assembler.py"
      provides: "rewritten contract tests + cap/order/overflow/filter/stamp coverage"
  key_links:
    - from: "tools/memory_regen/inject.py"
      to: ".memory/agreements/*.md"
      via: "sorted(agreements_dir.glob('*.md')) — non-recursive, no symlink follow"
      pattern: "sorted\\(.*glob\\(\"\\*\\.md\"\\)\\)"
    - from: "tools/memory_regen/inject.py"
      to: "tools.harness_lint.parse_frontmatter"
      via: "fail-closed status filter + updated: stamp read"
      pattern: "parse_frontmatter"
---

<objective>
The phase's core change, and the **only plan that touches `inject.py`**. Turn the injector from a
"distrust your own work" banner into a two-block payload: (a) a full-body, never-dropped,
self-capped **working-agreements directive** at priority-0, and (b) a separate **data-scoped
provenance banner**. Reword the activeContext pointer into a progress-log pointer carrying the
verbatim `updated:` stamp written by 13-01.

Purpose: `inject.py` is ONE 148-line file — two plans editing it would conflict, so it is decomposed
by **file ownership**, not by requirement. This plan owns it end to end.
Output: reframed `inject.py`, rewritten assembler tests, refreshed determinism snapshot.

**This plan owns the D-09 `agreements_dir` parameter** (13-02 deliberately stayed `inject.py`-free).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-CONTEXT.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-VALIDATION.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-01-SUMMARY.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-02-SUMMARY.md
@docs/adr/0006-process-memory-channel-and-provenance-reframe.md
@AGENTS.md
@harness/skills/two-plane-memory/SKILL.md
@harness/skills/python-conventions/SKILL.md

<interfaces>
<!-- Settled during planning — do NOT re-derive. -->

**Reuse, do not hand-roll** (`13-RESEARCH.md` § Don't Hand-Roll — every mechanic has an in-repo
precedent; net-new code should be ~1 composer + ~1 render function, anything larger is a smell):

```python
from tools.harness_lint import parse_frontmatter   # precedent: tools/harness_emit/generate.py:35
# signature: parse_frontmatter(md_text: str) -> tuple[dict, str]   (frontmatter_dict, body_text)
# - CRLF-normalized at frontmatter.py:40 (boundary invariant §4.3)
# - unclosed fence -> ({}, md_text), fail-safe (frontmatter.py:54)
# - raises ValueError if the block parses to a non-mapping (frontmatter.py:65) -> CATCH IT
# - uses ruamel YAML(typ="safe"). Do NOT `uv add pyyaml`. Do NOT re-slice `---` fences.
```

**VERIFIED in planning — the `updated:` stamp scalar (settles RESEARCH assumption A6):**
`updated: 2026-07-15` (bare) parses to `datetime.date`; `updated: "2026-07-15"` (quoted) parses to
`str`. 13-01 wrote the **quoted** form. Still `str()`-coerce defensively at the render site.

**`sorted()` precedents (D-08 — `glob()` yields filesystem order, not sorted):**
`tools/harness_emit/generate.py:151,191` · `tools/memory_regen/repo_map.py:61` ·
`tools/memory_regen/contracts_index.py:75`

**`_confine` precedents (D-19):** `tools/docs_sync/generate.py` · `golden_runner._confine`

**Today's `assemble()`** (`inject.py:105-109`) — you are adding `agreements_dir` as a peer:
```python
def assemble(budget_chars: int = 4000, derived_dir: Path = DERIVED_DIR,
             state_dir: Path = STATE_DIR) -> str: ...
```
`_drift_summary()` takes **no params** and is not injectable (calls `run_gate()` on the real tree).

**Measured budget arithmetic (13-RESEARCH.md §2 — measured, not estimated):**
| | chars |
|---|---|
| base after reword, zero agreements | 3216 |
| headroom for the agreements block | **784** |
| full-cap block M=700 | total **3917** ✓ repo-map survives (83 slack) |
| M=800 | 4017 ✗ repo-map evicted **every session** |

**Fixture from 13-02** (`conftest.py::tmp_agreements_tree`): 5 files created **non-alphabetically**
— `zeta-proceed.md` (active), `alpha-ground.md` (active), `_TEMPLATE.md` (decoy, `status: active`,
placeholder text), `middle-retired.md` (retired), `README.md` (no frontmatter). Sorted-active order
is `alpha-ground` → `zeta-proceed`, the **reverse** of creation order — that is what makes the
sorted-iteration test falsifiable.
</interfaces>

<planned_red_tests>
These are a **planned lockstep update, NOT regressions**. Rewrite them in Task 2; do not "fix"
`inject.py` to keep them green:
- `test_inject_assembler.py:19-24` `test_first_line_is_provisional_banner` — asserts `"provisional" in first.lower()`
- `test_inject_assembler.py:27-34` `test_banner_asserts_adr_contract_override` — asserts `"provisional" in banner`
- `test_inject_assembler.py:71` — `mandatory = len(BANNER) + len(_drift_summary()) + 1`; the mandatory set grows
- `test_inject_assembler.py:116-122` `test_main_prints_banner_first`

**MUST STAY GREEN** — do not touch:
- `test_inject_assembler.py:88-103` — `test_no_full_contract_schema_body_leaks` (no `$schema`) and
  `test_active_context_is_pointer_not_body` (the `## In flight` body must not leak into the payload).
</planned_red_tests>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agreements_dir param + the capped, fail-closed, confined agreements composer</name>

  <read_first>
    - `tools/memory_regen/inject.py` (148 lines — read fully; you are editing it)
    - `tools/harness_lint/frontmatter.py` (68 lines — read fully; you must reuse `parse_frontmatter` and know its failure modes)
    - `tools/memory_regen/tests/conftest.py` — the `tmp_agreements_tree` fixture 13-02 added
    - `.memory/agreements/_TEMPLATE.md` — the entry shape you parse (and the `status: active` trap)
    - `tools/memory_regen/contracts_index.py` lines 70-80 — the in-module `sorted()` iteration precedent
  </read_first>

  <files>tools/memory_regen/inject.py, tools/memory_regen/tests/test_inject_assembler.py</files>

  <behavior>
    - Zero active agreements ⇒ block is **omitted entirely** (empty string, section skipped). No "none recorded yet" filler. This is the state today, so today's payload shape is unchanged.
    - `status: retired` ⇒ excluded. `_TEMPLATE.md` ⇒ excluded. `README.md` ⇒ excluded. Missing/malformed/unparseable status ⇒ **excluded** (fail-closed, never defaulted-in).
    - Iteration is `sorted()` ⇒ the fixture's non-alphabetical creation order still yields `alpha-ground` before `zeta-proceed`, and a shuffled tree yields an identical payload.
    - `> 6` entries ⇒ whole block replaced by the pointer (no entry bodies). Block `> 700` chars ⇒ same. **Never partial**, never a mid-line cut.
    - The rendered entry is **title + one-line rule only**. `provenance`, `added`, `status`, and the `Related:` line never appear in the payload.
    - A symlink inside the dir, or any path resolving outside `agreements_dir`, is refused — not followed.
    - An unreadable file (`OSError`) is skipped, never raised.
  </behavior>

  <action>
    **(a) Constants (D-02 — named module constants so re-ratification is a one-line edit):**
    - `AGREEMENTS_DIR = _REPO_ROOT / ".memory" / "agreements"` (peer of `DERIVED_DIR` / `STATE_DIR`)
    - `_AGREEMENTS_MAX_ENTRIES = 6`  (D-01)
    - `_AGREEMENTS_MAX_CHARS = 700`  (D-01 — block total, **header included**)
    - `AGREEMENTS_HEADER` — the scope-limiting header (D-15). It MUST state that these are
      **working-style** directives and that they **never override `contracts/`, `docs/adr/`, or the
      gates**, so a planted agreement cannot self-authorize constitution writes. Keep it terse:
      a heading line plus **one** scope-limit line of ≤80 chars.
      ⚠️ **Budget note (surfaced at planning):** the ratified M=700 counts the header. At ~97 chars
      per rendered entry, a ~136-char scope-limited header leaves room for ~5 average entries before
      M binds — so N=6 is reachable only with entries ≤~94 chars. This is **not a defect**: M is the
      binding guard by design and N=6 is the curation guard (13-RESEARCH.md §2: "a verbose entry
      trips M before N — correct"). Keep both values exactly as ratified; keep the scope-limit line
      terse to preserve entry room.
    - `AGREEMENTS_POINTER` — a **fixed, deterministic, countless** overflow string reusing
      `AGREEMENTS_HEADER` plus a line pointing at `.memory/agreements/` and telling the reader to
      read them before acting. **Do NOT interpolate the count** (D-03) — `f"{len(active)} active
      agreements"` churns the payload bytes on every add/retire and weakens byte-identity.

    **(b) `_render_agreement(body: str) -> str | None`** — from the **body only** (`parse_frontmatter`
    already stripped the frontmatter, which is what structurally guarantees `provenance` can never
    leak — D-16):
    - `title` = the text after the first `# ` H1.
    - `rule`  = the first non-empty body line that is not the title, does not start with `#`, and
      does not start with `Related:`.
    - Return `- **{title}** — {rule}` (one line, deterministic, budget-predictable).
    - If title **or** rule is missing ⇒ return `None` ⇒ **skip the entry** (fail-closed). Never emit
      a half-directive.

    **(c) `_agreements_block(agreements_dir: Path = AGREEMENTS_DIR) -> str`:**
    - Iterate `sorted(Path(agreements_dir).glob("*.md"))` — **`glob`, not `rglob`** (D-19,
      non-recursive), and `sorted` (D-08).
    - **3-layer exclusion (D-17, hazard (c) — `_TEMPLATE.md` ships `status: active`, a live trap):**
      1. skip names starting with `_`; 2. skip `README.md`; 3. treat missing/malformed `status` as
      NOT active — only the exact string `"active"` admits an entry
      (`str(fm.get("status", "")).strip() != "active"` ⇒ skip).
    - **Confinement (D-19):** skip `p.is_symlink()`, and assert `p.resolve()` is under
      `Path(agreements_dir).resolve()` (mirror the `_confine` precedent) — refuse anything else.
    - **Tolerance (T-13-05):** wrap the read + parse in `except (OSError, ValueError): continue`.
      `parse_frontmatter` raises `ValueError` on a non-mapping block (`frontmatter.py:65`), and
      `memory-inject.sh:40` `|| echo ''` would swallow any raise into an **empty payload** — silently
      killing drift AND agreements. Read with explicit `encoding="utf-8"`.
    - Compose: no entries ⇒ return `""`. Else build the block; if
      `len(entries) > _AGREEMENTS_MAX_ENTRIES` **or** `len(block) > _AGREEMENTS_MAX_CHARS` ⇒ return
      `AGREEMENTS_POINTER` (whole-block, all-or-nothing — D-03). Else return the block.
    - **`tools/memory_regen` must READ this plane and NEVER write it** — the composer must not
      create, normalize, or rewrite any agreement file (`.memory/agreements/README.md:4-5`).

    **(d) `assemble()` gains `agreements_dir: Path = AGREEMENTS_DIR`** (D-09) as a peer of
    `derived_dir` / `state_dir` — the testability prerequisite. Wire `_agreements_block(agreements_dir)`
    into the `sections` list in Task 2 (this task may add the param + call site).

    **Tests (add to `test_inject_assembler.py`, using `tmp_agreements_tree`):**
    `test_only_active_non_template_agreements_compose` (retired + `_TEMPLATE.md` + `README.md` +
    missing-status all excluded; the placeholder text `<One-line working-style` never appears);
    `test_agreements_order_is_sorted_not_filesystem` (fixture's reverse creation order still yields
    `alpha-ground` before `zeta-proceed`); `test_overflow_degrades_to_pointer` (N+1 entries ⇒ pointer
    and no entry bodies; a >M-char block ⇒ pointer; assert the pointer text contains **no digit**, per
    D-03); `test_render_excludes_provenance` (no `provenance`, `added:`, `status:`, or `Related:` in
    the payload); `test_agreements_header_states_scope_limit` (header names `contracts/` and
    `docs/adr/` and says these never override them); `test_agreements_reads_are_confined` (a symlink
    inside the dir pointing at a file outside is not followed/rendered).
  </action>

  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_only_active_non_template_agreements_compose -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_agreements_order_is_sorted_not_filesystem -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_overflow_degrades_to_pointer -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_render_excludes_provenance -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_agreements_header_states_scope_limit -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_agreements_reads_are_confined -x` passes
    - Constants exist with the ratified values — this prints `6 700` and exits 0:
      `uv run python -c "from tools.memory_regen import inject; print(inject._AGREEMENTS_MAX_ENTRIES, inject._AGREEMENTS_MAX_CHARS)"`
    - `assemble` accepts `agreements_dir` — this prints `True`:
      `uv run python -c "import inspect; from tools.memory_regen import inject; print('agreements_dir' in inspect.signature(inject.assemble).parameters)"`
    - Non-recursive glob only: `grep -c "rglob" tools/memory_regen/inject.py` returns `0`
    - Sorted iteration present: `grep -q "sorted(" tools/memory_regen/inject.py` succeeds
    - Against the **real** (empty-active) dir the block is omitted — this prints `''`:
      `uv run python -c "from tools.memory_regen import inject; print(repr(inject._agreements_block()))"`
    - The overflow pointer interpolates no count (D-03) — this prints `True`:
      `uv run python -c "from tools.memory_regen import inject; print(not any(c.isdigit() for c in inject.AGREEMENTS_POINTER))"`
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q` — **still fully green** (13-02's net must survive this task)
    - The real `.memory/agreements/` is unmodified: `git diff --name-only | grep -c "^.memory/agreements/"` returns `0`
    - `uv run ruff check tools/memory_regen/inject.py` and `uv run ruff format --check tools/memory_regen/inject.py` pass
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_inject_assembler.py tools/memory_regen/tests/test_inject_determinism.py -q && uv run ruff check tools/memory_regen/inject.py</automated>
  </verify>

  <done>`assemble()` accepts `agreements_dir`; `_agreements_block` composes only exact-`status: active`, non-`_`, non-README entries via a sorted non-recursive glob with symlink/traversal refusal and OSError/ValueError tolerance; renders title + rule only (provenance structurally impossible); caps at 6/700 named constants with countless whole-block overflow-to-pointer; omits entirely when the active set is empty. 13-02's determinism net still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Priority renumber to agreements-first, reword BANNER + pointer, surface the stamp verbatim</name>

  <read_first>
    - `tools/memory_regen/inject.py` — as edited by Task 1
    - `tools/memory_regen/tests/test_inject_assembler.py` lines 16-34, 63-75, 88-103, 113-123 — the four planned-red sites AND the two that must stay green
    - `.memory/state/activeContext.md` — the quoted `updated:` frontmatter 13-01 wrote (this is what you now read)
    - `docs/adr/0006-process-memory-channel-and-provenance-reframe.md` — the landed model this reframe implements
  </read_first>

  <files>tools/memory_regen/inject.py, tools/memory_regen/tests/test_inject_assembler.py</files>

  <behavior>
    - Payload section order is: agreements(0) → banner(1) → drift(2) → contracts(3) → repomap(4) → progress-log pointer(5).
    - At `budget_chars=1`, agreements + banner + drift are ALL present (never-dropped tuple).
    - `BANNER.lower()` contains `data`, `contract`, `adr`; contains none of `provisional`, `hint, not truth`, `confirm before trusting`.
    - `ACTIVE_HEADER` names a progress log; the payload no longer contains `confirm against contracts/ADR before trusting`.
    - A state file with `updated: "2026-01-02"` ⇒ payload contains `[updated: 2026-01-02]` verbatim.
    - No frontmatter / missing key / missing file / non-mapping frontmatter ⇒ graceful `unknown` text, **no raise**.
    - When agreements exist, the agreements header and the banner are two distinct blocks, both present.
  </behavior>

  <action>
    **(a) `sections` list (`inject.py:118-124`) — insert agreements at index 0 (D-04):**
    `("agreements", _agreements_block(agreements_dir))` before `("banner", BANNER)`. Resulting order:
    agreements(0) → banner(1) → drift(2) → contracts(3) → repomap(4) → active(5).

    **(b) Never-drop tuple (`inject.py:131`) becomes `("agreements", "banner", "drift")`** (D-05).
    The agreements block is safe to never-drop **because it is already self-capped** — the cap is
    what makes never-dropping safe.

    **(c) `BANNER` reword (`inject.py:41-44`).** Target the 316-char measured text from
    13-RESEARCH.md §6 (Claude's discretion on exact wording, subject to the tested constraints).
    Required properties: lead with a **noun phrase naming the domain** (`DATA PROVENANCE`) rather
    than the bare unscoped adjective `PROVISIONAL`; the subject is *the summaries*, not the reader;
    scope the conflict as a **DATA conflict** before the authority claim lands; and close with an
    explicit clause foreclosing over-generalization — that this is about which artifact wins a
    contradiction, **NOT** a reason to distrust, retract, or re-verify your own grounded working
    context. Banned substrings (case-insensitive): `provisional`, `hint, not truth`,
    `confirm before trusting`. Required (case-insensitive): `data`, `contract`, `adr`.

    **(d) `ACTIVE_HEADER` (`inject.py:48`)** becomes `"## Progress log (pointer)"` and the pointer
    body (`inject.py:96-99`) is reworded to ~200 chars: it names
    `.memory/state/activeContext.md` as the **session progress log**, states that **git holds the
    full completed history** (encodes §7a / D-14), restates authority **data-scoped** ("on a data
    conflict, contracts/ADR win"), and appends the stamp. **Delete the direct behavioral imperative**
    `volatile; confirm against contracts/ADR before trusting` — it is worse than the banner: an
    instruction addressed to the agent with a precondition on *trusting*.

    **(e) Verbatim stamp in `_active_context_pointer(state_dir)` (D-10/D-11/D-12).** The function
    currently **ignores** its parameter — **remove the `# noqa: ARG001` at `inject.py:94`**, since
    `state_dir` becomes genuinely used. Read `Path(state_dir) / "activeContext.md"` with
    `encoding="utf-8"`, `parse_frontmatter` it, take `fm.get("updated")`, `str()`-coerce and strip.
    Append `" [updated: {stamp}]"` when truthy, else `" [updated: unknown — run /checkpoint]"`.
    - **NO wall-clock** (D-11 / Q6): no `datetime`, no `date.today`, no `.now()`. Never parse the
      stamp into a date, never compare it, never reformat it — it is an **opaque string**. Freshness
      is judged agent-side against the session date; there is no threshold. 13-02's
      `test_inject_module_has_no_wallclock` enforces this and must stay green.
    - **Never raise** (D-12 / T-13-05): wrap in `except (OSError, ValueError)` and degrade. Mirror
      the tolerant idiom at `inject.py:69-74`.

    **(f) Docstring (`inject.py:13-17`, `:110-116`) — record D-06, do not silently absorb it.**
    Update the priority list to the new 0–5 order. Update `assemble()`'s docstring: "Banner (0) and
    drift (1) are always present" ⇒ the agreements/banner/drift never-drop set. Add an explicit
    note that **agreements-at-priority-0 retires the D-02 "banner-first" invariant** in favour of
    *directive-first, banner-second*, and that when the active set is empty the block is omitted so
    the banner is the first line again (the state today). Keep the "no timestamps ⇒ delete+regen
    identical" claim — 13-02 now actually tests it.

    **(g) Rewrite the four planned-red tests** (lockstep, NOT regressions):
    - `:19-34` → `test_banner_is_data_scoped` — assert the banned/required substrings from (c).
    - `:71` → the mandatory floor becomes `len(BANNER) + len(_drift_summary()) + 1` **plus** the
      agreements block length; against the real (empty-active) dir the block is `""`, so pass the
      fixture dir explicitly rather than relying on that incidental emptiness.
    - `:116-122` `test_main_prints_banner_first` → rewrite to `test_main_prints_capped_payload`:
      `rc == 0`, `len(out.rstrip("\n")) <= 4000`, and the first line is `inject.BANNER` — documenting
      in the docstring that this holds **only because the real active set is empty**, and that a
      populated set puts the agreements block first (D-06).
    - **Do NOT touch `:88-103`** — `test_no_full_contract_schema_body_leaks` and
      `test_active_context_is_pointer_not_body` must stay green. The pointer reword must keep
      `.memory/state/activeContext.md` in the payload and must still never leak the `## In flight`
      body.

    **New tests:** `test_two_distinct_blocks_emitted` (agreements header ≠ banner; both present with
    a populated fixture); `test_agreements_banner_drift_never_dropped` (`budget_chars=1` ⇒ all three
    present); `test_pointer_is_progress_log_not_imperative` (payload lacks
    `confirm against contracts/ADR before trusting`; `ACTIVE_HEADER` says progress log);
    `test_updated_stamp_surfaced_verbatim` (fixture state file with `updated: "2026-01-02"` ⇒
    `[updated: 2026-01-02]` in payload); `test_absent_stamp_degrades_gracefully` (parametrize: no
    frontmatter / missing key / **missing file** / non-mapping frontmatter ⇒ `unknown` text, no raise).
  </action>

  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py -q` — **fully green** (all rewrites + new tests)
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_banner_is_data_scoped -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_two_distinct_blocks_emitted -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_agreements_banner_drift_never_dropped -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_pointer_is_progress_log_not_imperative -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_updated_stamp_surfaced_verbatim -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_absent_stamp_degrades_gracefully -x` passes
    - `uv run pytest "tools/memory_regen/tests/test_inject_assembler.py::test_active_context_is_pointer_not_body" "tools/memory_regen/tests/test_inject_assembler.py::test_no_full_contract_schema_body_leaks" -x` — **both still pass** (untouched)
    - Banner is data-scoped — this prints `True`:
      `uv run python -c "from tools.memory_regen import inject; b=inject.BANNER.lower(); print(all(t in b for t in ('data','contract','adr')) and not any(t in b for t in ('provisional','hint, not truth','confirm before trusting')))"`
    - `ACTIVE_HEADER` reworded — this prints `## Progress log (pointer)`:
      `uv run python -c "from tools.memory_regen import inject; print(inject.ACTIVE_HEADER)"`
    - The imperative is gone: `uv run python -m tools.memory_regen.inject | grep -c "confirm against contracts/ADR before trusting"` returns `0`
    - `# noqa: ARG001` removed: `grep -c "ARG001" tools/memory_regen/inject.py` returns `0`
    - The live payload surfaces 13-01's real stamp: `uv run python -m tools.memory_regen.inject | grep -q "\[updated: 2026-07-16\]"` succeeds
    - **No wall-clock** — 13-02's static gate still green: `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_inject_module_has_no_wallclock -x` passes
    - Never-drop tuple updated: `grep -q '"agreements", "banner", "drift"' tools/memory_regen/inject.py` succeeds
    - Docstring records D-06: `grep -qi "banner-first" tools/memory_regen/inject.py` succeeds (the retirement is stated, not silently absorbed)
    - `uv run ruff check tools/memory_regen/inject.py` and `uv run ruff format --check tools/memory_regen/inject.py` pass
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_inject_assembler.py -q && uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_inject_module_has_no_wallclock -x && uv run ruff check tools/memory_regen/inject.py</automated>
  </verify>

  <done>Sections are agreements(0)→banner(1)→drift(2)→contracts(3)→repomap(4)→pointer(5) with never-drop `("agreements","banner","drift")`; BANNER is data-scoped (no `provisional`/`hint, not truth`/`confirm before trusting`); `ACTIVE_HEADER` is `## Progress log (pointer)` and the imperative is deleted; the `updated:` stamp is surfaced verbatim with graceful no-raise degradation and zero wall-clock; the docstring records the D-06 banner-first retirement; the two pointer-only tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Budget proof at full cap, repo-map survival, and refresh the determinism snapshot</name>

  <read_first>
    - `tools/memory_regen/inject.py` — as reframed by Tasks 1-2
    - `tools/memory_regen/tests/test_inject_determinism.py` — 13-02's net, which you now extend
    - `tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr` — the snapshot you are intentionally updating
  </read_first>

  <files>tools/memory_regen/tests/test_inject_assembler.py, tools/memory_regen/tests/test_inject_determinism.py, tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr</files>

  <action>
    **(a) Budget tests (`test_inject_assembler.py`) — prove the §2 knee, which no test currently guards.**
    Build a fixture agreements dir whose composed block is at/near the **full 700-char cap**, plus a
    realistic `derived_dir` (a `repo-map.md` and a `contracts-index.md` of realistic size — the real
    ones measure ~2116 and ~501 chars; `_HEAD_LINES = 20` caps the head).
    - `test_budget_holds_with_full_agreements_block` — `len(assemble(...)) <= 4000` with a full-cap
      block present.
    - `test_repo_map_survives_full_cap_agreements` — `inject.REPO_MAP_HEADER in payload` at a
      full-cap block. **This is the test 13-RESEARCH.md Pitfall 4 exists for:** the repo-map is
      *supposed* to drop under the existing priority-truncate, so an over-generous cap would evict it
      **every session, invisibly — no test would fail**. This test is that alarm. If it goes red, the
      correct response is to report the arithmetic, **not** to raise `_AGREEMENTS_MAX_CHARS`.

    **(b) Extend 13-02's byte-identity coverage with the new dimension.** 13-02 could not pass
    `agreements_dir` (the param did not exist). Now extend `test_assemble_is_byte_identical` (or add
    a sibling) to assemble **with a populated `agreements_dir`** twice and assert identical sha256 —
    this is the T-13-06 coverage 13-VALIDATION.md specifies ("incl. populated agreements dir"). Also
    assert a **shuffled** tree (same files, different creation order, fresh `tmp_path`) yields a
    byte-identical payload — the D-08 `sorted()` guarantee.

    **(c) Refresh the snapshot — an intentional, reviewed diff.**
    The reframe deliberately changes the payload text (BANNER +183 chars, pointer +83), so
    `test_payload_matches_snapshot` **will be red** until regenerated. That is the snapshot doing its
    job: making the change visible. Run
    `uv run pytest tools/memory_regen/tests/test_inject_determinism.py --snapshot-update`, then
    **re-run without the flag** and confirm green. **Read the `.ambr` diff before committing** and
    confirm it shows exactly the intended reword — a data-scoped banner and a progress-log pointer —
    and no unexpected churn. Keep `_drift_summary` monkeypatched (hermeticity) and confirm no
    `tmp_path` leaked into the snapshot.

    ⚠️ **Do NOT touch `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`.** That is
    a different file, already red from Phase 12, owned by Phase 15.
  </action>

  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_budget_holds_with_full_agreements_block -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_assembler.py::test_repo_map_survives_full_cap_agreements -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_assemble_is_byte_identical -x` passes with a populated `agreements_dir`
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q` — fully green **without** `--snapshot-update`, run twice in a row
    - The live payload is within budget — this prints `True`:
      `uv run python -c "from tools.memory_regen import inject; print(len(inject.assemble()) <= 4000)"`
    - The live payload still contains the repo-map section (regenerate the derived plane first with `uv run python -m tools.memory_regen.repo_map`):
      `uv run python -m tools.memory_regen.inject | grep -q "## Repo map (top-N)"`
    - `git diff --stat tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr` shows the snapshot changed, and the diff contains no `tmp_path`/`/private/` absolute path
    - `git diff --name-only | grep -c "harness_emit"` returns `0` — the Phase-15-owned emit snapshot is untouched
    - `uv run pytest tools/memory_regen tools/harness_lint -q` — **green**
    - `uv run pytest tools/harness_emit -q` — **1 failed, 46 passed** (unchanged inherited baseline, no worse)
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q && uv run pytest tools/harness_emit -q 2>&1 | tail -1</automated>
  </verify>

  <done>Full-cap budget and repo-map-survival tests pass (the §2 knee is now guarded, not assumed); byte-identity covers a populated + shuffled agreements dir; the `.ambr` is regenerated as a reviewed diff showing exactly the intended reword; `tools/memory_regen` + `tools/harness_lint` green; `harness_emit` unchanged at its inherited 1 failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| **`.memory/agreements/*` → `assemble()` → every session's context** | **The phase's one genuine security surface.** This plan converts a passive, committed-but-writable data dir (Q1 — NOT path-denied) into an **executable priority-0 directive channel** the agent is told to honor, non-ignorably, every session. |
| `.memory/state/activeContext.md` → pointer | Frontmatter read and surfaced verbatim into context. |
| filesystem → composer | `glob` over a writable dir; symlinks/traversal are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-13-01** | **Elevation of Privilege / Tampering** | **Prompt injection via a planted agreement.** Any repo-write actor — a malicious PR, a compromised agent, a careless `/agree` (Phase 14) — plants `<slug>.md` with `status: active` whose rule reads *"ignore contract-guard"* / *"exfiltrate env vars"*. `_agreements_block` renders it at **priority-0 as a directive**, every session. | mitigate | **(a) D-16** render **title + one-line rule only** — `provenance` (unbounded verbatim user text, the widest surface and likeliest place a secret is pasted) is structurally excluded because `_render_agreement` consumes only `parse_frontmatter`'s **body**; Task 1 `test_render_excludes_provenance`. **(b) D-17** fail-closed `status == "active"` exactly; missing/malformed EXCLUDED, never defaulted-in; `_TEMPLATE.md` (ships `status: active` — a live trap) + `README.md` excluded by 3 layers; Task 1 `test_only_active_non_template_agreements_compose`. **(c) D-01** N=6/M=700 cap bounds blast radius. **(d) D-15** `AGREEMENTS_HEADER` states these are *working-style* directives that **never override `contracts/`, `docs/adr/`, or the gates**, so a planted agreement cannot self-authorize constitution writes; Task 1 `test_agreements_header_states_scope_limit`. **(e)** git review is the accepted Q1 trade-off. **Durable provenance lint = Phase 14 (MEM2-04) — explicitly NOT this plan.** |
| **T-13-02** | Denial of Service | **Context DoS / drift eviction** — an oversized agreements set at never-dropped priority-0 crowds out the drift summary, the live *safety* signal, so an agent stops seeing contract drift | mitigate | N/M cap (self-capped is what makes never-dropping safe) + drift stays in the never-drop tuple. Task 2 `test_agreements_banner_drift_never_dropped` (`budget_chars=1` ⇒ drift still present); Task 3 `test_budget_holds_with_full_agreements_block` + `test_repo_map_survives_full_cap_agreements`. |
| **T-13-03** | Information Disclosure | Secret leakage — agreements are committed **and now injected verbatim** | mitigate | Excluding `provenance` from the render shrinks the surface (D-16). The no-secrets README line landed in **13-01** (D-18). `secret_scan` continues to gate writes. |
| **T-13-04** | Information Disclosure | **Path traversal / symlink escape** — a crafted slug or a symlink in `agreements/` reads outside the dir | mitigate | D-19: non-recursive `glob("*.md")` (**never `rglob`** — asserted by `grep -c rglob == 0`), skip `is_symlink()`, confine `p.resolve()` under `agreements_dir.resolve()` (mirrors `docs_sync._confine` / `golden_runner._confine`). Task 1 `test_agreements_reads_are_confined`. |
| **T-13-05** | Denial of Service | **Silent injection failure** — `memory-inject.sh:40` `\|\| echo ''` turns any traceback into an **empty payload**, silently killing drift + agreements | mitigate | Every new read catches `(OSError, ValueError)` and degrades — `ValueError` included because `parse_frontmatter` raises it on a non-mapping block (`frontmatter.py:65`). Task 2 `test_absent_stamp_degrades_gracefully` covers missing file / no frontmatter / missing key / non-mapping. |
| **T-13-06** | Tampering | Determinism break as a tamper channel — non-sorted iteration makes the payload environment-dependent (`glob()` yields readdir order, which varies APFS vs ext4) | mitigate | D-08 `sorted()` + 13-02's byte-identity net, extended in Task 3 with a populated **and shuffled** agreements dir. D-03's countless overflow pointer keeps the bytes stable across add/retire. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | **No installs.** Every mechanic has an in-repo precedent (§Don't Hand-Roll). `parse_frontmatter` exists; **do NOT `uv add pyyaml`** — that is a flagged scope violation. |

**Explicitly NOT threats — do not theatricalize:** sha256 is a determinism check, not an integrity
control. No network, no auth, no PII, no untrusted deserialization (ruamel `YAML(typ="safe")`, never
`yaml.load`).
</threat_model>

<verification>
- `uv run pytest tools/memory_regen tools/harness_lint -q` — **green**.
- `uv run pytest tools/harness_emit -q` — **no worse than 1 failed, 46 passed**.
  ⚠️ `test_projected_tree_matches_committed_snapshot` is **already red on a clean tree before any
  Phase 13 work** — inherited from Phase 12's deferred re-emit. Not a regression from this plan.
  **Do NOT regenerate that `.ambr`**: it would bless Phase 12's un-emitted tree and steal Phase 15's
  gate. (The `.ambr` this plan DOES regenerate is `tools/memory_regen/.../test_inject_determinism.ambr`
  — a different file.)
- `uv run ruff check tools/memory_regen/` + `uv run ruff format --check tools/memory_regen/`.
- **Manual (operator, wording quality — tests only assert banned-phrase absence):** read
  `uv run python -m tools.memory_regen.inject` and confirm the payload *reads* as a directive plus a
  data-scoped provenance note, not as distrust of grounded work.
</verification>

<success_criteria>
- Two distinct blocks: a full-body capped agreements directive at priority-0 + a data-scoped
  provenance banner; activeContext pointer reworded to a progress-log pointer (SC1).
- Cap = 6 entries / 700 chars as named constants; overflow degrades whole-block to a countless
  pointer; block omitted when the active set is empty (SC1).
- delete+regen byte-identical incl. a populated + shuffled agreements dir; payload ≤ 4000 with a
  full-cap block; repo-map survives (SC2).
- `updated:` stamp surfaced verbatim; no wall-clock in `assemble()`; absent stamp degrades with no
  raise (SC3).
- Planned-red tests rewritten in lockstep; `test_active_context_is_pointer_not_body` +
  `test_no_full_contract_schema_body_leaks` still green.
- `harness_emit` unchanged at its inherited 1 failure. No packages installed.
</success_criteria>

<output>
Create `.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-03-SUMMARY.md` when done.
</output>
