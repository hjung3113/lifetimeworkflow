---
phase: 13-injector-reframe-channel-wiring-v2-1-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/memory_regen/tests/conftest.py
  - tools/memory_regen/tests/test_inject_determinism.py
  - tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr
autonomous: true
requirements: [MEM2-02, MEM2-05]

must_haves:
  truths:
    - "A test asserts assemble() is byte-identical across delete+regen — the net SC2 claims exists now actually exists"
    - "A committed syrupy snapshot pins the payload over a fixed fixture tree"
    - "A static test proves inject.py contains no wall-clock symbol"
    - "A static test proves neither hook wrapper contains wall-clock code"
    - "A tmp_agreements_tree fixture exists with a non-alphabetical creation order, so 13-03's sorted-iteration test is meaningful"
  artifacts:
    - path: "tools/memory_regen/tests/test_inject_determinism.py"
      provides: "byte-identity + snapshot + no-wall-clock statics"
      min_lines: 60
    - path: "tools/memory_regen/tests/conftest.py"
      provides: "tmp_agreements_tree fixture"
      contains: "def tmp_agreements_tree"
    - path: "tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr"
      provides: "committed determinism reference for the payload"
  key_links:
    - from: "tools/memory_regen/tests/test_inject_determinism.py"
      to: "tools.memory_regen.inject.assemble"
      via: "direct import + sha256 over two calls"
      pattern: "hashlib\\.sha256"
---

<objective>
Build the **determinism safety net that does not exist**. `inject.py:20-22` *claims* delete+regen
byte-identity, but no test asserts it anywhere in the repo — so SC2's "determinism preserved" is
currently **unfalsifiable**.

Purpose: this net must land **before** the 13-03 reframe, so it is a genuine regression net rather
than a post-hoc rubber stamp. It lands **green against today's `inject.py`** (verified during
planning: `assemble()` already hashes stably), which is exactly what makes it a real net.
Output: a new determinism test module, a `tmp_agreements_tree` fixture, a committed snapshot.

**No `inject.py` edit in this plan.** The `agreements_dir` parameter (D-09) is owned by **13-03**
(RESEARCH § Sequencing note, option (a)) — this keeps Wave 1 strictly `inject.py`-free.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-CONTEXT.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-VALIDATION.md
@AGENTS.md
@harness/skills/python-conventions/SKILL.md

<interfaces>
<!-- Settled during planning — do NOT re-derive, do NOT go exploring. -->

**Today's `assemble()` signature** (`inject.py:105-109`) — `agreements_dir` does NOT exist yet:

```python
def assemble(
    budget_chars: int = 4000,
    derived_dir: Path = DERIVED_DIR,
    state_dir: Path = STATE_DIR,
) -> str: ...
```

Module constants available today: `BANNER`, `DRIFT_HEADER`, `CONTRACTS_HEADER`,
`REPO_MAP_HEADER`, `ACTIVE_HEADER`; helpers `_drift_summary()`, `_read_head(path)`,
`_contracts_summary(derived_dir)`, `_repo_map_topN(derived_dir)`,
`_active_context_pointer(state_dir)`, `main(argv)`.

`_drift_summary()` takes **no parameters** and calls `run_gate()` against the **real** contracts
tree — it is NOT injectable. Live value today:
`'## Contract drift (live)\ncontract-drift: clean (live manifest matches baseline)'`.
⇒ For the **snapshot** test it MUST be monkeypatched, or a future real contract drift silently
churns the committed snapshot.

**CORRECTION to 13-RESEARCH.md / 13-VALIDATION.md Wave 0:** they claim
`tools/memory_regen/tests/__snapshots__/` "does not exist for this package". **It DOES exist**
(verified during planning) and already holds `test_contracts_index.ambr` +
`test_repo_map_determinism.ambr`. syrupy is wired and green in this package — you are ADDING one
`.ambr`, not bootstrapping snapshot infra.

**In-package syrupy precedents (copy their shape):**
- `tools/memory_regen/tests/test_repo_map_determinism.py:82` — `def test_render_matches_committed_snapshot(tmp_source_tree, snapshot)`
- `tools/memory_regen/tests/test_contracts_index.py:112`
- `tools/docs_sync/tests/test_docs_sync_determinism.py:43` — the canonical delete+regen byte-identity model

`conftest.py` already provides `repo_root` (session-scoped), `tmp_source_tree`,
`tmp_contracts_tree`, and the `sys.path` wiring. Add to it; do not restructure it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the tmp_agreements_tree fixture (non-alphabetical creation order) + a fixture self-test</name>

  <read_first>
    - `tools/memory_regen/tests/conftest.py` (full — you are appending a fixture; match its docstring style and the existing `tmp_source_tree` / `tmp_contracts_tree` shape)
    - `.memory/agreements/_TEMPLATE.md` (11 lines — the exact entry shape your synthetic fixtures must mimic, including `status: active # active | retired` on line 2)
    - `.memory/agreements/README.md` (the contract the entry shape must satisfy)
  </read_first>

  <files>tools/memory_regen/tests/conftest.py</files>

  <action>
    Add a `tmp_agreements_tree(tmp_path)` fixture returning the synthetic agreements dir `Path`.
    It exists because **the real active agreement set is EMPTY today** (`.memory/agreements/` holds
    only `_TEMPLATE.md` + `README.md`), so cap/order/overflow tests against the real dir would pass
    vacuously and rot the moment someone adds an agreement.

    **Do NOT copy the real `.memory/agreements/` dir into the fixture, and do NOT author real
    agreement content anywhere.** Agents must not invent agreements (§2 property 2); Phase 14 +
    the operator own real content. These are synthetic test doubles only.

    Create the files in this **exact, deliberately non-alphabetical** order, and record that order
    as a module-level constant (e.g. `_AGREEMENTS_CREATION_ORDER`) so the self-test can assert the
    fixture is meaningful:

    1. `zeta-proceed.md`     — frontmatter `status: active`, `added: "2026-01-02"`, a `provenance:` line. Body: an H1 title, a one-line rule, a `Related:` link line.
    2. `alpha-ground.md`     — same shape, `status: active`. Distinct title + rule.
    3. `_TEMPLATE.md`        — the **decoy**: `status: active` (mirrors the real template trap), body containing the literal placeholder text `<One-line working-style or methodology rule.>`.
    4. `middle-retired.md`   — same shape but `status: retired`.
    5. `README.md`           — **no frontmatter at all** (plain prose), mirroring the real README.

    Why this exact set: sorted order is
    `README.md, _TEMPLATE.md, alpha-ground.md, middle-retired.md, zeta-proceed.md` — which differs
    from creation order, AND the two active entries sort `alpha-ground` before `zeta-proceed` while
    being *created* in the opposite order. That is what makes 13-03's sorted-iteration test
    genuinely falsifiable rather than accidentally passing.

    Quote the `added:` dates (a bare YAML date loads as a `datetime.date` — verified in planning).
    Give each active entry a title and rule short enough that two of them fit well inside the
    700-char block cap 13-03 introduces (~96 chars per rendered entry is the design target).

    Write files with explicit `encoding="utf-8"` (repo convention, `inject.py:72`).

    Also add a fixture **self-test** in `conftest.py`'s sibling test module is NOT wanted — put the
    self-test in `test_inject_determinism.py` (Task 2). Here, only export the fixture and the
    creation-order constant.
  </action>

  <acceptance_criteria>
    - `grep -q "def tmp_agreements_tree" tools/memory_regen/tests/conftest.py` succeeds
    - `grep -q "_AGREEMENTS_CREATION_ORDER" tools/memory_regen/tests/conftest.py` succeeds
    - The fixture's creation order is NOT alphabetical — this prints `True`:
      `uv run python -c "import sys; sys.path.insert(0,'tools/memory_regen/tests'); import conftest; print(conftest._AGREEMENTS_CREATION_ORDER != sorted(conftest._AGREEMENTS_CREATION_ORDER))"`
    - The fixture yields exactly 5 `.md` files: `README.md`, `_TEMPLATE.md`, `alpha-ground.md`, `middle-retired.md`, `zeta-proceed.md`
    - `_TEMPLATE.md` in the fixture carries `status: active` AND the literal `<One-line working-style or methodology rule.>`
    - `middle-retired.md` carries `status: retired`
    - `README.md` in the fixture has NO `---` on its first line
    - `uv run pytest tools/memory_regen -q` stays green (298-passed baseline preserved; a new fixture alone adds no failures)
    - No file was created under the **real** `.memory/agreements/` — `ls .memory/agreements/` still lists exactly `README.md` and `_TEMPLATE.md`
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen -q</automated>
  </verify>

  <done>`tmp_agreements_tree` exists in conftest.py producing 5 synthetic files (2 active, 1 retired, 1 template decoy carrying `status: active`, 1 no-frontmatter README) created in a documented non-alphabetical order; the real agreements dir is untouched; `tools/memory_regen` green.</done>
</task>

<task type="auto">
  <name>Task 2: Build test_inject_determinism.py — byte-identity, committed snapshot, no-wall-clock statics</name>

  <read_first>
    - `tools/docs_sync/tests/test_docs_sync_determinism.py` lines 34-70 — **the canonical delete+regen byte-identity model you are copying**
    - `tools/memory_regen/tests/test_repo_map_determinism.py` lines 75-95 — the in-package syrupy snapshot shape + the "tmp_path must never leak into output" discipline (its docstring line 13)
    - `tools/memory_regen/inject.py` (148 lines — read fully; you assert against its source text and call its helpers)
    - `.claude/hooks/memory-inject.sh` (45 lines — read fully; you assert against its source text)
    - `harness/plugins/session-inject.ts` lines 1-30 — the authored-only opencode twin you assert against
    - `tools/memory_regen/tests/conftest.py` — as extended by Task 1
  </read_first>

  <files>tools/memory_regen/tests/test_inject_determinism.py, tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr</files>

  <action>
    Create `tools/memory_regen/tests/test_inject_determinism.py` with a module docstring explaining
    that it backfills the net `inject.py:20-22` claims but never asserted (SC2), and that it is
    intentionally written **before** the 13-03 reframe so it protects the reframe rather than
    ratifying it.

    Implement these tests. Use `hashlib.sha256` over `.encode("utf-8")` — never `git diff` (the
    derived plane is gitignored, `.gitignore:23`, so git diff proves nothing).

    **1. `test_assemble_is_byte_identical`** — call `inject.assemble()` twice with the SAME fixture
    `derived_dir` + `state_dir` (build a tmp derived dir holding a small `contracts-index.md` and
    `repo-map.md`); assert the two sha256 digests are equal. Covers SC2 / T-13-06.

    **2. `test_assemble_delete_regenerate_is_byte_identical`** — mirror
    `docs_sync:43` for a function that returns a `str` rather than writing files: assemble → write
    the payload to a `tmp_path` file → sha256 → `unlink()` → assert not exists → assemble again →
    write → sha256 → assert digests equal. This is the literal "delete+regen identical" claim from
    the `inject.py` docstring.

    **3. `test_payload_matches_snapshot(snapshot, monkeypatch, tmp_path)`** — the committed
    determinism reference.
    - **`monkeypatch.setattr(inject, "_drift_summary", lambda: <fixed string>)`** — REQUIRED.
      `_drift_summary()` takes no params and calls `run_gate()` against the real contracts tree; an
      unpatched call makes the snapshot churn the day a real contract drifts. Use a fixed literal
      that includes `inject.DRIFT_HEADER`.
    - Pass fixture `derived_dir` + `state_dir` (tmp_path-based) so nothing reads the real `.memory/`.
    - `assert payload == snapshot`.
    - **Also assert `str(tmp_path) not in payload`** — the tmp path must never leak into the output;
      that leak is exactly what would make the committed snapshot unstable
      (`test_repo_map_determinism.py:13` states this discipline).
    - Generate the `.ambr` with `uv run pytest tools/memory_regen/tests/test_inject_determinism.py --snapshot-update`, then **re-run without the flag** and confirm green. Commit the `.ambr`.

    **4. `test_inject_module_has_no_wallclock`** (SC3 / D-11) — read `inject.py`'s source text and
    assert NONE of these substrings appear: `datetime`, `date.today`, `.now()`, `time.time`,
    `time.monotonic`. **Verified during planning: `inject.py` is CLEAN of all five today**, so this
    lands green. Assert on the full source (these tokens are unambiguous and do not appear in the
    module's prose, so no comment-stripping is needed).

    **5. `test_hook_wrappers_have_no_wallclock`** (SC3 / D-11) — assert the two runtime envelopes
    contain no wall-clock code:
    - `.claude/hooks/memory-inject.sh` — ⚠️ **DO NOT gate on the bare substring `date`.** It
      **false-positives**: line 4 contains `gsd-check-update.js` (up**date**). Verified in planning.
      Gate on **command-shaped** tokens only: `$(date`, a backtick-`date`, and a line matching the
      regex `^\s*date\b`. Assert each is absent.
    - `harness/plugins/session-inject.ts` — assert `Date.now` and `new Date` are absent (verified
      CLEAN today).
    Resolve both paths from the `repo_root` fixture.

    **6. `test_tmp_agreements_tree_fixture_shape(tmp_agreements_tree)`** — the fixture self-test, so
    Task 1's fixture is not dead code in Wave 1. Assert: exactly the 5 expected filenames exist;
    `_TEMPLATE.md` carries `status: active` and the placeholder text; `middle-retired.md` carries
    `status: retired`; the fixture `README.md` has no leading `---`; and
    `conftest._AGREEMENTS_CREATION_ORDER != sorted(...)` (the fixture is genuinely non-alphabetical).

    **Scope fence — do NOT do these here:**
    - Do NOT add `agreements_dir` to `assemble()` — that is **13-03's** (D-09).
    - Do NOT pass agreements into `assemble()`; the parameter does not exist yet. The
      byte-identity coverage over a *populated agreements dir* is added by 13-03 when it extends
      this module.
    - Do NOT edit `inject.py`, `test_inject_assembler.py`, or any `.memory/` file.
    - Do NOT `uv add` anything — pytest + syrupy + ruamel are already in the workspace.
  </action>

  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q` — **all tests pass** (this net lands GREEN against today's `inject.py`; a red test here means the test is wrong, not `inject.py`)
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_assemble_is_byte_identical -x` passes (13-VALIDATION.md command, verbatim)
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_payload_matches_snapshot -x` passes (13-VALIDATION.md command, verbatim)
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_inject_module_has_no_wallclock -x` passes
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_hook_wrappers_have_no_wallclock -x` passes
    - `tools/memory_regen/tests/__snapshots__/test_inject_determinism.ambr` exists and is committed
    - Snapshot is stable without the update flag: running the module twice in a row with **no** `--snapshot-update` is green both times
    - `grep -q "monkeypatch" tools/memory_regen/tests/test_inject_determinism.py` succeeds (drift is patched for snapshot hermeticity)
    - The shell wall-clock gate is not the naive one: `grep -q "gsd-check-update\|\\$(date" tools/memory_regen/tests/test_inject_determinism.py` — the test must reference command-shaped tokens, and a bare `"date" not in text` assertion MUST NOT appear
    - `git diff --name-only` shows only `conftest.py`, `test_inject_determinism.py`, and the new `.ambr` — **`inject.py` is NOT in the diff**
    - `uv run pytest tools/memory_regen tools/harness_lint -q` — green
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q && git diff --name-only | grep -q "inject.py$" && echo "FAIL: inject.py must not be touched in 13-02" && exit 1 || uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q</automated>
  </verify>

  <done>`test_inject_determinism.py` exists and is fully green against today's `inject.py`: byte-identity (two-call + delete/regen), a committed hermetic `.ambr` snapshot with drift monkeypatched and no tmp_path leak, no-wall-clock statics for `inject.py` + both hook wrappers (using command-shaped shell tokens, not the false-positiving bare `date`), and a fixture self-test. `inject.py` untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → `assemble()` | Synthetic `tmp_path` trees stand in for the real `.memory/` so tests never depend on (or mutate) the committed memory planes. |
| repo source → static gates | `inject.py` / `memory-inject.sh` / `session-inject.ts` source text is the input to the no-wall-clock assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | Determinism break as a tamper channel — non-sorted iteration or a wall-clock makes the payload environment-dependent, so diffs become unreviewable and an attacker-influenced ordering could shift which entries land | mitigate | This plan **is** the mitigation: `test_assemble_is_byte_identical` + `test_assemble_delete_regenerate_is_byte_identical` + a committed `.ambr` reference. Landing it in Wave 1 (before 13-03) makes it a regression net, not a rubber stamp. |
| T-13-05 | Denial of Service | Silent injection failure — `memory-inject.sh:40` `\|\| echo ''` turns any traceback into an empty payload, silently killing drift + agreements | mitigate | The byte-identity + snapshot tests call `assemble()` directly and would surface a raise as a test error rather than a silent blank. 13-03 adds the missing-file/`OSError` degradation tests for the new reads. |
| — (D-11 enforcement) | Tampering | Wall-clock in `assemble()` or a hook wrapper would break determinism AND violate Q6 (agent-side freshness, no threshold) | mitigate | `test_inject_module_has_no_wallclock` + `test_hook_wrappers_have_no_wallclock`, using command-shaped shell tokens to avoid the verified `gsd-check-update.js` false positive. |
| T-13-01 | Elevation of Privilege | This plan does NOT author agreement content; fixtures are synthetic doubles under `tmp_path` | accept | Acceptance criterion asserts the real `.memory/agreements/` still holds exactly `README.md` + `_TEMPLATE.md`. Fixture `_TEMPLATE.md` deliberately reproduces the `status: active` trap so 13-03's fail-closed filter is tested against it. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | **No installs.** pytest, syrupy, ruamel already in the uv workspace (`13-RESEARCH.md` § Package Legitimacy Audit: "Not applicable — Phase 13 installs no external packages"). A `uv add` — including `pyyaml` — is a scope violation; `parse_frontmatter` already exists. |

**Explicitly NOT threats — do not theatricalize:** sha256 here is a determinism check, **not** an
integrity control. No network, no auth, no PII, no untrusted deserialization (`parse_frontmatter`
uses ruamel `YAML(typ="safe")` on in-repo text — never `yaml.load`).
</threat_model>

<verification>
- `uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q` — fully green.
- `uv run pytest tools/memory_regen tools/harness_lint -q` — green (baseline **298 passed**, now higher).
- `uv run pytest tools/harness_emit -q` — **no worse than 1 failed, 46 passed**.
  ⚠️ That failure (`test_projected_tree_matches_committed_snapshot`) is **already red on a clean tree
  before any Phase 13 work**, inherited from Phase 12's deferred re-emit. Not caused by this plan.
  **Do NOT regenerate that `.ambr`** — it would bless Phase 12's un-emitted tree and steal Phase 15's
  gate. (The `.ambr` you DO create here is a different file, for `tools/memory_regen`.)
- `git diff --name-only` must NOT contain `tools/memory_regen/inject.py`.
</verification>

<success_criteria>
- The SC2 determinism net exists and is green: two-call + delete/regen byte-identity (SC2).
- A committed, hermetic `.ambr` pins the payload; drift is monkeypatched; no tmp_path leak (SC2).
- No wall-clock in `inject.py` or either hook wrapper, proven statically (SC3), with a shell gate
  that does not false-positive on `gsd-check-update.js`.
- `tmp_agreements_tree` exists with a non-alphabetical creation order and a self-test (enables SC2's
  sorted-iteration test in 13-03).
- `inject.py` untouched; `agreements_dir` deferred to 13-03; no packages installed.
</success_criteria>

<output>
Create `.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-02-SUMMARY.md` when done.
</output>
