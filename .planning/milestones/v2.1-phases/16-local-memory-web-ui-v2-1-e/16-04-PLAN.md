---
phase: 16-local-memory-web-ui-v2-1-e
plan: 04
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - harness/plugins/session-inject.ts
  - harness/commands/orient.md
  - harness/commands/refresh-memory.md
  - .opencode/plugin/session-inject.ts
  - .claude/hooks/memory-inject.sh
  - .opencode/command/orient.md
  - .claude/commands/orient.md
  - .opencode/command/refresh-memory.md
  - .claude/commands/refresh-memory.md
autonomous: true
requirements: [MEM2-07]
must_haves:
  truths:
    - "The pointer-index generator is wired into the derived-regen path (SessionStart + /orient + /refresh-memory) so it is generated, never hand-maintained"
    - "The harness source edits round-trip through the Phase-7 emitter to both runtimes with no model id; emit-drift is clean"
    - "inject.py's byte-identity determinism + no-wall-clock gates stay green"
  artifacts:
    - path: "harness/plugins/session-inject.ts"
      provides: "SessionStart regen loop including tools.memory_regen.pointer_index"
      contains: "pointer_index"
    - path: "harness/commands/refresh-memory.md"
      provides: "/refresh-memory runs the pointer-index generator"
      contains: "pointer_index"
  key_links:
    - from: "harness/plugins/session-inject.ts"
      to: "tools.memory_regen.pointer_index"
      via: "regen module list (line ~36)"
      pattern: "tools\\.memory_regen\\.pointer_index"
    - from: "harness/ source"
      to: ".opencode/ + .claude/"
      via: "tools.harness_emit round-trip"
      pattern: "pointer_index"
---

<objective>
Wire the pointer-index generator into the existing derived-regen path so SC2's "generated, not
hand-maintained" is structurally true, then round-trip the harness source edits through the Phase-7
emitter to both runtime trees. This is the deliberate emit round-trip flagged in RESEARCH Open-Q3 /
A4 — it is a distinct concern from the UI and gets its own plan so the round-trip is not accidental.

Purpose: `pointer-index.{json,md}` regenerates alongside `repo_map` + `contracts_index` at
SessionStart and on `/orient` / `/refresh-memory`, matching the curator posture; the emit-drift gate
must stay clean (Phase-15 lesson: touching `harness/` requires re-emit + committing both trees).
Output: three harness source edits + the emitted `.opencode/` and `.claude/` trees.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-RESEARCH.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-PATTERNS.md

<interfaces>
From harness/plugins/session-inject.ts (line ~36):
- `for (const mod of ["tools.memory_regen.repo_map", "tools.memory_regen.contracts_index"]) { ... }`  → append `"tools.memory_regen.pointer_index"`

From harness/commands (regen macro lines):
- orient.md:21 `!` uv run python -m tools.memory_regen.repo_map && ... contracts_index `!`
- refresh-memory.md:21/28 `!` uv run python -m tools.memory_regen.repo_map `!` / `... contracts_index`

Emitter: `python -m tools.harness_emit` projects harness/ → .opencode/ + .claude/ (glob discovery; zero emitter code change needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pointer_index to the SessionStart regen loop + /orient + /refresh-memory</name>
  <files>harness/plugins/session-inject.ts, harness/commands/orient.md, harness/commands/refresh-memory.md</files>
  <read_first>
    - harness/plugins/session-inject.ts (line 36 regen module array)
    - harness/commands/orient.md (lines 21, 30 regen macros)
    - harness/commands/refresh-memory.md (lines 21, 28, 34 regen macros + the "invokes ONLY tools.memory_regen.* / tools.docs_sync" note)
  </read_first>
  <action>
    Append `"tools.memory_regen.pointer_index"` to the regen module list in
    `harness/plugins/session-inject.ts` (line ~36), preserving array order/format. Add a
    `uv run python -m tools.memory_regen.pointer_index` invocation to the regen macro block in both
    `harness/commands/orient.md` and `harness/commands/refresh-memory.md` alongside the existing
    `repo_map`/`contracts_index` calls (keep the refresh-memory note "invokes ONLY tools.memory_regen.*
    + tools.docs_sync" true — pointer_index is a `tools.memory_regen.*` module, so the note holds).
    Do NOT touch `inject.py` — the pointer-index is a separate derived artifact the injector does not
    need to read; the byte-identity determinism + no-wall-clock gates on `inject.py` must stay green.
  </action>
  <verify>
    <automated>grep -q "tools.memory_regen.pointer_index" harness/plugins/session-inject.ts && grep -q "pointer_index" harness/commands/orient.md && grep -q "pointer_index" harness/commands/refresh-memory.md && uv run pytest tools/memory_regen/tests -k determinism -q</automated>
  </verify>
  <acceptance_criteria>
    - `session-inject.ts` regen list, `orient.md`, and `refresh-memory.md` each invoke `tools.memory_regen.pointer_index`.
    - `inject.py` is unmodified; `inject` determinism + no-wall-clock tests stay green.
    - The refresh-memory "tools.memory_regen.* only" note remains accurate.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Emit round-trip to both runtimes + verify emit-drift clean</name>
  <files>.opencode/plugin/session-inject.ts, .claude/hooks/memory-inject.sh, .opencode/command/orient.md, .claude/commands/orient.md, .opencode/command/refresh-memory.md, .claude/commands/refresh-memory.md</files>
  <read_first>
    - .planning/phases/16-local-memory-web-ui-v2-1-e/16-RESEARCH.md (Pitfall 3 — emit round-trip requirement; the Phase-15 lesson)
    - tools/harness_emit (the emitter entrypoint + emit-drift check semantics)
  </read_first>
  <action>
    Run `python -m tools.harness_emit` (uv) to project the Task-1 source edits into `.opencode/` and
    `.claude/`. Commit exactly the emitter's output — do NOT hand-edit the runtime trees and do NOT
    `--snapshot-update` any emit fixture to force green (Phase-15 CR-01 lesson: the proof is the CI
    replica `re-emit && git diff --exit-code`, not a blessed snapshot). Verify NO model id leaked into
    any emitted artifact (the emitter validator enforces this). Confirm the two runtime trees carry
    the pointer_index wiring and that a re-emit is a byte-identical no-op.
  </action>
  <verify>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- .opencode .claude && uv run pytest tools/harness_emit -q</automated>
  </verify>
  <acceptance_criteria>
    - After emit, a re-emit + `git diff --exit-code` over `.opencode/` and `.claude/` is exit 0 (emit-drift clean).
    - Both runtime trees reference `tools.memory_regen.pointer_index` in the regen path.
    - `harness_emit` test suite green; no placeholder-tier/real model id in emitted output.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/ source → emitted runtime trees | an un-emitted edit leaves the runtimes stale (emit-drift); a model id could leak into a shipped artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Tampering | model id / drift leaking into emitted `.opencode`/`.claude` | mitigate | Run `tools.harness_emit`; commit only its output; emitter validator rejects real model ids; verify `re-emit && git diff --exit-code` = 0 |
| T-16-13 | Tampering | breaking `inject.py` determinism by over-wiring | mitigate | Do not touch `inject.py`; pointer-index is a separate derived artifact; re-run inject determinism + no-wall-clock gates |
| T-16-SC | Tampering | pip/uv installs | accept | Zero external packages; wiring is source edits + emit only |
</threat_model>

<verification>
- `python -m tools.harness_emit` then `git diff --exit-code -- .opencode .claude` = 0.
- pointer_index present in SessionStart regen + /orient + /refresh-memory.
- inject.py determinism + no-wall-clock gates green.
</verification>

<success_criteria>
- SC2 fully met: the pointer-index is wired into the derived-regen path (generated, never hand-maintained) and the change is round-tripped to both runtimes with no drift and no model id.
</success_criteria>

<output>
Create `.planning/phases/16-local-memory-web-ui-v2-1-e/16-04-SUMMARY.md` when done.
</output>
