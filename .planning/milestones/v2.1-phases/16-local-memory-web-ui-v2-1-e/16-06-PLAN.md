---
phase: 16-local-memory-web-ui-v2-1-e
plan: 06
type: execute
wave: 4
depends_on: [16-05]
files_modified: []
autonomous: false
requirements: [MEM2-07]
must_haves:
  truths:
    - "A human confirms the localhost page renders and the view/edit/retire round-trip works in a real browser"
    - "The N-referrer orphan-warning dialog appears and blocks retire behind an explicit confirm"
  artifacts: []
  key_links: []
---

<objective>
Final human verification of SC1/SC3's visual round-trip — the one behavior automated pytest cannot
cover (16-VALIDATION Manual-Only). Claude has already automated everything (generator, routes,
server, page, wiring); this plan is the human confirming the browser experience.

Purpose: prove the end-to-end surface works in a real browser, including the load-bearing orphan
confirm dialog, before `/gsd:verify-work`.
Output: human approval (or a defect list to feed a gap-closure plan).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-UI-SPEC.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human browser round-trip verification</name>
  <action>Human runs the local tool and confirms the browser round-trip per the steps below; automation is already complete (all pytest green). This task pauses for the human to verify the visual/interactive surface pytest cannot cover.</action>
  <what-built>
    A local, no-auth memory UI: `python -m tools.memory_ui` serves a single inlined page on
    127.0.0.1 that lists progress state + agreements, views/edits/retires them through the sanctioned
    writers, and surfaces "what points to this item" from the derived pointer-index with a
    surface-and-confirm orphan guard. All handler logic, pointer-index generation, and orphan
    detection are already covered by green pytest.
  </what-built>
  <how-to-verify>
    1. Run `uv run python -m tools.memory_ui` and open the printed `http://127.0.0.1:<port>` in a browser.
    2. Confirm the two-column layout renders with the Progress state group (activeContext.md,
       progress.md) and the Agreements group (empty-state copy is expected — the active set is
       legitimately empty today).
    3. Select `activeContext.md`, make a small body edit, Save — confirm the "Saved. `updated:`
       stamped …" message and that the `updated:` frontmatter is a quoted date.
    4. Confirm the Referrers ("What points to this item") sub-panel renders for a selected item and
       shows the zero-referrer reassurance copy or a `file:line` list with `path`/`slug` tags.
    5. To exercise the orphan dialog: with a scan-root document that references a memory item, trigger
       a retire/edit and confirm the amber N-referrer `<dialog>` appears, `Cancel` holds default
       focus, `Esc` closes, and only `Retire anyway` proceeds — and that the referrer files are NOT
       rewritten by the tool.
    6. Confirm nothing is fetched from the network (offline still renders; no external asset).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues (each becomes a gap for `/gsd:plan-phase --gaps`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human operator → running local tool | manual confirmation of the localhost-only, no-external-fetch posture in a real browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Info Disclosure | live bind exposure | mitigate | Human confirms the URL is `127.0.0.1` and the page renders offline (no external fetch) |
| T-16-SC | Tampering | pip/uv installs | accept | No installs in this plan; verification only |
</threat_model>

<verification>
- Human approval that the browser round-trip (list/view/edit/retire + orphan confirm) works and nothing is fetched off-origin.
</verification>

<success_criteria>
- SC1 + SC3 confirmed end-to-end in a real browser; any defect is captured for gap closure.
</success_criteria>

<output>
Create `.planning/phases/16-local-memory-web-ui-v2-1-e/16-06-SUMMARY.md` when done.
</output>
