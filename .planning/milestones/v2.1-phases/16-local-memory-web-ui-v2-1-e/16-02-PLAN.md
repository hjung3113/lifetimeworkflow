---
phase: 16-local-memory-web-ui-v2-1-e
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - tools/memory_regen/pointer_index.py
  - tools/memory_regen/tests/__snapshots__/test_pointer_index.ambr
autonomous: true
requirements: [MEM2-07]
must_haves:
  truths:
    - "Running `python -m tools.memory_regen.pointer_index` writes .memory/derived/pointer-index.{json,md} deterministically"
    - "The index answers 'what points to this memory item' keyed by memory-item → [{file,line,kind}]"
    - "Regenerating twice is byte-identical; the .md twin carries the DERIVED header; no timestamp/float in the body"
  artifacts:
    - path: "tools/memory_regen/pointer_index.py"
      provides: "DERIVED reference-scanner generator (SC2)"
      exports: ["build_index", "render_md", "write", "main", "DERIVED_HEADER"]
      min_lines: 60
    - path: "tools/memory_regen/tests/__snapshots__/test_pointer_index.ambr"
      provides: "Committed syrupy reference over the tmp fixture"
  key_links:
    - from: "tools/memory_regen/pointer_index.py"
      to: ".memory/derived/pointer-index.json"
      via: "write()"
      pattern: "pointer-index\\.json"
    - from: "tools/memory_regen/pointer_index.py"
      to: "tools.harness_lint.agreements.iter_agreement_files"
      via: "enumerate memory items"
      pattern: "iter_agreement_files"
---

<objective>
Build the load-bearing new engine of Phase 16: a DERIVED reference-index generator
`tools/memory_regen/pointer_index.py` that scans a fixed set of roots and produces
`.memory/derived/pointer-index.{json,md}` answering "what points to this memory item" (SC2). It is a
faithful clone of `repo_map.py`'s generator shape — module-level paths + `DERIVED_HEADER`, the
`build_index → render_md → write → main` quartet, deterministic sort, and NO timestamp/wall-clock/float.

Purpose: this index is the data source for the UI's Referrers panel AND the referential-integrity
orphan check. It must be gitignored/session-ephemeral (like `repo-map.md`), so its correctness is
proven by regenerate-and-hash, never `git diff`.
Output: the generator module + the committed syrupy snapshot; the RED tests from 16-01 turn GREEN.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-RESEARCH.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-PATTERNS.md
@.memory/README.md
@.memory/agreements/README.md

<interfaces>
From tools/memory_regen/repo_map.py (clone the shape):
- `_REPO_ROOT = Path(__file__).resolve().parents[2]`
- `DERIVED_DIR = _REPO_ROOT / ".memory" / "derived"`
- `_iter_source_files(source_roots)` — symlink-confined, sorted, suffix-gated walk (lines 53-71)

From tools/harness_lint/agreements.py:
- `iter_agreement_files(agreements_dir) -> list[Path]`
- `load_agreement(path) -> tuple[dict, str] | None`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pointer scan — build_index() over the fixed scan roots</name>
  <files>tools/memory_regen/pointer_index.py</files>
  <read_first>
    - tools/memory_regen/repo_map.py (lines 33-71 for paths + _iter_source_files; lines 88-128 for sort discipline)
    - tools/harness_lint/agreements.py (iter_agreement_files / load_agreement)
    - .planning/phases/16-local-memory-web-ui-v2-1-e/16-PATTERNS.md (Pattern Assignments → pointer_index, lines 29-82)
    - .planning/phases/16-local-memory-web-ui-v2-1-e/16-RESEARCH.md (Pattern 2 — what counts as a referrer)
  </read_first>
  <behavior>
    - Memory items enumerated = `.memory/state/activeContext.md`, `.memory/state/progress.md`, plus every file from `iter_agreement_files(agreements_dir)` (active AND retired).
    - Scan roots (D-16-02): `docs/`, `harness/` (recursive), `tools/memory_regen/inject.py`, `.memory/README.md`, `AGENTS.md` (single-file roots).
    - A referrer is recorded when a scanned line contains (a) the item's `.memory/...` POSIX path string → `kind:"path"`, or (b) for agreements only, the slug as a word-boundaried token → `kind:"slug"`.
    - `.memory/derived/` is excluded from any walk (no self-reference); non-text suffixes skipped via an allow-list (`.md`,`.ts`,`.py`,`.json`,`.toml`; suffixless treated as `.md`).
    - Output keys and each referrer list `(file, line, kind)` are sorted; all paths are repo-relative POSIX.
  </behavior>
  <action>
    Create `tools/memory_regen/pointer_index.py`. Clone `repo_map.py` module-level constants
    (`_REPO_ROOT`, `DERIVED_DIR`, `JSON_PATH = DERIVED_DIR/"pointer-index.json"`, `MD_PATH =
    DERIVED_DIR/"pointer-index.md"`, `DERIVED_HEADER = "DERIVED — do not hand-edit
    (tools/memory_regen/pointer_index.py)"`). Reuse `repo_map._iter_source_files`'s symlink-confined
    walk idiom (per D-16 discretion: import-and-share preferred; a suffix-gated local copy is the
    fixture-parity fallback). Implement `build_index(*, base_dir=None, scan_roots=None) -> dict`:
    resolve `base_dir` (default `_REPO_ROOT`) to locate `.memory/state` + `.memory/agreements`;
    enumerate items; for each scanned file, read line-by-line and match path strings + word-boundaried
    slug tokens using `re.search(r"(?<![\w-])" + re.escape(slug) + r"(?![\w-])", line)`. Prefer a
    full `.memory/agreements/<slug>.md` path hit (`kind:"path"`) over a bare-slug hit. Sort everything
    before return. Exclude any path under `.memory/derived/`.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_pointer_index.py::test_no_self_reference_and_no_false_positive tools/memory_regen/tests/test_pointer_index.py::test_referrer_shape -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `build_index` returns `{item: [{"file","line","kind"}, ...]}` with sorted keys/lists and `kind in {"path","slug"}`.
    - No referrer path lies under `.memory/derived/`; slug `plan` does not match token `planner`.
    - Items include both state files and every agreement (active + retired) even with zero referrers (empty list value).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render_md/write/main + DERIVED header + committed snapshot</name>
  <files>tools/memory_regen/pointer_index.py, tools/memory_regen/tests/__snapshots__/test_pointer_index.ambr</files>
  <read_first>
    - tools/memory_regen/contracts_index.py (render/write/main quartet; lines 83-123)
    - tools/memory_regen/repo_map.py (write/main; lines 188-211)
    - tools/memory_regen/tests/test_pointer_index.py (the assertions this must satisfy — authored in 16-01)
  </read_first>
  <behavior>
    - `render_md(index)` first line is `# {DERIVED_HEADER}`, then a stable sorted table; no timestamp, no raw float, no wall-clock anywhere.
    - `write(json_path=JSON_PATH, md_path=MD_PATH, *, base_dir=None, scan_roots=None)` mkdirs parents, writes `json.dumps(index, indent=2, sort_keys=True) + "\n"` and the md twin; returns both paths.
    - `main(argv=None)` calls `write()`, prints `wrote {path.relative_to(_REPO_ROOT)}`, returns 0; runnable as `python -m tools.memory_regen.pointer_index`.
    - Two consecutive `write()` calls are byte-identical (determinism).
  </behavior>
  <action>
    Add `render_md`, `write`, and `main` to `pointer_index.py`, cloning `contracts_index.py`'s
    render/write/main and `repo_map.py`'s no-timestamp discipline. Then generate the committed syrupy
    snapshot by running the snapshot test with `--snapshot-update` ONCE so
    `tools/memory_regen/tests/__snapshots__/test_pointer_index.ambr` is created over the tmp fixture
    (keyed to base_dir; never leaks tmp paths). Confirm the whole `test_pointer_index.py` suite is
    green afterwards. Do NOT `--snapshot-update` a second time to "fix" a failure — a genuine
    determinism break must surface as a red test, not be blessed.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_pointer_index.py -q && uv run python -m tools.memory_regen.pointer_index && test -f .memory/derived/pointer-index.json && head -1 .memory/derived/pointer-index.md | grep -q "DERIVED — do not hand-edit (tools/memory_regen/pointer_index.py)"</automated>
  </verify>
  <acceptance_criteria>
    - Full `test_pointer_index.py` suite passes (determinism, header, no-timestamp, snapshot, scan).
    - `python -m tools.memory_regen.pointer_index` writes both `.memory/derived/pointer-index.json` and `.md`; the `.md` first line carries the DERIVED header naming `pointer_index.py`.
    - `grep -E '[0-9]{4}-[0-9]{2}-[0-9]{2}' .memory/derived/pointer-index.*` returns nothing (no timestamp).
    - The `.ambr` snapshot is committed and contains no absolute `/tmp`/`tmp_path` fragments.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan roots → generator | untrusted-shaped filesystem (symlinks) crosses into the walk |
| generator → `.memory/agreements/` | tier contract: memory_regen may READ agreements but must NEVER write them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering/Elevation | scan-root file walk (symlink escaping the subtree) | mitigate | Reuse `repo_map._iter_source_files` symlink confinement (`root_resolved not in resolved.parents → skip`) |
| T-16-10 | Tampering | generator writing into `.memory/agreements/` | mitigate | Generator writes ONLY under `.memory/derived/`; never opens agreements for write; enumerates via read-only `iter_agreement_files` |
| T-16-11 | Info Disclosure (churn) | self-reference / non-determinism in derived output | mitigate | Exclude `.memory/derived/` from walk; `sort_keys=True`; no timestamp/float; proven by write→hash→delete→regenerate (NOT git diff — target gitignored) |
| T-16-SC | Tampering | pip/uv installs | accept | Zero external packages (stdlib `json`/`re`/`pathlib`/`hashlib`); module lives inside the existing `tools/memory_regen` member — no lock change |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen/tests/test_pointer_index.py -q` green.
- `python -m tools.memory_regen.pointer_index` writes both artifacts under gitignored `.memory/derived/`.
- No timestamp/float in the output; `.md` header names the generator.
</verification>

<success_criteria>
- SC2 core met: a machine-built derived pointer-index exists, is deterministic, and is generated (never hand-maintained).
- The UI (16-03) and orphan check can now read `pointer-index.json`.
</success_criteria>

<output>
Create `.planning/phases/16-local-memory-web-ui-v2-1-e/16-02-SUMMARY.md` when done.
</output>
