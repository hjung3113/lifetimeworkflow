# Phase 9: Self-Maintaining Derived Artifacts + Curator (v2.0 α) - Pattern Map

**Mapped:** 2026-07-13
**Files analyzed:** 12 (2 new, 8 modified, 1 regenerated-derived, 1 Wave-0 tree reconcile)
**Analogs found:** 11 / 11 (every new/modified file has a live in-tree analog — this is a reuse-not-rebuild phase)

> **Reuse-not-rebuild.** Every generator, gate, persona-shape, and emitter this phase needs already
> exists and was read from the live tree this session. The planner should COPY the analogs below and
> apply the small deltas — no new generation engine, no new CI machinery pattern, no new persona
> frontmatter shape.

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `harness/agents/curator.md` (NEW) | agent/persona | request-response (read-mostly invoker) | `harness/agents/python-engineer.md` (edit+bash) + `harness/agents/code-reviewer.md` (read-mostly boundary) | role-match (hybrid) |
| `harness/commands/refresh-memory.md` (NEW) | command (macro) | batch / shell-out | `harness/commands/orient.md` | exact |
| `tools/harness_lint/caps.py` (MOD) | config (SSOT constants) | transform | its own `EXPECTED_PERSONAS` frozenset (line 54) | exact (in-file) |
| `tools/harness_lint/tests/test_agents.py` (MOD) | test | assertion | its own `test_expected_personas_present_no_sprawl` + `test_read_only_personas...` | exact (in-file) |
| `tools/docs_sync/generate.py` (MOD) | service/generator | file-I/O (write→prune-then-write) | its own `write()` (line 222) + `tools/harness_emit/manifest.prune_then_write` | role-match |
| `tools/docs_sync/tests/test_docs_sync_determinism.py` (MOD) | test | assertion | its own `test_seed_schemas_map_one_to_one_to_pages` (line 106) | exact (in-file) |
| `.github/workflows/ci.yml` (MOD) | config / CI job | request-response (regen→diff) | its own `emit-drift` job (lines 179-189) + `gate.needs` (line 198) | exact |
| `harness/commands/verify-work.md` (MOD) | command (composite gate) | shell-out | its own steps 1-4 (lines 23-44), esp. the presence-safe `/golden` step | exact (in-file) |
| `.gitignore` (MOD) | config | — | its own `.memory/derived/` rule (line 19) | exact (in-file) |
| `tools/harness_emit/emit-manifest.json` (REGEN) | derived artifact | — | its own path list (regenerated by `python -m tools.harness_emit`) | exact |
| `harness/skills/two-plane-memory/SKILL.md` (MOD) | skill/doc | — | its own plane-semantics prose (add committed-derived sub-tier) | in-file |
| `docs/reference/` Wave-0 reconcile (TREE) | derived artifact | file-I/O | `docs_sync.write()` output set = `EXPECTED_PAGES` | exact |

## Pattern Assignments

### `harness/agents/curator.md` (NEW — read-mostly persona with a narrow write affordance)

**Primary analog:** `harness/agents/python-engineer.md` (has `edit: allow` + `bash` — the write+shell shape curator needs)
**Boundary analog:** `harness/agents/code-reviewer.md` (dual-representation frontmatter; the "never writes constitution" prose)

**Frontmatter shape to copy** (`python-engineer.md` lines 1-16 — dual-representation `permission:` + Claude `tools:`):
```markdown
---
name: python-engineer
mode: subagent
permission:
  read: allow
  edit: allow
  bash:
    "*": ask
    "uv *": allow
    "pytest *": allow
tools: Read, Edit, Bash, Grep, Glob
---
```

**Delta to apply:**
- `name: curator`; `mode: subagent` (NOT primary — orchestrator stays the sole primary, see the topology gate below).
- `description:` MUST carry a routing-trigger token (`use`/`when`) — enforced by `test_description_is_routing_signal` (test_agents.py line 89). Copy the `code-reviewer` description voice ("Use when the derived plane needs refresh...").
- Keep `edit: allow` + `bash` with `uv *`/`python -m *` allowed (curator runs generators + writes derived). Because it has these allows, curator is NOT read-only and MUST stay out of `READ_ONLY_PERSONAS`.
- NO real `model:` key — omit it, or use ONLY the placeholder `provider/explorer-tier` (test_agents.py `test_no_real_model_identifier`, line 120).
- Body: state the D-05/D-06 boundary in prose (writes derived only; regenerates ONLY by invoking `tools.memory_regen` + `tools.docs_sync`; never hand-edits a derived file; never writes `contracts/`/`docs/adr/`/`golden/`). The HARD deny is already global data (see Shared Pattern: Constitution write-deny) — the prose is advisory, the matrix is the enforcement.
- Keep domain-neutral: no `examples/`, `equipment`, `standard-log`, `dotnet` tokens (GEN-04 guard `test_core_no_example_dep.py`).

**Constitution-deny prose to echo** (`code-reviewer.md` lines 32-35):
```markdown
- Attempts to self-bless a golden, hand-edit derived plane, or write the constitution plane
  (`contracts/`, `docs/adr/`, `golden/`).
```

---

### `harness/commands/refresh-memory.md` (NEW — macro over the full regen set)

**Analog:** `harness/commands/orient.md` (exact — already shells `repo_map` + `contracts_index` + `inject` as `!`-prefixed steps)

**Shape to copy** (`orient.md` lines 1-8 frontmatter + lines 17-29 shell steps):
```markdown
---
description: >-
  Use at the start of a session to get oriented — regenerates the derived memory plane ...
agent: orchestrator
subtask: true
---
# /orient — deterministic onboarding entry point
## 1. Regenerate the derived memory plane (never hand-edited)
!`uv run python -m tools.memory_regen.repo_map && uv run python -m tools.memory_regen.contracts_index`
## 2. Print the orientation payload (the injector's own output)
!`uv run python -m tools.memory_regen.inject`
```

**Delta to apply:**
- `agent: curator` (not orchestrator) — `check_command` (validate.py) requires `agent` to be a well-formed slug; `curator` qualifies once it exists.
- Superset of `/orient`: ALSO run `uv run python -m tools.docs_sync` (the committed-derived docs half `/orient` omits).
- Ordered steps per RESEARCH Pattern 3: (1) session-derived `repo_map` (gitignored, free), (2) committed-derived `docs_sync` + `contracts_index` (the gated set), (3) `inject` (refresh SessionStart payload).
- Description carries a `use` trigger token (command descriptions are routing signals too).
- Auto-discovered by `iter_commands` (`harness/commands/*.md`) — no registration edit beyond the emit re-run. `test_commands.py` uses a SUBSET check (per RESEARCH A2), so adding a command does NOT trip an exact-count gate.

---

### `tools/harness_lint/caps.py` (MOD — bump `EXPECTED_PERSONAS` 4→5)

**Analog:** its own frozenset (line 54).

**Current** (caps.py line 52-57):
```python
# Exactly the four enumerated CORE personas ...
EXPECTED_PERSONAS = frozenset({"orchestrator", "python-engineer", "code-reviewer", "explorer"})

# Personas that MUST be read-only in both representations (AGENT-04 reviewer, AGENT-05 explorer).
READ_ONLY_PERSONAS = frozenset({"code-reviewer", "explorer"})
```

**Delta to apply:**
- Add `"curator"` to `EXPECTED_PERSONAS` (→ 5 members). Update the comment ("four enumerated" → "five", noting curator = derived-freshness owner).
- Leave `READ_ONLY_PERSONAS` UNCHANGED — curator writes derived, so `is_read_only()` (line 84) correctly returns False for it.
- This single SSOT edit lands in BOTH the structural lints AND the `tools.harness_emit` emit-time validators (they import from caps.py). No second declaration to touch.

---

### `tools/harness_lint/tests/test_agents.py` (MOD — admit curator, assert its boundary)

**Analog:** its own `test_expected_personas_present_no_sprawl` (line 57) + `test_read_only_personas_have_no_write_affordance` (line 130).

**Delta to apply:**
- The sprawl test (line 60, `names == set(EXPECTED_PERSONAS)`) passes automatically once caps.py is bumped AND `curator.md` exists — no edit needed there, but confirm.
- `test_single_primary_carries_conductor_signal` (line 66): curator is `mode: subagent`, so `primaries` stays length-1 (orchestrator). Verify curator does NOT set `mode: primary`.
- ADD (Wave-0 gap per RESEARCH SC1): a new assertion that `"curator" not in READ_ONLY_PERSONAS` and that constitution paths are in `permission-matrix.json` `path_deny_globs`, plus (SC1/D-06) a body-scan test that `curator.md` + `refresh-memory.md` reference only `tools.memory_regen`/`tools.docs_sync` module paths (no inline derivation logic).

---

### `tools/docs_sync/generate.py` (MOD — add prune-then-write)

**Analog (write path):** its own `write()` (line 222-239).
**Analog (prune pattern):** `tools/harness_emit/manifest.prune_then_write` (referenced in-code; the emitter's prune-then-write precedent).

**Current write()** (lines 222-239):
```python
def write(contracts=CONTRACTS_DIR, out=REFERENCE_DIR) -> list[Path]:
    out_dir = _resolve(out)
    out_dir.mkdir(parents=True, exist_ok=True)
    written: list[Path] = []
    for name, schema in iter_schemas(contracts):
        target = _confine(out_dir / f"{name}.md", out_dir)
        target.write_text(render(name, schema), encoding="utf-8")
        written.append(target)
    return written
```

**Delta to apply (RESEARCH P2 / Pitfall):**
- After writing current pages, enumerate existing `<name>.md` under `out_dir`, delete any whose `<name>` is not in the current schema set. PRESERVE `README.md` (and any non-`<schema>.md` file) explicitly.
- Every delete target MUST pass `_confine(...)` (line 187) — reuse the existing confinement so a prune can never delete outside `docs/reference/` (V12 File/Path, ASVS).
- Keep it deterministic: sort the delete set; prune has no effect on written-page bytes (P6 — determinism preserved).
- Note the stale docstring: line 11 + line 21 still say "5 seed schemas → 5 pages" / "the five seed schemas" — reality is 2 (`greeting`, `format-conventions`). Update the docstring while here (non-load-bearing but drift).

---

### `tools/docs_sync/tests/test_docs_sync_determinism.py` (MOD — add prune test)

**Analog:** its own `test_seed_schemas_map_one_to_one_to_pages` (line 106) + `EXPECTED_PAGES` (line 26).

**Current** (lines 26-31, 106-110):
```python
EXPECTED_PAGES = frozenset({"format-conventions", "greeting"})
...
def test_seed_schemas_map_one_to_one_to_pages(tmp_path: Path) -> None:
    written = docs_sync.write(out=tmp_path / "reference")
    names = {p.stem for p in written}
    assert names == EXPECTED_PAGES
```

**Delta to apply (RESEARCH SC2/P2):**
- ADD a prune test: create a stray `docs/reference/x.md` with no backing schema in a `tmp_path` out dir → `write()` removes it; assert a co-located `README.md` is PRESERVED.
- `EXPECTED_PAGES` already `{format-conventions, greeting}` — correct for the current 2-schema tree; no change needed there.

---

### `.github/workflows/ci.yml` (MOD — NEW `stale-derived` job)

**Analog:** the `emit-drift` job (lines 179-189) — exact clone target.

**Current emit-drift** (lines 179-189):
```yaml
  emit-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: astral-sh/setup-uv@v8.3.2
      - name: Sync workspace (all packages)
        run: uv sync --all-packages
      - name: Re-emit the harness surface
        run: uv run python -m tools.harness_emit
      - name: Fail on any hand-edited generated-artifact drift
        run: git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json
```

**Delta to apply (D-07/D-08, Pitfall P1):**
- Clone into a `stale-derived` job: same checkout + setup-uv + `uv sync --all-packages`.
- Regen step: `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`.
- Diff step MUST use `git add -A -- docs/reference .memory/derived/contracts-index.md` then `git diff --cached --exit-code -- ...` — NOT bare `git diff` (emit-drift's outputs are all pre-tracked; the derived set can gain NEW untracked files like `greeting.md`, which bare `git diff` misses — P1).
- On failure, echo a copy-pasteable fix (`/refresh-memory` or the literal two-command regen + `git add ... && git commit`) — D-08.
- Add `stale-derived` to `gate.needs` (line 198): `needs: [setup, lang-tests, contract-check, drift, golden, core-suite, emit-drift, stale-derived]`.
- Keep top-level `permissions: { contents: read }` — the job needs no write scope; never interpolate `${{ github.event.* }}` into a `run:` (existing posture).

---

### `harness/commands/verify-work.md` (MOD — splice a freshness step)

**Analog:** its own steps 1-4 (lines 23-44), especially the presence-safe `/golden` step (line 44).

**Shape to copy** (numbered `!`-shell step, presence-safe/announced):
```markdown
## 4. Golden cases (`/golden`) — .NET-gated + presence-safe
!`shopt -s nullglob; cases=(golden/*/); if [ ${#cases[@]} -eq 0 ]; then echo 'SKIP...'; ... fi`
```

**Delta to apply (D-10 / RESEARCH Pattern 4):**
- Append `## 5. Derived freshness (mirror of the CI stale-derived gate)` after step 4.
- Body: `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index; git add -A -- docs/reference .memory/derived/contracts-index.md; git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md || { echo 'FAIL: derived plane stale — commit regenerated docs/reference + contracts-index'; exit 1; }`.
- Keep presence-safe/announced like step 4 so a bare template with no contracts still exits 0.
- Update the header/intro prose (line 21 "Run the four gates") → five.

---

### `.gitignore` (MOD — flip contracts-index to committed-derived)

**Analog:** its own `.memory/derived/` rule (line 19).

**Current** (lines 17-19):
```
# Two-plane memory: DERIVED plane is machine-regenerated every session (D-03/D-04) — never commit.
# STATE plane (.memory/state/**) and .memory/README.md stay TRACKED (small state survives sessions).
.memory/derived/
```

**Delta to apply (D-01/D-03, Pitfall P3):**
- Replace line 19 `.memory/derived/` with `.memory/derived/*` THEN `!.memory/derived/contracts-index.md`. The `/` (directory) form CANNOT be re-included from — git refuses re-inclusion under an excluded directory. The `/*` (contents) form + negation works.
- `repo-map.md` stays ignored via `*`; `contracts-index.md` becomes tracked. Keep `INDEX_PATH` unchanged at `.memory/derived/contracts-index.md` (confirmed: `contracts_index.py` line 30) so `inject.py` needs NO change (minimal blast radius, RESEARCH A1).
- Verify: `git check-ignore .memory/derived/repo-map.md` (still ignored) vs `.memory/derived/contracts-index.md` (NOT ignored, exit 1). Confirmed CURRENTLY ignored (exit 0) — the flip must change this.

---

### `tools/harness_emit/emit-manifest.json` (REGEN — not hand-edited)

**Analog:** the file itself (the emitter's prune-then-written path list).

**Delta to apply (D-11):** Do NOT hand-edit. After adding `curator.md` + `refresh-memory.md` and bumping caps.py, run `uv run python -m tools.harness_emit`. The manifest must then list all FOUR new emitted paths:
```
.opencode/agent/curator.md
.claude/agents/curator.md
.opencode/command/refresh-memory.md
.claude/commands/refresh-memory.md
```
Commit the regenerated manifest + both runtime trees. The `emit-drift` CI job will red if source and committed output diverge.

---

### `harness/skills/two-plane-memory/SKILL.md` (MOD — document the committed-derived sub-tier)

**Analog:** its own plane-semantics prose (not read this pass — plan-time targeted read recommended).

**Delta to apply (D-03 / RESEARCH Open Q1):** Add a "committed-derived (machine-write + CI-verify)" sub-tier so `contracts-index` moving from gitignored → tracked is documented ("why is one file under `.memory/derived/` tracked"). Keep the invariant: derived-never-hand-edited is satisfied by machine-write + gate, not by human edits.

---

### `docs/reference/` Wave-0 reconcile (TREE — pre-existing drift blocks a green gate)

**Analog:** `docs_sync.write()` output set == `EXPECTED_PAGES` = `{format-conventions, greeting}`.

**Current tracked state** (`git ls-files docs/reference/`):
```
README.md  correction-rules.md  equipment-master.md  equipment-progress.md  format-conventions.md  standard-log.md
```
Contracts present: `contracts/sample/greeting.schema.json`, `contracts/normalization/format-conventions.schema.json`.

**Delta to apply (RESEARCH P2, critical hazard):**
- DELETE (`git rm`) the 4 orphans left by the Phase-5 domain move: `correction-rules.md`, `equipment-master.md`, `equipment-progress.md`, `standard-log.md` (no backing schema).
- GENERATE + commit `greeting.md` (its schema exists but the page is missing — `format-conventions.md` is already present/tracked).
- After reconcile, `docs/reference/` = `README.md` + `format-conventions.md` + `greeting.md`. Only THEN will `stale-derived` be green on a fresh regen. Do this in Wave 0 BEFORE wiring the gate, or the gate reds immediately.

## Shared Patterns

### Constitution write-deny (curator's derived-only boundary)
**Source:** `harness/permission-matrix.json` `path_deny_globs` (`contracts/**`, `docs/adr/**`, `golden/**`, `*.env`) + the Phase-4 contract-guard hook.
**Apply to:** `curator.md` (and every persona — it is GLOBAL data).
**Why not a per-persona glob:** opencode's `edit` permission key is NOT path-globbable (matrix `_note`). Curator's "derived-only" boundary is a fact of the global deny data + the contract-guard hook, NOT a frontmatter path list. The `curator.md` prose is advisory; the matrix is enforcement. No matrix edit needed — the deny already covers curator.

### Regenerate-then-diff CI gate (fail-on-diff, actionable message)
**Source:** `.github/workflows/ci.yml` `emit-drift` (lines 179-189) + `drift` (lines 125-135).
**Apply to:** the new `stale-derived` job.
**Copy verbatim:** checkout@v7.0.0 → setup-uv@v8.3.2 → `uv sync --all-packages` → regen → diff-exit-code. The ONE deviation: `git add -A` + `git diff --cached --exit-code` (untracked-file-safe, P1), not bare `git diff`.

### `!`-shell macro command that shells to a `tools.*` module (no inline logic)
**Source:** `harness/commands/orient.md`, `harness/commands/docs-sync.md`, `harness/commands/verify-work.md`.
**Apply to:** `refresh-memory.md` + the `/verify-work` step 5.
**Invariant (D-06):** commands NEVER re-implement derivation — they only invoke `uv run python -m tools.{memory_regen.*,docs_sync}`. A second index/hash impl silently diverges from the drift gate (the reason `contracts_index.py` reuses `contract_hash`/`contract_drift`).

### Emitter round-trip, no model id (dual-runtime projection)
**Source:** `tools/harness_emit` (`iter_agents`/`iter_commands` auto-discover; `project_agent`/`project_command` split frontmatter per runtime) + `caps.py` SSOT.
**Apply to:** `curator.md` + `refresh-memory.md`.
**Invariant (D-11):** author once in `harness/`, `uv run python -m tools.harness_emit` projects to `.opencode/` + `.claude/`; NO real `model:` (only `provider/explorer-tier` placeholder); stay domain-neutral (GEN-04). Commit the regenerated trees + manifest.

### Path confinement on writes AND deletes
**Source:** `tools/docs_sync/generate.py::_confine` (line 187), mirror of `tools/golden_runner/runner.py::_confine`.
**Apply to:** the new `docs_sync` prune — every delete target must `_confine(... , out_dir)` before `unlink()`; preserve `README.md` explicitly.

## No Analog Found

None. Every new/modified file has a concrete in-tree analog. This phase adds zero net-new patterns —
the only genuinely new CODE is (a) the `docs_sync` prune-then-write (analog: emitter's
`prune_then_write`) and (b) YAML/markdown authoring cloned from the analogs above.

## Assumptions vs. Actual Tree — Flags for the Planner

| CONTEXT/RESEARCH assumption | Actual tree | Impact |
|-----------------------------|-------------|--------|
| `.gitignore` "lines 17-19" hold the `.memory/derived/` rule | Rule is on **line 19 only** (17 blank/prior, 18 comment, 19 `.memory/derived/`) | Cosmetic — planner edits line 19; the flip logic (P3) is unchanged. |
| RESEARCH: "`docs_sync` writes 2 pages + `greeting.md` untracked" | `format-conventions.md` IS present+tracked; **`greeting.md` does NOT exist at all** (must be generated). 4 orphans ARE tracked. | Wave-0 reconcile = `git rm` 4 orphans + generate & commit `greeting.md`. Matches RESEARCH intent; state is slightly further along (format-conventions already synced). |
| `generate.py` docstring: "5 seed schemas → 5 pages" | Only **2** schemas today (`greeting`, `format-conventions`); `EXPECTED_PAGES` test already = 2 | Stale docstring (lines 11, 21) — update opportunistically during the prune edit; not load-bearing. |
| `EXPECTED_PERSONAS` = 4 | Confirmed: `{orchestrator, python-engineer, code-reviewer, explorer}` (caps.py line 54) | Bump to 5 is correct. |
| Curator derives from `harness/agents/templates/` | Templates dir holds `component-engineer.md`, `engineer.md` (fill-in SCAFFOLDS, not emitted personas) | Curator is a CONCRETE emitted persona → belongs in `harness/agents/` (bumps EXPECTED_PERSONAS), NOT templates. RESEARCH agrees (Alternatives). |
| `contracts-index` INDEX_PATH | Confirmed `.memory/derived/contracts-index.md` (contracts_index.py line 30); currently `git check-ignore` exit 0 (ignored) | Keep path; the `.gitignore` negation is what flips it. `inject.py` untouched. |

## Metadata

**Analog search scope:** `harness/agents/`, `harness/agents/templates/`, `harness/commands/`, `tools/memory_regen/`, `tools/docs_sync/` (+ tests), `tools/harness_lint/` (+ tests), `tools/harness_emit/`, `.github/workflows/`, `docs/reference/`, `.gitignore`, `contracts/`.
**Files scanned:** ~18 read directly this session (all reuse targets read from the live tree).
**Pattern extraction date:** 2026-07-13
