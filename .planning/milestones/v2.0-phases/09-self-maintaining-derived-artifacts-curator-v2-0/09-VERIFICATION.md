---
phase: 09-self-maintaining-derived-artifacts-curator-v2-0
verified: 2026-07-13T16:04:48Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 09: Self-Maintaining Derived Artifacts — Curator Verification Report

**Phase Goal:** The derived plane stays fresh on its own — repo-map, contracts-index, docs `reference/`,
and `.memory/` are regenerated by machines and verified by CI, with a single `curator` agent owning
"derived freshness" and no human ever hand-editing a derived artifact. Reuses existing
`tools/memory_regen`, `/docs-sync`, and two-plane-memory machinery (no rebuild).

**Verified:** 2026-07-13T16:04:48Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Read-mostly `curator` agent exists, owns derived freshness, regenerates only by invoking existing tools, never hand-edits a derived file; NOT in READ_ONLY_PERSONAS; EXPECTED_PERSONAS bumped 4→5 | ✓ VERIFIED | `harness/agents/curator.md` exists with `mode: subagent`, `edit: allow`, `write: deny`, body states invocation-only regeneration via `tools.memory_regen.*`/`tools.docs_sync`, no model id. Emitted byte-consistent to `.opencode/agent/curator.md` + `.claude/agents/curator.md`. `tools/harness_lint/caps.py:57-59` — `EXPECTED_PERSONAS = frozenset({"orchestrator","python-engineer","code-reviewer","explorer","curator"})` (5 members). `READ_ONLY_PERSONAS = frozenset({"code-reviewer","explorer"})` (curator absent, confirmed False for `is_read_only()`) |
| 2 | CI `stale-derived` gate in ci.yml regenerates committed-derived set and FAILS on any diff via `git add -A` + `git diff --cached --exit-code` (not bare `git diff`); wired into `gate.needs` | ✓ VERIFIED | `.github/workflows/ci.yml:203-231` — job `stale-derived` regenerates via `tools.docs_sync` + `tools.memory_regen.contracts_index`, then `git add -A -- docs/reference .memory/derived/contracts-index.md` followed by `git diff --cached --exit-code -- ...`. `gate.needs` (line 240) includes `stale-derived`. Ran the exact gate logic locally on current tree: exits 0 (green) |
| 3 | Hook posture split by cost: no new heavy on-write memory hook (no memory_regen/docs_sync on any Pre/PostToolUse write path); test_derived_freshness.py enforces this | ✓ VERIFIED | `.claude/settings.json` PreToolUse/PostToolUse hook groups contain no `memory_regen`/`docs_sync` tokens (manually grepped, confirmed absent). `harness/plugins/session-inject.ts` invokes `tools.memory_regen.*` only from the `event`/`session.created` hook (session-open, not a write-path `tool.execute.before/after` hook) — pre-existing Phase-2/3 machinery, correctly out of scope per the test's own docstring. `tools/harness_lint/tests/test_derived_freshness.py` has 4 tests (invokes-only-tools ×2, no-on-write-regen ×2 for settings.json + plugins) — all pass |
| 4 | `/refresh-memory` exists (both runtimes) and `/verify-work` incorporates the freshness regen+diff step | ✓ VERIFIED | `harness/commands/refresh-memory.md` exists (`agent: curator`, 3 ordered steps: repo-map, docs_sync+contracts_index, inject), emitted to `.opencode/command/refresh-memory.md` + `.claude/commands/refresh-memory.md`. `harness/commands/verify-work.md` step 5 "Derived freshness (mirror of the CI stale-derived gate)" runs the identical regen + `git add -A` + `git diff --cached --exit-code` pattern; intro updated to "five gates" |
| 5 | curator + refresh-memory + verify-work round-trip the emitter to BOTH runtimes with no model id; emit is byte-identical (idempotent); core stays example-independent (GEN-04 green) | ✓ VERIFIED | `uv run python -m tools.harness_emit` re-run produced output then `git diff --exit-code -- .opencode .claude AGENTS.md CLAUDE.md opencode.json tools/harness_emit/emit-manifest.json` exits 0 (idempotent, no drift). Grep for real model identifiers (`claude-opus`, `claude-sonnet`, `gpt-`, `anthropic/claude`) across `.opencode/`, `.claude/`, `harness/` finds none. `tools/harness_lint/tests/test_core_no_example_dep.py` (GEN-04): 18 passed |

**Score:** 5/5 truths verified

### Additional Contract Checks (explicit user asks)

| Check | Expected | Status | Evidence |
|-------|----------|--------|----------|
| contracts-index committed-derived | `.memory/derived/contracts-index.md` tracked; `repo-map.md` still gitignored | ✓ VERIFIED | `git ls-files .memory/derived/` → `.memory/derived/contracts-index.md` only. `git check-ignore -v .memory/derived/repo-map.md` → matches `.gitignore:23:.memory/derived/*` (ignored, exit 0). `git check-ignore .memory/derived/contracts-index.md` → exit 1 (not ignored) |
| docs/reference reconciled | tracked set == {README.md, format-conventions.md, greeting.md} | ✓ VERIFIED | `git ls-files docs/reference/` → exactly `README.md`, `format-conventions.md`, `greeting.md`. The 4 Phase-5 orphan pages (correction-rules, equipment-master, equipment-progress, standard-log) are gone |
| stale-derived gate green on current tree | regen + `git add -A` + `git diff --cached --exit-code` exits 0 | ✓ VERIFIED | Ran the exact CI job commands locally: `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git add -A -- docs/reference .memory/derived/contracts-index.md && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md` → exit 0, "STALE-DERIVED GATE: GREEN" printed. `git status --porcelain` clean afterward (no accidental stage left behind) |
| Full test suite | `uv run pytest tools/ libs/python` green | ✓ VERIFIED | 514 passed, 4 snapshots passed, 0 failures. Also ran full `uv run pytest` (whole repo): 514 passed |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `harness/agents/curator.md` | read-mostly curator persona | ✓ VERIFIED | exists, substantive body, no model id, domain-neutral |
| `.opencode/agent/curator.md` | emitted opencode curator | ✓ VERIFIED | present, byte-consistent with source, idempotent re-emit |
| `.claude/agents/curator.md` | emitted Claude curator | ✓ VERIFIED | present, byte-consistent with source, idempotent re-emit |
| `tools/harness_lint/caps.py` | EXPECTED_PERSONAS bumped 4→5, READ_ONLY_PERSONAS unchanged | ✓ VERIFIED | grep confirms both |
| `.github/workflows/ci.yml` | `stale-derived` job + `gate.needs` entry | ✓ VERIFIED | job present lines 203-231, in `gate.needs` line 240 |
| `tools/harness_lint/tests/test_ci_stale_derived.py` | structural + negative-control tests | ✓ VERIFIED | exists, 8 tests pass |
| `tools/harness_lint/tests/test_derived_freshness.py` | body-scan + hook-posture tests | ✓ VERIFIED | exists, 4 tests pass |
| `harness/commands/refresh-memory.md` | macro over regen tools, agent: curator | ✓ VERIFIED | exists, invokes only `tools.memory_regen.*`/`tools.docs_sync` |
| `harness/commands/verify-work.md` | step 5 derived-freshness | ✓ VERIFIED | step 5 present with `git diff --cached --exit-code` |
| `.gitignore` | contents-form + negation for contracts-index | ✓ VERIFIED | `.memory/derived/*` + `!.memory/derived/contracts-index.md` present |
| `.memory/derived/contracts-index.md` | committed-derived, tracked | ✓ VERIFIED | tracked, regenerates byte-identical |
| `docs/reference/greeting.md` | new reference page | ✓ VERIFIED | tracked, present |
| `tools/harness_emit/emit-manifest.json` | lists 4 new emitted paths | ✓ VERIFIED | grep confirms all 4 paths present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `harness/agents/curator.md` | `tools.memory_regen` + `tools.docs_sync` | invocation-only regeneration | ✓ WIRED | body references only these modules; `test_curator_invokes_only_regen_tools` passes |
| `harness/commands/verify-work.md` step 5 | `docs/reference` + `.memory/derived/contracts-index.md` | regen + `git add -A` + `git diff --cached --exit-code` | ✓ WIRED | exact pattern present and matches CI gate |
| `tools/harness_emit` re-emit | `.opencode/` + `.claude/` + `emit-manifest.json` | `python -m tools.harness_emit` | ✓ WIRED | ran live; idempotent; manifest lists curator + refresh-memory paths |
| `.github/workflows/ci.yml stale-derived` | `docs/reference` + `.memory/derived/contracts-index.md` | regen → `git add -A` → `git diff --cached --exit-code` → fail-on-diff | ✓ WIRED | ran live locally, exits 0 (green) |
| `gate.needs` | `stale-derived` | fan-in dependency | ✓ WIRED | present in `jobs.gate.needs` list |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| stale-derived gate discriminates clean tree | `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git add -A -- docs/reference .memory/derived/contracts-index.md && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md` | exit 0, "STALE-DERIVED GATE: GREEN" | ✓ PASS |
| emitter idempotence | `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode .claude AGENTS.md CLAUDE.md opencode.json tools/harness_emit/emit-manifest.json` | exit 0, no diff | ✓ PASS |
| GEN-04 core independence | `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` | 18 passed | ✓ PASS |
| full derived-plane test suite | `uv run pytest tools/ libs/python -q` | 514 passed, 4 snapshots passed | ✓ PASS |
| gitignore split | `git check-ignore .memory/derived/repo-map.md` (ignored) vs `.memory/derived/contracts-index.md` (not ignored) | repo-map ignored (exit 0), contracts-index not ignored (exit 1) | ✓ PASS |

### Probe Execution

No dedicated `scripts/*/tests/probe-*.sh` probes declared for this phase; the CI gate itself was executed directly as the functional equivalent (see Behavioral Spot-Checks above).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MAINT-01 | 09-03 | curator agent, read-mostly, single owner of derived freshness, reuses tools/memory_regen + /docs-sync + two-plane-memory | ✓ SATISFIED | curator.md authored + emitted both runtimes, EXPECTED_PERSONAS bumped, boundary tests pass |
| MAINT-02 | 09-01, 09-02, 09-04 | CI stale-derived gate regenerates repo-map/contracts-index/docs-reference, fails on diff, decides committed vs gitignored derived tiers | ✓ SATISFIED | stale-derived job in ci.yml + gate.needs; docs/reference reconciled; contracts-index flipped committed-derived; gate verified green locally |
| MAINT-03 | 09-03 | hook posture — cheap format-on-write refresh, heavy regen deferred to PR/CI, no per-commit heavy hook | ✓ SATISFIED | no memory_regen/docs_sync on Pre/PostToolUse write path (verified directly + by test_derived_freshness.py) |
| MAINT-04 | 09-03 | /refresh-memory (or curator invocation) command for pre-handoff local regen; /verify-work includes freshness check | ✓ SATISFIED | refresh-memory.md + verify-work.md step 5 both present and correctly wired |

No orphaned requirements — all 4 MAINT IDs mapped in REQUIREMENTS.md to Phase 9 and claimed by a plan's `requirements:` frontmatter.

### Anti-Patterns Found

None. Scanned all phase-modified files (`tools/docs_sync/generate.py`, `.gitignore`, `harness/agents/curator.md`,
`harness/commands/refresh-memory.md`, `harness/commands/verify-work.md`, `tools/harness_lint/caps.py`,
`tools/harness_lint/tests/test_derived_freshness.py`, `.github/workflows/ci.yml`,
`tools/harness_lint/tests/test_ci_stale_derived.py`, `harness/skills/two-plane-memory/SKILL.md`) for
TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER/"not yet implemented"/"coming soon" markers. Only incidental matches were
the pre-existing `todowrite` tool-name token and the deliberately-named `PLACEHOLDER_MODEL` constant in
`caps.py` (not debt markers).

### Human Verification Required

None. All must-haves were verifiable via direct command execution (git plumbing, pytest, the actual CI gate
logic run locally) — no visual, real-time, or external-service behavior in scope for this phase.

### Gaps Summary

No gaps. All 5 roadmap success criteria plus the additional explicit contract checks (contracts-index
committed-derived split, docs/reference reconciliation, stale-derived gate green on current tree, full test
suite) were independently verified against the live codebase rather than trusted from SUMMARY.md claims. The
stale-derived CI job was executed with its exact command sequence locally and confirmed green; the emitter
was re-run and confirmed byte-identical; the full derived-plane and repo-wide test suites both pass (514/514).

---

_Verified: 2026-07-13T16:04:48Z_
_Verifier: Claude (gsd-verifier)_
