---
phase: 09-self-maintaining-derived-artifacts-curator-v2-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/docs_sync/generate.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - docs/reference/correction-rules.md
  - docs/reference/equipment-master.md
  - docs/reference/equipment-progress.md
  - docs/reference/standard-log.md
  - docs/reference/greeting.md
autonomous: true
requirements: [MAINT-02]

must_haves:
  truths:
    - "Regenerating docs/reference deletes pages whose backing schema no longer exists (orphans pruned)"
    - "docs/reference tracked set == {README.md, format-conventions.md, greeting.md} after reconcile"
    - "docs_sync prune never deletes outside docs/reference and always preserves README.md"
    - "Generating docs/reference twice is byte-identical (determinism preserved)"
  artifacts:
    - path: "tools/docs_sync/generate.py"
      provides: "prune-then-write: writes current pages, deletes orphaned <name>.md, preserves README.md"
      contains: "_confine"
    - path: "docs/reference/greeting.md"
      provides: "reference page for contracts/sample/greeting.schema.json (was missing)"
    - path: "tools/docs_sync/tests/test_docs_sync_determinism.py"
      provides: "prune test: stray page removed, README.md preserved"
      contains: "README.md"
  key_links:
    - from: "tools/docs_sync/generate.py::write"
      to: "docs/reference/*.md"
      via: "prune-then-write (enumerate pages, delete non-schema names, _confine each delete)"
      pattern: "_confine"
    - from: "prune delete target"
      to: "docs/reference/ (out_dir)"
      via: "_confine(target, out_dir) before unlink; README.md exempt"
      pattern: "unlink"
---

<objective>
Reconcile the pre-existing `docs/reference/` drift and give `tools/docs_sync` a prune-then-write step,
so the committed-derived docs half is clean and self-healing BEFORE the `stale-derived` gate lands.

Purpose: The stale-derived CI gate (Plan 04) regenerates `docs/reference/**` and fails on any diff. Today
the tracked tree carries four orphan domain pages left by the Phase-5 domain move (`correction-rules`,
`equipment-master`, `equipment-progress`, `standard-log`) and is MISSING `greeting.md`. A gate would red on
arrival. `docs_sync.write()` also never deletes orphaned pages. This plan fixes both (RESEARCH Pitfall P2).

Output: prune-capable `docs_sync`, a prune test, and a reconciled `docs/reference/` = {README, format-conventions, greeting}.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md

<interfaces>
<!-- From tools/docs_sync/generate.py — the write() path to extend (PATTERNS lines 132-142). -->
- `write(contracts=CONTRACTS_DIR, out=REFERENCE_DIR) -> list[Path]` — writes one page per schema via `iter_schemas`.
- `_confine(path, base)` (generate.py ~line 187) — refuses any path escaping `base`; reuse it for deletes.
- `render(name, schema)`, `iter_schemas(contracts)` — unchanged.
- Entrypoint: `python -m tools.docs_sync`.
<!-- From tests/test_docs_sync_determinism.py — EXPECTED_PAGES = frozenset({"format-conventions", "greeting"}) (line 26). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prune-then-write to docs_sync.write()</name>
  <files>tools/docs_sync/generate.py</files>
  <read_first>
    - tools/docs_sync/generate.py (the full file — `write()` ~lines 222-239, `_confine` ~line 187, the module docstring lines 1-25 with the stale "5 seed schemas → 5 pages" claim)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md (§ `tools/docs_sync/generate.py` (MOD — add prune-then-write) — the exact delta)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md (§ Common Pitfalls P2 + § Security Domain: prune must `_confine` deletes)
  </read_first>
  <behavior>
    - After writing all current pages, enumerate existing `*.md` under `out_dir`; delete each whose stem is not in the current schema-name set.
    - `README.md` (and any non-`<schema>.md` file) is PRESERVED — never deleted.
    - Every delete target passes `_confine(target, out_dir)` before `unlink()` (deletes confined to docs/reference, ASVS V12).
    - Delete set is sorted (deterministic); prune has zero effect on written-page bytes (P6 determinism holds).
  </behavior>
  <action>
    Extend `write()` in `tools/docs_sync/generate.py` with a prune step after the existing write loop: build
    `current = {name for name, _ in iter_schemas(contracts)}`, then for each `p` in `sorted(out_dir.glob("*.md"))`
    skip `p.name == "README.md"`, skip `p.stem in current`, else `_confine(p, out_dir)` and `p.unlink()`.
    Preserve `README.md` explicitly (exact-name exempt). Do NOT change `render`, `iter_schemas`, or the write-loop
    bytes. Fix the stale docstring (lines ~11 and ~21 say "5 seed schemas → 5 pages" / "the five seed schemas";
    reality is 2: `greeting`, `format-conventions`) to say "one page per current schema (prune-then-write:
    orphaned pages removed, README.md preserved)".
  </action>
  <verify>
    <automated>uv run pytest tools/docs_sync -x</automated>
  </verify>
  <acceptance_criteria>
    - `tools/docs_sync/generate.py` `write()` contains a prune loop calling `_confine(...)` then `.unlink()` on orphaned pages.
    - `README.md` is exempted by exact name (grep shows a `README.md` guard in the prune path).
    - Docstring no longer claims "5 seed schemas"; states prune-then-write behavior.
    - `uv run pytest tools/docs_sync -x` exits 0.
  </acceptance_criteria>
  <done>Running docs_sync on a dir containing a schema-less `x.md` removes `x.md` and keeps `README.md`; no delete escapes docs/reference.</done>
</task>

<task type="auto">
  <name>Task 2: Add prune test + reconcile the docs/reference tree</name>
  <files>tools/docs_sync/tests/test_docs_sync_determinism.py, docs/reference/correction-rules.md, docs/reference/equipment-master.md, docs/reference/equipment-progress.md, docs/reference/standard-log.md, docs/reference/greeting.md</files>
  <read_first>
    - tools/docs_sync/tests/test_docs_sync_determinism.py (EXPECTED_PAGES line 26; `test_seed_schemas_map_one_to_one_to_pages` line 106)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md (§ test_docs_sync_determinism.py delta + § docs/reference/ Wave-0 reconcile — the exact tracked-state list)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md (§ State of the Art — the 4 orphan pages are deprecated Phase-5 artifacts)
  </read_first>
  <action>
    (a) Add a prune test to `test_docs_sync_determinism.py`: in a `tmp_path/"reference"` out dir, drop a stray
    `x.md` (no backing schema) and a `README.md`, call `docs_sync.write(out=...)`, assert `x.md` is gone and
    `README.md` still exists. Keep `EXPECTED_PAGES = frozenset({"format-conventions", "greeting"})` — already correct.
    (b) Reconcile the live tree: run `uv run python -m tools.docs_sync` (prune now deletes the 4 orphans and writes
    the missing `greeting.md`); then `git rm docs/reference/{correction-rules,equipment-master,equipment-progress,standard-log}.md`
    and `git add docs/reference/greeting.md`. Final tracked set MUST be exactly `README.md`, `format-conventions.md`,
    `greeting.md`. Confirm `format-conventions.md` is unchanged (already synced).
  </action>
  <verify>
    <automated>uv run python -m tools.docs_sync && git status --porcelain -- docs/reference | grep -qv correction-rules && test -f docs/reference/greeting.md && uv run pytest tools/docs_sync -x</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files docs/reference/` returns exactly `README.md`, `format-conventions.md`, `greeting.md` (4 orphans gone, greeting added).
    - Second run of `uv run python -m tools.docs_sync` leaves `git status --porcelain -- docs/reference` EMPTY (byte-identical regen).
    - New prune test asserts stray page removed AND `README.md` preserved; `uv run pytest tools/docs_sync -x` exits 0.
  </acceptance_criteria>
  <done>docs/reference is regenerated-clean: a fresh `python -m tools.docs_sync` produces no diff, and the tree matches the current 2-schema contract set + README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → filesystem (docs/reference) | The new prune step DELETES files — the untrusted vector is a page path escaping the output dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01-01 | Tampering | `docs_sync.write` prune (delete path) | mitigate | Every delete target passes `_confine(target, out_dir)` before `unlink()`; `README.md` exempt by exact name — a delete cannot escape `docs/reference/` (ASVS V12) |
| T-9-01-02 | Denial of Service | prune deletes a still-valid page | mitigate | Delete only pages whose stem is absent from `iter_schemas(contracts)` current set; determinism test proves the surviving set == EXPECTED_PAGES |
| T-9-01-SC | Tampering | package installs | accept | N/A — no npm/pip/cargo install this plan (RESEARCH Package Legitimacy Audit: not applicable; all reuse targets in-lock uv members) |
</threat_model>

<verification>
- `uv run pytest tools/docs_sync -x` green (prune + determinism).
- `git ls-files docs/reference/` == {README.md, format-conventions.md, greeting.md}.
- `uv run python -m tools.docs_sync` twice → empty `git status --porcelain -- docs/reference`.
</verification>

<success_criteria>
docs/reference is reconciled to the current contract set, `docs_sync` prunes orphaned pages under confinement,
and a fresh regen produces zero diff — the precondition for a green `stale-derived` gate (Plan 04). (MAINT-02)
</success_criteria>

<output>
Create `.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-01-SUMMARY.md` when done
</output>
