---
phase: 09-self-maintaining-derived-artifacts-curator-v2-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .memory/derived/contracts-index.md
autonomous: true
requirements: [MAINT-02]

must_haves:
  truths:
    - "`.memory/derived/contracts-index.md` is TRACKED (committed-derived); `.memory/derived/repo-map.md` stays gitignored (D-01, D-02)"
    - "The generator's INDEX_PATH is unchanged, so inject.py needs no edit (minimal blast radius) (D-03)"
    - "Regenerating contracts-index twice is byte-identical (determinism preserved)"
  artifacts:
    - path: ".gitignore"
      provides: "contents-form ignore + negation so only contracts-index.md is re-included"
      contains: "!.memory/derived/contracts-index.md"
    - path: ".memory/derived/contracts-index.md"
      provides: "committed-derived contracts index (the flipped, gate-guarded artifact)"
  key_links:
    - from: ".gitignore rule .memory/derived/*"
      to: "!.memory/derived/contracts-index.md"
      via: "git contents-exclusion + single-file re-include (P3: cannot re-include under an excluded DIRECTORY)"
      pattern: "\\.memory/derived/\\*"
    - from: "tools.memory_regen.contracts_index INDEX_PATH"
      to: ".memory/derived/contracts-index.md"
      via: "unchanged path; inject.py reads the same location"
      pattern: "contracts-index"
---

<objective>
Flip `contracts-index` from gitignored-derived to committed-derived (D-01/D-03) so the `stale-derived`
gate (Plan 04) can guard it, keeping `repo-map` gitignored/session-ephemeral (D-02).

Purpose: Only the committed-derived set is diff-gated. `contracts-index` is cheap, deterministic, and a
genuine contract-drift signal — worth gating. The flip is a `.gitignore` change ONLY (keep the generator's
`INDEX_PATH` at `.memory/derived/contracts-index.md` so `inject.py` is untouched — RESEARCH A1, minimal blast radius).

Output: amended `.gitignore` (contents-form + negation) and the tracked `contracts-index.md`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md

<interfaces>
<!-- .gitignore line 19 today (PATTERNS §.gitignore): `.memory/derived/` (directory form). -->
<!-- tools/memory_regen/contracts_index.py line 30: INDEX_PATH = .memory/derived/contracts-index.md (UNCHANGED). -->
<!-- inject.py reads .memory/derived/contracts-index.md by that exact path — do NOT move it. -->
<!-- Entrypoint: python -m tools.memory_regen.contracts_index -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend .gitignore to re-include contracts-index.md</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (line 19: `.memory/derived/` — the directory-form rule to replace; note surrounding comment lines 17-18)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md (§ `.gitignore` (MOD) — the exact two-line replacement + the git re-include gotcha)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md (§ Common Pitfalls P3 — "cannot re-include a file under an excluded directory")
  </read_first>
  <action>
    Replace the directory-form line 19 `.memory/derived/` with the contents-form + negation pair, in order:
    `.memory/derived/*` on one line, then `!.memory/derived/contracts-index.md` on the next. The directory
    form (trailing `/`) blocks descent so a `!` negation under it does NOTHING (P3); the `/*` contents form lets
    the single-file negation take effect. Keep the two preceding comment lines. `repo-map.md` stays ignored via `*`.
    Update the comment to note contracts-index.md is committed-derived (machine-write + CI-verify).
  </action>
  <verify>
    <automated>git check-ignore .memory/derived/repo-map.md && ! git check-ignore .memory/derived/contracts-index.md</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore .memory/derived/repo-map.md` prints the path (still ignored, exit 0).
    - `git check-ignore .memory/derived/contracts-index.md` prints nothing and exits 1 (NOT ignored).
    - `.gitignore` contains both `.memory/derived/*` and `!.memory/derived/contracts-index.md` (directory-form line gone).
  </acceptance_criteria>
  <done>repo-map stays ignored; contracts-index is now trackable — the gitignore re-include gotcha (P3) is avoided.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and track the committed contracts-index.md</name>
  <files>.memory/derived/contracts-index.md</files>
  <read_first>
    - tools/memory_regen/contracts_index.py (INDEX_PATH line ~30; confirm it stays `.memory/derived/contracts-index.md`)
    - tools/memory_regen/inject.py (confirm it reads that exact path — must remain unchanged, RESEARCH A1)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md (§ Runtime State Inventory — `git rm --cached` NOT needed; the file was never tracked, just add + commit)
  </read_first>
  <action>
    Run `uv run python -m tools.memory_regen.contracts_index` to (re)generate `.memory/derived/contracts-index.md`,
    then `git add .memory/derived/contracts-index.md`. Do NOT edit `INDEX_PATH` and do NOT touch `inject.py`.
    Verify determinism: run the generator a second time and confirm `git status --porcelain -- .memory/derived/contracts-index.md`
    is empty after staging (byte-identical regen). No `git rm --cached` is needed — the file was never tracked.
  </action>
  <verify>
    <automated>uv run python -m tools.memory_regen.contracts_index && git add .memory/derived/contracts-index.md && uv run python -m tools.memory_regen.contracts_index && git diff --cached --exit-code -- .memory/derived/contracts-index.md</automated>
  </verify>
  <acceptance_criteria>
    - `.memory/derived/contracts-index.md` appears in `git ls-files .memory/derived/` (tracked).
    - Regenerating twice leaves no diff (`git diff --cached --exit-code -- .memory/derived/contracts-index.md` passes after re-run).
    - `tools/memory_regen/inject.py` is unmodified (git diff shows no change to inject.py).
  </acceptance_criteria>
  <done>contracts-index.md is committed-derived and deterministically regenerable; inject.py and INDEX_PATH untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gitignore policy → committed tree | Flipping a derived file to tracked could accidentally track unintended derived/secret content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02-01 | Information Disclosure | `.gitignore` negation scope | mitigate | Negation names EXACTLY `!.memory/derived/contracts-index.md`; `repo-map.md` + everything else under `.memory/derived/` stays ignored via `*`; `*.env` deny (permission-matrix path_deny_globs) unaffected |
| T-9-02-02 | Tampering | committed-derived contents | accept | contracts-index is machine-written from `contracts/**` via the reused deterministic generator; the Plan-04 gate re-verifies on every PR (derived-never-hand-edited satisfied by machine-write + CI-verify) |
| T-9-02-SC | Tampering | package installs | accept | N/A — no package installs this plan (RESEARCH audit not applicable) |
</threat_model>

<verification>
- `git check-ignore` split: repo-map ignored, contracts-index NOT ignored.
- `.memory/derived/contracts-index.md` tracked and byte-stable across regen.
- `inject.py` and `INDEX_PATH` unchanged.
</verification>

<success_criteria>
The committed-derived set gains `contracts-index` (tracked), `repo-map` stays session-ephemeral, and the
generator/inject path is untouched — the artifact is now in the exact scope the `stale-derived` gate guards. (MAINT-02)
</success_criteria>

<output>
Create `.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-02-SUMMARY.md` when done
</output>
