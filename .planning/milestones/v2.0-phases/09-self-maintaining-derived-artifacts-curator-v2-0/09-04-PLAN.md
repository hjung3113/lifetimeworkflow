---
phase: 09-self-maintaining-derived-artifacts-curator-v2-0
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - .github/workflows/ci.yml
  - tools/harness_lint/tests/test_ci_stale_derived.py
autonomous: true
requirements: [MAINT-02]

must_haves:
  truths:
    - "A `stale-derived` CI job regenerates the committed-derived set and FAILS on any diff, including new/untracked pages (D-07)"
    - "`stale-derived` is a non-bypassable member of `gate.needs` (fan-in gate covers it)"
    - "On failure the job prints a copy-pasteable fix command (the local /refresh-memory regen + commit) (D-08)"
    - "The gate cannot be tricked by a newly-created page (git add -A + git diff --cached, not bare git diff)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "stale-derived job (clone of emit-drift) + gate.needs entry"
      contains: "stale-derived"
    - path: "tools/harness_lint/tests/test_ci_stale_derived.py"
      provides: "structural + negative-control assertions for the gate"
      contains: "git diff --cached --exit-code"
  key_links:
    - from: ".github/workflows/ci.yml stale-derived job"
      to: "docs/reference + .memory/derived/contracts-index.md"
      via: "regen → git add -A → git diff --cached --exit-code → fail-on-diff"
      pattern: "git diff --cached --exit-code"
    - from: "gate.needs"
      to: "stale-derived"
      via: "fan-in dependency (non-bypassable)"
      pattern: "stale-derived"
---

<objective>
Add the `stale-derived` CI job — the machine half of "machines gate, humans ratify" for the derived plane
(MAINT-02 / D-07 / D-08). It regenerates the committed-derived set on a PR and fails on any diff, mirroring
the Phase-7 `emit-drift` job, with the untracked-file fix (P1).

Purpose: A stale derived plane cannot merge. This lands LAST (Wave 3) because it depends on a
regenerated-clean tree: Plan 01 reconciled docs/reference + added prune, Plan 02 tracked contracts-index,
Plan 03 created the `/refresh-memory` command the fix message points at. If this job landed before those, CI
would red on arrival.

Output: the `stale-derived` job wired into `gate.needs`, plus a structural + negative-control test for the gate.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md

<interfaces>
<!-- ci.yml emit-drift job (lines 179-189): checkout@v7.0.0 → setup-uv@v8.3.2 → `uv sync --all-packages` → re-emit → git diff --exit-code. -->
<!-- ci.yml gate.needs (line 198): needs: [setup, lang-tests, contract-check, drift, golden, core-suite, emit-drift] — append stale-derived. -->
<!-- Top-level permissions: { contents: read } (line 31) — the stale-derived job needs NO write scope. -->
<!-- Regen entrypoints: `uv run python -m tools.docs_sync` + `uv run python -m tools.memory_regen.contracts_index`. -->
<!-- ruamel.yaml IS available in the lock (via check-jsonschema) — use it to parse ci.yml in the test; do NOT add pyyaml. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the stale-derived job + wire it into gate.needs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (emit-drift job lines 179-189; gate.needs line 198; top-level permissions line 31)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md (§ ci.yml (MOD — NEW stale-derived job) — the exact clone + the git add -A / --cached deviation)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md (§ Pattern 2, § Common Pitfalls P1, § Security Domain — CI job posture)
  </read_first>
  <action>
    Add a `stale-derived` job cloning `emit-drift`: same `runs-on: ubuntu-latest`, `actions/checkout@v7.0.0`,
    `astral-sh/setup-uv@v8.3.2`, `uv sync --all-packages`. Regen step:
    `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`. Diff step MUST use
    `git add -A -- docs/reference .memory/derived/contracts-index.md` THEN
    `git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md` — NOT bare `git diff`
    (bare misses NEW untracked pages like a freshly-added reference page — P1). On failure, `echo` a
    copy-pasteable fix: run `/refresh-memory` (or the literal `uv run python -m tools.docs_sync && uv run python -m
    tools.memory_regen.contracts_index`), then `git add docs/reference .memory/derived/contracts-index.md && git
    commit`, then `exit 1` (D-08). Append `stale-derived` to `gate.needs` (line 198). Keep top-level
    `permissions: { contents: read }` — the job needs no write scope; do NOT interpolate any `${{ github.event.* }}`
    into a `run:` shell.
  </action>
  <verify>
    <automated>uv run python -c "from ruamel.yaml import YAML; y=YAML(typ='safe'); d=y.load(open('.github/workflows/ci.yml')); j=d['jobs']['stale-derived']; assert 'stale-derived' in d['jobs']['gate']['needs']; assert any('git diff --cached --exit-code' in str(s.get('run','')) for s in j['steps']); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` defines a `stale-derived` job whose diff step uses `git add -A` + `git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md`.
    - `stale-derived` is present in `jobs.gate.needs`.
    - On-failure step echoes a copy-pasteable fix command (contains `/refresh-memory` or the literal regen commands) then `exit 1`.
    - No `${{ github.event.` interpolation appears in the stale-derived `run:` steps; top-level `permissions: contents: read` intact.
  </acceptance_criteria>
  <done>The stale-derived gate regenerates the committed-derived set, fails on any diff (untracked-safe), is non-bypassable via gate.needs, and self-documents the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Structural + negative-control test for the gate</name>
  <files>tools/harness_lint/tests/test_ci_stale_derived.py</files>
  <read_first>
    - tools/harness_lint/tests/test_agents.py (test style + how frontmatter/YAML is parsed with ruamel.yaml in this suite)
    - tools/harness_lint/frontmatter.py (ruamel.yaml usage precedent — YAML(typ="safe"); NO pyyaml)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-VALIDATION.md (§ Per-Task Verification Map SC2 row — structural presence + negative-control mutate-a-page)
  </read_first>
  <action>
    Add `tools/harness_lint/tests/test_ci_stale_derived.py`. Structural test: parse `.github/workflows/ci.yml`
    with `ruamel.yaml` (`YAML(typ="safe")`), assert (a) `jobs["stale-derived"]` exists, (b) some step `run` contains
    both `git add -A` and `git diff --cached --exit-code` over `docs/reference` and `.memory/derived/contracts-index.md`,
    (c) `"stale-derived"` is in `jobs["gate"]["needs"]`, (d) no step `run` contains `github.event.`. Negative-control
    test (git plumbing, no network): in a `tmp_path` git repo, stage a file then modify it and assert
    `git diff --cached --exit-code` returns non-zero, and on a clean staged tree returns zero — proving the gate's
    diff primitive discriminates stale vs clean (documents the gate's core assertion locally). Use `subprocess` with
    `shell=False`.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_ci_stale_derived.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_ci_stale_derived.py` structural test asserts the job exists, uses `git add -A` + `git diff --cached --exit-code` over the committed-derived paths, and is in `gate.needs`.
    - Structural test asserts no `github.event.` interpolation in stale-derived run steps.
    - Negative-control test proves `git diff --cached --exit-code` fails on a mutated staged file and passes on a clean staged tree.
    - `uv run pytest tools/harness_lint/tests/test_ci_stale_derived.py -x` exits 0.
  </acceptance_criteria>
  <done>The gate's presence, correct diff primitive, non-bypassable wiring, and no-event-interpolation posture are automated; the stale-vs-clean discrimination is proven locally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR contributor → CI workflow | An attacker-authored PR could try to bypass the freshness gate or inject commands via event input |
| CI job → repo write | A job with excess scope could mutate the tree; the gate must stay read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | Tampering / Repudiation | stale-derived gate bypass | mitigate | Gate uses `git add -A` + `git diff --cached --exit-code` (untracked-safe, P1) so a new page can't slip through; `stale-derived` is in `gate.needs` (fan-in, non-bypassable); test asserts both |
| T-9-02-EoP | Elevation of Privilege | CI job scope / event injection | mitigate | Job inherits top-level `permissions: { contents: read }` (no write); NEVER interpolates `${{ github.event.* }}` into a `run:`; only fixed `uv run python -m tools.*` module strings — test asserts no `github.event.` |
| T-9-02-SC | Tampering | package installs | accept | N/A — no npm/pip/cargo install this plan; `uv sync --all-packages` resolves the existing locked workspace only |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_ci_stale_derived.py -x` green.
- ci.yml parses; `stale-derived` present, in `gate.needs`, uses `--cached` diff, no event interpolation.
- Manual (per VALIDATION § Manual-Only): a throwaway PR mutating a committed derived page reds the `stale-derived` job with the fix message.
</verification>

<success_criteria>
A non-bypassable `stale-derived` CI gate regenerates the committed-derived set and fails on any diff (untracked-safe),
prints a copy-pasteable fix, and is proven by a structural + negative-control test — completing MAINT-02.
</success_criteria>

<output>
Create `.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-04-SUMMARY.md` when done
</output>
