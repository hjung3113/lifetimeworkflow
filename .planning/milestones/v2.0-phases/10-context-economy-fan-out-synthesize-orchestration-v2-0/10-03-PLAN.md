---
phase: 10-context-economy-fan-out-synthesize-orchestration-v2-0
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - .opencode/skill/fan-out-synthesize/SKILL.md
  - .opencode/skill/fan-out-synthesize/references/fan-out-return.schema.json
  - .opencode/skill/context-budget/SKILL.md
  - .opencode/command/fan-out-synthesize.md
  - .opencode/agent/orchestrator.md
  - .opencode/command/orient.md
  - .claude/skills/fan-out-synthesize/SKILL.md
  - .claude/skills/fan-out-synthesize/references/fan-out-return.schema.json
  - .claude/skills/context-budget/SKILL.md
  - .claude/commands/fan-out-synthesize.md
  - .claude/agents/orchestrator.md
  - .claude/commands/orient.md
  - opencode.json
  - tools/harness_emit/emit-manifest.json
  - AGENTS.md
autonomous: true
requirements: [ECON-01, ECON-02, ECON-03]

must_haves:
  truths:
    - "Both new skills, the return-contract reference, and the /fan-out-synthesize command are emitted byte-identically to .opencode/** AND .claude/**"
    - "A re-emit produces zero git diff (emit-drift clean)"
    - "The full non-example suite is green; GEN-04 core→example guard is green; no model identifier in any emitted file"
    - "EXPECTED_PERSONAS stays 5; EXPECTED_SKILLS stays 11"
    - "Locked CONTEXT.md decisions covered here — D-12: every new skill/command round-trips the Phase-7 emitter to BOTH runtimes (opencode primary, Claude secondary), NO model identifier, GEN-04 core→example guard green, EXPECTED_SKILLS enumerated (11)"
  artifacts:
    - path: ".opencode/skill/fan-out-synthesize/references/fan-out-return.schema.json"
      provides: "opencode-runtime copy of the return contract (byte-identical to source)"
    - path: ".claude/skills/context-budget/SKILL.md"
      provides: "Claude-runtime copy of the context-budget skill"
    - path: "tools/harness_emit/emit-manifest.json"
      provides: "Regenerated owned-path set covering the new surface"
  key_links:
    - from: "harness/skills/**"
      to: ".opencode/** + .claude/**"
      via: "tools.harness_emit projection (glob-driven, byte-deterministic)"
      pattern: "fan-out-synthesize"
    - from: "AGENTS.md managed block"
      to: "harness agent/command/skill index"
      via: "emitter Regime-B splice (auto, do not hand-edit)"
      pattern: "fan-out-synthesize"
---

<objective>
Round-trip all Phase-10 authored surface through the Phase-7 emitter (D-12): project the two new
skills (with the return-contract reference), the new command, and the edited orchestrator/orient
surface into BOTH runtime trees (`.opencode/**` primary, `.claude/**` secondary), regenerate
`opencode.json` + `emit-manifest.json` + the root `AGENTS.md` managed block, commit the derived
trees, and prove the whole phase is green: emit-drift clean, full non-example suite green, GEN-04
core→example guard green, no model identifier anywhere, anti-sprawl counts held (11 skills / 5
personas).

Purpose: The `.opencode/`/`.claude/` trees are DERIVED — the deployed runtimes read them, and the
emit-drift CI gate fails on any hand-edit or stale tree. This plan is the mandatory single-source →
dual-runtime delivery + the phase-completion gate.
Output: regenerated + committed derived trees and a fully green gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-RESEARCH.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-PATTERNS.md

<interfaces>
<!-- The emit pipeline + gates. Read directly — no exploration needed. -->
- Emit entry point: `uv run python -m tools.harness_emit` (glob-driven iter_skills / iter_commands /
  iter_reference_files; new files auto-discovered, no emitter code change needed).
- Live layout to mirror: .opencode/skill/golden-debug/{SKILL.md,references/} and
  .claude/skills/golden-debug/{SKILL.md,references/} — the new skills land at the same shape.
- Loud-fail validators (emit writes nothing if any fire): validate.check_skill_set (skill-set
  drift), placeholder-model regex (real model ID), opencode.json schema check.
- GEN-04 guard: tools/harness_lint/tests/test_core_no_example_dep.py (scans tracked core planes for
  `examples/`, moved-asset proper nouns, domain vocab).
- emit-drift: re-emit then `git diff --exit-code` over the manifest path set (CI job; run it locally
  as the wave gate per 10-VALIDATION sampling).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-emit both runtimes and commit the regenerated derived trees</name>
  <files>.opencode/skill/fan-out-synthesize/**, .opencode/skill/context-budget/**, .opencode/command/fan-out-synthesize.md, .opencode/agent/orchestrator.md, .opencode/command/orient.md, .claude/skills/fan-out-synthesize/**, .claude/skills/context-budget/**, .claude/commands/fan-out-synthesize.md, .claude/agents/orchestrator.md, .claude/commands/orient.md, opencode.json, tools/harness_emit/emit-manifest.json, AGENTS.md</files>
  <read_first>
    - .planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-RESEARCH.md Pitfall 1 (forgetting to re-emit + commit) and the "files to author/modify" tail listing what to commit
    - .planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-PATTERNS.md "Emitter round-trip (D-12)" shared-pattern section
    - tools/harness_emit/emit-manifest.json (the current owned-path set — regenerated, not hand-edited)
    - harness/skills/fan-out-synthesize/SKILL.md, harness/skills/context-budget/SKILL.md (the source being projected — from plans 10-01/10-02)
  </read_first>
  <action>
    Run `uv run python -m tools.harness_emit` to project the harness source into both runtime trees.
    Do NOT hand-edit any file under `.opencode/`/`.claude/` — they carry the DERIVED marker on line 2
    and are emitter-owned. Then stage and commit ALL regenerated outputs: the new
    `.opencode/skill/{fan-out-synthesize,context-budget}/**` (including
    `fan-out-synthesize/references/fan-out-return.schema.json`), `.opencode/command/fan-out-synthesize.md`,
    the re-emitted `.opencode/agent/orchestrator.md` + `.opencode/command/orient.md`, the `.claude/`
    twins of all of the above, the regenerated `opencode.json`, `tools/harness_emit/emit-manifest.json`,
    and the auto-spliced root `AGENTS.md` managed block (agent/command/skill index). Use the project
    commit convention (no model identifier in the message or any artifact).
  </action>
  <verify>
    <automated>uv run python -m tools.harness_emit && git status --porcelain && git diff --exit-code</automated>
  </verify>
  <acceptance_criteria>
    - After a fresh `uv run python -m tools.harness_emit`, `git diff --exit-code` exits 0 (re-emit is byte-stable / nothing uncommitted)
    - `.opencode/skill/fan-out-synthesize/references/fan-out-return.schema.json` exists AND is byte-identical to `harness/skills/fan-out-synthesize/references/fan-out-return.schema.json`
    - `.claude/skills/context-budget/SKILL.md` exists
    - `.opencode/command/fan-out-synthesize.md` and `.claude/commands/fan-out-synthesize.md` both exist
    - The root AGENTS.md managed block lists `fan-out-synthesize` and `context-budget`
  </acceptance_criteria>
  <done>All Phase-10 surface emitted byte-identically to both runtimes and committed; re-emit yields zero diff.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suite + GEN-04 + emit-drift + anti-sprawl green</name>
  <files>(verification only — no new files)</files>
  <read_first>
    - tools/harness_lint/tests/test_core_no_example_dep.py (the GEN-04 core→example guard)
    - tools/harness_lint/tests/test_skills.py, tools/harness_lint/tests/test_agents.py (anti-sprawl counts)
    - .planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-VALIDATION.md (phase-gate sampling + sign-off)
  </read_first>
  <action>
    Run the full phase gate and confirm green: the full non-example suite, the GEN-04 core→example
    guard, the emit-drift re-emit-diff, and the anti-sprawl counts (11 skills, 5 personas). If any gate
    is red, fix the source in `harness/` (never hand-edit a derived tree), re-run `tools.harness_emit`,
    re-commit, and re-run the gate until clean. Confirm no emitted file pins a real model identifier
    (the placeholder-model validator loud-fails at emit; verify no failure occurred).
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q && uv run python -m tools.harness_emit && git diff --exit-code</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0 (full non-example suite green)
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0 (GEN-04 green)
    - `uv run python -m tools.harness_emit && git diff --exit-code` exits 0 (emit-drift clean)
    - `uv run pytest tools/harness_lint/tests/test_agents.py -q` confirms EXPECTED_PERSONAS == 5; test_skills confirms EXPECTED_SKILLS == 11
  </acceptance_criteria>
  <done>Full suite + GEN-04 + emit-drift + anti-sprawl all green; phase deliverable proven end-to-end and ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/ source → derived runtime trees | The emitter is the ONLY writer of `.opencode/`/`.claude/`; any other writer is tampering. |
| core planes → examples/ | GEN-04 forbids core (tools/harness/libs) referencing an example instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | A hand-edited derived tree diverging from the harness source | mitigate | Only `tools.harness_emit` writes the trees; the emit-drift gate (re-emit + `git diff --exit-code`) fails on any hand-edit — asserted in both task verifies. |
| T-10-03 | Information Disclosure | A real model ID reaching an emitted file | mitigate | Placeholder-model regex validator loud-fails at emit (writes nothing); Task 2 confirms no failure. |
| T-10-07 | Tampering | A domain/`examples/` token leaking into the emitted core surface (GEN-04 break) | mitigate | test_core_no_example_dep.py run explicitly in Task 2; all Phase-10 source authored domain-neutral. |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (authored surface + Python tests only). |
</threat_model>

<verification>
- `uv run python -m tools.harness_emit && git diff --exit-code` clean (emit-drift).
- `uv run pytest -q` full non-example suite green.
- GEN-04 guard green; EXPECTED_SKILLS == 11; EXPECTED_PERSONAS == 5; no model identifier emitted.
</verification>

<success_criteria>
- Both new skills + return-contract reference + command emitted byte-identically to opencode AND Claude trees.
- Re-emit yields zero diff; full suite + GEN-04 + anti-sprawl all green.
- Phase deliverable proven end-to-end (ECON-01/02/03 delivered to both runtimes, D-12 satisfied).
</success_criteria>

<output>
Create `.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-03-SUMMARY.md` when done
</output>
