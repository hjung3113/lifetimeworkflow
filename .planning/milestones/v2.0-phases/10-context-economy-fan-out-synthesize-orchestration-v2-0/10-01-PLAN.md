---
phase: 10-context-economy-fan-out-synthesize-orchestration-v2-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/skills/fan-out-synthesize/SKILL.md
  - harness/skills/fan-out-synthesize/references/fan-out-return.schema.json
  - harness/commands/fan-out-synthesize.md
  - tools/harness_lint/caps.py
  - tools/harness_lint/tests/test_fan_out_return_contract.py
autonomous: true
requirements: [ECON-01, ECON-02]

must_haves:
  truths:
    - "A fan-out-synthesize skill documents decompose → dispatch N explorer subtasks → recover schema-bounded summaries → orchestrator synthesizes, without re-reading raw files"
    - "A domain-neutral return-contract JSON Schema is co-located under the skill's references/ (not under contracts/)"
    - "A thin /fan-out-synthesize command routes to agent: orchestrator with subtask: true"
    - "EXPECTED_SKILLS contains fan-out-synthesize (skill-set equality holds)"
    - "Locked CONTEXT.md decisions covered here — D-01: ship both fan-out-synthesize skill AND /fan-out-synthesize command; D-02: named first-class workflow (not scattered prose); D-03: dispatch via the orchestrator's existing Task/subtask affordance, NO bespoke engine; D-04: deep-research/Workflow are shape-only, not runtime dependencies; D-05: workers reuse read-only explorer (EXPECTED_PERSONAS stays 5); D-06: orchestrator synthesizes the recovered summaries; D-07: schema-bounded citation-bearing return contract; D-08: schema under skill references/, NOT contracts/; D-09: ephemeral return, conformance validator intentionally omitted"
  artifacts:
    - path: "harness/skills/fan-out-synthesize/SKILL.md"
      provides: "The decompose→dispatch→recover→synthesize procedure (ECON-01)"
      contains: "name: fan-out-synthesize"
    - path: "harness/skills/fan-out-synthesize/references/fan-out-return.schema.json"
      provides: "Citation-bearing return contract (ECON-02)"
      contains: "json-schema.org/draft/2020-12/schema"
    - path: "harness/commands/fan-out-synthesize.md"
      provides: "Thin entry point usable by human AND conductor (ECON-01)"
      contains: "agent: orchestrator"
    - path: "tools/harness_lint/tests/test_fan_out_return_contract.py"
      provides: "Structural gate for the return contract (ECON-02)"
  key_links:
    - from: "harness/commands/fan-out-synthesize.md"
      to: "harness/skills/fan-out-synthesize/SKILL.md"
      via: "command body points at the skill as the procedure"
      pattern: "fan-out-synthesize"
    - from: "tools/harness_lint/caps.py"
      to: "harness/skills/fan-out-synthesize/"
      via: "EXPECTED_SKILLS enumeration set-equality"
      pattern: "fan-out-synthesize"
---

<objective>
Author the ECON-01/ECON-02 core surface: the `fan-out-synthesize` skill (the named
decompose → dispatch N read-only `explorer` subagents → recover schema-bounded citation-bearing
summaries → orchestrator synthesizes procedure), its co-located domain-neutral return-contract
JSON Schema, a thin `/fan-out-synthesize` command entry point, the anti-sprawl enumeration entry,
and the Wave-0 structural test for the return contract.

Purpose: This is the reusable context-economy substrate — a conductor (or human) covers a large
surface without any single context ballooning, because workers return compact paths+claims, not
raw file dumps. It is the substrate Phase 11 (γ, multi-repo) generalizes.
Output: 4 authored/new files + a 1-line `caps.py` enumeration edit, all domain-neutral.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-CONTEXT.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-RESEARCH.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-PATTERNS.md

<interfaces>
<!-- Analog surface the executor copies. Read these directly — no exploration needed. -->
- Skill shape + references/ tail: harness/skills/golden-debug/SKILL.md (folded `>-` description
  opening "Use when …"; numbered procedure; `## Deeper reference` tail pointing into references/).
- references/ byte-copy convention: harness/skills/golden-debug/references/canonicalization-axes.md
  (any regular file under references/ is emitted byte-for-byte to both runtimes).
- Thin prose command: harness/commands/review.md (frontmatter `agent:` + `subtask: true`,
  prose-only body handing off to a persona).
- Anti-sprawl enumeration: tools/harness_lint/caps.py EXPECTED_SKILLS frozenset (currently 9
  members) — set-equality is asserted by test_skills.py and enforced at emit by
  validate.check_skill_set.
- Worker persona (reused verbatim, NOT edited): harness/agents/explorer.md — read-only
  (Read/Grep/Glob, edit: deny) and already "returns concrete file paths and line references".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author fan-out-synthesize skill + return-contract schema + enumerate it</name>
  <files>harness/skills/fan-out-synthesize/SKILL.md, harness/skills/fan-out-synthesize/references/fan-out-return.schema.json, tools/harness_lint/caps.py</files>
  <read_first>
    - harness/skills/golden-debug/SKILL.md (frontmatter + numbered-procedure + `## Deeper reference` tail to copy)
    - harness/skills/golden-debug/references/canonicalization-axes.md (references/ file convention)
    - harness/agents/explorer.md (the read-only worker the skill dispatches — cite it by path in the tail)
    - harness/agents/orchestrator.md (the synthesizer — cite it by path in the tail)
    - tools/harness_lint/caps.py lines 122-139 (EXPECTED_SKILLS frozenset + the stale comment above it)
    - .planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-RESEARCH.md Pattern 1 (recommended schema shape) and 10-PATTERNS.md fan-out-synthesize sections
  </read_first>
  <behavior>
    - test_skills.py::test_expected_skills_present_no_sprawl passes: set of skill dir names == EXPECTED_SKILLS (now includes fan-out-synthesize).
    - test_skills.py description/name caps pass for the new SKILL.md (name == dir, matches ^[a-z0-9]+(-[a-z0-9]+)*$, ≤64; description ≤1024, carries a "use"/"when" trigger, no `anthropic`/`claude`, no `<`/`>`, disjoint from all other skills).
    - The schema parses as JSON and declares Draft 2020-12.
  </behavior>
  <action>
    Create `harness/skills/fan-out-synthesize/SKILL.md` with YAML frontmatter `name: fan-out-synthesize`
    and a folded `description: >-` opening "Use when a task spans a large surface that would balloon a
    single context —" (mirror golden-debug's frontmatter, disjoint from the existing 9 skills). Body:
    a "why context economy matters" opener, then four numbered steps — 1. DECOMPOSE the task into N
    independent analysis units; 2. DISPATCH via the runtime's native `task`/`Task` affordance (state
    explicitly: NO bespoke dispatch engine, per D-03) to read-only `explorer` workers; 3. RECOVER each
    worker's schema-bounded citation-bearing return (paths + claims, never raw file dumps); 4.
    SYNTHESIZE — the orchestrator combines the compact returns and never re-reads the raw files (D-06).
    Add a `## Deeper reference` tail pointing at `references/fan-out-return.schema.json` and at
    `harness/agents/explorer.md` + `harness/agents/orchestrator.md`.
    Create `harness/skills/fan-out-synthesize/references/fan-out-return.schema.json` — JSON Schema Draft
    2020-12, self-contained `$id` (NO `$ref` into `contracts/**`), `type: object`,
    `required: ["unit","claims"]`, `additionalProperties: false`; `claims` is an array of objects
    `{claim, confidence?, citations}` where `claim`'s description says "a single terse assertion, NOT a
    file excerpt", `citations` is a non-empty array of `{path, lines?, symbol?}` with
    `additionalProperties: false`; include optional `status` enum and `open_questions`. Keep it strictly
    domain-neutral (no `examples/` token, no domain vocab, no model ID).
    Edit `tools/harness_lint/caps.py`: add `"fan-out-synthesize"` to the EXPECTED_SKILLS frozenset
    (9 → 10 here; the sibling plan adds the 11th). Do NOT touch EXPECTED_PERSONAS.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_skills.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_skills.py -q` exits 0
    - `python -c "import json; json.load(open('harness/skills/fan-out-synthesize/references/fan-out-return.schema.json'))"` exits 0
    - caps.py EXPECTED_SKILLS contains the literal "fan-out-synthesize"
    - The schema file contains `additionalProperties` set to false and no `$ref` substring targeting `contracts/`
  </acceptance_criteria>
  <done>fan-out-synthesize skill + return schema exist, domain-neutral, enumerated in EXPECTED_SKILLS; test_skills green.</done>
</task>

<task type="auto">
  <name>Task 2: Author /fan-out-synthesize command + return-contract Wave-0 test</name>
  <files>harness/commands/fan-out-synthesize.md, tools/harness_lint/tests/test_fan_out_return_contract.py</files>
  <read_first>
    - harness/commands/review.md (frontmatter `agent:` + `subtask: true` + prose-only body to mirror)
    - harness/commands/refresh-memory.md (second command frontmatter precedent)
    - tools/harness_lint/tests/test_orchestrator_topology.py (the `_REPO_ROOT = parents[3]` + read-file idiom)
    - tools/harness_lint/tests/test_core_no_example_dep.py (the domain-neutrality scan idiom to mirror)
    - harness/skills/fan-out-synthesize/references/fan-out-return.schema.json (the file under test — authored in Task 1)
  </read_first>
  <action>
    Create `harness/commands/fan-out-synthesize.md` with frontmatter mirroring `/review`: a folded
    `description: >-` opening "Use when …" (routing trigger, disjoint from other commands), `agent:
    orchestrator`, `subtask: true`. Body is prose-only (NO `!`shell`` block — dispatch is the runtime's
    native `task` affordance): an H1 `# /fan-out-synthesize — decompose → dispatch N → recover →
    synthesize`, then prose that (a) points at the `fan-out-synthesize` skill as the procedure, (b)
    points at `references/fan-out-return.schema.json` as the return contract, (c) hands the workflow to
    the `orchestrator` (dispatches `explorer` workers, synthesizes). Keep domain-neutral.
    Create `tools/harness_lint/tests/test_fan_out_return_contract.py` using
    `_REPO_ROOT = Path(__file__).resolve().parents[3]`: assert the schema file exists; `json.loads`
    parses it; `$schema` == the Draft 2020-12 URL; `additionalProperties` is false; the required list
    includes `unit` and `claims`; no `$ref` targets `contracts/`; the raw text carries no `examples/`
    token (domain-neutral, keeps GEN-04 green).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_fan_out_return_contract.py tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_agent_referential_integrity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_fan_out_return_contract.py -q` exits 0
    - `uv run pytest tools/harness_lint/tests/test_commands.py -q` exits 0 (new command frontmatter is a valid routing signal)
    - `uv run pytest tools/harness_lint/tests/test_agent_referential_integrity.py -q` exits 0 (`agent: orchestrator` resolves to a real persona)
    - harness/commands/fan-out-synthesize.md contains `agent: orchestrator` and `subtask: true` and contains no line beginning with a backtick-shell marker
  </acceptance_criteria>
  <done>/fan-out-synthesize command exists routing to orchestrator; return-contract structural test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fan-out worker → conductor | An `explorer` subtask returns free-form text the orchestrator ingests and synthesizes. |
| harness/ source → derived runtime trees | Authored files are byte-emitted to `.opencode/`/`.claude/` (emit handled in plan 10-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | Return contract smuggling raw file contents (defeats the context economy) | mitigate | Schema `additionalProperties: false`; `claim` documented as "a single terse assertion, NOT a file excerpt"; `citations` carry path+lines only. Bounded shape asserted by test_fan_out_return_contract.py. |
| T-10-02 | Elevation of Privilege | A fan-out worker gaining write/shell | mitigate | Workers reuse the read-only `explorer` persona (edit: deny, tools = Read/Grep/Glob); no new persona, no frontmatter change (D-05). `is_read_only` keeps it write-denied in both runtimes. |
| T-10-03 | Information Disclosure | A real model ID leaking into the new surface | mitigate | Keep all new files model-agnostic; the placeholder-model validator loud-fails at emit (plan 10-03). |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint -q` green after both tasks.
- The new schema is valid JSON, Draft 2020-12, `additionalProperties: false`, no `contracts/` `$ref`.
- No `examples/` token or domain vocab in any new file (GEN-04 stays green; fully verified in 10-03).
</verification>

<success_criteria>
- fan-out-synthesize skill + return-contract schema + /fan-out-synthesize command authored, domain-neutral.
- EXPECTED_SKILLS includes fan-out-synthesize; test_skills set-equality holds.
- Return-contract Wave-0 structural test present and green.
</success_criteria>

<output>
Create `.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-01-SUMMARY.md` when done
</output>
