---
phase: 11-multi-repo-workspace-v2-0
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - tools/contract_drift/drift.py
  - tools/contract_drift/tests/test_workspace_drift.py
  - tools/golden_runner/runner.py
  - tools/golden_runner/tests/test_workspace_golden.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [MREPO-03]
must_haves:
  truths:
    - "Cross-repo drift iterates each declared member's own manifest via run_gate (verbatim reuse, no merged manifest) AND resolves each edge's contract in its producer member — failing loud on a producer-side drift or an unresolved edge contract"
    - "The golden runner resolves a golden case under a declared member root and its _confine allowlist is widened (threaded param) to include member roots — never removing the path-escape guard"
    - "A separate workspace CI job iterates members + edges and is registered in gate.needs (mirrors emit-drift / stale-derived separate-job idiom), not folded into the per-repo drift/golden jobs"
  artifacts:
    - path: "tools/contract_drift/drift.py"
      provides: "workspace_drift() cross-repo gate + --workspace CLI flag"
      contains: "def workspace_drift"
    - path: "tools/golden_runner/runner.py"
      provides: "_confine allowed_roots widening (threaded) + workspace-aware golden resolution"
      contains: "allowed_roots"
    - path: ".github/workflows/ci.yml"
      provides: "separate `workspace` job + gate.needs entry"
      contains: "workspace:"
  key_links:
    - from: "tools/contract_drift/drift.py"
      to: "tools.workspace_config"
      via: "members()/edges() resolve member roots at runtime"
      pattern: "from tools.workspace_config import|workspace_config"
    - from: ".github/workflows/ci.yml"
      to: "tools.contract_drift.drift --workspace"
      via: "workspace CI job step"
      pattern: "--workspace"
---

<objective>
Extend the contract-first safety net across repo boundaries (MREPO-03): cross-repo contract-drift
(reuse `run_gate` per member + a new edge-resolution check) and workspace-aware golden path resolution
(widen `_confine`'s allowlist to declared member roots, threaded — never remove the guard), plus a
separate workspace-level CI job registered in `gate.needs`. Every reused function is already
parametrized — pass member-scoped values into the existing signatures; do not re-parametrize.

Purpose: Contract-first and golden equivalence must hold when an edge spans a repo boundary; machines
gate, humans ratify (constitution plane stays human-owned per repo).
Output: extended `tools/contract_drift/drift.py` + `tools/golden_runner/runner.py`, two new cross-repo
test modules, and a `workspace` job in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md
@.planning/phases/11-multi-repo-workspace-v2-0/11-PATTERNS.md

<interfaces>
<!-- Already-parametrized functions to REUSE (do not add new signatures). Executor calls these. -->

tools/contract_drift/drift.py:
  run_gate(contracts_dir=CONTRACTS_DIR, baseline_path=MANIFEST_PATH) -> {"ok": bool, "drifted": [(rel, kind, cls), ...]}
  build_manifest(contracts_dir)  # keys relative to contracts_dir.parent — DO NOT merge across members (Pitfall 2)
  main(argv): argparse --contracts-dir / --baseline (already cross-tree; CI drift job proves it)

tools/golden_runner/runner.py:
  _confine(path) -> Path                         # L88-102: allowed_roots = (REPO_ROOT, /tmp, $TMPDIR); raises GoldenRunnerError on escape
  run_golden_case(case, out_path, ..., converter="dotnet", golden_dir=None) -> GoldenResult   # golden_dir overrides case root; converter="identity" = no .NET
  case_dir/seed_path/verified_path(case, golden_dir=None)

tools/workspace_config/loader.py (from 11-01):
  load_workspace() -> dict; members(cfg=None) -> list[{"id","root"}]; edges(cfg=None) -> list[{"from","to","contract"}]; split_endpoint(ep) -> (member|None, stage)

.github/workflows/ci.yml — separate-job idiom to mirror:
  drift job (L121-135): checkout@v7.0.0 → setup-uv@v8.3.2 → `uv sync --all-packages` → run CLI
  gate.needs (L240): [setup, lang-tests, contract-check, drift, golden, core-suite, emit-drift, stale-derived] — ADD `workspace`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-repo contract-drift — workspace_drift() + --workspace CLI + test</name>
  <files>tools/contract_drift/drift.py, tools/contract_drift/tests/test_workspace_drift.py</files>
  <read_first>
    - tools/contract_drift/drift.py (run_gate L134-163 reuse verbatim per member; main() argparse; _git_show shell=False discipline L115-131)
    - tools/contract_hash/hash.py (build_manifest key-base = contracts_dir.parent — why NOT to merge, Pitfall 2)
    - tools/workspace_config/loader.py (members/edges/split_endpoint to resolve member roots + producer)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md (Code Examples §Cross-repo drift; Pitfall 2 no-merge)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-PATTERNS.md (drift.py section — reuse-as-is + the one new edge-resolution check)
  </read_first>
  <action>
    Add `workspace_drift(ws_path=None) -> dict` to `tools/contract_drift/drift.py`. Resolve member
    roots from the manifest via `tools.workspace_config` (`members()`, keyed by id, each root joined to
    the repo root) — never hardcode fixture paths (keeps drift.py clean under the 11-02 GEN-04 twin).
    Step 1 — per-member drift by VERBATIM reuse: for each member call
    `run_gate(contracts_dir=<root>/"contracts", baseline_path=<root>/"contracts/.hashes/manifest.json")`
    (each member has its OWN baseline — do NOT merge manifests; `build_manifest` keys are
    `.parent`-relative so `contracts/...` keys collide across members, Pitfall 2). Step 2 — edge
    resolution: for each `edge` in `edges(cfg)`, resolve the producer via
    `split_endpoint(edge["from"])[0]`, glob `{p.name.removesuffix(".schema.json") for p in
    (producer_root/"contracts").rglob("*.schema.json")}`, and assert `edge["contract"]` is present —
    fail loud naming the edge when a producer does not actually track the edge's contract. Return
    `{"members": {id: run_gate_result}, "ok": bool}` where `ok` is all members clean AND every edge
    resolved; print a VISIBLE SKIP if the workspace declares zero edges (mirror CI `contract-check`
    zero-pair SKIP, Pitfall 5). Add a `--workspace` flag to `main()` that runs `workspace_drift()` and
    exits non-zero on any member drift or unresolved edge (keep the existing `--contracts-dir`/`--baseline`
    single-tree path untouched — additive). Create `tools/contract_drift/tests/test_workspace_drift.py`
    asserting: (a) `workspace_drift()` over the committed fixture returns `ok=True` (all members clean,
    edge resolves); (b) a member-side drift is detected — build a tmp copy of a member tree with a
    mutated schema and a stale baseline, point a tmp `workspace.toml` (or call `run_gate` directly on
    the tmp tree) and assert `ok=False`; (c) the edge-resolution check flags an edge whose `contract`
    is absent from its producer (construct via a tmp manifest or a monkeypatched `edges` return) — never
    mutating the committed fixture.
    CRITICAL — this new test file lives under `tools/`, which the 11-02 GEN-04 guard
    (`test_core_no_workspace_member_dep.py`) scans (both plans run in wave 2; 11-04 re-runs the full
    suite as the closeout gate). It MUST therefore resolve the fixture member roots it needs at runtime
    via `tools.workspace_config.members()` / `load_workspace()` (e.g. to locate a member tree to copy
    into tmp), NOT as a hardcoded `tests/fixtures/workspace/...` string literal — a literal would trip
    the new guard and block the phase gate.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/contract_drift/tests/test_workspace_drift.py -q` exits 0.
    - `uv run python -m tools.contract_drift.drift --workspace` exits 0 against the committed fixture and prints per-member OK.
    - `grep -c 'tests/fixtures/workspace' tools/contract_drift/drift.py` returns 0 (member roots resolved via loader, not hardcoded).
    - `grep -c 'tests/fixtures/workspace' tools/contract_drift/tests/test_workspace_drift.py` returns 0 (the test resolves member roots via `tools.workspace_config.members()`/`load_workspace()` at runtime, not string literals — so it passes the 11-02 GEN-04 twin).
    - The test proves BOTH a per-member drift FAIL and an unresolved-edge-contract FAIL (two distinct assertions), and a clean-fixture PASS.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/contract_drift/tests/test_workspace_drift.py -q</automated>
  </verify>
  <done>workspace_drift iterates member baselines (no merge) + resolves edge contracts in producers, fails loud on drift or an unresolved edge, and exposes a --workspace CLI; the fixture gates clean.</done>
</task>

<task type="auto">
  <name>Task 2: Workspace-aware golden — widen _confine allowlist (threaded) + resolution + negative control</name>
  <files>tools/golden_runner/runner.py, tools/golden_runner/tests/test_workspace_golden.py</files>
  <read_first>
    - tools/golden_runner/runner.py (_confine L88-102 — the allowlist to WIDEN, never remove; run_golden_case L206-227 golden_dir override; run_identity_converter L142-153)
    - tools/workspace_config/loader.py (members — resolve member roots to thread into the allowlist)
    - tests/fixtures/workspace/member-a/golden/greeting-edge/ (the spanning golden case shipped by 11-01)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md (Pitfall 1 — widen allowed_roots as a threaded param + negative control; Open Q3 — land the widening now)
  </read_first>
  <action>
    Widen `_confine` in `tools/golden_runner/runner.py` to accept an optional `allowed_roots`
    parameter (default = the current `(REPO_ROOT, /tmp, $TMPDIR)` tuple) — resolve each extra root and
    append it to the allowlist, keeping the escape guard: a path outside EVERY allowed root still
    raises `GoldenRunnerError`. Thread the declared member roots (resolved from `tools.workspace_config
    .members()`) through a workspace-aware golden entry — either an optional `allowed_roots=` kwarg on
    the golden path helpers or a thin `workspace_golden_case(case, member_id, ...)` wrapper that looks
    up the member root, passes `golden_dir=<member_root>/"golden"` to the existing `run_golden_case`,
    and threads that member root into `_confine`'s allowlist. Do NOT add new signatures to
    `run_golden_case`/`compare` beyond the additive `allowed_roots` thread — reuse `golden_dir` and
    `converter="identity"` verbatim. Create `tools/golden_runner/tests/test_workspace_golden.py`
    asserting: (a) the fixture's `greeting-edge` case runs via the workspace entry against
    `member-a`'s golden dir with `converter="identity"` and PASSes (members are in-repo so default
    confinement already allows it); (b) a negative control — `_confine` on a synthetic path outside
    REPO_ROOT, `/tmp`, and every declared member root STILL raises `GoldenRunnerError` (the widening
    extends, never removes, the guard); (c) `_confine` on a path inside a threaded member root
    resolves successfully.
    CRITICAL — this new test file lives under `tools/`, which the 11-02 GEN-04 guard
    (`test_core_no_workspace_member_dep.py`) scans (both plans run in wave 2; 11-04 re-runs the full
    suite as the closeout gate). It MUST resolve the fixture member root(s) it exercises at runtime via
    `tools.workspace_config.members()` / `load_workspace()`, NOT as a hardcoded
    `tests/fixtures/workspace/...` string literal — a literal would trip the new guard and block the
    phase gate.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/golden_runner/tests/test_workspace_golden.py -q` exits 0.
    - `uv run pytest tools/golden_runner -q` (existing golden suite) still green — the additive `allowed_roots` param does not change default behavior.
    - The negative-control test asserts `pytest.raises(GoldenRunnerError)` for a path outside all allowed + member roots.
    - `grep -c 'tests/fixtures/workspace' tools/golden_runner/runner.py` returns 0 (member roots resolved via loader, not hardcoded in the tool).
    - `grep -c 'tests/fixtures/workspace' tools/golden_runner/tests/test_workspace_golden.py` returns 0 (the test resolves member roots via `tools.workspace_config.members()`/`load_workspace()` at runtime, not string literals — so it passes the 11-02 GEN-04 twin).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/golden_runner/tests/test_workspace_golden.py tools/golden_runner -q</automated>
  </verify>
  <done>The golden runner resolves an edge-spanning case under a member root and its _confine allowlist is widened via a threaded param while still rejecting out-of-root paths; the fixture case PASSes .NET-free.</done>
</task>

<task type="auto">
  <name>Task 3: Workspace CI job + gate.needs registration</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the `drift` job L121-135 shape to mirror; `golden` job L142-155; `gate.needs` L240; security posture L18-23 — pinned actions, contents:read, no event interpolation)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-PATTERNS.md (ci.yml section — separate-job idiom, fan-in registration)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md (§CI — mirror emit-drift/stale-derived separate job; do not fold into per-repo drift/golden)
  </read_first>
  <action>
    Add a NEW `workspace` job to `.github/workflows/ci.yml` (separate job — do NOT fold cross-repo
    gates into the existing `drift`/`golden` jobs, anti-pattern). Mirror the `drift` job shape:
    `actions/checkout@v7.0.0` → `astral-sh/setup-uv@v8.3.2` → `uv sync --all-packages`, then two
    steps: (1) cross-repo drift — `uv run python -m tools.contract_drift.drift --workspace`; (2)
    cross-repo golden + workspace gates — `uv run pytest tools/workspace_config tools/harness_lint/tests/test_workspace_config.py tools/harness_lint/tests/test_core_no_workspace_member_dep.py tools/golden_runner/tests/test_workspace_golden.py tools/contract_drift/tests/test_workspace_drift.py`.
    Keep the security posture: pinned action versions, no `${{ github.event.* }}` interpolation into any
    `run:` shell (matrix/data from repo-owned files only), inherit the top-level `permissions: contents:
    read`. Register the job in fan-in: add `workspace` to the `gate` job's `needs` array (L240) so a
    cross-repo failure fails the required `gate` check. Validate the workflow structurally with the
    builtin schema.
  </action>
  <acceptance_criteria>
    - `uv run check-jsonschema --builtin-schema vendor.github-workflows .github/workflows/ci.yml` exits 0.
    - `.github/workflows/ci.yml` contains a top-level `workspace:` job and the `gate` job's `needs` list includes `workspace`.
    - The `workspace` job runs `tools.contract_drift.drift --workspace` and the cross-repo pytest set; no `${{ github.event.` appears in any `workspace`-job `run:` step.
  </acceptance_criteria>
  <verify>
    <automated>uv run check-jsonschema --builtin-schema vendor.github-workflows .github/workflows/ci.yml</automated>
  </verify>
  <done>A separate `workspace` CI job iterates members + edges (drift --workspace + cross-repo pytest) and is registered in gate.needs, mirroring the emit-drift/stale-derived separate-job idiom; the workflow validates structurally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member `root` / edge string → filesystem path resolution | manifest data drives which trees drift/golden read |
| golden case path → _confine | a case path could try to escape the repo/member roots |
| CI job → shell | workflow `run:` steps must not interpolate event input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering / Elevation | Malicious member/golden path escaping the workspace (`../`, absolute) | mitigate | `_confine` allowlist is WIDENED (member roots threaded), never removed; negative-control test proves an out-of-all-roots path still raises `GoldenRunnerError`. |
| T-11-07 | Tampering | Symlinked member `contracts/` pointing outside its tree | mitigate | Reuse `build_manifest`'s existing `root not in resolved.parents` symlink defense (hash.py) unchanged per member. |
| T-11-08 | Repudiation | Silent no-op cross-repo gate on an edge-less workspace | mitigate | `workspace_drift` prints a VISIBLE SKIP on zero edges (mirror CI `contract-check` zero-pair SKIP). |
| T-11-09 | Tampering | Subprocess/shell injection via member/edge strings | mitigate | All subprocess calls stay `shell=False` argv lists (`_git_show`, converter spawn); CI job interpolates no `github.event.*`. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | None — no package installs (reuse of existing in-repo tools + stdlib only). |
</threat_model>

<verification>
- `uv run pytest tools/contract_drift tools/golden_runner tools/workspace_config tools/harness_lint -q` green.
- `uv run pytest -q` full core suite green (no regression to existing drift/golden behavior — extensions are additive).
- `uv run check-jsonschema --builtin-schema vendor.github-workflows .github/workflows/ci.yml` exits 0; `gate.needs` includes `workspace`.
</verification>

<success_criteria>
Cross-repo drift iterates per-member baselines (no merge) + resolves edge contracts in producers and
fails loud on drift/unresolved edges; the golden runner resolves an edge-spanning case under a member
root with a widened-not-removed `_confine` allowlist; a separate `workspace` CI job in `gate.needs`
enforces both. Constitution plane stays human-owned per repo. MREPO-03 satisfied.
</success_criteria>

<output>
Create `.planning/phases/11-multi-repo-workspace-v2-0/11-03-SUMMARY.md` when done.
</output>
</content>
