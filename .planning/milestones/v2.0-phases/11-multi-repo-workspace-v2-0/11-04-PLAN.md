---
phase: 11-multi-repo-workspace-v2-0
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - harness/agents/orchestrator.md
  - harness/skills/fan-out-synthesize/SKILL.md
  - .opencode/agent/orchestrator.md
  - .opencode/skill/fan-out-synthesize/SKILL.md
  - .claude/agents/orchestrator.md
  - .claude/skills/fan-out-synthesize/SKILL.md
  - AGENTS.md
autonomous: true
requirements: [MREPO-02]
must_haves:
  truths:
    - "The orchestrator routes a workspace-wide analysis by fanning out one read-only worker per MEMBER REPO, synthesizing compact citation-bearing returns without any single context holding every repo"
    - "The fan-out-synthesize skill documents that a member repo is a valid fan-out unit and a worker never reads a sibling member (the guarantee that keeps a single context from holding all repos)"
    - "The wiring is prose-only (no new skill/command): EXPECTED_SKILLS stays 11, EXPECTED_PERSONAS stays 5, and the emitter round-trips the changed source to both runtimes byte-identical"
  artifacts:
    - path: "harness/agents/orchestrator.md"
      provides: "Routing row + intake note: member repo = fan-out unit (MREPO-02)"
      contains: "member repo"
    - path: "harness/skills/fan-out-synthesize/SKILL.md"
      provides: "Member-repo-as-unit note + no-sibling-read guarantee"
      contains: "member repo"
    - path: "AGENTS.md"
      provides: "Re-emitted managed block (machine-written, emit-drift verified)"
  key_links:
    - from: "harness/agents/orchestrator.md"
      to: ".opencode/agent/orchestrator.md + .claude/agents/orchestrator.md"
      via: "tools.harness_emit projection"
      pattern: "member repo"
    - from: "harness/skills/fan-out-synthesize/SKILL.md"
      to: "harness/agents/explorer.md"
      via: "one read-only worker per member repo"
      pattern: "explorer"
---

<objective>
Apply the Phase-10 β fan-out/synthesize substrate across the workspace (MREPO-02) by PROSE wiring
only — a member repo is exactly the "one per directory/subsystem/contract/question" fan-out unit the
skill already defines. Add an orchestrator routing row + intake note and a fan-out-synthesize skill
note that a member repo is a valid unit and a worker never reads a sibling member. Then round-trip the
Phase-7 emitter to both runtimes (glob discovery, zero emitter code change), carrying no model
identifier, keeping EXPECTED_SKILLS=11 / EXPECTED_PERSONAS=5 (no new surface). This is the phase
closeout: it lands last and runs the full-suite / GEN-04 / emit-drift phase gate.

Purpose: Workspace-wide reconnaissance stays context-cheap — per-repo analysis, workspace-level
synthesis, no single context holding every repo.
Output: prose edits to `harness/agents/orchestrator.md` + `harness/skills/fan-out-synthesize/SKILL.md`
and their re-emitted `.opencode/` + `.claude/` twins + the regenerated `AGENTS.md` managed block.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md
@.planning/phases/11-multi-repo-workspace-v2-0/11-PATTERNS.md

<interfaces>
<!-- The source surfaces to edit + the emitter round-trip contract. Prose only, no new surface. -->

harness/agents/orchestrator.md:
  "Intake → decompose" step 3 "Budget the context (delegate vs inline)" — names context-budget + fan-out-synthesize
  "Routing decision table (work shape → persona / command)" — includes row: "Large surface to cover / would balloon one context | (self) fan out | fan-out-synthesize skill, /fan-out-synthesize"

harness/skills/fan-out-synthesize/SKILL.md:
  §1 DECOMPOSE — "one per directory, subsystem, contract, or question" (a member repo is exactly such a unit)
  §2 DISPATCH — read-only explorer per unit (harness/agents/explorer.md, edit: deny)

tools/harness_lint/caps.py:
  EXPECTED_SKILLS frozenset (11: ...fan-out-synthesize, context-budget)  # MUST stay 11 — prose-only, no bump
  EXPECTED_PERSONAS (5)                                                   # MUST stay 5

Emitter round-trip:
  uv run python -m tools.harness_emit            # glob discovery, no emitter code change
  git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json   # emit-drift gate must be clean after commit
  Constraint: NO model identifier in any artifact (placeholder-tier only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prose-wire member-repo fan-out into orchestrator + fan-out-synthesize skill</name>
  <files>harness/agents/orchestrator.md, harness/skills/fan-out-synthesize/SKILL.md</files>
  <read_first>
    - harness/agents/orchestrator.md (Intake step 3 + the Routing decision table — where the member-repo unit row/note lands)
    - harness/skills/fan-out-synthesize/SKILL.md (§1 DECOMPOSE unit definition + §2 DISPATCH read-only worker — where the member-repo-as-unit + no-sibling-read note lands)
    - harness/agents/explorer.md (the read-only worker each member unit is dispatched to — Read/Grep/Glob, edit: deny)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-PATTERNS.md (workspace-analyze.md section — PROSE wiring strongly preferred over new surface; Pitfall 4 count discipline)
  </read_first>
  <action>
    Add MREPO-02 wiring as PROSE only — no new skill, command, or persona (Pitfall 4: a new skill
    would force an EXPECTED_SKILLS bump + full re-emit fixture churn; CONTEXT prefers reuse). In
    `harness/agents/orchestrator.md`: add ONE routing-table row for workspace-scoped analysis —
    "Analyze a multi-repo workspace / cover several member repos" → route to "(self) fan out, one
    read-only worker per member repo" → entry "fan-out-synthesize skill, /fan-out-synthesize"; and
    extend the "Budget the context" intake step (or add a short adjacent clause) noting a workspace
    member repo is a natural fan-out unit so per-repo workers absorb the reading and the conductor
    synthesizes workspace-level. Do NOT write any member-root path literal (e.g. no
    `tests/fixtures/workspace/...`) — keep it generic ("a workspace member repo") so the 11-02 GEN-04
    twin stays green. In `harness/skills/fan-out-synthesize/SKILL.md` §1 DECOMPOSE: add a sentence that
    a member repo (declared in `workspace.toml`) is a valid unit, and in §2/§3 state the guarantee that
    a per-repo worker is read-only and NEVER reads a sibling member — this is what keeps any single
    context from holding every repo at once (MREPO-02's core guarantee, already the substrate's design).
    Reference `workspace.toml` by name only (its consumers are the loader + gate, not this skill).
  </action>
  <acceptance_criteria>
    - `grep -c 'member repo' harness/agents/orchestrator.md` returns >= 1 and `grep -c 'member repo' harness/skills/fan-out-synthesize/SKILL.md` returns >= 1.
    - No member-root path literal in either source file: `grep -c 'tests/fixtures/workspace' harness/agents/orchestrator.md harness/skills/fan-out-synthesize/SKILL.md` returns 0.
    - `uv run pytest tools/harness_lint -q` exits 0 (persona/skill structural + anti-sprawl gates + GEN-04 example & workspace-member twins still green; no count change).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint -q</automated>
  </verify>
  <done>The orchestrator routes workspace-wide analysis by fanning out one read-only worker per member repo and the fan-out skill documents the member-repo unit + no-sibling-read guarantee — prose only, no new surface, structural gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Emitter round-trip to both runtimes + phase-gate verification</name>
  <files>.opencode/agent/orchestrator.md, .opencode/skill/fan-out-synthesize/SKILL.md, .claude/agents/orchestrator.md, .claude/skills/fan-out-synthesize/SKILL.md, AGENTS.md</files>
  <read_first>
    - tools/harness_emit/generate.py (glob-discovery emit → .opencode + .claude; no emitter code change needed)
    - tools/harness_lint/caps.py (EXPECTED_SKILLS = 11, EXPECTED_PERSONAS = 5 — MUST stay unchanged; placeholder-tier model regex, no real model id)
    - .github/workflows/ci.yml (emit-drift job L179-189 — the git diff --exit-code path set the round-trip must leave clean)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md (Pitfall 4 — count discipline + emit-drift; §Runtime State Inventory emitter-owned derived surface)
  </read_first>
  <action>
    Re-run the Phase-7 emitter to project the two edited source surfaces to BOTH runtimes:
    `uv run python -m tools.harness_emit`. This regenerates `.opencode/agent/orchestrator.md`,
    `.opencode/skill/fan-out-synthesize/SKILL.md`, `.claude/agents/orchestrator.md`,
    `.claude/skills/fan-out-synthesize/SKILL.md`, and the `AGENTS.md` managed block (agent/command/skill
    index — sorted, deterministic). Do NOT change emitter code or `caps.py` counts (prose-only wiring;
    EXPECTED_SKILLS stays 11, EXPECTED_PERSONAS stays 5). Confirm no real model identifier landed in any
    regenerated artifact (placeholder-tier only). If the emitter maintains fixture twins/`.ambr`
    snapshots or an `emit-manifest.json`, verify they are unchanged except for the two projected surfaces
    (this is a content edit to existing surfaces, not a new surface — command/skill counts do not move).
    Then run the phase gate: full core suite + a re-emit-diff over the documented emit-drift path set,
    which must be clean AFTER committing the regenerated trees.
  </action>
  <acceptance_criteria>
    - `uv run python -m tools.harness_emit` runs clean, then `git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` exits 0 after staging/committing the regenerated trees (emit-drift clean).
    - `.opencode/agent/orchestrator.md` and `.claude/agents/orchestrator.md` both contain "member repo"; the projected fan-out-synthesize SKILL twins contain it too.
    - `uv run pytest tools/harness_emit -q` green (EXPECTED_SKILLS still 11, EXPECTED_PERSONAS still 5; no real model id).
    - `uv run pytest -q` full core suite green (all four MREPO gates + existing suite).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m tools.harness_emit && git add -A -- .opencode opencode.json .claude AGENTS.md CLAUDE.md && git diff --cached --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json && uv run pytest tools/harness_emit -q</automated>
  </verify>
  <done>The edited orchestrator + fan-out skill are projected byte-identical to both runtimes with no model id and no count change; emit-drift is clean and the full core suite (incl. all four MREPO gates) is green — phase closeout complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored harness/ source → emitted .opencode/.claude derived trees | machine-written derivatives; hand-edited drift is a gate FAIL |
| per-repo worker → sibling member repos | a worker reading a sibling would defeat the context-economy guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information disclosure / DoS | A fan-out worker reading sibling member repos (context balloon) | mitigate | Skill prose states each per-repo worker is read-only and never reads a sibling member; synthesis is workspace-level from compact citation-bearing returns. |
| T-11-11 | Tampering | Hand-edited generated .opencode/.claude drift | mitigate | Emitter round-trip regenerates both runtimes; emit-drift CI job `git diff --exit-code` fails on any divergence. |
| T-11-12 | Spoofing | Model identifier leaking into an emitted artifact | mitigate | Placeholder-tier only; `harness_emit` validators loud-fail a real model id; verified post-emit. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | None — no package installs (prose edit + deterministic re-emit only). |
</threat_model>

<verification>
- `uv run pytest -q` full core suite green (all four MREPO gates + existing suite, no regression).
- `uv run python -m tools.harness_emit` then `git diff --exit-code` over the emit-drift path set is clean.
- EXPECTED_SKILLS == 11 and EXPECTED_PERSONAS == 5 unchanged; no real model identifier in any emitted artifact.
- The generalized GEN-04 twin (`test_core_no_workspace_member_dep.py`) stays green (orchestrator/skill prose carries no member-root path token).
</verification>

<success_criteria>
Workspace-wide analysis is prose-wired to fan out one read-only worker per member repo with
workspace-level synthesis (no single context holds every repo); the wiring adds no new surface
(EXPECTED_SKILLS=11, EXPECTED_PERSONAS=5) and round-trips the emitter byte-identical to both runtimes
with no model id; the full phase gate (core suite + emit-drift + GEN-04 twins) is green. MREPO-02
satisfied and Phase 11 closed out.
</success_criteria>

<output>
Create `.planning/phases/11-multi-repo-workspace-v2-0/11-04-SUMMARY.md` when done.
</output>
</content>
