---
phase: 11-multi-repo-workspace-v2-0
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - tools/harness_lint/tests/test_core_no_workspace_member_dep.py
  - tools/workspace_config/tests/test_endpoints.py
autonomous: true
requirements: [MREPO-04]
must_haves:
  truths:
    - "A repo-qualified `repo:stage` edge endpoint parses to (member, stage); a bare stage stays single-repo (backward-compatible with Phase-8 core topology)"
    - "The fixture's one edge crosses a repo boundary (from-member != to-member) and the workspace gate accepts it as well-formed"
    - "A generalized GEN-04 guard proves no core file (tools/harness/libs) path-references a workspace member root; workspace.toml pointer lines are key-scoped exempt; a non-pointer member leak is still flagged"
  artifacts:
    - path: "tools/harness_lint/tests/test_core_no_workspace_member_dep.py"
      provides: "MREPO-04 GEN-04 twin: core → workspace-member single-direction guard"
      contains: "git ls-files"
    - path: "tools/workspace_config/tests/test_endpoints.py"
      provides: "repo:stage endpoint parse + cross-boundary edge semantics"
      contains: "split_endpoint"
  key_links:
    - from: "tools/harness_lint/tests/test_core_no_workspace_member_dep.py"
      to: "workspace.toml member roots"
      via: "resolve forbidden tokens from load_workspace() at test time"
      pattern: "load_workspace|members"
    - from: "tools/workspace_config/tests/test_endpoints.py"
      to: "tools.workspace_config.split_endpoint"
      via: "import + assert repo:stage parse"
      pattern: "split_endpoint"
---

<objective>
Generalize the Phase-8 pipeline topology so a declared edge can cross a repo boundary
(`repo:stage` endpoints), and generalize the GEN-04 no-dependency guard to core→workspace-member
(MREPO-04). The `split_endpoint` helper already lands in the Wave-1 loader; this plan proves the
cross-boundary semantics explicitly and adds the guard twin that keeps the core independent of every
workspace member — mirroring `test_core_no_example_dep.py` byte-for-byte in idiom (git ls-files scan,
key-scoped sanctioned-pointer exemption, live negative controls).

Purpose: A workspace edge that spans two members is the topology generalization; the guard twin is
the template invariant (core depends on NO member) that keeps the harness reusable.
Output: `tools/harness_lint/tests/test_core_no_workspace_member_dep.py`,
`tools/workspace_config/tests/test_endpoints.py`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md
@.planning/phases/11-multi-repo-workspace-v2-0/11-PATTERNS.md

<interfaces>
<!-- The existing GEN-04 guard to CLONE (the whole file is the template). Executor mirrors it. -->

tools/harness_lint/tests/test_core_no_example_dep.py — clone target:
  _REPO_ROOT = Path(__file__).resolve().parents[3]
  _CORE_ROOTS = ("tools", "harness", "libs")
  _SELF = Path(__file__).resolve()                          # exclude self (holds negative-control literals)
  _tracked_core_files(): subprocess.run(["git","ls-files",*_CORE_ROOTS], cwd=_REPO_ROOT, shell=False, check=True) → skip resolved == _SELF
  _INSTANCE_ROOT_FILE = "harness/project.toml"; _INSTANCE_POINTER_LINE = re.compile(r"\s*(root|persona|test_paths)\s*=")
  _is_instance_pointer_line(rel, line): return rel == _INSTANCE_ROOT_FILE and _INSTANCE_POINTER_LINE.match(line)
  _scan_lines(rel, text): skip pointer lines; flag forbidden tokens → [(lineno, line)]
  test_core_has_no_example_dependency / test_negative_control_flags_* / test_instance_*_is_exempt / test_negative_control_flags_nonexempt_project_toml_leak

tools/workspace_config/loader.py (from 11-01):
  load_workspace(path=_DEFAULT_WORKSPACE) -> dict; members(cfg=None) -> list[dict]; edges(cfg=None) -> list[dict]
  split_endpoint(endpoint) -> tuple[str|None, str]   # "repo:stage" → (repo, stage); "stage" → (None, stage)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalized GEN-04 guard — test_core_no_workspace_member_dep.py</name>
  <files>tools/harness_lint/tests/test_core_no_workspace_member_dep.py</files>
  <read_first>
    - tools/harness_lint/tests/test_core_no_example_dep.py (the ENTIRE clone target: git ls-files core scan, _SELF exclusion, key-scoped pointer exemption L81-113, live negative controls L157-219)
    - workspace.toml (the member `root` values that become the forbidden tokens; the pointer lines to exempt)
    - tools/workspace_config/loader.py (load_workspace/members — resolve forbidden tokens at test time, never hardcode)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-RESEARCH.md (Pitfall 3 — key-scoped exemption is critical; loader carries no member token so it passes clean)
  </read_first>
  <action>
    Create `tools/harness_lint/tests/test_core_no_workspace_member_dep.py` cloning
    `test_core_no_example_dep.py`'s structure. Scan tracked files under `_CORE_ROOTS =
    ("tools", "harness", "libs")` via `git ls-files` (`subprocess`, `shell=False`, `check=True`),
    excluding `_SELF` (this guard holds the forbidden tokens as negative-control literals). Derive the
    forbidden tokens at test time from the manifest: `member_roots = {m["root"] for m in
    members(load_workspace())}` (e.g. `tests/fixtures/workspace/member-a`) — do NOT hardcode fixture
    paths, so the guard tracks the manifest. Define the sanctioned-pointer exemption key-scoped to
    `workspace.toml`: `_WORKSPACE_FILE = "workspace.toml"` with a pointer regex matching `root =`,
    `from =`/`to =`, and `contract =` lines (the ONE place a config file may name a member — ADR-0002(c)
    precedent). `_scan_lines(rel, text)` skips exempt pointer lines and flags any core line containing a
    `member_roots` token → `[(lineno, line)]`. NOTE the guard scans ALL tracked files under `tools/`,
    which now includes the Wave-2 sibling test files `tools/contract_drift/tests/test_workspace_drift.py`
    and `tools/golden_runner/tests/test_workspace_golden.py` (11-03) — those files must therefore resolve
    member roots via the loader at runtime, never as `tests/fixtures/workspace/...` string literals, or
    this guard flags them; 11-03's Task 1/Task 2 acceptance criteria enforce that. Encode tests
    mirroring the example guard: (a) `test_core_has_no_workspace_member_dependency` (no offender across
    the scanned core set); (b) a LIVE negative control proving a synthetic
    `x = "tests/fixtures/workspace/member-a/foo"` core line IS flagged (the scan cannot silently
    no-op); (c) `test_workspace_root_pointer_is_exempt` proving a
    `root = "tests/fixtures/workspace/member-a"` line in `workspace.toml` is exempt; (d)
    `test_negative_control_flags_nonexempt_workspace_leak` proving a member path on a NON-pointer key
    (e.g. `member = "tests/fixtures/workspace/member-a"`) is STILL flagged — the exemption is
    key-scoped, not a blanket file pass.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_core_no_workspace_member_dep.py -q` exits 0.
    - The guard resolves forbidden tokens from `load_workspace()`/`members()` at runtime (grep finds `load_workspace` or `members` in the file) — no hardcoded `tests/fixtures/workspace` string outside negative-control literals.
    - The negative-control tests for both a synthetic core leak AND a non-pointer workspace.toml leak assert `hits` is non-empty (scan is provably live and key-scoped).
    - `uv run pytest -q` full core suite still green (the twin does not flag any existing core file, incl. the 11-01 loader).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_workspace_member_dep.py -q</automated>
  </verify>
  <done>The generalized GEN-04 guard proves core→workspace-member single-direction dependency, with key-scoped workspace.toml pointer exemption and live negative controls; the whole core suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: repo:stage endpoint parse + cross-boundary edge semantics test</name>
  <files>tools/workspace_config/tests/test_endpoints.py</files>
  <read_first>
    - tools/workspace_config/loader.py (split_endpoint — the helper under test)
    - tools/harness_lint/tests/test_pipeline_config.py (the Phase-8 single-repo endpoint gate that stays UNCHANGED — bare stages, no repo qualification)
    - workspace.toml (the fixture edge member-a:emit → member-b:ingest that must be cross-member)
    - .planning/phases/11-multi-repo-workspace-v2-0/11-PATTERNS.md (Pattern 5 — repo:stage parse; core Phase-8 gate stays unchanged, generalization lives in the workspace layer)
  </read_first>
  <action>
    Create `tools/workspace_config/tests/test_endpoints.py` proving the topology generalization
    (MREPO-04). Assert: `split_endpoint("member-a:emit") == ("member-a", "emit")`;
    `split_endpoint("member-b:ingest") == ("member-b", "ingest")`; a bare `split_endpoint("emit") ==
    (None, "emit")` (backward-compatible single-repo endpoint — the Phase-8 core default form stays
    valid). Then load the real manifest and assert the fixture's one edge CROSSES a repo boundary:
    for the edge, `split_endpoint(edge["from"])[0] != split_endpoint(edge["to"])[0]` (from-member is
    `member-a`, to-member is `member-b`) — this is the observable "edge crosses a repo boundary"
    behavior MREPO-04 requires. Add an assertion that the Phase-8 core gate is untouched by importing
    `tools.harness_config.pipeline` and confirming its edges use bare stages (no `:` — the core
    single-repo default is not generalized; the generalization lives only in the workspace layer, per
    Pattern 5's anti-pattern warning).
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/workspace_config/tests/test_endpoints.py -q` exits 0.
    - The test asserts the fixture edge is cross-member: `split_endpoint(from)[0] != split_endpoint(to)[0]`.
    - The test confirms the core `harness/project.toml` `[pipeline]` edges carry NO `:` (Phase-8 single-repo gate stays unchanged).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/workspace_config/tests/test_endpoints.py -q</automated>
  </verify>
  <done>repo:stage parses to (member, stage), bare stays single-repo, the fixture edge is proven to cross a repo boundary, and the Phase-8 core topology gate is confirmed unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core plane (tools/harness/libs) → workspace member | a core file that names a member root would be a one-directional-dependency leak |
| workspace.toml endpoint strings → parser | `repo:stage` strings drive member resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Elevation | Core plane depending on a workspace member (template invariant break) | mitigate | GEN-04 twin scans git ls-files core set for member-root tokens; live negative controls prove the scan cannot silently no-op. |
| T-11-05 | Tampering | Blanket file exemption hiding a real leak in workspace.toml | mitigate | Exemption is key-scoped (root/from/to/contract only); `test_negative_control_flags_nonexempt_workspace_leak` proves a non-pointer member leak is still flagged. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | None — no package installs in this plan (tests only, stdlib + in-repo members). |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_core_no_workspace_member_dep.py tools/workspace_config/tests/test_endpoints.py -q` green.
- `uv run pytest -q` full core suite green (GEN-04 example twin + new workspace-member twin + all existing gates).
</verification>

<success_criteria>
A `repo:stage` edge crosses a repo boundary in the workspace layer (parse + fixture edge proven
cross-member), the Phase-8 core single-repo topology gate is untouched, and a generalized GEN-04 guard
proves the core references no workspace member with a key-scoped pointer exemption and live negative
controls. MREPO-04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/11-multi-repo-workspace-v2-0/11-02-SUMMARY.md` when done.
</output>
</content>
