---
phase: 35-carried-debt-dispositions-v2-4-b
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/docs_guard/impact.py
  - tools/docs_guard/cli.py
  - tools/docs_guard/__init__.py
  - tools/docs_guard/tests/test_impact.py
  - tools/docs_guard/tests/test_report.py
  - tools/memory_regen/docs_staleness.py
autonomous: true
requirements: [DEBT-03]

must_haves:
  truths:
    - "The contract graph is compiled ONCE per report. Both live sites (cli.py's comprehension, docs_staleness.rows' loop) stop compiling once per binding."
    - "The impact.py signature/cache question is DECIDED EXPLICITLY and the rationale is recorded AT THE SITE, not only in a planning file. A hidden cache in a module that advertises purity is forbidden."
    - "impact_ids keeps its exact public signature and behaviour -- the residual's 'change the signature' horn is not taken."
    - "No module-level mutable state is introduced. A second call with a different cfg must answer for that cfg."
    - "Determinism is PROVEN by byte-identity: `python -m tools.docs_guard` stdout+stderr sha256 identical before and after, exit 0 both sides."
    - "A test exists that FAILS if the report regresses to a per-binding compile. It must count live reads, not inspect output -- a per-binding loop renders the IDENTICAL report, so output cannot witness the defect."
    - "The batch result equals the per-binding loop it replaces, including EMPTY entries -- NEVER FABRICATE is unchanged."
  artifacts:
    - path: "tools/docs_guard/impact.py"
      provides: "`impact_map(bindings, cfg)` -- pure batch entry point compiling once; `_ids_for` shared traversal; both rejected alternatives recorded in the docstring"
      contains: "impact_map"
    - path: "tools/docs_guard/tests/test_impact.py"
      provides: "batch-equals-loop equivalence, one-compile-per-report count, zero-compiles-at-zero-bindings, no-state-between-calls"
      contains: "compiles_the_graph_exactly_once"
    - path: "tools/docs_guard/tests/test_report.py"
      provides: "report-level compile count over 3 bindings + rendered-text byte-identity between batch and loop paths"
      contains: "compiles_the_graph_once_for_many_bindings"
  key_links:
    - from: "tools/docs_guard/cli.py::main"
      to: "tools/docs_guard/impact.py::impact_map"
      via: "one call per report, inside the existing degradation try"
      pattern: "impact_map"
    - from: "tools/memory_regen/docs_staleness.py::rows"
      to: "tools/docs_guard/impact.py::impact_map"
      via: "one call over the QUALIFYING bindings, built before the render loop"
      pattern: "impact_map"
---

<objective>
Close 28 IN-03: compile the contract graph once per report instead of once per binding, with the
`impact.py` signature-vs-cache question decided explicitly rather than carried a fourth time.

Purpose: the residual was left open because both named options were bad. The plan's real work is
finding whether the binary is real, deciding in the open, and proving the report did not move.

Output: a batch entry point, both call sites converted, and determinism proven by hash.
</objective>

## Tasks

### Task 1 — Verify the sites, then capture the baseline

Confirm the audit's line numbers before trusting them. Capture the pre-change bytes:

```
uv run python -m tools.docs_guard > baseline.out 2> baseline.err ; shasum -a 256 baseline.*
```

<automated>
Both sites located and line numbers confirmed or corrected in the SUMMARY. Baseline sha256 recorded.
</automated>

### Task 2 — Decide the shape, in the open

Evaluate the residual's two horns (public-signature change; cached state in a pure module) and any
third option. **Record the decision and the rejection of the alternatives in the code**, at the new
entry point, so a future reader reaching for a cache finds the reason not to at the site.

<automated>
`impact.py` names both rejected alternatives with their reasons. `impact_ids`'s signature is
byte-identical to its pre-change form.
</automated>

### Task 3 — Convert both call sites

Convert `cli.py` (`main`'s comprehension **and** `render`'s fallback) and `docs_staleness.rows`.
Update the lazy re-export map. Fix any test that patched the old call site — record the ripple
rather than hiding it.

<automated>
`uv run pytest tools/docs_guard tools/memory_regen -q` green.
</automated>

### Task 4 — Prove determinism, and guard it

Re-run the report; stdout and stderr must hash to the Task-1 baseline, exit 0. Add the tests that go
red on a regression: they must **count the live reads**, because the regression produces an
identical report.

<automated>
`diff baseline.out after.out` empty; sha256 equal; the compile-count test asserts exactly 1 for a
multi-binding report.
</automated>

## Verification

- Byte-identity of the live report, by hash, before and after.
- A test that fails under a per-binding regression.
- `impact_ids` unchanged in signature and behaviour; no module-level state.
