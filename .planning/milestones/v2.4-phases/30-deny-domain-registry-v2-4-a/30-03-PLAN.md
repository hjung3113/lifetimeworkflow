---
phase: 30-deny-domain-registry-v2-4-a
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified:
  - tools/harness_lint/tests/test_deny_domain_registry.py
autonomous: true
requirements: [SEAL-01]

must_haves:
  truths:
    - "The drift test FAILS when a hook's live constant disagrees with its declaration — proven by SEVEN mutations, one per check, each run in-suite with a `finally` restore (the `test_contract_guard.py:352-381` idiom), never asserted in prose"
    - "Check 1 compares declared `globs` to the live constant ELEMENT-WISE AND ORDERED, via `deny_domains.resolve_owner` — identity of the module attribute, so a stale copy cannot pass"
    - "Check 5 (bypass semantics) is proven by DRIVING the hook: `monkeypatch.setenv` then call the hook's own `decide()`/`main()` path. A static source read is forbidden and the test says why — a token scan goes green on an unused import (28-RESEARCH P4)"
    - "`secret_scan` and `ledger_guard` are proven to still DENY with BOTH `GOLDEN_APPROVE_HUMAN` and `HARNESS_DEV_BYPASS` exported — their declared `bypasses: []` is a behavioural claim, and this is the only assertion that tests it"
    - "Check 6 reads `tools.harness_emit.merge.HARNESS_HOOK_GROUPS` — the emitter SOURCE — never `.claude/settings.json`, which is its projection and is already gated by `emit-drift` (ci.yml:218)"
    - "Check 7 PARSES the opencode adapter's `input.tool !== \"…\"` chain out of the `.ts` source and compares it to the declared surfaces; the test states that this is a source parse because no opencode runtime exists here (.opencode/plugin/ledger-guard.ts:4-8), and the mutation proof is what keeps it falsifiable"
    - "Check 4 asserts matrix reconciliation is empty in BOTH directions, so an unowned `path_deny_globs` row — the ADR-0010:154-160 inert-data shape — fails the suite"
    - "The test asserts NO hook imports `tools.deny_domains` (D-02), so the comparison can never degenerate into `x == x`"
    - "Zero production files are modified: this plan adds exactly one test file"
  artifacts:
    - path: "tools/harness_lint/tests/test_deny_domain_registry.py"
      provides: "the SEAL-01 drift gate — seven checks over the declaration vs the live constants, hooks, emitter and adapters, each with its mutation proof"
      contains: "HARNESS_HOOK_GROUPS"
  key_links:
    - from: "tools/harness_lint/tests/test_deny_domain_registry.py"
      to: "tools/deny_domains/registry.py"
      via: "load_registry / resolve_owner / matrix_reconciliation — the loader plan 30-02 built"
      pattern: "resolve_owner"
    - from: "tools/harness_lint/tests/test_deny_domain_registry.py"
      to: "tools/harness_emit/merge.py::HARNESS_HOOK_GROUPS"
      via: "the AUTHORITATIVE Claude matcher declaration; .claude/settings.json is its projection"
      pattern: "HARNESS_HOOK_GROUPS"
    - from: "tools/harness_lint/tests/test_deny_domain_registry.py"
      to: ".opencode/plugin/{contract-guard,secret-scan,ledger-guard}.ts"
      via: "regex parse of the `input.tool !== \"x\"` guard chain at :70"
      pattern: "input.tool"
---

<objective>
Make the declaration unable to rot: a `harness_lint` test that goes RED the moment a hook's globs,
its bypass behaviour, its Claude matcher, or its opencode adapter surface disagrees with what
`contracts/harness/security/deny-domains.json` claims — each disagreement class proven by an actual
mutation.

Purpose: SEAL-01's acceptance is not "a registry exists" but "a drift test fails when a hook's live
constant disagrees with its declaration, proven by a mutation that makes it fail"
(`.planning/REQUIREMENTS.md:24-25`). A registry with a test that only re-reads the JSON is exactly
the defect ADR-0010:158-160 names.

Output: one test file. Zero production change.
</objective>

<execution_context>
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-deny-domain-registry-v2-4-a/30-CONTEXT.md
@.planning/phases/30-deny-domain-registry-v2-4-a/30-RESEARCH.md
@.planning/phases/30-deny-domain-registry-v2-4-a/30-02-PLAN.md
@contracts/harness/security/deny-domains.json
@tools/hooks/tests/test_contract_guard.py
@tools/harness_emit/merge.py
@tools/harness_lint/tests/test_contract_graph_config.py
@docs/adr/0010-human-docs-review-obligation-model.md

<interfaces>
<!-- VERIFIED against source in this session. -->

From tools/deny_domains/registry.py (plan 30-02):

    load_registry(path=DEFAULT_REGISTRY_PATH) -> dict
    resolve_owner(domain: dict) -> object                  # the LIVE constant object
    matrix_reconciliation(...) -> {"undeclared_in_matrix": set, "unowned_matrix_globs": set}
    class DenyDomainError(ValueError)

Hook decision entrypoints (all return a deny dict or None; all exit 0 from `main`):

    tools.hooks.contract_guard.decide(file_path, content, approved) -> dict | None
    tools.hooks.contract_guard.main() -> int          # reads GOLDEN_APPROVE_HUMAN (:114) + dev_bypassed() (:115)
    tools.hooks.secret_scan.decide(file_path, content) -> dict | None      # NO env read at all (:81-91)
    tools.hooks.ledger_guard.decide(file_path) -> dict | None              # NO env read at all (:82-86)

`decide` signatures differ per hook — do NOT write a uniform caller that assumes one arity. Dispatch
on `domain["id"]` with an explicit per-domain probe path and call the real signature.

Bypass note that decides how check 5 must be written: `contract_guard.decide` takes `approved` as a
PARAMETER; the env reading happens in `main()`. So the constitution bypass claim can only be tested
through `main()` with `monkeypatch.setenv` + `capsys`, not through `decide`. `secret_scan` and
`ledger_guard` read no env in either function, which is exactly the claim `bypasses: []` makes —
drive `main()` for those too, with both env vars set, and assert a deny is still printed.

`tools/harness_emit/merge.py:112-175` — `HARNESS_HOOK_GROUPS` is
`{event: [ {"matcher": str, "hooks": [{"command": str, ...}]}, ... ]}`. Find the group whose command
contains the domain's `owner_module`, read its `matcher`, split on `|`. Live values:
`contract_guard` -> `Write|Edit`; `secret_scan` -> `Read|Write|Edit`; `ledger_guard` -> `Write|Edit`.

opencode adapters, each at `:70`, all of the shape:
    if (input.tool !== "write" && input.tool !== "edit") return;
Parse with `re.findall(r'input\.tool\s*!==\s*"([a-z]+)"', source)`. Live: contract-guard
`["write","edit"]`, secret-scan `["read","write","edit"]`, ledger-guard `["write","edit"]`.
Declared surfaces are capitalized (`Write`); adapter tools are lowercase (`write`) — normalize at the
comparison seam and say so, or the test fails for the wrong reason.

`tools/harness_lint/tests/conftest.py:19-22` already puts the repo root on `sys.path`; no new
conftest is needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the adversarial mutation table FIRST, as the test file's own docstring</name>
  <files>tools/harness_lint/tests/test_deny_domain_registry.py</files>
  <read_first>
    30-RESEARCH.md §"The drift test — what it can actually falsify" (the seven-row table),
    tools/harness_lint/tests/test_contract_graph_config.py:1-13 (module-docstring house style)
  </read_first>
  <action>
    Create the file with ONLY its module docstring and the seven-row table, before any assertion is
    written. Each row: the check, the exact mutation that must make it RED, and the failure it
    prevents. This is the phase's anti-pattern fence (26.1 / 27.1 / 27.2 / 28) — the adversarial
    table is authored first so the checks are written to the table rather than the table being
    back-filled from whatever the checks happened to catch.

    The docstring must also state the two forbidden shortcuts and why:
      - **Never a static source read for a behavioural claim.** A `"dev_bypassed" in source` scan
        goes green on an unused import (28-RESEARCH P4). Bypass claims are driven.
      - **Never assert against `.claude/settings.json`.** It is the emitter's projection and
        `emit-drift` (ci.yml:218) already gates it; the source of truth is
        `tools.harness_emit.merge.HARNESS_HOOK_GROUPS`.

    Commit this docstring-only file as its own step so the table is reviewable independently of the
    assertions that follow.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_deny_domain_registry.py -q --collect-only</automated>
  </verify>
  <acceptance_criteria>
    The file collects with zero tests. The docstring contains seven distinct mutation rows and both
    forbidden-shortcut statements.
  </acceptance_criteria>
  <done>The adversarial table exists in the repo, authored before any assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Checks 1-4 — the declaration vs the live constants and the matrix, both directions</name>
  <files>tools/harness_lint/tests/test_deny_domain_registry.py</files>
  <read_first>
    tools/deny_domains/registry.py,
    tools/hooks/{contract_guard,secret_scan,ledger_guard}.py,
    harness/permission-matrix.json
  </read_first>
  <action>
    Implement, parametrized over the three declared domains where it makes sense:

    - **Check 1 — `test_declared_globs_match_the_live_constant`**: `resolve_owner(domain)` then
      `assert list(live) == domain["globs"]`. Ordered and element-wise; a set comparison would pass on
      a reordering that changes nothing today but hides a future prefix-shadowing bug. The failure
      message must name the domain, the declared list and the live list.
    - **Check 2 — `test_owner_module_and_constant_resolve`**: `resolve_owner` raises `DenyDomainError`
      for a bogus module/attribute (reuse plan 30-02's cases by reference; here just assert the happy
      path resolves for all three).
    - **Check 3 — `test_declared_domains_are_pairwise_disjoint`**: over the REAL contract, assert
      `load_registry()` succeeds (its disjointness rule is what proves it) AND, independently, that
      for every ordered pair the probe corpus of one domain never resolves `deny` against the other's
      globs. Cite `contract_guard.py:16-20` and ADR-0010:170-175 in the docstring — this is the
      invariant that keeps `GOLDEN_APPROVE_HUMAN` from ever shadowing the ledger domain.
    - **Check 4 — `test_matrix_path_deny_globs_reconcile_both_directions`**: assert BOTH
      `undeclared_in_matrix` and `unowned_matrix_globs` are empty against the real matrix. The
      docstring names ADR-0010:154-160: an unowned matrix row is a claimed control that does not
      exist, and it is precisely the defect that made `ledger_guard` necessary.

    Plus **`test_no_hook_imports_the_registry`**: grep `tools/hooks/*.py` for `deny_domains` and
    assert no hit, with a docstring stating that a hook reading the registry would make every check
    above a tautology (D-02).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_deny_domain_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    All checks 1-4 pass against the untouched tree. Each has a distinct, informative failure message
    naming the domain. `uv run ruff check tools/harness_lint` exits 0.
  </acceptance_criteria>
  <done>Checks 1-4 are green against the real contract, the real hooks and the real matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Checks 5-7 — bypass semantics by live drive, Claude matchers from the emitter, opencode surfaces by source parse</name>
  <files>tools/harness_lint/tests/test_deny_domain_registry.py</files>
  <read_first>
    tools/hooks/contract_guard.py:106-129 (main's env reads and the dev-note branch),
    tools/hooks/secret_scan.py:81-91 and tools/hooks/ledger_guard.py:75-86 (no env read),
    tools/harness_emit/merge.py:112-175,
    .opencode/plugin/{contract-guard,secret-scan,ledger-guard}.ts (the :70 guard chain)
  </read_first>
  <action>
    - **Check 5 — `test_declared_bypasses_match_live_behaviour`**, driven, never read:
      - `constitution`: with neither env var, `main()` on a `contracts/x.json` probe prints a deny.
        With `GOLDEN_APPROVE_HUMAN=1` it does not. With only `HARNESS_DEV_BYPASS` set it does not, and
        the stderr dev-note appears (`contract_guard.py:119-128`). An empty/whitespace token must NOT
        bypass (`contract_guard.py:114`) — assert that too, since it is a declared behaviour of the
        token the registry names.
      - `secret` and `review-ledger`: with **both** env vars exported, `main()` on that domain's probe
        path STILL prints a deny. This is the only assertion in the repo that tests `bypasses: []` as
        a behavioural claim rather than a documentation claim.
      Use `monkeypatch.setenv` + `monkeypatch.setattr(sys, "stdin", ...)` or the hooks' `_stdin` seam
      to feed the event envelope, and `capsys` to read the decision. Restore env via monkeypatch's
      own teardown — never `os.environ` mutation.
    - **Check 6 — `test_declared_claude_surfaces_match_the_emitter`**: for each domain, find the
      `HARNESS_HOOK_GROUPS["PreToolUse"]` group whose command contains `owner_module`, split its
      `matcher` on `|`, and assert set-equality with the declared `runtime_adapters[claude].surfaces`
      and with `covered_tool_surfaces`. Fail loudly if no group matches — a hook that lost its wiring
      must not silently satisfy a vacuous comparison.
    - **Check 7 — `test_declared_opencode_surfaces_match_the_adapter_source`**: read the adapter file
      named by `runtime_adapters[opencode].path`, `re.findall` the `input.tool !== "x"` chain,
      case-normalize, and assert set-equality with the declared surfaces. Assert the regex found at
      least one entry — a rename that breaks the parse must fail, not pass vacuously. Docstring: this
      is a SOURCE PARSE because there is no opencode runtime in this container
      (`.opencode/plugin/ledger-guard.ts:4-8`), and Task 4's mutation is what keeps it honest.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_deny_domain_registry.py -q && uv run pytest tools/hooks -q</automated>
  </verify>
  <acceptance_criteria>
    All seven checks pass. `uv run pytest tools/hooks -q` still exits 0 (no env or module state leaked
    into the hook suite). `grep -n "os.environ\[" tools/harness_lint/tests/test_deny_domain_registry.py`
    returns nothing — env is only ever touched through `monkeypatch`.
  </acceptance_criteria>
  <done>Bypass semantics, Claude matchers and opencode surfaces are all asserted against live behaviour or the authoritative source, with no static token scan anywhere.</done>
</task>

<task type="auto">
  <name>Task 4: Prove all seven checks load-bearing — seven mutations, in-suite, restored</name>
  <files>tools/harness_lint/tests/test_deny_domain_registry.py</files>
  <read_first>tools/hooks/tests/test_contract_guard.py:352-381 (the delete-each-member + `finally` restore idiom)</read_first>
  <action>
    Add one mutation proof per check, each asserting that the corresponding check RAISES when the
    tree is mutated and that the tree is restored afterwards. Follow the `finally`-restore idiom so a
    failed assertion cannot leak state.

      1. delete `"golden/**"` from `contract_guard.CONSTITUTION_GLOBS` → check 1 fails.
      2. `monkeypatch.delattr` the constant → check 2 raises `DenyDomainError`.
      3. append `"docs/.docs-review-ledger.toml"` to a copy of the constitution globs → check 3 fails
         naming both domains.
      4. add an unowned glob to a tmp matrix → check 4's `unowned_matrix_globs` is non-empty.
      5. monkeypatch `ledger_guard.main` to consult `HARNESS_DEV_BYPASS` → check 5 fails. This is the
         single most important row: it is the difference between a documented no-bypass and an
         enforced one.
      6. patch a copy of `HARNESS_HOOK_GROUPS` so `secret_scan`'s matcher is `Write|Edit` → check 6
         fails (the dropped `Read` surface).
      7. feed check 7 a synthetic adapter source with `"edit"` removed → check 7 fails.

    Where a mutation would otherwise require editing a real file (rows 4, 6, 7), pass the synthetic
    input through the check's own parameter rather than writing to disk. Structure each check as a
    small pure helper taking its inputs, with a thin test calling it on the real values — that is
    what makes the mutation possible without touching the repo.

    Finally, assert the whole suite is hermetic: after the module runs,
    `git diff --exit-code -- harness/ contracts/ tools/hooks/ .opencode/` is clean.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint -q && uv run pytest tools/hooks tools/deny_domains -q && git diff --exit-code -- harness/ contracts/ tools/hooks/ .opencode/</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint -q` exits 0 with all seven checks and all seven mutation proofs
    passing. `uv run pytest tools/hooks tools/deny_domains -q` exits 0 — no cross-suite state leak.
    `git diff --exit-code -- harness/ contracts/ tools/hooks/ .opencode/` is clean. `git status
    --porcelain` shows exactly one modified file, the test.
  </acceptance_criteria>
  <done>Every check has a mutation that makes it RED, run in-suite, with the tree provably unmodified afterwards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → hook module state | Mutations are `monkeypatch`-scoped or applied to copies, with `finally` restore; the hook suite is re-run to prove no leak. |
| Test suite → environment | `GOLDEN_APPROVE_HUMAN` / `HARNESS_DEV_BYPASS` are set only via `monkeypatch.setenv`; `os.environ[...]` assignment is forbidden and grepped for. |
| Declaration → runtime adapters | opencode side is a source parse, not an execution; the gap is stated in the test docstring and bounded by mutation row 7. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | Spoofing (false assurance) | a check that only re-reads the JSON | mitigate | Checks 5-7 drive behaviour or read the authoritative source; the module docstring forbids the static-read shortcut by name (ADR-0010:158-160, 28-RESEARCH P4). |
| T-30-12 | Spoofing (vacuous pass) | a matcher/regex that finds nothing and compares two empty sets | mitigate | Checks 6 and 7 assert a non-empty match before comparing, and fail loudly when the group or the guard chain is not found. |
| T-30-13 | Tampering (state leak) | monkeypatched module constants | mitigate | `finally` restore per `test_contract_guard.py:352-381`; `uv run pytest tools/hooks` re-run as a verify step; `git diff --exit-code` over four trees. |
| T-30-14 | Elevation of Privilege | an ambient `GOLDEN_APPROVE_HUMAN` in the developer's shell making check 5 pass vacuously | mitigate | Check 5 asserts the NO-token case FIRST and uses `monkeypatch.delenv(..., raising=False)` before it. This is the DEBT-04 / `DEF-05-02-1` failure shape (`REQUIREMENTS.md:71-74`) — do not reproduce it in a new suite. |
| T-30-15 | Repudiation | a mutation proof described but not run | mitigate | All seven mutations run in-suite as ordinary tests, not as prose in a SUMMARY. |
| T-30-SC | Tampering (supply chain) | package installs | accept | Zero new packages; `re`, `json`, `importlib` are stdlib. Package Legitimacy Gate: not applicable. |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint -q` exits 0 (7 checks + 7 mutation proofs).
- `uv run pytest tools/hooks tools/deny_domains -q` exits 0.
- `git diff --exit-code -- harness/ contracts/ tools/hooks/ .opencode/` is clean.
- `grep -n "os.environ\[" tools/harness_lint/tests/test_deny_domain_registry.py` returns nothing.
- `uv run ruff check tools/harness_lint && uv run ruff format --check tools/harness_lint` exit 0.
</verification>

<success_criteria>
- A hook constant, a bypass behaviour, a Claude matcher or an opencode adapter surface that drifts
  from the declaration turns the suite RED.
- Each of the seven classes is proven by a mutation that runs, not by a claim.
- The declaration and the hooks remain independently authored — no hook imports the registry.
- Nothing outside the one test file is modified.
</success_criteria>

<output>
Create `.planning/phases/30-deny-domain-registry-v2-4-a/30-03-SUMMARY.md` when done. It MUST list the
seven checks with their seven mutations and the test id of each proof, and record the pytest count.
</output>
