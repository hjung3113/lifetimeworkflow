---
phase: 30-deny-domain-registry-v2-4-a
plan: 04
type: execute
wave: 4
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - docs/doc-dependencies.toml
  - docs/.docs-review-ledger.toml
autonomous: false
requirements: [SEAL-01]

must_haves:
  truths:
    - "The full gate fan-in is green with the registry landed: `uv run pytest`, `tools.contract_drift.drift`, `check-jsonschema` over the new pair, `tools.harness_emit` round-trip with a clean `git diff`, `tools.docs_guard`, and the GEN-04 core-no-example guard"
    - "The emit round-trip is proven by RE-RUNNING the emitter and diffing, not by trusting that nothing changed — this phase touched no `harness/` source, so the diff must be EMPTY and an empty diff is the assertion"
    - "GEN-04 stays green: `contracts/harness/security/deny-domains.json` names no `examples/**` path, no instance module, and no instance glob — the core depends on no instance"
    - "A `[[binding]]` row binds the registry instance to `harness/skills/gate-model/SKILL.md` at `severity = \"required\"`, so the document that teaches the gate model owes a review when the declared domains change (D-16)"
    - "The resulting `first_seen-unratified` obligation is discharged by a HUMAN-authored ledger row — the agent drafts the exact row and STOPS; `ledger_guard` denies the write with no token and no dev bypass (ledger_guard.py:26-30), and that refusal is correct behaviour to be demonstrated, not worked around"
    - "No `HARNESS_DEV_BYPASS` and no fabricated `GOLDEN_APPROVE_HUMAN` appears anywhere in this phase's execution, for any file"
    - "SEAL-01's five declared fields are each traceable to a live artifact, and the traceability is recorded in the SUMMARY as a table, not asserted"
  artifacts:
    - path: "docs/doc-dependencies.toml"
      provides: "one new [[binding]] row: the deny-domain registry -> harness/skills/gate-model/SKILL.md, severity required"
      contains: "deny-domain-registry-gate-model"
    - path: "docs/.docs-review-ledger.toml"
      provides: "the human-authored disposition row discharging the new binding (HUMAN WRITE ONLY — agent drafts, human lands)"
      contains: "deny-domain-registry-gate-model"
  key_links:
    - from: "contracts/harness/security/deny-domains.json"
      to: "harness/skills/gate-model/SKILL.md"
      via: "docs/doc-dependencies.toml [[binding]] — the declared domains and the doc that teaches them cannot drift apart silently"
      pattern: "deny-domain-registry-gate-model"
    - from: "docs/doc-dependencies.toml"
      to: "docs/.docs-review-ledger.toml"
      via: "first_seen-unratified — a registry row changes what is WATCHED; only a human ledger row changes what is GREEN (ADR-0010 clause 3b)"
      pattern: "deny-domain-registry-gate-model"
---

<objective>
Close the phase: prove every gate the registry touches is green, bind the declaration to the
document that teaches it, and take the resulting review obligation through the human path rather
than around it.

Purpose: plans 30-01..03 landed a declaration and a gate. Two things remain unproven — that the
emitted runtime surface and the GEN-04 core/instance split are untouched, and that the declaration
is *obliged* to stay in sync with the prose that describes the gate model. A control declaration no
document owes a review to is the shape this milestone exists to stop.

Output: a verified fan-in, one proposed registry binding, and one drafted-then-human-landed ledger
disposition.
</objective>

<execution_context>
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-deny-domain-registry-v2-4-a/30-CONTEXT.md
@.planning/phases/30-deny-domain-registry-v2-4-a/30-RESEARCH.md
@.planning/REQUIREMENTS.md
@docs/doc-dependencies.toml
@harness/skills/gate-model/SKILL.md
@tools/hooks/ledger_guard.py
@docs/adr/0010-human-docs-review-obligation-model.md

<interfaces>
<!-- VERIFIED against source in this session. -->

The full fan-in, in run order:

    uv run pytest -q                                                   # whole suite
    uv run python -m tools.contract_drift.drift                        # ci.yml:133
    uv run check-jsonschema --schemafile contracts/harness/security/deny-domains.schema.json \
                            contracts/harness/security/deny-domains.json      # ci.yml:108-127
    uv run python -m tools.harness_emit && git diff --exit-code         # ci.yml:218 emit-drift
    uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index \
      && git diff --exit-code -- docs/reference .memory/derived          # ci.yml:227 stale-derived
    uv run python -m tools.docs_guard                                   # ci.yml:308
    uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q # GEN-04

`docs/doc-dependencies.toml` row shape (read the file, do not retype from memory): `id`, `sources`
(array), `target`, `severity` (`required` | `advisory`), `dispositions`. The `dispositions` value for
a NON-adr target is `["updated", "reviewed-no-change"]`; the forced ADR pair applies only to
`docs/adr/**` targets (the `contract-graph-adr-0009` row is the example).

`tools/hooks/ledger_guard.py:26-30,48` — `docs/.docs-review-ledger.toml` is denied to an agent write
and honours NO token: not `GOLDEN_APPROVE_HUMAN`, not `HARNESS_DEV_BYPASS`. A `/adopt`-style write
hits the same refusal at `tools/adoption_apply/apply.py:227`. There is no legitimate agent path.

Ledger row shape — DO NOT restate it here or in a draft literal. Read
`tools/docs_guard/ledger.py::_ROW_KEYS` when authoring the draft and build the row from that
frozenset; a hardcoded key list goes stale the moment the ledger schema moves (the 29-02 CR-03
lesson).

`tools/docs_guard/cli.py:72-107` — reason strings. A new binding with no ledger row reports
`first_seen-unratified`; the guard exits 1. That red is EXPECTED between Task 2 and Task 3 and must
be reported as such, never suppressed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate fan-in — prove the emit round-trip and GEN-04 are untouched</name>
  <files></files>
  <read_first>this plan's `<interfaces>` block</read_first>
  <action>
    Run the fan-in in the order given in `<interfaces>`, capturing each exit code. Two results are
    load-bearing and must be reported explicitly rather than as "all green":

    - **emit-drift.** This phase edited nothing under `harness/`, so re-running
      `uv run python -m tools.harness_emit` must leave `git diff` EMPTY. A non-empty diff means the
      emitter's output depends on something this phase changed — investigate before proceeding; do
      not commit the emitted delta as if it were intended.
    - **`contract-check`.** Confirm the job now emits a real `check-jsonschema:` line for the new
      pair and no longer prints the VISIBLE SKIP (`ci.yml:121-123`). Record the before/after; this is
      the first schema/instance pair in the repo and the transition should be visible in the record.

    Also confirm GEN-04: `grep -n "examples/" contracts/harness/security/deny-domains.json` returns
    nothing, and `test_core_no_example_dep.py` passes.

    Do not fix anything found here inside this task — a failure is a finding that routes back to the
    owning plan.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run python -m tools.contract_drift.drift && uv run check-jsonschema --schemafile contracts/harness/security/deny-domains.schema.json contracts/harness/security/deny-domains.json && uv run python -m tools.harness_emit && git diff --exit-code && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q</automated>
  </verify>
  <acceptance_criteria>
    Every command above exits 0. `git diff --exit-code` after `tools.harness_emit` is clean.
    `grep -n "examples/" contracts/harness/security/deny-domains.json` returns nothing.
    `uv run python -m tools.docs_guard` exits 0 at this point (no binding proposed yet).
  </acceptance_criteria>
  <done>The whole suite, drift, contract-check, the emit round-trip, stale-derived and GEN-04 are green with the registry landed, and the contract-check SKIP→validate transition is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Propose the registry's review binding — and let the guard go red</name>
  <files>docs/doc-dependencies.toml</files>
  <read_first>
    docs/doc-dependencies.toml (full — the row style and the pairing rule in its header comment),
    harness/skills/gate-model/SKILL.md (confirm it is the document that teaches the deny domains)
  </read_first>
  <action>
    Add ONE `[[binding]]` row, placed with the other `required` rows:

        id           = "deny-domain-registry-gate-model"
        sources      = ["contracts/harness/security/deny-domains.json"]
        target       = "harness/skills/gate-model/SKILL.md"
        severity     = "required"
        dispositions = ["updated", "reviewed-no-change"]

    The registry file header states the pairing rule: a binding whose sources do not genuinely
    determine its target is noise that trains reviewers to rubber-stamp. This row satisfies it — the
    skill's whole subject is what is gated and how, and the registry is now the enumeration of
    exactly that. Add a short comment above the row saying so, in the style of the existing
    `lifecycle-eval-shadow-metrics` comment.

    Do NOT add a second row for the schema: the schema constrains shape, the instance carries the
    claims, and a binding on both would fire twice for one change.

    Then run `uv run python -m tools.docs_guard`. It MUST exit 1 with `first_seen-unratified` for the
    new binding. **That red is the correct outcome of this task**, not a failure to fix: an agent
    extending coverage can only ever create an obligation for itself, never discharge one (ADR-0010
    clause 3b). Capture the exact guard output for Task 3.
  </action>
  <verify>
    <automated>uv run python -m tools.docs_guard; test $? -eq 1 && uv run python -m tools.docs_guard 2>&1 | grep -q "first_seen-unratified"</automated>
  </verify>
  <acceptance_criteria>
    `docs/doc-dependencies.toml` has exactly one new row. `uv run python -m tools.docs_guard` exits 1
    and names `deny-domain-registry-gate-model` with reason `first_seen-unratified`. No file under
    `docs/.docs-review-ledger.toml` was created or modified. `git status --porcelain` shows exactly
    one modified file.
  </acceptance_criteria>
  <done>The binding is proposed, the guard is red for exactly the right reason, and the agent has not attempted to discharge its own obligation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Human ratification — the ledger disposition for the new binding</name>
  <files>docs/.docs-review-ledger.toml</files>
  <read_first>
    Task 2's captured `tools.docs_guard` output,
    tools/docs_guard/ledger.py::_ROW_KEYS (build the draft row from this frozenset, never from memory),
    harness/skills/gate-model/SKILL.md
  </read_first>
  <action>
    Present, and STOP. The agent drafts; it does not write. `ledger_guard` will refuse an agent write
    to `docs/.docs-review-ledger.toml` with no token and no dev bypass — demonstrate that refusal once
    (attempt the write, show the deny reason, do not retry) so the control is shown working rather
    than assumed, then hand the drafted row to the human.

    Present in this order:

    1. `harness/skills/gate-model/SKILL.md` as it stands, alongside
       `contracts/harness/security/deny-domains.json`. Ask the human the only question that matters:
       does the skill still describe the gate model accurately now that the three domains, their
       bypass semantics and their uncovered `Bash` surface are enumerated in one place?
    2. The two candidate dispositions, with the honest recommendation:
       - `updated` — if the skill needs a pointer to the registry and a statement that the bash
         surface is uncovered pending SEAL-02/03. **Recommended**: the skill currently teaches the
         domains in prose, and the registry is now the enumeration it should point at.
       - `reviewed-no-change` — if the human judges the skill already accurate.
       The agent MAY author the SKILL.md edit if `updated` is chosen (it is agent-writable prose, not
       constitution plane) and must then re-run `tools.harness_emit` so `.opencode/` and `.claude/`
       round-trip, plus `git diff --exit-code` to prove the emit is clean.
    3. The drafted ledger row, with every key read from `_ROW_KEYS` and every digest taken from the
       guard's own output — never computed by hand, never pasted from the live digest without the
       human having actually reviewed (the anti-rubber-stamp control at ADR-0010 clause 2 is
       specifically designed to catch that).

    On the human's resume-signal, they land the ledger row. The agent then re-runs the fan-in.
  </action>
  <what-built>
    A review obligation binding the deny-domain registry to `harness/skills/gate-model/SKILL.md`, and
    its first disposition. If `updated` is chosen, also a bounded edit to that skill pointing at the
    registry as the enumeration of the deny domains and naming the still-uncovered `Bash` surface,
    round-tripped through `tools.harness_emit` into both runtime trees.
  </what-built>
  <how-to-verify>
    BLOCKING human checkpoint. The agent does NOT author the ledger row.

    1. Confirm the demonstrated `ledger_guard` refusal names no token as a way through — the deny
       reason should state that no token applies and that a human edits the file directly.
    2. Confirm the drafted row's keys match `tools/docs_guard/ledger.py::_ROW_KEYS` exactly.
    3. Confirm the disposition chosen matches what actually happened: `updated` only if the skill was
       edited, `reviewed-no-change` only if it genuinely needed none.
    4. After landing the row:

           uv run python -m tools.docs_guard            # must exit 0
           uv run python -m tools.harness_emit && git diff --exit-code
           uv run pytest -q
           uv run python -m tools.contract_drift.drift

    5. Confirm no `HARNESS_DEV_BYPASS` or fabricated `GOLDEN_APPROVE_HUMAN` was used anywhere in this
       phase, for any file.
  </how-to-verify>
  <verify>
    <automated>uv run python -m tools.docs_guard && uv run python -m tools.harness_emit && git diff --exit-code && uv run pytest -q</automated>
  </verify>
  <resume-signal>Reply "ratified" once the ledger row is landed, or state which disposition you want and any skill edit needed first.</resume-signal>
  <done>The binding has a human-authored disposition, docs-guard exits 0, the emit round-trip is clean, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → `docs/doc-dependencies.toml` | Agent-writable by design: a registry row changes what is WATCHED (ADR-0010 clause 3b). |
| Agent → `docs/.docs-review-ledger.toml` | DENIED, no token, no dev bypass (`ledger_guard.py:26-30`, `apply.py:227`). Only a human authors a disposition. |
| Agent → `harness/skills/gate-model/SKILL.md` | Agent-writable prose; the emitted `.opencode/`/`.claude/` copies are generated and must round-trip cleanly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-16 | Elevation of Privilege (self-blessing) | the new binding's first disposition | mitigate | Task 3 is a blocking human checkpoint; the agent demonstrates the `ledger_guard` refusal rather than seeking a way past it. |
| T-30-17 | Spoofing (rubber-stamp) | a `reviewed-no-change` row pasted from the live digest | mitigate | The disposition is chosen by the human after reading the skill against the registry; ADR-0010 clause 2's disposition/digest coherence check is the machine backstop. |
| T-30-18 | Repudiation | a red `docs-guard` suppressed rather than reported | mitigate | Task 2's acceptance criteria REQUIRE exit 1 with `first_seen-unratified`. A green guard at that point would mean the binding did not take effect. |
| T-30-19 | Tampering | an unintended emit delta committed as if intended | mitigate | Task 1 asserts an EMPTY `git diff` after re-running the emitter, since this phase touches no `harness/` source; Task 3 re-asserts it after any skill edit. |
| T-30-20 | Spoofing (false coverage) | an `examples/**` reference leaking into the core registry | mitigate | Explicit grep plus `test_core_no_example_dep.py` (GEN-04) in Task 1. |
| T-30-SC | Tampering (supply chain) | package installs | accept | Zero new packages. Package Legitimacy Gate: not applicable. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0.
- `uv run python -m tools.contract_drift.drift` exits 0.
- `uv run check-jsonschema --schemafile contracts/harness/security/deny-domains.schema.json contracts/harness/security/deny-domains.json` exits 0.
- `uv run python -m tools.harness_emit && git diff --exit-code` is clean.
- `uv run python -m tools.docs_guard` exits 0 after Task 3 (and exits 1 between Tasks 2 and 3, by design).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0.
- `git log -p -1 --stat` shows no `HARNESS_DEV_BYPASS` usage anywhere in the phase.
</verification>

<success_criteria>
- Every gate the registry touches is green, and the emit round-trip is proven empty rather than assumed.
- The declaration is bound to the document that teaches the gate model, at `required` severity.
- The obligation the binding created was discharged by a human, and the agent's inability to
  discharge it was demonstrated live.
- SEAL-01's five fields — owner, bypass semantics, covered tool surfaces, runtime adapters, plus the
  uncovered surface that makes the bash gap data — are each traceable to a live artifact, recorded as
  a table in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/30-deny-domain-registry-v2-4-a/30-04-SUMMARY.md` when done. It MUST contain:
the fan-in table with exit codes; the `contract-check` SKIP→validate before/after; the verbatim
`ledger_guard` deny reason from the demonstrated refusal; the disposition chosen and by whom
(role, not identity); and the SEAL-01 field→artifact traceability table.
</output>
