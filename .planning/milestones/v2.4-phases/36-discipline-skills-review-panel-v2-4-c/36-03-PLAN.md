---
phase: 36-discipline-skills-review-panel-v2-4-c
plan: 03
type: execute
wave: 3
depends_on: [36-01, 36-02]

files_modified:
  - harness/skills/clarify/SKILL.md
  - harness/skills/test-driven-change/SKILL.md
  - harness/skills/diagnose/SKILL.md
  - harness/skills/domain-modeling/SKILL.md
  - harness/skills/adversarial-review-panel/SKILL.md
  - harness/skills/adversarial-review-panel/references/panel-seat.schema.json
  - harness/commands/discipline.md
  - tools/harness_lint/caps.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_lint/tests/test_discipline_wiring.py
  - .opencode/**, .claude/** (emitted)
autonomous: true
requirements: [LANE-01, LANE-02]

must_haves:
  truths:
    - "Five skills exist, one per declared discipline, each with a DISJOINT routing description that passes the shipped skill-cap gate (name regex, ≤64/≤1024, no reserved vendor word, no angle brackets, a routing trigger token)"
    - "The panel skill dispatches through the EXISTING fan-out substrate and the read-only explorer persona — it defines no new dispatcher and no new persona, and says so"
    - "Every discipline named in harness/risk-policy.toml has a declaration in harness/disciplines.toml, and every declaration's skill is a real directory under harness/skills/ — a drift test FAILS otherwise, proven by a mutation"
    - "EXPECTED_SKILLS moves 13 -> 18 and the command count 25 -> 26 in the SAME change as the authored files"
    - "The emit round-trip leaves `git status --porcelain` EMPTY including untracked files — verified by running the emitter, not assumed"
    - "The panel seats are DATA in harness/disciplines.toml, not prose in the skill body — changing min_experts changes what the gate requires without editing a skill"
  artifacts:
    - path: "harness/skills/adversarial-review-panel/SKILL.md"
      provides: "the STRICT+ multi-seat adversarial review procedure over the fan-out substrate"
      contains: "fan-out-synthesize"
    - path: "tools/harness_lint/tests/test_discipline_wiring.py"
      provides: "the policy <-> declaration <-> skill drift gate and its mutation proof"
      contains: "disciplines.toml"
  key_links:
    - from: "harness/skills/adversarial-review-panel/SKILL.md"
      to: "harness/skills/fan-out-synthesize/SKILL.md"
      via: "explicit reuse instruction, no second dispatcher"
      pattern: "fan-out-synthesize"
    - from: "tools/harness_lint/tests/test_discipline_wiring.py"
      to: "harness/risk-policy.toml"
      via: "every policy-named discipline resolves to a declaration and a skill directory"
      pattern: "required_disciplines"
---

<objective>
Author the discipline surface — five skills and one thin command — and gate it so the declaration
and the authored surface cannot drift apart.

Purpose: plans 01–02 made the requirement refusable. This plan supplies the *method* an agent
follows to satisfy it, in the runtime-neutral `harness/` source, emitted to both runtimes.

Output: five SKILL.md files, one command, updated emit counters, one drift test, and the emitted
trees.
</objective>

<execution_context>
Atomic commits, `uv run` everywhere, no model identifier in any artifact.
</execution_context>

<context>
@.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-CONTEXT.md
@.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-RESEARCH.md
@harness/skills/fan-out-synthesize/SKILL.md
@harness/skills/docs-upkeep/SKILL.md
@harness/commands/docs-update.md
@tools/harness_lint/caps.py
@tools/harness_lint/tests/test_skills.py
@tools/harness_lint/tests/test_docs_update_wiring.py

<interfaces>
<!-- VERIFIED against shipped code this session. -->

Skill caps (tools/harness_lint/caps.py):
```python
_NAME_MAX = 64 ; _DESC_MAX = 1024 ; _BODY_WARN_LINES = 500
_NAME_RE = re.compile(r"^[a-z0-9]+(-[a-z0-9]+)*$")
_RESERVED_WORDS = ("anthropic", "claude")     # banned from name AND description
_XML_CHARS = ("<", ">")                        # banned from name AND description
EXPECTED_SKILLS = frozenset({...13 entries...})   # tools/harness_emit/validate.py:182 asserts EQUALITY
```
`test_skills.py` additionally requires a routing-trigger token in each description and that all
descriptions be mutually distinct.

Command gate (`tools/harness_lint/tests/test_commands.py:34`): the description must literally
contain "use" or "when".

Command count (`tools/harness_emit/tests/test_coexist.py:39,65,66`): the literal `25` appears in the
test NAME, the docstring and two assertions.

Emitter: `uv run python -m tools.harness_emit` projects `harness/` to `.opencode/` + `.claude/`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: The four method skills</name>
  <files>harness/skills/{clarify,test-driven-change,diagnose,domain-modeling}/SKILL.md</files>
  <behavior>
    Each skill is a short procedure (well under the 500-line warn cap; target ~60-110 lines) that
    ends by naming exactly what its discipline record must contain, so the skill and the checker
    agree by construction:

    - **clarify** — surface the ambiguity before building. Enumerate the unknowns, decide which are
      load-bearing, resolve them from the repo or escalate them as questions, and record the
      decisions. Record outputs: the written decision list.
    - **test-driven-change** — the failing test comes first. Write the adversarial-input table, run
      it RED against unmodified code, record the RED output, then implement, then re-run. Explicitly
      names the repo's standing rule that a control-shaped change without a demonstrable failure
      mode is not evidence.
    - **diagnose** — cause before fix. Reproduce deterministically, bisect the hypothesis space,
      name the mechanism, and only then choose the repair. Points at `golden-debug` for the seven
      canonicalization axes rather than restating them.
    - **domain-modeling** — settle the vocabulary before the schema. Nouns, their invariants, and
      the boundary at which each is authoritative; points at `docs/glossary.md` as the gated home of
      ubiquitous language and at `data-contracts` for the contract mechanics.

    Each description must route on a situation ("Use when …"), not read as a label, and must be
    distinct from every other skill's.
  </behavior>
  <action>Author the four files. Do NOT restate the §4.3-4.6 invariants, the gate model, or the
  contract rules — point at the skill that owns each. Cross-reference by skill name so the routing
  graph stays navigable.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_skills.py -q</automated>
  </verify>
  <done>All four pass the cap gate with disjoint routing descriptions.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: The adversarial review panel skill (LANE-02)</name>
  <files>harness/skills/adversarial-review-panel/SKILL.md, harness/skills/adversarial-review-panel/references/panel-seat.schema.json</files>
  <behavior>
    The STRICT+ panel procedure, built ON the fan-out substrate:

    1. **Seats, not opinions.** Each seat is a distinct adversarial frame with its own question —
      e.g. contract/boundary, security & gating, failure modes & rollback, evidence integrity,
      simplicity/altitude. A seat's value is the frame it refuses to leave; three seats reading the
      same diff with the same question is one opinion typed three times.
    2. **Dispatch is the runtime's own subtask affordance** to the read-only `explorer` persona,
      exactly as `fan-out-synthesize` §2 prescribes. NO new dispatcher, NO new persona.
    3. **Returns are schema-bounded** — `references/panel-seat.schema.json`: `expert`, `verdict`
      (`pass` | `concerns` | `block`), `findings[]` each with a claim and citations.
    4. **Landing.** Every finding a seat raises is written into the packet's `evidence.json`
      `findings` (severity + disposition), and the panel record cites those ids. A `block` verdict
      with an open blocker/major finding cannot reach COMPLETE — the shipped
      `_has_unresolved_major_finding` already refuses it.
    5. **Minimum seats and verdict vocabulary are DATA** in `harness/disciplines.toml`, not prose
      here. The skill points at the declaration; it does not restate the number.

    The body states plainly that `/review` (one `code-reviewer` seat) is not superseded — it remains
    the single-seat entry point, and the panel is the STRICT+ lane requirement.
  </behavior>
  <action>Author both files. The schema is a `references/` payload, not a contract — Draft 2020-12,
  `additionalProperties: false`, modelled on
  `harness/skills/fan-out-synthesize/references/fan-out-return.schema.json`. Keep the seat list an
  illustrative default, explicitly extensible, so Phase 37's capability routing can replace the
  naming without rewriting the procedure.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_skills.py -q</automated>
  </verify>
  <done>The panel skill passes the cap gate and contains no second dispatch engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The thin command, the counters, and the drift gate</name>
  <files>harness/commands/discipline.md, tools/harness_lint/caps.py, tools/harness_emit/tests/test_coexist.py, tools/harness_lint/tests/test_discipline_wiring.py</files>
  <behavior>
    - `/discipline` is a THIN command over `uv run python -m tools.discipline` with the 0/1/3 exit
      routing spelled out; the procedure lives in the skills. Its description contains "use" or
      "when" (a live gate).
    - `EXPECTED_SKILLS` 13 → 18 with a comment recording WHY these five (the anti-sprawl note),
      matching the existing comment style.
    - `test_coexist.py`'s command count 25 → 26 in the test name, docstring and both assertions.
    - `test_discipline_wiring.py` asserts: every discipline in `harness/risk-policy.toml`
      `required_disciplines` (all lanes) has a `[discipline.<id>]` block in `harness/disciplines.toml`;
      every declaration's `skill` is a real `harness/skills/<name>/SKILL.md`; every declaration's
      `owed_by_phase` is a phase in the transitions contract; and the reverse — a declaration nobody
      requires is flagged, so a dead discipline cannot accumulate.
    - Mutation proof: with an extra discipline injected into a copied policy, the check FAILS. The
      test must operate on a copy, never on the real files.
  </behavior>
  <action>RED first for the wiring test: assert against a deliberately broken copy before the real
  files satisfy it, and record the RED output. Then land the counters and command in the same
  commit as the authored surface, because a skill directory without its `EXPECTED_SKILLS` entry
  reds the emit validator immediately.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint tools/harness_emit -q</automated>
  </verify>
  <done>The drift gate fails on a mutated policy and passes on the real one; counters moved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Emit round-trip to both runtimes</name>
  <files>.opencode/**, .claude/**, AGENTS.md, CLAUDE.md (emitter-managed blocks)</files>
  <behavior>The authored surface appears in BOTH runtime trees and re-running the emitter is a
  no-op.</behavior>
  <action>Run `uv run python -m tools.harness_emit`. Then run `git status --porcelain` — it must show
  the newly emitted files as expected additions, and after committing them a SECOND emitter run must
  leave it EMPTY. Use `git status --porcelain`, not bare `git diff`: `git diff` is blind to
  untracked files and would pass green on a wrong tree (the 29 D-03 blind spot).

  The emitter also rewrites the HARNESS-MANAGED blocks in `AGENTS.md` / `CLAUDE.md`. Do not
  hand-edit inside those fences; commit whatever the emitter produced.</action>
  <verify>
    <automated>uv run python -m tools.harness_emit && git status --porcelain</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run python -m tools.ruff_baseline</automated>
  </verify>
  <done>Second emitter run is a no-op with an empty porcelain; full suite and ruff baseline green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored `harness/` source -> emitted runtime trees | a hand-edit in the emitted tree is silently reverted |
| declaration -> authored skill | a requirement naming a skill that does not exist is a claimed control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11 | Spoofing | a lane requires a discipline whose skill does not exist | mitigate | the wiring drift gate, with a mutation proof |
| T-36-12 | Tampering | a hand-edit inside an emitted tree | mitigate | emit round-trip verified with untracked-visible porcelain |
| T-36-13 | Repudiation | a dead discipline nobody requires accumulates | mitigate | the reverse direction of the drift gate |
| T-36-14 | Elevation of Privilege | the panel skill grows its own dispatcher with write powers | mitigate | dispatch is the runtime affordance to the read-only `explorer`; stated in the body and asserted by the wiring test's substrate reference |
| T-36-SC | Tampering | dependency install | accept | none added |
</threat_model>

<verification>
- `uv run pytest -q` green.
- `uv run pytest tools/harness_lint -q` green.
- `uv run python -m tools.harness_emit && git status --porcelain` empty on the second run.
- `uv run python -m tools.ruff_baseline` exits 0.
</verification>

<success_criteria>
Five disjoint discipline skills and one thin command exist in the runtime-neutral source, are
emitted byte-identically to both runtimes, and cannot drift from the policy that requires them.
</success_criteria>

<output>
Create `.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-03-SUMMARY.md` when done.
</output>
