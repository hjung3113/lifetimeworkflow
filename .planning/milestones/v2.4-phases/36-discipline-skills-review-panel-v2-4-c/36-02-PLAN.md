---
phase: 36-discipline-skills-review-panel-v2-4-c
plan: 02
type: execute
wave: 2
depends_on: [36-01]

files_modified:
  - tools/task_control/manager.py
  - tools/task_control/phase_gate.py
  - tools/task_control/__main__.py
  - tools/discipline/__main__.py
  - tools/task_control/tests/test_discipline_enforcement.py
autonomous: true
requirements: [LANE-01, LANE-02]

must_haves:
  truths:
    - "A STRICT task attempting PLAN -> EXECUTE without a clarify record FAILS with a deterministic error naming the missing discipline — this is the phase's headline demonstration and its exact invocation and output go in the SUMMARY verbatim"
    - "A STRICT task attempting REVIEW -> VERIFY without an adversarial-review-panel record FAILS (LANE-02: the panel is a declared lane requirement, refused when absent)"
    - "The same transitions SUCCEED once a valid record exists — the gate is not 'always red', proven by a positive control per refusal"
    - "A FAST task is unaffected: it owes no discipline and every FAST transition behaves exactly as before"
    - "phase_gate() reports an unsatisfied discipline in its refresh list, so a RESUMED session is refused too, not only a fresh transition"
    - "The refusal is proven LOAD-BEARING by mutation: with the lane's required_disciplines emptied, the same transition succeeds"
    - "A panel record whose only distinguishing feature is duplicate expert seats does NOT satisfy the panel — 3 identical seats is one opinion typed three times"
  artifacts:
    - path: "tools/task_control/tests/test_discipline_enforcement.py"
      provides: "the refusal cases, positive controls, the FAST no-op control, the resume refusal, and the mutation proof"
      contains: "required_disciplines"
  key_links:
    - from: "tools/task_control/manager.py::transition"
      to: "tools/discipline/check.py::missing_disciplines"
      via: "raise TaskControlError before any state write"
      pattern: "missing_disciplines"
    - from: "tools/task_control/phase_gate.py::phase_gate"
      to: "tools/discipline/check.py::missing_disciplines"
      via: "extend the refresh list"
      pattern: "discipline:"
---

<objective>
Turn the declaration into a refusal. After this plan, a lane's declared discipline is something the
lifecycle machinery **fails on** — the exact property LANE-01 says is missing and LANE-02 asks for
on the panel specifically.

Purpose: a skill that merely exists is advisory. The test that matters is a command that fails.

Output: two call sites, a CLI surface, and the enforcement test file.
</objective>

<execution_context>
Atomic commits, `uv run` everywhere, no model identifier in any artifact, `HARNESS_DEV_BYPASS`
unused.
</execution_context>

<context>
@.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-CONTEXT.md
@.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-01-SUMMARY.md
@tools/task_control/manager.py
@tools/task_control/phase_gate.py
@tools/task_control/__main__.py
@tools/task_control/tests/test_task_control.py

<interfaces>
<!-- VERIFIED against shipped code this session. -->

From tools/task_control/manager.py
```python
class TaskControlError(ValueError): ...
def transition(task_dir, target, expected_revision, *, next_action=None, current_ref=None) -> dict
    # order today: blockers -> _required_artifacts -> is_transition_allowed -> BLOCKED guard
    #           -> required_artifacts_for_phase/missing_artifacts -> evidence (VERIFY/COMPLETE)
    #           -> COMPLETE-only checks -> _cas_write
def missing_artifacts(task_dir, required=None) -> list[str]
```

From tools/task_control/phase_gate.py
```python
def phase_gate(task_dir, expected_revision, *, repo_root=None, baseline=None,
               prohibited_actions=None, publication_parent=None) -> list[str]
    # accumulates `refresh`; raises TaskControlError("refresh required: " + ", ".join(sorted(set(refresh))))
```
Reuse the existing `tools/task_control/tests/` packet-builder fixtures — do NOT build a second task
packet factory. Read the existing conftest/helpers first and extend them if a discipline record
helper is needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refuse the transition</name>
  <files>tools/task_control/manager.py, tools/task_control/tests/test_discipline_enforcement.py</files>
  <behavior>
    In `transition()`, after the artifact check and before the evidence check, compute
    `missing_disciplines(task_dir, target)` and raise
    `TaskControlError(f"missing required disciplines: {', '.join(missing)}")` when non-empty.

    Placement rationale to record in the summary: after artifacts (a task missing its spec should
    hear about the spec first) and before evidence (evidence is the heavier check and a
    discipline-less task will never satisfy it anyway). The check must run BEFORE `_cas_write`, so a
    refused transition leaves state and revision untouched — assert that.
  </behavior>
  <action>RED first. Write the refusal cases and run them against unmodified `manager.py`, recording
  the RED output. Then implement.

  Cases: STRICT PLAN→EXECUTE without `clarify` refuses; STRICT REVIEW→VERIFY without the panel
  refuses; each has a positive control that succeeds once the record is written; a refused
  transition leaves `show(task_dir)["revision"]` and `["phase"]` unchanged; FAST INTAKE→EXECUTE
  succeeds with no discipline record at all; a panel record with 3 reviews but 2 distinct experts
  refuses; and the mutation proof — monkeypatch the loaded policy so STRICT's
  `required_disciplines` is `[]` and assert the same transition now succeeds.</action>
  <verify>
    <automated>uv run pytest tools/task_control/tests/test_discipline_enforcement.py -q</automated>
  </verify>
  <done>Every refusal has a positive control and the mutation flips the outcome; no state is written
  on a refusal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refuse the resume, and expose the CLI</name>
  <files>tools/task_control/phase_gate.py, tools/task_control/__main__.py, tools/discipline/__main__.py, tools/task_control/tests/test_discipline_enforcement.py</files>
  <behavior>
    - `phase_gate()` extends `refresh` with `f"discipline: {item}"` for each missing discipline at
      the CURRENT phase. A resumed session with an unsatisfied discipline is refused with the same
      deterministic message shape as every other refresh reason.
    - `python -m tools.discipline <task_dir> [--phase PHASE]` prints the owed disciplines and their
      status and exits 0 when nothing is missing, 1 when something is, 3 on a malformed declaration
      or policy (`DisciplineError`). This mirrors the 0/1/3 convention `tools.docs_guard` already
      uses — do not invent a fourth convention.
    - `tools/task_control/__main__.py` gains no new subcommand; the discipline surface is its own
      module so the task-control CLI's argument surface is unchanged.
  </behavior>
  <action>Add the phase_gate case and the CLI exit-code cases to the same test file. Assert the exit
  codes by invoking the module's `main(argv)` directly, not by shelling out.

  NOTE for the summary: `tools/task_control/phase_gate.py` is a source of the `task-control-cli-howto`
  docs binding. Plan 04 owns the target-document edit and the drafted ledger row; do not edit
  `docs/how-to/task-lifecycle.md` here.</action>
  <verify>
    <automated>uv run pytest tools/task_control tools/discipline -q</automated>
  </verify>
  <done>Resume is refused for an unsatisfied discipline; the CLI's three exit codes are asserted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: The demonstration, recorded</name>
  <files>—</files>
  <behavior>One reproducible invocation that fails because a lane's declared discipline is
  unsatisfied, captured verbatim.</behavior>
  <action>Build a real STRICT task packet under a scratch directory, run the transition, and capture
  the exact command and the exact error text. Put both in the SUMMARY under a "Demonstration"
  heading. Then run the full suite and the ruff baseline.</action>
  <verify>
    <automated>uv run pytest -q</automated>
    <automated>uv run python -m tools.ruff_baseline</automated>
  </verify>
  <done>The SUMMARY carries a copy-pasteable failing invocation and its output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-driven transition -> state file | the CAS write is the point of no return |
| resumed session -> in-flight task | a fresh session must not inherit an unearned green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Elevation of Privilege | a transition proceeds despite an unsatisfied discipline | mitigate | the check raises before `_cas_write`; asserted by an unchanged revision after a refusal |
| T-36-07 | Repudiation | a resumed session skips the check a fresh transition would have made | mitigate | `phase_gate()` carries the same check into the resume path |
| T-36-08 | Tampering | a test passes because the control is absent | mitigate | the mutation proof empties `required_disciplines` and shows the transition succeed |
| T-36-09 | Spoofing | three identical seats presented as a panel | mitigate | distinct-expert assertion, with its own case |
| T-36-10 | Denial of Service | the gate becomes always-red and the lifecycle is unusable | mitigate | a positive control per refusal, plus the FAST no-op control |
| T-36-SC | Tampering | dependency install | accept | none added |
</threat_model>

<verification>
- `uv run pytest -q` green.
- `uv run python -m tools.ruff_baseline` exits 0.
- `uv run python -m tools.contract_drift` clean.
- The failing demonstration is reproducible from the SUMMARY's transcript.
</verification>

<success_criteria>
A declared discipline is refusable at both lifecycle choke points, every refusal has a positive
control, and neutralizing the policy entry demonstrably flips the outcome.
</success_criteria>

<output>
Create `.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-02-SUMMARY.md` when done.
</output>
