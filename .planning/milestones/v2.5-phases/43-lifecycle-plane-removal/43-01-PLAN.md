---
phase: 43-lifecycle-plane-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/commands/checkpoint.md
  - harness/commands/orient.md
  - harness/commands/review.md
  - harness/commands/verify-work.md
  - harness/agents/orchestrator.md
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-01: the five surviving artifacts (checkpoint, orient, review, verify-work, orchestrator) no longer shell any module the plane deletes"
    - "D-02: repair happens before deletion — this plan's commit lands before any package/skill/contract/CI deletion in this phase"
    - "D-03: repair means removing the lifecycle step, not replacing it — no successor mechanism, no shim, no comment-only stub is added in place of the removed step"
    - "D-15: every repair is confirmed by RUNNING the affected command/test, not by reading the diff — each of Phases 41 and 42 found consumers its plan had not listed, and every one surfaced from an actual run"
    - "W-1: the four harness/** files this plan edits are each covered by a LIVE-TREE gate test with hardcoded content assertions (test_orchestrator_topology.py, test_context_budget_wiring.py, test_checkpoint_command.py) — each task runs those tests as an acceptance criterion, because an over-broad deletion here goes red at RUNTIME and pytest --collect-only cannot see it"
    - "W-4 (phase-wide RULE, stated in <emit_snapshot_rule>): every commit that edits anything under harness/agents/, harness/commands/, harness/skills/ or harness/opencode.json regenerates tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr in THAT SAME commit. BOTH tasks of this plan trigger the rule — the .ambr renders command and agent BODIES verbatim, not merely their presence"
    - "Every task in this plan ends fully green. This phase sanctions no expected-red window; an executor told to amend-if-red must be able to treat any red as a real defect"
  artifacts:
    - path: "harness/commands/checkpoint.md"
      provides: "Step 1 no longer invokes tools.handoff generate/validate/activate; commit step keeps only the state-plane git add/commit"
    - path: "harness/commands/orient.md"
      provides: "Section 1 (active-handoff resume barrier + /phase-gate routing) removed; read-order step 5 no longer lists a deleted skill"
    - path: "harness/commands/review.md"
      provides: "Step 4 no longer invokes tools.evidence.capture add-finding; finding capture prose removed"
    - path: "harness/commands/verify-work.md"
      provides: "The five-gate list and its evidence-capture wrapper paragraph no longer reference tools.evidence.capture"
    - path: "harness/agents/orchestrator.md"
      provides: "Routes by declared topology/stage only; no tools.capability invocation, no harness/capabilities.toml reference, no discipline/lane language, no adversarial-review-panel routing row"
    - path: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      provides: "Regenerated twice in this plan — once in each task's commit — so the committed projection snapshot never lags the harness source it renders"
  key_links:
    - from: "harness/commands/checkpoint.md"
      to: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      via: "test_projected_tree_matches_committed_snapshot renders the live harness tree"
      pattern: "iter_commands\\(_COMMANDS_DIR\\)"
---

<objective>
Repair the five surviving harness-source artifacts that D-01 (43-CONTEXT.md) proves still shell or
reference modules this phase's later waves delete: `harness/commands/{checkpoint,orient,review,
verify-work}.md` and `harness/agents/orchestrator.md`. This is Wave 1 of the phase's repair-before-
delete structure (D-02): nothing here deletes a package — it only stops the surviving surface from
invoking the dying plane, so no intermediate commit in this phase ever ships a command that crashes.

Per D-03, repair means *removing* the lifecycle step the command performed, never substituting a
replacement mechanism. `orchestrator.md` additionally loses its capability-routing step (tied to
`harness/capabilities.toml`, deleted in Wave 3 of this phase) and its `adversarial-review-panel`
routing-table row (one of the 5 discipline skills deleted in Wave 3) — both must stop being named
here, in the source, before the emitter re-projects these files in Plan 43-03.

**Revision note (W-1):** every file this plan edits is under a live-tree gate test that asserts
specific surviving content by literal string. Deleting one line too many here does not surface as a
dangling import — it surfaces as a runtime assertion failure that `pytest --collect-only` is blind
to. The exact literals each edited file must retain are enumerated in `<survival_assertions>` below,
and both tasks carry an explicit `uv run pytest` exit-0 criterion over the covering tests.

**Revision note (W-4) — the projection snapshot renders BODIES, not presence.** The committed syrupy
snapshot `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` contains the full
rendered text of every `harness/commands/*.md` and `harness/agents/*.md` file, in BOTH runtime
shapes. Every line this plan deletes from those files is physically present in that `.ambr` today
(verified 2026-07-28 — see `<emit_snapshot_rule>` for the exact line citations). Both of this plan's
commits therefore regenerate the snapshot in-commit. Without that step this plan's very first commit
is red, which is the defect this revision closes.

Purpose: keep the surviving product surface coherent through every commit of the phase.
Output: five repaired `harness/**` source files and a regenerated projection snapshot; no
emitted-tree changes yet (that is Plan 43-03).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
</context>

<emit_snapshot_rule>
<!-- W-4. This is a RULE for the whole phase, not a footnote on one task. It is restated verbatim in
     43-03 and 43-04, the other two plans it binds. Live-verified 2026-07-28. -->

`tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot`
(`:57-85`) renders, into one committed syrupy snapshot:
- every `harness/agents/*.md` — `iter_agents(_AGENTS_DIR)`, both opencode and Claude projections;
- every `harness/commands/*.md` — `iter_commands(_COMMANDS_DIR)`, both projections;
- every `harness/skills/*/SKILL.md` — `iter_skills(_SKILLS_DIR)`;
- `build_opencode_config(_HARNESS_DIR)` (`:82`), which reads **`harness/opencode.json`** verbatim
  (`generate.py:210`) and replaces only its `permission` block.

`_AGENTS_DIR`/`_COMMANDS_DIR`/`_SKILLS_DIR`/`_HARNESS_DIR` are the REAL repo paths (`:24-27`), not
fixtures. The snapshot stores rendered BODY TEXT, so an edit to a single sentence invalidates it.

**THE RULE:**
> Every commit that edits anything under `harness/agents/`, `harness/commands/`, `harness/skills/`,
> or `harness/opencode.json` MUST regenerate
> `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` in **that same commit**.

Procedure, run BEFORE staging:
```
uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q
uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q     # must exit 0
```
and `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` MUST appear in that commit's
`git commit -- <pathspec>`. The `.ambr` holds exactly one snapshot
(`# name: test_projected_tree_matches_committed_snapshot`), so a file-scoped `--snapshot-update`
rewrites that one entry and nothing else.

The trigger is ANY edit to those four source locations — additions, deletions, and prose rewrites
alike. Evidence that this plan's edits are inside the snapshot (live line numbers, 2026-07-28):
`:946` (the `tools.handoff generate/validate/activate` `!` line Task 1 deletes from
`checkpoint.md`), `:970-972` (checkpoint's handoff `git add`/`git commit` clauses), `:2042` and
`:2101` (both runtime projections of `orient.md`'s handoff-resume barrier), `:2590` and `:2642`
(`review.md`'s finding-capture paragraph), `:2820-2821` and `:2885-2886` (`verify-work.md`'s
evidence-capture wrapper), `:201-432` (the entire `orchestrator.md` projection, opencode block at
`:201-325` and Claude block at `:326-444`), and `:4175-4177` inside the
`===== opencode.json =====` block, which carries the literal `"harness/plugins/resume-gate.ts"`
sourced from `harness/opencode.json:31`.

Commits in this phase bound by the rule: **43-01 Task 1**, **43-01 Task 2**, **43-03 Task 1** (which
now owns the `harness/opencode.json` edit), and **43-04 Task 1**.
</emit_snapshot_rule>

<interfaces>
<!-- Exact current-state excerpts the executor needs verbatim; do not re-read these files to find
     the lines below, they are current as of 2026-07-28. -->

From `harness/commands/checkpoint.md` (line 17-22, the block to remove):
```
1. If an active task packet exists, first ensure its task/state/evidence/artifact revision is already
   committed and `state.current_ref` is that `HEAD`. Generate the HANDOFF, then write only its task
   ID/revision/path pointer to `.memory/state/active-task.json`;
   never copy packet, evidence, artifact, contract, or log bodies into `.memory/state/`.

   !`uv run python -m tools.handoff generate .workflow/tasks/<task-id> && uv run python -m tools.handoff validate .workflow/tasks/<task-id> && uv run python -m tools.handoff activate .workflow/tasks/<task-id> --state-dir .memory/state`
```
Step 3 (git add/commit, line 38-48) currently has three conditional `!` lines for
`active-task.json`, the HANDOFF artifact, and a `commit` line that also runs
`tools.handoff validate` after committing — the handoff-specific clauses in all of these go too.

From `harness/commands/orient.md` (line 17-23, section "1. Active HANDOFF first" — remove the whole
section, renumber 2→1, 3→2, 4→3):
```
## 1. Active HANDOFF first (fresh-session resume barrier)

If `.memory/state/active-task.json` exists, it is a pointer only. Validate its HANDOFF and run the
existing Phase 20 gate before any EXECUTE, REVIEW, or VERIFY action. A stale pointer, state
revision, ref, or artifact must fail and be regenerated via `/handoff`; do not auto-correct it.

!`test ! -f .memory/state/active-task.json || uv run python -m tools.handoff resume --state-dir .memory/state --repo-root .`
```
Read-order step 5 (line 51-53) lists `gate-model` (stays, Phase 44) and no discipline skill by
name today — leave it as-is; only the section-1 block above and its cross-reference at line 46
("Active HANDOFF, when surfaced above — validate it, then run `/phase-gate`") need removal.

From `harness/commands/review.md` (line 49-52, the finding-capture paragraph):
```
When the review belongs to an active task packet, record each result only through
`python -m tools.evidence.capture add-finding` after the read-only review completes; do **not** edit
`evidence.json` directly. Each finding requires its `blocker`/`major`/`minor`/`nit` severity, its
`open`/`resolved`/`accepted` disposition, and (for resolved or accepted) an `E-*` capture reference.
```

From `harness/commands/verify-work.md` (line 22-27, the evidence-capture wrapper paragraph under the
intro, and the per-gate capture instruction inside it):
```
When an active `.workflow/tasks/<id>/` packet exists, capture each gate's **existing command**
through `python -m tools.evidence.capture capture --task-dir <task-dir> --gate <gate> -- <existing argv>`.
The adapter is only a child-process recorder: keep the five commands below, their order, and their
presence-safe behavior unchanged. Record the observed `PASSED`, `FAILED`, `SKIPPED`, or `BLOCKED`
result; never convert an announced skip into a pass. Attach the applicable `AC-*` IDs to the capture.
Use `--source ci` only for a CI-produced reference, otherwise retain the default `local` source.
```
The five numbered gate sections (lint, test, contract-check, golden, derived-freshness) and their
`!` commands are untouched — only the evidence-capture wrapper paragraph above them is removed.

From `harness/agents/orchestrator.md`, every line naming a deleted surface (verified 2026-07-28):
- Line 23-25: "...to the specialist whose least-privilege scope fits the work. Routing by capability
  is the rule, not a nicety: what a piece of work needs is a *kind* of agent, and
  `harness/capabilities.toml` is the single place that says which personas may serve each kind."
- Line 46-49 intake numbering: step 2 "Name the capability, then resolve a provider..." (line 52-65,
  including the `tools.capability list` / `tools.capability route` code block and the
  "a lane's discipline record names the agent... `missing required disciplines:...`" sentence) —
  delete this entire numbered step and renumber the remaining steps (3→2, 4→3, 5→4, 6→5, 7→6, 8→7).
- Line 66 (old step 3): "pick the command, and the persona the capability's allowlist resolved to" —
  rephrase to drop "the capability's allowlist resolved to"; route by topology/stage + language only.
- Line 92-96 (routing table intro): "Route on three dimensions, in this order: the **declared
  capability** (which kind of agent — `harness/capabilities.toml` holds the allowlist), the
  **pipeline stage/component**... but the capability allowlist is what says whether the persona you
  landed on is permitted." — rewrite to two dimensions: pipeline stage/component, then language.
- Line 98: "current resolution of the capability registry" → "current resolution of the routing
  table below" (or equivalent; no registry survives to resolve against).
- Table row line 116: `| Review written code | `adversarial-review` capability → **code-reviewer**
  (read-only) | `/review` |` — drop the `` `adversarial-review` capability → `` prefix, keep
  `**code-reviewer** (read-only)`.
- Table row line 117: `| High-risk change needing several review frames (STRICT+ lane requirement) |
  `adversarial-review` capability, one provider per seat | `adversarial-review-panel` skill |` —
  DELETE this entire row (the skill it names is deleted in Wave 3; no successor).
- Table row line 118: `| "Which agents may do this kind of work?" | (self) |
  `uv run python -m tools.capability list` |` — DELETE this entire row.
- Table row line 123: `| Locate code / map unfamiliar area | `reconnaissance` capability →
  **explorer** | (read-only search) |` — drop the `` `reconnaissance` capability → `` prefix, keep
  `**explorer**`.
- Line 128-129 (closing sentence): "Where a capability declares an allowlist, that allowlist is the
  boundary — a route outside it is refused by the lifecycle machinery, not merely discouraged here."
  — DELETE (no allowlist machinery survives).
</interfaces>

<survival_assertions>
<!-- W-1. Three EXISTING gate tests render over the four harness/** files this plan edits and assert
     surviving content by literal string. They are already committed; this plan adds no gate — it
     only names what must NOT be deleted, and runs them. Line numbers live-verified 2026-07-28. -->

`tools/harness_lint/tests/test_orchestrator_topology.py` (covers `harness/agents/orchestrator.md`):
- `:42-49` `test_conductor_has_topology_intake` — body (lowercased) must still contain
  `"trace the topology"` AND `"topology"`.
- `:52-61` `test_conductor_routes_by_stage_component` — body must still contain `"stage"`, at least
  one of `("component", "parser", "converter", "scheduler", "collector")`, AND `"/pipeline"`.
- `:30-39` frontmatter must stay `name: orchestrator`, `mode: primary`.
**`/pipeline` MUST SURVIVE this plan's orchestrator edit.** It appears in 4 routing-table rows
(`:105-108`) that this plan does not touch; `/pipeline` is Phase 44's to remove, not Phase 43's.
Removing it here turns `test_conductor_routes_by_stage_component` red.

`tools/harness_lint/tests/test_context_budget_wiring.py` (covers `orchestrator.md` AND `orient.md`):
- `:43-48` `test_orchestrator_wires_the_heuristic` — `orchestrator.md` body must still contain BOTH
  `"context-budget"` and `"fan-out"`.
- `:51-56` `test_orient_wires_the_heuristic` — `orient.md` body must still contain BOTH
  `"context-budget"` and `"fan-out"`.
This is the live constraint on Task 1's orient.md read-order renumbering: the read-order item this
plan deletes is the *Active HANDOFF* one, NOT the items naming `context-budget` / `fan-out-synthesize`.
Renumber around them; do not collapse them away.

`tools/memory_regen/tests/test_checkpoint_command.py` (covers `harness/commands/checkpoint.md`):
- `:22-26` — text must still contain `"updated:"`, match `/quote|quoted/i`, and contain BOTH
  `"activeContext.md"` and `"progress.md"`.
- `:29-33` — text must still match `/last .*done|no.*done-log|git holds/i` AND contain the literal
  `"git holds the full completed history"`.
- `:36-42` — text must still contain neither `"$(date"` nor `` "`date" ``.
The handoff-clause removal in step 3's `git add`/`git commit` lines must therefore keep the
`activeContext.md` / `progress.md` pathspec and the surrounding prose intact.
</survival_assertions>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repair the four surviving commands + regenerate the projection snapshot (D-01, D-02, D-03, W-1, W-4)</name>
  <files>harness/commands/checkpoint.md, harness/commands/orient.md, harness/commands/review.md, harness/commands/verify-work.md, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>
  <read_first>
    The `<interfaces>` block above (exact current line ranges for all four files), the
    `<survival_assertions>` block (what must NOT be deleted from `checkpoint.md` and `orient.md`),
    the `<emit_snapshot_rule>` block (why the `.ambr` is in this task's pathspec), and
    `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-01/D-02/D-03.
  </read_first>
  <action>
    In `checkpoint.md`: delete step 1's handoff-generation paragraph and its `!` command (line
    17-22); renumber step 2→1 and step 3→2; in the renumbered step 2 (old step 3), drop the two
    conditional `!` lines that add `active-task.json` and the HANDOFF revision file, and drop the
    ` && uv run python -m tools.handoff validate ...` clause from the commit line, leaving only the
    plain `git add` + `git commit` sequence for `activeContext.md`/`progress.md`. Update the file's
    intro prose (line 17-20) if it still describes the removed handoff step. Per
    `<survival_assertions>`, the `updated:`/quoted stamp instruction, the `activeContext.md` and
    `progress.md` pathspec, and the literal sentence "git holds the full completed history" all
    STAY — the handoff clauses come out from around them, they do not come out with them.
    In `orient.md`: delete the entire "## 1. Active HANDOFF first" section (line 17-23) including
    its `!` command; renumber sections 2→1, 3→2, 4→3; remove the cross-reference to it in the new
    step 2 (old step 4)'s read-order item 2 ("Active HANDOFF, when surfaced above — validate it,
    then run `/phase-gate`; it is the highest-priority task pointer...") — either delete that
    read-order line entirely or fold its non-handoff content (if any) into the surrounding items;
    renumber the remaining read-order items accordingly. Per `<survival_assertions>`, the read-order
    items naming `context-budget` and `fan-out-synthesize` MUST survive the renumbering — only the
    Active-HANDOFF item is removed.
    In `review.md`: delete the finding-capture paragraph (line 49-52) that invokes
    `tools.evidence.capture add-finding`; leave step 4's first two sentences (the code-reviewer
    hand-off + re-run-until-clean instruction) intact.
    In `verify-work.md`: delete the evidence-capture wrapper paragraph (line 22-27) that invokes
    `tools.evidence.capture capture`; leave the five numbered gate sections and their `!` commands
    byte-unchanged — this task removes only the paragraph wrapping them, not the gates themselves.
    Do not add any replacement mechanism, comment, or TODO in place of what is removed (D-03) — a
    clean removal, not a stub.

    Then apply the `<emit_snapshot_rule>` (W-4). These four files are rendered verbatim into
    `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` — the exact lines deleted
    above sit at `:946`, `:970-972`, `:2042`, `:2101`, `:2590`, `:2642`, `:2820-2821` and
    `:2885-2886` — so the committed snapshot is stale the moment the edits are saved and BEFORE
    anything is staged. Run, before staging:
    `uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q`
    then re-run without the flag and confirm exit 0:
    `uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q`.

    Commit with `git commit -- harness/commands/checkpoint.md harness/commands/orient.md
    harness/commands/review.md harness/commands/verify-work.md
    tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` only. Never `git add -A`,
    `git add .`, `git commit -a`, or `git checkout <ref> -- .`. Inspect
    `git diff --cached --name-only` first (D-13) and confirm the `.ambr` is staged alongside the
    four commands.
  </action>
  <acceptance_criteria>
    `grep -rn "tools\.handoff\|tools\.evidence" harness/commands/checkpoint.md harness/commands/orient.md harness/commands/review.md harness/commands/verify-work.md` returns no output (exit 1).
    `grep -n "phase-gate" harness/commands/orient.md` returns no output.
    All four files still have valid YAML frontmatter and at least one numbered/lettered step remains in each (files are not emptied).
    `uv run pytest tools/memory_regen/tests/test_checkpoint_command.py tools/harness_lint/tests/test_context_budget_wiring.py -q` exits 0 — W-1: these are the two live-tree gate tests covering `checkpoint.md` and `orient.md`; their assertions are RUNTIME string checks that `pytest --collect-only` cannot surface, so running them is the criterion, not reading the diff (D-15).
    `grep -c "context-budget" harness/commands/orient.md` returns at least `1` and `grep -c "fan-out" harness/commands/orient.md` returns at least `1` (renumbering did not collapse the surviving read-order items).
    W-4: `uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q` exits 0 (run WITHOUT `--snapshot-update`, after the update run).
    W-4: `git log -1 --name-status` shows `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` as `M` in the same commit as the four command files, and `git status --porcelain -- tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` is empty (not left dirty).
    `uv run pytest -q` exits 0 — this task ends fully green. Any red is a real defect; this phase sanctions no expected-red window.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repair orchestrator.md + regenerate the projection snapshot (D-01, D-03, W-1, W-4)</name>
  <files>harness/agents/orchestrator.md, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>
  <read_first>
    The `<interfaces>` block above (every exact line/row this task removes or rewrites), the
    `<survival_assertions>` block (the literals `orchestrator.md` must retain — in particular
    `/pipeline`, which is Phase 44's to remove, not this phase's), the `<emit_snapshot_rule>` block,
    and `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-01, D-03.
  </read_first>
  <action>
    Rewrite `harness/agents/orchestrator.md` so it routes purely by declared pipeline
    stage/component (`tools.harness_config`'s `components()`/`pipeline()`, and
    `tools.contract_graph`'s `direct`/`reverse`/`transitive` queries for non-linear topology) and
    language, with NO reference to `tools.capability`, `harness/capabilities.toml`,
    `harness/disciplines.toml`, "lane", "discipline record", or an "allowlist" gate. Specifically:
    remove the "Routing by capability is the rule..." sentence (line 23-25) and the whole numbered
    intake step "Name the capability, then resolve a provider" (including its `tools.capability
    list`/`route` code block and the "a lane's discipline record..." sentence), renumbering the
    remaining intake steps so they run 1-7 with no gap. Rewrite the "Routing decision table" intro
    (line 92-96) to describe two dimensions only — pipeline stage/component, then language — dropping
    "declared capability" and the `harness/capabilities.toml` clause. In the table itself: drop the
    `` `adversarial-review` capability → `` and `` `reconnaissance` capability → `` prefixes from the
    two rows that carry them (keep the persona names), delete the "High-risk change needing several
    review frames (STRICT+ lane requirement)" row and its `adversarial-review-panel` skill reference
    outright (no successor), and delete the "'Which agents may do this kind of work?'" row and its
    `tools.capability list` command. Delete the closing "Where a capability declares an allowlist..."
    sentence (line 128-129). Leave the topology-tracing step (`Trace the topology`, step 5 before
    renumbering) and every other routing-table row untouched — this task removes ONLY
    capability/discipline/panel language, it does not touch the topology or language-boundary logic.
    Per `<survival_assertions>`: the four `/pipeline` routing rows (line 105-108), the `stage` and
    component tokens, the `context-budget` and `fan-out` references, and the `name: orchestrator` /
    `mode: primary` frontmatter all SURVIVE this edit. `/pipeline` in particular belongs to Phase 44,
    not Phase 43 — do not remove it here.

    Then apply the `<emit_snapshot_rule>` (W-4). `orchestrator.md` is rendered verbatim into the
    committed snapshot TWICE — the opencode projection at
    `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr:201-325` and the Claude
    projection at `:326-444` — so every line this task rewrites appears there twice. Run, before
    staging: `uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q`,
    then re-run without the flag and confirm exit 0.

    Commit with `git commit -- harness/agents/orchestrator.md
    tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` only. Never `git add -A`,
    `git add .`, `git commit -a`, or `git checkout <ref> -- .`. Inspect
    `git diff --cached --name-only` first (D-13).
  </action>
  <acceptance_criteria>
    `grep -n "tools\.capability\|capabilities\.toml\|disciplines\.toml\|adversarial-review-panel\|lane's discipline\|missing required disciplines" harness/agents/orchestrator.md` returns no output (exit 1).
    `grep -c "Trace the topology" harness/agents/orchestrator.md` returns exactly `1` (topology-routing step survives untouched).
    `grep -c "/pipeline" harness/agents/orchestrator.md` returns `4` (Phase 44's target is untouched by this phase).
    The file's YAML frontmatter (`name: orchestrator`, `mode: primary`) is unchanged.
    `uv run pytest tools/harness_lint/tests/test_orchestrator_topology.py tools/harness_lint/tests/test_context_budget_wiring.py -q` exits 0 — W-1: the two live-tree gate tests covering `orchestrator.md`; run them, do not infer from the diff (D-15).
    W-4: `uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q` exits 0 (run WITHOUT `--snapshot-update`, after the update run).
    W-4 (SCOPED to the orchestrator projections — an unscoped `grep -c "tools.capability"` over the whole `.ambr` is UNSATISFIABLE at this commit and must not be used): `awk '/^  ===== (opencode|claude)-agent\/orchestrator =====$/{f=1;next} /^  ===== /{f=0} f' tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr | grep -c "tools.capability"` returns `0`.
    Measured 2026-07-28 against HEAD: the whole `.ambr` carries **12** `tools.capability` matches, of which **6** are the orchestrator's (`:252`, `:253`, `:313` in the opencode projection, `:371`, `:372`, `:432` in the Claude projection) and this task removes exactly those 6. The other **6** are the five discipline `SKILL.md` projections and are deleted by Plan 43-04, not here — so the unscoped whole-file count is still `6` after this commit, by design. (An earlier revision of this criterion claimed "5 matches" and grepped unscoped; both were wrong. The `awk` range delimiters are the emitter's own `===== <tree>-agent/<name> =====` block markers, so the scoping survives the line-number shifts this edit causes.)
    W-4: `git log -1 --name-status` shows the `.ambr` as `M` alongside `harness/agents/orchestrator.md`, and `git status --porcelain -- tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` is empty.
    `uv run pytest -q` exits 0 — this task ends fully green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Command/agent prose → invoked CLI | Harness-authored Markdown shells `uv run python -m tools.X`; a stale reference to a soon-to-be-deleted module is the only risk this plan mitigates |
| Edited harness prose → live-tree gate tests | `test_orchestrator_topology.py`, `test_context_budget_wiring.py`, and `test_checkpoint_command.py` assert surviving content by literal string; an over-broad deletion here is a RUNTIME red that `--collect-only` cannot see |
| Harness source → committed projection snapshot | `test_emit_determinism.ambr` stores the rendered BODY of every agent/command/skill plus the serialized `harness/opencode.json`; it is invalidated by prose edits, not only by add/remove, and it goes stale before staging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | Emitted command surface pointing at a deleted module | mitigate | This plan removes every such reference at source before Wave 3 deletes the module (D-02 ordering) |
| T-43-10 | Denial of Service | Over-broad deletion in `orchestrator.md`/`orient.md`/`checkpoint.md` silently removing a literal a live gate test asserts (e.g. `/pipeline`, `context-budget`, "git holds the full completed history") | mitigate | `<survival_assertions>` enumerates every asserted literal; both tasks run the three covering test files as an exit-0 acceptance criterion (W-1) |
| T-43-16 | Denial of Service | A `harness/**` prose edit landing without its `test_emit_determinism.ambr` regeneration, leaving the very first commit of the phase red on a committed snapshot | mitigate | `<emit_snapshot_rule>` states the trigger as a phase-wide rule; both tasks carry the `--snapshot-update` step, the `.ambr` in the pathspec, and an exit-0 non-updating re-run as an acceptance criterion |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package install in this phase or plan (deletion-only, no new dependency) |
</threat_model>

<verification>
`grep -rn "tools\.handoff\|tools\.evidence" harness/commands/checkpoint.md harness/commands/orient.md harness/commands/review.md harness/commands/verify-work.md` — exit 1, no output.
`grep -n "tools\.capability\|capabilities\.toml\|disciplines\.toml\|adversarial-review-panel" harness/agents/orchestrator.md` — exit 1, no output.
`uv run pytest tools/harness_lint/tests/test_orchestrator_topology.py tools/harness_lint/tests/test_context_budget_wiring.py tools/memory_regen/tests/test_checkpoint_command.py -q` — exits 0 (W-1: the three live-tree gate tests that render over the five files this plan edits).
`uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q` — exits 0 without `--snapshot-update` (W-4).
`uv run pytest -q` — green at BOTH of this plan's commits.
</verification>

<success_criteria>
The five D-01 surviving artifacts no longer shell or name a module this phase deletes in Wave 3.
No replacement mechanism was introduced. Every literal the three covering gate tests assert
(`/pipeline`, `trace the topology`, `stage` + a component token, `context-budget`, `fan-out`,
`updated:`, `activeContext.md`, `progress.md`, "git holds the full completed history") still
survives, proven by running those tests. The committed projection snapshot was regenerated in each
of this plan's two commits, so `uv run pytest -q` is green at both. Commits land before any deletion
task in this phase.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-01-SUMMARY.md` when done
</output>

