---
phase: 43-lifecycle-plane-removal
plan: 03
type: execute
wave: 2
depends_on: ["43-01", "43-02"]
files_modified:
  - harness/opencode.json
  - tools/harness_emit/merge.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - tools/hooks/tests/test_settings_coexist.py
  - .opencode/command/checkpoint.md
  - .opencode/command/orient.md
  - .opencode/command/review.md
  - .opencode/command/verify-work.md
  - .opencode/agent/orchestrator.md
  - opencode.json
  - .claude/commands/checkpoint.md
  - .claude/commands/orient.md
  - .claude/commands/review.md
  - .claude/commands/verify-work.md
  - .claude/agents/orchestrator.md
  - .claude/settings.json
  - AGENTS.md
  - CLAUDE.md
  - tools/harness_emit/emit-manifest.json
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-02: repair is fully landed and re-emitted BEFORE any deletion task runs — this plan is the last Wave-1/2 gate before Wave 3 deletion begins"
    - "D-14: every fix reached the emitted trees through the emitter (python -m tools.harness_emit), never by hand-editing .opencode/** or .claude/**"
    - "D-06: RETIRED_SIGNATURES is set AND emptied again inside this one plan — it exists only to make the re-emit drop the resume_gate hook group, and per D-06 must not persist as a running log"
    - "B-6 (this revision): the whole resume_gate retirement CAUSE now lives here with its effect — merge.py's signature/group edit, harness/opencode.json's plugin-array edit and test_coexist.py's _SEED_SETTINGS fixture all land in Task 1's commit, alongside the emitter run and the test_expected_slot_counts literal. Splitting cause from effect across waves is what reds tools/harness_emit/tests/test_settings_merge.py, which asserts merge_settings(<parsed live>) reproduces the LIVE .claude/settings.json byte-for-byte (measured post-edit: byte-identical False, PreToolUse 8 -> 7)"
    - "B-1: test_settings_coexist.py::test_expected_slot_counts asserts len(hooks['PreToolUse']) == 8 against the LIVE .claude/settings.json. The invalidating change is THIS plan's emitter run, which takes that file 8 -> 7. The literal, its message, and its docstring are therefore corrected in the SAME commit as that emitter run"
    - "B-4: this plan's D-02 gate greps are scoped to the FIVE surviving D-01 artifacts by name, not repo-wide — the repo-wide form cannot pass at Wave 2 because handoff.md, resume-gate.ts and the 5 discipline SKILL.mds are Wave 3's to delete; the repo-wide sweep already exists, correctly placed, at 43-04-PLAN.md's Task 2"
    - "W-2 (corrected this revision): the emitter does NOT prune .opencode/plugin/resume-gate.ts at this wave. generate.py:172-180 globs harness/plugins/*.ts and :426-432 copies every hit; harness/plugins/resume-gate.ts is not deleted until 43-04 Task 1, so at Wave 2 the .ts is RE-EMITTED and its emit-manifest.json row is correctly retained. The prune happens at 43-04 Task 2's re-emit"
    - "W-4 (phase-wide RULE, see <emit_snapshot_rule>): this plan edits harness/opencode.json, so it regenerates tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr in Task 1's commit — the .ambr's ===== opencode.json ===== block carries the literal harness/plugins/resume-gate.ts at :4177"
    - "Every task in this plan ends fully green. This phase sanctions no expected-red window; an executor told to amend-if-red must be able to treat any red as a real defect"
  artifacts:
    - path: "tools/harness_emit/merge.py"
      provides: "tools.hooks.resume_gate removed from HARNESS_SIGNATURES and from HARNESS_HOOK_GROUPS[PreToolUse]; RETIRED_SIGNATURES used for the drop then reset to ()"
    - path: "harness/opencode.json"
      provides: "plugin array no longer lists harness/plugins/resume-gate.ts"
    - path: "tools/harness_emit/tests/test_coexist.py"
      provides: "_SEED_SETTINGS with no resume_gate PreToolUse group, so the full-emit byte-for-byte seed test stays green in the same commit as the merge.py edit"
    - path: ".claude/settings.json"
      provides: "No PreToolUse hook group shelling tools.hooks.resume_gate; exactly 7 PreToolUse groups (4 GSD + 3 harness)"
    - path: "tools/hooks/tests/test_settings_coexist.py"
      provides: "test_expected_slot_counts asserting 7 PreToolUse slots (4 GSD + 3 harness gates), PostToolUse still 4, corrected in the same commit as the emit that caused 8 -> 7"
    - path: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      provides: "Regenerated projection snapshot whose opencode.json block no longer lists harness/plugins/resume-gate.ts"
  key_links:
    - from: "harness/commands/checkpoint.md"
      to: ".claude/commands/checkpoint.md"
      via: "python -m tools.harness_emit projection"
      pattern: "harness_emit"
    - from: "tools/harness_emit/merge.py"
      to: ".claude/settings.json"
      via: "RETIRED_SIGNATURES drop path in merge_settings"
      pattern: "retired_signatures"
---

<objective>
Land the source repairs from Plans 43-01 and 43-02 into both runtime trees by running the emitter
(D-14), together with the entire `resume_gate` hook-retirement change — cause and effect in one
commit — then verify the whole repaired-but-not-yet-deleted state is green. This is the phase gate
between Wave 1 (repair) and Wave 3 (delete): nothing in Wave 3 may run until this plan proves the
surviving surface is fully coherent with nothing still invoking the dying plane.

**Revision note (B-6) — the retirement CAUSE moved here, next to its effect.** A previous revision
put `tools/harness_emit/merge.py`'s signature/group edit, `harness/opencode.json`'s plugin-array
edit and `test_coexist.py`'s `_SEED_SETTINGS` fixture in Plan 43-02 (Wave 1), leaving only the
`test_expected_slot_counts` literal here. That is the same cause/effect split B-1 was created to
eliminate, one level down. Measured 2026-07-28 by running the post-edit `merge.py` logic against the
live `.claude/settings.json`: `merge_settings(<parsed live>)` stops re-serializing byte-for-byte and
`PreToolUse` goes 8 → 7 — which reds `tools/harness_emit/tests/test_settings_merge.py`
(`:29` binds `SETTINGS_PATH` to the REAL `.claude/settings.json`; `:54-62` and `:104` both assert
byte-for-byte reproduction of it). No plan named that file. Since `.claude/settings.json` is
emitter-owned and D-14 forbids hand-editing it, only THIS plan — the one that runs the emitter — can
make that change green. So `merge.py`, `test_coexist.py` and `harness/opencode.json` now land in
Task 1's commit. Note the coupling that must be preserved: `test_coexist.py`'s
`test_seeded_settings_json_reproduced_byte_for_byte` (`:227-236`) runs a FULL emit over
`_SEED_SETTINGS`, so the `_SEED_SETTINGS` edit and the `merge.py` edit must land in the same commit
as each other. They do, here.

Per D-06/Phase-41 precedent, `RETIRED_SIGNATURES` is a transient drop mechanism: this plan sets it,
runs the emitter that uses it, then resets it to `()` — all inside Task 1.

**Revision note (B-1) — the slot-count literal lives here, with the emit that invalidates it.**
`tools/hooks/tests/test_settings_coexist.py::test_expected_slot_counts` asserts
`len(hooks["PreToolUse"]) == 8` by reading the REAL `.claude/settings.json` (live-verified: 8 today).
The literal, its assertion message, and its docstring are corrected in Task 1's commit. Plan 43-02
removed only the sibling `_NEW_GATES` tuple, which is fixture data and stays green on its own.

**Revision note (W-2, CORRECTED) — the emitter does NOT prune `.opencode/plugin/resume-gate.ts`
here.** A previous revision claimed this plan's re-emit prunes that file and its manifest row. It
does not. `generate.py:172-180` (`iter_plugins`) globs `harness/plugins/*.ts` and `:426-432` copies
every hit byte-for-byte; `harness/plugins/resume-gate.ts` is not deleted until 43-04 Task 1, so at
this wave it is re-emitted unchanged and its `emit-manifest.json` row is correctly retained. Removing
`"harness/plugins/resume-gate.ts"` from `harness/opencode.json`'s `plugin` ARRAY (which this plan
does) changes the emitted root `opencode.json` config only — it does not stop the file copy. The
prune happens at 43-04 Task 2's re-emit, after the `.ts` is deleted. `emit-manifest.json` stays in
this plan's pathspec purely as a defensive no-op. **Do not hand-edit the manifest** — a hand-edit
here would be exactly the D-14 violation the plan otherwise forbids.

**Revision note (B-4) — what this gate can and cannot assert at Wave 2.** This plan's job is to prove
the FIVE surviving D-01 artifacts are clean. It cannot assert a repo-wide absence of
`tools.handoff` / `tools.evidence` / `tools.capability`, because at Wave 2 those tokens legitimately
still live in artifacts Wave 3 deletes: `harness/commands/handoff.md` and both emitted copies,
`harness/plugins/resume-gate.ts`, and the 5 discipline `SKILL.md`s. A repo-wide grep here would
return ~22 matches and the D-02 gate could never go green, which would mean Wave 3 never
legitimately starts. Both greps below are scoped to the five surviving artifacts by name. The
repo-wide form belongs AFTER the deletions and lives at 43-04 Task 2 — do not duplicate it here.

Purpose: prove the repair wave is complete and the tree is internally consistent before the
irreversible deletion wave starts.
Output: both `.opencode/` and `.claude/` trees reflect the five repaired commands/agent, no
`resume_gate` hook group in either runtime, `.claude/settings.json` at 7 PreToolUse groups with the
matching test literal corrected in the same commit, `RETIRED_SIGNATURES` back to `()`, the
projection snapshot regenerated, full suite green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md
</context>

<emit_snapshot_rule>
<!-- W-4. Restated verbatim from 43-01-PLAN.md; it is a phase-wide RULE, not a per-task footnote. -->

`tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot`
(`:57-85`) renders the LIVE `harness/agents/`, `harness/commands/`, `harness/skills/` (`:24-27`) AND
`build_opencode_config(_HARNESS_DIR)` (`:82`) into one committed syrupy snapshot.
`build_opencode_config` reads **`harness/opencode.json`** verbatim (`generate.py:210`) and replaces
only its `permission` block, so the authored `plugin` array is serialized into the snapshot.

**THE RULE:**
> Every commit that edits anything under `harness/agents/`, `harness/commands/`, `harness/skills/`,
> or `harness/opencode.json` MUST regenerate
> `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` in **that same commit**.

Procedure, run BEFORE staging:
```
uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q
uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q     # must exit 0
```
and the `.ambr` MUST appear in that commit's `git commit -- <pathspec>`.

**This plan is bound by the rule because Task 1 edits `harness/opencode.json`.** The literal
`"harness/plugins/resume-gate.ts"` sits at `test_emit_determinism.ambr:4177`, inside the
`===== opencode.json =====` block (`:4175-4177`), and is sourced from `harness/opencode.json:31`.
Removing it from the authored config without regenerating the snapshot leaves this plan's own gate
commit red. (This plan does NOT edit `harness/agents/`, `harness/commands/` or `harness/skills/` —
43-01 already regenerated the snapshot for those.)
</emit_snapshot_rule>

<interfaces>
<!-- The five surviving D-01 artifacts, by name, in both emitted trees. Every scoped grep in this
     plan uses exactly this path set. Live-verified 2026-07-28. -->

```
_D01_EMITTED = \
  .opencode/command/checkpoint.md .opencode/command/orient.md \
  .opencode/command/review.md .opencode/command/verify-work.md \
  .opencode/agent/orchestrator.md \
  .claude/commands/checkpoint.md .claude/commands/orient.md \
  .claude/commands/review.md .claude/commands/verify-work.md \
  .claude/agents/orchestrator.md
```
The source-side counterpart (`harness/`) is the same five names:
`harness/commands/{checkpoint,orient,review,verify-work}.md harness/agents/orchestrator.md`.

**Emitted-config path, corrected.** The emitter writes the opencode project config to the REPO-ROOT
`opencode.json` (`generate.py:436-438`: `_confine(Path(root) / "opencode.json", Path(root))`). There
is **no `.opencode/opencode.json`** (verified absent 2026-07-28). Use `opencode.json` in every
pathspec and grep; a previous revision named the non-existent nested path.

**B-6 — the resume_gate retirement cluster, moved here from 43-02.**

`tools/harness_emit/merge.py:87-94` — remove the `"tools.hooks.resume_gate"` line (`:93`):
```python
HARNESS_SIGNATURES: tuple[str, ...] = (
    "tools.hooks.format_on_write",
    "tools.hooks.contract_guard",
    "tools.hooks.secret_scan",
    "tools.hooks.commit_gate",
    "tools.hooks.resume_gate",      # <-- REMOVE
)
```
`tools/harness_emit/merge.py:112` — `RETIRED_SIGNATURES: tuple[str, ...] = ()` becomes
`("tools.hooks.resume_gate",)` for the emitter run, then back to `()` before commit (D-06).
`tools/harness_emit/merge.py:189-199` — delete the LAST dict in `HARNESS_HOOK_GROUPS["PreToolUse"]`:
```python
        {
            "matcher": "Write|Edit|Bash",
            "hooks": [
                {
                    "type": "command",
                    "command": _GUARD_PREFIX + "uv run python -m tools.hooks.resume_gate",
                    "timeout": 15,
                }
            ],
        },
```
The drop path is `merge.py:250-258`: a group matching no CURRENT signature falls through as
`sig is None` and is then checked against `retired_signatures`; a hit is `continue`d (dropped).

`harness/opencode.json:31`:
```json
"plugin": ["harness/plugins/session-inject.ts", "harness/plugins/resume-gate.ts"]
```
Remove `"harness/plugins/resume-gate.ts"`, leaving only `"harness/plugins/session-inject.ts"`.

`tools/harness_emit/tests/test_coexist.py:178-187` — inside `_SEED_SETTINGS`'s PreToolUse hooks
list, the LAST group (immediately after the commit_gate group), containing
`_GUARD_PREFIX + "uv run python -m tools.hooks.resume_gate"` at `:184`. Delete that dict.
**This edit is COUPLED to the `merge.py` edit and must land in the same commit as it:**
`test_seeded_settings_json_reproduced_byte_for_byte` (`:227-236`) runs a full `_emit()` over the
seeded tree and asserts `settings.json` comes back byte-for-byte unchanged. With `merge.py` edited
and `_SEED_SETTINGS` not (or vice versa) that test goes red.

`tools/harness_emit/tests/test_settings_merge.py` — **do not edit; it must simply stay green.**
`:29` binds `SETTINGS_PATH = REPO_ROOT / ".claude" / "settings.json"` (the REAL file); `:54-62`
(`test_merge_reproduces_live_settings_byte_for_byte`) and `:104` (the byte-equality tail of the
de-dup test) both assert `_serialize(merge_settings(<parsed live>)) == <live bytes>`. Once the
emitter has taken the live file to 7 groups AND `merge.py` no longer emits the resume_gate group,
both assertions hold again. That is why cause, effect and re-emit must be one commit.
`test_merge_is_idempotent` and `test_session_start_stays_exactly_four_groups` are unaffected either
way (SessionStart stays 4; resume_gate was PreToolUse-only).

**B-1 — the slot-count literal this plan's emit invalidates.**
`.claude/settings.json` `hooks.PreToolUse`, live-verified 2026-07-28 (pre-re-emit): **8** groups.
Groups 1-4 are the GSD guards (`gsd-prompt-guard`, `gsd-read-guard`, `gsd-workflow-guard`,
`gsd-validate-commit`); 5-7 are `contract_guard`, `secret_scan`, `commit_gate`; group **8** is
`uv run python -m tools.hooks.resume_gate` (`matcher: "Write|Edit|Bash"`). This plan's Task 1
re-emit drops group 8 via `RETIRED_SIGNATURES`, leaving **7**. `hooks.PostToolUse` stays at **4**
(live-verified), and `hooks.SessionStart` stays at **4**.

The test that hardcodes the old value, in `tools/hooks/tests/test_settings_coexist.py`:
```python
def test_expected_slot_counts() -> None:
    """8 PreToolUse (4 GSD + 4 harness) and 4 PostToolUse (3 GSD + 1 harness)."""
    hooks = _load()["hooks"]
    assert len(hooks["PreToolUse"]) == 8, "expected 8 PreToolUse slots (4 GSD + 4 harness gates)"
    assert len(hooks["PostToolUse"]) == 4, "expected 4 PostToolUse slots (3 GSD + 1 new gate)"
```
`_load()` (via `_SETTINGS`, line 22-23) reads the REAL `.claude/settings.json`, not a fixture. Make
three edits, all in Task 1's commit:
- `== 8` → `== 7`
- message `"expected 8 PreToolUse slots (4 GSD + 4 harness gates)"` →
  `"expected 7 PreToolUse slots (4 GSD + 3 harness gates)"`
- docstring first clause `8 PreToolUse (4 GSD + 4 harness)` → `7 PreToolUse (4 GSD + 3 harness)`
Leave the `PostToolUse` assertion and its message byte-unchanged.

**Locate the function by NAME, not by line number.** It sat at line 112-116 in the pre-phase file,
but Plan 43-02 Task 2 removes one line from `_NEW_GATES` above it, so by the time this plan runs it
has shifted up one line (≈111-115). Before editing, confirm Plan 43-02's commits have landed:
`grep -c "tools.hooks.resume_gate" tools/hooks/tests/test_settings_coexist.py` must return `0`
(the `_NEW_GATES` tuple is gone) while `grep -c '== 8' ...` still returns `1` (the count is still
untouched, waiting for this plan), and `grep -rn "TASK_HEADER" tools/` must return nothing (43-02
Task 1's W-6 repair landed).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retire the resume_gate hook (cause + effect in one commit), re-emit both runtime trees, correct the PreToolUse slot count, reset RETIRED_SIGNATURES, regenerate the projection snapshot (D-14, D-06, B-6, B-1, W-2, W-4)</name>
  <files>tools/harness_emit/merge.py, harness/opencode.json, tools/harness_emit/tests/test_coexist.py, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, tools/hooks/tests/test_settings_coexist.py, .opencode, .claude, opencode.json, AGENTS.md, CLAUDE.md, tools/harness_emit/emit-manifest.json</files>
  <read_first>
    `.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md` §"D-06's RETIRED_SIGNATURES
    mechanism"; the `<interfaces>` block above (the corrected `opencode.json` path, the B-6 cluster
    excerpts with the `_SEED_SETTINGS`↔`merge.py` coupling, and the B-1 excerpt giving the exact
    three edits to `test_expected_slot_counts` plus the locate-by-name instruction); the
    `<emit_snapshot_rule>` block. Confirm Plans 43-01 and 43-02 have landed before starting:
    `grep -rn "TASK_HEADER" tools/` returns nothing, `grep -c "tools.hooks.resume_gate"
    tools/hooks/tests/test_settings_coexist.py` returns `0`, `grep -c '== 8'
    tools/hooks/tests/test_settings_coexist.py` returns `1`, and `uv run pytest -q` is green.
  </read_first>
  <action>
    Step 1 — edit the retirement CAUSE (B-6), all in the working tree, nothing staged yet. In
    `tools/harness_emit/merge.py`: remove `"tools.hooks.resume_gate"` from `HARNESS_SIGNATURES`
    (`:93`), set `RETIRED_SIGNATURES: tuple[str, ...] = ("tools.hooks.resume_gate",)` (`:112`), and
    delete the resume_gate dict from `HARNESS_HOOK_GROUPS["PreToolUse"]` (`:189-199`). In
    `harness/opencode.json`: remove `"harness/plugins/resume-gate.ts"` from the `plugin` array
    (`:31`), leaving only `"harness/plugins/session-inject.ts"`. In
    `tools/harness_emit/tests/test_coexist.py`: delete the resume_gate dict from `_SEED_SETTINGS`'s
    PreToolUse hooks list (`:178-187`) — this MUST be in the same commit as the `merge.py` edit
    because `test_seeded_settings_json_reproduced_byte_for_byte` (`:227-236`) runs a full emit over
    that seed.

    Step 2 — run `python -m tools.harness_emit`. This projects the five repaired
    `harness/commands/*.md` + `harness/agents/orchestrator.md` files into both `.opencode/` and
    `.claude/` trees, drops the `resume_gate` hook group from `.claude/settings.json` (via
    `RETIRED_SIGNATURES`), and rewrites the repo-root `opencode.json` whose `plugin` array no longer
    lists `harness/plugins/resume-gate.ts`. Do NOT hand-edit any file under `.opencode/` or
    `.claude/`, and do NOT hand-edit `tools/harness_emit/emit-manifest.json` — every change must come
    from this emitter run (D-14).
    Per W-2, expect `.opencode/plugin/resume-gate.ts` to still be present and re-emitted, and its
    `emit-manifest.json` row to remain: `harness/plugins/resume-gate.ts` is still on disk until
    43-04 Task 1 deletes it, and `generate.py:172-180`/`:426-432` copy every `harness/plugins/*.ts`
    unconditionally. That file is pruned by 43-04 Task 2's re-emit, not by this one. If the manifest
    shows no diff, that is the correct outcome, not a missed step.

    Step 3 — that emit takes `.claude/settings.json` from 8 PreToolUse groups to 7, so repair the
    hardcoded expectation it invalidates IN THIS SAME COMMIT (B-1): in
    `tools/hooks/tests/test_settings_coexist.py`, locate `test_expected_slot_counts` by name (its
    line number shifted when 43-02 shortened `_NEW_GATES`) and apply the three edits given in
    `<interfaces>` — `== 8` → `== 7`, the assertion message to "expected 7 PreToolUse slots (4 GSD +
    3 harness gates)", and the docstring's first clause to "7 PreToolUse (4 GSD + 3 harness)". Leave
    the `PostToolUse` assertion and message byte-unchanged.

    Step 4 — reset the transient mechanism (D-06). Once the diff looks correct
    (`git diff --stat -- .opencode .claude opencode.json AGENTS.md CLAUDE.md
    tools/harness_emit/emit-manifest.json`), edit `tools/harness_emit/merge.py` to set
    `RETIRED_SIGNATURES: tuple[str, ...] = ()` again — the signature has done its one job. Run
    `python -m tools.harness_emit` a second time and confirm it produces an EMPTY diff (idempotent:
    resetting `RETIRED_SIGNATURES` to `()` must not re-add the already-dropped group, because
    `HARNESS_SIGNATURES` no longer names it and the group is already absent from the emitted file).

    Step 5 — apply the `<emit_snapshot_rule>` (W-4). `harness/opencode.json` is serialized into
    `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` (`:4175-4177` carries the
    literal `"harness/plugins/resume-gate.ts"`), so regenerate it before staging:
    `uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q`, then
    re-run without the flag and confirm exit 0.

    Step 6 — verify green BEFORE committing:
    `uv run pytest tools/hooks/tests/test_settings_coexist.py
    tools/harness_emit/tests/test_settings_merge.py tools/harness_emit/tests/test_coexist.py
    tools/harness_emit/tests/test_emit_determinism.py -q` must exit 0, then `uv run pytest -q`.
    Commit with `git commit -- tools/harness_emit/merge.py harness/opencode.json
    tools/harness_emit/tests/test_coexist.py
    tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
    tools/hooks/tests/test_settings_coexist.py .opencode .claude opencode.json AGENTS.md CLAUDE.md
    tools/harness_emit/emit-manifest.json` only. Never `git add -A`, `git add .`, `git commit -a`,
    or `git checkout <ref> -- .`. Inspect `git diff --cached --name-only` first (D-13) and confirm
    `merge.py`, `harness/opencode.json`, both test files, the `.ambr` and the emitted
    `.claude/settings.json` are ALL staged together — the whole point of B-6 and B-1 is that they
    land as one commit.
  </action>
  <acceptance_criteria>
    `grep -n "resume_gate\|resume-gate" .claude/settings.json opencode.json harness/opencode.json` returns no output.
    `grep -n "resume_gate" tools/harness_emit/merge.py tools/harness_emit/tests/test_coexist.py` returns no output (`RETIRED_SIGNATURES` was reset to `()` in this same commit, so no match survives).
    `grep -n "RETIRED_SIGNATURES" tools/harness_emit/merge.py` shows `RETIRED_SIGNATURES: tuple[str, ...] = ()`.
    Idempotence is a BEFORE-vs-AFTER comparison across the second emitter run, NOT a comparison against HEAD. Measured 2026-07-28: `git diff --stat -- .opencode .claude opencode.json` after the second run is **not** empty — it shows 12 files, which is exactly the Wave-1 propagation this commit exists to make. The real assertion is that the second run ADDS nothing to the first:
    ```
    git diff -- .opencode .claude opencode.json > /tmp/emit-before.diff
    python -m tools.harness_emit
    git diff -- .opencode .claude opencode.json > /tmp/emit-after.diff
    diff /tmp/emit-before.diff /tmp/emit-after.diff     # must exit 0
    ```
    `diff` exits 0, proving the re-emit is idempotent after `RETIRED_SIGNATURES` was reset to `()` (`.claude/settings.json` stays at 7 PreToolUse groups; the reset does not re-add the dropped group). The post-commit form of the same check — `python -m tools.harness_emit && git diff --exit-code -- .opencode .claude opencode.json` exits 0 — is also valid and is what the `emit-drift` CI job runs.
    B-1: `uv run python -c "import json; h=json.load(open('.claude/settings.json'))['hooks']; assert len(h['PreToolUse'])==7, len(h['PreToolUse']); assert len(h['PostToolUse'])==4, len(h['PostToolUse']); assert len(h['SessionStart'])==4, len(h['SessionStart'])"` exits 0.
    B-1: `grep -c 'PreToolUse"\]) == 7' tools/hooks/tests/test_settings_coexist.py` returns `1`; `grep -c "4 GSD + 3 harness" tools/hooks/tests/test_settings_coexist.py` returns `2` (assertion message + docstring); `grep -c "4 GSD + 4 harness" tools/hooks/tests/test_settings_coexist.py` returns `0`; `grep -c 'PostToolUse"\]) == 4' tools/hooks/tests/test_settings_coexist.py` returns `1` (PostToolUse untouched).
    B-6: `uv run pytest tools/harness_emit/tests/test_settings_merge.py tools/harness_emit/tests/test_coexist.py tools/hooks/tests/test_settings_coexist.py -q` exits 0 — `test_settings_merge.py` is the file the previous cause/effect split reded; it is the direct proof B-6 is closed.
    W-4: `uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q` exits 0 (run WITHOUT `--snapshot-update`); `grep -c "resume-gate" tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` returns `0`.
    W-2: `test -f .opencode/plugin/resume-gate.ts` exits 0 and `grep -c "resume-gate.ts" tools/harness_emit/emit-manifest.json` returns at least `1` — the plugin file and its manifest row are CORRECTLY still present at this wave; 43-04 Task 2 prunes them. An executor that hand-removes either here has violated D-14.
    `git log -1 --name-status` shows `.claude/settings.json`, `tools/harness_emit/merge.py`, `harness/opencode.json`, `tools/harness_emit/tests/test_coexist.py`, `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` and `tools/hooks/tests/test_settings_coexist.py` all modified in this single commit.
    B-4 (scoped, NOT repo-wide): `grep -rnE "tools\.handoff|tools\.evidence|tools\.capability" .opencode/command/checkpoint.md .opencode/command/orient.md .opencode/command/review.md .opencode/command/verify-work.md .opencode/agent/orchestrator.md .claude/commands/checkpoint.md .claude/commands/orient.md .claude/commands/review.md .claude/commands/verify-work.md .claude/agents/orchestrator.md` returns no output (exit 1). A repo-wide grep here would still match `handoff.md`, `resume-gate.ts` and the 5 discipline `SKILL.md`s, all of which are Wave 3's to delete — that sweep lives at 43-04 Task 2, not here.
    `uv run pytest -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full-suite verification gate before Wave 3 deletion begins</name>
  <files></files>
  <read_first>
    `.planning/phases/43-lifecycle-plane-removal/43-VALIDATION.md` §"Sampling Rate" and §"Per-Task
    Verification Map" — this task is the "after every wave" full-suite checkpoint. Also this plan's
    objective §"Revision note (B-4)" for why the structural sweep here is name-scoped, and
    §"Revision note (W-2)" for what is legitimately still present at this wave.
  </read_first>
  <action>
    Run the full verification set now that Wave 1's repairs are committed and re-emitted, and BEFORE
    any Wave-3 deletion task begins: `uv run pytest -q` (expect green — nothing has been deleted yet,
    only repaired and re-emitted, and Task 1 landed the retirement cause, the emit, and the
    slot-count correction together), `uv run python -m tools.contract_drift.drift` (expect exit 0 —
    no contract touched this wave), and a name-scoped structural sweep confirming the five D-01
    artifacts (both source and emitted copies — 15 files) name nothing this phase will delete. Record
    the pytest pass count in the plan SUMMARY as the pre-deletion baseline Plan 43-04 and 43-05 must
    not regress below (minus the counts of tests this phase intentionally deletes with their
    modules, and minus the 3 tests Wave 1 removed: 2 in `test_inject_assembler.py`, 1 in
    `test_handoff.py`).
  </action>
  <acceptance_criteria>
    `uv run pytest -q` exits 0 — fully green, no tolerated failure. This phase sanctions no expected-red window; any red here is a real defect.
    `uv run python -m tools.contract_drift.drift` exits 0.
    B-4 (scoped sweep over the 15 D-01 files — 5 source + 10 emitted): `grep -rnE "tools\.handoff|tools\.evidence|tools\.capability|resume_gate|resume-gate" harness/commands/checkpoint.md harness/commands/orient.md harness/commands/review.md harness/commands/verify-work.md harness/agents/orchestrator.md .opencode/command/checkpoint.md .opencode/command/orient.md .opencode/command/review.md .opencode/command/verify-work.md .opencode/agent/orchestrator.md .claude/commands/checkpoint.md .claude/commands/orient.md .claude/commands/review.md .claude/commands/verify-work.md .claude/agents/orchestrator.md` returns no output (exit 1).
    The repo-wide form of that sweep is NOT an acceptance criterion of this plan — it is 43-04 Task 2's, and running it here will (correctly) still match `harness/commands/handoff.md`, `harness/plugins/resume-gate.ts`, `.opencode/plugin/resume-gate.ts`, the two emitted `handoff.md` copies, and the 5 discipline `SKILL.md`s, all scheduled for Wave 3.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Emitter output vs hand-edited files | Every change in this plan must originate from `python -m tools.harness_emit`, never a direct edit to `.opencode/**`, `.claude/**`, root `opencode.json`, or `emit-manifest.json` |
| Emitted config vs the tests that hardcode its shape | `.claude/settings.json` may only change via the emitter, so any test literal counting its groups — and any merge-logic change that alters what the emitter produces for it — must land in the plan that runs the emitter |
| Retirement cause vs retirement effect | `merge.py`'s signature/group edit is the CAUSE; the emitted 8→7 file and the test literals are the EFFECT. Separating them across commits reds `test_settings_merge.py` and `test_coexist.py`, neither of which any earlier plan may repair (B-6) |
| Gate scope vs wave position | A gate that asserts a post-Wave-3 condition at Wave 2 is unsatisfiable and silently blocks the phase; every sweep here is scoped to what Wave 1 actually repaired |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | Hand-editing an emitted file (or `emit-manifest.json`) instead of re-running the emitter | mitigate | D-14 explicit rule; acceptance criteria assert an idempotent second re-emit, which would fail if a hand-edit diverged from what the emitter itself produces; W-2 explicitly forbids hand-pruning the manifest row that legitimately survives this wave |
| T-43-11 | Denial of Service | The re-emit dropping PreToolUse group 8 while `test_expected_slot_counts` still asserts 8, leaving the phase gate red and Wave 3 blocked (B-1) | mitigate | The literal, its message and its docstring are corrected in this same commit; acceptance criteria assert both the emitted file (7 groups) and the test file appear in one `git log -1 --name-status` |
| T-43-17 | Denial of Service | `merge.py`'s retirement edit landing a wave early, reding `test_settings_merge.py`'s byte-for-byte reproduction of the LIVE settings.json for three commits (B-6) | mitigate | Cause, emit and both invalidated literals are one commit here; `test_settings_merge.py` green is an explicit acceptance criterion |
| T-43-12 | Denial of Service | An unsatisfiable D-02 gate (repo-wide grep at Wave 2) permanently blocking Wave 3, or being waived by hand and thereby waiving the real gate (B-4) | mitigate | Both greps name the 5 surviving D-01 artifacts explicitly; the repo-wide form is stated as out of scope here and located at 43-04 Task 2 |
| T-43-16 | Denial of Service | `harness/opencode.json` edited without regenerating `test_emit_determinism.ambr`, leaving the phase's own Wave-2 gate commit red (W-4) | mitigate | `<emit_snapshot_rule>` restated here; Task 1 step 5 regenerates the snapshot and an acceptance criterion re-runs the test without `--snapshot-update` |
| T-43-19 | Repudiation | An executor "fixing" the surviving `.opencode/plugin/resume-gate.ts` or its manifest row by hand at this wave, believing the plan's (previously wrong) pruning claim | mitigate | W-2 corrected in the objective, the must_haves, and as a positive acceptance criterion asserting the file and row are STILL present here |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan |
</threat_model>

<verification>
`python -m tools.harness_emit && git diff --stat -- .opencode .claude opencode.json` — empty after the reset-and-second-run sequence.
`uv run python -c "import json; h=json.load(open('.claude/settings.json'))['hooks']; assert len(h['PreToolUse'])==7 and len(h['PostToolUse'])==4 and len(h['SessionStart'])==4"` — exit 0.
`uv run pytest tools/hooks/tests/test_settings_coexist.py tools/harness_emit/tests/test_settings_merge.py tools/harness_emit/tests/test_coexist.py tools/harness_emit/tests/test_emit_determinism.py -q` — exit 0.
`uv run pytest -q` — green, with no tolerated failure anywhere in the phase.
</verification>

<success_criteria>
Both runtime trees reflect every Wave-1 repair; the `resume_gate` retirement cause, its emitted
effect, and both invalidated test literals landed in one commit; `.claude/settings.json` has 7
PreToolUse groups and `test_expected_slot_counts` expects 7; `RETIRED_SIGNATURES` is back to `()`;
the projection snapshot no longer lists `harness/plugins/resume-gate.ts`; `.opencode/plugin/resume-gate.ts`
and its manifest row are still present and untouched (43-04's to prune); full suite green; none of
the five surviving D-01 artifacts — in either tree — names a module Wave 3 is about to delete.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-03-SUMMARY.md` when done
</output>
