---
phase: 44-non-goal-surface-removal
plan: 02
subsystem: harness-emit / runtime-gates
tags: [deletion, secret_scan, retired-signatures, tombstone, CER-08]
requires: ["44-01"]
provides:
  - "RETIRED_SIGNATURES with two permanent tombstones (resume_gate, secret_scan)"
  - "A membership pin per tombstone that reds when the tuple is cleared"
  - "PreToolUse reduced to 6 slots; no secret_scan anywhere"
affects: [tools/harness_emit, tools/hooks, tools/harness_lint, harness/commands, README.md]
tech-stack:
  added: []
  patterns: ["append-only tombstone tuple", "membership pin + loop, not loop alone"]
key-files:
  created: []
  modified:
    - tools/harness_emit/merge.py
    - tools/harness_emit/tests/test_settings_merge.py
    - tools/harness_emit/tests/test_coexist.py
    - tools/hooks/tests/test_settings_coexist.py
    - README.md
    - harness/commands/review.md
  deleted:
    - tools/hooks/secret_scan.py
    - tools/hooks/tests/test_secret_scan.py
    - harness/plugins/secret-scan.ts
    - .opencode/plugin/secret-scan.ts
decisions: [D-06, D-07, D-08, D-14, D-15, D-16, D-17, D-18]
metrics:
  commits: 2
  duration: ~20m
  completed: 2026-07-29
---

# Phase 44 Plan 02: secret_scan whole-surface removal Summary

`secret_scan` is gone in every form — hook module, test, `.ts` plugin, emitter signature, emitted
hook group and prose — with no replacement, and the `RETIRED_SIGNATURES` coverage hole that let
Phase 43 ship a stale-checkout defect is closed by a membership pin that reds on the exact
"clear the tuple" move.

## Commits

| Hash | Message | Shortstat |
|------|---------|-----------|
| `a494cd3` | `test(44-02): cover every RETIRED_SIGNATURES tombstone, not entry [0]` | 1 file changed, 36 insertions(+), 17 deletions(-) |
| `ddfa6af` | `chore(44-02): delete secret_scan whole and append its permanent tombstone` | 24 files changed, 50 insertions(+), 536 deletions(-) |

## Mutation results (verbatim)

**Task 1 — clear `RETIRED_SIGNATURES` (`= ()`), run `test_settings_merge.py`:**

```
E       AssertionError: tools.hooks.resume_gate was dropped from RETIRED_SIGNATURES — tombstones are permanent; a checkout still carrying the pre-43 settings.json would invoke a deleted module
E       assert 'tools.hooks.resume_gate' in ()
E        +  where () = merge.RETIRED_SIGNATURES
tools/harness_emit/tests/test_settings_merge.py:168: AssertionError
FAILED tools/harness_emit/tests/test_settings_merge.py::test_retired_signature_group_is_dropped_from_a_stale_checkout
FAILED tools/harness_emit/tests/test_settings_merge.py::test_retired_signatures_are_permanent_tombstones
2 failed, 6 passed in 0.01s
```

Restored with `git checkout HEAD -- tools/harness_emit/merge.py` (explicit single pathspec) →
`8 passed in 0.01s`.

**Task 2 — delete only `"tools.hooks.secret_scan"` from the tuple, leaving `resume_gate`:**

```
E       AssertionError: tools.hooks.secret_scan was dropped from RETIRED_SIGNATURES — tombstones are permanent; a checkout still carrying the pre-44 settings.json would invoke a deleted module
E       assert 'tools.hooks.secret_scan' in ('tools.hooks.resume_gate',)
E        +  where ('tools.hooks.resume_gate',) = merge.RETIRED_SIGNATURES
tools/harness_emit/tests/test_settings_merge.py:173: AssertionError
FAILED tools/harness_emit/tests/test_settings_merge.py::test_retired_signatures_are_permanent_tombstones
1 failed, 7 passed in 0.01s
```

Restored → `8 passed`. Note this is the non-vacuous case the plan cares about: the **loop stayed
green** on this mutation (7 passed), only the membership pin red. The plan predicted the red at
`:171`; it landed at `:173` — a 2-line offset from an unrelated docstring rewrap in the same file.
Nothing asserts the line number.

## emit-manifest.json row-delta

Exactly one row pruned, self-generated by the re-emit (never hand-edited):

```
-    ".opencode/plugin/secret-scan.ts",
```

## Verification

- `uv run pytest -q` → **933 passed, 7 snapshots** (was 950; −18 from `test_secret_scan.py`'s
  deletion, −1 from the retired `_NEW_GATES` row, +2 from this plan's new assertions/test).
- `uv run pytest examples/log-parser/tests -q` → **14 passed**
- `uv run python -m tools.harness_emit` → **83 artifacts, exit 0**; `git diff --exit-code` clean
- `uv run python -m tools.contract_drift.drift` → OK
- `uv run python -m tools.ruff_baseline` → **80 findings (baseline 84)** — PASS, no rule class above
  baseline. `--update` deliberately NOT run.
- `python3 tools/harness_lint/workspace_check.py` → OK (no lock refresh needed; `tools/hooks`
  remains a workspace member)
- `.claude/settings.json`: **PreToolUse 6 / PostToolUse 4**, string `secret_scan` absent
- `uv run pytest tools/adoption_scan/tests -q` → **88 passed** with `SECRET_PATH_GLOBS` and
  `SECRET_CONTENT_PATTERNS` byte-unchanged (diff touches comments only) — D-08 proof
- `! grep -n "secret-scan" README.md` → exit 0; `README.md:119` untouched
- `git status --porcelain` empty after both commits
- No replacement anywhere: no new hook module, no CI job, no `.pre-commit-config.yaml` entry

Final `git grep "secret_scan\|secret-scan" -- tools harness libs examples .github` returns 5 lines,
all of them the tombstone itself (`merge.py:118,123`, `test_settings_merge.py:172-174`). Nothing
names a live hook.

## Documented intra-commit red

Between `git rm` and `git commit`, exactly the three `adoption_scan` tests named in the plan were
red (`test_catalog_invariant_to_untracked_local_state`,
`test_contract_candidate_matches_real_repo_schema_count`, `test_ci_yml_false_positive_closed`) —
the `git ls-files` + `stat()` root cause. All three went green at the commit. Not repaired.

## Deviations from Plan

Two prose sites named `secret_scan` that the `<measured_anchors>` sweep list did not enumerate, but
which the plan's own `<verification>` block forbids ("nothing that names a live hook"). Both files
were already in `files_modified`; both edits are comment/docstring-only:

1. **`tools/harness_emit/tests/test_settings_merge.py:4`** — the module docstring listed
   `secret_scan` among the four hand-wired live Phase-4 harness hooks. Now lists three.
2. **`harness/commands/review.md:3-4`** — the frontmatter `description` advertised "secret-scan
   posture", the step this commit deletes. Reworded to "surfaces the working diff". Re-emitted and
   snapshot-updated with the rest.

One repair the plan told me to discover by running rather than reading (D-18):
**`tools/harness_emit/tests/test_coexist.py`** — the `_SEED_SETTINGS` fixture seeded a
`Read|Write|Edit` `secret_scan` group;
`test_seeded_settings_json_reproduced_byte_for_byte` failed because the retired group is now
correctly dropped by the merge. Fix was to remove the group from the seed. The file's command count
and `test_all_NN_commands_emit_to_both_trees` name were not touched.

Nothing else deviated. No criterion failed to hold.

## Unanticipated

- Nothing material. The one surprise worth recording: my first `test_settings_coexist.py` docstring
  edit collapsed a wrapped line to 155 chars, which would have added an `F`/`E501` finding above
  baseline. Caught by re-wrapping before staging; the ratchet stayed at 80.
- The Task 1 mutation (clearing the tuple entirely) reds **two** tests, not one — the pre-existing
  non-vacuity guard `assert merge.RETIRED_SIGNATURES` also fires on an empty tuple. The
  single-entry-deletion mutation in Task 2 is the sharper control and reds the pin alone.

## Self-Check: PASSED

- `tools/hooks/secret_scan.py` MISSING (expected)
- `harness/plugins/secret-scan.ts` MISSING (expected)
- `.opencode/plugin/secret-scan.ts` MISSING (expected)
- commit `a494cd3` FOUND
- commit `ddfa6af` FOUND
- `.planning/phases/44-non-goal-surface-removal/44-02-SUMMARY.md` FOUND
