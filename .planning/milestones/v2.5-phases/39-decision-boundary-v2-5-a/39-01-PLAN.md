---
phase: 39-decision-boundary-v2-5-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0001-walking-skeleton-golden-core.md
  - docs/adr/0010-human-docs-review-obligation-model.md
  - docs/adr/0011-gate-right-sizing-dev-light-ci-strong.md
  - docs/adr/0012-ci-and-merge-as-decision-authority.md
  - docs/adr/README.md
  - .planning/phases/39-decision-boundary-v2-5-a/39-01-SUMMARY.md
autonomous: false
requirements: [CER-01, CER-02]

must_haves:
  truths:
    - "docs/adr/0012-ci-and-merge-as-decision-authority.md exists with Status: accepted and non-empty Date/Deciders"
    - "ADR-0012 names every v2.5-deleted surface (Phases 40-44's targets) component by component, and declares the bash surface a permanent residual by design"
    - "ADR-0012 scopes clause (b) as intent recorded at ratification time — a later phase narrowing or widening its own scope does not falsify or require superseding this ADR"
    - "ADR-0012 states the DEV/PRODUCT boundary and its operative rule (no product capability may be declined because GSD covers it; only a named shipped artifact may cover it), citing tools/adoption_scan/destinations.py::_CATEGORY_GLOBS and generate.py:41-43"
    - "ADR-0012 states that the Phase 39 human-ratification checkpoint is a one-time transition step establishing no standing gate"
    - "ADR-0011 has non-empty Date and Deciders and records that its code landed in commit bc9a6d9 before this ratification"
    - "ADR-0001 and ADR-0010 each carry Status: superseded by 0012 and Superseded by: pointing at 0012, with every other line byte-identical to the pre-phase version"
    - "docs/adr/README.md lists rows for 0010 (superseded by 0012), 0011 (accepted), and 0012 (accepted)"
  artifacts:
    - path: "docs/adr/0012-ci-and-merge-as-decision-authority.md"
      provides: "the one new ratified ADR for this phase"
      contains: "- **Status:** accepted"
    - path: "docs/adr/README.md"
      provides: "index rows for 0011 and 0012, and 0010's status flip"
      contains: "0012-ci-and-merge-as-decision-authority.md"
  key_links:
    - from: "docs/adr/0001-walking-skeleton-golden-core.md"
      to: "docs/adr/0012-ci-and-merge-as-decision-authority.md"
      via: "Superseded by: frontmatter pointer"
      pattern: "Superseded by:.*0012"
    - from: "docs/adr/0010-human-docs-review-obligation-model.md"
      to: "docs/adr/0012-ci-and-merge-as-decision-authority.md"
      via: "Superseded by: frontmatter pointer"
      pattern: "Superseded by:.*0012"
    - from: "docs/adr/0011-gate-right-sizing-dev-light-ci-strong.md"
      to: "bc9a6d9"
      via: "code-before-ratification note in an added Consequences/Links line"
      pattern: "bc9a6d9"
---

<objective>
Author the single human-ratified ADR-0012 that makes CI + the merge the decision authority for
milestone v2.5, ratifies the DEV/PRODUCT boundary and its operative rule, supersedes ADR-0001's
constitution-member list and retires ADR-0010, and formally accepts ADR-0011 (filling its empty
`Date`/`Deciders` and recording that its code landed in commit `bc9a6d9` before ratification).

Purpose: every later v2.5 deletion phase (40-46) needs a written decision to cite instead of
re-litigating scope — this is that decision, landed per CER-01 and CER-02. Per owner decision D2
(39-REVIEWS.md Divergent Views), ADR-0012 must also state that the human-ratification checkpoint
this phase uses is a one-off transition step, not a new standing gate — consistent with the
milestone's own de-ceremony goal (v2.4→v2.5 takes human-authored gates 5→0).

Output: `docs/adr/0012-ci-and-merge-as-decision-authority.md` (new), `docs/adr/0001-*.md` and
`docs/adr/0010-*.md` (frontmatter-only supersede edit), `docs/adr/0011-*.md` (frontmatter fill +
one added note), `docs/adr/README.md` (index rows).

**Hard constraint — read before starting:** `docs/adr/**` is on the constitution plane.
`tools/hooks/contract_guard.py:53` (`CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**",
"golden/**", "docs/glossary.md"]`) denies any agent `Write`/`Edit` under this tree unless a human
has set a non-empty `GOLDEN_APPROVE_HUMAN` in the session env. **The executor must NEVER edit
`contract_guard.py`, `CONSTITUTION_GLOBS`, the `GOLDEN_APPROVE_HUMAN` check, or any hook/permission
file to get past this deny.** The only sanctioned path is: ask the human to set the token in the
session environment (this is the ONE delivery path — do not offer `.claude/settings.local.json` or
any other path as an alternative), wait for their explicit approval, perform the writes, then ask
them to unset it. This plan has no other tasks besides these gated ADR edits — do not add code,
gate, or CI changes (non-goal, per ROADMAP Phase 39 scope).

**Revision note (this plan was revised per cross-AI review; see 39-REVIEWS.md):** exact-count
verification, dirty-worktree baselines, single token path, confirm-at-ratification Date/Deciders,
precise + time-scoped clause (b), and the one-off-checkpoint clause (f) are all corrections applied
in this revision — do not regress them in future edits to this plan.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-decision-boundary-v2-5-a/39-RESEARCH.md
@.planning/phases/39-decision-boundary-v2-5-a/39-VALIDATION.md
@.planning/phases/39-decision-boundary-v2-5-a/39-REVIEWS.md
@docs/adr/README.md
@docs/adr/0001-walking-skeleton-golden-core.md
@docs/adr/0010-human-docs-review-obligation-model.md
@docs/adr/0011-gate-right-sizing-dev-light-ci-strong.md
@harness/commands/adr.md
@tools/hooks/contract_guard.py

<interfaces>
<!-- The exact frontmatter shape every ADR in this repo uses (from docs/adr/0001-*.md:1-9 and
0010-*.md:1-14) — the executor must reproduce this shape verbatim for the new ADR-0012, and touch
ONLY the listed lines on 0001/0010/0011. -->

ADR frontmatter block (verified against all 11 existing records):
```
# N. Title

*MADR 4.x · plane: constitution (human-owned, immutable, append-only)*

- **Status:** accepted
- **Date:** YYYY-MM-DD
- **Deciders:** kimhyojung (CODEOWNERS)
- **Supersedes:** —
- **Superseded by:** —

## Context and Problem Statement
## Decision Drivers
## Considered Options
## Decision Outcome
## Consequences
## Links
```

Current state of the three edited files (verified this session, `git show --stat bc9a6d9` and
direct reads):
- `docs/adr/0001-*.md` line 5 `- **Status:** accepted`, line 9 `- **Superseded by:** —`. Body
  (lines 11-62) is the walking-skeleton decision and the four-member constitution-plane
  declaration at line 48 — do not touch any of it.
- `docs/adr/0010-*.md` line 5 `- **Status:** accepted` (frontmatter), line 9
  `- **Superseded by:** —`. Note: the file's own `## Approval` section (near line 395) contains
  stale prose ("Not yet ratified... Status: proposed") that contradicts its own frontmatter, and
  `docs/adr/README.md` also still shows it as `proposed` — this is a pre-existing inconsistency in
  the repo, not something this phase introduces or is asked to fix. Do not edit the `## Approval`
  section or any other body text; only the two frontmatter lines (`Status`, `Superseded by`)
  change, exactly as with 0001.
- `docs/adr/0011-*.md` line 3 `- **Status:** proposed`, line 4 `- **Date:** —`, line 5
  `- **Deciders:** —`. No `Supersedes`/`Superseded by` lines exist in this file at all — do not add
  them (out of scope; only Status/Date/Deciders are required non-empty per CER-01).
- `docs/adr/README.md` line 39 lists ADR-0010 as `proposed`; there is no row at all for ADR-0011.

<!-- Corrections applied per 39-REVIEWS.md consensus + owner decisions D1/D2 (see
39-REVIEWS.md "Consensus Summary" and "Divergent Views"). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Present the ADR-0012 content plan and the exact 0001/0010/0011 diffs for ratification</name>
  <files>docs/adr/0012-ci-and-merge-as-decision-authority.md, docs/adr/0001-walking-skeleton-golden-core.md, docs/adr/0010-human-docs-review-obligation-model.md, docs/adr/0011-gate-right-sizing-dev-light-ci-strong.md, docs/adr/README.md</files>
  <action>
    Draft the ADR-0012 content and the exact 0001/0010/0011/README diffs described below. Do NOT
    call Write/Edit against any `docs/adr/**` path in this task — `contract_guard.py` will deny it
    without a human-set `GOLDEN_APPROVE_HUMAN`, and the deny must never be worked around. Present
    the drafts to the human per `<what-built>` and wait for `<resume-signal>`. If the human requests
    changes instead of approving, revise the draft addressing exactly the requested changes and
    re-present the full updated draft under the same `<what-built>` structure — do not proceed to
    Task 2 until an explicit "approved" is received on a specific draft version. Repeat this
    revise-and-re-present loop as many times as the human requires.
  </action>
  <read_first>
    docs/adr/0001-walking-skeleton-golden-core.md, docs/adr/0010-human-docs-review-obligation-model.md,
    docs/adr/0011-gate-right-sizing-dev-light-ci-strong.md, docs/adr/README.md,
    harness/commands/adr.md, .planning/ROADMAP.md (Phase 39 detail + the "DEV/PRODUCT boundary"
    paragraph in the v2.5 milestone block, and the Phase 40-44 detail sections for the precise
    component names each phase deletes), .planning/REQUIREMENTS.md (CER-01, CER-02),
    .planning/phases/39-decision-boundary-v2-5-a/39-REVIEWS.md (owner decisions D1/D2)
  </read_first>
  <what-built>
    A complete draft (not yet written to disk — `docs/adr/**` is constitution-plane and any
    Write/Edit will be denied by `tools/hooks/contract_guard.py` until you set the ratification
    token) of:
    1. The full text of `docs/adr/0012-ci-and-merge-as-decision-authority.md`, following the
       frontmatter shape in `<interfaces>` above, `Status: accepted`, `Date:` — leave as a
       placeholder for the human to confirm or correct at this checkpoint (do not assume today's
       date is correct if ratification lands on a later day), `Deciders:` — likewise a placeholder
       for the human to confirm or correct (do not assume `kimhyojung (CODEOWNERS)` is final without
       asking), `Supersedes: 0001, 0010`, `Superseded by: —`, and a `Decision Outcome` adopted as ONE
       ratified unit (mirroring ADR-0010's numbered-clause style) covering exactly these six clauses:
       (a) **CI + the merge are the authority** — enforcement's home is CI fan-in and the merge to
           `main` being the acceptance point, under this repository's applicable review policy —
           do NOT assert that CODEOWNERS review is enforced by branch protection as a fact (branch
           protection was never confirmed this session per 39-REVIEWS.md codex finding); phrase it
           as "CI + the merge, under whatever review policy this repository applies," not a claim
           about a specific enforced mechanism;
       (b) **names every surface this milestone deletes, component by component, scoped as intent
           recorded at ratification time** — enumerate the exact components each of Phase 40-44
           deletes (read each phase's ROADMAP.md detail section and list the actual artifact names,
           not bundle labels). The phase-to-component mapping is ROADMAP.md lines 195-234 and must
           be reproduced EXACTLY — do not reorder or reassign:
           **Phase 40** — `tools/skill_registry`, `harness/skills/registry.lock`, CI `registry-lock`
           + its `gate.needs` entry.
           **Phase 41** — `tools/docs_guard`, the review ledger, hook `ledger_guard` + its
           `path_deny_globs` entry, the `/docs-update` command, skill `docs-upkeep`,
           `contracts/harness/docs/*`, CI `docs-guard` + its `gate.needs` entry.
           **Phase 42** — decoupling, not deletion (adoption/task-control decoupling +
           `_CATEGORY_GLOBS` repair); include it only as a named non-deletion so the enumeration is
           not read as exhaustive-of-deletions-only.
           **Phase 43** — 8 `tools/` packages, the 7 task-control contracts, commands
           `intake·phase-gate·handoff·discipline`, hook `resume_gate`, the 5 discipline skills,
           `harness/{capabilities,disciplines,risk-policy}.toml`, `.workflow/tasks/`, CI
           `lifecycle-eval` + its `gate.needs` entry.
           **Phase 44** — `secret_scan`, `deny-domains.*`, `gate-registry.json` + their
           `DATA_CONTRACT_PATHS` entries, `tools/memory_ui`, `tools/strangler_guard` +
           `/strangler-step`, `/pipeline` + `pipeline-map` + `[pipeline].edges`, skill `gate-model`,
           `/component`'s topology half, **and** the golden stack's relocation to
           `examples/log-parser/` — all one phase, not two. Immediately
           follow this enumeration with an explicit sentence: this clause records the enumeration
           as *intent at the ratification date*, and a later phase narrowing or widening its own
           scope does NOT falsify this ADR and does NOT require superseding it;
       (c) **the DEV/PRODUCT boundary + its operative rule** — DEV is this checkout (Claude Code +
           GSD, never installed); PRODUCT is what
           `tools/adoption_scan/destinations.py::_CATEGORY_GLOBS` installs into a target monorepo
           (cite that file, and `tools/harness_emit/generate.py:41-43` as the emitter that projects
           this checkout into itself, distinct from the install channel); the operative rule: *no
           product capability may be declined on the ground that GSD covers it; only a named
           shipped artifact may cover it*;
       (d) **supersedes ADR-0001's constitution-member list (golden leaves the core) and retires
           ADR-0010** — state plainly that between this ADR and Phase 44's actual code move,
           `tools/hooks/contract_guard.py`'s `CONSTITUTION_GLOBS` and the pinned test
           `tools/hooks/tests/test_contract_guard.py:352-375` will KNOWINGLY still enforce
           `golden/**` as a fourth member — name this as an expected, temporary ADR-vs-code
           inconsistency assigned to Phase 44, not a defect of this ADR. Repeat the exact test path
           `tools/hooks/tests/test_contract_guard.py:352-375` again under the `## Consequences`
           section specifically (not only inside the Decision Outcome clause), so a future executor
           reading only Consequences still finds it without re-deriving it;
       (e) **the bash surface is a permanent residual by design** — cite ADR-0011's "What this
           deliberately accepts" section and `.planning/STATE.md`'s per-tool-deny-spelling finding
           as the two things being closed as obsolete-by-deletion (not repaired);
       (f) **this phase's human checkpoint is a one-time transition step, not a standing gate** —
           state explicitly that the human-ratification checkpoint used to land ADR-0012 itself
           (Task 1 of this plan) is a **one-time transition** step for Phase 39 only — the ADR text
           MUST contain the literal phrase `one-time transition`, because Task 2's automated verify
           greps for it — that no new
           recurring human-authored gate is created by this milestone, and that this is consistent
           with the v2.5 de-ceremony goal of reducing human-authored gates from 5 to 0.
       Add `- **Complements:** [ADR-0011](0011-gate-right-sizing-dev-light-ci-strong.md)` to the
       frontmatter per the research's recommendation (cites, does not supersede, ADR-0011).
    2. The exact diff for `docs/adr/0001-*.md`: change `- **Status:** accepted` (line 5) to
       `- **Status:** superseded by 0012`, and line 9 `- **Superseded by:** —` to
       `- **Superseded by:** [0012](0012-ci-and-merge-as-decision-authority.md)`. No other line
       changes.
    3. The exact diff for `docs/adr/0010-*.md`: same two-line pattern — `Status` to
       `superseded by 0012`, `Superseded by` to the 0012 pointer. No other line changes (leave the
       stale `## Approval` section exactly as-is; do not touch it).
    4. The exact diff for `docs/adr/0011-*.md`: `Status: proposed` to `Status: accepted`,
       `Date: —` to the date the human confirms at this checkpoint, `Deciders: —` to the
       decider name/handle the human confirms at this checkpoint; plus ONE new
       `## Ratification note` subsection (added, never replacing existing prose) stating verbatim
       that its code landed in commit `bc9a6d9` ("feat(hooks): guard commands degrade instead of
       deadlock, and dev can opt out") before this ADR's formal ratification.
    5. The exact new rows for `docs/adr/README.md`: flip 0010's status cell to
       `superseded by 0012`; add a row for `[0011](0011-gate-right-sizing-dev-light-ci-strong.md) |
       Gate right-sizing — dev-light, CI-strong | accepted`; add a row for
       `[0012](0012-ci-and-merge-as-decision-authority.md) | CI and the Merge as Decision Authority
       | accepted`.
  </what-built>
  <how-to-verify>
    Read the five drafted texts/diffs above in full. Confirm: (1) ADR-0012 covers all six clauses,
    cites `tools/adoption_scan/destinations.py::_CATEGORY_GLOBS` + `generate.py:41-43` verbatim, and
    clause (b) names components by name (not bundle labels) with the ratification-intent-scoping
    sentence present; (2) the ADR-0001/0010 edits touch ONLY the `Status`/`Superseded by` lines,
    nothing else; (3) the ADR-0011 edit fills `Date`/`Deciders` and adds (never replaces) the
    `bc9a6d9` note; (4) the README rows are additive; (5) clause (a) does not assert CODEOWNERS
    branch-protection enforcement as fact; (6) clause (f) states the checkpoint is one-off, no
    standing gate created.
    Confirm or correct the `Date` and `Deciders` values now — state the exact values to use (e.g.
    today's actual date, and the decider name/handle), which the executor will use verbatim in
    Task 2 for both ADR-0011's and ADR-0012's frontmatter.
    If acceptable, set `GOLDEN_APPROVE_HUMAN` to a non-empty value in the session environment
    (this is the only sanctioned delivery path — do not use a settings file) and reply "approved"
    along with the confirmed Date/Deciders values. If changes are needed, describe them instead —
    the executor will revise and re-present, and will NOT write anything to `docs/adr/**` without
    your approval and the token both present.
  </how-to-verify>
  <resume-signal>Type "approved" (with the token set and Date/Deciders confirmed) to proceed to Task 2, or describe required changes to trigger a revise-and-re-present cycle</resume-signal>
  <verify>
    <automated>MISSING — this task performs no file write; verification is the human's explicit "approved" reply (with confirmed Date/Deciders) captured in resume-signal, checked by Task 2's read_first before any write is attempted</automated>
  </verify>
  <done>Human has replied "approved" with GOLDEN_APPROVE_HUMAN set and Date/Deciders confirmed; no docs/adr/** file has been written yet</done>
  <acceptance_criteria>
    - Human has replied "approved" (or equivalent explicit affirmative) in-session, on a specific draft version (not a stale earlier draft if revisions occurred)
    - `GOLDEN_APPROVE_HUMAN` is confirmed non-empty in the session env before Task 2 starts
    - The confirmed `Date` and `Deciders` values are recorded from the human's reply, not assumed
    - No file under `docs/adr/**` has been written yet at the end of this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write the ratified ADR-0012, apply the 0001/0010/0011 frontmatter edits, and update the README index</name>
  <files>docs/adr/0012-ci-and-merge-as-decision-authority.md, docs/adr/0001-walking-skeleton-golden-core.md, docs/adr/0010-human-docs-review-obligation-model.md, docs/adr/0011-gate-right-sizing-dev-light-ci-strong.md, docs/adr/README.md</files>
  <read_first>
    docs/adr/0001-walking-skeleton-golden-core.md, docs/adr/0010-human-docs-review-obligation-model.md,
    docs/adr/0011-gate-right-sizing-dev-light-ci-strong.md, docs/adr/README.md,
    tools/hooks/contract_guard.py (confirm `GOLDEN_APPROVE_HUMAN` is set in env before attempting
    any write — if the write is denied, STOP and report the deny message; do not edit the hook)
  </read_first>
  <action>
    Before any write, capture a dirty-worktree baseline for exactly the five target files: run
    `git status --porcelain -- docs/adr/0001-*.md docs/adr/0010-*.md docs/adr/0011-*.md
    docs/adr/README.md` and record `git rev-parse HEAD:docs/adr/0001-walking-skeleton-golden-core.md`
    (and the equivalent for 0010, 0011, README.md) as the pre-write blob-hash baseline. If the
    `git status --porcelain` output for any of these files is non-empty (already dirty before this
    task starts), STOP and report which file is dirty and its uncommitted diff — do not proceed
    with writes on top of an unprovable baseline, and do not attempt to stash or reconcile it
    yourself.
    With `GOLDEN_APPROVE_HUMAN` set (confirmed by Task 1's approval) and the baseline clean,
    perform exactly these five writes, using the content ratified in Task 1 verbatim (no new claims
    beyond what was approved, and using the human-confirmed `Date`/`Deciders` values from Task 1):
    (1) Write new file `docs/adr/0012-ci-and-merge-as-decision-authority.md` with the frontmatter
    and six-clause Decision Outcome from Task 1. (2) Edit `docs/adr/0001-*.md`: change only the
    `Status` line to `superseded by 0012` and the `Superseded by` line to the 0012 pointer. (3) Edit
    `docs/adr/0010-*.md`: identical two-line change. (4) Edit `docs/adr/0011-*.md`: change `Status`
    to `accepted`, fill `Date` and `Deciders` with the confirmed values, and append the new
    `## Ratification note` subsection citing `bc9a6d9`. (5) Edit `docs/adr/README.md`: flip 0010's
    status cell, add the 0011 row and the 0012 row, per Task 1's exact text. Do not touch any file
    outside this list. Do not edit `tools/hooks/contract_guard.py` or any permission/hook file
    under any circumstance — if any write is denied, stop and report the exact denial message
    instead of working around it.
    After all five writes land, compute the post-write diff for 0001, 0010, and README against the
    pre-write baseline captured above (not a blind `git diff` on a tree assumed clean) and confirm
    only the expected lines changed. Then tell the human to unset `GOLDEN_APPROVE_HUMAN`.
  </action>
  <verify>
    <automated>test -f docs/adr/0012-ci-and-merge-as-decision-authority.md; test "$(grep -c '^- \*\*Status:\*\* accepted' docs/adr/0012-*.md)" -eq 1; test "$(grep -c '^- \*\*Supersedes:\*\* 0001, 0010' docs/adr/0012-*.md)" -eq 1; test "$(grep -c 'no product capability may be declined' docs/adr/0012-*.md)" -eq 1; test "$(grep -c 'one-time transition' docs/adr/0012-*.md)" -ge 1; test "$(grep -c 'tools/hooks/tests/test_contract_guard.py:352-375' docs/adr/0012-*.md)" -ge 2; test "$(grep -cE '^- \*\*(Date|Deciders):\*\* [^—[:space:]]' docs/adr/0011-*.md)" -eq 2; test "$(grep -c 'bc9a6d9' docs/adr/0011-*.md)" -eq 1; test "$(grep -c 'Superseded by:.*0012' docs/adr/0001-*.md)" -eq 1; test "$(grep -c 'Superseded by:.*0012' docs/adr/0010-*.md)" -eq 1; test "$(grep -c '\[0011\]' docs/adr/README.md)" -eq 1; test "$(grep -c '\[0012\]' docs/adr/README.md)" -eq 1</automated>
  </verify>
  <done>
    `docs/adr/0012-ci-and-merge-as-decision-authority.md` exists and is `accepted`, containing all
    six clauses; `docs/adr/0001-*.md` and `docs/adr/0010-*.md` show `Status: superseded by 0012`
    with only frontmatter lines changed relative to the pre-write baseline (confirmed by comparing
    against the recorded blob hashes, not a blind `git diff`); `docs/adr/0011-*.md` has non-empty
    `Date`/`Deciders` (the human-confirmed values) and cites `bc9a6d9`; `docs/adr/README.md` lists
    0010 (superseded), 0011 (accepted), 0012 (accepted).
  </done>
  <acceptance_criteria>
    - A pre-write baseline (`git status --porcelain` + blob hashes) for the five target files was
      recorded before any write, and the task STOPPED instead of writing if any file was already dirty
    - `git diff --stat docs/adr/0001-*.md` against the recorded baseline shows only frontmatter
      lines changed (2 lines, not the body)
    - `git diff --stat docs/adr/0010-*.md` against the recorded baseline shows only frontmatter
      lines changed (2 lines, not the body)
    - `docs/adr/0012-ci-and-merge-as-decision-authority.md` exists and contains
      `- **Status:** accepted`, exactly one `Supersedes: 0001, 0010` line, and cites
      `tools/hooks/tests/test_contract_guard.py:352-375` in both the Decision Outcome and the
      Consequences section
    - `tools/hooks/contract_guard.py` shows zero diff (`git diff --stat tools/hooks/contract_guard.py` is empty)
    - `GOLDEN_APPROVE_HUMAN` was set only for the duration of this task's writes (human confirms unset afterward)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| agent -> constitution plane (`docs/adr/**`) | untrusted agent-issued Write/Edit crosses into the human-owned, CODEOWNERS-gated plane |
| human ratification token -> gate decision | `GOLDEN_APPROVE_HUMAN` env value is the sole signal `contract_guard.py::decide()` trusts to flip DENY to ALLOW |
| pre-write baseline -> supersede-don't-edit proof | an unrecorded or dirty starting state undermines the "only two lines changed" proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|------------|-------------|------------------|
| T-39-01 | Elevation of Privilege | `tools/hooks/contract_guard.py::decide()` | mitigate | Never fabricate or programmatically set `GOLDEN_APPROVE_HUMAN`; the token has exactly one delivery path (session env var); Task 1 is a blocking human checkpoint with an explicit revise-and-re-present loop that must precede every write in Task 2; Task 2 explicitly instructs the executor never to edit the hook to bypass the deny |
| T-39-02 | Repudiation | `docs/adr/0001-*.md`, `docs/adr/0010-*.md` | mitigate | Supersede-don't-edit: only `Status`/`Superseded by` frontmatter lines change; a pre-write baseline (blob hash / `git status --porcelain`) is captured before any edit, and the post-write diff is compared against that recorded baseline (not a blind `git diff` on a possibly-dirty tree); if a target file is already dirty at task start, the task STOPS rather than producing an unprovable diff |
| T-39-03 | Tampering (silent scope creep into code/gates) | any file outside `docs/adr/**` | accept | Phase is decision-record-only by ROADMAP non-goals; `files_modified` in this plan's frontmatter is the enforced boundary — no task in this plan touches `tools/**`, CI, or contracts |
</threat_model>

<verification>
Run after Task 2 completes:
- `test "$(grep -c "Superseded by:.*0012" docs/adr/0001-*.md)" -eq 1 && test "$(grep -c "Superseded by:.*0012" docs/adr/0010-*.md)" -eq 1` — both files show exactly 1, keyed against the pre-write baseline recorded in Task 2
- `git diff docs/adr/0001-*.md docs/adr/0010-*.md` — reviewer confirms only frontmatter lines differ from the recorded baseline
- `uv run pytest -k contract_guard` — the existing `contract_guard` test suite is unaffected (this
  plan touches no code); `tools/hooks/tests/test_contract_guard.py::test_every_declared_plane_member_is_independently_enforced`
  stays green (it targets `CONSTITUTION_GLOBS`, untouched by this plan) — legitimately goes stale
  only in Phase 44, not here
- `uv run python -m tools.contract_drift.drift` — unaffected (no `contracts/**` change)
</verification>

<success_criteria>
1. `docs/adr/0012-ci-and-merge-as-decision-authority.md` exists, `Status: accepted`, non-empty
   `Date`/`Deciders`, names the deleted v2.5 surfaces component by component (scoped as intent at
   ratification time), the ADR-0001/ADR-0010 supersession, the bash-residual declaration, and the
   one-off-checkpoint clause (ROADMAP Success Criterion 1).
2. ADR-0012 states the DEV/PRODUCT boundary and its operative rule (ROADMAP Success Criterion 2).
3. `docs/adr/0011-*.md` has non-empty `Date` and `Deciders` (human-confirmed values) and cites
   `bc9a6d9` (ROADMAP Success Criterion 3).
4. `docs/adr/0001-*.md` and `docs/adr/0010-*.md` carry a superseded-by pointer to 0012 with decision
   bodies unedited, proven against a recorded pre-write baseline (ROADMAP Success Criterion 5).
5. No code, gate, contract, or CI file changed in this plan (zero diff outside `docs/adr/**`).
</success_criteria>

<output>
Create `.planning/phases/39-decision-boundary-v2-5-a/39-01-SUMMARY.md` when done
</output>
