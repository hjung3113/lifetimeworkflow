---
phase: 42-adoption-decoupling-install-set-repair
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_scan/scan.py
  - tools/adoption_scan/__init__.py
  - tools/adoption_scan/tests/test_scan_exclusions.py
autonomous: true
requirements: [CER-06]

must_haves:
  truths:
    - "D-04: the 8 secret patterns live as a module-level tuple in scan.py, adjacent to SECRET_PATH_GLOBS, following that existing precedent rather than a new idiom."
    - "D-05: the tuple is byte-identical to gate-registry.json's secret_patterns array -- proven by the existing secret-redaction tests passing UNCHANGED."
    - "D-06: gate-registry.json itself is not deleted or edited by this plan -- only scan.py's _GATE_REGISTRY_PATH read goes away."
  artifacts:
    - path: "tools/adoption_scan/scan.py"
      provides: "SECRET_CONTENT_PATTERNS module-level tuple (8 entries) and a _secret_pattern() that compiles from it, with no contracts/harness/task-control/ filesystem read"
  key_links:
    - from: "tools/adoption_scan/scan.py::_secret_pattern"
      to: "tools/adoption_scan/scan.py::SECRET_CONTENT_PATTERNS"
      via: "module-level tuple reference, no json.loads/file read"
      pattern: "SECRET_CONTENT_PATTERNS"
---

<objective>
Inline the 8 `secret_patterns` `adoption_scan/scan.py` currently reads from
`contracts/harness/task-control/gate-registry.json` as a locally-owned module-level tuple,
following the exact precedent `SECRET_PATH_GLOBS` (`scan.py:52-54`) already set for this. Repoint
the one test (`test_scan_exclusions.py:211`) that reads the old path constant directly — the
easiest miss in the phase, since it names neither `task_control` nor `GOLDEN_APPROVE_HUMAN`.

Purpose: sever `tools/adoption_scan/`'s only remaining dependency on the task-control contract
(`gate-registry.json`), which Phase 44 (CER-08) deletes.
Output: `scan.py` owns `SECRET_CONTENT_PATTERNS` (8 byte-identical regex strings) and no longer
reads `contracts/harness/task-control/gate-registry.json`; `test_scan_exclusions.py` repointed.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-adoption-decoupling-install-set-repair/42-CONTEXT.md
@.planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md

D-12/D-13/D-14 ordering discipline applies (see Plan 01's context block). This plan has no wave
dependency on Plans 01/02 — `scan.py`/`test_scan_exclusions.py` are untouched by them, so this
plan may execute in parallel with Wave 1/2 if the executor chooses, though the ROADMAP records
sequential execution as fine and likely correct given shared git state; `depends_on` is empty here
because there is no FILE overlap, not because ordering doesn't matter operationally.
</context>

<interfaces>
<!-- The exact 8-pattern array, byte-verified this session from the live contracts file, and the
     exact target shape from RESEARCH.md's Code Examples section. -->

`contracts/harness/task-control/gate-registry.json`'s `secret_patterns` array (8 entries, copy
byte-identical, do not retype from memory — read the live file before transcribing):
```
"AKIA[0-9A-Z]{16}"
"(?:api[_-]?key|secret|password|token)\\s*[:=]\\s*(?:(?=[^\\s]*(?-i:[A-Z]))(?=[^\\s]*(?-i:[a-z]))|(?=[^\\s]*(?-i:[A-Z]))(?=[^\\s]*[0-9])|(?=[^\\s]*(?-i:[a-z]))(?=[^\\s]*[0-9]))[^\\s]{20,}"
"(?:ghp_|gho_|ghu_|ghs_|github_pat_)[A-Za-z0-9_-]+"
"sk-[A-Za-z0-9_-]{16,}"
"xox[bp]-[A-Za-z0-9-]+"
"-----BEGIN [A-Z ]*PRIVATE KEY-----"
"eyJ[A-Za-z0-9_-]{10,}\\.[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+"
"Authorization:\\s*Bearer\\s+[^\\s]+"
```

`tools/adoption_scan/scan.py` current shape (read before editing):
- `_GATE_REGISTRY_PATH = _REPO_ROOT / "contracts" / "harness" / "task-control" / "gate-registry.json"` (`:48`) — delete.
- `SECRET_PATH_GLOBS = [...]` (`:52-54`) — the precedent to place the new tuple adjacent to, UNCHANGED.
- `@functools.lru_cache(maxsize=1) def _secret_pattern() -> re.Pattern[str]:` (`:108-113`) — currently
  `registry = json.loads(_GATE_REGISTRY_PATH.read_text(encoding="utf-8")); patterns =
  registry["secret_patterns"]; return re.compile("(?:" + "|".join(patterns) + ")", re.IGNORECASE)`.
  Change the body to build from the new local tuple instead of the file read; keep the
  `functools.lru_cache(maxsize=1)` decorator and `re.IGNORECASE` flag exactly as-is (D-04).

`tools/adoption_scan/tests/test_scan_exclusions.py:211` — inside
`test_secret_patterns_1_branch_attribution`: `registry = json.loads(scan._GATE_REGISTRY_PATH.read_text(encoding="utf-8"));
branch_only = re.compile(registry["secret_patterns"][1], re.IGNORECASE)`. This is a FUNCTIONAL test
dependency (not prose) that will raise `AttributeError: module 'tools.adoption_scan.scan' has no
attribute '_GATE_REGISTRY_PATH'` once the constant is deleted — repoint it at
`scan.SECRET_CONTENT_PATTERNS[1]` directly, dropping the `json.loads`/file-read entirely.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inline the 8 secret patterns into scan.py, drop the gate-registry.json read</name>
  <files>tools/adoption_scan/scan.py, tools/adoption_scan/__init__.py</files>
  <read_first>tools/adoption_scan/scan.py:1-33,42-113 (module docstring, constants, _secret_pattern), tools/adoption_scan/__init__.py, contracts/harness/task-control/gate-registry.json (secret_patterns array, read live, do not trust this plan's transcription without verifying)</read_first>
  <action>
  Add `SECRET_CONTENT_PATTERNS: tuple[str, ...] = (...)` as a module-level tuple in `scan.py`,
  adjacent to `SECRET_PATH_GLOBS` (`:52-54`), containing the 8 patterns above transcribed
  byte-identical from the live `gate-registry.json` (read it fresh, do not rely on this plan's
  copy without a final diff). Change `_secret_pattern()`'s body to
  `re.compile("(?:" + "|".join(SECRET_CONTENT_PATTERNS) + ")", re.IGNORECASE)`, keeping the
  `@functools.lru_cache(maxsize=1)` decorator unchanged. Delete the `_GATE_REGISTRY_PATH` module
  constant and the now-unused `import json` if nothing else in `scan.py` uses `json` (check —
  `_dump()` at `:182-187` uses `json.dumps`, so `import json` stays; only the `_GATE_REGISTRY_PATH`
  line and the `json.loads` call inside `_secret_pattern()` go). Update `scan.py`'s module
  docstring item 2 (`:9-14`, "Secret pattern set — read as DATA from
  ``contracts/harness/task-control/gate-registry.json``'s ``secret_patterns`` array...") to
  describe the new local-tuple sourcing instead. Update `tools/adoption_scan/__init__.py:6`'s
  matching docstring phrase the same way. `git add` both files; `git commit -- tools/adoption_scan/scan.py
  tools/adoption_scan/__init__.py`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -x</automated>
  </verify>
  <done>scan.py owns SECRET_CONTENT_PATTERNS (8 entries) and no longer reads gate-registry.json.</done>
  <acceptance_criteria>
  `grep -c "_GATE_REGISTRY_PATH" tools/adoption_scan/scan.py` returns `0`.
  `grep -c "SECRET_CONTENT_PATTERNS" tools/adoption_scan/scan.py` returns at least `2` (definition
  + use inside `_secret_pattern`).
  `python3 -c "import json; d=json.load(open('contracts/harness/task-control/gate-registry.json'));
  import re,ast,tools.adoption_scan.scan as s; assert tuple(d['secret_patterns']) ==
  s.SECRET_CONTENT_PATTERNS, 'byte-mismatch'"` run via `uv run python -c "..."` exits 0 (proves
  D-05's byte-identical copy directly, not by inspection).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repoint test_scan_exclusions.py:211 at the new local tuple</name>
  <files>tools/adoption_scan/tests/test_scan_exclusions.py</files>
  <read_first>tools/adoption_scan/tests/test_scan_exclusions.py:195-217 (test_secret_patterns_1_branch_attribution and its neighbors)</read_first>
  <action>
  Inside `test_secret_patterns_1_branch_attribution`, replace `registry =
  json.loads(scan._GATE_REGISTRY_PATH.read_text(encoding="utf-8")); branch_only =
  re.compile(registry["secret_patterns"][1], re.IGNORECASE)` with `branch_only =
  re.compile(scan.SECRET_CONTENT_PATTERNS[1], re.IGNORECASE)`. If `json`/`re` imports at the top of
  the file become unused after this change, check the rest of the file before removing them (`re`
  is almost certainly still used by the `re.compile` call itself and other tests; `json` may become
  unused — grep the whole file for other `json.` usages before deleting the import). `git add
  tools/adoption_scan/tests/test_scan_exclusions.py`; `git commit -- tools/adoption_scan/tests/test_scan_exclusions.py`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan -q</automated>
  </verify>
  <done>test_scan_exclusions.py reads scan.SECRET_CONTENT_PATTERNS directly; full adoption_scan suite green.</done>
  <acceptance_criteria>
  `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -x` is green (all tests,
  including `test_secret_patterns_1_branch_attribution` and the other
  `test_secret_patterns_1_*` cases — these must pass UNCHANGED per D-05, proving the copy is
  faithful).
  `grep -rn "task_control" tools/adoption_scan/` returns nothing.
  `uv run pytest tools/adoption_scan -q` is green (full package, not just this file).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| scanned target content -> secret redaction | Unchanged by this plan — only the SOURCE of the pattern list moves, never the redaction mechanism (`classify_exclusions`'s path-first, no-content-echo recording) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | Information Disclosure | Secret-pattern fidelity during the inline (a dropped or mistyped pattern silently weakens redaction) | mitigate | Task 1's acceptance criterion diffs the live tuple against `gate-registry.json`'s array element-by-element; Task 2's unchanged `test_secret_patterns_1_*` tests are the behavioral proof |
</threat_model>

<verification>
`uv run pytest tools/adoption_scan -q` green.
`grep -rn "task_control" tools/adoption_scan/` returns nothing.
`git log --oneline -5` shows exactly 2 new commits for this plan's 2 tasks.
</verification>

<success_criteria>
`scan.py` reads no file under `contracts/harness/task-control/`. Its 8 secret patterns are owned
locally, byte-identical to `gate-registry.json`'s array. Every `test_secret_patterns_1_*` test
passes unchanged.
</success_criteria>

<output>
Create `.planning/phases/42-adoption-decoupling-install-set-repair/42-03-SUMMARY.md` when done
</output>
