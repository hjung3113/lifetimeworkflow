---
phase: 45-projection-repair
plan: 04
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - AGENTS.md
  - README.md
  - README.ko.md
autonomous: true
requirements: [CER-11]
user_setup: []

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-06 (only a live file describing a control, path or command that no longer exists is changed), D-07 (root AGENTS.md is repaired at the sites that sit OUTSIDE the emitter's managed block, which a re-emit will never fix), D-09 (README.md AND README.ko.md — the Korean file that four deferral lists never named), D-18 (the AGENTS.md managed block is exactly :100-109; every target here is outside it, so hand-editing is correct and was verified not to revert on re-emit)."
    - "Root AGENTS.md names no deleted module, command, hook or directory: the two tools.golden_runner command rows and the root golden/ core-map line are gone, and the engine list no longer claims golden_runner."
    - "AGENTS.md's constitution-plane sentence names three members."
    - "README.md and README.ko.md name no retired command: /golden, /golden-approve and /pipeline are gone from both, replaced by the live 17-command set."
    - "AGENTS.md:7-8 is left unchanged on the record — measured, both hooks it names are live, and the phrase 'the true backstop' that CONTEXT and ROADMAP quote does not appear in the file."
    - "tools/memory_regen's AGENTS.md structural gate stays green because the literal golden/ token survives at the INSTANCE map line and 'uv run pytest' survives in the command table."
  artifacts:
    - path: "AGENTS.md"
      provides: "the root nearest-wins rules and golden-path command table"
      contains: "HARNESS-MANAGED"
    - path: "README.ko.md"
      provides: "the Korean project overview"
  key_links:
    - from: "tools/memory_regen/tests/test_agents_md.py"
      to: "AGENTS.md"
      via: "literal-token structural assertions at :44 and :48-50"
      pattern: "golden/"
---

<objective>
Repair the three hand-owned root documents so they stop describing modules, commands and directories
that four deletion phases removed.

Purpose: CER-11 names these explicitly because they sit OUTSIDE the emitter's HARNESS-MANAGED block
(`AGENTS.md:100-109`), so no amount of re-emitting will ever fix them. `README.ko.md` in particular
survived four phases because every prior deferral list named `README.md` and never the Korean file.

Output: one commit, suite unchanged at 876.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-projection-repair/45-CONTEXT.md
@.planning/phases/45-projection-repair/45-RESEARCH.md
@.planning/phases/45-projection-repair/45-01-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- The one gate that reads AGENTS.md. Keep it green without weakening it. -->

tools/memory_regen/tests/test_agents_md.py
  :44     requires the literal substring "golden/" to appear in AGENTS.md
  :48-50  requires any of ("tools.contract_drift", "tools.golden_runner", "uv run pytest")
  MEASURED: after deleting AGENTS.md:66, :67 and :79 the gate stays green — "golden/" still comes
  from the INSTANCE map line (`examples/<instance>/   Own contracts/, golden/, ...`) and
  "uv run pytest" from the command-table row at :64. Verified at 82 passed.

Live command set (ls harness/commands/*.md — 17): add-language, adopt, adr, agree, build,
  checkpoint, component, contract-check, docs-sync, fan-out-synthesize, lint, new-contract-rule,
  orient, refresh-memory, review, test, verify-work.
  /golden, /golden-approve and /pipeline are all absent.

Live CI job set (.github/workflows/ci.yml): setup, lang-tests, contract-check, drift, golden,
  core-suite, lint, emit-drift, stale-derived, workspace, gate. The `golden` JOB still exists —
  README.md:60's fan-in list is correct and must NOT be edited.

Live ADR range: docs/adr/0001 … 0012.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair root AGENTS.md outside the managed block</name>
  <files>AGENTS.md</files>
  <action>
Implements D-07, D-06 and D-18. Every target is between `:7` and `:91`, i.e. OUTSIDE the
HARNESS-MANAGED block at `:100-109`. Hand-editing is correct here and was verified not to revert on
a re-emit. Do not touch `:100-109`.

`:27-28` — the sentence lists "`contracts/`, `docs/adr/`, `golden/`, or `docs/glossary.md` (the four
members declared by [ADR-0001](docs/adr/0001-walking-skeleton-golden-core.md) §Decision)". Make it
three members and cite ADR-0012 clause (d) as having superseded ADR-0001:48 to the extent that
`golden/**` leaves the constitution-plane core. Keep the following sentence's "No agent self-blesses a
golden baseline" — instance goldens still exist and are still human-ratified at the PR, so that rule
is live; add that the ratification is the CODEOWNERS `/examples/*/golden/` route.

`:66` and `:67` — DELETE both rows of the golden-path command table
(`python -m tools.golden_runner.runner` and `python -m tools.golden_runner.approve --approve --adr
<id>`). The package was relocated to the instance overlay in Phase 44; this table is the CORE command
table, so a repointed row would claim a core capability that no longer exists. Deletion is the
correct repair, and it was measured to keep `tools/memory_regen` green.

`:79` — DELETE the `golden/       Constitution plane. Approved equivalence baselines (.verified).
Human-promoted only.` line from the CORE block of the monorepo map. Root `golden/` was deleted in
Phase 44. Do NOT touch `:91` (`examples/<instance>/   Own contracts/, golden/, components/, ...`) —
it is correct AND it is what keeps `test_agents_md.py:44`'s literal `golden/` requirement satisfied.

`:84` — the engine list reads "tools/  The engine (Python): contract_hash, contract_drift,
golden_runner, harness_config, harness_lint, memory_regen, bootstrap." Remove `golden_runner` (it is
no longer a core tools package).

`:7-8` — LEAVE UNCHANGED and record the reason in the summary. It reads "Prose is advisory — the
backstop is the SessionStart injector plus the hooks (contract-guard, polyglot-boundary linter)."
Measured: all three named artifacts are live (`harness/plugins/session-inject.ts`,
`harness/plugins/contract-guard.ts`, `harness/plugins/polyglot-lint.ts`), and the phrase "the true
backstop" that CONTEXT D-07 and ROADMAP SC-4 both quote does not appear anywhere in the file. This is
a citation defect in the requirement, not a defect in AGENTS.md. Correcting a line that is already
true would be scope invention.

Also record in the summary: `REQUIREMENTS.md:105` and `ROADMAP.md:245` cite `AGENTS.md:52-62` as the
stale golden-path table. Measured, `:52-62` is §B *Working agreements* and is not stale; the table is
at `:62-71`. Use the line numbers in this plan, not the requirement's.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_regen -q 2>&1 | tail -3   # expect: 82 passed</automated>
    <automated>grep -n 'golden_runner\|^golden/' AGENTS.md   # expect: no output</automated>
    <automated>grep -c 'golden/' AGENTS.md   # expect >=1 — the instance map line must survive for test_agents_md.py:44</automated>
    <automated>uv run python -m tools.harness_emit && git diff --stat -- AGENTS.md   # expect the edits to survive the re-emit unchanged</automated>
  </verify>
  <done>AGENTS.md names no `tools.golden_runner` command and no root `golden/` directory; its plane sentence says three members; `tools/memory_regen` is 82 passed; a re-emit does not revert the edits; `:7-8` is untouched with the reason recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Repair README.md and README.ko.md, and unlink task-lifecycle.md ahead of plan 05</name>
  <files>README.md, README.ko.md</files>
  <action>
Implements D-09 and D-06. No test reads either file (verified: `git grep 'README'` over the test tree
returns no file reads), so these are pure prose commits — which is exactly why they rotted for four
phases.

`README.md`:
  - `:55` — the two-plane row lists the constitution as "`contracts/`, `docs/adr/`, `golden/`". Three
    members.
  - `:79` and `:83` — the mermaid `CONST` subgraph contains a `G["golden/"]` node and an edge
    `K -->|normalized compare| GOLD["golden runner"]`. Root `golden/` is gone from the constitution
    plane; re-scope the node to the instance overlay (or drop it and leave the edge pointing at the
    instance-side runner). Keep the diagram honest: the golden runner still exists, it is just no
    longer core and no longer constitution-plane.
  - `:118-119` — the quick-start step `uv run python -m tools.golden_runner.runner`. The module is not
    in the core tree; either delete the step or repoint it at the instance path. Prefer deletion — the
    surrounding block is the CORE quick start.
  - `:123-124` — the command list names `/verify-work`, `/golden`, `/golden-approve`,
    `/contract-check`, `/refresh-memory`, `/fan-out-synthesize`, `/pipeline`, `/checkpoint`, `/review`.
    `/golden`, `/golden-approve` and `/pipeline` are all retired (`/pipeline` in Phase 44 — CONTEXT
    never named it). Replace with the live set from the interfaces block above.
  - `:139` — the tree line `golden/              # constitution plane — approved equivalence
    baselines`. Delete or re-scope to `examples/<instance>/golden/`.
  - `:141` — the tools list names `golden_runner`. Remove it.
  - `:163` — the bullet "**Machines gate, humans ratify** — `/golden-approve` refuses to promote a
    baseline without an ...". The principle is live; the command is not. Restate it in terms of the
    human `GOLDEN_APPROVE_HUMAN` token plus the CODEOWNERS `/examples/*/golden/` route.
  - DO NOT touch `:46`, `:53`, `:59`, `:153`, `:186`, `:216` — those name the golden/drift GATES, which
    still exist. DO NOT touch `:60` — every job in that CI fan-in list, including `golden`, exists in
    `.github/workflows/ci.yml` today.

`README.ko.md` (114 lines; 5 stale lines plus one this plan must handle for plan 05):
  - `:45` — two-plane row, constitution listed as `contracts/`·`docs/adr/`·`golden/`. Three members.
  - `:49` — "`/golden-approve`는 ... baseline 승격을 거부" — retired command; restate as the human token
    + CODEOWNERS ratification, matching README.md:163.
  - `:79` — `harness/task-control/  # gate-registry`. The directory died in Phase 43 and
    `gate-registry.json` in Phase 44. Delete the line.
  - `:80` — `golden/              # 헌법 평면 — 승인된 등가 baseline`. Delete or re-scope to the instance.
  - `:81` — `docs/                # Diátaxis + adr/(0001–0008) + glossary + how-to/task-lifecycle.md`.
    TWO defects: the ADR range is now 0001–0012, and it links `how-to/task-lifecycle.md`, which plan 05
    deletes. ⚠ This line MUST stop naming `task-lifecycle.md` in THIS commit — plan 05 depends on it,
    and if it lands after the deletion the repo carries a new dangling reference in between.
  - `:82` — the tools list names `golden_runner`. Remove it.
  - `:107` — "contract-drift/golden 게이트 green 유지" names the gates, which are live. Leave it.

Commit both files together, message before the pathspec.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>grep -n 'golden_runner\|golden-approve\|/pipeline\|task-control\|task-lifecycle' README.md README.ko.md   # expect: no output</automated>
    <automated>for c in $(grep -oE '`/[a-z-]+`' README.md | tr -d '`/'); do test -f harness/commands/$c.md || echo "DANGLING /$c"; done   # expect: no DANGLING lines</automated>
  </verify>
  <done>Neither README names a retired command, a deleted directory or a relocated module; `README.ko.md:81` no longer links `task-lifecycle.md`, clearing plan 05's deletion; every slash-command named in `README.md` resolves to a file under `harness/commands/`; suite at 876.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root prose → every agent session | `AGENTS.md` is the nearest-wins rule source an agent reads first; a false rule here propagates everywhere |
| README → a new human contributor | the quick start is executed verbatim by someone who cannot yet tell live surface from dead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-11 | Spoofing | `AGENTS.md` teaches a four-member plane the guard does not enforce | mitigate | `:27-28` corrected to three members with the ADR-0012 citation, in the same phase as the guard change. |
| T-45-12 | Denial of Service | a contributor runs a documented command that no longer exists | mitigate | Both golden-runner command rows deleted from the core table; every slash command in README is asserted to resolve to a file under `harness/commands/`. |
| T-45-13 | Tampering | an edit lands inside the emitter's managed block and is silently reverted | mitigate | Managed block measured at `:100-109`; all targets are `:7-91`; the verify step re-runs the emitter and diffs `AGENTS.md`. |
| T-45-14 | Repudiation | weakening `test_agents_md.py` to make the edit fit | mitigate | The gate is left untouched; the instance map line at `:91` is explicitly preserved to satisfy it, and `tools/memory_regen` is run as a verify step. |
| T-45-SC | Tampering | npm/pip/cargo install | accept | No package-manager install; `uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest -q` → 876 passed, 7 snapshots after each commit.
- `uv run pytest tools/memory_regen -q` → 82 passed (the only gate that reads `AGENTS.md`).
- `uv run python -m tools.harness_emit && git status --porcelain` → empty; the `AGENTS.md` edits must
  survive the re-emit byte-identical.
</verification>

<success_criteria>
- ROADMAP SC-4 holds: root `AGENTS.md` names no deleted module, command or hook.
- `README.md` and `README.ko.md` name no deleted surface.
- `README.ko.md` no longer links `docs/how-to/task-lifecycle.md`, unblocking plan 05.
- `AGENTS.md:7-8` is deliberately unchanged, with the requirement's citation defect recorded.
</success_criteria>

<output>
Create `.planning/phases/45-projection-repair/45-04-SUMMARY.md` when done. Record the `:7-8` no-change
decision and the corrected `AGENTS.md:52-62` → `:62-71` citation for the milestone-close PR.
</output>
