# Phase 18-01 Plan — Task Packet Contract Ratification

## 작업 목록과 순서

1. **계약 고정 (TCP-01, SC1)** — `contracts/harness/task-control/`에 Draft 2020-12
   `task`/`state`/`evidence`/`handoff` schema를 작성한다. D-06~D-10의 enum, ID,
   baseline, 참조 shape를 schema에 고정한다.
2. **데이터 사례 고정 (SC2, SC4)** — `tools/task_packet/tests/fixtures/`에 domain-neutral
   positive packet 5개 이상과 필수필드 누락·미정의 enum/전이·dangling ref·baseline 부재
   negative 사례를 작성한다.
3. **작업 슬롯 예시 (TCP-02, SC3)** — `.workflow/tasks/README.md`에 committed volatile 규약과
   `.memory/state/` 책임 경계를 기록하고, schema-conformant 예제 packet 하나를 추가한다.
4. **최소 계약 소비기 (SC1~SC5, SC7~SC9)** — `tools/task_packet/`에 jsonschema 검증,
   lane별 허용 전이표, cross-file ID 참조 검사를 구현한다. 라우팅·원자적 상태 변경·evidence
   수집은 후속 phase로 남긴다.
5. **계약 gate 등록 (SC6)** — `contracts/.hashes/manifest.json`을 공식 hasher로 갱신하고,
   schema/fixture 및 deletion-independence를 pytest로 검증한다.

## 완료 기준 매핑

- 설계 §4 Phase 1 기준 1~5: fixture 기반 `tools/task_packet` 테스트와 CLI 실패 코드.
- 기준 6: 네 schema의 drift baseline 등록 및 paired fixture/golden 검증 테스트.
- 기준 7~8: `.memory/state/`와 task packet 양방향 deletion-independence 테스트.
- 기준 9: core schema/fixture의 instance·domain 경로/용어 부재 테스트.
- 최종 gate: `uv run pytest tools/task_packet`와 `bash tools/contract_drift/check.sh` green,
  LF/no-BOM 및 모델 식별자 부재 확인 후 단일 Phase 18 커밋.
