# Phase 21 plan

1. Extend the evidence contract with capture provenance, finding resolution, and
   redaction-report shapes; re-baseline the development contract manifest.
2. Add a small `tools.evidence` adapter that only spawns supplied gate commands,
   stores immutable output artifacts, hashes them, and rejects sensitive output.
3. Make task-control consume passing criterion evidence and unresolved review or
   constitution-approval evidence at VERIFY/COMPLETE without changing gate logic.
4. Wire the authored review and verify-work command guidance to the adapter,
   emit runtime projections, and prove the completion criteria with focused tests.
