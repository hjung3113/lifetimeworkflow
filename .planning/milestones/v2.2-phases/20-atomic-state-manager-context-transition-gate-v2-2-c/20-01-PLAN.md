# Phase 20 implementation plan

1. Add a deterministic `tools.task_control` CLI and library around the existing task/state/evidence contracts: atomic same-filesystem replacement, revision CAS, transition and blocker rules, and artifact diagnostics.
2. Add a fail-closed phase gate that checks repository identity, state revision/ref/baseline, required artifacts, source-hash attestation coverage, and completion evidence.
3. Add the authored `/phase-gate` command, emit both runtimes, and cover every Phase 3 acceptance criterion with focused tests before running the repository gates.
