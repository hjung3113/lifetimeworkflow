# Phase 22 implementation plan

1. Extend the dev-draft HANDOFF contract with immutable state, evidence, artifact, and read-path references.
2. Add deterministic `tools.handoff` generation, validation, and fresh-session reconstruction.
3. Revise checkpoint, orient, and SessionStart pointer-only behavior; emit both runtime trees.
4. Cover the eight Phase 5 completion criteria with focused handoff/inject tests, then run the required gates.
