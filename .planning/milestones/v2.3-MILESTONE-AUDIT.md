---
milestone: v2.3
milestone_name: Contract Graph, Brownfield Adoption, Living Docs
audited: 2026-07-21
status: ratifications_discharged_rat1_rat2_rat3
scores:
  requirements: 19/21     # TOPO-01..07 + ADOPT-01..07 + DOCSUP-01..05 evidenced; DOCSUP-06/07 machinery-complete but gated on a human ledger write
  phases: 7/10            # VERIFICATION.md present + passing for 24, 25, 26, 26.1, 26.2, 27.1, 27.2; 28 is human_needed; 27 and 29 have none
  integration: 12/13      # every SC-4 fan-in item confirmed green from observed output except `lifecycle-eval` step 2 (see gaps.integration)
  flows: 1/2              # /adopt -> registry -> guard -> refusal round-trip proven live; the docs drive loop's 0 -> 1 -> 0 leg is not observable until a human authors the ledger
gaps:
  requirements:
    - "DOCSUP-06/07: the command, skill, exclusions classifier and self-green proofs all ship and are gate-tested, but `uv run python -m tools.docs_guard` exits 1 (6 x broken-binding) because `docs/.docs-review-ledger.toml` does not exist. That file is human-only by construction (three enforcement layers), so the requirement cannot be marked satisfied by any agent action."
  integration:
    - "`lifecycle-eval` CI job step 2 is RED at collection, platform-independently. `uv run pytest tools/lifecycle_eval` (ci.yml:194, the command as written) errors: `ModuleNotFoundError: No module named 'tools'`. Cause: `tools/lifecycle_eval/tests/` is the only tools member whose tests dir carries neither `__init__.py` nor a `conftest.py` that inserts the repo root into `sys.path` — every sibling member has one (e.g. `tools/harness_lint/tests/conftest.py:20-23`). Under the FULL suite the module imports fine (a sibling conftest has already inserted the repo root), which is why the full run is 1473 passed while the isolated job command errors. Step 1 (`uv run python -m tools.lifecycle_eval.runner`) is green: 20/20 fixtures PASS, `false_downgrades_enforced_zero: true`. NOT repaired here, per T-29-21 — a closeout that repairs on the way past reports a state that never existed."
    - "`docs-guard` CI job is RED by design until a human authors the ledger. Expected and correct; recorded, not counted as a defect."
    - "`golden` job step 2 (`uv run pytest examples/log-parser/tests`) is red on THIS macOS host only: 2 failed, 10 passed, `toy-converter exited 3: path confinement violation`. Cause is a host artifact, not a repo defect — pytest's `tmp_path` realpaths to `/private/var/folders/...` while .NET's `Path.GetTempPath()` returns the `/var/folders/...` spelling, and `IsConfined` (`examples/log-parser/components/toy-converter/Program.cs:163-183`) compares with `StringComparison.Ordinal`. On the job's `ubuntu-latest` runner both are `/tmp` and the comparison holds. Step 1 (`uv run pytest tools/golden_runner`) is green: 17 passed."
  flows:
    - "The `/docs-update` drive loop's `0 -> 1` leg was never expressed as a ledger state (29-04 deviation D-2): task 3 executed before the human-only task 2, so the binding was BROKEN before AND after the bounded edit, for the strictly prior reason 'no [[reviewed]] row'. Edit visibility to the gate was evidenced instead by the live target digest moving `4568f3a9... -> 8df85e6e...`. Observing the literal transition requires the human to take Option B in `29-04-SUMMARY.md`."
nyquist:
  compliant_phases: ["24", "25", "26", "26.1", "26.2", "27.1", "29"]
  partial_phases: ["27", "27.2", "28"]
  missing_phases: []
  overall: partial
  observed_by_ls: |
    VALIDATION.md present: 24, 25, 26, 26.1, 26.2, 27, 27.1, 29 (29 authored by plan 29-05).
    VALIDATION.md ABSENT:  27.2, 28.
    VERIFICATION.md present: 24, 25, 26, 26.1, 26.2, 27.1, 27.2, 28.
    VERIFICATION.md ABSENT:  27, 29.
    `workflow.nyquist_validation: true` is set, so each phase owes a VALIDATION.md. These are
    RECORDED as observed, never back-filled after the fact — the v2.1 audit carried exactly this
    kind of row for phases 13/14 rather than manufacturing a document.
  discharged_after_audit:
    date: 2026-07-21
    note: |
      The observation above is left EXACTLY as recorded at audit time and is not rewritten — that
      record is what the audit is for. What follows is a dated addendum, not a correction.

      `27.2-VALIDATION.md` and `28-VALIDATION.md` were authored AFTER this audit, each carrying
      `authored: at-closeout` in its frontmatter and an opening note stating plainly that it was
      written at closeout, could not have steered the phase it describes, and claims no prospective
      authority. Every row in both is transcribed from artifacts that already existed — the PLANs'
      `<automated>` verify blocks, the SUMMARYs, `*-FIXES.md`, `*-REVIEW.md` and the two
      VERIFICATION.md reports — so neither manufactures coverage; they state, checkably, that the
      coverage each phase owed was in fact delivered.

      Gap analysis outcome: NO MISSING and NO PARTIAL rows in either phase among agent-executable
      requirements. 27.2 is fully covered. 28's only non-green rows are DOCSUP-02's first ledger row
      and ADR-0010's ratification — un-ratified, not unbuilt, and both listed with the enforcement
      layers that forbid an agent to close them.

      One real defect was surfaced by the exercise and is recorded rather than quietly repaired:
      `28-08-PLAN.md:257`'s phase-closing fan-in `&&`-chains `uv run python -m tools.docs_guard`,
      which cannot exit 0 before RAT-1 lands. The gate was satisfied by running its members
      individually (`28-VERIFICATION.md:72-82`). A fan-in command whose members include a
      designed-non-zero gate must not be `&&`-chained.

      Phase 27's missing VERIFICATION.md is NOT discharged and remains recorded debt.
ratification_outcome:
  discharged_2026_07_22:
    - "RAT-2 — ADR-0010 ratified by the human (commit ad4e339); registry._adr_status now returns accepted."
    - "RAT-1 + RAT-3 — first docs/.docs-review-ledger.toml authored and committed by the human (c32c08d). That commit IS the ratification of the eight seeded bindings. docs_guard moved exit 1 -> exit 0, 8/8 FRESH, 7 uncovered against uncovered_max = 7."
  authored_against: "digests re-derived at 27acf96, NOT the 29-04-SUMMARY.md proposal — the constitution-plane drift repair that had to land first moved four of the eight."
  prerequisite_repair: "quick task 20260722-glossary-constitution-plane-drift. ADR-0001:48 declares a FOUR-member constitution plane; the Phase-4 stack enforced three, leaving docs/glossary.md agent-writable while four documents said it was gated. Two Claude audits recommended the OPPOSITE repair by citing ADR-0010 (then proposed) over ADR-0001 (accepted); an external audit caught the inversion. The gate caught an agent's reasoning, not a typo."
  repoint_note: "lifecycle-eval-shadow-metrics classified FRESH, not amber, despite being repointed — first_seen-unratified compares against the PREVIOUS COMMITTED ledger and this is the first. Repointing before the ledger existed is why no amber cycle occurred; the A->B->C->ADR->ledger ordering was chosen for that."
  gate_still_holds: "ledger_guard.decide() re-verified AFTER the ledger existed: DENY under {}, GOLDEN_APPROVE_HUMAN, HARNESS_DEV_BYPASS, and both together. That is the only test of the gate that matters."
  still_open: ["RAT-4 (Phase-28 HARNESS_DEV_BYPASS schema write)", "RAT-5 (ADR-0004/5/6/7 unmerged to main)"]
outstanding_ratifications:
  - id: RAT-1
    what: "Hand-author `docs/.docs-review-ledger.toml` from the byte-exact proposal in `29-04-SUMMARY.md`, outside an agent session."
    against: "the POST-edit tree. 29-04's bounded gate-model prose fix moved that binding's target_digest from `4568f3a9...` to `8df85e6e...`; the summary gives two ordered options (A: one round straight to exit 0; B: two rounds, which observes the 0 -> 1 leg for real and is what the plan intended)."
    why_human: "ADR-0010 clause 3b. Three layers deny the agent write: `tools/hooks/ledger_guard.py` (PreToolUse, honours NEITHER `GOLDEN_APPROVE_HUMAN` NOR `HARNESS_DEV_BYPASS`), `refuse_unsafe_destination` -> `ReviewLedgerRefusal` on the adoption-apply path, and the `first_seen-unratified` history test. A self-blessed row and an honest seed row are byte-identical; only the human commit separates them."
    blocks: "DOCSUP-06, DOCSUP-07, the `docs-guard` CI job, Phase 29 closure."
  - id: RAT-2
    what: "Review ADR-0010 and flip `- **Status:** proposed` to accepted, filling Date and Deciders (both are em-dashes today, `docs/adr/0010-human-docs-review-obligation-model.md:5-7`)."
    why_human: "`registry._adr_status` treats a non-accepted ADR as a rejection, so this is a real gate, not a formality. The ADR is append-only once accepted — Phase 29 cannot amend it."
    blocks: "Phase 28 closure."
  - id: RAT-3
    what: "Ratify the eight seeded bindings' dispositions in `docs/doc-dependencies.toml` before their first `[[reviewed]]` rows are recorded."
    why_human: "Confirming each (sources, target) pair is a review obligation the human genuinely accepts. Amber-on-landing resolves to FRESH only after the row lands in a COMMIT."
    blocks: "the same as RAT-1; discharged together with it."
  - id: RAT-4
    what: "The Phase 28 constitution-plane schema write — `contracts/harness/docs/doc-dependencies.schema.json`, plan 28-01, plus the WR-02 pattern rebaseline — landed via `HARNESS_DEV_BYPASS` per ADR-0007."
    why_human: "A dev bypass is explicitly NOT a human ratification (ADR-0007's whole point is that the two are distinct). Recorded as OUTSTANDING, never as ratified. CODEOWNERS at PR merge is the real gate and has not fired."
    blocks: "nothing mechanically; it is an unclosed provenance obligation."
  - id: RAT-5
    what: "ADR-0004 / 0005 / 0006 / 0007 remain unmerged to `main` (standing item, carried from STATE.md)."
    why_human: "The GitHub DEFAULT BRANCH is the working branch itself, so pushes bypass CODEOWNERS entirely; and @hjung3113 is both the sole owner and the PR author, which GitHub forbids from self-approving. The mechanical CODEOWNERS gate cannot fire on a solo-authored PR — empirically confirmed on PR #3 (`reviewRequests` came back EMPTY). Durable fix is repo-config: flip the default branch back to `main`."
    blocks: "ratification of four ADRs; structural, not neglect."
tech_debt:
  - phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
    source: 28-REVIEW.md (deep, 20 files, 3 critical / 4 warning / 3 info)
    items:
      - "CR-01 drift suppression demoted blocking coherence findings, a permanent self-green escape — FIXED `86ecad5`. `SUPPRESSIBLE_REASONS = frozenset({REASON_STALE})` plus an independent `and not blocking_by_id.get(binding.id)` half on the SUPPRESSED branch. Deletion-proven by `test_self_blessed_binding_is_not_rescued_by_a_drifted_source`."
      - "CR-02 the ledger's `path_deny_globs` entry was enforced by nothing (ADR-0010 layer 1 absent) — FIXED `ef793a8`, by making layer 1 REAL rather than weakening the ADR. New `tools/hooks/ledger_guard.py`, a THIRD disjoint deny domain honouring no opt-out; `apply.py` imports the constant instead of copying it; wired through `merge.py` HARNESS_HOOK_GROUPS + `harness/plugins/ledger-guard.ts`; the vacuous matrix assertion replaced by five that can fail."
      - "CR-03 `first_seen-unratified` keyed on the binding id alone, so repointing a ratified id was silently FRESH — FIXED `2b504a6`, deliberately WITHOUT the reviewer's proposed `binding_digest` column: `registry.identity_digest(sources, target)` + a previous-committed-REGISTRY comparison via a REQUIRED `repointed_ids` argument. Rationale: the ledger is hand-authored, and a third derived value a human must compute is a real usability cost for no gain in strength. `_ROW_KEYS` is still the four DOCSUP-02 keys."
      - "WR-01 `uncovered_max` read from the working tree, not the committed ledger — FIXED `d49bdc9`. Now symmetric with `binding_min`; the working-tree value is consulted only when there is no committed ledger, where it can add a constraint but never relax one."
      - "WR-02 a registry `target` containing a newline injected fabricated rows into the derived queue and inflated the SessionStart pointer — FIXED `d6a7f63` at BOTH ends: a schema `pattern` forbidding C0/DEL and `|`, the same rule restated in `tools.docs_guard.registry` ahead of the shape pass (so the operator gets a reason, not a regex), and visible per-cell escaping in `docs_staleness.render`. Landed as one atomic constitution-plane commit with the hash rebaseline."
      - "WR-03 `cli.main()`'s 0/1/3 exit contract was escapable by a traceback from `impact_ids` — FIXED `59870d9`. The impact map is computed in `main()` behind a containment that degrades to an empty map, matching `impact.py`'s established NEVER FABRICATE answer."
      - "WR-04 `docs_staleness.main()` classified twice and the printed count could disagree with the file — FIXED `d17e764`. `write_rows()` split out as the pure publish half; `main()` computes once."
      - "IN-01 a live model identifier committed as a test fixture — FIXED `b329fce`. Replaced with a shape-matching, non-existent id; the detector is anchored on shape so coverage is unchanged. Re-verified: `grep -rn` over `tools/ docs/ harness/ contracts/` returns none."
      - "IN-02 a backslash-spelled destination bypassed the ledger classification (Windows-relevant) — FIXED `1810085`. `\\` folds to `/` in the classification exactly as the `..`-segment pre-check already did; a `backslash_separator` row joined the parametrized table. NOTE: distinct from 27.1's IN-02 (flock/NFS) below — same id, different phase."
      - "IN-03 the contract graph is recompiled once per binding in the report path — ACCEPTED AS A RESIDUAL, not fixed. Correctness is unaffected (deterministic result); WR-03's fix reduced the call sites from two to one but the graph is still compiled per binding (verified live: `cli.py:233` comprehension, `docs_staleness.py:100` loop). Closing it means changing `impact.py`'s public signature (rippling into `docs_staleness.rows`, `cli`, three test modules) or adding cached state to a module whose docstring makes a point of being pure — both deserve their own adversarial row for cache invalidation. Carried forward."
      - "`examples/**` instance-local docs registry overlay — the seam left OPEN by 28-CONTEXT D-14. Not built, deliberately out of scope for both 28 and 29."
  - phase: 27.2-adoption-apply-robustness-clean-refusal-completeness-regress
    items:
      - "AD-01 — `BrokenBarrierError` legibility under CI load in the concurrency test. Recorded in `27.2-FIXES.md` Residuals."
      - "AD-02 — `monkeypatch` on the singleton `fcntl` module (pytest-xdist hazard). Recorded in `27.2-FIXES.md` Residuals."
      - "D-04 deviation — `check_valid` returns a bare `bool`, not a reason string. Accepted with rationale: `check_valid` was ALWAYS `-> bool`, so D-04's premise was factually wrong. `27.2-FIXES.md` AD-03."
      - "Pre-existing ruff `I001` (unsorted import block) on `tools/adoption_apply/cli.py:29`. Byte-identity to `85948fc` re-confirmed by the 27.2 verifier; re-confirmed live in this audit (`uv run ruff check tools/adoption_apply/cli.py` -> `Found 1 error`, 1 fixable). Not introduced by 27.2 and not fixed here. Part of the wider standing item that `ruff check` is not a CI gate at all."
      - "`manifest.schema.json` still admits `\".\"` / `\"./\"` / `\"a/\"` — runtime-only closure per 27.2 D-08."
  - phase: 27.1-adoption-safety-path-normalization-bypass-apply-mode-confine
    items:
      - "IN-02 — advisory `flock` semantics over NFS. Distinct from 28's IN-02; keep them separable by phase."
      - "WR-08 — parent-directory symlink-swap TOCTOU on the apply path."
  - phase: harness-wide (carried since 26.2)
    items:
      - "`tools/hooks/secret_scan.py:44-47` — the shape-anchored `PATTERNS` list is HARDCODED in the module instead of being read from the contract. Re-verified present and unchanged in this audit. Fenced out of 26.2, 27.1, 27.2 AND 28; carried into the v2.3 close rather than allowed to vanish (D-15)."
      - "`ruff check` is not a CI gate. The repo carries pre-existing E-code debt in `tools/` (STATE.md records ~57 at the Phase-14 baseline) plus the `I001` above. Worth a decision, not a silent carry."
  - phase: 15-emit-round-trip-gates-v2-1-d (carried across two milestones)
    items:
      - "CARRIED (15-REVIEW.md CR-01, still open): the `emit-drift` CI gate uses BARE `git diff --exit-code` (`ci.yml:213`), which is blind to untracked files — a newly emitted artifact can land untracked and the gate passes green. The correct idiom (`git add -A` + `--cached`) already exists one job away at `ci.yml:238`. Verified still bare in this audit. Every emit round-trip in Phases 28 and 29 was therefore checked with `git status --porcelain` by hand (29-CONTEXT D-03); 29-03 recorded a real instance of bare `git diff` reporting clean while four emitted files were untracked."
  - phase: 23-lifecycle-evaluation-docs-ci-v2-2-f (surfaced by THIS audit)
    items:
      - "`uv run pytest tools/lifecycle_eval` — the `lifecycle-eval` job's step 2 as written at `ci.yml:194` — errors at collection with `ModuleNotFoundError: No module named 'tools'`. `tools/lifecycle_eval/tests/` is the only tools member's tests dir with neither `__init__.py` nor a repo-root-inserting `conftest.py`. Green under the full suite by side effect of a sibling conftest, red in isolation. NEW finding, recorded not repaired."
  - phase: open-quick-task
    items:
      - "DEF-05-02-1 (open since 05-02): 3 commit_gate drift-block tests leak the live `GOLDEN_APPROVE_HUMAN` token (missing `monkeypatch.delenv`) and fail only when the session token is exported. Green in CI."
---

# v2.3 (Contract Graph, Brownfield Adoption, Living Docs) — Milestone Audit

**Status: `blocked_on_human`.** Every piece of machinery the three themes call for is built,
gate-tested and observed green. The milestone does not close because five human ratifications are
outstanding, and the two that matter most (RAT-1, RAT-2) are precisely the writes an agent is
architecturally forbidden to perform — a forbiddance that is itself Theme C's central deliverable
and is verified working.

> **Note on STATE.md.** STATE.md's counters read `completed_phases: 9`, `completed_plans: 38`,
> `percent: 100`. Those are a mechanical count of SUMMARY files on disk and they OVERSTATE: Phases
> 28 and 29 are NOT closed while RAT-1..RAT-3 are outstanding. STATE.md is not evidence for
> anything in this audit — the per-requirement table below is. The v2.1 audit recorded this exact
> hazard about its own `milestone_complete / 117%` line, and it has recurred.

## SC-4 fan-in — actual numbers

Run at commit `bad6749`, working tree clean (`git status --porcelain` empty). Every row is a
command that was executed, with its observed exit code and headline number. Nothing was repaired
during the run.

| # | SC-4 item | CI home | Command | Exit | Observed |
|---|-----------|---------|---------|------|----------|
| 1 | full `uv run pytest` | `core-suite` | `uv run pytest -q` | **0** | **1473 passed, 8 snapshots passed** in 74.05s |
| 2 | contract-drift (root) | `drift` | `uv run python -m tools.contract_drift.drift` | **0** | `OK — live manifest matches the committed baseline` |
| 2b | contract-drift (example) | `drift` | same, `--contracts-dir examples/log-parser/contracts --baseline .../manifest.json` | **0** | `OK — live manifest matches the committed baseline` |
| 3 | golden (root identity) | `golden` | `uv run pytest tools/golden_runner` | **0** | **17 passed** |
| 3b | golden (example .NET parity) | `golden` | `uv run pytest examples/log-parser/tests` | **1** | **2 failed, 10 passed** — `toy-converter exited 3: path confinement violation`. macOS-host artifact (`/var` vs `/private/var` tmp spelling vs `StringComparison.Ordinal`); the job runs `ubuntu-latest`. Recorded, not repaired. |
| 4 | workspace drift | `workspace` | `uv run python -m tools.contract_drift.drift --workspace` | **0** | `all members clean and every edge resolved` (1 member baseline, 1 edge) |
| 4b | cross-repo pytest set | `workspace` | the five paths at `ci.yml:312` | **0** | **31 passed** |
| 5 | stale-derived | `stale-derived` | `docs_sync && memory_regen.contracts_index`, then diff | **0** | **13 reference pages + 15 contracts indexed**, regenerated; `git diff --exit-code` over `docs/reference` + `contracts-index.md` = **0**, porcelain empty |
| 6 | lifecycle | `lifecycle-eval` | `uv run python -m tools.lifecycle_eval.runner` | **0** | **20/20 fixtures PASS** (5 FAST, 5 STANDARD, 5 STRICT, 5 CONTROLLED), `false_downgrades_enforced_zero: true` |
| 6b | lifecycle tests | `lifecycle-eval` | `uv run pytest tools/lifecycle_eval` | **2** | **RED — 1 error during collection**, `ModuleNotFoundError: No module named 'tools'`. NEW finding, see `gaps.integration`. |
| 7 | GEN-04 twin | `core-suite` + `workspace` | `uv run pytest .../test_core_no_example_dep.py .../test_core_no_workspace_member_dep.py` | **0** | **24 passed** |
| 7b | full harness lint | — | `uv run pytest tools/harness_lint -q` | **0** | **316 passed** |
| 8 | docs guard | `docs-guard` | `uv run python -m tools.docs_guard` | **1** | **6 x `broken-binding`, 2 x `STALE_ADVISORY`; 8 bindings, 7 uncovered, no ratchet.** EXPECTED AND CORRECT — `docs/.docs-review-ledger.toml` does not exist (RAT-1). |
| 8b | docs guard unit tests | `docs-guard` | `uv run pytest tools/docs_guard tools/memory_regen/tests/test_docs_staleness.py -q` | **0** | **244 passed, 1 snapshot passed** |
| 9 | emit-drift | `emit-drift` | `uv run python -m tools.harness_emit` then `git status --porcelain -- .opencode .claude opencode.json AGENTS.md CLAUDE.md` | **0** | **100 artifacts emitted, porcelain EMPTY.** Checked untracked-visible, not with bare `git diff` — the CI job's own blind spot is carried debt (15-REVIEW CR-01). The bare `git diff --exit-code` at `ci.yml:213` also returns 0. |
| 10 | model-identifier lint | `core-suite` (+ `emit-drift`) | `uv run pytest .../test_agents.py .../test_opencode_json.py tools/harness_emit/tests/test_validators.py .../test_opencode_config.py -q` | **0** | **47 passed** (5 of them the `-k model` subset) |
| 11 | injector budget | `core-suite` | `uv run pytest tools/memory_regen/tests/test_inject_assembler.py -q` | **0** | **22 passed** (~4000-char budget + byte-identity) |
| 12 | `git diff --check` | **NOT a job** (D-12) | `git diff --check` | **0** | clean — working tree has no modifications at `bad6749` |
| — | contract-check | `contract-check` | the `ci.yml:109-127` loop | **0** | **2 schema/instance pairs**, both valid, `fail=0` |

**Phase 29 added ZERO CI jobs and ZERO `gate.needs` entries (D-11).** `gate.needs` is the same
eleven-plus-`gate` list it was before the phase (`ci.yml:321`); `docs-guard` was Phase 28's. **And
`git diff --check` remains a verification command, never a job (D-12)** — making it one would
double-report `polyglot_lint`.

**One standing hygiene note on `git diff --check`:** it is clean here only because the tree is
clean. `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` carries **983** committed
trailing-whitespace lines (980 at the Phase-29 base). That is a syrupy generator artifact in a
derived snapshot, not hand-authored debt, and it surfaces whenever the snapshot is regenerated.

## Requirements Coverage (per-requirement evidence)

One row per requirement. The evidence column cites the artifact that proves it. **STATE.md's
percentages are not evidence and appear nowhere below.**

| REQ | Phase | Traceability | VERIFICATION.md | Live integration / evidence | Final |
|-----|-------|--------------|-----------------|------------------------------|-------|
| TOPO-01 | 24 | Complete | **passed** (`24-VERIFICATION.md`) | `contracts/harness/topology/relationship.schema.json` ratified through the contract-hash path; positive/negative fixtures in the TEST plane to keep GEN-04 green; drift **OK** live | **satisfied** |
| TOPO-02 | 24 | Complete | **passed** | additive `[[contract_graph.relationships]]` accessor returns raw data only; legacy loader API byte-unchanged | **satisfied** |
| TOPO-03 | 24 | Complete | **passed** | `effective_relationships()` lowers legacy `[pipeline].edges`, unions with explicit records, raises on dup-id / dup-semantic-edge / contradiction; linear fixtures byte-unchanged | **satisfied** |
| TOPO-04 | 25 | Complete | **passed** (`25-VERIFICATION.md`) | `tools/contract_graph/compile.py` — stable-sorted, repo-confined, diagnostics returned as a sorted `list[str]`, never raised | **satisfied** |
| TOPO-05 | 25 | Complete | **passed** | `query.py` direct/reverse/transitive over the compiled adjacency; iterative visited-set worklist is cycle-safe; the no-task-evidence / no-contract-preload invariant is a STRUCTURAL source test | **satisfied** |
| TOPO-06 | 25 | Complete | **passed** | `/pipeline` + `pipeline-map` gained an indented-tree render as an ADDITIONAL section; the locked linear render is pinned by a hardcoded literal-string regression test (0 removed lines); no new command or persona | **satisfied** |
| TOPO-07 | 25 | Complete | **passed** | every non-linear shape fixture-proven (shared-contract fan-out, request/response as two separate records, event fan-out, legal cycle, cross-repo authority); log-parser instance provably untouched; GEN-04 green (**24 passed** live) | **satisfied** |
| ADOPT-01 | 26 | Complete | **passed** (`26-VERIFICATION.md`) | `tools/adoption_scan` — confined, ignore-respecting, size-capped evidence pointers + hashes; `destination_catalog()` rule-derives from the real git-tracked tree (26-07 CR-01) | **satisfied** |
| ADOPT-02 | 26 | Complete | **passed** | every candidate classified observed/inferred/unknown with source evidence; unresolved ownership becomes a `questionRecord` carrying no `owner` — the `OWNER_TBD` never-fabricate rule, re-asserted live by 29-02's test | **satisfied** |
| ADOPT-03 | 26 | Complete | **passed** | `plan.schema.json` `dispositionEnum` is exactly the six required values; `classify()` walks `schema_surfaces` per evidence pointer (26-09 WR-05) | **satisfied** |
| ADOPT-04 | 27 | Complete | **passed** (via `27.1`/`27.2`; Phase 27 has no VERIFICATION.md — see nyquist) | batches live under `.workflow/tasks/<task-id>/artifacts/adoption/<batch-id>/`; `batch.py` content-derives the batch id, zero `transitions.json` edit | **satisfied** |
| ADOPT-05 | 27 | Complete | **passed** (`27.1`, `27.2`) | `refuse_unsafe_destination` is the single choke point (`apply.py:109`); case-insensitive constitution globs always-on; directory-shaped + ancestor-is-a-file refusals closed in 27.2 with a 10-row table; concurrency test proven to go RED when `flock` is neutered | **satisfied** |
| ADOPT-06 | 27 | Complete | **passed** (`27.1`) | `approval.schema.json` binds (draft_hash, task_revision, git_ref); `check_valid` never raises across a 12-row corruption table (27.2 WR-06) | **satisfied** |
| ADOPT-07 | 27 | Complete | **passed** | one thin `/adopt` + `brownfield-adoption` skill over deterministic tools; three generic fixtures (polyglot single-repo, 2-repo client/server, partial-adoption/collision), one carrying CRLF+BOM input | **satisfied** |
| DOCSUP-01 | 28 | Complete | **human_needed** (`28-VERIFICATION.md` SC-1, 4/4 truths) | `registry.py:275-375` implements all five rejections as SEMANTIC passes after the schema pass; ~26 rejection rows across 6 classes incl. class D generated by ITERATING the imported `DERIVED_GLOBS`, plus a 6-row VALID negative control. Live: an escaping registry exits **3** | **satisfied** |
| DOCSUP-02 | 28 | Complete | **human_needed** | `_ROW_KEYS` is the four-key allowlist — no timestamp, identity, prose or model token; `digest.py` interleaves `path\n` + per-file `sha256\n`; `_confine` raises on a root escape including via an in-tree symlink | **satisfied** |
| DOCSUP-03 | 28 | Complete | **human_needed** | `guard.py:383-434` five-state first-match-wins with BROKEN ordered first; `ok=False` only for BROKEN/STALE_REQUIRED; both ratchets read from the previous COMMITTED ledger post-WR-01. Observed live: **6 broken-binding, 2 STALE_ADVISORY, 8 bindings, 7 uncovered** | **satisfied** |
| DOCSUP-04 | 28 | Complete | **human_needed** | queue regenerates byte-identically (md5 compared across two runs); the never-drop tuple was NOT widened and the docs pointer sits in the droppable region; budget still 4000. Live: **22 passed** in `test_inject_assembler.py` | **satisfied** |
| DOCSUP-05 | 28 | Complete | **human_needed** | `_drifted_paths` carries the PATH half only, so drift is never restated (`test_drift_findings_not_restated`); accepted ADRs yield only `REVIEWED_STILL_CURRENT` / `SUPERSEDING_ADR_REQUIRED` | **satisfied** |
| DOCSUP-06 | 29 | **Complete (machinery)** | **missing** (29 not yet verified) | `tools/docs_guard/exclusions.py` — a pure `exclusion_reason()` built from glob lists IMPORTED from `contract_guard` / `destinations` / `registry`, with 15 static rows over 6 classes and three per-class DELETION proofs, so removing a glob at its home reds named rows repo-wide. `/docs-update` + `docs-upkeep` emitted byte-identically to both runtimes (**100 artifacts, porcelain empty**); `EXPECTED_SKILLS` 12->13 and command count 24->25 moved in the same change; the wiring lint asserts the command body contains NO `.memory/` path at all (D-04's fresh-clone false green cannot be reintroduced) | **BLOCKED on RAT-1** |
| DOCSUP-07 | 29 | **Complete (machinery)** | **missing** | 14 integration tests across all three ADR-0010 clause-3b layers: one apply cycle lands the registry proposal and buckets the ledger destination `refused` with a zero-write spy over `builtins.open` + `tempfile.mkstemp`; the raised type is `ReviewLedgerRefusal` and NOT a `ConstitutionRefusal`; a fresh id and a REPOINTED already-ratified id both fail to classify FRESH; every claim mutation-proven | **BLOCKED on RAT-1** |

**Requirement score: 19/21 satisfied.** The two open ones are not unbuilt — they are un-ratified.

## Nyquist Coverage

| Phase | VALIDATION.md | VERIFICATION.md | Action |
|-------|---------------|-----------------|--------|
| 24 | exists | passed | none — COMPLIANT |
| 25 | exists | passed | none — COMPLIANT |
| 26 | exists | passed | none — COMPLIANT |
| 26.1 | exists | passed | none — COMPLIANT |
| 26.2 | exists | passed | none — COMPLIANT |
| 27 | exists | **MISSING** | recorded as debt; ADOPT-04..07 are evidenced through 27.1/27.2 instead |
| 27.1 | exists | passed (5/5) | none — COMPLIANT |
| 27.2 | **MISSING** | passed (5/5) | recorded as debt |
| 28 | **MISSING** | human_needed (4/4 truths) | recorded as debt; the phase cannot reach `passed` until RAT-1/RAT-2 |
| 29 | exists (authored by 29-05) | **MISSING** | `/gsd:verify-phase 29` after the human gates |

Recorded as observed. Not back-filled — a VALIDATION.md written after the fact records nothing.
Phase 29 authored its own (`29-VALIDATION.md`) so that a phase auditing two missing files did not
silently become a third.

### Addendum — 2026-07-21, after this audit

The table above is the audit-time observation and is deliberately left standing. Two of its three
debt rows have since been discharged:

| Phase | Then | Now | How |
|-------|------|-----|-----|
| 27.2 | VALIDATION.md **MISSING** | `27.2-VALIDATION.md` exists, `authored: at-closeout` | transcribed from both PLANs' `<automated>` verify blocks + `27.2-VERIFICATION.md` (passed, 5/5). No MISSING, no PARTIAL rows |
| 28 | VALIDATION.md **MISSING** | `28-VALIDATION.md` exists, `authored: at-closeout` | transcribed from the nine PLANs' verify blocks + `28-VERIFICATION.md` (human_needed, 4/4). No MISSING, no PARTIAL among agent-executable rows; the **three** human-gated rows carry their enforcement layers |
| 27 | VERIFICATION.md **MISSING** | still **MISSING** | not discharged — remains recorded debt |

The sentence above — "a VALIDATION.md written after the fact records nothing" — is the reason both
documents open by saying, in their own text, that they were written at closeout and could not have
steered the phase they describe. What an after-the-fact VALIDATION.md *can* do is state checkably
that the coverage the phase owed was delivered, sourced entirely to artifacts that predate it. What
it must never do is present itself as having been there.

One defect surfaced while authoring them, recorded rather than repaired in passing:
`28-08-PLAN.md:257` `&&`-chains `uv run python -m tools.docs_guard` into the phase-closing fan-in,
and that member exits 1 by design until RAT-1 lands. The gate was in fact satisfied member-by-member
(`28-VERIFICATION.md:72-82`). A fan-in whose members include a designed-non-zero gate must not be
`&&`-chained.

**Correction, same day.** The first draft of `28-VALIDATION.md` recorded 28-01 Task 2 as a second
automated task with a green tick. It is a `checkpoint:human-verify` with `gate="blocking"` — the
human ratification of the constitution-plane schema write, mitigating T-28-01, and it is **RAT-4**
in this audit's own ratification list. The phase therefore has **three** human-gated rows, not two.
Two further flattenings were corrected in the same pass: 28-07 Task 1's gate was RED on arrival
(DEF-28-01, inherited from plan 28-05, correctly not auto-fixed), and 28-08 Task 2's fan-in first
ran `4 failed, 1384 passed` before reaching 1388. Each error pointed the same direction — a phase
recorded as greener than it was — which is the exact failure mode Phase 28 exists to prevent, and
it recurred here in the document *about* that phase. Recorded rather than silently amended.

## Outstanding human gates — the order to discharge them

1. **RAT-1 — author `docs/.docs-review-ledger.toml`.** Byte-exact content is in
   `29-04-SUMMARY.md`; author against the **POST-edit** tree (`gate-model-permission-surface`
   carries `8df85e6e...`, not `4568f3a9...`). Option A is one round straight to exit 0; Option B is
   two rounds and observes the `0 -> 1` leg for real, which is what the plan intended. RAT-3 is the
   review you perform while doing this.
2. **RAT-2 — ratify ADR-0010.** Read clause 3b with care; ADRs are append-only once accepted.
3. **RAT-4 / RAT-5 — the provenance backlog.** The Phase-28 schema write landed under
   `HARNESS_DEV_BYPASS`, and four ADRs remain unmerged to `main` behind a CODEOWNERS gate that
   structurally cannot fire. The durable fix for both is repo-config: flip the GitHub default
   branch back to `main`.
4. Then `/gsd:verify-phase 29`, and `/gsd:complete-milestone` — which moves committed history and
   is deliberately not automated inside plan 29-05.

## Recommended before close (non-blocking)

- Give `tools/lifecycle_eval/tests/` a `conftest.py` matching its eighteen siblings, so the
  `lifecycle-eval` job's step 2 stops erroring at collection.
- Close 15-REVIEW CR-01: switch `emit-drift` from bare `git diff --exit-code` to the
  `git add -A` + `--cached` idiom that already exists at `ci.yml:238`.
- Backfill VALIDATION.md for 27.2 and 28, and VERIFICATION.md for 27 — or accept them as the
  recorded debt above.
