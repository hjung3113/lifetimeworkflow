---
phase: 47-package-facts
plan: 04
type: execute
wave: 3
depends_on: [47-02]
files_modified:
  - tools/contract_graph/ownership.py
  - tools/contract_graph/__init__.py
  - tools/contract_graph/tests/test_ownership.py
autonomous: true
requirements: [MONO-04]

must_haves:
  truths:
    - "Given a contract path, contract_graph reports the package that owns it, using the Phase 47-02 package facts — no second traversal/graph engine."
    - "The nearest-enclosing-package-directory rule falls back to the root package when no manifest directory encloses the contract path, and this fallback is asserted explicitly, not left implicit."
  artifacts:
    - path: "tools/contract_graph/ownership.py"
      provides: "owning_package(packages, contract_path) — a pure lookup, not a traversal"
      contains: "def owning_package"
    - path: "tools/contract_graph/tests/test_ownership.py"
      provides: "domain-neutral synthetic fixtures + one instance-style-fallback proof using a SYNTHETIC path"
      contains: "def owning_package"
  key_links:
    - from: "tools/contract_graph/ownership.py::owning_package"
      to: "tools/memory_regen/package_facts.py::build_facts"
      via: "caller passes build_facts()['packages'] in; owning_package never imports package_facts itself"
      pattern: "def owning_package"
    - from: "tools/contract_graph/__init__.py"
      to: "tools/contract_graph/ownership.py::owning_package"
      via: "PEP 562 lazy re-export, added to the existing _SOURCE_MODULE dispatch dict"
      pattern: "\"owning_package\": \"ownership\""
---

<objective>
Add `owning_package(packages, contract_path)` to a new `tools/contract_graph/ownership.py` — a
PURE lookup (nearest-enclosing-package-directory string-prefix walk, falling back to the root
package), never a second traversal engine, never touching `compile.py`'s adjacency or `query.py`'s
`direct`/`reverse`/`transitive`.

Purpose: MONO-04's contract → owning-package attribution, reusing only the existing
`tools/contract_graph` module (as MONO-04 explicitly names it) and the Plan 47-02 package facts —
callers pass `build_facts()["packages"]` in; `owning_package` itself never imports
`package_facts` (keeps it a pure, dependency-free lookup, mirroring `compile.py`'s
`_tracked_schemas` glob-existence idiom, not `query.py`'s adjacency traversal).
**GEN-04 note (hard constraint):** the ratified root-fallback rule means real
`examples/log-parser/contracts/**` paths resolve to the root package (no manifest exists at the
instance root). This behavior MUST be asserted explicitly — but the assertion MUST use a
SYNTHETIC instance-style path fixture (e.g. `"instance/contracts/widget.schema.json"` with no
matching package dir), never the literal live `examples/log-parser/contracts/...` path — hard
constraint, no exception taken, overriding any prior research note suggesting a narrow exception.
Output: `owning_package()`, its lazy re-export, and a domain-neutral test suite.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-package-facts/47-CONTEXT.md
@.planning/phases/47-package-facts/47-RESEARCH.md
@.planning/phases/47-package-facts/47-PATTERNS.md
@.planning/phases/47-package-facts/47-02-SUMMARY.md
</context>

<interfaces>
<!-- build_facts()'s package shape, from Plan 47-02 — owning_package's `packages` param uses
     this shape (id, dir only needed). -->
`tools.memory_regen.package_facts.build_facts()["packages"]` is
`[{"id": str, "manifest": str, "dir": str, "language": str}, ...]` — `owning_package` reads only
`"id"` and `"dir"`.

<!-- The pure-lookup idiom to mirror, source: tools/contract_graph/compile.py:39-46 (already read
     in full this phase): -->
```python
def _tracked_schemas(contracts_dir: Path) -> set[str]:
    """Return the set of tracked contract ids under contracts_dir ...
    A missing directory yields an empty set ... the caller then reports unknown-contract.
    """
    return {p.name.removesuffix(".schema.json") for p in contracts_dir.rglob("*.schema.json")}
```

<!-- The lazy re-export dispatch to extend, source: tools/contract_graph/__init__.py (full file
     already read this phase): -->
```python
_SOURCE_MODULE = {
    "compile_graph": "compile",
    "direct": "query",
    "reverse": "query",
    "transitive": "query",
}

def __getattr__(name: str):
    if name in _SOURCE_MODULE:
        import importlib
        module = importlib.import_module(f"tools.contract_graph.{_SOURCE_MODULE[name]}")
        return getattr(module, name)
    raise AttributeError(f"module {__name__!r} has no attribute {name!r}")
```

<!-- Domain-neutral fixture convention (GEN-04-required), source: tools/contract_graph/tests/
     test_compile.py:13-15 (already read): -->
```
All hand-built fixtures use domain-neutral names (authorities "a"/"b", contract "widget") so this
core-plane test stays GEN-04-clean (no instance-path or domain-prose tokens).
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement owning_package() + wire the lazy re-export</name>
  <files>
    tools/contract_graph/ownership.py
    tools/contract_graph/__init__.py
  </files>
  <read_first>
    tools/contract_graph/compile.py (full file, already read — `_tracked_schemas`'s pure-lookup
    idiom + the module docstring register: names the requirement id, states what this module does
    NOT do), tools/contract_graph/query.py (full file, already read — confirms `direct`/`reverse`/
    `transitive` and the `graph["adjacency"]` shape are NOT touched by this new module),
    tools/contract_graph/__init__.py (full file, already read — the `_SOURCE_MODULE` dispatch
    dict to extend), 47-CONTEXT.md ("Override slot & contract ownership" section — "nearest
    enclosing package directory... falling back to the root package"), 47-RESEARCH.md
    ("Pitfall 4" — the root-fallback behavior and why it fires for the entire example-instance
    contracts tree today).
  </read_first>
  <behavior>
    - `owning_package(packages: list[dict], contract_path: str) -> str` — pure function, no file
      I/O, no import of `tools.memory_regen.package_facts` (the caller supplies `packages`).
    - Normalize `contract_path` to POSIX form (`PurePosixPath(contract_path)`). For every package
      in `packages`, its `"dir"` field is a POSIX-relative directory path (`"."` for the repo
      root, or e.g. `"tools/contract_graph"` for a nested package). A package "encloses"
      `contract_path` when `contract_path`'s parts start with that package's `dir`'s parts (the
      package at `dir="."` encloses every path, by definition — every path's parts start with the
      empty parts sequence).
    - Among all enclosing packages, the WINNER is the one whose `dir` has the MOST path segments
      (the nearest/deepest ancestor — e.g. `"tools/contract_graph"` wins over `"tools"` wins over
      `"."` for a contract under `tools/contract_graph/...`). Ties (two packages declaring the
      identical `dir`) are broken by sorted `"id"` (deterministic, never dict/set-iteration-order
      dependent).
    - If NO package has `dir == "."` (no root package) AND no package encloses `contract_path`,
      raise `ValueError` naming `contract_path` — fail loud, never fabricate an owner (mirrors
      this module family's existing "never invent" posture). In every realistic input (a `packages`
      list that includes the actual repo-root manifest), a `dir="."` package always exists and the
      function never raises — the raise path exists only to guard a genuinely malformed
      `packages` list a caller might pass.
    - Module docstring register (mirrors `compile.py`'s and `query.py`'s): name MONO-04
      explicitly; state explicitly that this is a PURE LOOKUP, not a traversal — it does not touch
      `compile_graph`'s adjacency and does not call `direct`/`reverse`/`transitive`; state the
      fallback rule and that it is a DELIBERATE, ratified consequence of "no manifest exists
      between an instance's contracts directory and its own root" — not a bug.
  </behavior>
  <action>
    Create `tools/contract_graph/ownership.py` implementing `owning_package` exactly as specified.
    Add `"owning_package": "ownership"` to the `_SOURCE_MODULE` dict and `"owning_package"` to
    `__all__` in `tools/contract_graph/__init__.py` — the existing per-name dispatch dict already
    supports adding a third source module with zero structural change to `__getattr__`. Keep every
    comment/docstring domain-neutral — no `examples/` literal anywhere in this file (GEN-04).
  </action>
  <acceptance_criteria>
    `grep -c "^def owning_package" tools/contract_graph/ownership.py` returns `1`.
    `grep -c "owning_package" tools/contract_graph/__init__.py` returns at least `2`.
    `uv run python -c "from tools.contract_graph import owning_package"` (from the repo root)
    succeeds. `grep -c "examples/" tools/contract_graph/ownership.py
    tools/contract_graph/__init__.py` returns `0` for each file. `grep -c "direct\|reverse\|transitive"
    tools/contract_graph/ownership.py` returns `0` (proves no traversal-engine coupling).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Domain-neutral tests + the required synthetic root-fallback proof</name>
  <files>tools/contract_graph/tests/test_ownership.py</files>
  <read_first>
    tools/contract_graph/tests/test_compile.py (first 60 lines, already read — the exact
    domain-neutral synthetic-fixture register: `"a"`/`"b"`/`"widget"` vocabulary, explicit
    docstring statement about staying GEN-04-clean), 47-CONTEXT.md "Resolved after research"
    section (the root-fallback rule MUST be asserted, and the hard constraint requires a SYNTHETIC
    instance-style path, never the live `examples/log-parser/contracts/...` string).
  </read_first>
  <action>
    Create `tools/contract_graph/tests/test_ownership.py` with domain-neutral synthetic fixtures
    throughout (ids `"a"`/`"b"`/`"widget"`-style, `dir` values like `"."`, `"components/a"`,
    `"components/a/sub"` — never a literal `examples/log-parser/...` string anywhere in this
    file):
    - `test_root_package_owns_unenclosed_contract`: `packages = [{"id": "root", "dir": "."}]`;
      `owning_package(packages, "contracts/widget.schema.json") == "root"`.
    - `test_nearest_enclosing_package_wins_over_root`: `packages = [{"id": "root", "dir": "."},
      {"id": "a", "dir": "components/a"}]`; a contract under `"components/a/contracts/widget.schema.json"`
      resolves to `"a"`, while a contract under `"contracts/widget.schema.json"` (not under
      `components/a`) still resolves to `"root"`.
    - `test_deepest_ancestor_wins_over_shallower_ancestor`: `packages = [{"id": "root", "dir":
      "."}, {"id": "a", "dir": "components"}, {"id": "b", "dir": "components/a"}]`; a contract
      under `"components/a/contracts/widget.schema.json"` resolves to `"b"` (deepest), not `"a"`
      or `"root"`.
    - `test_synthetic_instance_style_fallback_documented`: THE required root-fallback proof
      (CONTEXT.md "Resolved after research" — must be asserted explicitly). Build
      `packages = [{"id": "root", "dir": "."}, {"id": "core-pkg", "dir": "components/core"}]` and
      a SYNTHETIC instance-style contract path with no enclosing manifest directory, e.g.
      `"instance/contracts/log-specs/widget.schema.json"` (mirrors the real shape — an
      instance-style subtree with no manifest of its own — without ever spelling `examples/` or
      `log-parser` literally); assert `owning_package(packages, that_path) == "root"`. Comment
      this test explicitly as the sanctioned documentation of the Pitfall-4 fallback behavior,
      proven on synthetic data per the phase's hard constraint (never the live example path).
    - `test_no_root_package_raises_for_unenclosed_path`: `packages = [{"id": "a", "dir":
      "components/a"}]` (no `dir == "."` package), contract path `"contracts/widget.schema.json"`
      (not enclosed by `"components/a"`) — assert `ValueError` is raised, never a fabricated
      owner.
    - `test_tie_break_is_deterministic_sorted_id`: two packages sharing the identical `dir` (a
      contrived but legal input) — assert the winner is the lexicographically smaller `"id"`,
      and assert two calls with reordered but identical `packages` input yield the same result
      (order-independence).
  </action>
  <acceptance_criteria>
    `uv run pytest tools/contract_graph/tests/test_ownership.py -x -q` passes. Mutation check:
    temporarily change `test_deepest_ancestor_wins_over_shallower_ancestor`'s expected id to `"a"`
    (the shallower package) and confirm the test FAILS against the real implementation, then
    revert. `grep -c "examples/" tools/contract_graph/tests/test_ownership.py` returns `0`.
    `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x -q` still passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `contract_path` argument → `owning_package` | Caller-supplied string, used only for POSIX-part comparison against known package `dir` values — never opened as a file or used to construct a filesystem path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04-01 | Tampering | `owning_package`'s path-part comparison | mitigate | Pure string/`PurePosixPath`-parts comparison, never `Path.resolve()`/`open()` on `contract_path` — a crafted `../../../etc/passwd`-shaped input simply fails to match any known package `dir` (or matches root, which is the documented, safe fallback) and is never used to read a file. |
</threat_model>

<verification>
`uv run pytest tools/contract_graph/tests/test_ownership.py -x -q` green.
`uv run pytest tools/contract_graph -q` (full package, no regression) green.
</verification>

<success_criteria>
`owning_package()` exists as a pure lookup (no adjacency/traversal coupling), correctly resolves
nearest-enclosing-package attribution including the deterministic tie-break, and the root-fallback
rule is explicitly asserted on a synthetic instance-style fixture — never the live example path.
</success_criteria>

<output>
Create `.planning/phases/47-package-facts/47-04-SUMMARY.md` when done.
</output>
