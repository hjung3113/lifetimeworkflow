---
phase: 47-package-facts
plan: 05
type: execute
wave: 3
depends_on: [47-02]
files_modified:
  - .github/workflows/ci.yml
  - .gitignore
  - tools/harness_lint/tests/test_ci_stale_derived.py
  - harness/commands/refresh-memory.md
  - harness/agents/curator.md
autonomous: true
requirements: [MONO-01, MONO-04]

must_haves:
  truths:
    - "ci.yml's job set and gate.needs are byte-unchanged from the phase's base commit — no new gate, no new CI job."
    - "The new artifact's freshness rides the EXISTING stale-derived job (widened regen command + widened diff path list), never a new job."
    - "The stale-derived job never interpolates ${{ github.event.* }} in any run shell — the invariant survives the widening."
    - ".memory/derived/package-facts.md is committed (not gitignored), following the same .gitignore contents-form re-inclusion idiom as contracts-index.md."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "stale-derived job widened to regen + diff .memory/derived/package-facts.md"
      contains: "tools.memory_regen.package_facts"
    - path: ".gitignore"
      provides: "package-facts.md re-included via the contents-form .memory/derived/* exclusion"
      contains: "!.memory/derived/package-facts.md"
    - path: "tools/harness_lint/tests/test_ci_stale_derived.py"
      provides: "the SC5 proof — _DERIVED_PATHS widened to 3 entries, the third regen module asserted"
      contains: ".memory/derived/package-facts.md"
  key_links:
    - from: ".github/workflows/ci.yml (stale-derived job)"
      to: "tools/memory_regen/package_facts.py"
      via: "the existing job's regen step gains a third `&&`-chained module invocation"
      pattern: "tools\\.memory_regen\\.package_facts"
    - from: "harness/commands/refresh-memory.md"
      to: "tools/memory_regen/package_facts.py"
      via: "the command's step-2 committed-derived regen line gains the third module"
      pattern: "tools\\.memory_regen\\.package_facts"
---

<objective>
Wire the Plan 47-02 `.memory/derived/package-facts.md` artifact into the EXISTING `stale-derived`
CI job (widen its regen command + its diff path list — never add a job), re-include it via
`.gitignore`'s contents-form pattern (mirroring `contracts-index.md`'s exact re-inclusion idiom),
and widen the `stale-derived` gate's own structural test
(`tools/harness_lint/tests/test_ci_stale_derived.py`) so `_DERIVED_PATHS` and its two
module-invocation assertions actually prove the new artifact rides the existing job — SC5's
explicit requirement, per CONTEXT.md: "widening them is what proves the new artifact rides the
existing job (SC5) instead of a new one." Also wire the local `/refresh-memory` command + the
`curator` persona to regenerate `package-facts.md` alongside `contracts-index.md`, and round-trip
that prose edit through the existing emitter (`uv run python -m tools.harness_emit`) so both
runtime trees (`.opencode/`, `.claude/`) stay byte-clean — reusing the ALREADY-non-bypassable
`emit-drift` CI job, not a new one.

Purpose: this plan is the phase's binding no-growth proof (`ci.yml`'s job set + `gate.needs`
byte-unchanged from base commit; `stale-derived`'s "never interpolate `${{ github.event.* }}`"
invariant preserved) and the artifact's actual committed-status (SC1's "committed derived
artifact" requires the `.gitignore` re-inclusion this plan adds — without it, `package-facts.md`
would silently stay gitignored like `repo-map.md`, contradicting MONO-01).
Output: 3 mechanical, exact-line widenings (`ci.yml`, `.gitignore`, `test_ci_stale_derived.py`)
plus a 4th prose-and-emit wiring (`refresh-memory.md`, `curator.md`, re-emit).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/47-package-facts/47-CONTEXT.md
@.planning/phases/47-package-facts/47-RESEARCH.md
@.planning/phases/47-package-facts/47-PATTERNS.md
@.planning/phases/47-package-facts/47-02-SUMMARY.md
</context>

<interfaces>
<!-- Exact widening targets, all already read in full this phase. -->

`.github/workflows/ci.yml:278-283` (stale-derived job, existing, to widen):
```yaml
- name: Regenerate the committed-derived set
  run: uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index
- name: Fail on any stale committed-derived artifact (untracked-safe)
  run: |
    git add -A -- docs/reference .memory/derived/contracts-index.md
    if ! git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md; then
```
(the failure-echo block below these lines also lists the two paths in its printed fix-command
text — widen those echoed lines too, for consistency, though they are not gated by any test).

`.gitignore:20-27` (existing, to widen by exactly one `!` line):
```gitignore
.memory/derived/*
!.memory/derived/contracts-index.md
```

`tools/harness_lint/tests/test_ci_stale_derived.py:32` (existing tuple, to widen):
```python
_DERIVED_PATHS = ("docs/reference", ".memory/derived/contracts-index.md")
```
and `:74-83` (existing assertion function, to widen by one line):
```python
def test_stale_derived_regenerates_both_derived_generators() -> None:
    ci = _load_ci()
    joined = "\n".join(_run_texts(ci["jobs"]["stale-derived"]))
    assert "tools.docs_sync" in joined
    assert "tools.memory_regen.contracts_index" in joined
```
`test_stale_derived_is_in_gate_needs` (`:95-100`) — DO NOT touch; `gate.needs` itself stays
unchanged (no new job).

`harness/commands/refresh-memory.md:30` (existing, to widen):
```
!`uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`
```
`harness/agents/curator.md:25-26` (existing, to widen by one bullet):
```
- Contracts-index (committed-derived): `uv run python -m tools.memory_regen.contracts_index`.
- Reference docs (committed-derived): `uv run python -m tools.docs_sync`.
```
`tools/harness_lint/tests/test_derived_freshness.py` `_ALLOWED_TOOL_MODULES` (`:27`) is
`frozenset({"memory_regen", "docs_sync"})` — `tools.memory_regen.package_facts` is already inside
the allowed `memory_regen` module family, so this test needs NO edit (verify, do not touch).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen ci.yml, .gitignore, and the stale-derived structural test</name>
  <files>
    .github/workflows/ci.yml
    .gitignore
    tools/harness_lint/tests/test_ci_stale_derived.py
  </files>
  <read_first>
    .github/workflows/ci.yml (lines 255-300, already read in full — the exact `stale-derived` job
    body including its failure-echo block), .gitignore (full file, already read — the contents-
    form re-inclusion idiom + its own documented "pitfall P3" comment), 
    tools/harness_lint/tests/test_ci_stale_derived.py (full file, already read — the structural +
    negative-control test shape; only `_DERIVED_PATHS` and
    `test_stale_derived_regenerates_both_derived_generators` change), 47-RESEARCH.md "Pattern 3"
    section (states the exact widened lines verbatim).
  </read_first>
  <action>
    In `.github/workflows/ci.yml`'s `stale-derived` job: append
    `&& uv run python -m tools.memory_regen.package_facts` to the "Regenerate the committed-derived
    set" step's `run:` line; append `.memory/derived/package-facts.md` to BOTH `git add -A --`
    and `git diff --cached --exit-code --` path lists in the "Fail on any stale committed-derived
    artifact" step; append the same path + module name to the failure-echo block's printed
    fix-commands (the `echo` lines listing the regen command and the `git add` command) so a
    developer reading a failed CI run sees the complete, copy-pasteable fix. Do NOT touch
    `gate.needs`, the job name, or any other job in the file.

    In `.gitignore`: add `!.memory/derived/package-facts.md` directly beneath the existing
    `!.memory/derived/contracts-index.md` line, same contents-form syntax, same comment block
    context (do not move or duplicate the comment above it).

    In `tools/harness_lint/tests/test_ci_stale_derived.py`: widen `_DERIVED_PATHS` (line 32) to
    `("docs/reference", ".memory/derived/contracts-index.md", ".memory/derived/package-facts.md")`
    — `test_stale_derived_uses_untracked_safe_diff_primitive` already iterates
    `_DERIVED_PATHS` so its coverage widens automatically, no other edit needed there. Add
    `assert "tools.memory_regen.package_facts" in joined` as a new line inside
    `test_stale_derived_regenerates_both_derived_generators` (after the existing
    `"tools.memory_regen.contracts_index"` assertion) — this is the SC5 proof line; consider
    renaming the function (Claude's discretion on the name only, e.g. to
    `test_stale_derived_regenerates_all_three_derived_generators`) but the module-string
    assertions are load-bearing and must all remain. Do NOT touch
    `test_stale_derived_is_in_gate_needs` or any negative-control test in this file.
  </action>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint/tests/test_ci_stale_derived.py -x -q` passes, including the
    widened `test_stale_derived_regenerates_...` test. `grep -c "package-facts.md" .gitignore`
    returns `1`. `grep -c "tools.memory_regen.package_facts" .github/workflows/ci.yml` returns at
    least `1` (in the regen step) — confirm via `grep -n` that it appears inside the
    `stale-derived` job specifically, not another job. Diff the `jobs:` top-level key list of
    `.github/workflows/ci.yml` before/after this task (e.g. `git show HEAD:.github/workflows/ci.yml
    | grep -E "^  [a-z-]+:$"` vs the working tree) and confirm the SET of job names is unchanged.
    `grep -n "needs:" .github/workflows/ci.yml | grep -A1 "gate:"` (or equivalent) confirms
    `gate.needs` is byte-unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire refresh-memory.md + curator.md, then re-emit both runtimes</name>
  <files>
    harness/commands/refresh-memory.md
    harness/agents/curator.md
  </files>
  <read_first>
    harness/commands/refresh-memory.md (full file, already read — step 2's exact line to widen),
    harness/agents/curator.md (lines 1-30, already read — the bullet list to widen),
    tools/harness_lint/tests/test_derived_freshness.py (full file, already read — confirms
    `tools.memory_regen.package_facts` needs no allowlist change, since `memory_regen` is already
    an allowed module family; this task must NOT introduce any OTHER `tools.*` reference into
    either file, or that gate would fail), 47-RESEARCH.md "Validation Architecture" section
    ("Phase gate" row — states the emit re-emit-diff-clean requirement explicitly).
  </read_first>
  <action>
    In `harness/commands/refresh-memory.md`, section "## 2. Committed-derived (the gated set —
    commit after)": widen the shell-out line to also invoke
    `tools.memory_regen.package_facts` (e.g. `!`uv run python -m tools.docs_sync && uv run python
    -m tools.memory_regen.contracts_index && uv run python -m tools.memory_regen.package_facts``)
    and update the paragraph above it to mention `package-facts.md` alongside
    `docs/reference/**` + `contracts-index.md` as part of "the committed-derived set" the gate
    guards.

    In `harness/agents/curator.md`, add one new bullet immediately after the existing
    "Contracts-index (committed-derived): ..." bullet: "- Package facts (committed-derived):
    `uv run python -m tools.memory_regen.package_facts`." — same register, same "committed-derived"
    phrasing.

    Then run `uv run python -m tools.harness_emit` from the repo root to project both edited
    Markdown files' managed-block content into `.opencode/` and `.claude/` (glob-discovery based —
    no emitter code change needed, mirrors every prior phase's "prose-only, re-emit, byte-clean"
    pattern per STATE.md's Phase 10/15/25 precedents). Stage and note the emitted diffs in the
    plan's SUMMARY (which `.opencode/`/`.claude/` files changed).
  </action>
  <acceptance_criteria>
    `grep -c "tools.memory_regen.package_facts" harness/commands/refresh-memory.md` returns at
    least `1`. `grep -c "tools.memory_regen.package_facts" harness/agents/curator.md` returns at
    least `1`. `uv run pytest tools/harness_lint/tests/test_derived_freshness.py -x -q` still
    passes (proves the new reference stays inside the allowed `memory_regen` module family and no
    on-write hook gained a regen reference). `uv run python -m tools.harness_emit` exits 0 a
    second time immediately after the first run with NO further diff (idempotent — the emit-drift
    CI job's exact check: re-emit, `git diff --exit-code` over the documented emit path set,
    reused here as a local pre-check before committing).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI shell steps (`stale-derived` job) | GitHub Actions `run:` blocks — must never interpolate `${{ github.event.* }}` (untrusted PR-controllable input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05-SC | Tampering | `stale-derived` job's widened `run:` shell steps | mitigate | The added text is 3 static strings (a module invocation, a path in `git add -A --`, the same path in `git diff --cached --exit-code --`) — no variable interpolation of any kind, let alone `${{ github.event.* }}`; `test_stale_derived_never_interpolates_event_input` (untouched by this plan) continues to prove the invariant holds after the widening. |
</threat_model>

<verification>
`uv run pytest tools/harness_lint -q` (full package) green.
`uv run python -m tools.harness_emit` re-emit is idempotent (no diff on a second run).
`uv run python -m tools.memory_regen.package_facts` followed by `git status --porcelain
.memory/derived/package-facts.md` shows the file staged/tracked (not gitignored).
</verification>

<success_criteria>
`ci.yml`'s job set and `gate.needs` are unchanged; the `stale-derived` job's regen + diff steps
cover `package-facts.md`; `.gitignore` re-includes it; `test_ci_stale_derived.py` proves the
widening structurally; `/refresh-memory` + `curator` locally regenerate it too; the emit round-trip
to both runtimes is byte-clean.
</success_criteria>

<output>
Create `.planning/phases/47-package-facts/47-05-SUMMARY.md` when done.
</output>
