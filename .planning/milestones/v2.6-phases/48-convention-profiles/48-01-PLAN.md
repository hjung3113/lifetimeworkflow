---
phase: 48-convention-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/harness_config/loader.py
  - tools/harness_config/__init__.py
  - tools/harness_config/tests/test_conventions_for.py
autonomous: true
requirements: [MONO-05, MONO-06]

must_haves:
  truths:
    - "An agent can ask conventions_for(path) and get the nearest-enclosing package's profile, not the repo-wide default, for a path inside libs/python"
    - "A path with no enclosing package gets an explicitly-marked default profile (is_default is distinguishable from 'this package happens to match the default')"
    - "Editing a [[languages]] command in harness/project.toml changes every profile that inherits it, with zero profile-authoring code edited (the MONO-06 strong falsifiable proof)"
    - "A package whose language has no [[languages]] row reports missing commands instead of raising"
  artifacts:
    - path: tools/harness_config/loader.py
      provides: "conventions_for(path, cfg=None, facts=None) + _nearest_agents_md(dir_) functions"
    - path: tools/harness_config/__init__.py
      provides: "conventions_for re-exported via the existing PEP 562 lazy dispatch"
    - path: tools/harness_config/tests/test_conventions_for.py
      provides: "5 tests proving MONO-05 nearest-wins/default and MONO-06's falsifiable command-inheritance form"
  key_links:
    - from: "tools/harness_config/loader.py::conventions_for"
      to: "tools/contract_graph/ownership.py::owning_package"
      via: "module-level import + direct call, no reimplementation"
      pattern: "from tools\\.contract_graph import owning_package"
    - from: "tools/harness_config/loader.py::conventions_for"
      to: "harness/project.toml [[languages]]"
      via: "languages(cfg) lookup by owner['language']"
      pattern: "languages\\(cfg\\)"
---

<objective>
Add `conventions_for(path)` — a pure join over the Phase-47 package facts and the `[[languages]]`
config that answers "which conventions apply at this path?" with nearest-wins resolution, reusing
`owning_package()` unchanged.

Purpose: this is the one net-new function Phase 48 needs (per CONTEXT.md/RESEARCH.md); Plan 02
(the derived-artifact rendering) and Plan 03 (`/component` step 2) both build on it directly.
Output: `tools.harness_config.conventions_for`, importable and tested, with zero new command/gate/
contract/dependency.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-convention-profiles/48-CONTEXT.md
@.planning/phases/48-convention-profiles/48-RESEARCH.md
@.planning/phases/48-convention-profiles/48-PATTERNS.md
@harness/skills/python-conventions/SKILL.md
@harness/skills/two-plane-memory/SKILL.md

<interfaces>
<!-- Exact signatures this plan reads/extends. Extracted from the checkout — no exploration needed. -->

From tools/harness_config/loader.py (existing, unmodified except for the new function):
```python
_REPO_ROOT = Path(__file__).resolve().parents[2]

def load_project(path: str | Path = _DEFAULT_PROJECT) -> dict: ...
def languages(cfg: dict | None = None) -> list[dict]: ...
def effective_packages(cfg: dict | None = None, facts: dict | None = None) -> list[dict]:
    # loader.py:200-250. Layers [[components]] over build_facts()["packages"]. A declared-only
    # component (no matching derived package id) carries NO "dir" key — see Pitfall 1.
```

From tools/contract_graph/ownership.py (existing, DO NOT MODIFY):
```python
def owning_package(packages: list[dict], contract_path: str) -> str:
    # packages: [{"id": str, "dir": str, ...}]; only "id"/"dir" read, bare subscript (no .get).
    # Deepest-ancestor-wins, sorted-id tie-break, dir="." root always matches, raises ValueError
    # only if no root package is present in `packages`.
```

From tools/harness_config/__init__.py (existing):
```python
__all__ = [
    "components", "contract_graph_relationships", "effective_packages",
    "effective_relationships", "language_bash_scopes", "languages", "load_project", "pipeline",
]
def __getattr__(name: str):  # PEP 562 — generic dispatch, no per-symbol branch needed
    if name in __all__:
        from tools.harness_config import loader
        return getattr(loader, name)
    raise AttributeError(...)
```

Each `[[languages]]` row (`harness/project.toml`) carries: `id`, `bash_scope`, `test`, `format`
(no `lint` field — MONO-06 must use the literal field names `test`/`format`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement conventions_for() + _nearest_agents_md() and export it</name>
  <files>tools/harness_config/loader.py, tools/harness_config/__init__.py</files>
  <read_first>
    tools/harness_config/loader.py (full — read this session, see interfaces above)
    tools/harness_config/__init__.py (full — see interfaces above)
    tools/contract_graph/ownership.py (full — DO NOT MODIFY this file)
    .planning/phases/48-convention-profiles/48-RESEARCH.md (Code Examples, Architecture Patterns, Pitfall 1/2, Q2)
    .planning/phases/48-convention-profiles/48-PATTERNS.md (the `tools/harness_config/loader.py` and `__init__.py` sections — copy the shown shapes)
  </read_first>
  <behavior>
    - `conventions_for("libs/python/normalize/x.py")` (real tree, no injected cfg/facts) returns a
      dict whose `agents_md` field equals `"libs/python/AGENTS.md"`, `package` equals
      `logparser-normalize`, `dir` equals `"libs/python"`, `is_default` is `False`.
    - `conventions_for("tools/some_module/y.py")` (real tree, no enclosing non-root package)
      returns `agents_md == "AGENTS.md"`, `is_default is True`.
    - Passing `cfg={"languages": [...]}` and `facts={"packages": [...]}` explicitly bypasses all
      filesystem/config reads for the `test`/`format`/`bash_scope`/`language`/`package`/`dir`/
      `is_default` fields (only the `agents_md` walk still touches the real filesystem).
    - A package whose `language` has no matching row in `cfg["languages"]` returns
      `test`/`format`/`bash_scope` all `None`, `language` unchanged, and does NOT raise.
  </behavior>
  <action>
    In `tools/harness_config/loader.py`: add a module-level import `from tools.contract_graph
    import owning_package` next to the existing `import tomllib` / `from pathlib import Path`
    block (confirm no cycle first: `ownership.py` imports nothing from `tools.harness_config`, so
    a module-level import here is safe — unlike `effective_packages`'s lazy import of
    `package_facts`, which exists to avoid the REVERSE direction). Add a private helper
    `_nearest_agents_md(dir_: str) -> str | None` immediately before `conventions_for`: resolve
    `_REPO_ROOT / dir_`, then walk that path together with its `.parents`, at each candidate
    checking `(candidate / "AGENTS.md").is_file()` and returning the POSIX-relative string
    (`candidate.relative_to(_REPO_ROOT)`) of the first hit; the walk must never inspect anything
    above `_REPO_ROOT` (stop once a candidate is `_REPO_ROOT` itself, after checking it) and must
    return `None` if nothing is found — this is the V5 path-containment note from RESEARCH.md,
    not optional hardening. Add `conventions_for(path: str, cfg: dict | None = None, facts: dict |
    None = None) -> dict` after `effective_packages` (same file), following the identical
    optional-cfg convention (`if cfg is None: cfg = load_project()`): call `pkgs =
    effective_packages(cfg, facts)`; build `dir_pkgs = [p for p in pkgs if "dir" in p]` (the
    required adapter filter — Pitfall 1/Q2, do NOT add this filter to `ownership.py`); call
    `owner_id = owning_package(dir_pkgs, path)`; resolve `owner = next(p for p in dir_pkgs if
    p["id"] == owner_id)`; look up `lang = next((l for l in languages(cfg) if l["id"] ==
    owner.get("language")), None)` — never raise when no row matches. Return a dict with exactly
    these keys: `package` (`owner["id"]`), `dir` (`owner["dir"]`), `language`
    (`owner.get("language")`, the raw value, NOT the matched row's id — so an absent-from-config
    language is still visible), `test`/`format`/`bash_scope` (from `lang` when found, else `None`
    for all three), `agents_md` (`_nearest_agents_md(owner["dir"])`), `is_default`
    (`owner["dir"] == "."`). Update the module docstring's opening paragraph to add one sentence
    naming MONO-05/MONO-06 alongside the existing D-03 reference — do not remove or restructure
    the existing docstring content. In `tools/harness_config/__init__.py`, insert
    `"conventions_for"` into `__all__` in alphabetical order (between `"components"` and
    `"effective_packages"`); make no other edit — `__getattr__`'s generic dispatch already covers
    the new name.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from tools.harness_config import conventions_for; p =
      conventions_for('libs/python/normalize/x.py'); assert p['agents_md'] ==
      'libs/python/AGENTS.md', p; assert p['package'] == 'logparser-normalize', p; assert
      p['is_default'] is False, p"` exits 0
    - `uv run python -c "from tools.harness_config import conventions_for; p =
      conventions_for('tools/some_module/y.py'); assert p['agents_md'] == 'AGENTS.md', p; assert
      p['is_default'] is True, p"` exits 0
    - `grep -c "def conventions_for" tools/harness_config/loader.py` returns `1`
    - `grep -c "def _nearest_agents_md" tools/harness_config/loader.py` returns `1`
    - `grep -c "conventions_for" tools/harness_config/__init__.py` returns at least `1`
    - `grep -n "from tools.contract_graph import owning_package" tools/harness_config/loader.py`
      returns exactly 1 line
    - `git diff --stat -- tools/contract_graph/ownership.py` is EMPTY (the module stays untouched)
    - `uv run python -c "import tools.harness_config"` exits 0 (no import-cycle regression)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from tools.harness_config import conventions_for; p = conventions_for('libs/python/normalize/x.py'); assert p['agents_md']=='libs/python/AGENTS.md', p"</automated>
  </verify>
  <done>`conventions_for` is importable from `tools.harness_config`, correctly resolves the real
  `libs/python` vs root split, degrades (never raises) on a missing language row, has zero import
  cycle, and `tools/contract_graph/ownership.py` is byte-unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests proving MONO-05 nearest-wins/default and MONO-06's falsifiable form</name>
  <files>tools/harness_config/tests/test_conventions_for.py</files>
  <read_first>
    tools/harness_config/tests/test_effective_packages.py (full — the idiom to copy exactly: synthetic cfg/facts, no monkeypatch, no temp file, domain-neutral ids)
    .planning/phases/48-convention-profiles/48-RESEARCH.md (Q3 — the verbatim falsifiable test; Q4 — the real nested-pair rationale; Pitfall 2 — why commands must NOT be asserted equal/different on the real pair)
    .planning/phases/48-convention-profiles/48-PATTERNS.md (`tools/harness_config/tests/test_conventions_for.py (new)` section — the 5 test bodies to copy)
    tools/contract_graph/tests/test_ownership.py (GEN-04 domain-neutral id convention: "a"/"b"/"root")
  </read_first>
  <behavior>
    - MONO-06 strong falsifiable form: two synthetic `cfg`s differing ONLY in one language's
      `test` command, held against the SAME `facts`, produce profiles whose `test` field changes
      accordingly for BOTH a root and a nested package — proving the value is a live read, not a
      copied literal.
    - MONO-05 nearest-wins on the real tree: `libs/python` and a `tools/*` path resolve to
      different `package`/`dir`/`agents_md`, but (Pitfall 2) identical `test`/`format` since both
      are `python` — the test must assert the EQUALITY on commands, not a difference, to avoid
      writing an unfalsifiable/wrong expectation.
    - A path with no enclosing package (single-package synthetic fixture) resolves to that
      package with `is_default is True`.
    - A package whose `language` has no `[[languages]]` row reports `test`/`format` as `None`,
      never raises.
    - A synthetic two-language nested pair (supplementary, per CONTEXT.md "fixtures may
      supplement") proves the commands-DO-differ case that the real tree cannot exercise.
  </behavior>
  <action>
    Create `tools/harness_config/tests/test_conventions_for.py` importing `from
    tools.harness_config import conventions_for`. Write exactly 5 test functions, each passing
    `cfg=`/`facts=` explicitly for the synthetic cases (no `monkeypatch`, no temp-file config —
    mirror `test_effective_packages.py`'s idiom exactly) and domain-neutral ids (`"root"`,
    `"inner"`, never `examples/*` literals — GEN-04):
    `test_editing_language_command_changes_every_affected_profile_with_no_profile_edit` (copy the
    exact scenario from 48-RESEARCH.md Q3 / 48-PATTERNS.md verbatim: `facts` with a `root`
    (`dir="."`) and an `inner` (`dir="libs/x"`) package, both `language="python"`; `cfg_v1` with
    `test="OLD"`, `cfg_v2` with `test="NEW"`; assert both packages report `"OLD"` under `cfg_v1`
    and `"NEW"` under `cfg_v2`); `test_real_nested_pair_libs_python_vs_root_differ_on_package_and_agents_md`
    (no injected cfg/facts — real tree; `conventions_for("libs/python/normalize/x.py")` vs
    `conventions_for("tools/some_module/y.py")`; assert `package`/`dir`/`agents_md` differ as
    specified in `<behavior>`, AND explicitly assert `inner["test"] == outer["test"]` with a
    comment citing Pitfall 2 — both are `python`, so commands are identical by design, this is
    NOT the nested-case proof); `test_path_outside_any_package_returns_explicit_default`
    (single-package synthetic `facts`/`cfg`, assert `is_default is True` and `package == "root"`);
    `test_package_whose_language_is_absent_from_languages_reports_no_commands` (synthetic package
    `language="rust"`, `cfg["languages"]` only has a `python` row; assert `language == "rust"`,
    `test is None`, `format is None`); `test_synthetic_two_language_nested_pair_commands_differ`
    (synthetic root=`python` + nested=`csharp`, two-row `cfg["languages"]`; assert
    `outer["test"] != inner["test"]`). Do not add a conftest or fixture file — each test is
    fully self-contained per the established idiom.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/harness_config/tests/test_conventions_for.py -x -v` → 5 passed
    - `grep -c "examples/" tools/harness_config/tests/test_conventions_for.py` returns `0`
    - `grep -c "monkeypatch" tools/harness_config/tests/test_conventions_for.py` returns `0`
    - Mutation check (run and record the actual output, per this repo's checks-that-cannot-fail
      defect pattern): temporarily edit
      `test_editing_language_command_changes_every_affected_profile_with_no_profile_edit`'s final
      assertion to compare `after_root["test"]` against `"OLD"` instead of `"NEW"` — confirm
      `uv run pytest tools/harness_config/tests/test_conventions_for.py -k
      test_editing_language_command -x` FAILS with an `AssertionError` naming `'NEW' != 'OLD'` (or
      equivalent); revert the edit; re-confirm green.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_config/tests/test_conventions_for.py -x -q</automated>
  </verify>
  <done>5 new tests green; MONO-05's nested-case + explicit-default behavior proven; MONO-06's
  strong falsifiable form proven AND demonstrated to actually fail when mutated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller-supplied `path` → `conventions_for` → `_nearest_agents_md` filesystem walk | An internal caller (agent, `/component`) passes an arbitrary string; the function performs a bounded filesystem existence check derived from it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering / Information Disclosure | `tools/harness_config/loader.py::_nearest_agents_md` | mitigate | Resolve `_REPO_ROOT / dir_` and walk only `candidate`/`candidate.parents` that are `_REPO_ROOT` or a descendant of it; stop after checking `_REPO_ROOT` itself. A crafted `dir_` (e.g. containing `../../..`) cannot cause an `AGENTS.md` check outside the repo tree. Internal-caller-only surface (agents/commands, not untrusted external input), so this is a bounded design note, not a blocking gate — no new CI job is added for it (v2.6 no-growth constraint). |
| T-48-02 | (package legitimacy) | N/A | accept | No external package is installed by this plan (pure stdlib + in-repo modules); the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
`uv run pytest tools/harness_config/tests -x -q` — all tests green, including the 5 new
`test_conventions_for.py` tests and the pre-existing `test_effective_packages.py` /
`test_topology_relationships.py` suites (unmodified, still passing — proves no regression).
</verification>

<success_criteria>
- `conventions_for(path, cfg=None, facts=None)` exists in `tools.harness_config`, reuses
  `owning_package()` unmodified, and never restates a `[[languages]]` command literal.
- MONO-05's nested-case proof (real `libs/python` vs root) and explicit-default behavior are
  green.
- MONO-06's strong falsifiable form is green AND was proven to actually fail under mutation.
- `tools/contract_graph/ownership.py` is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/48-convention-profiles/48-01-SUMMARY.md` when done
</output>
