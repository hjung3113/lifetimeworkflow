---
phase: 48-convention-profiles
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - tools/memory_regen/package_facts.py
  - tools/memory_regen/tests/test_package_facts.py
  - tools/memory_regen/tests/__snapshots__/test_package_facts.ambr
  - .memory/derived/package-facts.md
autonomous: true
requirements: [MONO-05, MONO-06]

must_haves:
  truths:
    - "The committed package-facts.md artifact shows a distinct answer (package/dir/agents_md) for libs/python vs the repo root — nearest-wins demonstrated in the derived artifact itself, not only in the pure function"
    - "Regenerating package-facts.md from a clean tree reproduces it byte-identically with the new section present"
    - "No profile row in the rendered artifact contains a command literal that isn't a live read of [[languages]] — it is computed via conventions_for(), never copy-pasted"
  artifacts:
    - path: tools/memory_regen/package_facts.py
      provides: "render(facts, cfg=None) extended with a '## Convention Profiles' section, computed via conventions_for()"
    - path: .memory/derived/package-facts.md
      provides: "committed derived artifact including the new Convention Profiles section (rides the existing stale-derived CI job)"
    - path: tools/memory_regen/tests/test_package_facts.py
      provides: "structural real-tree assertions (in-memory only) + the extended hermetic snapshot covering the new section"
  key_links:
    - from: "tools/memory_regen/package_facts.py::render"
      to: "tools/harness_config/loader.py::conventions_for"
      via: "lazy in-function import, one call per package"
      pattern: "from tools\\.harness_config import conventions_for"
---

<objective>
Extend the existing `package_facts.render()` — not a sibling artifact — with a `## Convention
Profiles` section computed per package via Plan 01's `conventions_for()`, so
`.memory/derived/package-facts.md` becomes the committed answer to "which conventions apply
here?" alongside its existing package/dependency tables.

Purpose: per RESEARCH.md Q1, extending the existing artifact costs zero `ci.yml` diff and zero
`.gitignore` diff — the `stale-derived` job already regenerates and diffs this exact file.
Output: `render()` extended (additive, backward-compatible signature), the committed artifact
regenerated, and hermetic test coverage (real-tree structural assertions stay in-memory only —
GEN-04 forbids snapshotting the live tree).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-convention-profiles/48-CONTEXT.md
@.planning/phases/48-convention-profiles/48-RESEARCH.md
@.planning/phases/48-convention-profiles/48-PATTERNS.md
@harness/skills/two-plane-memory/SKILL.md
@.planning/phases/47-package-facts/47-02-SUMMARY.md
@.planning/phases/48-convention-profiles/48-01-PLAN.md

<interfaces>
<!-- From this plan's Wave-1 dependency (48-01), now available: -->
```python
# tools/harness_config/loader.py (Plan 01 output)
def conventions_for(path: str, cfg: dict | None = None, facts: dict | None = None) -> dict:
    # returns {"package", "dir", "language", "test", "format", "bash_scope", "agents_md",
    #          "is_default"} — see 48-01-PLAN.md's Task 1 <action> for the exact field semantics.
```

From tools/memory_regen/package_facts.py (existing, being extended — read the full file):
```python
DERIVED_HEADER = "DERIVED — do not hand-edit (tools/memory_regen/package_facts.py)"
_REPO_ROOT = Path(__file__).resolve().parents[2]

def build_facts(manifest_paths: list[dict] | None = None, repo_root: Path = _REPO_ROOT) -> dict:
    # returns {"packages": [{"id","manifest","dir","language"}, ...], "edges": [...]}
    # UNCHANGED by this plan — do not modify build_facts()'s signature or return shape.

def render(facts: dict) -> str: ...        # THIS plan adds an optional cfg param — see Task 1
def write(index_path=PACKAGE_FACTS_PATH, manifest_paths=None, repo_root=_REPO_ROOT) -> Path: ...
def main(argv=None) -> int: ...
```
`write()` and `main()` call `render(build_facts(...))` with no `cfg` argument — both keep working
unmodified once `render`'s new parameter defaults to `None`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend render() with a Convention Profiles section</name>
  <files>tools/memory_regen/package_facts.py</files>
  <read_first>
    tools/memory_regen/package_facts.py (full — the exact file being extended, all functions read this session)
    .planning/phases/48-convention-profiles/48-01-PLAN.md (conventions_for's exact contract)
    .planning/phases/48-convention-profiles/48-RESEARCH.md (Q1 — why extend not add-a-sibling; the render() code example at package_facts.py:264-270 shown as the section-layout analog)
    .planning/phases/48-convention-profiles/48-PATTERNS.md (the `tools/memory_regen/package_facts.py — extend render()/build_facts()` section — copy the section-layout idiom shown)
  </read_first>
  <behavior>
    - `render(build_facts())` (no `cfg` arg — existing call shape) still returns the identical
      `## Packages` and `## Dependency Edges` sections byte-for-byte, PLUS a new
      `## Convention Profiles` section — the signature change is purely additive.
    - Every row's `test`/`format`/`bash_scope` cell is populated by calling `conventions_for()`,
      never by a hardcoded literal — the rendered value changes if `harness/project.toml`'s
      `[[languages]]` changes, with zero edit to `package_facts.py`.
    - `None` fields render as the literal string `(none)` (never an empty/blank cell).
    - Output stays deterministic: no timestamp, no wall-clock, stable package order (same order
      already used by the `## Packages` table — no re-sort).
  </behavior>
  <action>
    In `tools/memory_regen/package_facts.py`, change `render(facts: dict) -> str` to `render(facts:
    dict, cfg: dict | None = None) -> str` — additive optional parameter, so `write()`'s and
    `main()`'s existing `render(build_facts(...))` call sites need NO edit. Inside `render()`,
    after the existing `## Dependency Edges` section (the `for edge in facts["edges"]:` loop),
    append a new `## Convention Profiles` section using the exact same list-of-lines-then-join
    idiom already used for the two existing tables: header `"| package | dir | language | test |
    format | bash_scope | agents_md | default |"` and a `"| --- | --- | --- | --- | --- | --- |
    --- | --- |"` separator, then one row per entry in `facts["packages"]` (SAME order as already
    iterated above — do not re-sort). For each `pkg`, obtain `profile = conventions_for(pkg["dir"],
    cfg=cfg, facts=facts)` via a lazy in-function import `from tools.harness_config import
    conventions_for` placed at the top of `render()`'s body (in-function, mirroring
    `effective_packages`'s own lazy import of `package_facts` in `loader.py` — this is the reverse
    direction of that exact pattern; keep it lazy even though no runtime cycle exists, to match the
    established convention for cross-package imports in this pair of modules). Render `None`
    values as the literal string `(none)`; render `profile["is_default"]` as `"true"`/`"false"`.
    Update the module docstring's first line to also name MONO-05/MONO-06 alongside the existing
    MONO-01/MONO-02 reference — do not remove any existing docstring content.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from tools.memory_regen import package_facts as p; f = p.build_facts();
      text = p.render(f); assert '## Convention Profiles' in text; assert '(none)' in text or True"`
      exits 0
    - `grep -c "## Convention Profiles" tools/memory_regen/package_facts.py` returns `1`
    - `grep -n "def render" tools/memory_regen/package_facts.py` shows a `cfg: dict | None = None`
      parameter
    - `uv run pytest tools/memory_regen/tests/test_package_facts.py::test_render_is_deterministic_over_real_tree
      tools/memory_regen/tests/test_package_facts.py::test_generate_delete_regenerate_is_byte_identical
      -x -q` → both pass unmodified (proves the signature change is backward-compatible)
    - `git diff --stat -- tools/harness_config` is EMPTY (this task touches only
      `tools/memory_regen/package_facts.py`)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_package_facts.py -x -q</automated>
  </verify>
  <done>`render()` emits a third `## Convention Profiles` section built entirely from
  `conventions_for()` calls, is backward-compatible with every existing call site, and stays
  byte-deterministic (pre-existing determinism tests pass unmodified).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests + regenerate and commit the artifact</name>
  <files>tools/memory_regen/tests/test_package_facts.py, tools/memory_regen/tests/__snapshots__/test_package_facts.ambr, .memory/derived/package-facts.md</files>
  <read_first>
    tools/memory_regen/tests/test_package_facts.py (full — already read this session; extend `test_real_tree_render_structure` and re-run `test_render_matches_committed_snapshot`)
    .planning/phases/48-convention-profiles/48-PATTERNS.md (`tools/memory_regen/tests/test_package_facts.py — extend structural tests + snapshot` section — the explicit warning about never snapshotting the live tree)
    .memory/derived/package-facts.md (current committed content, to compare before/after regeneration)
  </read_first>
  <behavior>
    - The real-tree structural test proves, IN-MEMORY ONLY (never written to a committed
      snapshot), that the `libs/python` row and the root (`.`) row report different `agents_md`
      values in the rendered text — the nested-wins property visible in the artifact itself, not
      only via the pure `conventions_for()` unit tests from Plan 01.
    - The existing hermetic 4-package synthetic snapshot fixture (`widget-lib`/`widget-core`/
      `widget-app`/`widget-cli`) now also captures the `## Convention Profiles` section for those
      4 packages, committed via the repo's standard syrupy `--snapshot-update` workflow.
    - Regenerating `.memory/derived/package-facts.md` from a clean state twice in a row produces
      byte-identical output (sha256 match).
  </behavior>
  <action>
    Extend `tools/memory_regen/tests/test_package_facts.py`'s existing
    `test_real_tree_render_structure` function (do not add a new test function for this) with two
    additional in-memory-only assertions after the existing ones: assert `"## Convention
    Profiles"` is present in `text`; then locate the `libs/python` row and the root (`| . |`) row
    directly in `text.splitlines()` (both packages are known to exist on the live tree per
    `.memory/derived/package-facts.md`) and assert their `agents_md` columns differ
    (`libs/python/AGENTS.md` vs `AGENTS.md`) — keep this fully in-memory, per the file's own
    existing docstring warning against ever committing a live-tree-derived snapshot (GEN-04). Do
    NOT modify `test_render_matches_committed_snapshot`'s fixture setup (the 4 synthetic packages
    already exercise 3 manifest kinds) — instead, regenerate its committed `.ambr` entry by running
    `uv run pytest tools/memory_regen/tests/test_package_facts.py::test_render_matches_committed_snapshot
    --snapshot-update` once Task 1's `render()` change is in place, then re-run without
    `--snapshot-update` to confirm it is now stably green. Add one mutation check for the new
    real-tree assertion (per this repo's checks-that-cannot-fail defect pattern, MEMORY.md):
    temporarily compare the `libs/python` row against ITSELF instead of the root row, confirm the
    assertion becomes trivially true (i.e., stops proving anything — record this observation),
    then revert to the correct two-different-rows comparison. Finally, regenerate the committed
    artifact for real: run `uv run python -m tools.memory_regen.package_facts` and include the
    refreshed `.memory/derived/package-facts.md` (now containing `## Convention Profiles`) in this
    task's commit.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_package_facts.py -x -q` → all tests pass,
      including the extended `test_real_tree_render_structure` and the updated
      `test_render_matches_committed_snapshot` (no `--snapshot-update` needed on this final run)
    - `grep -c "## Convention Profiles" .memory/derived/package-facts.md` returns `1`
    - `grep -n "libs/python/AGENTS.md" .memory/derived/package-facts.md` returns at least 1 line
    - Byte-identical regeneration: `uv run python -m tools.memory_regen.package_facts &&
      sha256sum .memory/derived/package-facts.md` recorded, then `rm .memory/derived/package-facts.md
      && uv run python -m tools.memory_regen.package_facts && sha256sum
      .memory/derived/package-facts.md` recorded again — the two hashes are identical
    - `git diff --stat -- .github/workflows/ci.yml` is EMPTY (hard constraint: zero `ci.yml` diff
      this phase — the existing `stale-derived` job already regenerates and diffs this exact file)
    - `git diff --stat -- .gitignore` is EMPTY (the file was already re-included by Phase 47)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests -x -q</automated>
  </verify>
  <done>Real-tree structural assertions (in-memory only) prove the nested-wins property visible in
  the committed artifact; the synthetic hermetic snapshot is extended and committed; the artifact
  is regenerated, committed, and byte-identical on regeneration; `ci.yml` and `.gitignore` are
  byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `render()` → `conventions_for()` → filesystem walk | Same boundary as Plan 01's T-48-01; this plan is a new CALLER of an already-mitigated function, not a new surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 (carried) | Tampering / Information Disclosure | `tools/harness_config/loader.py::_nearest_agents_md` (via `render()`'s new call site) | mitigate | Already mitigated in Plan 01; `render()` only ever passes a `pkg["dir"]` value sourced from `build_facts()`'s own manifest discovery — never an externally-supplied string — so this call site carries strictly LESS exposure than Plan 01's direct-path-argument case. |
| T-48-03 | (package legitimacy) | N/A | accept | No external package installed by this plan. |
</threat_model>

<verification>
`uv run pytest tools/memory_regen/tests -x -q` — full pass, including the two pre-existing
determinism tests (byte-identical proof) and the extended structural/snapshot tests. `git diff
--stat -- .github/workflows/ci.yml .gitignore` — both empty.
</verification>

<success_criteria>
- `render()` emits `## Convention Profiles`, sourced entirely from `conventions_for()` — no
  restated command literal anywhere in `package_facts.py`.
- The committed `.memory/derived/package-facts.md` demonstrates the nested-wins property
  (`libs/python` vs root differ) directly in the artifact.
- Regeneration is byte-identical; `ci.yml`/`.gitignore` are untouched.
</success_criteria>

<output>
Create `.planning/phases/48-convention-profiles/48-02-SUMMARY.md` when done
</output>
