---
phase: 49-contract-impact
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - harness/commands/impact.md
  - harness/agents/orchestrator.md
  - tools/harness_lint/tests/test_commands.py
  - .opencode/command/impact.md
  - .opencode/agent/orchestrator.md
  - .claude/commands/impact.md
  - .claude/agents/orchestrator.md
  - tools/harness_emit/emit-manifest.json
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
autonomous: true
requirements: [MONO-08, MONO-09]

must_haves:
  truths:
    - "Running /impact <contract> from a developer session invokes tools.contract_graph.impact.report and prints its result"
    - "The contract-change route's Repository evidence block names /impact and no longer contains the inline uv run python -c one-liner"
    - "All four routes (small-change, bugfix, feature, contract-change) still carry exactly one Repository evidence subsection each, in the same five-subsection order"
    - "Command count is 19 and EXPECTED_COMMAND_NAMES includes impact — both guards updated in this same change"
    - "The SessionStart injector's assembled output is byte-identical with and without this phase (tools/memory_regen/inject.py is untouched)"
    - "No CI job or hook references /impact"
    - ".opencode/ and .claude/ carry impact.md and the updated orchestrator.md only through the emitter, never hand-edited, and a second re-emit produces zero further diff"
  artifacts:
    - path: "harness/commands/impact.md"
      provides: "thin command macro: description + agent:orchestrator + subtask:true frontmatter, one shell line invoking tools.contract_graph.impact"
      contains: "tools.contract_graph.impact"
    - path: "harness/agents/orchestrator.md"
      provides: "contract-change route's Repository evidence block naming /impact, five-subsection structure preserved on all four routes"
      contains: "/impact"
    - path: "tools/harness_lint/tests/test_commands.py"
      provides: "test_command_count_is_stable() == 19; EXPECTED_COMMAND_NAMES includes impact"
      contains: "\"impact\""
  key_links:
    - from: "harness/commands/impact.md"
      to: "tools/contract_graph/impact.py"
      via: "uv run python -m tools.contract_graph.impact \"$ARGUMENTS\""
      pattern: "tools\\.contract_graph\\.impact"
    - from: "harness/agents/orchestrator.md"
      to: "harness/commands/impact.md"
      via: "contract-change route's Repository evidence block names /impact"
      pattern: "/impact"
    - from: "harness/commands/impact.md"
      to: ".opencode/command/impact.md"
      via: "tools.harness_emit re-emit (glob-discovered, no emitter code change)"
      pattern: "generated by tools.harness_emit"
---

<objective>
Wire `tools/contract_graph/impact.py` (Plan 01) into the harness surface: a new thin
`harness/commands/impact.md`, the `contract-change` route in `harness/agents/orchestrator.md`
rewritten to name `/impact` instead of its inline one-liner, the Phase-48 command-surface guards
bumped from 18 to 19 in the same change, and a byte-clean emit round-trip to both runtime trees.
Also structurally proves MONO-09: nothing touches `tools/memory_regen/inject.py`, no CI job
references `/impact`, and the injector's assembled output stays byte-identical.

Purpose: this is the one sanctioned surface addition in v2.6 — the phase's whole payoff is turning
five inline calls into one named, on-demand command with no growth anywhere else (no gate, no CI
job, no SessionStart injection).

Output: `harness/commands/impact.md`, an edited `harness/agents/orchestrator.md`, bumped
`test_commands.py` constants, and both runtime trees (`.opencode/`, `.claude/`) re-emitted
byte-clean via the existing, unmodified `tools.harness_emit`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-contract-impact/49-CONTEXT.md
@.planning/phases/49-contract-impact/49-RESEARCH.md
@.planning/phases/49-contract-impact/49-VALIDATION.md
@.planning/phases/49-contract-impact/49-01-SUMMARY.md

<interfaces>
<!-- The reporter this command wires to (Plan 01's output, already landed by wave 1). -->
From tools/contract_graph/impact.py:
```python
def report(contract_path: str, cfg: dict | None = None, graph: dict | None = None, facts: dict | None = None) -> dict: ...
def main(argv: list[str] | None = None) -> int: ...  # CLI entry: prints json.dumps(report(...), sort_keys=True, indent=2)
```

<!-- Thin command shape to mirror, verbatim structure. -->
From harness/commands/component.md (frontmatter + shell-line shape):
```markdown
---
description: >-
  Use when <trigger>. Invoke to <what it computes>.
agent: orchestrator
subtask: true
---

# /command-name — <one-line>

<short prose>

!`uv run python -m tools.some_module ...`
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create harness/commands/impact.md and bump the command-surface guards</name>
  <files>harness/commands/impact.md, tools/harness_lint/tests/test_commands.py</files>
  <read_first>
    harness/commands/component.md (thin-command shape, full file)
    harness/commands/lint.md (thin-command shape with a `uv run python -m ...` line, full file)
    tools/harness_lint/tests/test_commands.py (EXPECTED_COMMAND_NAMES frozenset lines 52-73, test_command_count_is_stable lines 92-99, test_command_names_are_stable lines 102-115)
    tools/contract_graph/impact.py (Plan 01's output — the module this command invokes)
  </read_first>
  <action>
    Write `harness/commands/impact.md` with frontmatter `description` (a routing paragraph
    containing "Use"/"when" per `test_description_is_routing_signal`), `agent: orchestrator`
    (mirrors `component.md` — `/impact` is invoked from within the orchestrator-owned
    `contract-change` route), and `subtask: true`. Body: a one-line H1, a short paragraph stating
    it composes `direct`/`reverse`/`transitive` and defines no traversal of its own, and one shell
    line `!`uv run python -m tools.contract_graph.impact "$ARGUMENTS"`` (mirrors `lint.md`'s
    `uv run python -m tools.polyglot_lint.lint` idiom). State plainly that an unresolved contract
    returns a clean refusal (non-zero exit), never an empty "nothing affected" success. In the SAME
    task, edit `tools/harness_lint/tests/test_commands.py`: change `test_command_count_is_stable`'s
    `== 18` to `== 19`, and add `"impact"` to `EXPECTED_COMMAND_NAMES` (insert alphabetically
    between `"flow"` and `"lint"`).
  </action>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint/tests/test_commands.py -v` — all tests pass, including
    `test_command_count_is_stable` and `test_command_names_are_stable`, and the new
    `test_description_is_routing_signal`/`test_agent_field_well_formed`/
    `test_subtask_is_boolean_when_present` parametrized cases for `impact`.
    Mutation-prove the count guard: temporarily revert `== 19` to `== 18`, confirm
    `test_command_count_is_stable` FAILS with `assert 19 == 18`, then restore `== 19` — record the
    failing line in the plan SUMMARY.
    `grep -c "python -c" harness/commands/impact.md` returns `0` (uses `python -m`, not an inline
    `-c` script, keeping the command file thin per CONTEXT.md).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py -q</automated>
  </verify>
  <done>`harness/commands/impact.md` exists with valid, well-formed frontmatter and a thin macro body; `test_commands.py` pins the count at 19 and includes "impact" in EXPECTED_COMMAND_NAMES; both changes committed together.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the contract-change route's Repository evidence block</name>
  <files>harness/agents/orchestrator.md</files>
  <read_first>
    harness/agents/orchestrator.md (full file, especially the contract-change route lines 246-302 and the sibling routes' identical five-subsection structure lines 116-244 for the pattern to preserve)
  </read_first>
  <action>
    In the `### Route: contract-change` section's **Repository evidence** subsection
    (`harness/agents/orchestrator.md:274-296`), delete the inline `uv run python -c "from
    tools.harness_config import components, effective_relationships; from tools.contract_graph
    import compile_graph, direct, reverse, transitive; ..."` bash block and its surrounding
    explanatory sentences, and the trailing "A single-command form of this block is planned..."
    sentence (this phase discharges that forward-reference specifically for `contract-change`).
    Replace with prose that: (a) keeps the existing "*The question:*" sentence unchanged; (b) adds
    "*The command that answers it:*" followed by a `/impact <path/to/contract.schema.json>` bash
    block; (c) explains that `/impact` composes the existing `direct`/`reverse`/`transitive`
    queries plus the Phase-47 package facts, reports affected contracts, affected packages, and the
    owning engineer per side, and that an unresolved contract is a clean refusal, never an empty
    "nothing affected" success. Do NOT touch the other three routes' (`small-change`, `bugfix`,
    `feature`) Repository evidence blocks or their own "A single-command form of this block is
    planned" sentences — they remain out of this phase's scope. Do NOT change the section headers,
    their order, or any other route's content.
  </action>
  <acceptance_criteria>
    `grep -c "Repository evidence" harness/agents/orchestrator.md` returns `4` (unchanged — one per
    route).
    `grep -c "uv run python -c.*direct(graph, node); print(reverse" harness/agents/orchestrator.md`
    returns `0` (the contract-change one-liner is gone).
    `grep -c "/impact" harness/agents/orchestrator.md` returns `>= 1`.
    `grep -c "A single-command form of this block is planned" harness/agents/orchestrator.md`
    returns `3` (down from 4 — only `contract-change`'s sentence is removed; the other three
    routes' identical sentences survive unchanged).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Repository evidence" harness/agents/orchestrator.md | grep -qx 4 && grep -c "/impact" harness/agents/orchestrator.md</automated>
  </verify>
  <done>The contract-change route names /impact in its Repository evidence block, the inline one-liner is gone, and all four routes keep their five-subsection structure with the other three routes byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Emit round-trip + no-growth proofs (injector, CI)</name>
  <files>.opencode/command/impact.md, .opencode/agent/orchestrator.md, .claude/commands/impact.md, .claude/agents/orchestrator.md, tools/harness_emit/emit-manifest.json, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>
  <read_first>
    tools/harness_emit/generate.py (glob-driven iter_commands/iter_agents — no emitter code change needed for a new command file or an edited agent file, lines 1-50)
    .planning/phases/48-convention-profiles/48-03-SUMMARY.md (the exact re-emit + idempotency-check + snapshot-update sequence Phase 48 followed for an analogous command edit)
    tools/memory_regen/inject.py (confirm zero coupling to commands/contract_graph before asserting the diff must stay empty)
  </read_first>
  <action>
    Run `uv run python -m tools.harness_emit` to project the new `harness/commands/impact.md` and
    the edited `harness/agents/orchestrator.md` into both runtime trees
    (`.opencode/command/impact.md`, `.opencode/agent/orchestrator.md`,
    `.claude/commands/impact.md`, `.claude/agents/orchestrator.md`), updating
    `tools/harness_emit/emit-manifest.json` accordingly. Run the emitter a second time immediately
    and confirm zero further diff (idempotent — mirrors `48-03-SUMMARY.md`'s verified check).
    Regenerate the emit-determinism snapshot (`tools/harness_emit/tests/__snapshots__/
    test_emit_determinism.ambr`) via the project's existing `--snapshot-update` mechanism if the
    projected `component.md`-adjacent fixture set changed shape (only if the test suite reports a
    snapshot mismatch — do not regenerate speculatively). Then run the two MONO-09 no-growth
    proofs: `git diff --stat -- tools/memory_regen/inject.py` must be empty, and
    `uv run pytest tools/memory_regen/tests/test_inject_determinism.py -q` must stay green
    unmodified; `grep -rn "impact" .github/workflows/ci.yml` must return zero hits, and `git diff
    --stat -- .github/workflows/ci.yml` must be empty from this phase's base commit.
  </action>
  <acceptance_criteria>
    `uv run python -m tools.harness_emit && git status --porcelain .opencode .claude` shows the
    expected new/modified files; running it a second time immediately yields `git status
    --porcelain .opencode .claude tools/harness_emit/emit-manifest.json` empty (idempotent).
    `uv run pytest tools/harness_emit tools/memory_regen -q` passes.
    `grep -rn "impact" .github/workflows/ci.yml` returns no output (exit 1 from grep, zero hits).
    `git diff --stat -- .github/workflows/ci.yml tools/memory_regen/inject.py` is empty.
    `uv run pytest -q` (full suite) passes with no regressions.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>Both runtime trees carry impact.md and the updated orchestrator.md solely through the emitter (idempotent re-emit proven), emit-manifest.json and the determinism snapshot are current, ci.yml and inject.py are byte-unchanged, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `$ARGUMENTS` (developer/agent input) → `impact.md`'s shell line → `impact.py main()` | Same risk class as the already-shipped `component.md`/`lint.md` commands, which already pass `$ARGUMENTS`/tracked-file paths into a `python -c`/`python -m` invocation — not a new surface this phase introduces. |
| Emitted `.opencode/`/`.claude/` trees | Machine-written only; `_confine()` in `tools/harness_emit/generate.py` already refuses any emitted path escaping the harness lane — untouched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Tampering | `harness/commands/impact.md`'s `$ARGUMENTS` interpolation | accept | Mirrors `component.md`'s existing `$ARGUMENTS`-in-shell-line pattern verbatim — same risk class as two already-shipped commands, not a new one; widening the shell surface beyond that pattern is explicitly out of scope. |
| T-49-05 | Elevation of Privilege (scope creep) | SessionStart injector / CI | mitigate | Structural proofs (Task 3): empty diff on `tools/memory_regen/inject.py`, zero `grep` hits for `impact` in `.github/workflows/ci.yml`, empty diff on `ci.yml` — MONO-09's "on demand only" constraint is enforced by these checks, not by convention. |
</threat_model>

<verification>
`uv run pytest -q` (full suite) green. `uv run python -m tools.harness_emit` run twice produces zero
diff the second time. `grep -c "Repository evidence" harness/agents/orchestrator.md` == 4. `grep -rn
"impact" .github/workflows/ci.yml` empty. `git diff --stat -- tools/memory_regen/inject.py
.github/workflows/ci.yml` empty.
</verification>

<success_criteria>
`/impact <contract>` exists as command 19 of 19, invoking Plan 01's `report()`. The `contract-change`
route names `/impact` in its Repository evidence block with all five subsections intact across all
four routes. Both runtime trees are re-emitted byte-clean and idempotently. Nothing is injected into
SessionStart and no CI job or hook references `/impact` — both proven structurally, not asserted.
</success_criteria>

<output>
Create `.planning/phases/49-contract-impact/49-02-SUMMARY.md` when done
</output>
