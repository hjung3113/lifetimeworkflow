# v2.4 — the human queue

Six artifacts across four phases need a human's own hands. Every one is drafted to exact bytes with
landing instructions; none was routed around. The agent session verified everything it could verify
without landing them.

**Why none of these were self-served.** `contract_guard` denies agent writes to `contracts/**`,
`docs/adr/**`, `golden/**` and `docs/glossary.md`; `ledger_guard` denies ledger writes and honours
neither `GOLDEN_APPROVE_HUMAN` nor `HARNESS_DEV_BYPASS`. Both denials were verified live this
session, not assumed. `HARNESS_DEV_BYPASS` would have opened the contract path — and using it is
precisely what produced **RAT-4**, the provenance debt v2.4 exists to discharge. Using it here would
have made the milestone self-refuting.

---

## 1. Phase 30 — the deny-domain contract pair *(blocks phases 30–33)*

**This is the critical path.** Landing it unblocks plans 30-02 and 30-03, which are already written,
and lets phases 31–33 proceed.

| Draft | Destination |
|---|---|
| `.planning/phases/30-deny-domain-registry-v2-4-a/drafts/deny-domains.schema.json` | `contracts/harness/security/deny-domains.schema.json` |
| `.planning/phases/30-deny-domain-registry-v2-4-a/drafts/deny-domains.json` | `contracts/harness/security/deny-domains.json` |

Instructions: `.planning/phases/30-deny-domain-registry-v2-4-a/drafts/LANDING.md` — the write must be
**atomic** with the `contract_hash` `DATA_CONTRACT_PATHS` edit, the hash rebaseline, `docs_sync`,
`contracts_index` and both syrupy snapshots, or `drift` / `stale-derived` goes red on a half-landed
state.

**Independently verified before drafting** (`30-ORCHESTRATOR-VERIFICATION.md`): schema is valid
Draft 2020-12; instance validates with zero errors; all three declared glob sets match the live
constants element-wise and in order; and the `bypasses` values were **driven, not read** —
`ledger_guard` denies under `{}`, `GOLDEN_APPROVE_HUMAN`, `HARNESS_DEV_BYPASS` and both together.

---

## 2–4. Three docs-review ledger rows *(`docs-guard` is RED until these land)*

`uv run python -m tools.docs_guard` currently exits 1:

```
[STALE_REQUIRED] task-control-cli-howto      <- fails the gate
[STALE_ADVISORY] lifecycle-eval-shadow-metrics <- warns only
```

| Draft | From | Disposition |
|---|---|---|
| `.planning/phases/34-*/drafts/docs-review-ledger-task-control-cli-howto.md` | phase 34's ruff autofix moved a bound source digest | `reviewed-no-change` |
| `.planning/phases/36-*/drafts/ledger-rows.md` | phase 36's discipline wiring | see draft |
| `.planning/phases/37-*/drafts/ledger-rows.md` | phase 37's capability routing | see draft |

Each agent reviewed its own target document and reached a disposition, then **refused to write the
row**. That refusal is the point: an agent landing a ledger row for its own change is the self-green
attack ADR-0010 §3b exists to block, and a self-blessed row is byte-identical to an honest one —
only your commit distinguishes them. Read each target document yourself before landing; the drafts
record the reasoning, not your review.

---

## What is NOT waiting on you

Phases **34, 35, 36, 37** are complete and verified. Full suite **1683 passed / 8 snapshots**;
`ruff_baseline`, `contract_drift`, `workspace_check` and the emit round-trip are all clean.

## What is blocked behind item 1

Phases **30 (plans 02–04), 31, 32, 33**. Phase 31 is the posture ADR — itself a `docs/adr/` write,
so it will return here as a second human item once drafted. Phase 32's acceptance is written against
whatever posture 31 ratifies, deliberately, so that it cannot expand without bound.
