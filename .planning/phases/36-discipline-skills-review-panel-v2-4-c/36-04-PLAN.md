---
phase: 36-discipline-skills-review-panel-v2-4-c
plan: 04
type: execute
wave: 4
depends_on: [36-01, 36-02, 36-03]

files_modified:
  - docs/how-to/task-lifecycle.md
  - docs/explanation/task-lifecycle-shadow-metrics.md
  - .planning/phases/36-discipline-skills-review-panel-v2-4-c/drafts/*
autonomous: true
requirements: [LANE-01, LANE-02]

must_haves:
  truths:
    - "The human-facing lifecycle how-to describes the discipline gate that now exists, so a reader following it does not hit an undocumented refusal"
    - "Every ledger row this phase owes is DRAFTED with exact intended bytes and landing instructions — never authored into docs/.docs-review-ledger.toml, which ADR-0010 §3b reserves to a human"
    - "The pre-existing [STALE_REQUIRED] task-control-cli-howto red is reported, not repaired and not worked around"
    - "The phase's full gate fan-in is run and its ACTUAL numbers recorded, not claimed"
  artifacts:
    - path: "docs/how-to/task-lifecycle.md"
      provides: "the discipline gate documented at the point a reader meets it"
      contains: "discipline"
    - path: ".planning/phases/36-discipline-skills-review-panel-v2-4-c/drafts/ledger-rows.md"
      provides: "the exact ledger bytes a human must land, with the command that recomputes the digests"
      contains: "docs/.docs-review-ledger.toml"
---

<objective>
Close the phase: document the new refusal where a human meets it, draft (never land) the human-gated
review-ledger rows this change owes, and run the full gate fan-in for real.

Purpose: a gate nobody documented is a trap, and a ledger row an agent lands is a self-approved
binding. Both failure modes are already recorded in this repo's history.
</objective>

<execution_context>
Atomic commits, `uv run` everywhere, no model identifier in any artifact.
</execution_context>

<context>
@.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-CONTEXT.md
@docs/how-to/task-lifecycle.md
@docs/doc-dependencies.toml
@docs/adr/0010-human-docs-review-obligation-model.md
@harness/skills/docs-upkeep/SKILL.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bounded edits to the two bound targets</name>
  <files>docs/how-to/task-lifecycle.md, docs/explanation/task-lifecycle-shadow-metrics.md</files>
  <behavior>
    - `docs/how-to/task-lifecycle.md` gains the discipline step: what a lane owes, where the record
      goes, the `/discipline` invocation, and the exact refusal text a reader will see. Bounded —
      the edit covers what THIS phase changed, nothing else. It does NOT attempt to repair whatever
      made the binding stale in phase 34.
    - `docs/explanation/task-lifecycle-shadow-metrics.md` is reviewed against the `router.py` change.
      If the change does not affect any of its five metric definitions, the honest disposition is
      `reviewed-no-change` and the document is NOT edited to manufacture a diff.
  </behavior>
  <action>Read each target fully before editing. Neither file is constitution plane; both are
  agent-writable human prose. Do not touch `docs/adr/**`, `docs/glossary.md`, `contracts/**` or
  `golden/**`.</action>
  <verify>
    <automated>uv run python -m tools.docs_guard; echo "exit=$?"</automated>
  </verify>
  <done>Both targets reflect reality; the guard's output is captured verbatim for the summary,
  including the known-red row.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Draft the human-gated ledger rows</name>
  <files>.planning/phases/36-discipline-skills-review-panel-v2-4-c/drafts/ledger-rows.md</files>
  <behavior>
    For each binding this phase moved, the draft carries: the binding id, the disposition, the exact
    TOML row built from the SHIPPED `tools/docs_guard/ledger.py::_ROW_KEYS` (read it; do not restate
    a key list from memory), the command that recomputes the digests, and a one-paragraph statement
    of what the human is being asked to attest.
  </behavior>
  <action>Write the draft. Do NOT create or modify `docs/.docs-review-ledger.toml` under any
  circumstance, and do not set `GOLDEN_APPROVE_HUMAN` or `HARNESS_DEV_BYPASS`. State plainly in the
  draft that the digests must be recomputed at landing time because any later commit moves them.</action>
  <verify>
    <automated>test ! -f docs/.docs-review-ledger.toml || git diff --quiet -- docs/.docs-review-ledger.toml</automated>
  </verify>
  <done>The draft is complete enough to land without re-deriving anything except the digests, and
  the ledger is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Gate fan-in with actual numbers</name>
  <files>—</files>
  <behavior>Every gate the lead named is run and its real output recorded.</behavior>
  <action>Run, in order, capturing actual output: `uv run pytest -q`;
  `uv run python -m tools.ruff_baseline`; `uv run python -m tools.contract_drift`;
  `uv run python -m tools.harness_emit && git status --porcelain`;
  `uv run pytest tools/harness_lint -q`; `uv run python -m tools.docs_guard`.
  Record the numbers in the SUMMARY — test count delta from the 1543 baseline, each exit code, and
  the porcelain result. Report the docs_guard red as known and not-mine, with its exact finding.

  Also record the proposed `.planning/STATE.md` delta as TEXT in the summary. Do not edit
  `.planning/STATE.md`.</action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>Real numbers, not claims; every deviation named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> review ledger | the ledger is the greenness authority and is human-only |
| agent -> phase state record | STATE.md is not this agent's to move |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-15 | Elevation of Privilege | an agent lands a ledger disposition | mitigate | drafted only; asserted by a check that the ledger is unmodified |
| T-36-16 | Repudiation | a green claimed without running the gate | mitigate | actual captured output in the summary |
| T-36-17 | Tampering | a doc edited to manufacture a review diff | mitigate | `reviewed-no-change` is an available and preferred honest disposition |
| T-36-SC | Tampering | dependency install | accept | none added |
</threat_model>

<verification>
- The six fan-in commands above, with captured output.
- `docs/.docs-review-ledger.toml` unmodified.
- `.planning/STATE.md` unmodified.
</verification>

<success_criteria>
The new refusal is documented where a reader meets it, every human-gated item is drafted rather than
landed, and the phase's claims are backed by captured gate output.
</success_criteria>

<output>
Create `.planning/phases/36-discipline-skills-review-panel-v2-4-c/36-04-SUMMARY.md` when done.
</output>
