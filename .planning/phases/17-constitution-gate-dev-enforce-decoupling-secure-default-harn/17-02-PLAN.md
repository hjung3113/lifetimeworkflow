---
phase: 17-constitution-gate-dev-enforce-decoupling-secure-default-harn
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/17-02-ADR-0007.draft.md
  - docs/adr/0007-constitution-gate-dev-enforce-decoupling.md
  - docs/adr/README.md
autonomous: false
requirements: [DEVBYPASS-ADR]

must_haves:
  truths:
    - "ADR-0007 records the posture change: a secure-default HARNESS_DEV_BYPASS dev opt-out (default=enforce; blank/whitespace=no bypass), DISTINCT from GOLDEN_APPROVE_HUMAN, byte-hygiene never waived, honored by both gates via one shared dev_bypassed(); append-only next number after 0006"
    - "ADR-0007 states explicitly that CODEOWNERS at PR merge remains the real, non-bypassable gate, and records the accepted agent-self-enabling risk (an agent could write the flag into gitignored .claude/settings.local.json — same trust model as dropping the hook)"
    - "An agent Write to docs/adr/ WITHOUT the token is DENIED by contract-guard — the deny is the expected, verified behavior; ADR-0007 lands ONLY via the human-ratified path (human supplies GOLDEN_APPROVE_HUMAN or writes the file), CODEOWNERS ratifying at merge"
    - "ADR-0007 LINKS to (never restates) the design spec, 17-CONTEXT, the shipped code (plan 17-01) and ADR-0004; it is append-only (no existing 0001–0006 row removed)"
  artifacts:
    - path: ".planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/17-02-ADR-0007.draft.md"
      provides: "Durable pre-gate scratch draft (Block A = ADR body, Block B = README index row) that survives across sessions for the human landing"
      contains: "## Links"
    - path: "docs/adr/0007-constitution-gate-dev-enforce-decoupling.md"
      provides: "The MADR record of the dev/enforce-decoupling posture (mirrors ADR-0004/0006 shape) — landed by the human"
      contains: "Status:"
    - path: "docs/adr/README.md"
      provides: "Append-only index row for 0007 (0001–0006 intact)"
      contains: "0007"
  key_links:
    - from: "docs/adr/0007-*.md"
      to: "the shipped HARNESS_DEV_BYPASS code + the design spec + ADR-0004"
      via: "Links section (link, not restate)"
      pattern: "HARNESS_DEV_BYPASS"
    - from: "docs/adr/README.md"
      to: "docs/adr/0007-*.md"
      via: "index row"
      pattern: "0007"
---

<objective>
Author ADR-0007 recording the dev/enforce-decoupling posture that plan 17-01 shipped, and land it via
the human-ratified constitution path. The agent DRAFTS the ADR into a durable scratch file and
REPRODUCES the contract-guard deny on a tokenless Write to `docs/adr/` as EXPECTED behavior; a HUMAN
supplies `GOLDEN_APPROVE_HUMAN` (or writes the file) and CODEOWNERS ratifies at merge.

Purpose: ratify the posture change (secure-default dev opt-out, distinct from the ratification token,
byte-hygiene never waived, CODEOWNERS still the real gate) as an append-only MADR — mirroring exactly
how ADR-0006 landed in plan 12-03.

Output: the durable scratch draft `17-02-ADR-0007.draft.md` (agent-side) and, after the human landing,
`docs/adr/0007-constitution-gate-dev-enforce-decoupling.md` (Status: accepted) + a new `docs/adr/README.md`
row. CRITICAL: `docs/adr/**` is a constitution-plane path; an agent Write is CORRECTLY DENIED by
contract-guard without a human token. This plan is `autonomous: false`. NEVER fabricate the token,
NEVER edit the contract-guard hook, NEVER route to a non-gated path, NEVER use the very HARNESS_DEV_BYPASS
flag this phase adds to self-land (that would defeat the human-ratification point).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/17-CONTEXT.md
@.planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/17-RESEARCH.md
@docs/superpowers/specs/2026-07-14-contract-guard-dev-bypass-design.md
@docs/adr/README.md
@docs/adr/0004-constitution-hook-fail-open-posture.md
@docs/adr/0006-process-memory-channel-and-provenance-reframe.md
@.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md
@harness/commands/adr.md

<interfaces>
<!-- Deny mechanics (verified in 17-RESEARCH.md, tools/hooks/contract_guard.py:42-98):
     CONSTITUTION_GLOBS includes "docs/adr/**". A Write/Edit there is DENIED unless GOLDEN_APPROVE_HUMAN
     is a non-empty, non-blank string. The deny is the ratified design (ADR-0004). ADR-0002/0003/0005/0006
     all landed via the human token gate — precedent. -->
<!-- Next number = highest docs/adr/NNNN-*.md + 1. Latest is 0006, so next = 0007. -->
<!-- Draft-to-scratch precedent: .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md —
     header warning block ("durable scratch, NOT the ADR"), then "## Block A — docs/adr/0007-...md" with
     copy-from-here rulers, then "## Block B — docs/adr/README.md" index row. MIRROR this file's shape. -->
<!-- MADR sections mirror ADR-0006/0005: title H1 ("# 7. ..."), the
     "*MADR 4.x · plane: constitution (human-owned, immutable, append-only)*" line, Status/Date/Deciders/
     Supersedes/Superseded-by/Complements, Context and Problem Statement, Decision Drivers, Considered
     Options, Decision Outcome, Consequences, Links. Append-only: add a README row, remove none. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft ADR-0007 content + the index row into the durable scratch file (agent-side, pre-gate)</name>
  <files>.planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/17-02-ADR-0007.draft.md (durable scratch — the ONLY file Task 1 writes; NOT under docs/adr/ so contract-guard does not gate it), docs/adr/0007-constitution-gate-dev-enforce-decoupling.md (final gated path — Task 1 must NOT write it), docs/adr/README.md (final gated path — Task 1 must NOT write it)</files>
  <read_first>
    - 17-CONTEXT.md (LOCKED implementation decisions 1–5 + Risk Summary — the agent-self-enabling accepted risk MUST be recorded).
    - docs/superpowers/specs/2026-07-14-contract-guard-dev-bypass-design.md (the authoritative decision, non-goals, semantics — the ADR records THIS).
    - Format analog: docs/adr/0006-process-memory-channel-and-provenance-reframe.md (full MADR shape incl. header block + `## Links`) and docs/adr/README.md (index table + "Do not remove rows").
    - Draft-file analog: .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md (mirror its scratch shape: warning block + Block A rulers + Block B row).
    - harness/commands/adr.md (append-only convention: next number, never edit an existing ADR).
    - Plan 17-01-SUMMARY.md (the shipped code this ADR records — link, do not restate).
  </read_first>
  <action>
    Confirm next number = 0007 (`ls docs/adr/ | grep -E '^[0-9]{4}-' | sort | tail -1` shows `0006-*`). Draft
    the ADR body in the ADR-0006 MADR shape: H1 "# 7. Constitution-Gate Dev/Enforce Decoupling
    (Secure-Default HARNESS_DEV_BYPASS)"; the "*MADR 4.x · plane: constitution (human-owned, immutable,
    append-only)*" line; Status: accepted; Date: 2026-07-15; Deciders (operator-delegated, from the approved
    brainstorm spec); Supersedes: —; Superseded by: —; Complements: ADR-0004 (constitution-hook posture).
    Content records: (a) the secure-default dev opt-out `HARNESS_DEV_BYPASS` — default=enforce, unset/blank/
    whitespace ⇒ no bypass; (b) it is DISTINCT from `GOLDEN_APPROVE_HUMAN` so a dev-bypassed write is never
    "human ratified" (distinct dev-only stderr note); (c) byte-hygiene §4.3-4.6 is NEVER waived; (d) one shared
    `dev_bypassed()` honored by both `contract_guard` and `commit_gate` (DRIFT-ONLY there); (e) the flag lives
    ONLY in gitignored `.claude/settings.local.json`, never in committed settings.json/CI/opencode; (f) the
    EXPLICIT statement that CODEOWNERS at PR merge remains the real, non-bypassable gate; and (g) the ACCEPTED
    agent-self-enabling risk (an agent could write the flag into `.claude/settings.local.json` — same trust model
    as an agent editing settings to drop the hook; the real defense is the PR diff + CODEOWNERS). Decision Drivers
    / Considered Options / Decision Outcome / Consequences per MADR. `## Links` LINKS to (does NOT restate) the
    design spec, 17-CONTEXT, plan 17-01 (the shipped code), and ADR-0004. Draft the `docs/adr/README.md` index
    row `| [0007](0007-constitution-gate-dev-enforce-decoupling.md) | Constitution-Gate Dev/Enforce Decoupling | accepted |` —
    remove no existing row. Write the full ADR body (Block A) AND the README row (Block B) into the durable
    scratch file `.planning/phases/17-.../17-02-ADR-0007.draft.md`, mirroring 12-03-ADR-0006.draft.md's shape
    (warning header + copy-from-here rulers). Do NOT write to `docs/adr/` in this task; if the executor's own
    Write to `docs/adr/` is denied by contract-guard, that DENY is expected — do not fabricate the token, edit
    the hook, or route to a non-gated path. The scratch draft IS the staged content for the human to land.
  </action>
  <acceptance_criteria>
    - Next-number correctness: `ls docs/adr/ | grep -E '^[0-9]{4}-' | sort | tail -1 | cut -c1-4` = `0006` (so 0007 is correct).
    - The durable scratch draft exists with MADR shape: a Status line, "Context and Problem Statement", "Decision Outcome", and "## Links".
    - The draft records the non-negotiables: it mentions HARNESS_DEV_BYPASS, GOLDEN_APPROVE_HUMAN (as distinct), byte-hygiene never waived, and CODEOWNERS as the real gate.
    - No literal `GOLDEN_APPROVE_HUMAN` value is hardcoded anywhere (`! grep -rn 'GOLDEN_APPROVE_HUMAN=[A-Za-z0-9]' docs/ .planning/phases/17-*`).
  </acceptance_criteria>
  <verify>
    <automated>D=.planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/17-02-ADR-0007.draft.md; test -f "$D" && grep -q "## Links" "$D" && grep -q "Status" "$D" && grep -q "HARNESS_DEV_BYPASS" "$D" && grep -q "GOLDEN_APPROVE_HUMAN" "$D" && grep -qi "byte-hygiene\|§4.3" "$D" && grep -q "CODEOWNERS" "$D" && test "$(ls docs/adr/ | grep -E '^[0-9]{4}-' | sort | tail -1 | cut -c1-4)" = "0006" && ! grep -rn "GOLDEN_APPROVE_HUMAN=[A-Za-z0-9]" docs/ .planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/ 2>/dev/null</automated>
  </verify>
  <done>ADR-0007 body + README index row are drafted into the durable scratch file in the correct MADR shape, recording all LOCKED decisions and the accepted self-enabling risk, LINK-not-restate Links; staged for the human-gated landing; no token fabricated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Land ADR-0007 via the human-ratified constitution path (deny is expected)</name>
  <files>docs/adr/0007-constitution-gate-dev-enforce-decoupling.md, docs/adr/README.md</files>
  <read_first>
    - .planning/phases/17-.../17-02-ADR-0007.draft.md (the durable staged ADR body + index row from Task 1 — the content the human lands; survives across sessions).
    - 17-CONTEXT.md (Success Criteria + Risk Summary) + the design spec (the deny is the intended dogfood).
    - tools/hooks/contract_guard.py:42-98 (deny mechanics) — READ ONLY, never edit.
  </read_first>
  <action>
    This is a BLOCKING human checkpoint — the agent does NOT land the write. The agent's role is to (a)
    reproduce/confirm the contract-guard DENY on a tokenless agent Write to `docs/adr/` (expected behavior — do
    NOT bypass it), then PAUSE and hand off to the human, who supplies `GOLDEN_APPROVE_HUMAN` in their own shell
    (or writes the file directly) so the drafted ADR-0007 + the README row land. The agent MUST NOT set the
    token, MUST NOT enable/use the new `HARNESS_DEV_BYPASS` flag to self-land, MUST NOT edit
    `tools/hooks/contract_guard.py`, MUST NOT use `--no-verify`, and MUST NOT write to a non-gated path. After
    the human lands the file, the agent re-verifies the acceptance criteria (file present, Status accepted, index
    row added with 0001–0006 intact, Links reference the shipped code + design spec, hook unmodified) and
    confirms CODEOWNERS routes the PR to the human owner at merge.
  </action>
  <what-built>
    The agent drafted ADR-0007 (Task 1). An agent Write to `docs/adr/0007-*.md` is CORRECTLY DENIED by
    contract-guard without a human-supplied `GOLDEN_APPROVE_HUMAN` token — this deny is the ratified design
    (ADR-0004), not a bug. The ADR must land via the human ratification act.
  </what-built>
  <how-to-verify>
    1. (Behavioral proof the gate holds) Confirm an agent Write to `docs/adr/` WITHOUT the token is denied —
       reproduce via `uv run pytest tools/hooks -k contract_guard -q` (the deny path is asserted there), OR
       observe the deny JSON when the executor attempted the Write in Task 1.
    2. (Human ratification — choose one) Copy the staged content from
       `.planning/phases/17-.../17-02-ADR-0007.draft.md`, then either: export a golden-approve-style token in
       your own shell (`export GOLDEN_APPROVE_HUMAN="<your ratification value>"`) and let the write proceed;
       OR write/commit `docs/adr/0007-constitution-gate-dev-enforce-decoupling.md` + the `docs/adr/README.md`
       index row yourself. The agent MUST NOT set the token or use HARNESS_DEV_BYPASS.
    3. Confirm the landed file: `test -f docs/adr/0007-constitution-gate-dev-enforce-decoupling.md` &&
       `grep -qi 'accepted' docs/adr/0007-*.md` && `grep -q '0007' docs/adr/README.md` &&
       `grep -q 'HARNESS_DEV_BYPASS' docs/adr/0007-*.md` (Links/records the shipped posture, not a restatement).
    4. At merge, CODEOWNERS routes the PR to the human owner — the merge-time ratification backstop.
  </how-to-verify>
  <acceptance_criteria>
    - Deny reproduced: an agent/tokenless Write to `docs/adr/` is denied (contract-guard suite green OR the deny JSON observed) — the gate is intact and was NOT bypassed (and HARNESS_DEV_BYPASS was NOT used to self-land).
    - `test -f docs/adr/0007-constitution-gate-dev-enforce-decoupling.md` exits 0.
    - `grep -qi 'accepted' docs/adr/0007-constitution-gate-dev-enforce-decoupling.md` exits 0 (Status: accepted).
    - `grep -q '0007' docs/adr/README.md` exits 0 AND pre-existing 0001–0006 rows all still present (`for n in 0001 0002 0003 0004 0005 0006; do grep -q "$n" docs/adr/README.md || exit 1; done`).
    - `grep -q 'HARNESS_DEV_BYPASS' docs/adr/0007-*.md` exits 0 (records the shipped posture).
    - `git diff --quiet tools/hooks/contract_guard.py` exits 0 (the hook was NOT edited to bypass).
    - No literal `GOLDEN_APPROVE_HUMAN` value is committed anywhere in the repo.
  </acceptance_criteria>
  <verify>
    <human-check>Human supplies GOLDEN_APPROVE_HUMAN (or writes the file); then: test -f docs/adr/0007-constitution-gate-dev-enforce-decoupling.md && grep -qi accepted docs/adr/0007-*.md && grep -q 0007 docs/adr/README.md && grep -q HARNESS_DEV_BYPASS docs/adr/0007-*.md && for n in 0001 0002 0003 0004 0005 0006; do grep -q "$n" docs/adr/README.md || exit 1; done && git diff --quiet tools/hooks/contract_guard.py</human-check>
  </verify>
  <resume-signal>Type "approved" once ADR-0007 has landed via the token/human write with 0001–0006 intact and CODEOWNERS set to ratify, or describe issues.</resume-signal>
  <done>ADR-0007 landed via the human-ratified path (agent Write denied as expected → human token/write), Status accepted, index row added with 0001–0006 intact, records HARNESS_DEV_BYPASS + CODEOWNERS-as-real-gate, contract-guard hook unmodified, flag not used to self-land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → `docs/adr/**` (constitution plane) | Untrusted write attempt crosses the contract-guard gate. Deny unless a human supplies a non-empty `GOLDEN_APPROVE_HUMAN`. CODEOWNERS is the merge-time backstop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Elevation of Privilege / Repudiation | agent self-ratifies the ADR (fabricates `GOLDEN_APPROVE_HUMAN`, uses the new HARNESS_DEV_BYPASS flag, edits contract-guard, `--no-verify`, or writes to a non-gated path) | mitigate (BLOCKING) | Task 2 is `checkpoint:human-verify` gate=blocking-human. The deny is asserted as EXPECTED; only a human supplies the token or writes the file; `git diff --quiet tools/hooks/contract_guard.py` proves the hook is unmodified; the flag must NOT be used to self-land; CODEOWNERS ratifies at merge; ADR is append-only (tamper-evident). |
| T-17-05 | Tampering | an existing ADR (0001–0006) is edited/removed instead of appending 0007 | mitigate | Acceptance asserts all 0001–0006 index rows still present and only 0007 is added (append-only, `/adr` convention). |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs — one markdown ADR + one index row; existing pytest suite only. |
</threat_model>

<verification>
- Behavioral: agent/tokenless Write to `docs/adr/` is denied (contract-guard suite green) — gate intact, not bypassed, flag not used to self-land.
- `docs/adr/0007-*.md` exists, Status: accepted, MADR-shaped, records HARNESS_DEV_BYPASS + the CODEOWNERS-as-real-gate statement + the accepted self-enabling risk.
- `docs/adr/README.md` has a 0007 row; 0001–0006 rows all still present.
- `git diff --quiet tools/hooks/contract_guard.py`; no committed `GOLDEN_APPROVE_HUMAN` value.
</verification>

<success_criteria>
- ADR-0007 records the dev/enforce-decoupling posture (append-only, next number after 0006), lands via the
  human-ratified path (agent Write correctly denied → human token/write + CODEOWNERS), and LINKS to (never
  restates) the design spec, 17-CONTEXT, plan 17-01, and ADR-0004.
</success_criteria>

<output>
Create `.planning/phases/17-constitution-gate-dev-enforce-decoupling-secure-default-harn/17-02-SUMMARY.md` when done.
Record: the deny was reproduced (gate intact, flag not used to self-land), the human ratification method used
(token vs direct write), and the append-only confirmation (0001–0006 intact, 0007 added).
</output>
