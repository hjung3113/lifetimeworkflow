---
phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
plan: 08
type: execute
wave: 5
depends_on: ["28-06", "28-07"]
files_modified:
  - docs/adr/0010-human-docs-review-obligation-model.md
  - docs/adr/README.md
autonomous: false
requirements: [DOCSUP-01, DOCSUP-02, DOCSUP-03, DOCSUP-04, DOCSUP-05]

must_haves:
  truths:
    - "D-15: ONE ADR records the phase's architecturally load-bearing decisions — the disposition-coherence rule (D-04) and the uncovered ratchet (D-06) — as a single ratified unit, following the 25-05 pattern exactly: the agent authors `Status: proposed`, a HUMAN flips it to `accepted`"
    - "The ADR states D-16/A7 explicitly: the registry is TOML in `docs/`, so `/contract-check` step 1 (schema paired with a sibling `.yaml`/`.yml`/`.json`) does not cover it and `tools.docs_guard.registry` is its ONLY validator — so nobody later assumes CI validates it twice"
    - "The ADR records D-03's deliberate divergence from `approval.py:57-63` (interleaved path + per-file digest, not raw concatenation) with the reason, so a future reviewer does not 'fix' it back toward the precedent"
    - "The ADR records D-08's fail-closed-for-required / warn-for-advisory posture on unverifiable git history and the distinct `unverified-disposition` reason string"
    - "The ADR records the D-01 plane split and WHY it is forced rather than preferential: `contract_guard.py:44` denies agent writes under `contracts/**`, and DOCSUP-07 requires `/adopt` to PROPOSE registry entries"
    - "The phase-closing fan-in runs the FULL gate set ONCE, after every plan has landed — this is the only place the full suite runs in this phase, because plans 28-01/02, 28-03/04, and 28-06/07 each ran concurrently in their waves"
    - "The seeded ledger dispositions from plan 28-07 are HUMAN-ratified in the same checkpoint — an agent may author them (they are plain reviewed config by design), but it may not be the authority that declares them reviewed"
    - "No model identifier appears in the ADR, the commit messages, or any artifact this phase produced"
  artifacts:
    - path: "docs/adr/0010-human-docs-review-obligation-model.md"
      provides: "the MADR record for the human-docs review obligation model — plane split, digest algorithm, disposition coherence, uncovered ratchet, exit-code contract, and the registry's single-validator fact"
      contains: "unverified-disposition"
    - path: "docs/adr/README.md"
      provides: "index row for ADR-0010"
      contains: "0010"
  key_links:
    - from: "docs/adr/0010-human-docs-review-obligation-model.md"
      to: "tools/docs_guard/ledger.py"
      via: "the ADR is the ratified rationale for the coherence rule the module implements"
      pattern: "disposition-incoherent"
    - from: "docs/adr/0010-human-docs-review-obligation-model.md"
      to: "docs/adr/README.md"
      via: "index row, added in the same commit"
      pattern: "0010"
---

<objective>
Close Phase 28: author the phase's one ADR, run the full gate fan-in once, and take the single
blocking human ratification that covers both the ADR and the seeded ledger dispositions.

Purpose: D-04's coherence rule and D-06's ratchet are architecturally load-bearing decisions of the
same weight as ADR-0009's graph model — Phase 29's `/docs-update` and `/adopt` both bind to them.
They belong in an append-only ADR, ratified by a human, not self-declared by the agent that
implemented them. This plan is also the only place the full suite runs, because every prior wave had
two plans in flight concurrently.

Output: ADR-0010 accepted, the full gate set green, and the phase closed.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-CONTEXT.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-RESEARCH.md
@docs/adr/0009-contract-relationship-graph-model.md
@docs/adr/README.md
@.planning/phases/25-contract-relationship-graph-conductor-routing/25-05-PLAN.md

<interfaces>
<!-- Verified against source in this session. -->

Existing ADRs: `docs/adr/0001-*.md` .. `0009-*.md`. The next number is **0010**.

`docs/adr/0009-contract-relationship-graph-model.md` is the live shape to mirror: MADR form with a
`- **Status:** accepted` line plus `Supersedes:` / `Superseded by:` / `Complements:` fields, then
Context / Decision Drivers / Considered Options / Decision Outcome / Consequences.

25-05's ratification pattern (the one to follow exactly): Task 1 creates the ADR with
`Status: proposed`; Task N is a `checkpoint:human-verify` with `gate="blocking"` where the human
flips `proposed -> accepted`. `docs/adr/**` is in `CONSTITUTION_GLOBS` (`contract_guard.py:44`) and
CODEOWNERS-gated, so the draft lands through the same approved constitution path prior ADRs used —
never a fabricated `GOLDEN_APPROVE_HUMAN`, never `HARNESS_DEV_BYPASS` self-landing.

25-05's human-check gate form:

    <human-check>Human ratifies; then: grep -qi accepted docs/adr/0010-*.md && grep -q 0010 docs/adr/README.md</human-check>

Full gate set for this phase's fan-in, from ROADMAP Phase 28 SC-4 and Phase 29 SC-4:
`uv run pytest` (full) · `contract-drift` · `golden` · workspace drift · `stale-derived` local
equivalent · GEN-04 twin · the new `docs-guard` · `emit-drift` · the model-identifier lint ·
injector budget · `git diff --check`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0010 with Status: proposed</name>
  <files>docs/adr/0010-human-docs-review-obligation-model.md, docs/adr/README.md</files>
  <read_first>
    docs/adr/0009-contract-relationship-graph-model.md (full — the MADR shape, the status/field
      header, and the section order to mirror),
    docs/adr/README.md (the index-row convention and the append-only/supersede-not-edit statement),
    .planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-CONTEXT.md (D-01..D-18 —
      the decisions this ADR ratifies),
    the SUMMARY files from plans 28-01 through 28-07 (what actually landed, which may differ in
      detail from what was planned — the ADR records what SHIPPED)
  </read_first>
  <action>
    Create `docs/adr/0010-human-docs-review-obligation-model.md` with `- **Status:** proposed`,
    mirroring ADR-0009's MADR shape field-for-field. `Complements:` ADR-0009 (the graph model this
    phase's impact ids consume) and ADR-0008 where relevant; `Supersedes:` nothing.

    Record the phase's decisions as ONE ratified unit — not a partial or single-topic record. The
    Decision Outcome must cover, each with its reason:

    1. **The plane split (D-01).** Registry DATA at `docs/doc-dependencies.toml` (plain reviewed
       config); registry SHAPE at `contracts/harness/docs/doc-dependencies.schema.json`
       (constitution plane, hash-gated, one ratification). State that this is FORCED, not
       preferential: `contract_guard.py:44` denies agent writes under `contracts/**`, and DOCSUP-07
       requires `/adopt` to PROPOSE registry entries — a registry under `contracts/` makes the
       downstream requirement unimplementable without handing agents the human token.
    2. **The registry's single validator (D-16/A7).** `/contract-check` step 1 pairs a schema with a
       sibling `.yaml`/`.yml`/`.json` instance; the registry is `.toml` and lives in `docs/`, so CI
       silently skips it. `tools.docs_guard.registry` is its ONLY validator. State this plainly so
       nobody later assumes double validation.
    3. **The digest algorithm (D-03).** Interleaved POSIX path + per-file hex digest over the sorted
       resolved set, deliberately NOT the raw-byte concatenation at `approval.py:57-63`. Give the
       reason — that concatenation is safe only for a fixed file tuple, and on a variable
       selector-expanded set it makes a byte moving between files and the addition of an empty file
       invisible — so a future reviewer does not "fix" it back toward the precedent. Also record
       that NO §4.3-4.6 normalization runs before hashing, and why: the digest must agree with what
       a human sees in `git diff`.
    4. **Disposition coherence (D-04)** — the anti-rubber-stamp mechanism, both halves:
       `reviewed-no-change` is content-bound and history-free; `updated` additionally requires a
       target-digest delta versus the previous COMMITTED ledger. State the attack it defeats
       (paste the live digest, claim `updated`, leave the document untouched) and why digests alone
       cannot detect it.
    5. **The uncovered ratchet (D-06).** Stored as `[coverage] uncovered_max`; the guard NEVER
       writes it; raising it is a human edit. A gate that can lower its own threshold is
       self-blessing.
    6. **The unverifiable-history posture (D-08).** Fail-closed for `required`, warn for `advisory`,
       with the DISTINCT `unverified-disposition` reason string — because an indistinguishable
       failure teaches the wrong fix.
    7. **The five states and the exit-code contract (D-05).** First-match-wins with `BROKEN` before
       any staleness check; exit 0 / 1 / 3 with 3 reserved for registry-invalid because the operator
       action differs. Note that Phase 29's `/docs-update` binds to these, so they are pinned.
    8. **The accepted-ADR vocabulary (D-09).** `docs/adr/**` targets are restricted to
       `REVIEWED_STILL_CURRENT` / `SUPERSEDING_ADR_REQUIRED`; the latter is an open obligation that
       can never make a binding green; the report never suggests an in-place ADR edit.
    9. **Contract/golden stay leading (D-13).** The guard reads `run_gate()` for SUPPRESSION only
       and never restates another gate's findings.
    10. **The derived queue's placement and ignore status (D-10).** Generator under
        `tools/memory_regen/` because `test_derived_freshness.py:32` restricts the `/refresh-memory`
        surface; the queue stays gitignored and out of `stale-derived`.

    In Consequences, record honestly what this model does NOT claim: it detects review OBLIGATIONS,
    not semantic accuracy — a `FRESH` binding means the reviewed digests still match, never that the
    document is correct. Also record the carried-forward items: the D-14 instance-overlay seam is
    built but unused; DOCSUP-06/07 (the `/docs-update` loop and `/adopt` seeding) are Phase 29; and
    `tools/hooks/secret_scan.py:44-47`'s hardcoded pattern list remains carried forward from
    26.2 / 27.1 / 27.2.

    Add the index row to `docs/adr/README.md`. Do not edit any existing ADR — append-only /
    supersede-not-edit.

    No model identifier anywhere in the file. Leave `Status: proposed` — Task 3 is the ratification
    authority.
  </action>
  <verify>
    <automated>grep -q 'Status:\*\* proposed' docs/adr/0010-human-docs-review-obligation-model.md && grep -q 0010 docs/adr/README.md</automated>
  </verify>
  <acceptance_criteria>
    The ADR exists with `Status: proposed`, mirrors ADR-0009's MADR shape, and its Decision Outcome
    covers all ten items above as one unit. `docs/adr/README.md` carries the 0010 row. `git status
    --porcelain docs/adr` shows only the new file and the README. No existing ADR was modified.
    `grep -rniE 'claude|gpt|opus|sonnet|anthropic' docs/adr/0010-*.md` returns no model identifier.
  </acceptance_criteria>
  <done>ADR-0010 drafts the full Phase 28 obligation model in MADR form with `Status: proposed`, landed through the standard human-ratified constitution path, and the index row is added.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-closing fan-in — run the full gate set once</name>
  <files></files>
  <read_first>
    .github/workflows/ci.yml (the authoritative command for each gate — mirror them exactly rather
      than inventing local equivalents),
    the SUMMARY files from plans 28-01 through 28-07
  </read_first>
  <action>
    Every prior wave in this phase deliberately narrowed its test selection because two plans were
    in flight concurrently. This is the single fan-in. Run each gate and record its verbatim result:

      uv run pytest -q
      uv run python -m tools.contract_drift.drift
      uv run python -m tools.contract_drift.drift --contracts-dir examples/log-parser/contracts --baseline examples/log-parser/contracts/.hashes/manifest.json
      uv run python -m tools.contract_drift.drift --workspace
      uv run python -m tools.docs_guard
      uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude
      uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index
      git add -A -- docs/reference .memory/derived/contracts-index.md && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md
      uv run pytest tools/harness_lint -q
      uv run pytest tools/golden_runner -q
      git diff --check

    Also confirm, explicitly:
      - `git check-ignore -q .memory/derived/docs-staleness.md` succeeds — the queue is still
        ignored and did not sneak into the commit.
      - `grep -n 'docs-staleness' .github/workflows/ci.yml` is empty — the queue never joined
        `stale-derived`.
      - The model-identifier lint (`tools/harness_lint` covers the emitted-config surface; run it
        plus an explicit `grep -rniE 'claude|gpt|opus|sonnet' docs/doc-dependencies.toml
        docs/.docs-review-ledger.toml contracts/harness/docs/ docs/adr/0010-*.md`) finds nothing.
      - `git log --format=%B <phase range>` contains no model identifier.
      - The SIX named injector tests from plan 28-06 are green in the full run.

    If any gate is red, STOP and fix it before the checkpoint. Do not present a red fan-in for
    ratification. Record every command's output verbatim in the SUMMARY under `## Phase fan-in`.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run python -m tools.contract_drift.drift && uv run python -m tools.docs_guard && uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    Every command above exits 0, with output recorded verbatim. `git check-ignore -q
    .memory/derived/docs-staleness.md` succeeds. No model identifier appears in any new artifact or
    in this phase's commit messages. The `stale-derived` local equivalent is clean.
  </acceptance_criteria>
  <done>The full gate set — pytest, root/example/workspace drift, docs-guard, emit round-trip, stale-derived, GEN-04, golden, whitespace, and the model-identifier lint — is green in one run, with output recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human ratification — ADR-0010 and the seeded ledger dispositions</name>
  <files>docs/adr/0010-human-docs-review-obligation-model.md, docs/.docs-review-ledger.toml</files>
  <read_first>
    docs/adr/0010-human-docs-review-obligation-model.md (Task 1's draft),
    docs/doc-dependencies.toml and docs/.docs-review-ledger.toml (plan 28-07's seed),
    Task 2's recorded fan-in output,
    .planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-07-SUMMARY.md (the
      per-binding justifications)
  </read_first>
  <action>
    Present ADR-0010's draft, plan 28-07's seeded registry and ledger with their per-binding
    justifications, and Task 2's fan-in output for blocking human review, then STOP. The agent does
    not self-ratify either item: it does not flip `Status: proposed` to `accepted` and does not
    declare the seeded dispositions reviewed until the human resume-signal arrives. On "ratified",
    flip the status through the same human-authorized constitution write path prior ADRs used —
    never a fabricated `GOLDEN_APPROVE_HUMAN`, never `HARNESS_DEV_BYPASS` self-landing — and re-run
    the two confirmation commands. If the human rejects a binding, remove it from the registry AND
    its ledger row, re-derive `uncovered_max`, and re-run the fan-in before asking again.
  </action>
  <what-built>
    ADR-0010, recording the Phase 28 human-docs review obligation model as one unit — the plane
    split, the digest algorithm and its deliberate divergence, disposition coherence, the uncovered
    ratchet, the unverifiable-history posture, the five states and the 0/1/3 exit contract, the
    accepted-ADR vocabulary, contract/golden leading, and the queue's placement — drafted at
    `docs/adr/0010-human-docs-review-obligation-model.md` with `Status: proposed`. Alongside it, the
    seeded registry and ledger from plan 28-07, whose dispositions an agent authored and only a human
    may declare reviewed.
  </what-built>
  <how-to-verify>
    This is a BLOCKING human checkpoint. The agent does NOT self-ratify either item. Present, in this
    order:

    1. The full text of ADR-0010. Confirm all ten Decision Outcome items are present, that the
       Consequences section states plainly that the model detects review OBLIGATIONS and never
       claims semantic accuracy, and that the carried-forward items are listed.
    2. `docs/doc-dependencies.toml` with plan 28-07's one-sentence justification per binding.
       Confirm each pairing is real — a binding whose sources do not genuinely determine its target
       is noise that trains people to rubber-stamp, which is the failure mode this phase exists to
       prevent. Reject any binding you do not believe.
    3. `docs/.docs-review-ledger.toml`. Confirm: only the permitted keys; every disposition is
       `reviewed-no-change` / `REVIEWED_STILL_CURRENT` (never `updated` on a first-ever ledger); and
       `uncovered_max` equals the exact live count, not a padded one.
    4. Task 2's fan-in output — every gate green.
    5. Confirm the ratification path: the standard human-authorized constitution write for the ADR,
       with NO fabricated `GOLDEN_APPROVE_HUMAN` and NO `HARNESS_DEV_BYPASS` self-landing.

    Then flip `- **Status:** proposed` to `- **Status:** accepted` via that same path, and confirm:

        uv run python -m tools.docs_guard
        uv run pytest -q
  </how-to-verify>
  <verify>
    <human-check>Human ratifies; then: grep -qi 'Status:\*\* accepted' docs/adr/0010-human-docs-review-obligation-model.md && grep -q 0010 docs/adr/README.md && uv run python -m tools.docs_guard</human-check>
    <automated>uv run python -m tools.docs_guard && uv run pytest -q</automated>
  </verify>
  <resume-signal>Reply "ratified" to flip ADR-0010 to `accepted` and accept the seeded dispositions, or describe the correction needed (including any binding you want removed) before ratification.</resume-signal>
  <done>ADR-0010 is `accepted` and human-ratified via the standard constitution path, the seeded ledger dispositions are human-accepted, and the full gate set is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent draft → constitution-plane ADR | `docs/adr/**` is CODEOWNERS-gated and denied to direct agent writes by `contract_guard`. Task 1's output is a DRAFT (`Status: proposed`) only; Task 3 is the ratification authority. |
| Agent-authored ledger dispositions → gate greenness | The ledger is plain reviewed config by design (D-01), so an agent CAN write it. The control is human review, taken here, not a write-side deny. |
| Phase artifacts → the repo's permanent record | Commit messages and committed files must carry no model identifier and no human wall-clock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-48 | Elevation of Privilege (self-ratification) | `docs/adr/0010-*.md` | mitigate | The agent never sets `Status: accepted`; Task 3 is a blocking human checkpoint and the constitution write path is unchanged by this phase. |
| T-28-49 | Repudiation | agent-authored ledger dispositions declared reviewed by the agent that authored them | mitigate | Task 3 ratifies the seeded dispositions explicitly, with per-binding justifications presented and any binding rejectable. "Machines gate, humans ratify." |
| T-28-50 | Spoofing (false assurance) | the ADR overclaiming — reading as if the gate verifies documentation CORRECTNESS | mitigate | The Consequences section states plainly that the model detects review OBLIGATIONS and makes no semantic-accuracy claim; the checkpoint verifies that sentence is present. |
| T-28-51 | Tampering | an existing ADR edited instead of superseded | mitigate | Append-only: only the new file and the README index row appear in `git status --porcelain docs/adr`, asserted in Task 1's acceptance criteria. |
| T-28-52 | Repudiation | a green phase claim resting on narrowed in-flight selections | mitigate | Task 2 runs the FULL gate set once, after every plan has landed, with every command's output recorded verbatim; a red fan-in must not be presented for ratification. |
| T-28-53 | Information Disclosure | a model identifier in a committed artifact or commit message | mitigate | Explicit greps over the new registry, ledger, schema, ADR, and this phase's commit messages in Task 2. |
| T-28-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new package installs across the entire phase; 28-RESEARCH.md records the Package Legitimacy Gate as not applicable. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 (full suite).
- Root, example, and workspace `contract_drift` all exit 0.
- `uv run python -m tools.docs_guard` exits 0.
- `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude` is a clean no-op.
- The `stale-derived` local equivalent is clean; `git check-ignore -q .memory/derived/docs-staleness.md` succeeds.
- `uv run pytest tools/harness_lint tools/golden_runner -q` exits 0.
- `git diff --check` is clean.
- ADR-0010 is `accepted` and indexed.
</verification>

<success_criteria>
- One ADR records the phase's load-bearing decisions as a single human-ratified unit (D-15).
- The seeded ledger dispositions are human-accepted, not agent self-declared.
- The full gate set — including the new `docs-guard` job's commands — is green in one fan-in run.
- No model identifier, wall-clock, or human identity appears in any artifact this phase produced.
</success_criteria>

<output>
Create `.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-08-SUMMARY.md` when
done. It MUST contain:

- a `## Phase fan-in` section with the verbatim output of every command in Task 2;
- a `## Carried-forward residuals` section recording, unchanged and NOT acted on: the D-14
  instance-overlay seam (built, unused); DOCSUP-06/07 (Phase 29); and
  `tools/hooks/secret_scan.py:44-47`'s hardcoded pattern list (carried from 26.2 / 27.1 / 27.2).
</output>
