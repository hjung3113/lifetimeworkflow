---
phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
verified: 2026-07-21T00:00:00Z
status: human_needed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Author `docs/.docs-review-ledger.toml` from the byte-exact proposal in 28-07-SUMMARY.md, by hand, outside an agent session (28-07 Task 2)."
    expected: "`uv run python -m tools.docs_guard` transitions from exit 1 (6 × broken-binding) to exit 0; the `docs-guard` CI job goes green."
    why_human: "A ledger disposition is the greenness authority. ADR-0010 clause 3b forbids any agent from authoring it, and `tools/hooks/ledger_guard.py` denies the write with NO token — not GOLDEN_APPROVE_HUMAN, not HARNESS_DEV_BYPASS. Verified live: `decide()` still denies with both variables set."
  - test: "Review ADR-0010 (`docs/adr/0010-human-docs-review-obligation-model.md`) and flip `- **Status:** proposed` to `accepted`, filling deciders (28-08 Task 3)."
    expected: "ADR-0010 is `accepted`; the `contract-graph-adr-0009`-style ADR bindings and the registry's accepted-ADR policy hold against it."
    why_human: "The ADR landed via HARNESS_DEV_BYPASS and has never been human-ratified. `registry._adr_status` treats a non-accepted ADR as a rejection, so this is a real gate, not a formality."
  - test: "Ratify the eight seeded bindings' dispositions in `docs/doc-dependencies.toml` — confirm each (sources, target) pair is a review obligation you accept, before recording its first [[reviewed]] row."
    expected: "Each seeded binding is a pair the reviewer genuinely intends to be bound; amber-on-landing resolves to FRESH only after the ledger row lands in a COMMIT."
    why_human: "`first_seen-unratified` is a HISTORY test by design: a self-blessed row and an honest first seed row are byte-identical. Only a human committing the row separates them."
  - test: "Read the docs-guard operator output once (`uv run python -m tools.docs_guard`) and confirm the remediation lines are actionable and the BROKEN/STALE_ADVISORY split reads correctly."
    expected: "The operator can tell 'fix the docs' from 'fix the ledger' from 'fix the registry' without reading source."
    why_human: "Message clarity and operator ergonomics are not programmatically checkable."
---

# Phase 28: Human-Docs Registry, Guard, Derived Queue — Verification Report

**Phase Goal:** detect and surface human-doc review obligations precisely, without claiming semantic accuracy and without competing with the derived generators.
**Verified:** 2026-07-21
**Status:** human_needed
**Re-verification:** No — initial verification
**Verified at commit:** `56cbac7` (branch `claude/data-pipeline-harness-8aypct`)

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Registry validation rejects path escape, duplicate id, empty required selector, derived/reference target, accepted-ADR edit policy | ✓ VERIFIED | `tools/docs_guard/registry.py:275-375` implements all five as SEMANTIC passes after the schema pass. `tests/test_registry.py` carries 6 classes / ~26 rejection rows (A escape ×5 incl. a real symlink escape, B duplicate, C empty-required, D **generated by iterating the imported `DERIVED_GLOBS`** — 8 rows incl. `docs/reference/**`, E ADR ×6, F control-char ×6) plus a 6-row `VALID_CASES` negative control that proves no over-refusal. Live: an escaping registry exits **3** with `binding 'a' sources[0] contains a '..' path segment`. |
| 2 | source-only fails; doc+ledger passes; unexplained ledger-only bump fails; `reviewed-no-change` passes only against the exact current digest | ✓ VERIFIED | `ledger.py:333` (any digest mismatch → `stale-digest`, fail for `required`), `_check_updated:478-489` (source moved, target unmoved → `disposition-incoherent` — the paste-the-live-digest control), `_check_content_bound:398-421`. `_ROW_KEYS` at `ledger.py:73` is **still the four DOCSUP-02 keys** — CR-03 added no `binding_digest` column, exactly as briefed. Repoint keying confirmed: `guard.py:335-348` retrieves the previous committed REGISTRY and derives `repointed_ids` from `identity_digest(sources, target)`; `check_coherence` takes it as a **required** argument. |
| 3 | `BROKEN` / `STALE_REQUIRED` fail, `STALE_ADVISORY` warns, uncovered non-regression enforced | ✓ VERIFIED | `guard.py:383-434` first-match-wins with BROKEN ordered first; `ok=False` only for BROKEN/STALE_REQUIRED (`:433`). CR-01 scoping confirmed live: `SUPPRESSIBLE_REASONS = frozenset({REASON_STALE})` (`:148`) **plus** the independent `and not blocking_by_id.get(binding.id)` half of the SUPPRESSED branch (`:408`). WR-01 confirmed: `uncovered_max` is read from the previous COMMITTED ledger (`:483-491`), working-tree value only when there is no history — symmetric with `binding_min` (`ledger.py:509`). |
| 4 | Derived queue regenerates deterministically; injector byte-identity + ~4000-char budget green; `/docs-sync`, `/refresh-memory`, stale-derived semantics unchanged | ✓ VERIFIED | Ran the generator twice: identical md5 `a099a684…`, `git status --porcelain` empty (the queue is gitignored, as designed). `inject.py:196-229`: never-drop tuple is `("agreements","banner","drift","task")` — **not widened**; `("docs", _docs_staleness_pointer(derived_dir))` sits in the droppable region; `derived_dir` is a parameter on `_docs_staleness_pointer` (`:80`). 12 injector tests + 22 stale-derived/docs_sync tests green. `/refresh-memory` chains the generator (`harness/commands/refresh-memory.md:25`). |

**Score:** 4/4 truths verified

### Adversarial Mutation Spot-Checks

Every control the 28-REVIEW caught as bypassable was deleted and the suite re-run. **All four failed loud** — none survived deletion.

| Mutation | Test that reds | Observed failure |
|----------|----------------|------------------|
| CR-01 — `SUPPRESSIBLE_REASONS` widened to include `BLOCKING_REASONS`, AND the `not blocking_by_id` half of the SUPPRESSED branch deleted | `test_self_blessed_binding_is_not_rescued_by_a_drifted_source` | `AssertionError: drift demoted a ratification-authority finding — the self-green escape is open; assert ['note'] == ['fail']` |
| CR-03 — `repointed_ids` forced to always-empty (id-only history test restored) | `test_repointing_a_ratified_binding_is_not_fresh` | `AssertionError: a repointed binding inherited its earlier ratification`, with `'findings': [], 'ok': True` |
| CR-02 — the `Write\|Edit` `ledger_guard` hook group deleted from `merge.py:HARNESS_HOOK_GROUPS` | `test_review_ledger_hook_is_wired_into_the_emitted_pretooluse_set` | `AssertionError: ledger_guard is not wired into the emitted PreToolUse set — ADR-0010's layer 1 is inert` |
| WR-01 — `uncovered_max` read from the working tree | `test_uncovered_max_comes_from_the_committed_ledger` | `AssertionError: the enforced ceiling must be the COMMITTED one; assert 99 == 0` |

Tree restored to `git status --porcelain` empty after each mutation.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| ledger_guard denies the ledger | `ledger_guard.decide("<root>/docs/.docs-review-ledger.toml")` | deny dict returned | ✓ PASS |
| ledger_guard honours NO token | same, with `GOLDEN_APPROVE_HUMAN=1` **and** `HARNESS_DEV_BYPASS=1` | still denies | ✓ PASS |
| the registry stays agent-writable | `decide("<root>/docs/doc-dependencies.toml")` | `None` | ✓ PASS |
| CLI exit 3 for an invalid registry | `uv run python -m tools.docs_guard --registry /tmp/badreg/r.toml` | exit **3**, `registry path escape: … '..' path segment` | ✓ PASS |
| queue determinism | generator run twice, md5 compared | identical | ✓ PASS |

### Gates — ACTUAL numbers at `56cbac7`

| Gate | Result |
|------|--------|
| `uv run pytest -q` | **1459 passed, 8 snapshots passed** in 73.75s |
| `uv run python -m tools.contract_drift.drift` | **OK — live manifest matches the committed baseline** |
| `uv run pytest tools/harness_lint -q` | **316 passed** (up from the 300 recorded in 28-FIXES.md — Phase 29 lint work has since landed on this branch) |
| `uv run python -m tools.harness_emit` + `git status --porcelain` | **100 artifacts emitted, tree clean** |
| `uv run python -m tools.docs_guard` | **exit 1** — 6 × `broken-binding`, 2 × `STALE_ADVISORY`, 8 bindings, 7 uncovered, no ratchet |

Note: 1459 matches the merge-commit figure exactly; drift and emit are clean. `harness_lint` and the emit artifact count are HIGHER than 28-FIXES.md recorded because the branch has since advanced into Phase 29 (`56cbac7`). No Phase-28 gate regressed.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `contracts/harness/docs/doc-dependencies.schema.json` | registry shape + WR-02 char pattern | ✓ VERIFIED | Hash-gated; drift OK |
| `tools/docs_guard/registry.py` | 5 DOCSUP-01 rejections | ✓ VERIFIED | 389 lines, wired, exercised by CLI |
| `tools/docs_guard/ledger.py` | read-only committed shape + coherence | ✓ VERIFIED | 550 lines; no writer (grep-confirmed, plus its own static write-call scan test) |
| `tools/docs_guard/guard.py` | 5-state classifier + 2 ratchets | ✓ VERIFIED | 556 lines; `classify()` consumed by `cli.py` and `docs_staleness.rows` |
| `tools/docs_guard/cli.py` | 0/1/3 exit mapping | ✓ VERIFIED | Live exit 3 and exit 1 both observed |
| `tools/docs_guard/impact.py` | graph ids, never fabricated | ✓ VERIFIED | 105 lines; `(none)` printed, never `TBD` |
| `tools/hooks/ledger_guard.py` | layer-1 PreToolUse deny | ✓ VERIFIED | Wired in `merge.py:91,149-158`; present in `.claude/settings.json:165`; opencode twin `harness/plugins/ledger-guard.ts` |
| `tools/memory_regen/docs_staleness.py` | derived queue generator | ✓ VERIFIED | Deterministic; `.memory/derived/docs-staleness.md` regenerates byte-identical |
| `docs/doc-dependencies.toml` | 8 seeded bindings | ✓ VERIFIED | Loads clean; 6 required + 2 advisory |
| `docs/adr/0010-…md` | obligation model + authority boundary | ⚠️ PRESENT, UNRATIFIED | `- **Status:** proposed` at line 5; disclosed at line 395 |
| `docs/.docs-review-ledger.toml` | human-authored ledger | ✗ ABSENT **BY DESIGN** | 28-07 Task 2 is a human-only gate; agent write is denied by three layers |
| `.github/workflows/ci.yml` `docs-guard` job | separate job, joined to fan-in | ✓ VERIFIED | `ci.yml:281-291`, in `gate.needs` at `:321`. **Red until the human authors the ledger** — intended. |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `guard.classify` | previous committed registry | `ledger.previous_document` + `registry.identity_digest` | ✓ WIRED (`guard.py:335-348`) |
| `guard.classify` | `check_coherence` | required `repointed_ids` arg | ✓ WIRED (`guard.py:357-363`) |
| `merge.py` | emitted `.claude/settings.json` | `HARNESS_HOOK_GROUPS` PreToolUse `Write\|Edit` | ✓ WIRED (mutation-proven) |
| `adoption_apply/apply.py` | `REVIEW_LEDGER_GLOBS` | import, single home | ✓ WIRED (`apply.py:65`) |
| `inject.assemble` | derived queue | `_docs_staleness_pointer(derived_dir)`, droppable | ✓ WIRED (`inject.py:215`) |
| `/refresh-memory` | queue generator | shell chain | ✓ WIRED (both runtime trees) |
| CI `gate` | `docs-guard` | `needs:` fan-in | ✓ WIRED (`ci.yml:321`) |

### Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| DOCSUP-01 registry + validation | ✓ SATISFIED | SC-1 above |
| DOCSUP-02 committed ledger shape | ✓ SATISFIED | `_ROW_KEYS` allowlist; no timestamp/identity/model token |
| DOCSUP-03 five states + coherence | ✓ SATISFIED | SC-2, SC-3 |
| DOCSUP-04 derived queue + injector | ✓ SATISFIED | SC-4 |
| DOCSUP-05 no double-report of drift | ✓ SATISFIED | `_drifted_paths` carries the PATH only; `test_drift_findings_not_restated` |

### Anti-Patterns Found

No debt markers. `grep -n "TBD|FIXME|XXX"` over the phase's files returns only references to the **`OWNER_TBD` never-fabricate house rule** (`impact.py:75-76`, `test_impact.py:9,111`, ADR-0010:367) — a named convention, not an unresolved marker.

### Known-and-Intended (recorded, not gaps)

- `docs/.docs-review-ledger.toml` absent; `tools.docs_guard` exits 1 with 6 `broken-binding`; `docs-guard` CI job red. Designed pre-ratification state.
- ADR-0010 `Status: proposed`, landed via `HARNESS_DEV_BYPASS`. Disclosed in the ADR body, never claimed as accepted.
- The eight seeded bindings are amber on landing (`first_seen-unratified` is a history test).
- IN-03 (graph recompiled per binding) — accepted residual, correctness unaffected.

### Residual Durability Check

| Residual | Durably recorded? |
|----------|-------------------|
| IN-03 graph-per-binding | ✓ `28-FIXES.md:265-286`; carried into `29-05-PLAN.md:119` tech_debt |
| `tools/hooks/secret_scan.py:44-47` | ✓ `29-CONTEXT.md` D-15, `29-05-PLAN.md:97`, ROADMAP:595/645/676 |
| Outstanding ratifications (ledger, ADR-0010) | ✓ `ROADMAP.md:725` "**Phase status: BLOCKED ON HUMAN**"; `29-05-PLAN.md:21` |
| `harness/skills/gate-model/SKILL.md` stale `path_deny_globs` prose | ⚠️ PARTIAL — see below |

**⚠️ WARNING (non-blocking).** The gate-model prose is confirmed stale in the tree: `SKILL.md:19-26` still names only the three constitution trees (omitting `*.env` and the ledger), and the hook table at `:30-37` omits `ledger_guard` entirely. `29-04-PLAN.md` is the designated discharge vehicle and lists the file — but **grep finds no occurrence of `ledger_guard` anywhere under `.planning/phases/29-…/`**. The `28-FIXES.md:292` note ("the entry now needs to name `ledger_guard` as well") therefore lives only in the Phase-28 record and has not propagated into the plan that will execute the fix. Risk: the correction lands naming `*.env` but not `ledger_guard`, and the binding's target digest is ratified against still-incomplete prose. **Recommendation:** add the `ledger_guard` requirement explicitly to 29-04's acceptance criteria before it executes.

### Gaps Summary

None. All four ROADMAP success criteria are achieved in the codebase, verified independently of the SUMMARY narrative, and each of the four post-review controls survives its own deletion test. The phase is not `passed` solely because it is **blocked on human ratification by design** — the two blocking gates are precisely the ones an agent is architecturally forbidden to close, and the enforcement of that forbiddance is itself the phase's central deliverable and is verified working.

---

_Verified: 2026-07-21_
_Verifier: gsd-verifier_
