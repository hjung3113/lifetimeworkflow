---
phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
plan: 05
type: execute
wave: 3
depends_on: ["28-03", "28-04"]
files_modified:
  - tools/docs_guard/guard.py
  - tools/docs_guard/impact.py
  - tools/docs_guard/cli.py
  - tools/docs_guard/tests/test_guard.py
  - tools/docs_guard/tests/test_impact.py
  - tools/docs_guard/tests/test_report.py
autonomous: true
requirements: [DOCSUP-03, DOCSUP-05]

must_haves:
  truths:
    - "SC-3: classification is FIRST-MATCH-WINS with `BROKEN` ordered BEFORE every staleness check — a missing target or a selector expanding to zero paths must never be reported as merely stale (D-05)"
    - "SC-3: `BROKEN` and `STALE_REQUIRED` fail (exit 1); `STALE_ADVISORY` warns on stderr and leaves the exit code unchanged; an uncovered-count REGRESSION fails"
    - "D-05 exit codes are pinned now because Phase 29's `/docs-update` binds to them: 0 clean, 1 broken/stale-required/uncovered-regression, 3 registry-invalid. 2 is argparse's usage error and is never reused"
    - "D-06: `live_uncovered < uncovered_max` PASSES and prints `ratchet can tighten: set uncovered_max = <live>` — the guard prints the suggestion and never applies it. The same holds for the `binding_min` count ratchet"
    - "SELF-GREEN CLOSURE: a binding whose only ledger row is a `first_seen-unratified` finding (plan 28-04) is NOT `FRESH` — the state chain must consume that finding, or the classifier would re-open the hole ledger.py just closed"
    - "D-07: the uncovered corpus is `docs/{tutorials,how-to,explanation}/**` + `docs/glossary.md` + root `AGENTS.md` + root `CLAUDE.md` + `.memory/README.md`, MINUS `DERIVED_GLOBS`; `.planning/**` (GSD-owned) and `examples/**` (GEN-04) are excluded. The set is stated as a module constant, not inferred inside a loop"
    - "P6: corpus enumeration is GIT-TRACKED-ONLY — an untracked working-tree file must not change the count, or CI's clean checkout disagrees with a developer's tree (this was Phase 26 CR-01)"
    - "D-13: a binding whose sources include a CURRENTLY-DRIFTED contract reports `SUPPRESSED (contract-drift leading)`, not `STALE_REQUIRED`. `run_gate()` is read for the suppression decision ONLY and its findings are never re-rendered"
    - "D-12: impact ids are EMPTY, never fabricated, when a changed source path is not a tracked contract. Under-delivering is the safe direction (the `OWNER_TBD` never-fabricate house rule)"
    - "D-09: the report NEVER suggests an in-place edit of an accepted ADR. A report that teaches the wrong action is itself the defect, independent of `contract_guard` denying the write"
    - "The report opens with one line stating that contract-drift and golden are LEADING and authoritative — it never restates their findings (D-13 / research Q9)"
  artifacts:
    - path: "tools/docs_guard/guard.py"
      provides: "`classify(...)` -> `run_gate`-shaped result dict `{ok, bindings: [...], uncovered: {...}, coverage: {...}, findings: [...]}`; `STATES`; the corpus constant; git-tracked-only enumeration; both ratchets"
      contains: "SUPPRESSED"
    - path: "tools/docs_guard/impact.py"
      provides: "`impact_ids(source_paths)` — contract path -> schema stem -> authority endpoint -> `compile_graph` + `direct`/`transitive` ids; empty list when unmapped"
      contains: "compile_graph"
    - path: "tools/docs_guard/cli.py"
      provides: "argparse entry, the stable grouped report, and the 0/1/3 exit mapping"
      contains: "contract-drift and golden are leading"
    - path: "tools/docs_guard/tests/test_guard.py"
      provides: "`STATE_ORDER_CASES` (a binding that is simultaneously broken AND stale must classify BROKEN), `RATCHET_CASES`, `SUPPRESSION_CASES`, and the exit-code matrix"
      contains: "STATE_ORDER_CASES"
    - path: "tools/docs_guard/tests/test_report.py"
      provides: "report-grouping stability, the ADR no-in-place-edit assertion, and the drift-not-restated assertion"
      contains: "adr"
  key_links:
    - from: "tools/docs_guard/guard.py"
      to: "tools/contract_drift/drift.py::run_gate"
      via: "read for SUPPRESSION only — never re-rendered (D-13)"
      pattern: "run_gate"
    - from: "tools/docs_guard/impact.py"
      to: "tools/contract_graph/query.py"
      via: "direct/transitive over compile_graph adjacency; ids only, paths discarded"
      pattern: "from tools.contract_graph"
    - from: "tools/docs_guard/cli.py"
      to: "tools/docs_guard/__main__.py"
      via: "`main()` delegate — `python -m tools.docs_guard`"
      pattern: "def main"
---

<objective>
Implement DOCSUP-03's classifier and DOCSUP-05's report: five states with a first-match-wins order,
the uncovered ratchet, contract-drift suppression, graph impact ids, and the pinned 0/1/3 exit
mapping.

Purpose: this is the plan that turns three read-only modules into a gate. Phase 29's `/docs-update`
binds to these exit codes and to this vocabulary, so both are pinned here rather than discovered
later.

Output: `guard.py`, `impact.py`, `cli.py`, and `python -m tools.docs_guard` working end to end.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-CONTEXT.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-RESEARCH.md
@tools/contract_drift/drift.py
@tools/contract_graph/query.py
@tools/contract_graph/compile.py
@tools/adoption_scan/destinations.py
@tools/docs_guard/registry.py
@tools/docs_guard/ledger.py

<interfaces>
<!-- Verified against source in this session. -->

`tools/contract_drift/drift.py:177-216` — the result-dict shape to mirror, and the suppression input:

    def run_gate(contracts_dir=CONTRACTS_DIR, baseline_path=MANIFEST_PATH) -> dict
    # -> {"ok": bool, "drifted": [(rel_path, kind, classification), ...]}
    #    kind ∈ {changed, added, removed}; classification ∈ {breaking, non-breaking, unknown}

`tools/contract_graph/compile.py:39-49`:

    def _tracked_schemas(contracts_dir: Path) -> set[str]
    # {p.name.removesuffix(".schema.json") for p in contracts_dir.rglob("*.schema.json")}

    def compile_graph(cfg: dict | None = None) -> dict
    # -> {"relationships": [...], "adjacency": {authority: sorted[dependents]},
    #     "diagnostics": sorted[str]}

`tools/contract_graph/query.py:29,39,55` — cycle-safe, deterministic, and query.py:5 literally names
this consumer ("future documentation-impact reports (DOCSUP)"):

    direct(graph, node)     -> {"ids": [...sorted...], "paths": [[node, dep], ...]}
    reverse(graph, node)    -> same shape over incoming edges
    transitive(graph, node) -> same shape, reachable-excluding-start

Report `ids` only. `paths` are for the conductor tree render, not a docs report.

`tools/harness_config/loader.py:90-197`:

    def effective_relationships(cfg: dict | None = None) -> list[dict]
    # each record: {"id", "contract", "authority", "dependents": [...]}

So the mapping chain for a changed source path is:
`contracts/<...>/<stem>.schema.json` -> `<stem>` (if in `_tracked_schemas`) -> the record whose
`contract == <stem>` -> that record's `authority` endpoint -> `direct`/`transitive` on the graph.
A source path that is not a tracked contract maps to NOTHING and yields an EMPTY id list.

`tools/adoption_scan/destinations.py:39-49` — the git-tracked-only enumeration rule and its
failure-tolerant degradation (git missing / invocation failing -> unfiltered rather than raising).
This is Phase 26's CR-01; copy the posture.

`tools/docs_guard/registry.py` (plan 28-03): `load_registry(path=..., root=...)` -> sorted bindings;
`RegistryError`. A MISSING registry yields `[]`.

`tools/docs_guard/ledger.py` (plan 28-04): `load_ledger(path)` -> `(coverage, rows)` where
`coverage` carries `uncovered_max` and `binding_min`; `previous_ledger(path, cwd)`;
`check_coherence(rows, previous, live_digests, bindings)` -> sorted findings; `LedgerError`.
No writer exists.

`check_coherence` emits THREE distinct reason constants this classifier must consume without
conflating them: `disposition-incoherent` (the paste-the-live-digest attack),
`first_seen-unratified` (a content-bound disposition whose id has no previous committed row — the
brand-new-binding self-bless), and `unverified-disposition` (git could not retrieve any previous
ledger). A `first_seen-unratified` binding must NOT reach `FRESH`.

`tools/docs_guard/digest.py` (plan 28-02): `resolve(selectors, root)`, `compute(paths)`,
`MissingSourceError`.

Repo exit-code conventions: `drift.py:370-390` uses 0/1; `approve.py` refusal uses 3;
`adoption_apply/cli.py` stale-approval uses 4. Exit 2 is argparse's stdlib usage error — never reuse it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED then GREEN — five-state classification, the uncovered ratchet, and drift suppression</name>
  <files>tools/docs_guard/guard.py, tools/docs_guard/tests/test_guard.py</files>
  <read_first>
    tools/contract_drift/drift.py lines 177-216 (the result-dict shape to mirror),
    tools/adoption_scan/destinations.py lines 25-55 (git-tracked-only enumeration + its degradation),
    tools/docs_guard/registry.py and tools/docs_guard/ledger.py (plans 28-03/04 — the inputs),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    Three adversarial tables, authored BEFORE the classifier.

    `STATE_ORDER_CASES` — first-match-wins, and each row is constructed so a naive
    evaluation order gets it WRONG:
      - `broken_beats_stale`: the target file is DELETED and the ledger digests are stale. A
        classifier that checks staleness first reports `STALE_REQUIRED`. Expected `BROKEN`. This is
        the row that proves the ORDER, not merely the states.
      - `broken_zero_expansion`: `sources = ["libs/gone/**"]` expands to zero paths while every
        digest happens to match the ledger. A digest-equality-first classifier reports `FRESH`.
        Expected `BROKEN`.
      - `broken_no_ledger_row_required`: a `required` binding with no `[[reviewed]]` row at all,
        with all files present. Expected `BROKEN`.
      - `advisory_no_ledger_row`: same, `advisory`. Expected `STALE_ADVISORY`, not `BROKEN` — the
        negative control that keeps the previous row from becoming "any missing row is broken".
      - `fresh`: both digests match -> `FRESH`.
      - `stale_required` / `stale_advisory`: one digest differs, per severity.
      - `superseding_adr_never_fresh`: an ADR binding with `SUPERSEDING_ADR_REQUIRED` and BOTH
        digests matching. Expected: NOT `FRESH` — an open obligation can never be green (D-09).
      - `first_seen_never_fresh` (the self-green closure at the CLASSIFIER level): a brand-new
        binding with a matching `reviewed-no-change` row and no previous committed row — i.e.
        `check_coherence` returned `first_seen-unratified`. BOTH digests match, so a classifier that
        keys `FRESH` on digest equality alone reports `FRESH` and re-opens the hole `ledger.py` just
        closed. Expected: NOT `FRESH`, and `ok` is False for a `required` binding. Pair with
        `second_commit_is_fresh` (the same binding once its row is in the previous committed ledger)
        -> `FRESH`, so the rule cannot degrade into "a new binding can never be green".

    `RATCHET_CASES`:
      - `uncovered_regression`: `live_uncovered = uncovered_max + 1` -> fail.
      - `uncovered_equal`: `live == max` -> pass, no suggestion printed.
      - `uncovered_tightened`: `live < max` -> PASS, and the exact string
        `ratchet can tighten: set uncovered_max = <live>` is emitted.
      - `ratchet_not_written` (D-06, the structural row): run the full classify over
        `uncovered_tightened`, then sha256 the ledger file and assert it is BYTE-IDENTICAL to before.
        Pair it with an assertion that `guard.py`'s source contains no write call and no import of
        `ledger`'s (nonexistent) writer, with a live planted-token negative control so the scan is
        known-live. A guard-authored bump must be structurally impossible, not merely unobserved.
      - `binding_count_regression`: `binding_min = 3`, live registry has 2 bindings -> fail, naming
        both numbers. Plus `binding_count_equal` (pass, no suggestion) and `binding_count_grew`
        (pass, emits `ratchet can tighten: set binding_min = <live>`).
      - `binding_deleted_outside_corpus` (why the count ratchet is not redundant): delete a binding
        whose TARGET lies outside `HUMAN_CORPUS`, so `live_uncovered` does not move at all. The
        uncovered ratchet stays green; only `binding_min` catches it. Without this row the two
        ratchets look interchangeable and someone will later delete one.
      - `uncovered_untracked_file` (P6 / Phase 26 CR-01): create an UNTRACKED file inside the corpus
        and assert `live_uncovered` is UNCHANGED. A filesystem-walk implementation counts it and CI's
        clean checkout then disagrees with a developer's tree.
      - `corpus_excludes`: assert `.planning/**`, `examples/**`, and every `DERIVED_GLOBS` path are
        absent from the enumerated corpus, and that `docs/glossary.md`, root `AGENTS.md`, root
        `CLAUDE.md`, and `.memory/README.md` are PRESENT. This pins the ratchet's meaning (D-07/A4).

    `SUPPRESSION_CASES` (D-13):
      - `drifted_source_suppressed`: a binding whose `sources` include a contract schema that
        `run_gate()` reports as drifted, with stale digests. Expected `SUPPRESSED (contract-drift
        leading)`, NOT `STALE_REQUIRED`, and the suppressed binding does not contribute to exit 1.
      - `undrifted_source_not_suppressed` (negative control): the same binding with a clean
        `run_gate()`. Expected `STALE_REQUIRED` — suppression must be conditional, not blanket.
      - `drift_findings_not_restated`: assert the classify result contains NONE of `run_gate()`'s
        drifted tuples verbatim — the guard reads `run_gate` for the decision and never carries its
        findings forward.

    RED: run each table against a THROWAWAY classifier that evaluates staleness before brokenness,
    counts the corpus by filesystem walk, and ignores `run_gate` entirely. Confirm
    `broken_beats_stale`, `broken_zero_expansion`, `uncovered_untracked_file`, and
    `drifted_source_suppressed` all FAIL for their stated reasons and record the verbatim messages.
    Confirm the negative controls (`advisory_no_ledger_row`, `uncovered_equal`,
    `undrifted_source_not_suppressed`) are GREEN even against the throwaway.
  </behavior>
  <action>
    Write `test_guard.py` with all three tables, build the throwaway classifier, run:

        ! uv run pytest tools/docs_guard/tests/test_guard.py -k "broken_beats_stale or broken_zero_expansion or uncovered_untracked_file or drifted_source_suppressed" -q

    (inverted form; never pipe pytest into `head`/`tail`). Record the verbatim output in the SUMMARY
    under `## RED evidence (DOCSUP-03 classification)`.

    Then write the real `guard.py`:

    `classify(registry_path=..., ledger_path=..., root=...)` returns a `run_gate`-shaped dict:
    `{"ok": bool, "bindings": [...sorted by id...], "uncovered": {"live": int, "max": int | None,
    "paths": [...sorted...]}, "findings": [...sorted...]}`. Per binding, evaluate STRICTLY in this
    order and stop at the first match — write the order as a numbered comment so a future editor
    cannot reorder it accidentally:

      1. `BROKEN` — target missing, OR any `sources` selector expanding to zero paths, OR no
         `[[reviewed]]` row for a `required` binding. `digest.MissingSourceError` maps here; a
         missing file is NEVER hashed as empty.
      2. `SUPPRESSED` — any source is a contract schema currently in `run_gate()["drifted"]`.
         Call `run_gate()` ONCE per classify, not per binding. Never copy its tuples into the result.
      3. `FRESH` — both stored digests equal the live ones AND the disposition is not
         `SUPERSEDING_ADR_REQUIRED` (an open obligation can never be green) AND `check_coherence`
         emitted no blocking finding for this id — specifically not `disposition-incoherent`, not
         `first_seen-unratified`, and not `unverified-disposition`. Write this as an explicit
         guard against the finding set, not as an afterthought: digest equality is NECESSARY but
         not SUFFICIENT for green, and that is the whole content of the self-green closure.
      4. `STALE_REQUIRED` / `STALE_ADVISORY` — a digest differs, by severity.

    `UNCOVERED` is a per-DOCUMENT state, not a binding state. Define the corpus as an explicit
    module constant `HUMAN_CORPUS` (D-07): `docs/tutorials/**`, `docs/how-to/**`,
    `docs/explanation/**`, `docs/glossary.md`, `AGENTS.md` (root), `CLAUDE.md` (root),
    `.memory/README.md` — MINUS the imported `DERIVED_GLOBS`, and excluding `.planning/**` (the
    GSD-owned lane; reuse `destinations.is_gsd_owned` rather than a new predicate) and `examples/**`
    (GEN-04). Enumerate GIT-TRACKED files only, with `destinations.py:39-49`'s failure-tolerant
    degradation (git missing or failing -> unfiltered, never raise). Subtract every registry
    `target`; the remainder is `live_uncovered`. Comment that this constant SETS the ratchet's
    meaning and must not be edited without moving the ratchet deliberately.

    Uncovered ratchet: `live > max` -> fail. `live == max` -> pass. `live < max` -> pass AND emit
    `ratchet can tighten: set uncovered_max = <live>`. `max is None` (no ledger) -> no ratchet check,
    no failure.

    Binding-count ratchet, evaluated the same way against `coverage["binding_min"]` and the live
    registry binding count: `live < binding_min` -> fail naming both numbers; `==` -> pass;
    `>` -> pass plus `ratchet can tighten: set binding_min = <live>`; `None` -> no check. Comment
    that this is NOT redundant with the uncovered ratchet — a binding whose target lies outside
    `HUMAN_CORPUS` can be deleted without moving the uncovered count, so deletion would otherwise be
    entirely unguarded (proven by `binding_deleted_outside_corpus`).

    `classify` NEVER writes either ratchet.

    Fold in `ledger.check_coherence`'s findings. `ok` is False iff any `BROKEN`, any
    `STALE_REQUIRED`, any failing coherence finding (including a `required`-severity
    `first_seen-unratified`), an uncovered regression, or a binding-count regression exists.
    `STALE_ADVISORY` and warn-level findings never flip `ok`.

    No `datetime`, no `time`, no `float`, no `set` iteration reaching output.
  </action>
  <verify>
    <automated>uv run pytest tools/docs_guard/tests/test_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    Every table row was RED against the throwaway for its stated reason (verbatim recorded) and is
    GREEN against the real classifier, including all negative controls. `uncovered_untracked_file`
    proves the count is git-tracked-only. `ratchet_not_written` proves the ledger is byte-identical
    after a full classify, and the no-write static scan has a live planted-token negative control.
    `drift_findings_not_restated` passes. `grep -nE 'write_text|write_bytes|open\(.*[\"'\'']w'
    tools/docs_guard/guard.py` returns nothing. `uv run pytest tools/docs_guard -q` exits 0.
  </acceptance_criteria>
  <done>`guard.py` classifies with a pinned first-match-wins order in which digest equality is necessary but not sufficient for `FRESH`, enforces both read-only ratchets over an explicitly-stated git-tracked corpus, and suppresses rather than stacks on contract-drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Graph impact ids — real mapping, empty when unmapped, never fabricated</name>
  <files>tools/docs_guard/impact.py, tools/docs_guard/tests/test_impact.py</files>
  <read_first>
    tools/contract_graph/query.py (full — the three query shapes and their determinism invariants),
    tools/contract_graph/compile.py lines 30-60 (`_tracked_schemas`, `compile_graph`),
    tools/harness_config/loader.py lines 85-100 (`effective_relationships` record shape),
    tools/memory_regen/contracts_index.py lines 40-46 (the `OWNER_TBD` never-fabricate house rule),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    - `impact_ids(["contracts/sample/greeting.schema.json"])` where `greeting` is a tracked contract
      with an `authority` endpoint returns that endpoint's sorted `direct` + `transitive` ids.
    - `impact_ids(["docs/how-to/task-lifecycle.md"])` returns `[]` — a human doc is not a tracked
      contract. This is the COMMON case and must be empty, never a guess.
    - `impact_ids(["contracts/sample/untracked-name.schema.json"])` where the stem has no
      relationship record returns `[]`.
    - `impact_ids([])` returns `[]`.
    - A contract stem appearing in `_tracked_schemas` but with NO `authority` in
      `effective_relationships` returns `[]` — the A5 assumption failing must under-deliver, never
      mis-deliver.
    - Results are sorted and deduplicated across multiple input paths; repeated calls are
      byte-identical.
    - `paths` from the query API are DISCARDED — ids only (research Q8).
    - A cyclic relationship set terminates (inherited from `query.transitive`'s cycle-safety) —
      assert it rather than assume it, since a hang in a CI gate is indistinguishable from a
      timeout.
  </behavior>
  <action>
    Write `test_impact.py` first with the cases above, driving `compile_graph(cfg=...)` from an
    explicit in-memory `cfg` dict (the `load_project(path=...)` seam) so the tests are hermetic and
    do not depend on the live `harness/project.toml`.

    Then write `impact.py` as a small PURE helper — no filesystem writes, no CLI:

      `_contract_stem(path)` — `contracts/<...>/<stem>.schema.json` -> `<stem>`, else `None`.
      `_authority_for(stem, cfg)` — the `effective_relationships` record whose `contract == stem`,
      returning its `authority`, else `None`.
      `impact_ids(source_paths, cfg=None)` — map each path through the chain; for each resolved
      authority endpoint, union `direct(graph, node)["ids"]` and `transitive(graph, node)["ids"]`;
      return the sorted deduplicated list. Compile the graph ONCE per call, not per path.

    Mandatory comment above the unmapped branch: emit an EMPTY list, never a fabricated one — the
    `OWNER_TBD` never-fabricate house rule (contracts_index.py:43-45). Most human-doc sources will
    not be tracked contracts, so empty is the normal, correct answer and under-delivering is the
    safe direction. Also record that the path -> node mapping is 28-RESEARCH.md's A5 assumption at
    MEDIUM confidence: if it is wrong, ids are empty rather than incorrect, which is why this shape
    was chosen.

    Never call `reverse()` here — a docs report wants "what does changing this affect", which is the
    outgoing direction.
  </action>
  <verify>
    <automated>uv run pytest tools/docs_guard/tests/test_impact.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/docs_guard/tests/test_impact.py -q` exits 0. Every unmapped case returns
    exactly `[]` with no placeholder, no `TBD` string, and no partial guess. The cycle case
    terminates. `grep -n 'paths' tools/docs_guard/impact.py` shows `paths` is never returned to the
    caller. `uv run pytest tools/docs_guard -q` exits 0.
  </acceptance_criteria>
  <done>`impact.py` maps a changed contract source through schema stem -> authority endpoint -> the Phase 25 query API and returns sorted ids, emitting an empty list wherever the mapping does not resolve.</done>
</task>

<task type="auto">
  <name>Task 3: The stable report and the 0/1/3 exit mapping</name>
  <files>tools/docs_guard/cli.py, tools/docs_guard/tests/test_report.py</files>
  <read_first>
    tools/contract_drift/drift.py lines 360-395 (the CLI/exit-code convention),
    tools/adoption_apply/cli.py (the argparse + clean-refusal convention: a documented exit code and
      a stderr diagnostic, never a traceback),
    tools/docs_guard/guard.py (Task 1's result dict),
    this plan's `<interfaces>` block
  </read_first>
  <behavior>
    - Exit 0: no `BROKEN`, no `STALE_REQUIRED`, no failing coherence finding, uncovered within the
      ratchet. `STALE_ADVISORY` may be present, is printed to STDERR, and does not change the code.
    - Exit 1: any `BROKEN` / `STALE_REQUIRED` / failing coherence finding / uncovered regression.
    - Exit 3: `RegistryError` or `LedgerError` — registry/ledger INVALID. Distinct from 1 because the
      operator action is different: fix the registry, not the docs. A MISSING registry is exit 0
      with zero bindings, not exit 3.
    - Exit 2 is never produced deliberately (argparse usage only).
    - A `RegistryError`/`LedgerError` surfaces as a clean stderr diagnostic — `"Traceback" not in
      stderr`, stderr non-empty, and the message never echoes registry or ledger file CONTENT.
    - Report grouping per DOCSUP-05, one stable block per binding, columns in a FIXED order: changed
      source path + hash prefix, graph impact ids, target doc, severity, required disposition.
    - The report's FIRST line states that contract-drift and golden are leading and authoritative.
    - `test_report_never_suggests_adr_edit`: over a registry containing an accepted-ADR binding in
      every reachable state (`FRESH`, `STALE_REQUIRED`, `SUPERSEDING_ADR_REQUIRED`), the full report
      text contains none of `edit the ADR`, `update the ADR`, `modify`, or any imperative pointing at
      the ADR path itself; it must instead name the `/adr` supersede path. Assert on the RENDERED
      text of every state, not one sampled state — this is the row that keeps the report from
      teaching the wrong action (D-09).
    - `test_report_does_not_restate_drift`: with a drifted contract present, no drifted tuple from
      `run_gate()` appears in the report; the suppressed binding renders as
      `SUPPRESSED (contract-drift leading)`.
    - `test_report_is_byte_identical`: two renders of the same result dict are equal — no set
      iteration, no dict-order leakage, no clock.
    - `test_report_distinguishes_three_ledger_reasons`: a result carrying all three of
      `disposition-incoherent`, `first_seen-unratified`, and `unverified-disposition` renders three
      DISTINCT remediation lines. The `first_seen-unratified` line must say that a human review
      commit landing the ledger row is the ratification — not that the digests are wrong, which is
      the remedy for staleness and would send the operator down the wrong path.
    - `test_report_omits_diff_when_git_unretrievable`: in a repo with no `HEAD`, the old-to-new diff
      section is absent and the `unverified-disposition` reason is shown instead (DOCSUP-05's
      "diff only when retrievable from git").
  </behavior>
  <action>
    Write `test_report.py` first, then `cli.py`:

    argparse entry `main(argv=None) -> int` with `--registry`, `--ledger`, and `--root` options
    (explicit paths, defaulting to the repo constants — the D-14 overlay seam). Call
    `guard.classify`, render, return the exit code. Wrap the classify call so `RegistryError` and
    `LedgerError` map to exit 3 with a one-line stderr diagnostic naming the offending id or key —
    NEVER the file's contents, since a hostile registry may carry arbitrary bytes. No other exception
    class is swallowed.

    Render deterministically: fixed column order, bindings sorted by id, findings sorted, impact ids
    sorted. `STALE_ADVISORY` and warn-level findings go to STDERR; failures and the summary go to
    STDOUT. No `datetime`, no `float`, no `set` iteration in output.

    Write `docs/adr/**` guidance as a NAMED constant so the no-in-place-edit rule has one home: the
    remediation text for `SUPERSEDING_ADR_REQUIRED` instructs authoring a NEW ADR via the existing
    `/adr` command and the human constitution path, and explicitly says the existing ADR is not to
    be edited. Keep the disposition vocabulary spelled EXACTLY as in the registry so Phase 29's
    `/docs-update` can filter structurally.

    Finally, wire `tools/docs_guard/__main__.py`'s lazy `main()` import to `cli.main` and confirm
    `uv run python -m tools.docs_guard --help` works and `uv run python -m tools.docs_guard` runs
    against the live tree. With no registry committed yet (plan 28-07 seeds it), the live run must
    exit 0 with zero bindings and an uncovered count — record that number in the SUMMARY, because
    plan 28-07 uses it to set the initial `uncovered_max`.
  </action>
  <verify>
    <automated>uv run pytest tools/docs_guard -q && uv run python -m tools.docs_guard</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/docs_guard -q` exits 0. `uv run python -m tools.docs_guard` exits 0 against
    the live tree (no registry committed yet -> zero bindings) and prints an uncovered count, which
    is recorded verbatim in the SUMMARY for plan 28-07. An invalid registry fixture yields exit 3
    with `"Traceback" not in stderr` and non-empty stderr. `test_report_never_suggests_adr_edit`
    passes across ALL reachable ADR states. The report's first line names contract-drift and golden
    as leading. Do NOT run the full suite in flight — this is the only wave-3 plan, but the
    full-suite / drift / emit / GEN-04 fan-in belongs to plan 28-08.
  </acceptance_criteria>
  <done>`python -m tools.docs_guard` produces a stable grouped report with pinned 0/1/3 exit codes, never restates contract-drift, and never suggests editing an accepted ADR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `run_gate()` result → suppression decision | Read-only consumption of another gate's state. Its findings must not cross into this gate's output. |
| Registry/ledger error → CLI exit code | Exit 3 is the documented "fix the registry" channel; a traceback here would be an undocumented failure mode. |
| Report text → the human's next action | The report is an instruction surface. Naming the wrong remediation is itself a defect, independent of write-side gates. |
| Working-tree enumeration → the uncovered count | Untracked local files must not enter the count, or CI and a developer's tree disagree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-23 | Spoofing (false assurance) | a missing target or zero-expansion selector reported as merely stale | mitigate | `BROKEN` is ordered FIRST in a numbered, commented first-match-wins chain; proven by `broken_beats_stale` and `broken_zero_expansion`, which a staleness-first classifier gets wrong. |
| T-28-24 | Elevation of Privilege | the guard writing or lowering `uncovered_max` / `binding_min` | mitigate | `classify` never writes; `ratchet_not_written` asserts the ledger is byte-identical after a full run, backed by a no-write static scan with a live planted-token negative control (D-06). |
| T-28-24b | Elevation of Privilege (self-blessing) | a classifier that keys `FRESH` on digest equality alone, re-opening the hole `ledger.py` closed | mitigate | `FRESH` requires digest equality AND an empty blocking-finding set for that id; proven by `first_seen_never_fresh`, with `second_commit_is_fresh` as the non-degradation control. |
| T-28-24c | Tampering | silently deleting a binding whose target lies outside `HUMAN_CORPUS` | mitigate | The `binding_min` count ratchet, proven independently necessary by `binding_deleted_outside_corpus` (the uncovered ratchet stays green through that deletion). |
| T-28-25 | Repudiation | a non-reproducible uncovered count (Phase 26 CR-01) | mitigate | Git-tracked-only enumeration with `destinations.py:39-49`'s failure-tolerant degradation; `uncovered_untracked_file` asserts an untracked file does not move the count. |
| T-28-26 | Spoofing (false assurance) | fabricated graph impact ids | mitigate | Empty list whenever the path→stem→authority chain does not resolve; the `OWNER_TBD` never-fabricate rule, asserted for every unmapped case. |
| T-28-27 | Tampering | the report instructing an in-place edit of an accepted ADR | mitigate | A single named remediation constant pointing at `/adr` + the human constitution path; asserted across ALL reachable ADR states, not one sample (D-09). |
| T-28-28 | Denial of Service | double-red — every contract change failing both `drift` and `docs-guard` with unrelated-looking messages | mitigate | `SUPPRESSED (contract-drift leading)` for drifted sources, plus a negative control proving suppression is conditional and a test proving drift findings are not restated (D-13). |
| T-28-29 | Denial of Service | a cyclic relationship set hanging the gate | mitigate | Reuse of `query.transitive`'s cycle-safe iterative worklist, asserted by an explicit cycle case rather than assumed. |
| T-28-30 | Information Disclosure | a hostile registry's bytes echoed into a CLI diagnostic | mitigate | Exit-3 diagnostics name the offending id or key only, never file content. |
| T-28-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new external packages — stdlib plus existing in-repo modules only. |
</threat_model>

<verification>
- `uv run pytest tools/docs_guard -q` exits 0.
- `uv run python -m tools.docs_guard` exits 0 against the live tree and prints an uncovered count.
- `grep -nE 'write_text|write_bytes' tools/docs_guard/guard.py tools/docs_guard/cli.py` is empty.
- `git status --porcelain contracts docs .github` is empty — this plan touches only `tools/docs_guard`.

The full-suite / drift / emit / GEN-04 fan-in belongs to plan 28-08.
</verification>

<success_criteria>
- SC-3 (ROADMAP): `BROKEN` and `STALE_REQUIRED` fail, `STALE_ADVISORY` warns, and both the
  uncovered and binding-count non-regression ratchets are enforced — with `BROKEN` proven to win
  over staleness by construction.
- Digest equality is necessary but NOT sufficient for `FRESH`: a `first_seen-unratified` binding
  cannot be green, and the report tells the operator the right remedy for it.
- Exit codes 0/1/3 are pinned and exercised, including the registry-invalid channel.
- Impact ids are real where the mapping resolves and empty everywhere else.
- The report is byte-identical across renders, never restates contract-drift, and never suggests
  editing an accepted ADR in any state.
</success_criteria>

<output>
Create `.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-05-SUMMARY.md` when
done, with a `## RED evidence (DOCSUP-03 classification)` section and a `## Live uncovered count`
section recording the exact number `python -m tools.docs_guard` reported, for plan 28-07's ratchet.
</output>
