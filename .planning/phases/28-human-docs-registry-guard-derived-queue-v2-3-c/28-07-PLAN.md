---
phase: 28-human-docs-registry-guard-derived-queue-v2-3-c
plan: 07
type: execute
wave: 4
depends_on: ["28-05"]
files_modified:
  - docs/doc-dependencies.toml
  - docs/.docs-review-ledger.toml
  - .github/workflows/ci.yml
autonomous: true
requirements: [DOCSUP-01, DOCSUP-02, DOCSUP-03]

must_haves:
  truths:
    - "The core registry ships MINIMAL — a small set of bindings that exercises every severity and disposition class the guard implements. The full highest-risk-document seeding pass is DOCSUP-07 / Phase 29 and is explicitly NOT attempted here"
    - "GEN-04: the CORE registry contains no `examples/` token in any selector or target. `tools/harness_lint/tests/test_core_no_example_dep.py` scans core files for that token, and an instance overlay is the D-14 seam, deliberately not built in Phase 28"
    - "Every seed ledger row uses `reviewed-no-change` (or `REVIEWED_STILL_CURRENT` for an ADR binding), NEVER `updated`. This is forced, not stylistic: `updated` requires a target-digest delta versus a PREVIOUS committed ledger, and on a first-ever ledger there is none — it would classify `unverified-disposition` and fail closed for a `required` binding (D-08)"
    - "`[coverage] uncovered_max` is set to the EXACT live uncovered count recorded in the 28-05 SUMMARY, not a padded or rounded number — a ratchet set above the live count silently permits regression up to that slack"
    - "The registry and ledger carry NO timestamp, NO reviewer/human identity, NO prose copy, and NO model identifier — enforced at load by `ledger.py`'s allowlist and asserted here"
    - "The `docs-guard` CI job is a SEPARATE job with its own `gate.needs` entry, mirroring the `emit-drift` / `stale-derived` / `workspace` separate-job idiom — never folded into `drift`"
    - "`docs/.docs-review-ledger.toml` is COMMITTED (it is the baseline the gate diffs against) — unlike the derived queue, which stays gitignored"
    - "Neither file is on the constitution plane: they are plain reviewed config, so `/adopt` can propose rows in Phase 29 (D-01, the whole reason for the split)"
  artifacts:
    - path: "docs/doc-dependencies.toml"
      provides: "the core human-docs dependency registry — minimal seed bindings covering required + advisory severities and, if an accepted ADR is bound, the forced ADR disposition pair"
      contains: "[[binding]]"
    - path: "docs/.docs-review-ledger.toml"
      provides: "the committed review ledger — `[coverage] uncovered_max` plus one `[[reviewed]]` row per seeded binding, storing only id + the two exact digests + the disposition"
      contains: "uncovered_max"
    - path: ".github/workflows/ci.yml"
      provides: "a separate `docs-guard` job running `uv run python -m tools.docs_guard` plus the docs_guard test package, and a `docs-guard` entry appended to `gate.needs`"
      contains: "docs-guard"
  key_links:
    - from: ".github/workflows/ci.yml::docs-guard"
      to: "ci.yml::gate.needs"
      via: "fan-in entry — without it the job runs but cannot block"
      pattern: "docs-guard"
    - from: "docs/doc-dependencies.toml"
      to: "contracts/harness/docs/doc-dependencies.schema.json"
      via: "validated in-process by tools.docs_guard.registry — NOT by /contract-check (TOML instance, D-16/A7)"
      pattern: "binding"
---

<objective>
Seed the core registry and its ledger, and wire the `docs-guard` CI job so the gate can actually
block.

Purpose: without committed data the guard is machinery nobody exercises, and without a `gate.needs`
entry a green job proves nothing. This plan lands the minimum data that exercises every class the
guard implements, plus the CI wiring. The broad seeding pass over the highest-risk existing
documents is DOCSUP-07 (Phase 29) and is deliberately out of scope.

Output: `docs/doc-dependencies.toml`, `docs/.docs-review-ledger.toml`, and a separate `docs-guard`
CI job joined to the fan-in gate.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-CONTEXT.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-RESEARCH.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-05-SUMMARY.md
@.github/workflows/ci.yml
@tools/docs_guard/registry.py
@tools/docs_guard/ledger.py
@contracts/harness/docs/doc-dependencies.schema.json

<interfaces>
<!-- Verified against source in this session. -->

`.github/workflows/ci.yml` — the separate-job idiom to mirror (see `stale-derived` at 227 and
`workspace` at 266). Every job is:

    <name>:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v7.0.0
        - uses: astral-sh/setup-uv@v8.3.2
        - name: Sync workspace (all packages)
          run: uv sync --all-packages
        - name: <step>
          run: <command>

Fan-in at ci.yml:285:

    gate:
      needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lifecycle-eval, emit-drift, stale-derived, workspace]

Security posture to preserve: pinned action SHAs/tags as written, top-level `permissions: contents:
read` (ci.yml:31), and NO `${{ github.event.* }}` interpolation into any `run` shell.

`actions/checkout@v7.0.0` defaults to fetch-depth 1. `git show HEAD:./<path>` WORKS at depth 1 — the
research verified this — so the ledger's previous-committed retrieval is available in CI. Do NOT add
`fetch-depth: 0`; that is unnecessary cost, and the `unverified-disposition` degradation exists
precisely for the cases where history genuinely is absent.

Registry row shape (plan 28-01's schema constrains it):

    [[binding]]
    id           = "kebab-case-id"
    sources      = ["libs/python/normalize/**"]
    target       = "docs/how-to/task-lifecycle.md"
    severity     = "required"          # required | advisory
    dispositions = ["updated", "reviewed-no-change"]

Ledger shape (plan 28-04's allowlist enforces it — any other key is rejected by name):

    [coverage]
    uncovered_max = <exact live count from 28-05-SUMMARY>

    [[reviewed]]
    id            = "kebab-case-id"
    source_digest = "<64 hex>"
    target_digest = "<64 hex>"
    disposition   = "reviewed-no-change"

Human-authored docs available as binding targets today: `docs/how-to/task-lifecycle.md`,
`docs/explanation/{template-and-instances,task-lifecycle-shadow-metrics,agent-workflow-skillset-design-guide,next-milestone-task-control-plane}.md`,
`docs/glossary.md`, root `AGENTS.md`, root `CLAUDE.md`, `.memory/README.md`. Accepted ADRs live at
`docs/adr/0001-*.md` .. `docs/adr/0009-*.md`.

`tools/harness_lint/tests/test_core_no_example_dep.py` scans core files for an `examples/` token —
the core registry must not contain one (GEN-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed the core registry and its ledger with honest, content-bound dispositions</name>
  <files>docs/doc-dependencies.toml, docs/.docs-review-ledger.toml</files>
  <read_first>
    .planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-05-SUMMARY.md
      (the `## Live uncovered count` section — the exact number `uncovered_max` takes),
    tools/docs_guard/registry.py (plan 28-03 — the five rejections these rows must survive),
    tools/docs_guard/ledger.py (plan 28-04 — the allowlisted key set),
    docs/how-to/task-lifecycle.md and docs/explanation/template-and-instances.md (candidate targets),
    this plan's `<interfaces>` block
  </read_first>
  <action>
    Author `docs/doc-dependencies.toml` with a MINIMAL set of bindings — enough to exercise every
    class the guard implements, and no more. The broad seeding pass over the highest-risk documents
    is DOCSUP-07 / Phase 29; do not attempt it here. Cover at minimum:

      - one `severity = "required"` binding whose `sources` are real, stable core source selectors
        and whose `target` is a genuinely dependent human doc;
      - one `severity = "advisory"` binding;
      - one binding targeting an ACCEPTED ADR, with `dispositions` exactly
        `["REVIEWED_STILL_CURRENT", "SUPERSEDING_ADR_REQUIRED"]` — this is the row that proves the
        forced ADR pair is representable in practice, not only in a test fixture. Verify the target
        ADR's `- **Status:**` line reads `accepted` before binding it: plan 28-03 rejects a
        `proposed` ADR target.

    Choose each `sources` selector so it names something the target document actually describes. A
    binding whose source has no real relationship to its target is noise that trains people to
    rubber-stamp — the exact failure mode this phase exists to prevent. Justify each pairing in one
    sentence in the SUMMARY.

    GEN-04: no `examples/` token anywhere in the file. Instance bindings are the D-14 overlay seam
    and are not built in Phase 28.

    Then author `docs/.docs-review-ledger.toml`. Compute each row's digests by RUNNING the guard —
    never by hand and never by a separate hashing command, so the ledger cannot disagree with the
    gate:

        uv run python -m tools.docs_guard

    Read the live digests the report emits for each binding and write them verbatim.

    Every seed row's `disposition` is `reviewed-no-change` (or `REVIEWED_STILL_CURRENT` for the ADR
    binding). NEVER `updated`. This is forced by D-04/D-08, not a style choice: `updated` requires a
    target-digest delta versus a PREVIOUS committed ledger, and on a first-ever ledger no previous
    exists, so an `updated` row would classify `unverified-disposition` and fail closed for a
    `required` binding. Record that reasoning as a comment at the top of the ledger so the next
    person seeding rows does not reach for `updated`.

    Set `[coverage] uncovered_max` to the EXACT live uncovered count from the 28-05 SUMMARY. Do not
    pad or round it: any slack above the live count silently permits regression up to that slack.
    If the live count has shifted since 28-05 (this plan adds bindings, which SUBTRACT from
    uncovered), re-run the guard and use the new exact number — and note both numbers in the SUMMARY.

    The ledger carries ONLY `[coverage] uncovered_max` and `[[reviewed]]` rows of
    `{id, source_digest, target_digest, disposition}`. No timestamp, no reviewer, no prose, no model
    identifier — `ledger.py`'s allowlist rejects any other key by name, so a stray key fails loudly
    rather than accumulating.

    Both files are COMMITTED (the ledger is the baseline the gate diffs against). Neither is on the
    constitution plane, so no `GOLDEN_APPROVE_HUMAN` is involved — that is the point of the D-01
    split. A human ratifies the seeded dispositions in plan 28-08.
  </action>
  <verify>
    <automated>uv run python -m tools.docs_guard && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run python -m tools.docs_guard` exits 0 with every seeded binding reported `FRESH` (or, for
    the ADR binding, as a correctly-dispositioned open obligation that does not fail the gate) and
    the uncovered count at or below `uncovered_max`. `uv run pytest
    tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0 and `grep -n 'examples/'
    docs/doc-dependencies.toml` returns nothing. `grep -nEi
    'date|time|reviewer|author|approved_by|excerpt|note|summary|claude|gpt|opus|sonnet'
    docs/.docs-review-ledger.toml` returns nothing outside the explanatory comment. `grep -n
    'updated' docs/.docs-review-ledger.toml` matches only the comment, never a `disposition` value.
    `git status --porcelain tools/memory_regen harness .opencode .claude` is empty — plan 28-06 owns
    those in this same wave, and a change there is the other wave-4 plan in flight, not this plan's
    regression.
  </acceptance_criteria>
  <done>A minimal, honestly-paired core registry and a content-bound ledger are committed, with `uncovered_max` at the exact live count and every disposition of a kind that a first-ever ledger can legitimately carry.</done>
</task>

<task type="auto">
  <name>Task 2: Add the docs-guard CI job and join it to the fan-in gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml lines 195-295 (the `emit-drift`, `stale-derived`, `workspace` job
      idiom and the `gate.needs` fan-in list),
    this plan's `<interfaces>` block
  </read_first>
  <action>
    Add ONE new job named `docs-guard`, placed after `stale-derived` and before `workspace`,
    following the separate-job idiom verbatim: `runs-on: ubuntu-latest`, the same pinned
    `actions/checkout@v7.0.0` and `astral-sh/setup-uv@v8.3.2` steps as written elsewhere in the file,
    `uv sync --all-packages`, then two steps:

      1. `Docs guard — registry validation + binding classification`:
         `uv run python -m tools.docs_guard`
      2. `Docs guard unit tests`:
         `uv run pytest tools/docs_guard tools/memory_regen/tests/test_docs_staleness.py -q`

    Do NOT set `fetch-depth: 0` — `git show HEAD:./<path>` works at checkout's default depth 1, and
    the `unverified-disposition` degradation covers the genuinely history-free case.

    Do NOT add `.memory/derived/docs-staleness.md` to the `stale-derived` job's path list
    (ci.yml:236-237). The queue is gitignored and its content is a function of the files being
    edited; adding it would red every ordinary source commit (research P2). This job must not
    regenerate or stage it either.

    Do NOT fold docs-guard into the `drift` job. Contract-drift stays LEADING and authoritative
    (D-13); a separate job is what keeps the two failures distinguishable in CI output, and it
    mirrors the existing `emit-drift` / `stale-derived` / `workspace` separation.

    Append `docs-guard` to the `gate.needs` list at ci.yml:285. Without that entry the job runs but
    cannot block, which is the difference between a gate and a decoration.

    Add a leading comment block above the job, matching the file's existing commentary style,
    stating: the three exit codes and what each means for the operator (0 clean; 1 broken/stale-
    required/uncovered regression — fix the docs or the ledger; 3 registry invalid — fix the
    registry, not the docs); that contract-drift and golden remain leading, so a drifted-contract
    binding reports `SUPPRESSED` rather than failing twice; and that the registry is a `.toml`
    instance in `docs/`, so the `contract-check` job's schema/instance pairing does NOT cover it and
    `tools.docs_guard` is its only validator (D-16/A7).

    Preserve the file's security posture exactly: pinned actions as already written, top-level
    `permissions: contents: read` untouched, and no `${{ github.event.* }}` interpolation into any
    `run` shell.
  </action>
  <verify>
    <automated>uv run python -c "import sys,pathlib; t=pathlib.Path('.github/workflows/ci.yml').read_text(); sys.exit(0 if 'docs-guard' in t.split('needs: [')[1].split(']')[0] else 1)"</automated>
  </verify>
  <acceptance_criteria>
    `.github/workflows/ci.yml` parses as valid YAML (`uv run python -c "import
    yaml,pathlib;yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text())"` succeeds, or
    an equivalent check if `yaml` is unavailable). The `docs-guard` job exists as a separate job and
    appears in `gate.needs`. `grep -n 'docs-staleness' .github/workflows/ci.yml` returns nothing (the
    queue never joins `stale-derived`). `grep -n 'fetch-depth' .github/workflows/ci.yml` returns
    nothing new. `git diff .github/workflows/ci.yml` shows only the added job, the added `gate.needs`
    entry, and the comment block — no change to any existing job, action pin, or the `permissions`
    block. Both of the job's commands succeed locally.
  </acceptance_criteria>
  <done>A separate `docs-guard` job runs the gate and its tests in CI and is joined to the fan-in gate, without touching `stale-derived`, `drift`, or the workflow's security posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/doc-dependencies.toml` + `docs/.docs-review-ledger.toml` → the gate | Plain reviewed config, agent-writable by design (D-01) so `/adopt` can propose rows in Phase 29. Human review is the control, not a write-side deny. |
| Seed ledger disposition → gate greenness | A seeded row makes a binding green. Its disposition must be one a first-ever ledger can honestly carry. |
| `ci.yml` → branch protection | The `gate` job is the single required status check; a job absent from `gate.needs` cannot block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-40 | Repudiation | a seed row claiming `updated` on a first-ever ledger, which no history can verify | mitigate | Every seed row uses the content-bound `reviewed-no-change` / `REVIEWED_STILL_CURRENT`; the reasoning is a comment in the ledger so the next seeder does not reach for `updated`. Asserted by a grep in the acceptance criteria. |
| T-28-41 | Elevation of Privilege | a padded `uncovered_max` silently permitting regression | mitigate | Set to the EXACT live count from the 28-05 SUMMARY, re-derived if the count shifted, with both numbers recorded. |
| T-28-42 | Spoofing (false assurance) | a job that runs but cannot block | mitigate | `docs-guard` appended to `gate.needs`; the automated verify parses the `needs` list for the entry. |
| T-28-43 | Denial of Service | the derived queue joining `stale-derived` and redding every source commit | mitigate | Asserted absent from `ci.yml`; the job neither regenerates nor stages the queue (research P2). |
| T-28-44 | Information Disclosure | wall-clock, reviewer identity, or a model identifier entering a committed ledger | mitigate | `ledger.py`'s allowlist rejects any extra key by name; a grep in the acceptance criteria backs it. |
| T-28-45 | Tampering | GEN-04 breach — a core registry naming `examples/` | mitigate | No `examples/` token; `test_core_no_example_dep.py` run as an automated gate. The instance overlay is the D-14 seam, deliberately unbuilt. |
| T-28-46 | Tampering | CI security posture weakened by the new job | mitigate | Pinned actions copied verbatim, `permissions: contents: read` untouched, no `${{ github.event.* }}` in any `run`; the diff is asserted to contain only additions. |
| T-28-47 | Denial of Service | double-red on every contract change | mitigate | Separate job (never folded into `drift`) plus the D-13 suppression already implemented in plan 28-05; the job's comment block tells the operator which gate leads. |
| T-28-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new packages; the job reuses the existing pinned `setup-uv` action and `uv sync --all-packages`. |
</threat_model>

<verification>
- `uv run python -m tools.docs_guard` exits 0 against the committed registry + ledger.
- `uv run pytest tools/docs_guard tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0.
- `.github/workflows/ci.yml` parses; `docs-guard` is present as a job AND in `gate.needs`.
- `grep -n 'docs-staleness' .github/workflows/ci.yml` is empty.
- `git status --porcelain tools/memory_regen harness .opencode .claude contracts` is empty.

Do NOT run the full suite in flight: plan 28-06 is concurrently editing `tools/memory_regen/inject.py`
and re-emitting the runtime trees in this same wave. The fan-in belongs to plan 28-08.
</verification>

<success_criteria>
- A minimal core registry exists whose bindings are honestly paired and which survives all five
  DOCSUP-01 rejections.
- A committed ledger exists carrying only the permitted keys, with content-bound dispositions and an
  exact `uncovered_max`.
- A separate `docs-guard` CI job runs the gate and its tests and is joined to the fan-in gate,
  leaving `drift`, `stale-derived`, and the workflow's security posture untouched.
</success_criteria>

<output>
Create `.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-07-SUMMARY.md` when
done. It MUST contain: a one-sentence justification per seeded binding for why its sources genuinely
determine its target; the exact `uncovered_max` value and the live count it came from (both numbers
if it shifted); and the verbatim `uv run python -m tools.docs_guard` output.
</output>
