---
phase: 53-managed-adopt-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py
  - contracts/harness/adoption/manifest.schema.json
  - contracts/.hashes/manifest.json
  - .memory/derived/contracts-index.md
  - docs/reference/manifest.md
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
autonomous: false
requirements: [MONO-12]

must_haves:
  truths:
    - "The manifest contract can express the disposition value `update` and the shape of an installed-file record BEFORE any code emits either — contract entry first, never after."
    - "The constitution-plane edit is performed by the human-authorized path (GOLDEN_APPROVE_HUMAN), never by an agent write; `contract_guard` denies `contracts/**` unconditionally to agents."
    - "The contract COUNT stays exactly 6 — one enum value and one optional array were added to an EXISTING schema; no sibling `installed.schema.json` was created (ROADMAP.md:231 binding boundary, re-checked by Phase 54 SC-2)."
    - "The RFC 8785 drift gate is green only because the committed baseline was re-derived in the same authorized action."
    - "The edit is backward compatible: every Phase-52-era manifest document (no `installed` key) still validates unchanged."
    - "Every derived artifact carrying the old manifest schema hash is regenerated, so the derived plane is not left stale."
  artifacts:
    - path: "contracts/harness/adoption/manifest.schema.json"
      provides: "dispositionEnum with 7 values incl. `update`; $defs.installedRecord; optional top-level installed[]"
      contains: "installedRecord"
    - path: "contracts/.hashes/manifest.json"
      provides: "Re-derived JCS SHA-256 baseline for the changed schema"
    - path: ".planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py"
      provides: "Idempotent, human-runnable off-plane applier for the constitution-plane edit"
  key_links:
    - from: "contracts/harness/adoption/manifest.schema.json"
      to: "contracts/.hashes/manifest.json"
      via: "tools.contract_hash.hash.write_manifest() rebaseline in the same invocation"
      pattern: "manifest.schema.json"
    - from: "contracts/.hashes/manifest.json"
      to: ".memory/derived/contracts-index.md"
      via: "tools.memory_regen.contracts_index regeneration"
      pattern: "manifest.schema.json"
---

<objective>
Land the ONE contract change Phase 53 is permitted, on the ONE contract file it is permitted to
touch: `contracts/harness/adoption/manifest.schema.json` gains the 7th `dispositionEnum` value
`update`, a `$defs.installedRecord` definition, and one OPTIONAL top-level `installed[]` array —
through the full contract-first chain (contract entry → `/contract-check` → paired baseline +
derived-plane regeneration).

Purpose: Plans 02/03 emit `update` and read/write installed records. `manifest.schema.json` is
`additionalProperties: false` with an enumerated disposition list, so code cannot land first
without turning every artifact-conformance test red. Contract before code, always.

**No new contract file.** `.planning/ROADMAP.md:231` binds the whole v2.7 milestone to
"contracts 6 ... do not increase" and Phase 54 SC-2 (`ROADMAP.md:314`) re-checks it. A sibling
`installed.schema.json` would make 7 and red Phase 54 before it runs. 53-RESEARCH.md's Open
Question #1 is RESOLVED in favour of extending this file; do not resurrect the sibling schema.

Output: the extended schema, a re-derived hash baseline, regenerated derived artifacts, and an
idempotent off-plane script that performed the write under human authorization.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/53-managed-adopt-updates/53-CONTEXT.md
@.planning/phases/53-managed-adopt-updates/53-VALIDATION.md
@AGENTS.md
@harness/commands/new-contract-rule.md
@harness/commands/contract-check.md
@harness/skills/data-contracts/SKILL.md
</context>

<parallel_execution_note>
This plan is ALONE in wave 1 and must fully land — including the human checkpoint — before ANY
wave-2 work starts. Between Task 2 (schema + hash land) and Task 3 (derived regeneration),
`tools/memory_regen/tests/test_contracts_index.py` is RED **by construction**: its syrupy snapshot
embeds the manifest schema's old hash prefix `c10b9b9e22d7`. Nothing else may run inside that
window; a sibling plan seeing that red would be tempted to weaken an assertion.
</parallel_execution_note>

<interfaces>
All commands run from `/Users/hyojung/Desktop/2026/lifetimeworkflow`.

**Exact pre-state being edited** — `contracts/harness/adoption/manifest.schema.json`:

`$defs.dispositionEnum.enum` currently holds, in this order:
`create`, `preserve`, `conflict`, `marker-merge`, `derived-regenerate`, `human-ratification-required`.
Its `description` is currently the literal string:
`Exactly these 6 values (D-03/D-04's total rule chain), no more, no fewer.`

`$defs` currently holds exactly: `evidenceRef`, `dispositionEnum`, `dispositionRecord`,
`excludedDestinationRecord`. Top-level `properties` currently holds exactly: `target_ref`,
`dispositions`, `excluded`; `required` is `["target_ref", "dispositions", "excluded"]`;
`additionalProperties` is `false`.

The pre-change JCS digest recorded in `contracts/.hashes/manifest.json` for this schema is
`c10b9b9e22d7002bf9527e5b648b1bb59c7c27126ac76b5912b06d7149a25471` (prefix `c10b9b9e22d7`, which is
also what `.memory/derived/contracts-index.md:8` carries today).

**The destination-path pattern** reused verbatim by `installedRecord.destination` (identical to
`dispositionRecord.properties.destination.pattern`, never retyped by hand into a different form):
`^(?!/)(?!.*(?:^|/)\.\.(?:/|$)).+$`

**Shipped entry points — never re-implement any of them:**
- rebaseline: `tools.contract_hash.hash.write_manifest()` (call it from the script; `schema_hash(path)` for digests)
- drift gate: `uv run python -m tools.contract_drift.drift` (exit 0 == live tree matches the baseline)
- instance validation: the `/contract-check` § 1 loop (`harness/commands/contract-check.md`)
- derived regeneration: `uv run python -m tools.docs_sync` and
  `uv run python -m tools.memory_regen.contracts_index`

`contracts/**` is on the constitution plane (`tools/hooks/contract_guard.py`
`CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**", "docs/glossary.md"]`), which denies agent
Write/Edit unless `GOLDEN_APPROVE_HUMAN` is non-empty. An agent must NEVER set or fabricate that
token. The write happens through a human-run script, exactly as `.planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py` did.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the idempotent off-plane manifest-schema applier and prove it is write-free today</name>
  <files>.planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py</files>
  <read_first>
    .planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py (the EXACT precedent this script mirrors: --check default / --write, hard-coded pre-state, exit 2 on surprise, rebaseline via the shipped hasher, `REPO_ROOT = Path(__file__).resolve().parents[4]`),
    contracts/harness/adoption/manifest.schema.json (the full pre-state: $defs member set, dispositionEnum list and description, top-level properties/required/additionalProperties, and dispositionRecord.destination's pattern string),
    tools/contract_hash/hash.py (`schema_hash`, `write_manifest`, `MANIFEST_PATH` — reused, never re-derived),
    tools/contract_drift/drift.py (module docstring: an additive enum value + an optional property is classified `non-breaking`),
    tools/hooks/contract_guard.py (`CONSTITUTION_GLOBS`, `APPROVAL_ENV` — why this script exists at all),
    harness/commands/new-contract-rule.md (the repo's own contract-change procedure — a COMMAND, not a skill)
  </read_first>
  <action>
    Create `apply-manifest-update-enum.py`: stdlib-only, two modes, `--check` (default, writes
    nothing) and `--write`. Model it line-for-line on `apply-inventory-enum.py` — same argparse
    shape, same `check_state()` returning `"pre"`/`"post"` and `sys.exit(2)` on any other shape,
    same `json.dumps(..., indent=2, ensure_ascii=False) + "\n"` re-serialization, same deferred
    `from tools.contract_hash.hash import schema_hash, write_manifest` import inside the `--write`
    branch. `REPO_ROOT` is `Path(__file__).resolve().parents[4]`.

    **Pre-state assertion (runs in BOTH modes, before anything else).** `"pre"` requires ALL of:
    `$defs.dispositionEnum.enum` equals the exact 6-value list in `<interfaces>`; `$defs.dispositionEnum.description`
    equals the exact 6-values sentence in `<interfaces>`; `"installedRecord"` is absent from `$defs`;
    `"installed"` is absent from top-level `properties`. `"post"` requires ALL of: the enum equals
    that list plus `"update"`; `"installedRecord"` is in `$defs`; `"installed"` is in `properties`.
    Anything else → exit 2 with a message naming what was found. Never "repair" a surprise shape.

    **The edit, in `--write` mode, is exactly these four mutations and no others:**
    1. Append the literal `"update"` to `$defs.dispositionEnum.enum`, AFTER `"human-ratification-required"`.
    2. Replace `$defs.dispositionEnum.description` with:
       `Exactly these 7 values, no more, no fewer: D-03/D-04's total rule chain plus MONO-12's `update` — fired when the target's current hash equals the recorded installed_sha256 AND the recomputed harness payload hash now differs (a harness-side move, not a target-side edit).`
    3. Add `$defs.installedRecord`: `{"type": "object", "additionalProperties": false, "required": ["destination", "installed_sha256", "batch_id"], "properties": {...}}` where
       `destination` is `{"type": "string", "minLength": 1, "pattern": <the pattern string copied verbatim from dispositionRecord.properties.destination.pattern, READ from the loaded schema at runtime rather than retyped>, "description": "A repo-relative POSIX path in the TARGET tree that /adopt itself wrote."}`;
       `installed_sha256` is `{"type": "string", "pattern": "^[0-9a-f]{64}$", "description": "sha256 of the bytes /adopt actually wrote at this destination, AS WRITTEN (post-splice for harness/project.toml). Exactly one hash is stored: the source side is recomputed every run by destinations.harness_proposed_hashes(), so a second source_sha256 field would only go stale."}`;
       `batch_id` is `{"type": "string", "minLength": 1, "description": "The adoption batch that wrote these bytes."}`.
       Insert it after `excludedDestinationRecord` so `$defs` key order stays stable.
    4. Add the top-level OPTIONAL property `installed`:
       `{"type": "array", "items": {"$ref": "#/$defs/installedRecord"}, "description": "Optional (MONO-12): the installed-file records this manifest's dispositions were resolved against, copied verbatim from the target's .harness/adoption/installed.json. Absent on a first-ever adopt and on every Phase-52-era manifest — this array is never added to `required`, so older documents stay valid."}`.
       Do NOT add `"installed"` to the top-level `required` list. Do NOT change `additionalProperties`.

    Then call `write_manifest()` once and print the old and new `schema_hash` of
    `contracts/harness/adoption/manifest.schema.json`.

    Re-running `--write` after a successful run must change nothing and exit 0.

    Do NOT edit `contracts/harness/adoption/manifest.schema.json` from this task — Task 2 is the
    authorized write.
  </action>
  <verify>
    <automated>uv run python .planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py --check || exit 1; git diff --quiet -- contracts/ || exit 1; uv run python -m tools.contract_drift.drift || exit 1; test "$(find contracts -name '*.schema.json' | wc -l | tr -d ' ')" = "6" || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `--check` exits 0 and `git diff --quiet -- contracts/` still succeeds afterwards (check mode provably wrote nothing).
    - Every write call (`write_text`/`write_bytes`/`open(..., "w")`) in the script is reachable only under the `--write` guard; confirm by reading the source and by the `git diff --quiet` above.
    - **Mutation check (record the observed output in the SUMMARY):** copy the schema to a scratch path, rename one enum value in the copy, point the script's `SCHEMA_PATH` at it (or run a small harness that calls `check_state` on the mutated dict), and observe **exit 2**, not exit 0. A script that "succeeds" against an unexpected schema is worthless. Restore afterwards; `git diff --quiet -- contracts/` must still pass.
    - **Mutation check 2:** feed `check_state` a dict that has the new enum value but NOT `installedRecord`, and observe exit 2 — a half-applied schema must not be reported as `"post"`.
    - `uv run python -m tools.contract_drift.drift` exits 0 (tree still baseline-clean before Task 2).
    - `find contracts -name '*.schema.json' | wc -l` prints `6`.
  </acceptance_criteria>
  <done>The applier exists, is provably write-free in check mode, refuses both an unexpected and a half-applied schema shape, and the contracts tree is untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Human runs the constitution-plane write</name>
  <files>contracts/harness/adoption/manifest.schema.json, contracts/.hashes/manifest.json</files>
  <read_first>
    .planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py (the exact edit it will make),
    contracts/harness/adoption/manifest.schema.json (the pre-write state),
    tools/hooks/contract_guard.py (why this write needs human authorization)
  </read_first>
  <action>Pause and hand control to the human. Do not write to `contracts/**` from any agent tool call and never set or fabricate `GOLDEN_APPROVE_HUMAN`. Present the commands in `<how-to-verify>`, wait for the resume signal, then re-read both files to confirm what landed.</action>
  <verify>
    <automated>git diff --name-only -- contracts/ | LC_ALL=C sort | tr '\n' ' ' | grep -Fq 'contracts/.hashes/manifest.json contracts/harness/adoption/manifest.schema.json' || exit 1; test "$(find contracts -name '*.schema.json' | wc -l | tr -d ' ')" = "6" || exit 1; grep -Fq '"update"' contracts/harness/adoption/manifest.schema.json || exit 1; grep -Fq 'installedRecord' contracts/harness/adoption/manifest.schema.json || exit 1; grep -Fq 'c10b9b9e22d7002bf9527e5b648b1bb59c7c27126ac76b5912b06d7149a25471' contracts/.hashes/manifest.json && exit 1; uv run python -m tools.contract_drift.drift || exit 1; exit 0</automated>
  </verify>
  <what-built>An idempotent script that appends exactly one enum value (`update`), rewrites that enum's description, adds `$defs.installedRecord`, and adds ONE optional top-level `installed[]` array to `contracts/harness/adoption/manifest.schema.json` — then re-derives `contracts/.hashes/manifest.json` via the shipped `tools.contract_hash.hash.write_manifest()` in the same invocation. No new contract file: the count stays 6.</what-built>
  <how-to-verify>
    1. Review the pending change (writes nothing):
       `uv run python .planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py --check`
    2. Apply it under human authorization, from `/Users/hyojung/Desktop/2026/lifetimeworkflow`:
       `GOLDEN_APPROVE_HUMAN=1 uv run python .planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py --write`
    3. Confirm the diff is exactly two files: `git diff --stat -- contracts/` shows only
       `contracts/harness/adoption/manifest.schema.json` and `contracts/.hashes/manifest.json`.
    4. Confirm the contract COUNT is still 6: `find contracts -name '*.schema.json' | wc -l`.
    5. Confirm the drift gate is green: `uv run python -m tools.contract_drift.drift`.
  </how-to-verify>
  <resume-signal>Type "approved" once the script has been run and the diff shows exactly those two files, or describe what differed.</resume-signal>
  <acceptance_criteria>
    - `git diff --name-only -- contracts/` lists exactly `contracts/.hashes/manifest.json` and `contracts/harness/adoption/manifest.schema.json` — no third file, and specifically no `installed.schema.json`.
    - `find contracts -name '*.schema.json' | wc -l` prints `6`.
    - The `manifest.schema.json` digest in `contracts/.hashes/manifest.json` differs from the pre-change value `c10b9b9e22d7002bf9527e5b648b1bb59c7c27126ac76b5912b06d7149a25471` (a hash that did NOT move means the edit never landed).
    - `python -c "import json;json.load(open('contracts/harness/adoption/manifest.schema.json'))"` succeeds, and top-level `required` still equals `["target_ref","dispositions","excluded"]` (the new array is OPTIONAL).
  </acceptance_criteria>
  <done>The schema carries `update`, `installedRecord`, and the optional `installed[]`, and the hash baseline was re-derived in the same authorized action.</done>
</task>

<task type="auto">
  <name>Task 3: Run /contract-check in full, prove backward compatibility, regenerate the derived plane</name>
  <files>.memory/derived/contracts-index.md, docs/reference/manifest.md, tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr</files>
  <read_first>
    contracts/harness/adoption/manifest.schema.json (post-write state),
    contracts/.hashes/manifest.json (the re-derived baseline),
    harness/commands/contract-check.md (§ 1 the check-jsonschema instance loop and its presence-safe SKIP semantics; § 2 the drift gate — BOTH halves run here, verbatim),
    tools/memory_regen/contracts_index.py (the hash-prefix row format that just went stale),
    tools/memory_regen/tests/test_contracts_index.py (the syrupy snapshot embedding the hash prefix),
    tools/docs_sync/generate.py (how docs/reference/manifest.md is derived from the schema),
    tools/adoption_apply/tests/test_manifest_schema_conformance.py (the existing conformance suite that must stay green unchanged — the backward-compatibility proof),
    harness/commands/refresh-memory.md (the exact regeneration argv this repo ships)
  </read_first>
  <action>
    First run `/contract-check` in FULL, both halves, and capture the literal output for the SUMMARY.
    Run it as the command if the runtime exposes it; otherwise run the two literal steps it wraps,
    in order: (1) the presence-safe instance loop from `harness/commands/contract-check.md` § 1;
    (2) `uv run python -m tools.contract_drift.drift`. `manifest.schema.json` has no sibling
    instance file, so (1) is expected to print its documented `SKIP: … no-op.` line and exit 0 —
    record that literal line; a documented no-op that RAN is different from a gate that was skipped.

    Then prove backward compatibility explicitly: validate a Phase-52-era manifest document (one
    with `target_ref`/`dispositions`/`excluded` and NO `installed` key — use a committed fixture or
    the existing conformance suite's own document) against the NEW schema and confirm it still
    validates. This is the load-bearing claim behind making `installed[]` optional.

    Then run the shipped derived-plane regenerators — never hand-edit their outputs:
    `uv run python -m tools.docs_sync` and `uv run python -m tools.memory_regen.contracts_index`.

    The contracts-index syrupy snapshot embeds each contract's first-12-hex hash prefix, so it is
    stale by construction. Update it with
    `uv run pytest tools/memory_regen/tests/test_contracts_index.py --snapshot-update`, then re-run
    that file WITHOUT the flag to confirm it passes on its own. Do the same for any docs_sync
    snapshot that reds.

    Then run the whole suite. Do NOT touch `tools/adoption_scan/**` or `tools/adoption_apply/**`
    here — Plans 02/03 own the code; this plan's suite run must show that the contract edit alone
    changed no behavior.
  </action>
  <verify>
    <automated>shopt -s nullglob globstar 2>/dev/null; fail=0; any=0; for schema in contracts/**/*.schema.json; do base="${schema%.schema.json}"; for inst in "$base".yaml "$base".yml "$base".json; do [ -f "$inst" ] || continue; any=1; uv run check-jsonschema --schemafile "$schema" "$inst" || fail=1; done; done; test "$fail" -eq 0 || exit 1; uv run python -m tools.contract_drift.drift || exit 1; test "$(find contracts -name '*.schema.json' | wc -l | tr -d ' ')" = "6" || exit 1; uv run python -c '
import json, sys
from jsonschema import Draft202012Validator
S = json.load(open("contracts/harness/adoption/manifest.schema.json"))
assert S["$defs"]["dispositionEnum"]["enum"][-1] == "update", S["$defs"]["dispositionEnum"]["enum"]
assert len(S["$defs"]["dispositionEnum"]["enum"]) == 7
assert "installedRecord" in S["$defs"]
assert "installed" in S["properties"] and "installed" not in S["required"]
legacy = {"target_ref": "unknown", "dispositions": [{"destination": "a.py", "disposition": "preserve"}], "excluded": []}
errs = list(Draft202012Validator(S).iter_errors(legacy))
assert not errs, "BACKWARD COMPAT BROKEN: " + str(errs[0].message)
withnew = dict(legacy); withnew["installed"] = [{"destination": "a.py", "installed_sha256": "0"*64, "batch_id": "b1"}]
errs = list(Draft202012Validator(S).iter_errors(withnew))
assert not errs, "installed[] rejected: " + str(errs[0].message)
bad = dict(legacy); bad["installed"] = [{"destination": "a.py", "installed_sha256": "nothex", "batch_id": "b1"}]
assert list(Draft202012Validator(S).iter_errors(bad)), "installedRecord accepted a non-sha256 hash"
print("schema shape + backward compat + negative case OK")
' || exit 1; uv run python -c '
import json, sys
KEY = "contracts/harness/adoption/manifest.schema.json"
want = json.load(open("contracts/.hashes/manifest.json"))[KEY][:12]
rows = [l for l in open(".memory/derived/contracts-index.md") if KEY in l]
if not rows: sys.exit("contracts-index.md has no row for " + KEY)
if want not in rows[0]: sys.exit("hash prefix DISAGREEMENT: manifest says " + want + " but row is: " + rows[0].strip())
print("hash agreement OK: " + want)
' || exit 1; uv run pytest -q || exit 1; git diff --quiet -- tools/adoption_scan/ tools/adoption_apply/ || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - BOTH `/contract-check` halves ran and the SUMMARY quotes the instance loop's literal `SKIP: … no-op.` line AND the drift gate's exit 0. Naming only `tools.contract_drift.drift` does not satisfy the validation contract.
    - `dispositionEnum.enum` has exactly 7 values ending in `update`; `$defs.installedRecord` exists; `installed` is in `properties` and NOT in `required` — all machine-asserted in `<verify>`.
    - **Backward compatibility is asserted positively AND negatively:** a Phase-52-shaped manifest with no `installed` key validates clean, a manifest carrying a well-formed `installed[]` validates clean, and a manifest whose `installed_sha256` is not 64 hex chars is REJECTED. The third assertion is the anti-vacuous guard — a schema that accepts everything would pass the first two.
    - **Mutation check (record in the SUMMARY):** hand-edit one hex char of the manifest-schema row in `.memory/derived/contracts-index.md`, observe the hash-agreement gate in `<verify>` go RED, revert.
    - `uv run pytest tools/memory_regen/tests/test_contracts_index.py -q` passes WITHOUT `--snapshot-update`.
    - `uv run pytest -q` fully green; `git diff --quiet -- tools/adoption_scan/ tools/adoption_apply/` succeeds (no code smuggled in ahead of the contract).
    - `find contracts -name '*.schema.json' | wc -l` is `6`.
  </acceptance_criteria>
  <done>The contract-first chain is complete and green: /contract-check run in full, schema extended in place, backward compatibility proven both ways, hash rebaselined, derived plane regenerated, whole suite passing, and no code emits `update` yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → constitution plane (`contracts/**`) | A CODEOWNERS-gated, hash-baselined plane an agent must not write unilaterally. |
| schema edit → drift/derived baselines | A schema change unpaired with its baseline lets later drift pass unnoticed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-01 | Elevation of Privilege | `contracts/harness/adoption/manifest.schema.json` write | high | mitigate | The write happens only via Task 2's `checkpoint:human-action` under a human-supplied `GOLDEN_APPROVE_HUMAN`; Task 1 is provably write-free and the agent never sets or fabricates the token. |
| T-53-02 | Tampering | Schema edit exceeding the four sanctioned mutations | medium | mitigate | The applier asserts an exact pre-state (enum list, enum description, absence of `installedRecord`, absence of `installed`), applies exactly four mutations, exits 2 on any surprise, and Task 2's acceptance limits the diff to two files via `git diff --name-only`. |
| T-53-03 | Repudiation | Hash change without a paired baseline/derived update | high | mitigate | Rebaseline runs inside the same script invocation; Task 3 gates on the full `/contract-check` plus a machine-checked hash-prefix agreement between the baseline and the derived contracts-index. |
| T-53-04 | Tampering | Contract-count growth breaking the milestone boundary | medium | mitigate | Every task's `<verify>` asserts `find contracts -name '*.schema.json' \| wc -l` == 6; no sibling `installed.schema.json` is authored anywhere in this phase. |
| T-53-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages (53-RESEARCH.md: zero new dependencies); the Package Legitimacy Gate has nothing to audit. |
</threat_model>

<verification>
- `/contract-check` both halves exit 0 (instance loop with its documented SKIP; drift gate green).
- `uv run pytest -q` fully green.
- `find contracts -name '*.schema.json' | wc -l` == 6.
- `git diff --name-only` for this plan touches no file under `tools/adoption_scan/` or `tools/adoption_apply/` (except the memory_regen snapshot).
</verification>

<success_criteria>
`manifest.schema.json` can express `update` and the installed-record shape; `/contract-check` ran in
full; backward compatibility is proven positively and negatively; the drift gate, derived plane, and
whole suite are green; the contract count is still 6; and no code emits the new value yet — Plan 02
is unblocked and contract-first order is provably intact.
</success_criteria>

<output>
Create `.planning/phases/53-managed-adopt-updates/53-01-SUMMARY.md` when done.
</output>
