---
context: phase
phase: 53-managed-adopt-updates
milestone: v2.7
task: 0
total_tasks: 12
status: planned_not_started
last_updated: 2026-08-02T14:19:59.036Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through actual failure or explicit user
> correction this session. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **Scope expansion past MONO-12** — During planning I added a `conflicts.json`
      artifact, a `source_sha256` field, and a distinct exit code 3. None is required by any
      MONO-12 success criterion. The user stopped the session and was right. Structural mitigation:
      every plan carries a `grep` guard asserting those three strings never appear in
      `tools/adoption_apply/` (see 53-03:249, 53-03:363, 53-04:274). **Do not weaken or remove
      those guards.** Before adding any field, artifact, flag, or file, name which of the three
      success criteria requires it. If you cannot, do not add it.

- [ ] CONSTRAINT: **contracts/ cannot be written by an agent** — `tools/hooks/contract_guard.py`
      structurally refuses it and CODEOWNERS gates it. Plan 53-01 Task 2 is a HUMAN action.
      Structural mitigation: 53-01 is `autonomous: false` and every later plan carries
      `depends_on: ["53-01"]`, so the gate cannot be skipped by accident. Do not attempt an
      Edit/Write against `contracts/` — author the off-plane script and stop.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Scope creep past the requirement | Adding artifacts/fields/exit-codes that no success criterion asks for, justified as "nice to have" or "machine-checkable" | blocking | Grep guards in 53-03/53-04 acceptance criteria; name the SC that requires any addition before making it |
| Checks that cannot fail | This repo's signature defect. Here specifically: a suite testing only the **no-op** passes even if the `update` branch is deleted entirely. Phase 52's CR-03 self-caught three vacuous tests. | blocking | 53-VALIDATION.md's paired positive/negative table; five named mutations in 53-03:330 plus the rule that any assertion staying GREEN under mutation must be REPLACED and re-mutated |
| Claiming green on pytest alone | Carried from v2.6: `uv run pytest` is NOT the CI gate. CI caught 23 ruff violations after the tree was reported green several times. | advisory | Run `uv run python -m tools.ruff_baseline` before claiming green |
| Trusting RESEARCH.md on scope | `53-RESEARCH.md` was written BEFORE the scope cut and still describes `conflicts.json`, `source_sha256`, and exit 3. | advisory | **53-CONTEXT.md wins over 53-RESEARCH.md on any scope question.** The cut items are marked `[SCOPE-CUT 2026-08-01]` in CONTEXT.md. |

<current_state>
Phase 53 is **fully planned and verified. Zero execution has happened.** Working tree clean,
everything committed through `1186547`.

Artifacts on disk: `53-CONTEXT.md`, `53-RESEARCH.md`, `53-VALIDATION.md`, and
`53-01..04-PLAN.md`. No `scripts/` directory yet — 53-01 Task 1 creates it.

`.planning/STATE.md` records the phase as planned, 4 plans, ready to execute.
</current_state>

<completed_work>

- Smart discuss — 4 grey areas, 16 decisions, all accepted (`daa37be`)
- Research — `53-RESEARCH.md` with file:line citations throughout (`9c19ad9`)
- **Scope cut** — dropped three items, switched from adding a contract to extending one (`61bdc79`)
- Validation strategy — `53-VALIDATION.md`, anti-vacuous test contract (`a7ac3d8`)
- Planning — 4 plans, 4 sequential waves (`1186547`)
- Plan verification — **run inline**, passed. The `gsd-plan-checker` subagent died on a session
  limit before reading any file, so every check assigned to it was performed directly: sequencing
  gate, contract-count gate, cut-item leakage scan, splice-fix enforcement, anti-vacuous pairing,
  MONO-12 coverage. **The plans are verified — do not treat them as unchecked.**
</completed_work>

<remaining_work>

All 12 tasks. Waves are strictly sequential (`files_modified` overlap plus the constitution gate
forbid parallelism).

| Wave | Plan | Tasks | Autonomous |
|------|------|-------|------------|
| 1 | 53-01 | Author off-plane applier · **human runs it** · `/contract-check` + derived regen | **no** |
| 2 | 53-02 | `disposition()` step 6.5 + `build_manifest(installed=)` · `installed.py` read+write validated · paired safety tests | yes |
| 3 | 53-03 | `update` branch + six-way summary · CLI wiring + post-splice comparison · end-to-end paired coverage | yes |
| 4 | 53-04 | Phase-local re-run driver · four-cycle real-target run + evidence + disposal · docs + re-emit | yes |
</remaining_work>

<decisions_made>

- **Extend `manifest.schema.json` in place; do NOT add `installed.schema.json`.** `ROADMAP.md:229`
  binds v2.7 to "contracts 6 … do not increase" and Phase 54 SC-2 (`ROADMAP.md:313`) re-checks it.
  A sibling would make 7 and red Phase 54 before it runs. Retiring
  `contracts/sample/greeting.schema.json` to make room was priced and rejected — load-bearing
  across ~10 test modules. The original rationale for a sibling ("keeps the manifest hash clean")
  was void anyway: the `update` enum changes that hash regardless.
- **Store exactly ONE hash: `installed_sha256`.** `draft` already recomputes the source hash every
  run and already reconstructs the full payload including the splice, so "did the source move?" is
  answered by comparing the recomputed payload hash. A second stored hash only goes stale.
- **No `conflicts.json`, no `conflicts[]` array** — the drafted `manifest.json` already carries a
  `disposition: "conflict"` row per diverged destination and is already schema-validated and
  batch-local. That IS the report SC-3 asks for. Apply adds only a stderr summary.
- **No exit code 3** — no success criterion mentions exit codes and no caller reads them by number.
- **The manifest must compare `harness/project.toml` against the POST-splice payload hash.** See
  Blockers below — this is the single most load-bearing implementation detail in the phase.
- **Skipped `gsd-pattern-mapper`** — `53-CONTEXT.md`'s `<code_context>` already names every analog.
</decisions_made>

<blockers>

- **Human action, blocking:** run
  `.planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py` (authored by
  53-01 Task 1). It edits `contracts/harness/adoption/manifest.schema.json` to add the 7th
  `dispositionEnum` value `update` and the optional top-level `installed[]` array. Nothing in
  waves 2–4 may land until this runs and `python -m tools.contract_drift` is clean.

- **Not a blocker, but do not re-litigate:** the plan-checker subagent died on a session limit.
  Verification was done inline and passed.

- **The trap that would silently break SC-2.** `tools/adoption_apply/cli.py:292` writes
  `harness/project.toml` as `harness_payload + b"\n" + sidecar_bytes`. If the manifest keeps
  comparing that destination against the RAW `harness_proposed_hashes()` value, post-splice bytes
  never equal the proposed hash, `preserve` can never fire, and the **new** `update` step fires on
  **every** re-run — a permanent rewrite that silently breaks the no-op. WR-08 would not die, it
  would invert. The fix is pinned in 53-03 Task 2: derive the sidecar BEFORE `build_manifest` and
  override `proposed_hashes["harness/project.toml"]` with the sha256 of the exact payload apply
  will write, with the splice expression factored into one helper (`_spliced_project_toml`) so the
  two call sites cannot drift. Mutation check (a) requires the WR-08 test to red if the override is
  removed.
</blockers>

## Required Reading (in order)

1. `.planning/phases/53-managed-adopt-updates/53-CONTEXT.md` — the locked decisions. **Authoritative
   over RESEARCH.md on any scope question.** The `[SCOPE-CUT 2026-08-01]` markers say what was
   removed and why.
2. `.planning/phases/53-managed-adopt-updates/53-VALIDATION.md` — the anti-vacuous test contract.
   The paired positive/negative table is blocking.
3. `.planning/phases/53-managed-adopt-updates/53-01-PLAN.md` — start here; it is the human gate.
4. `.planning/phases/53-managed-adopt-updates/53-RESEARCH.md` — file:line citations and the threat
   table. **Read for mechanism, not for scope** — it predates the cut.
5. `.planning/ROADMAP.md:224-240` and `:305-316` — the v2.7 binding boundary (19 commands, 8 skills,
   6 contracts) and Phase 54's re-check of it.
6. `AGENTS.md` (root) — contract-first, two-plane memory, polyglot boundary non-negotiables.

## Infrastructure State

- Working tree: **clean**, all work committed through `1186547`.
- Branch: `claude/data-pipeline-harness-8aypct`.
- No background processes, servers, or watchers running.
- No FeedbackOps worktree currently provisioned — 53-04 Task 2 provisions and disposes its own,
  following the Phase 52 sequence.
- .NET SDK still absent in this container (BOOT-01, egress-denied). Irrelevant to this phase —
  it is Python-only.
- Live surface at pause: 19 commands, 8 skills, **6 contracts**. Phase 53 must leave all three
  unchanged.

<context>
The phase turns on one idea. `/adopt` starts writing a target-side
`.harness/adoption/installed.json` recording ONE hash per managed file — the bytes as actually
written, post-splice. That single stored hash is the whole mechanism: on a re-run it lets the tool
distinguish "the harness source moved" (→ new `update` disposition, rewrite the file) from "a human
edited the target" (→ `conflict`, leave the file byte-identical, name it on stderr). Phase 52's two
deferred findings, WR-07 and WR-08, die as side effects rather than needing their own work.

The session's real lesson was not technical. I kept adding things that sounded defensible —
a machine-checkable conflict artifact, a second hash "for clarity", a CI-friendly exit code — and
none of them was required by MONO-12. The user stopped me. The plans now encode that correction as
executable guards rather than as prose, which is the only form that survives a context reset.
</context>

<next_action>
Start with: `/gsd:execute-phase 53`.

It begins at plan 53-01 Task 1 — author
`.planning/phases/53-managed-adopt-updates/scripts/apply-manifest-update-enum.py` and prove it is
write-free against the current tree — then **stops** for the human to run it. Do not attempt to
write `contracts/` yourself.
</next_action>
