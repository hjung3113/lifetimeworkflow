---
phase: 53-managed-adopt-updates
plan: 04
type: execute
wave: 4
depends_on: ["53-03"]
files_modified:
  - .planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py
  - .planning/phases/53-managed-adopt-updates/evidence/
  - .planning/phases/53-managed-adopt-updates/53-ADOPTION-EVIDENCE.md
  - harness/commands/adopt.md
  - harness/skills/brownfield-adoption/SKILL.md
  - .claude/commands/adopt.md
  - .claude/skills/brownfield-adoption/SKILL.md
  - .opencode/command/adopt.md
  - .opencode/skill/brownfield-adoption/SKILL.md
autonomous: true
requirements: [MONO-12]

must_haves:
  truths:
    - "All three MONO-12 success criteria are reproduced against the REAL FeedbackOps target, not only against fixtures — matching the Phase 51/52 evidence discipline."
    - "The original FeedbackOps `develop` checkout is byte-unchanged before and after; only a freshly created detached worktree is ever a `--target`."
    - "The unchanged re-run's zero-byte claim is proven by a before/after tree comparison with literal captured values, never by a log line alone."
    - "The harness checkout is byte-unchanged at the end of the run — the deliberate source mutation used to force an `update` was reverted, and that is asserted, not assumed."
    - "`/adopt`'s own documentation describes re-run-as-update WITHOUT adding a sub-verb, command, or skill (NG-01)."
    - "The emitted `.claude/` and `.opencode/` trees are regenerated from `harness/`, never hand-edited."
  artifacts:
    - path: ".planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py"
      provides: "Phase-local re-run comparison driver (D-21 placement, not a tools/ module)"
    - path: ".planning/phases/53-managed-adopt-updates/53-ADOPTION-EVIDENCE.md"
      provides: "SC-1/SC-2/SC-3 verdicts with literal captured values"
      contains: "SC-3"
    - path: "harness/commands/adopt.md"
      provides: "Re-run-as-update behavior documented on the existing apply sub-verb"
      contains: "update"
  key_links:
    - from: ".planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py"
      to: "tools.adoption_apply.apply.HARNESS_MANAGED_LOCK_SIDECARS"
      via: "imported verbatim as the sidecar allowlist, never retyped"
      pattern: "HARNESS_MANAGED_LOCK_SIDECARS"
    - from: "harness/commands/adopt.md"
      to: ".claude/commands/adopt.md and .opencode/command/adopt.md"
      via: "tools.harness_emit re-emission"
      pattern: "adopt"
---

<objective>
Prove the three MONO-12 success criteria against the real target, and tell a human how the behavior
works — without growing the governed surface.

53-CONTEXT.md's "Proof & surface budget" decision is explicit: fixture tests alone are insufficient
for this phase. Phases 51 and 52 established the discipline — a real-target observation before the
claim is trusted — and this phase inherits it.

Output: a phase-local re-run driver, an evidence directory of literal captures, a
`53-ADOPTION-EVIDENCE.md` with a quoted deciding value per success criterion, and updated `/adopt`
documentation re-emitted into both runtime trees.

**Surface budget (NG-01):** no new command, skill, agent, contract, CI job, or gate. Phase-local
scripts under `.planning/phases/53-.../scripts/` do not count against the governed surface (Phase 52
precedent, D-21). Editing `harness/commands/adopt.md` and the `brownfield-adoption` skill text is
permitted; adding new ones is not.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-managed-adopt-updates/53-CONTEXT.md
@.planning/phases/53-managed-adopt-updates/53-VALIDATION.md
@.planning/phases/53-managed-adopt-updates/53-03-SUMMARY.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-ADOPTION-EVIDENCE.md
@AGENTS.md
@harness/skills/brownfield-adoption/SKILL.md
</context>

<interfaces>
All commands run from `/Users/hyojung/Desktop/2026/lifetimeworkflow`.

**Paths (Phase 52 precedent, `52-05-PLAN.md` lines 83-96):**
- original target repo — READ-ONLY, NEVER a `--target`: `/Users/hyojung/Desktop/2026/FeedbackOps`
- fresh detached worktree — the ONLY `--target`: `/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-53-rerun`
- evidence root: `.planning/phases/53-managed-adopt-updates/evidence/`

**The pipeline argv, unchanged from Phase 52:**
- discover: `uv run python -m tools.adoption_scan --target WORKTREE --out EVIDENCE/cycle-N/discover`
- draft: `uv run python -m tools.adoption_apply draft --task-dir EVIDENCE/cycle-N/draft --target WORKTREE`
- apply: `uv run python -m tools.adoption_apply apply --task-dir EVIDENCE/cycle-N/draft --batch-id ID --target WORKTREE`
  where `ID` is read from that cycle's own `status.json`.

**Provisioning/disposal sequence** — reuse Phase 52's exactly: re-read `develop`'s HEAD live
(`git -C ORIG rev-parse develop`; the Phase-52 SHA `919d152a...` is stale and must not be reused),
capture the original checkout's before-proof (`status --porcelain=v2 --untracked-files=all`,
`rev-parse HEAD`, `ls-files -s | sha256sum`, `ls-files --others --exclude-standard -z | LC_ALL=C sort -z | sha256sum`,
`worktree list`), then `git -C ORIG worktree add --detach WORKTREE SHA`. Refuse to run if the
worktree path already exists. At the end: capture the after-proof, assert byte-identity with the
before-proof, then `git -C ORIG worktree remove --force WORKTREE` and confirm it is absent from
`worktree list`.

**Batch IDs differ per cycle.** `batch_id_for` is content-derived from `(target_ref, UTC date)`, so
same-day cycles against the same worktree RESUME the same batch directory. Give each cycle its own
`--task-dir` (`EVIDENCE/cycle-N/draft`) so the four cycles' artifacts never overwrite each other.

**Sidecar allowlist:** import `HARNESS_MANAGED_LOCK_SIDECARS`/`expected_lock_sidecars` from
`tools.adoption_apply.apply` verbatim (D-21, `compare-worktree-writes.py`'s own precedent). The three
`.lock` sidecars are never unlinked (D-15), so they exist from cycle 1 onward; their MTIME is exempt
from the no-op claim, their CONTENT is not.

**Emitter:** the `.claude/` and `.opencode/` trees are generated from `harness/` by
`tools.harness_emit` (see `AGENTS.md` line 104 and `tools/harness_emit/emit-manifest.json` for the
owned-path set). Never hand-edit the emitted files; edit `harness/` and re-emit.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The phase-local re-run driver</name>
  <files>.planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py</files>
  <read_first>
    .planning/phases/52-evidence-bounded-real-target-adoption/scripts/compare-worktree-writes.py (the full script: porcelain-v2 parsing, the `expected_lock_sidecars` import, the `matches`/`unexpected_paths`/`product_code_paths` output shape, `REPO_ROOT = Path(__file__).resolve().parents[4]` — copy and adapt, do not rebuild),
    tools/adoption_apply/apply.py (`HARNESS_MANAGED_LOCK_SIDECARS`, `expected_lock_sidecars`),
    tools/adoption_apply/installed.py (`INSTALLED_REL`, `read_installed_record` — the driver reads the record to report it),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-ADOPTION-EVIDENCE.md (the evidence-file shape this phase's own record mirrors)
  </read_first>
  <action>
    Create `rerun-managed-update.py` — phase-local, NOT a `tools/` module (D-21). It compares two
    `git status --porcelain=v2 --untracked-files=all` captures of the worktree taken around one apply
    invocation, plus that cycle's `manifest.json` and the worktree's
    `.harness/adoption/installed.json`, and writes one JSON report per cycle containing:
    - `changed_paths` — the porcelain delta;
    - `expected_disposition_paths` — destinations whose manifest disposition is `create`, `update`,
      or `marker-merge` (the candidate write set — note this now includes `update`, which Phase 52's
      script did not know about);
    - `expected_lock_sidecars` — imported, never retyped;
    - `unexpected_paths` — `changed_paths` minus both allowlists and minus `INSTALLED_REL`;
    - `product_code_paths` — any changed path under `apps/`, `packages/`, or `src/`;
    - `matches` — true iff `unexpected_paths` is empty;
    - `disposition_counts` — a count per disposition value in that cycle's manifest;
    - `installed_record_sha256` and `installed_record_destinations` — so a no-op cycle's record can
      be shown byte-identical across cycles and an update cycle's shown to have advanced.

    Add a `--require-no-writes` flag that exits NON-ZERO when `changed_paths` minus the sidecar
    allowlist is non-empty. That is SC-2's negative proof as a runnable gate rather than a human
    reading a diff.

    Reuse `compare-worktree-writes.py`'s porcelain parsing verbatim rather than writing a second
    parser; if that means copying the regex and its comment, copy them and cite the source file in
    the module docstring. The script writes nothing into the target.
  </action>
  <verify>
    <automated>uv run python .planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py --help >/dev/null || exit 1; grep -q 'HARNESS_MANAGED_LOCK_SIDECARS' .planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py || exit 1; grep -q 'update' .planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py || exit 1; grep -q 'require-no-writes' .planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - The sidecar allowlist is IMPORTED from `tools.adoption_apply.apply`; the script contains no retyped `.lock` filename literals.
    - `expected_disposition_paths` includes `update` destinations — a driver that only knows Phase 52's three values would report every legitimate update as `unexpected` and make SC-2b unprovable.
    - **The `--require-no-writes` gate is proven to fire before it is trusted:** run it once against a hand-constructed pair of captures differing by one non-sidecar path and confirm a NON-ZERO exit; quote that run in the SUMMARY. A gate never observed failing is not a gate (52-05-PLAN.md's W-6 precedent).
    - The script writes nothing outside `.planning/phases/53-managed-adopt-updates/`.
  </acceptance_criteria>
  <done>A runnable comparator exists that can prove or disprove the zero-write claim, and it has been observed disproving it.</done>
</task>

<task type="auto">
  <name>Task 2: Four-cycle real-target run, evidence capture, and disposal</name>
  <files>.planning/phases/53-managed-adopt-updates/evidence/, .planning/phases/53-managed-adopt-updates/53-ADOPTION-EVIDENCE.md</files>
  <read_first>
    .planning/phases/52-evidence-bounded-real-target-adoption/52-05-PLAN.md (Task 1's provisioning + before-proof sequence and Task 3's capture discipline — argv/cwd/stdout/stderr/exit-code per invocation),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-06-PLAN.md (the after-proof, drift attribution and disposal sequence),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-ADOPTION-EVIDENCE.md (the SC-verdict-with-quoted-deciding-value format this file mirrors),
    .planning/phases/53-managed-adopt-updates/scripts/rerun-managed-update.py (the Task-1 driver),
    .planning/phases/53-managed-adopt-updates/53-CONTEXT.md ("Proof & surface budget" — why fixtures alone are insufficient)
  </read_first>
  <action>
    Capture phase-start metadata (harness HEAD sha, the live `develop` sha re-read NOW, tool
    versions), the original checkout's four-artifact before-proof, then provision the fresh detached
    worktree at that live SHA. Refuse and record the refusal if the worktree path already exists.

    Then run four cycles, capturing argv / cwd / stdout / stderr / exit-code for EVERY invocation and
    a worktree porcelain status immediately before and after each apply:

    - **Cycle 1 — first adopt.** discover, draft, apply, then the Task-1 driver.
      SC-1 evidence: the worktree's `.harness/adoption/installed.json` destination set equals
      `applied ∪ updated` from the apply stderr summary. Capture the literal counts and the record's
      row count, and assert the record carries NO timestamp and NO run-counter key.
    - **Cycle 2 — unchanged re-run (SC-2 negative).** draft, apply, nothing changed on either side.
      Run the driver with `--require-no-writes` and require exit 0. Capture the literal stderr
      summary line and assert it contains `applied=0 updated=0 unchanged=`. Assert
      `installed_record_sha256` is IDENTICAL to cycle 1's.
    - **Cycle 3 — harness source moves (SC-2 positive).** Append one comment line to the harness's
      own `.memory/README.md` (a catalogued, non-derived, non-constitution destination), then draft
      and apply. Assert three things with literal captured values: that destination's disposition in
      manifest 3 is `update`; the worktree file's bytes now equal the mutated harness content; and
      its `installed_sha256` in the record ADVANCED from the cycle-2 value. Then
      `git checkout -- .memory/README.md` and record the restore.
    - **Cycle 4 — target-side divergence (SC-3).** Append one line to a managed file INSIDE the
      worktree — a destination cycle 1 wrote and cycle 3 did not touch — and record its pre-apply
      sha256. Then draft and apply. Assert: the disposition is `conflict`; the file's post-apply
      sha256 equals its PRE-apply sha256 exactly (byte-unchanged — not merely "different from the
      harness content"); the apply stderr names that destination with both its recorded and its
      current hash; and the exit code is 0 with other rows still processed.

    Then capture the original checkout's after-proof and assert byte-identity with the before-proof,
    attributing any delta to third-party commit movement as Phase 52 did, in a separate
    `evidence/isolation/external-drift.json`, never in the OBS-D namespace. Dispose the worktree
    (`worktree remove --force`) and confirm it is absent from `worktree list`.

    Finally write `53-ADOPTION-EVIDENCE.md`: one section per success criterion, each with a VERDICT
    and the deciding value QUOTED from the captures rather than paraphrased, plus a short section
    recording the cycle-3 harness-source mutation and its revert.
  </action>
  <verify>
    <automated>E=.planning/phases/53-managed-adopt-updates/evidence; for c in cycle-1 cycle-2 cycle-3 cycle-4; do for f in apply/argv.txt apply/stderr.txt apply/exit-code.txt compare.json; do test -f "$E/$c/$f" || exit 1; done; grep -Eq -- '--target[[:space:]]+/Users/hyojung/Desktop/2026/FeedbackOps($|[[:space:]])' "$E/$c/apply/argv.txt" && exit 1; grep -Fq 'FeedbackOps-worktrees/v27-53-rerun' "$E/$c/apply/argv.txt" || exit 1; done; grep -Fq 'applied=0 updated=0 unchanged=' "$E/cycle-2/apply/stderr.txt" || exit 1; uv run python .planning/phases/53-managed-adopt-updates/scripts/check-cycle-invariants.py 2>/dev/null || uv run python -c 'import json,sys
E=".planning/phases/53-managed-adopt-updates/evidence"
r=[json.load(open(f"{E}/cycle-{i}/compare.json")) for i in (1,2,3,4)]
assert r[1]["installed_record_sha256"]==r[0]["installed_record_sha256"], "NO-OP REWROTE THE RECORD"
assert r[1]["matches"] is True, "NO-OP WROTE UNEXPECTED PATHS"
assert r[2]["installed_record_sha256"]!=r[1]["installed_record_sha256"], "UPDATE DID NOT ADVANCE THE RECORD"
assert r[2]["disposition_counts"].get("update",0)>=1, "NO update DISPOSITION IN CYCLE 3 - the positive case never fired"
assert r[3]["disposition_counts"].get("conflict",0)>=1, "NO conflict IN CYCLE 4"
print("cycle invariants OK")' || exit 1; test -s .planning/phases/53-managed-adopt-updates/53-ADOPTION-EVIDENCE.md || exit 1; git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree list | grep -Fq 'v27-53-rerun' && exit 1; git diff --quiet -- .memory/README.md || exit 1; git diff --quiet -- tools/ contracts/ || exit 1; uv run pytest -q || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - Every apply's `argv.txt` names the WORKTREE as `--target` and never `/Users/hyojung/Desktop/2026/FeedbackOps`. The anchored guard in `<verify>` must be proven to FIRE before it is trusted: run it once against a hand-written argv line ending in the original path and confirm it matches; record that check in the SUMMARY (52-05-PLAN.md's W-6 precedent — a mis-targeting guard that never fired is decoration).
    - **SC-1** verdict quotes the literal record row count and the literal `applied=`/`updated=` counts, and states that the two sets are equal in BOTH directions.
    - **SC-2** verdict quotes cycle 2's literal stderr summary line AND cycle 3's literal `update` disposition plus the two `installed_sha256` values (before and after). The negative alone does not satisfy the criterion — cycle 3 is what proves the branch is not dead code.
    - **SC-3** verdict quotes the pre-apply and post-apply sha256 of the diverged file (equal), the literal stderr conflict line showing both hashes, and the exit code `0`.
    - The original checkout's after-proof is byte-identical to its before-proof, or any delta is attributed in `external-drift.json` as pure commit movement with reconstructed index digests (Phase 52 precedent), never left unexplained.
    - `git diff --quiet -- .memory/README.md` and `git diff --quiet -- tools/ contracts/` both succeed at the end — the cycle-3 mutation was reverted and no production code was edited during the evidence run.
    - The worktree is disposed and absent from `git worktree list`.
  </acceptance_criteria>
  <done>All three success criteria have a real-target verdict backed by quoted values, the original checkout and the harness checkout are both clean, and the worktree is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Document re-run-as-update on the existing surface and re-emit both runtimes</name>
  <files>harness/commands/adopt.md, harness/skills/brownfield-adoption/SKILL.md, .claude/commands/adopt.md, .claude/skills/brownfield-adoption/SKILL.md, .opencode/command/adopt.md, .opencode/skill/brownfield-adoption/SKILL.md</files>
  <read_first>
    harness/commands/adopt.md (the full file: the three sub-verb sections and the Notes block — this task edits the `apply` section and Notes, it does NOT add a fourth sub-verb),
    harness/skills/brownfield-adoption/SKILL.md (the discover/draft/review/apply runbook and its current length — Claude's SKILL.md limits are name <= 64 chars, description <= 1024 chars, body well under 500 lines),
    tools/harness_emit/emit-manifest.json (the owned-path set: which emitted files this re-emit will rewrite),
    AGENTS.md (lines around 101-110: the HARNESS-MANAGED marker contract — emitted trees are derived, never hand-edited),
    .planning/phases/53-managed-adopt-updates/53-ADOPTION-EVIDENCE.md (the real behavior being documented — describe what shipped, not what was planned)
  </read_first>
  <action>
    Edit `harness/commands/adopt.md`'s existing `### apply` section to state, in the same terse
    register as the rest of the file: a re-run against an already-adopted target UPDATES the files
    `/adopt` previously installed rather than reinstalling them; the record of what it manages lives
    in the target at `.harness/adoption/installed.json`; a run where neither the harness source nor
    the target changed writes nothing and reports `applied=0 updated=0 unchanged=N conflicts=0`; a
    target-side edit to a managed file is reported as a conflict on stderr and that file is left
    byte-unchanged; and conflicts do not abort the run or change the exit code. Add one Notes bullet
    stating that the drafted `manifest.json` IS the conflict report — there is no separate conflicts
    artifact.

    Add the equivalent paragraph to `harness/skills/brownfield-adoption/SKILL.md`'s runbook, in the
    re-run/idempotence position rather than as a new top-level section.

    **Add no new sub-verb, command, skill, or agent.** NG-01 caps commands at 19 and skills at 8, and
    Phase 54 re-counts them.

    Then re-emit both runtime trees with `tools.harness_emit` (the argv this repo ships — read
    `AGENTS.md` / `tools/harness_emit/` for the exact form) and commit the regenerated
    `.claude/`/`.opencode/` files as OUTPUT of the emitter. Never hand-edit them.
  </action>
  <verify>
    <automated>grep -Fq 'installed.json' harness/commands/adopt.md || exit 1; grep -Fq 'updated=' harness/commands/adopt.md || exit 1; grep -Fq 'installed.json' harness/skills/brownfield-adoption/SKILL.md || exit 1; grep -Fq 'conflicts.json' harness/commands/adopt.md harness/skills/brownfield-adoption/SKILL.md && exit 1; test "$(ls harness/commands/*.md | wc -l | tr -d ' ')" = "19" || exit 1; test "$(ls -d harness/skills/*/ | wc -l | tr -d ' ')" = "8" || exit 1; test "$(find contracts -name '*.schema.json' | wc -l | tr -d ' ')" = "6" || exit 1; diff <(sed -n '/^# /,$p' harness/commands/adopt.md) <(sed -n '/^# /,$p' .claude/commands/adopt.md) >/dev/null || echo 'NOTE: emitted body differs from source - confirm this is the emitter transform, not a stale emit'; git diff --quiet -- .claude/ .opencode/ && exit 1; uv run pytest -q || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `ls harness/commands/*.md | wc -l` is `19` and `ls -d harness/skills/*/ | wc -l` is `8` — machine-asserted, since NG-01 and Phase 54 SC-2 both re-count these.
    - `find contracts -name '*.schema.json' | wc -l` is `6`.
    - `harness/commands/adopt.md` still has exactly three `###` sub-verb headings (`discover`, `draft`, `apply`) — verify by listing them; a fourth heading is a surface breach even if no new file was added.
    - Neither doc mentions `conflicts.json`, `source_sha256`, or an exit code 3 — those were cut and must not be documented into existence.
    - The `.claude/` and `.opencode/` diffs are produced by running the emitter, not by editing: state in the SUMMARY the exact emitter argv that was run, and confirm re-running it a second time produces no further diff (idempotence).
    - The documented summary-line shape matches the one `tools/adoption_apply/cli.py` actually prints — copy it from the code or from `53-ADOPTION-EVIDENCE.md`'s captured stderr, do not retype it from memory.
    - `uv run pytest -q` fully green.
  </acceptance_criteria>
  <done>A human reading `/adopt`'s own documentation learns the re-run semantics, both runtime trees are regenerated from the single harness source, and no governed surface grew.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → real third-party repo (FeedbackOps) | A real developer's checkout; a write into the ORIGINAL rather than the worktree is unrecoverable damage. |
| evidence run → harness checkout | Cycle 3 deliberately mutates a harness source file to force an `update`; an unreverted mutation ships as an accidental commit. |
| `harness/` source → emitted `.claude`/`.opencode` trees | A hand-edit to an emitted file is silently lost on the next re-emit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-53-13 | Tampering | An apply mis-targeted at `/Users/hyojung/Desktop/2026/FeedbackOps` instead of the worktree | high | mitigate | Every cycle's `argv.txt` is checked by an ANCHORED pattern (end-of-line OR whitespace, since `--target PATH` is the last token), and that guard is proven to fire against a hand-written mis-targeted argv before being trusted; plus a four-artifact before/after proof on the original checkout. |
| T-53-14 | Tampering | The cycle-3 harness-source mutation left in the tree | high | mitigate | Task 2's `<verify>` runs `git diff --quiet -- .memory/README.md` and `git diff --quiet -- tools/ contracts/` at the end; the mutation and its revert are both recorded in the evidence file. |
| T-53-15 | Repudiation | A no-op claim backed by a log line rather than a filesystem fact | medium | mitigate | `--require-no-writes` exits non-zero on any non-sidecar write, and cycle 2's `installed_record_sha256` is asserted identical to cycle 1's; the gate is observed failing before it is trusted. |
| T-53-16 | Tampering | Governed-surface growth slipping in via documentation work | medium | mitigate | Task 3 machine-asserts 19 commands, 8 skills, 6 contracts, and exactly three `adopt` sub-verb headings. |
| T-53-17 | Information Disclosure | Evidence captures embedding third-party product code or secrets | medium | mitigate | Captures are argv/status/hash artifacts and manifest metadata only; no target file CONTENT is copied into `evidence/`, and `product_code_paths` is reported as a count/path list, never as content. |
| T-53-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed in this plan (53-RESEARCH.md: zero new dependencies). |
</threat_model>

<verification>
- All four cycles' captures exist with argv naming only the worktree.
- Cycle 2 `matches: true` under `--require-no-writes`; cycle 3 produces at least one `update`; cycle 4 at least one `conflict`.
- The original FeedbackOps checkout's after-proof is byte-identical to its before-proof (or the delta is attributed as commit movement).
- `git diff --quiet -- .memory/README.md tools/ contracts/` succeeds; the worktree is disposed.
- 19 commands, 8 skills, 6 contracts.
- `uv run pytest -q` fully green; `uv run python -m tools.contract_drift.drift` exits 0.
</verification>

<success_criteria>
Each MONO-12 success criterion has a real-target verdict in `53-ADOPTION-EVIDENCE.md` backed by a
quoted deciding value — including the POSITIVE halves (an `update` that fired and advanced the
record, a `conflict` that was classified and reported) and not only the no-op. The third-party
checkout and the harness checkout are both provably clean, the worktree is gone, `/adopt`'s own
documentation describes the new behavior, both runtime trees are re-emitted from `harness/`, and the
governed surface is unchanged at 19/8/6.
</success_criteria>

<output>
Create `.planning/phases/53-managed-adopt-updates/53-04-SUMMARY.md` when done.
</output>
