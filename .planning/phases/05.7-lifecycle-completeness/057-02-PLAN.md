---
phase: 5.7
plan: "02"
slug: should-have-lifecycle
wave: 2
status: ready
requirements: [LIFE-06, LIFE-07, LIFE-08, LIFE-09, LIFE-10, LIFE-11]
created: 2026-07-09
---

# Phase 5.7 · Plan 02 — SHOULD-HAVE (full lifecycle: onboard→plan→review→integrate)

Completes the lifecycle stages the audit found MISSING/THIN. Same in-wave guard discipline.

## Tasks

### T1 — LIFE-06 `/orient` command (deterministic onboarding entry point)
- **Add** `harness/commands/orient.md` (`agent: orchestrator`, `subtask: true`). Body: read-order +
  golden-path; `!` macros regenerate + surface derived memory
  (`python -m tools.memory_regen.repo_map`, `... .contracts_index`, `... .inject`) and point at
  AGENTS.md. Replaces reliance on the deferred opencode injector with an explicit command.
- **Verify:** `test_commands` + referential-integrity green.

### T2 — LIFE-07 `/review` workflow command (reviewer → executable step)
- **Add** `harness/commands/review.md` (`agent: code-reviewer`, `subtask: true`). Body: `!git diff`
  the working change → route to the read-only `code-reviewer` → severity-classified findings →
  return to the scoped engineer; surfaces the `secret_scan` posture. `code-reviewer` stays read-only
  (no persona change).
- **Verify:** `test_commands`, referential-integrity, and `test_agents` read-only invariant green.

### T3 — LIFE-08 `gate-model` skill (why/what is gated)
- **Add** `harness/skills/gate-model/SKILL.md` (+ `references/` if needed). Map: constitution plane
  (`contracts/**`, `docs/adr/**`, `golden/**` `path_deny_globs`), machines-gate/humans-ratify, exit-3
  refusals (`golden_runner.approve`, `strangler_guard`), hook surface (contract-guard, polyglot-lint,
  secret-scan, commit-gate), the `GOLDEN_APPROVE_HUMAN` ratification token.
- **Add** `gate-model` to `EXPECTED_SKILLS`.
- **Verify:** `test_skills` green.

### T4 — LIFE-09 `two-plane-memory` skill (constitution vs derived)
- **Add** `harness/skills/two-plane-memory/SKILL.md`. Map: constitution plane (human-owned, gated)
  vs derived plane (auto-regenerated); committed `.memory/state/` vs gitignored `.memory/derived/`;
  derived-never-hand-edited; lazy-load/pointer rule. Distills AGENTS.md #3/#4/#5.
- **Add** `two-plane-memory` to `EXPECTED_SKILLS`.
- **Verify:** `test_skills` green (desc disjoint from `gate-model`/`data-contracts`).

### T5 — LIFE-10 `/verify-work` pre-handoff composite gate
- **Add** `harness/commands/verify-work.md` (`agent: orchestrator`, `subtask: true`). Body: chain the
  in-session gates — `/lint` + `/test` + `/contract-check` + `/golden` (touched cases). Explicitly
  scoped as in-session self-verify, distinct from Phase-6 CI (non-session) and `/checkpoint` (state
  persistence). Presence-safe (dotnet-gated skips, no-op when no golden cases).
- **Verify:** `test_commands` + referential-integrity green.

### T6 — LIFE-11 orchestrator routing playbook augmentation
- **Edit** `harness/agents/orchestrator.md`: add a compact routing decision table (work-shape →
  persona/command) + a minimal intake→decompose procedure. No new persona; keep frontmatter/perms.
- **Verify:** `test_agents` (still exactly 4 personas, valid perms, routing description) green.

## Wave 2 gate
`uv run pytest` (non-example) GREEN; `EXPECTED_SKILLS` == 8 (adds gate-model, two-plane-memory);
all new commands resolve to real personas; `code-reviewer` still read-only.
