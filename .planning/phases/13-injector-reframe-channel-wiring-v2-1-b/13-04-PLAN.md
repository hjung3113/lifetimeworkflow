---
phase: 13-injector-reframe-channel-wiring-v2-1-b
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - .memory/.inject-disabled
  - .claude/hooks/memory-inject.sh
  - .memory/state/activeContext.md
  - .memory/state/progress.md
  - .planning/STATE.md
  - harness/commands/orient.md
  - harness/plugins/session-inject.ts
autonomous: true
requirements: [MEM2-02]

must_haves:
  truths:
    - "SessionStart injection actually runs — the kill switch is gone and the hook emits the reframed payload"
    - "No committed surface still claims injection is disabled"
    - "No committed source surface still describes the payload as a provisional/banner-first hint-not-truth"
  artifacts:
    - path: ".claude/hooks/memory-inject.sh"
      provides: "injector with the TEMPORARY DISABLE short-circuit removed"
    - path: "harness/commands/orient.md"
      provides: "orientation prose matching the reframed priority order"
  key_links:
    - from: ".claude/hooks/memory-inject.sh"
      to: "tools.memory_regen.inject"
      via: "uv run python -m tools.memory_regen.inject, no longer short-circuited"
      pattern: "tools\\.memory_regen\\.inject"
---

<objective>
Realize the phase's value: **turn the injection back on** (D-20) and retire every committed surface
that now asserts something false.

Purpose: `.memory/.inject-disabled` short-circuits the hook to an empty payload — Phase 13 would
otherwise ship a perfect reframe that **never runs** (13-RESEARCH.md Pitfall 7). Five surfaces
currently promise or assert the old world: the flag file, the hook's TEMPORARY DISABLE block, two
state files, `.planning/STATE.md`, plus the `provisional`/`banner-first` prose in `orient.md` and
`session-inject.ts`. They share ONE concern — *assertions the reframe made false*.

Output: injection live; stale assertions gone. **Strictly after 13-03 is green** (D-20).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-CONTEXT.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-03-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Located during planning — go straight to these lines, do NOT go hunting. -->

**Surfaces asserting "injection is disabled"** (all become false in this plan):
| File | Line(s) | Content |
|---|---|---|
| `.memory/.inject-disabled` | whole file | the kill switch itself — **delete** |
| `.claude/hooks/memory-inject.sh` | **22-31** | `# --- TEMPORARY DISABLE …` comment + the `if [ -f … ]` short-circuit — **strip** |
| `.memory/state/activeContext.md` | 12-13 | "injection is temporarily **DISABLED** … Phase 13 reframes it" |
| `.memory/state/progress.md` | 9 (pre-13-01 numbering) | "SessionStart injection: temporarily DISABLED … until MEM2" |
| `.planning/STATE.md` | ~196 | "[MEM2 context — SessionStart injection is DISABLED] … Re-enable with `rm .memory/.inject-disabled`" |

**Surfaces asserting the retired `provisional` / banner-first framing** (D-06):
| File | Line | Content |
|---|---|---|
| `harness/commands/orient.md` | 25 | "provisional banner → live contract-drift → the" |
| `harness/commands/orient.md` | 37 | "what was in flight (provisional; contracts/ADR override it)" |
| `harness/commands/orient.md` | 45 | "Provisional-banner-first: `.memory/` is a hint, not truth — …" |
| `harness/plugins/session-inject.ts` | 13 | "~1k-token cap, same provisional banner — the ONLY difference is the runtime envelope." |

**Verified during planning — nothing pins what you are removing:**
- No test references `.inject-disabled` (grep across `*.py`/`*.sh`/`*.ts` — only prose + planning docs).
- `test_hook_wiring.py:34-40` asserts **4 SessionStart groups**; `:51-60` asserts the 4th slot wires
  `memory-inject.sh` and that the file exists. Stripping the disable block changes **neither**.
- `test_hook_wiring.py:66-69` asserts `session-inject.ts` still references `tools.memory_regen.inject`
  — a **comment-only** edit at line 13 keeps this green.
</interfaces>

<scope_fence>
- **Do NOT re-emit `.opencode/` or `.claude/`.** Editing `harness/commands/orient.md` makes the
  generated copies (`.claude/commands/orient.md:21`, `.opencode/command/orient.md`) stale. That is
  **expected and correct** — **Phase 15 (MEM2-06)** owns re-emitting. Phase 12 set this precedent.
- **Do NOT regenerate `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`.** It is
  **already red on a clean tree** (inherited from Phase 12) and already embeds the orient.md prose
  (6 `provisional` hits, verified). Your edit makes that existing diff **larger**, but the suite
  stays at **exactly 1 failed** — the same test. Regenerating it would bless Phase 12's un-emitted
  tree and steal Phase 15's gate.
- **Do NOT hand-edit `.claude/commands/orient.md`** — it is generated.
- `session-inject.ts` is **authored-only, execution deferred** (`session-inject.ts:4-8`) — there is
  no opencode runtime in this container. Edit the comment; do **not** try to run or test it.
- Do NOT add `/agree`, provenance lint, agreement content, or a memory web UI (Phases 14/16).
</scope_fence>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-enable injection — delete the kill switch, strip the disable block, correct every "disabled" assertion</name>

  <read_first>
    - `.claude/hooks/memory-inject.sh` (45 lines — read fully; you are removing lines 22-31 and must not disturb `set -euo pipefail`, the `PROJECT_DIR` resolution, or the node envelope)
    - `.memory/.inject-disabled` (3 lines — the file you are deleting)
    - `.memory/state/activeContext.md` — as rewritten by 13-01 (frontmatter + `## In flight`)
    - `.memory/state/progress.md` — as rewritten by 13-01 (`## Recently done (last 5)` + `## Remaining`)
    - `.planning/STATE.md` around line 196 — the MEM2 blocker note
    - `.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-03-SUMMARY.md` — confirm the reframe actually landed green before you flip the switch (D-20: **after** the reframe is green)
  </read_first>

  <files>.memory/.inject-disabled, .claude/hooks/memory-inject.sh, .memory/state/activeContext.md, .memory/state/progress.md, .planning/STATE.md</files>

  <action>
    **Precondition (D-20 — verify, do not assume):** confirm `uv run pytest tools/memory_regen tools/harness_lint -q` is green before flipping the switch. Injection re-enabled on a red reframe is worse than injection disabled.

    **(a) Delete the kill switch:** `git rm .memory/.inject-disabled`.

    **(b) Strip `.claude/hooks/memory-inject.sh` lines 22-31** — the whole `# --- TEMPORARY DISABLE
    (until the MEM2 memory-model upgrade) ---` comment block AND the `if [ -f
    "$PROJECT_DIR/.memory/.inject-disabled" ]; then … exit 0; fi` short-circuit. Its stated reason
    ("the 'provisional / confirm-before-trusting' framing that MEM2 will reframe") is exactly what
    13-03 fixed, so the block is obsolete, not merely inactive.
    - Keep everything else intact: `set -euo pipefail` (line 11), the `PROJECT_DIR` resolution, the
      `NODE` fallback, the two `|| true` derived-plane regens (lines 36-37), the `assemble` call
      (line 40), and the node argv-encoded envelope (line 43).
    - Leave `|| echo ''` at line 40 as is — it is the T-13-05 surface, but 13-03's mitigation is
      tolerant reads inside `assemble()`, not a change to the wrapper. Changing the wrapper's error
      handling is out of scope.
    - **Add no wall-clock** (D-11/Q6): do not add a `$(date)` line. 13-02's
      `test_hook_wrappers_have_no_wallclock` gates this and must stay green.

    **(c) Correct the "disabled" assertions** in `.memory/state/activeContext.md` (lines 12-13),
    `.memory/state/progress.md`, and `.planning/STATE.md` (~line 196). Remove the DISABLED /
    "re-enable with `rm .memory/.inject-disabled`" claims. Replace with a terse statement that
    SessionStart injection is **live** and emits the reframed two-block payload. In `STATE.md`, that
    note is framed as a **blocker/context caveat** ("a fresh session does NOT auto-inject … run
    `/orient` manually") — that caveat is now false and must go, not just be softened.
    - Keep 13-01's `updated:` frontmatter stamps intact and re-stamp both state files to today's
      ISO date, **double-quoted** (this IS a checkpoint-shaped edit — follow the `/checkpoint`
      mandate 13-01 wrote).
    - Keep `## In flight` byte-exact (`test_inject_assembler.py:100-101` fixture guard).
    - Keep progress tight (D-14): update the bounded `## Recently done (last 5)` summary; do **not**
      start an accumulating done-log — git holds the full completed history.
  </action>

  <acceptance_criteria>
    - `test -e .memory/.inject-disabled` fails (file deleted) and `git status --porcelain .memory/.inject-disabled` shows a staged deletion (`D`)
    - `grep -c "inject-disabled" .claude/hooks/memory-inject.sh` returns `0`
    - `grep -c "TEMPORARY DISABLE" .claude/hooks/memory-inject.sh` returns `0`
    - `grep -rc "inject-disabled" .memory/state/activeContext.md .memory/state/progress.md .planning/STATE.md` returns `0` for each
    - The hook still has its guardrails: `grep -q "set -euo pipefail" .claude/hooks/memory-inject.sh` and `grep -q "tools.memory_regen.inject" .claude/hooks/memory-inject.sh` both succeed
    - **The hook actually injects a non-empty reframed payload.** This prints a JSON object whose `additionalContext` is non-empty and contains `DATA` — and NOT `""`:
      `bash .claude/hooks/memory-inject.sh | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const c=JSON.parse(s).hookSpecificOutput.additionalContext; console.log('len=',c.length,'hasDATA=',c.includes('DATA'),'hasProvisional=',/provisional/i.test(c));})"`
      ⇒ expect `len=` > 1000, `hasDATA= true`, `hasProvisional= false`
    - No wall-clock added: `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_hook_wrappers_have_no_wallclock -x` passes
    - Hook wiring intact: `uv run pytest tools/memory_regen/tests/test_hook_wiring.py -q` green (still 4 SessionStart groups)
    - Both state files still carry a quoted stamp: `grep -cE '^updated: "[0-9]{4}-[0-9]{2}-[0-9]{2}"$' .memory/state/activeContext.md` returns `1` (same for `progress.md`)
    - `grep -c '^## In flight$' .memory/state/activeContext.md` returns `1`
    - `uv run pytest tools/memory_regen tools/harness_lint -q` — green
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q && ! test -e .memory/.inject-disabled && ! grep -q "inject-disabled" .claude/hooks/memory-inject.sh && bash .claude/hooks/memory-inject.sh | grep -q "DATA PROVENANCE\|DATA"</automated>
  </verify>

  <done>`.memory/.inject-disabled` is deleted, the hook's TEMPORARY DISABLE block is stripped, and `bash .claude/hooks/memory-inject.sh` emits a non-empty reframed payload containing no `provisional`. No committed surface (state files, STATE.md, hook) still claims injection is disabled. Stamps re-written and quoted; `## In flight` preserved; hook wiring + no-wall-clock gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the superseded "provisional / banner-first" prose (D-06)</name>

  <read_first>
    - `harness/commands/orient.md` (full — you are editing lines 25, 37, 45; read the surrounding prose so the rewrite is coherent, not a find-replace)
    - `harness/plugins/session-inject.ts` lines 1-30 — the authored-only header comment containing line 13
    - `tools/memory_regen/inject.py` — as reframed by 13-03; the docstring priority list is the source of truth your prose must now match
  </read_first>

  <files>harness/commands/orient.md, harness/plugins/session-inject.ts</files>

  <action>
    D-06: agreements-at-priority-0 **retires the D-02 "banner-first" invariant**. It must be
    **recorded, not silently absorbed**. These are the last two SOURCE surfaces still asserting it.

    **(a) `harness/commands/orient.md`** — align the prose to `inject.py`'s new docstring order:
    - **Line 25** — "The same `assemble()` payload both runtimes honor: provisional banner → live
      contract-drift → the …". Rewrite the sequence to the reframed order: **working-agreements
      directive → data-provenance banner → live contract-drift → contracts-index → repo-map →
      progress-log pointer**. Note that the agreements block is omitted when the active set is empty
      (the state today), so the banner leads by default.
    - **Line 37** — "**`.memory/state/activeContext.md`** — what was in flight (provisional;
      contracts/ADR override it)." Rewrite data-scoped: it is the **session progress log**; on a
      **data** conflict contracts/ADR win; git holds the full completed history. Delete
      "provisional".
    - **Line 45** — "Provisional-banner-first: `.memory/` is a hint, not truth — `contracts/` and
      `docs/adr/` always win." This bullet **is** the D-02 invariant. Replace it with the retirement:
      state that the payload is **directive-first, banner-second**, and that the banner is
      **data-scoped** — `contracts/` and `docs/adr/` win a **data** conflict, which is **not** a
      reason to distrust or re-verify grounded working context.
    - Banned across the file afterwards (case-insensitive): `provisional`, `hint, not truth`,
      `confirm before trusting`. Do not introduce them elsewhere.
    - Do not restructure the command, change its frontmatter, or add steps — this is a prose reword.

    **(b) `harness/plugins/session-inject.ts` line 13** — "same ~1k-token cap, same provisional
    banner — the ONLY difference is the runtime envelope." Change **the comment only**: "same
    data-provenance banner" (or equivalent), preserving the sentence's point — that this adapter
    consumes the SAME single injection contract (D-01) and the only difference is the runtime
    envelope. Keep the RESUME NOTE box's authored-only/execution-deferred warning intact. **Do not
    touch the `INJECT_ARGV` constant or any code** — `test_hook_wiring.py:66-69` asserts the stub
    still references `tools.memory_regen.inject`.
  </action>

  <acceptance_criteria>
    - `grep -ic "provisional" harness/commands/orient.md` returns `0`
    - `grep -ic "hint, not truth" harness/commands/orient.md` returns `0`
    - `grep -ic "confirm before trusting" harness/commands/orient.md` returns `0`
    - `grep -ic "provisional" harness/plugins/session-inject.ts` returns `0`
    - `orient.md` names the reframed order — it contains `agreements` and `drift` and `repo map` (case-insensitive)
    - `orient.md` records the retirement: contains `directive-first` or `banner-second` (case-insensitive)
    - `session-inject.ts` code is unchanged — `grep -q "tools.memory_regen.inject" harness/plugins/session-inject.ts` succeeds and `git diff harness/plugins/session-inject.ts` shows changed lines only inside the `//` comment header
    - `uv run pytest tools/memory_regen/tests/test_hook_wiring.py -q` green
    - `uv run pytest tools/memory_regen tools/harness_lint -q` — green
    - **`uv run pytest tools/harness_emit -q` is still exactly `1 failed, 46 passed`** — the same inherited `test_projected_tree_matches_committed_snapshot`. Your orient.md edit widens that snapshot's diff (expected; Phase 15 owns the re-emit) but must not add a **second** failure
    - `git diff --name-only | grep -c "^\.claude/commands/\|^\.opencode/\|harness_emit"` returns `0` — no generated tree and no emit snapshot was hand-edited
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q && ! grep -qi "provisional" harness/commands/orient.md harness/plugins/session-inject.ts && uv run pytest tools/harness_emit -q 2>&1 | tail -1</automated>
  </verify>

  <done>`orient.md` describes the reframed agreements→banner→drift→contracts→repo-map→pointer order, is data-scoped, and explicitly records the D-02 banner-first retirement as directive-first/banner-second; `session-inject.ts:13`'s comment no longer says "provisional banner" and its code is untouched. No `provisional` remains in either. `harness_emit` still exactly 1 failed / 46 passed. No generated tree hand-edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.memory/.inject-disabled` → hook | A **security-relevant kill switch** (ASVS V14 Configuration): while present, the hook emits an empty payload. This plan removes it, activating the injection channel for real. |
| `.memory/agreements/*` → live session context | After this plan, the priority-0 directive channel is **actually live** — T-13-01's surface becomes real rather than theoretical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-13-01** | Elevation of Privilege / Tampering | Removing the kill switch **activates** the planted-agreement surface: from here on, an `.memory/agreements/<slug>.md` with `status: active` renders as a priority-0 directive every session | mitigate (accepted residual) | Gated on 13-03 being green, so all four ratified mitigations are already in force before the switch flips: title+rule-only render (D-16), fail-closed status filter (D-17), N/M cap (D-01), scope-limiting header (D-15). The **active set is empty today**, so the block is omitted on day one — the change is inert until an agreement is deliberately added. Residual risk accepted per Q1 (committed-but-writable, git review is the gate); the durable provenance lint lands in **Phase 14 (MEM2-04)**. |
| **T-13-05** | Denial of Service | Silent injection failure — `memory-inject.sh:40` `\|\| echo ''` swallows any traceback into an empty payload. Before this plan the payload was *always* empty, so the failure mode was invisible | mitigate | Task 1's acceptance criterion **executes the hook end-to-end** and asserts `additionalContext.length` > 1000 and contains `DATA` — a real assertion that the payload is non-empty, which is the only way to catch the `\|\| echo ''` swallow. 13-03's tolerant `(OSError, ValueError)` reads are the upstream fix. |
| — (D-11 / Q6) | Tampering | Wall-clock creeping into the hook wrapper while editing it | mitigate | Task 1 forbids adding `$(date)`; 13-02's `test_hook_wrappers_have_no_wallclock` is re-run as an acceptance criterion (using command-shaped tokens, since bare `date` false-positives on `gsd-check-update.js` at line 4). |
| — (V14 Configuration) | Denial of Service | Re-enabling on a **red** reframe would inject a broken/undetermined payload into every session | mitigate | D-20 ordering: wave 3, `depends_on: ["13-03"]`, and Task 1's explicit precondition to confirm `tools/memory_regen` + `tools/harness_lint` green **before** deleting the flag. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | **No installs** — deletions and prose edits only. |

**Explicitly NOT threats:** no network, no auth, no PII, no untrusted deserialization. The
`session-inject.ts` edit is comment-only and the file is authored-only/never executed here.
</threat_model>

<verification>
- `uv run pytest tools/memory_regen tools/harness_lint -q` — **green**.
- `uv run pytest tools/harness_emit -q` — **exactly 1 failed, 46 passed**.
  ⚠️ `test_projected_tree_matches_committed_snapshot` was **already red on a clean tree before any
  Phase 13 work** (Phase 12's deferred re-emit). This plan's `orient.md` edit **widens that
  snapshot's diff** — expected, since **Phase 15 owns the re-emit**. The gate is *no worse than 1
  failed*: one test, not two. **Do NOT regenerate the `.ambr`.**
- **End-to-end:** `bash .claude/hooks/memory-inject.sh` emits a non-empty `additionalContext`
  containing the data-scoped banner and no `provisional`.
- **Manual (operator):** read `uv run python -m tools.memory_regen.inject` and confirm the payload
  reads as a directive + data-scoped provenance note, not as distrust of grounded work.
</verification>

<success_criteria>
- `.memory/.inject-disabled` deleted; `memory-inject.sh:22-31` stripped; the hook emits a real,
  non-empty, reframed payload (D-20).
- No committed surface — hook, both state files, `.planning/STATE.md` — still claims injection is
  disabled.
- No source surface — `orient.md`, `session-inject.ts` — still says `provisional` / banner-first;
  the D-02 retirement is **recorded** as directive-first/banner-second (D-06).
- `tools/memory_regen` + `tools/harness_lint` green; `harness_emit` still exactly 1 failed.
- No generated tree (`.claude/`, `.opencode/`) or emit snapshot hand-edited — Phase 15's scope intact.
</success_criteria>

<output>
Create `.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-04-SUMMARY.md` when done.
</output>
