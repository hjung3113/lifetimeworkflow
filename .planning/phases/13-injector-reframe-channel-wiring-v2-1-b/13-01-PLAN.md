---
phase: 13-injector-reframe-channel-wiring-v2-1-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .memory/state/activeContext.md
  - .memory/state/progress.md
  - .memory/agreements/README.md
  - harness/commands/checkpoint.md
autonomous: true
requirements: [MEM2-05, MEM2-02]

must_haves:
  truths:
    - "Both state files carry a quoted `updated: \"<ISO-date>\"` frontmatter stamp that parse_frontmatter returns as a str"
    - "/checkpoint mandates writing the stamp on every checkpoint"
    - "/checkpoint mandates in-flight + remaining + short last-N-done and forbids done-log accumulation"
    - ".memory/agreements/README.md warns against secrets, matching the state-plane warning"
  artifacts:
    - path: ".memory/state/activeContext.md"
      provides: "updated: stamp in YAML frontmatter; '## In flight' heading preserved"
      contains: "updated:"
    - path: ".memory/state/progress.md"
      provides: "updated: stamp + tight §7a shape (recently-done + remaining)"
      contains: "updated:"
    - path: "harness/commands/checkpoint.md"
      provides: "stamp mandate + tight-progress mandate + anti-accumulation rule"
      contains: "updated:"
    - path: ".memory/agreements/README.md"
      provides: "no-secrets warning (T-13-03 mitigation, D-18)"
  key_links:
    - from: ".memory/state/activeContext.md"
      to: "tools.harness_lint.parse_frontmatter"
      via: "YAML frontmatter fence read by inject.py in plan 13-03"
      pattern: "^updated:"
---

<objective>
Land the **write half** of MEM2-05: a quoted `updated:` ISO-date stamp in both committed state
files, a `/checkpoint` instruction that mandates writing it plus the §7a tight-progress shape, and
the D-18 no-secrets line on the agreements channel.

Purpose: plan 13-03 surfaces this stamp verbatim. The stamp must EXIST before it can be read —
otherwise SC3 only ever exercises the absent-stamp fallback and is unproven end-to-end.
Output: 4 prose/state-plane files. **No `inject.py` edit** — this plan is code-free by design.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-CONTEXT.md
@.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-VALIDATION.md
@AGENTS.md
@harness/skills/two-plane-memory/SKILL.md

<interfaces>
<!-- Settled during planning — do NOT re-derive. -->

`tools/harness_lint/frontmatter.py` uses **ruamel.yaml `YAML(typ="safe")`**, NOT a hand-rolled
loader (13-RESEARCH.md's "_load_yaml ships its own parser / no PyYAML" is imprecise — ruamel is
already a workspace dep via check-jsonschema; do NOT `uv add` anything).

**RESEARCH assumption A6 is now VERIFIED (executed this planning session):**

    parse_frontmatter('---\nupdated: 2026-07-15\n---\n')   -> datetime.date(2026, 7, 15)   # BARE
    parse_frontmatter('---\nupdated: "2026-07-15"\n---\n') -> '2026-07-15'  (str)          # QUOTED

⇒ The stamp **MUST be quoted**. A bare date round-trips as a `datetime.date` object, and rendering
it would depend on `str(date)` coincidence. Quote it. This is settled — do not re-verify.

Public API: `from tools.harness_lint import parse_frontmatter`
Signature:  `parse_frontmatter(md_text: str) -> tuple[dict, str]`  (CRLF-normalized at :40)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stamp both state files, tighten progress to the §7a shape, add the agreements no-secrets line</name>

  <read_first>
    - `.memory/state/activeContext.md` (22 lines — read fully; you are prepending frontmatter and must preserve existing headings)
    - `.memory/state/progress.md` (10 lines — read fully)
    - `.memory/agreements/README.md` (19 lines — read fully; the new line must match its voice)
    - `tools/memory_regen/tests/test_inject_assembler.py` lines 94-103 — the fixture guard that pins the literal `## In flight` marker. **Read it so you understand why that heading is untouchable.**
  </read_first>

  <files>.memory/state/activeContext.md, .memory/state/progress.md, .memory/agreements/README.md</files>

  <action>
    **(a) Both state files — prepend YAML frontmatter (they have NONE today; both start with an H1).**

    Add exactly this fence at the very top of BOTH `.memory/state/activeContext.md` and
    `.memory/state/progress.md`, followed by a blank line, then the existing H1 unchanged:

    an opening `---` fence, the single key `updated` whose value is the **double-quoted** ISO date
    `"2026-07-16"`, then a closing `---` fence.

    The value MUST be double-quoted (per D-10 + the verified A6 result in `<interfaces>`) so
    `parse_frontmatter` yields a `str`, not a `datetime.date`. Use today's date `2026-07-16`.

    **(b) `.memory/state/activeContext.md` — preserve `## In flight` EXACTLY.**

    Do NOT rename, reword, or re-case the `## In flight` heading (line 8). It is a fixture guard at
    `test_inject_assembler.py:100-101`, which asserts the marker is present in the file body AND
    absent from the injected payload. Renaming it turns a green pointer-only test red.
    Keep the `## Next` heading. Keep the existing DATA AUTHORITY blockquote (Phase 12 shipped it —
    do not re-word it; MEM2-03 is done).

    Leave lines 12-13 (the "injection is temporarily DISABLED / Phase 13 re-enables" note) **AS IS**.
    Plan 13-04 owns removing it, after the reframe is green. Do not touch it here.

    **(c) `.memory/state/progress.md` — apply the §7a tight shape.**

    Restructure the existing flat 4-bullet list under two headings, keeping the content terse:
    - `## Recently done (last 5)` — a short bounded summary, newest first. NOT a growing log.
    - `## Remaining` — what is still open.

    Keep the existing DATA AUTHORITY blockquote. Do not add a `## Done` / changelog section; git
    holds the full completed history (D-14).

    **(d) `.memory/agreements/README.md` — add the D-18 no-secrets line.**

    Add one sentence stating that agreements are committed **and injected verbatim into every
    session**, so they must never contain secrets, tokens, credentials, or PII. Place it with the
    entry-shape paragraph (the one ending "never delete it.", currently lines 8-12). Match the
    parity precedent at `.memory/state/activeContext.md:5` ("No secrets, tokens, credentials, or
    PII here."). This is the T-13-03 mitigation.

    Do NOT add `/agree` docs, provenance-lint docs, or any real agreement file — Phase 14 owns the
    write path, and an agent must never invent agreement content (§2 property 2).
  </action>

  <acceptance_criteria>
    - `head -1 .memory/state/activeContext.md` is exactly `---`
    - `head -1 .memory/state/progress.md` is exactly `---`
    - `grep -c '^updated: "2026-07-16"$' .memory/state/activeContext.md` returns `1`
    - `grep -c '^updated: "2026-07-16"$' .memory/state/progress.md` returns `1`
    - The stamp parses as a **str**, not a date. This command prints `str str` and exits 0:
      `uv run python -c "from pathlib import Path; from tools.harness_lint import parse_frontmatter; print(*[type(parse_frontmatter(Path(p).read_text()) [0]['updated']).__name__ for p in ('.memory/state/activeContext.md','.memory/state/progress.md')])"`
    - `grep -c '^## In flight$' .memory/state/activeContext.md` returns `1` (heading preserved verbatim)
    - `grep -q '^## Recently done (last 5)$' .memory/state/progress.md` succeeds
    - `grep -q '^## Remaining$' .memory/state/progress.md` succeeds
    - `.memory/agreements/README.md` contains both the substring `secret` and the substring `inject` (case-insensitive) on the same added sentence
    - `.memory/state/activeContext.md` still contains `.inject-disabled` (13-04 removes it, not this plan)
    - No new file exists under `.memory/agreements/` — `ls .memory/agreements/` lists exactly `README.md` and `_TEMPLATE.md`
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q</automated>
  </verify>

  <done>Both state files carry a quoted `updated: "2026-07-16"` stamp that parse_frontmatter returns as a str; `## In flight` is byte-preserved; progress.md has the bounded `## Recently done (last 5)` + `## Remaining` shape; agreements README carries the no-secrets warning; `tools/memory_regen` + `tools/harness_lint` stay green (298 passed baseline).</done>
</task>

<task type="auto">
  <name>Task 2: Mandate the stamp + the tight-progress shape in /checkpoint</name>

  <read_first>
    - `harness/commands/checkpoint.md` (35 lines — read fully; you are editing its Steps + Notes)
    - `.memory/state/progress.md` — as rewritten by Task 1, so the mandate matches the shape that now exists
  </read_first>

  <files>harness/commands/checkpoint.md</files>

  <action>
    `/checkpoint` is **prose-only** — `checkpoint.md:6` is `agent: orchestrator` and the steps are
    Markdown instructions plus `!` shell lines. There is **no runnable script** (D-13). So this is an
    **instruction edit**, verified structurally by grep. Do NOT write a Python writer, do NOT add a
    runnable, and do NOT add the command to `EXPECTED_COMMANDS` (no new command — `/checkpoint`
    already exists; Phase 15 owns emit/count changes).

    Extend **Step 1** so it mandates, for BOTH `.memory/state/activeContext.md` and
    `.memory/state/progress.md`:

    1. **The stamp (D-10):** set the YAML frontmatter key `updated` to today's ISO date
       (`YYYY-MM-DD`), **double-quoted** — state explicitly that the quotes are required so the value
       round-trips as a string rather than a YAML date object. Say the stamp is written on **every**
       checkpoint.
    2. **The tight shape (D-14 / §7a):** `activeContext.md` records what is **in flight** and what is
       **next**; `progress.md` records a **short last-N-done summary** (`## Recently done (last 5)`)
       plus **what remains** (`## Remaining`).
    3. **The anti-accumulation rule (D-14):** the done summary is **bounded — never append-only**.
       Older entries are dropped, not accumulated. Include the literal phrase
       **`git holds the full completed history`** as the justification. State that a growing done-log
       is forbidden.

    Add a bullet to `## Notes` restating that memory is not a changelog: durable decisions belong in
    append-only `docs/adr/`, completed history belongs in git, and `.memory/state/` stays tight.

    Do NOT add wall-clock automation (no `$(date)` shell line to compute the stamp) — the
    orchestrator agent writes the date it already has in session context. Q6/D-11 forbid
    hook/wrapper wall-clock; keeping `/checkpoint` free of it keeps the discipline uniform and keeps
    plan 13-02's static gate meaningful.

    Leave Step 2's `git add` / `git commit` lines unchanged (they already stage exactly the two
    state files).
  </action>

  <acceptance_criteria>
    - `grep -q "updated:" harness/commands/checkpoint.md` succeeds (13-VALIDATION.md command, verbatim)
    - `grep -qi "last .*done\|no.*done-log\|git holds" harness/commands/checkpoint.md` succeeds (13-VALIDATION.md command, verbatim)
    - `grep -q "git holds the full completed history" harness/commands/checkpoint.md` succeeds
    - `harness/commands/checkpoint.md` mentions both `activeContext.md` and `progress.md` in the stamp mandate
    - `checkpoint.md` contains the word `quote` or `quoted` (the quoting requirement is explicit, not implied)
    - **No wall-clock added:** `grep -n '\$(date\|`date' harness/commands/checkpoint.md` returns no matches
    - `checkpoint.md:6` still reads `agent: orchestrator` and no runnable script was added
    - `git diff --stat harness/commands/checkpoint.md` shows only `checkpoint.md` changed
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest tools/memory_regen tools/harness_lint -q && grep -q "updated:" harness/commands/checkpoint.md && grep -q "git holds the full completed history" harness/commands/checkpoint.md && ! grep -q '\$(date' harness/commands/checkpoint.md</automated>
  </verify>

  <done>`checkpoint.md` mandates the quoted `updated:` stamp for both state files, mandates the in-flight + remaining + short last-N-done shape, forbids done-log accumulation with the "git holds the full completed history" rationale, adds no wall-clock, and stays prose-only. `tools/memory_regen` + `tools/harness_lint` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-write actor → `.memory/agreements/` | Committed-but-writable (Q1), NOT path-denied. Content becomes an injected directive in plan 13-03. |
| `.memory/state/*` → `assemble()` | Frontmatter authored here is read by `inject.py` in plan 13-03 and surfaced into every session's context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information Disclosure | `.memory/agreements/*` — committed AND (from 13-03) injected verbatim; a secret pasted into a rule enters every session's context | mitigate | D-18: add the no-secrets line to `.memory/agreements/README.md` (Task 1d), parity with `activeContext.md:5`. The durable provenance lint is **Phase 14** — out of scope here. `secret_scan` continues to gate writes. |
| T-13-05 | Denial of Service | The `updated:` frontmatter this plan adds is read by `_active_context_pointer` in 13-03; a malformed fence would raise, and `memory-inject.sh:40` `\|\| echo ''` would swallow it into an **empty payload** — silently killing drift + agreements | mitigate | Stamp is a single well-formed quoted scalar in a properly closed `---` fence. Acceptance criteria assert `parse_frontmatter` returns a `str` for both files, proving the fence parses before 13-03 depends on it. `parse_frontmatter` itself fails safe on an unclosed fence (`frontmatter.py:54`). |
| T-13-01 | Elevation of Privilege | This plan does NOT author agreement content | accept | No active agreement is created (acceptance criterion: `.memory/agreements/` still holds exactly `README.md` + `_TEMPLATE.md`). Agents must not invent agreements (§2). The render-shape/cap/fail-closed mitigations land in 13-03. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | **No package installs in this plan** — prose + state-plane edits only. `13-RESEARCH.md` § Package Legitimacy Audit: "Not applicable — Phase 13 installs no external packages." A `uv add` here is a scope violation. |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen tools/harness_lint -q` — green (baseline: **298 passed**).
- `uv run pytest tools/harness_emit -q` — **no worse than 1 failed, 46 passed**.
  ⚠️ That 1 failure (`test_projected_tree_matches_committed_snapshot`) is **already red on a clean
  tree before any Phase 13 work** — inherited from Phase 12's deferred re-emit. It is NOT a
  regression from this plan. **Do NOT regenerate the `.ambr`** to make it green: that would bless
  Phase 12's un-emitted tree and steal Phase 15's gate.
- Structural greps in each task's `<acceptance_criteria>`.
</verification>

<success_criteria>
- Both state files carry a quoted `updated: "2026-07-16"` stamp parsing to `str` (SC3 write half).
- `## In flight` preserved byte-exact; `test_active_context_is_pointer_not_body` still green.
- `progress.md` is tight: `## Recently done (last 5)` + `## Remaining`, no accumulating done-log (SC4).
- `checkpoint.md` mandates the stamp + tight shape + anti-accumulation, prose-only, no wall-clock.
- `.memory/agreements/README.md` carries the no-secrets warning (T-13-03).
- `inject.py` untouched. No new agreement files. No package installs.
</success_criteria>

<output>
Create `.planning/phases/13-injector-reframe-channel-wiring-v2-1-b/13-01-SUMMARY.md` when done.
</output>
