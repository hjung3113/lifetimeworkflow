---
phase: 41-docs-review-plane-removal
plan: 04
type: execute
wave: 4
depends_on: ["41-03"]
files_modified:
  - contracts/harness/docs/
  - contracts/.hashes/manifest.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [CER-05]
must_haves:
  truths:
    - "contracts/harness/docs/doc-dependencies.schema.json and the now-empty contracts/harness/docs/ directory no longer exist"
    - "contract-drift is green against the rebaselined manifest, in the same commit as the schema deletion"
    - "The docs-guard CI job no longer exists and gate.needs has no dangling docs-guard token, verified by YAML resolution not grep"
  artifacts:
    - path: "contracts/harness/docs/doc-dependencies.schema.json"
      provides: "MUST NOT EXIST after this plan"
    - path: "contracts/.hashes/manifest.json"
      provides: "no contracts/harness/docs/doc-dependencies.schema.json key"
    - path: ".github/workflows/ci.yml"
      provides: "no docs-guard job block, gate.needs has 11 entries (was 12), no other entry changed"
  key_links:
    - from: ".github/workflows/ci.yml gate job"
      to: "job graph"
      via: "needs: list"
      pattern: "needs:.*docs-guard"
      note: "this pattern MUST NOT match after the plan — it is an absence link"
---

<objective>
Delete `contracts/harness/docs/doc-dependencies.schema.json` and rebaseline
`contracts/.hashes/manifest.json` in the same commit (D-02), then delete the CI `docs-guard` job
(`ci.yml:317-351`) and its fan-in `needs` entry (`ci.yml:381`) in a separate, later commit — per
RESEARCH.md's Safe Deletion Order steps 6 and 8, CI job removal comes last so local consumers are
already fixed (Plans 01-03) before CI stops directly exercising them.

Purpose: the contract is the last constitution-plane artifact this phase touches; `contract_guard`
does not fire on a Bash `git rm` (confirmed: both hooks match only `Write|Edit`), so no
`HARNESS_DEV_BYPASS` is needed for this deletion (RESEARCH.md Gate Mechanics). The CI job deletion
is what finally turns the ROADMAP's stated success criterion — "the CI fan-in gate is green" — true.

Output: two pathspec-scoped commits.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-review-plane-removal/41-CONTEXT.md
@.planning/phases/41-docs-review-plane-removal/41-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-03-SUMMARY.md

<interfaces>
Verified this session (2026-07-27).

From contracts/.hashes/manifest.json: line 6 is the key
"contracts/harness/docs/doc-dependencies.schema.json" mapped to its sha256 hash. This key
disappears via `uv run python -m tools.contract_hash.hash --write` (confirmed exact flag name at
tools/contract_hash/hash.py's argparse definition: --write, "Write the manifest to --manifest
(default: contracts/.hashes/manifest.json)"). Never hand-edit this JSON.

From .github/workflows/ci.yml (verified this session, exact line numbers):
- The docs-guard job's comment block starts at line 317 and the job body ends at line 351. Delete
  lines 317-351 inclusive.
- The gate job is declared at line 380; its needs: line is 381, current exact content: needs:
  [setup, lang-tests, contract-check, drift, golden, core-suite, lint, lifecycle-eval, emit-drift,
  stale-derived, docs-guard, workspace] — target: remove only the docs-guard token, leaving 11
  entries with everything else (order, spacing, commas) unchanged.
- docs-guard has no needs: clause of its own to orphan (the only two needs: occurrences in the
  whole file are lang-tests: needs: setup at line 80 and gate's fan-in at line 381).
- After deleting lines 317-351, resolve the YAML with a parser (not grep alone) to confirm no
  dangling reference (D-14) — write a small script under the scratchpad directory that loads the
  workflow with PyYAML and asserts jobs.gate.needs has exactly 11 entries and does not contain
  "docs-guard"; do not embed the assertion inline in a shell one-liner with nested quotes.

contracts/harness/security/deny-domains.json names tools/docs_guard/ledger.py:54 and
tools/hooks/ledger_guard.py:decide in location fields of an inventory record — this file is
explicitly OUT OF SCOPE for this phase (Phase 44 territory per RESEARCH.md; nothing reads or gates
on it). Do NOT touch it in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the contract and rebaseline the manifest</name>
  <files>contracts/harness/docs/, contracts/.hashes/manifest.json</files>
  <read_first>
    contracts/harness/docs/doc-dependencies.schema.json
    contracts/.hashes/manifest.json
    tools/contract_hash/hash.py
    .planning/phases/41-docs-review-plane-removal/41-RESEARCH.md, "Gate Mechanics" contract-drift row and the contract_guard/Bash-git-rm negative finding
  </read_first>
  <action>
    Delete contracts/harness/docs/doc-dependencies.schema.json and, since it was the only file in
    that directory, the now-empty contracts/harness/docs/ directory. In the SAME commit, rebaseline
    contracts/.hashes/manifest.json via `uv run python -m tools.contract_hash.hash --write` — this
    removes exactly the one contracts/harness/docs/doc-dependencies.schema.json key and touches no
    other manifest entry. No HARNESS_DEV_BYPASS is needed: `git rm` (Bash) does not trigger
    contract_guard (matcher is Write|Edit only, confirmed), and the rebaseline script runs via `uv
    run python`, also not through the Claude Write/Edit tool. Stage with an explicit pathspec
    covering exactly these two paths, inspect `git diff --cached --name-only`, then commit. Verify
    with `uv run python -m tools.contract_drift.drift` (expect exit 0).
  </action>
  <acceptance_criteria>
    - `test ! -f contracts/harness/docs/doc-dependencies.schema.json` exits 0.
    - `test ! -d contracts/harness/docs` exits 0.
    - `grep -c doc-dependencies contracts/.hashes/manifest.json` returns 0.
    - `uv run python -m tools.contract_drift.drift` exits 0.
    - `git diff --cached --name-only` (checked immediately before commit) lists only paths under
      contracts/harness/docs/ and contracts/.hashes/manifest.json.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e contracts/harness/docs && uv run python -m tools.contract_hash.hash --write && ! grep -q doc-dependencies contracts/.hashes/manifest.json && uv run python -m tools.contract_drift.drift</automated>
  </verify>
  <done>The schema and its now-empty parent directory are gone; contracts/.hashes/manifest.json has the doc-dependencies key removed and no other change; contract-drift is green; one pathspec-scoped commit lands.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the CI docs-guard job and its fan-in needs entry</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml, lines 310-355 and 375-385 (exact excerpts already verified in this plan's interfaces section)
    .planning/phases/40-self-gate-teardown/40-01-SUMMARY.md, "Finding: the deletion-phase ordering constraint"
  </read_first>
  <action>
    Delete .github/workflows/ci.yml lines 317-351 wholesale (the docs-guard comment block through
    the job's closing step). Edit line 381's gate.needs list, removing only the docs-guard token —
    do not add, reorder, or remove any other entry. Resolve the YAML with a parser (not grep) to
    confirm the gate.needs list has exactly 11 entries and no dangling docs-guard reference (D-14):
    write a short Python script to the scratchpad directory that loads the workflow with PyYAML
    and prints jobs.gate.needs, then confirm by eye it is the expected 11-entry list. Stage with an
    explicit pathspec (git add -- .github/workflows/ci.yml), inspect
    `git diff --cached --name-only`, then commit.
  </action>
  <acceptance_criteria>
    - `grep -c docs-guard .github/workflows/ci.yml` returns 0.
    - `grep -n needs: .github/workflows/ci.yml` returns exactly 2 lines.
    - The YAML-parsed jobs.gate.needs list has exactly 11 entries, docs-guard absent, every other
      original entry present unchanged.
    - `git diff --cached --name-only` (checked immediately before commit) lists only
      .github/workflows/ci.yml.
  </acceptance_criteria>
  <verify>
    <automated>grep -c docs-guard .github/workflows/ci.yml | grep -qx 0</automated>
    <automated>test $(grep -c "needs:" .github/workflows/ci.yml) -eq 2</automated>
  </verify>
  <done>The docs-guard job and its comment block are gone; gate.needs lists exactly 11 entries with docs-guard removed and nothing else touched; one pathspec-scoped commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant trust boundary. This plan deletes a constitution-plane contract file (via
`git rm`, which does not trigger `contract_guard`) and a CI job definition; no request path, stored
data, auth, or secret is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Tampering | `contracts/.hashes/manifest.json` rebaseline | mitigate | `uv run python -m tools.contract_drift.drift` run explicitly as an acceptance criterion, in the same commit as the schema deletion (D-02) — a mismatched or partial rebaseline is caught before commit. |
| T-41-04-02 | Tampering | `.github/workflows/ci.yml` `gate.needs` edit | mitigate | Acceptance criterion asserts the exact resulting entry count (11) and the exact removed token — a mis-edit widening or narrowing the fan-in is caught locally, not just at CI dispatch (D-14, resolved via YAML parse, not grep). |
| T-41-04-SC | Tampering | package installs | n/a | No package install in this plan. |
</threat_model>

<verification>
`uv run python -m tools.contract_drift.drift` exits 0 after Task 1. A YAML parse of
`.github/workflows/ci.yml` after Task 2 shows `jobs.gate.needs` with exactly 11 entries and no
`docs-guard`. Full-suite green and the residue sweep are Plan 05's job.
</verification>

<success_criteria>
1. `contracts/harness/docs/doc-dependencies.schema.json` and its parent directory no longer exist.
2. `contracts/.hashes/manifest.json` is rebaselined with only that one key removed; contract-drift
   is green.
3. The CI `docs-guard` job no longer exists; `gate.needs` has 11 entries, `docs-guard` absent, every
   other entry unchanged, verified by YAML parse.
4. Two pathspec-scoped commits land.
</success_criteria>

<output>
Create `.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md` when done. Quote the exact
YAML-parsed `jobs.gate.needs` list as observed evidence, not a restated assumption.
</output>
