---
phase: 41-docs-review-plane-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/doc-dependencies.toml
  - docs/.docs-review-ledger.toml
  - tools/docs_guard/
  - docs/reference/doc-dependencies.md
autonomous: true
requirements: [CER-05]
must_haves:
  truths:
    - "grep -c '^\\[\\[binding\\]\\]' docs/doc-dependencies.toml returns 0 before the file itself is deleted (D-09 unbind-first)"
    - "No file under tools/docs_guard/ exists anywhere in the tree"
    - "docs/.docs-review-ledger.toml does not exist"
    - "No commit in this plan adds or edits a [[reviewed]] disposition row (D-08 — deleting the ledger is not authoring one)"
  artifacts:
    - path: "tools/docs_guard"
      provides: "MUST NOT EXIST after this plan (6110 LOC removed)"
    - path: "docs/doc-dependencies.toml"
      provides: "MUST NOT EXIST after this plan (D-02 — registry has no consumer once the guard is gone)"
    - path: "docs/.docs-review-ledger.toml"
      provides: "MUST NOT EXIST after this plan"
    - path: "docs/reference/doc-dependencies.md"
      provides: "MUST NOT EXIST after this plan (derived page, no source left to generate it from)"
  key_links: []
---

<objective>
Unbind the docs-review plane (D-09) and delete its guard package and registry/ledger data files —
the first two steps of RESEARCH.md's Safe Deletion Order. This plan does NOT touch the hook, the
emitter, the contract, the CI job, or any consumer — those are later, sequential plans (they depend
on this plan's deletions landing first, per D-09/D-10).

Purpose: nothing downstream (the guard, later phases) may ever again point at a `[[binding]]` row
or a ledger disposition once this plan lands. CER-05, ROADMAP Phase 41 scope items 1-3, D-01/D-02/
D-08/D-09.

Output: two commits. Commit 1 empties the registry and deletes the ledger. Commit 2 deletes the
guard package, the now-bindingless registry file itself, and its derived reference page.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-review-plane-removal/41-CONTEXT.md
@.planning/phases/41-docs-review-plane-removal/41-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-VALIDATION.md
@.planning/phases/40-self-gate-teardown/40-01-SUMMARY.md

<interfaces>
<!-- Verified this session (2026-07-27). Executor should use these directly. -->

From `docs/doc-dependencies.toml` (verified count):
- `grep -c '^\[\[binding\]\]' docs/doc-dependencies.toml` = **8** (mechanical count, matches D-09;
  a loose grep counting a prose mention returns 9 — do not use that count).
- The file's header comment (lines 1-19) documents the registry's own design rationale (agent-
  writable registry vs human-only ledger, GEN-04 core-only scope). This entire file is deleted in
  this plan's second commit — do not preserve any of its prose "for the record"; ADR-0010/0012
  already hold the history (D-06).

From `tools/adoption_apply/tests/test_docs_binding_proposal.py` (244 lines) — **NOT touched by this
plan.** It imports `contracts/harness/docs/doc-dependencies.schema.json` and `docs/doc-dependencies.toml`
by path (`REGISTRY_REL`, line 45) and imports `tools.hooks.ledger_guard` (line 42). It goes red the
moment this plan's commit 2 lands (the registry file it reads is gone) — that is EXPECTED and is
Plan 02's job to delete (D-04), not this plan's. Do not delete it here; do not skip it either — its
red state between Plan 01 and Plan 02 landing is a known, accepted intermediate (this repo is not
required to be green mid-plan, only at phase close, per D-10's precedent).

`contracts/harness/docs/` currently contains exactly one file
(`doc-dependencies.schema.json`) — **not deleted by this plan** (Plan 04's job, contract-plane
rebaseline). Leaving it in place after this plan's guard-package deletion is correct and expected;
the schema and its rebaseline are a distinct, later step (D-02's "same commit as contract removal"
rule applies to Plan 04, not here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unbind — empty the registry and delete the ledger (D-09)</name>
  <files>docs/doc-dependencies.toml, docs/.docs-review-ledger.toml</files>
  <read_first>
    .planning/phases/41-docs-review-plane-removal/41-CONTEXT.md (D-01, D-08, D-09, D-10, D-11)
    docs/doc-dependencies.toml
    docs/.docs-review-ledger.toml
  </read_first>
  <action>
    Confirm the binding count is exactly 8 via `grep -c '^\[\[binding\]\]' docs/doc-dependencies.toml`
    before editing (D-09's mechanical count, never prose). Remove all 8 `[[binding]]` table entries
    from `docs/doc-dependencies.toml`, leaving the file's header comment block (lines 1-19) intact
    for now — the whole file is deleted in Task 2, but per D-09 the unbind step must land as its own
    commit before any tool deletion, so this task edits the file rather than deleting it outright.
    Delete `docs/.docs-review-ledger.toml` (90 lines) entirely — this is a file deletion, not an edit
    to a `[[reviewed]]` row, so it does not violate D-08's "no commit may add or modify a reviewed
    disposition" rule. Stage with an explicit pathspec (`git add -- docs/doc-dependencies.toml
    docs/.docs-review-ledger.toml`), inspect `git diff --cached --name-only`, then commit with
    `git commit -- docs/doc-dependencies.toml docs/.docs-review-ledger.toml`. Do not use
    `git add -A`/`git add .`/`git commit -a` (D-11).
  </action>
  <acceptance_criteria>
    - `grep -c '^\[\[binding\]\]' docs/doc-dependencies.toml` returns 0.
    - `test ! -f docs/.docs-review-ledger.toml` exits 0.
    - `git diff --cached --name-only` (checked immediately before the commit) lists exactly
      `docs/doc-dependencies.toml` and `docs/.docs-review-ledger.toml`, nothing else.
    - The committed diff for `docs/doc-dependencies.toml` shows only `[[binding]]` table removals —
      no `[[reviewed]]` row added or edited anywhere in this commit.
  </acceptance_criteria>
  <verify>
    <automated>test $(grep -c '^\[\[binding\]\]' docs/doc-dependencies.toml) -eq 0 && test ! -f docs/.docs-review-ledger.toml</automated>
  </verify>
  <done>docs/doc-dependencies.toml has zero binding rows; docs/.docs-review-ledger.toml is gone; both changes are in one committed pathspec-scoped commit.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the guard package, the emptied registry, and its derived page</name>
  <files>tools/docs_guard/, docs/doc-dependencies.toml, docs/reference/doc-dependencies.md</files>
  <read_first>
    .planning/phases/41-docs-review-plane-removal/41-RESEARCH.md ("Verified Deletion/Edit Inventory" DELETE table, "Safe Deletion Order" step 2)
    .planning/phases/40-self-gate-teardown/40-01-SUMMARY.md ("Finding: the deletion-phase ordering constraint")
  </read_first>
  <action>
    Delete the entire `tools/docs_guard/` directory: `guard.py`, `cli.py`, `ledger.py`, `registry.py`,
    `impact.py`, `digest.py`, `exclusions.py`, `__main__.py`, `__init__.py`, `pyproject.toml`, and its
    `tests/` subdirectory (9 files) — 6110 LOC total (verified `wc -l`). Delete
    `docs/doc-dependencies.toml` outright now that it is bindingless and has no consumer left once
    the guard is gone (D-02). Delete `docs/reference/doc-dependencies.md` (the generated page for the
    now-deleted registry). Do NOT touch `contracts/harness/docs/doc-dependencies.schema.json` — that
    is Plan 04's rebaseline-paired deletion. Stage with an explicit pathspec covering exactly these
    three targets, inspect `git diff --cached --name-only`, then commit. Per D-10, `uv run pytest
    --collect-only -q` will show a collection error for `tools/docs_guard`'s own now-nonexistent
    tests only if something outside this deletion still imports it — expected result here is a clean
    collection since nothing internal to `tools/docs_guard` imports anything else. Do not run the
    full suite expecting green yet: `tools/memory_regen/docs_staleness.py` still imports
    `tools.docs_guard` at this point and will only be fixed in Plan 03 — a `ModuleNotFoundError` from
    that module (and from `tools/adoption_apply/tests/test_docs_binding_proposal.py`) after this
    commit is the EXPECTED intermediate state, not a defect to fix here.
  </action>
  <acceptance_criteria>
    - `test ! -e tools/docs_guard` exits 0.
    - `test ! -f docs/doc-dependencies.toml` exits 0.
    - `test ! -f docs/reference/doc-dependencies.md` exits 0.
    - `git diff --cached --name-only` (checked immediately before commit) lists only paths under
      `tools/docs_guard/`, `docs/doc-dependencies.toml`, and `docs/reference/doc-dependencies.md`.
    - `contracts/harness/docs/doc-dependencies.schema.json` still exists (untouched — Plan 04's job).
  </acceptance_criteria>
  <verify>
    <automated>test ! -e tools/docs_guard && test ! -f docs/doc-dependencies.toml && test ! -f docs/reference/doc-dependencies.md && test -f contracts/harness/docs/doc-dependencies.schema.json</automated>
  </verify>
  <done>tools/docs_guard/ (6110 LOC), docs/doc-dependencies.toml, and docs/reference/doc-dependencies.md are all gone in one committed pathspec-scoped commit; the schema file is untouched pending Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant trust boundary. This is a deletion of a dev-tooling package and two data
files; no request path, stored data, auth, or secret is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | `docs/doc-dependencies.toml` edit (Task 1) | mitigate | Acceptance criterion asserts the committed diff touches only `[[binding]]` rows, never a `[[reviewed]]` disposition — D-08's "deleting is not authoring" rule is checked mechanically, not by review alone. |
| T-41-01-02 | (operational) intermediate red state between this plan and Plan 03 | n/a — not a security threat | accept | `tools/memory_regen/docs_staleness.py` and `tools/adoption_apply/tests/test_docs_binding_proposal.py` will red after Task 2's commit because they still import the deleted package; this is expected per D-09's ordering and is fixed by Plan 02/03, not backed out here. |
| T-41-01-SC | Tampering | package installs | n/a | No package install in this plan. |
</threat_model>

<verification>
After Task 2's commit: `uv run pytest --collect-only -q` is expected to show exactly two
collection failures (`tools/memory_regen/tests/test_docs_staleness.py` and
`tools/adoption_apply/tests/test_docs_binding_proposal.py`, both importing the now-deleted
`tools.docs_guard`) — record this as the expected intermediate state, not a regression. Full green
is not required until Plan 05's final wave.
</verification>

<success_criteria>
1. `docs/doc-dependencies.toml` has zero `[[binding]]` rows before it is itself deleted (D-09).
2. `docs/.docs-review-ledger.toml` no longer exists anywhere in the tree.
3. `tools/docs_guard/` (6110 LOC) no longer exists anywhere in the tree.
4. `docs/doc-dependencies.toml` and `docs/reference/doc-dependencies.md` no longer exist.
5. Two commits land, each scoped to an explicit pathspec, neither touching `[[reviewed]]`.
</success_criteria>

<output>
Create `.planning/phases/41-docs-review-plane-removal/41-01-SUMMARY.md` when done. State the exact
LOC removed (`git diff --stat` across both commits) and name the two now-expected-red test files
that Plan 02/03 will fix.
</output>
