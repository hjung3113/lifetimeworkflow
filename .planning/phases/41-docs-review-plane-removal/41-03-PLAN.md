---
phase: 41-docs-review-plane-removal
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - harness/commands/docs-update.md
  - harness/skills/docs-upkeep/
  - tools/harness_emit/emit-manifest.json
  - .opencode/
  - .claude/
  - AGENTS.md
  - tools/memory_regen/docs_staleness.py
  - tools/memory_regen/tests/test_docs_staleness.py
  - tools/memory_regen/inject.py
  - tools/memory_regen/tests/test_inject_docs_pointer.py
autonomous: true
requirements: [CER-05]
must_haves:
  truths:
    - "harness/commands/docs-update.md and harness/skills/docs-upkeep/ no longer exist"
    - "Re-emitting produces an empty diff against the committed .opencode/ and .claude/ trees (emit-drift clean) after the source deletion lands"
    - "The .claude/settings.json ledger_guard PreToolUse hook group is gone as a re-emit side effect of Plan 02's merge.py edit"
    - "tools/memory_regen/docs_staleness.py and its two dependent test files no longer exist, and inject.py no longer has a docs pointer row"
  artifacts:
    - path: "harness/commands/docs-update.md"
      provides: "MUST NOT EXIST after this plan"
    - path: "harness/skills/docs-upkeep"
      provides: "MUST NOT EXIST after this plan"
    - path: "tools/memory_regen/docs_staleness.py"
      provides: "MUST NOT EXIST after this plan (233 LOC removed)"
  key_links:
    - from: "tools/memory_regen/inject.py"
      to: "tools.memory_regen.docs_staleness"
      via: "assemble() row tuple"
      pattern: "_docs_staleness_pointer"
      note: "this pattern MUST NOT match after the plan — it is an absence link"
---

<objective>
Delete the runtime surface at its source (`harness/commands/docs-update.md`,
`harness/skills/docs-upkeep/`), re-emit both runtime trees so the deletion propagates and Plan 02's
`merge.py` edit removes the `ledger_guard` hook group from `.claude/settings.json`, and delete the
derived staleness queue (`tools/memory_regen/docs_staleness.py` + its test + the `inject.py` pointer
row + its test).

Purpose: RESEARCH.md's Safe Deletion Order steps 4-5. `tools/harness_emit/emit-manifest.json`
auto-repairs via re-emit (never a hand-edit, per Phase-7/9 precedent) — this plan proves that rather
than assumes it. D-03/D-12.

Output: two pathspec-scoped commits — one for the source deletion + re-emit output, one for the
staleness-queue deletion.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-review-plane-removal/41-CONTEXT.md
@.planning/phases/41-docs-review-plane-removal/41-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-02-SUMMARY.md

<interfaces>
<!-- Verified this session (2026-07-27). -->

From `tools/harness_emit/emit-manifest.json` (verified this session): the file currently lists
`.claude/commands/docs-update.md`, `.opencode/command/docs-update.md`,
`.claude/skills/docs-upkeep/SKILL.md`, `.opencode/skill/docs-upkeep/SKILL.md`, and
`.opencode/plugin/ledger-guard.ts` among its `paths` array (the exact rows CONTEXT.md's D-12 names
at `:18,41,71,89,101`). Do NOT hand-edit this JSON — it is prune-then-write, regenerated by
`tools.harness_emit.generate.emit()` on every run. Deleting the two `harness/` source files and
re-emitting removes these five rows automatically.

From `tools/memory_regen/inject.py` (verified this session): the `assemble()` row tuple list
(around line 210-218) contains `("docs", _docs_staleness_pointer(derived_dir)),` immediately after
the `contracts` row and before the `repomap` row — remove exactly this one tuple row. The
`_docs_staleness_pointer` function definition itself (around line 82-95) imports from
`tools.docs_guard` indirectly via `docs_staleness.py` — delete the function definition too.

From `tools/memory_regen/docs_staleness.py` (233 LOC, verified): imports
`tools.docs_guard.guard.{DEFAULT_LEDGER_PATH, REPO_ROOT, classify}` and
`tools.docs_guard.impact.impact_map` / `tools.docs_guard.registry.DEFAULT_REGISTRY_PATH` — both
packages are already deleted (Plan 01), so this module is already import-broken; this plan simply
removes the dead file.

From `AGENTS.md:104-109` (verified this session, `<!-- BEGIN HARNESS-MANAGED -->` fence opens at
line 104): the command/skill index lines (106-107) inside this fence are spliced by
`tools.harness_emit.generate.emit()` on every run — do NOT hand-edit them. Re-emitting in this plan
repairs them automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the runtime surface at source and re-emit both trees</name>
  <files>harness/commands/docs-update.md, harness/skills/docs-upkeep/, tools/harness_emit/emit-manifest.json, .opencode/, .claude/, AGENTS.md</files>
  <read_first>
    harness/commands/docs-update.md
    harness/skills/docs-upkeep/SKILL.md
    .planning/phases/41-docs-review-plane-removal/41-RESEARCH.md ("Safe Deletion Order" step 4, "Pitfall 1: Assuming emit-manifest.json needs a hand-edit")
    tools/harness_emit/emit-manifest.json
  </read_first>
  <action>
    Delete `harness/commands/docs-update.md` and the whole `harness/skills/docs-upkeep/` directory
    (its `SKILL.md`). Then run `python -m tools.harness_emit` (via `uv run python -m
    tools.harness_emit`) — this prunes the five now-orphaned rows from
    `tools/harness_emit/emit-manifest.json`, deletes the projected files from `.opencode/command/`,
    `.opencode/skill/`, `.opencode/plugin/ledger-guard.ts`, `.claude/commands/`, `.claude/skills/`,
    removes the `ledger_guard` PreToolUse hook group from `.claude/settings.json` (a consequence of
    Plan 02's `merge.py` edit already landed), and splices the `AGENTS.md` managed-block
    command/skill index. Do NOT hand-edit any file under `.opencode/` or `.claude/` or the
    emit-manifest JSON directly. Stage the source deletion together with every re-emit output file
    with an explicit pathspec (do not use `git add -A`/`git add .`), inspect
    `git diff --cached --name-only`, then commit. Verify emit-drift is clean by re-running the
    emitter a second time and confirming `git diff --exit-code` over the emitted path set is empty.
  </action>
  <acceptance_criteria>
    - `test ! -f harness/commands/docs-update.md` exits 0.
    - `test ! -d harness/skills/docs-upkeep` exits 0.
    - `grep -c 'docs-update\|docs-upkeep' tools/harness_emit/emit-manifest.json` returns 0.
    - `find .opencode .claude -iname '*docs-update*' -o -iname '*docs-upkeep*'` returns nothing.
    - `grep -c 'ledger_guard' .claude/settings.json` returns 0.
    - Re-running `uv run python -m tools.harness_emit` a second time and diffing produces an empty
      diff (idempotent re-emit).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m tools.harness_emit && test ! -f harness/commands/docs-update.md && test ! -d harness/skills/docs-upkeep && ! grep -q 'docs-update\|docs-upkeep' tools/harness_emit/emit-manifest.json && ! grep -q 'ledger_guard' .claude/settings.json</automated>
    <automated>uv run python -m tools.harness_emit && git add -A -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json && git diff --cached --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json</automated>
  </verify>
  <done>docs-update command and docs-upkeep skill are gone at source; re-emit produced the pruned emit-manifest.json, removed the projected files from both runtime trees, dropped the ledger_guard hook group from .claude/settings.json, and repaired AGENTS.md; the re-emit is idempotent (empty diff on second run); one pathspec-scoped commit lands.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the derived staleness queue</name>
  <files>tools/memory_regen/docs_staleness.py, tools/memory_regen/tests/test_docs_staleness.py, tools/memory_regen/inject.py, tools/memory_regen/tests/test_inject_docs_pointer.py</files>
  <read_first>
    tools/memory_regen/docs_staleness.py
    tools/memory_regen/inject.py
    tools/memory_regen/tests/test_inject_docs_pointer.py
    .planning/phases/41-docs-review-plane-removal/41-CONTEXT.md (D-03)
  </read_first>
  <action>
    Delete `tools/memory_regen/docs_staleness.py` (233 LOC) and
    `tools/memory_regen/tests/test_docs_staleness.py` (273 lines) outright. Delete
    `tools/memory_regen/tests/test_inject_docs_pointer.py` (186 lines) outright. Edit
    `tools/memory_regen/inject.py`: remove the `_docs_staleness_pointer` function definition
    (~lines 82-95) and remove the `("docs", _docs_staleness_pointer(derived_dir)),` row from the
    `assemble()` tuple list (~line 217), leaving the `contracts` row directly followed by the
    `repomap` row. Do not touch any other row (`agreements`, `banner`, `drift`, `task`, `active`).
    Stage with an explicit pathspec covering exactly these four files, inspect
    `git diff --cached --name-only`, then commit. Verify by running `uv run pytest
    tools/memory_regen -q` and confirming `python -m tools.memory_regen.inject` still runs (no
    `NameError`/`ImportError` for the removed function).
  </action>
  <acceptance_criteria>
    - `test ! -f tools/memory_regen/docs_staleness.py` exits 0.
    - `test ! -f tools/memory_regen/tests/test_docs_staleness.py` exits 0.
    - `test ! -f tools/memory_regen/tests/test_inject_docs_pointer.py` exits 0.
    - `grep -c '_docs_staleness_pointer\|docs_staleness' tools/memory_regen/inject.py` returns 0.
    - `uv run pytest tools/memory_regen -q` exits 0.
    - `git diff --cached --name-only` (checked immediately before commit) lists exactly the four
      files in this task's `<files>`.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f tools/memory_regen/docs_staleness.py && test ! -f tools/memory_regen/tests/test_docs_staleness.py && test ! -f tools/memory_regen/tests/test_inject_docs_pointer.py && ! grep -q 'docs_staleness' tools/memory_regen/inject.py && uv run pytest tools/memory_regen -q</automated>
  </verify>
  <done>docs_staleness.py and both its dependent test files are gone; inject.py has no docs-staleness row or function; uv run pytest tools/memory_regen -q is green; one pathspec-scoped commit lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant trust boundary. This plan deletes a runtime command/skill surface and a
derived-plane pointer computation; no request path, stored data, or secret is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Tampering | hand-edited `.opencode/`/`.claude/` trees | mitigate | Acceptance criteria require the source deletion to happen FIRST and the re-emit to be the only path that touches the emitted trees; the idempotent-re-emit check catches any residual drift. |
| T-41-03-02 | Repudiation | `inject.py`'s SessionStart payload silently still references a deleted module | mitigate | `uv run pytest tools/memory_regen -q` plus a manual `python -m tools.memory_regen.inject` smoke run catch a stray reference before commit. |
| T-41-03-SC | Tampering | package installs | n/a | No package install in this plan. |
</threat_model>

<verification>
`uv run python -m tools.harness_emit` re-run is idempotent (empty diff). `uv run pytest
tools/memory_regen -q` green. `grep -rn 'docs-update\|docs-upkeep' .opencode .claude
tools/harness_emit/emit-manifest.json AGENTS.md` returns nothing.
</verification>

<success_criteria>
1. `harness/commands/docs-update.md` and `harness/skills/docs-upkeep/` no longer exist.
2. Re-emit produced an empty second-run diff (emit-drift clean) and removed the `ledger_guard` hook
   group from `.claude/settings.json` as a consequence of Plan 02's `merge.py` edit.
3. `tools/memory_regen/docs_staleness.py` (233 LOC) and its two dependent test files no longer
   exist; `inject.py` has no docs-staleness row.
4. Two pathspec-scoped commits land.
</success_criteria>

<output>
Create `.planning/phases/41-docs-review-plane-removal/41-03-SUMMARY.md` when done. Confirm the
emit-drift idempotency check's exact output and name the LOC removed from the staleness queue.
</output>
