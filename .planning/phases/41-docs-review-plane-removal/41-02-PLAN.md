---
phase: 41-docs-review-plane-removal
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - tools/hooks/ledger_guard.py
  - harness/plugins/ledger-guard.ts
  - harness/permission-matrix.json
  - tools/harness_emit/merge.py
  - tools/adoption_apply/apply.py
  - tools/adoption_apply/tests/test_constitution_refusal.py
  - tools/adoption_apply/tests/test_docs_binding_proposal.py
autonomous: true
requirements: [CER-05]
must_haves:
  truths:
    - "No file imports tools.hooks.ledger_guard anywhere in the tree"
    - "tools.adoption_apply.apply imports cleanly (no ModuleNotFoundError) after ledger_guard is deleted"
    - "harness/permission-matrix.json no longer names docs/.docs-review-ledger.toml in path_deny_globs and its _note prose no longer describes a third path-deny domain"
    - "tools/harness_emit/merge.py no longer references tools.hooks.ledger_guard in HARNESS_SIGNATURES or the hook-group list"
  artifacts:
    - path: "tools/hooks/ledger_guard.py"
      provides: "MUST NOT EXIST after this plan"
    - path: "harness/plugins/ledger-guard.ts"
      provides: "MUST NOT EXIST after this plan"
    - path: "tools/adoption_apply/apply.py"
      provides: "ReviewLedgerRefusal class, the ledger_guard import, and the ledger-glob check removed; refuse_unsafe_destination and apply_manifest still work for the surviving ConstitutionRefusal path"
  key_links:
    - from: "tools/adoption_apply/apply.py"
      to: "tools.hooks.ledger_guard"
      via: "module-level import"
      pattern: "from tools\\.hooks\\.ledger_guard import"
      note: "this pattern MUST NOT match after the plan — it is an absence link"
---

<objective>
Delete the `ledger_guard` PreToolUse hook and its opencode twin, edit the two files that reference
it as data (`harness/permission-matrix.json`'s deny glob + `_note`, and `tools/harness_emit/merge.py`'s
hand-maintained hook-wiring literal), and fix the two undocumented Python-level consumers this
phase's RESEARCH.md found: `tools/adoption_apply/apply.py` (a module-level import that will break
`import tools.adoption_apply.apply` the moment the hook file is gone) and its test file
`tools/adoption_apply/tests/test_constitution_refusal.py` (a large parametrized ledger-specific test
section, lines ~370-601, distinct from the already-scoped `test_docs_binding_proposal.py`). Also
delete `test_docs_binding_proposal.py` outright (D-04 — the adoption docs-binding proposal path).

Purpose: RESEARCH.md's primary finding — deleting the hook file alone breaks `tools.adoption_apply`
at import time, cascading into every test that imports it. This plan closes that gap in the same
wave the hook itself is deleted, per CONTEXT.md D-01/D-04 and RESEARCH.md's Pitfall 2.

Output: this plan's tasks land as two pathspec-scoped commits. After this plan, nothing in the tree
imports `tools.hooks.ledger_guard`, and `tools.adoption_apply` imports cleanly.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-review-plane-removal/41-CONTEXT.md
@.planning/phases/41-docs-review-plane-removal/41-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-01-SUMMARY.md

<interfaces>
<!-- Verified this session (2026-07-27) by direct read. Executor should use these directly. -->

From `harness/permission-matrix.json` (verified exact):
- `_note` (line 2) is one long paragraph; the sentence to remove ends "...`docs/.docs-review-ledger.toml`
  is a THIRD path-deny domain, distinct from constitution and from secret: it is the docs plane's
  greenness authority, so only a human may author a review disposition and no token (not even
  GOLDEN_APPROVE_HUMAN, which authorizes constitution writes) legitimizes an agent-authored one.
  Note it does NOT cover `docs/doc-dependencies.toml`..." — remove this ledger-specific sentence
  span; keep every other clause in `_note` (the CONFIG-02/resolver/bash-ordering/constitution-glob
  prose is unrelated and stays verbatim).
- `path_deny_globs` (around line 34) currently ends with the literal string
  `"docs/.docs-review-ledger.toml"` as its last array entry before the closing `]` — remove exactly
  that one array entry, changing nothing else in the array (the `golden/**`, `docs/glossary.md`,
  `*.env`, `**/*.env` entries stay).

From `tools/harness_emit/merge.py` (verified exact, lines 88 and 168-183):
- `HARNESS_SIGNATURES` tuple currently has 6 entries: `tools.hooks.format_on_write`,
  `tools.hooks.contract_guard`, `tools.hooks.ledger_guard`, `tools.hooks.secret_scan`,
  `tools.hooks.commit_gate`, `tools.hooks.resume_gate`. Remove the `tools.hooks.ledger_guard` line,
  leaving 5.
- The hook-group dict block (`{"matcher": "Write|Edit", "hooks": [{"type": "command", "command":
  _GUARD_PREFIX + "uv run python -m tools.hooks.ledger_guard", "timeout": 10}]}`, preceded by a
  multi-line comment referencing ADR-0010 clause 3b and a test name
  `test_ledger_guard_is_wired_into_pretooluse` that does not exist under that exact name anywhere in
  the repo — a stale/aspirational comment reference, confirmed by repo-wide grep) — remove the
  entire dict block AND its preceding comment. This is hand-maintained Python inside the emitter
  tool, NOT a `harness/` source file — editing it does not require a re-emit of anything else, but
  Plan 03's re-emit will pick up the resulting `.claude/settings.json` change.

From `tools/adoption_apply/apply.py` (verified exact, this session):
- Lines ~53-64: a comment block explaining "the human-review ledger — a THIRD path-deny domain...
  IMPORTED as DATA from the PreToolUse gate that owns it" — remove this comment block along with the
  import it justifies.
- Line 65: `from tools.hooks.ledger_guard import REVIEW_LEDGER_GLOBS` — remove.
- `class ReviewLedgerRefusal(ValueError):` (with its docstring, ~lines 72-79) — remove the whole
  class.
- Inside `refuse_unsafe_destination(...)`: the block `if resolve_path(REVIEW_LEDGER_GLOBS,
  relative.lower()) == "deny": raise ReviewLedgerRefusal(...)` (verified at the point immediately
  before `refuse_if_constitution(relative.lower())` is called) — remove this block; the function's
  remaining body still ends with the `refuse_if_constitution(relative.lower())` call and
  `return target_path`, unchanged.
- Inside `apply_manifest(...)`: `except (ConstitutionRefusal, ReviewLedgerRefusal):` becomes
  `except ConstitutionRefusal:` — the `summary["refused"].append(destination); continue` body is
  unchanged.

From `tools/adoption_apply/tests/test_constitution_refusal.py` (verified exact, this session,
601 lines total):
- Line 20: `from tools.hooks import ledger_guard` — remove.
- Everything from the `REVIEW_LEDGER_DESTINATIONS = [...]` constant (line ~380) through the end of
  the file (line 601) is ledger-specific: the constant itself, and the test functions
  `test_review_ledger_destination_is_refused`, `test_review_ledger_destination_is_refused_end_to_end`,
  `test_review_ledger_adjacent_destination_stays_allowed`,
  `test_review_ledger_ordinary_tool_path_is_denied_by_a_real_hook`,
  `test_review_ledger_hook_honours_no_token_and_no_dev_bypass`,
  `test_review_ledger_hook_leaves_the_registry_and_neighbours_writable`,
  `test_review_ledger_hook_is_a_disjoint_third_domain`,
  `test_review_ledger_hook_is_wired_into_the_emitted_pretooluse_set`,
  `test_review_ledger_permission_matrix_still_carries_the_glob`,
  `test_review_ledger_permission_matrix_keeps_registry_writable`,
  `test_review_ledger_matrix_edit_left_existing_denies_intact` — remove this entire trailing section
  (line ~370 through 601, everything after the last non-ledger test in the file), leaving the file's
  earlier `ConstitutionRefusal`-only tests (the ones not listed above) intact and passing. Per
  RESEARCH.md's Open Question 2 recommendation: delete the rows/tests wholesale rather than keeping
  them as dangling negative controls against now-nonexistent paths.

From `tools/adoption_apply/tests/test_docs_binding_proposal.py` (244 lines, verified this session) —
imports `tools.hooks.ledger_guard` (line 42), `tools.adoption_scan.plan`,
`contracts/harness/docs/doc-dependencies.schema.json` by path (line 56/120), and
`docs/doc-dependencies.toml` (`REGISTRY_REL`, line 45) — the latter two are already deleted by
Plan 01. Delete this file outright (D-04); it is the "adoption docs-binding proposal" path
CONTEXT.md names, and no production code in `adoption_apply`/`adoption_scan` has a dedicated
doc-binding-proposal branch outside this test file (verified: no non-test hit for
`doc-dependencies`/`docs_binding`/`DOCSUP-07` in either package's source modules).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the hook + its emitter wiring, edit permission-matrix.json</name>
  <files>tools/hooks/ledger_guard.py, harness/plugins/ledger-guard.ts, harness/permission-matrix.json, tools/harness_emit/merge.py</files>
  <read_first>
    tools/hooks/ledger_guard.py
    harness/plugins/ledger-guard.ts
    harness/permission-matrix.json
    tools/harness_emit/merge.py
    .planning/phases/41-docs-review-plane-removal/41-RESEARCH.md ("The plane's own surface (delete targets)" and its merge.py note)
  </read_first>
  <action>
    Delete `tools/hooks/ledger_guard.py` (90 lines) and `harness/plugins/ledger-guard.ts` outright.
    Edit `harness/permission-matrix.json`: remove the `"docs/.docs-review-ledger.toml"` entry from
    `path_deny_globs`, and trim the `_note` prose sentence describing the ledger as a "THIRD
    path-deny domain" (exact span given in this plan's `<interfaces>` block) — leave every other
    clause of `_note` untouched. Edit `tools/harness_emit/merge.py`: remove
    `"tools.hooks.ledger_guard"` from `HARNESS_SIGNATURES` (6 entries -> 5) and delete the
    `ledger_guard` hook-group dict block plus its preceding ADR-0010-clause-3b comment (lines
    ~168-183). Do NOT hand-edit `.claude/settings.json` or `.opencode/**` — that is Plan 03's
    re-emit job, run after `harness/commands/docs-update.md` and `harness/skills/docs-upkeep/` are
    also deleted. Stage with an explicit pathspec covering exactly these four files, inspect
    `git diff --cached --name-only`, then commit with `git commit -- <pathspec>`.
  </action>
  <acceptance_criteria>
    - `test ! -f tools/hooks/ledger_guard.py` exits 0.
    - `test ! -f harness/plugins/ledger-guard.ts` exits 0.
    - `grep -c 'docs/.docs-review-ledger.toml' harness/permission-matrix.json` returns 0.
    - `grep -ci 'third path-deny domain' harness/permission-matrix.json` returns 0.
    - `grep -c 'tools.hooks.ledger_guard' tools/harness_emit/merge.py` returns 0.
    - `git diff --cached --name-only` (checked immediately before commit) lists exactly the four
      files in this task's `<files>`.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f tools/hooks/ledger_guard.py && test ! -f harness/plugins/ledger-guard.ts && ! grep -q 'docs/.docs-review-ledger.toml' harness/permission-matrix.json && ! grep -qi 'third path-deny domain' harness/permission-matrix.json && ! grep -q 'tools.hooks.ledger_guard' tools/harness_emit/merge.py</automated>
  </verify>
  <done>ledger_guard.py and ledger-guard.ts are gone; permission-matrix.json's glob entry and note prose are trimmed; merge.py's HARNESS_SIGNATURES and hook-group dict no longer name ledger_guard; one pathspec-scoped commit lands.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the undocumented adoption_apply consumer and delete the docs-binding proposal test</name>
  <files>tools/adoption_apply/apply.py, tools/adoption_apply/tests/test_constitution_refusal.py, tools/adoption_apply/tests/test_docs_binding_proposal.py</files>
  <read_first>
    tools/adoption_apply/apply.py
    tools/adoption_apply/tests/test_constitution_refusal.py
    tools/adoption_apply/tests/test_docs_binding_proposal.py
    .planning/phases/41-docs-review-plane-removal/41-RESEARCH.md ("Pitfall 2: Missing the tools/adoption_apply import-time breakage" and "Code Examples")
  </read_first>
  <action>
    In `tools/adoption_apply/apply.py`: remove the `from tools.hooks.ledger_guard import
    REVIEW_LEDGER_GLOBS` import and its preceding justification comment, remove the
    `ReviewLedgerRefusal` exception class, remove the ledger-glob check block inside
    `refuse_unsafe_destination`, and change `except (ConstitutionRefusal, ReviewLedgerRefusal):` to
    `except ConstitutionRefusal:` inside `apply_manifest`. In
    `tools/adoption_apply/tests/test_constitution_refusal.py`: remove `from tools.hooks import
    ledger_guard` (line 20) and remove the entire ledger-specific trailing section (the
    `REVIEW_LEDGER_DESTINATIONS` constant through end-of-file, ~line 370-601), leaving the earlier
    `ConstitutionRefusal`-only tests intact. Delete `tools/adoption_apply/tests/test_docs_binding_proposal.py`
    outright (D-04). Stage with an explicit pathspec covering exactly these three files, inspect
    `git diff --cached --name-only`, then commit. Verify by running
    `uv run pytest tools/adoption_apply -q` and `uv run pytest --collect-only -q` — both must be
    clean; a `ModuleNotFoundError: tools.hooks.ledger_guard` at this point means an edit above was
    missed.
  </action>
  <acceptance_criteria>
    - `grep -c 'ledger_guard\|ReviewLedgerRefusal\|REVIEW_LEDGER_GLOBS' tools/adoption_apply/apply.py` returns 0.
    - `grep -c 'ledger_guard\|ReviewLedgerRefusal' tools/adoption_apply/tests/test_constitution_refusal.py` returns 0.
    - `test ! -f tools/adoption_apply/tests/test_docs_binding_proposal.py` exits 0.
    - `uv run pytest tools/adoption_apply -q` exits 0.
    - `uv run pytest --collect-only -q` exits 0 with no `ModuleNotFoundError: tools.hooks.ledger_guard`
      or `tools.docs_guard` anywhere in the collection output.
    - `git diff --cached --name-only` (checked immediately before commit) lists exactly the three
      files in this task's `<files>`.
  </acceptance_criteria>
  <verify>
    <automated>! grep -q 'ledger_guard\|ReviewLedgerRefusal\|REVIEW_LEDGER_GLOBS' tools/adoption_apply/apply.py && ! grep -q 'ledger_guard\|ReviewLedgerRefusal' tools/adoption_apply/tests/test_constitution_refusal.py && test ! -f tools/adoption_apply/tests/test_docs_binding_proposal.py && uv run pytest tools/adoption_apply -q</automated>
    <automated>uv run pytest --collect-only -q</automated>
  </verify>
  <done>apply.py has no ledger_guard reference; test_constitution_refusal.py has no ledger-specific tests; test_docs_binding_proposal.py is deleted; uv run pytest tools/adoption_apply -q is green; full-suite collection has no ModuleNotFoundError for either deleted package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant trust boundary. This plan deletes a PreToolUse hook (a dev-workflow gate, not
an access-control boundary for a shipped product) and edits a Python module that consumes its data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Tampering | `harness/permission-matrix.json` `path_deny_globs` edit | mitigate | Acceptance criterion asserts the exact removed entry and that no other glob is touched — a mis-edit widening or narrowing the constitution/secret deny domains is caught locally, not just at CI. |
| T-41-02-02 | Repudiation | `tools/harness_emit/merge.py` hook-group removal | mitigate | `uv run python -m tools.harness_emit` (Plan 03) plus `git diff --cached --exit-code` over the emitted trees proves the hook group is actually gone from `.claude/settings.json`, not merely from the source literal. |
| T-41-02-03 | (operational) import-time breakage in `tools.adoption_apply` | n/a — not a security threat | mitigate | Task 2's acceptance criteria run `uv run pytest --collect-only -q` explicitly to catch a `ModuleNotFoundError` before it reaches CI — this is RESEARCH.md's primary undocumented-consumer finding. |
| T-41-02-SC | Tampering | package installs | n/a | No package install in this plan. |
</threat_model>

<verification>
`uv run pytest tools/adoption_apply -q` and `uv run pytest --collect-only -q` both green after
Task 2's commit. Full-suite green is not required until Plan 05's final wave — other plans
(03, 04) still owe their own edits.
</verification>

<success_criteria>
1. No file imports `tools.hooks.ledger_guard`; the hook and its opencode twin no longer exist.
2. `harness/permission-matrix.json` and `tools/harness_emit/merge.py` no longer reference the
   ledger domain.
3. `tools.adoption_apply.apply` imports cleanly; `uv run pytest tools/adoption_apply -q` is green.
4. `test_docs_binding_proposal.py` no longer exists (D-04).
5. Two pathspec-scoped commits land.
</success_criteria>

<output>
Create `.planning/phases/41-docs-review-plane-removal/41-02-SUMMARY.md` when done. Name the exact
LOC removed from `apply.py` and `test_constitution_refusal.py` (via `git diff --stat`), and confirm
the `uv run pytest --collect-only -q` result.
</output>
