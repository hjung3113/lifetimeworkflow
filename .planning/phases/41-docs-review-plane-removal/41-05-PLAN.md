---
phase: 41-docs-review-plane-removal
plan: 05
type: execute
wave: 5
depends_on: ["41-04"]
files_modified:
  - tools/harness_lint/caps.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/hooks/tests/test_settings_coexist.py
  - tools/harness_lint/tests/test_docs_update_wiring.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - harness/skills/gate-model/SKILL.md
  - tools/harness_lint/tests/test_tests_are_isolatable.py
  - tools/harness_lint/tests/test_workspace_member_completeness.py
  - uv.lock
autonomous: true
requirements: [CER-05]
must_haves:
  truths:
    - "uv run pytest -q is green with no collection error, after uv.lock is refreshed and the docs_guard workspace member is gone"
    - "The residue sweep (grep across tools/ harness/ contracts/ docs/ .github/ .claude/ .opencode/ AGENTS.md .memory/README.md uv.lock) returns zero hits outside .planning/"
    - "emit-drift, stale-derived, contract-drift, and the ruff ratchet are all clean"
    - "CI fan-in gate.needs resolves with no dangling entry (confirmed in Plan 04, re-confirmed here as part of the final wave)"
  artifacts:
    - path: "tools/harness_lint/tests/test_docs_update_wiring.py"
      provides: "MUST NOT EXIST after this plan (its subject was deleted in Plan 03)"
    - path: "uv.lock"
      provides: "no logparser-docs-guard virtual workspace member"
  key_links:
    - from: "residue sweep"
      to: "tools/ harness/ contracts/ docs/ .github/ .claude/ .opencode/ AGENTS.md .memory/README.md uv.lock"
      via: "grep -rnE"
      pattern: "docs_guard|docs-guard|docs-review-ledger|ledger_guard|docs-upkeep|docs-update|doc-dependencies"
      note: "this pattern MUST return zero hits outside .planning/ after this plan — it is the phase's headline absence link"
---

<objective>
Sweep every remaining test/prose reference to the deleted plane (D-13), refresh `uv.lock` so the
`logparser-docs-guard` virtual workspace member disappears, and run the phase's full final
verification wave: `uv run pytest -q`, the residue grep sweep, `emit-drift`, `stale-derived`,
`contract-drift`, and the ruff ratchet.

Purpose: this is the phase's closing plan. Every prior plan (01-04) left the tree correct at the
file level but several tests still hardcode counts or names that must be updated to match (D-13,
RESEARCH.md Pitfall 5), and the workspace member removal has been deferred until now because it
depends on the guard package (deleted in Plan 01) actually being gone. Two D-13-named files were
independently re-verified this session and require correction to the phase's own scope claim,
recorded in this plan rather than left implicit: `test_workspace_member_completeness.py` DOES need
a one-word edit (a docstring example list, verified — CONTEXT.md's citation was directionally right
even though RESEARCH.md's own re-check called it a no-op); `.memory/README.md` needs NO edit
(confirmed zero hits for ledger/docs-review/docs_guard/third/domain in its 69 lines — the sentence
CONTEXT.md describes actually lives in `harness/permission-matrix.json` and `gate-model/SKILL.md`,
both already fixed in Plan 02).

Output: two pathspec-scoped commits (the prose/test sweep, then the uv.lock refresh) plus a
verification-only final step.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-docs-review-plane-removal/41-CONTEXT.md
@.planning/phases/41-docs-review-plane-removal/41-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-VALIDATION.md
@.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md

<interfaces>
Verified this session (2026-07-27), exact anchors.

From tools/harness_lint/caps.py: the EXPECTED_SKILLS frozenset (starting ~line 137) currently
includes the literal "docs-upkeep" (line 151) — remove it. Trim the "Phase 29 (Docs Drive Loop)
adds one skill: docs-upkeep..." comment sentence (lines 128-129) from the block comment above
EXPECTED_SKILLS; the surrounding comment lines about other phases' skill additions stay.

From tools/harness_emit/tests/test_coexist.py: `test_all_26_commands_emit_to_both_trees` (line 40)
asserts `len(opencode_cmds) == 26` and `len(claude_cmds) == 26` (lines 67-68) — change both literals
to 25. Its docstring (lines 41-50) narrates the phase-by-phase count history ending "Phase 36 adds
/discipline, taking it 25 -> 26" — append a line noting Phase 41 removes /docs-update, taking it
back to 25, so the narrative stays accurate rather than silently wrong.

From tools/hooks/tests/test_settings_coexist.py: `_NEW_GATES` (line 81) contains the row
`("PreToolUse", "tools.hooks.ledger_guard", "Write|Edit")` at line 88 — remove this one row; the
other four gate rows stay.

From tools/harness_lint/tests/test_docs_update_wiring.py: this is the DOCSUP-06 structural gate for
exactly harness/commands/docs-update.md + harness/skills/docs-upkeep/, both already deleted in
Plan 03 — delete this whole file now (ordering rule: a deleted harness/ artifact's dedicated gate
test dies in the same phase).

From tools/docs_sync/tests/test_docs_sync_determinism.py: EXPECTED_PAGES (line 28) is a frozenset
currently containing "doc-dependencies" (line 33) among 14 entries — remove that one entry, leaving
13. This is a different generator (tools/docs_sync, not tools/docs_guard) — do not touch anything
else in this file.

From harness/skills/gate-model/SKILL.md: trim exactly three docs-plane claims while leaving the
skill itself intact (Phase 44 deletes it whole):
1. The "Gated != unwatched" callout (lines 33-35) mentioning a doc-review binding — trim the
   binding-specific clause, keep the gated/ownership point.
2. The "review ledger" bullet (lines 43-47) describing docs/.docs-review-ledger.toml as the docs
   plane's greenness authority — remove this bullet entirely.
3. The hook-surface table row for ledger_guard (line 56) — remove this table row.
4. The numbered walkthrough's ledger mention (line 80, "docs/.docs-review-ledger.toml -> the review
   ledger, path-denied with no token path at all") — remove this walkthrough step/clause.

From tools/harness_lint/tests/test_tests_are_isolatable.py: line 19 has a docstring example
`"pytest tools/docs_guard ..."` — update the example to reference a still-existing package (e.g.
tools/harness_lint or tools/adoption_apply) instead; this file's assertion logic does not depend on
docs_guard existing (cosmetic only).

From tools/harness_lint/tests/test_workspace_member_completeness.py (verified this session, line
13): its module docstring lists PreToolUse guards generically — "contract_guard, secret_scan,
ledger_guard, commit_gate, resume_gate" — as illustrative prose explaining why a broken workspace
member cascades into every hook failing. Remove "ledger_guard, " from that list (leaving
contract_guard, secret_scan, commit_gate, resume_gate) — this is a docstring-only edit; the file has
no structural assertion naming docs_guard/ledger_guard/doc-dependencies, so nothing else in it
changes. (This corrects RESEARCH.md's Assumption A2, which found no hit; a direct grep this
session found this one docstring line.)

From uv.lock: the logparser-docs-guard virtual workspace member entry (formerly keyed by
`source = { virtual = "tools/docs_guard" }`) disappears once `uv sync --all-packages` is re-run
against the now-deleted tools/docs_guard/ directory — no pyproject.toml edit needed (the `tools/*`
glob member list already excludes it once the directory is gone, mirroring Phase 40's exact
precedent).

.memory/README.md: verified this session — zero hits for ledger, docs-review, docs_guard, third, or
domain anywhere in its 69 lines. No edit needed; CONTEXT.md's D-13 citation of this file is a
mis-citation (the sentence it describes lives in harness/permission-matrix.json and
gate-model/SKILL.md, both already fixed in Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prose and test sweep (D-13)</name>
  <files>tools/harness_lint/caps.py, tools/harness_emit/tests/test_coexist.py, tools/hooks/tests/test_settings_coexist.py, tools/harness_lint/tests/test_docs_update_wiring.py, tools/docs_sync/tests/test_docs_sync_determinism.py, harness/skills/gate-model/SKILL.md, tools/harness_lint/tests/test_tests_are_isolatable.py, tools/harness_lint/tests/test_workspace_member_completeness.py</files>
  <read_first>
    tools/harness_lint/caps.py
    tools/harness_emit/tests/test_coexist.py
    tools/hooks/tests/test_settings_coexist.py
    tools/docs_sync/tests/test_docs_sync_determinism.py
    harness/skills/gate-model/SKILL.md
    tools/harness_lint/tests/test_tests_are_isolatable.py
    tools/harness_lint/tests/test_workspace_member_completeness.py
    .planning/phases/41-docs-review-plane-removal/41-RESEARCH.md, "Pitfall 5: Assuming the count in test_coexist.py is self-correcting"
  </read_first>
  <action>
    Apply every edit named in this plan's interfaces section: remove "docs-upkeep" from caps.py's
    EXPECTED_SKILLS and trim its Phase-29 comment sentence; change test_coexist.py's two `== 26`
    literals to `== 25` and extend its docstring narrative; remove the ledger_guard row from
    test_settings_coexist.py's _NEW_GATES; delete test_docs_update_wiring.py outright; remove
    "doc-dependencies" from test_docs_sync_determinism.py's EXPECTED_PAGES; trim the four
    docs-plane-claim locations in gate-model/SKILL.md while leaving the rest of the skill intact;
    fix the docs_guard example in test_tests_are_isolatable.py's docstring; remove "ledger_guard, "
    from test_workspace_member_completeness.py's docstring guard list. Stage with an explicit
    pathspec covering exactly these eight files, inspect `git diff --cached --name-only`, then
    commit. Verify with `uv run pytest tools/harness_lint tools/harness_emit tools/hooks
    tools/docs_sync -q`.
  </action>
  <acceptance_criteria>
    - `grep -c docs-upkeep tools/harness_lint/caps.py` returns 0.
    - `grep -n "== 2" tools/harness_emit/tests/test_coexist.py` shows `== 25` in both prior `== 26`
      locations.
    - `grep -c ledger_guard tools/hooks/tests/test_settings_coexist.py` returns 0.
    - `test ! -f tools/harness_lint/tests/test_docs_update_wiring.py` exits 0.
    - `grep -c doc-dependencies tools/docs_sync/tests/test_docs_sync_determinism.py` returns 0.
    - `grep -c ledger_guard harness/skills/gate-model/SKILL.md` returns 0.
    - `grep -c docs_guard tools/harness_lint/tests/test_tests_are_isolatable.py` returns 0.
    - `grep -c ledger_guard tools/harness_lint/tests/test_workspace_member_completeness.py` returns 0.
    - `uv run pytest tools/harness_lint tools/harness_emit tools/hooks tools/docs_sync -q` exits 0.
    - `git diff --cached --name-only` (checked immediately before commit) lists exactly the eight
      files in this task's `<files>`.
  </acceptance_criteria>
  <verify>
    <automated>! grep -q docs-upkeep tools/harness_lint/caps.py && ! grep -q ledger_guard tools/hooks/tests/test_settings_coexist.py && test ! -f tools/harness_lint/tests/test_docs_update_wiring.py && ! grep -q doc-dependencies tools/docs_sync/tests/test_docs_sync_determinism.py && ! grep -q ledger_guard harness/skills/gate-model/SKILL.md && ! grep -q docs_guard tools/harness_lint/tests/test_tests_are_isolatable.py && ! grep -q ledger_guard tools/harness_lint/tests/test_workspace_member_completeness.py</automated>
    <automated>uv run pytest tools/harness_lint tools/harness_emit tools/hooks tools/docs_sync -q</automated>
  </verify>
  <done>All eight files are edited per the interfaces section; the touched packages' own tests are green; one pathspec-scoped commit lands.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh uv.lock</name>
  <files>uv.lock</files>
  <read_first>
    .planning/phases/40-self-gate-teardown/40-01-SUMMARY.md ("uv Workspace Removal Mechanics" precedent)
  </read_first>
  <action>
    Run `uv lock` to regenerate uv.lock now that tools/docs_guard/ no longer exists (deleted in
    Plan 01) — this removes the logparser-docs-guard virtual workspace member block. No
    pyproject.toml edit is needed (the `tools/*` glob member list already excludes a nonexistent
    directory). Run `uv sync --all-packages` to prove every remaining workspace member still
    installs against the refreshed lock. Stage with an explicit pathspec (`git add -- uv.lock`),
    inspect `git diff --cached --name-only`, then commit.
  </action>
  <acceptance_criteria>
    - `grep -c logparser-docs-guard uv.lock` returns 0.
    - `uv sync --all-packages` exits 0.
    - `git diff --cached --name-only` (checked immediately before commit) lists only `uv.lock`.
  </acceptance_criteria>
  <verify>
    <automated>uv lock && ! grep -q logparser-docs-guard uv.lock && uv sync --all-packages</automated>
  </verify>
  <done>uv.lock no longer names the docs_guard virtual member; uv sync --all-packages is green; one pathspec-scoped commit lands.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification wave</name>
  <files></files>
  <read_first>
    .planning/phases/41-docs-review-plane-removal/41-VALIDATION.md ("Residue sweep" and "Per-Task Verification Map")
    tools/ruff_baseline/ratchet.py (already confirmed this session: comparison is found &gt; allowed, i.e. a decrease is reported as an improvement and still PASSes — no rebaseline is required for this deletion-only phase; running `--update` afterward is optional cleanup, not a gate requirement)
  </read_first>
  <action>
    Run the phase's full closing verification, recording each command's literal output as evidence
    (not "passed"): `uv run pytest -q` (expect 0 failed); the residue sweep grep
    `docs_guard|docs-guard|docs-review-ledger|ledger_guard|docs-upkeep|docs-update|doc-dependencies`
    across `tools/ harness/ contracts/ docs/ .github/ .claude/ .opencode/ AGENTS.md
    .memory/README.md uv.lock` (expect exit 1, no output — `.planning/` is exempt and not swept);
    the three-step emit-drift check (`uv run python -m tools.harness_emit` -> `git add -A` over the
    emitted path set -> `git diff --cached --exit-code`, expect exit 0); the three-step
    stale-derived check (`uv run python -m tools.docs_sync && uv run python -m
    tools.memory_regen.contracts_index` -> `git add -A` over `docs/reference
    .memory/derived/contracts-index.md` -> `git diff --cached --exit-code`, expect exit 0);
    `uv run python -m tools.contract_drift.drift` (expect exit 0); `uv run python -m
    tools.ruff_baseline` (expect exit 0 — PASS is expected either as "every rule class at baseline"
    or as an improvement, since findings can only have gone down or stayed flat after a large
    deletion; do not treat an improvement message as a failure). This task is read-only/verification
    only unless a check fails, in which case repair the specific plan's commit via
    `git commit --amend` on that commit (not a new stray commit) and re-run this task's checks.
  </action>
  <acceptance_criteria>
    - `uv run pytest -q` exits 0, 0 failed.
    - The residue sweep grep exits 1 (no output) outside `.planning/`.
    - The three-step emit-drift check ends with `git diff --cached --exit-code` exiting 0.
    - The three-step stale-derived check ends with `git diff --cached --exit-code` exiting 0.
    - `uv run python -m tools.contract_drift.drift` exits 0.
    - `uv run python -m tools.ruff_baseline` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
    <automated>! grep -rnE "docs_guard|docs-guard|docs-review-ledger|ledger_guard|docs-upkeep|docs-update|doc-dependencies" tools/ harness/ contracts/ docs/ .github/ .claude/ .opencode/ AGENTS.md .memory/README.md uv.lock</automated>
    <automated>uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline</automated>
  </verify>
  <done>Full suite green, residue sweep zero hits outside .planning/, emit-drift and stale-derived both empty-diff, contract-drift and ruff-ratchet both clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No security-relevant trust boundary. This plan is a prose/test sweep, a lockfile refresh, and a
verification-only wave; no request path, stored data, auth, or secret is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05-01 | Tampering | hardcoded test counts (`test_coexist.py`, `caps.py`) silently drift from reality | mitigate | Acceptance criteria assert the exact new literal values (25, EXPECTED_SKILLS without docs-upkeep) rather than "the test passes" — a mis-update that still happens to pass for the wrong reason is caught by the exact-value check. |
| T-41-05-02 | (operational) residue sweep false-negative | n/a — not a security threat | mitigate | The sweep command is run verbatim from VALIDATION.md, not reinvented, and its exit code (1 = no match) is the acceptance gate, not eyeballed output. |
| T-41-05-SC | Tampering | package installs | n/a | `uv lock`/`uv sync --all-packages` install no new package; they remove a virtual workspace member. Package Legitimacy Audit gate is not triggered. |
</threat_model>

<verification>
This plan's Task 3 IS the phase's overall verification: full `uv run pytest -q`, the residue sweep,
emit-drift, stale-derived, contract-drift, and the ruff ratchet, all run and recorded as literal
output — this is exactly the done-condition bundle D-15 defines. The one remaining item — the CI
fan-in `gate` job resolving green on the pushed branch — is manual-only per 41-VALIDATION.md and
happens after push, outside any local task. Per D-16, no mutation-proof / adversarial-input table
is owed by this plan: nothing control-shaped is added by a deletion-only phase, and the residue
sweep plus the green fan-in are this phase's whole proof obligation.
</verification>

<success_criteria>
(This bundle is the D-15 done-condition — CI fan-in green plus items 1-4 below.)
1. `uv run pytest -q` is green, 0 failed, no collection error.
2. The residue sweep returns zero hits outside `.planning/` for the seven-token pattern.
3. `emit-drift`, `stale-derived`, `contract-drift`, and the ruff ratchet are all clean.
4. `uv.lock` no longer names the `docs_guard` workspace member.
5. Net surface change across the whole phase (Plans 01-05) is deletion-only: the guard package
   (6110 LOC), the ledger, the registry, the hook + its emitter wiring, the command, the skill, the
   contract, the CI job, and the staleness queue (233 LOC) are all gone; zero new tools, gates,
   contracts, or replacement jobs were added (D-06).
6. The CI fan-in `gate` job goes green on the pushed branch (manual-only verification, per
   41-VALIDATION.md).
</success_criteria>

<output>
Create `.planning/phases/41-docs-review-plane-removal/41-05-SUMMARY.md` when done. Report the total
LOC removed across all five plans, measured from `git diff --stat` (per D-17 — counted, not
estimated) from the phase's first commit to its last, and state explicitly whether the ruff
baseline moved (and if so, whether `--update` was run) so the next phase inherits an accurate
baseline description rather than an assumption.
</output>
