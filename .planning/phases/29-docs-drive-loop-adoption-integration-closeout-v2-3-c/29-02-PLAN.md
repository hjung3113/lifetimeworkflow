---
phase: 29-docs-drive-loop-adoption-integration-closeout-v2-3-c
plan: 02
type: execute
wave: 1
depends_on: []
blocked_by_external: "Phase 28 review fixes (28-REVIEW.md CR-01 + CR-03) must land before this plan executes — see <external_dependency>"

files_modified:
  - tools/adoption_apply/tests/test_docs_binding_proposal.py
  - tools/docs_guard/tests/test_selfgreen_end_to_end.py
autonomous: true
requirements: [DOCSUP-07]

must_haves:
  truths:
    - "SC-2: in ONE apply cycle, a `docs/doc-dependencies.toml` record is APPLIED and a `docs/.docs-review-ledger.toml` record is REFUSED — the propose/ratify asymmetry is observable end to end, not asserted twice in two unrelated unit tests"
    - "The ledger refusal happens BEFORE any filesystem write, proven by a zero-write spy over open/os.link/os.replace (the 27.1 SC-2 idiom)"
    - "A binding an agent could legally propose is NOT green: with a registry row and no [[reviewed]] row it classifies BROKEN (required) / not-FRESH (advisory), and a `reviewed-no-change` row landed in the SAME commit as the binding is caught by `first_seen-unratified`"
    - "The refusal is proven LOAD-BEARING by mutation: with `REVIEW_LEDGER_GLOBS` neutralized the ledger record is applied instead of refused"
    - "Ledger row shape is READ from `tools/docs_guard/ledger.py::_ROW_KEYS` at authoring time, never restated in this plan — CR-03's fix adds `binding_digest`, and a fixture that hardcodes today's four keys is rejected the moment it lands"
    - "The not-green claim holds for the DRIFTED-SOURCE case too, which is CR-01's fix (today a drifted-source x unratified-row binding reports SUPPRESSED with ok=True); this plan declares that dependency and does not duplicate CR-01's fixture"
    - "Unresolved ownership becomes a question record, never a fabricated binding (ADOPT-02 / DOCSUP-07's 'inferred ownership은 미해결로 남기고')"
  artifacts:
    - path: "tools/adoption_apply/tests/test_docs_binding_proposal.py"
      provides: "the mixed-manifest end-to-end apply cycle, the zero-write spy, the mutation proof, and the unknown-ownership question assertion"
      contains: "doc-dependencies.toml"
    - path: "tools/docs_guard/tests/test_selfgreen_end_to_end.py"
      provides: "registry-row-without-ledger-row is not green; same-commit self-blessing is caught by first_seen-unratified"
      contains: "first_seen-unratified"
  key_links:
    - from: "tools/adoption_apply/tests/test_docs_binding_proposal.py"
      to: "tools/adoption_apply/apply.py::apply_manifest"
      via: "one manifest carrying both destinations; assert the applied/refused buckets"
      pattern: "apply_manifest"
    - from: "tools/docs_guard/tests/test_selfgreen_end_to_end.py"
      to: "tools/docs_guard/guard.py::classify"
      via: "classify over a tmp registry+ledger pair in the hermetic docs_repo fixture"
      pattern: "classify"
---

<objective>
Prove DOCSUP-07 end to end against the SHIPPED controls: `/adopt`'s write path may extend
`docs/doc-dependencies.toml` (what is WATCHED) but is structurally unable to author
`docs/.docs-review-ledger.toml` (what is GREEN), and a binding that only an agent has touched can
never classify FRESH.

Purpose: Phase 28 plan 09 landed `REVIEW_LEDGER_GLOBS` / `ReviewLedgerRefusal` and plan 04 landed
`first_seen-unratified`, each unit-tested in isolation. SC-2 is a claim about the two together in
one cycle. No new gate machinery is built here — per 29-RESEARCH §4.1/§4.2 and D-07 the registry
half needs none, and inventing a docs-binding proposer would be new authority, not new proof.

Output: two integration test files. Zero production-code change.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-CONTEXT.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-RESEARCH.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-09-SUMMARY.md
@tools/adoption_apply/apply.py
@tools/adoption_apply/tests/test_constitution_refusal.py
@tools/docs_guard/guard.py
@tools/docs_guard/ledger.py
@tools/docs_guard/tests/conftest.py
@docs/adr/0010-human-docs-review-obligation-model.md

<interfaces>
<!-- VERIFIED against shipped code this session. -->

From tools/adoption_apply/apply.py
```python
REVIEW_LEDGER_GLOBS = ["docs/.docs-review-ledger.toml"]
class ReviewLedgerRefusal(ValueError): ...        # NOT a subclass of ConstitutionRefusal
def refuse_unsafe_destination(destination: str, target_root: str | Path) -> Path: ...
def apply_manifest(manifest, target_root, *, payloads=None, block_bodies=None) -> dict[str, list[str]]
    # -> {"applied": [...], "skipped": [...], "refused": [...]}, destinations only,
    #    iterating manifest["dispositions"] sorted by destination;
    #    ConstitutionRefusal and ReviewLedgerRefusal are caught per-record into "refused".
```

From tools/docs_guard/guard.py
```python
def classify(*, registry_path=DEFAULT_REGISTRY_PATH, ledger_path=DEFAULT_LEDGER_PATH,
             root=REPO_ROOT) -> dict   # {"ok", "bindings", "uncovered", "coverage", "findings"}
STATES: tuple[str, ...]                 # includes BROKEN / STALE_REQUIRED / STALE_ADVISORY / FRESH / UNCOVERED
```

Finding reasons already shipped (tools/docs_guard/cli.py:72-107 REMEDIATION keys):
`broken-binding`, `first_seen-unratified`, `disposition-incoherent`, `unverified-disposition`,
`stale-digest`, `superseding-adr-required`, `unknown-binding`, `uncovered-regression`,
`binding-count-regression`.

`tools/docs_guard/tests/conftest.py` provides the hermetic real-`git init` `docs_repo` fixture that
28-04 used for its `git show HEAD:./<path>` path — reuse it, do not build a second one.

Ledger row shape — DO NOT restate it here or in a fixture literal. Read
`tools/docs_guard/ledger.py::_ROW_KEYS` when you author the file and build every fixture row from
that frozenset. At planning time it is `{id, source_digest, target_digest, disposition}`, but
28-REVIEW CR-03's fix ADDS `binding_digest`, and `ledger.py:198-201` rejects any extra key and any
missing key BY NAME — a hardcoded four-key fixture goes red the moment that fix lands.

The 6-value dispositionEnum for a manifest record
(tools/adoption_scan/destinations.py:DISPOSITION_ENUM): create, preserve, conflict, marker-merge,
derived-regenerate, human-ratification-required.
</interfaces>
</context>

<external_dependency>
This plan must execute AFTER 28-REVIEW.md's CR-01 and CR-03 fixes land. Both are already tracked as
Phase 28 review-fix work; this plan consumes them and deliberately does NOT duplicate their fixtures.

- **CR-01** (drift suppression demotes blocking coherence findings): until it is fixed, a binding
  whose source is a currently-drifted contract reports `SUPPRESSED` with `ok=True` even when its
  ledger row is unratified — which directly contradicts truth #3 of this plan. CR-01 owns that
  fixture; assert the plain (undrifted) not-green cases here and cite CR-01 for the drifted one.
- **CR-03** (`first_seen-unratified` keys on the binding id only): its fix adds `binding_digest` to
  the ledger row shape. Every fixture in this plan is built from `_ROW_KEYS` as shipped, so it
  follows the fix rather than pinning a stale shape.

If either fix has not landed when this plan is picked up, STOP and report — do not work around it by
loosening an assertion or by hardcoding the old row shape.
</external_dependency>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The mixed-manifest apply cycle, the zero-write spy, and the mutation proof</name>
  <files>tools/adoption_apply/tests/test_docs_binding_proposal.py</files>
  <behavior>
    - `test_registry_applied_and_ledger_refused_in_one_cycle`: one manifest with two `create`
      records — `docs/doc-dependencies.toml` and `docs/.docs-review-ledger.toml`. After
      `apply_manifest`, the registry destination is in `applied` and the ledger destination is in
      `refused`; the registry file EXISTS on disk with the proposed bytes and the ledger file DOES
      NOT EXIST. One cycle, both facts.
    - `test_ledger_refused_before_any_write`: a spy over `builtins.open` (write modes), `os.link`,
      `os.replace` records zero calls for the ledger destination; the raised type is
      `ReviewLedgerRefusal` and `not isinstance(exc, ConstitutionRefusal)`.
    - `LEDGER_SPELLINGS` table rows already covered by 28-09 are NOT duplicated here; instead assert
      the ONE spelling `/adopt` would realistically emit plus the case variant, and reference
      `test_constitution_refusal.py` in the docstring as the spelling-table home.
    - `test_refusal_is_load_bearing` (MUTATION proof): with `apply.REVIEW_LEDGER_GLOBS` monkeypatched
      to `[]`, the same manifest applies the ledger record instead of refusing it. This is the RED
      confirmation for a control that already shipped — a plain pre-fix RED run is impossible, and
      a test that cannot be shown failing against a neutralized control is not evidence.
    - `test_unknown_ownership_becomes_a_question`: an inventory whose docs destination ownership is
      `unknown` yields a `questionRecord` from `tools.adoption_scan.plan.generate_questions` and no
      binding-shaped proposal asserting an owner (the `OWNER_TBD` never-fabricate rule).
  </behavior>
  <action>Write the file. Build the manifest inline as a dict (no fixture repo scaffolding beyond
  `tmp_path`) and give the registry record a small but VALID `[[binding]]` payload so the test is
  about authority, not about TOML shape. Docstring must state the SC-2 claim in one sentence and
  cite ADR-0010 clause 3b as the ratified boundary being exercised.

  Do NOT edit `harness/skills/brownfield-adoption/SKILL.md` or `harness/skills/gate-model/SKILL.md`
  in this plan: both are TARGETS of seeded bindings, and moving a target digest now would invalidate
  the digests the human is asked to sign in plan 29-04.

  Run the narrow selection plain: `uv run pytest tools/adoption_apply/tests/test_docs_binding_proposal.py -q`
  (explicit path — no other wave-1 plan's in-flight file may be collected). Record in the summary the
  verbatim assertion message produced by `test_refusal_is_load_bearing` when the mutation is applied
  in reverse (i.e. temporarily invert the test to confirm it can fail) — or, more cheaply and
  equivalently, record the observed `refused` vs `applied` bucket contents under the neutralized
  glob list. Commit with
  `git commit -- tools/adoption_apply/tests/test_docs_binding_proposal.py` after inspecting
  `git diff --cached --name-only`.</action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_docs_binding_proposal.py -q</automated>
  </verify>
  <done>All five tests pass; the mutation proof demonstrably flips the outcome; no production file
  and no SKILL.md was modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove a proposed binding cannot be green</name>
  <files>tools/docs_guard/tests/test_selfgreen_end_to_end.py</files>
  <behavior>
    Using the hermetic `docs_repo` fixture:
    - `test_registry_row_without_ledger_row_is_not_green`: a `required` binding with no `[[reviewed]]`
      row classifies `BROKEN`, produces a `broken-binding` finding, and `result["ok"] is False`.
    - `test_advisory_row_without_ledger_row_is_not_fresh`: the advisory twin is not `FRESH`.
    - `test_same_commit_self_blessing_is_caught`: a binding AND a matching `reviewed-no-change` row
      with the exact live digests, both introduced in the same commit, do not yield `FRESH` — the
      `first_seen-unratified` finding fires because it keys on the PREVIOUS COMMITTED ledger. This
      is the byte-identical-to-the-attack case 28-07 refused to author.
    - `test_first_seen_is_load_bearing` (mutation): monkeypatch the previous-committed-ledger lookup
      to return the working-tree ledger and assert the self-blessing case now reports FRESH —
      proving the finding, not the digests, is what stops it.
  </behavior>
  <action>Write the file next to the existing docs_guard tests; reuse `docs_repo` from
  `tools/docs_guard/tests/conftest.py` (do not create a second git fixture). Derive every digest by
  CALLING the shipped digest/classify code, never by hand-hashing — a hand-computed digest that
  disagrees with the gate would make the test assert its own arithmetic.

  Run the EXPLICIT path selection `uv run pytest tools/docs_guard/tests/test_selfgreen_end_to_end.py -q`,
  never the bare `tools/docs_guard` package: plan 29-01 runs in the same wave and its task 1
  deliberately commits a RED `test_exclusions.py`, so a package-wide selection would judge this plan
  on that plan's transient state. Commit with
  `git commit -- tools/docs_guard/tests/test_selfgreen_end_to_end.py`.</action>
  <verify>
    <automated>uv run pytest tools/docs_guard/tests/test_selfgreen_end_to_end.py -q</automated>
  </verify>
  <done>Four tests pass; the mutation proof flips the self-blessing case to FRESH, showing which
  control is load-bearing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored manifest -> docs plane | an agent decides what to write under `docs/` |
| working-tree ledger -> previous committed ledger | the only history that distinguishes review from self-blessing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Elevation of Privilege | agent writes a ledger disposition | mitigate | shipped `ReviewLedgerRefusal` at `refuse_unsafe_destination`; this plan asserts it end to end plus a zero-write spy |
| T-29-06 | Repudiation | self-blessing via a same-commit `reviewed-no-change` row | mitigate | shipped `first_seen-unratified`; this plan asserts it and proves it load-bearing by mutation |
| T-29-07 | Spoofing | fabricated ownership in a proposed binding | mitigate | unknown ownership must surface as a questionRecord (ADOPT-02); asserted in task 1 |
| T-29-08 | Tampering | a test that passes because the control is absent | mitigate | every claim carries a mutation proof; a control-shaped assertion with no demonstrable failure mode is not accepted |
| T-29-22 | Tampering | a fixture pinning a stale ledger row shape | mitigate | rows built from the shipped `_ROW_KEYS`; the plan restates no key list |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | none in this plan — no dependency is added |
</threat_model>

<verification>
- `uv run pytest tools/adoption_apply tools/docs_guard -q` green.
- `git status --porcelain` shows only the two intended test files.
- `harness/skills/**` untouched: `git diff --name-only HEAD~2 -- harness/skills | wc -l` is 0.
</verification>

<success_criteria>
One apply cycle demonstrably applies a registry proposal and refuses the ledger before any write,
and a registry-only (or same-commit self-blessed) binding demonstrably cannot classify FRESH — each
claim backed by a mutation that flips it.
</success_criteria>

<output>
Create `.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-02-SUMMARY.md` when done.
</output>
