---
phase: 29-docs-drive-loop-adoption-integration-closeout-v2-3-c
plan: 05
type: execute
wave: 4
depends_on: ["29-01", "29-02", "29-03", "29-04"]
files_modified:
  - .planning/v2.3-MILESTONE-AUDIT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [DOCSUP-06, DOCSUP-07]

must_haves:
  truths:
    - "SC-4 is an INVENTORY confirmed green with actual numbers, not a to-do list — net new CI jobs from Phase 29 is ZERO (D-11)"
    - "`git diff --check` stays a verification command, never a CI job (D-12) — a job would double-report polyglot_lint"
    - "Every one of the four v2.3 themes' requirements has a per-requirement evidence row; STATE.md's percentages are NOT the evidence (the v2.1 audit's own recorded hazard)"
    - "D-15: the standing residuals survive the close — `tools/hooks/secret_scan.py:44-47`, 27.2's AD-01/AD-02/`I001`, 27.1's IN-02/WR-08, plus every outstanding ratification"
    - "Nyquist gaps are RECORDED as observed, never fabricated: `nyquist_validation: true` is set, and phases 27.2 and 28 have no VALIDATION.md while phases 27 and 28 have no VERIFICATION.md"
    - "ADR-0010 is still `Status: proposed` and was landed under `HARNESS_DEV_BYPASS`; the audit records the outstanding human ratification rather than assuming it"
  artifacts:
    - path: ".planning/v2.3-MILESTONE-AUDIT.md"
      provides: "the v2.3 closeout audit — frontmatter scores/gaps/nyquist/tech_debt plus the per-requirement traceability table"
      contains: "tech_debt"
  key_links:
    - from: ".planning/v2.3-MILESTONE-AUDIT.md"
      to: ".planning/milestones/v2.1-MILESTONE-AUDIT.md"
      via: "same frontmatter shape (D-14) — precedent, not a new format"
      pattern: "nyquist"
    - from: ".planning/v2.3-MILESTONE-AUDIT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "one evidence row per TOPO-01..07 / ADOPT-01..07 / DOCSUP-01..07"
      pattern: "DOCSUP-07"
---

<objective>
Close milestone v2.3: run the full SC-4 fan-in and report actual numbers, then write the milestone
audit carrying every standing residual and every outstanding ratification forward.

Purpose: SC-4's list reads like twelve jobs to build; it is not. All twelve CI jobs already exist —
`docs-guard` was Phase 28's, and `git diff --check` has never been a CI job here. The work is
confirming green with observed output and making sure nothing that has been deliberately fenced out
since Phase 26.2 disappears at the close.

Output: `.planning/v2.3-MILESTONE-AUDIT.md`, plus the requirement/roadmap/state bookkeeping.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-CONTEXT.md
@.planning/milestones/v2.1-MILESTONE-AUDIT.md
@.github/workflows/ci.yml

<interfaces>
<!-- VERIFIED this session. -->

`gate.needs` (`.github/workflows/ci.yml:321`) — TWELVE jobs, `docs-guard` already present:
`[setup, lang-tests, contract-check, drift, golden, core-suite, lifecycle-eval, emit-drift,
stale-derived, docs-guard, workspace]` plus `gate` itself.

SC-4 item -> where it lives (net Phase-29 delta: ZERO new jobs, ZERO `gate.needs` edits):
| item | home |
|---|---|
| `uv run pytest` | job `core-suite` |
| contract-drift | job `drift` |
| golden | job `golden` |
| workspace drift | job `workspace` |
| stale-derived | job `stale-derived` |
| lifecycle | job `lifecycle-eval` |
| GEN-04 twin | `tools/harness_lint/tests/test_core_no_example_dep.py` in `core-suite`; the workspace twin also runs in `workspace` |
| docs guard | job `docs-guard` (Phase 28) |
| emit-drift | job `emit-drift` |
| model-identifier lint | `test_agents.py:129-135`, `test_opencode_config.py:81,89`, `validate.py:192,215-222` — all inside `core-suite` (+ `emit-drift`) |
| injector budget | `test_inject_assembler.py:32-33,152-157` — inside `core-suite` |
| `git diff --check` | NOT a job anywhere; a verification-time command (D-12) |

Nyquist state, observed by `ls` this session (`workflow.nyquist_validation: true`):
VALIDATION.md present for 24, 25, 26, 26.1, 26.2, 27, 27.1 — ABSENT for 27.2 and 28.
VERIFICATION.md present for 24, 25, 26, 26.1, 26.2, 27.1, 27.2 — ABSENT for 27 and 28.

Standing residuals to carry (D-15):
- `tools/hooks/secret_scan.py:44-47` — pattern list hardcoded instead of read from the contract;
  fenced out of 26.2, 27.1, 27.2 and 28.
- 27.2: AD-01 (barrier-timeout legibility), AD-02 (`fcntl` singleton monkeypatching), the
  pre-existing `I001` on `tools/adoption_apply/cli.py`.
- 27.1: IN-02 (flock/NFS), WR-08 (parent-dir symlink TOCTOU).
- Outstanding ratifications: ADR-0010 is `Status: proposed` with `Date`/`Deciders` as em-dashes and
  was landed under `HARNESS_DEV_BYPASS`; check every other v2.3 ADR's status line the same way
  rather than assuming.
- Phase 28 D-14's unbuilt seam: the instance-local (`examples/**`) docs registry overlay.
- The `emit-drift` bare-`git diff` untracked blindness (carried from 15-REVIEW CR-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the SC-4 fan-in and record actual numbers</name>
  <files>(no repo file — output feeds task 2)</files>
  <action>Run each item and capture the exit code and the headline number verbatim. Run the full
  suite ONCE, here — the earlier plans deliberately used narrow selections so that no two same-wave
  plans both gated on a green full suite.

  - `uv run pytest` — record the collected/passed/failed counts, not "green".
  - `uv run python -m tools.contract_drift` (root and the example manifest, matching the `drift` job's
    two invocations as written in `ci.yml`).
  - the `golden` job's commands (`tools/golden_runner` + the example tests).
  - `drift --workspace` plus the cross-repo pytest set the `workspace` job runs.
  - the `stale-derived` job's regeneration + diff.
  - the `lifecycle-eval` job's command.
  - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/harness_lint/tests/test_core_no_workspace_member_dep.py`
  - `uv run python -m tools.docs_guard` — expect exit 0 after plan 29-04.
  - emit round-trip: `uv run python -m tools.harness_emit` then
    `git status --porcelain -- .opencode .claude opencode.json AGENTS.md CLAUDE.md` (porcelain, not
    bare `git diff` — the untracked blind spot) and `git diff --exit-code` over the same paths.
  - model-identifier lint and injector budget: the four named test files.
  - `git diff --check`.

  Read the exact command lines out of `.github/workflows/ci.yml` rather than reconstructing them from
  memory. Do not fix anything you find here; if an item is red, record it verbatim and carry it into
  task 2's `gaps` — a closeout that repairs on the way past reports a state that never existed.</action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3; uv run python -m tools.docs_guard > /dev/null; echo "docs-guard exit: $?"; git diff --check && echo "diff-check clean"</automated>
  </verify>
  <done>All twelve SC-4 items have a recorded command, exit code and headline number; nothing was
  repaired during the run.</done>
</task>

<task type="auto">
  <name>Task 2: Write the v2.3 milestone audit</name>
  <files>.planning/v2.3-MILESTONE-AUDIT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Create `.planning/v2.3-MILESTONE-AUDIT.md` using `.planning/milestones/v2.1-MILESTONE-AUDIT.md`'s
  frontmatter shape (D-14): `milestone`, `milestone_name`, `audited`, `status`, `scores`
  (requirements / phases / integration / flows), `gaps` per category, `nyquist`
  (`compliant_phases` / `partial_phases` / `missing_phases` / `overall`), `tech_debt`.

  Body: one row per requirement — TOPO-01..07, ADOPT-01..07, DOCSUP-01..07 — with columns
  REQ | Phase | Traceability | VERIFICATION.md | Live integration | Final. The per-requirement
  evidence table is the audit's evidence; STATE.md is not (v2.1's audit records that hazard about its
  own `milestone_complete / 117%` line, and STATE.md is stale again today).

  Populate `nyquist` from task 1's observed `ls`: 27.2 and 28 lack VALIDATION.md; 27 and 28 lack
  VERIFICATION.md. Record them as partial/missing — v2.1's audit carried exactly this kind of row for
  phases 13/14 rather than back-filling a document after the fact.

  Populate `tech_debt` with every residual listed in `<interfaces>`, each with its file:line and the
  phase that fenced it. A fence deliberately carried since 26.2 must not vanish at the close.

  Include a short "SC-4 fan-in" section with task 1's actual numbers, and one sentence stating that
  Phase 29 added zero CI jobs and zero `gate.needs` entries (D-11) and that `git diff --check`
  remains a verification command (D-12).

  Then the bookkeeping: mark DOCSUP-06 and DOCSUP-07 `[x]` in `.planning/REQUIREMENTS.md` and add
  their Traceability rows; tick Phase 29's plan checkboxes in `.planning/ROADMAP.md`; update
  `.planning/STATE.md` to the real position (it currently reads `stopped_at: Completed
  27.1-03-PLAN.md`, `completed_phases: 7` — stale by four phases). No wall-clock timestamp, no human
  identity, and no model identifier in any of these artifacts.

  Commit with an explicit four-path pathspec after inspecting `git diff --cached --name-only`.</action>
  <verify>
    <automated>test -f .planning/v2.3-MILESTONE-AUDIT.md && grep -c 'DOCSUP-07' .planning/v2.3-MILESTONE-AUDIT.md && grep -c 'secret_scan.py' .planning/v2.3-MILESTONE-AUDIT.md && git diff --check && echo OK</automated>
  </verify>
  <done>The audit exists with all 21 requirement rows, the observed nyquist gaps, every D-15
  residual, and task 1's actual fan-in numbers; REQUIREMENTS/ROADMAP/STATE agree with it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN reviews the closeout and decides on the outstanding ratifications</name>
  <files>.planning/v2.3-MILESTONE-AUDIT.md, docs/adr/0010-human-docs-review-obligation-model.md</files>
  <action>Present the audit and the outstanding-ratification list for blocking human review, then
  STOP. The agent does not flip ADR-0010 to `accepted`, does not fill its Date/Deciders fields, and
  does not run `/gsd:complete-milestone`.</action>
  <done>The human has approved the closeout or listed what must change first.</done>
  <what-built>The SC-4 fan-in was run with actual numbers recorded and
  `.planning/v2.3-MILESTONE-AUDIT.md` written against the v2.1 precedent, carrying every standing
  residual and every outstanding ratification forward.</what-built>
  <how-to-verify>
    1. Read `.planning/v2.3-MILESTONE-AUDIT.md` — especially `gaps`, `nyquist` and `tech_debt`.
       Anything you consider resolved should be resolved by evidence, not by deleting the row.
    2. Decide on ADR-0010: it is still `Status: proposed`, with `Date`/`Deciders` as em-dashes, and
       it was landed under `HARNESS_DEV_BYPASS`. Ratifying it (flipping to `accepted` and filling
       both fields) is a constitution-plane human write — the agent cannot do it, and the audit
       records it as outstanding until you do.
    3. Confirm the two nyquist gaps (27.2/28 VALIDATION.md, 27/28 VERIFICATION.md) are acceptable as
       recorded debt, or ask for them to be produced before the milestone closes.
    4. When satisfied, run `/gsd:complete-milestone` yourself to archive `.planning/phases/*` into
       `.planning/milestones/v2.3-phases/` and snapshot REQUIREMENTS/ROADMAP. That command moves
       committed history and is deliberately not automated inside this plan.
  </how-to-verify>
  <resume-signal>Type "closeout approved", or list what must change first.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate output -> audit claim | the audit asserts a state the reader will not re-run |
| milestone close -> carried debt | anything not written down here disappears |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-18 | Repudiation (false assurance) | an audit claiming green without observed output | mitigate | task 1 records command + exit code + headline number for all twelve items; task 2 quotes them |
| T-29-19 | Tampering | a residual or a nyquist gap dropped at the close | mitigate | D-15 residual list is enumerated in `<interfaces>` and gated by a `grep` in task 2's verify |
| T-29-20 | Elevation of Privilege | agent flips ADR-0010 to `accepted` | mitigate | ratification is a human checkpoint; `HARNESS_DEV_BYPASS` is not used and the ADR stays `proposed` until a human acts |
| T-29-21 | Repudiation | repairing a red gate mid-audit and reporting the repaired state | mitigate | task 1 forbids repair; a red item becomes a `gaps` row |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | none in this plan |
</threat_model>

<verification>
- Task 1's twelve recorded items, each with an exit code.
- `.planning/v2.3-MILESTONE-AUDIT.md` exists with 21 requirement rows.
- `git diff --check` clean; no wall-clock, human identity or model identifier in the new artifacts.
</verification>

<success_criteria>
The full SC-4 fan-in is confirmed green (or its failures are recorded verbatim as gaps) with actual
numbers, and the v2.3 audit carries every requirement, every observed nyquist gap, every standing
residual and every outstanding ratification into the milestone record.
</success_criteria>

<output>
Create `.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-05-SUMMARY.md` when done.
</output>
