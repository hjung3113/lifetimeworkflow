---
phase: 29-docs-drive-loop-adoption-integration-closeout-v2-3-c
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03"]
blocked_by_external: "Phase 28 review fixes (28-REVIEW.md CR-03, and CR-01/WR-01 if they touch ledger reads) must land BEFORE the human authors the ledger — see <external_dependency>"
files_modified:
  - docs/.docs-review-ledger.toml
  - harness/skills/gate-model/SKILL.md
  - .opencode/skill/gate-model/SKILL.md
  - .claude/skills/gate-model/SKILL.md
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
autonomous: false
requirements: [DOCSUP-06, DOCSUP-07]

must_haves:
  truths:
    - "SC-3: every seeded `required` binding is FRESH or exactly dispositioned — `python -m tools.docs_guard` exits 0 and the `docs-guard` CI job is green"
    - "D-08: the ledger half is landed by a HUMAN. The agent computes and PRESENTS the digests by running the shipped guard; it never creates, stages, or drafts a sibling of `docs/.docs-review-ledger.toml`"
    - "The drive loop is exercised for real once: a STALE_REQUIRED binding is produced by an honest documentation change, `/docs-update` routes on exit 1, a bounded edit is drafted, and a human re-records the row"
    - "Exit routing is observed, not asserted from prose: exit 1 before the human re-records, exit 0 after"
    - "No agent-authored ledger write is attempted; `GOLDEN_APPROVE_HUMAN` and `HARNESS_DEV_BYPASS` are never used — neither authorizes a ledger disposition (ADR-0010 clause 3b)"
    - "The row shape presented to the human is READ from `tools/docs_guard/ledger.py::_ROW_KEYS` at execution time, never from this plan's prose — 28-REVIEW CR-03's fix adds `binding_digest`, and asking a human to sign a stale shape costs a second human authoring round"
  artifacts:
    - path: "docs/.docs-review-ledger.toml"
      provides: "the human-authored committed baseline: [coverage] uncovered_max + binding_min, and one [[reviewed]] row per binding"
      contains: "binding_min"
  key_links:
    - from: "docs/.docs-review-ledger.toml"
      to: "tools/docs_guard/guard.py::classify"
      via: "the committed baseline the gate diffs the working tree against"
      pattern: "reviewed"
    - from: "harness/skills/gate-model/SKILL.md"
      to: "docs/doc-dependencies.toml::gate-model-permission-surface"
      via: "the binding whose target this plan deliberately moves to exercise the loop"
      pattern: "path_deny_globs"
---

<objective>
Close SC-3: land the human-authored review ledger, then exercise the drive loop once for real
against a genuinely stale document, and land the resulting review.

Purpose: plan 28-07 seeded eight bindings and deliberately did NOT author the ledger, because a
binding plus its own matching `reviewed-no-change` row is byte-identical to the self-green attack —
authorship is the only distinguishing fact. `python -m tools.docs_guard` therefore fails today with
six `broken-binding` findings and the `docs-guard` CI job is red. This plan does not weaken a rule,
create the ledger by agent, or delete a binding to make the gate green; it routes the two
human-only writes through explicit blocking checkpoints.

Output: a committed ledger, a corrected `gate-model` skill, and an observed 1 -> 0 exit transition.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-CONTEXT.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-07-SUMMARY.md
@.planning/phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-08-SUMMARY.md
@docs/adr/0010-human-docs-review-obligation-model.md
@docs/doc-dependencies.toml
@tools/docs_guard/cli.py
@harness/skills/gate-model/SKILL.md
@harness/permission-matrix.json

<interfaces>
<!-- VERIFIED this session. -->

Current state, run at planning time:
`uv run python -m tools.docs_guard` -> FAILED, exit 1, "8 binding(s); 7 uncovered human-authored
document(s) (no ratchet)", six `broken-binding` findings (one per required binding, each "has no
[[reviewed]] row in the ledger"). `docs/.docs-review-ledger.toml` does not exist.

`28-07-SUMMARY.md` carries a byte-exact PROPOSED ledger (a `[coverage]` table with
`uncovered_max = 7`, `binding_min = 8`, and eight `[[reviewed]]` rows with full 64-hex digests).
It is a PROPOSAL for human authorship, not an artifact to copy blindly: plans 29-01..29-03 changed
files in the repo, so every digest must be RE-DERIVED from a fresh guard run before the human signs.

Ledger shape is an ALLOWLIST enforced by `tools/docs_guard/ledger.py` — READ IT, do not trust this
paragraph. `[coverage]` keys and the per-row keys live in that module (`_ROW_KEYS` at
`ledger.py:73`, enforced at `:198-201`, which rejects extra AND missing keys by name). At planning
time the row keys were `{id, source_digest, target_digest, disposition}`, but 28-REVIEW CR-03's fix
ADDS `binding_digest`; task 1 must re-read the frozenset and build the presented TOML from it.
Invariant either way: no timestamp, no reviewer identity, no prose, no model identifier.

Disposition rule for a FIRST-EVER ledger: every row is `reviewed-no-change` (or, for the ADR-track
binding `contract-graph-adr-0009`, `REVIEWED_STILL_CURRENT`) and NEVER `updated` — `updated` is
verified against a PREVIOUS COMMITTED ledger, and there is none, so it would classify
`unverified-disposition` and fail closed.

`28-08-SUMMARY.md` records the deliberate deferral this plan discharges: "`harness/skills/gate-model/
SKILL.md`'s stale `path_deny_globs` prose was deliberately NOT corrected here — that file is the
TARGET of the `gate-model-permission-surface` binding, so editing it would move a target digest the
human is being asked to sign. Recorded in the ADR as a Phase-29 `/docs-update` item." The matrix now
carries a sixth entry, `docs/.docs-review-ledger.toml`, which that skill's prose does not mention.
</interfaces>
</context>

<external_dependency>
The human ledger authoring in task 2 must happen AFTER 28-REVIEW.md's CR-03 fix lands, because that
fix changes the `[[reviewed]]` row shape (`binding_digest`). Sequencing it the other way costs the
human a second authoring round on a file only they may write — the most expensive kind of rework in
this phase. Check `tools/docs_guard/ledger.py::_ROW_KEYS` before starting task 1; if the fix has not
landed, STOP and report rather than seeding a shape that is about to be rejected.
</external_dependency>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive the exact ledger digests from a fresh guard run</name>
  <files>(no repo file — output goes to the checkpoint below and the plan summary)</files>
  <action>Run `uv run python -m tools.docs_guard` and capture its full output. For each of the eight
  bindings, obtain the live source and target digests by CALLING the shipped code
  (`tools.docs_guard.guard.classify(...)` — the same entry point the gate uses), never by hand-hashing
  and never by a separate hashing command: a hand-computed digest that disagrees with the gate would
  make the human sign a value the gate rejects.

  Re-derive `uncovered_max` as the EXACT live uncovered count from that run (no padding — slack above
  the live count silently permits regression up to that slack) and `binding_min` as the EXACT number
  of `[[binding]]` rows in `docs/doc-dependencies.toml`.

  Diff your values against `28-07-SUMMARY.md`'s proposed ledger and state explicitly which digests
  moved and why (plans 29-01..29-03 touched `tools/docs_guard/**`, `tools/harness_lint/caps.py` and
  `tools/harness_emit/**`; check each against the binding source selectors and report the result
  either way — "unchanged" is a finding too).

  Build the presented TOML from the SHIPPED shape: read `tools/docs_guard/ledger.py::_ROW_KEYS` and
  the `[coverage]` key set out of the module and emit exactly those keys — never the key list quoted
  in this plan's `<interfaces>`. If `binding_digest` (or any other key) is present in `_ROW_KEYS`,
  derive its value by calling the shipped code the same way as the other digests.

  Produce the complete, paste-ready TOML in your message. Do NOT write it to disk. Do NOT create
  `docs/.docs-review-ledger.toml`, any `.proposed`/`.draft` sibling, or stage anything. Confirm the
  file's absence: `test ! -e docs/.docs-review-ledger.toml && echo LEDGER-ABSENT-OK`.</action>
  <verify>
    <automated>test ! -e docs/.docs-review-ledger.toml && echo LEDGER-ABSENT-OK && uv run python -c "from tools.docs_guard.ledger import _ROW_KEYS; print(sorted(_ROW_KEYS))" && uv run python -m tools.docs_guard; echo "guard exit: $?"</automated>
  </verify>
  <done>Eight rows of re-derived digests plus both ratchet values are presented, each traced to a
  guard run; the ledger file still does not exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: HUMAN authors and lands the review ledger</name>
  <files>docs/.docs-review-ledger.toml</files>
  <action>Present task 1's paste-ready ledger block for blocking human review, then STOP. The agent
  does not write, stage, or draft a sibling of `docs/.docs-review-ledger.toml`: the write is denied
  at the permission layer and refused in code, and if a path around either is found, that is a DEFECT
  to report — never a route to take.</action>
  <done>A human has landed the ledger and `uv run python -m tools.docs_guard` exits 0.</done>
  <what-built>Task 1 re-derived every digest and both ratchet values from the shipped guard and
  presented a paste-ready `docs/.docs-review-ledger.toml`. The agent did not create the file: only a
  human may author a review disposition, and no token — not `GOLDEN_APPROVE_HUMAN`, not
  `HARNESS_DEV_BYPASS` — authorizes an agent to (ADR-0010 clause 3b; refused in code at
  `tools/adoption_apply/apply.py::REVIEW_LEDGER_GLOBS` and denied at the permission layer).</what-built>
  <how-to-verify>
    1. Review each of the eight bindings in `docs/doc-dependencies.toml` and satisfy yourself that
       each target document is actually current with respect to its named sources. This is the
       review; the digests only record that you did it.
    2. Create `docs/.docs-review-ledger.toml` yourself, outside the agent session, with the content
       from task 1 (adjust anything you disagree with — your row, your call).
    3. `uv run python -m tools.docs_guard` — expect exit 0 and "docs-guard: OK".
    4. `git add docs/.docs-review-ledger.toml && git commit -- docs/.docs-review-ledger.toml`.
       Inspect `git diff --cached --name-only` first: `git commit` without a pathspec publishes the
       entire shared index.
    5. If any digest is rejected, re-run task 1 rather than hand-editing a value.
  </how-to-verify>
  <resume-signal>Type "ledger landed" (or describe what the guard reported).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Exercise the drive loop once against a genuinely stale document</name>
  <files>harness/skills/gate-model/SKILL.md, .opencode/skill/gate-model/SKILL.md, .claude/skills/gate-model/SKILL.md, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>
  <action>Discharge the item 28-08 deferred: `harness/skills/gate-model/SKILL.md` describes
  `path_deny_globs` as it stood before Phase 28 added the sixth entry
  `docs/.docs-review-ledger.toml`, so the skill teaches an operator a wrong answer about why a write
  was refused.

  Run the loop as `/docs-update` prescribes rather than editing straight to the answer, so the
  routing is OBSERVED:
  1. Run `uv run python -m tools.docs_guard` and record the exit code and the state of
     `gate-model-permission-surface` (expected: FRESH, exit 0 — the state after task 2).
  2. Make the bounded documentation edit: update the skill's `path_deny_globs` prose to match
     `harness/permission-matrix.json`, including the review-ledger entry and the fact that it is a
     THIRD deny domain disjoint from constitution and secret. Bounded means: this binding's target
     only. Do not fix unrelated prose, do not touch other skills, do not expand to UNCOVERED
     documents.

     REQUIRED COVERAGE — the edit is incomplete unless BOTH of these land, because the human
     ratifies this binding against whatever prose exists at signing time (verifier-28 finding):
       a. `:19-26` currently names three trees and omits BOTH `*.env` AND
          `docs/.docs-review-ledger.toml`. Both must appear.
       b. the hook table at `:30-37` omits `ledger_guard` — the PreToolUse(Write|Edit) enforcer
          Phase 28's CR-02 fix added at `tools/hooks/ledger_guard.py`. It must be listed, with the
          fact that it honours NEITHER `GOLDEN_APPROVE_HUMAN` NOR `HARNESS_DEV_BYPASS` (unlike
          `contract_guard`), since that asymmetry is the whole point of the third domain.
     Acceptance: `grep -c 'ledger_guard' harness/skills/gate-model/SKILL.md` returns >= 1 and
     `grep -c '\*\.env' harness/skills/gate-model/SKILL.md` returns >= 1. A fix that names `*.env`
     but not `ledger_guard` leaves the human ratifying against still-incomplete prose.
  3. Re-run the guard. Record the verbatim output: the binding must now report a stale state with a
     `stale-digest` reason and the process must exit 1. If it does not, the loop is not being
     exercised and the discrepancy is the finding — report it rather than forcing the outcome.
  4. Re-emit (`uv run python -m tools.harness_emit`), regenerate the syrupy snapshot, and verify with
     `git status --porcelain -- .opencode .claude` (untracked-visible) that exactly the two gate-model
     twins moved. Second emit must be a byte no-op.
  5. Re-derive the binding's new source and target digests from a guard run and present the exact
     one-row ledger delta for the human. Write nothing to `docs/.docs-review-ledger.toml`.

  Commit the skill source, its two twins and the snapshot with an explicit pathspec, after inspecting
  `git diff --cached --name-only`. The guard is expected to be RED at this commit — that is the point
  of the exercise, and the next task closes it. Say so in the commit message.</action>
  <verify>
    <automated>uv run python -m tools.docs_guard; echo "guard exit: $?"; uv run pytest tools/harness_emit -q</automated>
  </verify>
  <done>The guard exit code moved 0 -> 1 with a `stale-digest` reason on
  `gate-model-permission-surface`; the corrected prose matches the shipped matrix; the emitted twins
  are tracked and a second emit is a no-op; the one-row ledger delta is presented, not written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: HUMAN re-records the reviewed row and closes SC-3</name>
  <files>docs/.docs-review-ledger.toml</files>
  <action>Present task 3's one-row ledger delta and the skill diff for blocking human review, then
  STOP. The agent does not write the ledger row.</action>
  <done>A human has replaced the `gate-model-permission-surface` row and the guard exits 0.</done>
  <what-built>Task 3 made the bounded edit `/docs-update` would draft for the exit-1 state and
  presented the exact replacement `[[reviewed]]` row for `gate-model-permission-surface`. The guard
  is currently RED by design.</what-built>
  <how-to-verify>
    1. Read the diff to `harness/skills/gate-model/SKILL.md` and confirm the prose now matches
       `harness/permission-matrix.json`. This is the review.
    2. Replace that binding's row in `docs/.docs-review-ledger.toml` with the presented digests.
       `disposition = "updated"` is now the correct value — unlike the seed, there IS a previous
       committed ledger and the target digest genuinely moved.
    3. `uv run python -m tools.docs_guard` — expect exit 0.
    4. `git commit -- docs/.docs-review-ledger.toml` (inspect `git diff --cached --name-only` first).
  </how-to-verify>
  <resume-signal>Type "review recorded" (or describe what the guard reported).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-computed digest -> human signature | the human ratifies a value the agent produced |
| agent session -> `docs/.docs-review-ledger.toml` | the greenness authority; agent writes are denied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-14 | Elevation of Privilege | agent lands the ledger to make the gate green | mitigate | both ledger writes are `checkpoint:human-verify` with `gate="blocking-human"`; task 1's automated gate asserts the file's ABSENCE; `auto_advance` is false and these checkpoints are never auto-approvable |
| T-29-15 | Tampering | making the gate green by deleting a binding or raising a ratchet | mitigate | shipped `binding_min` + `uncovered_max` ratchets (the guard never raises or lowers either); no plan task edits `docs/doc-dependencies.toml` |
| T-29-16 | Repudiation | a digest that disagrees with the gate | mitigate | every digest is derived by CALLING `classify`, never hand-hashed; a rejected digest sends the operator back to task 1, never to a hand edit |
| T-29-22 | Repudiation | the human signs a row shape that is about to be rejected | mitigate | `_ROW_KEYS` is read from shipped code at execution time; the plan declares the CR-03 sequencing dependency instead of restating the shape |
| T-29-17 | Tampering | unbounded loop expansion (fixing unrelated docs, chasing advisory/uncovered) | mitigate | task 3 scopes the edit to one binding target and forbids expansion, matching the skill's stated boundedness properties |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | none in this plan |
</threat_model>

<verification>
- `uv run python -m tools.docs_guard` exits 0 after task 4.
- `uv run pytest tools/docs_guard tools/harness_emit -q` green.
- `git log --stat` shows `docs/.docs-review-ledger.toml` in commits authored outside the agent's task
  commits (both ledger writes are human).
- `git diff --check` clean; the ledger carries no timestamp, identity, prose or model identifier.
</verification>

<success_criteria>
Every required seed binding is FRESH or exactly dispositioned, the guard and its CI job are green,
and the loop's 0 -> 1 -> 0 exit transition was observed on a real documentation drift with both
ledger writes performed by a human.
</success_criteria>

<output>
Create `.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-04-SUMMARY.md` when done.
Record the guard's verbatim output at each of the three observation points.
</output>
