---
phase: 29-docs-drive-loop-adoption-integration-closeout-v2-3-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/docs_guard/exclusions.py
  - tools/docs_guard/tests/test_exclusions.py
  - tools/docs_guard/__init__.py
autonomous: true
requirements: [DOCSUP-06]

must_haves:
  truths:
    - "SC-1 (D-06): the five exclusions are a TESTED STRUCTURAL control — a pure function whose deletion turns named table rows RED — not prose in a SKILL body"
    - "`exclusion_reason(target)` IMPORTS `CONSTITUTION_GLOBS` and `DERIVED_GLOBS` from their existing homes; deleting a glob at its home fails this test file, repo-wide"
    - "Path-spelling variants (`./contracts/x`, `docs/./adr/x`, `CONTRACTS/x`, `a/../contracts/x`) are refused identically to the plain spelling — 27.1's CR-01 must not replay on this surface"
    - "The accepted-ADR class returns a DISTINCT reason from the generic constitution class, because its remediation is different (supersede via /adr, never an in-place edit)"
    - "A negative control (`docs/how-to/task-lifecycle.md`) returns None — the function is not a blanket refusal"
    - "The emitted twins of THIS phase's own artifacts (.opencode/skill/docs-upkeep/SKILL.md, .claude/commands/docs-update.md) are refused as derived-plane"
  artifacts:
    - path: "tools/docs_guard/exclusions.py"
      provides: "`exclusion_reason(target: str, root: Path = REPO_ROOT) -> str | None` plus the three reason constants"
      exports: ["exclusion_reason", "REASON_CONSTITUTION", "REASON_DERIVED", "REASON_ACCEPTED_ADR"]
    - path: "tools/docs_guard/tests/test_exclusions.py"
      provides: "the >=6-row adversarial table with spelling variants, the negative control, and the two glob-source deletion proofs"
      contains: "EXCLUSION_CASES"
  key_links:
    - from: "tools/docs_guard/exclusions.py"
      to: "tools/hooks/contract_guard.py::CONSTITUTION_GLOBS"
      via: "import, never retyped"
      pattern: "from tools.hooks.contract_guard import CONSTITUTION_GLOBS"
    - from: "tools/docs_guard/exclusions.py"
      to: "tools/adoption_scan/destinations.py::DERIVED_GLOBS"
      via: "import, never retyped"
      pattern: "from tools.adoption_scan.destinations import DERIVED_GLOBS"
    - from: "tools/docs_guard/exclusions.py"
      to: "tools/harness_perms::resolve_path"
      via: "the one CONFIG-02 glob resolver, fed a resolved + lowered candidate"
      pattern: "resolve_path"
---

<objective>
Turn the five DOCSUP-06 exclusions (accepted ADRs, `docs/reference/**`, `.memory/derived/**`,
contracts, golden) into one pure, tested function in `tools/docs_guard/` so that SC-1's "gate-tested"
clause is satisfied by code, not by a sentence in a skill body.

Purpose: this is the crux of SC-1. Phase 27.1 SC-3 exists because a SKILL body asserted a control
the shipped code did not have. `/docs-update` (plan 29-03) will carry ZERO glob literals and will
name this function instead.

Output: `tools/docs_guard/exclusions.py`, its adversarial table, and the lazy re-export.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-CONTEXT.md
@.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-RESEARCH.md
@tools/docs_guard/registry.py
@tools/docs_guard/__init__.py
@tools/adoption_apply/apply.py
@tools/adoption_scan/destinations.py

<interfaces>
<!-- VERIFIED against shipped Phase 28 code this session. Use directly; do not re-explore. -->

From tools/hooks/contract_guard.py:43
```python
CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**", "golden/**"]
```

From tools/adoption_scan/destinations.py:105-114
```python
DERIVED_GLOBS: list[str] = [
    "docs/reference/**", ".memory/derived/**", ".memory/state/**",
    ".opencode/**", ".claude/agents/**", ".claude/commands/**",
    ".claude/skills/**", "opencode.json",
]
```

From tools/docs_guard/registry.py (all already public via `__all__`)
```python
REPO_ROOT = Path(__file__).resolve().parents[2]
ADR_GLOBS: list[str] = [g for g in CONSTITUTION_GLOBS if g.startswith("docs/adr/")]
def _adr_status(target_path: Path) -> str | None: ...   # PRIVATE — see task 2
```

From tools/adoption_apply/apply.py:215-226 — the normalization shape to COPY (resolved,
root-relative, lower the CANDIDATE only, never the globs; `resolve_path` is `fnmatchcase`, i.e.
case-SENSITIVE, which is precisely why the candidate is lowered):
```python
relative = target_path.relative_to(resolved_root).as_posix()
if resolve_path(REVIEW_LEDGER_GLOBS, relative.lower()) == "deny": ...
refuse_if_constitution(relative.lower())
```

From tools/adoption_apply/apply.py:116 — reuse, do not reimplement:
```python
def refuse_if_outside_root(path: str | Path, root: str | Path) -> None   # raises PathEscapeError
```

From tools/docs_guard/__init__.py:24-42 — `_SUBMODULE_OF` maps export name -> owning submodule and
`__getattr__` dispatches on it with an explicit if/elif chain. Both need one new branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the adversarial exclusion table and record its RED run</name>
  <files>tools/docs_guard/tests/test_exclusions.py</files>
  <behavior>
    `EXCLUSION_CASES` — a parametrized table, one row per (class, spelling), each row
    `(case_id, target, expected_reason)`. Minimum coverage, per D-06 and 29-RESEARCH §3.3:
    - contracts: `contracts/harness/docs/doc-dependencies.schema.json`, `./contracts/...`,
      `CONTRACTS/harness/docs/doc-dependencies.schema.json`, `docs/../contracts/...`
      -> REASON_CONSTITUTION
    - golden: a `golden/**` path -> REASON_CONSTITUTION
    - accepted ADR: `docs/adr/0009-contract-relationship-graph-model.md` and
      `docs/adr/./0009-...md` -> REASON_ACCEPTED_ADR (explicitly asserted `!= REASON_CONSTITUTION`)
    - derived: `docs/reference/manifest.md`, `.memory/derived/contracts-index.md`,
      `.opencode/skill/docs-upkeep/SKILL.md`, `.claude/commands/docs-update.md`
      -> REASON_DERIVED
    - negative controls: `docs/how-to/task-lifecycle.md` and
      `harness/skills/brownfield-adoption/SKILL.md` -> None
    Plus two DELETION-PROOF tests using monkeypatch to empty the imported glob list the module
    reads at call time: with the constitution term neutralized the constitution/ADR rows stop being
    refused; with the derived term neutralized the derived rows stop being refused. These are what
    make "deleting an exclusion turns the test RED" an asserted fact rather than a claim.
  </behavior>
  <action>Write the test file only — `tools/docs_guard/exclusions.py` must NOT exist yet. Import the
  module under test as `from tools.docs_guard import exclusions`. Model the file's docstring and the
  parametrize/ids style on `tools/adoption_apply/tests/test_constitution_refusal.py` (the 28-09
  table). Design the deletion proofs so they patch the ATTRIBUTE the implementation reads
  (`exclusions.CONSTITUTION_GLOBS` / `exclusions.DERIVED_GLOBS`) — this constrains task 2 to read
  the imported names at call time rather than baking a frozen copy into a default argument, which is
  the mutation-resistance the control needs. Do not author any implementation in this task.

  Then run the RED selection PLAIN (never `! uv run pytest` — an inverted gate exits 0 on a
  collection error and masquerades as RED):
  `uv run pytest tools/docs_guard/tests/test_exclusions.py -x -q`
  Record the VERBATIM first failure line in the plan summary. The expected failure is a
  `ModuleNotFoundError`/collection error naming `tools.docs_guard.exclusions`; if instead the run
  reports an unrelated error, fix the test file, not the expectation.

  Commit with an explicit pathspec — `git commit` publishes the ENTIRE shared index:
  inspect `git diff --cached --name-only` first, then
  `git commit -- tools/docs_guard/tests/test_exclusions.py`.</action>
  <verify>
    <automated>uv run pytest tools/docs_guard/tests/test_exclusions.py -q 2>&1 | tail -5; git diff --cached --name-only</automated>
  </verify>
  <done>The table exists with >=6 classes and >=12 rows including the four spelling variants and two
  negative controls; the RED run's verbatim message is recorded; the commit touched exactly one
  file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exclusion_reason and turn the table GREEN</name>
  <files>tools/docs_guard/exclusions.py, tools/docs_guard/__init__.py</files>
  <behavior>
    `exclusion_reason(target: str, root: Path = REPO_ROOT) -> str | None` returns exactly one of
    `REASON_CONSTITUTION` ("constitution-plane"), `REASON_DERIVED` ("derived-plane"),
    `REASON_ACCEPTED_ADR` ("accepted-adr"), or None. Accepted-ADR is checked BEFORE the generic
    constitution class so `docs/adr/**` never returns the generic reason.
  </behavior>
  <action>Create `tools/docs_guard/exclusions.py`. Import `CONSTITUTION_GLOBS` from
  `tools.hooks.contract_guard`, `DERIVED_GLOBS` from `tools.adoption_scan.destinations`,
  `ADR_GLOBS` + `REPO_ROOT` from `tools.docs_guard.registry`, `resolve_path` from
  `tools.harness_perms`, and `refuse_if_outside_root` from `tools.adoption_apply.apply`. Retype no
  glob list — the import IS the control (D-06).

  Normalization, copied from `refuse_unsafe_destination` (apply.py:169-226) and NOT reinvented:
  reject an absolute target or one carrying a literal `..` SEGMENT (`foo/bar..md` is fine); resolve
  `root / target` with `strict=False`; confine with `refuse_if_outside_root`; take
  `.relative_to(root.resolve()).as_posix()`; classify with `resolve_path(<globs>, relative.lower())
  == "deny"`, lowering the candidate only. Read `CONSTITUTION_GLOBS` / `DERIVED_GLOBS` / `ADR_GLOBS`
  as module attributes at call time so task 1's deletion proofs bite.

  Classification order: ADR globs first (return REASON_ACCEPTED_ADR), then DERIVED_GLOBS, then the
  remaining CONSTITUTION_GLOBS, else None. Unlike `registry._adr_status`, do NOT read the ADR's
  `Status:` line here: registry.py already refuses a non-accepted ADR as a binding target at load
  time, so by the time `/docs-update` holds a binding the ADR is accepted, and re-reading file
  CONTENT inside a path-shaped predicate would make the function impure and I/O-dependent. State
  that reasoning in the docstring, citing registry.py's rule so the split is legible.

  Refusal to draft is expressed by the RETURN VALUE, not by raising — this function is a classifier
  consumed by a command, and an exception would force call sites into try/except and invite one to
  swallow it. Note in the docstring that `contract_guard` and `path_deny_globs` remain the runtime
  write-side backstops; this function is the decision layer.

  Add two `_SUBMODULE_OF` rows (`"exclusion_reason"` and the three reason constants -> `"exclusions"`)
  and one `elif` branch in `__getattr__` in `tools/docs_guard/__init__.py`. Phase 28's freeze note in
  that docstring applied within Phase 28; append one line saying Phase 29 extended it for
  `exclusions`.

  Run `uv run pytest tools/docs_guard -q` (narrow selection — the full suite is plan 29-05's job).
  Passes reported alongside any failure prove collection succeeded. Then `ruff check` +
  `ruff format --check` on the two files. Commit with
  `git commit -- tools/docs_guard/exclusions.py tools/docs_guard/__init__.py` after inspecting
  `git diff --cached --name-only`.</action>
  <verify>
    <automated>uv run pytest tools/docs_guard -q && uv run ruff check tools/docs_guard/exclusions.py tools/docs_guard/__init__.py</automated>
  </verify>
  <done>Every EXCLUSION_CASES row passes, both deletion proofs pass, `grep -v '^#' tools/docs_guard/exclusions.py | grep -c 'contracts/\*\*'` is 0 (no retyped glob), and the whole `tools/docs_guard` selection is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry target string -> filesystem path | an attacker-shaped or merely sloppy spelling crosses into a path decision |
| glob-list home -> this module | a deletion at the home must not be silently absorbed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | `exclusion_reason` path matching | mitigate | resolved-path + lowered-candidate matching copied from `refuse_unsafe_destination`; `./`, `..`, and case variants are table rows (27.1 CR-01 replay) |
| T-29-02 | Tampering | glob list drift | mitigate | import `CONSTITUTION_GLOBS`/`DERIVED_GLOBS`, never retype; two monkeypatch deletion proofs assert the dependency is real |
| T-29-03 | Repudiation | accepted-ADR remediation | mitigate | distinct `accepted-adr` reason so a caller cannot route an ADR to the in-place-edit branch |
| T-29-04 | Elevation of Privilege | path escape via absolute/`..` target | mitigate | structural pre-check + `refuse_if_outside_root` before any classification |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | none in this plan — zero new dependencies; no install task exists |
</threat_model>

<verification>
- `uv run pytest tools/docs_guard -q` green.
- `uv run ruff check tools/docs_guard` clean.
- No glob literal retyped: `grep -v '^#' tools/docs_guard/exclusions.py | grep -c 'docs/reference/\*\*'` returns 0.
- `git diff --check` clean on the touched files.
</verification>

<success_criteria>
`exclusion_reason` classifies all five exclusion classes plus their spelling variants, returns a
distinct reason for accepted ADRs, returns None for the two negative controls, and its two
imported glob sources are proven load-bearing by monkeypatch deletion tests.
</success_criteria>

<output>
Create `.planning/phases/29-docs-drive-loop-adoption-integration-closeout-v2-3-c/29-01-SUMMARY.md` when done.
Record the verbatim RED failure line from task 1.
</output>
