---
phase: 45-projection-repair
plan: 06
type: execute
wave: 5
depends_on: ["45-03", "45-05"]
files_modified:
  - harness/agents/templates/component-engineer.md
  - harness/agents/templates/engineer.md
  - .planning/phases/43-lifecycle-plane-removal/43-VERIFICATION.md
  - tools/ruff_baseline/baseline.json
autonomous: true
requirements: [CER-10, CER-11]
user_setup: []

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-03 (the relocated baselines stay gated at the merge — no in-session hook is added for them), D-12 (component-engineer.md is CORRECTED, not deleted: two live gates depend on it and ADR-0003:95 cites it), D-13 (the core→instance coupling is RECORDED at its measured 6-test extent and deliberately not partially fixed), D-14 (the ADR-0008 AND ADR-0003 dangling-citation gaps are recorded for a human-authored superseding ADR, never authored unilaterally), D-15 (43-VERIFICATION.md's unsatisfiable SC-1 wording is corrected to the executable-invocation form it already recorded), D-19 (verify by running, per commit), D-20 (any commit touching the instance carries an explicit examples/log-parser pytest leg, because uv run pytest -q does not collect it), D-23 (whole-phase LOC reported from git diff --shortstat), D-24 (the CODEOWNERS enforcement residual is recorded and surfaced to the owner, NOT compensated by a new in-session control)."
    - "harness/agents/templates/component-engineer.md describes only steps that still exist: /component no longer copies it and the core [pipeline] table it referenced is gone."
    - "Both template corrections obey the GEN-04 phrasing rule — the instance-scoped golden CODEOWNERS route is named in words, with no examples/ token, because harness/ is under _CORE_ROOTS."
    - "The component-engineer template survives — deleting it would red the core suite's exact-set template gate AND dangle an accepted, append-only ADR."
    - "The ruff ratchet records whatever the live count is at phase close — a measured value, not a pinned one — and it is updated LAST so it captures every prose edit in the phase."
    - "The phase closes with all four derived generators producing an empty diff, both suites green, and the four residuals named in writing."
  artifacts:
    - path: "harness/agents/templates/component-engineer.md"
      provides: "the persona template ADR-0003 and two live tests depend on"
    - path: "tools/ruff_baseline/baseline.json"
      provides: "the lint ratchet floor"
      contains: "total"
  key_links:
    - from: "examples/log-parser/tests/test_pipeline_topology.py:115"
      to: "harness/agents/templates/component-engineer.md"
      via: "_CORE_RESOLUTION_DOCS — instance suite only, NOT collected by uv run pytest"
      pattern: "component-engineer"
    - from: "tools/harness_lint/tests/test_agent_templates.py:33"
      to: "harness/agents/templates/"
      via: "EXPECTED_TEMPLATES exact-set assertion in the CORE suite"
      pattern: "EXPECTED_TEMPLATES"
---

<objective>
Correct the two persona templates' self-descriptions, fix the verification record whose wording can
never pass, refresh the lint ratchet, and put the phase's four residuals in writing.

Purpose: this is the "record itself" tier. Three of the four items are things the phase deliberately
does NOT do — and an undocumented non-action is indistinguishable from an oversight at the next
review. The ratchet lands last because E501 is line-length-sensitive and every prose commit in plans
01–05 moves it.

Output: three commits. Suite 876 core / 31 instance.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-projection-repair/45-CONTEXT.md
@.planning/phases/45-projection-repair/45-RESEARCH.md
@.planning/phases/45-projection-repair/45-01-SUMMARY.md
@.planning/phases/45-projection-repair/45-05-SUMMARY.md
@.planning/phases/43-lifecycle-plane-removal/43-VERIFICATION.md

<gen04_phrasing_rule>
⚠ READ THIS BEFORE WRITING ANY NEW SENTENCE INTO `harness/agents/templates/**`.

`tools/harness_lint/tests/test_core_no_example_dep.py` scans EVERY git-tracked file under
`_CORE_ROOTS = ("tools", "harness", "libs")` (`:44`) for the `_PATH_TOKENS` entry `"examples/"`
(`:53`). `harness/agents/templates/**` is inside that scan — being absent from the emit manifest
exempts it from the EMITTER, not from the GUARD.
`tools/contract_graph/tests/test_cross_repo_authority.py::test_gen04_core_no_instance_dep_guard_stays_green`
reds alongside it — measured RED on task 1's commit when `component-engineer.md:46` and
`engineer.md:39` were repointed at `/examples/*/golden/` as the first draft instructed.

**RULE:** phrase the route **without the literal token `examples/`** — e.g. *"the instance-scoped
golden CODEOWNERS route"*. Reuse the exact wording recorded in `45-01-SUMMARY.md` / `45-03-SUMMARY.md`
so all five corrected sites read the same. Never write `/examples/*/golden/` into a file under
`harness/`.
</gen04_phrasing_rule>

<interfaces>
<!-- The three dependents of component-engineer.md. Two are gates; one is an append-only ADR. -->

tools/harness_lint/tests/test_agent_templates.py:33
  EXPECTED_TEMPLATES = frozenset({"engineer", "component-engineer"})   # CORE suite, exact set
examples/log-parser/tests/test_pipeline_topology.py:115
  _CORE_RESOLUTION_DOCS = (_REPO_ROOT/"harness"/"agents"/"templates"/"component-engineer.md",)
  ⚠ INSTANCE suite only. `uv run pytest -q` uses testpaths = ["libs/python", "tools"] and does NOT
  collect it. This plan is the one place in Phase 45 that needs the explicit instance leg.
docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md:95
  accepted, append-only — describes /component copying this template.

harness/agents/templates/** is absent from tools/harness_emit/emit-manifest.json, so it is NOT
emitted and editing it does not move .claude/ or .opencode/. It IS scanned by the GEN-04 guard —
see the phrasing rule above.

tools/ruff_baseline/baseline.json:4   "total": 84
  73 is the PRE-PHASE live count, measured at 6c41989 — it is NOT the value this task records.
  Every prose edit in plans 01–05 lengthens lines, and the replay measured the count landing at 79
  after all thirteen commits. The ratchet PASSES on any shrink (79 < 84), so CI does not fail either
  way; what is being fixed is a committed derived count that no longer describes reality (D-06).
  RECORD WHAT `--update` REPORTS. Do not pin a number in advance, and do not treat a rise from 73 as
  a regression — a rise is expected and is exactly why this task runs last.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the persona templates' self-descriptions (needs the instance leg)</name>
  <files>harness/agents/templates/component-engineer.md, harness/agents/templates/engineer.md</files>
  <action>
Implements D-12 and D-06. DECISION, to be restated in the summary: the template is CORRECTED, not
deleted. Deleting it would red `tools/harness_lint/tests/test_agent_templates.py:33` (an exact-set
assertion in the CORE suite), break `examples/log-parser/tests/test_pipeline_topology.py:115`, and
dangle `docs/adr/0003:95` — an accepted, append-only, constitution-plane ADR whose citation cannot be
repaired without a superseding ADR. Re-wiring `/component` to restore the copy step is product work
and belongs to Phase 46; flag it, do not do it.

`harness/agents/templates/component-engineer.md`:
  - `:6-7` — "`/component` instantiates a COPY of this file into the active instance's own agents/
    directory (the instance root is declared by harness/project.toml [instance] root) as
    <COMPONENT>.md". Phase 44 deleted that step. Restate what is true now: this is a template a human
    or agent copies by hand into the instance's `agents/` directory; `/component` no longer performs
    the copy. Keep the surrounding explanation of WHY the file lives under `templates/` (the persona
    anti-sprawl gate globs `harness/agents/*.md` non-recursively) — that is still exactly true and is
    load-bearing for `test_agent_templates.py`.
    ⚠ PHRASING CONSTRAINT: write the replacement WITHOUT the literal phrase `instantiates a COPY`.
    The verify below greps for that phrase as the stale-claim signature; re-using it in a corrected
    sentence ("`/component` no longer instantiates a COPY…") would falsify the check.
  - `:12-14` — "`<STAGE>` (its ordinal in the `[pipeline]`)". The core `[pipeline]` table was deleted
    in Phase 44. Either drop the `<STAGE>` slot's stale derivation or restate it as instance-declared.
    ⚠ PHRASING CONSTRAINT: the replacement must not contain the literal token `[pipeline]` — say
    "the instance's own pipeline declaration" instead. Same reason as above.
  - `:46` — "promotion is human review at the PR (the `golden/` CODEOWNERS entry)". Plan 01 deleted
    that route; repoint it to the instance-scoped golden CODEOWNERS route, described in words per the
    GEN-04 PHRASING RULE — no `examples/` token.

`harness/agents/templates/engineer.md:39` — the identical `golden/` CODEOWNERS-entry pointer.
Repoint it the same way, with the same wording and the same GEN-04 constraint.

Do NOT touch either file's front-matter `name` or `description`, and do not touch the `<PLACEHOLDER>`
token names — the instance test resolves against them.

⚠ This commit REQUIRES both legs (D-20): `uv run pytest -q` does not collect `examples/**`, so the
instance test that reads `component-engineer.md` is invisible to the default run. Run both before
committing.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>uv run pytest examples/log-parser -q 2>&1 | tail -3   # expect: 31 passed — REQUIRED, not optional</automated>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/contract_graph/tests/test_cross_repo_authority.py -q 2>&1 | tail -3   # expect: all passed — no `examples/` token leaked into the templates</automated>
    <automated>uv run python -m tools.harness_emit && git status --porcelain   # expect: empty — templates are not emitted</automated>
    <automated>grep -n 'instantiates a COPY\|\[pipeline\]' harness/agents/templates/component-engineer.md   # expect: no output — satisfiable because the two phrasing constraints above forbid re-using either literal in the corrected text</automated>
    <automated>git grep -n '`golden/`' -- harness/agents/templates   # expect: no output — both route pointers repointed</automated>
  </verify>
  <done>Both templates describe only live steps and point at the instance-scoped golden CODEOWNERS route in words; GEN-04 is green; core suite 876 AND instance suite 31 both green; the emitted trees are untouched; the keep-don't-delete decision and the Phase-46 `/component` re-wiring flag are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the unsatisfiable verification wording and write the four residuals</name>
  <files>.planning/phases/43-lifecycle-plane-removal/43-VERIFICATION.md</files>
  <action>
Implements D-15 and D-14.

`43-VERIFICATION.md:8` — the `must_have` string reads "SC-1 — the ROADMAP's literal bare-token grep
over tools/ harness/ contracts/ .github/ .claude/ .opencode/ returns nothing". That can never pass:
the negative-control fixture at `tools/contract_graph/tests/test_query.py:75-78` MUST contain the
forbidden strings for the assertion above it to mean anything. Replace the wording with the
executable-invocation form the same record already carries at `:16-17` — the
`(python -m tools\.|from tools\.|import tools\.)` regex over the same six product-surface paths
returning zero real invocations. Leave the `reason`, `accepted_by` and `accepted_at` fields intact;
this corrects the criterion, it does not re-litigate the override.

Do NOT touch `tools/contract_graph/tests/test_query.py`. Those forbidden strings are the negative
control; stripping them to force a clean grep would delete the control. Phase 43's and Phase 44's
executors each refused this and both were right (D-05).

D-15's other half is moot and should be recorded as such: CONTEXT calls this "Phase 43's ROADMAP SC-1
wording", but the live ROADMAP Phase 43 entry has no numbered success criteria — only a one-line
`**Success:**` sentence — and there is no v2.5 milestone archive. There is nothing to reword in the
ROADMAP. Confirm this with `ls .planning/milestones/` and a read of the Phase 43 ROADMAP entry before
asserting it.

Then write the four residuals into `45-06-SUMMARY.md`. These are the phase's deliverable as much as
any edit is:

  (a) D-14 — TWO accepted ADRs cite documents this milestone's deletions made stale.
      `docs/adr/0008-task-control-plane-lifecycle.md:50` cites
      `docs/explanation/next-milestone-task-control-plane.md`, and
      `docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md:95` describes `/component` copying
      `component-engineer.md`. ADR-0003's defect has never been named before this phase. Both need a
      human-authored superseding ADR; ADR-0008 additionally still reads `Status: Accepted`,
      `Superseded by: —` while Phase 43 deleted its plane. Do not author either ADR.
  (b) D-13 — the core suite's coupling to the reference instance, at its MEASURED extent:
      `git rm -r examples/` reds SIX tests across FOUR modules
      (`tools/adoption_scan/tests/test_install_completeness.py::test_every_ci_pytest_path_argument_resolves`,
      `tools/contract_graph/tests/test_cross_repo_authority.py::test_reference_instance_config_is_untouched`,
      `tools/harness_config/tests/test_loader.py::test_persona_files_exist_on_disk`,
      `tools/harness_config/tests/test_topology_relationships.py::test_instance_config_needs_no_explicit_records`,
      `tools/harness_lint/tests/test_language_config.py::test_each_configured_persona_exists` and
      `::test_each_configured_language_has_test_paths`) — not the one test CONTEXT D-13 names, and
      that test is not even at the path CONTEXT gives (`tools/harness_lint/tests/test_ci_paths.py`
      does not exist). DECISION: defer whole, do not partially fix. Four of the six fail because
      `harness/project.toml`'s `[instance]` slot points at the instance, which is precisely ADR-0002's
      design — the instance is DATA in a slot. Fixing only the accidental `ci.yml` coupling would
      manufacture a false claim that the core is instance-independent while five real couplings
      remain. Making the slot optional is a design change, not projection repair.
      Also record the GEN-04 guard's TWO-SIDED shape, because both halves bit this phase:
        * `_CORE_ROOTS = ("tools", "harness", "libs")` (`test_core_no_example_dep.py:44`) scans
          neither `pyproject.toml` nor `.github/`, so the guard structurally cannot SEE either
          coupling in (b).
        * The same `_CORE_ROOTS` scans `tools/`, `harness/` and `libs/` in full — which is exactly the
          trees plans 01, 03 and 06 rewrite prose in. The replay measured five core-plane sentences
          RED-ing the guard (2 failed / 872) because they named `/examples/*/golden/`. That is why
          every one of those plans carries a GEN-04 PHRASING RULE. Record the rule as a standing
          convention for future phases, not a one-off workaround.
  (c) D-24 — the CODEOWNERS enforcement residual. D-01 removed the last IN-SESSION control on golden
      self-blessing. Measured via `gh api repos/hjung3113/lifetimeworkflow/branches/main/protection`:
      `main` requires the `gate` status check (strict) but has NO `required_pull_request_reviews`
      block at all, and `enforce_admins` is `false` — so "Require review from Code Owners" is OFF and
      `CODEOWNERS:36` auto-requests the owner without being able to block the merge, exactly as
      `.github/CODEOWNERS:9-14` warns. The required `gate` check does not close this: CI validates
      against whatever baseline is committed, so a mutated `baseline.verified.tsv` makes CI green.
      Surface the one-toggle remedy at the milestone-close PR — it is a repo-settings action no file
      in this repo can perform:
      `gh api -X PUT repos/hjung3113/lifetimeworkflow/branches/main/protection/required_pull_request_reviews -f require_code_owner_reviews=true`
      Per D-03 and D-24, do NOT add any in-session control to compensate. That is the surface growth
      the milestone constraint forbids. Re-run the `gh api` probe if it is available and record the
      answer as of phase close rather than restating the research value.
  (d) SC-1's CODEOWNERS clause — declined in plan 02, with the reason. Carry it forward here so the
      phase's own verification does not read it as an unexplained miss.

This commit touches one file. `.planning/` is tracked, so keep it out of the D-23 LOC figure by
reporting that figure from the code/doc paths, and say so.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>grep -n 'python -m tools' .planning/phases/43-lifecycle-plane-removal/43-VERIFICATION.md | head -3   # expect the corrected executable-invocation wording at the must_have</automated>
    <automated>git grep -c 'task_control' tools/contract_graph/tests/test_query.py   # expect >0 — the negative control must survive</automated>
  </verify>
  <done>`43-VERIFICATION.md:8` states a criterion that can actually pass; the negative control in `test_query.py` is untouched; the ROADMAP half of D-15 is confirmed moot by `ls .planning/milestones/` and a read of the Phase 43 entry; all four residuals are written out with their measured evidence, including the two-sided GEN-04 finding.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the ruff ratchet last and run the phase gate</name>
  <files>tools/ruff_baseline/baseline.json</files>
  <action>
Implements CER-10's one live residue and D-23. This MUST be the last commit of the phase: the ratchet
counts E501 (line length), and every prose edit in plans 01–05 moves that count. Updating it earlier
would immediately re-stale it.

Run `uv run python -m tools.ruff_baseline` first and RECORD WHAT IT PRINTS — do not check it against
a number from this plan. Context for reading the output: 73 was the count at `6c41989`, BEFORE any of
this phase's thirteen commits; the replay measured the post-phase count at 79, because the phase's own
prose corrections add ~6 E501 findings. A RISE from 73 is expected and correct. The committed baseline
is 84, and the ratchet passes on any value below it, so CI stays green throughout; what this task
fixes is that the committed derived count no longer describes reality (D-06), not a CI failure.

Then run `uv run python -m tools.ruff_baseline --update` and commit `tools/ruff_baseline/baseline.json`
alone. Do not hand-edit the JSON. Record the before/after totals it reports in the summary; the
post-update re-run must show zero delta.

Then run the whole phase gate and record every number in the summary:
  - `uv run pytest -q` → 876 passed, 7 snapshots
  - `uv run pytest examples/log-parser -q` → 31 passed
  - `uv run python -m tools.harness_emit` → empty `git status --porcelain`
  - `uv run python -m tools.contract_hash.hash` → empty diff
  - `uv run python -m tools.docs_sync.generate` → empty diff
  - `uv run python -m tools.memory_regen.contracts_index` → empty diff
  - `uv run python -m tools.ruff_baseline` → PASS at the new floor with zero delta
  - `git diff --shortstat 354dbdf..HEAD` → the whole-phase LOC figure for D-23. Expect a small net
    deletion: this phase corrects and deletes prose, it does not remove machinery.
  - The two DECLARATION sweeps (scoped, not bare-token — see the note below):
      `git grep -n '"golden/\*\*"' -- tools harness .github` → no output
      `uv run python -c "import json; from tools.hooks.contract_guard import CONSTITUTION_GLOBS as g; m=json.load(open('harness/permission-matrix.json'))['path_deny_globs']; assert list(g)==list(m)==['contracts/**','docs/adr/**','docs/glossary.md'], (g, m); print('declarations OK')"`
      `git grep -n '\`golden/\`\|\`golden/\*\*\`' -- harness .claude .opencode` → no output
  - `git grep -n 'golden-approve' -- . ':!.planning/' ':!docs/adr/' ':!docs/references/' ':!examples/' ':!CLAUDE.md' ':!docs/glossary.md' ':!tools/harness_emit/tests/test_coexist.py' ':!tools/harness_lint/tests/test_commands.py'` → no output

⚠ Why the sweeps are scoped rather than bare-token. A bare `git grep -n 'golden/\*\*' -- tools harness
.github` CANNOT pass at phase close and must not be used: plan 01 deliberately writes the ADR-0012
supersession narration ("…`golden/**` leaves the constitution-plane core…") into the
`harness/permission-matrix.json` `_note` and the `contract_guard.py:43` comment, and that narration is
the point of the phase, not residue. Scoping to the QUOTED literal `"golden/**"` tests the same thing
— that no DECLARATION entry survives — while staying satisfiable on a correct commit. Likewise the
`golden-approve` sweep carries three exclusions beyond the original set: `docs/glossary.md` (owned by
plan 05 task 3; untouched under the `defer-to-pr` branch), and
`tools/harness_emit/tests/test_coexist.py:56` + `tools/harness_lint/tests/test_commands.py:42`, which
narrate the Phase-44 RETIREMENT of the command pair and are therefore accurate history, not stale
claims (D-06). If plan 05 task 3 resolved to `ratify-now`, drop the `':!docs/glossary.md'` exclusion
and require zero hits there too.

If any derived generator dirties the tree at this point, that is a CER-10 finding and must be
reported, not silently regenerated into the ratchet commit.
  </action>
  <verify>
    <automated>uv run python -m tools.ruff_baseline 2>&1 | tail -3   # expect: PASS with zero delta at the newly recorded floor (whatever --update reported)</automated>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>uv run pytest examples/log-parser -q 2>&1 | tail -3   # expect: 31 passed</automated>
    <automated>uv run python -m tools.harness_emit && uv run python -m tools.contract_hash.hash && uv run python -m tools.docs_sync.generate && uv run python -m tools.memory_regen.contracts_index && git status --porcelain   # expect: empty</automated>
    <automated>git grep -n '"golden/\*\*"' -- tools harness .github   # expect: no output — no declaration entry survives anywhere in the core planes or CI config</automated>
    <automated>git grep -n 'golden-approve' -- . ':!.planning/' ':!docs/adr/' ':!docs/references/' ':!examples/' ':!CLAUDE.md' ':!docs/glossary.md' ':!tools/harness_emit/tests/test_coexist.py' ':!tools/harness_lint/tests/test_commands.py'   # expect: no output</automated>
  </verify>
  <done>The ratchet records the live measured count (expected to be above the pre-phase 73 and below the old 84) and prints a clean PASS with zero delta; all four derived generators produce an empty diff; both suites green; both scoped declaration sweeps and the `golden-approve` sweep return nothing; the whole-phase `git diff --shortstat` figure is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core suite → reference instance | `harness/project.toml`'s `[instance]` slot; the GEN-04 guard cannot see the `pyproject.toml` / `.github/` half of it, but DOES scan every prose line in `tools/`, `harness/` and `libs/` |
| PR → main | the required `gate` status check; CODEOWNERS is advisory unless a repo setting is flipped |
| committed derived counts → CI verdicts | a stale baseline silently loosens the ratchet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-18 | Tampering | agent mutates `examples/log-parser/golden/**/baseline.verified.tsv` and CI greens against it | accept + record | D-01/D-03 ratified merge-time gating; measured branch protection has no `required_pull_request_reviews`, so CODEOWNERS cannot block. Recorded verbatim for the owner with the exact one-command remedy. Explicitly NOT compensated in-session — D-24 forbids that as surface growth. |
| T-45-19 | Repudiation | a partial D-13 fix creates a false "core is instance-independent" claim | mitigate | Deferred whole and recorded with the measured 6-test/4-module extent and the ADR-0002 reasoning, so a later reader sees a decision rather than an omission. |
| T-45-20 | Tampering | ratchet baseline updated before the phase's prose edits, re-staling it immediately | mitigate | The ratchet commit is ordered last in the last plan of the last wave, with the ordering reason stated, and the task records the measured value rather than a pinned one. |
| T-45-21 | Repudiation | deleting the negative-control strings in `test_query.py` to force a clean grep | mitigate | Explicitly forbidden; a verify step asserts the strings still exist. |
| T-45-23 | Repudiation | a phase-close sweep is written so broadly it cannot pass, tempting the executor to strip deliberate prose to force it green | mitigate | Both `golden/**` sweeps are scoped to the quoted DECLARATION literal plus a direct equality assertion on the two loaded declarations; the `golden-approve` sweep excludes the two retirement records and the plan-05-owned glossary. The reason for each exclusion is stated inline. |
| T-45-24 | Tampering | a template route repair leaks an `examples/` token into `harness/`, RED-ing GEN-04 | mitigate | The GEN-04 PHRASING RULE is stated in the context block and re-flagged at both sites; the guard and its cross-repo companion run as a dedicated verify leg in task 1. |
| T-45-SC | Tampering | npm/pip/cargo install | accept | No package-manager install anywhere in Phase 45; `uv.lock` is untouched by all six plans. |
</threat_model>

<verification>
- Full phase gate as enumerated in task 3: both suites, all four derived generators empty-diff, ratchet
  PASS at the newly measured floor, and the three scoped sweeps returning nothing.
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/contract_graph/tests/test_cross_repo_authority.py -q`
  → green on task 1, before the commit.
- `uv run pytest examples/log-parser -q` is MANDATORY for task 1 (D-20) — it is the only commit in the
  phase whose blast radius reaches a test the default run does not collect.
</verification>

<success_criteria>
- Both persona templates describe only live steps; neither is deleted; neither carries an `examples/`
  token.
- `43-VERIFICATION.md`'s SC-1 `must_have` states a criterion that can pass.
- The ratchet floor equals the live measured count at phase close.
- All four residuals (ADR-0008 + ADR-0003 gaps, the D-13 measured coupling plus the two-sided GEN-04
  finding, the D-24 CODEOWNERS enforcement gap with its one-command remedy, and the declined SC-1
  CODEOWNERS clause) are written down with their evidence.
- ROADMAP SC-7 holds: `emit-drift`, `stale-derived`, `contract-drift` and the ruff ratchet are green
  with an empty diff, and `uv run pytest -q` was green at every commit of the phase.
- ROADMAP SC-8 holds: net +0 gates, tools, contracts, dependencies.
</success_criteria>

<output>
Create `.planning/phases/45-projection-repair/45-06-SUMMARY.md` when done. It carries the phase's four
residuals and the D-23 LOC figure; treat it as a deliverable, not a formality.
</output>
