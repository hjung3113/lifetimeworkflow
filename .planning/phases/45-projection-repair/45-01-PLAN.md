---
phase: 45-projection-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/permission-matrix.json
  - tools/hooks/contract_guard.py
  - tools/hooks/tests/test_contract_guard.py
  - tools/harness_perms/tests/test_resolver.py
  - tools/harness_perms/tests/test_order_resolution.py
  - tools/harness_lint/tests/test_agents.py
  - tools/adoption_apply/tests/test_constitution_refusal.py
  - .github/CODEOWNERS
  - tools/adoption_scan/destinations.py
autonomous: true
requirements: [CER-11]
user_setup: []

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-01 (golden/** is REMOVED from the constitution plane per ADR-0012 clause (d), not repointed), D-02 (the plane is three members everywhere it is duplicated, moved in ONE commit), D-04 (the *.env deny rows and their asserting tests die together), D-08 (contract_guard's live refusal text stops naming /golden-approve and the relocated approve.py path), D-16 (every live-tree-rendering test is repaired in the same commit that invalidates it), D-17 (git commit -m <msg> -- <pathspec>; never git add -A), D-19 (run the suite per commit; pytest --collect-only catches none of this), D-21 (no glob or CODEOWNERS route matches zero paths while claiming a plane)."
    - "The constitution plane is exactly contracts/**, docs/adr/**, docs/glossary.md in every one of the eleven copies of the declaration."
    - "harness/permission-matrix.json declares no deny row without a live enforcer: path_deny_globs holds three entries and no *.env row."
    - "No test asserts a deny that nothing performs: test_golden_write_denied, test_dotenv_denied and test_unapproved_golden_write_denied are gone, and the byte-hygiene CRLF control is re-subjected rather than deleted."
    - "contract_guard's user-visible refusal string names GOLDEN_APPROVE_HUMAN and CODEOWNERS, and names no retired command."
    - "No CODEOWNERS route in the constitution block matches zero paths: /golden/ and /approvals/ are gone."
    - "uv run pytest -q is green at the end of every commit in this plan (874 passed, 7 snapshots)."
  artifacts:
    - path: "tools/hooks/contract_guard.py"
      provides: "the only ENFORCED constitution declaration"
      contains: "CONSTITUTION_GLOBS"
    - path: "harness/permission-matrix.json"
      provides: "the inert permission data mirror of that declaration"
      contains: "path_deny_globs"
    - path: ".github/CODEOWNERS"
      provides: "merge-time human ratification of the plane"
  key_links:
    - from: "harness/permission-matrix.json"
      to: "tools/hooks/contract_guard.py"
      via: "tools/harness_lint/tests/test_agents.py sync assertion"
      pattern: "_CONSTITUTION_DENY_GLOBS"
    - from: "tools/hooks/contract_guard.py"
      to: "tools/adoption_apply/apply.py and tools/adoption_scan/destinations.py"
      via: "imported CONSTITUTION_GLOBS constant"
      pattern: "from .*contract_guard import"
---

<objective>
Collapse the constitution-plane declaration from four members to three (`contracts/**`,
`docs/adr/**`, `docs/glossary.md`) in every one of its eleven copies, delete the two enforcer-less
`*.env` deny rows with the tests that kept them green, and remove the two CODEOWNERS routes and the
one adoption-catalog glob that match zero paths.

Purpose: this is the only security-relevant half of Phase 45. A glob that matches nothing while its
comment claims a plane is a false claim about what the repo protects. ADR-0012 clause (d)
(`docs/adr/0012-ci-and-merge-as-decision-authority.md:139-147`) already ratified the removal and
named this exact transitional state; Phase 44 made the code move, so no new ADR is needed.

Output: two commits, each ending green at 874 passed.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-projection-repair/45-CONTEXT.md
@.planning/phases/45-projection-repair/45-RESEARCH.md
@docs/adr/0012-ci-and-merge-as-decision-authority.md
@AGENTS.md

<interfaces>
<!-- The declaration and its consumers. Do not go exploring; these are the contracts. -->

tools/hooks/contract_guard.py
  CONSTITUTION_GLOBS: list[str]   # line 52 — the ONLY enforced copy
  is_constitution(relative_path) -> bool          # line 66, calls resolve_path(CONSTITUTION_GLOBS, ...)
  decide(path, content, approved) -> dict | None  # emits the user-visible refusal string at :86-90

tools/harness_perms/resolver.py
  resolve_path(globs, path) -> "deny" | "allow"   # fnmatchcase: `*` crosses `/`, `**` is just `*`

Importers of CONSTITUTION_GLOBS (production, must keep working):
  tools/adoption_apply/apply.py:51,87
  tools/adoption_scan/destinations.py:87,353

Readers of harness/permission-matrix.json path_deny_globs (tests only — no production reader):
  tools/harness_perms/tests/test_resolver.py
  tools/harness_perms/tests/test_order_resolution.py
  tools/harness_lint/tests/test_agents.py:196-198
tools/harness_emit/permissions.py:23 STRIPS `_note` and `path_deny_globs` before emit
  (_RESOLVER_ONLY_KEYS) — this is why Tier 1 does NOT touch the emitted trees.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the plane to three members and delete the *.env deny rows (one commit)</name>
  <files>harness/permission-matrix.json, tools/hooks/contract_guard.py, tools/hooks/tests/test_contract_guard.py, tools/harness_perms/tests/test_resolver.py, tools/harness_perms/tests/test_order_resolution.py, tools/harness_lint/tests/test_agents.py, tools/adoption_apply/tests/test_constitution_refusal.py</files>
  <action>
Implements D-01, D-02, D-04, D-08, D-16. All seven files change in ONE commit — removing `golden/**`
from the two data sites and repairing nothing was measured at 7 failures across 5 modules, all
runtime assertions, zero collection errors. `pytest --collect-only` sees none of it, so a partial
commit would look fine and be red.

Do the edits in this order, then verify once, then commit once.

(1) `harness/permission-matrix.json`. In `path_deny_globs` remove the `golden/**`, `*.env` and
`**/*.env` entries, leaving exactly `contracts/**`, `docs/adr/**`, `docs/glossary.md`. Then rewrite
the `_note` prose at `:2` so the file stops contradicting its own data: it currently says "The
constitution subset is the FOUR members declared by docs/adr/0001-walking-skeleton-golden-core.md:48
— contracts/**, docs/adr/**, golden/** and the literal file docs/glossary.md". Make it THREE members,
state that ADR-0001:48's four-member declaration is superseded by ADR-0012 clause (d) to the extent
that `golden/**` leaves the constitution-plane core, and that the relocated instance baselines are
ratified at the merge by the CODEOWNERS `/examples/*/golden/` route rather than in-session (D-03).
Also correct the phrase "constitution/secret path-scoped denies" — after this commit there are no
secret rows — and add one sentence recording that `path_deny_globs` is now an exact duplicate of
`contract_guard.CONSTITUTION_GLOBS` with no independent production reader, kept in sync by
`tools/harness_lint/tests/test_agents.py`. That sentence is the SC-8-compliant answer (a recorded
fact, not a new mechanism).

(2) `tools/hooks/contract_guard.py`. Set `CONSTITUTION_GLOBS` at `:52` to the three members. Then
repair every prose copy in the same file, because six of them are now false:
`:4` (module docstring plane list — drop `golden/**`); `:9` and `:75` (both name the retired
`/golden-approve` command as the ratification path — replace with a human `GOLDEN_APPROVE_HUMAN`
token ratified at the PR through CODEOWNERS); `:17-18` (the caveat "NOT the full matrix
path_deny_globs union (which also carries *.env)" is false after this commit — rewrite it to state
that the two lists are now identical and are kept in sync by `tools/harness_lint/tests/test_agents.py`);
`:43` (the comment "The four members are DECLARED by docs/adr/0001-...:48" — make it three and cite
ADR-0012 clause (d) as the superseding authority); `:55` (the path `tools/golden_runner/approve.py`
was relocated in Phase 44 — repoint to `examples/log-parser/golden_runner/approve.py`);
`:88` (the refusal string embeds `(contracts/ · docs/adr/ · golden/ · docs/glossary.md)` — drop
`golden/`); `:89` (the refusal string says "only be changed via /golden-approve with a human
GOLDEN_APPROVE_HUMAN token" — rewrite so it names no retired command, e.g. only be changed by a human
who sets GOLDEN_APPROVE_HUMAN, ratified at the PR through CODEOWNERS).
CONSTRAINT on the new refusal string: it MUST still contain the literal substring `CODEOWNERS`
(asserted at `test_contract_guard.py:52` and `:289`, which must stay green) and MUST NOT contain
`golden-approve`.

(3) `tools/hooks/tests/test_contract_guard.py`. Update the module docstring plane lists at `:3` and
`:10` and the ADR-0001 comment block near `:272-273`. Change the two `assert "golden-approve" in
reason` assertions at `:51` and `:288` to assert a token the new message actually carries — use
`GOLDEN_APPROVE_HUMAN`; leave the neighbouring `assert "CODEOWNERS" in reason` lines untouched.
DELETE `test_unapproved_golden_write_denied` (`:61-64`) — it asserts a deny that nothing performs.
Do NOT delete `test_approved_constitution_with_crlf_still_denied` (`:124`): it is a byte-hygiene
control that merely happened to use a `golden/` probe path, and it is that control's only test —
re-subject the probe to a live constitution path such as `contracts/x.schema.json`. In
`test_every_declared_plane_member_is_independently_enforced`, remove the `"golden/**"` probe entry
from the dict at `:361` and correct the assertion message at `:365` that says ADR-0001:48 "declares
exactly these four members" to three members plus the ADR-0012 supersession.

(4) `tools/harness_perms/tests/test_resolver.py`. DELETE `test_golden_write_denied` (`:59-60`) and
`test_dotenv_denied` (`:63-64`). Both assert denies that nothing performs.

(5) `tools/harness_perms/tests/test_order_resolution.py`. Update the constitution-plane sentence in
the module docstring at `:9`, and remove three parametrize cases from
`test_constitution_and_secret_paths_denied`: `golden/repr-only/expected/baseline.verified.tsv`
(`:114`), `config/prod.env` (`:117`) and `components/collector/.env` (`:118`). Leave
`test_ordinary_source_paths_allowed` alone — its params are `libs/python/foo.py`,
`components/parser/Program.cs` and `tools/hooks/commit_gate.py`, none of which is affected.

(6) `tools/harness_lint/tests/test_agents.py`. Set `_CONSTITUTION_DENY_GLOBS` at `:44` to the same
three members. The assertion at `:197` compares it against the matrix.

(7) `tools/adoption_apply/tests/test_constitution_refusal.py`. The parametrize list carries
`"golden/y/baseline.verified.tsv"` (around `:34`). RE-SUBJECT it to a still-denied constitution
path (for example `docs/adr/y/0099-example.md`) rather than deleting the case — the refusal control
must keep a probe. This choice is also what makes the arithmetic below come out.

Test-count arithmetic to self-check against (measured): 880 − 3 whole tests deleted
(`test_golden_write_denied`, `test_dotenv_denied`, `test_unapproved_golden_write_denied`)
− 3 parametrize cases removed from `test_constitution_and_secret_paths_denied` = 874. The two probe
swaps (steps 3 and 7) deliberately do not change the count. If you land on 873, you deleted a
parametrize case that should have been re-subjected.

Commit with the message before the pathspec, listing only these seven paths. Nothing is deleted from
the tree, so no `git rm` is involved.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 874 passed, 7 snapshots</automated>
    <automated>uv run python -m tools.harness_emit && git status --porcelain   # expect: only the seven edited source files, NO .opencode/ or .claude/ change</automated>
    <automated>git grep -n "golden/\*\*" -- tools harness .github | grep -v '^tools/adoption_scan/destinations.py'   # expect: no output</automated>
  </verify>
  <done>`uv run pytest -q` reports 874 passed / 7 snapshots; `harness_emit` leaves the emitted trees byte-identical; no `golden/**` survives in `tools/`, `harness/` or `.github/` outside `destinations.py` (which Task 2 removes); the refusal string contains `CODEOWNERS` and not `golden-approve`; one commit created.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the two dead CODEOWNERS routes and the dead adoption-catalog glob</name>
  <files>.github/CODEOWNERS, tools/adoption_scan/destinations.py</files>
  <action>
Implements D-02, D-21 and ROADMAP SC-1's CODEOWNERS clause. Kept as its own commit so the
security-relevant declaration change (Task 1) is isolated in history.

In `.github/CODEOWNERS`: delete the route `/golden/ @hjung3113` at `:30` (root `golden/` was deleted
in Phase 44) and the route `/approvals/ @hjung3113` at `:32` (`approvals/` has never existed in this
tree and the route sits inside the `# ── Constitution plane (core) ──` block, so it claims a plane it
cannot protect). Then repair the two prose copies of the declaration in the same file: `:6-7` says
"The plane's four members are declared by docs/adr/0001-walking-skeleton-golden-core.md:48; this file
must not drift from that list" — make it three members and cite ADR-0012 clause (d) as the
supersession; `:27` says "The fourth member is a single FILE, not a tree" — it is now the third.
Leave the ENFORCEMENT CAVEAT block at `:9-14`, the solo-repo nuance at `:16-21` and the instance
routes at `:34-36` exactly as they are: `/examples/*/contracts/` and `/examples/*/golden/` are the
live merge-time control for the relocated baselines (D-03) and must not be touched.

In `tools/adoption_scan/destinations.py`: delete the `"golden/**/*"` entry from `_CATEGORY_GLOBS`
(around `:144`). Under that module's own matcher (`pathlib.Path.glob`, where `**` is recursive) it
resolves to zero tracked files, so it can never contribute a destination. Measured: removing it
alone leaves the suite at 880/0 failures, i.e. it costs no test. Do NOT touch any other
`_CATEGORY_GLOBS` row and do NOT touch `scan.py`'s `SECRET_PATH_GLOBS` — that declaration's subject
is a scanned brownfield TARGET repo, not this checkout, so matching zero paths here is correct
behavior and not a dead control.

Commit these two files with the message before the pathspec.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 874 passed, 7 snapshots</automated>
    <automated>grep -n '^/' .github/CODEOWNERS   # expect 4 routes: /contracts/ /docs/adr/ /docs/glossary.md /examples/*/... and NO /golden/ or /approvals/</automated>
    <automated>grep -c 'golden' tools/adoption_scan/destinations.py   # expect 0</automated>
  </verify>
  <done>CODEOWNERS routes only paths that exist; `_CATEGORY_GLOBS` carries no zero-match entry; suite still 874 passed; second commit created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent tool-call → repo write | `contract_guard` PreToolUse deny is the in-session control on constitution-plane writes |
| PR → main | `.github/CODEOWNERS` + the required `gate` status check are the merge-time control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | agent self-blesses a golden baseline | accept | ADR-0012 clause (d) ratified moving this control from in-session to the merge. Residual = CODEOWNERS `/examples/*/golden/`. ⚠ Measured advisory-only (D-24) — recorded for the owner in plan 06, NOT compensated in-session (that would be the surface growth SC-8 forbids). |
| T-45-02 | Info Disclosure | agent writes a secret into a `.env` | accept | The `*.env` deny rows removed here had no enforcer since `secret_scan` was deleted in Phase 44 — the control was already fictional, so removal is no new exposure. ADR-0012 records secret detection at the tool boundary as a permanent residual caught at CI/PR review. Re-adding an enforcer is forbidden. |
| T-45-03 | Tampering | constitution write carrying BOM/CRLF slips through | mitigate | `test_approved_constitution_with_crlf_still_denied` is RE-SUBJECTED to `contracts/x.schema.json`, not deleted — it is the byte-hygiene path's only test. |
| T-45-04 | Elevation of Privilege | the guard's deny is silently narrowed further by a later edit | mitigate | `test_every_declared_plane_member_is_independently_enforced` keeps probing each surviving member individually; `test_agents.py` keeps the matrix and the guard in sync. |
| T-45-SC | Tampering | npm/pip/cargo install | accept | This plan installs no packages and adds no dependency; `uv.lock` is untouched. |
</threat_model>

<verification>
- `uv run pytest -q` → 874 passed, 7 snapshots, after EACH of the two commits (not just at the end).
- `uv run python -m tools.harness_emit` then `git status --porcelain` → no `.opencode/`, `.claude/` or
  `opencode.json` change. Tier 1 is measured not to reach the emitted trees because
  `tools/harness_emit/permissions.py:23` strips `_note` and `path_deny_globs` before emit.
- `uv run pytest examples/log-parser -q` → 31 passed (this plan does not touch the instance, but the
  run is cheap and confirms the guard change did not reach it).
- `uv run python -m tools.ruff_baseline` → PASS. Do NOT run `--update` here; that belongs to plan 06
  after every prose edit in the phase has landed, because E501 counts move with line lengths.
</verification>

<success_criteria>
- The constitution plane reads `contracts/**`, `docs/adr/**`, `docs/glossary.md` in all eleven copies
  reachable from this plan's nine files.
- `path_deny_globs` has three entries and no row without a live enforcer.
- No surviving test asserts a deny that nothing performs.
- No CODEOWNERS route and no `_CATEGORY_GLOBS` entry matches zero tracked paths.
- Two commits, each ending at 874 passed / 7 snapshots.
</success_criteria>

<output>
Create `.planning/phases/45-projection-repair/45-01-SUMMARY.md` when done. Record the measured test
count at each commit and confirm the emitted trees were untouched.
</output>
