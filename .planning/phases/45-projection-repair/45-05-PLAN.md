---
phase: 45-projection-repair
plan: 05
type: execute
wave: 4
depends_on: ["45-04"]
files_modified:
  - docs/how-to/task-lifecycle.md
  - docs/explanation/task-lifecycle-shadow-metrics.md
  - docs/how-to/README.md
  - docs/explanation/next-milestone-task-control-plane.md
  - docs/explanation/template-and-instances.md
  - .memory/README.md
  - .gitignore
  - docs/glossary.md
autonomous: false
requirements: [CER-11]
user_setup: []

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-05 (append-only ADR text and deliberate history notes survive; the vendored docs/references bundle is not this repo's self-description and is excluded from every sweep), D-06 (a live file describing a control, path or command that no longer exists is the test applied to every candidate), D-10 (the docs/ surface, corrected against measurement: approve-a-golden.md is ALREADY deleted and the live defect is its inbound link), D-14 (the ADR-0008 and ADR-0003 dangling-citation gap is recorded, not unilaterally repaired — a superseding ADR is human-gated)."
    - "The two whole-file corpses are deleted and no surviving file links to them."
    - "docs/explanation/next-milestone-task-control-plane.md is NOT deleted: docs/adr/0008:50 cites it, ADRs are append-only and constitution-plane, so deleting it would create a permanent dangling reference FROM the constitution plane. It carries a one-line historical header instead."
    - "docs/how-to/README.md stops claiming it is constitution plane — a live false plane claim that test_resolver.py:79 directly contradicts."
    - "docs/explanation/agent-workflow-skillset-design-guide.md is deliberately unchanged: its /intake and handoff tokens name EXTERNAL skill ecosystems it surveys, never this repo's deleted surface."
    - "docs/glossary.md is constitution plane and is ratified by a human rather than self-edited by an agent."
  artifacts:
    - path: "docs/how-to/README.md"
      provides: "the how-to index"
    - path: "docs/explanation/next-milestone-task-control-plane.md"
      provides: "the design record ADR-0008 cites as its design authority"
      contains: "historical"
  key_links:
    - from: "docs/adr/0008-task-control-plane-lifecycle.md:50"
      to: "docs/explanation/next-milestone-task-control-plane.md"
      via: "Design authority citation from an accepted, append-only ADR"
      pattern: "next-milestone-task-control-plane"
---

<objective>
Delete the two `docs/` files whose entire subject is a plane Phase 43 removed, repair every inbound
link to them, correct the derived-plane and root-config prose that still names root `golden/` and
`/golden-approve`, and route the one constitution-plane document through a human.

Purpose: `docs/` sits outside every sweep the harness runs and `tools/docs_guard` was deleted in
Phase 41 on purpose, so nothing gates any of this — which is precisely why it must be swept by
meaning, once, with the false-positive sources named up front.

Output: two autonomous commits plus one human-gated decision. Suite unchanged at 876.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-projection-repair/45-CONTEXT.md
@.planning/phases/45-projection-repair/45-RESEARCH.md
@.planning/phases/45-projection-repair/45-04-SUMMARY.md
@docs/adr/0012-ci-and-merge-as-decision-authority.md

<interfaces>
<!-- Sweep boundaries. Getting these wrong is the phase's largest measured failure mode. -->

EXCLUDE from every docs/ sweep:
  docs/references/opencode-matt-workflows/   79 tracked files, a VENDORED third-party bundle.
    29 of them match `handoff` / `discipline`. It is not this repo's account of itself.
    Every sweep command must carry ':!docs/references/'. A hit count above ~10 for a single
    token means the exclusion was forgotten.
  docs/adr/**   append-only, constitution plane, and gated by contract_guard. Never edited here.

Constitution-plane resolution (tools/harness_perms/resolver.py via contract_guard):
  docs/glossary.md                     -> deny   (in-session Write/Edit REFUSED)
  docs/how-to/task-lifecycle.md        -> allow  (asserted at tools/harness_perms/tests/test_resolver.py:79)
  docs/how-to/**, docs/explanation/**  -> allow
  Glob resolution never touches disk, so deleting a file used as a negative-control STRING in a
  fixture does not red the test. Affected fixtures: test_resolver.py:79,
  tools/hooks/tests/test_contract_guard.py:326. Cosmetic only — flag, do not change.

Inbound links to docs/how-to/task-lifecycle.md (all three must be handled):
  docs/how-to/README.md:14
  README.ko.md:81                      -- already cleared by plan 04 (verify it)
  docs/explanation/next-milestone-task-control-plane.md:330
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the two doc corpses and repair every inbound link in the same commit</name>
  <files>docs/how-to/task-lifecycle.md, docs/explanation/task-lifecycle-shadow-metrics.md, docs/how-to/README.md, docs/explanation/next-milestone-task-control-plane.md</files>
  <action>
Implements D-10 and D-06, corrected against measurement.

`git rm docs/how-to/task-lifecycle.md` — 106 lines, 8 command blocks invoking
`tools.risk_router`, `tools.task_control`, `tools.evidence` and `tools.handoff`, all deleted in
Phase 43. Every executable instruction in it is a lie.

`git rm docs/explanation/task-lifecycle-shadow-metrics.md` — 13 lines of metrics for that same
deleted plane, with zero inbound links.

Do NOT `git rm docs/how-to/approve-a-golden.md`. It does not exist — it was already removed in an
earlier phase. CONTEXT D-10 and ROADMAP both list it as a whole file to delete; both are stale.
`ls` it first and record the no-op.

`docs/how-to/README.md` (16 lines) — three defects:
  - `:3` claims "*Diátaxis quadrant: **task-oriented**. Hand-authored (constitution plane).*" This is
    a live FALSE plane claim: `docs/how-to/**` is not in `CONSTITUTION_GLOBS`, and
    `tools/harness_perms/tests/test_resolver.py:79` explicitly asserts
    `docs/how-to/task-lifecycle.md` resolves to `allow`. Say hand-authored, agent-writable, NOT
    constitution plane.
  - `:11` — the `approve-a-golden.md` bullet links a file that no longer exists and describes the
    retired `/golden-approve` flow. Delete the bullet.
  - `:14` — the `task-lifecycle.md` bullet. Delete it, same commit as the `git rm`.

`docs/explanation/next-milestone-task-control-plane.md` (504 lines) — DO NOT DELETE, and state this
choice explicitly in the summary. `docs/adr/0008-task-control-plane-lifecycle.md:50` names it as
"Design authority", and ADR-0008 is accepted, append-only and constitution-plane, so the citation
cannot be repaired without a superseding ADR — which D-14 defers as human-gated. Deleting the file
would convert a stale-but-resolvable pointer into a permanent dangling reference FROM the
constitution plane, which is strictly worse than the residue it removes. Instead:
  - Add a one-line historical header immediately under the title: this document designs the
    task-control plane, which Phase 43 deleted; it is retained because ADR-0008 cites it as design
    authority and ADRs are append-only; see ADR-0012. That header is the "explicit history note" SC-5
    allows, so the file's remaining `handoff` / `task_control` / `risk_router` tokens are then
    covered by design and must NOT be stripped.
  - `:330` links `docs/how-to/task-lifecycle.md`, which this commit deletes. Repair that one link
    (drop it, or point it at this file's own history header) so the deletion creates no new dangler.

Before committing, confirm plan 04 already cleared `README.ko.md:81`. If it did not, STOP and report
rather than editing `README.ko.md` here — that file belongs to plan 04 and a cross-plan edit would
break the ownership that keeps these plans parallel-safe.

`git rm` already stages; add only the two MODIFIED files. Never `git add -A`.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>git grep -n 'task-lifecycle\.md\|task-lifecycle-shadow-metrics\|approve-a-golden' -- . ':!.planning/' ':!docs/references/'   # expect: no output</automated>
    <automated>git grep -rn 'tools\.risk_router\|tools\.task_control\|tools\.evidence\|tools\.handoff' -- docs ':!docs/references/' ':!docs/adr/' ':!docs/explanation/next-milestone-task-control-plane.md'   # expect: no output</automated>
  </verify>
  <done>Both corpses are deleted; no file outside `.planning/` and `docs/references/` links to either; `docs/how-to/README.md` no longer claims constitution plane; `next-milestone-task-control-plane.md` survives with a history header and a repaired outbound link; suite at 876.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the derived-plane, gitignore and template-split prose</name>
  <files>.memory/README.md, .gitignore, docs/explanation/template-and-instances.md</files>
  <action>
Implements D-06 and D-02's reach into the two-plane declaration. None of these three files is named
by CONTEXT or the ROADMAP; all three were found by measurement.

`.memory/README.md` — the two-plane declaration AGENTS.md cites as a convention reference:
  - `:13` — the CONSTITUTION row lists "`contracts/`, `docs/adr/`, `docs/glossary.md`, `golden/`".
    Three members.
  - `:25` — "`golden/` — approved equivalence baselines (`.verified`), promoted only by human
    `/golden-approve`." Both the root directory and the command are gone. Re-scope to
    `examples/<instance>/golden/` and restate promotion as the human `GOLDEN_APPROVE_HUMAN` token
    ratified through the CODEOWNERS `/examples/*/golden/` route, or delete the bullet.
  - `:29` cites ADR-0001 for the membership — add the ADR-0012 clause (d) supersession pointer.
  - Leave `:28` ("Contract changes are ratified by humans and accompanied by the golden /
    contract-drift gates") alone: both gates are live.

`.gitignore:14` — the comment "# Golden machine-proposed baselines (transient; promoted to .verified
only by /golden-approve, P9)." names a retired command. Restate as human promotion. Do NOT touch
`:17` (`**/golden/**/baseline.received.tsv`) — the unanchored pattern still matches the instance path
and is correct; `:15-16` already explains why it is unanchored.

`docs/explanation/template-and-instances.md:34` — says the language-neutral Python normalize core is
"imported by core tools (`polyglot_lint`, `golden_runner`)". `golden_runner` was relocated OUT of the
core tree into the instance overlay in Phase 44, so naming it as a core tool inverts the very
template/instance split this document exists to explain. Correct it — the instance-side runner
importing the core is the right direction to state, if you state one at all.

RECORD AS DELIBERATE NO-CHANGE, in the summary, with the reason:
  - `docs/explanation/agent-workflow-skillset-design-guide.md` (791 lines). CONTEXT D-10 lists it as
    carrying Phase-43 plane prose. Measurement does not support that: its `handoff`, `/intake`,
    `/grill`, `/plan`, `/ship` tokens all name EXTERNAL skill ecosystems the document surveys and
    compares (mattpocock/skills, GSD, OpenSpec, wshobson/agents) plus a recommended-V1 design. None of
    them was ever this repo's shipped surface, so by D-06 none of them is stale. Editing it would be
    sweeping by token, which D-05 forbids.
  - `docs/adr/README.md`. CONTEXT D-10 lists it too. Measured accurate: the index correctly shows 0001
    and 0010 as superseded by 0012. Its only defect is ADR-0008 showing `accepted`, which IS D-14 and
    is human-gated. It is also constitution plane. No change.
  - `CLAUDE.md:42,48,73` name `/golden-approve` but sit inside a GSD-managed block regenerated from
    `PROJECT.md`; an edit here can be silently reverted by a GSD regeneration. Leave and record.
  - `docs/references/opencode-matt-workflows/**` — vendored, 79 files, excluded by design.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>git grep -n 'golden-approve' -- . ':!.planning/' ':!docs/adr/' ':!docs/references/' ':!examples/' ':!CLAUDE.md'   # expect: no output</automated>
    <automated>git grep -n 'golden_runner' -- docs .memory ':!docs/references/' ':!docs/adr/'   # expect: no output</automated>
  </verify>
  <done>The two-plane declaration in `.memory/README.md` reads three members; no live file outside ADR text, `examples/`, `.planning/` and the GSD-managed `CLAUDE.md` block names `/golden-approve`; `template-and-instances.md` no longer calls `golden_runner` a core tool; the four no-change verdicts are recorded with reasons.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Ratify docs/glossary.md — the one constitution-plane target in this phase</name>
  <files>docs/glossary.md</files>
  <decision>How to repair `docs/glossary.md`, which is itself constitution plane</decision>
  <context>
`docs/glossary.md` carries three stale claims:
  `:19` — the contract-drift gate description ends "...Promotion is the `/golden-approve` step."
  `:20` — the `.received`/`.verified` row names `/golden-approve` as the promotion step.
  `:23` — the **Constitution plane** row declares the membership as "`contracts/`, `golden/`,
          `docs/adr/`, `docs/glossary.md`" — the eleventh copy of the declaration this phase collapses.

It is the ONE Phase-45 target that is itself on the constitution plane:
`resolve_path(CONSTITUTION_GLOBS, "docs/glossary.md")` returns `deny`, so an in-session Write or Edit
is REFUSED by `contract_guard` unless a human sets `GOLDEN_APPROVE_HUMAN`, and `.github/CODEOWNERS`
gates it again at the merge. The repo's own stated rule is that a human ratifies this plane; an agent
quietly bypassing the deny to tidy prose would be the exact failure mode this milestone exists to
prevent.

ROADMAP SC-5 names `docs/` explicitly, so leaving it silently unrepaired fails a stated criterion.
Either outcome is defensible; what is not defensible is drifting into one without recording it.
  </context>
  <options>
    <option id="ratify-now">
      <name>Human sets GOLDEN_APPROVE_HUMAN and the executor applies the three-line edit now</name>
      <pros>SC-5 closes inside the phase; the last copy of the four-member declaration dies with the other ten; one fewer item on the milestone-close PR.</pros>
      <cons>Requires a human in the loop mid-phase; the plane's ratification is granted for a prose tidy rather than a decision.</cons>
    </option>
    <option id="defer-to-pr">
      <name>Defer to the milestone-close PR, bundled with D-14's superseding ADR</name>
      <pros>Coherent — a human ratifies the constitution plane at the PR, which is ADR-0012's whole thesis; it lands alongside the ADR-0008/ADR-0003 gap that is already human-gated.</pros>
      <cons>SC-5 stays open at phase close and must be recorded as a named, accepted deferral rather than a pass.</cons>
    </option>
  </options>
  <resume-signal>Select: ratify-now (and export GOLDEN_APPROVE_HUMAN before resuming) or defer-to-pr</resume-signal>
  <action>
If `ratify-now`: with the token set, edit `docs/glossary.md` at `:19`, `:20` and `:23` only —
membership to three members with an ADR-0012 clause (d) pointer, and `/golden-approve` restated as
human promotion via `GOLDEN_APPROVE_HUMAN` ratified through CODEOWNERS. Do not touch `:13`, `:21` or
any other row; "no agent self-blesses a golden" is still true. Commit `docs/glossary.md` alone so the
gated write is a single reviewable act. Then re-run the suite (876) and confirm
`uv run python -m tools.harness_emit` leaves the tree clean.

If `defer-to-pr`: change nothing. Record in the summary that SC-5 has one named open item, quote the
three lines verbatim so the human PR can apply them without re-deriving, and cross-reference it to
D-14's ADR-0008 gap.

Do NOT use `HARNESS_DEV_BYPASS` to route around the deny. Bypassing the gate to edit the plane the
gate protects would falsify this phase's own thesis.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots (either branch)</automated>
    <automated>git status --porcelain   # expect: clean under either branch — committed or untouched, never dirty</automated>
  </verify>
  <done>Either `docs/glossary.md` reads three members with no retired command and is committed as its own human-ratified change, or the deferral is recorded verbatim with the three replacement lines ready for the milestone-close PR. In both cases the suite is 876 and the tree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → constitution plane | `contract_guard` denies `docs/glossary.md` writes without a human token; CODEOWNERS re-gates at the merge |
| this repo's docs → vendored third-party docs | `docs/references/**` is not this repo's self-description and must not be swept |
| accepted ADR → the documents it cites | ADRs are append-only, so a deletion downstream of a citation is unrepairable in-phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-15 | Elevation of Privilege | agent bypasses the constitution deny (`HARNESS_DEV_BYPASS` or a fabricated token) to edit `docs/glossary.md` | mitigate | Explicitly forbidden in task 3; the only sanctioned path is a human-set `GOLDEN_APPROVE_HUMAN`, and the alternative is a recorded deferral to the PR. |
| T-45-16 | Repudiation | deleting a doc an accepted ADR cites creates a permanent dangling constitution-plane reference | mitigate | `next-milestone-task-control-plane.md` is retained with a history header; the ADR-0008 and ADR-0003 gaps are recorded for a human-authored superseding ADR (D-14). |
| T-45-17 | Tampering | a token sweep strips the vendored bundle or append-only ADR text to force a clean grep | mitigate | Both exclusions are named in the interfaces block and every verify command carries `':!docs/references/'` and `':!docs/adr/'`. |
| T-45-SC | Tampering | npm/pip/cargo install | accept | No package-manager install; `uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest -q` → 876 passed, 7 snapshots after each commit (the doc deletions are measured not
  to red any test: glob resolution never touches disk, so the two negative-control fixtures that name
  `docs/how-to/task-lifecycle.md` stay green).
- `git grep` sweeps for `/golden-approve`, `golden_runner`, `task-control`, `task-lifecycle`, always
  with `':!docs/references/'` and `':!docs/adr/'`.
- `uv run python -m tools.harness_emit && git status --porcelain` → empty; nothing here is emitted.
</verification>

<success_criteria>
- ROADMAP SC-5 holds for every file under `docs/` except append-only ADR text, the explicit history
  note on `next-milestone-task-control-plane.md`, the vendored `docs/references/` bundle, and — if
  the checkpoint resolves to `defer-to-pr` — the one named, recorded `docs/glossary.md` deferral.
- No file links to a deleted document.
- No accepted ADR gains a dangling reference as a result of this phase.
- Suite at 876 passed / 7 snapshots.
</success_criteria>

<output>
Create `.planning/phases/45-projection-repair/45-05-SUMMARY.md` when done. Record the four deliberate
no-change verdicts, the `next-milestone-task-control-plane.md` retention decision, and the glossary
checkpoint outcome.
</output>
