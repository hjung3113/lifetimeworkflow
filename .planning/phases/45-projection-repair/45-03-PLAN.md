---
phase: 45-projection-repair
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - harness/agents/code-reviewer.md
  - harness/agents/curator.md
  - harness/agents/python-engineer.md
  - harness/commands/adopt.md
  - harness/commands/refresh-memory.md
  - harness/skills/brownfield-adoption/SKILL.md
  - harness/skills/data-contracts/SKILL.md
  - harness/skills/python-conventions/SKILL.md
  - harness/skills/two-plane-memory/SKILL.md
  - .claude/agents/code-reviewer.md
  - .claude/agents/curator.md
  - .claude/agents/python-engineer.md
  - .claude/commands/adopt.md
  - .claude/commands/refresh-memory.md
  - .claude/skills/brownfield-adoption/SKILL.md
  - .claude/skills/data-contracts/SKILL.md
  - .claude/skills/python-conventions/SKILL.md
  - .claude/skills/two-plane-memory/SKILL.md
  - .opencode/agent/code-reviewer.md
  - .opencode/agent/curator.md
  - .opencode/agent/python-engineer.md
  - .opencode/command/adopt.md
  - .opencode/command/refresh-memory.md
  - .opencode/skill/brownfield-adoption/SKILL.md
  - .opencode/skill/data-contracts/SKILL.md
  - .opencode/skill/python-conventions/SKILL.md
  - .opencode/skill/two-plane-memory/SKILL.md
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
autonomous: true
requirements: [CER-10, CER-11]
user_setup: []

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-02 (the three-member plane list reaches every copy, including the nine that project into both runtime trees), D-05 (sweep by meaning — golden-as-a-concept survives where the gate still exists; only membership and CODEOWNERS-route claims are corrected), D-06 (a live file describing a control, path or command that no longer exists is what gets fixed), D-18 (source-first: edit harness/**, then run tools.harness_emit; never hand-edit .opencode/ or .claude/), D-19 (the emitter and the suite are run, not reasoned about)."
    - "Every emitted agent, command and skill that states the constitution plane's membership states three members — including harness/skills/python-conventions/SKILL.md:44, which the first draft of this plan missed and which is emitted into both runtime trees and the .ambr."
    - "No emitted artifact points a reader at the deleted CODEOWNERS route /golden/; the instance-scoped route is named in words, without an examples/ token, because harness/ is under the GEN-04 guard's _CORE_ROOTS."
    - "The membership sweep is verified mechanically, not by eye: no backticked `golden/` or `golden/**` token survives in harness/agents, harness/commands or harness/skills outside templates/."
    - "The emitted trees are byte-consistent with harness/ — a second harness_emit run produces an empty diff and emit-drift is green."
    - "The syrupy projection snapshot is regenerated in the same commit as the source edit, so no commit ends red."
  artifacts:
    - path: "harness/skills/two-plane-memory/SKILL.md"
      provides: "the plane declaration a deployed agent reads first"
      contains: "contracts/**"
    - path: "harness/skills/python-conventions/SKILL.md"
      provides: "the non-negotiables a Python-writing agent reads before it edits"
      contains: "constitution plane"
    - path: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      provides: "the pinned projection of the emitted trees"
  key_links:
    - from: "harness/**"
      to: ".claude/** and .opencode/**"
      via: "python -m tools.harness_emit"
      pattern: "harness_emit"
    - from: "tools/harness_emit/tests/test_emit_determinism.py"
      to: "the .ambr snapshot"
      via: "syrupy test_projected_tree_matches_committed_snapshot"
      pattern: "snapshot"
---

<objective>
Correct the nine `harness/` source artifacts that state the constitution plane's membership or point
at the deleted `/golden/` CODEOWNERS route, then re-project both runtime trees and regenerate the
syrupy snapshot in the same commit.

Purpose: these are what a deployed agent actually reads. Leaving them at four members would make the
shipped harness contradict its own enforced guard. This is the ONLY commit in Phase 45 that touches
the emitted trees (measured), so it is isolated deliberately.

Output: one commit — 9 sources + their 18 emitted twins + 1 `.ambr` = 28 files — ending green at 876.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-projection-repair/45-CONTEXT.md
@.planning/phases/45-projection-repair/45-RESEARCH.md
@.planning/phases/45-projection-repair/45-01-SUMMARY.md
@AGENTS.md

<gen04_phrasing_rule>
⚠ READ THIS BEFORE WRITING ANY NEW SENTENCE INTO A FILE UNDER `harness/`.

`tools/harness_lint/tests/test_core_no_example_dep.py` scans EVERY git-tracked file under
`_CORE_ROOTS = ("tools", "harness", "libs")` (`:44`) for the `_PATH_TOKENS` entry `"examples/"`
(`:53`) and fails `test_core_has_no_example_dependency` on any hit. The ONLY exemption is
`harness/project.toml`'s `root =` / `persona =` / `test_paths =` lines (`:81-88`).
`tools/contract_graph/tests/test_cross_repo_authority.py::test_gen04_core_no_instance_dep_guard_stays_green`
reds alongside it — measured RED on this commit when `harness/agents/python-engineer.md:29` was
repointed at `/examples/*/golden/` as the first draft instructed.

**RULE:** when a `harness/` file must describe the instance-scoped golden route or the instance's
baselines, phrase it **without the literal token `examples/`**. Use wording such as *"the
instance-scoped golden CODEOWNERS route"*. Never write `/examples/*/golden/` or
`examples/<instance>/golden/` into a file under `harness/`. Reuse the exact wording plan 01 recorded
in `45-01-SUMMARY.md` so the phase's phrasing is consistent.

This applies to BOTH sites in this plan that name the route or the baselines' location:
`harness/agents/python-engineer.md:29` and `harness/skills/data-contracts/SKILL.md:34`.
</gen04_phrasing_rule>

<interfaces>
<!-- The emit contract. Do not hand-edit either runtime tree. -->

python -m tools.harness_emit
  harness/agents/<n>.md   -> .claude/agents/<n>.md      + .opencode/agent/<n>.md
  harness/commands/<n>.md -> .claude/commands/<n>.md    + .opencode/command/<n>.md
  harness/skills/<n>/SKILL.md -> .claude/skills/<n>/SKILL.md + .opencode/skill/<n>/SKILL.md
  harness/agents/templates/** is NOT in emit-manifest.json and is NOT emitted (plan 06 owns it).
  ⚠ harness/skills/python-conventions/SKILL.md IS in emit-manifest.json (`:32`, `:68`) — it emits to
    both trees like every other skill. It is the ninth source, not an exception.

tools/harness_lint/caps.py
  Hard-fails the emitter BEFORE it writes if a skill/agent/command count or name declaration moves.
  This plan changes body prose only — no name, no description, no count — so caps must stay silent.

tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot
  syrupy. Reds on any emitted-tree change until the .ambr is regenerated with --snapshot-update.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the nine emitted-source artifacts (source edits only, do not emit yet)</name>
  <files>harness/agents/code-reviewer.md, harness/agents/curator.md, harness/agents/python-engineer.md, harness/commands/adopt.md, harness/commands/refresh-memory.md, harness/skills/brownfield-adoption/SKILL.md, harness/skills/data-contracts/SKILL.md, harness/skills/python-conventions/SKILL.md, harness/skills/two-plane-memory/SKILL.md</files>
  <action>
Implements D-02 and D-06 across the emitted surface. Edit only `harness/**`; the runtime trees are
projected in task 2.

Membership claims — drop `golden/` from the parenthesised plane list, leaving `contracts/`,
`docs/adr/` and `docs/glossary.md`:
  - `harness/agents/code-reviewer.md:32-33` — "write the constitution plane (`contracts/`, `docs/adr/`, `golden/`)"
  - `harness/agents/curator.md:39-40` — same list inside the out-of-bounds bullet
  - `harness/commands/adopt.md:40` — "refuses any constitution-plane (`contracts/` · `docs/adr/` · `golden/`) destination". ⚠ This is a claim about `adoption_apply`'s ACTUAL behavior, which is driven by the imported `CONSTITUTION_GLOBS`; after plan 01 it no longer refuses `golden/`, so leaving it would be a false behavioral claim, not just stale prose.
  - `harness/commands/refresh-memory.md:41` — "never writes the constitution plane (`contracts/`, `docs/adr/`, `golden/`)"
  - `harness/skills/brownfield-adoption/SKILL.md:52` — same behavioral claim as adopt.md
  - `harness/skills/python-conventions/SKILL.md:44` — "Contract-first; never write the constitution plane (`contracts/`, `docs/adr/`, `golden/`) from Python". ⚠ NINTH SOURCE, added on review: this is a membership claim identical in form to the six above, it is emitted into `.claude/skills/`, `.opencode/skill/` and the `.ambr`, and it sits in the Non-negotiables block a Python-writing agent reads before every edit. Left alone it would survive the whole phase and teach a deployed agent a four-member plane the guard no longer enforces. Do NOT also edit the `libs/python/AGENTS.md` pointer in the following line — that file is out of this phase's scope and says nothing about membership.
  - `harness/skills/two-plane-memory/SKILL.md:17` — "**What:** `contracts/**`, `docs/adr/**`, `golden/**`, plus the language-neutral spec `libs/normalize-spec.md`"

CODEOWNERS-route pointer — `harness/agents/python-engineer.md:29` says "promotion is human review at
the PR (the `golden/` CODEOWNERS entry)". Plan 01 deleted that route. Repoint it to the
instance-scoped golden CODEOWNERS route, ⚠ described in words per the GEN-04 PHRASING RULE — no
`examples/` token.

Relocation claim — `harness/skills/data-contracts/SKILL.md:34` says "`golden/` is a **separate
top-level sibling**, not `contracts/golden/`". Root `golden/` was deleted in Phase 44 and the
baselines live in the instance overlay. Rewrite so the sibling relationship is stated instance-scoped;
keep the point it is actually making (goldens are never nested under `contracts/`). Two phrasing
constraints on this one line: (a) GEN-04 — no `examples/` token; (b) do not re-emit the literal
backticked token `` `golden/` `` — write it as e.g. "the golden baseline directory" — because the
mechanical verify below greps for exactly that token as the membership/route signature.

DO NOT TOUCH, and say so in the summary — these name the golden concept or the golden GATE, both of
which still exist in the instance, so by D-06 they are not stale:
  `harness/agents/code-reviewer.md:32` ("self-bless a golden"), `harness/agents/curator.md:6`,
  `harness/agents/orchestrator.md:69`, `harness/agents/python-engineer.md:6`,
  `harness/commands/build.md:3`, `harness/commands/contract-check.md:18,33,42`,
  `harness/commands/orient.md:32,36`, `harness/commands/review.md:32`, `harness/commands/test.md:5`,
  `harness/skills/data-contracts/SKILL.md:7,47`, `harness/skills/two-plane-memory/SKILL.md:22,59`,
  and the whole of `harness/skills/golden-testing/` and `harness/skills/golden-debug/`.
None of those carries the backticked `` `golden/` `` token, which is why the mechanical verify below
can be exact instead of eye-graded.
Also do not touch `harness/agents/templates/**` — not emitted, and plan 06 owns it because its
dependent test lives in the instance suite.

Change body prose ONLY. Do not touch any front-matter `name`, `description`, `mode` or `tools` field:
`tools/harness_lint/caps.py` hard-fails the emitter before it writes if a count or name declaration
moves, and a `description` rewrite silently changes agent routing with no gate to catch it (an
accepted consequence recorded in Phase 40).

Do NOT run the emitter and do NOT commit in this task.
  </action>
  <verify>
    <automated>git status --porcelain | grep -c '^ M harness/'   # expect 9</automated>
    <automated>git status --porcelain | grep -E '^ M \.(claude|opencode)/' | wc -l   # expect 0 — nothing emitted yet</automated>
    <automated>git grep -n '`golden/`\|`golden/\*\*`' -- harness/agents harness/commands harness/skills ':!harness/agents/templates'   # expect: NO OUTPUT. Mechanical, not eye-graded: those two backticked tokens are the exact signature of a membership list or a route pointer, and every DO-NOT-TOUCH survivor above uses `golden` bare or `golden/approval`, never the backticked path token.</automated>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/contract_graph/tests/test_cross_repo_authority.py -q 2>&1 | tail -3   # expect: all passed — no `examples/` token leaked into harness/</automated>
    <automated>git grep -l 'golden' -- harness/skills/golden-testing harness/commands/contract-check.md   # expect: non-empty — the concept survives; the sweep was by meaning, not by token</automated>
  </verify>
  <done>Exactly nine `harness/` files are modified, no runtime-tree file is modified, no front-matter field changed, the backticked-token grep returns nothing, GEN-04 is green, and every surviving `golden` mention in those files refers to the concept or the still-live instance gate rather than to plane membership or a deleted route.</done>
</task>

<task type="auto">
  <name>Task 2: Project both runtime trees, regenerate the snapshot, and commit as one</name>
  <files>.claude/agents/code-reviewer.md, .claude/agents/curator.md, .claude/agents/python-engineer.md, .claude/commands/adopt.md, .claude/commands/refresh-memory.md, .claude/skills/brownfield-adoption/SKILL.md, .claude/skills/data-contracts/SKILL.md, .claude/skills/python-conventions/SKILL.md, .claude/skills/two-plane-memory/SKILL.md, .opencode/agent/code-reviewer.md, .opencode/agent/curator.md, .opencode/agent/python-engineer.md, .opencode/command/adopt.md, .opencode/command/refresh-memory.md, .opencode/skill/brownfield-adoption/SKILL.md, .opencode/skill/data-contracts/SKILL.md, .opencode/skill/python-conventions/SKILL.md, .opencode/skill/two-plane-memory/SKILL.md, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>
  <action>
Implements D-18 and D-16. Run `uv run python -m tools.harness_emit`. Then inspect
`git status --porcelain` and confirm the ONLY modified paths are the nine sources from task 1, their
eighteen emitted twins, and nothing else. If any other emitted file moved, a front-matter field was
touched in task 1 — revert and redo rather than committing the collateral.

Then run the suite. Exactly one failure is expected and it is measured:
`tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot`
(runtime, syrupy). Close it with
`uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q`, which reports
1 snapshot updated and touches `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`.

Re-run the full suite. Then run `uv run python -m tools.harness_emit` a SECOND time and confirm
`git status --porcelain` gains nothing — that is the emit-drift/idempotence proof CER-10 asks for.

Commit source + emitted + snapshot together — 28 paths — message before the pathspec. Splitting them
would leave an expected-red window, which this phase forbids.

Because CER-10 is measured already satisfied at HEAD, also run the other three derived-plane
generators here as a verification (not as work): `uv run python -m tools.contract_hash.hash`,
`uv run python -m tools.docs_sync.generate`, `uv run python -m tools.memory_regen.contracts_index`.
Each must leave `git status --porcelain` empty. If any of them dirties the tree, STOP — that is an
unbudgeted CER-10 finding and belongs in the summary before you commit it.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>uv run python -m tools.harness_emit && git status --porcelain   # expect: empty (idempotent re-emit after commit)</automated>
    <automated>uv run python -m tools.contract_hash.hash && uv run python -m tools.docs_sync.generate && uv run python -m tools.memory_regen.contracts_index && git status --porcelain   # expect: empty</automated>
    <automated>git grep -n '`golden/`\|`golden/\*\*`' -- .claude .opencode   # expect: no output. Measured: at HEAD this returns exactly the 18 emitted twins of the 9 sources and nothing else, so no exclusion is needed and the check cannot be satisfied by a partial projection.</automated>
    <automated>git show --stat --name-only HEAD | grep -cE '^(harness|\.claude|\.opencode|tools)/'   # expect 28 — 9 sources + 18 twins + 1 .ambr</automated>
    <automated>uv run pytest examples/log-parser -q 2>&1 | tail -3   # expect: 31 passed</automated>
  </verify>
  <done>One commit contains 9 sources + 18 emitted files + 1 `.ambr`; a repeat `harness_emit` produces an empty diff; the three other derived generators produce an empty diff; core suite 876, instance suite 31.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/ source → emitted runtime trees | the only sanctioned direction; a hand-edit downstream is drift |
| emitted skill/agent prose → deployed agent behavior | agents act on this text; a false plane claim mis-teaches every session |
| core plane → instance overlay | GEN-04 forbids a `harness/` file from naming `examples/`, prose included |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-08 | Spoofing | an emitted skill teaches a plane membership the guard no longer enforces | mitigate | All NINE membership/route copies — including `python-conventions/SKILL.md:44`, which reaches `.claude/skills/`, `.opencode/skill/` and the `.ambr` — are corrected at source and re-projected in the same commit, and the sweep is verified by a mechanical token grep rather than by reading. |
| T-45-09 | Tampering | hand-edit of `.claude/**` or `.opencode/**` | mitigate | Task 1 forbids it and its verify step asserts zero runtime-tree modifications before the emitter runs; `emit-drift` reds on any residual hand-edit. |
| T-45-10 | Elevation of Privilege | a front-matter `description`/`tools` change silently reroutes an agent | mitigate | Body-prose-only constraint stated explicitly; `caps.py` hard-fails the emitter on a count/name move; the porcelain check bounds the blast radius to 27 tracked artifacts plus the snapshot. |
| T-45-11 | Tampering | a route repair leaks an `examples/` token into `harness/`, RED-ing GEN-04 | mitigate | The GEN-04 PHRASING RULE is stated in the context block and re-flagged at both affected sites; the guard is run as a dedicated verify leg in task 1, before the emitter. |
| T-45-SC | Tampering | npm/pip/cargo install | accept | No package-manager install; `uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest -q` → 876 passed, 7 snapshots (after the snapshot regen, within the same commit).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/contract_graph/tests/test_cross_repo_authority.py -q`
  → green, run in task 1 BEFORE the emitter so a phrasing leak is caught at source.
- Second `uv run python -m tools.harness_emit` → empty `git status --porcelain` (emit-drift proof).
- `contract_hash.hash`, `docs_sync.generate`, `memory_regen.contracts_index` → empty diff each
  (stale-derived / CER-10 proof; measured already satisfied at HEAD, so any dirt is a finding).
- `uv run pytest examples/log-parser -q` → 31 passed.
</verification>

<success_criteria>
- No emitted artifact states a four-member constitution plane or points at the deleted `/golden/`
  CODEOWNERS route — verified by the backticked-token grep over both runtime trees, not by reading.
- No `harness/` file gained an `examples/` token; GEN-04 and its cross-repo companion stay green.
- Golden-as-a-concept and the still-live instance golden gate survive untouched — swept by meaning,
  not by token.
- One commit of 28 paths; emitted trees byte-consistent with `harness/`; `.ambr` regenerated, never
  hand-edited.
</success_criteria>

<output>
Create `.planning/phases/45-projection-repair/45-03-SUMMARY.md` when done. List the exact file count in
the commit and the surviving-by-design `golden` mentions you deliberately did not touch.
</output>
