---
phase: 45-projection-repair
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - tools/harness_lint/tests/test_agents.py
  - tools/hooks/tests/test_contract_guard.py
  - tools/harness_config/tests/test_topology_relationships.py
  - tools/adoption_scan/tests/test_install_completeness.py
  - tools/hooks/commit_gate.py
  - harness/git-hooks/pre-commit
autonomous: true
requirements: [CER-11]
user_setup: []

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-06 (a live file describing a control, path or command that no longer exists is the test applied to every candidate), D-11 (the drained tautology, the name/floor mismatch and the SKIP vocabulary are all repaired), D-16 (test repairs ride with the change that invalidates them), D-19 (the suite is run, not read, at each commit), D-22 (no mutation-proof table is owed — this phase removes claims and adds no control)."
    - "SC-1's glob half is asserted mechanically rather than by reading: every entry of path_deny_globs and of CONSTITUTION_GLOBS is proven to match at least one git-tracked file, using each declaration's own matcher."
    - "SC-1's CODEOWNERS half is DECLINED on the record: no module parses CODEOWNERS routes, so covering it would require a new parser module, which SC-8 forbids; the clause is satisfied instead by plan 01 deleting the two dead routes."
    - "No surviving test asserts a tautology a deletion drained, and no test name contradicts its own assertion."
    - "commit_gate and the pre-commit shim describe only the two components they actually run."
    - "Net surface change from this plan is +0 gates, +0 tools, +0 contracts, +0 dependencies — only assertions added inside modules that already load the declarations they now cover."
  artifacts:
    - path: "tools/harness_lint/tests/test_agents.py"
      provides: "matrix-side glob liveness coverage"
      contains: "path_deny_globs"
    - path: "tools/hooks/tests/test_contract_guard.py"
      provides: "guard-side glob liveness coverage"
      contains: "CONSTITUTION_GLOBS"
  key_links:
    - from: "tools/harness_lint/tests/test_agents.py"
      to: "the git index"
      via: "git ls-files subprocess, the idiom already used by test_core_no_example_dep.py"
      pattern: "ls-files"
---

<objective>
Make ROADMAP SC-1's glob clause mechanical instead of read-by-eye, and repair the three assertions
that four deletion phases drained of meaning.

Purpose: SC-1 says "asserted mechanically, not by reading". A zero-match glob is exactly the defect
that Phase 44's review caught by hand (CR-01) rather than at the commit. The coverage goes INSIDE the
two test modules that already load those declarations — no new module, no new gate, no new tool.
SC-1's CODEOWNERS half is declined here deliberately and the reasoning is recorded so a reviewer need
not re-litigate it.

Output: two commits. Suite moves 874 → 876 (two added tests) and stays there for the rest of the phase.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-projection-repair/45-CONTEXT.md
@.planning/phases/45-projection-repair/45-RESEARCH.md
@.planning/phases/45-projection-repair/45-01-SUMMARY.md

<interfaces>
<!-- Existing idiom to copy. Do not invent a new one. -->

tools/harness_lint/tests/test_core_no_example_dep.py
tools/harness_lint/tests/test_core_no_workspace_member_dep.py
  Both shell out to `git ls-files` and assert over the tracked set. This is the established
  precedent for a repository-grounded assertion in this repo.

tools/harness_perms/resolver.py:47
  resolve_path uses fnmatchcase — `*` crosses `/`, `**` behaves as a plain `*`.
tools/adoption_scan/destinations.py:263
  destination_catalog uses pathlib.Path.glob — `**` is recursive. DIFFERENT matcher; probing one
  declaration with the other's matcher produced 13 false DEAD verdicts during research.

tools/hooks/commit_gate.py
  run_composition() -> int      # line ~198; runs [check_drift(), check_polyglot(staged_files())]
  GateResult(name, status, detail)   # line ~57; status docstring still advertises SKIP
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assert mechanically that no declared path glob matches zero tracked files</name>
  <files>tools/harness_lint/tests/test_agents.py, tools/hooks/tests/test_contract_guard.py</files>
  <action>
Implements ROADMAP SC-1's glob clause under the SC-8 constraint of +0 gates/tools/modules.

Add ONE test function to `tools/harness_lint/tests/test_agents.py`, next to the existing
`test_constitution_paths_denied_globally` (around `:188-198`) which already loads
`harness/permission-matrix.json`. It must assert that every entry in `path_deny_globs` matches at
least one file in `git ls-files`, using `fnmatch.fnmatchcase` — the same matcher
`tools/harness_perms/resolver.py:47` uses at runtime, so the test proves the same thing the resolver
would decide. Fail with the offending glob in the message.

Add ONE test function to `tools/hooks/tests/test_contract_guard.py`, next to
`test_every_declared_plane_member_is_independently_enforced` (around `:352`) which already
mutation-proves `CONSTITUTION_GLOBS`. Same assertion, same matcher, over `CONSTITUTION_GLOBS`.

Follow the `git ls-files` idiom already used by `tools/harness_lint/tests/test_core_no_example_dep.py`
and `test_core_no_workspace_member_dep.py` — subprocess out to git, decode, split lines. Do not add a
helper module, a fixture file, a conftest entry, a CI job or a package. Two functions, inside two
modules that already exist and already load these two declarations. That is coverage of an existing
declaration, not new surface.

SCOPE CONSTRAINT — do NOT extend this to `tools/adoption_scan/scan.py`'s `SECRET_PATH_GLOBS` or to
`tools/adoption_scan/destinations.py`'s `_CATEGORY_GLOBS`. Their subject is a scanned brownfield
TARGET repository, not this checkout; matching zero paths here is correct behavior, and asserting
otherwise would be a false failure. Put that reason in a comment above each new test so a later
reader does not "helpfully" widen it.

DECLINED, and record it in the commit message and the summary: SC-1's CODEOWNERS clause ("no
CODEOWNERS route matches zero paths") is NOT built. Measured: no module in this repo parses
`.github/CODEOWNERS` routes — `test_contract_guard.py`, `test_commands.py` and `test_detect.py` only
match the literal string "CODEOWNERS", and `destination_catalog()` reads the file as a path, never its
routes. Covering it therefore requires a new CODEOWNERS parser with no existing home, which SC-8
forbids. The clause is satisfied instead by plan 01 having deleted the two dead routes (`/golden/`,
`/approvals/`). The honest residual — nothing mechanically prevents a FUTURE dead CODEOWNERS route —
is recorded, not papered over.

Expect the suite to move from 874 to 876. Commit both files together.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>uv run pytest tools/harness_lint/tests/test_agents.py tools/hooks/tests/test_contract_guard.py -q 2>&1 | tail -3</automated>
    <automated>git status --porcelain | wc -l   # expect 0 after commit — no new files were created</automated>
  </verify>
  <done>Suite reports 876 passed. Both new tests fail if a `golden/**` row is reinstated (spot-check by hand-editing the matrix, observing the failure, reverting). No new file exists anywhere in the tree. The declined CODEOWNERS half is recorded in the commit message.</done>
</task>

<task type="auto">
  <name>Task 2: Re-subject the drained assertions and narrow the SKIP vocabulary</name>
  <files>tools/harness_config/tests/test_topology_relationships.py, tools/adoption_scan/tests/test_install_completeness.py, tools/hooks/commit_gate.py, harness/git-hooks/pre-commit</files>
  <action>
Implements D-11 and D-06. Note the corrected path: the topology test is under
`tools/harness_config/tests/`, NOT `tools/harness_lint/tests/` as CONTEXT D-11 and the ROADMAP both
state.

(1) `tools/harness_config/tests/test_topology_relationships.py` — `test_output_is_deterministic`
(around `:54-57`) calls `load_project()` and asserts `effective_relationships(cfg) ==
effective_relationships(cfg)`. Verified live: that expression returns `[]`, so the test now asserts
`[] == []` and a deletion drained it. Re-subject it to a synthetic cfg carrying at least two explicit
relationship records — the module's own `test_accessor_is_raw_passthrough` (around `:40-49`) already
constructs exactly that shape and is the pattern to copy. Assert both that the two calls are equal
AND that the result is non-empty, so the test can never silently drain again. Leave the sibling
`test_accessor_returns_empty_on_linear_default` alone: its name is still literally true of the
generic default config, so it is a correct assertion, not a drained one.

(2) `tools/adoption_scan/tests/test_install_completeness.py` — `test_discovers_at_least_twelve_modules`
(`:196`) asserts `>= 11`. The live count is 11 and the docstring already narrates the 20 → 12 → 11
history, so this is a RENAME only; do not touch the floor and do not raise it toward the live value
(the docstring explicitly forbids that — it is a vacuity guard, not a census). Rename to
`test_discovers_at_least_eleven_modules` at the `def` and update the one comment reference at `:222`
("# non-vacuous, backstopped by test_discovers_at_least_twelve_modules above"). Measured: no other
callers.

(3) `tools/hooks/commit_gate.py` — the module runs exactly two components, both unconditionally
(`run_composition` builds `[check_drift(), check_polyglot(staged_files())]`), so no component can
produce SKIP. The module's own header at `:14-16` already records that Phase 44 removed the third
equivalence-parity component. Narrow the surviving vocabulary at `:18` ("exits 0 iff every
non-skipped component passes"), `:60` (`GateResult` docstring "PASS | FAIL | SKIP") and `:203` ("A SKIP
never blocks and never suppresses a sibling FAIL (T-04-13)") to PASS | FAIL. Docstrings only — do not
change `GateResult`'s fields or any control flow. No test asserts these strings.

(4) `harness/git-hooks/pre-commit` — `:4-6` still says the composed gate is "contract-drift + polyglot
§4.3-4.6 + golden-parity" and that "golden-parity is SKIPped-with-a-log when .NET is absent". Both
claims are false after Phase 44. Narrow to the two live components. This file is version-controlled
source that is NOT emitted (it is absent from `tools/harness_emit/emit-manifest.json`) and is manually
installed into `.git/hooks`, so editing it is correct and does not move the emitted trees.

Commit all four files together. Expect the suite to stay at 876.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3   # expect: 876 passed, 7 snapshots</automated>
    <automated>uv run pytest tools/harness_config tools/adoption_scan tools/hooks -q 2>&1 | tail -3</automated>
    <automated>grep -c 'SKIP' tools/hooks/commit_gate.py harness/git-hooks/pre-commit   # expect 0 in both</automated>
    <automated>uv run python -m tools.harness_emit && git status --porcelain   # expect empty — pre-commit is not an emitted artifact</automated>
  </verify>
  <done>`test_output_is_deterministic` asserts over a non-empty synthetic cfg and fails if the accessor returns `[]`; the module-count test's name matches its floor at both sites; neither `commit_gate.py` nor the pre-commit shim mentions SKIP or golden-parity; suite at 876; emitted trees unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| declaration → enforcement | a glob may silently stop matching anything while its comment still claims a plane |
| pre-commit shim → developer | the shim's prose is what a human reads to decide whether the gate covered them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05 | Spoofing | a deny glob that matches zero paths presents as protection | mitigate | The two new `git ls-files`-grounded assertions fail the suite the moment any `path_deny_globs` or `CONSTITUTION_GLOBS` entry stops matching a tracked file. This is the mechanical form SC-1 demands. |
| T-45-06 | Repudiation | a dead CODEOWNERS route reappears unnoticed | accept | Declined by SC-8 — no module parses CODEOWNERS routes and adding a parser is new surface. Residual recorded explicitly in the summary rather than silently. |
| T-45-07 | Tampering | a drained test greens a control that no longer runs | mitigate | `test_output_is_deterministic` is re-subjected to a non-empty cfg and additionally asserts non-emptiness, so draining it again is a failure rather than a pass. |
| T-45-SC | Tampering | npm/pip/cargo install | accept | No package-manager install in this plan; `uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest -q` → 876 passed, 7 snapshots after each commit.
- Negative control for task 1: temporarily re-add `"golden/**"` to `harness/permission-matrix.json`,
  confirm the new `test_agents.py` assertion FAILS, then revert. A mechanical assertion that cannot
  fail is not a mechanical assertion.
- `uv run python -m tools.harness_emit && git status --porcelain` → empty.
</verification>

<success_criteria>
- Every entry of `path_deny_globs` and `CONSTITUTION_GLOBS` is proven against the git index by a test
  in a module that already loaded it — no new module, gate, tool, contract or dependency.
- The declined CODEOWNERS clause and its reason are on the record.
- No test asserts `[] == []`; no test name contradicts its assertion.
- `commit_gate` and the pre-commit shim advertise only the two components that run.
- Suite at 876 passed / 7 snapshots, and it stays there for the remainder of the phase.
</success_criteria>

<output>
Create `.planning/phases/45-projection-repair/45-02-SUMMARY.md` when done. Record the negative-control
result for the new assertions and the declined-CODEOWNERS reasoning verbatim.
</output>
