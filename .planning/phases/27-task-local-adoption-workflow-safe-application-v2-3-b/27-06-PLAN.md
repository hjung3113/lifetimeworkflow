---
phase: 27-task-local-adoption-workflow-safe-application-v2-3-b
plan: 06
type: execute
wave: 3
depends_on: ["27-01", "27-03", "27-04"]
files_modified:
  - harness/commands/adopt.md
  - harness/skills/brownfield-adoption/SKILL.md
  - tools/harness_lint/caps.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/test_emit_determinism.py
  - .opencode
  - .claude
  - tools/harness_emit/emit-manifest.json
autonomous: false
requirements: [ADOPT-07]

must_haves:
  truths:
    - "Exactly ONE new command (/adopt) and ONE new skill (brownfield-adoption) exist — no new persona, no model identifier anywhere in either file"
    - "The anti-sprawl question (skill-creator SKILL.md Step 0: why can't this live in an existing skill?) is answered in writing in this plan before the skill directory is created"
    - "/adopt is agent: orchestrator, matching ADR-0009's no-new-persona/no-new-graph-command precedent already applied to /pipeline"
    - "The command body never constructs a shell string from manifest/draft/scanned content — every invocation is a fixed python -m tools.adoption_apply... argv form"
    - "harness/ source and both emitted trees (.opencode, .claude) are byte-identical round-trips — SC-4"
    - "GEN-04 core->example independence stays green after this plan's additions"
    - "Full uv run pytest suite is green, contract-drift clean, uv.lock unchanged"
  artifacts:
    - path: "harness/commands/adopt.md"
      provides: "The thin /adopt command, agent: orchestrator, argument-routed sub-verbs"
      contains: "agent: orchestrator"
    - path: "harness/skills/brownfield-adoption/SKILL.md"
      provides: "The discover -> draft -> human review -> promote -> apply runbook skill"
      contains: "name: brownfield-adoption"
    - path: "tools/harness_lint/caps.py"
      provides: "EXPECTED_SKILLS widened by exactly one entry (brownfield-adoption)"
      contains: "brownfield-adoption"
  key_links:
    - from: "harness/commands/adopt.md"
      to: "tools.adoption_scan / tools.adoption_apply"
      via: "fixed python -m module invocations, positional $ARGUMENTS only, no shell-string construction"
      pattern: "python -m tools.adoption"
    - from: "harness/skills/brownfield-adoption/SKILL.md"
      to: "harness/commands/adopt.md"
      via: "Related section cross-reference, mirroring pipeline-map <-> /pipeline"
      pattern: "harness/commands/adopt.md"
---

<objective>
Author the single thin `/adopt` command and `brownfield-adoption` skill ADOPT-07/D-08 require —
composing the deterministic tools built in Plans 27-01/03/04/05, the existing
explorer/fan-out/orchestrator/task machinery, and the promotion workflow, with NO new persona and NO
model identifier anywhere — then round-trip the harness emitter to both runtimes and close the
phase-level gates (GEN-04, full suite, contract-drift, `uv.lock` unchanged).

Per D-08 (locked): one command, one skill, `agent: orchestrator` (mirrors `/pipeline`'s and
`/golden-approve`'s single-entry-point style and ADR-0009's "no new graph command and no new
persona" precedent). Per the `skill-creator` skill's mandatory Step 0, this plan answers "why can't
this live in an existing skill?" explicitly before creating the `brownfield-adoption` directory: none
of the 11 existing skills (`python-conventions`, `golden-testing`, `data-contracts`, `skill-creator`,
`golden-debug`, `polyglot-boundary`, `gate-model`, `two-plane-memory`, `pipeline-map`,
`fan-out-synthesize`, `context-budget`) own the discover→draft→review→promote→apply adoption
lifecycle; the closest candidates (`data-contracts`, `gate-model`) are about authoring/checking
contracts already known to exist, not about discovering an UNKNOWN brownfield tree and proposing
what its contracts might be — a genuinely disjoint routing trigger, so a new skill is justified.

Purpose: this is the phase's user-facing surface and closeout gate.
Output: `harness/commands/adopt.md`, `harness/skills/brownfield-adoption/SKILL.md`, a widened
`EXPECTED_SKILLS`, both runtime trees re-emitted and committed, and the full phase gate green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-RESEARCH.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-PATTERNS.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-VALIDATION.md
@harness/commands/golden-approve.md
@harness/commands/pipeline.md
@harness/skills/pipeline-map/SKILL.md
@harness/skills/skill-creator/SKILL.md
@tools/harness_lint/caps.py
@tools/harness_emit/tests/test_coexist.py
@tools/harness_emit/tests/test_emit_determinism.py
@tools/harness_lint/tests/test_commands.py
@tools/harness_lint/tests/test_skills.py
@tools/harness_lint/tests/test_core_no_example_dep.py

<interfaces>
<!-- Exact structural gates this plan's new files must satisfy, extracted this session. -->

`tools/harness_lint/caps.py:129-142` — `EXPECTED_SKILLS: frozenset[str]` currently 11 entries
(`python-conventions, golden-testing, data-contracts, skill-creator, golden-debug,
polyglot-boundary, gate-model, two-plane-memory, pipeline-map, fan-out-synthesize,
context-budget`). Add exactly one: `"brownfield-adoption"`. This single edit is consumed by BOTH
`tools/harness_lint/tests/test_skills.py::test_expected_skills_present_no_sprawl` (exact-set
assertion) AND `tools/harness_emit/validate.py`/`tools/harness_emit/tests/test_emit_determinism.py`
(the emitted skill set must equal `EXPECTED_SKILLS` exactly).

`tools/harness_emit/tests/test_coexist.py::test_all_23_commands_emit_to_both_trees` — currently
asserts `len(opencode_cmds) == 23` / `len(claude_cmds) == 23`, with a docstring narrating each prior
phase's count bump. Adding `harness/commands/adopt.md` bumps this to 24 — update BOTH the assertion
numbers, the test function's name (`test_all_24_commands_emit_to_both_trees`), and append one more
docstring sentence in the SAME narrative style ("Phase 27 adds `/adopt` (the brownfield-adoption
composition entry point), taking it 23 → 24.").

`tools/harness_lint/tests/test_commands.py` — glob-driven (`_command_files()` = `sorted(_COMMANDS_DIR
.glob("*.md"))`); `adopt.md` is automatically covered by `test_frontmatter_parses`/
`test_description_is_routing_signal`/`test_agent_field_well_formed`/`test_subtask_is_boolean_when_
present` — no edit needed to this file itself, only compliance.

`tools/harness_lint/tests/test_skills.py` — glob-driven the same way; `brownfield-adoption/SKILL.md`
is automatically covered once `EXPECTED_SKILLS` is widened — no edit to this test file itself.

`harness/commands/golden-approve.md` (full, already read) — the "thin macro, do NOT re-implement,
positional `$ARGUMENTS`, no shell-string construction" body shape to mirror for the human-gate
sub-verb of `/adopt` (promote).

`harness/commands/pipeline.md` (full, already read) — the multi-step-composition-with-`subtask:
true` body shape to mirror for `/adopt`'s multi-sub-verb (`discover`/`draft`/`apply`/`promote`)
structure.

`harness/skills/pipeline-map/SKILL.md` (full, already read) — the opening-statement + per-stage
section + module-reference body shape to mirror for `brownfield-adoption/SKILL.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author /adopt + brownfield-adoption skill (no new persona, no model id)</name>
  <files>
    harness/commands/adopt.md,
    harness/skills/brownfield-adoption/SKILL.md,
    tools/harness_lint/caps.py
  </files>
  <read_first>
    harness/commands/golden-approve.md (full — thin macro / positional-args / no-shell-string
      discipline),
    harness/commands/pipeline.md (full — multi-step `subtask: true` composition shape),
    harness/skills/pipeline-map/SKILL.md (full — opening statement / per-stage section / module
      reference body shape, ~140 lines, well under the ~500-line warn cap),
    harness/skills/skill-creator/SKILL.md (full, ~40 lines — Step 0's mandatory anti-sprawl
      question; this task's objective block already answers it, cite that answer verbatim in the
      SKILL.md's own opening paragraph so the disposition is discoverable from the skill itself),
    tools/harness_lint/caps.py lines 120-142 (`EXPECTED_SKILLS` — the exact set to widen)
  </read_first>
  <action>
    Write `harness/commands/adopt.md` with frontmatter `description` (a "use when / does X / invoke
    when Y" routing-trigger paragraph naming brownfield discovery, task-local batches, human-gated
    promotion, and safe apply) and `agent: orchestrator` + `subtask: true` (per D-08 and the
    `pipeline.md` multi-step precedent). Body: a thin macro documenting FOUR sub-verbs
    (`discover`/`draft`/`apply`/`promote`), each invoked via a FIXED `python -m
    tools.adoption_scan`/`python -m tools.adoption_apply` module form with `$ARGUMENTS` passed
    positionally — never interpolated into a constructed shell string (mirrors `golden-approve.md`'s
    exact discipline, and is itself ADOPT-07's "임의 command 미실행" fixture-proof requirement at the
    command-authoring layer). Document the refuse-by-default promotion gate (mirrors
    `golden-approve.md`'s "An agent running this command cannot self-approve" paragraph, adapted to
    the `(draft_hash, task_revision, git_ref)` binding). No model identifier anywhere in the file.

    Write `harness/skills/brownfield-adoption/SKILL.md` with frontmatter `name: brownfield-adoption`
    (matches the directory name) and a `description` ≤1024 chars carrying a routing trigger disjoint
    from all 11 existing skills' descriptions (verify via
    `tools/harness_lint/tests/test_skills.py::test_descriptions_are_disjoint` in Task 2). Body: open
    with the anti-sprawl disposition (why this isn't `data-contracts`/`gate-model`, per this plan's
    `<objective>`), then one section per lifecycle stage (discover, draft, human review, promote,
    apply), each naming the concrete module (`tools.adoption_scan`, `tools.adoption_apply.batch`,
    `tools.adoption_apply.approval`, `tools.adoption_apply.apply`) the way `pipeline-map/SKILL.md`
    names `tools.harness_config`. Add a `## Related` section cross-referencing
    `harness/commands/adopt.md` (mirrors `pipeline-map`'s own `## Related` section referencing
    `pipeline.md`). Keep the body well under ~500 lines.

    Edit `tools/harness_lint/caps.py`: add `"brownfield-adoption"` to `EXPECTED_SKILLS` (the single
    entry — do not touch any other set member).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_skills.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_skills.py
    -q` exits 0, including `test_expected_skills_present_no_sprawl` and
    `test_descriptions_are_disjoint`. `grep -riE "claude-[0-9]|gpt-[0-9]|opus|sonnet|haiku"
    harness/commands/adopt.md harness/skills/brownfield-adoption/SKILL.md` finds nothing (no model
    identifier). `grep -n "agent:" harness/commands/adopt.md` shows `agent: orchestrator`.
  </acceptance_criteria>
  <done>/adopt and brownfield-adoption exist, pass the structural gates, cite the anti-sprawl
  disposition, and name no persona but orchestrator and no model identifier.</done>
</task>

<task type="auto">
  <name>Task 2: Emit round-trip + coexist count bump + GEN-04 + full phase gate</name>
  <files>
    tools/harness_emit/tests/test_coexist.py,
    tools/harness_emit/tests/test_emit_determinism.py,
    .opencode,
    .claude,
    tools/harness_emit/emit-manifest.json
  </files>
  <read_first>
    tools/harness_emit/tests/test_coexist.py lines 39-65 (`test_all_23_commands_emit_to_both_trees`
      — the exact narrative-docstring style to extend, and the count assertion to bump),
    tools/harness_emit/tests/test_emit_determinism.py (the `EXPECTED_SKILLS`-driven emitted-skill-set
      assertion — no code change needed here beyond re-running against the widened caps.py set,
      unless a committed `.ambr` snapshot needs regeneration),
    27-VALIDATION.md's SC-4 row (`uv run python -m tools.harness_emit.generate && git diff
      --exit-code -- .opencode .claude`) and the repo invariant rows (`uv.lock` unchanged,
      committed-derived plane fresh)
  </read_first>
  <action>
    In `tools/harness_emit/tests/test_coexist.py`, rename
    `test_all_23_commands_emit_to_both_trees` to `test_all_24_commands_emit_to_both_trees`, bump both
    `== 23` assertions to `== 24`, and append one docstring sentence in the established narrative
    style: "Phase 27 adds `/adopt` (the brownfield-adoption composition entry point), taking it 23 →
    24." Then run, in this strict order: `uv run pytest tools/harness_emit -q` (confirms the source-
    level counts/sets are consistent BEFORE touching the committed trees — mirrors the gate-theft-
    avoidance ordering STATE.md records for prior emit round-trips: source-level test green first,
    THEN commit the emitted trees, so no snapshot is blessed over a stale tree); `uv run python -m
    tools.harness_emit.generate` (re-emits `.opencode/` + `.claude/` + `tools/harness_emit/
    emit-manifest.json` from the widened `harness/` source); `git diff --stat -- .opencode .claude`
    (review the diff — expect new `.opencode/command/adopt.md`, `.claude/commands/adopt.md`,
    `.opencode/skill/brownfield-adoption/**` or `.claude/skills/brownfield-adoption/**` per this
    repo's existing skill-projection shape, and an updated `opencode.json`/`AGENTS.md` managed block
    if skills are listed there). Finally close the phase gate: `uv run pytest -q` (full suite),
    `uv run python -m tools.contract_drift.drift` (clean — no plan in this phase touched
    `contracts/` after Plan 27-02's ratified commit), `uv run pytest
    tools/harness_lint/tests/test_core_no_example_dep.py -q` (GEN-04 green), `uv sync --all-packages
    && git diff --exit-code uv.lock` (no new dependency across the whole phase).
  </action>
  <verify>
    <automated>uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude`
    exits 0 on a SECOND run after the first emit is committed (byte-identical re-emit — SC-4). `uv
    run pytest -q` (full suite) exits 0. `uv run python -m tools.contract_drift.drift` exits 0. `uv
    run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0. `uv sync
    --all-packages && git diff --exit-code uv.lock` shows no diff across the entire phase.
  </acceptance_criteria>
  <done>Both runtime trees carry /adopt + brownfield-adoption byte-identically; the full phase gate
  (suite, drift, GEN-04, uv.lock) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `harness/commands/adopt.md`'s `$ARGUMENTS` → the invoked module | Must never be interpolated into a constructed shell string — positional passthrough only. |
| Emitted `.opencode`/`.claude` trees ← `harness/` source | One-directional; hand-editing either emitted tree is never sanctioned (emit-drift CI catches it). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Elevation of Privilege | `/adopt`'s `$ARGUMENTS` reaching a constructed shell string | mitigate | Fixed `python -m tools.adoption_scan`/`tools.adoption_apply` module invocations with positional passthrough only, mirroring `golden-approve.md`'s proven discipline; structurally reviewed in Task 1's acceptance criteria. |
| T-27-06-02 | Spoofing | A hand-edited emitted tree silently diverging from `harness/` source | mitigate | `git diff --exit-code -- .opencode .claude` after a fresh re-emit is the non-bypassable proof; CI's `emit-drift` job mirrors this. |
| T-27-06-03 | Information Disclosure | A model identifier leaking into a committed command/skill/agent file | mitigate | Explicit `grep` for model-name tokens in Task 1's acceptance criteria; the emitter's own `check_*` validators (loud-fail) are a second, independent layer. |
| T-27-06-SC | Tampering (supply chain) | N/A | accept | Zero new package installs across the whole phase; `uv.lock` unchanged is the explicit closing gate. |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_skills.py -q`
- `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude`
- `uv run pytest -q` (full suite)
- `uv run python -m tools.contract_drift.drift`
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q`
- `uv sync --all-packages && git diff --exit-code uv.lock`
</verification>

<success_criteria>
- ADOPT-07 fully satisfied: one thin `/adopt` command + `brownfield-adoption` skill compose the
  deterministic tools + existing gates + the promotion workflow; the 3 fixtures (Plan 27-05) prove
  idempotence/no-arbitrary-exec/constitution-refusal/emitter-closure/GEN-04.
- SC-4 (ROADMAP): `/adopt` + skill round-trip byte-identical to both runtimes; no new persona, no
  model id.
- Phase 27 gate: full suite green, contract-drift clean, GEN-04 clean, `uv.lock` unchanged.
</success_criteria>

<output>
Create `.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-06-SUMMARY.md`
when done.
</output>
