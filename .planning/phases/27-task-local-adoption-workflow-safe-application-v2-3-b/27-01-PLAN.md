---
phase: 27-task-local-adoption-workflow-safe-application-v2-3-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_apply/pyproject.toml
  - tools/adoption_apply/__init__.py
  - tools/adoption_apply/__main__.py
  - tools/adoption_apply/batch.py
  - tools/adoption_apply/tests/__init__.py
  - tools/adoption_apply/tests/conftest.py
  - tools/adoption_apply/tests/test_batch_layout.py
autonomous: true
requirements: [ADOPT-04]

must_haves:
  truths:
    - "tools/adoption_apply is a real uv workspace member (uv sync --all-packages succeeds, no uv.lock diff) discovered automatically by the root tools/* wildcard — no root pyproject.toml edit needed"
    - "A batch directory lives at .workflow/tasks/<id>/artifacts/adoption/<batch-id>/, satisfying tools.task_control.manager.missing_artifacts()'s existing artifacts/<kind>/<run-id>/ convention with zero manager.py changes"
    - "<batch-id> is content-derived from (target_ref, discover-time UTC date) per D-02: a same-day re-discover against an unchanged target_ref resumes the SAME batch directory without mutating it"
    - "Batch status mutations (not batch creation) are revision-CAS-guarded with the exact same lock+compare+atomic-replace shape tools.task_control.manager::_cas_write uses — a stale expected_revision is rejected the same way any other task mutation is"
    - "No edit lands in contracts/harness/task-control/transitions.json — adoption batches are purely additive evidence per D-01, never a phase-transition gate"
  artifacts:
    - path: "tools/adoption_apply/pyproject.toml"
      provides: "New virtual uv workspace member (package = false, dependencies = []), auto-discovered by root pyproject.toml's tools/* wildcard"
    - path: "tools/adoption_apply/batch.py"
      provides: "batch_id_for(), create_or_resume_batch(), read_status(), update_status() — the CAS-guarded artifacts/adoption/<batch-id>/ layout"
      contains: "def create_or_resume_batch"
    - path: "tools/adoption_apply/tests/test_batch_layout.py"
      provides: "test_resume_safely + test_batch_uses_existing_cas — the two ADOPT-04 Nyquist rows"
      contains: "def test_resume_safely"
  key_links:
    - from: "tools/adoption_apply/batch.py"
      to: "tools/task_control/manager.py"
      via: "same lock+compare+atomic-replace CAS shape (fcntl.flock, expected_revision check, atomic temp+os.replace), not a second scheme"
      pattern: "fcntl.flock"
    - from: "tools/adoption_apply/batch.py"
      to: ".workflow/tasks/<id>/artifacts/adoption/<batch-id>/"
      via: "missing_artifacts()'s existing artifact-kind convention (artifact_root.is_dir() and any child dir non-empty)"
      pattern: "artifacts.*adoption"
---

<objective>
Stand up `tools/adoption_apply` as a new uv workspace member and implement `batch.py`: the
task-local batch layout ADOPT-04 requires. A batch is NOT a new task-control concept — it is a new
*artifact kind* under the existing `artifacts/<kind>/<run-id>/` convention
`tools/task_control/manager.py::missing_artifacts()` already enforces, with `<batch-id>` playing the
role of `<run-id>`. Per D-02, `<batch-id>` is content-derived from `(target_ref, discover-time UTC
date)` so a same-day re-discover against an unchanged ref resumes the same batch (SC-1's "안전하게
재개"). Per D-01, this plan makes ZERO edit to `contracts/harness/task-control/transitions.json` —
adoption never gates a phase transition, it is purely additive evidence.

Purpose: gives every later plan (apply, approval, fixtures, `/adopt`) a concrete, tested home
directory and a CAS-safe status-mutation primitive to build on, without touching the existing
task-control machinery.
Output: `tools/adoption_apply/{pyproject.toml,__init__.py,__main__.py,batch.py}` +
`tools/adoption_apply/tests/{conftest.py,test_batch_layout.py}`, all green, `uv sync --all-packages`
clean with zero `uv.lock` diff.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-RESEARCH.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-PATTERNS.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-VALIDATION.md
@tools/adoption_scan/pyproject.toml
@tools/adoption_scan/__main__.py
@tools/adoption_scan/tests/conftest.py
@tools/task_control/manager.py
@pyproject.toml

<interfaces>
<!-- Exact primitives this plan's batch.py reuses/mirrors. Extracted from tools/task_control/manager.py
     this session — no re-derivation needed. -->

`tools/task_control/manager.py:150-173` — `_atomic_create(path, value)`: `tempfile.mkstemp` in the
SAME directory as `path` → write/flush/`os.fsync` → publish via `os.link(temporary, path)` (raises
`FileExistsError` on an existing target — the collision check, no separate `exists()` race) →
best-effort parent-directory fsync → always `os.unlink(temporary)` in `finally`.

`tools/task_control/manager.py:106-147` — `_cas_write(task_dir, expected_revision, next_state, ...)`:
`fcntl.flock(LOCK_EX)` on a sidecar `.{name}.lock` file → re-read current document → raise if
`current["revision"] != expected_revision` (`"stale writer: expected revision {expected_revision},
found {current['revision']}"`) → raise if `next_state["revision"] != expected_revision + 1` →
atomic-replace via `os.replace`.

`tools/task_control/manager.py:223-235` — `missing_artifacts()`'s artifact-kind convention:
`artifact_root = root / "artifacts" / artifact; present = artifact_root.is_dir() and any(child.is_dir()
and any(child.iterdir()) for child in artifact_root.iterdir())`. A batch at
`artifacts/adoption/<batch-id>/{inventory.json,plan.json,manifest.json,...}` satisfies this
convention with `artifact == "adoption"` and ZERO code change to `manager.py` — do not add
`"adoption"` to `contracts/harness/task-control/transitions.json`'s
`required_artifacts_by_target_phase` (D-01 — batches are purely additive evidence, never a
phase-transition gate).

`tools/adoption_scan/pyproject.toml` (full, 11 lines) — exact `[project]` + `[tool.uv] package =
false` shape to clone verbatim (only `name`/`description` differ).

`tools/adoption_scan/__main__.py` (full, 7 lines) — exact module-entrypoint shape:
```
"""Package entrypoint so ``python -m tools.adoption_scan`` runs the CLI."""
from tools.adoption_scan.cli import main
if __name__ == "__main__":
    raise SystemExit(main())
```

`tools/adoption_scan/tests/conftest.py:19-29` — the `sys.path` wiring pattern (repo root inserted
at index 0 so `from tools.adoption_apply import ...` resolves under bare pytest collection):
`_REPO_ROOT = Path(__file__).resolve().parents[3]` (for `tools/adoption_apply/tests/conftest.py`,
same depth: `tests -> adoption_apply -> tools -> repo root`).

Root `pyproject.toml:34` — `members = ["libs/python", "tools/*"]`: `tools/adoption_apply/` is
auto-discovered by the `tools/*` glob. Do NOT edit root `pyproject.toml`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace member scaffold</name>
  <files>
    tools/adoption_apply/pyproject.toml,
    tools/adoption_apply/__init__.py,
    tools/adoption_apply/__main__.py,
    tools/adoption_apply/tests/__init__.py,
    tools/adoption_apply/tests/conftest.py
  </files>
  <read_first>
    tools/adoption_scan/pyproject.toml (full, clone shape),
    tools/adoption_scan/__main__.py (full, clone shape),
    tools/adoption_scan/tests/conftest.py lines 1-35 (sys.path wiring only — do NOT copy
      `tmp_minirepo`, that fixture is scoped to Phase 26's own tests),
    root pyproject.toml lines 25-34 (`[tool.uv.workspace]` — confirms `tools/*` auto-discovery,
      no edit needed here)
  </read_first>
  <action>
    Create `tools/adoption_apply/pyproject.toml` cloning `tools/adoption_scan/pyproject.toml`
    verbatim except `name = "logparser-adoption-apply"` and a description naming ADOPT-04..07 (task
    -local batch storage, atomic/collision-safe apply, structural constitution refusal,
    human-ratification approval, `/adopt` composition) and explicitly stating "Zero new external
    packages" — `dependencies = []`, `[tool.uv] package = false`. Create `tools/adoption_apply/
    __init__.py` (empty, marks the package). Create `tools/adoption_apply/__main__.py` cloning
    `adoption_scan/__main__.py`'s shape, importing `from tools.adoption_apply.cli import main`
    (the `cli.py` module is authored in a later plan of this wave/phase — this file only needs to
    exist and be import-syntactically valid; do NOT create `cli.py` in this task, this task's scope
    is batch.py only per the plan's `files_modified`). Create `tools/adoption_apply/tests/
    __init__.py` (empty) and `tools/adoption_apply/tests/conftest.py` with ONLY the `sys.path`
    wiring (`_REPO_ROOT = Path(__file__).resolve().parents[3]`, `if str(_REPO_ROOT) not in sys.path:
    sys.path.insert(0, str(_REPO_ROOT))`) plus a `repo_root` session fixture returning `_REPO_ROOT` —
    mirroring `adoption_scan/tests/conftest.py`'s wiring section exactly, no `tmp_minirepo`-style
    fixture in this file.
  </action>
  <verify>
    <automated>uv sync --all-packages && git diff --exit-code uv.lock && uv run python -c "import tools.adoption_apply"</automated>
  </verify>
  <acceptance_criteria>
    `uv sync --all-packages` exits 0 and `git diff --exit-code uv.lock` shows no diff (no new
    external dependency was introduced). `uv run python -c "import tools.adoption_apply"` exits 0.
    `tools/adoption_apply/tests/conftest.py` contains no `tmp_minirepo`-named fixture.
  </acceptance_criteria>
  <done>`tools/adoption_apply` is a real, importable, zero-dependency uv workspace member with test
  import-path wiring in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: batch.py — content-derived batch id, CAS-guarded status</name>
  <files>
    tools/adoption_apply/batch.py,
    tools/adoption_apply/tests/test_batch_layout.py
  </files>
  <read_first>
    tools/task_control/manager.py lines 106-235 (this plan's `<interfaces>` block already quotes
      the three load-bearing functions: `_atomic_create`, `_cas_write`, `missing_artifacts` — read
      the full 130-line span for exact error-message shapes and the `nullcontext()`/`fcntl.flock`
      idiom, since batch.py mirrors the SHAPE, not the private function by cross-package import),
    27-RESEARCH.md "Pattern 1: Task-Local Batch as a New Artifact Kind" and "Open Question 1" (the
      explicit D-01 default: no `transitions.json` change) and "Open Question 2" (D-02's
      content-derivation recommendation, now LOCKED by CONTEXT D-02),
    27-PATTERNS.md "tools/adoption_apply/batch.py" section (the exact `missing_artifacts`/
      `_cas_write` quotes and the "batch.py does not need its own CAS/lock scheme... route through
      ordinary manager calls OR reuse the mechanic" framing)
  </read_first>
  <behavior>
    `batch_id_for(target_ref: str, discovered_at: datetime | None = None) -> str`: returns
    `hashlib.sha256(f"{target_ref}|{date}".encode()).hexdigest()[:16]` where `date` is
    `(discovered_at or datetime.now(UTC)).date().isoformat()` — deterministic given the same
    `(target_ref, date)` pair; two calls on the same UTC calendar date with the same `target_ref`
    return the SAME id; a different `target_ref` OR a different UTC date returns a DIFFERENT id.

    `create_or_resume_batch(task_dir: Path, target_ref: str, *, discovered_at: datetime | None =
    None) -> dict`: computes `batch_id = batch_id_for(target_ref, discovered_at)`; if
    `task_dir/"artifacts"/"adoption"/batch_id/"status.json"` already exists, this is a SAFE RESUME —
    read and return the existing status dict UNMODIFIED (no write, no revision bump, no lock taken)
    — proves SC-1's "안전하게 재개". If absent, create the directory and write a fresh
    `status.json` at `revision: 0` via the `_atomic_create`-shaped idiom (own copy of the
    tempfile+os.link sequence — see `<interfaces>`), containing exactly `{"batch_id",
    "target_ref", "discovered_at" (UTC ISO date), "revision": 0}`.

    `read_status(task_dir: Path, batch_id: str) -> dict | None`: returns the parsed `status.json`
    or `None` if absent — pure read, no lock.

    `update_status(task_dir: Path, batch_id: str, expected_revision: int, patch: dict) -> dict`:
    the CAS-guarded mutation entrypoint every later plan's batch-state write goes through (approval
    recording, apply completion). Reuses the EXACT `manager.py::_cas_write` shape scoped to this
    batch's own `status.json` (own sidecar `.status.json.lock`, own `fcntl.flock(LOCK_EX)`): re-read
    current status, raise `BatchError` if `current["revision"] != expected_revision` (message
    mirrors `_cas_write`'s `"stale writer: expected revision {expected}, found {actual}"` shape),
    require `next_status["revision"] == expected_revision + 1`, merge `patch` into a copy of
    `current`, atomic-replace via `os.replace` (mirrors `manager.py::_atomic_replace`, own copy).

    Test matrix (`test_batch_layout.py`):
    - `test_resume_safely`: `create_or_resume_batch` called twice, same `task_dir`/`target_ref`/
      same-UTC-date `discovered_at` — asserts the SAME `batch_id` and directory, and the SECOND call
      does not change `status.json`'s bytes (byte-identical before/after — a resume is a true no-op).
    - `test_different_ref_or_date_mints_new_batch`: a different `target_ref` (same date) OR the same
      `target_ref` with `discovered_at` one UTC day later — asserts a DIFFERENT `batch_id`/directory.
    - `test_batch_uses_existing_cas`: create a batch, call `update_status` with a STALE
      `expected_revision` (e.g. `expected_revision=5` against a real `revision=0` status) — asserts
      `BatchError` is raised and `status.json`'s bytes are UNCHANGED (the same "reject stale writer,
      touch nothing" guarantee `manager.py::_cas_write` gives any other task mutation).
    - `test_update_status_requires_exact_increment`: `update_status` with `expected_revision` correct
      but a `patch` that would NOT advance revision by exactly one — asserts `BatchError`.
  </behavior>
  <action>
    Implement `batch.py` per the `<behavior>` block above: `BatchError(ValueError)`, `batch_id_for`,
    `create_or_resume_batch`, `read_status`, `update_status`, plus a private
    `_batch_dir(task_dir, batch_id) -> Path` returning `Path(task_dir) / "artifacts" / "adoption" /
    batch_id`. Do NOT import `tools.task_control.manager`'s private underscore functions across the
    package boundary (27-RESEARCH's "Don't Hand-Roll" table: copy the exact sequence, not a
    cross-package private import) — write `batch.py`'s own `_atomic_create_status`/`_atomic_replace_
    status` helpers using the identical `tempfile.mkstemp`/`os.link`/`os.fsync`/`fcntl.flock`
    sequence. Never write anything under `contracts/harness/task-control/`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_batch_layout.py -x</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_batch_layout.py::test_resume_safely -x` passes.
    `uv run pytest tools/adoption_apply/tests/test_batch_layout.py::test_batch_uses_existing_cas -x`
    passes and its assertion explicitly checks `status.json`'s bytes are unchanged after the
    rejected stale write (not just that an exception was raised). `git diff --stat -- contracts/`
    shows no changes from this task.
  </acceptance_criteria>
  <done>batch.py implements the content-derived, CAS-guarded, resume-safe artifact-kind layout;
  all four tests in test_batch_layout.py are green; zero contracts/ touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent/CLI session → `.workflow/tasks/<id>/artifacts/adoption/<batch-id>/status.json` | Task-local, task-transaction-scoped mutable pointer; not constitution-plane, but still needs concurrency-safety (two writers racing on the same batch). |
| Same-day re-discover → existing batch directory | Must be provably a no-op read, never a silent overwrite of a batch a human may already be reviewing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | `update_status`'s CAS write | mitigate | Exact `fcntl.flock` + `expected_revision` compare-before-write shape as `manager.py::_cas_write`; proven by `test_batch_uses_existing_cas` asserting zero bytes change on a rejected stale write. |
| T-27-01-02 | Denial of Service (self-inflicted) | `create_or_resume_batch` racing two concurrent `discover` invocations | accept | Out of this task's proof scope — `_atomic_create_status`'s `os.link` raises `FileExistsError` on a true race, which the resume path already treats as "someone else created it first, read and return" rather than crashing; full concurrent-writer proof is deferred to Plan 27-04's SC-1 integration test, which exercises the end-to-end resume cycle. |
| T-27-01-SC | Tampering (supply chain) | N/A | accept | Zero new package installs (stdlib `hashlib`/`tempfile`/`fcntl`/`os` only); no slopcheck required. |
</threat_model>

<verification>
- `uv sync --all-packages && git diff --exit-code uv.lock` — no new dependency.
- `uv run pytest tools/adoption_apply -q` — all Task 1/2 tests green.
- `git diff --stat -- contracts/ .github/` — empty (this plan touches no constitution-plane or CI file).
</verification>

<success_criteria>
- ADOPT-04 (partial, batch-layout half): a batch at `artifacts/adoption/<batch-id>/` resumes safely
  on an unchanged `(target_ref, date)`, and its status mutations are CAS-guarded identically to any
  other task mutation.
- Zero `contracts/harness/task-control/transitions.json` edit (D-01 upheld).
</success_criteria>

<output>
Create `.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-01-SUMMARY.md`
when done.
</output>
