---
phase: 27-task-local-adoption-workflow-safe-application-v2-3-b
plan: 05
type: execute
wave: 3
depends_on: ["27-01", "27-03", "27-04"]
files_modified:
  - tools/adoption_apply/tests/fixtures/polyglot-single
  - tools/adoption_apply/tests/fixtures/client-server
  - tools/adoption_apply/tests/fixtures/partial-collision-crlf
  - tools/adoption_apply/tests/test_fixtures.py
autonomous: true
requirements: [ADOPT-07]

must_haves:
  truths:
    - "3 fixtures exist and each drives inventory -> plan -> manifest -> apply through the real tools.adoption_scan + tools.adoption_apply pipeline end to end"
    - "polyglot-single is a NEW static, checked-in tree (never a tmp_path-materialized clone of adoption_scan's tmp_minirepo, per 27-PATTERNS.md's explicit finding)"
    - "client-server extends the existing tests/fixtures/workspace/{member-a,member-b} two-member shape rather than inventing a new 2-repo layout"
    - "partial-collision-crlf is NEW and carries at least one genuine CRLF/BOM input file, since neither tmp_minirepo nor tests/fixtures/workspace carries one today"
    - "All 3 fixture trees are domain-neutral vocabulary (widget/source/sink style) — GEN-04 stays green, no examples/ reference anywhere in this plan's files"
  artifacts:
    - path: "tools/adoption_apply/tests/fixtures/polyglot-single/"
      provides: "SC-3 fixture 1: single-repo polyglot target with a create/preserve/conflict/marker-merge mix"
    - path: "tools/adoption_apply/tests/fixtures/client-server/"
      provides: "SC-3 fixture 2: 2-repo target extending tests/fixtures/workspace/{member-a,member-b}"
    - path: "tools/adoption_apply/tests/fixtures/partial-collision-crlf/"
      provides: "SC-3 fixture 3: partial-adoption/collision target with a real CRLF/BOM input file"
    - path: "tools/adoption_apply/tests/test_fixtures.py"
      provides: "One end-to-end test per fixture driving scan -> plan -> destinations -> apply_manifest"
      contains: "def test_polyglot_single_end_to_end"
  key_links:
    - from: "tools/adoption_apply/tests/test_fixtures.py"
      to: "tools.adoption_scan.{scan,plan,destinations}"
      via: "the same scan -> plan -> build_manifest pipeline tools.adoption_scan.cli.main already wires — never reimplemented"
      pattern: "from tools.adoption_scan import"
    - from: "tools/adoption_apply/tests/test_fixtures.py"
      to: "tools.adoption_apply.apply.apply_manifest"
      via: "applies the real manifest produced by the scan pipeline against each fixture's target"
      pattern: "apply_manifest"
---

<objective>
Author the 3 generic fixtures ADOPT-07/SC-3 require (single-repo polyglot, 2-repo client/server,
partial-adoption/collision with mandatory CRLF/BOM input) and one end-to-end test per fixture that
drives the REAL pipeline: `tools.adoption_scan.scan.build_inventory` →
`tools.adoption_scan.plan.build_plan` → `tools.adoption_scan.destinations.build_manifest` →
`tools.adoption_apply.apply.apply_manifest` — never a mocked/stubbed subset of that chain. Per
27-PATTERNS.md's file-by-file findings (the one MEDIUM-confidence gap RESEARCH flagged and this
phase's pattern-mapping pass closed): `tmp_minirepo` is a `conftest.py`-materialized `tmp_path`
fixture with no CRLF/BOM file, so `polyglot-single` must be a NEW static checked-in tree (borrowing
individual synthetic elements, never importing the fixture body); `client-server` extends the real,
static `tests/fixtures/workspace/{member-a,member-b}` two-member layout; `partial-collision-crlf` is
NEW and is the ONLY fixture required to carry the CRLF/BOM input.

Purpose: closes SC-3 and the fixture-proof half of ADOPT-07 (idempotence, no-arbitrary-command-
execution, constitution refusal, at-least-one-CRLF/BOM input) with real, checked-in, domain-neutral
targets any future contributor can inspect without regenerating anything.
Output: 3 fixture trees + `test_fixtures.py`, all green, GEN-04 unaffected.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-RESEARCH.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-PATTERNS.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-VALIDATION.md
@tools/adoption_scan/tests/conftest.py
@tools/adoption_scan/cli.py
@tools/adoption_scan/destinations.py
@tools/harness_emit/merge.py
@tools/adoption_apply/apply.py
@tools/adoption_apply/batch.py
@tests/fixtures/workspace/member-a/contracts/greeting.schema.json
@tools/harness_lint/tests/test_core_no_example_dep.py

<interfaces>
<!-- 27-PATTERNS.md's "Fixture Reuse" section, already read in full this session — the exact
     file-by-file findings this plan's fixtures must match. -->

`tools/adoption_scan/tests/conftest.py:38-159`'s `tmp_minirepo(tmp_path)` — a PROGRAMMATIC fixture
(no static directory), embedding 18 named cases (secret-by-path, secret-by-content, binary,
vendored, generated, over-cap, source-dump x2, hash-equal/hash-different collision trio, extensionless,
escaping symlink, manifest, CI surface, test surface, ADR surface, marker-capable root `AGENTS.md`,
schema surface under `contracts/`, CODEOWNERS surface). Domain-neutral vocabulary (`widget`/`source`/
`sink`). **No CRLF/BOM file exists in it today.**

`tests/fixtures/workspace/{member-a,member-b}/` (repo-root level) — a REAL, static, checked-in
two-member fixture: each member has its own `contracts/` + `contracts/.hashes/manifest.json`;
`member-a` additionally has a `golden/greeting-edge/` case. This is the exact shape to extend for
`client-server`.

`tools/harness_emit/merge.py`'s own CRLF-handling contract (`_normalize`:
`text.replace("\r\n", "\n").replace("\r", "\n")`) — the known-correct semantic reference for
`partial-collision-crlf`'s marker-merge-target file, so the fixture's expected outcome is
independently derivable, not invented.

`tools/adoption_scan/cli.py::main` — the exact `scan.build_inventory` → `plan.build_plan` →
`destinations.harness_proposed_hashes` + `destinations.build_manifest` wiring order to reproduce (as
plain function calls, not a subprocess) inside `test_fixtures.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: polyglot-single + client-server fixture trees</name>
  <files>
    tools/adoption_apply/tests/fixtures/polyglot-single,
    tools/adoption_apply/tests/fixtures/client-server
  </files>
  <read_first>
    tools/adoption_scan/tests/conftest.py lines 38-159 (full `tmp_minirepo` body — read for
      SYNTHETIC ELEMENT SHAPE only, e.g. the hash-equal/hash-different trio and the marker-capable
      root `AGENTS.md`; do NOT import or call this fixture),
    tests/fixtures/workspace/member-a/contracts/greeting.schema.json and
      tests/fixtures/workspace/member-a/contracts/.hashes/manifest.json (the exact per-member
      layout to extend),
    tools/adoption_scan/destinations.py lines 117-124 (`DISPOSITION_ENUM` — `polyglot-single` must
      exercise at least `create`, `preserve`, `conflict`, and `marker-merge` so the fixture actually
      demonstrates a disposition MIX, not just `create`),
    27-PATTERNS.md "Fixture Reuse" section (already summarized in this plan's `<interfaces>` block)
  </read_first>
  <action>
    Build `tools/adoption_apply/tests/fixtures/polyglot-single/` as a static, checked-in tree with
    domain-neutral (`widget`/`source`/`sink`) content: a `pyproject.toml` manifest file (language
    detection), a root `AGENTS.md` carrying the literal BEGIN/END HARNESS-MANAGED markers
    (marker-merge target), a `widget_a.py`/`widget_a_copy.py` hash-equal pair (proves `preserve`),
    a `widget_b.py` with a `widget_b_modified.py` sibling of different content at the SAME relative
    destination-shape (proves `conflict` — i.e. structure the fixture's "proposed" content and
    "existing target" content to differ at one path), and at least one genuinely new destination
    path with no existing file (proves `create`). Build `tools/adoption_apply/tests/fixtures/
    client-server/` by copying `tests/fixtures/workspace/{member-a,member-b}`'s two-member shape
    (each member keeps its own `contracts/` + hash manifest) and adding one adoption-relevant
    addition to ONE member (e.g. a root `AGENTS.md` with markers in `member-a`, absent from
    `member-b`) so the fixture proves BOTH: a normal single-repo apply cycle per member, AND that
    adoption never crosses a member boundary (no cross-member write). Every path/identifier in both
    trees must be domain-neutral — grep both trees for `examples/`/domain vocabulary before
    finishing (GEN-04 guard).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` stays green after adding
    both trees. `grep -rl "examples/" tools/adoption_apply/tests/fixtures/polyglot-single
    tools/adoption_apply/tests/fixtures/client-server` finds nothing. `polyglot-single` contains at
    least one file pair proving each of `create`/`preserve`/`conflict`/`marker-merge`.
  </acceptance_criteria>
  <done>Both static fixture trees exist, domain-neutral, exercising a real disposition mix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: partial-collision-crlf fixture + end-to-end test_fixtures.py (SC-3)</name>
  <files>
    tools/adoption_apply/tests/fixtures/partial-collision-crlf,
    tools/adoption_apply/tests/test_fixtures.py
  </files>
  <read_first>
    tools/harness_emit/merge.py's `_normalize` function (the CRLF-handling reference this fixture's
      expected outcome must match),
    tools/adoption_scan/cli.py::main (full — the exact `scan -> plan -> destinations` wiring to
      reproduce in-process),
    tools/adoption_apply/apply.py (Plan 27-03's output — `apply_manifest`'s signature),
    27-VALIDATION.md's `SC-3` row and Wave 0 requirements (the exact assertion: "3 fixtures ...
      pass end-to-end")
  </read_first>
  <behavior>
    `partial-collision-crlf/` is a NEW static tree containing: one file written with genuine CRLF
    line endings AND a UTF-8 BOM prefix (the mandatory CRLF/BOM input this fixture alone carries —
    neither `polyglot-single` nor `client-server` needs one), plus a hash-equal/hash-different
    collision pair mirroring `tmp_minirepo`'s item (h) shape (`widget_a.py == widget_a_copy.py`,
    `widget_a_modified.py` differs) so the fixture ALSO demonstrates the "partial adoption" half of
    its name (some destinations already correctly adopted = `preserve`, one genuinely conflicting =
    `conflict`).

    `test_fixtures.py` (one test per fixture, all following the same shape): build the inventory via
    `tools.adoption_scan.scan.build_inventory(fixture_target)`, the plan via
    `tools.adoption_scan.plan.build_plan(inventory)`, the manifest via
    `tools.adoption_scan.destinations.build_manifest(inventory, fixture_target,
    tools.adoption_scan.destinations.harness_proposed_hashes())`, then
    `tools.adoption_apply.apply.apply_manifest(manifest, fixture_target, batch_dir=...)` — asserts:
    (a) the full cycle completes with no exception outside the expected `ConstitutionRefusal`/
    `ConcurrentDriftError` cases; (b) re-running the identical cycle a second time is byte-identical
    (idempotence, SC-3's implicit "safe to re-run" property); (c) for `partial-collision-crlf`
    specifically, the CRLF/BOM input file's disposition and post-apply bytes match
    `harness_emit.merge._normalize`'s documented contract; (d) a `unittest.mock.patch("subprocess.
    run")` spy across the WHOLE cycle records zero calls for any of the three fixtures (ADOPT-07's
    "임의 command 미실행" proven per-fixture, not only in `test_atomic_apply.py`'s unit-level proof).
  </behavior>
  <action>
    Build `partial-collision-crlf/` per the `<behavior>` block. Implement
    `test_polyglot_single_end_to_end`, `test_client_server_end_to_end`, and
    `test_partial_collision_crlf_end_to_end` in `test_fixtures.py`, each following the shared
    scan→plan→manifest→apply shape described above; factor the shared plumbing into a small
    module-level helper (`_run_pipeline(target: Path, batch_dir: Path) -> dict`) rather than
    triplicating it.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_fixtures.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_fixtures.py -q` exits 0, three tests, each
    proving idempotence and zero `subprocess.run` calls. The CRLF/BOM file's post-apply bytes are
    asserted against `harness_emit.merge._normalize`'s documented transform, not an ad hoc
    hand-computed expectation. `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py
    -q` stays green.
  </acceptance_criteria>
  <done>All 3 fixtures pass end-to-end through the real scan->plan->manifest->apply pipeline,
  idempotently, with zero arbitrary command execution, and GEN-04 stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture content → the real scan/plan/apply pipeline | Fixtures are the ONLY input this plan trusts; the pipeline itself is unmodified from Plans 26/27-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Elevation of Privilege | Fixture-driven end-to-end cycle spawning an unexpected subprocess | mitigate | Per-fixture `subprocess.run` spy proving zero calls across the whole scan->plan->apply cycle, not just the unit-level proof in Plan 27-03. |
| T-27-05-02 | Tampering | CRLF/BOM fixture input silently mis-handled (wrong normalization applied) | mitigate | Assertion is against `harness_emit.merge._normalize`'s OWN documented transform, not an invented expectation — if the reference itself is wrong, both would be wrong together, but they cannot silently diverge. |
| T-27-05-SC | Tampering (supply chain) | N/A | accept | Zero new package installs; fixture content only. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_apply/tests/test_fixtures.py -q` — all 3 fixtures green.
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` — GEN-04 unaffected.
</verification>

<success_criteria>
- SC-3 (ROADMAP): 3 fixtures (single-repo polyglot, 2-repo client/server, partial/collision incl.
  CRLF/BOM) pass end to end.
- ADOPT-07 (fixture half): idempotence and no-arbitrary-command-execution proven per fixture.
</success_criteria>

<output>
Create `.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-05-SUMMARY.md`
when done.
</output>
