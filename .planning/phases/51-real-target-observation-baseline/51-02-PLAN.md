---
phase: 51-real-target-observation-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/phases/51-real-target-observation-baseline/evidence/discover/
  - .planning/phases/51-real-target-observation-baseline/evidence/draft/
  - .planning/phases/51-real-target-observation-baseline/evidence/apply/
  - .planning/phases/51-real-target-observation-baseline/evidence/apply/blocked.json
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.json
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.argv.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.cwd.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.stderr.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.exit-code.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.json
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.argv.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.cwd.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.stderr.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.exit-code.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/member-comparison.json
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/workspace-edge-comparison.json
  - .planning/phases/51-real-target-observation-baseline/evidence/downstream/convention-comparison.json
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.before-apply.status.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.after-apply.status.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.changed-paths.json
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.status.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.head.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.index.sha256
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.untracked-set.sha256
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.worktree-list.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json
autonomous: false
requirements: [OBS-01, OBS-03]

must_haves:
  truths:
    - "Discover, draft, and apply are attempted independently with the exact shipped argv; a nonzero exit is evidence, not a reason to repair or rewrite an input. (D-10, D-11)"
    - "Apply writes only to the detached worktree and is human-confirmed; missing draft input produces an explicit blocked-by observation instead of a fabricated batch id or edited manifest. (D-05)"
    - "Package facts and conventions are called as library functions with the FeedbackOps worktree passed explicitly, so a green command cannot silently describe the lifetimeworkflow checkout. (D-12)"
    - "Member discovery and workspace dependency-edge recording are compared separately, and either confirmed or refuted OBS-03 output is acceptable. (D-14, D-16)"
    - "The original checkout's before/after proof is compared explicitly; inequality becomes an observation and is never retried away. (D-03)"
  artifacts:
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/discover/inventory.json"
      provides: "Real-target inventory when discover produces it"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/draft/artifacts/adoption/"
      provides: "Unedited task-local draft batch, including status/inventory/plan/manifest when produced"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.json"
      provides: "Target-explicit package and dependency-edge facts"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.json"
      provides: "Target-explicit per-package nearest-wins observations and config source"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json"
      provides: "Explicit equality results for every D-03 proof dimension"
  key_links:
    - from: "evidence/draft/artifacts/adoption/*/status.json"
      to: "evidence/apply/argv.txt"
      via: "literal batch_id copied without editing manifest or status"
      pattern: "--batch-id"
    - from: "evidence/downstream/package-facts.json"
      to: "evidence/downstream/workspace-edge-comparison.json"
      via: "exact from/to/kind tuple comparison for @fops/shared dependencies"
      pattern: "@fops/shared"
    - from: "evidence/isolation/before.*"
      to: "evidence/isolation/after.*"
      via: "byte comparison recorded as booleans in comparison.json"
      pattern: "status_equal"
---

<objective>
Run the unmodified adoption pipeline at full depth against the detached FeedbackOps worktree,
capture each command's result without requiring success, then run the target-explicit downstream
package-fact and convention observations and complete the original-checkout after proof.

Purpose: produce the raw, falsifiable evidence needed for OBS-01 and the real-output decision for
OBS-03. This plan observes RTA-01..04 but implements none of them.

Output: discover/draft/apply command records and produced artifacts; package/member/edge/convention
comparisons; worktree mutation evidence; and the before/after isolation comparison.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/51-real-target-observation-baseline/51-CONTEXT.md
@.planning/phases/51-real-target-observation-baseline/51-RESEARCH.md
@.planning/phases/51-real-target-observation-baseline/51-VALIDATION.md
@.planning/phases/51-real-target-observation-baseline/51-01-SUMMARY.md
@harness/commands/adopt.md
@harness/skills/brownfield-adoption/SKILL.md
@harness/skills/python-conventions/SKILL.md
</context>

<interfaces>
All module commands run from `/Users/hyojung/Desktop/2026/lifetimeworkflow` through `uv run`.
The target is `/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline`; no command in
this plan may replace it with `/Users/hyojung/Desktop/2026/FeedbackOps`.

Every observed command has `argv.txt`, `cwd.txt`, `stdout.txt`, `stderr.txt`, and
`exit-code.txt`. A missing output artifact is distinguishable from an empty one. Stage failures do
not short-circuit later independent observations; a genuinely missing input is recorded with a
stable `blocked-by: OBS-D-NN` reference reserved at observation time.

Secret-scan note: the `sk-` alternative is boundary-anchored and dash-free to avoid matching ordinary path substrings such as `task-request`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Attempt discover and draft independently with complete captures</name>
  <files>.planning/phases/51-real-target-observation-baseline/evidence/discover/*, .planning/phases/51-real-target-observation-baseline/evidence/draft/*</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/discover/inventory.json (create if absent; if resuming, read all existing sidecars before deciding whether a rerun would overwrite evidence),
    .planning/phases/51-real-target-observation-baseline/evidence/draft/artifacts/adoption/ (create if absent; never edit a produced JSON input),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-06..D-13),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (exact Discover/Draft argv and Pitfalls 1/2),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (failure-is-pass evidence map),
    .planning/ROADMAP.md (SC-1, SC-2, SC-4),
    .planning/REQUIREMENTS.md (OBS-01; RTA-01/RTA-02 are observed only),
    harness/commands/adopt.md (fixed three-stage macro),
    harness/skills/brownfield-adoption/SKILL.md (discover/draft lifecycle),
    harness/skills/python-conventions/SKILL.md (uv module invocation),
    tools/adoption_scan/cli.py (required --target/--out and artifact writes),
    tools/adoption_scan/scan.py (read-only enumeration, secret exclusions, inventory shape),
    tools/adoption_scan/detect.py (manifest/language detection and package.json dependency parser),
    tools/adoption_scan/plan.py (question records),
    tools/adoption_scan/destinations.py (disposition manifest),
    tools/adoption_apply/cli.py (draft argv and batch write path),
    tools/adoption_apply/batch.py (status.json and batch_id source),
    tools/adoption_apply/apply.py (`refuse_if_outside_root` used by the real draft write path).
  </read_first>
  <action>
    First require Plan 01's provision exit code to be `0`, worktree HEAD equal to the recorded target
    SHA, and detached state. If any prerequisite fails, run no target command and never fall back to
    the original checkout. Instead, for both `discover` and `draft`, write the intended literal argv,
    cwd, empty stdout, explanatory stderr, and `exit-code.txt` equal to `not-run`; write
    `discover/enumeration-check.json` with `result: blocked`, a stable `blocked-by: OBS-D-NN`, and
    explicit empty `node_modules`/`minio_data` match arrays. Verify this capture, then stop Plan 02 as
    blocked before Task 2.

    Record and run exactly
    `uv run python -m tools.adoption_scan --target /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline --out .planning/phases/51-real-target-observation-baseline/evidence/discover`.
    Capture argv, cwd, stdout, stderr, and integer exit code without `set -e` swallowing the code.
    Preserve produced `inventory.json`, `plan.json`, and `manifest.json` byte-for-byte; record which
    are absent when exit is nonzero.

    Regardless of discover's exit, independently record and run exactly
    `uv run python -m tools.adoption_apply draft --task-dir .planning/phases/51-real-target-observation-baseline/evidence/draft --target /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline`.
    Capture the same five sidecars. Preserve the generated
    `artifacts/adoption/BATCH_ID/{status,inventory,plan,manifest}.json` byte-for-byte. Never edit
    Under D-11, never edit `plan.json`, `manifest.json`, or `status.json`; if draft fails after creating only some files,
    that partial set is the evidence.

    After both attempts, write `discover/enumeration-check.json`. Prefer discover's
    `inventory.json`; if discover produced none but draft produced an inventory, use that fallback
    and label the source `draft-fallback`. Record `result: observed`, the literal
    `enumeration_mode` (`git` or `builtin`), and path matches for both `node_modules` and
    `minio_data` across `included` and `excluded`. If neither inventory exists, record
    `result: blocked`, reserve the discover failure as its own `OBS-D-NN`, and do not invent an
    enumeration mode.

    Before committing, run the high-confidence secret-pattern scan over all discover/draft
    captures for AWS, GitHub, OpenAI-style, Slack, private-key, and bearer-token forms. A match is a
    blocking information-disclosure result: quarantine the affected generated capture outside the
    repo and stop for human handling; do not commit it or merely defer review to Plan 03.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; if rg -n -I -e '(AKIA[0-9A-Z]{16}|gh[pous]_[A-Za-z0-9_-]+|github_pat_[A-Za-z0-9_-]+|(^|[^A-Za-z0-9])sk-[A-Za-z0-9]{20,}|xox[bp]-[A-Za-z0-9-]+|-----BEGIN [A-Z ]*PRIVATE KEY-----|Authorization:[[:space:]]*Bearer[[:space:]]+[^[:space:]]+)' .planning/phases/51-real-target-observation-baseline/evidence/discover .planning/phases/51-real-target-observation-baseline/evidence/draft; then exit 1; else scan_rc=$?; test "$scan_rc" -eq 1 || exit 1; fi; for stage in discover draft; do test -s .planning/phases/51-real-target-observation-baseline/evidence/$stage/argv.txt &amp;&amp; test -s .planning/phases/51-real-target-observation-baseline/evidence/$stage/cwd.txt &amp;&amp; test -f .planning/phases/51-real-target-observation-baseline/evidence/$stage/stdout.txt &amp;&amp; test -f .planning/phases/51-real-target-observation-baseline/evidence/$stage/stderr.txt &amp;&amp; grep -Eq '^([0-9]+|not-run)$' .planning/phases/51-real-target-observation-baseline/evidence/$stage/exit-code.txt || exit 1; test "$(cat .planning/phases/51-real-target-observation-baseline/evidence/$stage/exit-code.txt)" != not-run || test -s .planning/phases/51-real-target-observation-baseline/evidence/$stage/stderr.txt || exit 1; done; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/discover/enumeration-check.json &gt;/dev/null &amp;&amp; grep -q '"node_modules"' .planning/phases/51-real-target-observation-baseline/evidence/discover/enumeration-check.json &amp;&amp; grep -q '"minio_data"' .planning/phases/51-real-target-observation-baseline/evidence/discover/enumeration-check.json &amp;&amp; grep -Eq '"result": *"(observed|blocked)"' .planning/phases/51-real-target-observation-baseline/evidence/discover/enumeration-check.json &amp;&amp; { grep -q '"result": *"blocked"' .planning/phases/51-real-target-observation-baseline/evidence/discover/enumeration-check.json || grep -Eq '"enumeration_mode": *"(git|builtin)"' .planning/phases/51-real-target-observation-baseline/evidence/discover/enumeration-check.json; }</automated>
  </verify>
  <acceptance_criteria>
    Both stages have all five sidecars and literal argv contains the exact worktree path; discover
    contains `uv run python -m tools.adoption_scan` with `--out`, and draft contains
    `uv run python -m tools.adoption_apply draft` with `--task-dir`. For each exit 0, all three
    promised JSON artifacts exist and parse as JSON; for each nonzero exit, stderr or the produced
    partial artifact set is non-empty and the result is reserved as an `OBS-D-NN` observation.
    `enumeration-check.json` names its inventory source, quotes `enumeration_mode` when observable,
    and records explicit `node_modules` and `minio_data` match arrays; a missing discover inventory
    has its own OBS-D id rather than disappearing behind draft fallback. The pre-commit secret scan
    returns zero matches across both capture trees.
    No criterion requires either exit code to be zero. `git status --porcelain -- tools/ harness/
    contracts/ docs/adr/` and the baseline-to-current diff over those paths are empty before the
    task commit.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve or defer the isolated adoption apply attempt</name>
  <files>none</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/draft/argv.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/draft/exit-code.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/draft/artifacts/adoption/*/status.json (if produced),
    .planning/phases/51-real-target-observation-baseline/evidence/draft/artifacts/adoption/*/manifest.json (if produced),
    .planning/phases/51-real-target-observation-baseline/evidence/apply/argv.txt (create if absent; read first on resume),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-05, D-10, D-11),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (exact Apply argv and disposition pitfalls),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (SC-1 and T-51-01),
    .planning/ROADMAP.md (SC-1 and SC-4),
    .planning/REQUIREMENTS.md (OBS-01; RTA-01 is observed only),
    harness/commands/adopt.md,
    harness/skills/brownfield-adoption/SKILL.md,
    tools/adoption_apply/cli.py (manifest load, payload source, exit mapping),
    tools/adoption_apply/batch.py (literal batch_id source),
    tools/adoption_apply/apply.py (root confinement and constitution-plane refusal),
    tools/adoption_scan/destinations.py (create/marker-merge/refusal dispositions).
  </read_first>
  <decision>Approve Task 3's exact `/adopt apply` write into the disposable detached worktree, or defer the write while still allowing Task 3 to record an unrun blocked outcome.</decision>
  <context>
    The decision authorizes no command in this checkpoint. If the draft is runnable, `approve`
    permits Task 3 to invoke the shipped apply CLI unchanged. If draft input is absent or the human
    selects `defer`, Task 3 writes the five unrun sidecars plus `blocked.json` and performs no target
    mutation.
  </context>
  <options>
    <option id="approve"><name>Approve apply</name><pros>Captures the real shipped write behavior in the disposable worktree.</pros><cons>May partially write harness artifacts before a captured failure.</cons></option>
    <option id="defer"><name>Defer apply</name><pros>Performs no apply write.</pros><cons>Records apply as blocked and may leave SC-1 incomplete.</cons></option>
  </options>
  <action>
    Present only this decision and run no command. Resume into Task 3 with the selected option.
  </action>
  <resume-signal>Select: approve or defer</resume-signal>
  <acceptance_criteria>
    A human selected exactly `approve` or `defer`; the checkpoint wrote no evidence and made no
    task commit. The selection is passed to Task 3 without editing draft inputs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Attempt or record blocked apply and capture worktree effects</name>
  <files>.planning/phases/51-real-target-observation-baseline/evidence/apply/*, .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.before-apply.status.txt, .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.after-apply.status.txt, .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.changed-paths.json</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/draft/argv.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/draft/exit-code.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/draft/artifacts/adoption/*/status.json (if produced),
    .planning/phases/51-real-target-observation-baseline/evidence/draft/artifacts/adoption/*/manifest.json (if produced),
    .planning/phases/51-real-target-observation-baseline/evidence/apply/argv.txt (create if absent; read first on resume),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-05, D-10, D-11),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (exact Apply argv and disposition pitfalls),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (SC-1 and T-51-01),
    .planning/ROADMAP.md (SC-1 and SC-4),
    .planning/REQUIREMENTS.md (OBS-01; RTA-01 is observed only),
    harness/commands/adopt.md,
    harness/skills/brownfield-adoption/SKILL.md,
    tools/adoption_apply/cli.py (manifest load, payload source, exit mapping),
    tools/adoption_apply/batch.py (literal batch_id source),
    tools/adoption_apply/apply.py (root confinement and constitution-plane refusal),
    tools/adoption_scan/destinations.py (create/marker-merge/refusal dispositions).
  </read_first>
  <action>
    Capture the worktree's `git status --porcelain=v2 --untracked-files=all` before apply. Locate
    exactly one produced `status.json`, read its literal `batch_id`, and require the sibling
    `manifest.json` to exist. If either condition fails, do not invent an id or edit an input:
    write all five sidecars with the literal unrun argv, cwd, empty stdout, explanatory stderr,
    and `exit-code.txt` equal to `not-run`; create `apply/blocked.json` with the reason and
    `blocked-by: OBS-D-NN` pointing to the reserved draft observation.

    To complete D-10's independent third-stage attempt, if runnable and Task 2 selected `approve`, record and run
    exactly `uv run python -m tools.adoption_apply apply --task-dir .planning/phases/51-real-target-observation-baseline/evidence/draft --batch-id LITERAL_STATUS_BATCH_ID --target /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline`.
    Capture the five sidecars on success or failure and never retry after editing input.
    If Task 2 selected `defer`, use the same five-sidecar `not-run` form and reserve its own blocked
    observation; run no apply command.

    Capture post-apply porcelain-v2 status. Produce `worktree.changed-paths.json` from Git's
    modified/untracked path set and compare it with the manifest's `create`/`marker-merge`
    destinations. Under D-05, record `unexpected_paths`, `expected_writable_destinations`, and `matches`; a
    changed product-code path or path absent from the manifest is a high-severity OBS-D defect,
    not an accepted side effect. Do not copy changed file contents into evidence. Before commit,
    run the same high-confidence secret-pattern scan over `evidence/apply` and the three worktree
    state captures; quarantine and stop on a match.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; if rg -n -I -e '(AKIA[0-9A-Z]{16}|gh[pous]_[A-Za-z0-9_-]+|github_pat_[A-Za-z0-9_-]+|(^|[^A-Za-z0-9])sk-[A-Za-z0-9]{20,}|xox[bp]-[A-Za-z0-9-]+|-----BEGIN [A-Z ]*PRIVATE KEY-----|Authorization:[[:space:]]*Bearer[[:space:]]+[^[:space:]]+)' .planning/phases/51-real-target-observation-baseline/evidence/apply .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.before-apply.status.txt .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.after-apply.status.txt .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.changed-paths.json; then exit 1; else scan_rc=$?; test "$scan_rc" -eq 1 || exit 1; fi; for f in argv cwd stdout stderr exit-code; do test -f ".planning/phases/51-real-target-observation-baseline/evidence/apply/$f.txt" || exit 1; done; grep -Eq '^([0-9]+|not-run)$' .planning/phases/51-real-target-observation-baseline/evidence/apply/exit-code.txt || exit 1; test -f .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.before-apply.status.txt &amp;&amp; test -f .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.after-apply.status.txt &amp;&amp; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.changed-paths.json &gt;/dev/null &amp;&amp; grep -q '"unexpected_paths"' .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.changed-paths.json &amp;&amp; grep -Eq '"matches": *(true|false)' .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.changed-paths.json || exit 1; test "$(cat .planning/phases/51-real-target-observation-baseline/evidence/apply/exit-code.txt)" != not-run || { uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/apply/blocked.json &amp;&amp; grep -q 'OBS-D-[0-9][0-9]' .planning/phases/51-real-target-observation-baseline/evidence/apply/blocked.json; }</automated>
  </verify>
  <acceptance_criteria>
    Apply always has all five command sidecars; exit code is an integer when invoked or `not-run`
    with a parseable `blocked.json` naming a real OBS-D id. When invoked,
    argv's batch id exactly equals `status.json` and its target is the detached worktree. The
    changed-path comparison contains explicit path arrays and a boolean; any unexpected/product
    path is assigned its own OBS-D record. Constitution
    destinations remain in the unedited manifest so the shipped refusal is observed. Both
    no-source-edit guards remain empty and the pre-commit secret scan returns zero matches.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Capture target-explicit package facts and conventions</name>
  <files>.planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.*, .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.*</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/apply/exit-code.txt (or blocked.json),
    .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.json (create if absent; read first on resume),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-12..D-16),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (Pitfalls 2..5 and exact library calls),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (OBS-03 and digest checks),
    .planning/ROADMAP.md (SC-1..SC-4),
    .planning/REQUIREMENTS.md (OBS-01/OBS-03; RTA-02..04 observation-only),
    harness/project.toml (dotnet/python language rows under observation),
    tools/adoption_scan/scan.py (inventory manifest list feeding the member comparison),
    tools/adoption_scan/detect.py (`_MANIFEST_KIND_BY_NAME` and `_dependencies_from_package_json`),
    tools/memory_regen/package_facts.py (`build_facts(repo_root=...)`; never use its default-root CLI),
    tools/harness_config/loader.py (`load_project(path)` and `conventions_for(path,cfg,facts)`).
  </read_first>
  <action>
    Under D-12, run the read-only package-facts and convention observations even if apply failed.
    Record argv/cwd and invoke exactly
    `uv run python -c "import json; from pathlib import Path; from tools.memory_regen.package_facts import build_facts; target=Path('/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline'); print(json.dumps({'repo_root':str(target), **build_facts(repo_root=target)}, indent=2, sort_keys=True))"`,
    writing stdout to `downstream/package-facts.json` and capturing stderr and exit code separately.
    Never run the bare `python -m tools.memory_regen.package_facts` CLI, because it silently
    observes lifetimeworkflow.

    Independently invoke `uv run python -c` with a literal
    `target=Path('/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline')`,
    `facts=build_facts(repo_root=target)`, and
    `cfg=load_project(target/'harness'/'project.toml') if that file exists else load_project()` to
    build the same target-explicit facts, choose
    `/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline/harness/project.toml` only if apply created it (otherwise load this harness's
    `harness/project.toml`), call `conventions_for(pkg['dir'], cfg=cfg, facts=facts)` for every
    observed package, and emit one JSON document containing `repo_root`, `config_source`, and the
    per-package results to `downstream/conventions.json`. Capture stderr/exit. The output must name
    FeedbackOps manifests/package ids; output naming `libs/python` or `examples/log-parser` as the
    observed package set is an OBS-D target-anchoring defect.

    Before commit, run the high-confidence secret-pattern scan over both JSON documents and their
    argv/cwd/stderr/exit-code sidecars; quarantine and stop on any match.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; if rg -n -I -e '(AKIA[0-9A-Z]{16}|gh[pous]_[A-Za-z0-9_-]+|github_pat_[A-Za-z0-9_-]+|(^|[^A-Za-z0-9])sk-[A-Za-z0-9]{20,}|xox[bp]-[A-Za-z0-9-]+|-----BEGIN [A-Z ]*PRIVATE KEY-----|Authorization:[[:space:]]*Bearer[[:space:]]+[^[:space:]]+)' .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.json .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.argv.txt .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.cwd.txt .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.stderr.txt .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.exit-code.txt .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.json .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.argv.txt .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.cwd.txt .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.stderr.txt .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.exit-code.txt; then exit 1; else scan_rc=$?; test "$scan_rc" -eq 1 || exit 1; fi; for stem in package-facts conventions; do test -s ".planning/phases/51-real-target-observation-baseline/evidence/downstream/$stem.json" &amp;&amp; uv run python -m json.tool ".planning/phases/51-real-target-observation-baseline/evidence/downstream/$stem.json" &gt;/dev/null || exit 1; test -s ".planning/phases/51-real-target-observation-baseline/evidence/downstream/$stem.argv.txt" &amp;&amp; test -s ".planning/phases/51-real-target-observation-baseline/evidence/downstream/$stem.cwd.txt" &amp;&amp; test -f ".planning/phases/51-real-target-observation-baseline/evidence/downstream/$stem.stderr.txt" &amp;&amp; grep -Eq '^[0-9]+$' ".planning/phases/51-real-target-observation-baseline/evidence/downstream/$stem.exit-code.txt" || exit 1; done; grep -Eq '"manifest": *"apps/backend/package.json"' .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.json || exit 1; grep -Eq '"package": *"@fops/backend"' .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.json || exit 1; for bad_anchor in examples/log-parser libs/python; do grep -q "$bad_anchor" .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.json &amp;&amp; exit 1 || true; grep -q "$bad_anchor" .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.json &amp;&amp; exit 1 || true; done</automated>
  </verify>
  <acceptance_criteria>
    Both downstream commands have literal target-explicit argv, cwd, integer exit, captured stderr,
    and non-empty parseable JSON. The package-facts document carries the explicit FeedbackOps
    `repo_root`; neither output substitutes this harness's `examples/log-parser` package set. The
    pre-commit secret scan returns zero matches and both source guards remain empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Build comparisons and complete the original-checkout after proof</name>
  <files>.planning/phases/51-real-target-observation-baseline/evidence/downstream/member-comparison.json, .planning/phases/51-real-target-observation-baseline/evidence/downstream/workspace-edge-comparison.json, .planning/phases/51-real-target-observation-baseline/evidence/downstream/convention-comparison.json, .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.*, .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/discover/inventory.json (if produced),
    .planning/phases/51-real-target-observation-baseline/evidence/downstream/package-facts.json,
    .planning/phases/51-real-target-observation-baseline/evidence/downstream/conventions.json,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.head.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.index.sha256,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.untracked-set.sha256,
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-03 and D-14..D-16),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (Pitfalls 2..5),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (OBS-03 and after-proof checks),
    .planning/ROADMAP.md (SC-1..SC-4),
    .planning/REQUIREMENTS.md (OBS-01/OBS-03; RTA-02..04 observation-only),
    tools/adoption_scan/detect.py (`_MANIFEST_KIND_BY_NAME` and `_dependencies_from_package_json`),
    tools/memory_regen/package_facts.py (`build_facts(repo_root=...)` output shape),
    tools/harness_config/loader.py (`conventions_for` output shape).
  </read_first>
  <action>

    Produce three falsifiable comparison JSON documents: (1) write the hardcoded paths under
    `expected_manifests` before the separate `discover_observed_manifests` and
    `package_facts_observed_manifests` arrays:
    `package.json`, `packages/ui/package.json`, `packages/shared/package.json`,
    `apps/frontend/package.json`, `apps/backend/package.json` versus both the discover inventory's
    observed manifest paths and package facts' observed manifest paths, with separate
    missing/unexpected arrays. If discover produced no inventory, set discover status to `blocked`,
    reserve that discover block as its own `OBS-D-NN`, and explicitly degrade the member comparison
    to package-facts-only rather than treating the absent discover set as empty success. (2) Compare
    the actual `from`/`to`/`kind` tuples involving `@fops/shared`. Write the following exact tuples
    under `expected_edges` before a separate `observed_edges` array:
    `{"from":"@fops/frontend","to":"@fops/shared","kind":"runtime"}` and
    `{"from":"@fops/backend","to":"@fops/shared","kind":"runtime"}` and
    `{"from":"@fops/ui","to":"@fops/shared","kind":"runtime"}`. The third UI edge is part of
    the expected set and must not become a spurious observation; the OBS-03 verdict remains keyed
    only on the frontend/backend pair. Set OBS-03 result `refuted` when both deciding workspace
    edges are present, `confirmed` when deciding output shows a version-string treatment or either
    deciding edge missing, and `blocked` only when no deciding output exists. (3) Record each package's observed
    `test`/`format`/`bash_scope` values and whether lint/test commands are missing. Keep member
    discovery and dependency-edge observations under separate OBS-D ids even when one masks the
    other.

    Finally rerun the four original-checkout D-03 commands from Plan 01 into `after.*`, plus
    `worktree list --porcelain`. Write `isolation/comparison.json` with explicit equality booleans
    for status, HEAD, index digest, and untracked-set digest and the before/after values or file
    references. Any false value is an OBS-D defect; do not restore or retry it away. Before commit,
    run the high-confidence secret-pattern scan over every downstream capture and comparison plus
    the after-proof captures; quarantine and stop on any match.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; if rg -n -I -e '(AKIA[0-9A-Z]{16}|gh[pous]_[A-Za-z0-9_-]+|github_pat_[A-Za-z0-9_-]+|(^|[^A-Za-z0-9])sk-[A-Za-z0-9]{20,}|xox[bp]-[A-Za-z0-9-]+|-----BEGIN [A-Z ]*PRIVATE KEY-----|Authorization:[[:space:]]*Bearer[[:space:]]+[^[:space:]]+)' .planning/phases/51-real-target-observation-baseline/evidence/downstream .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.status.txt .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.head.txt .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.index.sha256 .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.untracked-set.sha256 .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.worktree-list.txt; then exit 1; else scan_rc=$?; test "$scan_rc" -eq 1 || exit 1; fi; test -f .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.status.txt || exit 1; test -s .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.head.txt &amp;&amp; grep -Eq '^[0-9a-f]{40}$' .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.head.txt || exit 1; for f in .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.index.sha256 .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.untracked-set.sha256; do test -s "$f" &amp;&amp; grep -Eq '^[0-9a-f]{64}([[:space:]]|$)' "$f" || exit 1; done; test -s .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.worktree-list.txt &amp;&amp; grep -q '^worktree ' .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.worktree-list.txt || exit 1; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json &gt;/dev/null &amp;&amp; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/downstream/member-comparison.json &gt;/dev/null &amp;&amp; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/downstream/workspace-edge-comparison.json &gt;/dev/null &amp;&amp; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/downstream/convention-comparison.json &gt;/dev/null || exit 1; grep -q '"observed_edges"' .planning/phases/51-real-target-observation-baseline/evidence/downstream/workspace-edge-comparison.json || exit 1; grep -q '"discover_observed_manifests"' .planning/phases/51-real-target-observation-baseline/evidence/downstream/member-comparison.json || exit 1; edge_expected=$(mktemp); member_expected=$(mktemp); trap 'rm -f "$edge_expected" "$member_expected"' EXIT; awk '/"expected_edges"/{inside=1} /"observed_edges"/{inside=0} inside' .planning/phases/51-real-target-observation-baseline/evidence/downstream/workspace-edge-comparison.json &gt; "$edge_expected"; awk '/"expected_manifests"/{inside=1} /"discover_observed_manifests"/{inside=0} inside' .planning/phases/51-real-target-observation-baseline/evidence/downstream/member-comparison.json &gt; "$member_expected"; grep -Eq '"result": *"(confirmed|refuted|blocked)"' .planning/phases/51-real-target-observation-baseline/evidence/downstream/workspace-edge-comparison.json &amp;&amp; grep -q '@fops/backend' "$edge_expected" &amp;&amp; grep -q '@fops/frontend' "$edge_expected" &amp;&amp; grep -q '@fops/ui' "$edge_expected" || exit 1; for member in package.json packages/ui/package.json packages/shared/package.json apps/frontend/package.json apps/backend/package.json; do grep -Fq '"'$member'"' "$member_expected" || exit 1; done; grep -Eq '"discover_status": *"(observed|blocked)"' .planning/phases/51-real-target-observation-baseline/evidence/downstream/member-comparison.json || exit 1; for key in test format bash_scope; do grep -Eq '"'$key'"[[:space:]]*:' .planning/phases/51-real-target-observation-baseline/evidence/downstream/convention-comparison.json || exit 1; done; for key in status_equal head_equal index_equal untracked_set_equal; do grep -Eq '"'$key'": *(true|false)' .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    Each comparison JSON contains observed values,
    expected values, missing/unexpected arrays (where applicable), and a machine-readable result;
    changing/removing an expected member or edge changes that result, proving the check can fail.
    The hardcoded expected edge set contains frontend→shared, backend→shared, and UI→shared; the
    OBS-03 result is calculated from frontend/backend only. If discover inventory is absent,
    `member-comparison.json` says `discover_status: blocked`, names its separate OBS-D id, and uses
    the package-facts-only degraded form.
    `workspace-edge-comparison.json` never infers from code alone and never treats `refuted` as a
    failure. `isolation/comparison.json` compares every D-03 dimension explicitly. Both
    no-source-edit guards remain empty and the pre-commit secret scan returns zero matches.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped adoption CLI → isolated worktree | Apply can write harness artifacts only inside the detached target; the original checkout and harness source are outside the allowed write root. |
| drafted manifest → constitution plane | Manifest data may name gated destinations; the unmodified apply choke point must refuse them before write. |
| FeedbackOps files/output → committed evidence | Scanner exclusions and capture discipline must prevent secret/private file contents from entering evidence. |
| evidence comparisons → phase claims | A target-defaulted library call or incomplete digest could produce a green but irrelevant proof. |

## STRIDE Threat Register (ASVS L1, enforcement on)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-06 | Tampering | apply target and manifest destinations | high | block/observe | Human checkpoint, exact detached path, unchanged manifest, pre/post changed-path comparison; unexpected/product paths become a high-severity OBS-D and no retry occurs. |
| T-51-07 | Elevation of Privilege | constitution-plane destinations | high | mitigate | Preserve them in the real manifest and rely on `refuse_unsafe_destination`/`refuse_if_constitution`; capture refused output. Any actual constitution write is a blocking defect. |
| T-51-08 | Information Disclosure | stdout/stderr/inventory/facts | high | block | Capture only pipeline artifacts and derived package metadata; never copy target source contents. Before commit, inspect captures for secret-shaped values; quarantine and stop for human handling on any high-confidence hit. |
| T-51-09 | Repudiation | wrong-root downstream calls | high | mitigate | `build_facts(repo_root=Path('/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline'))` and explicit `cfg`/`facts`; compare exact FeedbackOps manifests and reject lifetimeworkflow package ids as a target-anchoring defect. |
| T-51-10 | Repudiation | narrow isolation proof | high | mitigate | Re-run and explicitly compare status, HEAD, index digest, and untracked path-set digest; retain worktree-list metadata disclosure. |
| T-51-11 | Tampering | source/constitution lanes | high | block | Empty status plus baseline-to-current diff under `tools/`, `harness/`, `contracts/`, `docs/adr/` before every task commit. No repair, fixture, command, schema, gate, or CI job is permitted. |
</threat_model>

<verification>
All three adoption stages are represented by a complete command record or an explicit blocked
record, without requiring success. Downstream captures are target-explicit and comparisons are
data-bearing/falsifiable. The original checkout proof has explicit equality results. Source lanes
remain unchanged.
</verification>

<success_criteria>
SC-1 raw execution evidence and SC-3 deciding evidence exist. Discover/draft/apply failure,
partial application, a missing convention profile, and either OBS-03 verdict are all representable
without repair. The member and dependency observations remain separate, and no product/source or
constitution write is accepted silently.
</success_criteria>

<output>
Create `.planning/phases/51-real-target-observation-baseline/51-02-SUMMARY.md` when executed.
</output>
