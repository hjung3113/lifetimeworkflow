---
phase: 51-real-target-observation-baseline
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/argv.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/cwd.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/stdout.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/stderr.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/exit-code.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.worktree-list.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.original-status.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.head.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.index.sha256
  - .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.untracked-set.sha256
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json
autonomous: false
requirements: [OBS-01, OBS-03]

must_haves:
  truths:
    - "51-BASELINE-EVIDENCE.md is a replayable record whose summary ids and detail ids match 1:1 and whose every OBS-D-NN detail has exactly the five locked fields. (D-06, D-07, D-13)"
    - "The OBS-03 section quotes literal deciding output and accepts confirmed or refuted equally; member discovery and dependency-edge recording keep distinct stable ids. (D-14, D-15, D-16)"
    - "Purpose tags are explicitly proposals for Phase 52, never binding triage or repair authorization. (D-08)"
    - "The record states the isolation proof's real scope, including the expected git worktree metadata mutation, and reports every before/after comparison without hiding inequality."
    - "The disposable worktree is removed only after the evidence record is complete and only after a human checkpoint; failure is captured and never followed by a broader deletion. (D-04)"
    - "No secret/private target content is committed, no repair precedes the record, and all changed paths introduced by this phase stay below the Phase-51 directory; no schema, parser, contract entry, or fixture is added. (D-09, D-17)"
  artifacts:
    - path: ".planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md"
      provides: "OBS-01 defect record, reproducibility header, isolation proof, and OBS-03 verdict"
      contains: "## OBS-03 verdict"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/disposal/exit-code.txt"
      provides: "Falsifiable worktree disposal result"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/disposal/final.worktree-list.txt"
      provides: "Proof that no Phase-51 worktree registration remains when disposal succeeds"
  key_links:
    - from: "51-BASELINE-EVIDENCE.md summary table"
      to: "51-BASELINE-EVIDENCE.md ### OBS-D-NN sections"
      via: "exact id-set equality with no orphan or duplicate ids"
      pattern: "OBS-D-[0-9]{2}"
    - from: "51-BASELINE-EVIDENCE.md ## OBS-03 verdict"
      to: "evidence/downstream/workspace-edge-comparison.json"
      via: "literal quoted excerpt plus detect.py/package_facts.py path:line"
      pattern: "confirmed|refuted"
    - from: "evidence/disposal/argv.txt"
      to: "evidence/disposal/final.worktree-list.txt"
      via: "exact-path git worktree remove --force followed by list verification"
      pattern: "v27-51-baseline"
---

<objective>
Turn the raw Phase-51 captures into the stable OBS-01/OBS-03 evidence contract, perform a
security/internal-consistency review, then obtain human confirmation to dispose of the isolated
worktree and finalize the phase gate.

Purpose: SC-2 and SC-3 make this record the sole Phase-52 planning input; SC-4 forbids any repair
before it is complete. D-04 requires the observed worktree state to be discarded so Phase 52 starts
fresh.

Output: `51-BASELINE-EVIDENCE.md`, complete disposal captures, and a verified no-source-edit,
no-leftover-worktree closeout.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/51-real-target-observation-baseline/51-CONTEXT.md
@.planning/phases/51-real-target-observation-baseline/51-RESEARCH.md
@.planning/phases/51-real-target-observation-baseline/51-VALIDATION.md
@.planning/phases/51-real-target-observation-baseline/51-01-SUMMARY.md
@.planning/phases/51-real-target-observation-baseline/51-02-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
</context>

<interfaces>
Each `### OBS-D-NN` section contains exactly these labeled fields and no substitute names:
`- **symptom:**`, `- **reproduction:**`, `- **code location:**`, `- **purpose tag:**`,
`- **proposed disposition:**`.
`purpose tag` is one of ①②③④ or `outside purpose` and is marked `PROPOSAL ONLY`.
`proposed disposition` is `repair-in-52` or `no-change-evidence-backed`.

The header records harness SHA, target SHA, live tool versions, and per-command cwd/literal argv/
exit code. The summary table indexes every stable id. `## OBS-03 verdict` states `confirmed` or
`refuted` from literal real-output evidence; if the deciding command was genuinely blocked, it
states the `blocked-by` id and does not falsely claim SC-3 complete.
</interfaces>

Secret-scan note: the `sk-` alternative is boundary-anchored and dash-free to avoid matching ordinary path substrings such as `task-request`.

<tasks>

<task type="auto">
  <name>Task 1: Author the complete baseline evidence record from raw captures</name>
  <files>.planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md (create if absent; on resume preserve assigned OBS-D ids and never renumber them),
    .planning/phases/51-real-target-observation-baseline/evidence/metadata/*,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/*,
    .planning/phases/51-real-target-observation-baseline/evidence/discover/*,
    .planning/phases/51-real-target-observation-baseline/evidence/draft/*,
    .planning/phases/51-real-target-observation-baseline/evidence/apply/*,
    .planning/phases/51-real-target-observation-baseline/evidence/downstream/*,
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-06..D-17 exact record contract),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (three predictions, code citations, replay argv),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (Nyquist map and manual-only checks),
    .planning/ROADMAP.md (all four Phase-51 success criteria),
    .planning/REQUIREMENTS.md (OBS-01/OBS-03; Phase-52-only requirements),
    .planning/PROJECT.md (the owner's four purposes ①②③④ and no-more-machinery boundary),
    .planning/STATE.md,
    AGENTS.md,
    harness/commands/adopt.md,
    harness/skills/brownfield-adoption/SKILL.md,
    harness/skills/two-plane-memory/SKILL.md,
    harness/skills/python-conventions/SKILL.md,
    tools/adoption_scan/cli.py,
    tools/adoption_scan/scan.py,
    tools/adoption_scan/detect.py,
    tools/adoption_scan/plan.py,
    tools/adoption_scan/destinations.py,
    tools/adoption_apply/cli.py,
    tools/adoption_apply/batch.py,
    tools/adoption_apply/apply.py,
    tools/memory_regen/package_facts.py,
    tools/harness_config/loader.py,
    harness/project.toml.
  </read_first>
  <action>
    Under D-06 and D-09, write the Markdown `51-BASELINE-EVIDENCE.md` plus committed raw JSON from
    captured values, never from the research predictions, without adding a schema, contract entry,
    or evidence parser. Under D-13, start with a reproducibility header containing full harness/target SHA, actual git/uv/Python/
    pnpm versions, exact target/worktree paths, and a command table with each stage's literal cwd,
    argv, exit code, stdout/stderr paths, and produced/missing artifacts. State that nonzero and
    blocked stages are valid observations. State the proof scope: original working tree status,
    HEAD, index listing digest, and untracked path-set digest; untracked content and Git
    administrative metadata are out of the byte comparison, and `.git/worktrees/` changes during
    provisioning are disclosed.

    Add the exact heading `## Observation summary`, its summary table, then the distinct heading
    `## Observations` before one stable
    `### OBS-D-NN` section for every actual defect, block, or
    evidence-backed no-change observation that Phase 52 must trace. Each section has exactly the
    five D-07 fields. `reproduction` includes literal `uv run python -m ...` or target-explicit
    `uv run python -c ...`, cwd, both SHAs, and exit code. `code location` is a resolvable
    `path:line` into this unchanged checkout. Under D-08, mark each purpose tag `PROPOSAL ONLY` and
    never design a fix. Reserve ids once in discovery order and never compress gaps or renumber on
    later edits. Under D-17, describe a useful fixture shape in the record only; add no fixture in
    Phase 51.

    Under D-16, give pnpm member discovery and dependency-edge recording separate ids even when both are
    no-change confirmations or one masks the other. For member discovery, quote the observed
    manifest set and cite `tools/adoption_scan/detect.py:46-50` plus the actual detection path. For
    dependency edges, quote the literal `workspace-edge-comparison.json`/`package-facts.json`
    tuple(s) and cite `tools/adoption_scan/detect.py:273-284` and
    `tools/memory_regen/package_facts.py:216-245`. Record the convention result separately, citing
    `harness/project.toml:26-45` and `tools/harness_config/loader.py:297-357` when the captured
    values show the predicted language-row gap.

    Under D-15, add `## OBS-03 verdict` with the literal deciding output excerpt in a Markdown quote
    and the implicated path:line citations. Under D-14, state `refuted` if both real `@fops/shared` workspace edges are
    present, or `confirmed` if deciding output shows version-string treatment or either real edge
    absent. Treat both as PASS. If no deciding output exists, record `blocked-by: OBS-D-NN` and do
    not claim SC-3 met. Add the explicit original-checkout comparison result, worktree changed-path
    comparison, and a pre-disposal note saying the record is complete and the worktree will now be
    discarded under D-04.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; record=.planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md; test -s "$record" &amp;&amp; grep -q '^## OBS-03 verdict$' "$record" &amp;&amp; grep -q 'PROPOSAL ONLY' "$record" &amp;&amp; grep -q '\.git/worktrees/' "$record" || exit 1; check_dir=$(mktemp -d); trap 'rm -rf "$check_dir"' EXIT; awk '/^## Observation summary$/{inside=1; next} /^## /{if(inside) exit} inside' "$record" | grep -Eo '^\|[[:space:]]*OBS-D-[0-9]{2}' | grep -Eo 'OBS-D-[0-9]{2}' &gt; "$check_dir/summary.raw"; grep -Eo '^### OBS-D-[0-9]{2}' "$record" | sed 's/^### //' &gt; "$check_dir/detail.raw"; test -s "$check_dir/summary.raw" &amp;&amp; test -s "$check_dir/detail.raw" || exit 1; sort "$check_dir/summary.raw" | uniq -d &gt; "$check_dir/summary.dupes"; sort "$check_dir/detail.raw" | uniq -d &gt; "$check_dir/detail.dupes"; test ! -s "$check_dir/summary.dupes" &amp;&amp; test ! -s "$check_dir/detail.dupes" || exit 1; sort -u "$check_dir/summary.raw" &gt; "$check_dir/summary.ids"; sort -u "$check_dir/detail.raw" &gt; "$check_dir/detail.ids"; comm -3 "$check_dir/summary.ids" "$check_dir/detail.ids" &gt; "$check_dir/id.delta"; test ! -s "$check_dir/id.delta" || exit 1; awk '/^## OBS-03 verdict$/{inside=1; next} /^## /{if(inside) exit} inside' "$record" | grep -Eiq '(confirmed|refuted|blocked-by)' || exit 1; awk 'function complete(){return symptom==1 &amp;&amp; reproduction==1 &amp;&amp; code_location==1 &amp;&amp; purpose_tag==1 &amp;&amp; disposition==1} /^### OBS-D-[0-9][0-9]/{if(in_record &amp;&amp; !complete()) exit 1; in_record=1; symptom=reproduction=code_location=purpose_tag=disposition=0; next} in_record &amp;&amp; /^- \*\*symptom:\*\*/{symptom++} in_record &amp;&amp; /^- \*\*reproduction:\*\*/{reproduction++} in_record &amp;&amp; /^- \*\*code location:\*\*/{code_location++} in_record &amp;&amp; /^- \*\*purpose tag:\*\*/{purpose_tag++} in_record &amp;&amp; /^- \*\*proposed disposition:\*\*/{disposition++} /^## / &amp;&amp; in_record{if(!complete()) exit 1; in_record=0} END{if(in_record &amp;&amp; !complete()) exit 1}' "$record"</automated>
  </verify>
  <acceptance_criteria>
    The header copies both full SHAs and live versions from raw captures and indexes every command's
    cwd/argv/exit. The summary-table OBS-D id set equals the detail-heading id set 1:1 with no
    duplicate/orphan id; the automated per-section check confirms every detail has exactly one of
    each of the five locked, non-empty field labels, and manual inspection resolves each path:line.
    The OBS-03 section contains `confirmed`, `refuted`, or `blocked-by` and quotes a literal line/JSON
    excerpt whose source file exists and derives its verdict from that value, not the prediction.
    Member and edge observations have distinct ids. The isolation section includes every boolean
    from `comparison.json` and the Git metadata scope disclosure. Both no-source-edit guards are
    empty before the task commit.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve exact Phase-51 worktree disposal</name>
  <files>none</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md (must already be complete),
    .planning/phases/51-real-target-observation-baseline/evidence/metadata/target.develop.sha.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/provisioned.worktree-list.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.worktree-list.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/disposal/argv.txt (create if absent; read first on resume),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-04 disposal),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (step 7 exact removal scope),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (phase-gate no-leftover check),
    .planning/ROADMAP.md (SC-1 discard requirement),
    .planning/REQUIREMENTS.md (OBS-01; Phase 52 starts fresh),
    AGENTS.md.
  </read_first>
  <decision>Approve removal of exactly the registered Phase-51 worktree.</decision>
  <context>
    Approval authorizes Task 3—not this checkpoint—to run one exact `git worktree remove --force`
    command. The evidence record must already be complete, and no other worktree or path is in
    scope.
  </context>
  <options>
    <option id="approve"><name>Approve disposal</name><pros>Discards the observed worktree so Phase 52 starts fresh.</pros><cons>Irreversibly removes the disposable worktree contents.</cons></option>
  </options>
  <action>
    Present only this approval. Run no Git command, write no sidecar, and make no task commit.
    Resume into Task 3 only on `approve`; otherwise stop before accepting this task.
  </action>
  <resume-signal>Select: approve</resume-signal>
  <acceptance_criteria>
    A human selected `approve`; this checkpoint itself changed no file.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Dispose of the exact worktree and re-run the full D-03 proof</name>
  <files>.planning/phases/51-real-target-observation-baseline/evidence/disposal/*, .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md (must already be complete),
    .planning/phases/51-real-target-observation-baseline/evidence/metadata/target.develop.sha.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/provisioned.worktree-list.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/after.worktree-list.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json,
    .planning/phases/51-real-target-observation-baseline/evidence/disposal/argv.txt (create if absent; read first on resume),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-03 and D-04 disposal),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (step 7 exact removal scope),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (phase-gate no-leftover check),
    .planning/ROADMAP.md (SC-1 discard requirement),
    .planning/REQUIREMENTS.md (OBS-01; Phase 52 starts fresh),
    AGENTS.md.
  </read_first>
  <action>
    Require Task 2's `approve` decision. Resolve the registered worktree entry and require it to equal exactly
    `/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline`, remain detached, and have
    HEAD equal to `target.develop.sha.txt`. Require the complete evidence record and raw captures to
    exist. Do not use a glob and do not remove any other worktree.

    Under D-04, only after the evidence record is complete, record literal argv/cwd and run exactly
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree remove --force /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline`.
    Capture stdout, stderr, and integer exit code. Regardless of result, capture the original repo's
    final `git worktree list --porcelain` and re-run all four D-03 dimensions exactly:
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps status --porcelain=v2 --untracked-files=all`,
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps rev-parse HEAD`,
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps ls-files -s | sha256sum`, and
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps ls-files --others --exclude-standard -z | LC_ALL=C sort -z | sha256sum` into
    `final.original-status.txt`, `final.head.txt`, `final.index.sha256`, and
    `final.untracked-set.sha256`. Extend `evidence/isolation/comparison.json` with a
    `post_disposal` object containing explicit `status_equal`, `head_equal`, `index_equal`, and
    `untracked_set_equal` booleans comparing the final captures to the original `before.*` files.
    If removal fails or the entry/path remains, record that fact; do not escalate to `rm -rf`,
    `worktree prune`, or a broader cleanup.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; for f in argv cwd; do test -s ".planning/phases/51-real-target-observation-baseline/evidence/disposal/$f.txt" || exit 1; done; for f in stdout stderr final.worktree-list final.original-status; do test -f ".planning/phases/51-real-target-observation-baseline/evidence/disposal/$f.txt" || exit 1; done; grep -Eq '^[0-9]+$' .planning/phases/51-real-target-observation-baseline/evidence/disposal/exit-code.txt || exit 1; grep -Fq 'git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree remove --force /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline' .planning/phases/51-real-target-observation-baseline/evidence/disposal/argv.txt || exit 1; test -s .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.head.txt &amp;&amp; grep -Eq '^[0-9a-f]{40}$' .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.head.txt || exit 1; for f in .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.index.sha256 .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.untracked-set.sha256; do test -s "$f" &amp;&amp; grep -Eq '^[0-9a-f]{64}([[:space:]]|$)' "$f" || exit 1; done; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json &gt;/dev/null || exit 1; uv run python -c "import json,sys; p=json.load(open('.planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json')).get('post_disposal'); keys=('status_equal','head_equal','index_equal','untracked_set_equal'); sys.exit(0 if isinstance(p,dict) and all(isinstance(p.get(k),bool) for k in keys) else 1)" || exit 1; test "$(cat .planning/phases/51-real-target-observation-baseline/evidence/disposal/exit-code.txt)" -ne 0 || { test ! -e /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline &amp;&amp; ! grep -Fq '/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline' .planning/phases/51-real-target-observation-baseline/evidence/disposal/final.worktree-list.txt; } || exit 1; test "$(cat .planning/phases/51-real-target-observation-baseline/evidence/disposal/exit-code.txt)" -eq 0 || test -s .planning/phases/51-real-target-observation-baseline/evidence/disposal/stderr.txt</automated>
  </verify>
  <acceptance_criteria>
    All five disposal sidecars exist and argv names one exact absolute worktree path. On exit 0,
    the path is absent on disk and absent from `final.worktree-list.txt`; on nonzero exit, stderr is
    non-empty, the residual entry/path is recorded as an OBS-D defect, and no broader deletion was
    attempted. All four final D-03 files contain the expected value shape, and `comparison.json`'s
    `post_disposal` booleans compare them with `before.*`; any false value is an observation rather
    than a retry. Both no-source-edit guards remain empty before the task commit.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Finalize disposal status, evidence safety, and the phase gate</name>
  <files>.planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md,
    .planning/phases/51-real-target-observation-baseline/evidence/disposal/*,
    .planning/phases/51-real-target-observation-baseline/evidence/downstream/workspace-edge-comparison.json,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/comparison.json,
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-04, D-06..D-17),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (Security Domain and validation architecture),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (all sign-off checks),
    .planning/ROADMAP.md (SC-1..SC-4),
    .planning/REQUIREMENTS.md (OBS-01, OBS-03, no-growth boundary),
    AGENTS.md,
    tools/adoption_scan/scan.py (secret-path/content exclusion guarantees),
    tools/adoption_apply/apply.py (constitution and confinement controls),
    tools/memory_regen/package_facts.py,
    tools/harness_config/loader.py.
  </read_first>
  <action>
    Add the literal disposal command result and final worktree-list excerpt to the evidence record.
    If disposal failed, add/update its stable OBS-D record and state that SC-1 remains incomplete;
    never report a clean closeout. Recheck summary/detail id equality after this addition.

    Review every committed-intent evidence file for secret-shaped target content (private keys,
    bearer tokens, GitHub/OpenAI/Slack token forms, and credential assignment patterns). The
    scanner's excluded records may retain path/size/reason but never secret excerpts. If a
    high-confidence secret/private content value appears in stdout/stderr or copied JSON, stop
    before commit, quarantine the affected generated capture outside the repo, and request human
    handling; do not publish or quote it. This safety stop may block D-06 completion and must be
    reported honestly.

    Run the no-source-edit proof from the recorded harness SHA through current HEAD and working
    tree for `tools/`, `harness/`, `contracts/`, and `docs/adr/`; require empty. Compare the
    phase-start status with current status so the pre-existing `.planning/config.json` change is
    preserved, and require every new Phase-51 path to be below
    `.planning/phases/51-real-target-observation-baseline/`. Run `uv run --frozen pytest`; because this
    phase changed no code, any regression is reported rather than repaired and leaves this phase
    gate incomplete. Do not run the adoption pipeline again.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; if rg -n -I -e '(AKIA[0-9A-Z]{16}|gh[pous]_[A-Za-z0-9_-]+|github_pat_[A-Za-z0-9_-]+|(^|[^A-Za-z0-9])sk-[A-Za-z0-9]{20,}|xox[bp]-[A-Za-z0-9-]+|-----BEGIN [A-Z ]*PRIVATE KEY-----|Authorization:[[:space:]]*Bearer[[:space:]]+[^[:space:]]+)' .planning/phases/51-real-target-observation-baseline/evidence .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md; then exit 1; else scan_rc=$?; test "$scan_rc" -eq 1 || exit 1; fi; record=.planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md; check_dir=$(mktemp -d); trap 'rm -rf "$check_dir"' EXIT; awk '/^## Observation summary$/{inside=1; next} /^## /{if(inside) exit} inside' "$record" | grep -Eo '^\|[[:space:]]*OBS-D-[0-9]{2}' | grep -Eo 'OBS-D-[0-9]{2}' &gt; "$check_dir/summary.raw"; grep -Eo '^### OBS-D-[0-9]{2}' "$record" | sed 's/^### //' &gt; "$check_dir/detail.raw"; test -s "$check_dir/summary.raw" &amp;&amp; test -s "$check_dir/detail.raw" || exit 1; sort "$check_dir/summary.raw" | uniq -d &gt; "$check_dir/summary.dupes"; sort "$check_dir/detail.raw" | uniq -d &gt; "$check_dir/detail.dupes"; test ! -s "$check_dir/summary.dupes" &amp;&amp; test ! -s "$check_dir/detail.dupes" || exit 1; sort -u "$check_dir/summary.raw" &gt; "$check_dir/summary.ids"; sort -u "$check_dir/detail.raw" &gt; "$check_dir/detail.ids"; comm -3 "$check_dir/summary.ids" "$check_dir/detail.ids" &gt; "$check_dir/id.delta"; test ! -s "$check_dir/id.delta" || exit 1; awk '/^## OBS-03 verdict$/{inside=1; next} /^## /{if(inside) exit} inside' "$record" | grep -Eiq '(confirmed|refuted|blocked-by)' || exit 1; awk 'function complete(){return symptom==1 &amp;&amp; reproduction==1 &amp;&amp; code_location==1 &amp;&amp; purpose_tag==1 &amp;&amp; disposition==1} /^### OBS-D-[0-9][0-9]/{if(in_record &amp;&amp; !complete()) exit 1; in_record=1; symptom=reproduction=code_location=purpose_tag=disposition=0; next} in_record &amp;&amp; /^- \*\*symptom:\*\*/{symptom++} in_record &amp;&amp; /^- \*\*reproduction:\*\*/{reproduction++} in_record &amp;&amp; /^- \*\*code location:\*\*/{code_location++} in_record &amp;&amp; /^- \*\*purpose tag:\*\*/{purpose_tag++} in_record &amp;&amp; /^- \*\*proposed disposition:\*\*/{disposition++} /^## / &amp;&amp; in_record{if(!complete()) exit 1; in_record=0} END{if(in_record &amp;&amp; !complete()) exit 1}' "$record" || exit 1; uv run --frozen pytest</automated>
  </verify>
  <acceptance_criteria>
    The record contains disposal argv/exit/final-list evidence and does not claim disposal when the
    worktree remains. Summary/detail OBS-D ids still match 1:1; every detail retains the five exact
    fields. `## OBS-03 verdict` contains `confirmed` or `refuted` plus literal evidence, or clearly
    marks blocked and SC-3 incomplete—never both. No high-confidence secret/private content is in
    committed-intent evidence. The baseline-to-current source diff and source status are empty,
    all newly introduced paths are phase-local, and `uv run --frozen pytest` exits 0 without
    modifying `uv.lock`. A nonzero suite result
    is reported without repair but does not satisfy this task's acceptance criteria.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw captures → committed evidence record | Only reproducible excerpts and metadata may cross; target secrets/private content must not. |
| evidence record → Phase 52 | Stable ids and proposal-only purpose tags constrain later repairs; the record must not smuggle in a design decision. |
| disposal command → external filesystem/Git metadata | One exact registered worktree may be removed; the original checkout and other worktrees must survive. |
| phase claims → isolation evidence | Closeout depends on all proof dimensions and actual worktree absence, not a narrow status-only check. |

## STRIDE Threat Register (ASVS L1, enforcement on)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-12 | Information Disclosure | raw stdout/stderr/JSON and quoted excerpts | high | block | Secret-pattern review before commit; excluded files never contribute content excerpts; quarantine and human stop on a high-confidence value. |
| T-51-13 | Tampering | original checkout or harness/source lanes | high | block | Baseline-to-current and working-tree guards; only phase-local new paths allowed. Any actual product/source or constitution write is a blocking OBS-D. |
| T-51-14 | Elevation of Privilege | constitution-plane apply result | high | block | Evidence must show refusal/no write; an actual gated-plane write is recorded as high severity and closeout cannot claim success. No repair or guard edit is allowed. |
| T-51-15 | Repudiation | summary ids/verdict/proof scope | high | mitigate | 1:1 id-set check, literal OBS-03 excerpt, four isolation equality fields, and `.git/worktrees/` scope disclosure. |
| T-51-16 | Tampering / Denial of Service | `worktree remove --force` | high | block | Human checkpoint, exact absolute path + SHA + detached-state validation; no glob, `rm -rf`, prune, or adjacent worktree operation. |
| T-51-17 | Elevation of Privilege | Phase-52 repair authority | high | mitigate | Purpose tags say PROPOSAL ONLY; dispositions are proposals, and this plan writes no design/code/contract/gate/fixture. |
</threat_model>

<verification>
`51-BASELINE-EVIDENCE.md` satisfies D-06..D-17 and indexes all observations 1:1. OBS-03 is decided
from a literal real-output excerpt when available. Disposal is captured and verified. Source lanes
are byte-unchanged relative to the recorded harness SHA, all new outputs are phase-local, and the
regression suite is green; an unchanged-code external failure is reported without repair and keeps
the phase gate open.
</verification>

<success_criteria>
SC-2 and SC-3 are embodied in the stable evidence record; SC-4 is proven by empty source diffs.
SC-1 is complete only when the original proof compares equal and the Phase-51 worktree is absent.
Neither a failed adoption stage nor a refuted OBS-03 hypothesis is treated as phase failure; an
incomplete proof, secret exposure, constitution write, or failed disposal is reported as a real
remaining limitation.
</success_criteria>

<output>
Create `.planning/phases/51-real-target-observation-baseline/51-03-SUMMARY.md` when executed.
</output>
