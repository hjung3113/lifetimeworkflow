---
phase: 51-real-target-observation-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/metadata/target.develop.sha.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/metadata/tool-versions.json
  - .planning/phases/51-real-target-observation-baseline/evidence/metadata/phase-start-status.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.exit-code.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.head.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.index.sha256
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.untracked-set.sha256
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.worktree-list.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.argv.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.cwd.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.stdout.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.stderr.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.exit-code.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/provisioned.worktree-list.txt
  - .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.head.txt
autonomous: false
requirements: [OBS-01]

must_haves:
  truths:
    - "The original FeedbackOps checkout is measured before any worktree operation with status, HEAD, tracked-index digest, and untracked-path-set digest; untracked file contents are deliberately not read or hashed. (D-03)"
    - "The observation target is a detached worktree outside the original checkout, pinned to the full develop SHA recorded at execution time. (D-01, D-02)"
    - "The evidence explicitly discloses that git worktree add changes the original repository's git-common-dir metadata under .git/worktrees even though the working-tree proof does not cover git metadata bytes."
    - "Provisioning failure is captured with argv, cwd, stdout, stderr, and integer exit code; it is never hidden by deleting or overwriting an existing path."
  artifacts:
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.txt"
      provides: "Pre-provision porcelain-v2 snapshot of the original checkout"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/isolation/before.index.sha256"
      provides: "Pre-provision digest of git ls-files -s for the original checkout"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.exit-code.txt"
      provides: "Falsifiable result of detached worktree provisioning"
    - path: ".planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.head.txt"
      provides: "Observed detached-worktree HEAD for comparison with target.develop.sha.txt"
  key_links:
    - from: "evidence/metadata/target.develop.sha.txt"
      to: "evidence/isolation/worktree.head.txt"
      via: "exact full-SHA equality after git worktree add --detach"
      pattern: "^[0-9a-f]{40}$"
    - from: "evidence/isolation/before.worktree-list.txt"
      to: "evidence/isolation/provisioned.worktree-list.txt"
      via: "explicit disclosure of the registered worktree and git-common-dir metadata effect"
      pattern: "v27-51-baseline"
---

<objective>
Establish the reproducible isolation boundary for Phase 51: capture the original FeedbackOps
checkout's pre-run proof and environment metadata, then—after a human confirms the external write—
create a detached worktree at `~/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline`, pinned to the
recorded `develop` SHA.

Purpose: SC-1 and D-01..D-05 require an honest baseline before any `/adopt` stage runs. The proof
must measure the original checkout while acknowledging the separate `.git/worktrees/` metadata
mutation made by Git itself.

Output: the complete pre-run proof, reproducibility metadata, and a verified detached worktree—or
a complete provisioning-failure capture that later stages can reference as blocked evidence.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/51-real-target-observation-baseline/51-CONTEXT.md
@.planning/phases/51-real-target-observation-baseline/51-RESEARCH.md
@.planning/phases/51-real-target-observation-baseline/51-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@AGENTS.md
</context>

<interfaces>
Original checkout: `/Users/hyojung/Desktop/2026/FeedbackOps`.
Isolated worktree: `/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline`.
Harness checkout/cwd: `/Users/hyojung/Desktop/2026/lifetimeworkflow`.

The D-03 proof has three dimensions: exact porcelain-v2 status bytes; exact HEAD plus a SHA-256
digest of `git ls-files -s`; and a SHA-256 digest of the sorted NUL-delimited untracked path set.
The last dimension hashes names only, never untracked content.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Capture phase metadata and the original-checkout before proof</name>
  <files>.planning/phases/51-real-target-observation-baseline/evidence/metadata/*, .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.*</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/metadata/phase-start-status.txt (create if absent; on resume read and preserve the original capture),
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.txt (create if absent; never overwrite a completed baseline without recording why),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-01..D-05 and D-13),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (Isolation proof and Standard Stack),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (SC-1 map and honest-proof manual check),
    .planning/ROADMAP.md (Phase 51 SC-1 and SC-4),
    .planning/REQUIREMENTS.md (OBS-01; RTA-01 is observation-only here),
    .planning/STATE.md (carried MONO-12 context),
    AGENTS.md (no constitution/derived writes and preserve unrelated changes),
    /Users/hyojung/Desktop/2026/FeedbackOps/AGENTS.md (if present; target-local rules—do not copy its prose into evidence).
  </read_first>
  <action>
    From `/Users/hyojung/Desktop/2026/lifetimeworkflow`, create only the evidence directories named
    in this plan. Record `git rev-parse HEAD` as `metadata/harness.sha.txt`, the full result of
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps rev-parse develop` as
    `metadata/target.develop.sha.txt`, and the existing harness `git status --short --branch` as
    `metadata/phase-start-status.txt` so the pre-existing `.planning/config.json` modification is
    never mistaken for Phase-51 output. Capture actual `git --version`, `uv --version`,
    `uv run python --version`, and `pnpm --version` into a small `tool-versions.json`; each value
    must come from live stdout and include its command argv, cwd, and integer exit-code fields.

    For the D-03 before-run proof, before any `git worktree add`, run against the ORIGINAL checkout exactly:
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps status --porcelain=v2 --untracked-files=all`,
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps rev-parse HEAD`,
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps ls-files -s | sha256sum`, and
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps ls-files --others --exclude-standard -z | LC_ALL=C sort -z | sha256sum`.
    Write their outputs to the four `before.*` files, record the status command's integer exit as
    `before.status.exit-code.txt`, and capture
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree list --porcelain` as
    `before.worktree-list.txt`. Do not read or hash untracked file contents. Validate every captured
    SHA as full 40-hex. The original checkout's current HEAD need not equal `develop`: record it as
    evidence, but always pin the worktree to the separately recorded full `develop` SHA and never
    substitute the scoping-time short SHA `1d1c8ed`.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; for f in .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt .planning/phases/51-real-target-observation-baseline/evidence/metadata/target.develop.sha.txt .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.head.txt; do test -s "$f" &amp;&amp; grep -Eq '^[0-9a-f]{40}$' "$f" || exit 1; done; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; for f in .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.index.sha256 .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.untracked-set.sha256; do test -s "$f" &amp;&amp; grep -Eq '^[0-9a-f]{64}([[:space:]]|$)' "$f" || exit 1; done; test -s .planning/phases/51-real-target-observation-baseline/evidence/metadata/phase-start-status.txt &amp;&amp; grep -q '^## ' .planning/phases/51-real-target-observation-baseline/evidence/metadata/phase-start-status.txt || exit 1; test -s .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.worktree-list.txt &amp;&amp; grep -q '^worktree ' .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.worktree-list.txt || exit 1; grep -Eq '^0$' .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.exit-code.txt || exit 1; test -f .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.txt &amp;&amp; cmp -s .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.status.txt &lt;(git -C /Users/hyojung/Desktop/2026/FeedbackOps status --porcelain=v2 --untracked-files=all) || exit 1; uv run python -m json.tool .planning/phases/51-real-target-observation-baseline/evidence/metadata/tool-versions.json &gt;/dev/null || exit 1; for key in git uv python pnpm; do grep -Eq '"'$key'": *"[^"]+"' .planning/phases/51-real-target-observation-baseline/evidence/metadata/tool-versions.json || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    `metadata/harness.sha.txt`, `metadata/target.develop.sha.txt`, and `isolation/before.head.txt`
    each contain exactly one full 40-hex SHA; `before.head.txt` may differ from the develop SHA and
    that difference does not block provisioning. The index and
    untracked-set digest files each contain exactly one `sha256sum` digest line. The recorded
    untracked command uses `git ls-files --others --exclude-standard -z` and therefore hashes path
    names, not contents. `git status --porcelain -- tools/ harness/ contracts/ docs/adr/` is empty,
    and `git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/`
    is empty before any task commit.
  </acceptance_criteria>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve detached external worktree provisioning</name>
  <files>none</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/metadata/target.develop.sha.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.worktree-list.txt,
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-01, D-02, D-04, D-05),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (Architecture diagram and worktree command),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (T-51-01 and honest scope),
    .planning/ROADMAP.md (Phase 51 SC-1),
    .planning/REQUIREMENTS.md (OBS-01 and Phase-52-only RTA-01 boundary),
    AGENTS.md,
    /Users/hyojung/Desktop/2026/FeedbackOps/AGENTS.md (if present).
  </read_first>
  <decision>Approve the exact external `git worktree add --detach` operation before any external write.</decision>
  <context>
    Approval permits Task 3—not this checkpoint—to populate exactly
    `/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline` and register Git metadata
    under the original repo's common directory. The original product checkout remains forbidden as
    an adoption target.
  </context>
  <options>
    <option id="approve"><name>Approve provisioning</name><pros>Allows the pinned detached baseline to proceed.</pros><cons>Creates the external directory and `.git/worktrees/` metadata.</cons></option>
  </options>
  <action>
    Present only this approval. Run no Git command and write no evidence file in this checkpoint.
    Resume into Task 3 only on `approve`; otherwise stop before accepting this task.
  </action>
  <resume-signal>Select: approve</resume-signal>
  <acceptance_criteria>
    A human selected `approve`; this checkpoint produced no filesystem change and no task commit.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Provision and capture the detached external worktree</name>
  <files>.planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.*, .planning/phases/51-real-target-observation-baseline/evidence/isolation/provisioned.worktree-list.txt, .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.head.txt</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/evidence/metadata/target.develop.sha.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/before.worktree-list.txt,
    .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.argv.txt (create if absent; read first on resume),
    .planning/phases/51-real-target-observation-baseline/51-CONTEXT.md (D-01, D-02, D-04, D-05),
    .planning/phases/51-real-target-observation-baseline/51-RESEARCH.md (Architecture diagram and worktree command),
    .planning/phases/51-real-target-observation-baseline/51-VALIDATION.md (T-51-01 and honest scope),
    .planning/ROADMAP.md (Phase 51 SC-1),
    .planning/REQUIREMENTS.md (OBS-01 and Phase-52-only RTA-01 boundary),
    AGENTS.md,
    /Users/hyojung/Desktop/2026/FeedbackOps/AGENTS.md (if present).
  </read_first>
  <action>
    Under D-01, require Task 2's `approve` decision and require the external destination path to be
    absent and absent from
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree list`; never remove, reuse, or
    overwrite a collision. Under D-02, use the separately recorded full develop SHA as the pinned
    replay commit. Record literal argv and cwd, then run
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree add --detach /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline FULL_SHA_FROM_TARGET_DEVELOP_SHA_TXT`.
    Capture stdout, stderr, and integer exit code even on failure. If exit is zero, capture
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline rev-parse HEAD`,
    `git -C ... symbolic-ref -q HEAD` exit status (nonzero is the detached-HEAD proof), and the
    original repo's post-provision `worktree list --porcelain`. Record explicitly that the
    `before/after` working-tree proof excludes Git's expected `.git/worktrees/` administrative
    metadata; do not falsely call the whole `.git` directory byte-unchanged.
  </action>
  <verify>
    <automated>git status --porcelain -- tools/ harness/ contracts/ docs/adr/ | grep -q . &amp;&amp; exit 1 || true; test -z "$(git diff --name-only "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/harness.sha.txt)" -- tools/ harness/ contracts/ docs/adr/)" || exit 1; for f in argv cwd; do test -s ".planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.$f.txt" || exit 1; done; for f in stdout stderr; do test -f ".planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.$f.txt" || exit 1; done; grep -Eq '^[0-9]+$' .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.exit-code.txt || exit 1; grep -q 'git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree add --detach /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline' .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.argv.txt || exit 1; test "$(cat .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.exit-code.txt)" -ne 0 || { test -s .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.head.txt &amp;&amp; grep -Eq '^[0-9a-f]{40}$' .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.head.txt &amp;&amp; test "$(cat .planning/phases/51-real-target-observation-baseline/evidence/isolation/worktree.head.txt)" = "$(cat .planning/phases/51-real-target-observation-baseline/evidence/metadata/target.develop.sha.txt)" &amp;&amp; ! git -C /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline symbolic-ref -q HEAD &gt;/dev/null 2&gt;&amp;1 &amp;&amp; grep -q '/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-51-baseline' .planning/phases/51-real-target-observation-baseline/evidence/isolation/provisioned.worktree-list.txt; } || exit 1; test "$(cat .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.exit-code.txt)" -eq 0 || test -s .planning/phases/51-real-target-observation-baseline/evidence/isolation/provision.stderr.txt</automated>
  </verify>
  <acceptance_criteria>
    The five `provision.{argv,cwd,stdout,stderr,exit-code}.txt` sidecars exist and exit-code parses as
    one integer. On exit 0, the registered path is outside the original checkout, worktree HEAD is
    byte-equal to the recorded develop SHA, and `symbolic-ref -q HEAD` proves detached state; on
    nonzero exit, stderr is non-empty and Plan 51-02 is explicitly blocked rather than pointed at
    the original checkout. The metadata-scope disclosure names `.git/worktrees/` and states that
    D-03 proves working tree/index/HEAD/untracked-path-set stability, not immutable Git
    administrative metadata. Both source guards from Task 1 remain empty before any task commit.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| original FeedbackOps checkout → external worktree | Git may write administrative metadata in the original repo while all target files must land only in the external worktree path. |
| harness checkout → phase evidence | This plan may write only below the Phase-51 directory; source and constitution planes are read-only. |
| target repository → committed evidence | Only Git state, hashes, tool versions, and fixed command output are captured; target file contents are not copied. |

## STRIDE Threat Register (ASVS L1, enforcement on)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Tampering | worktree path selection | high | block | Human checkpoint; require the exact external path absent, resolved outside both checkouts, and detached at the recorded full SHA. Never reuse or delete a collision. |
| T-51-02 | Elevation of Privilege | later adoption apply constitution destinations | high | block/observe | This plan does not run apply. It records the target boundary that Plan 02 must use; any attempt to substitute the original checkout is a hard stop. |
| T-51-03 | Information Disclosure | target data copied into evidence | high | mitigate | Capture no target content: status text, manifest-free Git metadata, and path-set digest only; untracked contents are deliberately not hashed. |
| T-51-04 | Repudiation | proof scoped narrowly enough to pass trivially | high | mitigate | Capture all three D-03 dimensions and both worktree lists; explicitly disclose the excluded `.git/worktrees/` metadata scope. |
| T-51-05 | Tampering | harness/source lanes | high | block | Require empty status and baseline-to-current diff under `tools/`, `harness/`, `contracts/`, and `docs/adr/` before every task commit. |
</threat_model>

<verification>
The pre-run proof artifacts exist and are internally consistent. Provisioning has a complete
five-sidecar command record. If provisioning succeeded, the worktree is detached at the exact
recorded SHA and registered outside the original checkout; if it failed, no later task substitutes
the original checkout. No source-lane diff exists.
</verification>

<success_criteria>
SC-1's isolation prerequisite is established honestly: the original checkout has a recorded
before snapshot, the external worktree is pinned and detached, and the Git metadata side effect is
disclosed. A captured provisioning failure is valid evidence but blocks Plan 51-02 from running
against any fallback target.
</success_criteria>

<output>
Create `.planning/phases/51-real-target-observation-baseline/51-01-SUMMARY.md` when executed.
</output>
