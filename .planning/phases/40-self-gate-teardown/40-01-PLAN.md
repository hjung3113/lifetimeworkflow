---
phase: 40-self-gate-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/skill_registry/registry.py
  - tools/skill_registry/__main__.py
  - tools/skill_registry/__init__.py
  - tools/skill_registry/pyproject.toml
  - tools/skill_registry/tests/conftest.py
  - tools/skill_registry/tests/test_skill_registry.py
  - harness/skills/registry.lock
  - tools/harness_lint/tests/test_skill_registry_lock.py
  - .github/workflows/ci.yml
  - uv.lock
autonomous: true
requirements: [CER-04]
must_haves:
  truths:
    - "No registry.lock exists anywhere in the tree and no tools/skill_registry/ directory exists"
    - "CI has no registry-lock job and gate.needs has no dangling registry-lock entry, with no other needs entry touched"
    - "uv run pytest is green with no collection error from a removed package"
    - "uv sync --all-packages resolves against the refreshed uv.lock"
    - "The emitted trees (.opencode/, .claude/) are unchanged by this deletion (emit-drift produces an empty diff)"
    - "The contract-drift gate and the ruff baseline ratchet stay clean"
  artifacts:
    - path: "tools/skill_registry"
      provides: "MUST NOT EXIST after this plan"
    - path: "harness/skills/registry.lock"
      provides: "MUST NOT EXIST after this plan"
    - path: "tools/harness_lint/tests/test_skill_registry_lock.py"
      provides: "MUST NOT EXIST after this plan"
    - path: ".github/workflows/ci.yml"
      provides: "no registry-lock job block, gate.needs has 12 entries (was 13), no other entry changed"
    - path: "uv.lock"
      provides: "no harness-skill-registry package block"
  key_links:
    - from: ".github/workflows/ci.yml gate job"
      to: "job graph"
      via: "needs: list"
      pattern: "needs:.*registry-lock"
      note: "this pattern MUST NOT match after the plan — it is an absence link, not a presence link"
---

<objective>
Delete the skill-registry self-gate in its entirety — the tool package
(`tools/skill_registry/`, 611 LOC), its declaration file (`harness/skills/registry.lock`), its
in-suite mirror test (`tools/harness_lint/tests/test_skill_registry_lock.py`), the CI job
(`registry-lock`), and the CI job's `gate.needs` entry — so that no later v2.5 phase (41, 43, 44)
can delete a skill and be blocked by a declaration *about* the skill tree.

Purpose: `registry.py:44,105-110` recomputes the skill surface from `harness/skills/**` and fails
on any diff against the committed lock. Phase 40 must land before Phase 41's first skill deletion
per ADR-0012 (`docs/adr/0012-ci-and-merge-as-decision-authority.md:96-97`) and milestone ordering
rule (1). This is a pure-deletion phase: nothing is created, no replacement gate is added (D-02).

Output: one atomic commit (D-01) containing every deletion below, `uv.lock` refreshed, all six
D-08 verification commands green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-self-gate-teardown/40-CONTEXT.md
@.planning/phases/40-self-gate-teardown/40-RESEARCH.md
@.planning/phases/40-self-gate-teardown/40-VALIDATION.md
@docs/adr/0012-ci-and-merge-as-decision-authority.md

<interfaces>
<!-- Exact current state of the two edit points in ci.yml, verbatim from the tree (2026-07-27). -->
<!-- Executor should use these directly — no need to re-locate line numbers before editing. -->

From `.github/workflows/ci.yml` (verified via `grep -n` this session — line numbers exact):
- Comment block + job to delete wholesale: lines 275-303 (comment `# ── registry-lock ──` at 275
  through the `--check` step ending the job body at 303).
- `gate.needs` line to edit: line 410, current exact content:
  `needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lint, lifecycle-eval, emit-drift, registry-lock, stale-derived, docs-guard, workspace]`
  target exact content (only `registry-lock` removed, no other token touched, comma/spacing
  otherwise identical):
  `needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lint, lifecycle-eval, emit-drift, stale-derived, docs-guard, workspace]`
- `registry-lock` has NO `needs:` clause of its own (verified: the only two `needs:` occurrences
  in the whole file are line 80 `lang-tests: needs: setup` and line 410 `gate`'s fan-in list) — so
  deleting it orphans nothing else.

From `uv.lock` (lines 195-198, verified exact):
```
[[package]]
name = "harness-skill-registry"
version = "0.0.0"
source = { virtual = "tools/skill_registry" }
```
This block disappears when `uv lock` is re-run after the directory is deleted. The entry is keyed
by package name `harness-skill-registry` (from `tools/skill_registry/pyproject.toml`'s
`[project].name`), not by a path string — grep for `harness-skill-registry` when checking `uv.lock`
directly.

From `pyproject.toml` (verified — NO edit needed to this file in this plan):
- Line 34: `members = ["libs/python", "tools/*"]` — a glob; deleting the directory removes the
  member automatically.
- Line 35: `exclude = ["tools/bootstrap"]` — unrelated, do not touch.
- Line 39: `testpaths = ["libs/python", "tools"]` — names the parent `tools` dir, not the deleted
  subdirectory; no edit needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight referent sweep (D-06/D-07) — record before any deletion</name>
  <files>docs/doc-dependencies.toml (read-only)</files>
  <read_first>
    .planning/phases/40-self-gate-teardown/40-CONTEXT.md (D-06, D-07)
    .planning/phases/40-self-gate-teardown/40-RESEARCH.md ("Removal-Safety Sweep" section)
    docs/doc-dependencies.toml
  </read_first>
  <action>
    Run and record the output of the opening grep sweep specified by D-06:
    `grep -rn "skill_registry\|registry-lock\|registry\.lock" tools/ libs/ harness/ .github/ docs/ pyproject.toml uv.lock .claude/ .opencode/ --exclude-dir=.git --exclude-dir=__pycache__ --exclude-dir=.claude/get-shit-done`.
    Confirm every hit falls into exactly one of these two categories, per RESEARCH.md's
    Removal-Safety Sweep table: (a) inside the deletion set enumerated in this plan's
    `files_modified`, or (b) one of the two documented exceptions —
    `docs/adr/0012-ci-and-merge-as-decision-authority.md:96-97` (ratified, append-only ADR, never
    edited) and `docs/explanation/agent-workflow-skillset-design-guide.md:564,595,663` (a
    different, unimplemented vendored-skill-provenance concept, D-05). If any hit falls outside
    both categories, STOP — do not proceed to Task 2 — the deletion set is incomplete and CONTEXT.md
    must be revisited.
    Separately, re-run D-07's check: read `docs/doc-dependencies.toml` in full and confirm none of
    its `[[binding]]` rows names `tools/skill_registry`, `harness/skills/registry.lock`, or
    `.github/workflows/ci.yml` as a `source`. This proves Phase 41's "unbind before delete" rule
    does not apply here and the `docs-guard` CI job stays green through this deletion.
    Do not delete anything in this task — it is verification-only, so Tasks 2-4 execute against a
    confirmed-safe referent set.
  </action>
  <acceptance_criteria>
    - The grep sweep from D-06 has been run and every hit classified into deletion-set or
      documented-exception; zero unclassified hits.
    - `docs/doc-dependencies.toml` has been read in full and confirmed to contain zero `[[binding]]`
      rows naming any of the three paths above as `source`.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "skill_registry\|registry-lock\|registry\.lock" tools/ libs/ harness/ .github/ docs/ pyproject.toml uv.lock .claude/ .opencode/ --exclude-dir=.git --exclude-dir=__pycache__ --exclude-dir=.claude/get-shit-done</automated>
  </verify>
  <done>Sweep output recorded; only deletion-set files and the two documented exceptions appear. No file outside the plan's `files_modified` list needs an edit.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the package, the lock, the mirror test, and the CI job (D-01, D-04, ordering rules 5/8)</name>
  <files>tools/skill_registry/registry.py, tools/skill_registry/__main__.py, tools/skill_registry/__init__.py, tools/skill_registry/pyproject.toml, tools/skill_registry/tests/conftest.py, tools/skill_registry/tests/test_skill_registry.py, harness/skills/registry.lock, tools/harness_lint/tests/test_skill_registry_lock.py, .github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (lines 240-303 and 395-412, already excerpted verbatim in this plan's context — read the live file before editing to confirm no drift since research)
    .planning/phases/40-self-gate-teardown/40-CONTEXT.md (D-01, D-04)
    tools/skill_registry/registry.py (confirm no other importer before deleting — RESEARCH.md already found none)
  </read_first>
  <action>
    Delete the following, in this order (RESEARCH.md's recommended ordering — safe because the
    final commit is atomic per D-01, but this order keeps local iteration failures loud and early):
    1. Delete the entire `tools/skill_registry/` directory: `registry.py`, `__main__.py`,
       `__init__.py`, `pyproject.toml`, `tests/conftest.py`, `tests/test_skill_registry.py`, and any
       `__pycache__/` beneath it.
    2. Delete `harness/skills/registry.lock`.
    3. Delete `tools/harness_lint/tests/test_skill_registry_lock.py`.
    4. Edit `.github/workflows/ci.yml`: remove lines 275-303 wholesale (the `# ── registry-lock ──`
       comment block through the job's closing `--check` step — remove the comment and the job body
       together, per RESEARCH.md's Open Question 1 recommendation, since the comment exclusively
       describes the job being deleted and a partial removal leaves dangling prose).
    5. In the same `ci.yml` edit, change line 410's `gate.needs` list from
       `needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lint, lifecycle-eval, emit-drift, registry-lock, stale-derived, docs-guard, workspace]`
       to
       `needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lint, lifecycle-eval, emit-drift, stale-derived, docs-guard, workspace]`
       — remove only the `registry-lock` token; do not add, reorder, or remove any other entry.
    Do NOT edit `tools/harness_lint/caps.py::EXPECTED_SKILLS` (D-03), do NOT edit
    `docs/explanation/agent-workflow-skillset-design-guide.md` (D-05), do NOT edit `pyproject.toml`
    (D-04 — the `members` glob and `testpaths` need no change), and do NOT hand-edit anything under
    `.claude/` or `.opencode/` (ordering rule 7).
  </action>
  <acceptance_criteria>
    - `test ! -e tools/skill_registry` exits 0 (directory absent).
    - `test ! -e harness/skills/registry.lock` exits 0.
    - `test ! -e tools/harness_lint/tests/test_skill_registry_lock.py` exits 0.
    - `grep -c "registry-lock" .github/workflows/ci.yml` returns `0`.
    - `grep -n "needs:" .github/workflows/ci.yml` returns exactly 2 lines (line 80's
      `lang-tests: needs: setup` and the `gate` job's `needs:` line), and the `gate` line contains
      exactly 12 comma-separated entries with `registry-lock` absent and every other entry from the
      original 13-entry list present unchanged.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e tools/skill_registry && test ! -e harness/skills/registry.lock && test ! -e tools/harness_lint/tests/test_skill_registry_lock.py && ! grep -q "registry-lock" .github/workflows/ci.yml</automated>
  </verify>
  <done>All four deletion targets are gone from the working tree; `ci.yml` has no `registry-lock` job and `gate.needs` lists exactly the 12 remaining jobs with no other token changed.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh uv.lock and run the full D-08 verification suite (D-01, D-04, D-08)</name>
  <files>uv.lock</files>
  <read_first>
    .planning/phases/40-self-gate-teardown/40-RESEARCH.md ("uv Workspace Removal Mechanics" and "Verification Commands" sections)
    .planning/phases/40-self-gate-teardown/40-VALIDATION.md (V-1..V-8 table)
  </read_first>
  <action>
    Refresh the lockfile now that `tools/skill_registry/` is gone: run `uv lock` (regenerates
    `uv.lock`, removing the `harness-skill-registry` package block at the former lines 195-198 —
    confirm via `grep "harness-skill-registry" uv.lock` returning nothing), then run
    `uv sync --all-packages` to prove every remaining workspace member still installs against the
    refreshed lock.
    Then run, in order, the remaining D-08 evidence commands exactly as specified in
    40-VALIDATION.md's V-1..V-8 table (do not invent variant commands):
    - V-1/V-2 closing grep: `grep -rn "skill_registry\|registry-lock\|registry\.lock" tools/ harness/ .github/ pyproject.toml uv.lock` — expect no output (grep exit 1); separately confirm the two prose survivors under `docs/` remain untouched.
    - V-4: `uv run pytest` — expect exit 0, no `ModuleNotFoundError`.
    - V-6 (emit-drift, three-step, exact path set from `ci.yml`'s `emit-drift` job):
      `uv run python -m tools.harness_emit`, then
      `git add -A -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json`, then
      `git diff --cached --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` — expect exit 0 (empty diff).
    - V-7: `uv run python -m tools.contract_drift.drift` — expect exit 0.
    - V-8: `uv run python -m tools.ruff_baseline` — expect exit 0.
    If any command fails, do not stage or commit — diagnose against the three Pitfalls in
    RESEARCH.md (dangling `gate.needs`, stale `uv.lock` path, surviving mirror test import) before
    retrying.
  </action>
  <acceptance_criteria>
    - `grep "harness-skill-registry" uv.lock` returns nothing.
    - `uv sync --all-packages` exits 0.
    - `uv run pytest` exits 0 with no collection error.
    - The three-step emit-drift check ends with `git diff --cached --exit-code` exiting 0.
    - `uv run python -m tools.contract_drift.drift` exits 0.
    - `uv run python -m tools.ruff_baseline` exits 0.
    - The closing V-1/V-2 grep sweep's only surviving hits are the two documented prose exceptions.
  </acceptance_criteria>
  <verify>
    <automated>uv lock && uv sync --all-packages && uv run pytest && uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline</automated>
  </verify>
  <done>uv.lock is refreshed with no harness-skill-registry entry; all six D-08 commands (uv sync, pytest, harness_emit diff, contract_drift, ruff_baseline, closing grep) are green. Ready to stage and commit as one atomic change (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase has **no security-relevant trust boundary**. It deletes a build-time consistency check
(`tools/skill_registry`, a CI-invoked Python CLI comparing a committed lock file against a recomputed
skill-surface digest) and its CI job. It touches no request path, no stored/persisted user data, no
authentication or session mechanism, and no secret. `security_enforcement` is not configured in
`.planning/config.json` for this repo.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | `.github/workflows/ci.yml` `gate.needs` edit | mitigate | Task 2's acceptance criteria assert the exact resulting `needs:` list content (12 entries, no other token changed) — a mis-edit widening or narrowing the fan-in is caught locally before commit, not just at CI dispatch. |
| T-40-02 | (operational, not STRIDE-security) mis-ordered deletion reds the CI fan-in gate | n/a — build/CI reliability, not a security threat | accept-with-mitigation | Task ordering (sweep → delete package+lock+test+CI job together → refresh uv.lock → verify) makes all three documented failure modes (dangling `gate.needs`, stale `uv.lock` path, surviving mirror-test import) unreachable inside the single commit, per RESEARCH.md's Common Pitfalls section. |
| T-40-SC | Tampering | package installs | n/a | Not applicable — this phase installs no npm/pip/cargo package; it removes one internal virtual workspace member. Package Legitimacy Audit gate is not triggered (RESEARCH.md confirms). |

**Accepted, recorded loss (not a threat, a deliberate scope decision — D-02):** after this phase, a
skill's `description` or an authored `references/` file can be rewritten, re-emit cleanly, and pass
every remaining gate with no objection — the content-drift guarantee `registry.lock` provided is
retired without replacement, per the milestone's binding constraint and ADR-0012 (CI + the merge are
the authority). This is a routing-integrity concern, not a security vulnerability, and is explicitly
out of scope to re-mechanize here.
</threat_model>

<verification>
Run all V-1..V-8 from `40-VALIDATION.md` exactly as specified (no invented variants):
- V-1/V-2: `grep -rn "skill_registry\|registry-lock\|registry\.lock" tools/ harness/ .github/ pyproject.toml uv.lock` (expect no output) and the same over `docs/` (expect only the two documented survivors).
- V-3: `grep -n "needs:" .github/workflows/ci.yml` — exactly 2 lines, `:410` has no `registry-lock`.
- V-4: `uv run pytest` — exit 0.
- V-5: `uv sync --all-packages` — exit 0.
- V-6: `uv run python -m tools.harness_emit` → `git add -A -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` → `git diff --cached --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` — exit 0.
- V-7: `uv run python -m tools.contract_drift.drift` — exit 0.
- V-8: `uv run python -m tools.ruff_baseline` — exit 0.
Manual-only (per 40-VALIDATION.md): after push, confirm the `gate` job resolves in GitHub Actions
(no "depends on unknown job" dispatch error) — this is the merge-time authority per ADR-0012 and is
not a pre-merge local blocker.
</verification>

<success_criteria>
1. No `registry.lock` anywhere in the tree and no `tools/skill_registry/` directory; the D-06/D-08
   closing grep over `tools/`, `harness/`, `.github/`, `pyproject.toml`, `uv.lock` returns nothing.
2. `.github/workflows/ci.yml` has no `registry-lock` job and `gate.needs` (`:410`) has no dangling
   `registry-lock` entry — no other `needs` entry added or removed.
3. `uv run pytest` is green with no collection error from a removed package, and
   `uv sync --all-packages` resolves against the refreshed `uv.lock`.
4. The emitted trees are unchanged: the three-step emit-drift check produces an empty diff.
5. The contract-drift gate stays clean.
6. Net surface change is deletion-only: −1 CI job, −1 tool package, −1 lock file, −2 gate tests,
   +0 commands/agents/skills/contracts/hooks. Everything lands in one atomic commit (D-01).
</success_criteria>

<output>
Create `.planning/phases/40-self-gate-teardown/40-01-SUMMARY.md` when done. The SUMMARY MUST state,
in one plain sentence, the accepted dropped guarantee (D-02): after this phase, editing a skill's
`description` changes agent routing with no gate objecting — this is accepted, not mitigated, per
ADR-0012 and the milestone's binding constraint. Also note which command Phase 41 no longer needs to
run before committing a skill change: `uv run python -m tools.skill_registry --write`.
</output>
