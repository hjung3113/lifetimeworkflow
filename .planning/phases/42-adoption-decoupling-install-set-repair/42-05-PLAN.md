---
phase: 42-adoption-decoupling-install-set-repair
plan: 05
type: execute
wave: 3
depends_on: ["42-02", "42-03", "42-04"]
files_modified:
  - harness/commands/adopt.md
  - harness/skills/brownfield-adoption/SKILL.md
  - .claude/commands/adopt.md
  - .opencode/command/adopt.md
  - .claude/skills/brownfield-adoption/SKILL.md
  - .opencode/skill/brownfield-adoption/SKILL.md
autonomous: true
requirements: [CER-06, PROD-01]

must_haves:
  truths:
    - "The /adopt command and the brownfield-adoption skill describe a draft -> apply lifecycle with no promote sub-verb, no human-gate section, no five-stage runbook -- both edited at harness/ source and re-emitted, never hand-edited in .opencode/ or .claude/."
    - "D-15: uv run pytest -q is green; grep -rn task_control and grep -rn GOLDEN_APPROVE_HUMAN over tools/adoption_apply/ tools/adoption_scan/ both return nothing; a draft -> apply run completes with GOLDEN_APPROVE_HUMAN unset; the fixture-install test passes; emit-drift, stale-derived, contract-drift, and the ruff ratchet are clean."
    - "D-16: no mutation-proof/adversarial-input table is produced for this phase -- it removes a gate and adds no control, and D-08's fixture-install test is coverage of a product property, not a contributor-facing gate."
    - "D-17: changed LOC is reported from git diff --stat over this phase's full commit range, not estimated."
  artifacts:
    - path: "harness/commands/adopt.md"
      provides: "the /adopt command source with no promote sub-verb and no human-gate section"
    - path: "harness/skills/brownfield-adoption/SKILL.md"
      provides: "a 4-stage adoption runbook (discover/draft/review/apply), old Stage 5 renumbered to Stage 4"
  key_links:
    - from: "harness/commands/adopt.md"
      to: ".claude/commands/adopt.md"
      via: "python -m tools.harness_emit, byte-identical projection"
      pattern: "harness_emit"
---

<objective>
Finish the phase: rewrite the two harness-source documents that still describe the deleted
promotion lifecycle (`/adopt`'s command source and the `brownfield-adoption` skill), re-emit both
runtime trees, then run the phase's full done-condition — every grep, every gate, the unset-env
`draft -> apply` run, and the `git diff --stat` LOC report.

Purpose: `emit-drift` treats a `harness/**` prose edit without a re-emit as red; a stale `/adopt`
command that still documents a `promote` sub-verb that no longer exists is exactly the kind of
divergence between what the harness declares and what it does that this milestone exists to close.
Output: `harness/commands/adopt.md` and `harness/skills/brownfield-adoption/SKILL.md` rewritten at
source and re-emitted to both `.claude/` and `.opencode/`; the phase's full verification sweep run
and its result recorded.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-adoption-decoupling-install-set-repair/42-CONTEXT.md
@.planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md
@.planning/phases/42-adoption-decoupling-install-set-repair/42-VALIDATION.md
@.planning/ROADMAP.md

D-12/D-13/D-14 ordering discipline applies (see Plan 01's context block). Never hand-edit
`.opencode/**` or `.claude/**` — every change to these two runtime trees in this plan MUST come
from `python -m tools.harness_emit` run against the edited `harness/` source, committed in the same
commit as the source edit (ROADMAP ordering rule 6: "every `harness/` change re-emits in the same
commit").

This plan depends on 42-02 (apply.py/cli.py prose finished), 42-03 (scan.py decoupled), and 42-04
(install-set repaired) because Task 2's full-suite/grep verification is only meaningful once every
other plan in the phase has landed.
</context>

<interfaces>
<!-- Exact prose sections to remove/rewrite, extracted from the live files this session. -->

`harness/commands/adopt.md` — current structure (full file read earlier this session, 81 lines):
- Frontmatter `description:` (`:2-7`) — currently ends "...with promotion of the batch's decisions
  human-gated (exit 3 refusal without an explicit human flag and confirmation value). Invoke to
  discover, draft, apply, or promote an adoption batch; never to bypass the promotion gate." Rewrite
  to drop the promotion clause — the command now covers discover/draft/apply only.
- Title line (`:12`) "# /adopt — brownfield adoption: discover, draft, apply, promote (human-gated)"
  — drop "promote (human-gated)".
- The prose paragraph at `:14-17` mentioning "human-ratification promotion logic" as part of
  `tools.adoption_apply`'s encoded workflow — drop the promotion clause; keep the discovery/drafting/
  application clauses.
- `### promote` sub-verb section (`:46-50`, including its `!`python -m tools.adoption_apply promote
  $ARGUMENTS`` line) — delete whole.
- `## The human gate (refusal is the default)` section (`:52-72`, including its worked example) —
  delete whole.
- `## Notes` section (`:74-80`) — drop the bullet "`promote` never self-approves" language; keep
  the "no arbitrary command execution" and "discovery is read-only; apply never touches the
  constitution plane" bullets, updating the trailing pointer sentence to describe the surviving
  4-stage runbook.

`harness/skills/brownfield-adoption/SKILL.md` — current structure (full file read earlier this
session, 76 lines):
- Frontmatter `description:` (`:3-8`) — currently says "...teaches the discover, draft, human
  review, promote, apply lifecycle..." — rewrite to "discover, draft, human review, apply".
- Body prose at `:22-24` — "This skill teaches the five-stage adoption runbook..." — rewrite to
  "four-stage".
- `## Stage 4: promote` section (`:50-58`) — delete whole.
- `## Stage 5: apply` (`:60-66`) — renumber to `## Stage 4: apply`; its body text is otherwise
  accurate and needs no further rewrite (already describes `apply_manifest` correctly — this phase
  did not change `apply.py`'s runtime behavior beyond the removed gate check, which this stage's
  prose never described).
- `## Related` section (`:68-75`) — the `gate-model` bullet describing "the general refuse-by-default
  human-gate pattern this skill's promotion stage instantiates" should drop the "promotion stage"
  reference since there is no longer a promotion stage; keep the rest of the bullet's substance if
  still accurate, or remove the bullet if `gate-model`'s only relevance here was the promotion gate
  (re-read `harness/skills/gate-model/SKILL.md` briefly to judge, or simply soften the wording to
  not name a stage that no longer exists — do not delete the whole `## Related` section, only the
  promotion-specific clause).

The command's example invocation at `harness/commands/adopt.md:69-72` (a human-run `promote`
example) is deleted along with the `## The human gate` section it lives in.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite adopt.md + brownfield-adoption SKILL.md at source, then re-emit</name>
  <files>harness/commands/adopt.md, harness/skills/brownfield-adoption/SKILL.md, .claude/commands/adopt.md, .opencode/command/adopt.md, .claude/skills/brownfield-adoption/SKILL.md, .opencode/skill/brownfield-adoption/SKILL.md</files>
  <read_first>harness/commands/adopt.md (full), harness/skills/brownfield-adoption/SKILL.md (full), harness/skills/gate-model/SKILL.md (for the ## Related cross-check), tools/harness_emit/emit-manifest.json (confirm the 4 emitted paths for these two source files)</read_first>
  <action>
  Rewrite `harness/commands/adopt.md` per the `<interfaces>` block above: trim the frontmatter
  description and title to drop the promote/human-gate language, delete the `### promote` sub-verb
  section and the `## The human gate` section whole, update the `## Notes` closing sentence to
  describe the surviving discover/draft/apply flow. Rewrite `harness/skills/brownfield-adoption/SKILL.md`
  per the same block: frontmatter description and the "five-stage" body sentence become
  "four-stage" (discover/draft/human review/apply), delete `## Stage 4: promote` whole, renumber
  the old `## Stage 5: apply` to `## Stage 4: apply`, soften the `## Related` bullet's
  "promotion stage" reference. Run `python -m tools.harness_emit` to project both edits into
  `.claude/commands/adopt.md`, `.opencode/command/adopt.md`,
  `.claude/skills/brownfield-adoption/SKILL.md`, `.opencode/skill/brownfield-adoption/SKILL.md` —
  never hand-edit these four files. Stage and commit the source edits and the emitted trees
  together in one commit (ordering rule 6): `git add harness/commands/adopt.md
  harness/skills/brownfield-adoption/SKILL.md .claude/commands/adopt.md .opencode/command/adopt.md
  .claude/skills/brownfield-adoption/SKILL.md .opencode/skill/brownfield-adoption/SKILL.md`;
  `git commit -- harness/commands/adopt.md harness/skills/brownfield-adoption/SKILL.md
  .claude/commands/adopt.md .opencode/command/adopt.md .claude/skills/brownfield-adoption/SKILL.md
  .opencode/skill/brownfield-adoption/SKILL.md`.
  </action>
  <verify>
    <automated>python -m tools.harness_emit && git status --porcelain</automated>
  </verify>
  <done>adopt.md and brownfield-adoption/SKILL.md describe a 4-stage, promote-free lifecycle at source and both runtime trees; emit-drift clean.</done>
  <acceptance_criteria>
  `grep -c "promote\|human gate\|five-stage" harness/commands/adopt.md
  harness/skills/brownfield-adoption/SKILL.md` returns `0` for both files (adjust the grep per file
  if a legitimate non-promotion use of one of these words remains — inspect any nonzero hit before
  treating it as a failure).
  `python -m tools.harness_emit && git status --porcelain` produces empty output (emit-drift clean
  — the just-committed emitted trees already match a fresh re-emit).
  `uv run pytest tools/harness_emit tools/harness_lint -q` is green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full phase-gate verification — D-15's done-condition, D-17's LOC report</name>
  <files>(no files modified — verification only; may amend a prior commit if a gate is found red, per D-12's amend-if-red)</files>
  <read_first>.planning/phases/42-adoption-decoupling-install-set-repair/42-VALIDATION.md "Structural assertions (SC-1/SC-2)" and "What a botched decoupling looks like" sections, .planning/ROADMAP.md "#### Phase 42" Success Criteria 1-6</read_first>
  <action>
  Run the phase's complete done-condition, in this order, fixing forward (amend the relevant
  earlier commit, never a fresh unrelated commit for a gate-fix) if anything reds: (1)
  `uv run pytest -q` — full suite green; (2) `grep -rn "task_control" tools/adoption_apply/
  tools/adoption_scan/` — must return nothing; (3) `grep -rn "GOLDEN_APPROVE_HUMAN"
  tools/adoption_apply/ tools/adoption_scan/` — must return nothing; (4) a scratch `draft -> apply`
  cycle with `GOLDEN_APPROVE_HUMAN` unset against a throwaway git repo + scratch target (mirror
  `test_cli.py`'s `git_repo`/`synthetic_target` fixture setup, run via `env -u GOLDEN_APPROVE_HUMAN`
  in front of both `python -m tools.adoption_apply draft ...` and `... apply ...` invocations) —
  must complete with no promotion step and exit 0; (5)
  `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -x` — green; (6)
  `python -m tools.harness_emit && git status --porcelain` — empty (emit-drift); (7) regenerate
  `docs/reference/**` + `.memory/derived/contracts-index.md` via their respective generators and
  `git status --porcelain` — empty (stale-derived); (8)
  `uv run python -m tools.contract_drift.drift` — exit 0; (9) the ruff-ratchet check (reuse the
  exact CI invocation from `.github/workflows/ci.yml`'s ruff job). Finally, run `git diff --stat
  <first-commit-of-this-phase>..HEAD` (find the first commit via `git log --oneline` back to the
  phase's Wave-1 start) and report the exact changed-LOC count (D-17 — measured, never estimated).
  </action>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>Full suite green; all D-15 greps/gates pass; D-17 LOC reported from git diff --stat.</done>
  <acceptance_criteria>
  All 9 checks in the action above pass with the exact evidence quoted (exit codes / empty
  `git status --porcelain` output / green pytest summaries) recorded in this task's completion
  notes, matching CONTEXT.md D-15's done-condition verbatim.
  `git diff --stat` output for the full phase commit range is captured and reported as this task's
  LOC figure.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `harness/` source -> emitted `.claude/`/`.opencode/` trees | The emitter is the ONLY sanctioned write path into the emitted trees; this plan's Task 1 is the boundary crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Tampering | Emitted `.claude/`/`.opencode/` trees hand-edited instead of re-emitted | mitigate | `emit-drift` (Task 1's + Task 2's acceptance criteria both re-run it) fails loud on any divergence between a fresh re-emit and the committed trees |
</threat_model>

<verification>
`uv run pytest -q` full suite green.
`emit-drift`, `stale-derived`, `contract-drift`, ruff-ratchet all clean.
`git log --oneline` for the phase shows Task 1's commit as the last content commit; Task 2 produces
no new commit unless a gate-fix amendment was needed.
</verification>

<success_criteria>
Every ROADMAP Phase-42 success criterion (1-6) holds: no `task_control` reference, no
`GOLDEN_APPROVE_HUMAN` reference, `scan.py` reads no task-control file with unchanged
secret-redaction tests, `_CATEGORY_GLOBS` ships `tools/**` proven by the fixture-install test,
`uv run pytest -q` green with all four structural gates clean, and net surface change is
deletion-only plus the one new test file and the `tools/**` data row.
</success_criteria>

<output>
Create `.planning/phases/42-adoption-decoupling-install-set-repair/42-05-SUMMARY.md` when done
</output>
