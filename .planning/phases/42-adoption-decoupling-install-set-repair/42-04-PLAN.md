---
phase: 42-adoption-decoupling-install-set-repair
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_scan/tests/test_install_completeness.py
  - tools/adoption_scan/destinations.py
autonomous: true
requirements: [PROD-01]

must_haves:
  truths:
    - "D-08: the fixture-install test is authored FIRST and observed to FAIL against the pre-fix catalog (no tools/** row means an emitted command's module is missing from the applied target) -- the RED result is recorded before the fix lands."
    - "D-07: exactly one blanket tools/** row is added to _CATEGORY_GLOBS, not an enumerated per-package list -- robust to phases 43/44 deleting packages later with zero edit required here."
    - "D-09: shipping each surviving tools/ package's tests/ directory along with its source is accepted, not filtered -- filtering would be a mechanism, and D-07 says data row."
  artifacts:
    - path: "tools/adoption_scan/tests/test_install_completeness.py"
      provides: "the ONE new file this phase adds -- walks every emitted python -m tools.X reference and asserts the module exists in an applied target tree"
    - path: "tools/adoption_scan/destinations.py"
      provides: "_CATEGORY_GLOBS with a tools/** row, near the pyproject.toml rows"
  key_links:
    - from: "tools/adoption_scan/tests/test_install_completeness.py"
      to: "tools/adoption_scan/destinations.py::_CATEGORY_GLOBS"
      via: "destination_catalog() called directly against the live _REPO_ROOT, no fixture copy"
      pattern: "destination_catalog\\(\\)"
---

<objective>
Prove PROD-01 with a genuinely new test, written RED-first: before `_CATEGORY_GLOBS` gains a
`tools/**` row, a fixture-install test that walks every `python -m tools.X` reference in the
emitted commands/skills and CI workflows must FAIL to find that module in a freshly-applied target
tree. Only after recording that failure does the plan add the one glob row that turns it GREEN.

Purpose: today `_CATEGORY_GLOBS` (`destinations.py:142-181`) ships every command/CI file that
shells `uv run python -m tools.X` but ships none of the Python those commands invoke — the product
is inert. This plan is the mechanical proof of both the defect and the fix.
Output: `tools/adoption_scan/tests/test_install_completeness.py` (new file, D-08's deliverable);
one `"tools/**"` row added to `_CATEGORY_GLOBS`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-adoption-decoupling-install-set-repair/42-CONTEXT.md
@.planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md
@.planning/phases/42-adoption-decoupling-install-set-repair/42-VALIDATION.md

D-12/D-13/D-14 ordering discipline applies (see Plan 01's context block). This plan has no file
overlap with Plans 01/02/03 (`destinations.py` and the new test file are untouched elsewhere), so
it may execute in parallel with Wave 1's other work.

This is the phase's one TDD-shaped task per the planning constraints: "authored and observed to
FAIL against the pre-fix catalog, then pass after the row lands. That RED observation is the proof
the test actually tests something." Task 1 below is deliberately ordered before Task 2 for exactly
this reason — do not add the glob row first and write the test after.
</context>

<interfaces>
<!-- Exact helpers the new test reuses, extracted from the live tree so no exploration is needed. -->

`tools/adoption_scan/destinations.py`:
- `destination_catalog() -> list[dict]` — rows are `{"destination": <repo-relative POSIX str>}`,
  called with no arguments against the live `_REPO_ROOT`.
- `harness_proposed_hashes() -> dict[str, str]` — `{destination: proposed_sha256}` over catalog rows
  with real content in this checkout.
- `build_manifest(inventory, target_root, proposed_hashes, *, catalog=None) -> dict` — `inventory`
  can be `{"included": [], "excluded": []}` (empty/near-empty is fine; every `tools/**` row then
  resolves to `create` since `tmp_path` starts empty). `catalog=None` uses the live
  `destination_catalog()`.
- `_CATEGORY_GLOBS: tuple[str, ...]` — the tuple this plan's Task 2 adds one row to, current last
  rows: `"pyproject.toml"`, `"**/pyproject.toml"`, then (existing) `"libs/normalize-spec.md"`,
  `"libs/normalize-fixtures/**/*"`, `".gitignore"`. Insert `"tools/**"` immediately after
  `"**/pyproject.toml"` (position is immaterial per RESEARCH.md's dedup analysis — this placement
  is for readability only).

`tools/adoption_apply/apply.py`:
- `apply_manifest(manifest, target_root, *, payloads=None, block_bodies=None) -> dict[str,
  list[str]]` — returns `{"applied": [...], "skipped": [...], "refused": [...]}`.

`tools/adoption_apply/cli.py`:
- `_harness_payload(destination: str) -> bytes` — reads `_REPO_ROOT / destination`'s bytes, or
  `b""` if the harness checkout has no file there. Reuse this (import it) rather than
  reimplementing the same read, since `_cmd_apply` already sources `create`-disposition payloads
  this exact way (CR-01: "proposed content is what the harness template would install").

Reference model for structure (read, do not copy verbatim): `tools/adoption_scan/tests/test_dispositions.py`'s
`test_total`/`test_no_fictional_placeholder_destinations` — the "query the live real harness tree
with no fixture copy" pattern this new test follows.

Module-invocation ground truth (21 distinct top-level packages, grep-enumerated from
`harness/commands/*.md`, `harness/skills/*/SKILL.md`, `.github/workflows/ci.yml` — re-derive this
list at execution time via the regex below rather than trusting this static count, since it is a
point-in-time snapshot):
```
tools.adoption_apply, tools.adoption_scan, tools.agree.write, tools.capability,
tools.contract_drift.drift, tools.contract_hash.hash, tools.discipline, tools.docs_sync,
tools.evidence.capture, tools.golden_runner.approve, tools.golden_runner.runner, tools.handoff,
tools.harness_emit, tools.harness_lint.provenance, tools.lifecycle_eval.runner,
tools.memory_regen.contracts_index, tools.memory_regen.inject, tools.memory_regen.pointer_index,
tools.memory_regen.repo_map, tools.polyglot_lint.lint, tools.risk_router.intake,
tools.ruff_baseline, tools.strangler_guard, tools.task_control, tools.task_packet.validate
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the fixture-install test and observe it RED against the pre-fix catalog</name>
  <files>tools/adoption_scan/tests/test_install_completeness.py</files>
  <read_first>tools/adoption_scan/tests/test_dispositions.py (structural model), tools/adoption_scan/destinations.py:228-320 (destination_catalog/harness_proposed_hashes/build_manifest signatures), tools/adoption_apply/apply.py::apply_manifest, tools/adoption_apply/cli.py::_harness_payload, .planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md "How to Write D-08's Fixture-Install Test" section</read_first>
  <behavior>
    - Test 1 (`test_every_referenced_tools_module_lands_in_applied_target`): a helper regex-walks
      `harness/commands/**/*.md`, `harness/skills/**/*.md`, and `.github/workflows/*.yml` for every
      `python -m tools\.([a-zA-Z0-9_.]+)` reference, normalizes each to its top-level package name
      (e.g. `tools.contract_drift.drift` -> `contract_drift`), then runs
      `destination_catalog()` + `harness_proposed_hashes()` + `build_manifest({"included": [],
      "excluded": []}, tmp_path, proposed_hashes)` + `apply_manifest(manifest, tmp_path,
      payloads={d: _harness_payload(d) for d in create-disposition destinations})` against a fresh
      `tmp_path`, then asserts `(tmp_path / "tools" / package_name).is_dir()` for every discovered
      package name.
    - Test 2 (a package-count sanity assertion, e.g. `test_discovers_at_least_twenty_modules`):
      the regex-walk helper finds at least 20 distinct top-level package names (guards against the
      helper's regex silently matching nothing and the main test vacuously passing).
  </behavior>
  <action>
  Author `tools/adoption_scan/tests/test_install_completeness.py` per the `<behavior>` block above,
  BEFORE touching `destinations.py`. Run `uv run pytest
  tools/adoption_scan/tests/test_install_completeness.py -x -v` and confirm it FAILS — at least one
  package directory assertion should fail because `_CATEGORY_GLOBS` currently ships zero `tools/**`
  rows, so `tools/<package>` never lands in the applied `tmp_path`. Record the exact RED output
  (which assertion failed, for which package) in this task's completion notes — this is the
  required proof the test is genuine coverage, not a vacuous pass. Do NOT edit `destinations.py` in
  this task. `git add tools/adoption_scan/tests/test_install_completeness.py`; `git commit --
  tools/adoption_scan/tests/test_install_completeness.py`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_install_completeness.py -x -v</automated>
  </verify>
  <done>test_install_completeness.py exists and is observed RED before the tools/** glob row lands.</done>
  <acceptance_criteria>
  `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -x` exits nonzero (RED) —
  this is the EXPECTED, required result for this task, not a failure to fix. The failure message
  names a specific missing `tools/<package>` directory. `test -f
  tools/adoption_scan/tests/test_install_completeness.py` exits 0 after commit.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the tools/** glob row and confirm the fixture-install test turns GREEN</name>
  <files>tools/adoption_scan/destinations.py</files>
  <read_first>tools/adoption_scan/destinations.py:142-181 (_CATEGORY_GLOBS)</read_first>
  <action>
  Add exactly one new row, `"tools/**"`, to `_CATEGORY_GLOBS` (D-07 — a blanket glob, never an
  enumerated per-package list), placed immediately after the existing `"**/pyproject.toml"` row.
  Do not add, remove, or reorder any other row. `git add tools/adoption_scan/destinations.py`;
  `git commit -- tools/adoption_scan/destinations.py`. Re-run
  `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -x -v` and confirm it now
  passes GREEN.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_install_completeness.py -x</automated>
  </verify>
  <done>_CATEGORY_GLOBS has one new tools/** row; the fixture-install test is GREEN.</done>
  <acceptance_criteria>
  `grep -c '"tools/\*\*"' tools/adoption_scan/destinations.py` returns `1`.
  `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -x` exits 0 (GREEN).
  `uv run pytest tools/adoption_scan -q` is green (confirms `test_total`'s `_MIN_CATALOG_ROWS` floor
  still holds and `test_no_fictional_placeholder_destinations` still passes — both are floor/absence
  checks unaffected by catalog growth per RESEARCH.md's "No existing test breaks" analysis).
  `uv run pytest tools/adoption_scan/tests/test_snapshots.py -q` is green (renders over a fixed
  `_FIXED_CATALOG`, immune to catalog growth by construction).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This checkout -> installed target monorepo | The install-set catalog determines what code a target actually receives; this plan closes the gap where commands/CI ship but their implementation does not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Tampering (supply-completeness) | `_CATEGORY_GLOBS` silently omitting a package a future command invokes | mitigate | `test_install_completeness.py` walks the actual references and asserts real post-apply existence, not glob-pattern-string matching — it automatically catches a future omission, no second guard mechanism needed |
| T-42-06 | Information Disclosure | Shipping `tools/*/tests/` (including any test fixture content) to an installed target | accept | D-09: explicitly accepted per CONTEXT.md — filtering `tests/` would be a mechanism, not a data row; a follow-up if it proves objectionable, not this phase |
</threat_model>

<verification>
`uv run pytest tools/adoption_scan -q` green after Task 2.
`git log --oneline -5` shows exactly 2 new commits for this plan's 2 tasks, Task 1's commit
preceding Task 2's.
</verification>

<success_criteria>
`test_install_completeness.py` exists, was observed RED before the glob row landed, and is GREEN
after. `_CATEGORY_GLOBS` contains exactly one new `"tools/**"` row.
</success_criteria>

<output>
Create `.planning/phases/42-adoption-decoupling-install-set-repair/42-04-SUMMARY.md` when done
</output>
