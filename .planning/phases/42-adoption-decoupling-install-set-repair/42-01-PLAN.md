---
phase: 42-adoption-decoupling-install-set-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_apply/approval.py
  - tools/adoption_apply/cli.py
  - tools/adoption_apply/tests/test_approval_invalidation.py
  - tools/adoption_apply/tests/test_cli.py
autonomous: true
requirements: [CER-06]

must_haves:
  truths:
    - "D-01: the whole ADOPT-06 approval gate is deleted -- approval.py, the promote subcommand, the check_valid apply-refusal, and the approval imports no longer exist in tools/adoption_apply/."
    - "D-03: apply no longer refuses on a missing promotion; draft -> apply completes with no promote step and no GOLDEN_APPROVE_HUMAN reference remaining in cli.py."
    - "D-12/D-13: every deletion in this plan is delete -> git add -> git commit -- <pathspec> -> verify -> amend-if-red, never git add -A/git add ./git commit -a, never git checkout <ref> -- ."
    - "D-14: verification runs the actual pytest/grep commands rather than reading the diff -- each task acceptance criterion is a command executed, not a file inspected by eye."
  artifacts:
    - path: "tools/adoption_apply/cli.py"
      provides: "draft/apply dispatcher with no promote subcommand, no approval import, no check_valid refusal"
    - path: "tools/adoption_apply/tests/test_cli.py"
      provides: "rewritten test suite: 5 promote/refusal-specific tests deleted, 4 apply-path tests edited to drop the _promote() precondition, _promote()/decisions_path/dead constants removed"
  key_links:
    - from: "tools/adoption_apply/cli.py::_cmd_apply"
      to: "tools/adoption_apply/apply.py::apply_manifest"
      via: "direct call, no intervening check_valid gate"
      pattern: "apply_manifest\\("
---

<objective>
Delete the whole ADOPT-06 human-ratification gate (D-01) from `tools/adoption_apply/` and land its
test-blast-radius fallout in the same wave: `approval.py` itself, the `promote` CLI subcommand, the
`check_valid` refusal that gates `apply`, and every test that exercises them. After this plan,
adoption is `draft -> apply` with no promotion step — the review moves to the PR (D-03), which is
the milestone's own accepted consequence, not a regression to soften.

Purpose: sever the ONE live `tools.task_control` import (`approval.py:37`) and the
`GOLDEN_APPROVE_HUMAN` env-token check, so `tools/adoption_apply/` stops depending on the
lifecycle plane phases 43/44 delete.
Output: `approval.py` deleted; `cli.py`'s `promote` subcommand + `check_valid` refusal + approval
imports deleted; `test_approval_invalidation.py` deleted whole; `test_cli.py` rewritten (5 tests
deleted, 4 tests edited, dead helper/fixture/constants removed).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-adoption-decoupling-install-set-repair/42-CONTEXT.md
@.planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md

Ordering discipline (D-12/D-13/D-14, carried from Phase 41 — measured, not stylistic): per task,
delete -> `git add` -> `git commit -- <pathspec>` -> verify -> amend-if-red. NEVER
`git add -A`/`git add .`/`git commit -a`. NEVER `git checkout <ref> -- .` (a Phase-41 executor did
this and silently reverted unrelated files — if you need to compare against an old commit, use a
disposable `git clone` under the scratchpad, never `git checkout <ref> -- .` on the working tree).
`destinations.py:217` reads `git ls-files`, so a tracked deletion reds until staged AND committed —
a red `uv run pytest --collect-only -q` or test failure immediately after `git rm`/deleting a file
but before `git commit` is EXPECTED, not a signal to stop.
</context>

<interfaces>
<!-- approval.py's public surface being deleted, and cli.py's exact import/call sites, extracted
     from the live tree so no exploration is needed. -->

`tools/adoption_apply/approval.py` (234 lines, whole file deleted):
- `class AdoptionApprovalRefused(Exception)`
- `def promote(task_dir, batch_id, repo_root, *, approve=False, decisions=None, confirmation=None) -> dict`
- `def check_valid(task_dir, batch_id, repo_root) -> bool`
- `HUMAN_TOKEN_ENV = "GOLDEN_APPROVE_HUMAN"`
- `from tools.task_control.manager import show` (line 37 — the ONE live cross-package import this
  phase severs)

`tools/adoption_apply/cli.py` exact deletion sites (line numbers as of 2026-07-28, re-verify by
reading the file first — this session's other edits may have shifted them slightly, but the
content below is byte-verified):
- `:41-47` — two import statements:
  `from tools.adoption_apply.approval import (AdoptionApprovalRefused, check_valid)` and
  `from tools.adoption_apply.approval import (promote as approval_promote)`
- `:145-157` — the whole `if not check_valid(...): ... return 4` block inside `_cmd_apply` (the
  comment block above it at `:146-150` referencing "CR-03 (ADOPT-06)" goes too)
- `:222-241` — the whole `_cmd_promote` function body
- `:266-273` — the `promote_parser` argparse registration block (`subparsers.add_parser("promote",
  ...)` through `promote_parser.set_defaults(func=_cmd_promote)`)
- `:1-27` module docstring — the final paragraph starting "``promote`` mirrors
  ``tools.golden_runner.approve.py::main``'s EXACT refuse-by-default exit-code idiom..." must be
  removed (there is no more `promote`); leave the rest of the docstring (the `draft`/`apply`
  paragraphs) intact.

Do NOT touch `apply_parser`'s `--repo-root` argument registration (`:263`) — it stays required and
simply becomes an unused `args.repo_root` inside `_cmd_apply` once the `check_valid` block is gone;
removing the flag itself is out of scope for D-01 (not named in CONTEXT.md's deletion list) and
would be an uninstructed CLI surface change.

`_cmd_apply`'s surviving body (post-deletion) proceeds directly from the argparse dispatch to
reading `batch_root / "manifest.json"` — no gate in between.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete approval.py + cli.py's promote/check_valid wiring + test_approval_invalidation.py</name>
  <files>tools/adoption_apply/approval.py, tools/adoption_apply/cli.py, tools/adoption_apply/tests/test_approval_invalidation.py</files>
  <read_first>tools/adoption_apply/approval.py, tools/adoption_apply/cli.py (full read before editing — confirm line numbers against the live file, not this plan's cached numbers), .planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md "Coupling Map (CER-06)" table</read_first>
  <action>
  `git rm tools/adoption_apply/approval.py`. Delete
  `tools/adoption_apply/tests/test_approval_invalidation.py` whole (`git rm`) — every test in it
  exercises `approval.py`'s `promote`/`check_valid` directly and has no substance once the module
  is gone. In `cli.py`: remove the two `from tools.adoption_apply.approval import ...` statements;
  remove the `if not check_valid(...)` refusal block (and its "CR-03 (ADOPT-06)" comment) from
  `_cmd_apply` so the function proceeds straight from argparse dispatch to reading
  `manifest_path`; remove the whole `_cmd_promote` function; remove the `promote_parser` argparse
  registration block from `main()`; remove the module docstring's final paragraph naming
  `promote`/`golden_runner.approve.py`'s exit-code idiom (leave the `draft`/`apply` paragraphs
  untouched — this is D-11's sweep starting here, finished in Plan 02). Do not touch
  `apply_parser`'s `--repo-root` registration. `git add tools/adoption_apply/cli.py`;
  `git commit -- tools/adoption_apply/approval.py tools/adoption_apply/tests/test_approval_invalidation.py tools/adoption_apply/cli.py`.
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q tools/adoption_apply</automated>
  </verify>
  <done>approval.py and test_approval_invalidation.py deleted; cli.py has no check_valid/promote wiring; collection succeeds.</done>
  <acceptance_criteria>
  `test ! -f tools/adoption_apply/approval.py` exits 0. `test ! -f
  tools/adoption_apply/tests/test_approval_invalidation.py` exits 0.
  `grep -n "approval\|promote\|check_valid" tools/adoption_apply/cli.py` returns zero matches for
  `check_valid`/`approval_promote`/`_cmd_promote`/`promote_parser` (a residual `--approve`-style
  string is not expected either, since the whole subcommand is gone).
  `uv run pytest --collect-only -q tools/adoption_apply` exits 0 (no import/collection error) —
  runtime failures inside `test_cli.py` from the now-deleted `promote` subcommand are EXPECTED and
  fixed by Task 2, not a blocker for this task's commit.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_cli.py — delete 5 tests, edit 4, remove dead helper/fixture/constants</name>
  <files>tools/adoption_apply/tests/test_cli.py</files>
  <read_first>tools/adoption_apply/tests/test_cli.py (full read — 659 lines, 10 test functions), .planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md "Blast Radius of Deleting the ADOPT-06 Gate" table and "Pitfall 2"</read_first>
  <action>
  Delete these 5 test functions outright: `test_cli_promote_refused_exit_code_3`,
  `test_cli_promote_refused_exit_code_3_subprocess`, `test_cli_promote_succeeds_with_full_human_signals`
  (assert behavior of the deleted `promote` subcommand), `test_cli_apply_refuses_without_approval`,
  `test_cli_apply_refuses_on_stale_approval` (assert the exact D-03 refusal that no longer exists).
  In the 4 surviving tests that call the `_promote(task_dir, batch_id, git_repo, decisions_path,
  monkeypatch)` helper as a precondition — `test_cli_apply_end_to_end`,
  `test_cli_apply_refuses_on_malformed_manifest`, `test_cli_apply_refuses_hostile_destination_cleanly`
  (two call sites inside it), `test_cli_apply_refuses_directory_shaped_destination` — remove the
  `_promote(...)` call and its `assert promote_exit == 0` line so `apply` runs directly after the
  batch is seeded (via `_seed_batch_dir`/`_seed_batch_with_manifest`), and drop the now-unused
  `decisions_path`/`monkeypatch`-for-promote parameters from each edited test's signature where
  `monkeypatch` is no longer needed for any other purpose in that test (check each test body
  individually — `monkeypatch.chdir(...)` in `test_cli_apply_end_to_end` is still needed, so keep
  `monkeypatch` there; drop it only where its sole use was `_promote`). `test_cli_draft_writes_into_batch_root`
  makes no `_promote`/`apply` call at all — leave it byte-unchanged. Once no test calls `_promote`
  anymore, delete the `_promote()` helper function (`:115-138`), the `decisions_path` fixture
  (`:141-145`), and the now-dead module-level constants `HUMAN_TOKEN_ENV` (`:22`), `_HUMAN_VALUE`
  (`:23`), `_DECISIONS` (`:25`) — re-grep the file after editing to confirm none of the three names
  appear anywhere else in it. `git add tools/adoption_apply/tests/test_cli.py`;
  `git commit -- tools/adoption_apply/tests/test_cli.py`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply -q</automated>
  </verify>
  <done>test_cli.py has exactly 5 test functions, all green, no _promote/decisions_path/dead constants remain.</done>
  <acceptance_criteria>
  `uv run pytest tools/adoption_apply -q` exits 0, all green.
  `grep -c "_promote(\|decisions_path\|HUMAN_TOKEN_ENV\|_HUMAN_VALUE\b" tools/adoption_apply/tests/test_cli.py`
  returns `0`.
  `grep -c "^def test_" tools/adoption_apply/tests/test_cli.py` returns `5` (10 original minus the
  5 deleted).
  `uv run pytest --collect-only -q --collect-only 2>&1 | tail -1` total test count matches this
  session's pre-plan baseline minus exactly 5 (spot-check per RESEARCH.md's Open Question 1 — no
  known suite-wide count guard exists, so this is a sanity signal, not a hard gate).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| DEV session -> `tools/adoption_apply` CLI | Removing a human-token gate; the surviving trust boundary (constitution-plane write confinement) is untouched by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Tampering | `cli.py::_cmd_apply` after the gate removal | accept | D-03/ADR-0012: CI + the merge (the PR) are the review authority now; the structural write-confinement (`refuse_unsafe_destination`, untouched by this plan) remains the real access-control boundary, not the removed token check |
| T-42-02 | Repudiation | Loss of the `approval.json` audit trail (who ratified what, when) | accept | Recorded scope decision (ADR-0012, D-03) — the PR review + git history is the new record; this phase adds no replacement mechanism per the milestone's binding constraint |
</threat_model>

<verification>
`uv run pytest tools/adoption_apply -q` green after both tasks.
`grep -rn "task_control" tools/adoption_apply/` still shows the prose-only hits enumerated in
RESEARCH.md's Coupling Map (`apply.py:16/:207/:241`, `batch.py` x5, `cli.py` module docstring
possibly) — these are Plan 02's job, not this plan's; do not chase them here.
`git log --oneline -5` shows exactly 2 new commits for this plan's 2 tasks.
</verification>

<success_criteria>
`approval.py` and `test_approval_invalidation.py` do not exist. `cli.py` has no `promote`
subcommand, no `check_valid` import/call, no approval import. `test_cli.py` has exactly 5 test
functions, none calling a `_promote` helper that no longer exists. `uv run pytest
tools/adoption_apply -q` is green.
</success_criteria>

<output>
Create `.planning/phases/42-adoption-decoupling-install-set-repair/42-01-SUMMARY.md` when done
</output>
