---
phase: 42-adoption-decoupling-install-set-repair
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - contracts/harness/adoption/approval.schema.json
  - contracts/.hashes/manifest.json
  - .memory/derived/contracts-index.md
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  - docs/reference/approval.md
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - tools/adoption_apply/apply.py
  - tools/adoption_apply/cli.py
  - tools/adoption_apply/batch.py
  - tools/adoption_apply/pyproject.toml
autonomous: true
requirements: [CER-06]

must_haves:
  truths:
    - "D-02: contracts/harness/adoption/approval.schema.json no longer exists, and contracts/.hashes/manifest.json is rebaselined in the same commit -- contract-drift is green."
    - "D-10: apply.py's :207 and :241 docstrings no longer claim to mirror tools.task_control.manager -- they describe the already-inlined sequence on its own terms."
    - "D-11: cli.py's module docstring and batch.py's five docstring/comment mentions no longer name tools.task_control.manager; grep -rn task_control tools/adoption_apply/ returns nothing."
  artifacts:
    - path: "contracts/.hashes/manifest.json"
      provides: "rebaselined hash manifest with the approval key removed, no other key touched"
    - path: ".memory/derived/contracts-index.md"
      provides: "committed-derived contracts-index regenerated to the post-deletion contract count"
  key_links:
    - from: "contracts/.hashes/manifest.json"
      to: "tools/contract_hash/hash.py::SCHEMA_GLOB"
      via: "generic **/*.schema.json glob, not DATA_CONTRACT_PATHS"
      pattern: "SCHEMA_GLOB"
---

<objective>
Delete the now-orphaned `approval.schema.json` contract (D-02) with a same-commit hash-manifest
rebaseline, prune its derived-plane page and index row, and finish the D-10/D-11 prose sweep in
`tools/adoption_apply/` that Plan 01 deliberately deferred (`apply.py`'s two stale docstrings,
`cli.py`'s remaining module-docstring residue, `batch.py`'s five prose-only mentions, and the
`pyproject.toml` description).

Purpose: reuse the exact Phase-41 contract-deletion procedure (`41-04-SUMMARY.md`) for the one
adoption contract this phase orphans, and close every remaining `task_control` string match in
`tools/adoption_apply/` so SC-1's grep returns nothing.
Output: `approval.schema.json` deleted; `contracts/.hashes/manifest.json` rebaselined;
`.memory/derived/contracts-index.md` + its syrupy snapshot regenerated; `docs/reference/approval.md`
pruned and `EXPECTED_PAGES` updated; `apply.py`/`cli.py`/`batch.py`/`pyproject.toml` prose edited.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-adoption-decoupling-install-set-repair/42-CONTEXT.md
@.planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md

Reuse the exact Phase-41 rebaseline procedure verbatim: `git rm` the schema (no `HARNESS_DEV_BYPASS`
needed — `contract_guard`'s `PreToolUse` hook matches only `Write|Edit`, never `Bash`), then in the
SAME commit `uv run python -m tools.contract_hash.hash --write` (rewrites
`contracts/.hashes/manifest.json`, dropping exactly the `approval` key — no
`DATA_CONTRACT_PATHS` edit needed since `approval.schema.json` is picked up by the generic
`SCHEMA_GLOB = "**/*.schema.json"`, not the explicit tuple), then
`uv run python -m tools.contract_drift.drift` must exit 0 before committing.

D-12/D-13/D-14 ordering discipline applies identically to this plan (see Plan 01's context block for
the full statement) — delete -> stage -> commit -> verify -> amend-if-red, never `git checkout <ref>
-- .`.
</context>

<interfaces>
<!-- Exact remaining prose-residue sites from RESEARCH.md's Coupling Map, so no re-exploration is
     needed. -->

- `tools/adoption_apply/apply.py:16` — module docstring: "Two publish idioms, reused verbatim from
  ``tools.task_control.manager``'s already-audited sequence..." — reword to describe the sequence
  on its own terms (it is a copied implementation, never cross-package-imported).
- `tools/adoption_apply/apply.py:207` — `atomic_create`'s docstring: "Mirrors
  ``tools.task_control.manager._atomic_create``'s exact sequence, operating on raw ``bytes``..." —
  reword to drop the `tools.task_control.manager` reference; describe the sequence itself
  (`tempfile.mkstemp` in the target directory -> write/flush/fsync -> `os.link` publish -> directory
  fsync -> `finally: os.unlink(tmp)`).
- `tools/adoption_apply/apply.py:241` — `_atomic_replace`'s docstring: "Mirrors
  ``tools.task_control.manager._atomic_replace``'s exact sequence." — same treatment
  (`os.replace`-based variant).
- `tools/adoption_apply/cli.py:1-9` module docstring (post-Plan-01 edit) — re-read after Plan 01's
  edit landed; confirm no residual `task_control`/`approval` string remains anywhere in the file
  (Plan 01 removed the `promote`-naming paragraph; this task is the final grep-clean sweep).
- `tools/adoption_apply/batch.py` — five docstring/comment mentions at lines 4, 6, 14, 59, 92, all
  naming `tools.task_control.manager` (zero live imports — confirmed by `grep -n "^import\|^from"
  batch.py` showing stdlib only). Reword each to describe the atomic-write idiom `batch.py` itself
  implements, without naming `tools.task_control`.
- `tools/adoption_apply/pyproject.toml:4` — `description` field still says
  "...human-ratification promotion..."; update to reflect the `draft`/`apply`-only lifecycle.

`tools/docs_sync/tests/test_docs_sync_determinism.py:28-42` — `EXPECTED_PAGES` frozenset currently
includes the literal string `"approval"`; remove that one entry (the set becomes 12 pages, not 13).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete approval.schema.json, rebaseline the hash manifest, prune the derived plane</name>
  <files>contracts/harness/adoption/approval.schema.json, contracts/.hashes/manifest.json, .memory/derived/contracts-index.md, tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr, docs/reference/approval.md, tools/docs_sync/tests/test_docs_sync_determinism.py</files>
  <read_first>contracts/.hashes/manifest.json (confirm the 4 adoption keys present: approval/inventory/manifest/plan), tools/docs_sync/tests/test_docs_sync_determinism.py:20-42 (EXPECTED_PAGES), .planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md "Self-Check: PASSED" section for the exact command sequence</read_first>
  <action>
  `git rm contracts/harness/adoption/approval.schema.json`. Run `uv run python -m
  tools.contract_hash.hash --write` to rebaseline `contracts/.hashes/manifest.json` (drops exactly
  the `approval` key). Run `uv run python -m tools.contract_drift.drift` and confirm exit 0 before
  committing. Edit `tools/docs_sync/tests/test_docs_sync_determinism.py`'s `EXPECTED_PAGES`
  frozenset: remove the `"approval"` entry (this test's docstring comment block above the set,
  which narrates each contract's provenance, should also drop its "Phase 27 Plan 02 (27-02) added
  the ADOPT-06 promotion approval record" sentence — prose accuracy, not required for the test to
  pass, but do it in the same edit). Re-run `python -m tools.docs_sync` to regenerate
  `docs/reference/**` (its prune-then-write semantics will delete the now-orphaned
  `docs/reference/approval.md` automatically); confirm with `git status --porcelain
  docs/reference/`. Regenerate the committed-derived contracts-index plane as an in-scope
  consequence of this task's own schema deletion (Phase-41 precedent): `python -m
  tools.memory_regen.contracts_index` then `uv run pytest
  tools/memory_regen/tests/test_contracts_index.py --snapshot-update`. Stage and commit everything
  from this task together: `git add contracts/.hashes/manifest.json .memory/derived/contracts-index.md
  tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  tools/docs_sync/tests/test_docs_sync_determinism.py`; `git commit -- contracts/harness/adoption/approval.schema.json
  contracts/.hashes/manifest.json .memory/derived/contracts-index.md
  tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr docs/reference/approval.md
  tools/docs_sync/tests/test_docs_sync_determinism.py`.
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift</automated>
  </verify>
  <done>approval.schema.json and docs/reference/approval.md deleted; manifest rebaselined; contract-drift exits 0.</done>
  <acceptance_criteria>
  `test ! -f contracts/harness/adoption/approval.schema.json` exits 0.
  `grep -c approval contracts/.hashes/manifest.json` returns `0`.
  `uv run python -m tools.contract_drift.drift` exits 0.
  `test ! -f docs/reference/approval.md` exits 0.
  `uv run pytest tools/docs_sync tools/memory_regen -q` is green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Finish the D-10/D-11 prose sweep — apply.py, cli.py, batch.py, pyproject.toml</name>
  <files>tools/adoption_apply/apply.py, tools/adoption_apply/cli.py, tools/adoption_apply/batch.py, tools/adoption_apply/pyproject.toml</files>
  <read_first>tools/adoption_apply/apply.py:1-37,200-266, tools/adoption_apply/batch.py:1-20,55-95, tools/adoption_apply/cli.py (post-Plan-01), .planning/phases/42-adoption-decoupling-install-set-repair/42-RESEARCH.md "Coupling Map" table rows for apply.py:16/:207/:241 and batch.py:4,6,14,59,92</read_first>
  <action>
  Reword `apply.py:16`'s module-docstring sentence, `apply.py:207`'s `atomic_create` docstring, and
  `apply.py:241`'s `_atomic_replace` docstring to drop every `tools.task_control.manager` reference
  — describe each sequence on its own terms (both are complete, self-contained, already-inlined
  implementations; nothing is being ported). Reword `batch.py`'s five `tools.task_control.manager`
  mentions (lines 4, 6, 14, 59, 92) the same way. Update `tools/adoption_apply/pyproject.toml`'s
  `description` field to drop "human-ratification promotion" language, reflecting the surviving
  `draft`/`apply` lifecycle. Re-read `cli.py` in full and confirm no `task_control`/`approval`
  string survives anywhere (Plan 01 already removed the `promote`-naming docstring paragraph and
  the code; this is the final check, not expected to require further edits unless one was missed).
  `git add` the four files; `git commit -- tools/adoption_apply/apply.py tools/adoption_apply/cli.py
  tools/adoption_apply/batch.py tools/adoption_apply/pyproject.toml`.
  </action>
  <verify>
    <automated>grep -rn "task_control" tools/adoption_apply/</automated>
  </verify>
  <done>apply.py/cli.py/batch.py/pyproject.toml no longer name tools.task_control.manager.</done>
  <acceptance_criteria>
  `grep -rn "task_control" tools/adoption_apply/ tools/adoption_scan/` returns nothing (the
  `tools/adoption_scan/` half is Plan 03's job and is expected to still show `scan.py`'s
  `_GATE_REGISTRY_PATH` reference at this point — re-run this exact grep after Plan 03 lands for
  the phase-level SC-1 gate; for THIS task, scope the check to `tools/adoption_apply/` only:
  `grep -rn "task_control" tools/adoption_apply/` returns nothing).
  `grep -rn "GOLDEN_APPROVE_HUMAN" tools/adoption_apply/` returns nothing.
  `uv run pytest tools/adoption_apply -q` is green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| constitution plane (`contracts/**`) | `git rm` of a schema file, gated by CODEOWNERS at merge, not by `contract_guard` (Bash `git rm` is outside its `Write|Edit` matcher) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-03 | Tampering | `contracts/.hashes/manifest.json` rebaseline | mitigate | `contract-drift` gate re-verifies the manifest against the live schema tree on every CI run; a hand-edited manifest that doesn't match the live schemas fails the gate |
</threat_model>

<verification>
`uv run pytest tools/adoption_apply tools/docs_sync tools/memory_regen -q` green.
`uv run python -m tools.contract_drift.drift` exits 0.
`git log --oneline -5` shows exactly 2 new commits for this plan's 2 tasks.
</verification>

<success_criteria>
`contracts/harness/adoption/approval.schema.json` and `docs/reference/approval.md` do not exist.
`contracts/.hashes/manifest.json` is rebaselined and `contract-drift` is green. No
`tools.task_control.manager` string remains in `tools/adoption_apply/`.
</success_criteria>

<output>
Create `.planning/phases/42-adoption-decoupling-install-set-repair/42-02-SUMMARY.md` when done
</output>
