---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/harness/adoption/inventory.schema.json
  - contracts/harness/adoption/plan.schema.json
  - contracts/harness/adoption/manifest.schema.json
  - contracts/.hashes/manifest.json
  - docs/reference/inventory.md
  - docs/reference/plan.md
  - docs/reference/manifest.md
  - .memory/derived/contracts-index.md
autonomous: false
requirements: [ADOPT-01, ADOPT-02, ADOPT-03]

must_haves:
  truths:
    - "D-01: all three adoption artifacts (inventory, mapping plan, disposition manifest) are governed by human-ratified Draft 2020-12 schemas under contracts/harness/adoption/, mirroring the contracts/harness/task-control/ precedent exactly"
    - "D-11: each of the three schemas is self-contained (zero cross-file $ref) and duplicates its own small $defs (evidence reference, classification enum, disposition enum) — matching the style of every one of the 8 existing schemas in this repo"
    - "D-10: inventory.schema.json's excludedEntry $def has NO sha256/content property at all (additionalProperties:false + required:[path,size,excluded]) so a secret-flagged exclusion cannot structurally carry a content hash or excerpt"
    - "D-05: plan.schema.json's questionRecord carries at minimum {id, target, evidence[]} plus kind/classification/question; its optional candidate is nested under a candidateRecord shape that is schema-incomplete relative to a ratified relationship (no required authority) so it can never be mistaken for one"
    - "D-04: manifest.schema.json's dispositionEnum is exactly the 6 values create/preserve/conflict/marker-merge/derived-regenerate/human-ratification-required, and a separate excluded[] array records GSD-owned lanes by reason so 'excluded' is distinguishable from 'missed'"
    - "The contract-drift gate is green with all three new schemas tracked in the rebaselined contracts/.hashes/manifest.json"
    - "The committed-derived plane (docs/reference/{inventory,plan,manifest}.md + .memory/derived/contracts-index.md) is regenerated in this SAME wave so the stale-derived CI job does not go red on a later, unrelated commit"
  artifacts:
    - path: "contracts/harness/adoption/inventory.schema.json"
      provides: "ADOPT-01 inventory artifact contract (included/excluded files, language/manifest/surface detection, enumeration_mode, max_file_bytes)"
      contains: "additionalProperties"
    - path: "contracts/harness/adoption/plan.schema.json"
      provides: "ADOPT-02 mapping-plan artifact contract (proposals, relationship candidates, question records)"
      contains: "^Q-"
    - path: "contracts/harness/adoption/manifest.schema.json"
      provides: "ADOPT-03 disposition manifest artifact contract (total 6-value disposition enum + excluded lanes)"
      contains: "human-ratification-required"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined JCS SHA-256 manifest including the 3 new schemas"
      contains: "contracts/harness/adoption/inventory.schema.json"
    - path: "docs/reference/inventory.md"
      provides: "Regenerated Diátaxis reference page for the inventory schema"
    - path: ".memory/derived/contracts-index.md"
      provides: "Regenerated contracts index including the 3 new adoption schemas"
  key_links:
    - from: "contracts/harness/adoption/plan.schema.json"
      to: "contracts/harness/topology/relationship.schema.json"
      via: "duplicated (not $ref'd) relationship-candidate shape, ids namespaced adoption/<contract>/<authority>-><dependent>"
      pattern: "adoption/"
---

<objective>
Author the three human-ratified Draft 2020-12 contracts (`inventory.schema.json`,
`plan.schema.json`, `manifest.schema.json`) under `contracts/harness/adoption/` that govern
Phase 26's whole output (D-01), rebaseline the contract-hash manifest so contract-drift is green,
and regenerate the committed-derived plane (`docs/reference/*.md`, `contracts-index.md`) in this
same wave — closing the hidden gate coupling the research flags before any scanner code exists.

Purpose: contract-first — these three schemas are the single source of truth every later plan in
this phase (and Phase 27's task-plane binding) validates against. Landing them first, ratified,
means Plan 02/03 write CODE that conforms to an already-fixed shape instead of guessing.
Output: `contracts/harness/adoption/{inventory,plan,manifest}.schema.json`, a rebaselined
`contracts/.hashes/manifest.json`, and a fresh committed-derived plane.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-RESEARCH.md
@contracts/harness/task-control/evidence.schema.json
@contracts/harness/task-control/attestation.schema.json
@contracts/harness/topology/relationship.schema.json
@contracts/sample/greeting.schema.json
@tools/contract_hash/hash.py
@tools/contract_drift/drift.py
@tools/docs_sync/generate.py
@tools/memory_regen/contracts_index.py

<interfaces>
<!-- Ratification entrypoints this plan invokes verbatim (no re-implementation). -->
Rebaseline: `write_manifest(manifest_path=MANIFEST_PATH, contracts_dir=CONTRACTS_DIR) -> Path` in
`tools/contract_hash/hash.py`; CLI `python -m tools.contract_hash.hash --write`. The `**/*.schema.json`
glob in `build_manifest()` auto-discovers the 3 new files — never hand-edit the JSON.

Verify: `run_gate(contracts_dir=CONTRACTS_DIR, baseline_path=MANIFEST_PATH) -> dict` in
`tools/contract_drift/drift.py`; CLI `python -m tools.contract_drift.drift` (exit 0 iff clean).
Note: `run_gate` reports the newly-added schemas as `("added", "non-breaking")` but STILL counts
that as drift (`ok = not drifted`) until the rebaseline runs — expected RED between the author step
and the rebaseline step, per 26-RESEARCH.md "Pitfall 2".

Derived regen: `python -m tools.docs_sync` (writes one `docs/reference/<name>.md` per current
schema via `iter_schemas()`'s glob — fully dynamic, no manual page-list to update) and
`python -m tools.memory_regen.contracts_index` (rewrites `.memory/derived/contracts-index.md`).
Exact CI mirror (`.github/workflows/ci.yml` `stale-derived` job): `uv run python -m tools.docs_sync
&& uv run python -m tools.memory_regen.contracts_index` then `git add -A -- docs/reference
.memory/derived/contracts-index.md && git diff --cached --exit-code`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three ratified adoption schemas</name>
  <files>
    contracts/harness/adoption/inventory.schema.json,
    contracts/harness/adoption/plan.schema.json,
    contracts/harness/adoption/manifest.schema.json
  </files>
  <read_first>
    contracts/harness/task-control/evidence.schema.json (full — $defs pattern, additionalProperties:false at every level, pattern-anchored id strings),
    contracts/harness/task-control/attestation.schema.json (nested array-of-objects with a closed enum),
    contracts/harness/topology/relationship.schema.json (the shape plan.schema.json's relationship candidates duplicate — id/contract/authority/dependents/kind?/labels?),
    contracts/sample/greeting.schema.json (simplest exemplar — $schema/$id/title/type/additionalProperties/required/properties),
    26-CONTEXT.md D-01, D-02, D-03, D-04, D-05, D-08, D-09, D-10, D-11,
    26-RESEARCH.md "Schema Authoring & Ratification Procedure" (schema style template) and
      "Authoritative Harness Destination Catalog" (the 40-row catalog + disposition resolution order)
      and "D-05: Question-Record Shape (recommended)" (the exact recommended question shape)
  </read_first>
  <action>
    Create three self-contained Draft 2020-12 schemas (no cross-file `$ref` anywhere — D-11), each
    with `"$schema": "https://json-schema.org/draft/2020-12/schema"`, `"$id"` mirroring its on-disk
    repo-relative path exactly (e.g. `"https://harness.local/contracts/harness/adoption/inventory.schema.json"`),
    `"additionalProperties": false` at every object level, and explicit `"required"` arrays.

    **`inventory.schema.json`** (title "Brownfield adoption inventory"): top-level `required` =
    `[target_ref, enumeration_mode, max_file_bytes, included, excluded, languages, manifests,
    documentation_surfaces, ci_surfaces, test_surfaces, candidate_process_boundaries]`.
    `target_ref` (string, minLength 1 — the scanned target's git ref, or the literal `"unknown"`
    when the target has no git — a fact about the INPUT, never a clock, per the no-timestamp
    determinism rule). `enumeration_mode` (`enum: [git, builtin]` — D-09, self-describing so a run
    states which enumeration path it took). `max_file_bytes` (integer, minimum 1 — the size cap
    used for this run, self-describing per the researcher's A7 note). `included` (array of
    `$defs/includedEntry`). `excluded` (array of `$defs/excludedEntry`). `languages` /
    `manifests` / `documentation_surfaces` / `ci_surfaces` / `test_surfaces` /
    `candidate_process_boundaries` (each an array of a corresponding `$defs` record — see below).
    `$defs`: `includedEntry` = object, `additionalProperties:false`, `required:[path,size,sha256]`,
    properties `path` (string minLength 1, repo-relative POSIX), `size` (integer minimum 0),
    `sha256` (string, `pattern: "^[0-9a-f]{64}$"`). `excludedEntry` = object,
    `additionalProperties:false`, `required:[path,size,excluded]`, properties `path`, `size`, and
    `excluded` (`enum: [secret-path, secret-content, binary, vendored, generated, source-dump,
    size-capped, symlink-escape]`) — **deliberately no `sha256` property anywhere in this def** so
    D-10 ("no content hash, no content excerpt" on exclusions) is a structural schema guarantee, not
    a code convention. `evidenceRef` = object, `additionalProperties:false`,
    `required:[path,sha256]`, properties `path`, `sha256` (same pattern), `size` (optional integer
    minimum 0) — a lightweight pointer INTO an already-hashed `includedEntry`, reused by the
    detection records below. `classification` = `enum: [observed, inferred, unknown]` (D-02).
    `languageRecord` = object, `additionalProperties:false`,
    `required:[name,classification,evidence]`, properties `name` (string minLength 1),
    `classification` (`$ref: "#/$defs/classification"`), `evidence` (array of `evidenceRef`,
    `minItems: 1`). `manifestRecord` = same shape plus a required `path` and `kind` (string
    minLength 1 — e.g. `pyproject.toml`/`package.json`/`*.csproj`/`go.mod`/`Cargo.toml`) instead of
    `name`. `surfaceRecord` = object, `additionalProperties:false`,
    `required:[target,classification,evidence]`, properties `target` (string minLength 1 — a
    repo-relative path or a destination label), `classification`, `evidence` (array of
    `evidenceRef`, may be empty for an `unknown`/absent surface — `minItems: 0`), `rationale`
    (optional string — the structural rule id that produced an `inferred` classification, per D-02).
    `documentation_surfaces` / `ci_surfaces` / `test_surfaces` / `candidate_process_boundaries` are
    all arrays of `surfaceRecord`.

    **`plan.schema.json`** (title "Brownfield adoption mapping plan"): top-level `required` =
    `[target_ref, proposals, relationships, questions]`. `target_ref` (same shape as inventory's,
    ties the plan to the same scan). `proposals` (array of `$defs/proposalRecord`). `relationships`
    (array of `$defs/relationshipCandidate`). `questions` (array of `$defs/questionRecord`).
    `$defs`: duplicate `evidenceRef` and `classification` verbatim from `inventory.schema.json`
    (D-11 — self-contained, not shared via `$ref`). `proposalRecord` = object,
    `additionalProperties:false`, `required:[id,kind,classification,target,evidence]`, properties
    `id` (string minLength 1), `kind` (`enum: [member, component, relationship, contract-candidate,
    test-command, docs-destination, agents-boundary]` — the ADOPT-02 category list), `classification`
    (`$ref: "#/$defs/classification"`), `target` (string minLength 1), `evidence` (array of
    `evidenceRef`, `minItems: 1`), `rationale` (optional string). `relationshipCandidate` = object,
    `additionalProperties:false`, `required:[id,contract,authority,dependents]` — the SAME shape as
    `contracts/harness/topology/relationship.schema.json` (duplicated per D-11, not `$ref`'d):
    `id`/`contract`/`authority` (string minLength 1 each), `dependents` (array, `minItems: 1`,
    `uniqueItems: true`, items string minLength 1), `kind`/`labels` optional exactly as the topology
    schema defines them. `questionRecord` = object, `additionalProperties:false`,
    `required:[id,kind,target,question,classification,evidence]`, properties `id` (string,
    `pattern: "^Q-[0-9a-f]{12}$"` — content-derived stable id per D-05), `kind` (`enum:
    [relationship-authority, contract-candidate, component-boundary, member-boundary, test-command,
    docs-destination, agents-boundary, codeowners-ownership, collision, ambiguous-language,
    excluded-file]`), `group` (optional string — render-time grouping, D-05), `target` (string
    minLength 1), `question` (string minLength 1), `classification` (`$ref`), `evidence` (array of
    `evidenceRef`, `minItems: 1`), `candidate` (optional, `$ref: "#/$defs/candidateRecord"`),
    `blocking` (optional boolean). `candidateRecord` = object, `additionalProperties:false`,
    `required:[record_kind,record]`, properties `record_kind` (string minLength 1) and `record`
    (`type: object` with NO `required`/`additionalProperties` constraint — deliberately open so a
    partial/incomplete candidate — e.g. a relationship missing `authority` — validates; this is the
    structural guarantee that a `candidate` can never be mistaken for a ratified record, per
    26-RESEARCH.md's D-05 section).

    **`manifest.schema.json`** (title "Brownfield adoption disposition manifest"): top-level
    `required` = `[target_ref, dispositions, excluded]`. `target_ref` (same shape). `dispositions`
    (array of `$defs/dispositionRecord`). `excluded` (array of `$defs/excludedDestinationRecord`).
    `$defs`: duplicate `evidenceRef` verbatim (D-11). `dispositionEnum` = `enum: [create, preserve,
    conflict, marker-merge, derived-regenerate, human-ratification-required]` — EXACTLY these 6
    values, matching D-03/D-04's total rule chain, no more, no fewer. `dispositionRecord` = object,
    `additionalProperties:false`, `required:[destination,disposition]`, properties `destination`
    (string minLength 1 — a repo-relative POSIX path in the TARGET tree), `disposition` (`$ref:
    "#/$defs/dispositionEnum"`), `reason` (optional string — which rule in the chain fired, e.g.
    `constitution-plane` / `marker-capable:AGENTS.md` / `hash-equal` / `hash-differ` /
    `no-existing-target`), `evidence` (optional array of `evidenceRef`, `minItems: 0` — may be empty
    for path-only rules that never read content). `excludedDestinationRecord` = object,
    `additionalProperties:false`, `required:[destination,reason]`, properties `destination` (string
    minLength 1), `reason` (string minLength 1 — e.g. `gsd-owned`) — this is the row-40 predicate
    from 26-RESEARCH.md: GSD-owned lanes are recorded here as EXCLUDED, never as a `dispositions[]`
    entry, so a reviewer can tell "excluded" apart from "missed" (roadmap success criterion 3,
    totality).

    Write all three files with LF line endings and no BOM (the constitution-plane byte-hygiene
    `contract_guard` + `polyglot_lint.lint_bytes` enforce this on write regardless). Writing these
    three new files under `contracts/harness/adoption/` is itself a constitution-plane write that
    `contract_guard` denies unless the human-authorized `GOLDEN_APPROVE_HUMAN` token is present or
    this is an equivalent human-present write — do NOT fabricate the token; if the write is denied,
    pause and request human authorization before proceeding (same requirement as Task 2's rebaseline
    below).
  </action>
  <verify>
    <automated>for f in contracts/harness/adoption/inventory.schema.json contracts/harness/adoption/plan.schema.json contracts/harness/adoption/manifest.schema.json; do python -m json.tool "$f" > /dev/null || exit 1; grep -q '"draft/2020-12/schema"' "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    All three files are valid JSON (`python -m json.tool` exits 0 on each) and contain the exact
    `"$schema": "https://json-schema.org/draft/2020-12/schema"` string. `inventory.schema.json`'s
    `excludedEntry` def contains the substring `"excluded"` inside its `required` array and does
    NOT contain the substring `sha256` inside that same def's `properties` block (structural D-10
    guarantee — verify by locating the `excludedEntry` object and confirming its `properties` keys
    are exactly `path`/`size`/`excluded`). `plan.schema.json`'s `questionRecord.properties.id`
    carries `"pattern": "^Q-[0-9a-f]{12}$"`. `manifest.schema.json`'s `dispositionEnum` is exactly
    `["create", "preserve", "conflict", "marker-merge", "derived-regenerate",
    "human-ratification-required"]` (6 values, no others). No file contains a `$ref` string
    pointing outside its own `#/$defs/...` (grep for `"$ref": "http` or `"$ref": "contracts/` finds
    nothing — proves self-containment per D-11).
  </acceptance_criteria>
  <done>All three schemas exist under contracts/harness/adoption/, are valid self-contained Draft 2020-12 documents, and encode D-01/D-02/D-03/D-04/D-05/D-08/D-09/D-10/D-11 structurally.</done>
</task>

<task type="auto">
  <name>Task 2: Ratify — rebaseline contract-hash manifest, verify drift green</name>
  <files>contracts/.hashes/manifest.json</files>
  <read_first>
    tools/contract_hash/hash.py (write_manifest / build_manifest ** glob — auto-discovers new schemas),
    tools/contract_drift/drift.py (run_gate / classify — added-schema-is-still-drift semantics),
    26-RESEARCH.md "Pitfall 2: Drift gate is RED between authoring and rebaselining"
  </read_first>
  <action>
    Run `uv run python -m tools.contract_hash.hash --write` to rebaseline
    `contracts/.hashes/manifest.json`. The `**/*.schema.json` glob in `build_manifest()`
    auto-discovers all three new files under `contracts/harness/adoption/` — do NOT hand-edit the
    JSON. This rebaseline IS the ratification checkpoint for this constitution-plane addition: the
    diff lands under `contracts/.hashes/`, which is CODEOWNERS-gated on `/contracts/`
    (`.github/CODEOWNERS`) — a human reviews and approves it at merge (machines gate via the drift
    tool now; humans ratify at review, per the Phase-24 precedent this plan mirrors). Writing into
    `contracts/**` in this session requires either the human-authorized `GOLDEN_APPROVE_HUMAN`
    token or an equivalent human-present write — do not fabricate the token. Then run
    `uv run python -m tools.contract_drift.drift` and confirm it exits 0.
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift</automated>
  </verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0. `contracts/.hashes/manifest.json`
    contains all three keys `contracts/harness/adoption/inventory.schema.json`,
    `contracts/harness/adoption/plan.schema.json`, `contracts/harness/adoption/manifest.schema.json`.
    `git diff --stat contracts/.hashes/manifest.json` shows only these 3 keys added — no other
    manifest entry's hash changed.
  </acceptance_criteria>
  <done>contract-drift is green with all three new schemas tracked in the rebaselined baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human ratification of the constitution-plane schema landing</name>
  <files>contracts/harness/adoption/inventory.schema.json, contracts/harness/adoption/plan.schema.json, contracts/harness/adoption/manifest.schema.json, contracts/.hashes/manifest.json</files>
  <read_first>.github/CODEOWNERS (the /contracts/ routing + advisory-CODEOWNERS + solo-approval caveats), .planning/phases/24-.../24-01-PLAN.md Task 3 (the precedent this checkpoint mirrors)</read_first>
  <action>
    This is a BLOCKING human checkpoint — the agent does NOT self-ratify. The agent presents the
    three new schemas plus the rebaselined `contracts/.hashes/manifest.json` diff for human review,
    then PAUSES. A human confirms the schemas accurately encode D-01/D-02/D-03/D-04/D-05/D-08/D-09/
    D-10/D-11 (Task 1's acceptance criteria) and that the rebaseline touches ONLY the three new
    manifest keys, then ratifies via the standard constitution-plane path (CODEOWNERS PR review or
    an explicit human-supplied write — never agent self-approval or `HARNESS_DEV_BYPASS` used to
    self-land the ratification).
  </action>
  <what-built>
    Three new constitution-plane schemas under `contracts/harness/adoption/` plus a rebaselined
    `contracts/.hashes/manifest.json` — the human-ratified vocabulary the rest of Phase 26 (and
    Phase 27's task-plane binding) will consume.
  </what-built>
  <how-to-verify>
    1. Confirm `uv run python -m tools.contract_drift.drift` reports OK (Task 2's automated check).
    2. Review the diff under `contracts/harness/adoption/**` and `contracts/.hashes/manifest.json`
       — this is the CODEOWNERS-gated constitution-plane change (`.github/CODEOWNERS` routes
       `/contracts/` to the repo owner) that must be ratified at PR merge, same as ADR-0004/0005/0009.
    3. Confirm no agent self-approved this change (no fabricated `GOLDEN_APPROVE_HUMAN` token in a
       committed file — the token, if used, lives only in gitignored `.claude/settings.local.json`).
  </how-to-verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0; `git diff --stat contracts/.hashes/manifest.json` shows only the 3 new adoption-schema keys added.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: uv run python -m tools.contract_drift.drift</human-check>
  </verify>
  <resume-signal>Type "approved" once the constitution-plane diff has been reviewed, or describe required changes.</resume-signal>
  <done>The three adoption schemas and the rebaselined manifest are human-ratified via the standard constitution-plane path (CODEOWNERS review), not agent self-approval.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the committed-derived plane (docs_sync + contracts-index)</name>
  <files>docs/reference/inventory.md, docs/reference/plan.md, docs/reference/manifest.md, .memory/derived/contracts-index.md</files>
  <read_first>
    tools/docs_sync/generate.py (iter_schemas glob — fully dynamic, no page-list to hand-maintain; write() prune-then-write),
    tools/memory_regen/contracts_index.py (index_rows / write — regenerates from build_manifest()),
    .github/workflows/ci.yml lines ~227-256 (the exact `stale-derived` job commands this task must reproduce locally),
    26-RESEARCH.md "HIGH-IMPACT GATE COUPLING — three more artifacts move"
  </read_first>
  <action>
    Run `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`.
    `docs_sync`'s `iter_schemas()` globs `contracts/**/*.schema.json` dynamically, so it picks up
    the 3 new adoption schemas with zero code change and writes
    `docs/reference/{inventory,plan,manifest}.md`. `contracts_index` regenerates
    `.memory/derived/contracts-index.md` from the same rebaselined manifest. Commit the regenerated
    files in the same wave as Task 1/2 — never hand-write a reference page, and never leave this
    for a later commit (that is exactly what REDs `stale-derived` on an unrelated future commit,
    per 26-RESEARCH.md's Pitfall 1).
  </action>
  <verify>
    <automated>uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git add -A -- docs/reference .memory/derived/contracts-index.md && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md</automated>
  </verify>
  <acceptance_criteria>
    The exact `stale-derived` CI command sequence exits 0 (no diff after regeneration — the
    committed files already match). `docs/reference/inventory.md`, `docs/reference/plan.md`, and
    `docs/reference/manifest.md` exist and each starts with the `DERIVED — do not hand-edit` header
    line. `.memory/derived/contracts-index.md` lists all three new schema paths.
  </acceptance_criteria>
  <done>docs/reference/{inventory,plan,manifest}.md and .memory/derived/contracts-index.md are regenerated and committed; the stale-derived CI mirror is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent session → `contracts/harness/adoption/**` | Constitution-plane write; denied by `contract_guard` unless human-authorized (`GOLDEN_APPROVE_HUMAN`) or dev-bypassed — never agent self-ratified. |
| `contract_hash --write` output → `contracts/.hashes/manifest.json` | Machine-computed baseline; must never be hand-edited (would desync ordering/formatting and silently RED the drift gate on the next real change). |
| `docs_sync` / `contracts_index` output → committed-derived plane | Machine-regenerated; CI's `stale-derived` job is the non-bypassable verify side of "never hand-edit a derived file." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Elevation of Privilege | `contracts/harness/adoption/*.schema.json` write path | accept (out-of-band) | Unchanged existing controls: `contract_guard` PreToolUse deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS `/contracts/`. This plan adds no new bypass; residual risk (CODEOWNERS is advisory unless branch protection is enabled, and a solo author cannot self-approve) is pre-accepted per Phase-24's `T-24-02` precedent. Task 3 of this plan is an explicit `checkpoint:human-verify` surfacing the ratification review. |
| T-26-01-02 | Tampering | `contracts/.hashes/manifest.json` | mitigate | Rebaseline ONLY via `tools.contract_hash.hash --write` (Task 2) — never hand-edited. `write_manifest` always emits `sort_keys=True, indent=2`, so the committed diff is byte-deterministic and reviewable. |
| T-26-01-03 | Repudiation | Schema content itself | mitigate | Each schema is `additionalProperties:false` at every level with explicit `required` arrays — a malformed downstream artifact fails `Draft202012Validator` loudly (Plan 02/03's schema-conformance tests) rather than silently passing. |
| T-26-01-04 | Information Disclosure | `manifest.schema.json` disposition-record `evidence` field | mitigate | `evidence` items are `evidenceRef` (path + sha256 of an ALREADY-hashed included file) — the manifest schema never introduces a path to embed raw secret content; that guarantee is enforced structurally in `inventory.schema.json`'s `excludedEntry` (Task 1, D-10). |
| T-26-01-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan (schema authoring + existing `tools.contract_hash`/`tools.contract_drift`/`tools.docs_sync`/`tools.memory_regen` CLIs only); no slopcheck required. |
</threat_model>

<verification>
- `python -m json.tool` on each of the 3 new schema files exits 0 and each carries the Draft 2020-12 `$schema` string.
- `uv run python -m tools.contract_drift.drift` exits 0 (drift gate green post-rebaseline).
- `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md` — committed-derived plane fresh.
</verification>

<success_criteria>
- `contracts/harness/adoption/{inventory,plan,manifest}.schema.json` exist, are valid self-contained Draft 2020-12 documents, and structurally encode D-01/D-02/D-03/D-04/D-05/D-08/D-09/D-10/D-11.
- `contracts/.hashes/manifest.json` is rebaselined and `contract_drift.run_gate` reports clean.
- `docs/reference/{inventory,plan,manifest}.md` and `.memory/derived/contracts-index.md` are regenerated and committed in this same wave.
- The constitution-plane diff has been surfaced for human ratification (Task 3 checkpoint).
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-01-SUMMARY.md` when done
</output>
