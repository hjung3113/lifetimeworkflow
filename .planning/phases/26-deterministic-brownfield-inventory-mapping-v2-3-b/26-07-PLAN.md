---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_scan/destinations.py
  - tools/adoption_scan/tests/test_dispositions.py
  - tools/adoption_scan/tests/test_snapshots.py
  - tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr
autonomous: true
requirements: [ADOPT-03]
gap_closure: true

must_haves:
  truths:
    - "26-VERIFICATION.md gap 1 (CR-01, ADOPT-03/roadmap goal 'fully CI-testable'), closed: destination_catalog() filters every glob match through a git-tracked-only check (git ls-files, fixed argv, shell=False, failure-tolerant) so a gitignored/untracked file present only on a developer's working tree (e.g. .memory/derived/pointer-index.json) never enters the catalog; a real git worktree checked out at HEAD produces the SAME catalog destination set as the current tree's git-tracked-filtered catalog, independent of any local untracked derived state"
    - "26-VERIFICATION.md gap 1 (CR-02), closed: the committed test_snapshots.ambr's manifest section is rendered over a small FIXED catalog list injected into build_manifest(), not the live destination_catalog() — so a future unrelated harness file add/remove (a new how-to page, a new ADR) does not red this test; live-catalog totality/coverage stays proven by the existing structural tests (test_catalog_covers_real_contract_schemas etc.), which are unchanged"
    - "WR-02 folded in: destination_catalog() skips any candidate whose path parts intersect scan.py's existing _VENDOR_SEGMENTS/_GENERATED_SEGMENTS constants (reused, not re-derived) — belt-and-suspenders for the git-unavailable fallback path where the git-tracked filter cannot run"
    - "WR-04 folded in: a destination string is derived from the ENUMERATED candidate path (candidate.relative_to(_REPO_ROOT)), never from the resolved symlink target — so two distinct in-repo symlinks to the same file produce two distinct catalog rows, not one deduplicated row naming the target's path"
    - "WR-11 folded in: test_total (renamed if needed) explicitly partitions destination_catalog() into dispositioned vs excluded results and asserts their counts sum to len(catalog) — a regression that made every row resolve to None (e.g. a broken is_gsd_owned) turns this test RED, not vacuously green; the '> 100' magic number is replaced by a named, commented _MIN_CATALOG_ROWS constant"
  artifacts:
    - path: "tools/adoption_scan/destinations.py"
      provides: "_tracked_repo_files() (git-tracked filter, D-09 idiom), _SKIP_SEGMENTS (WR-02 reuse of scan.py's vendor/generated denylists), destination derivation from the enumerated (not resolved) path (WR-04), build_manifest(..., *, catalog=None) injectable-catalog parameter (CR-02)"
      min_lines: 60
    - path: "tools/adoption_scan/tests/test_snapshots.py"
      provides: "test_artifacts_match_committed_snapshot renders the manifest section over a small, hand-listed fixed catalog passed to build_manifest(catalog=...), decoupled from the live repo's file count"
    - path: "tools/adoption_scan/tests/test_dispositions.py"
      provides: "A clean-worktree reproduction test proving catalog invariance to local untracked state (git worktree add --detach at HEAD), plus the WR-11 explicit-partition test_total rewrite"
  key_links:
    - from: "tools/adoption_scan/destinations.py::destination_catalog"
      to: "git ls-files (subprocess, fixed argv)"
      via: "_tracked_repo_files()"
      pattern: "git.*ls-files"
    - from: "tools/adoption_scan/tests/test_snapshots.py"
      to: "tools/adoption_scan/destinations.py::build_manifest"
      via: "explicit catalog= keyword argument, not the live destination_catalog() default"
      pattern: "catalog="
---

<objective>
Close 26-VERIFICATION.md's two new CRITICAL regressions introduced by Plan 26-06's rule-derived
catalog rewrite: CR-01 (the catalog is not reproducible on a clean checkout because it bakes in
gitignored/untracked `.memory/derived/*` files) and CR-02 (the committed snapshot pins the live
repo's exact file count, so unrelated harness growth reds an adoption-scanning test). Fold in three
cheap, same-file review warnings while the file is open: WR-02 (vendor/generated denylist reuse,
belt-and-suspenders for the git-unavailable path), WR-04 (symlink identity — derive the destination
from the enumerated path, not the resolved target), and WR-11 (make `test_total` an actual totality
proof instead of a vacuously-passable one).

Purpose: the phase goal's own text — "agent-free, fully CI-testable" — is violated by a test that
passes locally but reds on `actions/checkout` (CI's exact shape, no `.memory` regen step). This plan
makes `destination_catalog()` a function of git-tracked state only, and decouples the committed
snapshot from the live repo's size entirely, so both properties hold unconditionally going forward.
Output: `destinations.py` with a git-tracked filter + injectable catalog + symlink-identity fix +
vendor denylist reuse; `test_dispositions.py`/`test_snapshots.py` with the corresponding tests; a
refreshed committed snapshot.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VERIFICATION.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-REVIEW.md
@tools/adoption_scan/destinations.py
@tools/adoption_scan/tests/test_dispositions.py
@tools/adoption_scan/tests/test_snapshots.py
@tools/harness_lint/tests/test_core_no_example_dep.py
@tools/adoption_scan/scan.py

<interfaces>
<!-- The repo's ONE git-tracked-filter idiom (D-09-sanctioned: fixed argv, shell=False,
failure-tolerant), verbatim from tools/harness_lint/tests/test_core_no_example_dep.py — mirror this
shape exactly, do not invent a second one. -->
```python
def _tracked_core_files() -> list[Path]:
    completed = subprocess.run(
        ["git", "ls-files", *_CORE_ROOTS],
        cwd=_REPO_ROOT,
        capture_output=True,
        text=True,
        check=True,
    )
    ...
```
This plan's version must be FAILURE-TOLERANT (`check=False`, inspect `returncode`), unlike the guard
above (which is allowed to hard-fail since git is guaranteed present in this repo's own CI/dev
environment for that particular guard's purpose) — `destination_catalog()` must degrade to
unfiltered enumeration when git is unavailable, per D-09's "MUST NOT depend on git" rule, rather
than raising.

<!-- scan.py's existing vendor/generated denylist constants (WR-02 reuse target) — read, do not
redefine: -->
`tools/adoption_scan/scan.py` lines ~57-75: `_VENDOR_SEGMENTS: set[str]`,
`_GENERATED_SEGMENTS: set[str]` — both plain path-segment string sets already used by `scan.py`'s
own `enumerate_target` walk (lines ~211/216: `if any(seg in _VENDOR_SEGMENTS for seg in parts)`).
Import both directly; do not copy their contents into `destinations.py`.

<!-- The clean-worktree reproduction command sequence this plan's CR-01 test drives (git worktree,
not git clone — no network, reuses this repo's own object store, fast). Run from a test using
tmp_path for the worktree location and subprocess.run with explicit argv lists (never shell=True):
-->
```
git worktree add --detach <tmp_path>/clean-worktree HEAD
# then, with sys.executable and PYTHONPATH=<tmp_path>/clean-worktree (reusing THIS process's
# already-synced venv/interpreter -- the destinations.py import chain is stdlib-only + intra-repo
# tools/ imports, no third-party package needs re-installing in the worktree):
<sys.executable> -c "from tools.adoption_scan import destinations; import json, sys; \
  print(json.dumps(sorted(r['destination'] for r in destinations.destination_catalog())))"
# then, always, even on failure:
git worktree remove --force <tmp_path>/clean-worktree
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Git-tracked filter (CR-01) + vendor/generated denylist (WR-02) + symlink identity (WR-04) + totality rigor (WR-11)</name>
  <files>tools/adoption_scan/destinations.py, tools/adoption_scan/tests/test_dispositions.py</files>
  <read_first>
    tools/adoption_scan/destinations.py (full — `destination_catalog()`, `_CATEGORY_GLOBS`,
      `_EXCLUDED_PREFIX`, `_INSTANCE_DIR_NAME` all stay; only the enumeration loop's filtering and
      destination-derivation logic change),
    tools/harness_lint/tests/test_core_no_example_dep.py `_tracked_core_files()` (the git-tracked
      idiom to mirror, made failure-tolerant per the `<interfaces>` note),
    tools/adoption_scan/scan.py lines ~50-80 (`_VENDOR_SEGMENTS`/`_GENERATED_SEGMENTS` — the WR-02
      reuse target) and lines ~200-220 (how `enumerate_target` already applies them),
    26-REVIEW.md CR-01 + WR-02 + WR-04 + WR-11 sections (the exact reproduction evidence and
      suggested fixes this task must satisfy),
    tools/adoption_scan/tests/test_dispositions.py (full — `test_total`, `_PLACEHOLDER_DESTINATIONS`,
      `test_catalog_deterministic_across_calls` — the file this task extends)
  </read_first>
  <behavior>
    - Add `_tracked_repo_files() -> frozenset[str] | None` to `destinations.py`: runs
      `subprocess.run(["git", "-C", str(_REPO_ROOT), "ls-files"], capture_output=True, check=False,
      shell=False)`, catching `OSError` (git binary missing) as well as inspecting `returncode != 0`
      — both cases return `None` (git unavailable/failed — D-09's "MUST NOT depend on git" case).
      On success, returns `frozenset(proc.stdout.decode("utf-8", "surrogateescape").splitlines())`
      of repo-relative POSIX paths.
    - Add `_SKIP_SEGMENTS: frozenset[str]` built from
      `frozenset(scan._VENDOR_SEGMENTS) | frozenset(scan._GENERATED_SEGMENTS)` — import
      `tools.adoption_scan.scan` for this (module-level import of `scan`, or
      `from tools.adoption_scan.scan import _VENDOR_SEGMENTS, _GENERATED_SEGMENTS` — either is fine,
      pick one and use it consistently).
    - Rewrite `destination_catalog()`'s inner loop: after computing `resolved` and the containment
      check (unchanged), derive `destination = candidate.relative_to(_REPO_ROOT).as_posix()` — from
      the ENUMERATED `candidate` path, not `resolved.relative_to(root_resolved)` (WR-04: `resolved`
      is used ONLY for the containment check from here on, never for identity). Then, in this order:
      skip if `tuple(parts[:len(_EXCLUDED_PREFIX)]) == _EXCLUDED_PREFIX` (unchanged), skip if
      `parts[0] == _INSTANCE_DIR_NAME` (unchanged), skip if `any(seg in _SKIP_SEGMENTS for seg in
      parts)` (WR-02, new), skip if `tracked is not None and destination not in tracked` (CR-01,
      new) — call `_tracked_repo_files()` ONCE at the top of `destination_catalog()` (not per
      candidate) and reuse the result across the whole loop for both determinism and performance.
    - Update `destination_catalog()`'s docstring: state the git-tracked filter, its
      failure-tolerant fallback (unfiltered when git is unavailable, per D-09), the vendor/generated
      belt-and-suspenders skip, and the WR-04 identity fix (destination now derived from the
      enumerated path).
    - Update the module docstring's CR-01 narrative: the OLD CR-01 fix note (about
      `harness_proposed_hash`/target-vs-template independence) stays — it is a DIFFERENT CR-01 from
      a prior review round, still valid — but add a new paragraph documenting THIS plan's CR-01 fix
      (git-tracked-only catalog enumeration) distinctly, so a future reader does not conflate the two
      same-named findings from different review rounds.
    - Test (WR-11): rename or rewrite `test_total` so it computes
      `results = [destinations.disposition(row["destination"], tmp_path, proposed_sha=None) for row
      in catalog]`, `dispositioned = [r for r in results if r is not None]`,
      `excluded = [r for r in results if r is None]`, and asserts: `dispositioned` is non-empty,
      `len(dispositioned) + len(excluded) == len(catalog)`,
      `set(dispositioned) <= set(destinations.DISPOSITION_ENUM)`, and
      `len(catalog) >= _MIN_CATALOG_ROWS` where `_MIN_CATALOG_ROWS` is a new named module-level
      constant in `test_dispositions.py` set to `300` with a one-line comment explaining it is a
      loose sanity floor on the real, git-tracked-filtered catalog size, not an exact count.
    - Test (CR-01, the mandatory clean-checkout reproduction): add
      `test_catalog_invariant_to_untracked_local_state` in `test_dispositions.py`. Using
      `tmp_path_factory` or `tmp_path`, run `git worktree add --detach <worktree_dir> HEAD` via
      `subprocess.run` (explicit argv list, `cwd=str(_REPO_ROOT)` — import `destinations._REPO_ROOT`
      or recompute the repo root the same way test_core_no_example_dep.py does), wrapped in
      `try/finally` so `git worktree remove --force <worktree_dir>` always runs. Inside the `try`,
      invoke `subprocess.run([sys.executable, "-c", <the one-liner from context's <interfaces>
      block>], cwd=str(worktree_dir), capture_output=True, text=True, check=True)`, parse the
      printed JSON list, and assert it equals `sorted(row["destination"] for row in
      destinations.destination_catalog())` computed in the CURRENT tree — i.e. the catalog is
      byte-identical between a fresh worktree checkout at HEAD and the current (possibly
      untracked-state-laden) working tree, proving the untracked `.memory/derived/*` files (if
      present locally) never affected the result. Skip the test (`pytest.skip`) if `git worktree
      add` fails for environmental reasons (e.g. sandboxed/no-git-object-write environments) — but
      NOT if it fails because the catalogs differ; that must fail loud.
    - Test (CR-01, fast companion): add
      `test_catalog_excludes_untracked_file_in_matched_category`. Write a throwaway file at
      `_REPO_ROOT / ".memory" / "derived" / "__cr01_untracked_proof__.md"` (a real path under this
      checkout matching the `.memory/derived/**/*` category glob, genuinely gitignored per
      `.gitignore`'s `.memory/derived/*` rule), wrapped in `try/finally` so the file is always
      removed. Assert `".memory/derived/__cr01_untracked_proof__.md" not in {row["destination"] for
      row in destinations.destination_catalog()}` while the file exists.
    - Test (WR-02): add `test_catalog_excludes_vendor_and_generated_segments` asserting no
      catalog row's `destination.split("/")` intersects
      `destinations._SKIP_SEGMENTS` (import the private constant directly — this is a structural
      predicate test, not a fixture-creation test, since real vendor dirs like `.venv`/
      `node_modules` may or may not exist in this checkout at test time).
    - Test (WR-04): add `test_symlink_identity_uses_enumerated_path_not_resolved_target`. Using
      `monkeypatch.setattr(destinations, "_REPO_ROOT", tmp_path)`, build a tiny synthetic tree under
      `tmp_path` matching one `_CATEGORY_GLOBS` pattern twice — e.g. `tmp_path / "docs" / "adr" /
      "0001-real.md"` (real file, content arbitrary) and `tmp_path / "docs" / "adr" / "0002-link.md"`
      (a symlink to `0001-real.md`, skip via `pytest.skip` on `OSError` if symlinks are unprivileged
      here, mirroring `tmp_minirepo`'s existing symlink-skip precedent) — then assert
      `destinations.destination_catalog()` contains BOTH `"docs/adr/0001-real.md"` AND
      `"docs/adr/0002-link.md"` as two distinct rows (not deduplicated to one), proving destination
      identity is the enumerated path, not `resolved`'s target. NOTE: with `_REPO_ROOT`
      monkeypatched to a `tmp_path` outside git, `_tracked_repo_files()` will return `None` (git
      unavailable for that fake root, or the file is genuinely untracked) — that is fine and
      expected; the test is specifically isolating the WR-04 identity behavior, not the CR-01
      filter.
  </behavior>
  <action>
    Implement `_tracked_repo_files()` and `_SKIP_SEGMENTS`, and rewrite `destination_catalog()`'s
    loop exactly as specified in `<behavior>`. Update both docstrings. Add the six new tests to
    `test_dispositions.py` (WR-11 rewrite of `test_total`, the two CR-01 tests, the WR-02 test, the
    WR-04 test) exactly as specified.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_dispositions.py -q</automated>
  </verify>
  <acceptance_criteria>
    `test_dispositions.py` passes, including all new tests.
    `test_catalog_invariant_to_untracked_local_state` performs an actual `git worktree add --detach`
    at HEAD and compares the resulting catalog against the current tree's — this is a real
    clean-checkout reproduction, not a green-on-this-tree-only assertion.
    `test_catalog_excludes_untracked_file_in_matched_category` proves a live-created untracked
    `.memory/derived/*` file is excluded from the catalog while it exists.
    `destination_catalog()`'s destination derivation uses `candidate.relative_to(_REPO_ROOT)`, never
    `resolved.relative_to(...)`.
    `destinations.py` imports `_VENDOR_SEGMENTS`/`_GENERATED_SEGMENTS` from `scan.py` rather than
    redefining them.
  </acceptance_criteria>
  <done>destination_catalog() is filtered to git-tracked files with a failure-tolerant fallback, reuses scan.py's vendor/generated denylist, derives destination identity from the enumerated path (not the resolved symlink target), and test_total is an explicit, non-vacuous totality partition proof.</done>
</task>

<task type="auto">
  <name>Task 2: Injectable catalog for build_manifest (CR-02) + fixed-catalog snapshot test + full verify</name>
  <files>tools/adoption_scan/destinations.py, tools/adoption_scan/tests/test_snapshots.py, tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr</files>
  <read_first>
    tools/adoption_scan/destinations.py `build_manifest()` (the function this task adds a keyword
      parameter to — `disposition()` itself is unchanged),
    tools/adoption_scan/tests/test_snapshots.py (the single snapshot test this task updates),
    26-REVIEW.md CR-02 section (the exact suggested `catalog=None` signature and rationale)
  </read_first>
  <behavior>
    - `build_manifest(inventory, target_root, proposed_hashes, *, catalog=None) -> dict`: when
      `catalog` is `None`, call `destination_catalog()` as before (unchanged default behavior for
      every existing caller — `cli.py`, `test_dispositions.py`'s existing tests); when `catalog` is
      provided (a `list[dict]` of `{"destination": str}` rows, the same shape
      `destination_catalog()` returns), iterate that instead. No other change to the function body.
    - `test_snapshots.py`: replace the live `destinations.harness_proposed_hashes()` +
      `destinations.build_manifest(inventory, tmp_minirepo, proposed_hashes)` call with a small,
      hand-listed FIXED catalog — e.g. a module-level tuple of ~8-12 `{"destination": ...}` rows
      spanning every `DISPOSITION_ENUM` case reachable against the `tmp_minirepo` fixture (a
      `contracts/**` path for `human-ratification-required`, a `docs/reference/**` path for
      `derived-regenerate`, `"AGENTS.md"` for `marker-merge`, a path that exists in `tmp_minirepo`
      with harness-matching content for `preserve` if one exists or otherwise a `create`/`conflict`
      pair using real `pyproject.toml`/`.github/workflows/ci.yml` destinations, mirroring
      `test_cr01_conflict_reachable_through_real_cli`'s existing fixture destinations) — pass it as
      `destinations.build_manifest(inventory, tmp_minirepo, proposed_hashes, catalog=_FIXED_CATALOG)`
      so the manifest section of the snapshot is a function of this fixed list, never the live
      repo's file count.
    - Re-run `uv run pytest tools/adoption_scan/tests/test_snapshots.py -q --snapshot-update` and
      inspect the diff: the `===== manifest =====` section should now be small and STABLE (bounded
      by `_FIXED_CATALOG`'s length, not ~340 live rows); the `===== inventory =====`/
      `===== plan =====` sections should be unchanged from before this plan (Task 1 touches neither
      `scan.py` nor `plan.py`).
    - Confirm the existing live-catalog structural tests
      (`test_catalog_covers_real_contract_schemas`, `test_catalog_covers_real_nested_agents_md`,
      `test_no_fictional_placeholder_destinations`, `test_workflow_tasks_excluded`,
      `test_catalog_excludes_instance_directory`, `test_catalog_deterministic_across_calls`,
      `test_gsd_lanes_excluded`, `test_harness_proposed_hash_independent_of_target`,
      `test_cr01_conflict_reachable_through_real_cli`, `test_cr01_repro_throwaway_junk_target`) all
      still pass UNCHANGED — they continue to exercise the live `destination_catalog()` default
      (`catalog=None`), which is exactly the CR-02 fix's intent: live-catalog coverage stays proven
      by these, while the committed byte-snapshot decouples from repo size.
  </behavior>
  <action>
    Add the `catalog=None` keyword parameter to `build_manifest()` per the behavior above. Update
    `test_snapshots.py` to pass a small fixed catalog. Regenerate the snapshot with
    `--snapshot-update` and inspect the diff per the behavior notes. Run the full verification
    sequence.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan -q && uv run python -m tools.contract_drift.drift && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q && uv sync --all-packages && git diff --exit-code uv.lock</automated>
  </verify>
  <acceptance_criteria>
    Full `tools/adoption_scan` suite passes. `build_manifest`'s live-catalog default (`catalog=None`)
    is unchanged for every other caller. The refreshed `test_snapshots.ambr`'s manifest section is
    bounded by the fixed catalog's length (single-digit-to-low-double-digit row count), not the live
    repo's ~340. `uv run python -m tools.contract_drift.drift` exits 0 (no schema touched). The
    GEN-04 guard stays green. `uv.lock` is unchanged.
  </acceptance_criteria>
  <done>build_manifest() accepts an injectable catalog; the committed snapshot's manifest section is rendered over a small fixed catalog, decoupled from the live repo's file count; live-catalog coverage stays proven by the unchanged structural tests; full suite/drift/GEN-04/uv.lock all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_tracked_repo_files()`'s `git ls-files` subprocess | Fixed argv, `shell=False`, `cwd`-pinned to `_REPO_ROOT`, failure-tolerant (returns `None` on any error) — mirrors the repo's one sanctioned git-invocation idiom (D-09, `test_core_no_example_dep.py::_tracked_core_files`). Never executes a discovered/user-supplied command. |
| The clean-worktree reproduction test's `git worktree` subprocess | Also fixed argv, `shell=False`; operates only on THIS repo's own `.git` (no network, no external repo); always removed in a `finally` block. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Tampering | `destination_catalog()`'s reproducibility across checkouts | mitigate | The git-tracked filter (CR-01) plus the clean-worktree reproduction test make the catalog provably invariant to local untracked state — the exact regression this plan closes. |
| T-26-07-02 | Repudiation | Committed snapshot coupling to live repo size (CR-02) | mitigate | `build_manifest(..., catalog=...)` decouples the committed byte-snapshot from the live catalog; live coverage moves to the existing, unchanged structural tests. |
| T-26-07-03 | Tampering | `git worktree`/`git ls-files` subprocess argv injection | mitigate | Fixed, hardcoded argv lists only (`["git", "-C", str(_REPO_ROOT), "ls-files"]`, `["git", "worktree", "add", "--detach", str(worktree_dir), "HEAD"]`) — no user/target-supplied strings interpolated into the command, `shell=False` throughout. |
| T-26-07-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_scan -q` — full suite green, including the new git-tracked-filter/symlink-identity/totality tests and the clean-worktree reproduction.
- `uv run python -m tools.contract_drift.drift` — green (no schema touched by this plan).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` — GEN-04 guard green.
- `uv sync --all-packages && git diff --exit-code uv.lock` — lockfile unchanged.
</verification>

<success_criteria>
- `destination_catalog()` is invariant to local untracked/gitignored state — proven by an actual `git worktree` clean-checkout comparison, not merely green on this working tree.
- The committed `test_snapshots.ambr`'s manifest section is decoupled from the live repo's file count via an injectable `catalog` parameter.
- Symlink identity, vendor/generated denylist, and totality-test rigor (WR-04/WR-02/WR-11) are folded in.
- Full suite, contract-drift, GEN-04 guard, and `uv.lock` all stay green.
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-07-SUMMARY.md` when done
</output>
