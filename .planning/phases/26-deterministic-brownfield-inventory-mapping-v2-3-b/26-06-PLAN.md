---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_scan/destinations.py
  - tools/adoption_scan/tests/test_dispositions.py
  - tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr
autonomous: true
requirements: [ADOPT-03]
gap_closure: true

must_haves:
  truths:
    - "26-VERIFICATION.md gap 2 (ADOPT-03 / roadmap SC-3), closed: destination_catalog() is rule-derived — glob-enumerated from this harness checkout's own real file tree at call time, sorted, deduplicated — never a fixed-size literal sample; running it against this repo yields catalog rows for all 11 real *.schema.json files, all 10 real ADRs, all 23 real harness/commands/*.md, all 11 real harness/skills/*/SKILL.md, all 38 real .claude/agents/*.md, all 5 real .opencode/agent/*.md, and all 3 real non-root nested AGENTS.md files — not one representative sample row per category"
    - "None of the 10 confirmed-nonexistent placeholder paths from 26-VERIFICATION.md gap 2 (harness/agents/widget-engineer.md, harness/commands/widget-check.md, harness/skills/widget-conventions/SKILL.md, .opencode/agent/widget-engineer.md, .claude/agents/widget-engineer.md, golden/widget/verified/case.txt, docs/adr/0001-decision.md, .memory/agreements/0001-widget.md, .workflow/tasks/T-0001/task.json, tools/widget_tool/pyproject.toml) appears anywhere in destination_catalog()'s output"
    - "build_manifest() (unchanged) still resolves every non-GSD-owned catalog row to exactly one of the 6 dispositionEnum values (totality holds over the larger, real catalog, not just the old 40-row sample)"
    - ".workflow/tasks/** is explicitly, documentedly excluded from the rule-derived catalog (session-local per-run task data with no static template — enumerating it would make the destination set drift with every GSD session, breaking the committed snapshot's reproducibility across commits) — this is a stated scoping decision, not a silent gap; SC-3's 'every harness destination' is read as 'every destination the harness template can propose static content for'"
  artifacts:
    - path: "tools/adoption_scan/destinations.py"
      provides: "destination_catalog() rewritten to enumerate real files from _REPO_ROOT via a category-glob table, sorted + deduplicated, replacing the static 40-row _CATALOG tuple"
      min_lines: 60
    - path: "tools/adoption_scan/tests/test_dispositions.py"
      provides: "Live structural totality assertions (catalog counts cross-checked against fresh glob counts, not hardcoded literals) + placeholder-removal assertions + GSD-exclusion-by-predicate (not by hardcoded row number)"
  key_links:
    - from: "tools/adoption_scan/destinations.py"
      to: "tools/harness_emit/manifest.py"
      via: "is_gsd_owned() still the sole GSD-exclusion predicate, applied uniformly to every enumerated row regardless of catalog size"
      pattern: "is_gsd_owned"
---

<objective>
Close 26-VERIFICATION.md gap 2: rewrite `destinations.py::destination_catalog()` from a static,
hand-picked 40-row sample (10 of whose rows are literal nonexistent placeholder paths) into a
rule-derived enumeration of this harness checkout's OWN real file tree — one catalog row per real
file matching each named destination category's glob, sorted, deduplicated — so `build_manifest()`
genuinely covers "every harness destination" instead of one representative example per plane.

Purpose: ADOPT-03 / roadmap success criterion 3 requires "every harness destination resolves to
exactly one disposition." A fixed 40-row sample can never satisfy that literally — this plan makes
the catalog grow and shrink with the harness's actual file tree instead of a curated guess,
per the hard_constraints scoping instruction to prefer rule-derived enumeration over narrowing the
requirement's wording. `disposition()`'s 7-step rule chain (D-03/D-04) and `build_manifest()`
itself are UNCHANGED — they already operate generically over whatever `destination_catalog()`
returns; only the catalog's construction changes.
Output: `tools/adoption_scan/destinations.py` with a rule-derived `destination_catalog()`, updated
`test_dispositions.py`, and a refreshed committed snapshot's manifest section.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-VERIFICATION.md
@tools/adoption_scan/destinations.py
@tools/adoption_scan/tests/test_dispositions.py
@tools/harness_emit/manifest.py
@tools/harness_emit/merge.py

<interfaces>
<!-- UNCHANGED public surface — this plan does not touch these signatures. -->
`disposition(rel, target_root, proposed_sha, *, existing_sha=None) -> str | None` — the 7-step
chain, untouched.
`build_manifest(inventory, target_root, proposed_hashes) -> dict` — untouched; already iterates
`destination_catalog()` generically.
`harness_proposed_hash(destination) -> str | None` / `harness_proposed_hashes() -> dict[str, str]`
— untouched; now returns a hash for EVERY catalog row (since every row, by construction, exists in
`_REPO_ROOT` — the enumeration source IS `_REPO_ROOT`), which is a strictly more useful "proposed
content" answer than before, not a regression (CR-01's "no-template-source → None → forced
conflict" behavior remains available for any destination NOT in the catalog, e.g. one Phase 27
adds later).
`is_gsd_owned(rel: str) -> bool` from `tools.harness_emit.manifest` — still the sole GSD-exclusion
predicate; still applied inside `disposition()` step 1, unchanged.
`MARKER_CAPABLE: frozenset[str]` — unchanged, still exactly `{"AGENTS.md", "CLAUDE.md",
".claude/settings.json"}`.
`DISPOSITION_ENUM` — unchanged, the 6-value tuple.

<!-- The confined-walk idiom this task's enumeration reuses (already used by scan.py, harness_emit,
contract_hash — the repo's ONE traversal-defense idiom). -->
```python
root_resolved = root.resolve()
for p in sorted(root.rglob("*")):
    if not p.is_file():
        continue
    resolved = p.resolve()
    if root_resolved != resolved and root_resolved not in resolved.parents:
        continue
    files.append(p)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rule-derived destination_catalog() replacing the static 40-row sample</name>
  <files>tools/adoption_scan/destinations.py, tools/adoption_scan/tests/test_dispositions.py</files>
  <read_first>
    tools/adoption_scan/destinations.py (full — the file this task rewrites; `_REPO_ROOT`,
      `MARKER_CAPABLE`, `DERIVED_GLOBS`, `disposition()`, `build_manifest()` all stay as-is; only
      `_CATALOG`/`destination_catalog()` change),
    26-VERIFICATION.md gap 2 (`gaps[1]`) — the exact placeholder-path list and real-file-count
      evidence this task must make true,
    26-RESEARCH.md "Authoritative Harness Destination Catalog" (the category list — this task
      replaces its literal-path rows with glob patterns over the SAME categories),
    tools/harness_emit/manifest.py (`is_gsd_owned`, `_GSD_DIR_FRAGMENTS`, `_GSD_NAME_PREFIX` — the
      exclusion predicate that already handles `.claude/get-shit-done/**`/`.claude/hooks/**`/
      `.claude/commands/gsd/**`/`gsd-*` files structurally; this task's globs may legitimately match
      GSD-owned paths — that is fine, `disposition()` step 1 already excludes them into
      `manifest["excluded"]`, no pre-filtering needed in the catalog itself)
  </read_first>
  <behavior>
    - Replace the static `_CATALOG` tuple with a module-level `_CATEGORY_GLOBS: tuple[str, ...]` of
      glob patterns (relative to `_REPO_ROOT`, passed to `Path.glob()`), one entry per named
      ADOPT-03 destination category, covering (at minimum): `"contracts/**/*"`, `"golden/**/*"`,
      `"docs/adr/**/*"`, `"docs/tutorials/**/*"`, `"docs/how-to/**/*"`, `"docs/explanation/**/*"`,
      `"docs/glossary.md"`, `"docs/reference/**/*"`, `".memory/state/**/*"`,
      `".memory/derived/**/*"`, `".memory/agreements/**/*"`, `".memory/README.md"`,
      `"harness/project.toml"`, `"workspace.toml"`, `"harness/permission-matrix.json"`,
      `"harness/risk-policy.toml"`, `"harness/opencode.json"`,
      `"harness/opencode.config.schema.json"`, `"harness/agents/**/*"`, `"harness/commands/**/*"`,
      `"harness/skills/**/*"`, `"harness/plugins/**/*"`, `"harness/git-hooks/**/*"`,
      `".opencode/**/*"`, `".claude/agents/**/*"`, `".claude/commands/**/*"`,
      `".claude/skills/**/*"`, `"opencode.json"`, `".claude/settings.json"`, `"AGENTS.md"`,
      `"CLAUDE.md"`, `"**/AGENTS.md"` (nested — see dedup note below), `".github/CODEOWNERS"`,
      `".github/workflows/**/*"`, `"pyproject.toml"`, `"**/pyproject.toml"` (workspace members),
      `"libs/normalize-spec.md"`, `"libs/normalize-fixtures/**/*"`, `".gitignore"`.
      Deliberately EXCLUDED from `_CATEGORY_GLOBS`: any `.workflow/tasks/**` pattern — this is
      session-local, per-run task-plane data with no static template; document the rationale in a
      module-level comment directly above `_CATEGORY_GLOBS` (see must_haves truth 4) rather than
      silently omitting it.
    - `destination_catalog() -> list[dict]`: for each pattern in `_CATEGORY_GLOBS`, resolve
      `sorted(_REPO_ROOT.glob(pattern))`, keep only real files (`p.is_file()`), apply the SAME
      confinement guard already used elsewhere in this module/`scan.py`
      (`resolved == _REPO_ROOT or _REPO_ROOT in resolved.parents`, skip otherwise — defense in
      depth even though the source is this checkout, not an external target), convert to
      repo-relative POSIX via `p.resolve().relative_to(_REPO_ROOT).as_posix()`, and additionally
      skip any path whose parts start with `(".workflow", "tasks")` (belt-and-suspenders against a
      future glob accidentally reintroducing it). Deduplicate across overlapping glob patterns using
      a `dict[str, dict]` keyed by the destination string, populated in `_CATEGORY_GLOBS` iteration
      order (first match wins — e.g. the literal `"AGENTS.md"`/`"CLAUDE.md"` patterns are listed
      BEFORE `"**/AGENTS.md"` so the root file's row is produced by the marker-capable-labeled entry,
      not double-counted by the nested-glob entry — root `AGENTS.md`/`CLAUDE.md` must still appear
      exactly once in the final catalog). Each row is `{"destination": <repo-relative POSIX str>}` —
      drop the old `"num"`/`"plane"`/`"marker_capable"` keys entirely (they were never consumed by
      `disposition()`/`build_manifest()`; `MARKER_CAPABLE`/`DERIVED_GLOBS`/`CONSTITUTION_GLOBS`
      already carry the equivalent classification information at `disposition()`-call time). Return
      the deduplicated rows sorted by `destination`.
    - Update the module docstring's totality claim (currently "the static 40-row catalog") to
      describe the rule-derived enumeration and cite the `.workflow/tasks/**` exclusion rationale.
    - Test: rewrite `test_total` to iterate the LIVE `destination_catalog()` (no hardcoded `== 40`
      length assertion) and assert every non-`None` `disposition(...)` result is in
      `DISPOSITION_ENUM` — totality over whatever the real catalog currently contains.
    - Test: add `test_catalog_covers_real_contract_schemas` — computes
      `live_count = len([p for p in sorted((repo_root / "contracts").rglob("*.schema.json"))])`
      (fixture-independent, via the `repo_root` fixture) and asserts the number of
      `destination_catalog()` rows whose `destination` ends with `.schema.json` and starts with
      `"contracts/"` equals `live_count` — a live structural check, never a hardcoded literal count,
      so the test does not go stale as the repo's contract set grows.
    - Test: add `test_catalog_covers_real_nested_agents_md` — same live-count pattern for
      `**/AGENTS.md` files excluding the root, asserting the catalog contains exactly that set of
      nested paths (root `AGENTS.md` present exactly once, not duplicated by the nested glob).
    - Test: add `test_no_fictional_placeholder_destinations` — asserts each of the 10 confirmed-
      nonexistent literal strings from 26-VERIFICATION.md gap 2 (`"harness/agents/widget-engineer.md"`,
      `"harness/commands/widget-check.md"`, `"harness/skills/widget-conventions/SKILL.md"`,
      `".opencode/agent/widget-engineer.md"`, `".claude/agents/widget-engineer.md"`,
      `"golden/widget/verified/case.txt"`, `"docs/adr/0001-decision.md"`,
      `".memory/agreements/0001-widget.md"`, `".workflow/tasks/T-0001/task.json"`,
      `"tools/widget_tool/pyproject.toml"`) is NOT present in
      `{row["destination"] for row in destination_catalog()}`.
    - Test: add `test_workflow_tasks_excluded` — asserts no catalog row's `destination` starts with
      `".workflow/tasks/"` (the documented scoping exclusion).
    - Test: `destination_catalog()` called twice in the same process returns identical output
      (`test_catalog_deterministic_across_calls`) — proves the rewrite stays sorted/injectable-free
      of any nondeterministic glob ordering.
    - Test: rewrite `test_gsd_lanes_excluded` to locate a GSD-owned row via
      `next(row for row in destination_catalog() if is_gsd_owned(row["destination"]))` instead of
      the removed `row["num"] == 40` lookup (import `is_gsd_owned` in the test module), and assert
      `build_manifest()` places every such row in `excluded[]` with `reason == "gsd-owned"`, never in
      `dispositions[]`.
    - Test: `test_harness_proposed_hash_independent_of_target` — replace the removed placeholder
      assertion (`harness_proposed_hash("harness/agents/widget-engineer.md") is None`) with a
      fabricated definitely-absent path (e.g. `"harness/agents/definitely-not-a-real-agent.md"`) to
      keep proving the None-when-absent behavior without depending on a now-removed catalog row.
    - Test: `test_each_disposition_reachable`, `test_constitution_always_ratification`,
      `test_normalize_spec_always_ratification`, `test_collision_rule`, `test_marker_capable_set`,
      `test_cr01_conflict_reachable_through_real_cli`, `test_cr01_repro_throwaway_junk_target` all
      call `disposition()`/`cli.main()` directly with synthetic `tmp_path` targets and are UNCHANGED
      by this task — confirm they still pass as-is.
  </behavior>
  <action>
    Rewrite `_CATALOG`/`destination_catalog()` in `destinations.py` per the behavior above. Leave
    `disposition()`, `build_manifest()`, `harness_proposed_hash(es)`, `MARKER_CAPABLE`,
    `DERIVED_GLOBS`, `DISPOSITION_ENUM`, `_existing_hash`, `_EXCLUDED_SENTINEL` untouched. Update
    `test_dispositions.py` per the test list above.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_dispositions.py -q</automated>
  </verify>
  <acceptance_criteria>
    `test_dispositions.py` passes. `len(destinations.destination_catalog())` is well over 100 when
    run against this repo (real count, not asserted as a literal — proven by the live-count tests).
    None of the 10 placeholder strings from 26-VERIFICATION.md gap 2 appear in the catalog. No
    catalog row starts with `.workflow/tasks/`. `destination_catalog()` is deterministic across two
    calls in the same process.
  </acceptance_criteria>
  <done>destination_catalog() is rule-derived from this checkout's real file tree, covers every real file in every named ADOPT-03 category (minus the documented .workflow/tasks/** exclusion), and contains zero fictional placeholder rows.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh committed snapshot + full pipeline verification</name>
  <files>tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr</files>
  <read_first>
    tools/adoption_scan/tests/test_snapshots.py (the single snapshot test this task updates),
    26-RESEARCH.md "Pitfall 5: test artifacts written into the repo, then 'proven deterministic'
      with git diff"
  </read_first>
  <action>
    Run `uv run pytest tools/adoption_scan/tests/test_snapshots.py -q --snapshot-update`. Inspect
    the diff: it must show a substantially larger `===== manifest =====` section (many more
    `dispositions[]`/`excluded[]` entries, reflecting the real catalog size) and NO change to the
    `===== inventory =====`/`===== plan =====` sections (those are Plan 26-05's concern; if this
    task's run also picks up Plan 26-05's inventory/plan changes because both plans already landed
    by the time this task's wave runs, that is expected — do not revert them). Then run the full
    verification sequence.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan -q && uv run python -m tools.contract_drift.drift && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q && uv sync --all-packages && git diff --exit-code uv.lock</automated>
  </verify>
  <acceptance_criteria>
    The full `tools/adoption_scan` suite passes with the refreshed snapshot. `uv run python -m
    tools.contract_drift.drift` exits 0 (no schema touched by this plan). The GEN-04 guard stays
    green (no literal `"examples/"` or `"examples/log-parser"` contiguous token introduced anywhere
    in `destinations.py` or its tests — the glob-derived enumeration never needs to name an instance
    path literally). `uv.lock` is unchanged.
  </acceptance_criteria>
  <done>The committed snapshot reflects the rule-derived catalog's real, larger manifest section; full suite, contract-drift, GEN-04 guard, and uv.lock all stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `destination_catalog()`'s glob source (`_REPO_ROOT`) | This harness checkout's OWN file tree, not the scanned target — unchanged trust boundary from before this plan; still never reads the target to build the catalog. |
| `.workflow/tasks/**` exclusion | A deliberate scope boundary, not a security boundary — documented in the module docstring and enforced by both glob omission and a belt-and-suspenders path-segment skip, so a future glob addition cannot silently reintroduce session-local data into the committed snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Repudiation | Catalog enumeration order affecting the committed snapshot's byte content | mitigate | `destination_catalog()` always returns `sorted(..., key=destination)`; `test_catalog_deterministic_across_calls` proves two calls in the same process are byte-identical. |
| T-26-06-02 | Tampering | A future glob pattern accidentally matching `.workflow/tasks/**` and reintroducing session-local drift into the committed snapshot | mitigate | Explicit path-segment skip (`parts[:2] == (".workflow", "tasks")`) INDEPENDENT of which glob patterns are listed in `_CATEGORY_GLOBS` — a structural guard, not just an omission from the pattern list. `test_workflow_tasks_excluded` is a regression test for this specifically. |
| T-26-06-03 | Elevation of Privilege | Disposition chain misclassifying a constitution-plane path now that the catalog is much larger | mitigate | `disposition()` itself is UNCHANGED (Task 1 does not touch it) — step 2's `resolve_path(CONSTITUTION_GLOBS, rel) == "deny"` short-circuit still runs before any hash comparison, for every row regardless of catalog size; `test_constitution_always_ratification` (unchanged) continues to prove it. |
| T-26-06-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_scan -q` — full suite green, including the new live-count/placeholder-removal/determinism tests.
- `uv run python -m tools.contract_drift.drift` — still green (no schema touched here).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` — GEN-04 guard green.
- `uv sync --all-packages && git diff --exit-code uv.lock` — lockfile unchanged.
</verification>

<success_criteria>
- `destination_catalog()` is rule-derived from the harness's real file tree, not a static 40-row sample.
- Every one of the confirmed-nonexistent placeholder paths from 26-VERIFICATION.md gap 2 is absent from the catalog.
- Totality (every non-GSD-owned catalog row resolves to exactly one disposition) holds over the real, larger catalog.
- The `.workflow/tasks/**` exclusion is documented and structurally enforced, not silent.
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-06-SUMMARY.md` when done
</output>
