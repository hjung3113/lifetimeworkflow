---
phase: 26-deterministic-brownfield-inventory-mapping-v2-3-b
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - tools/adoption_scan/pyproject.toml
  - tools/adoption_scan/__init__.py
  - tools/adoption_scan/scan.py
  - tools/adoption_scan/detect.py
  - tools/adoption_scan/tests/__init__.py
  - tools/adoption_scan/tests/conftest.py
  - tools/adoption_scan/tests/fixtures/minirepo/**
  - tools/adoption_scan/tests/test_readonly.py
  - tools/adoption_scan/tests/test_scan_exclusions.py
  - tools/adoption_scan/tests/test_detect.py
  - tools/adoption_scan/tests/test_inventory_determinism.py
autonomous: true
requirements: [ADOPT-01]

must_haves:
  truths:
    - "D-07: the scanner is assembled from four existing repo primitives (confined-walk idiom, gate-registry secret patterns, harness_perms.resolve_path glob matcher, contract_hash-style sha256 hashing) — it does NOT build on tools/evidence/capture.py (which executes subprocesses and mutates task state, both forbidden here)"
    - "D-09: git ls-files -z --cached --others --exclude-standard (shell=False, fixed argv) is the preferred enumeration path with a complete builtin denylist-walk fallback; the run's enumeration_mode is recorded in the artifact so a run is self-describing"
    - "D-08: 'source dump' exclusion covers BOTH a whole-repo concatenation marker in the first 2 KiB AND an over-cap file whose path carries a dump/snapshot/backup segment"
    - "D-10: every excluded file is recorded as {path, size, excluded: <reason>} with NO sha256 and NO content read — verified by a dedicated test that a secret-content match never appears in the emitted excluded[] entry"
    - "The scanned target tree is byte-for-byte unchanged after a scan (every fixture file's sha256 before == after)"
    - "D-06: exactly one synthetic mini-repo fixture embeds every exclusion/detection case (secret-path, secret-content, binary, vendored, generated, size-capped, source-dump, symlink-escape, collision-equal, collision-different, ambiguous-extension) with a separate assert per case"
    - "Repeated inventory-level output (scan.build_inventory over the fixture) is byte-identical across two independent runs AND across a seeded-shuffled enumeration order"
  artifacts:
    - path: "tools/adoption_scan/pyproject.toml"
      provides: "Virtual uv-workspace member (dependencies=[], package=false) — uv.lock must stay unchanged"
    - path: "tools/adoption_scan/scan.py"
      provides: "enumerate()/classify_exclusions()/_file_hash()/build_inventory() — confined, read-only, deterministic"
      min_lines: 80
    - path: "tools/adoption_scan/detect.py"
      provides: "Language/manifest/documentation/CI/test-surface/candidate-process-boundary detection feeding build_inventory()"
      min_lines: 60
    - path: "tools/adoption_scan/tests/fixtures/minirepo/"
      provides: "THE single synthetic target tree (D-06), domain-neutral vocabulary only"
    - path: "tools/adoption_scan/tests/test_readonly.py"
      provides: "Byte-invariance proof: the target tree is unchanged after a scan"
    - path: "tools/adoption_scan/tests/test_inventory_determinism.py"
      provides: "Double-run + seeded-shuffle byte-identical proof at the inventory level"
  key_links:
    - from: "tools/adoption_scan/scan.py"
      to: "tools/memory_regen/repo_map.py"
      via: "confined + symlink-guarded walk idiom (resolve() + parents-containment check), copied not re-derived"
      pattern: "root_resolved not in resolved.parents"
    - from: "tools/adoption_scan/scan.py"
      to: "contracts/harness/task-control/gate-registry.json"
      via: "secret_patterns array read as DATA (json.load), same idiom as tools/evidence/capture.py::_sensitive_pattern"
      pattern: "secret_patterns"
---

<objective>
Build the read-only, confined, deterministic enumeration + exclusion-classification + detection
core (`tools/adoption_scan/scan.py` + `detect.py`) that produces an `inventory.json` conforming to
`contracts/harness/adoption/inventory.schema.json` (Plan 01) — assembled from four existing repo
primitives rather than a fresh engine (D-07), proven read-only and byte-identical across
enumeration-order shuffles (roadmap success criterion 1), over the ONE synthetic mini-repo fixture
(D-06) that also seeds Phase 27's future application fixtures.

Purpose: ADOPT-01's whole bounding contract (confined, ignore-respecting, size-capped, secret/
binary/vendor/generated/source-dump exclusion) lives here, built from primitives this repo already
uses five times over — not a new scanner engine.
Output: `tools/adoption_scan/{scan,detect}.py`, the mini-repo fixture, and the inventory-level test
suite (readonly, exclusions, detection, determinism).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-CONTEXT.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-RESEARCH.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-PATTERNS.md
@contracts/harness/adoption/inventory.schema.json
@tools/memory_regen/repo_map.py
@tools/memory_regen/pointer_index.py
@tools/harness_perms/__init__.py
@tools/hooks/contract_guard.py
@tools/hooks/secret_scan.py
@contracts/harness/task-control/gate-registry.json
@tools/docs_sync/pyproject.toml
@tools/harness_config/tests/conftest.py
@tools/harness_lint/tests/test_core_no_example_dep.py

<interfaces>
<!-- Confined + symlink-guarded walk idiom to COPY verbatim (tools/memory_regen/repo_map.py:53-71). -->
For every candidate path: resolve the walk root once (`root_resolved = root.resolve()`); for each
`p` in `sorted(root.rglob("*"))`, skip non-files, resolve `p`, and `continue` (skip, record as
`symlink-escape`) unless `root_resolved == resolved or root_resolved in resolved.parents`.

<!-- Reusable primitives this plan imports, never retypes (26-RESEARCH.md "Don't Hand-Roll"). -->
`tools.harness_perms.resolve_path(globs, rel_path) -> "allow"|"deny"|None` — the repo's ONE
last-wins glob resolver; use it for the secret-path glob check (`*.env`, `**/*.env`, extended per
this plan's own exclusion-glob list — do not import `tools.hooks.secret_scan.SECRET_PATH_GLOBS`
directly since that module is hook-specific; define this plan's own `SECRET_PATH_GLOBS` constant
mirroring its values, since `scan.py` is not a hook).
`contracts/harness/task-control/gate-registry.json` → top-level array `secret_patterns` (8
shape-anchored regex strings) — read as JSON data (`json.loads(path.read_bytes())`), compile with
`re.compile`, exactly as `tools/evidence/capture.py::_sensitive_pattern` does — do not retype the
patterns.
`hashlib.sha256` for raw file bytes (chunked read) — never run file bytes through `rfc8785`
(that is for JSON schema canonicalization only, see `tools.contract_hash`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 test infra + the ONE synthetic mini-repo fixture</name>
  <files>
    tools/adoption_scan/pyproject.toml,
    tools/adoption_scan/__init__.py,
    tools/adoption_scan/tests/__init__.py,
    tools/adoption_scan/tests/conftest.py,
    tools/adoption_scan/tests/fixtures/minirepo/**
  </files>
  <read_first>
    tools/docs_sync/pyproject.toml (virtual-member shape: dependencies=[], [tool.uv] package=false),
    tools/harness_config/tests/conftest.py (sys.path wiring idiom, parents[3]),
    tools/harness_lint/tests/test_core_no_example_dep.py (GEN-04/05 prose tokens this fixture must never contain — Pitfall 4),
    26-RESEARCH.md "Wave 0 Gaps" and "Exclusion Rules" (the exact case list the fixture must embed)
  </read_first>
  <action>
    Create `tools/adoption_scan/pyproject.toml` mirroring `tools/docs_sync/pyproject.toml`'s shape:
    `name = "adoption-scan"`, `requires-python = ">=3.11"`, `dependencies = []`, `[tool.uv]
    package = false` — a comment stating zero new external packages (stdlib `pathlib`/`hashlib`/
    `json`/`subprocess`/`re`/`argparse` only) so `uv sync --all-packages` cannot mutate `uv.lock`.
    Create `tools/adoption_scan/__init__.py` with a module docstring describing the package's role
    (ADOPT-01/02/03, read-only brownfield inventory + mapping, invoked as `python -m
    tools.adoption_scan`). Create `tools/adoption_scan/tests/__init__.py` (empty) and
    `tools/adoption_scan/tests/conftest.py` mirroring `tools/harness_config/tests/conftest.py`
    exactly (repo root via `parents[3]`, inserted onto `sys.path` if absent) plus one
    session-or-function-scoped `tmp_minirepo(tmp_path)` fixture that materializes THE single
    synthetic target tree under `tmp_path / "minirepo"` (D-06 — do not create a second fixture
    tree anywhere in this phase). The tree MUST embed, each as a clearly-named path: (a) a
    secret-by-path file (`.env` containing `DB_PASS=doesnotmatter`); (b) a secret-by-content file
    (a `.py` or `.txt` file whose body matches one of the `gate-registry.json` `secret_patterns`,
    e.g. an `AKIA`-prefixed 20-char token) that is NOT itself named `.env`; (c) a binary file
    (leading NUL bytes, e.g. `b"\x00\x01\x02..."`); (d) a vendored directory (`node_modules/pkg/index.js`);
    (e) a generated file (first line `# @generated — do not edit`); (f) an over-cap file
    (`>256 KiB`, e.g. `b"x" * (300 * 1024)`); (g) a source-dump file: one file whose path segment
    contains `dump` (e.g. `backups/repo-dump.txt`) that is ALSO over the size cap, satisfying D-08's
    over-cap-plus-segment reading, and — separately — a small file starting with a repomix-style
    concatenation banner in its first 2 KiB (D-08's marker reading) that is UNDER the size cap so
    the marker check is what excludes it, not the cap; (h) a hash-EQUAL collision pair: two
    identically-named-content pairs are not meaningful pre-scan (collision is computed against a
    PROPOSED disposition target in Plan 03) — instead embed two files with IDENTICAL byte content
    at different paths (`widget_a.py`, `widget_b.py`) so Plan 03's disposition tests can exercise
    hash-equal vs a mutated third copy; (i) an extensionless/ambiguous file (`README`, no suffix);
    (j) a symlink escaping the tree (`escape -> /etc/passwd` or another path outside `tmp_path`,
    created via `Path.symlink_to`, wrapped in a `try/except OSError: pytest.skip(...)` guard for
    filesystems that forbid unprivileged symlinks); (k) a `pyproject.toml` manifest (`[project]
    name = "widget"`); (l) a `.github/workflows/ci.yml` (CI surface); (m) a `tests/test_widget.py`
    (test surface); (n) a `docs/adr/0001-decision.md` (ADR surface); (o) a root `AGENTS.md`
    containing the literal harness BEGIN/END HARNESS-MANAGED HTML-comment markers (marker-capable
    surface, consumed by Plan 03). Every path and identifier in the fixture MUST be domain-neutral
    (`widget`/`source`/`sink`/`greeting` vocabulary only — never log-parser/dotnet/semiconductor
    terms, per Pitfall 4/GEN-04).
  </action>
  <verify>
    <automated>uv sync --all-packages && git diff --exit-code uv.lock && uv run python -c "import tools.adoption_scan"</automated>
  </verify>
  <acceptance_criteria>
    `uv sync --all-packages` exits 0 and `git diff --exit-code uv.lock` is clean (no lockfile
    mutation). `import tools.adoption_scan` succeeds. The `tmp_minirepo` fixture, when
    materialized, contains all 15 named cases (a)-(o) above as separate files/dirs under distinct
    paths. `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` stays green (no
    domain-token leak from the new fixture).
  </acceptance_criteria>
  <done>tools/adoption_scan is an importable virtual uv-workspace member; the one synthetic mini-repo fixture embeds every exclusion/detection case; uv.lock is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scan.py — confined enumeration, exclusion classification, hashing</name>
  <files>tools/adoption_scan/scan.py, tools/adoption_scan/tests/test_readonly.py, tools/adoption_scan/tests/test_scan_exclusions.py, tools/adoption_scan/tests/test_inventory_determinism.py</files>
  <read_first>
    tools/memory_regen/repo_map.py lines 53-71 (_iter_source_files confinement idiom),
    tools/memory_regen/pointer_index.py lines 1-70 (the generator-quartet + _rel idiom + injectable base_dir),
    tools/evidence/capture.py lines 94-128 (_sensitive_pattern / _looks_like_high_entropy_token — port the semantics, do not import the module),
    tools/harness_lint/tests/test_core_no_example_dep.py (git ls-files subprocess precedent: fixed argv, shell=False),
    26-RESEARCH.md "Code Examples" (canonical writer, evidence-pointer hashing, git-ls-files-with-fallback enumeration) and "Exclusion Rules" table
  </read_first>
  <behavior>
    - `enumerate_target(target, *, _force_builtin=False)` returns `(paths, mode)` where `mode` is
      `"git"` when `git -C <target> ls-files -z --cached --others --exclude-standard` exits 0, else
      `"builtin"`. A non-git target (no `.git`) or any `git` failure (`OSError`,
      non-zero exit, decode error) falls back to a full builtin denylist walk — never raises.
    - `classify_exclusions(path, *, base, max_bytes)` returns an exclusion dict
      `{"path": rel_posix, "size": n, "excluded": reason}` or `None` (included), checking in
      path-first order: vendored-segment denylist → generated-segment/suffix denylist →
      `stat().st_size > max_bytes` (size-capped — checked BEFORE any `open()`) → NUL-byte-in-first-
      8192-bytes or UTF-8 decode failure (binary) → secret-path glob → source-dump marker (first
      2 KiB banner OR a `dump`/`snapshot`/`backup` path segment combined with the size cap) →
      secret-content pattern match against a bounded (first 64 KiB) prefix. A file failing NONE of
      these is included.
    - `build_inventory(target, *, max_bytes=262144, _paths=None)` is pure and injectable: when
      `_paths` is provided (a pre-computed, possibly-shuffled list) it is used verbatim instead of
      calling `enumerate_target` — this is what makes the shuffled-order determinism test possible.
      It returns a dict with `target_ref` (the target's current git commit hex, or `"unknown"`),
      `enumeration_mode`, `max_file_bytes`, `included` (sorted by `path`), `excluded` (sorted by
      `path`) — the four inventory.schema.json top-level scalars plus the two file arrays; the four
      detection arrays (`languages`/`manifests`/`documentation_surfaces`/`ci_surfaces`/
      `test_surfaces`/`candidate_process_boundaries`) are populated by calling into `detect.py`
      (Task 3) — leave those four calls in place even though `detect.py` does not exist until Task
      3 lands in this SAME plan.
    - Test: the target tree's every file's sha256 is identical before and after a full
      `build_inventory` call (`test_readonly.py`).
    - Test: each of the fixture's 8 exclusion cases (secret-path, secret-content, binary, vendored,
      generated, size-capped, source-dump [both readings], symlink-escape) is excluded with EXACTLY
      the expected reason string, and no other file is spuriously excluded (`test_scan_exclusions.py`,
      one assert per case, per D-06).
    - Test: a secret-content excluded entry's serialized dict has no key other than
      `path`/`size`/`excluded` — the matched bytes never appear anywhere in the returned structure
      (`test_scan_exclusions.py::test_secret_content_excluded_and_not_echoed`).
    - Test: `build_inventory` run twice into two independent calls over the identical fixture
      produces byte-identical `json.dumps(..., sort_keys=True, indent=2, ensure_ascii=True)`
      serializations (`test_inventory_determinism.py::test_double_run_byte_identical`).
    - Test: `build_inventory(target, _paths=<seeded-shuffle of the normal enumeration>)` (seed
      `1337`, `random.Random(1337).sample(...)`) produces the SAME serialized bytes as the
      unshuffled run (`test_inventory_determinism.py::test_shuffled_enumeration_byte_identical`).
  </behavior>
  <action>
    Implement `tools/adoption_scan/scan.py` per the behavior above. Module-level:
    `_REPO_ROOT = Path(__file__).resolve().parents[2]`; `DEFAULT_MAX_FILE_BYTES = 256 * 1024`;
    `SECRET_PATH_GLOBS = ["*.env", "**/*.env", "*.pem", "*.key", "id_rsa*", ".npmrc", ".netrc"]`
    (this plan's OWN constant — do not import the hook-specific one); `_VENDOR_SEGMENTS =
    {"node_modules", "vendor", "third_party", ".venv", "venv", "site-packages", "__pycache__"}`;
    `_GENERATED_SEGMENTS = {"dist", "build", "bin", "obj", "target", "out", ".pytest_cache",
    ".ruff_cache"}`. Load `secret_patterns` from
    `contracts/harness/task-control/gate-registry.json` once at import time (or lazily, memoized)
    via stdlib `json`, compile with `re.compile`. Write `_dump(document) -> bytes` exactly as
    `26-RESEARCH.md`'s "Canonical artifact writer" shows:
    `json.dumps(document, sort_keys=True, indent=2, ensure_ascii=True) + "\n"` encoded UTF-8 — this
    is the ONE writer every artifact in this phase uses (Plan 03 reuses it, never redefines it).
    Every emitted path is `resolved.relative_to(base).as_posix()` (never an absolute or `tmp_path`
    string). Never call `open()` before `stat()` for the size check. Compute `target_ref` via a
    fixed-argv `git -C <target> rev-parse HEAD` (`shell=False`, `capture_output=True,
    check=False`); on any non-zero exit or `OSError`, use the literal string `"unknown"` — this is
    the single OTHER `subprocess` call in this module besides `ls-files`; state in a comment (per
    D-09) that both are fixed non-shell argv against `git` itself, not execution of anything
    discovered in the target, so neither violates the "no arbitrary command execution" invariant.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_readonly.py tools/adoption_scan/tests/test_scan_exclusions.py tools/adoption_scan/tests/test_inventory_determinism.py -q</automated>
  </verify>
  <acceptance_criteria>
    All three test files pass. `test_readonly.py` asserts a before/after sha256 map over the fixture
    is identical. `test_scan_exclusions.py` has >=8 parametrized cases (one per exclusion class) each
    asserting the EXACT reason string, plus the secret-content-not-echoed assertion. Both
    determinism tests in `test_inventory_determinism.py` pass with a fixed seed (`1337`) recorded in
    the test source (reproducible on failure).
  </acceptance_criteria>
  <done>scan.py enumerates confined + read-only, classifies all 8 exclusion classes with exact reasons, never leaks secret content, and build_inventory is byte-identical across a double-run and a seeded-shuffle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: detect.py — language/manifest/surface detection wired into build_inventory</name>
  <files>tools/adoption_scan/detect.py, tools/adoption_scan/tests/test_detect.py</files>
  <read_first>
    tools/memory_regen/repo_map.py (extension-based language detection idiom),
    contracts/harness/adoption/inventory.schema.json (the exact languageRecord/manifestRecord/surfaceRecord shapes Task 2's build_inventory must emit),
    26-RESEARCH.md "Evidence Classification Ladder" (D-02: observed only on direct evidence; inferred only on strong structural signals)
  </read_first>
  <behavior>
    - `detect_languages(included) -> list[dict]`: one `languageRecord` per distinct extension found
      among `included` entries, `classification: "observed"` (extension presence is direct
      evidence per D-02), `evidence` = the `evidenceRef`s (path+sha256) of a representative subset
      (at least one) of matching included files.
    - `detect_manifests(included) -> list[dict]`: one `manifestRecord` per recognized manifest file
      present in `included` (`pyproject.toml`, `package.json`, `*.csproj`, `go.mod`, `Cargo.toml`),
      `classification: "observed"` (literal file existence).
    - `detect_documentation_surfaces` / `detect_ci_surfaces` / `detect_test_surfaces` /
      `detect_candidate_process_boundaries` (`included) -> list[dict]`: `surfaceRecord`s.
      Documentation/CI/test surfaces are `"observed"` when the recognized path exists (e.g.
      `docs/adr/**`, `.github/workflows/*.yml`, a `tests/` dir containing `test_*.py`).
      `candidate_process_boundaries` are `"inferred"` (a directory containing a recognized manifest
      is a candidate component root) and each carries a `rationale` string naming the structural
      rule (e.g. `"manifest-directory"`). NEVER emit `"observed"` for a candidate process boundary —
      component/member existence is inherently inferred per D-02.
    - Test: a fixture with a `.py` file yields a `python` languageRecord classified `observed`.
    - Test: the fixture's `pyproject.toml` yields a `manifestRecord` classified `observed`.
    - Test: the fixture's `.github/workflows/ci.yml` yields a `ci_surfaces` entry classified
      `observed`.
    - Test: the fixture's manifest-containing directory yields a `candidate_process_boundaries`
      entry classified `inferred` with a non-empty `rationale`.
    - Test: `scan.build_inventory(fixture)`'s full serialized output validates with zero errors
      against `contracts/harness/adoption/inventory.schema.json` via `Draft202012Validator`
      (`test_schema_conformance`-style assertion, colocated in `test_detect.py` since this is where
      the four detection arrays first get populated end-to-end).
  </behavior>
  <action>
    Implement `tools/adoption_scan/detect.py` per the behavior above, and wire its four/six
    functions into `scan.build_inventory()`'s previously-stubbed detection calls (Task 2). Keep
    every detection rule EXTENSION/FILENAME/STRUCTURE-based only — no tree-sitter, no symbol
    parsing (per 26-RESEARCH.md's recommendation that D-02's conservative bias makes symbol-level
    inference `unknown` anyway). Never classify an ownership/authority fact here — that ladder step
    belongs to Plan 03's `plan.py`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_detect.py -q</automated>
  </verify>
  <acceptance_criteria>
    `test_detect.py` passes, including the schema-conformance assertion: `Draft202012Validator(
    inventory_schema).iter_errors(scan.build_inventory(fixture))` yields zero errors. No detection
    function ever returns `classification: "observed"` for a `candidate_process_boundaries` entry.
  </acceptance_criteria>
  <done>detect.py implements the six detection functions, build_inventory's full output validates against inventory.schema.json with zero errors, and every candidate-process-boundary is inferred, never observed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--target <path>` tree (untrusted brownfield input) → `scan.py` reads | The scanned tree is arbitrary, possibly adversarial, filesystem content; every read must be confined, bounded, and never interpreted as executable. |
| `scan.py` → one `git` subprocess (per call) | Fixed argv, `shell=False`, distinct from executing anything DISCOVERED inside the target. |
| `contracts/harness/task-control/gate-registry.json` → `scan.py` | Read as trusted repo-committed DATA (not target input) — never a target-supplied pattern set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering, Information Disclosure | Path traversal / symlink escape out of `--target` | mitigate | `resolved.relative_to`/`parents`-containment guard on EVERY path (the `repo_map` idiom, Task 2); an escaping symlink is skipped and recorded with reason `symlink-escape`, never followed. Proven by the fixture's escaping-symlink case (Task 1) + `test_scan_exclusions.py`. |
| T-26-02-02 | Denial of Service | Resource exhaustion on a huge or symlink-looped target | mitigate | `stat()` runs BEFORE any `open()`, so the size cap is a real bound, not a post-hoc truncation; escaping directory symlinks are never followed (same guard as T-26-02-01); content-based heuristics (binary/secret) only ever read a bounded prefix (first 8 KiB / 64 KiB), never the whole file. |
| T-26-02-03 | Information Disclosure | Secret exfiltration into the emitted `inventory.json` | mitigate | An excluded file is recorded as `{path, size, excluded: <reason>}` ONLY — enforced both by `inventory.schema.json`'s `excludedEntry` (Plan 01, no `sha256` property exists) and by this plan's own test (`test_secret_content_excluded_and_not_echoed`) asserting the matched bytes never appear in the emitted structure. Mirrors `tools/evidence/capture.py::_refuse_if_sensitive`'s field-names-only posture. |
| T-26-02-04 | Elevation of Privilege | Executing content discovered in the target (e.g. a detected "test command") | mitigate (structural) | `scan.py`/`detect.py` have NO execution path for any string read from the target — detected commands, if any, are recorded as `inferred` DATA only, never invoked (§147). |
| T-26-02-05 | Tampering | Command injection via the two `git` subprocess calls (`ls-files`, `rev-parse HEAD`) | mitigate | Both are fixed argv lists, `shell=False`, target path passed as a discrete `-C` argument — never string-interpolated or built from target-supplied content. Precedent: `tools/harness_lint/tests/test_core_no_example_dep.py`, `tools/contract_drift/drift.py::_git_show`. |
| T-26-02-06 | Repudiation | Non-deterministic inventory output silently varying between runs | mitigate | The full determinism recipe (sorted walk, sorted emission, repo-relative POSIX paths, no timestamps, canonical `_dump` writer) + the double-run and seeded-shuffle tests (Task 2) — a regression here would silently invalidate Phase 27's future approval-binding. |
| T-26-02-SC | Tampering (supply chain) | N/A | accept | Zero new package installs (`dependencies = []`); no slopcheck required. |
</threat_model>

<verification>
- `uv sync --all-packages && git diff --exit-code uv.lock` — new workspace member does not mutate the lockfile.
- `uv run pytest tools/adoption_scan/tests/test_readonly.py tools/adoption_scan/tests/test_scan_exclusions.py tools/adoption_scan/tests/test_inventory_determinism.py tools/adoption_scan/tests/test_detect.py -q` — full plan test suite green.
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` — GEN-04 guard stays green (domain-neutral fixture).
</verification>

<success_criteria>
- `tools/adoption_scan/{scan,detect}.py` exist; `build_inventory()` produces output that validates with zero errors against `contracts/harness/adoption/inventory.schema.json`.
- The target tree is provably byte-unchanged after any scan.
- Every one of D-06's embedded fixture cases is excluded/detected with the exact expected reason/classification.
- Double-run and seeded-shuffle-order runs over the fixture produce byte-identical serialized inventories.
</success_criteria>

<output>
Create `.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-02-SUMMARY.md` when done
</output>
