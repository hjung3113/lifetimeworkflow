---
phase: 14-write-path-anti-churn-guard-v2-1-c
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0006-process-memory-channel-and-provenance-reframe.md
autonomous: false
requirements: [MEM2-04]

must_haves:
  truths:
    - "D-12: docs/adr/0006-*.md:92-93 claims the tier ships '_TEMPLATE.md + README + one committed seed'. There is no seed and there never was — git ls-files .memory/agreements/ returns exactly README.md and _TEMPLATE.md, and commit 96b8db2 added exactly those two files. Resolved with a dated ## Errata note APPENDED to ADR-0006 — NOT by shipping a seed, NOT by superseding with ADR-0008."
    - "D-12 rationale — NOT A SEED: fabricating an agreement to retroactively make the ADR true would require inventing user feedback, the exact T-13-01 / anti-invent violation this phase exists to prevent. The phase must not open by committing the sin it closes."
    - "D-12 rationale — NOT ADR-0008: supersede is the instrument for CHANGING a decision (docs/adr/README.md:16). No decision changed; a factual claim about what shipped was wrong. Errata is append-only-legal because docs/adr/README.md:14 forbids editing DECISION content, and the false claim sits in ### Consequences, not ## Decision Outcome; README.md:13 explicitly permits fixing typos/links."
    - "D-12 landing path: this is a CONSTITUTION write (docs/adr/** ∈ contract_guard's CONSTITUTION_GLOBS). It lands via HARNESS_DEV_BYPASS (ADR-0007) or raw shell. NEVER forge GOLDEN_APPROVE_HUMAN — the whole point of a distinct variable is that a dev-bypassed write is never mislabeled human-ratified."
    - "D-12 byte hygiene: contract_guard.py:83-90 runs lint_bytes on the payload of even an APPROVED constitution write, and approved = token_present or dev_bypassed() — so the byte check runs identically on the dev-bypass path. The errata must be LF-only with no BOM. Byte-hygiene is never waived."
    - "D-13: the empty active agreement set is CORRECT, not a defect to repair. The errata must say so plainly, so a future agent reading ADR-0006 does not 'repair' the directory by inventing an entry."
    - "D-03 (honest framing): the errata's own existence is the argument — a factual error was corrected by appending a dated note, not by manufacturing evidence to match the claim."
  artifacts:
    - path: "docs/adr/0006-process-memory-channel-and-provenance-reframe.md"
      provides: "A dated ## Errata section appended after ## Links, correcting the phantom-seed claim and declaring the empty set correct"
      contains: "## Errata"
  key_links:
    - from: "docs/adr/0006-process-memory-channel-and-provenance-reframe.md"
      to: ".memory/agreements/"
      via: "the errata's falsifiable citation of git ls-files output + commit 96b8db2"
      pattern: "git ls-files"
---

<objective>
Correct ADR-0006's phantom-seed claim with a dated, append-only `## Errata` note that also states
plainly that the **empty** active agreement set is correct.

Purpose: ADR-0006:92-93 asserts a committed seed that never existed. Left alone, a future agent
reading the ADR would "repair" `.memory/agreements/` by authoring an entry — which requires inventing
user feedback, i.e. committing exactly the T-13-01 violation this phase exists to prevent. **The
phase must not open by committing the sin it closes.**
Output: an appended `## Errata` section on ADR-0006. No seed. No ADR-0008. No decision word touched.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-CONTEXT.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-VALIDATION.md
@AGENTS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

The false claim — docs/adr/0006-process-memory-channel-and-provenance-reframe.md:92-93, inside
`### Consequences` (NOT inside `## Decision Outcome` — this is why an errata is append-only-legal).
It does NOT grep as "committed seed": the phrase wraps across lines 92→93.
```
- **Neutral / accepted:** the tier ships as a **scaffold** — `_TEMPLATE.md` + README + one committed
  seed — with no write tooling until Phase 14; humans add agreements by hand in the interim.
```

The falsification (VERIFIED live):
```
$ git ls-files .memory/agreements/
.memory/agreements/README.md
.memory/agreements/_TEMPLATE.md
```
Commit `96b8db2` added exactly those two files. The file is **110 lines**; `## Links` is the final
section (the ADR ends with the "Enforcement precedent: ADR-0004 …" bullet).

The append-only rules — docs/adr/README.md:13-16:
- :13 explicitly PERMITS "Fixing typos/links"
- :14 FORBIDS editing *decision content*
- :16 supersede is the instrument for *changing a decision*

The constitution gate — tools/hooks/contract_guard.py. `decide()` at 60-92 runs, in order:
(1) off-plane → None (73-74); (2) on-plane + not approved → **deny** (76-81), naming /golden-approve
+ CODEOWNERS; (3) **on-plane + approved → lint_bytes(content.encode("utf-8")) → deny on any
violation (83-90)**; (4) clean → None. `docs/adr/**` ∈ CONSTITUTION_GLOBS (:43).
```python
approved = token_present or dev_bypassed()        # contract_guard.py:104
```
⇒ step 3 runs identically on the dev-bypass path. Its docstring (11-14) is explicit: "even an
APPROVED constitution write is DENIED if its payload bytes fail the reused POLY-01 lint_bytes
(§4.3-4.6: BOM / CRLF)." **Byte-hygiene is never waived.**

The bypass mechanism — tools/hooks/_stdin.py:39-44. A plain env var; it reaches the hook via the
settings file's `env` block **or any exported shell var** — nothing requires the JSON file.
```python
DEV_BYPASS_ENV = "HARNESS_DEV_BYPASS"
def dev_bypassed() -> bool:
    return bool((os.environ.get(DEV_BYPASS_ENV) or "").strip())
```
Registration: .claude/settings.json:125 — "command": "uv run python -m tools.hooks.contract_guard"
under PreToolUse, matcher `Write|Edit`.
⚠ `.claude/settings.local.json` is **gitignored (.gitignore:26-27) and ABSENT in this checkout** —
confirmed by `ls`. It must be created, or the var exported in the shell.

The lint that byte-hygiene reuses — tools/polyglot_lint/lint.py:55-62 (`lint_bytes`): flags a leading
UTF-8 BOM (`R1-BOM`) and any CR byte (`R2-CRLF`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the dated ## Errata to ADR-0006</name>

  <files>docs/adr/0006-process-memory-channel-and-provenance-reframe.md</files>

  <read_first>
    - `docs/adr/0006-process-memory-channel-and-provenance-reframe.md` — **in full (110 lines)**. Locate the false claim at 92-93 inside `### Consequences`, and confirm `## Links` is the final section so the errata appends after it.
    - `docs/adr/README.md:10-25` — the append-only rules: :13 permits fixing typos/links, :14 forbids editing decision content, :16 scopes supersede to *changing a decision*. This is the constraint D-12 reasons from.
    - `docs/adr/0007-constitution-gate-dev-enforce-decoupling.md` — `HARNESS_DEV_BYPASS`: the landing path, and the precedent for D-03's honest trust model ("the guard is accident-prevention, not a sandbox"). Note :52's normative rule and :82's accepted "agent self-enables the bypass" risk.
    - `tools/hooks/contract_guard.py:60-92, 103-117` — `decide()`'s control flow; `approved = token_present or dev_bypassed()`; the byte-hygiene step that runs on the bypass path too.
    - `tools/hooks/_stdin.py:39-44` — `dev_bypassed()`; the var is a plain env var, no JSON file required.
    - `.memory/agreements/README.md` — the tier contract the errata reaffirms.
  </read_first>

  <action>
**First, re-falsify — do not trust this plan's prose (Pitfall 7: "grep for the test before trusting
any 'is preserved' claim").** Run `git ls-files .memory/agreements/` and
`git show --stat 96b8db2 -- .memory/agreements/` and confirm with your own eyes that exactly two
files exist and that the add-commit added exactly those two. If a seed *does* exist, **stop and
report** — D-12's entire premise is void and the errata must not be written.

**Land it via the dev-bypass path (D-12).** `docs/adr/**` is constitution plane; `contract_guard`
denies an agent Write/Edit there by design. Export `HARNESS_DEV_BYPASS=1` for the write (or create
the gitignored `.claude/settings.local.json` with an `env` block — the file is **absent** in this
checkout). This is the sanctioned path and produces `contract_guard`'s audit stderr note ("allowed
via HARNESS_DEV_BYPASS (dev-only) — CODEOWNERS still gates merge"), which never claims human
approval.

**NEVER forge `GOLDEN_APPROVE_HUMAN`.** The whole point of `HARNESS_DEV_BYPASS` being a *distinct*
variable is that a dev-bypassed write is never mislabeled human-ratified. Do not read, set, guess, or
reference that token.

Prefer the dev-bypass path over a raw-shell `cat >>` **precisely because it keeps the byte check**:
`contract_guard`'s hook matcher is `Write|Edit`, so a Bash append never reaches the guard at all and
silently skips byte-hygiene. If you fall back to raw shell, run
`uv run python -m tools.polyglot_lint.lint` on the file manually afterward.

**Byte-hygiene still applies (verified by reading `decide()`, not its prose):** `approved` is
`token_present or dev_bypassed()`, and the `lint_bytes` step (83-90) runs on **any** approved
constitution write. **The errata must be LF-only with no BOM** — a CRLF paste is denied even with the
bypass set.

**Append a `## Errata` section after `## Links`** (the file's final section). Do **not** modify lines
92-93 or any other existing line — an appended, dated section leaves every decision word untouched,
which is what makes this append-only-legal (`docs/adr/README.md:14` forbids editing *decision
content*; the false claim sits in `### Consequences`, not `## Decision Outcome`, and :13 explicitly
permits fixing factual/link errors).

The errata must contain, dated **2026-07-16**:
1. **The correction, with its falsification inline.** Name the bullet ("Neutral / accepted" under
   *Consequences*) and its claim; state there is no seed and there never was; cite
   `git ls-files .memory/agreements/` → exactly `README.md` + `_TEMPLATE.md`, and commit `96b8db2`
   adding exactly those two files. Citing the falsification (not just asserting the correction) is
   what makes the note checkable by the next reader.
2. **D-13, stated plainly and unmissably:** the empty active agreement set is **CORRECT, not a defect
   to repair**. An agreement is written only in response to explicit user feedback (MEM2-04, proposal
   §2 property 2); no such feedback has been recorded, so there is nothing to record. **An agent that
   "fixes" this by authoring an agreement would self-invent user feedback — the exact T-13-01 threat
   this ADR's channel and the Phase-14 provenance guard exist to prevent. Do not seed this directory.**
3. **The append-only justification:** no decision recorded in this ADR is changed by the note; it
   corrects a factual claim about what shipped.

**Do NOT ship a seed (D-12).** Fabricating an agreement to retroactively make the ADR true would
require inventing user feedback — the exact anti-invent violation this phase exists to prevent. The
phase must not open by committing the sin it closes.

**Do NOT create ADR-0008 (D-12).** Supersede is the instrument for *changing a decision*
(`docs/adr/README.md:16`). No decision changed here; a factual claim was wrong.

**No model identifier** anywhere in the errata, the commit, or the PR body (project constraint).
  </action>

  <verify>
    <automated>uv run python -m tools.polyglot_lint.lint docs/adr/0006-process-memory-channel-and-provenance-reframe.md 2>&1 | tail -2; uv run pytest tools/harness_lint -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^## Errata" docs/adr/0006-process-memory-channel-and-provenance-reframe.md` → **1**.
    - `grep -n "2026-07-16" docs/adr/0006-process-memory-channel-and-provenance-reframe.md` → matches within the errata (the note is dated).
    - `grep -n "git ls-files" docs/adr/0006-process-memory-channel-and-provenance-reframe.md` → matches (falsification cited, not merely asserted).
    - `grep -n "96b8db2" docs/adr/0006-process-memory-channel-and-provenance-reframe.md` → matches.
    - `grep -Ein "correct, not a defect|do not seed" docs/adr/0006-process-memory-channel-and-provenance-reframe.md` → matches (D-13 stated plainly).
    - **Append-only proof — every pre-existing line is untouched:** `git diff -U0 docs/adr/0006-process-memory-channel-and-provenance-reframe.md | grep -c '^-[^-]'` → **0** (zero deletions/modifications; additions only).
    - **The errata appends AFTER the final pre-existing line:** `git diff -U0 docs/adr/0006-process-memory-channel-and-provenance-reframe.md | grep -E '^@@' | wc -l` → **1**, and that hunk's start line is ≥ 110.
    - **Byte hygiene (LF-only, no BOM):** `python3 -c "import pathlib; b=pathlib.Path('docs/adr/0006-process-memory-channel-and-provenance-reframe.md').read_bytes(); assert not b.startswith(b'\xef\xbb\xbf'), 'BOM'; assert b'\r' not in b, 'CR byte'"` → exits 0.
    - `ls docs/adr/0008-*.md 2>/dev/null | wc -l` → **0** (no supersede ADR invented).
    - `git status --porcelain .memory/agreements` → **empty** (**no seed shipped** — the single most important criterion in this plan).
    - `git ls-files .memory/agreements/ | wc -l` → **2** (still exactly README.md + _TEMPLATE.md).
    - `grep -ri "claude\|opus\|sonnet\|gpt" docs/adr/0006-process-memory-channel-and-provenance-reframe.md` → **no match** in the errata (no model identifier).
    - `uv run pytest tools/harness_lint -q` → **0 failed**.
  </acceptance_criteria>

  <done>ADR-0006 carries a dated `## Errata` correcting the phantom-seed claim and declaring the empty set correct; the diff is additions-only; the file is LF/no-BOM; no seed, no ADR-0008.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of the constitution-plane write</name>

  <files>docs/adr/0006-process-memory-channel-and-provenance-reframe.md</files>

  <read_first>
    - `docs/adr/0006-process-memory-channel-and-provenance-reframe.md` — the appended `## Errata` from Task 1, plus lines 92-93 (the claim it corrects) to confirm they are untouched.
    - `docs/adr/README.md:13-16` — the append-only rules, to judge whether an errata (not a supersede) is the right instrument.
    - `docs/adr/0007-constitution-gate-dev-enforce-decoupling.md:52, 82` — the normative bypass rule, and the explicitly accepted "agent self-enables the bypass" risk that this checkpoint pays down.
  </read_first>

  <action>
Present the Task-1 diff to the user and **stop**. Run no fixes, land no follow-up edits, and do not
self-approve. `contract_guard`'s bypass note is explicit that it "never claims human approval —
CODEOWNERS still gates merge"; this checkpoint is that human approval. Wait for the resume signal.
If the user asks for changes, apply them and re-present — do not proceed on silence.
  </action>

  <what-built>
A dated `## Errata` section appended to
`docs/adr/0006-process-memory-channel-and-provenance-reframe.md`, correcting the false "one committed
seed" claim at lines 92-93 and stating that the empty active agreement set is correct rather than a
defect to repair.

This is a **constitution-plane write** landed via `HARNESS_DEV_BYPASS` (ADR-0007), not via a human
ratification token. `contract_guard`'s own stderr note is explicit that the bypass "never claims
human approval — CODEOWNERS still gates merge", and `/docs/adr/ @hjung3113` in CODEOWNERS is the real
gate. ADR-0007:82 accepts that an agent can self-enable the bypass; **this checkpoint is where that
accepted risk is actually paid down.** Automation cannot substitute for it: no test can decide
whether an append to an append-only record is legitimate.
  </what-built>

  <how-to-verify>
1. Read the appended section:
   `git diff docs/adr/0006-process-memory-channel-and-provenance-reframe.md`
2. Confirm the diff is **additions only** — no pre-existing line changed, and in particular lines
   92-93 (the erroneous "Neutral / accepted" bullet) are **untouched**. Expect exactly one hunk,
   appending after the final `## Links` bullet:
   `git diff -U0 docs/adr/0006-*.md | grep -c '^-[^-]'` → must print `0`
3. Confirm the falsification independently, with your own commands — do not take the errata's word:
   `git ls-files .memory/agreements/` → must print exactly `README.md` and `_TEMPLATE.md`
   `git show --stat 96b8db2 -- .memory/agreements/` → must show exactly those two files added
4. Confirm **no seed was shipped** — the phase must not open by committing the sin it closes:
   `git status --porcelain .memory/agreements` → must be **empty**
   `git ls-files .memory/agreements/ | wc -l` → must print `2`
5. Confirm `GOLDEN_APPROVE_HUMAN` was **not** forged: the write should have gone through
   `HARNESS_DEV_BYPASS`. Check that no ratification token appears anywhere in the diff or in
   `.claude/settings.local.json` (which is gitignored and was absent before this task):
   `git diff | grep -ci "GOLDEN_APPROVE_HUMAN"` → must print `0`
6. Confirm the errata says the empty set is **correct** and instructs future agents not to seed the
   directory (D-13) — this is the sentence that stops a future "repair".
7. Confirm no ADR-0008 was created: `ls docs/adr/0008-*.md` → must report no such file.
8. Judgement call, yours alone: is an appended errata the right instrument here, versus a supersede
   ADR? D-12's reasoning is that supersede changes a *decision* (`docs/adr/README.md:16`) while here
   only a *factual claim about what shipped* was wrong, and the claim sits in `### Consequences`,
   not `## Decision Outcome`. Confirm you agree.
  </how-to-verify>

  <resume-signal>Type "approved" to accept the errata as written, or describe what should change (wording, placement, or instrument).</resume-signal>

  <verify>
    <human-check>The user has reviewed the additions-only diff and typed "approved" (or the requested changes have been applied and re-approved).</human-check>
    <automated>git diff -U0 docs/adr/0006-process-memory-channel-and-provenance-reframe.md | grep -c '^-[^-]' | grep -qx 0 && git status --porcelain .memory/agreements | wc -l | grep -qx 0 && echo "PRE-CHECKPOINT OK: append-only diff, no seed"</automated>
  </verify>

  <acceptance_criteria>
    - The automated pre-check above prints `PRE-CHECKPOINT OK` **before** the user is asked to review — never present a diff that already violates append-only or that shipped a seed.
    - The user has explicitly typed "approved" (or an equivalent affirmative). **Silence, an empty response, or a whitespace-only response is NOT approval** — the same blank-is-never-a-signal rule the codebase applies to `GOLDEN_APPROVE_HUMAN` and `HARNESS_DEV_BYPASS` (ADR-0007:52).
    - No agent-side edits were made to the ADR between presenting the diff and receiving the signal.
    - If the user requested changes, they were applied and the diff was re-presented and re-approved.
    - The approval (and any requested changes) is recorded verbatim in `14-04-SUMMARY.md`.
  </acceptance_criteria>

  <done>A human has reviewed the append-only errata diff, independently confirmed no seed was shipped, and explicitly approved the constitution-plane write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → `docs/adr/**` (constitution plane) | Path-denied by `contract_guard` by design. Crossed here only via the sanctioned dev-bypass, with CODEOWNERS as the real merge gate. |
| ADR-0006's prose → a future agent's behavior | The false seed claim is itself an attack surface: it instructs a future reader to create an entry that can only be created by inventing user feedback. |
| errata payload bytes → `lint_bytes` | Byte-hygiene (§4.3-4.6 BOM/CRLF) is never waived, even for an approved write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-14-15** | **Elevation of Privilege** | ADR-0006:92-93's phantom seed claim → a future agent "repairs" `.memory/agreements/` by inventing an entry | **mitigate** | This plan's whole purpose. The errata states explicitly that the empty set is **CORRECT** and that seeding would self-invent user feedback (T-13-01). Enforced by the `correct, not a defect` / `do not seed` grep and by `git ls-files .memory/agreements/ | wc -l` → 2. |
| **T-14-16** | **Repudiation** | "fixing" the ADR by shipping a seed | **mitigate** | D-12: **not a seed.** Fabricating an agreement to retroactively make the ADR true is the exact anti-invent violation this phase closes. `git status --porcelain .memory/agreements` → empty is a blocking criterion on **both** tasks. |
| T-14-17 | Tampering | editing decision content in an append-only record | mitigate | Append-only: additions-only diff, asserted by `git diff -U0 ... \| grep -c '^-[^-]'` → 0 and re-checked by the human at step 2. The claim sits in `### Consequences`, not `## Decision Outcome` (README:14), and README:13 permits fixing factual errors. |
| T-14-18 | Spoofing | forging `GOLDEN_APPROVE_HUMAN` to land a constitution write | **mitigate** | **Never forge it.** The landing path is `HARNESS_DEV_BYPASS` — a *distinct* variable precisely so a dev-bypassed write is never mislabeled human-ratified (`approve.py:9`, `contract_guard.py:7`, ADR-0007:55). Asserted by `git diff \| grep -ci "GOLDEN_APPROVE_HUMAN"` → 0 and by the human at step 5. |
| T-14-19 | Tampering | a CRLF/BOM payload slipping into the constitution plane | mitigate | `contract_guard.py:83-90` runs `lint_bytes` on **any approved** write (`approved = token_present or dev_bypassed()`), so the bypass path is byte-checked identically. Errata must be LF-only/no-BOM; asserted by the byte-hygiene criterion. **The dev-bypass path is preferred over raw shell precisely because raw `cat >>` never reaches the `Write|Edit` matcher and would skip this check.** |
| T-14-20 | Elevation of Privilege | agent self-enables `HARNESS_DEV_BYPASS` | **accept** | ADR-0007:82 names this explicitly as accepted; CODEOWNERS (`/docs/adr/ @hjung3113`) at merge is the real gate. Task 2's blocking human checkpoint is where that accepted residual is actually paid down rather than assumed. |
| T-14-21 | Repudiation | inventing ADR-0008 to "supersede" a factual error | mitigate | D-12: supersede is for *changing a decision* (README:16); nothing changed. Asserted by `ls docs/adr/0008-*.md` → 0. |
</threat_model>

<verification>
- `git diff -U0 docs/adr/0006-*.md | grep -c '^-[^-]'` → **0** (append-only: additions only).
- Byte hygiene: no BOM, no CR byte in the ADR.
- `git ls-files .memory/agreements/ | wc -l` → **2**; `git status --porcelain .memory/agreements` → **empty**. **No seed. This is the criterion that matters most in this plan.**
- `ls docs/adr/0008-*.md` → none.
- `git diff | grep -ci "GOLDEN_APPROVE_HUMAN"` → 0.
- `uv run pytest -q` → **exactly 1 failed / 620 passed** (unchanged; this plan touches no code). **`0 failed` is a FAILURE CONDITION.**
- `git status --porcelain .opencode .claude tools/harness_emit/tests/__snapshots__` → **empty**. Note `.claude/settings.local.json` is gitignored, so creating it does not dirty the tree.
- Human approval recorded at the Task 2 checkpoint.
</verification>

<success_criteria>
- ADR-0006 carries a dated `## Errata` that names the false claim, cites its falsification (`git ls-files` + `96b8db2`), and declares the empty active set **correct, not a defect to repair**.
- The diff is additions-only; lines 92-93 and every decision word are untouched.
- The file is LF-only with no BOM.
- No seed was shipped; `.memory/agreements/` still holds exactly two committed files.
- No ADR-0008; no forged `GOLDEN_APPROVE_HUMAN`; no model identifier.
- A human has reviewed and approved the constitution-plane write.
</success_criteria>

<output>
Create `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-04-SUMMARY.md` when done.
</output>
