---
phase: 14-write-path-anti-churn-guard-v2-1-c
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - tools/harness_lint/provenance.py
  - tools/harness_lint/tests/test_provenance.py
  - .memory/agreements/_TEMPLATE.md
  - harness/commands/lint.md
autonomous: true
requirements: [MEM2-04]

must_haves:
  truths:
    - "D-01: 'well-formed' is a structural regex over frontmatter mirroring _TEMPLATE.md exactly and nothing more — status ∈ {active, retired}; added an ISO YYYY-MM-DD; provenance matching ^added because \\S. No crypto, no signatures, no attestation."
    - "D-02 (as corrected by the research amendment): added: must be a QUOTED string and the lint asserts isinstance(value, str). The isinstance check PRECEDES the regex — an unquoted real date parses to datetime.date and must produce a Violation, NOT a TypeError. _TEMPLATE.md is fixed to added: \"YYYY-MM-DD\". No `import datetime` anywhere."
    - "D-03: the lint enforces SHAPE, not TRUTH. A well-formed but fabricated provenance PASSES. ROADMAP SC2/MEM2-04's 'agents cannot auto-invent' overclaims; this plan delivers the honest claim — an omitted/malformed stamp fails loud, and inventing an entry now requires deliberately fabricating a user quote that is visible in the git diff. NO PreToolUse provenance hook is built — it is REJECTED, not deferred, because it cannot enforce truth."
    - "D-04: the lint ships as a runnable module tools/harness_lint/provenance.py exposing a pure check/Violation API plus a main() exiting 1 on violation, cloning tools/polyglot_lint/lint.py's shape verbatim. Wired into /lint (fast local signal) AND covered by pytest under tools/harness_lint/tests/ (CI core-suite merge gate) — both, not either."
    - "D-14: the lint checks EVERY agreement file the shared predicate selects, including status: retired entries. It does not inherit the injector's L5 filter, so a status: pending typo is flagged rather than silently skipped."
    - "D-16: ROADMAP SC2's 'follows the existing stale-derived gate pattern (regenerate → verify)' is a CATEGORY ERROR — agreements are never regenerated (.memory/agreements/README.md:4-5), so there is nothing to regenerate. D-04's runnable-lint + pytest shape is the correct reading of SC2's intent. Flagged so /verify-work does not fail the phase on SC2's literal wording."
    - "The /lint provenance step is presence-safe: the active agreement set is legitimately EMPTY, and an empty dir must exit 0, never a false failure."
  artifacts:
    - path: "tools/harness_lint/provenance.py"
      provides: "The D-04 runnable provenance lint: Violation + pure checkers + main() exit 0/1"
      exports: ["Violation", "check_agreement", "lint_file", "lint_dir", "main"]
      min_lines: 60
    - path: "tools/harness_lint/tests/test_provenance.py"
      provides: "Validation-map rows 1-10, 12, 13 — positive + every negative case"
      min_lines: 60
    - path: ".memory/agreements/_TEMPLATE.md"
      provides: "The D-01/D-02 canonical shape with a quoted added: stamp"
      contains: "added: \"YYYY-MM-DD\""
  key_links:
    - from: "tools/harness_lint/provenance.py"
      to: "tools/harness_lint/agreements.py"
      via: "iter_agreement_files/load_agreement — the shared L1-L4 predicate (D-05)"
      pattern: "from tools\\.harness_lint\\.agreements import"
    - from: "harness/commands/lint.md"
      to: "tools/harness_lint/provenance.py"
      via: "presence-safe ! bash macro invoking the module by module path"
      pattern: "tools\\.harness_lint\\.provenance"
---

<objective>
Ship the durable mitigation for T-13-01: a runnable lint that fails loud when an agreement file's
origin stamp is absent or malformed, wired into both `/lint` (fast local signal) and pytest (the
non-bypassable CI merge gate).

Purpose: Phase 13 turned `.memory/agreements/` into a writable priority-0 instruction channel and
shipped only **interim** defenses. This is the durable guard. **Plan it as a security control, not a
formatting nicety — and to the honest claim (D-03): it enforces SHAPE, not TRUTH.**
Output: `tools/harness_lint/provenance.py`, its test suite, a corrected `_TEMPLATE.md`, and a
presence-safe `/lint` step.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-CONTEXT.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-VALIDATION.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-01-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

The shared predicate from Plan 01 (tools/harness_lint/agreements.py) — L1-L4 only; status is
deliberately NOT its concern (D-14), so retired entries ARE returned:
```python
def iter_agreement_files(agreements_dir: Path) -> list[Path]: ...   # sorted, confined, no symlinks
def load_agreement(path: Path) -> tuple[dict, str] | None: ...      # None on OSError/ValueError
```

The D-04 shape to clone — tools/polyglot_lint/lint.py:47-53, 118-146:
```python
@dataclass(frozen=True)
class Violation:
    """One §4.3-4.6 boundary breach: a stable rule ``code`` + a human-readable ``detail``."""
    rule: str
    detail: str

def main(argv: list[str] | None = None) -> int:
    import argparse                                    # argparse imported INSIDE main()
    parser = argparse.ArgumentParser(description="POLY-01 polyglot §4.3-4.6 boundary linter")
    parser.add_argument("path", help="TSV file to lint")
    args = parser.parse_args(argv)
    violations = lint_file(args.path, args.kinds)
    if not violations:
        print(f"polyglot-lint: OK — {args.path}")      # OK  -> stdout
        return 0
    for v in violations:
        print(f"polyglot-lint: FAIL [{v.rule}] {v.detail}", file=sys.stderr)   # FAIL -> stderr, ALL of them
    return 1

if __name__ == "__main__":
    raise SystemExit(main())
```

The current template being fixed — .memory/agreements/_TEMPLATE.md (line 3 is the D-02 defect):
```
---
status: active # active | retired
added: YYYY-MM-DD
provenance: "added because <verbatim user feedback>"
---
```

The tier contract it must mirror exactly and nothing more — .memory/agreements/README.md:
"Its YAML frontmatter must contain `status:` (`active` or `retired`), `added:` (an ISO date), and
`provenance:` (`"added because <verbatim user feedback>"`)."

VERIFIED LIVE — how parse_frontmatter renders each `added:` spelling (this is D-02's corrected
mechanism; the placeholder is str TODAY, a real unquoted date is NOT):
```
added: YYYY-MM-DD     -> 'YYYY-MM-DD'                str    (placeholder: not a parseable date)
added: 2026-07-16     -> datetime.date(2026, 7, 16)  date   <- the defect /agree would write
added: "2026-07-16"   -> '2026-07-16'                str    <- the fix
```

The presence-safe /lint macro precedent — harness/commands/lint.md:37 (note the `if [ -z "$files" ]`
announced-SKIP branch, the `|| fail=1` accumulate-then-fail loop, and `uv run python -m tools.<member>.<module>`):
```
!`fail=0; files=$(git ls-files '*.tsv'); if [ -z "$files" ]; then echo "SKIP: no tracked *.tsv wire files — polyglot §4.3-4.6 check is a no-op (exit 0)."; else ... for f in $files; do uv run python -m tools.polyglot_lint.lint "$f" || fail=1; done; if [ "$fail" -ne 0 ]; then echo "FAIL: ..."; exit 1; fi; echo "OK: ..."; fi`
```
lint.md:14 states the design law this plan inherits: "Thin macro over the canonical linters. Wraps
the existing tools; adds no new rules."

The fixture corpus (relocated by Plan 01 to tools/harness_lint/tests/conftest.py) — every synthetic
entry already carries a QUOTED `added: "2026-01-02"`, but `provenance: "synthetic test fixture"`
does NOT match `^added because \S`. Tests needing a passing corpus must build their own tmp_path
entries; do not retro-stamp the shared fixture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the runnable provenance lint</name>

  <files>tools/harness_lint/provenance.py</files>

  <read_first>
    - `tools/polyglot_lint/lint.py` — in full. This is the shape to clone verbatim: the frozen `Violation(rule, detail)` dataclass (47-53), pure checkers returning `[]` and never raising (55-103), `lint_file` (106-115), `main()` with `import argparse` inside it and exit 0/1 (118-142), and the `if __name__ == "__main__": raise SystemExit(main())` guard (145-146).
    - `tools/harness_lint/agreements.py` — the Plan-01 predicate this module consumes (D-05).
    - `.memory/agreements/_TEMPLATE.md` — the shape D-01 regexes against, "exactly and nothing more".
    - `.memory/agreements/README.md` — the tier's own contract: the authoritative wording for the three required frontmatter keys.
    - `tools/harness_lint/frontmatter.py` — do not re-roll fence slicing; `load_agreement` already delegates here.
    - `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-CONTEXT.md` — D-01, D-02 (+ the "Amendments from research" correction), D-03, D-04, D-14, D-16.
  </read_first>

  <behavior>
    - A well-formed entry (`status: active`, `added: "2026-01-02"`, `provenance: "added because the user asked for X"`) → `[]`.
    - `provenance:` absent → Violation.
    - `provenance: "because x"` (no `added ` prefix) → Violation.
    - `provenance: "added because"` (empty tail) → Violation.
    - `provenance: "added because   "` (whitespace-only tail) → Violation.
    - `added: 2026-07-16` unquoted → a `datetime.date` → **Violation, NOT TypeError** (D-02 ordering).
    - `added: "16-07-2026"` (non-ISO) → Violation.
    - `status: pending` → Violation (reachable only because D-14 kept L5 out of the shared predicate).
    - `status: retired` + a well-formed stamp → `[]` (retired entries ARE linted, not skipped).
    - Empty dir → `[]`, exit 0. `_TEMPLATE.md` and `README.md` are excluded by the predicate, never flagged.
    - `main()`: exit 0 clean (OK → stdout) / exit 1 dirty (every violation → stderr, one line each).
  </behavior>

  <action>
Create `tools/harness_lint/provenance.py` with `from __future__ import annotations`, cloning
`tools/polyglot_lint/lint.py`'s shape rather than inventing a new one (D-04). A plan that introduces
a new `Violation`-like type, a new refusal idiom, or a new frontmatter reader has failed.

Define module-level constants exactly mirroring `_TEMPLATE.md` and nothing more (D-01):
`_ISO_DATE = re.compile(r"^\d{4}-\d{2}-\d{2}$")`, `_PROVENANCE = re.compile(r"^added because \S")`,
`_STATUS = frozenset({"active", "retired"})`. No crypto, no signatures, no attestation — those are
theater against D-03's threat model.

Define a frozen `Violation(rule: str, detail: str)` dataclass, then:
- `check_agreement(frontmatter: dict) -> list[Violation]` — the pure checker. Clean input → `[]`,
  never `None`, **never raises**. Stable rule codes: `PROV-status`, `PROV-added-type`,
  `PROV-added-format`, `PROV-provenance`. Accumulate **all** violations; do not early-return.
  - **`added`: the `isinstance(added, str)` check MUST come first**, emitting `PROV-added-type`
    naming the received `type(added).__name__` and instructing `added: "YYYY-MM-DD"`. Only in the
    `elif` branch apply `_ISO_DATE.match(added)` → `PROV-added-format`. **Order is load-bearing:**
    applying the regex to a `datetime.date` raises `TypeError`, and a crashed gate lets the guarded
    thing through. **Do not `import datetime`** — `isinstance(x, str)` is already `False` for a date
    object without naming the type, which also keeps the door open to future gate widening.
  - **`status`:** flag anything not in `_STATUS`. Coerce for comparison the way `inject.py:105` does
    (`str(...).strip()`) but do **not** skip non-active values — per D-14 this lint checks every
    selected file, which is the only reason a `status: pending` typo is catchable at all.
  - **`provenance`:** require a `str` matching `_PROVENANCE`. The regex's `\S` is what rejects the
    empty and whitespace-only tails.
- `lint_file(path: str | Path) -> list[Violation]` — `load_agreement(path)`; `None` (unreadable /
  malformed frontmatter) → a `PROV-unparseable` Violation rather than a silent skip; else
  `check_agreement(frontmatter)`.
- `lint_dir(agreements_dir: Path = AGREEMENTS_DIR) -> list[tuple[Path, Violation]]` — select via
  `iter_agreement_files` and lint each. Define `AGREEMENTS_DIR` from
  `Path(__file__).resolve().parents[2] / ".memory" / "agreements"`, mirroring `inject.py:17-20`.
- `main(argv: list[str] | None = None) -> int` — `import argparse` **inside** the function; one
  optional positional `agreements_dir` defaulting to `AGREEMENTS_DIR`; exit **0** clean / **1** dirty;
  OK line → **stdout** (`provenance-lint: OK — N agreement(s)`); every violation → **stderr**
  (`provenance-lint: FAIL [{rule}] {path.name}: {detail}`), all of them, not just the first.
  Deliberate divergence to record in the docstring: this lint prints failures to **stderr** per
  `lint.py:141`, whereas `/agree` (Plan 03) prints its refusal to **stdout** per `approve.py` — each
  follows its own named precedent; it is a choice, not an inconsistency.

**Selection lives in this module, not in the `/lint` macro.** `main()` takes a *directory* and calls
`iter_agreement_files`; it must not accept a pre-globbed file list. If the macro re-derived the
`_`/README exclusion with `grep -v`, that macro would become a third copy of the predicate — exactly
the drift D-05 forbids, and `lint.md:14` bars a macro from adding rules.

Module docstring must state the honest claim (D-03): this enforces **shape, not truth**; a
well-formed but fabricated provenance passes; ROADMAP SC2/MEM2-04's "so agents cannot auto-invent
entries" overclaims. What it buys: an omitted or malformed stamp fails loud, and inventing an entry
now requires deliberately fabricating a quote attributed to the user — a visible, auditable act in
the git diff rather than a silent accident. Same trust model ADR-0007 already accepted for
`HARNESS_DEV_BYPASS` ("accident-prevention, not a sandbox"). Also record D-16: SC2's
"regenerate → verify" clause is a category error — this tier is never regenerated
(`.memory/agreements/README.md:4-5`), so validate-in-place is the correct pattern.

Do **not** check `Related:` presence or resolve link targets — deferred to Phase 16 (MEM2-07). Do
**not** scan for credential shapes — the no-secrets rule is documentation (README:13), not this
lint's job. Do **not** build a PreToolUse hook: D-03 **rejects** it outright.
  </action>

  <verify>
    <automated>uv run pytest tools/harness_lint -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -m tools.harness_lint.provenance` (real, empty-of-entries dir) → **exit 0**, prints an OK line to stdout.
    - `grep -Ec "^import datetime|^from datetime|datetime\." tools/harness_lint/provenance.py` → **0** (D-02: no datetime import).
    - Source order check — `isinstance` precedes the ISO regex: `python3 -c "import re,pathlib; t=pathlib.Path('tools/harness_lint/provenance.py').read_text(); assert t.index('isinstance(added') < t.index('_ISO_DATE.match'), 'D-02: isinstance MUST precede the regex'"` → exits 0.
    - `grep -n "from tools.harness_lint.agreements import" tools/harness_lint/provenance.py` → matches (D-05: consumes the shared predicate, no second glob).
    - `grep -c "glob(" tools/harness_lint/provenance.py` → **0** (selection is the predicate's job).
    - `grep -Ec "class Violation|rule: str|detail: str" tools/harness_lint/provenance.py` → ≥ 3 (cloned shape, not a new type).
    - `grep -n "file=sys.stderr" tools/harness_lint/provenance.py` → matches (FAIL → stderr).
    - `grep -Ein "shape, not truth|overclaim" tools/harness_lint/provenance.py` → matches (D-03 recorded honestly in the docstring).
    - `ruff check tools/harness_lint/provenance.py && ruff format --check tools/harness_lint/provenance.py` → clean.
  </acceptance_criteria>

  <done>A runnable `tools.harness_lint.provenance` module exists cloning `polyglot_lint`'s shape, consuming the shared predicate, checking every selected file (retired included), with `isinstance` before the regex and the honest D-03 claim in its docstring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every negative case — including the date-object crash path</name>

  <files>tools/harness_lint/tests/test_provenance.py</files>

  <read_first>
    - `tools/harness_lint/provenance.py` — the module under test (Task 1).
    - `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-VALIDATION.md` — the Per-Task Verification Map, **rows 1-10, 12, 13 are this task's spec**. Every row must have a test.
    - `tools/harness_lint/tests/conftest.py` — the `tmp_agreements_tree` corpus (relocated by Plan 01). Note its `provenance: "synthetic test fixture"` values do NOT match `^added because \S` — build purpose-made `tmp_path` entries for the passing cases rather than retro-stamping the shared fixture.
    - `tools/memory_regen/tests/test_inject_assembler.py:109-117` — `test_absent_stamp_degrades_gracefully`, the contrast case: the **injector** coerces and degrades; the **lint** must fail loud. Both behaviors are correct and must not be conflated.
  </read_first>

  <behavior>
    Rows 1-10, 12, 13 of the Per-Task Verification Map, each as its own test:
    - Row 1 — well-formed stamp → `[]`.
    - Row 2 — `provenance:` absent → Violation.
    - Row 3 — no `added because ` prefix → Violation.
    - Row 4 — `provenance: "added because"` (empty tail) → Violation.
    - Row 5 — whitespace-only tail → Violation.
    - **Row 6 — `added: 2026-07-16` unquoted → date object → Violation, NOT TypeError (D-02).**
    - Row 7 — `added: "16-07-2026"` → Violation.
    - Row 8 — `status: pending` → Violation (only reachable because of D-14).
    - Row 9 — `status: retired` entry **IS linted**, not skipped (D-14).
    - Row 10 — `_TEMPLATE.md` + `README.md` excluded, **not flagged**.
    - Row 12 — empty dir → `[]`, exit 0 (presence-safe).
    - Row 13 — `main()` exit 0 clean / 1 dirty; FAIL → stderr, OK → stdout (capsys).
  </behavior>

  <action>
Create `tools/harness_lint/tests/test_provenance.py` with `from __future__ import annotations`, one
test per behavior row above, each writing its entries into `tmp_path` and calling `lint_dir(tmp_path)`
or `lint_file(...)`. **Never write to the real `.memory/agreements/`** — the active set is
legitimately empty and that is correct behavior, not a bug.

**Row 6 is the highest-value test in this plan.** Write `added: 2026-07-16` **unquoted** into a
tmp entry so `parse_frontmatter` genuinely yields a `datetime.date`, then assert the result is a
`PROV-added-type` Violation. The test must fail if the implementation raises — do not wrap the call
in `pytest.raises`; a `TypeError` escaping here means the lint crashes instead of failing, and a
crashed gate lets the guarded thing through. Assert on the rule code, not on message prose.

**Row 9 pins D-14.** A `status: retired` entry with a **malformed** stamp must produce a Violation —
this is what proves the lint did not inherit the injector's L5 filter. Also assert a `status: retired`
entry with a **well-formed** stamp yields `[]`, so "retired is linted" does not degrade into
"retired is rejected".

**Row 10** asserts absence: drop a `_TEMPLATE.md` (whose own frontmatter has no `added`/`provenance`)
and a `README.md` (no frontmatter at all) into the tmp dir and assert `lint_dir` returns **zero**
violations. If either were flagged, `/lint` would be red on a clean checkout on day one.

**Row 13** uses `capsys`: assert `main([str(tmp_dir)])` returns 0 on a clean dir with the OK line in
`capsys.readouterr().out`, and returns 1 on a dirty dir with every violation in `.err` and nothing
alarming in `.out`. Assert **all** violations surface in one run, not just the first — the
accumulate-then-fail property.

Use parametrization where it keeps rows readable, but keep row 6 standalone and explicitly named
(e.g. `test_unquoted_date_yields_violation_not_typeerror`) so a future reader cannot miss why it
exists.
  </action>

  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_provenance.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_provenance.py -x -q` → all pass, **≥ 12 tests collected** (one per row 1-10, 12, 13).
    - `grep -n "test_unquoted_date_yields_violation_not_typeerror\|PROV-added-type" tools/harness_lint/tests/test_provenance.py` → matches (row 6 present and named).
    - `grep -c "added: 2026-07-16" tools/harness_lint/tests/test_provenance.py` → ≥ 1 **unquoted** occurrence (proves the date-object path is genuinely exercised, not simulated).
    - `grep -n "capsys" tools/harness_lint/tests/test_provenance.py` → matches (row 13).
    - `grep -n "status: pending" tools/harness_lint/tests/test_provenance.py` → matches (row 8, the D-14 payoff).
    - `grep -rn "\.memory/agreements" tools/harness_lint/tests/test_provenance.py` → **no match** (no real-dir writes).
    - **Mutation drill (run, then revert):** in `provenance.py`, swap the `isinstance`/regex order so the regex runs first → `uv run pytest tools/harness_lint/tests/test_provenance.py -q` must **FAIL** with a `TypeError`-driven failure → revert → green. This proves row 6 actually guards D-02's ordering rule.
    - `uv run pytest tools/harness_lint tools/memory_regen -q` → **0 failed**.
  </acceptance_criteria>

  <done>Every Per-Task Verification Map row 1-10, 12, 13 has a passing test; the date-object path provably yields a Violation rather than a crash; retired entries are provably linted.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the template's unquoted stamp and wire the presence-safe /lint step</name>

  <files>.memory/agreements/_TEMPLATE.md, harness/commands/lint.md</files>

  <read_first>
    - `.memory/agreements/_TEMPLATE.md` — all 11 lines. Line 3 (`added: YYYY-MM-DD`) is the D-02 fix; the body shape (title + one-line rule + `Related:`) must not change.
    - `harness/commands/lint.md` — in full. Line 14 is the design law ("adds no new rules"); line 37 is the presence-safe macro to clone; 39-44 is the Notes section to extend.
    - `.memory/agreements/README.md` — the tier contract the template must keep mirroring.
    - `tools/harness_lint/provenance.py` — the module the macro invokes (Task 1); confirm `main()`'s directory-argument signature before writing the macro.
  </read_first>

  <action>
**`_TEMPLATE.md` (D-02):** change line 3 from `added: YYYY-MM-DD` to `added: "YYYY-MM-DD"`. Change
nothing else — not the `status: active # active | retired` comment, not the `provenance:` line, not
the body, not the `Related:` line. The template is the shape D-01 regexes against "exactly and
nothing more", and it is asserted by `test_inject_determinism.py:98-100`
(`test_tmp_agreements_tree_fixture_shape` checks `"status: active" in template` and the
`<One-line working-style or methodology rule.>` marker) — those assertions must stay green.

Record the corrected mechanism (the research amendment supersedes D-02's original reasoning): the
placeholder `YYYY-MM-DD` parses to `str` **today** because it is not a parseable date — the template
is not broken right now. The defect is **latent and pedagogical**: the template teaches the
*unquoted shape*, and the instant `/agree` (Plan 03) substitutes a real ISO date into it, the value
becomes a `datetime.date` and the lint would reject every file `/agree` itself wrote. The fix stands;
only the reason changed.

**`harness/commands/lint.md` (D-04's "fast local signal" half):** add a new section after the
polyglot §4.3-4.6 section, mirroring that section's structure — a prose paragraph naming the engine
and the presence-safe property, then one `!`-prefixed bash macro.

The macro must be **presence-safe**: the active agreement set is legitimately **EMPTY** (D-13), so a
step that treats "no entries" as failure would make `/lint` red on every developer machine on day
one. Since `provenance.main()` already takes a directory and returns 0 on an empty dir (Task 1),
the macro is a single `uv run python -m tools.harness_lint.provenance` invocation — **no `git ls-files`
loop and no `grep -v` exclusion**. Re-deriving the `_`/README exclusion in shell would make the macro
a third copy of the predicate (the drift D-05 forbids) and would violate `lint.md:14`'s "adds no new
rules". Guard the directory's existence in shell only (`if [ ! -d .memory/agreements ]` → announced
SKIP, exit 0), so the step degrades the same way the `.NET` step does when its SDK is absent.

Extend the Notes section with one bullet: the provenance check reuses the `tools.harness_lint.provenance`
engine — the same engine the pytest merge gate (`core-suite`) calls — and it enforces **shape, not
truth** (D-03). Do not overstate it as preventing invented entries.

**Do NOT emit.** Do not touch `.opencode/` or `.claude/`, and do not run `tools.harness_emit`.
`test_coexist.py:53-54` asserts exactly **19** emitted commands; this plan authors no new command
file, so the count is unaffected — but a stray emit of the edited `lint.md` would still dirty the
trees and push `harness_emit` past its sanctioned 1-failed baseline. Phase 15 owns the round-trip (D-10).
  </action>

  <verify>
    <automated>uv run pytest tools/harness_lint tools/memory_regen -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'added: "YYYY-MM-DD"' .memory/agreements/_TEMPLATE.md` → **1**.
    - `grep -c '^added: YYYY-MM-DD$' .memory/agreements/_TEMPLATE.md` → **0** (unquoted form gone).
    - Template still parses to `str`: `uv run python -c "from tools.harness_lint import parse_frontmatter; import pathlib; fm,_=parse_frontmatter(pathlib.Path('.memory/agreements/_TEMPLATE.md').read_text()); assert isinstance(fm['added'], str), type(fm['added'])"` → exits 0.
    - `uv run pytest tools/memory_regen/tests/test_inject_determinism.py::test_tmp_agreements_tree_fixture_shape -q` → passes (template assertions intact).
    - `grep -n "tools.harness_lint.provenance" harness/commands/lint.md` → matches.
    - `grep -c "git ls-files '.memory/agreements" harness/commands/lint.md` → **0** (no shell-side re-derived predicate).
    - `uv run pytest tools/harness_lint/tests/test_commands.py -q` → passes (`lint.md`'s frontmatter/description still satisfy the glob lint).
    - `ls harness/commands/*.md | wc -l` → **19** (this plan authors no command; the count is Phase 15's to bump).
    - `git status --porcelain .opencode .claude tools/harness_emit/tests/__snapshots__` → **empty** (D-10).
    - `uv run pytest tools/harness_emit -q` → **exactly 1 failed / 46 passed**.
  </acceptance_criteria>

  <done>`_TEMPLATE.md` teaches the quoted stamp that round-trips as `str`; `/lint` carries a presence-safe provenance step that delegates selection to the shared predicate; nothing is emitted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.memory/agreements/*.md` → `inject.assemble()` → SessionStart | Committed-but-writable files become priority-0 agent directives (the accepted Q1 trade-off). This lint is that channel's durable guard. |
| agent/human write → `.memory/agreements/` | Not path-denied by design (D-08: agreements are not constitution plane). The stamp, not a write-lock, is the control. |
| CI `core-suite` → merge | The non-bypassable enforcement point. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 (inherited) | Elevation of Privilege | agent self-invents an agreement → priority-0 self-instruction | **mitigate (PARTIAL BY DESIGN — D-03)** | `provenance.py` fails loud on an **omitted or malformed** stamp. It does **not** and **cannot** detect a well-formed **fabricated** stamp. Residual risk explicitly accepted: inventing an entry now requires deliberately fabricating a user quote, visible in the git diff. **No PreToolUse hook** — D-03 rejects it as theater. |
| T-13-01b | Repudiation | a malformed/absent stamp slipping in silently | mitigate | Dual enforcement (D-04): `/lint` = fast local signal; `tools/harness_lint/tests/` → CI `core-suite` = non-bypassable merge gate. Both, not either. |
| T-14-02 | Repudiation | retired entries never re-linted → forged stamps become unauditable forever (D-09 keeps them forever) | mitigate | D-14: the lint does not inherit L5; every selected file is checked. Asserted by validation rows 8 + 9. |
| T-14-04 | Denial of Service | the lint **crashing** on a `datetime.date` (`TypeError`) instead of failing | mitigate | D-02 ordering: `isinstance` strictly before the regex. A crashed gate lets the guarded thing through. Asserted by row 6 + the ordering mutation drill. |
| T-14-05 | Denial of Service | the `/lint` provenance step failing on the legitimately EMPTY active set | mitigate | Presence-safe: `main()` returns 0 on an empty dir; the macro announces a SKIP when the dir is absent. Asserted by row 12. |
| T-14-06 | Tampering | the `/lint` macro re-deriving the `_`/README exclusion in shell → a third copy of the predicate | mitigate | Selection stays in `provenance.py` via `iter_agreement_files`; the macro passes a directory and adds no rules (`lint.md:14`). |
| T-14-07 | Information Disclosure | credentials pasted into an agreement, committed + injected every session | **accept** | Out of scope for D-01's regex (which mirrors `_TEMPLATE.md` "exactly and nothing more"). The no-secrets rule is stated at `.memory/agreements/README.md:13`; `secret_scan` PreToolUse catches shape-matching values on the Write path. The lint does not scan for credential shapes. |
| T-14-08 | Tampering | emitting, or "fixing" the red `harness_emit` snapshot | mitigate | D-10: source-only. `git status --porcelain .opencode .claude tools/harness_emit/tests/__snapshots__` → empty is an acceptance criterion on Task 3. **Never** run `--snapshot-update`. |
| T-13-SC | Tampering | package installs | n/a | **Zero external packages.** This phase must not resolve a single new package; `harness_lint/pyproject.toml:6-8` bars mutating `uv.lock` for external deps. No legitimacy audit required. |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint tools/memory_regen -q` → **0 failed**.
- `uv run pytest -q` → **exactly 1 failed / 620 passed**. **`0 failed` is a FAILURE CONDITION** (the snapshot was updated → Phase 15's gate stolen).
- `uv run python -m tools.harness_lint.provenance` against the real (entry-less) dir → **exit 0**.
- `ls harness/commands/*.md | wc -l` → 19; `git status --porcelain .opencode .claude` → empty.
- `ruff check . && ruff format --check .` → clean.
- **D-16 note for `/verify-work`:** ROADMAP SC2's "follows the existing `stale-derived` gate pattern (regenerate → verify)" is a **category error** and is NOT satisfied literally, by design. Agreements are never regenerated (`.memory/agreements/README.md:4-5`), so there is nothing to regenerate → diff. SC2's **substance** ("fails when an agreement file lacks a well-formed provenance/origin stamp") is fully satisfied by D-04's validate-in-place lint. Do not fail the phase on SC2's literal wording.
- **D-03 note for `/verify-work`:** SC2/MEM2-04's "so agents cannot auto-invent entries" **overclaims** and is NOT delivered, by design. The lint enforces shape, not truth. Verify the honest claim instead.
</verification>

<success_criteria>
- `tools.harness_lint.provenance` runs from the CLI: exit 0 clean / 1 dirty, OK → stdout, all FAILs → stderr.
- Every validation-map row 1-10, 12, 13 has a passing test; row 6 proves a `date` object yields a Violation, not a `TypeError`.
- Retired and `pending`-status entries are linted (D-14); `_TEMPLATE.md`/`README.md` are excluded, never flagged.
- `_TEMPLATE.md` ships `added: "YYYY-MM-DD"` and round-trips as `str`.
- `/lint` carries a presence-safe provenance step that adds no rules and re-derives no predicate.
- Nothing emitted; `harness_emit` still exactly 1 failed / 46 passed; command count still 19.
</success_criteria>

<output>
Create `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-02-SUMMARY.md` when done.
</output>
