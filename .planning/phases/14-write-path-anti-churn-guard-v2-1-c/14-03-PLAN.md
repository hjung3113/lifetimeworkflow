---
phase: 14-write-path-anti-churn-guard-v2-1-c
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - tools/agree/pyproject.toml
  - tools/agree/__init__.py
  - tools/agree/write.py
  - tools/agree/tests/__init__.py
  - tools/agree/tests/conftest.py
  - tools/agree/tests/test_agree_refusal.py
  - harness/commands/agree.md
  - uv.lock
autonomous: true
requirements: [MEM2-04]

must_haves:
  truths:
    - "D-07: /agree REFUSES rather than inventing a stamp. It requires an explicit --because \"<verbatim user feedback>\"; missing, empty, or whitespace-only → a dedicated AgreementRefused exception and CLI exit 3, mirroring tools/golden_runner/approve.py's GoldenApprovalRefused shape. The --because value becomes the provenance tail, so the stamp cannot be FORGOTTEN — only deliberately forged, which is D-03's accepted residual risk."
    - "D-08: /agree does NOT require GOLDEN_APPROVE_HUMAN. Agreements are deliberately not constitution plane; requiring the human token would reintroduce the capture friction §2 rejected and would mislabel a working-style note as constitution ratification. --because is the gate; the user directing the write IS the ratification."
    - "D-09: retire = flip status: to retired IN PLACE; never delete. --retire <slug> performs the flip only — no file removal, no history rewrite. The retired entry stays as the audit trail and inject.py's fail-closed L5 filter already excludes it from rendering."
    - "D-15: the --because value is YAML-SERIALIZED, never f-stringed into the template. A double-quote or newline in the user's verbatim feedback would otherwise break the quoted scalar and forge sibling frontmatter keys (e.g. inject status: active into a retired entry). This is a genuine injection surface on the write path — the exact thing this phase exists to harden."
    - "D-19: /agree's home is a NEW tools/agree/ member. tools/memory_regen is FORBIDDEN by the tier's own contract (.memory/agreements/README.md:4-5: 'never written by tools/memory_regen'). tools/agree/ is auto-enrolled by the existing tools/* workspace glob; measured lock cost is 4 deterministic lines, zero resolution, zero guard tests."
    - "D-10: SOURCE-ONLY. harness/commands/agree.md is authored; .opencode/ and .claude/ are NOT touched and the emitter is NOT run. test_coexist.py:53-54 asserts exactly 19 emitted commands — emitting makes it 20 and breaks a currently-passing test. Phase 15 owns the round-trip and the 19→20 bump."
    - "D-11: EXPECTED_COMMANDS does not exist — it is NOT created to satisfy ROADMAP SC3's wording. test_commands.py is glob-driven and auto-covers harness/commands/agree.md with zero test edits. The unrelated EXPECTED_GOLDEN_ADJACENT frozenset (test_commands.py:42-45) is the 8 golden-adjacent commands from a prior phase and must NOT gain 'agree'."
    - "D-20: plans, code, and docs must not quote approve.py's token-comparison line verbatim — secret_scan (secret_scan.py:47) matches its shape and DENIED the researcher's first write. Reword; never bypass, never weaken secret_scan to accommodate a doc."
    - "D-03: shape, not truth. /agree makes the stamp unforgettable, not unforgeable. The command doc must not claim it prevents invented entries."
  artifacts:
    - path: "tools/agree/write.py"
      provides: "AgreementRefused + add/retire + main() exit 0/3, YAML-serialized provenance"
      exports: ["AgreementRefused", "add", "retire", "main"]
      min_lines: 70
    - path: "tools/agree/pyproject.toml"
      provides: "Zero-dep virtual member; auto-enrolled by the tools/* glob"
      contains: "package = false"
    - path: "tools/agree/tests/test_agree_refusal.py"
      provides: "Validation-map rows 16-20 — refusal, YAML injection, lint round-trip, in-place retire"
      min_lines: 60
    - path: "harness/commands/agree.md"
      provides: "The source-side /agree command surface; a thin macro over the coded refusal gate"
      contains: "$ARGUMENTS"
  key_links:
    - from: "harness/commands/agree.md"
      to: "tools/agree/write.py"
      via: "! macro passing $ARGUMENTS positionally (no shell-string construction)"
      pattern: "tools\\.agree"
    - from: "tools/agree/tests/test_agree_refusal.py"
      to: "tools/harness_lint/provenance.py"
      via: "round-trip: a file /agree wrote must PASS the provenance lint"
      pattern: "from tools\\.harness_lint\\.provenance import"
    - from: "tools/agree/write.py"
      to: "tools/harness_lint/agreements.py"
      via: "iter_agreement_files for confined retire-target lookup (D-05/D-06)"
      pattern: "from tools\\.harness_lint\\.agreements import"
---

<objective>
Ship the write half of the PROCESS channel: a `/agree` command that is the sanctioned — and only —
way an agreement is added or retired, and that **refuses** rather than inventing an origin stamp.

Purpose: MEM2-04's write path. `--because` makes the provenance stamp impossible to *forget*; D-03's
accepted residual is that a determined agent can still *forge* one, visibly, in the git diff.
Output: a new zero-dep `tools/agree/` member, its refusal-first writer, its tests, and the
source-side command file. **Source-only — nothing is emitted.**
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-CONTEXT.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-VALIDATION.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-01-SUMMARY.md
@.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-02-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

The D-07 refusal shape to clone — tools/golden_runner/approve.py:29-30, 47-50, 91-98.
NOTE (D-20): approve.py's token-comparison line is NOT reproduced here — quoting it verbatim trips
the repo's own secret_scan (secret_scan.py:47 matches `token = <16+ chars>`). Described instead:
the guard reads the expected value from the env and refuses when it is falsy or does not match.
```python
class GoldenApprovalRefused(Exception):
    """Promotion .received → .verified refused (missing human sign-off / ADR / received file)."""

def promote(case: str, *, approve: bool = False, adr: str | None = None,
            human_token: str | None = None) -> Path:          # keyword-only signals after *
    if not approve:
        raise GoldenApprovalRefused(
            "REFUSED: promotion requires an explicit human --approve flag "
            "(agents must not self-bless the golden baseline, P9)."
        )
    ...

def main(argv: list[str] | None = None) -> int:
    import argparse
    ...
    try:
        verified = promote(args.case, approve=args.approve, adr=args.adr, human_token=args.confirm)
    except GoldenApprovalRefused as exc:
        print(str(exc))        # refusal -> STDOUT (approve.py's convention; the lint uses stderr)
        return 3
    print(f"PROMOTED: {verified} (ADR: {args.adr}).")
    return 0
```

The blank/whitespace rule — use the STRICTER `.strip()` spelling (tools/hooks/_stdin.py:42-44,
contract_guard.py:103), NOT approve.py's older bare-falsy check. ADR-0007:52 states it normatively:
"unset / empty / whitespace-only ⇒ no bypass."
```python
bool((os.environ.get(DEV_BYPASS_ENV) or "").strip())
```

The shared predicate (Plan 01, tools/harness_lint/agreements.py) — for confined retire lookup:
```python
def iter_agreement_files(agreements_dir: Path) -> list[Path]: ...   # sorted, confined, no symlinks
def load_agreement(path: Path) -> tuple[dict, str] | None: ...
```

The lint the round-trip test asserts against (Plan 02, tools/harness_lint/provenance.py):
```python
def lint_file(path: str | Path) -> list[Violation]: ...    # [] == well-formed
```
Its rules: status ∈ {active, retired}; added isinstance str AND ^\d{4}-\d{2}-\d{2}$;
provenance matching ^added because \S.

The template to fill — .memory/agreements/_TEMPLATE.md AFTER Plan 02's D-02 fix:
```
---
status: active # active | retired
added: "YYYY-MM-DD"
provenance: "added because <verbatim user feedback>"
---

# <Title — the working-agreement in a few words>

<One-line working-style or methodology rule.>

Related: [ADR-xxxx](../../docs/adr/0000-example.md) · [.planning/PROJECT.md § Key Decisions](../../.planning/PROJECT.md#key-decisions)
```

The command-authoring precedent — harness/commands/golden-approve.md:1-7, 16-19.
`description: >-` folded block STARTING with "Use when…" (test_commands.py:34's _ROUTING_TRIGGERS =
("use", "when")); `agent:` a bare lowercase-alnum-plus-hyphens slug; $ARGUMENTS passed POSITIONALLY:
```
---
description: >-
  Use when a human reviewer has approved a proposed golden baseline and wants to promote it —
  wraps the human-gated approval tool, which refuses (exit 3) without an explicit human flag, ...
agent: orchestrator
---
# /golden-approve — human-gated golden promotion (no agent self-bless)
Thin macro over the **already-coded** refusal gate. Do NOT re-implement approval logic — ...
!`python -m tools.golden_runner.approve $ARGUMENTS`
```

The member scaffold to clone — tools/harness_lint/pyproject.toml (zero-dep virtual member):
```toml
[project]
name = "logparser-harness-lint"
version = "0.0.0"
description = "..."
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
```
Root pyproject.toml:34 — `members = ["libs/python", "tools/*"]` is a GLOB: a new
tools/agree/pyproject.toml is auto-enrolled with NO root edit. `[tool.uv] package = false` is
MANDATORY — without it uv tries to build a wheel and `uv sync` fails.

The tests conftest sys.path shim to clone verbatim — tools/memory_regen/tests/conftest.py:26-33
(tests -> member -> tools -> repo root = parents[3]):
```python
_REPO_ROOT = Path(__file__).resolve().parents[3]
_LIBS_PYTHON = _REPO_ROOT / "libs" / "python"
for _p in (str(_REPO_ROOT), str(_LIBS_PYTHON)):
    if _p not in sys.path:
        sys.path.insert(0, _p)
```
⚠ tmp_agreements_tree (relocated to tools/harness_lint/tests/conftest.py by Plan 01) is NOT visible
to tools/agree/tests/ — pytest conftest scope is directory-based. Import it explicitly if needed.

The tier contract that FORBIDS the memory_regen home (.memory/agreements/README.md:4-5):
"It is not derived: it is never regenerated, is never written by `tools/memory_regen`, and does not
collide with `.memory/derived/`."

The glob-driven command lint that auto-covers agree.md with ZERO edits (test_commands.py:47-48):
```python
def _command_files() -> list[Path]:
    return sorted(_COMMANDS_DIR.glob("*.md"))
```
And the frozenset that must NOT gain "agree" (test_commands.py:42-45) — it is a prior phase's
golden-adjacent set, not an EXPECTED_COMMANDS:
```python
EXPECTED_GOLDEN_ADJACENT = frozenset(
    {"build", "test", "lint", "golden", "golden-approve", "adr", "checkpoint", "component"}
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the tools/agree member and build the refusal-first writer</name>

  <files>tools/agree/pyproject.toml, tools/agree/__init__.py, tools/agree/write.py, tools/agree/tests/__init__.py, tools/agree/tests/conftest.py, uv.lock</files>

  <read_first>
    - `tools/golden_runner/approve.py` — in full. The D-07 model: one dedicated exception class with a one-line docstring, keyword-only signals after `*`, cheapest/most-explicit guard first, the `REFUSED: <what is required> (<why, with the citation>).` message style, and `except ... → print(str(exc)); return 3`. **Do not quote its token-comparison line into any file — `secret_scan` will deny the write (D-20).**
    - `tools/hooks/_stdin.py:39-44` — `bool((os.environ.get(...) or "").strip())`, the stricter blank/whitespace spelling to mirror for `--because`.
    - `tools/harness_lint/pyproject.toml` — the zero-dep virtual-member scaffold (`dependencies = []`, `[tool.uv] package = false`).
    - `tools/memory_regen/tests/conftest.py:26-33` — the sys.path shim to clone verbatim.
    - `.memory/agreements/_TEMPLATE.md` — the shape to fill (post-Plan-02, with the quoted `added:`).
    - `.memory/agreements/README.md` — lines 4-5 forbid the `memory_regen` home (D-19); line 13 is the no-secrets rule to restate in the command doc.
    - `tools/harness_lint/frontmatter.py` — the parser; note it uses `ruamel.yaml` (`YAML(typ="safe")`). **Do NOT add pyyaml** — `harness_lint/pyproject.toml:8` is explicit that it is absent from the lock.
    - `tools/harness_lint/agreements.py` — `iter_agreement_files` for confined retire lookup.
  </read_first>

  <behavior>
    - `add(...)` with `because=None` / `""` / `"   "` → raises `AgreementRefused` (main → exit 3), and **writes no file**.
    - `add(...)` with real feedback → writes `<slug>.md` whose frontmatter passes the Plan-02 lint.
    - **D-15:** `because` containing `"` and/or `\n` round-trips: re-parsing the written file yields `"added because " + because` **exactly**, and yields **no extra frontmatter keys** (no forged `status:`).
    - `retire(slug)` flips `status: active` → `status: retired` in place; the file still exists; the diff touches only the status line.
    - `retire(unknown_slug)` → refuses; creates no file.
    - A slug containing `/`, `..`, or a leading `_` → refuses; writes nothing outside `agreements_dir`.
    - `add()` is pure w.r.t. the clock: it takes `added: str`. `main()` supplies today's ISO date.
  </behavior>

  <action>
**Scaffold the member (D-19).** `tools/memory_regen` is **FORBIDDEN** as the home by the tier's own
committed contract (`.memory/agreements/README.md:4-5`) — do not propose or use it. Create
`tools/agree/pyproject.toml` cloning `tools/harness_lint/pyproject.toml`'s shape: `name =
"logparser-agree"`, `version = "0.0.0"`, `requires-python = ">=3.11"`, **`dependencies = []`**, and
**`[tool.uv] package = false`** (mandatory — without it uv tries to build a wheel and `uv sync`
fails). Its `description` must honestly describe a *writer to `.memory/agreements/`*. **No root
`pyproject.toml` edit** — `members = ["libs/python", "tools/*"]` is a glob and auto-enrolls it.

Run `uv sync` once. The expected `uv.lock` diff is **exactly** one name in the sorted top list plus a
4-line `[[package]]` block (`name = "logparser-agree"`, `version = "0.0.0"`,
`source = { virtual = "tools/agree" }`) — **zero external resolution**. Phase 2's D-01 `uv.lock`
warning is about external *dependency* contention and does not apply to a zero-dep virtual member.
If `uv sync` resolves any new external package, stop: that is a scope violation.

Add empty `tools/agree/__init__.py` and `tools/agree/tests/__init__.py`, and
`tools/agree/tests/conftest.py` cloning `memory_regen/tests/conftest.py:26-33`'s shim verbatim
(`parents[3]` → repo root, plus `libs/python`).

**Build `tools/agree/write.py`** with `from __future__ import annotations`, cloning `approve.py`'s
refusal shape (D-07) — not a new idiom:
- `class AgreementRefused(Exception)` with a one-line docstring.
- `AGREEMENTS_DIR` from `Path(__file__).resolve().parents[2] / ".memory" / "agreements"`, mirroring
  `inject.py:17-20`.
- `add(slug, title, rule, *, because=None, added=None, related=None, agreements_dir=AGREEMENTS_DIR) -> Path`
  — keyword-only signals after `*`, raising rather than returning an error code. Guard order:
  cheapest/most-explicit first.
  1. `if not (because or "").strip(): raise AgreementRefused(...)` — use the **`.strip()` spelling**
     (`_stdin.py:44`, ADR-0007:52 "unset / empty / whitespace-only ⇒ no bypass"), NOT approve.py's
     older bare-falsy check, so `--because "   "` refuses. Message style mirrors `approve.py:47-50`:
     a `REFUSED: ` prefix naming the missing signal **and** the path to supply it, e.g. "REFUSED: an
     agreement is written ONLY in response to explicit user feedback. Supply the verbatim feedback
     with --because \"<what the user said>\"; it becomes the provenance stamp. An agent must not
     invent one (MEM2-04, §2 property 2)." The refusal message is a teaching surface, not just an error.
  2. Slug confinement (D-06 applied to the **write** path): refuse a slug containing `/`, `\`, `..`,
     whitespace, or a leading `_` (a leading `_` would make the entry invisible to the shared
     predicate — an entry the lint and injector both ignore). Validate against a slug regex mirroring
     `test_commands.py:37`'s bare-slug shape, then assert the resolved target is inside
     `agreements_dir.resolve()`.
  3. Refuse if the target already exists (never silently overwrite an agreement).
- **`--because` must be YAML-SERIALIZED, never f-stringed (D-15).** Build the frontmatter as a dict
  (`{"status": "active", "added": added, "provenance": f"added because {because}"}`) and dump it with
  `ruamel.yaml`'s `YAML(typ="safe")` — the repo's only YAML parser, already resolved transitively via
  `check-jsonschema`. **Do not add pyyaml** (`harness_lint/pyproject.toml:8`). Configure the dumper
  with `default_flow_style = False`. A `"` or newline in the user's verbatim feedback would otherwise
  close the quoted scalar and let an attacker forge sibling frontmatter keys — e.g. inject
  `status: active` into an entry meant to be retired. This is the genuine injection surface on the
  write path; it is not cosmetic. Ensure the dump emits `added` as a **quoted string** so it
  round-trips as `str` and passes the Plan-02 lint (D-02).
- Body: fill `_TEMPLATE.md`'s shape exactly — `# <title>`, a blank line, the one-line `rule`, a blank
  line, `Related: <related>`. `/agree` fills the template; it does not invent a new layout.
- Write LF-only, no BOM, `encoding="utf-8"`.
- `retire(slug, *, agreements_dir=AGREEMENTS_DIR) -> Path` (D-09) — locate the target via
  `iter_agreement_files(agreements_dir)` so lookup inherits the shared confinement/exclusion rather
  than re-globbing. Unknown slug → `AgreementRefused`. **Flip `status:` to `retired` in place.
  NEVER delete, never rewrite history.** The retired entry stays as the audit trail, and `inject.py`'s
  L5 filter already excludes it from rendering. Re-serialize the frontmatter through the same YAML
  dumper and preserve the body byte-for-byte, so the diff is limited to the status line. Retiring an
  already-retired entry is a no-op returning the path (idempotent, not an error).
- `main(argv=None) -> int` — `import argparse` **inside** the function. Positional `slug`;
  `--because`, `--title`, `--rule`, `--related`, `--added` (optional, **defaulting to today's ISO
  date in `main()` ONLY** — never inside `add()`, which takes `added: str`; this mirrors
  `assemble()`'s discipline of keeping the clock at the edge and out of the pure core, and keeps
  `add()` deterministically testable). `--retire` as `action="store_true"`.
  `try: ... except AgreementRefused as exc: print(str(exc)); return 3` — refusal to **stdout**, per
  `approve.py:94`. (Deliberate divergence, note it in the docstring: `/agree` follows `approve.py`
  (stdout) because D-07 names it as the precedent; the Plan-02 lint follows `lint.py` (stderr).)
  Success → `print(f"AGREED: {path}.")`, return 0. Close with
  `if __name__ == "__main__": raise SystemExit(main())`.

**D-08: do NOT require `GOLDEN_APPROVE_HUMAN`.** Agreements are deliberately not constitution plane
(committed-but-writable, per Q1). Requiring the human token would reintroduce the capture friction §2
rejected — "the trigger is a user typing feedback mid-work" — and would mislabel a working-style note
as constitution ratification. `--because` is the gate; the user directing the write **is** the
ratification. Never read or reference that env var here.

**Never write to the real `.memory/agreements/`** while building or exercising this. The active set
is legitimately empty and that is correct behavior (D-13). Do not seed an agreement (D-12).
  </action>

  <verify>
    <automated>uv run pytest tools/agree tools/harness_lint tools/memory_regen -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv sync` → succeeds; `git diff --stat uv.lock` → **≤ 6 lines changed**, and `git diff uv.lock | grep -c "^+name = " ` → 1 (only `logparser-agree`). Zero new external packages.
    - `grep -c "package = false" tools/agree/pyproject.toml` → 1; `grep -c "^dependencies = \[\]" tools/agree/pyproject.toml` → 1.
    - `git diff --stat pyproject.toml` → **empty** (the `tools/*` glob auto-enrolls; no root edit).
    - `uv run python -m tools.agree.write --help` → exits 0 and lists `--because`, `--retire`.
    - `uv run python -m tools.agree.write some-slug` (no `--because`) → **exit 3**, stdout starts with `REFUSED:` and names `--because`.
    - `grep -Ec "GOLDEN_APPROVE_HUMAN" tools/agree/write.py` → **0** (D-08).
    - `grep -Ec "^import yaml|^from yaml|pyyaml" tools/agree/write.py` → **0**; `grep -c "ruamel" tools/agree/write.py` → ≥ 1 (D-15 via the repo's only YAML parser).
    - `grep -c 'f"added because' tools/agree/write.py` → **0** — the provenance value must not be f-stringed into raw text; it must reach a YAML dumper as data (D-15). (An f-string building the *dict value* is fine; an f-string building the *frontmatter line* is not.)
    - `grep -Ec "unlink|rmtree|os\.remove" tools/agree/write.py` → **0** (D-09: never delete).
    - `grep -c "class AgreementRefused" tools/agree/write.py` → 1; `grep -c "return 3" tools/agree/write.py` → ≥ 1.
    - `grep -rn "date.today\|datetime" tools/agree/write.py` → present **only** inside `main()`, never inside `add()`: `python3 -c "import pathlib; t=pathlib.Path('tools/agree/write.py').read_text(); a=t.index('def add('); m=t.index('def main('); body=t[a:t.index('def ', a+5)]; assert 'today' not in body and 'datetime' not in body, 'clock leaked into the pure add()'"` → exits 0.
    - `git status --porcelain .memory/agreements` → **empty** (no real-dir writes, no seed).
    - `ruff check tools/agree && ruff format --check tools/agree` → clean.
  </acceptance_criteria>

  <done>A zero-dep `tools/agree/` member exists with a writer that refuses without `--because` (exit 3), YAML-serializes the provenance value, retires in place without deleting, and keeps the clock out of `add()`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove refusal, YAML-injection resistance, and the lint round-trip</name>

  <files>tools/agree/tests/test_agree_refusal.py</files>

  <read_first>
    - `tools/agree/write.py` — the module under test (Task 1).
    - `tools/harness_lint/provenance.py` — the lint the round-trip asserts against (Plan 02).
    - `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-VALIDATION.md` — the Per-Task Verification Map, **rows 16-20 are this task's spec**.
    - `tools/golden_runner/tests/test_approve_gate.py` — the in-repo precedent for testing a refusal gate (the REFUSAL path is the automated surface; the affirmative path is the human action).
    - `tools/harness_lint/frontmatter.py` — `parse_frontmatter`, used to re-parse written files and prove the round-trip.
  </read_first>

  <behavior>
    - Row 16 — `--because ""` / `"   "` / omitted → **exit 3**, and no file created.
    - **Row 17 — D-15:** `--because` containing `"` and `\n` cannot forge a sibling frontmatter key. Re-parsing yields `provenance == "added because " + because` exactly, and the frontmatter key set is exactly `{status, added, provenance}`.
    - Row 18 — `--because "<x>"` → a file whose stamp **passes the provenance lint** (`lint_file(path) == []`).
    - Row 19 — `--retire <slug>` flips status in place; **the file still exists**; the diff is limited to the status line (body byte-identical).
    - Row 20 — `--retire` unknown slug → refuse, **no file created**.
    - Slug traversal (`../../../contracts/x`) → refuse; nothing written outside the dir.
  </behavior>

  <action>
Create `tools/agree/tests/test_agree_refusal.py` with `from __future__ import annotations`, one test
per behavior row, all using `tmp_path` and `agreements_dir=`. **Never write to the real
`.memory/agreements/`.**

**Row 17 is this task's highest-value test.** Pass a `because` value containing both a double-quote
and a newline — specifically one crafted to *attempt* the forge, e.g. a value whose tail reads
`"\nstatus: active\n` — into `add(...)` on a retired-intent entry, then re-parse the written file
with `parse_frontmatter` and assert **(a)** `frontmatter["provenance"] == "added because " + because`
byte-for-byte, and **(b)** `set(frontmatter) == {"status", "added", "provenance"}` — no forged
sibling key — and **(c)** `frontmatter["status"]` is the value the writer intended, not the injected
one. Assertion (b) is what actually catches the attack; (a) alone would pass a naively-escaped
implementation that mangles the value.

**Row 18 is the integration seam that makes the phase cohere:** import `lint_file` from
`tools.harness_lint.provenance` and assert `lint_file(written_path) == []`. This is what proves
`/agree` cannot write a file its own phase's lint rejects — the exact failure D-02's template fix
exists to prevent (an unquoted date would make `/agree` produce files the lint rejects). Include a
value with a `"` in it here too, so the round-trip is proven against the lint, not just the parser.

**Row 19** must assert the file **still exists** after retire (D-09: never delete) and that the body
is byte-identical before/after — capture the body from `parse_frontmatter` pre- and post-retire and
compare. Also assert `status == "retired"` and that `lint_file` still returns `[]` (a retired entry
must remain well-formed — and per D-14 it is still linted).

For row 16, exercise all three spellings (omitted, `""`, `"   "`) — the whitespace-only case is the
one approve.py's older bare-falsy spelling would let through, so it must be explicit. Assert via
`main([...])` returning 3 **and** that `list(tmp_dir.glob("*.md"))` is empty: a refusal that still
wrote a file is not a refusal.

Note `tools/agree/tests/` has its own conftest and does **not** see `tmp_agreements_tree` (pytest
conftest scope is directory-based) — build the small fixtures this file needs locally.
  </action>

  <verify>
    <automated>uv run pytest tools/agree/tests/test_agree_refusal.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/agree/tests/test_agree_refusal.py -x -q` → all pass, **≥ 8 tests collected** (rows 16-20 + the three `--because` spellings + traversal).
    - `grep -n "from tools.harness_lint.provenance import" tools/agree/tests/test_agree_refusal.py` → matches (row 18 round-trip is real, not simulated).
    - `grep -n "status: active" tools/agree/tests/test_agree_refusal.py` → matches inside a `because` payload (row 17 attempts the actual forge).
    - `grep -En 'set\(frontmatter\)|set\(fm\)|sorted\(fm' tools/agree/tests/test_agree_refusal.py` → matches (row 17 asserts the key set, not just the value).
    - `grep -n "exists()" tools/agree/tests/test_agree_refusal.py` → matches (row 19: retire never deletes).
    - `grep -rn "\.memory/agreements" tools/agree/tests/test_agree_refusal.py` → **no match**.
    - **Mutation drill (run, then revert):** in `write.py`, replace the YAML dump of the provenance value with a plain f-string into the frontmatter text → `uv run pytest tools/agree/tests/test_agree_refusal.py -q` must **FAIL** on row 17 → revert → green. This proves the D-15 test actually bites rather than passing vacuously.
    - `uv run pytest tools/agree tools/harness_lint tools/memory_regen -q` → **0 failed**.
  </acceptance_criteria>

  <done>Rows 16-20 pass; a `--because` carrying a quote and a newline provably cannot forge a frontmatter key; a file `/agree` writes provably passes the Plan-02 lint; retire provably never deletes.</done>
</task>

<task type="auto">
  <name>Task 3: Author the source-side /agree command (no emit)</name>

  <files>harness/commands/agree.md</files>

  <read_first>
    - `harness/commands/golden-approve.md` — in full. The authoring precedent: `description: >-` folded block starting with "Use when…", an `agent:` slug, "Thin macro over the **already-coded** refusal gate. Do NOT re-implement approval logic", `$ARGUMENTS` passed positionally, and a "The human gate (refusal is the default)" section enumerating each required signal plus a worked example.
    - `harness/commands/adr.md` — the second command-authoring precedent.
    - `tools/harness_lint/tests/test_commands.py` — in full. It is **glob-driven** (`_command_files()` at 47-48), so `agree.md` is auto-covered with **zero test edits**. Note `_ROUTING_TRIGGERS = ("use", "when")` at :34, the `_AGENT_SLUG` shape at :37, and `EXPECTED_GOLDEN_ADJACENT` at :42-45 — **which must NOT gain "agree"**.
    - `tools/agree/write.py` — the module the macro invokes; confirm its actual argument names before writing the macro.
    - `.memory/agreements/README.md` — line 13's no-secrets rule, to restate in the command doc.
  </read_first>

  <action>
Create `harness/commands/agree.md` cloning `golden-approve.md`'s structure.

**Frontmatter:** a `description: >-` folded block **starting with "Use when…"** — `test_commands.py`
enforces `_ROUTING_TRIGGERS = ("use", "when")` on every command description, so a description that
reads as a bare label fails the glob lint. The trigger must describe the real routing condition: the
user has just given explicit feedback about working style or methodology and wants it captured as a
durable agreement (or wants one retired). Add an `agent:` field whose value is a bare
lowercase-alnum-plus-hyphens slug matching an existing persona under `harness/agents/` (`_AGENT_SLUG`
is structural, but referential integrity is checked by `test_agent_referential_integrity.py` — pick a
real one).

**Body:** state up front that this is a **thin macro over the already-coded refusal gate** — do NOT
re-implement the refusal logic in the command; `tools.agree.write` already encodes it (mirroring
`golden-approve.md:11-12`). Invoke it with `$ARGUMENTS` passed **positionally**:
`!`uv run python -m tools.agree.write $ARGUMENTS``. **No shell-string construction from arguments** —
that is the injection-safe form `golden-approve.md:16-19` establishes, and it matters doubly here
because `--because` carries arbitrary user text.

Include a "The gate (refusal is the default)" section stating:
- `--because "<verbatim user feedback>"` is **required** to add; missing/empty/whitespace-only →
  **exit 3** (D-07). The value becomes the provenance stamp.
- `/agree` deliberately does **NOT** require `GOLDEN_APPROVE_HUMAN` (D-08) — agreements are not
  constitution plane; the user directing the write **is** the ratification. Say this explicitly so a
  future reader does not "harden" it by adding the token and reintroducing the friction §2 rejected.
- `--retire <slug>` flips `status:` to `retired` **in place; it never deletes** (D-09). The retired
  entry remains the audit trail.
- The **no-secrets** rule (`.memory/agreements/README.md:13`): agreements are committed and injected
  verbatim into every session, so `--because` must never carry secrets, tokens, credentials, or PII.
- An agreement is working-style/methodology **only**; a project or architecture decision belongs in
  `docs/adr/` — link with `Related:`, never restate (§7c).
- A worked example, human-run, mirroring `golden-approve.md:34-38`.

**Be honest about what this buys (D-03).** Do **not** write that `/agree` prevents agents from
inventing entries. It makes the stamp impossible to **forget**, not impossible to **forge**; a
well-formed but fabricated provenance passes the lint. What it buys is that inventing an entry now
requires deliberately fabricating a quote attributed to the user — visible and auditable in the git
diff rather than a silent accident.

**D-11: do NOT create an `EXPECTED_COMMANDS` frozenset.** ROADMAP SC3 and MEM2-04 both name a
constant that **does not exist in this codebase**. The real touchpoint is `test_commands.py`'s glob,
which auto-covers `agree.md` with zero edits. Inventing a hand-maintained list purely to match a
mis-worded criterion would add exactly the thing the glob design deliberately avoids. Also do **not**
add `"agree"` to `EXPECTED_GOLDEN_ADJACENT` (`test_commands.py:42-45`) — that is a different,
prior-phase concept (the eight golden-adjacent commands), and `/agree` is not one of them.

**D-10: SOURCE-ONLY. Do NOT run the emitter and do NOT touch `.opencode/` or `.claude/`.** This is
load-bearing, not stylistic: `tools/harness_emit/tests/test_coexist.py:53-54` asserts **exactly 19**
emitted commands in each runtime tree. Authoring the source file leaves that green; emitting `/agree`
makes it 20 and turns a **currently-passing** test red, pushing `harness_emit` past its sanctioned
"no worse than 1 failed" baseline. Phase 15 owns the round-trip and owes the 19→20 bump. Precedent:
Phase 10 (10-01/10-02 source-only; 10-03 emit).
  </action>

  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_commands.py -q` → **all pass with `agree` appearing in the parametrized test ids** (proves the glob auto-covered it with zero test edits — D-11): `uv run pytest tools/harness_lint/tests/test_commands.py -q --collect-only 2>&1 | grep -c "agree"` → ≥ 1.
    - `git diff --stat tools/harness_lint/tests/test_commands.py` → **empty** (D-11: zero test edits required).
    - `grep -c "EXPECTED_COMMANDS" tools/harness_lint/tests/test_commands.py harness/commands/agree.md` → **0** in both (D-11: the constant is not invented).
    - `grep -c '"agree"' tools/harness_lint/tests/test_commands.py` → **0** (`EXPECTED_GOLDEN_ADJACENT` untouched).
    - `grep -c '\$ARGUMENTS' harness/commands/agree.md` → ≥ 1; `grep -n "tools.agree" harness/commands/agree.md` → matches.
    - `grep -Ei "use when" harness/commands/agree.md` → matches in the description (`_ROUTING_TRIGGERS`).
    - `grep -Ec "GOLDEN_APPROVE_HUMAN" harness/commands/agree.md` → ≥ 1 **in a sentence stating it is NOT required** (D-08 recorded, not silently omitted).
    - `grep -Ein "secret|token|credential" harness/commands/agree.md` → matches (the no-secrets rule restated).
    - **`ls harness/commands/*.md | wc -l` → 20** (source side gains the file) **AND** `ls .opencode/command/*.md 2>/dev/null | wc -l` unchanged **AND** `git status --porcelain .opencode .claude` → **empty** (D-10: source-only; the emitted trees stay at 19).
    - `uv run pytest tools/harness_emit -q` → **exactly 1 failed / 46 passed** — the `test_coexist.py` 19-count test must still **PASS**. If it fails, something was emitted; revert it.
    - `git status --porcelain tools/harness_emit/tests/__snapshots__` → **empty**. Never run `--snapshot-update`.
  </acceptance_criteria>

  <done>`harness/commands/agree.md` exists, is auto-covered by the glob lint with zero test edits, documents the refusal gate honestly (shape not truth; no human token), and nothing is emitted — the runtime trees stay at 19 commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user feedback (arbitrary text) → `--because` → YAML frontmatter → committed file | **The new injection surface this plan introduces.** Untrusted text crosses into a structured document that is later parsed and rendered as a priority-0 directive. |
| `/agree` CLI args → `<slug>` → filesystem write path | A slug is a path component; traversal writes outside the tier. |
| agent → `.memory/agreements/` | Not path-denied by design (D-08). The `--because` gate, not a write-lock, is the control. |
| `harness/commands/agree.md` → (Phase 15) emitted runtimes | Deliberately NOT crossed this phase (D-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-14-09 (D-15)** | **Tampering** | `--because` → YAML frontmatter | **mitigate** | **New this phase, covered by no prior decision.** A `"` or newline closes the quoted scalar and forges sibling frontmatter keys (e.g. inject `status: active` into a retired entry, resurrecting it into the priority-0 render set). Mitigation: build the frontmatter as a **dict** and dump it through `ruamel.yaml` `YAML(typ="safe")` — **never f-string the value into the template**. Asserted by validation row 17 (key-set assertion + the mutation drill). |
| T-13-01 (inherited) | Elevation of Privilege | agent self-invents an agreement → priority-0 self-instruction | **mitigate (PARTIAL BY DESIGN — D-03)** | `--because` is mandatory, so the stamp cannot be **forgotten**. It can still be **forged** — accepted residual: forging requires deliberately fabricating a quote attributed to the user, visible in the git diff. `/agree` is the *sanctioned* path, not a *sandbox*. Same trust model ADR-0007 accepted for `HARNESS_DEV_BYPASS`. **No PreToolUse hook (D-03 rejects it).** |
| T-14-10 | Tampering | path traversal via `<slug>` (`../../../contracts/x`) | mitigate | Slug regex (bare lowercase-alnum + hyphens; reject `/`, `\`, `..`, whitespace, leading `_`) + resolve-and-confine the target inside `agreements_dir` (D-06 applied to the write path). Asserted by a traversal test. |
| T-14-11 | Tampering | agreement silently overwritten by a re-`add` of the same slug | mitigate | `add()` refuses when the target exists; retire is the only in-place mutation, and it never deletes (D-09). |
| T-14-12 | Repudiation | retire implemented as delete → audit trail destroyed | mitigate | D-09: flip `status:` in place; **never delete**. Enforced by `grep -Ec "unlink\|rmtree\|os\.remove"` → 0 and by the row-19 file-still-exists assertion. |
| T-14-13 | Elevation of Privilege | `/agree` mislabeled as a constitution write by requiring `GOLDEN_APPROVE_HUMAN` | **accept / avoid** | D-08: the token is deliberately NOT required. Requiring it would mislabel a working-style note as constitution ratification and reintroduce the friction §2 rejected. Enforced by `grep` → 0 in `write.py`. |
| T-14-07 (inherited) | Information Disclosure | credentials pasted into `--because` → committed + injected every session | **accept + document** | Out of scope for D-01's regex. The command doc restates `.memory/agreements/README.md:13`'s no-secrets rule. `secret_scan` PreToolUse catches shape-matching values on the Write path. |
| **T-14-14 (D-20)** | — | `secret_scan` denying this plan's own writes | mitigate | `secret_scan.py:47` matches `token = <16+ chars>` and **denied the researcher's first write** for quoting `approve.py:57` verbatim. This plan describes that line rather than quoting it. **Reword; never bypass, never weaken `secret_scan` to accommodate a doc.** (It is itself a live demonstration of D-03's shape-not-truth model.) |
| T-14-08 | Tampering | emitting, or `--snapshot-update` on `harness_emit` | mitigate | D-10: source-only. Task 3's acceptance criteria assert `.opencode`/`.claude` are untouched, the emitted count stays 19, and `test_coexist.py` still passes. |
| T-14-SC | Tampering | uv/pip installs | **n/a — no packages** | `tools/agree/` is a **zero-dep virtual member**: `dependencies = []`, auto-enrolled by the `tools/*` glob, `uv.lock` gains exactly 4 deterministic lines with **zero resolution**. No `## Package Legitimacy Audit` is required because no package is installed. If `uv sync` resolves any new external package, that is a scope violation — stop. |
</threat_model>

<verification>
- `uv run pytest tools/agree tools/harness_lint tools/memory_regen -q` → **0 failed**.
- `uv run pytest -q` → **exactly 1 failed / 620 passed** (the `harness_emit` snapshot; Phase 15's debt). **`0 failed` is a FAILURE CONDITION.**
- `uv run pytest tools/harness_emit -q` → **exactly 1 failed / 46 passed**; `test_coexist.py`'s 19-count test **passes**.
- `ls harness/commands/*.md | wc -l` → **20** (source); `git status --porcelain .opencode .claude tools/harness_emit/tests/__snapshots__` → **empty** (D-10).
- `git status --porcelain .memory/agreements` → **empty** — no seed, no agent-authored entry (D-12/D-13).
- `git diff --stat pyproject.toml tools/harness_lint/tests/test_commands.py` → **empty**.
- `ruff check . && ruff format --check .` → clean.
- **D-11 note for `/verify-work`:** ROADMAP SC3 ("`/agree` is added to `EXPECTED_COMMANDS`") names a constant that **does not exist**. SC3's substance is satisfied by authoring `harness/commands/agree.md` and letting the glob-driven `test_commands.py` cover it. Do not fail the phase on SC3's literal wording, and do not invent the constant to satisfy it.
</verification>

<success_criteria>
- `/agree` refuses without `--because` (exit 3), including the whitespace-only spelling, and writes nothing on refusal.
- A `--because` carrying `"` and `\n` cannot forge a frontmatter key (D-15), proven by a key-set assertion and a mutation drill.
- A file `/agree` writes passes the Plan-02 provenance lint (round-trip).
- `--retire` flips status in place; the file still exists; the body is byte-identical.
- `tools/agree/` is a zero-dep member; `uv.lock` gained ≤ 6 lines and zero external packages; the root `pyproject.toml` is untouched.
- `harness/commands/agree.md` is auto-covered by the glob lint with zero test edits; `EXPECTED_COMMANDS` was not invented.
- Nothing emitted: runtime trees untouched, `harness_emit` still exactly 1 failed / 46 passed.
</success_criteria>

<output>
Create `.planning/phases/14-write-path-anti-churn-guard-v2-1-c/14-03-SUMMARY.md` when done.
</output>
