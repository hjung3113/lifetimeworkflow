---
phase: 48-convention-profiles
plan: 03
type: execute
wave: 3
depends_on: ["48-01", "48-02"]
files_modified:
  - harness/commands/component.md
  - .opencode/command/component.md
  - .claude/commands/component.md
  - tools/harness_lint/tests/test_commands.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
autonomous: true
requirements: [MONO-07]

must_haves:
  truths:
    - "Running /component produces a convention-profile regeneration + resolution assertion for the new package as part of step 2, inside the existing structure -> AGENTS.md -> tests order (no step 4 added)"
    - "The command count is unchanged at 18, proven by a durable regression test rather than a one-time manual measurement"
    - "No gate or CI job was added anywhere across Phase 48; ci.yml is byte-unchanged from the phase's base commit"
  artifacts:
    - path: harness/commands/component.md
      provides: "step 2 extended with the profile-regeneration + conventions_for resolution directive"
    - path: tools/harness_lint/tests/test_commands.py
      provides: "test_command_count_is_stable — pins the 18-command count"
    - path: .opencode/command/component.md
      provides: "re-emitted projection of the edited source"
    - path: .claude/commands/component.md
      provides: "re-emitted projection of the edited source"
  key_links:
    - from: "harness/commands/component.md"
      to: ".opencode/command/component.md + .claude/commands/component.md"
      via: "tools.harness_emit re-emit (never hand-edited)"
      pattern: "uv run python -m tools\\.harness_emit"
---

<objective>
Extend `/component` step 2 to populate a convention profile for the newly scaffolded package (via
Plan 01's `conventions_for()` + Plan 02's regenerated artifact) inside the EXISTING mandated
order, add zero new command, pin the live command count at 18 with a durable test, and re-emit
both runtime trees byte-clean.

Purpose: this closes MONO-07 and the phase's SC3/SC4 no-growth proof — the last of Phase 48's
three requirements.
Output: `harness/commands/component.md` edited in place (still exactly 3 numbered steps), both
runtime projections re-emitted, `test_command_count_is_stable` added, full suite green, `ci.yml`
byte-unchanged for the whole phase.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/48-convention-profiles/48-CONTEXT.md
@.planning/phases/48-convention-profiles/48-RESEARCH.md
@.planning/phases/48-convention-profiles/48-PATTERNS.md
@.planning/phases/47-package-facts/47-05-SUMMARY.md

<interfaces>
<!-- From Wave 1/2 (48-01, 48-02), now available: -->
```python
from tools.harness_config import conventions_for   # 48-01
# uv run python -m tools.memory_regen.package_facts  # 48-02 — regenerates .memory/derived/package-facts.md
```

Current `harness/commands/component.md` (35 lines, full file already read this session):
```markdown
1. **Structure first** — create the package tree under `components/<name>/` (or `libs/<name>/`)
   ...
2. **Self-sufficient per-package AGENTS.md** — write `components/<name>/AGENTS.md` that
   **RESTATES the non-negotiables verbatim** (...). Codex replaces nested AGENTS.md rather
   than concatenating, so the file must be self-sufficient — never inherit-only (P11).
3. **Test harness** — add the test scaffold (...) so the member is verifiable from creation.

## Guard
- The order is enforced: structure → self-sufficient AGENTS.md → tests. A member without its
  per-package rules or without a test harness is incomplete.
- Stage new files individually when committing; do not blanket-add the working tree.
```

`tools/harness_lint/tests/test_commands.py`'s existing helper (do not write a second glob):
```python
def _command_files() -> list[Path]:
    return sorted(_COMMANDS_DIR.glob("*.md"))
```
Live count today: 18 (`add-language, adopt, adr, agree, build, checkpoint, component,
contract-check, docs-sync, fan-out-synthesize, flow, lint, new-contract-rule, orient,
refresh-memory, review, test, verify-work`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend component.md step 2 + add the command-count regression test</name>
  <files>harness/commands/component.md, tools/harness_lint/tests/test_commands.py</files>
  <read_first>
    harness/commands/component.md (full — already read this session)
    tools/harness_lint/tests/test_commands.py (full — already read this session; note
    `_command_files()`, `EXPECTED_GOLDEN_ADJACENT`, and the module docstring's own note that
    `golden`/`golden-approve` were retired in Phase 44)
    .planning/phases/48-convention-profiles/48-RESEARCH.md (Q5 — live count is 18, no existing
    count assertion; the `component.md` step 2 insertion point at `component.md:23-26`)
    .planning/phases/48-convention-profiles/48-PATTERNS.md (`harness/commands/component.md — step
    2 body edit` and `tools/harness_lint/tests/test_commands.py — add test_command_count_is_stable`
    sections — copy the shown text/pattern)
  </read_first>
  <behavior>
    - `component.md` still has exactly 3 numbered top-level steps after the edit (no step 4).
    - Step 2's body, after the existing AGENTS.md-writing sentence, additionally directs:
      regenerate `.memory/derived/package-facts.md` and assert the new package resolves via
      `conventions_for()` (not falling through to the repo-wide default).
    - `test_command_count_is_stable()` asserts `len(_command_files()) == 18` and fails if a
      command file is added or removed without deliberately updating the constant.
  </behavior>
  <action>
    Edit `harness/commands/component.md`'s step 2 bullet (do NOT touch step 1 or step 3's text,
    do NOT add a step 4, do NOT renumber): append one sentence after "...never inherit-only
    (P11)." directing the reader to regenerate the derived package-facts artifact (`uv run python
    -m tools.memory_regen.package_facts`) and assert the newly scaffolded package now resolves —
    e.g. `python -c "from tools.harness_config import conventions_for; print(conventions_for('
    components/<name>/...'))"` should report the new package's own id, not the repo-wide default
    — as part of THIS step, not a new numbered step. Update the "## Guard" section's first bullet
    (the enforced-order sentence) to also state that the profile-regeneration + resolution check
    belongs inside step 2. In `tools/harness_lint/tests/test_commands.py`, add
    `test_command_count_is_stable()` near `test_golden_adjacent_commands_present` (same
    module-level "assert on the whole `_command_files()` set" grouping): `assert
    len(_command_files()) == 18`, with a docstring stating this pins the v2.6 no-growth
    constraint and that a legitimate new command means deliberately bumping the constant, never
    letting the assertion silently rot.
  </action>
  <acceptance_criteria>
    - `ls harness/commands/*.md | wc -l` → `18`, recorded as measured BEFORE this task starts and
      AGAIN after this task completes (both must equal 18 — Criterion 4's "18 → 18" proof)
    - `grep -c "^[0-9]\." harness/commands/component.md` returns `3` (still exactly 3
      top-level numbered steps)
    - `grep -ci "step 4" harness/commands/component.md` returns `0`
    - `uv run pytest tools/harness_lint/tests/test_commands.py -x -q` → all pass, including the
      new `test_command_count_is_stable`
    - Mutation check (run and record): temporarily create a throwaway 19th file
      `harness/commands/_zzz-throwaway.md` (any content) and confirm
      `uv run pytest tools/harness_lint/tests/test_commands.py -k test_command_count_is_stable -x`
      FAILS with `assert 19 == 18` (or equivalent); delete the throwaway file; re-confirm green.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py -x -q</automated>
  </verify>
  <done>`component.md` step 2 documents the profile-regeneration + resolution assertion inside the
  existing mandated order (still 3 steps, no step 4); command count pinned at 18 by a durable,
  falsifiable test.</done>
</task>

<task type="auto">
  <name>Task 2: Re-emit both runtimes, verify idempotency, and confirm zero ci.yml growth</name>
  <files>.opencode/command/component.md, .claude/commands/component.md, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>
  <read_first>
    .planning/phases/47-package-facts/47-05-SUMMARY.md (the exact re-emit + idempotency + stale-snapshot-update precedent this task mirrors)
    .planning/phases/48-convention-profiles/48-RESEARCH.md (Pitfall 4 — editing component.md without re-emitting)
    tools/harness_emit/tests/test_emit_determinism.py (partial — the sha256-per-file comparison idiom, if the committed snapshot needs updating)
  </read_first>
  <behavior>
    - After Task 1's source edit, `.opencode/command/component.md` and
      `.claude/commands/component.md` reflect the new step-2 text byte-for-byte via re-emit — never
      hand-edited.
    - A second immediate re-emit produces zero further diff (idempotent).
    - The full repository test suite passes; `.github/workflows/ci.yml` is byte-unchanged from
      this phase's base commit (job set AND `gate.needs` both untouched).
  </behavior>
  <action>
    Run `uv run python -m tools.harness_emit` to project the edited `harness/commands/component.md`
    into both runtime trees; stage `.opencode/command/component.md` and
    `.claude/commands/component.md` alongside the source edit. Run the emitter a second time
    immediately and confirm `git status --short .opencode .claude` is empty (idempotency, mirroring
    the Phase-47 precedent in `47-05-SUMMARY.md`). If `tools/harness_emit/tests/__snapshots__/
    test_emit_determinism.ambr` goes stale because it embeds `component.md`'s projected text,
    regenerate it via `uv run pytest tools/harness_emit/tests/test_emit_determinism.py
    --snapshot-update` and commit the updated `.ambr` in this task (mirrors 47-05's identical
    handling for `curator.md`/`refresh-memory.md`). Finally run the full suite
    (`uv run pytest`) once, and confirm `git diff --exit-code -- .github/workflows/ci.yml` is
    empty — the hard constraint binding this entire phase.
  </action>
  <acceptance_criteria>
    - `uv run python -m tools.harness_emit && git status --short .opencode .claude` shows exactly
      the `component.md` projections changed (and nothing else)
    - A second immediate `uv run python -m tools.harness_emit` run produces empty
      `git status --short .opencode .claude` output
    - `uv run pytest -x -q` → full suite green, 0 failed
    - `git diff --exit-code -- .github/workflows/ci.yml` → exit code 0, empty diff
    - `grep -n "needs:" .github/workflows/ci.yml` (the `gate.needs` line) is byte-identical to
      this phase's base-commit version (compare against `git show <phase-48-base-sha>:.github/workflows/ci.yml`)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -x -q</automated>
  </verify>
  <done>Both runtime trees re-emitted byte-clean from the edited `component.md`; re-emit is
  idempotent; full suite green; `ci.yml` byte-unchanged for the entire phase — the phase's
  no-growth constraint holds end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `harness/commands/component.md` source → emitter → `.opencode/`/`.claude/` projections | Existing, unchanged trust boundary (Phase 7's emitter); this plan is a content edit through an already-hardened path, not a new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04 | Tampering | `.opencode/command/component.md`, `.claude/commands/component.md` | accept | Both are machine-projected, never hand-edited; the `emit-drift` CI job (unchanged by this phase) already fails loud on any hand-edit or stale projection. No new mitigation required — this plan relies on the existing gate, not a new one (v2.6 no-growth constraint forbids adding one). |
| T-48-05 | (package legitimacy) | N/A | accept | No external package installed by this plan. |
</threat_model>

<verification>
`uv run pytest -x -q` — full repository suite green. `git diff --exit-code -- .github/workflows/ci.yml` — empty. `ls harness/commands/*.md | wc -l` — `18`.
</verification>

<success_criteria>
- `/component` step 2 populates the convention-profile assertion inside the existing mandated
  order; no step 4 added.
- Command count is pinned at 18 by a durable, falsifiable test (proven to fail under mutation).
- Both runtime trees re-emitted byte-clean and idempotently.
- `ci.yml`'s job set and `gate.needs` are byte-unchanged from the phase's base commit — the
  binding no-growth constraint holds for the whole of Phase 48.
</success_criteria>

<output>
Create `.planning/phases/48-convention-profiles/48-03-SUMMARY.md` when done
</output>
