---
phase: 48-convention-profiles
plan: 03
subsystem: harness-commands
tags: [opencode, claude-code, harness-emit, convention-profiles, no-growth-gate]

# Dependency graph
requires:
  - phase: 48-convention-profiles (48-01)
    provides: "tools.harness_config.conventions_for() — the join used by the new step-2 assertion"
  - phase: 48-convention-profiles (48-02)
    provides: "package_facts.render() Convention Profiles section — the artifact regenerated by step 2"
provides:
  - "/component step 2 extended with a profile-regeneration + conventions_for() resolution directive, inside the existing mandated order (no step 4)"
  - "test_command_count_is_stable() — durable regression pinning the live command count at 18"
  - "Both runtime projections (.opencode/command/component.md, .claude/commands/component.md) re-emitted byte-clean, idempotently"
affects: [49-*, any future phase adding a new harness/commands/*.md file]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "In-step directive extension (append to an existing numbered step's bullet) instead of adding a new step, to hold a command's step-count invariant"
    - "Command-count regression test co-located with test_golden_adjacent_commands_present, using the file's existing _command_files() glob helper — no second glob"

key-files:
  created: []
  modified:
    - harness/commands/component.md
    - tools/harness_lint/tests/test_commands.py
    - .opencode/command/component.md
    - .claude/commands/component.md
    - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr

key-decisions:
  - "Appended the profile-regeneration + resolution assertion to step 2's existing bullet (and the Guard section) rather than adding a step 4, per CONTEXT.md's explicit no-growth constraint"
  - "Mutation-proved the new test by adding then removing a throwaway 19th command file and confirming the assertion fails as 19 == 18 before removal"

patterns-established:
  - "Command-set invariants (count, golden-adjacency) get durable pytest assertions co-located in test_commands.py rather than one-time manual measurements"

requirements-completed: [MONO-07]

# Metrics
duration: 15min
completed: 2026-07-30
---

# Phase 48 Plan 03: Component Command Convention-Profile Wiring Summary

**Extended `/component` step 2 to regenerate package-facts and assert `conventions_for()` resolution for newly scaffolded packages, with zero new command and a mutation-proven 18-command regression test.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-07-30T09:44:00+09:00
- **Completed:** 2026-07-30T09:45:31+09:00
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- `harness/commands/component.md` step 2 now directs regenerating `.memory/derived/package-facts.md` and asserting the newly scaffolded package resolves its own convention profile via `conventions_for()` — still exactly 3 numbered steps, no step 4.
- `test_command_count_is_stable()` added to `tools/harness_lint/tests/test_commands.py`, pinning the live command count at 18; proven falsifiable by a real mutation test (throwaway 19th file → `assert 19 == 18` failure → removed → green again).
- Both runtime projections (`.opencode/command/component.md`, `.claude/commands/component.md`) re-emitted from the edited source; a second immediate re-emit produced zero further diff (idempotent).
- Full repository suite (923 tests) green; `.github/workflows/ci.yml` confirmed byte-unchanged from the phase's base commit (92cc635), including the `gate.needs` line — the phase's no-growth constraint holds end-to-end.

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend component.md step 2 + add the command-count regression test** - `f9e9d31` (feat)
2. **Task 2: Re-emit both runtimes, verify idempotency, and confirm zero ci.yml growth** - `2d7c8fe` (docs)

**Plan metadata:** (this commit, following)

## Files Created/Modified
- `harness/commands/component.md` - step 2 bullet extended with profile-regen + `conventions_for()` resolution directive; Guard section updated to match
- `tools/harness_lint/tests/test_commands.py` - added `test_command_count_is_stable()` pinning count at 18
- `.opencode/command/component.md` - re-emitted projection (never hand-edited)
- `.claude/commands/component.md` - re-emitted projection (never hand-edited)
- `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` - updated to reflect the new projected `component.md` text

## Decisions Made
- Extended step 2's existing bullet (plus the Guard section's enforced-order sentence) rather than adding a step 4, matching CONTEXT.md's binding "18 → 18, no step 4" constraint.
- Left `harness/agents/`, `.github/workflows/ci.yml`, and all contracts/gates untouched — this plan closes MONO-07 through a content edit to an already-hardened emit path, not a new trust boundary.

## Deviations from Plan

None - plan executed exactly as written. The snapshot update in Task 2 (`test_emit_determinism.ambr`) was explicitly anticipated and instructed by the plan's own action text, not an unplanned deviation.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 48 (Convention Profiles) is now fully complete: all three requirements (MONO-05 via 48-01, MONO-06 via 48-02, MONO-07 via 48-03) are closed.
- Command count remains durably pinned at 18; any future phase adding a `harness/commands/*.md` file will get an immediate, self-explaining test failure requiring a deliberate constant bump.
- `ci.yml` is byte-identical across the entire phase — no gate/job growth introduced by Phase 48.

---
*Phase: 48-convention-profiles*
*Completed: 2026-07-30*

## Self-Check: PASSED
