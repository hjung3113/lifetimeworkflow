---
phase: 16-local-memory-web-ui-v2-1-e
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/memory_ui/pyproject.toml
  - tools/memory_ui/__init__.py
  - tools/memory_ui/tests/__init__.py
  - tools/memory_ui/tests/conftest.py
  - tools/memory_ui/tests/test_routes.py
  - tools/memory_ui/tests/test_referential_integrity.py
  - tools/memory_regen/tests/conftest.py
  - tools/memory_regen/tests/test_pointer_index.py
  - uv.lock
autonomous: true
requirements: [MEM2-07]
must_haves:
  truths:
    - "The new tools/memory_ui workspace member is enrolled and importable under uv"
    - "Route + pointer-index behaviors are pinned by RED tests that inject tmp dirs and never touch real .memory/agreements or open a real socket"
    - "SC1/SC2/SC3 each map to at least one authored test"
  artifacts:
    - path: "tools/memory_ui/pyproject.toml"
      provides: "Zero-dep workspace member (name logparser-memory-ui, package=false)"
      contains: "dependencies = []"
    - path: "tools/memory_ui/tests/conftest.py"
      provides: "sys.path parents[3] wiring + re-export of tmp_agreements_tree"
    - path: "tools/memory_regen/tests/test_pointer_index.py"
      provides: "RED determinism + scan-correctness tests for the pointer-index generator"
    - path: "tools/memory_ui/tests/test_routes.py"
      provides: "RED tests pinning the pure route-function API"
    - path: "tools/memory_ui/tests/test_referential_integrity.py"
      provides: "RED tests for orphan surface-and-confirm"
  key_links:
    - from: "tools/memory_ui/tests/conftest.py"
      to: "tools.harness_lint.tests.conftest.tmp_agreements_tree"
      via: "re-export import"
      pattern: "tmp_agreements_tree"
---

<objective>
Establish Wave-0 test infrastructure for Phase 16: enroll the new `tools/memory_ui/` workspace
member and author the RED tests that pin the pointer-index generator API and the pure route-function
API before any implementation exists. Every test injects tmp directories (`agreements_dir`,
`state_dir`, `derived_dir`, `base_dir`, `scan_roots`) so no test ever writes a real agreement or
opens a real socket.

Purpose: interface-first ordering — the tests here ARE the contract the Wave-1/2/3 implementation
plans build against, so executors receive the API instead of hunting for it.
Output: member scaffold, shared conftest + fixtures, and three RED test files (collection failures /
assertion failures are the expected state until later waves implement the targets).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-CONTEXT.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-RESEARCH.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-PATTERNS.md

<interfaces>
<!-- The route-function API these tests pin. Wave-2 (16-03) implements EXACTLY these signatures. -->
<!-- All return (status:int, headers:dict, body:bytes); all take injected plane dirs. -->

routes.list_items(*, state_dir, agreements_dir, show_retired=False) -> (status, headers, body)
routes.view_item(item_id, *, state_dir, agreements_dir) -> (status, headers, body)
routes.add_agreement(slug, title, rule, *, because, related=None, agreements_dir, derived_dir) -> (status, headers, body)
routes.retire_agreement(slug, *, agreements_dir, derived_dir, confirm=False) -> (status, headers, body)
routes.save_progress(item_id, body_text, *, state_dir) -> (status, headers, body)
routes.pointer_lookup(item, *, base_dir, scan_roots) -> list[dict]   # regenerates inline then reads

<!-- The pointer-index generator API these tests pin (Wave-1 / 16-02 implements). -->
pointer_index.build_index(*, base_dir=None, scan_roots=None) -> dict   # {item: [{"file","line","kind"}]}
pointer_index.render_md(index) -> str
pointer_index.write(json_path=JSON_PATH, md_path=MD_PATH, *, base_dir=None, scan_roots=None) -> (Path, Path)
pointer_index.DERIVED_HEADER == "DERIVED — do not hand-edit (tools/memory_regen/pointer_index.py)"

<!-- Sanctioned writers routes delegate to (already exist — do NOT re-roll). -->
tools.agree.write.add(slug, title, rule, *, because, added, related=None, agreements_dir=AGREEMENTS_DIR) -> Path   # raises AgreementRefused on blank because / bad slug
tools.agree.write.retire(slug, *, agreements_dir=AGREEMENTS_DIR) -> Path   # flips status: retired in place, never deletes
tools.harness_lint.agreements.iter_agreement_files(agreements_dir) -> list[Path]
tools.harness_lint.agreements.load_agreement(path) -> tuple[dict, str] | None
tools.harness_lint.parse_frontmatter(md_text) -> tuple[dict, str]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enroll tools/memory_ui member + shared test conftest & fixtures</name>
  <files>tools/memory_ui/pyproject.toml, tools/memory_ui/__init__.py, tools/memory_ui/tests/__init__.py, tools/memory_ui/tests/conftest.py, tools/memory_regen/tests/conftest.py, uv.lock</files>
  <read_first>
    - tools/agree/pyproject.toml (verbatim clone target — rename only)
    - tools/agree/__init__.py (namespace member init)
    - tools/memory_regen/tests/conftest.py (parents[3] sys.path wiring + shared-corpus re-export, 16-PATTERNS.md lines 195-209)
    - tools/harness_lint/tests/conftest.py (the tmp_agreements_tree fixture to re-export; 16-RESEARCH.md Pitfall 2)
  </read_first>
  <action>
    Create `tools/memory_ui/pyproject.toml` as a verbatim clone of `tools/agree/pyproject.toml` with
    name `logparser-memory-ui`, `requires-python = ">=3.11"`, `dependencies = []`, `[tool.uv]
    package = false` (per 16-RESEARCH Installation + 16-PATTERNS pyproject block). Create empty
    `tools/memory_ui/__init__.py` and `tools/memory_ui/tests/__init__.py`.
    Create `tools/memory_ui/tests/conftest.py`: insert `parents[3]` repo root + `libs/python` on
    sys.path (mirror `tools/memory_regen/tests/conftest.py`) and re-export `tmp_agreements_tree` from
    `tools.harness_lint.tests.conftest` (noqa F401) so route tests use the SYNTHETIC corpus, never
    real agreements.
    In `tools/memory_regen/tests/conftest.py`, ADD a `tmp_pointer_scan_tree` fixture (do not remove
    existing fixtures): build a throwaway tree under `tmp_path` containing `.memory/state/activeContext.md`,
    `.memory/state/progress.md`, one active + one retired `.memory/agreements/<slug>.md`, and a
    `docs/` + a single-file scan target that each reference a known `.memory/...` path string AND an
    agreement slug (include a decoy line containing `<slug>ner` to exercise the word-boundary guard).
    Run `uv sync` so the member enrolls; the lock delta must be the expected zero-resolution member
    entry only (14-CONTEXT D-19 precedent, ~4 lines).
  </action>
  <verify>
    <automated>uv sync && uv run python -c "import tomllib,sys; d=tomllib.load(open('tools/memory_ui/pyproject.toml','rb')); assert d['project']['dependencies']==[] and d['project']['name']=='logparser-memory-ui' and d['tool']['uv']['package'] is False"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/memory_ui/pyproject.toml` parses; `dependencies == []`, `package = false`, name `logparser-memory-ui`.
    - `uv sync` succeeds; `git diff uv.lock` shows only the new zero-dep member entry (no unrelated resolution churn).
    - `tools/memory_ui/tests/conftest.py` re-exports `tmp_agreements_tree`; `tools/memory_regen/tests/conftest.py` exposes a `tmp_pointer_scan_tree` fixture that writes ONLY under `tmp_path`.
    - `git status` shows NO new/modified files under `.memory/agreements/`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests for the pointer-index generator (determinism + scan)</name>
  <files>tools/memory_regen/tests/test_pointer_index.py</files>
  <read_first>
    - tools/memory_regen/tests/test_repo_map_determinism.py (the determinism test to clone; 16-PATTERNS.md lines 86-113)
    - tools/memory_regen/repo_map.py (DERIVED_HEADER, write/render shape; lines 33-43)
    - tools/memory_regen/contracts_index.py (tabular render precedent)
  </read_first>
  <behavior>
    - test_render_twice_is_byte_identical: two build_index/render_md over the fixture are byte-identical.
    - test_write_delete_regenerate_is_byte_identical: write → sha256 → unlink → write → same sha256 (NOT git diff — target is gitignored).
    - test_derived_header_and_no_timestamp: `.md` first line starts `# DERIVED — do not hand-edit` and contains `pointer_index.py`; assert no `\d{4}-\d{2}-\d{2}` and no `0\.\d{3,}` anywhere in json+md.
    - test_no_self_reference_and_no_false_positive: no referrer path under `.memory/derived/`; slug `plan` does NOT match token `planner` (word-boundary guard).
    - test_render_matches_committed_snapshot: syrupy snapshot over `tmp_pointer_scan_tree`, keyed to base_dir so tmp paths never leak.
    - test_referrer_shape: index values are lists of `{"file","line","kind"}` with `kind in {"path","slug"}`, sorted deterministically.
  </behavior>
  <action>
    Author `tools/memory_regen/tests/test_pointer_index.py` importing `from tools.memory_regen import
    pointer_index` (module does not exist yet — RED is expected until 16-02). Use the
    `tmp_pointer_scan_tree` fixture; pass `base_dir=`/`scan_roots=` into `write`/`build_index` so
    nothing leaks to the real `.memory/derived/`. Do NOT create the `__snapshots__/*.ambr` here — the
    snapshot is generated when 16-02 first runs green (avoids a stale/empty snapshot). Clone the
    assertions from `test_repo_map_determinism.py` (16-PATTERNS lines 103-108) verbatim, swapping the
    header string and output paths.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_pointer_index.py --collect-only -q 2>&1 | grep -q "test_write_delete_regenerate_is_byte_identical"</automated>
  </verify>
  <acceptance_criteria>
    - All six named tests are collected (they may ERROR/FAIL because `pointer_index` is unimplemented — that is the intended RED state).
    - Every generator call in the file passes `base_dir=`/`scan_roots=`/`json_path=`/`md_path=` pointing under `tmp_path` (grep: no call writes to the real `.memory/derived/`).
    - The false-positive test encodes the `plan`≠`planner` word-boundary case.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED tests for route logic + referential-integrity surface-and-confirm</name>
  <files>tools/memory_ui/tests/test_routes.py, tools/memory_ui/tests/test_referential_integrity.py</files>
  <read_first>
    - tools/agree/write.py (add/retire/_target_for/AgreementRefused signatures; lines 33-99)
    - tools/agree/tests/test_agree_refusal.py (anti-invent + flip-in-place assertions to mirror)
    - tools/memory_regen/inject.py (assemble injected-dir DI idiom; lines 128-137)
    - .planning/phases/16-local-memory-web-ui-v2-1-e/16-UI-SPEC.md ("Referential-integrity confirm" — the load-bearing flow)
  </read_first>
  <behavior>
    - test_list_items: lists exactly the two state files + active agreements; `show_retired=True` appends retired ones.
    - test_view_item: returns 200 with the item body for a state file and for an agreement.
    - test_edit_calls_agree_writer: an agreement edit routes through `tools.agree.write` (assert via monkeypatch spy) — no direct file write in routes.
    - test_add_blank_because_refuses: `add_agreement(..., because="")` surfaces AgreementRefused as a non-200 with the verbatim REFUSED message; NOTHING written.
    - test_retire_flips_in_place: confirmed retire leaves the file present with `status: retired` (never deleted).
    - test_progress_save_stamps_quoted_date: save_progress rewrites body and sets `updated: "<ISO>"` quoted; other frontmatter keys preserved.
    - test_orphan_blocks_without_confirm: retire with referrers returns 409 + `{"orphans":[{file,line,kind}]}` and does NOT call the writer.
    - test_confirmed_retire_proceeds_and_leaves_docs_untouched: `confirm=True` retires via the writer; the referrer files are byte-unchanged.
    - test_referrer_file_line_accuracy: the orphan list `file:line` entries match the known references in the tmp docs fixture.
  </behavior>
  <action>
    Author `test_routes.py` (list/view/edit/add/retire/progress) and `test_referential_integrity.py`
    (orphan flow) importing `from tools.memory_ui import routes`. Thread `agreements_dir=`,
    `state_dir=`, `derived_dir=`/`base_dir=`, `scan_roots=` from the `tmp_agreements_tree` +
    tmp-docs fixtures into EVERY route call (16-RESEARCH Pitfall 2). Never start a live server here.
    Use a monkeypatch spy to assert the agreement write path delegates to `tools.agree.write` rather
    than writing files directly. Pin exactly the `<interfaces>` signatures above.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_ui/tests --collect-only -q 2>&1 | grep -q "test_orphan_blocks_without_confirm"</automated>
  </verify>
  <acceptance_criteria>
    - Both test files collect all named tests (RED expected until 16-03/16-05 implement).
    - No route call omits an injected dir; grep confirms no test opens a socket or references the real `.memory/agreements/`.
    - The anti-invent and flip-in-place cases mirror `tools/agree/tests/test_agree_refusal.py`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → real `.memory/` planes | tests must be confined to tmp fixtures; a leak would author/mutate real memory files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Tampering | tests writing real agreements / real `.memory/derived/` | mitigate | Every fixture writes under `tmp_path`; inject `agreements_dir`/`state_dir`/`derived_dir`/`base_dir`; assert `git status` clean under `.memory/` after run |
| T-16-SC | Tampering | pip/uv installs | accept | Zero new external packages (D-16-01, stdlib only); `dependencies = []`; only the zero-resolution member entry hits `uv.lock` — no registry/postinstall surface |
</threat_model>

<verification>
- `uv sync` clean; `uv.lock` delta is the single zero-dep member entry.
- `uv run pytest tools/memory_ui tools/memory_regen/tests/test_pointer_index.py --collect-only -q` collects all authored tests.
- `git status` shows no files under `.memory/agreements/` or `.memory/derived/` after any test run.
</verification>

<success_criteria>
- New member enrolled and importable; conftest re-exports the synthetic agreements corpus.
- RED tests exist and pin the generator API (SC2), route API (SC1), and orphan flow (SC3).
- No real agreement written, no live socket opened.
</success_criteria>

<output>
Create `.planning/phases/16-local-memory-web-ui-v2-1-e/16-01-SUMMARY.md` when done.
</output>
