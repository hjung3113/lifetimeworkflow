---
phase: 16-local-memory-web-ui-v2-1-e
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - tools/memory_ui/routes.py
  - tools/memory_ui/_stamp.py
autonomous: true
requirements: [MEM2-07]
must_haves:
  truths:
    - "The UI lists/views state files + agreements, edits/retires agreements through tools.agree.write, and never writes agreements directly"
    - "Add refuses a blank --because (anti-invent); the UI never fabricates one"
    - "Retire/edit that would orphan referrers returns confirm-required (409) and does not write until confirm=True"
    - "Progress save refreshes a quoted updated: stamp without introducing a wall-clock into the read path"
  artifacts:
    - path: "tools/memory_ui/routes.py"
      provides: "Pure route functions returning (status, headers, body) over injected dirs"
      exports: ["list_items", "view_item", "add_agreement", "retire_agreement", "save_progress", "pointer_lookup"]
      min_lines: 60
    - path: "tools/memory_ui/_stamp.py"
      provides: "Progress updated: stamp writer composed from parse_frontmatter + quoted-date scalar"
      exports: ["stamp_progress"]
  key_links:
    - from: "tools/memory_ui/routes.py"
      to: "tools.agree.write.retire"
      via: "sanctioned writer delegation"
      pattern: "agree.*\\.(add|retire)"
    - from: "tools/memory_ui/routes.py"
      to: "tools.memory_regen.pointer_index"
      via: "inline regenerate + lookup before destructive action"
      pattern: "pointer_index"
---

<objective>
Implement the testable core of the memory UI: pure route functions (`routes.py`) and the progress
stamp writer (`_stamp.py`). Routes take injected plane dirs and return `(status, headers, body)` so
they are unit-tested without a socket. All writes delegate to the sanctioned writers
(`tools.agree.write` for agreements; a `_stamp` helper for progress) — no new agreement writer, no
fabricated `--because`, no wall-clock in the read path. This plan delivers SC1's edit/retire logic
and SC3's surface-and-confirm referential integrity.

Purpose: keep the HTTP shell (16-05) trivial by concentrating all logic in tested pure functions.
Output: `routes.py` + `_stamp.py`; the RED route + referential-integrity tests from 16-01 turn GREEN.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-RESEARCH.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-PATTERNS.md
@.planning/phases/16-local-memory-web-ui-v2-1-e/16-UI-SPEC.md

<interfaces>
Delegate to (already exist — never re-roll):
- tools.agree.write.add(slug, title, rule, *, because, added, related=None, agreements_dir) -> Path   # raises AgreementRefused
- tools.agree.write.retire(slug, *, agreements_dir) -> Path   # flip status: retired in place
- tools.agree.write._target_for(slug, agreements_dir)   # slug/path confinement semantics to reuse (V5)
- tools.harness_lint.agreements.iter_agreement_files / load_agreement
- tools.harness_lint.parse_frontmatter(md_text) -> (dict, str)
- tools.memory_regen.pointer_index.build_index(*, base_dir, scan_roots)   # inline regenerate for the orphan check
- tools.agree.write._dump_frontmatter(frontmatter) -> str   # quoted-scalar YAML dump to mirror for the stamp
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _stamp.py — progress updated: writer (checkpoint semantics, no read-path clock)</name>
  <files>tools/memory_ui/_stamp.py</files>
  <read_first>
    - harness/commands/checkpoint.md (lines 18-20 — quoted `updated:` stamp semantics)
    - tools/agree/write.py (lines 21-30 — `_dump_frontmatter`, YAML safe, default_style='"')
    - tools/harness_lint/frontmatter.py (parse_frontmatter round-trip)
    - .memory/state/activeContext.md (the frontmatter shape it edits)
  </read_first>
  <behavior>
    - `stamp_progress(path, body_text, *, today)` reads the file, splits frontmatter via parse_frontmatter, sets `updated` to a QUOTED `"YYYY-MM-DD"` string, preserves all other frontmatter keys, replaces the body with `body_text`, writes back.
    - The date is quoted (round-trips as a string, per checkpoint.md), and is a WRITE-path value — it must not be importable/called by `inject.py::assemble` (read path stays clock-free).
  </behavior>
  <action>
    Create `tools/memory_ui/_stamp.py` with `stamp_progress(path, body_text, *, today: str)`. Compose
    from `parse_frontmatter` (split) + a quoted-scalar dump mirroring `tools.agree.write._dump_frontmatter`
    (`YAML(typ="safe")`, `default_style='"'`). `today` is injected by the caller (never a wall-clock
    read inside this module's import-time or inside `assemble`). Keep this helper OUT of any code path
    imported by `inject.py`. Per RESEARCH Open-Q1: the stamp writer lives tool-local in
    `tools/memory_ui/` (it is a UI write action).
  </action>
  <verify>
    <automated>uv run pytest tools/memory_ui/tests/test_routes.py::test_progress_save_stamps_quoted_date -x -q</automated>
  </verify>
  <acceptance_criteria>
    - After `stamp_progress`, the file's frontmatter has `updated: "<today>"` quoted; all other keys preserved; body replaced.
    - `grep -rn "_stamp\|stamp_progress" tools/memory_regen/inject.py` returns nothing (read path never imports the stamp writer).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: routes.py — list/view/add/retire/save + orphan surface-and-confirm</name>
  <files>tools/memory_ui/routes.py</files>
  <read_first>
    - tools/agree/write.py (add/retire/_target_for/AgreementRefused; lines 33-99)
    - tools/memory_regen/inject.py (assemble injected-dir DI idiom; lines 128-137)
    - tools/memory_regen/pointer_index.py (build_index — inline regenerate for the orphan check; created in 16-02)
    - tools/memory_ui/tests/test_routes.py + test_referential_integrity.py (the contract to satisfy)
    - .planning/phases/16-local-memory-web-ui-v2-1-e/16-UI-SPEC.md (Referential-integrity confirm flow — the 409 / N-referrer / confirm=1 sequence)
  </read_first>
  <behavior>
    - list_items: returns state files + active agreements; show_retired appends retired.
    - view_item: 200 + body for a state file or agreement; path/slug confined (reject traversal → non-200).
    - add_agreement: delegates to `agree_write.add` with `because` verbatim; blank/refused → non-200 carrying the verbatim REFUSED message; nothing written.
    - retire_agreement: `pointer_lookup` first (inline regenerate); if referrers and not confirm → 409 `{"orphans":[...]}` WITHOUT writing; if confirm → `agree_write.retire` (flip in place). Referrer files never modified.
    - save_progress: writes via `_stamp.stamp_progress`; refuses paths outside `.memory/state`.
    - pointer_lookup(item, *, base_dir, scan_roots): calls `pointer_index.build_index` inline (Pitfall 4 — correctness over cached freshness) and returns the item's referrer list.
  </behavior>
  <action>
    Create `tools/memory_ui/routes.py` implementing the six pure functions per the `<interfaces>`
    signatures. Each returns `(status:int, headers:dict, body:bytes)` with JSON bodies. Reuse
    `write._target_for` slug/path confinement (V5: `^[a-z0-9]+(?:-[a-z0-9]+)*$`, no `_`-prefix,
    `resolve().relative_to(base)`) for every item/slug param; reject traversal. Delegate ALL agreement
    writes to `tools.agree.write` (never author `.memory/agreements/*`); surface `AgreementRefused`
    verbatim and never retry with a fabricated `because`. For the orphan check, inline-regenerate the
    pointer-index via `build_index` then read the item's list (do not trust a possibly-stale cached
    file). Reuse `iter_agreement_files`/`load_agreement`/`parse_frontmatter` for listing/parsing.
  </action>
  <verify>
    <automated>uv run pytest tools/memory_ui/tests/test_routes.py tools/memory_ui/tests/test_referential_integrity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All route + referential-integrity tests pass.
    - `test_orphan_blocks_without_confirm`: 409 + orphans, no write; `test_confirmed_retire_proceeds_and_leaves_docs_untouched`: writer called, referrer files byte-unchanged.
    - `grep -n "open(" tools/memory_ui/routes.py` shows NO direct write to `.memory/agreements/` — writes go only through `tools.agree.write` / `_stamp`.
    - Path traversal on a slug/item param is rejected (reuses `_target_for` semantics).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser request → route function | untrusted slug/item/body params cross here |
| route → `.memory/agreements/` + `docs/`/`harness/` | writes must be confined + delegated; source/constitution planes must never be auto-mutated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Repudiation | add_agreement fabricating `--because` | mitigate | `because` passed verbatim from a required field; `tools.agree.write.add` refuses blank (AgreementRefused); UI never defaults or retries |
| T-16-03 | Tampering/Elevation | path traversal via slug/item param (`../../contracts/x`) | mitigate | Reuse `write._target_for` confinement (`resolve().relative_to(base)`, no `_`-prefix, slug regex); reject on failure |
| T-16-04 | Elevation | auto-mutating constitution/source planes to "fix" orphans | mitigate | D-16-03 surface-and-confirm only; routes never write `docs/`/`harness/`/`inject.py`; confirmed retire touches only the agreement file via the sanctioned writer |
| T-16-12 | Tampering | wall-clock in the read path | mitigate | Stamp date injected as `today=`; `_stamp` never imported by `inject.py::assemble`; determinism preserved |
| T-16-SC | Tampering | pip/uv installs | accept | Zero external packages (stdlib only); no lock change |
</threat_model>

<verification>
- `uv run pytest tools/memory_ui -q` green (except the live-bind test deferred to 16-05).
- No direct agreement write in `routes.py`; all through `tools.agree.write`.
- Orphan flow blocks without confirm and leaves referrers untouched on confirm.
</verification>

<success_criteria>
- SC1 edit/retire/save logic implemented via sanctioned writers.
- SC3 surface-and-confirm implemented (409 + orphans; confirm proceeds; referrers never rewritten).
</success_criteria>

<output>
Create `.planning/phases/16-local-memory-web-ui-v2-1-e/16-03-SUMMARY.md` when done.
</output>
