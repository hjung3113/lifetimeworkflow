---
phase: 25-graph-compiler-queries-conductor-proof-v2-3-a
plan: 05
type: execute
wave: 4
depends_on: ["25-03", "25-04"]
files_modified:
  - docs/adr/0009-contract-relationship-graph-model.md
  - docs/adr/README.md
autonomous: false
requirements: [TOPO-07]

must_haves:
  truths:
    - "D-04: ADR-0009 records the FULL model landed this phase as ONE ratified unit — the record/graph model (compiler resolution + diagnostic slugs), the affected-set query semantics (direct/reverse/transitive, {ids, paths}, cycle-safe), AND the conductor rendering contract (D-01 indented tree + cycle marker + linear byte-identity) — not a partial or single-topic record."
    - "ADR-0009 explicitly records the WR-01 (deferred, fixture-vocabulary-constrained) and WR-02 (closed, ValueError guard) dispositions decided in Plan 01/04, so the decision trail is traceable from ADR to code."
    - "ADR-0009 lands via the human-ratified append-only path (docs/adr/ is CODEOWNERS-gated; the agent scaffolds, a human flips Status: proposed -> accepted) — it is never agent-self-approved."
  artifacts:
    - path: "docs/adr/0009-contract-relationship-graph-model.md"
      provides: "the human-ratified MADR record for the full Phase-25 graph model"
    - path: "docs/adr/README.md"
      provides: "an added index row for ADR-0009 (append-only — no existing row removed/edited)"
---

<objective>
Author and land ADR-0009 — the single ratified decision record for the full model Phase 25 ships: the
compiler/graph model, the affected-set query semantics, and the conductor rendering contract, plus the
WR-01/WR-02 dispositions. Human ratification closes the phase.

Purpose: D-04 requires ONE ADR covering all three surfaces together, since Phases 26-29 depend on the
whole thing as a unit. `docs/adr/` is constitution-plane (CODEOWNERS-gated) — an agent scaffolds the
content, a human reviews and flips `Status: proposed -> accepted`.
Output: `docs/adr/0009-contract-relationship-graph-model.md` (ratified), an added `docs/adr/README.md`
index row.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-CONTEXT.md
@.planning/phases/24-contract-relationship-vocabulary-compatibility-v2-3-a/24-REVIEW.md

<interfaces>
`harness/commands/adr.md` (the `/adr` scaffold convention, read verbatim): next number = highest
existing `docs/adr/NNNN-*.md` + 1 (currently `0008`, so this is `0009`); MADR sections (Title, Status,
Context and Problem Statement, Decision Drivers, Considered Options, Decision Outcome, Consequences,
Links); append-only — never edit an existing ADR; add one row to `docs/adr/README.md`, never remove a
row.

`docs/adr/0008-task-control-plane-lifecycle.md` (the most recent ADR — mirror its exact section
structure, `Status`/`Deciders`/`Supersedes`/`Superseded by`/`Complements` header block, and its
"Ratified by human/CODEOWNERS on <date>" approval-footer convention).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ADR-0009 recording the full model + WR-01/WR-02 dispositions</name>
  <files>docs/adr/0009-contract-relationship-graph-model.md, docs/adr/README.md</files>
  <read_first>docs/adr/0008-task-control-plane-lifecycle.md (exact section/header structure to mirror), harness/commands/adr.md (the scaffold convention), .planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-CONTEXT.md (D-01..D-04 verbatim), contracts/harness/topology/relationship.schema.json (the record shape ADR-0009 documents), tools/contract_graph/compile.py + query.py (the finished Plan-01/02 files — the compiler/query semantics to describe), harness/commands/pipeline.md (the finished Plan-03 file — the D-01 render section to describe)</read_first>
  <action>
    Create `docs/adr/0009-contract-relationship-graph-model.md` with `Status: proposed`, `Complements:`
    linking ADR-0003 (pipeline-topology slot) and ADR-0002 (template de-specialization). Context and
    Problem Statement: the harness needed one general, deterministic way to validate and query
    contract-relationships beyond the linear pipeline model, consumed by the existing conductor surface
    with no second interpreter. Decision Outcome, as ONE ratified unit per D-04:
    (1) the record/graph model — `relationship.schema.json`'s per-record shape (Phase 24), the additive
    `[contract_graph]` TOML slot + `effective_relationships()` lowering/union (Phase 24), and this
    phase's `compile_graph()` endpoint/authority-owned-contract resolution with the three D-02
    diagnostic slugs (`unresolved-authority`, `dangling-endpoint`, `unknown-contract`), explicitly
    accepting fan-in/fan-out/disconnected/cycle as legal; (2) the affected-set query semantics — D-03's
    `direct`/`reverse`/`transitive` returning `{ids, paths}`, cycle-safe via a visited-set, creating no
    task-evidence requirement and preloading no contract body; (3) the conductor rendering contract —
    D-01's indented-tree render rooted at authorities with a `(cycle -> <node>)` terminal marker for
    non-linear graphs, while the existing linear render stays byte-identical, with no new command or
    persona (TOPO-06). Record the WR-01 disposition (24-REVIEW.md: the lowered id
    `pipeline/<contract>/<from>-><to>` is non-injective when components contain `/` or `->`; THIS
    milestone defers the code fix and constrains fixture vocabulary instead — Plan 04's automated corpus
    scan is the enforcement mechanism) and the WR-02 disposition (closed: `effective_relationships`
    now raises `ValueError` naming the offending record instead of a bare `KeyError`, Plan 01 Task 3).
    Add the Links section pointing at `contracts/harness/topology/relationship.schema.json`,
    `tools/harness_config/loader.py`, `tools/contract_graph/`, and the three conductor surfaces. Add one
    row to `docs/adr/README.md`'s index (append only).
  </action>
  <acceptance_criteria>
    `docs/adr/0009-contract-relationship-graph-model.md` exists and contains `"## Status"` (or the
    equivalent `- **Status:**` line matching ADR-0008's convention) plus all three model components
    named in its Decision Outcome (grep for `"unresolved-authority"`, `"transitive"`, and
    `"cycle"` all present).
    `docs/adr/README.md` gains exactly one new row referencing `0009-` (line-count diff `+1`, zero
    removed lines).
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/adr/0009-contract-relationship-graph-model.md && grep -q "unresolved-authority" docs/adr/0009-contract-relationship-graph-model.md && grep -q "transitive" docs/adr/0009-contract-relationship-graph-model.md && grep -q "cycle" docs/adr/0009-contract-relationship-graph-model.md && grep -q "0009" docs/adr/README.md</automated>
  </verify>
  <done>ADR-0009 drafts the full model + WR-01/WR-02 dispositions in MADR form, landed via the standard human-ratified constitution-plane path (contract-guard denies a direct agent write to docs/adr/; this lands through the same approved mechanism prior ADRs used — dev-bypass note or CODEOWNERS PR review, never agent self-approval), with Status left at `proposed` pending Task 2's human ratification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human ratification — flip ADR-0009 Status to accepted</name>
  <files>docs/adr/0009-contract-relationship-graph-model.md, docs/adr/README.md</files>
  <read_first>docs/adr/0009-contract-relationship-graph-model.md (Task 1's draft), docs/adr/0008-task-control-plane-lifecycle.md (the ratification-footer convention to mirror)</read_first>
  <action>
    This is a BLOCKING human checkpoint — the agent does NOT self-ratify. The agent presents the drafted
    ADR-0009 content for human review, then PAUSES. A human confirms the record accurately captures the
    compiler, query, and conductor-rendering decisions plus the WR-01/WR-02 dispositions, then flips
    `Status: proposed` -> `Status: accepted` (via the same human-ratified constitution-plane write path
    used for prior ADRs — CODEOWNERS PR review or an explicit human-supplied write, never agent
    self-approval or HARNESS_DEV_BYPASS used to self-land the ratification).
  </action>
  <what-built>ADR-0009, recording the full Phase-25 contract-relationship graph model (compiler + queries + conductor rendering contract) and the WR-01/WR-02 dispositions, drafted at `docs/adr/0009-contract-relationship-graph-model.md` with `Status: proposed`.</what-built>
  <how-to-verify>
    Read `docs/adr/0009-contract-relationship-graph-model.md` in full. Confirm it accurately reflects:
    (1) the compiler's endpoint/contract resolution and the three diagnostic slugs; (2) the query
    layer's `{ids, paths}` shape and cycle-safety guarantee; (3) the conductor's indented-tree render
    and the linear-byte-identity guarantee; (4) the WR-01 (deferred) and WR-02 (closed) dispositions.
    Confirm `docs/adr/README.md` gained exactly one new row and no existing row was altered.
  </how-to-verify>
  <acceptance_criteria>
    `grep -qi accepted docs/adr/0009-contract-relationship-graph-model.md` exits 0 (Status flipped).
    `grep -q 0009 docs/adr/README.md` exits 0.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: grep -qi accepted docs/adr/0009-contract-relationship-graph-model.md && grep -q 0009 docs/adr/README.md</human-check>
  </verify>
  <resume-signal>Reply "ratified" (or "accepted") to flip `Status: proposed` -> `Status: accepted` and finalize the record, or describe any correction needed before ratification.</resume-signal>
  <done>ADR-0009 Status is accepted, human-ratified via the standard constitution-plane path, and `docs/adr/README.md` carries its index row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent draft -> constitution-plane ADR | `docs/adr/` is CODEOWNERS-gated and denied to direct agent writes by `contract_guard`; this task's Task 1 output is a DRAFT (`Status: proposed`) only — Task 2's human checkpoint is the actual ratification authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Elevation of Privilege (self-ratification) | `docs/adr/0009-*.md` | mitigate | The agent never sets `Status: accepted` itself — Task 2 is a blocking human checkpoint, and the constitution-plane write path (contract_guard / CODEOWNERS) is unchanged by this phase, consistent with the repo's standing "machines gate, humans ratify" posture. |
</threat_model>

<verification>
`docs/adr/0009-contract-relationship-graph-model.md` exists with all required MADR sections and
references the compiler, queries, and conductor contract by name.
`docs/adr/README.md` diff is append-only (+1 row, 0 removed).
Human ratification recorded (`Status: accepted`) before phase closeout.
</verification>

<success_criteria>
ADR-0009 is authored, covers the full Phase-25 model as one unit per D-04, records the WR-01/WR-02
dispositions, and is human-ratified via the existing constitution-plane path.
</success_criteria>

<output>
Create `.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-05-SUMMARY.md` when done
</output>
