---
phase: 25-graph-compiler-queries-conductor-proof-v2-3-a
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - tools/contract_graph/tests/fixtures/graphs/valid/cases.json
  - tools/contract_graph/tests/test_proof_fixtures.py
  - tools/contract_graph/tests/test_cross_repo_authority.py
autonomous: true
requirements: [TOPO-07]

must_haves:
  truths:
    - "A generic, domain-neutral fixture proves shared-contract fan-out (one authority, multiple dependents on the same contract), request+response modeled as two SEPARATE relationship records (not one bidirectional record), and event fan-out (one authority, multiple independent dependent relationships) all compile with empty diagnostics."
    - "A generic fixture proves a legal (canonical) two-or-more-node cycle compiles with empty diagnostics AND that transitive() terminates on it, returning the correct {ids, paths}."
    - "A generic fixture proves cross-repo authority resolution: a `repo:stage` authority endpoint resolves against declared workspace members (via split_endpoint) and its contract is existence-checked in the PRODUCER member's own contracts/ tree — reusing the same idiom as test_edge_contracts_tracked_in_producer, not a new checker."
    - "WR-01 go/no-go is explicit: this plan DEFERS the WR-01 code fix (non-injective lowered-id join) and instead constrains every TOPO-07 fixture's endpoint/contract strings to exclude the literal substrings `/` and `->`, with an automated assertion proving the fixture corpus honors that constraint."
    - "The log-parser example instance (examples/log-parser/project.toml and its topology) is untouched by this plan, and the GEN-04 core→example guard stays green."
  artifacts:
    - path: "tools/contract_graph/tests/fixtures/graphs/valid/cases.json"
      provides: "domain-neutral non-linear proof fixtures: shared-contract fan-out, request/response as separate records, event fan-out, legal cycle"
    - path: "tools/contract_graph/tests/test_proof_fixtures.py"
      provides: "compile_graph + query coverage over every fixture in cases.json, plus the WR-01 fixture-vocabulary assertion"
    - path: "tools/contract_graph/tests/test_cross_repo_authority.py"
      provides: "the cross-repo authority-resolution proof + the log-parser-instance-untouched + GEN-04-green regression"
  key_links:
    - from: "tools/contract_graph/tests/test_cross_repo_authority.py"
      to: "tools/workspace_config/loader.py::split_endpoint"
      via: "cross-repo authority resolution reuses split_endpoint, never a second repo:stage parser"
      pattern: "split_endpoint"
---

<objective>
Author the domain-neutral proof fixtures TOPO-07 requires (shared-contract fan-out, request/response
as separate records, event fan-out, a legal cycle, cross-repo authority resolution), prove them against
Plan 01's compiler and Plan 02's queries, make an explicit WR-01 go/no-go call, and confirm the
log-parser instance + GEN-04 guard are untouched.

Purpose: TOPO-07 is the domain-neutral evidence that the general relationship model actually works
beyond the two-stage linear default — every non-linear shape the design promises must have a fixture
proving it, generically, with the existing GEN-04 core→example boundary held.
Output: `tools/contract_graph/tests/fixtures/graphs/valid/cases.json` + two new test files.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-CONTEXT.md
@.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-RESEARCH.md
@.planning/phases/24-contract-relationship-vocabulary-compatibility-v2-3-a/24-REVIEW.md

<interfaces>
Plan 01/02 outputs (read the finished files, do not re-implement):
- `tools.contract_graph.compile_graph(cfg=None) -> {"relationships", "adjacency", "diagnostics"}`.
- `tools.contract_graph.direct/reverse/transitive(graph, node) -> {"ids": [...], "paths": [[...]]}`.

Cross-repo precedent to mirror exactly (source: `tools/harness_lint/tests/test_workspace_config.py::test_edge_contracts_tracked_in_producer`):
glob `{producer_root}/contracts/**/*.schema.json`, strip the `.schema.json` suffix, membership-test the
contract id. `tools.workspace_config.split_endpoint("repo:stage") -> ("repo", "stage")`.

GEN-04-safe instance-path precedent (source: Phase-24 `24-02-SUMMARY.md` deviation note + read
`tools/harness_config/tests/test_topology_relationships.py`'s instance-regression test): build any
instance-path reference via NON-CONTIGUOUS `Path.joinpath("examples", "log-parser", ...)` segments (not
a single f-string containing the contiguous `"examples/log-parser"` substring), and phrase docstrings
as "instance-path" rather than the literal word "examples/" to avoid tripping
`tools/harness_lint/tests/test_core_no_example_dep.py`'s prose scan.

24-REVIEW.md WR-01 (read in full): the lowered id `pipeline/<contract>/<from>-><to>` is non-injective
when `contract`/`from`/`to` contain `/` or the literal `->`. This plan's disposition: DEFER the code
fix — constrain every fixture string in `cases.json` to exclude `/` and `->`, and assert that
constraint automatically (a fixture corpus scan), rather than editing `effective_relationships()`'s
id-construction again this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generic non-linear proof fixtures — fan-out, request/response split, event fan-out, legal cycle</name>
  <files>tools/contract_graph/tests/fixtures/graphs/valid/cases.json, tools/contract_graph/tests/test_proof_fixtures.py</files>
  <read_first>tools/harness_config/tests/fixtures/relationships/valid/cases.json (Phase-24 precedent for fixture JSON shape), contracts/harness/topology/relationship.schema.json (field shape), tools/contract_graph/compile.py (finished Plan-01 file), tools/contract_graph/query.py (finished Plan-02 file)</read_first>
  <behavior>
    - Test: "shared-contract fan-out" fixture (one authority, 3 dependents, one contract, all ids
      `/`/`->`-free) compiles with `diagnostics == []` and `adjacency[authority]` has all 3 sorted
      dependents.
    - Test: "request/response as separate records" fixture (two DISTINCT relationship records — one
      `id="req-flow"` authority=client dependents=[server] contract=request-contract; one
      `id="resp-flow"` authority=server dependents=[client] contract=response-contract — NOT one
      bidirectional record) compiles with `diagnostics == []` and both directions appear as separate
      adjacency entries.
    - Test: "event fan-out" fixture (one authority publishing to N independent dependent relationships,
      each a distinct record) compiles with `diagnostics == []`.
    - Test: "legal cycle" fixture (>=2 nodes, e.g. `a->b->c->a`) compiles with `diagnostics == []` AND
      `tools.contract_graph.transitive(graph, "a")` terminates, returning `ids=["b","c"]` (sorted) with
      valid paths, never revisiting `a` as a dependent-of-itself entry.
    - Test (WR-01 disposition): every `id`, `contract`, `authority`, and `dependents[*]` string across
      the ENTIRE `cases.json` fixture corpus contains neither the substring `/` nor `->` — an automated
      corpus scan, not a manual review, proving the deferred-WR-01 constraint holds.
  </behavior>
  <action>
    Author `tools/contract_graph/tests/fixtures/graphs/valid/cases.json` as a JSON array of
    domain-neutral relationship-record groups (each group is a small list of records forming one named
    scenario: `shared-contract-fanout`, `request-response-split`, `event-fanout`, `legal-cycle`),
    mirroring the field shape of `contracts/harness/topology/relationship.schema.json` exactly (`id`,
    `contract`, `authority`, `dependents`, optional `kind`/`labels`). Use only alphanumeric/hyphen
    identifiers (no `/`, no `->`) throughout, honoring the WR-01 disposition. Write
    `tools/contract_graph/tests/test_proof_fixtures.py` loading this JSON, wrapping each scenario's
    records into a synthetic cfg shape (`{"contract_graph": {"relationships": [...]}}`, `pipeline` empty
    — `effective_relationships()` unions cleanly with zero legacy edges), compiling each via
    `compile_graph`, and asserting the behaviors above. Add the corpus-wide WR-01 scan as its own test
    function iterating every string field across the loaded JSON.
  </action>
  <acceptance_criteria>`uv run pytest tools/contract_graph/tests/test_proof_fixtures.py -q` exits 0, all 5 behavior tests pass.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/contract_graph/tests/test_proof_fixtures.py -q</automated>
  </verify>
  <done>Four domain-neutral non-linear scenarios are fixture-proven against the compiler+queries, and the WR-01 disposition (deferred, fixture-vocabulary-constrained) is itself an automated, falsifiable test rather than a documentation-only claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cross-repo authority resolution + log-parser-instance-untouched + GEN-04 regression</name>
  <files>tools/contract_graph/tests/test_cross_repo_authority.py</files>
  <read_first>tools/workspace_config/loader.py (split_endpoint, members), tools/harness_lint/tests/test_workspace_config.py::test_edge_contracts_tracked_in_producer (the exact idiom to reuse), tools/harness_config/tests/test_topology_relationships.py (the GEN-04-safe non-contiguous-Path instance-reference precedent), tools/harness_lint/tests/test_core_no_example_dep.py (the guard this task must keep green)</read_first>
  <behavior>
    - Test: a synthetic workspace-shaped cfg with two declared `[[members]]` (`member-a`, `member-b`,
      in-repo fixture roots under `tests/fixtures/workspace/` reusing the existing Phase-11 2-member
      fixture rather than inventing new on-disk directories) and one explicit
      `[[contract_graph.relationships]]` record whose `authority` is `"member-a:emit"` and whose
      `dependents` is `["member-b:ingest"]` compiles with `diagnostics == []` — the compiler resolves
      the repo half (`member-a`) against declared members via `split_endpoint`, and the contract is
      existence-checked under `member-a`'s own `contracts/` tree (not the repo-root `contracts/`).
    - Test: an authority whose repo half is an UNDECLARED member (e.g. `"member-z:emit"`) produces an
      `unresolved-authority` diagnostic naming `member-z`.
    - Test (GEN-04-safe): `git diff --stat` (via subprocess, `shell=False`) against the base commit for
      `examples/log-parser/project.toml` is empty — this plan makes zero edits to the log-parser
      instance. Build the instance path via non-contiguous `Path.joinpath(...)` segments, not a
      contiguous f-string.
    - Test: `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` (invoked as a
      subprocess from within this test, mirroring the Phase-24 regression style) exits 0 — the GEN-04
      guard is unaffected by this plan's new test file.
  </behavior>
  <action>
    Write `tools/contract_graph/tests/test_cross_repo_authority.py` using the EXISTING 2-member
    workspace fixture under `tests/fixtures/workspace/` (Phase 11) as the on-disk member roots — do not
    create new fixture directories. Build the synthetic cfg dict in Python (members list + one explicit
    relationship record), call `compile_graph`, and assert against it directly — reuse
    `split_endpoint` and the schema-glob idiom exactly as `test_edge_contracts_tracked_in_producer`
    does, rather than writing a new resolver. Add the log-parser-instance-untouched check and the
    GEN-04 subprocess regression per the behavior list, phrasing any docstring/comment to avoid the
    literal contiguous `examples/` substring (say "instance path" instead) per the Phase-24 precedent.
  </action>
  <acceptance_criteria>
    `uv run pytest tools/contract_graph/tests/test_cross_repo_authority.py -q` exits 0.
    `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0.
    `git diff --stat -- examples/log-parser/project.toml` (against this plan's start commit) is empty.
    `uv run pytest -q` (full suite) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/contract_graph/tests/test_cross_repo_authority.py tools/harness_lint/tests/test_core_no_example_dep.py -q</automated>
  </verify>
  <done>Cross-repo authority resolution is fixture-proven via the existing split_endpoint/schema-glob idiom, the log-parser instance is provably untouched, and GEN-04 stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture corpus -> compiler | Fixture JSON is test-plane, trusted input — the risk here is process discipline (WR-01 vocabulary constraint), not adversarial input. |
| Cross-repo authority -> producer member's filesystem tree | The schema-glob resolution must stay confined to the resolved member's own `contracts/` subtree, never escape to an arbitrary path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Tampering | lowered relationship id construction (WR-01) | accept (deferred, constrained) | The non-injective join is a known, bounded-impact latent trap (24-REVIEW.md WR-01: worst case is a spurious duplicate-id `ValueError`, never silent data loss). This plan constrains its OWN fixture vocabulary to avoid triggering it and proves that constraint with an automated corpus scan, deferring the underlying code fix — an explicit, documented risk acceptance, not a silent gap. |
| T-25-08 | Tampering | cross-repo schema-glob resolution | mitigate | Reuses the exact `test_edge_contracts_tracked_in_producer` idiom, which globs only under the resolved member's OWN `contracts/` subtree (never a path built from unvalidated user input beyond a repo-relative member `root` already gated by `test_each_member_root_exists`). |
</threat_model>

<verification>
`uv run pytest tools/contract_graph -q` exits 0.
`uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q` exits 0.
`uv run pytest -q` (full suite) exits 0.
</verification>

<success_criteria>
Every TOPO-07 non-linear shape (shared-contract fan-out, request/response split, event fan-out, legal
cycle, cross-repo authority) is fixture-proven against the compiler and queries; WR-01's disposition is
explicit and automated; the log-parser instance and GEN-04 guard are unaffected.
</success_criteria>

<output>
Create `.planning/phases/25-graph-compiler-queries-conductor-proof-v2-3-a/25-04-SUMMARY.md` when done
</output>
