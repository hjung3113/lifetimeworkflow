---
phase: 10-context-economy-fan-out-synthesize-orchestration-v2-0
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - harness/skills/context-budget/SKILL.md
  - harness/agents/orchestrator.md
  - harness/commands/orient.md
  - tools/harness_lint/caps.py
  - tools/harness_lint/tests/test_context_budget_wiring.py
autonomous: true
requirements: [ECON-03]

must_haves:
  truths:
    - "A context-budget skill states the delegate-vs-inline heuristic (when to fan out vs work inline), domain-neutral"
    - "The orchestrator routing table + intake reference context-budget AND fan-out-synthesize, so the delegate-vs-inline decision is a named observable step"
    - "/orient read-order step 4 lists context-budget and fan-out-synthesize alongside gate-model/two-plane-memory"
    - "EXPECTED_SKILLS contains context-budget (11 total); EXPECTED_PERSONAS stays 5"
  artifacts:
    - path: "harness/skills/context-budget/SKILL.md"
      provides: "Delegate-vs-inline context-budget heuristic (ECON-03)"
      contains: "name: context-budget"
    - path: "tools/harness_lint/tests/test_context_budget_wiring.py"
      provides: "Structural gate that the heuristic is wired at both integration points (ECON-03)"
  key_links:
    - from: "harness/agents/orchestrator.md"
      to: "harness/skills/context-budget/SKILL.md"
      via: "routing-table row + intake step reference the skill"
      pattern: "context-budget"
    - from: "harness/commands/orient.md"
      to: "harness/skills/context-budget/SKILL.md"
      via: "read-order step 4 lists the skill"
      pattern: "context-budget"
---

<objective>
Author the ECON-03 surface: a dedicated `context-budget` skill (the delegate-vs-inline heuristic —
when to fan out vs work inline), and wire it at BOTH named integration points — the `orchestrator`
persona (routing table + intake step) AND `/orient` (read-order step 4) — so the delegate-vs-inline
routing decision is observable and repeatable, matching how `gate-model`/`two-plane-memory` are
surfaced today. Also surface the `fan-out-synthesize` skill (from plan 10-01) at both points.

Purpose: The routing decision "should I fan out or work inline?" must be a first-class, discoverable
heuristic, not buried in orchestrator prose — otherwise the context economy is not repeatable.
Output: 1 new skill + additive edits to orchestrator.md and orient.md + a 1-line caps.py entry + a
Wave-0 wiring test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-CONTEXT.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-RESEARCH.md
@.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-PATTERNS.md

<interfaces>
<!-- Analog surface + the exact seams to edit. Read directly — no exploration needed. -->
- Heuristic-map skill shape: harness/skills/gate-model/SKILL.md (frontmatter "Use when …" trigger →
  one-paragraph invariant → decision procedure → `## Related` tail) and
  harness/skills/two-plane-memory/SKILL.md (decision-forcing table form).
- orchestrator routing table: harness/agents/orchestrator.md lines 65-84 — the existing gate-model
  row is `| "Is this allowed / why is it blocked?" | (self) | gate-model skill |`.
- orchestrator intake: harness/agents/orchestrator.md lines 43-57 (numbered steps 1-6).
- /orient read-order step 4: harness/commands/orient.md lines 38-40 (lists gate-model / two-plane-memory).
- Wiring-test analog: tools/harness_lint/tests/test_orchestrator_topology.py (body-token presence gate,
  `_REPO_ROOT = parents[3]`, `parse_frontmatter` + lowercase body). Its existing token assertions
  (`topology`/`trace the topology`/`stage`/`/pipeline`) must NOT regress — edits are additive.
- Anti-sprawl enumeration: tools/harness_lint/caps.py EXPECTED_SKILLS (10 after plan 10-01) /
  EXPECTED_PERSONAS (stays 5).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author context-budget skill + enumerate it</name>
  <files>harness/skills/context-budget/SKILL.md, tools/harness_lint/caps.py</files>
  <read_first>
    - harness/skills/gate-model/SKILL.md (closest analog — heuristic-map shape + `## Related` tail)
    - harness/skills/two-plane-memory/SKILL.md (decision-forcing table form, lines ~50-55)
    - harness/skills/fan-out-synthesize/SKILL.md (authored in plan 10-01 — the `## Related` tail points here)
    - tools/harness_lint/caps.py lines 122-139 (EXPECTED_SKILLS frozenset)
    - .planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-PATTERNS.md context-budget section
  </read_first>
  <behavior>
    - test_skills.py set-equality passes with EXPECTED_SKILLS == 11 dirs (fan-out-synthesize + context-budget added).
    - context-budget description carries a "use"/"when" trigger, ≤1024, no `anthropic`/`claude`, no `<`/`>`, and is disjoint from fan-out-synthesize and the other 9 descriptions (test_descriptions_are_disjoint).
  </behavior>
  <action>
    Create `harness/skills/context-budget/SKILL.md` with frontmatter `name: context-budget` and a folded
    `description: >-` opening "Use when deciding whether to fan out / delegate a task or work it inline —"
    (disjoint from fan-out-synthesize's description). Body mirrors gate-model: a one-paragraph statement
    of the single invariant the heuristic protects (a single context must not balloon), then a decision
    procedure — signals that the surface is large enough to fan out (many files / independent units /
    would exceed a working context) → delegate via `fan-out-synthesize`; signals it fits → work inline.
    Add a `## Related` tail pointing at `harness/skills/fan-out-synthesize/SKILL.md` and `/orient`
    (mirror gate-model's `## Related`). Keep strictly domain-neutral (no `examples/` token, no domain
    vocab, no model ID).
    Edit `tools/harness_lint/caps.py`: add `"context-budget"` to EXPECTED_SKILLS (10 → 11). Optionally
    refresh the stale comment above the frozenset to mention the two Phase-10 skills; only the entry is
    load-bearing. Do NOT touch EXPECTED_PERSONAS (stays 5).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_skills.py tools/harness_lint/tests/test_agents.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_skills.py -q` exits 0 (set-equality holds at 11 skills)
    - `uv run pytest tools/harness_lint/tests/test_agents.py -q` exits 0 (EXPECTED_PERSONAS still 5)
    - caps.py EXPECTED_SKILLS contains the literal "context-budget"
    - harness/skills/context-budget/SKILL.md contains `name: context-budget`
  </acceptance_criteria>
  <done>context-budget skill authored, domain-neutral, enumerated (11 skills); persona count unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the heuristic into orchestrator + /orient, add Wave-0 wiring test</name>
  <files>harness/agents/orchestrator.md, harness/commands/orient.md, tools/harness_lint/tests/test_context_budget_wiring.py</files>
  <read_first>
    - harness/agents/orchestrator.md (full — routing table lines 65-84, intake lines 43-57; additive edits only)
    - harness/commands/orient.md (full — read-order step 4 at lines 38-40)
    - tools/harness_lint/tests/test_orchestrator_topology.py (copy structure near-verbatim; do NOT edit it)
    - harness/skills/context-budget/SKILL.md + harness/skills/fan-out-synthesize/SKILL.md (the skills being wired)
  </read_first>
  <action>
    Edit `harness/agents/orchestrator.md` — additively (do not remove any existing token, so
    test_orchestrator_topology stays green): append two routing-table rows near the gate-model row —
    one for "Large surface to cover / would balloon one context" → `(self) fan out` →
    `fan-out-synthesize skill, /fan-out-synthesize`, and one for "Should I delegate this or work
    inline?" → `(self)` → `context-budget skill`. Add a named delegate-vs-inline decision step to the
    intake procedure (e.g. as step between "Classify the work shape" and "Trace the topology", or a
    sub-note on classify) that references the `context-budget` skill by name so ECON-03 "observable and
    repeatable" is satisfied.
    Edit `harness/commands/orient.md` — additively append `context-budget` (delegate-vs-inline) and
    `fan-out-synthesize` (large-surface coverage) to the read-order step-4 skill list alongside
    `gate-model`/`two-plane-memory`, same one-item-per-skill shape.
    Create `tools/harness_lint/tests/test_context_budget_wiring.py` mirroring
    test_orchestrator_topology.py (`_REPO_ROOT = parents[3]`, `parse_frontmatter`, lowercased body):
    assert `context-budget` AND a fan-out token (`fan-out`) appear in `orchestrator.md` body AND in
    `orient.md` body. Keep domain-neutral (GEN-04).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_context_budget_wiring.py tools/harness_lint/tests/test_orchestrator_topology.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_context_budget_wiring.py -q` exits 0
    - `uv run pytest tools/harness_lint/tests/test_orchestrator_topology.py -q` exits 0 (topology tokens did not regress)
    - orchestrator.md body contains both `context-budget` and `fan-out-synthesize`
    - orient.md body contains both `context-budget` and `fan-out-synthesize`
  </acceptance_criteria>
  <done>context-budget + fan-out-synthesize wired into orchestrator routing/intake AND /orient read-order; wiring test green; topology test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/ source → derived runtime trees | Additive edits to orchestrator.md/orient.md are byte-emitted to both runtimes (emit in plan 10-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | An edit to orchestrator.md silently removing the topology routing/intake tokens (Phase-8 regression) | mitigate | Edits are additive only; test_orchestrator_topology.py re-runs and must stay green (asserted in Task 2 acceptance). |
| T-10-05 | Information Disclosure | A domain token / model ID leaking into the new skill or edits | mitigate | Keep context-budget + edits domain-neutral and model-agnostic; GEN-04 guard + placeholder-model validator loud-fail (fully verified in plan 10-03). |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase. |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint -q` green after both tasks (skills at 11, wiring + topology tests green).
- EXPECTED_PERSONAS unchanged at 5.
- No `examples/` token or domain vocab in the new skill or the edited seams.
</verification>

<success_criteria>
- context-budget skill authored, domain-neutral, enumerated (EXPECTED_SKILLS == 11).
- Heuristic wired at BOTH named integration points (orchestrator + /orient), plus fan-out-synthesize surfaced there.
- Wave-0 wiring test present and green; topology test not regressed.
</success_criteria>

<output>
Create `.planning/phases/10-context-economy-fan-out-synthesize-orchestration-v2-0/10-02-SUMMARY.md` when done
</output>
