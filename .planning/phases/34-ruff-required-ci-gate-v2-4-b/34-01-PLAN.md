---
phase: 34-ruff-required-ci-gate-v2-4-b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "The vendored third-party tree `docs/references/opencode-matt-workflows/**` is excluded at CONFIG level (`extend-exclude`), not by a per-rule ignore and not by a `# noqa` sweep"
    - "The exclusion removes exactly 193 findings (620 -> 427), and the count is measured with `--no-cache` because a warm `.ruff_cache` is demonstrably unreliable (34-RESEARCH §1)"
    - "The 24 ruff-classified SAFE fixes are applied; NO `--unsafe-fixes`, and the 3 hidden unsafe fixes stay unfixed"
    - "The full test suite stays green across the autofix commit — the autofixes are proven behaviour-preserving in this repo, not assumed"
    - "Nothing under contracts/, docs/adr/, golden/, docs/glossary.md is touched"
  artifacts:
    - path: "pyproject.toml"
      provides: "`[tool.ruff] extend-exclude` extended with the vendored tree"
  key_links:
    - from: "pyproject.toml::[tool.ruff].extend-exclude"
      to: "docs/references/opencode-matt-workflows"
      via: "config-level exclusion of vendored third-party sources"
      pattern: "docs/references/opencode-matt-workflows"
---

<objective>
Shrink the ruff finding set from 620 to the 403 that are genuinely this repo's, by (a) excluding
the vendored third-party tree at config level and (b) applying only ruff's safe autofixes.

Purpose: the baseline in plan 02 must hold real debt, not 193 findings in code this repo does not
own and 24 findings a machine can fix correctly.

Output: a two-commit diff — one config line, one mechanical autofix — and the measured
620 → 427 → 403 progression.
</objective>

<context>
@.planning/phases/34-ruff-required-ci-gate-v2-4-b/34-CONTEXT.md
@.planning/phases/34-ruff-required-ci-gate-v2-4-b/34-RESEARCH.md
@pyproject.toml

<interfaces>
<!-- MEASURED this session at f6ae4bd with ruff 0.15.20. Use directly; do not re-measure to plan. -->

Current config, pyproject.toml:43-49
```toml
[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [".dotnet", ".venv", "bin", "obj"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
```

Measured states (`uv run ruff check . --no-cache --statistics`):
  620 today | 427 after the exclusion | 403 after the safe fixes
Safe-fixable outside the vendored tree: I001 x15, F401 x6, UP017 x2, UP034 x1 = 24.
Expected 403 composition: E501 277, E702 102, E701 20, B007 1, B904 1, B905 1, F841 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude the vendored tree and record the drop</name>
  <files>pyproject.toml</files>
  <behavior>
    `[tool.ruff] extend-exclude` gains `"docs/references/opencode-matt-workflows"`, with a comment
    saying WHY (vendored third-party reference material, not this repo's code to fix).
  </behavior>
  <action>Record the before number first: `uv run ruff check . --no-cache --statistics`. Then add
  the entry. Re-measure with the same command. The drop must be exactly 193 and the resulting
  total exactly 427; E401 and E722 must vanish entirely (both classes live only in the vendored
  tree). If the numbers differ, stop and re-derive — a config exclusion that removes a different
  count than measured means the pattern is matching something else.

  Verify no non-vendored file was silently dropped:
  `uv run ruff check . --no-cache --output-format=concise | grep '^docs/'` must be empty.

  Commit with an explicit pathspec after inspecting `git diff --cached --name-only`.</action>
  <verify>
    <automated>uv run ruff check . --no-cache --statistics 2>&1 | tail -2</automated>
  </verify>
  <done>`Found 427 errors.`; E401 and E722 absent from the statistics table; the commit touched
  exactly `pyproject.toml`.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the 24 safe autofixes and prove the suite stays green</name>
  <files>(ruff-selected; ~16 files under tools/)</files>
  <behavior>
    I001, F401, UP017 and UP034 findings outside the vendored tree are gone. E501/E702/E701 and
    the four B/F semantic findings are untouched.
  </behavior>
  <action>Run `uv run ruff check . --no-cache --fix`. Do **not** pass `--unsafe-fixes` — the 3
  hidden unsafe fixes are deliberately left, and B904/B905/B007/F841 are semantic changes that do
  not belong in a mechanical commit.

  Inspect `git diff --stat` before committing and read the diff of at least the non-test files
  (`tools/task_control/__main__.py`, `tools/lifecycle_eval/runner.py`, `tools/memory_regen/inject.py`,
  `tools/handoff/handoff.py`, `tools/adoption_apply/cli.py`) — an unused-import removal is only
  safe if the import was genuinely unused, and this repo has modules that import for side effects.
  If any removed import looks load-bearing, restore it and leave that finding to the baseline.

  Then run the FULL suite: `uv run pytest -q`. Baseline is 1500 passed / 8 snapshots. Anything
  below that means an autofix was not behaviour-preserving here — revert that file and record it.

  Also run `uv run ruff format --check .` and confirm the count of would-reformat files did not
  grow (it is 25 before this task; the autofixes must not make formatting worse).

  Commit as its own atomic commit.</action>
  <verify>
    <automated>uv run ruff check . --no-cache --statistics 2>&1 | tail -2; uv run pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>`Found 403 errors.` with composition E501 277 / E702 102 / E701 20 / B007 1 / B904 1 /
  B905 1 / F841 1; `1500 passed` (or higher) with 8 snapshots; `ruff format --check` still reports
  25 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| vendored third-party source -> this repo's lint verdict | code this repo did not write must not dictate its gate |
| automated code rewrite -> committed source | a machine edit lands in the tree without a human reading each hunk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-01 | Tampering | `--fix` rewriting source | mitigate | safe fixes only (never `--unsafe-fixes`); the full 1500-test suite must stay green; the non-test diffs are read hunk-by-hunk before commit |
| T-34-02 | Repudiation | an over-broad `extend-exclude` hiding real findings | mitigate | the drop is asserted to be EXACTLY 193, and `grep '^docs/'` over the concise output must be empty — an exclusion that removed more would fail both |
| T-34-03 | Information disclosure | none | n/a | config + mechanical import edits only |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | zero dependency changes; `uv.lock` must not move |
</threat_model>

<verification>
- `uv run ruff check . --no-cache --statistics` reports 403 with the stated composition.
- `uv run pytest -q` >= 1500 passed, 8 snapshots.
- `git diff --check` clean.
- `git status --porcelain` empty after both commits.
- `uv.lock` unchanged (`git diff HEAD~2 -- uv.lock` empty).
</verification>

<success_criteria>
The finding set is 403, every removal is attributable (193 vendored, 24 safe autofixes), and the
suite is green.
</success_criteria>

<output>
Create `.planning/phases/34-ruff-required-ci-gate-v2-4-b/34-01-SUMMARY.md` when done, recording
the three measured totals verbatim.
</output>
