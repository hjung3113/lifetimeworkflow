---
phase: 34-ruff-required-ci-gate-v2-4-b
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - .github/workflows/ci.yml
  - tools/harness_lint/tests/test_ci_lint_gate.py
autonomous: true
requirements: [DEBT-01]

must_haves:
  truths:
    - "A `lint` job exists in ci.yml AND appears in `gate.needs` — a job absent from the fan-in is decorative, and the fan-in membership is asserted by a TEST, not by reading the file once"
    - "The gate was OBSERVED failing on a deliberately introduced new lint error, then observed passing after its removal — both runs recorded verbatim"
    - "The job does NOT run a bare `ruff check .` — that is red today and would make the job permanently red, i.e. the same non-gate the phase exists to remove"
    - "ci.yml still validates against the builtin GitHub-workflow schema"
    - "The security posture is unchanged: pinned action versions, no `${{ github.event.* }}` interpolated into any run shell"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "the `lint` job + its `gate.needs` membership"
      contains: "tools.ruff_baseline"
    - path: "tools/harness_lint/tests/test_ci_lint_gate.py"
      provides: "the structural assertion that `lint` exists, runs the ratchet, and is in gate.needs"
  key_links:
    - from: ".github/workflows/ci.yml::gate.needs"
      to: ".github/workflows/ci.yml::lint"
      via: "fan-in membership"
      pattern: "lint"
    - from: "tools/harness_lint/tests/test_ci_lint_gate.py"
      to: ".github/workflows/ci.yml"
      via: "parsed YAML, not a grep — a grep for `lint` matches a comment"
      pattern: "gate"
---

<objective>
Make the ratchet blocking, and then prove it blocks.

Purpose: DEBT-01's thesis is that a lint which cannot fail CI is not a gate. A `lint` job that
exists but is missing from `gate.needs` would reproduce that defect exactly, so fan-in membership
is asserted by a test alongside `test_ci_emit_drift.py` / `test_ci_stale_derived.py`, and the
failure is *observed*, not assumed.

Output: the `lint` job, its fan-in wiring, a structural test, and a recorded RED->GREEN cycle.
</objective>

<context>
@.planning/phases/34-ruff-required-ci-gate-v2-4-b/34-CONTEXT.md
@.planning/phases/34-ruff-required-ci-gate-v2-4-b/34-RESEARCH.md
@.github/workflows/ci.yml
@tools/harness_lint/tests/test_ci_stale_derived.py

<interfaces>
<!-- VERIFIED this session. -->

ci.yml has 11 jobs: setup, lang-tests, contract-check, drift, golden, core-suite, lifecycle-eval,
emit-drift, stale-derived, docs-guard, workspace. Fan-in at ci.yml:340:
```yaml
  gate:
    needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lifecycle-eval, emit-drift, stale-derived, docs-guard, workspace]
```

The shape to copy — docs-guard (ci.yml:302-313): checkout@v7.0.0, setup-uv@v8.3.2,
`uv sync --all-packages`, the tool CLI, then that tool's pytest selection.

Self-validation command documented at ci.yml:15-17:
`uv run check-jsonschema --builtin-schema vendor.github-workflows .github/workflows/ci.yml`

Existing CI-structure tests to model on: tools/harness_lint/tests/test_ci_emit_drift.py,
test_ci_stale_derived.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert the fan-in membership before the job exists</name>
  <files>tools/harness_lint/tests/test_ci_lint_gate.py</files>
  <behavior>
    Parses `.github/workflows/ci.yml` (YAML, never a grep — `lint` appears in comments and in
    `polyglot_lint`, so a substring match would pass against nothing) and asserts:
    - a job named `lint` exists;
    - one of its steps' `run` invokes `tools.ruff_baseline`;
    - NO step of that job runs a bare repo-wide `ruff check .` (D-10 pinned as a test, so a later
      well-meaning edit that "strengthens" the job into permanent redness fails here);
    - `"lint"` is a member of `gate.needs`;
    - every job defined in the file except `gate` itself is a member of `gate.needs` — a general
      invariant that makes the NEXT job someone forgets to wire in fail too.
  </behavior>
  <action>Model the file on `tools/harness_lint/tests/test_ci_stale_derived.py`, reusing whatever
  YAML loader it already uses (ruamel.yaml is the resolved parser in this workspace; do NOT add
  pyyaml — it is absent from the lock).

  Run the RED selection PLAIN: `uv run pytest tools/harness_lint/tests/test_ci_lint_gate.py -q`.
  Record the verbatim first failure — expect an assertion that no `lint` job exists. Commit.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_ci_lint_gate.py -q 2>&1 | tail -5</automated>
  </verify>
  <done>The test fails for the stated reason (no `lint` job); the verbatim line is recorded; the
  commit touched one file.</done>
</task>

<task type="auto">
  <name>Task 2: Add the lint job and wire it into the fan-in</name>
  <files>.github/workflows/ci.yml</files>
  <behavior>
    A `lint` job after `core-suite`, with a comment block in the file's established voice stating
    what it gates, why the baseline exists, why the job does not run a bare `ruff check .` yet,
    and what an operator does when it goes red (fix the finding, or shrink the baseline with
    `--update` — never grow it). `gate.needs` gains `lint`.
  </behavior>
  <action>Insert:
  ```yaml
    lint:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v7.0.0
        - uses: astral-sh/setup-uv@v8.3.2
        - name: Sync workspace (all packages)
          run: uv sync --all-packages
        - name: Ruff ratchet — no rule class may grow
          run: uv run python -m tools.ruff_baseline
        - name: Ruff baseline unit tests
          run: uv run pytest tools/ruff_baseline -q
  ```
  and add `lint` to `gate.needs`. Keep the pinned action versions byte-identical to the sibling
  jobs and interpolate NOTHING from `github.event.*`.

  Then: `uv run pytest tools/harness_lint/tests/test_ci_lint_gate.py -q` (now green) and
  `uv run check-jsonschema --builtin-schema vendor.github-workflows .github/workflows/ci.yml`.
  Commit.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint -q 2>&1 | tail -3; uv run check-jsonschema --builtin-schema vendor.github-workflows .github/workflows/ci.yml</automated>
  </verify>
  <done>The structural test is green, the workflow validates against the builtin schema, and
  `gate.needs` literally contains `lint`.</done>
</task>

<task type="auto">
  <name>Task 3: Observe the gate failing, then passing</name>
  <files>(none committed — a deliberate transient edit, reverted)</files>
  <behavior>
    A recorded RED->GREEN cycle against the exact command the `lint` job runs.
  </behavior>
  <action>RED: append one deliberately over-long line (> 100 chars) to a tracked, non-vendored
  Python file — use a comment line in `tools/ruff_baseline/ratchet.py` so the injected error is in
  this phase's own code and cannot disturb another phase's file. Run
  `uv run python -m tools.ruff_baseline; echo "exit=$?"`. It MUST exit non-zero and name `E501`
  with baseline 277 vs current 278. Capture the output VERBATIM.

  Then a second RED shape, because the two failure modes are different code paths: introduce a
  finding whose rule code is ABSENT from the baseline (e.g. a bare `except:` -> E722, which the
  vendored exclusion removed from the baseline entirely). It MUST exit non-zero and say the code
  is not in the baseline. Capture verbatim.

  GREEN: `git checkout -- tools/ruff_baseline/ratchet.py` (or revert the edit), re-run the same
  command, confirm exit 0. Capture verbatim. Confirm `git status --porcelain` is empty — the
  observation must leave NOTHING behind.

  Do NOT commit any of the three states. Both RED transcripts and the GREEN transcript go into
  `34-03-SUMMARY.md`.</action>
  <verify>
    <automated>uv run python -m tools.ruff_baseline; echo "exit=$?"; git status --porcelain</automated>
  </verify>
  <done>Both RED transcripts (an increase, and a new rule code) and the GREEN transcript are
  recorded verbatim in the summary; the working tree is clean.</done>
</task>

<task type="auto">
  <name>Task 4: Full-gate re-verification</name>
  <files>(none)</files>
  <action>Run the phase's exit gates and record the actual numbers, never a claim:
  `uv run pytest -q` (>= 1500 passed, 8 snapshots);
  `uv run python -m tools.contract_drift.drift`;
  `uv run python -m tools.harness_emit` then `git status --porcelain` (must be EMPTY — this phase
  touches nothing under `harness/`, so a non-empty status means something unexpected moved);
  `uv run python -m tools.ruff_baseline` (exit 0).
  Commit nothing unless the emit round-trip legitimately produces a diff, in which case stop and
  report rather than committing a surprise.</action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3; uv run python -m tools.contract_drift.drift; uv run python -m tools.harness_emit >/dev/null && git status --porcelain</automated>
  </verify>
  <done>All four gates recorded with their real output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CI workflow definition -> merge authority | a job that is not in the fan-in cannot block a merge |
| a transient RED edit -> the committed tree | a deliberately broken file must not survive the observation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-09 | Repudiation | a `lint` job absent from `gate.needs` | mitigate | asserted by a parsed-YAML test, plus the general "every job except gate is in gate.needs" invariant that catches the next omission too |
| T-34-10 | Tampering | the deliberate RED edit leaking into a commit | mitigate | the edit is confined to this phase's own new file, reverted, and `git status --porcelain` is asserted empty before the phase closes |
| T-34-11 | Spoofing | an unpinned or substituted action | mitigate | action versions copied byte-identically from sibling jobs; no new action introduced |
| T-34-12 | Elevation of Privilege | event-data interpolation into a run shell | mitigate | the job interpolates nothing; top-level `permissions: contents: read` unchanged |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | none — no dependency change in this plan |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint -q` green, including the new CI-structure test.
- `uv run check-jsonschema --builtin-schema vendor.github-workflows .github/workflows/ci.yml` passes.
- `grep -n 'needs: \[setup' .github/workflows/ci.yml` shows `lint` in the list.
- The RED->GREEN transcripts are in `34-03-SUMMARY.md`.
- `uv run pytest -q` >= 1500 passed; `tools.contract_drift` clean; emit round-trip leaves
  `git status --porcelain` empty.
</verification>

<success_criteria>
`lint` is a real, blocking CI job — present in `gate.needs`, asserted structurally, and observed
going red on a new finding and green on its removal.
</success_criteria>

<output>
Create `.planning/phases/34-ruff-required-ci-gate-v2-4-b/34-03-SUMMARY.md` when done, carrying
both verbatim RED transcripts, the GREEN transcript, and the four gate results.
</output>
