---
phase: 43-lifecycle-plane-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/commands/checkpoint.md
  - harness/commands/orient.md
  - harness/commands/review.md
  - harness/commands/verify-work.md
  - harness/agents/orchestrator.md
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-01: the five surviving artifacts (checkpoint, orient, review, verify-work, orchestrator) no longer shell any module the plane deletes"
    - "D-02: repair happens before deletion — this plan's commit lands before any package/skill/contract/CI deletion in this phase"
    - "D-03: repair means removing the lifecycle step, not replacing it — no successor mechanism, no shim, no comment-only stub is added in place of the removed step"
  artifacts:
    - path: "harness/commands/checkpoint.md"
      provides: "Step 1 no longer invokes tools.handoff generate/validate/activate; commit step keeps only the state-plane git add/commit"
    - path: "harness/commands/orient.md"
      provides: "Section 1 (active-handoff resume barrier + /phase-gate routing) removed; read-order step 5 no longer lists a deleted skill"
    - path: "harness/commands/review.md"
      provides: "Step 4 no longer invokes tools.evidence.capture add-finding; finding capture prose removed"
    - path: "harness/commands/verify-work.md"
      provides: "The five-gate list and its evidence-capture wrapper paragraph no longer reference tools.evidence.capture"
    - path: "harness/agents/orchestrator.md"
      provides: "Routes by declared topology/stage only; no tools.capability invocation, no harness/capabilities.toml reference, no discipline/lane language, no adversarial-review-panel routing row"
  key_links: []
---

<objective>
Repair the five surviving harness-source artifacts that D-01 (43-CONTEXT.md) proves still shell or
reference modules this phase's later waves delete: `harness/commands/{checkpoint,orient,review,
verify-work}.md` and `harness/agents/orchestrator.md`. This is Wave 1 of the phase's repair-before-
delete structure (D-02): nothing here deletes a package — it only stops the surviving surface from
invoking the dying plane, so no intermediate commit in this phase ever ships a command that crashes.

Per D-03, repair means *removing* the lifecycle step the command performed, never substituting a
replacement mechanism. `orchestrator.md` additionally loses its capability-routing step (tied to
`harness/capabilities.toml`, deleted in Wave 3 of this phase) and its `adversarial-review-panel`
routing-table row (one of the 5 discipline skills deleted in Wave 3) — both must stop being named
here, in the source, before the emitter re-projects these files in Plan 43-03.

Purpose: keep the surviving product surface coherent through every commit of the phase.
Output: five repaired `harness/**` source files, no emitted-tree changes yet (that is Plan 43-03).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
</context>

<interfaces>
<!-- Exact current-state excerpts the executor needs verbatim; do not re-read these files to find
     the lines below, they are current as of 2026-07-28. -->

From `harness/commands/checkpoint.md` (line 17-22, the block to remove):
```
1. If an active task packet exists, first ensure its task/state/evidence/artifact revision is already
   committed and `state.current_ref` is that `HEAD`. Generate the HANDOFF, then write only its task
   ID/revision/path pointer to `.memory/state/active-task.json`;
   never copy packet, evidence, artifact, contract, or log bodies into `.memory/state/`.

   !`uv run python -m tools.handoff generate .workflow/tasks/<task-id> && uv run python -m tools.handoff validate .workflow/tasks/<task-id> && uv run python -m tools.handoff activate .workflow/tasks/<task-id> --state-dir .memory/state`
```
Step 3 (git add/commit, line 38-48) currently has three conditional `!` lines for
`active-task.json`, the HANDOFF artifact, and a `commit` line that also runs
`tools.handoff validate` after committing — the handoff-specific clauses in all of these go too.

From `harness/commands/orient.md` (line 17-23, section "1. Active HANDOFF first" — remove the whole
section, renumber 2→1, 3→2, 4→3):
```
## 1. Active HANDOFF first (fresh-session resume barrier)

If `.memory/state/active-task.json` exists, it is a pointer only. Validate its HANDOFF and run the
existing Phase 20 gate before any EXECUTE, REVIEW, or VERIFY action. A stale pointer, state
revision, ref, or artifact must fail and be regenerated via `/handoff`; do not auto-correct it.

!`test ! -f .memory/state/active-task.json || uv run python -m tools.handoff resume --state-dir .memory/state --repo-root .`
```
Read-order step 5 (line 51-53) lists `gate-model` (stays, Phase 44) and no discipline skill by
name today — leave it as-is; only the section-1 block above and its cross-reference at line 46
("Active HANDOFF, when surfaced above — validate it, then run `/phase-gate`") need removal.

From `harness/commands/review.md` (line 49-52, the finding-capture paragraph):
```
When the review belongs to an active task packet, record each result only through
`python -m tools.evidence.capture add-finding` after the read-only review completes; do **not** edit
`evidence.json` directly. Each finding requires its `blocker`/`major`/`minor`/`nit` severity, its
`open`/`resolved`/`accepted` disposition, and (for resolved or accepted) an `E-*` capture reference.
```

From `harness/commands/verify-work.md` (line 22-27, the evidence-capture wrapper paragraph under the
intro, and the per-gate capture instruction inside it):
```
When an active `.workflow/tasks/<id>/` packet exists, capture each gate's **existing command**
through `python -m tools.evidence.capture capture --task-dir <task-dir> --gate <gate> -- <existing argv>`.
The adapter is only a child-process recorder: keep the five commands below, their order, and their
presence-safe behavior unchanged. Record the observed `PASSED`, `FAILED`, `SKIPPED`, or `BLOCKED`
result; never convert an announced skip into a pass. Attach the applicable `AC-*` IDs to the capture.
Use `--source ci` only for a CI-produced reference, otherwise retain the default `local` source.
```
The five numbered gate sections (lint, test, contract-check, golden, derived-freshness) and their
`!` commands are untouched — only the evidence-capture wrapper paragraph above them is removed.

From `harness/agents/orchestrator.md`, every line naming a deleted surface (verified 2026-07-28):
- Line 23-25: "...to the specialist whose least-privilege scope fits the work. Routing by capability
  is the rule, not a nicety: what a piece of work needs is a *kind* of agent, and
  `harness/capabilities.toml` is the single place that says which personas may serve each kind."
- Line 46-49 intake numbering: step 2 "Name the capability, then resolve a provider..." (line 52-65,
  including the `tools.capability list` / `tools.capability route` code block and the
  "a lane's discipline record names the agent... `missing required disciplines:...`" sentence) —
  delete this entire numbered step and renumber the remaining steps (3→2, 4→3, 5→4, 6→5, 7→6, 8→7).
- Line 66 (old step 3): "pick the command, and the persona the capability's allowlist resolved to" —
  rephrase to drop "the capability's allowlist resolved to"; route by topology/stage + language only.
- Line 92-96 (routing table intro): "Route on three dimensions, in this order: the **declared
  capability** (which kind of agent — `harness/capabilities.toml` holds the allowlist), the
  **pipeline stage/component**... but the capability allowlist is what says whether the persona you
  landed on is permitted." — rewrite to two dimensions: pipeline stage/component, then language.
- Line 98: "current resolution of the capability registry" → "current resolution of the routing
  table below" (or equivalent; no registry survives to resolve against).
- Table row line 116: `| Review written code | `adversarial-review` capability → **code-reviewer**
  (read-only) | `/review` |` — drop the `` `adversarial-review` capability → `` prefix, keep
  `**code-reviewer** (read-only)`.
- Table row line 117: `| High-risk change needing several review frames (STRICT+ lane requirement) |
  `adversarial-review` capability, one provider per seat | `adversarial-review-panel` skill |` —
  DELETE this entire row (the skill it names is deleted in Wave 3; no successor).
- Table row line 118: `| "Which agents may do this kind of work?" | (self) |
  `uv run python -m tools.capability list` |` — DELETE this entire row.
- Table row line 123: `| Locate code / map unfamiliar area | `reconnaissance` capability →
  **explorer** | (read-only search) |` — drop the `` `reconnaissance` capability → `` prefix, keep
  `**explorer**`.
- Line 128-129 (closing sentence): "Where a capability declares an allowlist, that allowlist is the
  boundary — a route outside it is refused by the lifecycle machinery, not merely discouraged here."
  — DELETE (no allowlist machinery survives).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Repair the four surviving commands (D-01, D-02, D-03)</name>
  <files>harness/commands/checkpoint.md, harness/commands/orient.md, harness/commands/review.md, harness/commands/verify-work.md</files>
  <read_first>
    The `<interfaces>` block above (exact current line ranges for all four files) and
    `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-01/D-02/D-03.
  </read_first>
  <action>
    In `checkpoint.md`: delete step 1's handoff-generation paragraph and its `!` command (line
    17-22); renumber step 2→1 and step 3→2; in the renumbered step 2 (old step 3), drop the two
    conditional `!` lines that add `active-task.json` and the HANDOFF revision file, and drop the
    ` && uv run python -m tools.handoff validate ...` clause from the commit line, leaving only the
    plain `git add` + `git commit` sequence for `activeContext.md`/`progress.md`. Update the file's
    intro prose (line 17-20) if it still describes the removed handoff step.
    In `orient.md`: delete the entire "## 1. Active HANDOFF first" section (line 17-23) including
    its `!` command; renumber sections 2→1, 3→2, 4→3; remove the cross-reference to it in the new
    step 2 (old step 4)'s read-order item 2 ("Active HANDOFF, when surfaced above — validate it,
    then run `/phase-gate`; it is the highest-priority task pointer...") — either delete that
    read-order line entirely or fold its non-handoff content (if any) into the surrounding items;
    renumber the remaining read-order items accordingly.
    In `review.md`: delete the finding-capture paragraph (line 49-52) that invokes
    `tools.evidence.capture add-finding`; leave step 4's first two sentences (the code-reviewer
    hand-off + re-run-until-clean instruction) intact.
    In `verify-work.md`: delete the evidence-capture wrapper paragraph (line 22-27) that invokes
    `tools.evidence.capture capture`; leave the five numbered gate sections and their `!` commands
    byte-unchanged — this task removes only the paragraph wrapping them, not the gates themselves.
    Do not add any replacement mechanism, comment, or TODO in place of what is removed (D-03) — a
    clean removal, not a stub.
  </action>
  <acceptance_criteria>
    `grep -rn "tools\.handoff\|tools\.evidence" harness/commands/checkpoint.md harness/commands/orient.md harness/commands/review.md harness/commands/verify-work.md` returns no output (exit 1).
    `grep -n "phase-gate" harness/commands/orient.md` returns no output.
    All four files still have valid YAML frontmatter and at least one numbered/lettered step remains in each (files are not emptied).
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repair orchestrator.md — drop capability routing + discipline/panel references (D-01, D-03)</name>
  <files>harness/agents/orchestrator.md</files>
  <read_first>
    The `<interfaces>` block above (every exact line/row this task removes or rewrites) and
    `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-01, D-03.
  </read_first>
  <action>
    Rewrite `harness/agents/orchestrator.md` so it routes purely by declared pipeline
    stage/component (`tools.harness_config`'s `components()`/`pipeline()`, and
    `tools.contract_graph`'s `direct`/`reverse`/`transitive` queries for non-linear topology) and
    language, with NO reference to `tools.capability`, `harness/capabilities.toml`,
    `harness/disciplines.toml`, "lane", "discipline record", or an "allowlist" gate. Specifically:
    remove the "Routing by capability is the rule..." sentence (line 23-25) and the whole numbered
    intake step "Name the capability, then resolve a provider" (including its `tools.capability
    list`/`route` code block and the "a lane's discipline record..." sentence), renumbering the
    remaining intake steps so they run 1-7 with no gap. Rewrite the "Routing decision table" intro
    (line 92-96) to describe two dimensions only — pipeline stage/component, then language — dropping
    "declared capability" and the `harness/capabilities.toml` clause. In the table itself: drop the
    `` `adversarial-review` capability → `` and `` `reconnaissance` capability → `` prefixes from the
    two rows that carry them (keep the persona names), delete the "High-risk change needing several
    review frames (STRICT+ lane requirement)" row and its `adversarial-review-panel` skill reference
    outright (no successor), and delete the "'Which agents may do this kind of work?'" row and its
    `tools.capability list` command. Delete the closing "Where a capability declares an allowlist..."
    sentence (line 128-129). Leave the topology-tracing step (`Trace the topology`, step 5 before
    renumbering) and every other routing-table row untouched — this task removes ONLY
    capability/discipline/panel language, it does not touch the topology or language-boundary logic.
  </action>
  <acceptance_criteria>
    `grep -n "tools\.capability\|capabilities\.toml\|disciplines\.toml\|adversarial-review-panel\|lane's discipline\|missing required disciplines" harness/agents/orchestrator.md` returns no output (exit 1).
    `grep -c "Trace the topology" harness/agents/orchestrator.md` returns exactly `1` (topology-routing step survives untouched).
    The file's YAML frontmatter (`name: orchestrator`, `mode: primary`) is unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Command/agent prose → invoked CLI | Harness-authored Markdown shells `uv run python -m tools.X`; a stale reference to a soon-to-be-deleted module is the only risk this plan mitigates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | Emitted command surface pointing at a deleted module | mitigate | This plan removes every such reference at source before Wave 3 deletes the module (D-02 ordering) |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package install in this phase or plan (deletion-only, no new dependency) |
</threat_model>

<verification>
`grep -rn "tools\.handoff\|tools\.evidence" harness/commands/checkpoint.md harness/commands/orient.md harness/commands/review.md harness/commands/verify-work.md` — exit 1, no output.
`grep -n "tools\.capability\|capabilities\.toml\|disciplines\.toml\|adversarial-review-panel" harness/agents/orchestrator.md` — exit 1, no output.
`uv run pytest --collect-only -q` — still 0 collection errors (this plan touches no Python).
</verification>

<success_criteria>
The five D-01 surviving artifacts no longer shell or name a module this phase deletes in Wave 3.
No replacement mechanism was introduced. Commit lands before any deletion task in this phase.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-01-SUMMARY.md` when done
</output>
