---
phase: 43-lifecycle-plane-removal
plan: 03
type: execute
wave: 2
depends_on: ["43-01", "43-02"]
files_modified:
  - .opencode/command/checkpoint.md
  - .opencode/command/orient.md
  - .opencode/command/review.md
  - .opencode/command/verify-work.md
  - .opencode/agent/orchestrator.md
  - .opencode/opencode.json
  - .claude/commands/checkpoint.md
  - .claude/commands/orient.md
  - .claude/commands/review.md
  - .claude/commands/verify-work.md
  - .claude/agents/orchestrator.md
  - .claude/settings.json
  - AGENTS.md
  - tools/harness_emit/emit-manifest.json
  - tools/harness_emit/merge.py
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-02: repair is fully landed and re-emitted BEFORE any deletion task runs — this plan is the last Wave-1/2 gate before Wave 3 deletion begins"
    - "D-14: every fix reached the emitted trees through the emitter (python -m tools.harness_emit), never by hand-editing .opencode/** or .claude/**"
    - "D-06: RETIRED_SIGNATURES is emptied again once the re-emit that dropped the resume_gate hook group has landed — it is a transient drop mechanism, not a running log"
  artifacts:
    - path: ".claude/settings.json"
      provides: "No PreToolUse hook group shelling tools.hooks.resume_gate"
    - path: "tools/harness_emit/merge.py"
      provides: "RETIRED_SIGNATURES reset to () after the re-emit that used it has landed"
  key_links:
    - from: "harness/commands/checkpoint.md"
      to: ".claude/commands/checkpoint.md"
      via: "python -m tools.harness_emit projection"
      pattern: "harness_emit"
---

<objective>
Land the source repairs from Plans 43-01 and 43-02 into both runtime trees by running the emitter
(D-14), then verify the whole repaired-but-not-yet-deleted state is green — this is the phase gate
between Wave 1 (repair) and Wave 3 (delete): nothing in Wave 3 may run until this plan proves the
surviving surface is fully coherent with nothing still invoking the dying plane.

Per D-06/Phase-41 precedent, `RETIRED_SIGNATURES` is used to drop the `resume_gate` hook group on
this re-emit, then reset to `()` in the same plan once the drop has landed — it is a transient
mechanism, not a permanent retirement log.

Purpose: prove the repair wave is complete and the tree is internally consistent before the
irreversible deletion wave starts.
Output: both `.opencode/` and `.claude/` trees reflect the five repaired commands/agent, no
`resume_gate` hook group in either runtime, `RETIRED_SIGNATURES` back to `()`, full suite green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-emit both runtime trees and reset RETIRED_SIGNATURES</name>
  <files>tools/harness_emit/merge.py</files>
  <read_first>
    `.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md` §"D-06's RETIRED_SIGNATURES
    mechanism"; confirm `tools/harness_emit/merge.py`'s current `RETIRED_SIGNATURES` value is
    `("tools.hooks.resume_gate",)` (set by Plan 43-02) before running the emitter.
  </read_first>
  <action>
    Run `python -m tools.harness_emit`. This projects the five repaired `harness/commands/*.md` +
    `harness/agents/orchestrator.md` files into both `.opencode/` and `.claude/` trees, drops the
    `resume_gate` hook group from `.claude/settings.json` (via `RETIRED_SIGNATURES`), drops
    `harness/plugins/resume-gate.ts` from `.opencode/opencode.json`'s plugin array, and prunes the
    now-orphaned `.opencode/plugin/resume-gate.ts` row from `tools/harness_emit/emit-manifest.json`.
    Do NOT hand-edit any file under `.opencode/` or `.claude/` — every change must come from this
    emitter run (D-14). After the run completes and the resulting diff looks correct
    (`git diff --stat -- .opencode .claude AGENTS.md tools/harness_emit/emit-manifest.json`), edit
    `tools/harness_emit/merge.py` to set `RETIRED_SIGNATURES: tuple[str, ...] = ()` again — the
    signature has done its one job (dropping the group on this re-emit) and per D-06 must not
    persist as a permanent log. Run `python -m tools.harness_emit` a second time and confirm it
    produces an EMPTY diff (idempotent — resetting `RETIRED_SIGNATURES` to `()` must not re-add the
    already-dropped group, because `HARNESS_SIGNATURES` no longer names it either).
  </action>
  <acceptance_criteria>
    `grep -n "resume_gate\|resume-gate" .claude/settings.json .opencode/opencode.json` returns no output.
    `git diff --stat -- .opencode .claude` after the SECOND emitter run is empty (idempotent re-emit).
    `grep -n "RETIRED_SIGNATURES" tools/harness_emit/merge.py` shows `RETIRED_SIGNATURES: tuple[str, ...] = ()`.
    `grep -rn "tools\.handoff\|tools\.evidence\|tools\.capability" .opencode/command .opencode/agent .claude/commands .claude/agents` returns no output.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full-suite verification gate before Wave 3 deletion begins</name>
  <files></files>
  <read_first>
    `.planning/phases/43-lifecycle-plane-removal/43-VALIDATION.md` §"Sampling Rate" and §"Per-Task
    Verification Map" — this task is the "after every wave" full-suite checkpoint.
  </read_first>
  <action>
    Run the full verification set now that Wave 1's repairs are committed and re-emitted, and BEFORE
    any Wave-3 deletion task begins: `uv run pytest -q` (expect green — nothing has been deleted yet,
    only repaired and re-emitted), `uv run python -m tools.contract_drift.drift` (expect exit 0 —
    no contract touched this wave), and a structural sweep confirming the five originally-named
    D-01 artifacts (both source and emitted copies) name nothing this phase will delete. Record the
    pytest pass count in the plan SUMMARY as the pre-deletion baseline Plan 43-04 and 43-05 must not
    regress below (minus the counts of tests this phase intentionally deletes with their modules).
  </action>
  <acceptance_criteria>
    `uv run pytest -q` exits 0.
    `uv run python -m tools.contract_drift.drift` exits 0.
    `grep -rnE "tools\.handoff|tools\.evidence|tools\.capability|resume_gate|resume-gate" harness/ .opencode/ .claude/` returns no output outside `.planning/`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Emitter output vs hand-edited files | Every change in this plan must originate from `python -m tools.harness_emit`, never a direct edit to `.opencode/**`/`.claude/**` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | Hand-editing an emitted file instead of re-running the emitter | mitigate | D-14 explicit rule; acceptance criteria assert an idempotent second re-emit, which would fail if a hand-edit diverged from what the emitter itself produces |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan |
</threat_model>

<verification>
`python -m tools.harness_emit && git diff --stat -- .opencode .claude` — empty after the reset-and-second-run sequence.
`uv run pytest -q` — green.
</verification>

<success_criteria>
Both runtime trees reflect every Wave-1 repair; `RETIRED_SIGNATURES` is back to `()`; full suite
green; nothing on the surviving surface names a module Wave 3 is about to delete.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-03-SUMMARY.md` when done
</output>
