---
phase: 43-lifecycle-plane-removal
plan: 05
type: execute
wave: 4
depends_on: ["43-04"]
files_modified:
  - contracts/harness/task-control/attestation.schema.json
  - contracts/harness/task-control/evidence.schema.json
  - contracts/harness/task-control/handoff.schema.json
  - contracts/harness/task-control/state.schema.json
  - contracts/harness/task-control/task.schema.json
  - contracts/harness/task-control/transitions.json
  - contracts/.hashes/manifest.json
  - tools/contract_hash/hash.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - docs/reference/attestation.md
  - docs/reference/evidence.md
  - docs/reference/handoff.md
  - docs/reference/state.md
  - docs/reference/task.md
  - .memory/derived/contracts-index.md
  - .memory/derived/repo-map.md
  - tools/harness_lint/tests/test_tests_are_isolatable.py
  - .github/workflows/ci.yml
  - uv.lock
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-05: exactly 6 of the 7 task-control contracts are deleted (attestation, evidence, handoff, state, task, transitions); gate-registry.json SURVIVES for Phase 44 (CER-08), and its DATA_CONTRACT_PATHS entry is untouched"
    - "D-10: CI job lifecycle-eval and its fan-in needs entry are deleted, verified by resolving ci.yml as YAML (ruamel.yaml), never by grep"
    - "D-16: uv run pytest -q is green; contract-drift exits 0 against a rebaselined manifest; emit-drift, stale-derived, and the ruff ratchet are clean; uv.lock is refreshed for the 8 removed workspace members"
    - "D-17: no mutation-proof / adversarial-input table is owed — this phase removes gates and adds no control; the only behavioural assertion retained is D-11's activeContext pointer survival"
    - "D-18: whole-phase LOC removed is reported from git diff --stat across all 5 plans of this phase, not estimated, and is expected to be >= 7021"
  artifacts:
    - path: "contracts/harness/task-control"
      provides: "Directory containing only gate-registry.json"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined manifest with no attestation/evidence/handoff/state/task/transitions entry"
    - path: ".github/workflows/ci.yml"
      provides: "No lifecycle-eval job; gate.needs YAML-resolves to exactly 10 entries"
  key_links:
    - from: "tools/contract_hash/hash.py"
      to: "contracts/.hashes/manifest.json"
      via: "uv run python -m tools.contract_hash.hash --write"
      pattern: "DATA_CONTRACT_PATHS"
---

<objective>
Close the phase's remaining deletion surface (D-05, D-10) and its derived-plane / dependency-lock
consequences: the 6 (of 7) task-control contract schemas and a rebaselined hash manifest, the CI
`lifecycle-eval` job and its fan-in entry (YAML-verified, D-10), the `docs_sync` `EXPECTED_PAGES`
shrink for the 5 orphaned reference pages, the `tools/harness_lint/tests/test_tests_are_isolatable.py`
hardcoded-literal repair (Blast-Radius item 5), and the `uv.lock` refresh for the 8 removed workspace
members. This is Wave 4, gated on Plan 43-04's package/skill/command deletion having landed and
re-emitted clean.

`contracts/harness/task-control/gate-registry.json` and its `DATA_CONTRACT_PATHS` entry MUST NOT be
touched here — CER-08/Phase 44 owns that file explicitly (D-05); touching it in this phase would
desynchronize the two phases' commit history and claim Phase 44's scope early.

Purpose: leave the constitution plane, CI, and the committed-derived plane internally consistent
with a deletion-only phase — no dangling contract, no dangling CI dependency, no stale derived page.
Output: `contracts/harness/task-control/` holds only `gate-registry.json`; CI fan-in is 10 entries;
`uv.lock` carries no reference to the 8 deleted workspace members; full suite + all named gates green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md
</context>

<interfaces>
<!-- Verified present 2026-07-28. -->

`tools/contract_hash/hash.py`'s current `DATA_CONTRACT_PATHS` (line 30-34):
```python
DATA_CONTRACT_PATHS = (
    Path("harness/task-control/transitions.json"),   # <-- REMOVE this entry only
    Path("harness/task-control/gate-registry.json"),  # <-- LEAVE untouched (Phase 44 / CER-08)
    Path("harness/security/deny-domains.json"),        # <-- LEAVE untouched (Phase 44)
)
```

`tools/docs_sync/tests/test_docs_sync_determinism.py`'s current `EXPECTED_PAGES` (line 29-43) —
remove exactly `"attestation"`, `"evidence"`, `"handoff"`, `"state"`, `"task"` (5 of 12 entries;
`transitions.json` is not a `.schema.json` file so `docs_sync` never generated a page for it and no
entry for it exists to remove); `"deny-domains"`, `"format-conventions"`, `"greeting"`, `"inventory"`,
`"manifest"`, `"plan"`, `"relationship"` stay untouched (7 remain).

`.github/workflows/ci.yml`'s current `lifecycle-eval` job (line 221-231, plus its bounding comment
banner) and its `gate.needs` entry (line 345):
```yaml
    needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lint, lifecycle-eval, emit-drift, stale-derived, workspace]
```
Removing `lifecycle-eval` from this list leaves 10 entries:
`[setup, lang-tests, contract-check, drift, golden, core-suite, lint, emit-drift, stale-derived, workspace]`.

`tools/harness_lint/tests/test_tests_are_isolatable.py`'s hardcoded literal (line ~125):
```python
for expected in ("tools/harness_lint/tests", "tools/lifecycle_eval/tests"):
    assert expected in found, f"{expected} should be enumerated; scan returned {sorted(found)}"
```
Replace `"tools/lifecycle_eval/tests"` with another surviving `tools/*/tests` directory that uses
the same `tests/conftest.py` root-insertion idiom the file's own docstring names — confirm via
`grep -l sys.path.insert tools/*/tests/conftest.py` at implementation time — or narrow the tuple to
the single surviving element `("tools/harness_lint/tests",)` if no equally-clear replacement is
found. `test_the_scan_finds_the_members_it_claims_to`'s `len(found) >= 10` threshold needs no change
(19 `tools/*/tests` dirs remain after this phase's deletions, still well above 10).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the 6 task-control contracts, rebaseline the hash manifest, shrink EXPECTED_PAGES, regenerate the derived plane (D-05)</name>
  <files>contracts/harness/task-control/attestation.schema.json, contracts/harness/task-control/evidence.schema.json, contracts/harness/task-control/handoff.schema.json, contracts/harness/task-control/state.schema.json, contracts/harness/task-control/task.schema.json, contracts/harness/task-control/transitions.json, contracts/.hashes/manifest.json, tools/contract_hash/hash.py, tools/docs_sync/tests/test_docs_sync_determinism.py, docs/reference/attestation.md, docs/reference/evidence.md, docs/reference/handoff.md, docs/reference/state.md, docs/reference/task.md, .memory/derived/contracts-index.md, .memory/derived/repo-map.md</files>
  <read_first>
    The `<interfaces>` block above and `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md`
    D-05; `.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md` for the exact
    contract-deletion + rebaseline procedure this task repeats.
  </read_first>
  <action>
    `git rm` the 5 `.schema.json` files and `transitions.json` from
    `contracts/harness/task-control/` — leave `gate-registry.json` in place, untouched. Edit
    `tools/contract_hash/hash.py`'s `DATA_CONTRACT_PATHS` to remove exactly the
    `Path("harness/task-control/transitions.json")` entry; leave the `gate-registry.json` and
    `deny-domains.json` entries byte-for-byte untouched. Run
    `uv run python -m tools.contract_hash.hash --write` to rebaseline
    `contracts/.hashes/manifest.json`, then `uv run python -m tools.contract_drift.drift` and
    confirm exit 0. In the same commit, shrink `EXPECTED_PAGES` in
    `test_docs_sync_determinism.py` by the 5 entries named in `<interfaces>`. Run
    `python -m tools.docs_sync` — its prune-then-write deletes the 5 orphaned
    `docs/reference/{attestation,evidence,handoff,state,task}.md` pages automatically; do not
    hand-delete them, let the generator prune. Run
    `python -m tools.memory_regen.contracts_index && python -m tools.memory_regen.repo_map` to
    regenerate `.memory/derived/contracts-index.md` (15→9 contracts) and `.memory/derived/repo-map.md`
    so the committed-derived plane does not go stale (Phase-41 precedent: this must land in-commit,
    not as a deferred sweep). Commit all of the above as one pathspec-scoped commit.
  </action>
  <acceptance_criteria>
    `ls contracts/harness/task-control/` shows only `gate-registry.json`.
    `uv run python -m tools.contract_drift.drift` exits 0.
    `grep -n "attestation\|evidence\|handoff\|state\|task" tools/docs_sync/tests/test_docs_sync_determinism.py` shows none of those 5 tokens inside `EXPECTED_PAGES` (the greeting/format-conventions/etc. entries remain; verify by reading the frozenset literal, not the grep alone).
    `test ! -f docs/reference/attestation.md && test ! -f docs/reference/task.md` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the CI lifecycle-eval job (YAML-verified), repair test_tests_are_isolatable.py, refresh uv.lock (D-10)</name>
  <files>.github/workflows/ci.yml, tools/harness_lint/tests/test_tests_are_isolatable.py, uv.lock</files>
  <read_first>
    The `<interfaces>` block above and `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md`
    D-10, D-16; `.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md`'s `ruamel.yaml`
    fan-in verification one-liner (D-14 precedent).
  </read_first>
  <action>
    Delete the `lifecycle-eval` job block (`ci.yml:221-231` plus its comment banner) and remove
    `lifecycle-eval` from the fan-in `needs` list (`ci.yml:345`) — do not add or remove any other
    entry. Verify the result by resolving the YAML, never by grep: run
    `uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n"`
    and confirm it exits 0 with no assertion error. Repair
    `test_tests_are_isolatable.py`'s hardcoded `"tools/lifecycle_eval/tests"` literal per the
    `<interfaces>` guidance. Run `uv sync --all-packages` (or `uv lock`) to drop the 8 removed
    workspace members (`evidence-adapters`, `harness-capability`, `harness-discipline`,
    `harness-handoff`, `harness-task-control`, `harness-task-packet`, `lifecycle-eval`,
    `risk-router`) from `uv.lock` and confirm it resolves cleanly. Commit all three files together.
    Then run the phase-gate full verification: `uv run pytest -q` (green), `uv run pytest
    --collect-only -q` (0 errors), `emit-drift` (`python -m tools.harness_emit && git diff
    --exit-code`), `stale-derived` (re-run the docs_sync + contracts_index generators and confirm no
    diff), and the ruff ratchet (`uv run python -m tools.ruff_baseline.ratchet` or the CI-equivalent
    invocation — confirm clean, optionally run `--update` per RESEARCH.md's Open Question 1, a
    low-risk hygiene commit since the ratchet can only shrink after a deletion). Finally, report the
    phase's total measured LOC removed via `git diff --stat` against the pre-Phase-43 baseline
    commit (D-18) in the plan SUMMARY — do not estimate it.
  </action>
  <acceptance_criteria>
    `uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n"` exits 0.
    `grep -c "lifecycle-eval\|lifecycle_eval" uv.lock` returns `0`.
    `uv run pytest -q` exits 0.
    `python -m tools.harness_emit && git diff --exit-code` exits 0 (emit-drift clean).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `contracts/.hashes/manifest.json` vs live schema tree | A stale manifest entry for a deleted schema file is an undetected contract-drift-gate mismatch |
| CI `gate.needs` fan-in | A dangling job name in `needs` fails the whole workflow to resolve; verified by parsing YAML, not grep, per D-10 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07 | Tampering | `DATA_CONTRACT_PATHS` accidentally dropping the `gate-registry.json` entry a phase early | mitigate | Explicit interfaces excerpt names the exact single entry to remove; acceptance criteria confirm `gate-registry.json` alone remains on disk |
| T-43-08 | Denial of Service | Dangling `gate.needs` entry breaking CI's YAML resolution for every future PR | mitigate | YAML-resolved assertion (not grep) as the acceptance criterion, per D-10/Phase-41 D-14 precedent |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | This task only removes 8 workspace members from `uv.lock`; no new dependency added |
</threat_model>

<verification>
`uv run python -m tools.contract_drift.drift` — exit 0.
`uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n"` — exit 0.
`uv run pytest -q` — green.
`python -m tools.harness_emit && git diff --exit-code` — exit 0.
</verification>

<success_criteria>
`contracts/harness/task-control/` holds only `gate-registry.json` with a rebaselined manifest; CI
has no `lifecycle-eval` job and a 10-entry YAML-resolved fan-in; `uv.lock` carries no reference to
the 8 deleted workspace members; full suite, contract-drift, emit-drift, stale-derived, and the ruff
ratchet are all clean; the phase's total removed LOC is reported from `git diff --stat`, not
estimated.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-05-SUMMARY.md` when done
</output>
