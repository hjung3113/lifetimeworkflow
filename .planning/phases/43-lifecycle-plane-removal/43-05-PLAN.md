---
phase: 43-lifecycle-plane-removal
plan: 05
type: execute
wave: 4
depends_on: ["43-04"]
files_modified:
  - contracts/harness/task-control/attestation.schema.json
  - contracts/harness/task-control/evidence.schema.json
  - contracts/harness/task-control/handoff.schema.json
  - contracts/harness/task-control/state.schema.json
  - contracts/harness/task-control/task.schema.json
  - contracts/harness/task-control/transitions.json
  - contracts/.hashes/manifest.json
  - tools/contract_hash/hash.py
  - tools/contract_hash/tests/test_hash.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  - docs/reference/attestation.md
  - docs/reference/evidence.md
  - docs/reference/handoff.md
  - docs/reference/state.md
  - docs/reference/task.md
  - .memory/derived/contracts-index.md
  - README.md
  - README.ko.md
  - uv.lock
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-05: exactly 6 of the 7 task-control contracts are deleted (attestation, evidence, handoff, state, task, transitions); gate-registry.json SURVIVES for Phase 44 (CER-08), and its DATA_CONTRACT_PATHS entry is untouched"
    - "D-10 (MOVED to 43-04 Task 1 by SIM-1): the CI lifecycle-eval job block and its jobs.gate.needs entry are deleted in the SAME commit as tools/lifecycle_eval itself, because .github/workflows/ci.yml:233's `python -m tools.lifecycle_eval.runner` is a ref that test_install_completeness.py:116 RESOLVES to a real .py file — a simulation pass measured `AssertionError: tools.lifecycle_eval.runner does not resolve to any real .py file in this checkout` across three commits when the job lagged into this plan. This plan therefore does NOT touch .github/workflows/ci.yml at all; it only READS the already-corrected YAML to regenerate README.md's fan-in prose (W-8)"
    - "D-16: uv run pytest -q is green; contract-drift exits 0 against a rebaselined manifest; emit-drift, stale-derived, and the ruff ratchet are clean; uv.lock is refreshed for the 8 removed workspace members"
    - "D-17: no mutation-proof / adversarial-input table is owed — this phase removes gates and adds no control; the only behavioural assertion retained is D-11's activeContext pointer survival"
    - "D-18: whole-phase LOC removed is reported from git diff --stat across all 5 plans of this phase, not estimated, and is expected to be >= 7021"
    - "BLOCKER-1 (close-out): tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr and tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr — both committed syrupy snapshots that render directly over the contracts tree this plan's Task 1 deletes from — are regenerated IN THE SAME COMMIT as the 6-contract deletion, not left for a later wave"
    - "B-3: tools/contract_hash/tests/test_hash.py's before-set assertion holds ONLY because DATA_CONTRACT_PATHS lists transitions.json; Task 1 removes that entry, so the expected set is narrowed to {gate-registry.json} IN THAT SAME COMMIT — this is the last wave, no later plan can absorb it"
    - "B-5: README.md and README.ko.md instruct the reader to run deleted modules and list a deleted CI job; they are swept HERE because ROADMAP SC-1 and 43-VALIDATION.md's structural-absence path list both exclude README*.md, and no later phase claims them — this is D-01's own failure class escaping every gate"
    - "W-8 (unchanged in substance; the source of truth just lands one plan earlier): README.md:61's hardcoded CI fan-in list is ALREADY WRONG today — it omits `lint`. Task 2 REWRITES the line from the ruamel.yaml-resolved jobs.gate.needs value rather than token-deleting `lifecycle-eval` from it, and the acceptance criterion is a comparison against that resolved list, not a token-absence grep. Because 43-04 Task 1 already removed `lifecycle-eval` from the YAML (SIM-1), the resolver reads 10 needs entries when this plan runs and the regenerated prose is correct by construction. README.ko.md has NO parallel fan-in line (verified absent) — do not add one"
  artifacts:
    - path: "contracts/harness/task-control"
      provides: "Directory containing only gate-registry.json"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined manifest with no attestation/evidence/handoff/state/task/transitions entry"
    - path: "tools/contract_hash/tests/test_hash.py"
      provides: "test_build_manifest_includes_ratified_data_contracts_and_detects_registry_mutation expecting only contracts/harness/task-control/gate-registry.json, with the transitions.json fixture retained as a negative control proving it is no longer picked up"
    - path: "tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr"
      provides: "Regenerated render() snapshot with no attestation/evidence/handoff/state/task page block"
    - path: "tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr"
      provides: "Regenerated contracts-index snapshot listing 9 contracts (15 minus the 6 deleted)"
    - path: "README.md"
      provides: "No runnable instruction naming a deleted module; a CI fan-in line REGENERATED from the YAML-resolved gate.needs (10 needs + gate = 11 tokens, including the `lint` job the current line wrongly omits); no /intake//phase-gate//handoff in the command tour; and the v2.2 milestone block recorded as removed-in-v2.5 without naming a deleted path"
    - path: "README.ko.md"
      provides: "Same sweep, Korean: the ## v2.2 plane section, the risk_router/lifecycle_eval quickstart block, and the repository-layout lines naming deleted packages/files are gone. No fan-in line exists in this file and none is added"
  key_links:
    - from: "tools/contract_hash/hash.py"
      to: "contracts/.hashes/manifest.json"
      via: "uv run python -m tools.contract_hash.hash --write"
      pattern: "DATA_CONTRACT_PATHS"
    - from: ".github/workflows/ci.yml"
      to: "README.md"
      via: "jobs.gate.needs resolved with ruamel.yaml (READ-ONLY here — 43-04 Task 1 already edited the YAML), then rendered into the fan-in prose"
      pattern: "jobs\\]\\[.gate.\\]\\[.needs.\\]"
---

<objective>
Close the phase's remaining deletion surface (D-05) and its derived-plane / dependency-lock
consequences: the 6 (of 7) task-control contract schemas and a rebaselined hash manifest, the
`docs_sync` `EXPECTED_PAGES`
shrink for the 5 orphaned reference pages, the `uv.lock` refresh for the 8 removed workspace members,
the two committed syrupy snapshots (`docs_sync`, `contracts_index`) that render directly over the
contracts tree this plan edits, the `contract_hash` test whose expected data-contract set this plan's
own `DATA_CONTRACT_PATHS` edit invalidates (B-3), and the two READMEs that still tell a reader to run
deleted modules (B-5). This is Wave 4, gated on Plan 43-04's package/skill/command deletion having
landed and re-emitted clean, with a FULL `uv run pytest -q` green (not `--collect-only` alone) and
the `test_tests_are_isolatable.py` and `test_install_completeness.py` literals already repaired there
(BLOCKER-2, B-2 and SIM-1 — all three ride with `tools/lifecycle_eval`'s own deletion commit in
43-04, not here; do not re-touch those files in this plan).

**Scope change this revision (SIM-1): `.github/workflows/ci.yml` is NO LONGER this plan's to edit.**
The `lifecycle-eval` job block and its `jobs.gate.needs` entry moved into 43-04 Task 1's commit,
because `ci.yml:233`'s `python -m tools.lifecycle_eval.runner` is a ref that
`tools/adoption_scan/tests/test_install_completeness.py:116` resolves to a real `.py` file, and a
simulation pass measured three consecutive red commits (`AssertionError: tools.lifecycle_eval.runner
does not resolve to any real .py file in this checkout`) while the job lagged into this plan. This
plan now only READS the already-corrected YAML — with `ruamel.yaml`, per D-10's rule — to regenerate
`README.md`'s hand-transcribed fan-in prose. Do not delete anything from `ci.yml` here; there is
nothing left to delete, and the resolver assertion in Task 2 will confirm it.

`contracts/harness/task-control/gate-registry.json` and its `DATA_CONTRACT_PATHS` entry MUST NOT be
touched here — CER-08/Phase 44 owns that file explicitly (D-05); touching it in this phase would
desynchronize the two phases' commit history and claim Phase 44's scope early.

**Revision note (W-8) — README.md's fan-in list is already wrong, so it is regenerated, not edited.**
`README.md:61` currently reads `setup, lang-tests, contract-check, drift, golden, core-suite,
emit-drift, stale-derived, workspace, lifecycle-eval, gate` — it **omits `lint`**, which IS in
`.github/workflows/ci.yml`'s `jobs.gate.needs` (live-verified at HEAD: 11 needs entries; **10** by the
time this plan runs, since 43-04 Task 1 already removed `lifecycle-eval` per SIM-1). Deleting the
`lifecycle-eval` token from the existing prose, as a previous revision instructed, would leave a
9-needs list where the resolved value is 10 — preserving today's error and adding a second one. This
is D-10's rule ("resolve the YAML, never grep") surfacing in prose instead of in a test. Task 2
therefore rewrites the line from the resolved value and asserts against it. `README.ko.md` has no
parallel fan-in line (verified absent 2026-07-28) — do not add one.

Purpose: leave the constitution plane, CI, the committed-derived plane, and the project's own
front-door documentation internally consistent with a deletion-only phase — no dangling contract, no
dangling CI dependency, no stale derived page, no stale committed snapshot, no README instructing a
reader to run something that no longer exists or reciting a fan-in that never matched CI.
Output: `contracts/harness/task-control/` holds only `gate-registry.json`; `README.md`'s fan-in prose
is byte-equal to the (already 10-entry) YAML-resolved `gate.needs` plus `gate`;
`uv.lock` carries no reference to the 8 deleted workspace members; the `docs_sync` and
`contracts_index` syrupy snapshots and the `contract_hash` expected-set are regenerated/narrowed
alongside the deletion that invalidated them; both READMEs are swept; full suite + all named gates
green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md
</context>

<interfaces>
<!-- Verified present 2026-07-28. -->

`tools/contract_hash/hash.py`'s current `DATA_CONTRACT_PATHS` (line 30-34):
```python
DATA_CONTRACT_PATHS = (
    Path("harness/task-control/transitions.json"),   # <-- REMOVE this entry only
    Path("harness/task-control/gate-registry.json"),  # <-- LEAVE untouched (Phase 44 / CER-08)
    Path("harness/security/deny-domains.json"),        # <-- LEAVE untouched (Phase 44)
)
```

**B-3 — `tools/contract_hash/tests/test_hash.py:93-101`**, the test that assertion feeds:
```python
def test_build_manifest_includes_ratified_data_contracts_and_detects_registry_mutation(tmp_path: Path) -> None:
    contracts = tmp_path / "contracts"
    directory = contracts / "harness" / "task-control"; directory.mkdir(parents=True)
    (directory / "transitions.json").write_text('{"version": 1, "phases": [], "lanes": {}}\n', encoding="utf-8")
    registry = directory / "gate-registry.json"; registry.write_text('{"version": "v1", "gates": {}}\n', encoding="utf-8")
    before = build_manifest(contracts)
    assert set(before) == {"contracts/harness/task-control/transitions.json", "contracts/harness/task-control/gate-registry.json"}
    registry.write_text('{"version": "v1", "gates": {"trivial": {}}}\n', encoding="utf-8")
    assert build_manifest(contracts) != before
```
The fixture is `tmp_path`-scoped, but `build_manifest()` decides WHICH non-`*.schema.json` data
contracts to include by consulting the module-level `DATA_CONTRACT_PATHS` tuple — the same tuple this
task edits. So the moment `Path("harness/task-control/transitions.json")` leaves that tuple, the
fixture's `transitions.json` stops being picked up and `set(before)` becomes
`{"contracts/harness/task-control/gate-registry.json"}` → the `:99` assertion goes RED. This is the
last wave of the phase; no later plan can absorb it, so it is repaired in Task 1's commit.
Narrow `:99` to the single surviving element:
```python
    assert set(before) == {"contracts/harness/task-control/gate-registry.json"}
```
**KEEP the `transitions.json` fixture write at `:96`** and add an explicit negative-control assertion
next to the narrowed one, e.g.
`assert "contracts/harness/task-control/transitions.json" not in before` — the file is still written
to the fixture tree, so the test now proves the entry was genuinely dropped from `DATA_CONTRACT_PATHS`
rather than merely proving the fixture no longer creates it. Do not touch the registry-mutation half
of the test (`:100-101`): `gate-registry.json` survives this phase, so that half is unchanged.

`tools/docs_sync/tests/test_docs_sync_determinism.py`'s current `EXPECTED_PAGES` (line 29-43) —
remove exactly `"attestation"`, `"evidence"`, `"handoff"`, `"state"`, `"task"` (5 of 12 entries;
`transitions.json` is not a `.schema.json` file so `docs_sync` never generated a page for it and no
entry for it exists to remove); `"deny-domains"`, `"format-conventions"`, `"greeting"`, `"inventory"`,
`"manifest"`, `"plan"`, `"relationship"` stay untouched (7 remain).

**D-10 / W-8 — `.github/workflows/ci.yml` is READ-ONLY in this plan (SIM-1).** The `lifecycle-eval`
job block (`:221-235`, banner included) and its `jobs.gate.needs` entry (`:345`) were deleted by
**43-04 Task 1**, in the same commit as `tools/lifecycle_eval` itself. Reason: `ci.yml:233`'s
`python -m tools.lifecycle_eval.runner` was the last discovery site for that package, and
`tools/adoption_scan/tests/test_install_completeness.py:116`
(`test_every_referenced_tools_module_lands_in_applied_target`) does not merely COUNT refs — it
RESOLVES each one to a real `.py` file via `_resolve_module_file` (`:49-68`), which raises at `:66`
when a ref resolves to nothing. A simulation pass measured the failure across three commits:
`AssertionError: tools.lifecycle_eval.runner does not resolve to any real .py file in this checkout`.
Counts tolerate a one-plan gap; resolution does not.

What THIS plan does with the file: nothing but read it. `jobs.gate.needs` already resolves to **10**
entries when this plan runs —
`[setup, lang-tests, contract-check, drift, golden, core-suite, lint, emit-drift, stale-derived, workspace]`
— and Task 2 uses that resolved value as the source for `README.md:61`'s regenerated fan-in prose
(W-8). Do NOT put `.github/workflows/ci.yml` in any pathspec in this plan.
(`test_ci_lint_gate.py::test_every_job_is_in_the_fan_in` stayed green through 43-04 because the job
and its `needs` entry moved together — SIM-2, simulation-measured at 6 passed.
`test_ci_lint_gate.py:10` still mentions `lifecycle-eval` in a module docstring; it asserts nothing,
and 43-04's `<surviving_residue>` records it as deliberate.)

`tools/docs_sync/tests/test_docs_sync_determinism.py::test_render_matches_committed_snapshot` and
`tools/memory_regen/tests/test_contracts_index.py::test_render_matches_committed_snapshot` both
render `render()` over the REAL, live `contracts/` tree and compare to a committed `.ambr` under
`__snapshots__/` — the same idiom as `test_emit_determinism.py`'s committed snapshot that Plans
43-01, 43-03 and 43-04 each had to regenerate. This plan's Task 1 deletes 6 files from
`contracts/harness/task-control/`, which changes what both `render()` calls produce, so both
snapshots go stale the instant the `git rm` lands, in this same task, before any doc/manifest edit
even runs. Regenerate both with `--snapshot-update` in the same commit as the schema deletion — do
not defer either to a later wave (there is no later wave in this phase).

**W-5 — `.memory/derived/repo-map.md` is NOT git-tracked.** `git ls-files .memory/` returns only
`README.md`, `agreements/README.md`, `agreements/_TEMPLATE.md`, `derived/contracts-index.md`,
`state/activeContext.md`, `state/progress.md`. Including `repo-map.md` in a `git commit -- <pathspec>`
is a silent no-op, so it has been removed from this plan's `files_modified` and from Task 1's commit
pathspec. Still RUN `python -m tools.memory_regen.repo_map` (it keeps the local SessionStart injector
honest and costs nothing), but expect and require zero staged change from it. Only
`.memory/derived/contracts-index.md` is committed from the derived plane.

**B-5 / W-8 — `README.md` / `README.ko.md`: the sites, live-verified 2026-07-28.** These files sit
outside BOTH the ROADMAP SC-1 sweep and `43-VALIDATION.md`'s structural-absence path list
(`tools/ harness/ contracts/ .github/ .claude/ .opencode/ AGENTS.md .memory/`), and no later phase
claims them — which is exactly how this survived two rounds of review. This revision also adds
`README*.md` to `43-VALIDATION.md`'s path list so the sweep catches it from here on.

`README.md`:
- `:12` — badge `[![tests](https://img.shields.io/badge/tests-904%20passing-...)]`; `:108` — the
  comment `# 2. Run the full harness test suite  (904 passing)`. Both hardcode a pass count this
  phase changes. Update both from THIS PLAN's own measured `uv run pytest -q` result in Task 2 — do
  not guess, and do not leave them stale.
- `:60` — the whole `| 🚦 | **Adaptive Task Control Plane** *(v2.2)* | ... |` feature-table row.
  The feature does not exist after this phase: DELETE the row.
- `:61` — **W-8, regenerate; do not token-edit.** The line currently reads:
  `| 🚦 | **CI fan-in** | A multi-job matrix (`setup, lang-tests, contract-check, drift, golden,
  core-suite, emit-drift, stale-derived, workspace, lifecycle-eval, gate`) all green before merge. |`
  It is missing `lint`, which is present in `jobs.gate.needs`. Do NOT delete the `lifecycle-eval`
  token from it; instead REPLACE the parenthesised list wholesale with the post-deletion
  YAML-resolved `jobs.gate.needs` value followed by `gate`, i.e.
  `setup, lang-tests, contract-check, drift, golden, core-suite, lint, emit-drift, stale-derived,
  workspace, gate` — 10 needs + `gate` = 11 tokens, in `needs` order. Derive it by running the
  resolver in Task 2's action, not by hand-typing from this plan.
- `:123-126` — the command tour. Remove `and the v2.2 task-control surface `/intake`, `/phase-gate`,
  `/handoff`` and fold `/checkpoint`, `/review` into the surviving list. `/pipeline` STAYS (Phase 44).
- `:128-132` — the entire second fenced `bash` block (`# Task Control Plane (v2.2): ...` +
  `tools.risk_router` + `tools.lifecycle_eval.runner`). Both its runnable lines invoke deleted
  modules, so the block goes in full, including its comment header.
- `:205-221` — the `<details open>` milestone-history block `✅ v2.2 — Adaptive Task Control Plane
  (Phases 18–23)` and its 6 bullets (A–F). This is SHIPPED HISTORY, so do not falsify it by deleting
  the record — but its bullets enumerate live paths (`.workflow/tasks/`, `risk-policy.toml`,
  `/intake`, `/phase-gate`, `resume_gate`, CI `lifecycle-eval`, `docs/how-to/task-lifecycle.md`) that
  no longer exist and that the extended VALIDATION sweep now matches. Collapse the six bullets to a
  short factual note that the plane shipped in v2.2 and was removed in v2.5 under CER-07, naming NO
  path this phase deletes, and change the block's `✅` marker accordingly. Do not rewrite the v1.0 /
  v2.0 / v2.1 blocks.
- `:134-156` (repository layout) — verified CLEAN, no dying-surface reference. Leave untouched.

`README.ko.md`:
- **No CI fan-in line exists in this file** — verified absent 2026-07-28 (`core-suite` / `emit-drift`
  / `setup, lang-tests` appear nowhere in it; only a generic `emit-drift` mention at `:46` describing
  the emit gate, which stays). Do NOT add a fan-in line, and do not "mirror" README.md `:61` here.
- `:51-98` — the entire `## v2.2 — Adaptive Task Control Plane` section and all six `###`
  subsections (Task Packet 계약 / Deterministic Risk Router incl. its lane table / Atomic State
  Manager + Phase Gate / Evidence Bundle Adapters / Handoff + Fresh-Session Resume / Lifecycle
  Evaluation). Unlike README.md's `<details>` block this is presented as CURRENT architecture, not
  history — DELETE the section outright; `## 빠른 시작` (`:99`) becomes the next heading.
- `:108` — `# 2. 전체 하네스 테스트 스위트 (904 통과)`; update from the measured count, as with
  README.md.
- `:118-120` — the quickstart block's `# 5. Task Control Plane: 위험 레인 라우팅 + lifecycle 증명`
  comment plus the `tools.risk_router` and `tools.lifecycle_eval.runner` lines. Delete all three;
  keep steps 1–4 and renumber only if the file numbers them contiguously.
- `:128` — the layout line `risk-policy.toml  #   v2.2 위험 라우팅 정책 ...`. Delete.
- `:131` — `harness/task-control/  # v2.2: task·state·evidence·handoff·transitions·gate-registry·attestation`.
  After this plan only `gate-registry.json` remains: rewrite to name only `gate-registry`.
- `:135` — `tools/  # Python 도구: risk_router·task_control·evidence·handoff·lifecycle_eval·` —
  remove the five deleted package names from the list, keep the surviving ones and the continuation
  line.
- `:150` — the 로드맵 bullet `✅ **v2.2 — Adaptive Task Control Plane (Phase 18–23)**: 위 6개 phase.
  TCP-01..18 완료, ADR-0008 비준.` — it now points at a section this task deletes ("위 6개 phase").
  Rewrite it to the same shipped-then-removed-in-v2.5 note used in README.md, naming no deleted path.
- `:133`'s `how-to/task-lifecycle.md` reference STAYS — that file survives this phase (see the
  carry-forward note below); only its prose is stale, and that is Phase 45's.

**NOTE (BLOCKER-2 / B-2 / SIM-1, all moved into 43-04):** three things that earlier revisions placed
here now ride with 43-04 Task 1's deletion commit, and NONE of them may be re-touched in this plan or
appear in its `files_modified`:
1. `tools/harness_lint/tests/test_tests_are_isolatable.py`'s `"tools/lifecycle_eval/tests"` code
   literal (BLOCKER-2).
2. `tools/adoption_scan/tests/test_install_completeness.py`'s `>= 20` sanity floor, now `>= 12` (B-2).
3. **`.github/workflows/ci.yml`'s `lifecycle-eval` job block and its `jobs.gate.needs` entry**
   (SIM-1) — see the D-10 block above for the measured red that forced the move.
Because (3) moved, the module-discovery count reaches its final **13** at 43-04's commit rather than
here, and `>= 12` clears it with margin. This plan changes that count by nothing at all. No floor
edit and no CI edit is owed here — confirm by running
`uv run pytest tools/adoption_scan/tests/test_install_completeness.py -q` (both tests, exit 0), not by
editing anything.

**CARRY-FORWARD (W-2) — do NOT fix in this phase.** Three human-owned Diátaxis docs survive this
phase describing the removed plane in stale prose:
`docs/how-to/task-lifecycle.md` (106 lines, 11 dying-surface references),
`docs/explanation/next-milestone-task-control-plane.md` (504 lines, 19 references), and
`docs/explanation/task-lifecycle-shadow-metrics.md` (13 lines, 0 direct references but framed on the
same plane). Live check 2026-07-28 found NO markdown links from these three into the
`docs/reference/{task,state,evidence,handoff,attestation}.md` pages this plan prunes — so the
originally-reported broken-link failure mode does not exist; the real residue is stale prose. No link
checker or docs-prose gate exists, and `docs/` is outside every structural sweep in this phase, so
this lands silently either way. It is recorded here and in `43-VALIDATION.md`'s deferred section as
**Phase 45 (projection repair)** scope. Repairing human-owned Diátaxis prose is not a deletion-phase
concern and would grow this phase past its budget.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the 6 task-control contracts, rebaseline the hash manifest, narrow the contract_hash expected set, shrink EXPECTED_PAGES, regenerate the derived plane and both invalidated syrupy snapshots (D-05, B-3, BLOCKER-1 close-out)</name>
  <files>contracts/harness/task-control/attestation.schema.json, contracts/harness/task-control/evidence.schema.json, contracts/harness/task-control/handoff.schema.json, contracts/harness/task-control/state.schema.json, contracts/harness/task-control/task.schema.json, contracts/harness/task-control/transitions.json, contracts/.hashes/manifest.json, tools/contract_hash/hash.py, tools/contract_hash/tests/test_hash.py, tools/docs_sync/tests/test_docs_sync_determinism.py, tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr, tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr, docs/reference/attestation.md, docs/reference/evidence.md, docs/reference/handoff.md, docs/reference/state.md, docs/reference/task.md, .memory/derived/contracts-index.md</files>
  <read_first>
    The `<interfaces>` block above — in particular the B-3 excerpt (why `test_hash.py:99` goes red on
    THIS task's `DATA_CONTRACT_PATHS` edit) and the W-5 note (why `.memory/derived/repo-map.md` is not
    in the commit pathspec) — plus `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-05 and
    `.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md` for the exact contract-deletion +
    rebaseline procedure this task repeats.
  </read_first>
  <action>
    `git rm` the 5 `.schema.json` files and `transitions.json` from
    `contracts/harness/task-control/` — leave `gate-registry.json` in place, untouched. Edit
    `tools/contract_hash/hash.py`'s `DATA_CONTRACT_PATHS` to remove exactly the
    `Path("harness/task-control/transitions.json")` entry; leave the `gate-registry.json` and
    `deny-domains.json` entries byte-for-byte untouched.

    In the SAME commit, repair the test that edit invalidates (B-3): in
    `tools/contract_hash/tests/test_hash.py`, narrow the `:99` expected set of
    `test_build_manifest_includes_ratified_data_contracts_and_detects_registry_mutation` to
    `{"contracts/harness/task-control/gate-registry.json"}`, KEEP the `transitions.json` fixture write
    at `:96`, and add the negative-control assertion
    `assert "contracts/harness/task-control/transitions.json" not in before` so the test now proves
    the entry was dropped from `DATA_CONTRACT_PATHS` rather than merely that the fixture stopped
    creating it. Leave the registry-mutation half (`:100-101`) untouched — `gate-registry.json`
    survives this phase.

    Run `uv run python -m tools.contract_hash.hash --write` to rebaseline
    `contracts/.hashes/manifest.json`, then `uv run python -m tools.contract_drift.drift` and
    confirm exit 0. In the same commit, shrink `EXPECTED_PAGES` in
    `test_docs_sync_determinism.py` by the 5 entries named in `<interfaces>`. Run
    `python -m tools.docs_sync` — its prune-then-write deletes the 5 orphaned
    `docs/reference/{attestation,evidence,handoff,state,task}.md` pages automatically; do not
    hand-delete them, let the generator prune. Run
    `python -m tools.memory_regen.contracts_index && python -m tools.memory_regen.repo_map` to
    regenerate `.memory/derived/contracts-index.md` (15→9 contracts) and the local, UNTRACKED
    `.memory/derived/repo-map.md` so the derived plane does not go stale (Phase-41 precedent: the
    tracked part must land in-commit, not as a deferred sweep). Per W-5, only `contracts-index.md` is
    git-tracked — do not put `repo-map.md` in the commit pathspec and expect it to stage nothing.

    Then, BEFORE committing, regenerate the two committed syrupy snapshots this task's deletion just
    invalidated (BLOCKER-1 close-out — both render over the live `contracts/` tree, the same
    staleness trigger the `<emit_snapshot_rule>` states for `test_emit_determinism.ambr` in Plans
    43-01/43-03/43-04):
    `uv run pytest tools/docs_sync/tests/test_docs_sync_determinism.py --snapshot-update -q` and
    `uv run pytest tools/memory_regen/tests/test_contracts_index.py --snapshot-update -q`. Confirm
    all three affected files pass green on a second, non-updating run:
    `uv run pytest tools/docs_sync/tests/test_docs_sync_determinism.py tools/memory_regen/tests/test_contracts_index.py tools/contract_hash/tests/test_hash.py -q`.

    **Pitfall 4 applies to this task too — expect a RED window between `git rm` and `git commit`.**
    `tools/adoption_scan` derives its catalog from `git ls-files`, so while the 6 contract deletions
    are staged-but-not-committed, three `adoption_scan` tests are red on top of the ones above:
    `tools/adoption_scan/tests/test_dispositions.py::test_catalog_invariant_to_untracked_local_state`,
    `tools/adoption_scan/tests/test_plan_classification.py::test_contract_candidate_matches_real_repo_schema_count`,
    and `tools/adoption_scan/tests/test_scan_exclusions.py::test_ci_yml_false_positive_closed`. A
    simulation pass confirmed all three clear the moment the commit lands. This is the SAME window
    43-04 documents in its objective (`git ls-files` sees a staged-only deletion as still tracked) —
    it is expected state, not a defect, and it must NOT trigger an amend-if-red loop or a rollback.
    Therefore: run the three scoped test files named above (`docs_sync`, `contracts_index`,
    `contract_hash`) before committing, but do NOT gate the commit on a full `uv run pytest -q` — run
    the full suite AFTER the commit, where the acceptance criterion places it.

    Commit all of the above — including both regenerated `.ambr` files and the repaired
    `test_hash.py` — as one pathspec-scoped commit with `git commit -- <the exact path list in this
    task's <files>>`; never `git add -A`, `git add .`, `git commit -a`, or `git checkout <ref> -- .`.
    Inspect `git diff --cached --name-only` first (D-13) and confirm neither `.ambr` is left
    uncommitted afterward. (Per 43-04's W-9, ` M uv.lock` is still dirty in this window — it is
    Task 2's to commit and must not enter this pathspec.)
  </action>
  <acceptance_criteria>
    `ls contracts/harness/task-control/` shows only `gate-registry.json`.
    `uv run python -m tools.contract_drift.drift` exits 0.
    B-3: `uv run pytest tools/contract_hash/tests/test_hash.py -q` exits 0; `grep -c "transitions.json" tools/contract_hash/tests/test_hash.py` returns `2` (the retained fixture write + the negative-control assertion), and `grep -n "transitions.json" tools/contract_hash/hash.py` returns no output.
    `grep -n "attestation\|evidence\|handoff\|state\|task" tools/docs_sync/tests/test_docs_sync_determinism.py` shows none of those 5 tokens inside `EXPECTED_PAGES` (the greeting/format-conventions/etc. entries remain; verify by reading the frozenset literal, not the grep alone).
    `test ! -f docs/reference/attestation.md && test ! -f docs/reference/task.md` exits 0.
    `uv run pytest tools/docs_sync/tests/test_docs_sync_determinism.py tools/memory_regen/tests/test_contracts_index.py tools/contract_hash/tests/test_hash.py -q` exits 0.
    `git status --porcelain -- tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr` is empty (both regenerated snapshots committed, not left dirty).
    W-5: `git ls-files .memory/derived/` lists `contracts-index.md` and NOT `repo-map.md` — confirming the pathspec omission was correct, not an oversight.
    `uv run pytest -q` exits 0 — **run AFTER the commit lands**, not before it. Per the Pitfall-4 note in the action, three `adoption_scan` tests (`test_dispositions.py::test_catalog_invariant_to_untracked_local_state`, `test_plan_classification.py::test_contract_candidate_matches_real_repo_schema_count`, `test_scan_exclusions.py::test_ci_yml_false_positive_closed`) are red between `git rm` and `git commit` because the catalog reads `git ls-files`; all three clear on commit. This task ends fully green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerate README's fan-in line from the (already-corrected) resolved YAML, sweep both READMEs, refresh uv.lock, full-suite close-out (B-5, W-8; the CI job deletion itself moved to 43-04 Task 1 per SIM-1)</name>
  <files>README.md, README.ko.md, uv.lock</files>
  <read_first>
    The `<interfaces>` block above — especially the D-10/W-8 `ci.yml` excerpt and the B-5 site-by-site
    list for `README.md` and `README.ko.md` — and
    `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-10, D-16;
    `.planning/phases/41-docs-review-plane-removal/41-04-SUMMARY.md`'s `ruamel.yaml`
    fan-in verification one-liner (D-14 precedent).
  </read_first>
  <action>
    **Do NOT edit `.github/workflows/ci.yml` in this task.** Per SIM-1 the `lifecycle-eval` job block
    and its `jobs.gate.needs` entry were already deleted by 43-04 Task 1, in the same commit as
    `tools/lifecycle_eval`. Start by CONFIRMING that, read-only, by resolving the YAML rather than
    grepping (D-10's rule):
    `uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n; assert 'lifecycle-eval' not in d['jobs'], sorted(d['jobs'])"`
    must exit 0. If it does not, 43-04 Task 1 did not land correctly — STOP and reconcile there; do
    not repair it here, or the deletion and its dependent tests end up in different commits again.

    W-8 — regenerate `README.md:61`'s fan-in list FROM that resolved value, do not token-edit the
    existing prose. The current line omits `lint`, so deleting `lifecycle-eval` from it would produce
    a 9-token list against a resolved 10. Print the truth and paste it:
    `uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); print(', '.join(d['jobs']['gate']['needs'] + ['gate']))"`
    and replace the parenthesised backticked list on that line with exactly that output (expected:
    `setup, lang-tests, contract-check, drift, golden, core-suite, lint, emit-drift, stale-derived,
    workspace, gate`). Leave the rest of the row's prose intact. `README.ko.md` has no fan-in line —
    verified absent; do not add one.

    Then sweep both READMEs (B-5) at every other site enumerated in `<interfaces>`: `README.md`
    `:60`, `:123-126`, `:128-132`, `:205-221`; `README.ko.md` `:51-98`, `:118-120`, `:128`, `:131`,
    `:135`, `:150`. Re-locate each site by content before editing — the line numbers shift as you
    delete. Keep `/pipeline` (Phase 44's), keep `README.ko.md:133`'s `how-to/task-lifecycle.md`
    reference (that file survives; its stale prose is Phase 45's, see the W-2 carry-forward note),
    and do not touch either README's repository-layout block beyond the three `README.ko.md` lines
    named. Where the milestone-history entries are rewritten, record the plane as shipped in v2.2 and
    removed in v2.5 under CER-07 — a factual note that names no deleted path — rather than deleting
    the historical record.

    Run `uv sync --all-packages` (or `uv lock`) to drop the 8 removed workspace members
    (`evidence-adapters`, `harness-capability`, `harness-discipline`, `harness-handoff`,
    `harness-task-control`, `harness-task-packet`, `lifecycle-eval`, `risk-router`) from `uv.lock`
    and confirm it resolves cleanly. Then run the phase-gate full verification: `uv run pytest -q`
    (green), `uv run pytest --collect-only -q` (0 errors), `emit-drift`
    (`python -m tools.harness_emit && git diff --exit-code`, run AFTER this task's commit lands —
    before it, the uncommitted README edits and the `uv.lock` still dirty from 43-04's W-9 make a BARE
    `git diff --exit-code` report dirty for reasons unrelated to emit drift; for a pre-commit signal,
    scope it: `git diff --exit-code -- .opencode .claude opencode.json AGENTS.md CLAUDE.md tools/harness_emit/emit-manifest.json`),
    `stale-derived` (re-run the docs_sync +
    contracts_index generators and confirm no diff), and the ruff ratchet
    (`uv run python -m tools.ruff_baseline.ratchet` or the CI-equivalent invocation — confirm clean,
    optionally run `--update` per RESEARCH.md's Open Question 1, a low-risk hygiene commit since the
    ratchet can only shrink after a deletion). Also confirm — by running, not editing — that
    43-04's `>= 12` module-discovery floor still holds: `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -q`
    exits 0 (BOTH tests in that module) with the live count at **13**. That 13 was already reached at
    43-04 Task 1's commit, since the CI job went with the package (SIM-1); this plan changes the count
    by nothing. If it reads anything else, do NOT adjust the floor — reconcile against 43-04.

    Take the pass count printed by that `uv run pytest -q` run and write it into the two hardcoded
    README test-count sites (`README.md:12` badge + `README.md:108` comment, `README.ko.md:108`
    comment) — measured, never estimated. Commit with
    `git commit -- README.md README.ko.md uv.lock` only (`.github/workflows/ci.yml` is
    deliberately absent — 43-04 Task 1 committed it); never
    `git add -A`, `git add .`, `git commit -a`, or `git checkout <ref> -- .`; inspect
    `git diff --cached --name-only` first (D-13). Finally, report the phase's total measured LOC
    removed via `git diff --stat` against the pre-Phase-43 baseline commit (D-18) in the plan
    SUMMARY — do not estimate it — and restate the W-2 carry-forward (the three surviving
    lifecycle docs) as Phase 45 scope.
  </action>
  <acceptance_criteria>
    SIM-1 (CONFIRMATION of 43-04's edit, not this task's own — `.github/workflows/ci.yml` is not in this task's `<files>` and must not appear in its commit): `uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n; assert 'lifecycle-eval' not in d['jobs'], sorted(d['jobs'])"` exits 0.
    SIM-1: `git log -1 --name-only` for THIS task's commit lists exactly `README.md`, `README.ko.md`, `uv.lock` — `.github/workflows/ci.yml` must NOT appear (it belongs to 43-04 Task 1's commit).
    B-2 close-out (run, do not edit): `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -q` exits 0 — BOTH tests, including `test_every_referenced_tools_module_lands_in_applied_target`, whose ref-resolution is the reason the CI job moved to 43-04 (SIM-1).
    W-8 (comparison against the resolved list, NOT a token-absence grep): `uv run python -c "import re; from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); want=', '.join(d['jobs']['gate']['needs']+['gate']); txt=open('README.md',encoding='utf-8').read(); assert want in txt, want"` exits 0 — the README's fan-in list is byte-equal to the YAML-resolved `gate.needs + gate`, which also proves the pre-existing missing-`lint` error is repaired.
    W-8: `grep -c "core-suite" README.ko.md` returns `0` — no fan-in line was invented in the Korean README.
    `grep -c "lifecycle-eval\|lifecycle_eval" uv.lock` returns `0`.
    B-5: `grep -rnE "tools\.risk_router|tools\.lifecycle_eval|lifecycle-eval|risk_router|task_control|task_packet|tools\.evidence|tools\.handoff|tools\.discipline|tools\.capability|risk-policy\.toml|capabilities\.toml|disciplines\.toml|\.workflow/tasks|/intake|/phase-gate" README.md README.ko.md` returns no output (exit 1).
    B-5: `grep -c "/pipeline" README.md` returns at least `1` (Phase 44's target is untouched by this phase).
    B-5: neither README's hardcoded test count is stale — the number in `README.md:12`'s badge, `README.md`'s `# 2. Run the full harness test suite` comment, and `README.ko.md`'s `# 2. 전체 하네스 테스트 스위트` comment all equal the pass count this task's own `uv run pytest -q` printed.
    B-2: the live discovered count is **13** and 43-04's `>= 12` floor clears it without a second edit anywhere in the phase.
    `uv run pytest -q` exits 0.
    `python -m tools.harness_emit && git diff --exit-code` exits 0 (emit-drift clean).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `contracts/.hashes/manifest.json` vs live schema tree | A stale manifest entry for a deleted schema file is an undetected contract-drift-gate mismatch |
| `DATA_CONTRACT_PATHS` vs the tests that assert its membership | `test_hash.py`'s expected set is derived from that tuple even though its fixture is tmp-scoped — editing the tuple silently invalidates the test in the same commit (B-3) |
| CI `gate.needs` fan-in | A dangling job name in `needs` fails the whole workflow to resolve; verified by parsing YAML, not grep, per D-10 |
| Hand-maintained prose mirroring machine truth | `README.md:61` transcribes `jobs.gate.needs` by hand and has silently drifted (missing `lint`); prose mirrors of resolved config must be REGENERATED from the resolver, never token-edited (W-8) |
| Live-tree-rendering syrupy snapshots vs the contracts tree they render | `test_docs_sync_determinism.ambr` and `test_contracts_index.ambr` both render over the real `contracts/` tree — they go stale the instant Task 1's `git rm` lands, so their regeneration belongs IN that same commit, not a follow-up |
| Project front-door docs vs every structural gate | `README*.md` is outside ROADMAP SC-1's sweep, outside `43-VALIDATION.md`'s path list, and outside `docs/`; a README instructing a reader to run a deleted module is D-01's failure class with no gate above it (B-5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-07 | Tampering | `DATA_CONTRACT_PATHS` accidentally dropping the `gate-registry.json` entry a phase early | mitigate | Explicit interfaces excerpt names the exact single entry to remove; acceptance criteria confirm `gate-registry.json` alone remains on disk and remains the sole expected member in `test_hash.py` |
| T-43-08 | Denial of Service | Dangling `gate.needs` entry breaking CI's YAML resolution for every future PR | mitigate | Job and `needs` entry are deleted together in 43-04 Task 1 (SIM-1/SIM-2), so `test_every_job_is_in_the_fan_in` stays green; this plan re-asserts the resolved state as a read-only confirmation criterion before consuming it for the README prose |
| T-43-25 | Denial of Service | An executor "helpfully" re-deleting the `lifecycle-eval` CI job here after 43-04 already removed it, producing a no-op edit or, worse, touching `jobs.gate.needs` twice and desynchronizing the fan-in | mitigate | `.github/workflows/ci.yml` is removed from this plan's `files_modified`, its `<files>`, and its commit pathspec; the YAML criterion is framed as a CONFIRMATION with an explicit STOP-and-reconcile instruction, and a `git log -1 --name-only` criterion asserts the file is absent from this commit |
| T-43-09 | Denial of Service | A stale committed syrupy snapshot (docs_sync or contracts_index) leaves `uv run pytest -q` red after this plan's own deletion | mitigate | Both snapshots regenerated with `--snapshot-update` and verified green in the SAME commit as Task 1's schema deletion; acceptance criteria assert neither `.ambr` is left dirty |
| T-43-14 | Denial of Service | `test_hash.py:99`'s expected set left at two entries after `transitions.json` leaves `DATA_CONTRACT_PATHS`, leaving the last wave of the phase permanently red with no later plan able to absorb it (B-3) | mitigate | Narrowed in Task 1's own commit; the `transitions.json` fixture write is retained as a negative control so the repair cannot be satisfied vacuously |
| T-43-15 | Spoofing | A shipped README telling a reader to `uv run python -m tools.lifecycle_eval.runner` — a command that no longer exists — misrepresenting the product's own surface (B-5) | mitigate | Both READMEs swept site-by-site in Task 2; `README*.md` added to `43-VALIDATION.md`'s structural-absence path list so future phases inherit the gate |
| T-43-22 | Spoofing | The README's CI fan-in list drifting further from `jobs.gate.needs` — it already omits `lint`, and a token-delete edit would have shipped a 9-entry list against a resolved 10 (W-8) | mitigate | The line is regenerated from the `ruamel.yaml`-resolved value in the action, and the acceptance criterion is a byte-equality comparison against that resolved value rather than a token-absence grep |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | This task only removes 8 workspace members from `uv.lock`; no new dependency added |
</threat_model>

<verification>
`uv run python -m tools.contract_drift.drift` — exit 0.
`uv run pytest tools/contract_hash/tests/test_hash.py -q` — exit 0 (B-3).
`uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n; assert 'lifecycle-eval' not in d['jobs'], sorted(d['jobs'])"` — exit 0 (CONFIRMS 43-04 Task 1's edit; this plan does not modify `ci.yml`).
`uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); want=', '.join(d['jobs']['gate']['needs']+['gate']); assert want in open('README.md',encoding='utf-8').read(), want"` — exit 0 (W-8).
`grep -rnE "tools\.risk_router|tools\.lifecycle_eval|lifecycle-eval|risk_router|task_control|task_packet|tools\.evidence|tools\.handoff|tools\.discipline|tools\.capability|risk-policy\.toml|capabilities\.toml|disciplines\.toml|\.workflow/tasks|/intake|/phase-gate" README.md README.ko.md` — no output (B-5).
`uv run pytest -q` — green.
`python -m tools.harness_emit && git diff --exit-code` — exit 0.
</verification>

<success_criteria>
`contracts/harness/task-control/` holds only `gate-registry.json` with a rebaselined manifest; the
`docs_sync` and `contracts_index` syrupy snapshots reflect the post-deletion contracts tree;
`test_hash.py` expects only `gate-registry.json` and proves `transitions.json` is no longer picked up;
CI's `lifecycle-eval` job and fan-in entry are confirmed already gone (deleted in 43-04 Task 1 per
SIM-1; this plan does not touch `.github/workflows/ci.yml`) with a 10-entry YAML-resolved fan-in;
`README.md`'s fan-in line is
byte-equal to that resolved list plus `gate` (so the pre-existing missing-`lint` drift is repaired)
and `README.ko.md` gained no fan-in line; both READMEs name no deleted module, command, package, or
CI job and carry a measured test count; `uv.lock` carries no reference to the 8 deleted workspace
members; full suite, contract-drift, emit-drift, stale-derived, and the ruff ratchet are all clean;
the phase's total removed LOC is reported from `git diff --stat`, not estimated; the W-2 docs
carry-forward is restated as Phase 45 scope.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-05-SUMMARY.md` when done
</output>
