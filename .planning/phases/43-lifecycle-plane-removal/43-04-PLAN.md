---
phase: 43-lifecycle-plane-removal
plan: 04
type: execute
wave: 3
depends_on: ["43-03"]
files_modified:
  - tools/task_control
  - tools/task_packet
  - tools/risk_router
  - tools/evidence
  - tools/handoff
  - tools/discipline
  - tools/capability
  - tools/lifecycle_eval
  - tools/hooks/resume_gate.py
  - harness/plugins/resume-gate.ts
  - harness/commands/intake.md
  - harness/commands/phase-gate.md
  - harness/commands/handoff.md
  - harness/commands/discipline.md
  - harness/skills/clarify
  - harness/skills/diagnose
  - harness/skills/domain-modeling
  - harness/skills/test-driven-change
  - harness/skills/adversarial-review-panel
  - harness/capabilities.toml
  - harness/disciplines.toml
  - harness/risk-policy.toml
  - .workflow/tasks
  - tools/harness_lint/caps.py
  - tools/harness_lint/tests/test_capability_wiring.py
  - tools/harness_lint/tests/test_discipline_wiring.py
  - tools/harness_lint/tests/test_tests_are_isolatable.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - tools/adoption_scan/tests/test_install_completeness.py
  - tools/adoption_scan/destinations.py
  - .github/workflows/ci.yml
  - .opencode
  - .claude
  - AGENTS.md
  - tools/harness_emit/emit-manifest.json
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-04: the 8 packages delete as one unit, in one commit — no leaf-first order exists because they are mutually referential"
    - "D-07: EXPECTED_SKILLS shrinks by exactly 5 in the SAME commit as the 5 discipline-skill deletions, because the emitter hard-fails before writing a byte on any mismatch"
    - "D-08: the 3 harness/*.toml declarations delete together with the wiring tests that die with them (test_capability_wiring.py, test_discipline_wiring.py)"
    - "D-09: .workflow/tasks/ (6 git-tracked files, including a real historical task packet) deletes as tracked state, not preserved outside git"
    - "D-12/D-13: delete/edit -> git add -> git commit -- <pathspec> -> verify -> amend-if-red; never git add -A/git add ./git commit -a; git diff --cached --name-only inspected first"
    - "W-4 (phase-wide RULE, see <emit_snapshot_rule>): this task edits harness/commands/, harness/skills/ and harness/plugins/, so tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr is regenerated in THAT SAME commit. The trigger is any edit under harness/agents|commands|skills or harness/opencode.json — not merely the 4 command / 5 skill deletions"
    - "BLOCKER-2: test_tests_are_isolatable.py's hardcoded \"tools/lifecycle_eval/tests\" literal is repaired IN THE SAME COMMIT as tools/lifecycle_eval's deletion (RESEARCH.md's own recommended sequencing), not deferred to Plan 43-05 — a repair sequenced after the deletion it depends on leaves the suite red on a runtime assertion that --collect-only cannot catch"
    - "SIM-1 (this revision, the reason the CI job moved in): test_install_completeness.py:116 test_every_referenced_tools_module_lands_in_applied_target RESOLVES every ref _discover_module_refs finds — including refs from .github/workflows/*.yml — to a real .py file, and _resolve_module_file:66 raises AssertionError when one resolves to nothing. Deleting tools/lifecycle_eval while .github/workflows/ci.yml:233 still runs `uv run python -m tools.lifecycle_eval.runner` makes that ref unresolvable. A simulation pass measured the failure directly: `AssertionError: tools.lifecycle_eval.runner does not resolve to any real .py file in this checkout`. The CI lifecycle-eval job block AND its jobs.gate.needs entry are therefore deleted in THIS task's commit, not in 43-05 Task 2 — the resolution coupling, unlike the count floor, cannot survive a one-plan gap"
    - "B-2 (recomputed for SIM-1): test_install_completeness.py's `>= 20` top-level-package sanity floor is lowered in this same deletion commit. Live measurement: 21 packages today, 20 after Wave 1, and 13 at this task's commit now that the CI job goes with the package deletion (previously 14, because the CI job lagged one plan behind). The floor is set to 12, which clears 13 with a real margin; 43-05's close-out check also reads 13, so no second floor edit is owed anywhere in the phase"
    - "SIM-2: the CI job block and its jobs.gate.needs entry MUST move together. test_ci_lint_gate.py::test_every_job_is_in_the_fan_in asserts set(jobs) - {gate} <= set(gate.needs); the simulation confirmed 6 passed when both move in one commit, and it goes red if only one of the two is deleted"
    - "W-9 (expected, not a defect): `uv run` re-locks the moment the 8 uv workspace members vanish, so ` M uv.lock` appears the instant this task's deletion lands and stays uncommitted until 43-05 Task 2 (which owns uv.lock). No test reads uv.lock, so it is not a red — but every UNSCOPED `git status`/`git diff --exit-code` in this window will report dirty. Use pathspec-scoped forms only"
    - "W-3: tools/adoption_scan/destinations.py's `harness/risk-policy.toml` _CATEGORY_GLOBS entry is dropped here — the hyphenated spelling escapes every underscore-based sweep regex in this phase, the exact hyphen-vs-underscore miss that bit Phase 42"
    - "W-7 (this revision): the phase's repo-wide structural sweep is scoped to EXECUTABLE invocations plus the product surface. Four surviving files legitimately keep the bare tokens in prose/fixtures and must not be edited (see <surviving_residue>); an unqualified repo-wide grep is unsatisfiable, which is the T-43-12 failure class"
  artifacts:
    - path: "tools/harness_lint/caps.py"
      provides: "EXPECTED_SKILLS with no clarify/diagnose/domain-modeling/test-driven-change/adversarial-review-panel entry"
    - path: ".opencode"
      provides: "No emitted command/agent/skill/plugin naming a deleted surface, re-projected via the emitter (not hand-edited)"
    - path: "tools/harness_emit/tests/test_coexist.py"
      provides: "Both command-count assertions read 21 (down from the live-verified 25), and the test function name no longer claims a stale count"
    - path: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      provides: "Regenerated projection snapshot with no /intake, /phase-gate, /handoff, /discipline command block and no deleted-skill block"
    - path: "tools/adoption_scan/tests/test_install_completeness.py"
      provides: "Sanity floor lowered from 20 to 12 with the phase-history rationale in its docstring, and its name/message no longer claiming 'twenty'"
    - path: "tools/adoption_scan/destinations.py"
      provides: "_CATEGORY_GLOBS with no harness/risk-policy.toml entry"
    - path: ".github/workflows/ci.yml"
      provides: "No lifecycle-eval job block and no lifecycle-eval entry in jobs.gate.needs (10 needs entries, YAML-resolved) — moved here from 43-05 Task 2 so tools/lifecycle_eval's deletion never leaves an unresolvable module ref (SIM-1)"
  key_links: []
---

<objective>
Delete the lifecycle plane's runtime surface as one unit (D-04): the 8 mutually-referential `tools/`
packages (7021 LOC), the `resume_gate` hook source, the 4 remaining task-control commands, the 5
discipline skills, the 3 `harness/*.toml` declarations and the two wiring tests that die with them,
and `.workflow/tasks/`'s 6 tracked files — then re-emit so both runtime trees reflect every deletion
in the same wave. This is Wave 3 of the phase; it may not start until Plan 43-03 has proven the
repair wave (Wave 1) is fully landed and re-emitted (D-02).

Per D-16/D-17, this phase adds no new control and owes no mutation-proof table — it removes gates.
Per D-12/D-13, delete/edit happens BEFORE commit, and `git ls-files`-dependent code (e.g.
`tools/adoption_scan/destinations.py:217`) will read the deleted packages as still-tracked until the
commit lands — a red between `git rm` and `git commit` is expected, not a defect (Pitfall 4,
RESEARCH.md).

**Revision note (W-4/BLOCKER-2/B-2/W-3/SIM-1):** this deletion invalidates FIVE live-tree test
artifacts and one dangling config literal. All six are explicit, in-commit steps of Task 1, matching
the pattern Phases 41 and 42 both used (repair the invalidated test in the SAME commit as the
deletion that invalidates it):
1. `tools/harness_emit/tests/test_coexist.py` hardcodes command counts (`25`) that render directly
   over `harness/commands/*.md` — it goes stale the moment the 4 commands are `git rm`'d.
2. `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` is a committed syrupy snapshot
   of the live harness tree. Its staleness trigger is the phase-wide rule in `<emit_snapshot_rule>`,
   not "the 4 command / 5 skill deletions" — ANY edit under `harness/agents/`, `harness/commands/`,
   `harness/skills/` or `harness/opencode.json` invalidates it, because the snapshot stores rendered
   BODY text. (43-01 and 43-03 regenerate it for their own edits; this task regenerates it for the
   deletions.)
3. `tools/harness_lint/tests/test_tests_are_isolatable.py` asserts `"tools/lifecycle_eval/tests"` is
   enumerated by a live filesystem scan — it goes red the moment `tools/lifecycle_eval` is deleted,
   and this is a RUNTIME assertion that `pytest --collect-only` (a collection-only check) cannot
   surface. It must be repaired in this same commit, not in Plan 43-05.
4. **(B-2)** `tools/adoption_scan/tests/test_install_completeness.py`'s
   `assert len(top_level_packages) >= 20` renders over `harness/commands/**`, `harness/skills/**` and
   `.github/workflows/*.yml` and drops below its own floor here — see `<interfaces>` for the
   wave-by-wave live measurement and the exact new value.
5. **(W-3)** `tools/adoption_scan/destinations.py:158` keeps `"harness/risk-policy.toml"` in
   `_CATEGORY_GLOBS` after this task deletes that file. The hyphenated spelling matches none of the
   underscore-based sweep regexes this phase uses, so nothing else in the phase would catch it.
6. **(SIM-1, moved in from 43-05 Task 2)** `tools/adoption_scan/tests/test_install_completeness.py`'s
   OTHER test — `test_every_referenced_tools_module_lands_in_applied_target` (`:116`) — resolves every
   discovered ref to a real `.py` file and `_resolve_module_file` (`:49-68`) raises at `:66` when one
   resolves to nothing. `.github/workflows/ci.yml:233` runs `uv run python -m tools.lifecycle_eval.runner`.
   Deleting `tools/lifecycle_eval` here while that CI job lived on until 43-05 left the ref
   unresolvable for three consecutive commits — a simulation pass measured it as
   `AssertionError: tools.lifecycle_eval.runner does not resolve to any real .py file in this checkout`.
   The CI `lifecycle-eval` job block and its `jobs.gate.needs` entry therefore move into THIS task's
   commit. A previous revision reasoned correctly that the COUNT floor could tolerate the one-plan
   gap and wrongly concluded the same of the RESOLUTION — counts degrade gracefully, resolution does
   not.

**W-9 — `uv.lock` goes dirty here and stays dirty until 43-05.** `uv run` re-locks as soon as the 8
uv workspace members disappear, so ` M uv.lock` appears the instant Task 1's `git rm` lands and
remains uncommitted through the rest of this plan (43-05 Task 2 owns `uv.lock`). Nothing reads it, so
this is expected state, not a defect — but it means every UNSCOPED `git status` or bare
`git diff --exit-code` in this window reports dirty. Every cleanliness check in this plan is
pathspec-scoped for exactly this reason; do not "fix" the dirt by committing `uv.lock` early, and do
not let it trigger an amend-if-red loop.

Purpose: make the 8-package plane unreachable in the product by construction — no shim, no stub, no
deprecation path (CER-07's own language).
Output: 8 package directories gone; hook, 4 commands, 5 skills, 3 toml declarations, `.workflow/tasks/`
gone; `caps.py`, `test_coexist.py`, `test_emit_determinism.ambr`, `test_tests_are_isolatable.py`,
`test_install_completeness.py` and `destinations.py` all updated to match; both runtime trees updated
to match; full `uv run pytest -q` green at the end of this plan (not merely `--collect-only`).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-03-SUMMARY.md
</context>

<emit_snapshot_rule>
<!-- W-4. Restated verbatim from 43-01-PLAN.md; it is a phase-wide RULE, not a per-task footnote. -->

`tools/harness_emit/tests/test_emit_determinism.py::test_projected_tree_matches_committed_snapshot`
(`:57-85`) renders the LIVE `harness/agents/`, `harness/commands/`, `harness/skills/` (`:24-27`) AND
`build_opencode_config(_HARNESS_DIR)` (`:82`, which reads `harness/opencode.json`) into one committed
syrupy snapshot. It stores rendered BODY text, not a file list.

**THE RULE:**
> Every commit that edits anything under `harness/agents/`, `harness/commands/`, `harness/skills/`,
> or `harness/opencode.json` MUST regenerate
> `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` in **that same commit**.

Procedure, run BEFORE staging:
```
uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q
uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q     # must exit 0
```
and the `.ambr` MUST appear in that commit's `git commit -- <pathspec>`.

Commits in this phase bound by the rule: 43-01 Task 1, 43-01 Task 2, 43-03 Task 1, and **this
plan's Task 1** (4 command deletions + 5 skill-directory deletions). `harness/plugins/*.ts` is NOT a
snapshot input (plugins are byte-copied, never rendered), so deleting `resume-gate.ts` does not by
itself trigger the rule — but the command/skill deletions in the same commit do.
</emit_snapshot_rule>

<interfaces>
<!-- Verified present 2026-07-28; exact contents an executor needs before deleting. -->

`tools/harness_lint/caps.py`'s current `EXPECTED_SKILLS` (line 135-153):
```python
EXPECTED_SKILLS = frozenset(
    {
        "python-conventions", "golden-testing", "data-contracts", "skill-creator",
        "golden-debug", "polyglot-boundary", "gate-model", "two-plane-memory",
        "pipeline-map", "fan-out-synthesize", "context-budget", "brownfield-adoption",
        "clarify", "test-driven-change", "diagnose", "domain-modeling",
        "adversarial-review-panel",
    }
)
```
Remove exactly `"clarify"`, `"test-driven-change"`, `"diagnose"`, `"domain-modeling"`,
`"adversarial-review-panel"` — the 12 remaining entries stay untouched. There is also a
multi-line comment immediately above this frozenset (line 129-134) explaining why those 5 are not
an anti-sprawl exception and citing `harness/risk-policy.toml`, `harness/disciplines.toml` and
`test_discipline_wiring.py` — that comment names three files this task deletes and must be removed or
rewritten in the same commit, not left dangling. (Note the HYPHENATED `risk-policy.toml` /
`disciplines.toml` spellings at `caps.py:129` and `caps.py:133`: they match no underscore-based sweep
regex in this phase — W-3's failure mode — so they must be handled by reading these exact lines, not
by grepping for `risk_policy`.)

`.workflow/tasks/` currently tracks exactly 6 files (verified via `git ls-files`):
```
.workflow/tasks/README.md
.workflow/tasks/T-20260718090000-contract-ratification/artifacts/README.md
.workflow/tasks/T-20260718090000-contract-ratification/evidence.json
.workflow/tasks/T-20260718090000-contract-ratification/handoff.json
.workflow/tasks/T-20260718090000-contract-ratification/state.json
.workflow/tasks/T-20260718090000-contract-ratification/task.json
```

Package LOC (verified, D-04): task_control 1677, handoff 1238, discipline 990, risk_router 877,
evidence 783, task_packet 605, lifecycle_eval 472, capability 379 = 7021 total.

`tools/harness_emit/tests/test_coexist.py`'s current state (live-verified 2026-07-28, NOT the
25/26 the original plan assumed uncritically — `ls harness/commands | wc -l` reads **25** today):
```python
def test_all_26_commands_emit_to_both_trees(tmp_path: Path) -> None:
    ...
    assert len(opencode_cmds) == 25, f"expected 25 opencode commands, got {len(opencode_cmds)}"
    assert len(claude_cmds) == 25, f"expected 25 Claude commands, got {len(claude_cmds)}"
```
The function name is already one phase stale (says 26, checks 25 — Phase 41 dropped `/docs-update`
26→25 and did not rename the function, per its own docstring). This task's 4 deletions
(`intake`, `phase-gate`, `handoff`, `discipline`) take the live `harness/commands/*.md` count from
**25 → 21**. Rename the function to `test_all_21_commands_emit_to_both_trees`, change both literals
from `25` to `21` (and both f-string messages to say "expected 21"), and append one line to the
docstring's phase-history list: `Phase 43 deletes /intake, /phase-gate, /handoff, /discipline (the
lifecycle-plane removal, CER-07), taking it 25 → 21.` Do not alter
`test_harness_commands_are_top_level_never_under_gsd`,
`test_seeded_gsd_command_survives_byte_unchanged_and_unlisted`, or
`test_seeded_settings_json_reproduced_byte_for_byte` — they assert structure, not count.
(Note: Plan 43-03 Task 1 already edited this file's `_SEED_SETTINGS` fixture; locate the count
assertions by name, not by line number.)

`tools/harness_lint/tests/test_tests_are_isolatable.py`'s hardcoded literal (line ~125):
```python
for expected in ("tools/harness_lint/tests", "tools/lifecycle_eval/tests"):
    assert expected in found, f"{expected} should be enumerated; scan returned {sorted(found)}"
```
`tools/lifecycle_eval` is one of the 8 packages this task's Task 1 deletes, so
`"tools/lifecycle_eval/tests"` stops existing in the same commit. No other surviving `tools/*/tests`
directory is named elsewhere in this file as canonically representative, so narrow the tuple to the
single surviving element already present: `("tools/harness_lint/tests",)`. Confirm via
`grep -l sys.path.insert tools/*/tests/conftest.py` (19 conftest.py files use the idiom post-deletion)
that this remains a live example of the pattern the file's own docstring describes.
`test_the_scan_finds_the_members_it_claims_to`'s `len(found) >= 10` threshold needs no change — 19
`tools/*/tests` dirs remain after this phase's deletions, still well above 10.
**Only the CODE literal (the double-quoted tuple element at `:125`) is in scope.** This file's module
docstring also names `lifecycle_eval` at `:19`, `:23`, `:33` and in an assertion message at `:108`,
as narrative about how the isolation idiom was established. That is shipped history, not a live
coupling; leave it, and see `<surviving_residue>` for why the phase's sweeps are shaped to permit it.

**B-2 — `tools/adoption_scan/tests/test_install_completeness.py:93-102`** (the third live-tree
literal this deletion invalidates; it appears in NO other plan's `files_modified`):
```python
def test_discovers_at_least_twenty_modules(repo_root: Path) -> None:
    """Sanity guard: the regex-walk helper must find a substantial number of distinct
    `python -m tools.X` references -- guards against the helper silently matching nothing and
    the main test vacuously passing."""
    refs = _discover_module_refs(repo_root)
    top_level_packages = {ref.split(".")[0] for ref in refs}
    assert len(top_level_packages) >= 20, (
        f"expected at least 20 distinct top-level tools packages, found "
        f"{len(top_level_packages)}: {sorted(top_level_packages)}"
    )
```
`_discover_module_refs` (`:31-48`) regex-walks `harness/commands/**/*.md`, `harness/skills/**/*.md`
and `.github/workflows/*.yml` for `python -m tools\.([a-zA-Z0-9_.]+)`.

**Measured wave-by-wave (2026-07-28; do not assume, these were computed against the live tree):**
| Point in the phase | Distinct top-level packages | Why it changed |
|---|---|---|
| Pre-phase (today) | **21** | `adoption_apply, adoption_scan, agree, capability, contract_drift, contract_hash, discipline, docs_sync, evidence, golden_runner, handoff, harness_emit, harness_lint, lifecycle_eval, memory_regen, polyglot_lint, risk_router, ruff_baseline, strangler_guard, task_control, task_packet` |
| After Wave 1 (43-01/02/03) | **20** | `evidence` was referenced ONLY by `harness/commands/{review,verify-work}.md`, both repaired in 43-01. Exactly at the `>= 20` floor — still green, which is why the floor edit does NOT ride with 43-01 |
| **This task's pre-check** (after the `git rm` INCLUDING the CI job, before the literal edit) | **13** | the 4 command + 5 skill deletions remove `capability, discipline, handoff, risk_router, task_control, task_packet`, and the `lifecycle-eval` CI job deletion (moved in per SIM-1) removes `lifecycle_eval`. Surviving: `adoption_apply, adoption_scan, agree, contract_drift, contract_hash, docs_sync, golden_runner, harness_emit, harness_lint, memory_regen, polyglot_lint, ruff_baseline, strangler_guard` |
| After 43-05 (end of phase) | **13** | unchanged — 43-05 no longer touches `ci.yml`; its close-out check just re-runs this test and reads the same 13 |

(A previous revision put **14** at this task's pre-check and 13 only after 43-05 Task 2, because the
CI job deletion then lagged one plan behind. SIM-1 moved the job here, so the count now lands at 13
in one step and never moves again.)

Change the floor to **12** (one below the final post-phase value of 13, preserving a real sanity
margin while still catching a helper that silently matches nothing), rename the function to
`test_discovers_at_least_twelve_modules`, update the f-string to say "at least 12", and append the
rationale to the docstring, e.g.:
`Floor lowered 20 -> 12 in Phase 43 (CER-07): the lifecycle-plane removal deleted 8 of the 21
top-level packages this helper discovered, taking the live count to 13.`
`>= 12` clears every point in the table above, so no intermediate commit is red. Do NOT raise the
floor back toward the live value later — it is a vacuity guard, not a census.

**SIM-1 — `.github/workflows/ci.yml`, the job block and the fan-in entry (moved here from 43-05
Task 2).** Live-verified 2026-07-28. The job and its comment banner occupy `:221-235`:
```yaml
  # ── lifecycle-eval ────────────────────────────────────────────────────────────────────────
  # Phase 23 composition coverage.  This invokes the fixture runner plus the focused
  # integration/regression suite; it deliberately reuses (not reimplements) the existing
  # schema, router, transition, context, evidence, handoff, resume, and emit gates above.
  lifecycle-eval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: astral-sh/setup-uv@v8.3.2
      - name: Sync workspace (all packages)
        run: uv sync --all-packages
      - name: Evaluate ratification-pending lane fixtures
        run: uv run python -m tools.lifecycle_eval.runner
      - name: Lifecycle fixture execution and negative-fixture binding
        run: uv run pytest tools/lifecycle_eval
```
Delete the whole block including its 4 banner/comment lines and the blank line that separated it from
the `emit-drift` banner. The fan-in entry is at `:345`:
```yaml
    needs: [setup, lang-tests, contract-check, drift, golden, core-suite, lint, lifecycle-eval, emit-drift, stale-derived, workspace]
```
`ruamel.yaml`-resolved: **11** entries today; removing `lifecycle-eval` leaves **10** —
`[setup, lang-tests, contract-check, drift, golden, core-suite, lint, emit-drift, stale-derived, workspace]`.
Change nothing else in the list. **SIM-2:** the job block and the `needs` entry must be deleted
TOGETHER in this commit — `tools/harness_lint/tests/test_ci_lint_gate.py::test_every_job_is_in_the_fan_in`
asserts `set(jobs) - {"gate"} <= set(gate.needs)`, and the simulation confirmed the file's suite stays
green (6 passed) only when both move together. `test_ci_lint_gate.py:10` names `lifecycle-eval` in a
module docstring only; it asserts nothing — leave it (see `<surviving_residue>`).
`.github/workflows/ci.yml:233` is the LAST `lifecycle_eval` discovery site in the repo, which is why
the B-2 count reaches its final 13 in this same commit. **README.md's fan-in prose is NOT this plan's
to touch** — 43-05 Task 2 still owns both READMEs and regenerates that line from the now-corrected
YAML.

**W-3 — `tools/adoption_scan/destinations.py:158`**, inside `_CATEGORY_GLOBS` (declared at `:142`):
```python
    "harness/permission-matrix.json",
    "harness/risk-policy.toml",        # <-- DELETE this line (file deleted by this task)
    "harness/opencode.json",
```
Remove exactly that one tuple entry; leave every other glob byte-unchanged. This is a dangling
literal, not a coupling: `destination_catalog()` filters by `git ls-files`, so the stale glob simply
matches nothing after the commit. It still must go — the hyphenated spelling matches none of the
phase's underscore-based sweeps, which is precisely the Phase-42 hyphen-vs-underscore miss.
No count guard is at risk: `tools/adoption_scan/tests/test_dispositions.py`'s `_MIN_CATALOG_ROWS`
is **300** and the live catalog measures **489** rows today, **408** after this phase's 81 row
deletions — comfortably above the floor, so that literal needs no change.

**Counts verified safe, requiring NO edit** (measured 2026-07-28, recorded so a later reader does not
re-litigate them): `test_hook_wiring.py:38` / `test_settings_merge.py:69` / `test_coexist.py:238`
all assert `SessionStart == 4`, and `resume_gate` was PreToolUse-only; `test_dispositions.py:19`
`_MIN_CATALOG_ROWS = 300` vs 408 post-phase; `test_tests_are_isolatable.py`'s `len(found) >= 10`
vs 19 post-phase.
</interfaces>

<surviving_residue>
<!-- W-7 (this revision). The phase's repo-wide sweep must be satisfiable, or it is the T-43-12
     failure class in a new costume: an unsatisfiable gate that either blocks the phase forever or
     gets waived by hand, waiving the real check with it. Measured 2026-07-28 by simulating the
     full post-43-04 tree. -->

After every deletion in this plan lands, these files still contain a bare token from the dying plane
and are **deliberately out of scope** — none is an executable reference, none can break:

| File | Sites | Why it stays |
|---|---|---|
| `tools/harness_lint/tests/test_tests_are_isolatable.py` | `:19`, `:23`, `:26-27`, `:33`, `:108` | Module-docstring narrative of how the test-isolation idiom was established across v2.3/v2.4, plus one assertion-failure hint string. Shipped history, not a live path dependency. Only the code literal at `:125` is repaired. |
| `tools/adoption_scan/tests/test_dispositions.py` | `:30`, `:314`, `:317` | `.workflow/tasks/...` appears as a synthetic fixture row and in a NEGATIVE-control assertion (`no catalog row's destination starts with '.workflow/tasks/'`). Deleting it would delete the control. |
| `tools/contract_graph/tests/test_query.py` | `:75-78` | Synthetic graph-input strings (`"import tools.task_packet"` etc.) fed to a tmp fixture; they name no real module and resolve nothing. |
| `tools/harness_config/tests/test_relationship_schema.py` | `:3` | One docstring sentence citing a since-deleted test as the idiom it mirrors. Prose only. |
| `tools/harness_lint/workspace_check.py` | `:22` | Module docstring lists ``resume_gate`` among the hook module names as narrative. Asserted by nothing; the check itself reads the live `tools/hooks/` tree. Informational — do NOT "clean" it. |
| `tools/harness_lint/tests/test_workspace_member_completeness.py` | `:14` | Same ``resume_gate`` mention in the same docstring sentence, mirrored into the test. Asserted by nothing. Informational — do NOT "clean" it. |
| `.github/workflows/ci.yml` (after this task's own edit) | `tools/harness_lint/tests/test_ci_lint_gate.py:10` | The `lifecycle-eval` token survives in `test_ci_lint_gate.py`'s module docstring as narrative about why the fan-in gate exists. Asserted by nothing. |

Consequently the repo-wide sweep in Task 2 is expressed in three satisfiable forms — executable
invocations, product surface, and declaration paths — never as a bare token grep over `tools/`.
Each was verified to return empty against the simulated post-deletion tree.

**Scope note on form 1 (this revision):** `README.md` and `README.ko.md` are NOT in form 1's path
list. A simulation pass measured form 1 at exit 0 with 4 matches — `README.md:130`, `:131`,
`README.ko.md:119`, `:120` — because both READMEs are swept by **43-05 Task 2**, one plan later.
They are not `<surviving_residue>` (they DO get repaired, just not here); they are simply another
plan's scope, and including them here would make form 1 unsatisfiable at its own commit — the same
T-43-12 failure class this section exists to prevent. 43-05 Task 2's B-5 grep covers both files and
was verified exit 1 once they are swept.
</surviving_residue>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete the 8 packages + hook source + 4 commands + 5 skills + 3 toml declarations + .workflow/tasks + the CI lifecycle-eval job, and refresh every test artifact and config literal this invalidates (D-04, D-06, D-07, D-08, D-09, W-4, BLOCKER-2, B-2, W-3, SIM-1, SIM-2)</name>
  <files>tools/task_control, tools/task_packet, tools/risk_router, tools/evidence, tools/handoff, tools/discipline, tools/capability, tools/lifecycle_eval, tools/hooks/resume_gate.py, harness/plugins/resume-gate.ts, harness/commands/intake.md, harness/commands/phase-gate.md, harness/commands/handoff.md, harness/commands/discipline.md, harness/skills/clarify, harness/skills/diagnose, harness/skills/domain-modeling, harness/skills/test-driven-change, harness/skills/adversarial-review-panel, harness/capabilities.toml, harness/disciplines.toml, harness/risk-policy.toml, .workflow/tasks, tools/harness_lint/caps.py, tools/harness_lint/tests/test_capability_wiring.py, tools/harness_lint/tests/test_discipline_wiring.py, tools/harness_lint/tests/test_tests_are_isolatable.py, tools/harness_emit/tests/test_coexist.py, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, tools/adoption_scan/tests/test_install_completeness.py, tools/adoption_scan/destinations.py, .github/workflows/ci.yml</files>
  <read_first>
    The `<interfaces>` block above (in particular the B-2 wave-by-wave measurement table), the
    `<emit_snapshot_rule>` block, the `<surviving_residue>` table, and
    `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-04, D-06 (second half — the hook
    source, distinct from the merge.py wiring already handled in Plan 43-03), D-07, D-08, D-09,
    D-10 (the CI half, moved into this task by SIM-1), D-12, D-13. Also read the `<interfaces>`
    SIM-1 excerpt for the exact `ci.yml` line ranges and the SIM-2 both-halves-together rule, and
    the W-9 note in the objective for why ` M uv.lock` is expected dirt from this commit onward.
  </read_first>
  <action>
    `git rm -r` in one pathspec-scoped commit: the 8 package directories under `tools/`
    (`task_control task_packet risk_router evidence handoff discipline capability lifecycle_eval`),
    `tools/hooks/resume_gate.py`, `harness/plugins/resume-gate.ts`,
    `harness/commands/{intake,phase-gate,handoff,discipline}.md`,
    `harness/skills/{clarify,diagnose,domain-modeling,test-driven-change,adversarial-review-panel}`,
    `harness/{capabilities,disciplines,risk-policy}.toml`, `.workflow/tasks`, and
    `tools/harness_lint/tests/{test_capability_wiring,test_discipline_wiring}.py` (ordering rule 8 —
    a deleted `harness/` artifact's dedicated gate test dies in the same commit).

    In the SAME commit, FIRST (before the module-count pre-check in step 4, which measures against
    it), apply the SIM-1 CI edit — this is the step moved in from 43-05 Task 2:
    0. Edit `.github/workflows/ci.yml` per `<interfaces>`: delete the `lifecycle-eval` job block
       (`:221-235`, i.e. the 4-line comment banner plus the job through
       `run: uv run pytest tools/lifecycle_eval`) AND delete the `lifecycle-eval` entry from the
       `jobs.gate.needs` list at `:345`. **SIM-2: both, together, in this commit** — deleting only one
       reds `test_ci_lint_gate.py::test_every_job_is_in_the_fan_in`. Change no other `needs` entry and
       no other job. Verify by resolving the YAML, never by grep:
       `uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n; assert 'lifecycle-eval' not in d['jobs'], sorted(d['jobs'])"`
       must exit 0. This is what keeps `tools/lifecycle_eval`'s deletion from leaving
       `.github/workflows/ci.yml:233`'s `python -m tools.lifecycle_eval.runner` as a ref that
       `test_install_completeness.py:116` cannot resolve. Do NOT touch `README.md`/`README.ko.md`'s
       fan-in prose here — 43-05 Task 2 regenerates that line from this now-corrected YAML.

    Then, still in the SAME commit and before running any snapshot regeneration:
    1. Edit `tools/harness_lint/caps.py`'s `EXPECTED_SKILLS` to drop the 5 discipline-skill entries and
       rewrite/remove the comment block above it (line 129-134) that names `harness/risk-policy.toml`,
       `harness/disciplines.toml` and `test_discipline_wiring.py` (all now deleted) — the
       `EXPECTED_SKILLS` edit CANNOT be its own commit sandwiched between the skill deletion and the
       re-emit (D-07's hard-fail-before-write ordering).
    2. Edit `tools/harness_lint/tests/test_tests_are_isolatable.py` per the `<interfaces>` guidance —
       narrow the CODE tuple at `:125` to `("tools/harness_lint/tests",)` (BLOCKER-2: this repairs the
       same commit that deletes `tools/lifecycle_eval`, not a later plan). Leave the module docstring
       and the `:108` hint string alone, per `<surviving_residue>`.
    3. Edit `tools/harness_emit/tests/test_coexist.py` per the `<interfaces>` guidance — rename the
       count function, change both `25` literals to `21`, update both f-string messages, and append
       the Phase-43 line to the docstring history. Do not touch `_SEED_SETTINGS` (43-03 already
       corrected it) or the seeded-settings byte-for-byte test.
    4. Edit `tools/adoption_scan/tests/test_install_completeness.py` per the `<interfaces>` guidance
       (B-2) — lower the sanity floor from `>= 20` to `>= 12`, rename the function to
       `test_discovers_at_least_twelve_modules`, update the f-string message, and add the
       phase-history rationale line to the docstring. Before editing, re-measure to confirm the
       figures still hold rather than trusting this plan:
       `uv run pytest tools/adoption_scan/tests/test_install_completeness.py::test_discovers_at_least_twenty_modules -q`
       must be RED at this point with a **"found 13"** shaped message. Thirteen, not fourteen:
       step 0 above already deleted `.github/workflows/ci.yml`'s `lifecycle-eval` job, which was
       `lifecycle_eval`'s last discovery site, so the count lands at its final value here. (An earlier
       revision said 14 because the CI job then lagged into 43-05; SIM-1 moved it in.) If the
       pre-check reports anything other than 13, stop and reconcile before changing the literal.
       `>= 12` clears 13 with margin and never needs a second edit anywhere in the phase.
       Also confirm the SIBLING test in this same module is green, not just the count one —
       `uv run pytest tools/adoption_scan/tests/test_install_completeness.py::test_every_referenced_tools_module_lands_in_applied_target -q`
       must exit 0 (SIM-1: this is the test that raised
       `AssertionError: tools.lifecycle_eval.runner does not resolve to any real .py file in this checkout`
       when the CI job lagged; if it is red here, step 0 was not applied).
    5. Edit `tools/adoption_scan/destinations.py` (W-3) — delete the single `"harness/risk-policy.toml"`
       entry from `_CATEGORY_GLOBS` (line 158). Change nothing else in that tuple.
    6. Apply the `<emit_snapshot_rule>` (W-4): this commit edits `harness/commands/` and
       `harness/skills/`, so run
       `uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update -q` to
       regenerate `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` against the
       now-deleted-on-disk source, then re-run without the flag and confirm exit 0. Doing this
       before staging is what keeps the snapshot in the same commit as the deletion.
    7. Run `uv run pytest tools/harness_emit/tests/test_coexist.py tools/harness_emit/tests/test_emit_determinism.py tools/harness_lint/tests/test_tests_are_isolatable.py tools/harness_lint/tests/test_ci_lint_gate.py tools/adoption_scan/tests/test_install_completeness.py tools/adoption_scan/tests/test_dispositions.py -q`
       and confirm all green BEFORE staging/committing (`test_ci_lint_gate.py` is the SIM-2 proof: the
       simulation measured 6 passed when the job and its `needs` entry moved together).

    Inspect `git diff --cached --name-only` before committing (D-13) — it MUST include the refreshed
    `.ambr` file and `.github/workflows/ci.yml` alongside the deletions, not leave either as an
    uncommitted dirty file. Commit with
    `git commit -- <the exact pathspec list above, including .github/workflows/ci.yml, the five
    refreshed test/config files and the regenerated .ambr>` — never `git add -A`, `git add .`,
    `git commit -a`, or `git checkout <ref> -- .`.

    **W-9 — expect ` M uv.lock` from this point on.** Running `uv run` after the 8 workspace members
    are gone re-locks the workspace, so `uv.lock` shows as modified for the rest of this plan. It is
    NOT in this commit's pathspec and must not be added to it — 43-05 Task 2 owns it. Treat an
    unscoped dirty `git status` in this window as expected; use the pathspec-scoped cleanliness
    checks in the acceptance criteria instead of a bare `git diff --exit-code`.
  </action>
  <acceptance_criteria>
    `test -d tools/task_control && echo FAIL || echo OK` — for each of the 8 package names, `test ! -d tools/<name>` exits 0.
    `test -f .workflow/tasks/README.md && echo FAIL || echo OK` — `test ! -e .workflow/tasks` exits 0.
    `grep -n "clarify\|diagnose\|domain-modeling\|test-driven-change\|adversarial-review-panel" tools/harness_lint/caps.py` returns no output.
    `grep -nE "risk-policy\.toml|disciplines\.toml|capabilities\.toml|test_discipline_wiring" tools/harness_lint/caps.py` returns no output (the hyphenated-spelling comment block is gone, W-3 class).
    `grep -n "== 25\|test_all_26_commands" tools/harness_emit/tests/test_coexist.py` returns no output; `grep -c "== 21" tools/harness_emit/tests/test_coexist.py` returns `2`.
    BLOCKER-2: `grep -c '"tools/lifecycle_eval/tests"' tools/harness_lint/tests/test_tests_are_isolatable.py` returns `0` — the DOUBLE-QUOTED code literal specifically. Do not use an unquoted `grep -n tools/lifecycle_eval/tests`: the module docstring at `:23` writes it in reST double-backticks and legitimately survives (see `<surviving_residue>`), so the unquoted form is unsatisfiable.
    B-2: `grep -n ">= 20\|at least 20\|twenty" tools/adoption_scan/tests/test_install_completeness.py` returns no output; `grep -c ">= 12" tools/adoption_scan/tests/test_install_completeness.py` returns `1`.
    SIM-1: `uv run python -c "from ruamel.yaml import YAML; d=YAML(typ='safe').load(open('.github/workflows/ci.yml')); n=d['jobs']['gate']['needs']; assert 'lifecycle-eval' not in n and len(n)==10, n; assert 'lifecycle-eval' not in d['jobs'], sorted(d['jobs'])"` exits 0 — the job AND its fan-in entry are both gone, verified by resolving the YAML rather than by grep (D-10's rule).
    SIM-1: `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -q` exits 0 — BOTH tests in the module, not just the count one. `test_every_referenced_tools_module_lands_in_applied_target` is the one that resolves every discovered ref to a real `.py` file; it is the measured failure this move exists to prevent.
    SIM-2: `uv run pytest tools/harness_lint/tests/test_ci_lint_gate.py -q` exits 0 (`test_every_job_is_in_the_fan_in` stays green because the job and the `needs` entry moved together).
    W-3: `grep -n "risk-policy" tools/adoption_scan/destinations.py` returns no output.
    W-4: `uv run pytest tools/harness_emit/tests/test_emit_determinism.py -q` exits 0 (run WITHOUT `--snapshot-update`); `grep -cE "tools\.(handoff|discipline|capability|task_control|task_packet|risk_router|evidence)" tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` returns `0`.
    `uv run pytest tools/harness_emit/tests/test_coexist.py tools/harness_emit/tests/test_emit_determinism.py tools/harness_lint/tests/test_tests_are_isolatable.py tools/adoption_scan/tests/test_install_completeness.py tools/adoption_scan/tests/test_dispositions.py -q` exits 0.
    `git log -1 --name-status` shows all listed paths as `D` or `M` in a single commit, including `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr` as `M` and `.github/workflows/ci.yml` as `M`.
    `git status --porcelain -- tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr .github/workflows/ci.yml` is empty (regenerated snapshot and the CI edit are committed, not left dirty). NOTE (W-9): do NOT widen this to a bare `git status --porcelain` — ` M uv.lock` is expected here and is 43-05 Task 2's to commit.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-emit both runtime trees and verify the FULL suite is green (D-14, D-16, BLOCKER-2 close-out, W-7)</name>
  <files>.opencode, .claude, AGENTS.md, tools/harness_emit/emit-manifest.json</files>
  <read_first>
    `.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md` D-14, D-15, D-16; the
    `<surviving_residue>` table above (why the repo-wide sweep is expressed in three scoped forms);
    and this plan's Task 1 commit (must be landed — committed, not merely staged — before this task
    runs, per Pitfall 4 in RESEARCH.md: `git ls-files`-dependent code sees a staged-only deletion as
    still tracked).
  </read_first>
  <action>
    Confirm Task 1's commit has landed (`git log -1 --oneline` shows it, and `git status --porcelain --`
    scoped to the deleted paths is clean). Per W-9, an UNSCOPED `git status` reports ` M uv.lock` here
    — that is expected (the workspace re-locked when the 8 members were deleted) and is 43-05 Task 2's
    to commit. Do not add it to any pathspec in this plan and do not treat it as a failed Task 1. Run `python -m tools.harness_emit` — this propagates the 4 command
    deletions, 5 skill deletions, and hook/plugin-source deletion into `.opencode/`/`.claude/`,
    prunes the corresponding `tools/harness_emit/emit-manifest.json` rows (including
    `.opencode/plugin/resume-gate.ts`, which Plan 43-03 correctly left in place because its
    `harness/plugins/*.ts` source still existed then), and updates `AGENTS.md`'s managed
    command/skill index. Do NOT hand-edit any emitted path or the manifest (D-14). Commit the
    re-emit output as its own commit with
    `git commit -- .opencode .claude AGENTS.md tools/harness_emit/emit-manifest.json` only — never
    `git add -A`, `git add .`, `git commit -a`, or `git checkout <ref> -- .`; inspect
    `git diff --cached --name-only` first (D-13).

    Then run the FULL suite, not merely a collection check: `uv run pytest -q` and confirm it exits 0
    green. `--collect-only` alone is insufficient here (VALIDATION.md requires a full
    `uv run pytest -q` after every wave; a `--collect-only`-only check would not have caught
    BLOCKER-2's or B-2's runtime assertion failures, which is exactly why Task 1 folded both repairs
    into the deletion commit instead of leaving them implicit). Also run
    `uv run pytest --collect-only -q` as a fast dangling-import sanity check first if desired, but the
    acceptance criterion is the full run. Per D-15, run this rather than reading source to infer
    correctness.
  </action>
  <acceptance_criteria>
    `uv run pytest -q` exits 0 (full suite green — not `--collect-only` alone).
    Product surface (emitted trees + the shared managed index): `grep -rnE "task_control|task_packet|risk_router|tools\.evidence|tools\.handoff|tools\.discipline|tools\.capability|lifecycle_eval|capabilities\.toml|disciplines\.toml|risk-policy\.toml|\.workflow/tasks" .opencode .claude AGENTS.md CLAUDE.md` returns no output.
    W-7 form 1 — no EXECUTABLE invocation of a deleted package survives in the harness source, the emitted trees, or the managed index: `grep -rnE "python -m tools\.(task_control|task_packet|risk_router|evidence|handoff|discipline|capability|lifecycle_eval)" tools/ harness/ .opencode/ .claude/ AGENTS.md` returns no output (exit 1). **`README.md` and `README.ko.md` are deliberately NOT in this path list** — a simulation pass measured this grep at exit 0 with 4 matches (`README.md:130`, `:131`, `README.ko.md:119`, `:120`), because both READMEs are swept by 43-05 Task 2 and are not in this plan's `<surviving_residue>` either. Including them here makes the criterion unsatisfiable at its own commit, which is the T-43-12 failure class. 43-05 Task 2's B-5 grep carries the equivalent coverage over both READMEs and was verified exit 1 once they are swept.
    W-7 form 2 — no declaration path of a deleted `harness/` artifact survives in source or emitted trees: `grep -rnE "capabilities\.toml|disciplines\.toml|risk-policy\.toml" tools/ harness/ .opencode/ .claude/ AGENTS.md` returns no output (exit 1).
    W-7 form 3 — no surviving Python module IMPORTS a deleted package: `grep -rnE "^\s*(from|import)\s+tools\.(task_control|task_packet|risk_router|evidence|handoff|discipline|capability|lifecycle_eval)\b" tools/` returns no output (exit 1).
    W-7: the bare-token repo-wide grep over `tools/` is deliberately NOT an acceptance criterion — the four files in `<surviving_residue>` keep those tokens in docstrings, negative-control assertions and synthetic fixture strings, so that form is unsatisfiable and would be waived by hand, waiving the three real checks above with it.
    `python -m tools.harness_emit && git diff --stat -- .opencode .claude opencode.json AGENTS.md CLAUDE.md tools/harness_emit/emit-manifest.json` is empty (idempotent re-emit).
    `test -e .opencode/plugin/resume-gate.ts && echo FAIL || echo OK` — the plugin file is now pruned (its source was deleted in Task 1); `grep -c "resume-gate.ts" tools/harness_emit/emit-manifest.json` returns `0`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Deleted-but-staged vs deleted-and-committed state | `tools/adoption_scan/destinations.py:217` reads `git ls-files`; a red between `git rm` and `git commit` is expected (D-12), not a signal of a botched deletion |
| Live-tree-rendering test snapshots/counts vs the source tree they render | `test_coexist.py`, `test_emit_determinism.py`'s committed snapshot, `test_tests_are_isolatable.py`'s scan and `test_install_completeness.py`'s module-discovery floor all read `harness/**`/`tools/**`/`.github/**` directly — they go stale the instant `git rm` lands, before any commit, so their repair belongs IN the deletion commit, not a follow-up |
| Hyphenated vs underscored path literals | `harness/risk-policy.toml` / `disciplines.toml` / `capabilities.toml` appear in Python source with hyphens; the phase's underscore-based sweep regexes are blind to them (the Phase-42 miss) |
| Executable reference vs narrative token | A deleted package named inside a docstring, a negative-control assertion or a synthetic fixture string is not a live coupling; a sweep that cannot tell them apart is unsatisfiable and gets waived (W-7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-05 | Repudiation | A skill/command deletion landing without its `caps.py`/emitter counterpart, leaving a dangling declaration | mitigate | `EXPECTED_SKILLS` edit forced into the same commit as the skill deletion (D-07); re-emit + full-suite check as a dedicated follow-up task |
| T-43-06 | Elevation of Privilege | A residual package left reachable (a "minimal state manager" shim) | mitigate | CER-07's own "no residue package" constraint; acceptance criteria assert directory absence, not merely reduced usage |
| T-43-09 | Denial of Service | A hardcoded test literal/count/snapshot referencing a just-deleted module leaves the suite red between this commit and a later plan's fix | mitigate | `test_coexist.py`, `test_emit_determinism.ambr`, `test_tests_are_isolatable.py` and `test_install_completeness.py` are ALL refreshed in the SAME commit as the deletion that invalidates them; Task 2 asserts full `uv run pytest -q`, not `--collect-only` alone |
| T-43-13 | Tampering | A dangling hyphenated path literal (`harness/risk-policy.toml` in `_CATEGORY_GLOBS`, or the `caps.py` comment block) surviving every sweep in the phase, exactly as five hyphenated docstrings survived Phase 42 | mitigate | W-3 handled by explicit line reference, not by grep; Task 2's W-7 form-2 sweep covers the three hyphenated `.toml` names across source AND emitted trees |
| T-43-20 | Denial of Service | An unsatisfiable repo-wide bare-token sweep (matching four surviving files' docstrings/fixtures) blocking Task 2 forever, or being hand-waived and taking the real structural checks with it | mitigate | `<surviving_residue>` enumerates every legitimate residue with its reason; the sweep is split into three scoped forms (executable invocation, declaration path, Python import), each verified empty against the simulated post-deletion tree |
| T-43-21 | Denial of Service | The B-2 pre-check quoting a module count that does not match the measured value at this commit, causing a mandated STOP on a correct measurement | mitigate | The `<interfaces>` wave-by-wave table gives the measured value at each point; with SIM-1's CI-job move the pre-check value is **13** and never changes again, and `>= 12` clears it with margin |
| T-43-23 | Denial of Service | `tools/lifecycle_eval` deleted while `.github/workflows/ci.yml:233` still invokes it, so `test_install_completeness.py::test_every_referenced_tools_module_lands_in_applied_target` raises at `_resolve_module_file:66` for three consecutive commits — a measured, reproduced red, not a hypothetical | mitigate | The CI `lifecycle-eval` job block AND its `jobs.gate.needs` entry are deleted in step 0 of this task's own commit (moved in from 43-05 Task 2); both the count test and the resolution test are run as separate exit-0 acceptance criteria, since only the latter catches this class |
| T-43-24 | Denial of Service | An unscoped `git status`/`git diff --exit-code` in this plan tripping on the expected ` M uv.lock` and driving a spurious amend-if-red loop | accept | W-9 records it as expected state; every cleanliness check in this plan is pathspec-scoped, and `uv.lock` is 43-05 Task 2's to commit. No test reads `uv.lock`, so the full suite stays green throughout the window |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package install — this task only removes 8 uv workspace members (their `uv.lock` refresh is Plan 43-05) |
</threat_model>

<verification>
`uv run pytest -q` — 0 failures (full suite, not `--collect-only` alone).
`grep -rnE "python -m tools\.(task_control|task_packet|risk_router|evidence|handoff|discipline|capability|lifecycle_eval)" tools/ harness/ .opencode/ .claude/ AGENTS.md` — no output (W-7 form 1; READMEs excluded on purpose — 43-05 Task 2 sweeps them).
`grep -rnE "capabilities\.toml|disciplines\.toml|risk-policy\.toml" tools/ harness/ .opencode/ .claude/ AGENTS.md` — no output (W-7 form 2).
`grep -rnE "^\s*(from|import)\s+tools\.(task_control|task_packet|risk_router|evidence|handoff|discipline|capability|lifecycle_eval)\b" tools/` — no output (W-7 form 3).
</verification>

<success_criteria>
None of the 8 package directories exists; the hook, plugin source, 4 commands, 5 skills, 3 toml
declarations, and `.workflow/tasks/` are gone; `caps.py` declares no deleted skill and names no
deleted `.toml`; `test_coexist.py` asserts 21 commands under a correctly named function;
`test_emit_determinism.ambr` reflects the post-deletion projection; `test_tests_are_isolatable.py`'s
code literal no longer names `tools/lifecycle_eval`; `test_install_completeness.py`'s sanity floor is
12 with its rationale recorded and its pre-check reconciled against the measured 14;
`destinations.py` carries no `harness/risk-policy.toml` glob; `.github/workflows/ci.yml` has no
`lifecycle-eval` job and a YAML-resolved 10-entry `jobs.gate.needs`, with both halves in the same
commit as `tools/lifecycle_eval`'s deletion; both runtime trees match via a
byte-identical re-emit with `.opencode/plugin/resume-gate.ts` and its manifest row pruned; the FULL
`uv run pytest -q` suite is green.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-04-SUMMARY.md` when done
</output>
