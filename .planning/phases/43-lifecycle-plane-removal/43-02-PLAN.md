---
phase: 43-lifecycle-plane-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/memory_regen/inject.py
  - tools/memory_regen/tests/test_inject_assembler.py
  - tools/handoff/tests/test_handoff.py
  - tools/hooks/tests/test_settings_coexist.py
  - tools/adoption_scan/scan.py
autonomous: true
requirements: [CER-07]

must_haves:
  truths:
    - "D-11: memory_regen strips the active-task block but the activeContext pointer survives, proven by an existing test assertion (test_active_context_is_pointer_not_body), not by reading output"
    - "D-02: this is Wave 1 repair — no package is deleted here, only the injector's active-task wiring and the two structural fixtures that would otherwise assert a false positive after Plan 43-03's re-emit"
    - "W-6 (NEW, this revision): tools/handoff/tests/test_handoff.py:26 imports TASK_HEADER from tools.memory_regen.inject and asserts on it at :591. Deleting TASK_HEADER without repairing that consumer is an ImportError at COLLECTION for the whole file. The consumer is repaired in the SAME commit as inject.py (the package it lives in is not deleted until 43-04, so it cannot be left dangling until then)"
    - "B-6 (scope boundary, this revision): tools/harness_emit/merge.py, tools/harness_emit/tests/test_coexist.py and harness/opencode.json are NOT in this plan. The merge.py edit alone makes merge_settings() stop reproducing the live .claude/settings.json byte-for-byte (measured: byte-identical False, PreToolUse 8 -> 7), which reds tools/harness_emit/tests/test_settings_merge.py — a file no plan named. That whole cluster moved to 43-03 Task 1, the commit that runs the emitter and is therefore allowed to change .claude/settings.json (D-14)"
    - "B-1 (unchanged): this plan does NOT touch test_expected_slot_counts. It reads the live emitter-owned .claude/settings.json, which only Plan 43-03 Task 1's emitter run may change, so the count edit belongs in that commit"
    - "Every task in this plan ends fully green. This phase sanctions no expected-red window; an executor told to amend-if-red must be able to treat any red as a real defect."
  artifacts:
    - path: "tools/memory_regen/inject.py"
      provides: "_active_task_pointer, its tools.handoff import block, TASK_HEADER, and the three assemble() task-wiring points removed; _active_context_pointer (line 120-134) untouched"
    - path: "tools/handoff/tests/test_handoff.py"
      provides: "No TASK_HEADER import and no test_real_generate_activate_assemble_injects_reserved_task_pointer — the one test whose whole subject is the injector wiring inject.py drops in this same commit (W-6)"
    - path: "tools/hooks/tests/test_settings_coexist.py"
      provides: "_NEW_GATES without the resume_gate tuple; test_expected_slot_counts left byte-unchanged for Plan 43-03 Task 1"
    - path: "tools/adoption_scan/scan.py"
      provides: "Module docstring no longer names tools/evidence/capture.py as its contrast example"
  key_links:
    - from: "tools/memory_regen/inject.py"
      to: "tools/handoff/tests/test_handoff.py"
      via: "from tools.memory_regen.inject import TASK_HEADER, assemble"
      pattern: "import TASK_HEADER"
---

<objective>
Repair the non-command half of the Wave-1 blast radius: `tools/memory_regen/inject.py`'s
active-task block (D-11) together with its one cross-package test consumer (W-6), and the two
structural test/doc sites RESEARCH.md's blast-radius scan found hardcode surviving state this phase
invalidates: `test_settings_coexist.py`'s `_NEW_GATES` list and `tools/adoption_scan/scan.py`'s one
docstring sentence naming `tools/evidence/capture.py`.

This plan and 43-01 are independent (disjoint file sets) and both must land before Plan 43-03 runs
the emitter — the emitter's re-emit propagates BOTH sets of Wave-1 repairs into `.opencode/**` and
`.claude/**` in one pass.

**Scope boundary (B-6) — the `resume_gate` retirement cluster moved OUT of this plan.** A previous
revision kept `tools/harness_emit/merge.py`'s `HARNESS_SIGNATURES`/`RETIRED_SIGNATURES`/
`HARNESS_HOOK_GROUPS` edit, `harness/opencode.json`'s plugin-array edit and `test_coexist.py`'s
`_SEED_SETTINGS` fixture here. That is the same split B-1 was created to eliminate, one level down:
the literal (`test_expected_slot_counts`) had moved to the emitter commit but its *cause* stayed a
commit earlier. Measured 2026-07-28 by running the post-edit `merge.py` logic against the live
`.claude/settings.json`: `merge_settings(<parsed live>)` no longer re-serializes byte-for-byte
(`PreToolUse` 8 → 7), which reds `tools/harness_emit/tests/test_settings_merge.py`
(`:54-62` `test_merge_reproduces_live_settings_byte_for_byte`, and the byte-equality assertion at
`:104` inside the de-dup test) — a file no plan in this phase named. `.claude/settings.json` cannot
be repaired from Wave 1 (D-14: emitter-owned). **All three files therefore moved to 43-03 Task 1**,
the commit that runs the emitter. Do not edit `tools/harness_emit/merge.py`,
`tools/harness_emit/tests/test_coexist.py` or `harness/opencode.json` in this plan.

**Scope boundary (B-1, unchanged).** `tools/hooks/tests/test_settings_coexist.py` carries TWO
literals this phase invalidates, and they belong to DIFFERENT commits:
- `_NEW_GATES` (line 81-87) lists gates the file asserts are *present*. Dropping the `resume_gate`
  tuple only removes an assertion, so it is safe here and stays green immediately — empirically
  confirmed, see `<interfaces>`.
- `test_expected_slot_counts` (line 112-116) asserts `len(hooks["PreToolUse"]) == 8` against the
  **live, emitter-owned `.claude/settings.json`**. That file goes 8 → 7 only when the emitter runs,
  which is Plan 43-03 Task 1's job. **Do not edit `test_expected_slot_counts` in this plan.**

Purpose: make the injector's pointer-survival mechanics correct at source, with every consumer of
the removed symbol repaired in the same commit, before anything is re-emitted or deleted.
Output: `inject.py` emits only the activeContext pointer (no active-task block); no test anywhere
imports the deleted `TASK_HEADER`; `_NEW_GATES` reflects the post-drop steady state; `scan.py` names
no dying module; the full suite is green at BOTH of this plan's commits.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-lifecycle-plane-removal/43-CONTEXT.md
@.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md
@.planning/phases/41-docs-review-plane-removal/41-03-SUMMARY.md
</context>

<interfaces>
<!-- Verbatim current-state excerpts, read 2026-07-28. Line numbers are exact; do not re-read to
     confirm them, only to view surrounding context if genuinely needed. -->

From `tools/memory_regen/inject.py`:
```python
# line 11: import json                     <- delete (only consumer is _active_task_pointer:143)
# line 16-22: the tools.handoff import block <- delete entirely
from tools.handoff.handoff import (
    HandoffError,
    packet_root_from_handoff,
)
from tools.handoff.handoff import (
    validate as validate_handoff,
)
# line 43: TASK_HEADER = "## Active task (validated HANDOFF pointer)"   <- delete
# line 120-134: _active_context_pointer(...)   <- STAYS VERBATIM, do not touch
# line 137-167: _active_task_pointer(...)      <- delete the whole function
# in assemble() (line 170-201):
#   line 180: task = _active_task_pointer(state_dir)                    <- delete
#   line 186: ("task", task),  (inside the `sections` list)             <- delete this tuple
#   line 197: name not in ("agreements", "banner", "drift", "task")     <- drop "task" from this tuple
```

From `tools/memory_regen/tests/test_inject_assembler.py`:
```
DELETE (whole test function, ~line 167 and ~179):
  test_malformed_active_task_is_fail_closed_and_capped
  test_absent_active_task_is_normal_no_task_session

KEEP verbatim (the D-11/SC-6 pointer-survival proof, do not touch):
  test_pointer_is_progress_log_not_imperative      (~line 98)
  test_updated_stamp_surfaced_verbatim              (~line 104)
  test_absent_stamp_degrades_gracefully             (~line 112)
  test_active_context_is_pointer_not_body           (~line 124) — the acceptance-criterion test:
      asserts the payload contains the literal string ".memory/state/activeContext.md"
```

**W-6 (NEW this revision) — `tools/memory_regen/inject.py` has a CROSS-PACKAGE test consumer.**
An exhaustive live grep for the symbols this task deletes (`TASK_HEADER`, `_active_task_pointer`)
returns exactly two files: `tools/memory_regen/tests/test_inject_assembler.py` (already handled
above) and:
```python
# tools/handoff/tests/test_handoff.py:26
from tools.memory_regen.inject import TASK_HEADER, assemble
# tools/handoff/tests/test_handoff.py:571
def test_real_generate_activate_assemble_injects_reserved_task_pointer(
# tools/handoff/tests/test_handoff.py:587-591
    payload = assemble(
        derived_dir=derived, state_dir=state_dir, agreements_dir=root / ".memory/agreements"
    )
    assert "ACTIVE HANDOFF INVALID" not in payload, payload
    assert TASK_HEADER in payload and "run the phase gate" in payload
```
Deleting `TASK_HEADER` from `inject.py` makes line 26 an **ImportError at collection time**, which
takes the ENTIRE `test_handoff.py` module red — not one assertion. `tools/handoff/` is not deleted
until Plan 43-04, four commits later, so leaving this dangling manufactures a three-commit red
window this phase does not sanction. `assemble` is imported on the same line and used only at `:587`
inside that one function, and `TASK_HEADER` appears nowhere else in the file (verified). The whole
subject of `test_real_generate_activate_assemble_injects_reserved_task_pointer` is the injector
wiring this task removes, so the correct repair is to delete that test function in full and drop the
now-unused `from tools.memory_regen.inject import TASK_HEADER, assemble` import line — in this same
commit. Everything else in `test_handoff.py` is untouched; the file itself dies with its package in
Plan 43-04.

Other `inject` importers were checked and are NOT affected — they use only surviving API:
`tools/memory_regen/tests/test_inject_determinism.py` (`inject.assemble`, `inject.DRIFT_HEADER`),
`tools/harness_lint/tests/test_agreements_predicate.py` (`inject._agreements_block`),
`tools/memory_regen/tests/test_hook_wiring.py` (string match on the injector shell/plugin only),
`tools/memory_ui/_stamp.py` and `tools/memory_ui/routes.py` (docstring mention / write path).

From `tools/hooks/tests/test_settings_coexist.py` — ONE edit only, at line 81-87 (`_NEW_GATES`):
```python
_NEW_GATES = [
    ("PreToolUse", "tools.hooks.contract_guard", "Write|Edit"),
    ("PreToolUse", "tools.hooks.secret_scan", "Read|Write|Edit"),
    ("PreToolUse", "tools.hooks.commit_gate", "Bash"),
    ("PostToolUse", "tools.hooks.format_on_write", "Write|Edit"),
    ("PreToolUse", "tools.hooks.resume_gate", "Write|Edit|Bash"),   # <-- DELETE this tuple
]
```
`_NEW_GATES` has exactly ONE consumer: `test_harness_gates_registered_with_expected_matcher`
(line 89-97), which loops the list and asserts each named gate IS registered under the expected
matcher. Removing an element strictly reduces the loop's iterations, so it cannot fail — and nothing
in this file asserts the ABSENCE of an unlisted gate. **Empirically confirmed 2026-07-28** by
applying exactly this edit with `.claude/settings.json` left untouched: `uv run pytest
tools/hooks/tests/test_settings_coexist.py -q` reported `4 passed`. This plan ends green.

**DO NOT TOUCH `test_expected_slot_counts` (line 112-116) in this plan.** Its
`len(hooks["PreToolUse"]) == 8` reads the real `.claude/settings.json`, which is emitter-owned; the
8 → 7 change and the matching literal/message/docstring edit both belong to Plan 43-03 Task 1's
commit, alongside the emitter run and the `merge.py` edit that cause them.
(Note for that later plan: after this task removes the `_NEW_GATES` tuple, the file shortens by one
line and `test_expected_slot_counts` shifts to line 111-115 — locate it by name, not by number.)

From `tools/adoption_scan/scan.py:1-6` (module docstring; the exact current sentence):
```
existing repo primitives, never a fresh scanning engine and never built on
``tools/evidence/capture.py`` (which executes subprocesses and mutates task state, both forbidden
here — see the plan's own must_haves.truths).
```
Rewrite so it states the same "no subprocess execution, no task-state mutation" constraint WITHOUT
citing the dead module path. Nothing asserts this docstring's content (verified: no test reads
`scan.__doc__`), so this is a pure hygiene edit and cannot go red. Note `scan.py:10-12` separately
cites `contracts/harness/task-control/gate-registry.json`, which SURVIVES this phase (Phase 44 /
CER-08 owns it) — leave that reference alone.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip inject.py's active-task block, keep the activeContext pointer, repair its one cross-package consumer (D-11, W-6)</name>
  <files>tools/memory_regen/inject.py, tools/memory_regen/tests/test_inject_assembler.py, tools/handoff/tests/test_handoff.py</files>
  <read_first>
    The `<interfaces>` block above (exact line ranges, including the W-6 cross-package consumer) and
    `.planning/phases/43-lifecycle-plane-removal/43-RESEARCH.md` §"inject.py's exact goes/stays
    split (D-11)".
  </read_first>
  <behavior>
    - `assemble()` still returns a payload containing the literal string
      ".memory/state/activeContext.md" when `.memory/state/activeContext.md` exists with an
      `updated:` frontmatter stamp (existing `test_active_context_is_pointer_not_body` must still
      pass unmodified).
    - `assemble()` no longer imports `tools.handoff` at all, and no longer emits a
      "## Active task (validated HANDOFF pointer)" section under any input, because the function
      producing it is deleted, not merely made to return "".
    - No module anywhere in the repo imports `TASK_HEADER`; `uv run pytest --collect-only -q`
      reports 0 collection errors immediately after this commit.
  </behavior>
  <action>
    Delete `_active_task_pointer` (line 137-167) entirely, the `tools.handoff` import block
    (line 16-22), the now-unused `import json` (line 11 — its only consumer was
    `_active_task_pointer`), and `TASK_HEADER` (line 43). In `assemble()`, remove the
    `task = _active_task_pointer(state_dir)` call, the `("task", task)` tuple from `sections`, and
    drop `"task"` from the never-drop exemption tuple on line 197 (leaving
    `("agreements", "banner", "drift")`). Leave `_active_context_pointer` (line 120-134) byte-for-byte
    untouched — do not reformat, reorder, or rename it. In
    `tools/memory_regen/tests/test_inject_assembler.py`, delete
    `test_malformed_active_task_is_fail_closed_and_capped` and
    `test_absent_active_task_is_normal_no_task_session` in full; leave
    `test_pointer_is_progress_log_not_imperative`, `test_updated_stamp_surfaced_verbatim`,
    `test_absent_stamp_degrades_gracefully`, and `test_active_context_is_pointer_not_body` untouched.

    W-6, in this SAME commit: in `tools/handoff/tests/test_handoff.py`, delete the import line
    `from tools.memory_regen.inject import TASK_HEADER, assemble` (`:26`) and delete the test
    function `test_real_generate_activate_assemble_injects_reserved_task_pointer` (`:571` through
    the end of its body at `:591`) in full — it exists solely to prove the injector task-pointer
    wiring this task removes, and `TASK_HEADER`/`assemble` are used nowhere else in the file. Change
    nothing else in `test_handoff.py`; the module and its package die in Plan 43-04. Without this
    edit `TASK_HEADER`'s deletion is an ImportError at COLLECTION for the whole file, so it is not a
    failing assertion an executor could triage — it is a module-wide red for three commits.

    Commit with `git commit -- tools/memory_regen/inject.py
    tools/memory_regen/tests/test_inject_assembler.py tools/handoff/tests/test_handoff.py` only.
    Never `git add -A`, `git add .`, `git commit -a`, or `git checkout <ref> -- .`. Inspect
    `git diff --cached --name-only` first (D-13).
  </action>
  <acceptance_criteria>
    `uv run pytest tools/memory_regen/tests/test_inject_assembler.py -q` — all remaining tests pass, and `test_active_context_is_pointer_not_body` is present and green.
    W-6: `uv run pytest tools/handoff/tests/test_handoff.py -q` exits 0 (the file still COLLECTS — this is the criterion the ImportError would have broken).
    W-6: `grep -rn "TASK_HEADER" tools/` returns no output (exit 1) — repo-wide, not just in `inject.py`.
    `grep -n "tools.handoff\|_active_task_pointer\|^import json" tools/memory_regen/inject.py` returns no output.
    `grep -c "def _active_context_pointer" tools/memory_regen/inject.py` returns exactly `1`.
    `uv run pytest --collect-only -q` reports 0 collection errors.
    `uv run pytest -q` exits 0 — this task ends fully green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Drop the resume_gate tuple from _NEW_GATES + sweep scan.py's docstring (B-1 boundary held)</name>
  <files>tools/hooks/tests/test_settings_coexist.py, tools/adoption_scan/scan.py</files>
  <read_first>
    The `<interfaces>` block above — in particular the single-edit scope in
    `test_settings_coexist.py`, the explicit "DO NOT TOUCH `test_expected_slot_counts`" boundary, and
    the B-6 note in `<objective>` explaining why `merge.py` / `harness/opencode.json` /
    `test_coexist.py` are NOT in this plan.
  </read_first>
  <action>
    In `tools/hooks/tests/test_settings_coexist.py`: delete ONLY the
    `("PreToolUse", "tools.hooks.resume_gate", "Write|Edit|Bash")` tuple from `_NEW_GATES`
    (line 86). Leave `test_expected_slot_counts` (line 112-116) byte-unchanged — its
    `len(hooks["PreToolUse"]) == 8` reads the live emitter-owned `.claude/settings.json`, and both
    that file's 8 → 7 change and the matching literal edit belong to Plan 43-03 Task 1's commit,
    together with the `merge.py` edit that causes them. Do not touch `.claude/settings.json`, do not
    touch `tools/harness_emit/merge.py`, and do not run the emitter in this plan.

    In `tools/adoption_scan/scan.py`: rewrite the module-docstring sentence that names
    `tools/evidence/capture.py` as a contrast example so it no longer names a module this phase
    deletes — state the same "no subprocess execution, no task-state mutation" constraint without
    citing the specific dead module path (reference the constraint itself, not the file that will no
    longer exist). Leave the `contracts/harness/task-control/gate-registry.json` citation lower in
    the same docstring alone — that contract survives this phase (D-05 / CER-08).

    Commit with `git commit -- tools/hooks/tests/test_settings_coexist.py
    tools/adoption_scan/scan.py` only. Never `git add -A`, `git add .`, `git commit -a`, or
    `git checkout <ref> -- .`. Inspect `git diff --cached --name-only` first (D-13).
  </action>
  <acceptance_criteria>
    `grep -n "resume_gate" tools/hooks/tests/test_settings_coexist.py` returns no output.
    Scope boundary held: `grep -c 'PreToolUse"\]) == 8' tools/hooks/tests/test_settings_coexist.py` returns `1` and `grep -c "4 GSD + 4 harness" tools/hooks/tests/test_settings_coexist.py` returns `2` (measured 2026-07-28: the string appears TWICE in `test_expected_slot_counts` — once in its docstring at `:113` and once in the `PreToolUse` assertion message at `:115`; Plan 43-03's mirror criterion already states 2, and an earlier revision of THIS line wrongly said 1) — `test_expected_slot_counts` is deliberately UNCHANGED here; Plan 43-03 Task 1 owns it. An executor that "helpfully" corrects it early fails this check.
    B-6 boundary held: `git log -1 --name-only` lists ONLY `tools/hooks/tests/test_settings_coexist.py` and `tools/adoption_scan/scan.py` — `tools/harness_emit/merge.py`, `tools/harness_emit/tests/test_coexist.py` and `harness/opencode.json` must NOT appear; they belong to 43-03 Task 1.
    `grep -n "tools/evidence/capture" tools/adoption_scan/scan.py` returns no output.
    `uv run pytest tools/hooks/tests/test_settings_coexist.py tools/harness_emit/tests/test_settings_merge.py tools/harness_emit/tests/test_coexist.py -q` exits 0 (live-verified shape: `test_settings_coexist.py` reports `4 passed` with `.claude/settings.json` untouched; the other two are the files B-6 protects and must be untouched and green here).
    `uv run pytest -q` exits 0 — this plan ends fully green. Any red here is a real defect; this phase sanctions no expected-red window.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| SessionStart injector output | `inject.py`'s `assemble()` payload is trusted context injected every session; removing the active-task block must not silently also remove the activeContext pointer (a real fail-closed-safety regression if it happened) |
| Deleted module symbol vs its cross-package importers | `TASK_HEADER` is imported from a package (`tools/handoff`) that this phase does not delete until Wave 3; a symbol deletion here is an ImportError at COLLECTION in that package, not a triageable assertion failure (W-6) |
| Source-owned fixtures vs live-tree assertions | `_NEW_GATES` is fixture data this plan owns; `test_expected_slot_counts` reads the emitter-owned live config and is Plan 43-03's to edit. Repairing a live-tree literal from a plan that cannot change the live file is what manufactures a red window. |
| Wiring change vs the emitted config it invalidates | `merge.py`'s signature/group edit changes what `merge_settings()` produces for the LIVE `.claude/settings.json`; only a commit permitted to re-emit that file can absorb it (B-6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Repudiation | `inject.py` active-task removal accidentally dropping the activeContext pointer too | mitigate | `test_active_context_is_pointer_not_body` is kept and re-run as the acceptance criterion (D-11/SC-6) |
| T-43-11 | Denial of Service | The `test_expected_slot_counts` literal edited HERE, leaving this plan red on `.claude/settings.json` — a file it may not touch (D-14) — and normalizing a tolerated red in a phase whose executors are told to amend-if-red | mitigate | The count edit is assigned to Plan 43-03 Task 1, in the same commit as the emitter run that causes 8 → 7; this plan's acceptance criteria assert the literal is still `== 8` here |
| T-43-17 | Denial of Service | `merge.py`'s hook-retirement edit landing in Wave 1, silently reding `tools/harness_emit/tests/test_settings_merge.py` (byte-for-byte reproduction of the LIVE settings.json) for three commits — the cause/literal split B-1 was created to eliminate, recurring one level down (B-6) | mitigate | The entire `merge.py` + `_SEED_SETTINGS` + `harness/opencode.json` cluster moved to 43-03 Task 1; Task 2's acceptance criteria assert those files are absent from this plan's commit AND that `test_settings_merge.py` is green here |
| T-43-18 | Denial of Service | `TASK_HEADER` deleted while `tools/handoff/tests/test_handoff.py:26` still imports it — a module-wide collection error surviving until Plan 43-04 deletes the package (W-6) | mitigate | The importer is repaired in the same commit; acceptance criteria include a repo-wide `grep -rn "TASK_HEADER" tools/` returning nothing and a `--collect-only` 0-error run |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package install in this plan (deletion/repair-only, no new dependency) |
</threat_model>

<verification>
`uv run pytest tools/memory_regen/tests/test_inject_assembler.py tools/handoff/tests/test_handoff.py tools/hooks/tests/test_settings_coexist.py -q` — all green.
`grep -rn "TASK_HEADER" tools/` — no output (W-6).
`uv run pytest --collect-only -q` — 0 collection errors.
`uv run pytest -q` — green at BOTH of this plan's commits. This plan introduces no red at any commit.
`git log -1 --name-only` after Task 2 lists no file from the B-6 cluster.
</verification>

<success_criteria>
`inject.py` emits only the activeContext pointer; no module in the repo imports the deleted
`TASK_HEADER`; `_NEW_GATES` reflects the post-drop steady state while `test_expected_slot_counts` is
left for Plan 43-03 Task 1; `scan.py` names no module this phase deletes; `merge.py`,
`test_coexist.py` and `harness/opencode.json` are untouched here and reserved for 43-03 Task 1; the
full suite is green at every commit of this plan.
</success_criteria>

<output>
Create `.planning/phases/43-lifecycle-plane-removal/43-02-SUMMARY.md` when done
</output>
