---
phase: 46-product-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/agents/orchestrator.md
  - .opencode/agent/orchestrator.md
  - .claude/agents/orchestrator.md
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
autonomous: true
requirements: [PROD-02, PROD-03, PROD-05]
must_haves:
  truths:
    - "Locked decisions honored by this plan: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-11, D-12, D-13, D-17, D-18, D-19, D-21"
    - "harness/agents/orchestrator.md carries four route sections — small-change, bugfix, feature, contract-change — each with an explicit stop condition, the delegation-packet fields, and a literal next command"
    - "The six-field completion contract appears verbatim: Outcome / Artifacts or changes / Verification / Decisions and assumptions / Risks or unresolved items / Next command"
    - "research appears as no route heading anywhere in the file"
    - "The 19-row routing decision table that began at :72 is gone"
    - "Every backticked /command and every tools.<module> the rewritten file cites resolves to a live artifact, asserted by a loop, not by eye"
    - "Each route's Repository evidence block cites only harness_config / contract_graph call sites that resolve today"
    - "Five operative discipline sentences survive, each greppable by a fixed lead token"
    - "The rewritten source is projected to both runtime trees by the emitter and the .ambr snapshot is regenerated in the same commit"
  artifacts:
    - path: "harness/agents/orchestrator.md"
      provides: "The product's four-route lifecycle, authored where the planner persona already lives"
      contains: "## Route: contract-change"
    - path: ".opencode/agent/orchestrator.md"
      provides: "opencode projection of the rewritten persona"
    - path: ".claude/agents/orchestrator.md"
      provides: "Claude projection of the rewritten persona"
    - path: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      provides: "Projected-tree body snapshot, regenerated for the new persona body"
  key_links:
    - from: "harness/agents/orchestrator.md"
      to: "harness/commands/*.md"
      via: "backticked /command citations, each resolving to a source command file"
      pattern: "`/(orient|lint|test|review|verify-work|checkpoint|contract-check|new-contract-rule|add-language|fan-out-synthesize)`"
    - from: "harness/agents/orchestrator.md"
      to: "tools/harness_config, tools/contract_graph"
      via: "Repository evidence blocks naming module + function, executed as python -c (neither package has a __main__)"
      pattern: "tools\\.(harness_config|contract_graph)"
    - from: "harness/agents/orchestrator.md"
      to: ".opencode/agent/orchestrator.md and .claude/agents/orchestrator.md"
      via: "python -m tools.harness_emit"
      pattern: "orchestrator"
---

<objective>
Rewrite `harness/agents/orchestrator.md` — the file that already declares itself *"the only planner
in the deployed harness (GSD is dev-side and is not emitted)"* (`:45`) — so that claim becomes true.
The 19-row routing decision table (`:72`, 19 rows, in a 102-line file) is retired and replaced by
**four route sections**: `small-change · bugfix · feature · contract-change`. Each carries an
explicit stop condition, the delegation-packet fields, a *Repository evidence* block computed from
this monorepo's own facts, a literal next command, and the six-field completion contract copied
verbatim from the vendored reference **as text** — no file is imported.

This is the phase's long pole and is separable from the `flow.md` addition (Plan 02).

Purpose: the deployed harness is driven by a weaker in-house model with no GSD habit. It needs
explicit stop conditions and a literal next command far more than it needs elegance. The delegation
packet and the completion contract are the two things that make a handoff survive a context boundary.

Output: a rewritten source persona, projected to both runtime trees, with the `.ambr` body snapshot
regenerated in the same commit.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-product-flow/46-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@harness/agents/orchestrator.md
@AGENTS.md

Read for text only, never import (D-12): `docs/references/opencode-matt-workflows/WORKFLOW_CONTRACTS.md:36-46`.

<interfaces>
<!-- Contracts the executor needs. Do not go exploring; these were extracted at plan time. -->

Live command surface — `harness/commands/*.md`, **17** files:
add-language, adopt, adr, agree, build, checkpoint, component, contract-check, docs-sync,
fan-out-synthesize, lint, new-contract-rule, orient, refresh-memory, review, test, verify-work.
(Plan 02 adds `flow` → 18. This plan MUST NOT cite `/flow` — it does not exist yet at this commit
and the citation loop below would red.)

`tools/harness_config/loader.py` — **no `__main__`**, call as a library:
```python
load_project(path=...) -> dict
languages(cfg=None) -> list[dict]
components(cfg=None) -> list[dict]
pipeline(cfg=None) -> dict
contract_graph_relationships(cfg=None) -> list[dict]
effective_relationships(cfg=None) -> list[dict]
language_bash_scopes(cfg=None) -> set[str]
```

`tools/contract_graph/` — **no `__main__`**, call as a library:
```python
compile.compile_graph(cfg=None) -> dict
query.direct(graph, node) -> dict     # ids AND connecting paths
query.reverse(graph, node) -> dict
query.transitive(graph, node) -> dict
```

The six-field completion contract, verbatim (`WORKFLOW_CONTRACTS.md:39-46`):
```text
Outcome
Artifacts or changes
Verification
Decisions and assumptions
Risks or unresolved items
Next command
```

Frontmatter that must survive the rewrite byte-identical (`orchestrator.md:1-18`): `name`,
`description`, `mode: primary`, the five `permission` keys, `tools:`. `tools/harness_lint/caps.py`
validates `EXPECTED_PERSONAS`, `VALID_PERMISSION_KEYS`, `VALID_MODES`, `READ_ONLY_PERSONAS`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire the 19-row table; author the four routes with stop conditions, the delegation packet, and the completion contract</name>
  <files>harness/agents/orchestrator.md</files>
  <action>
Edit the source persona only (D-17 — never hand-edit `.opencode/**`, `.claude/**`, `AGENTS.md`,
`CLAUDE.md`, or root `opencode.json`).

KEEP byte-identical: frontmatter `:1-18`, the persona intro, the specialist list, "Rules of
engagement", and the "Intake → decompose (minimal procedure)" section including its self-declaration
at `:45` (D-03 — it is at `:45`, not `:48`; this phase makes the claim true rather than editing it).

DELETE: the entire `## Routing decision table (work shape → persona / command)` section — heading,
the two explanatory paragraphs, the 19-row table, and the closing one-liner (`:72` to EOF). D-02:
the file is **102** lines and the table has **19** rows starting at **`:72`** — the requirement's
"25-row (`:90-129`)" is stale prose written before phases 43–44 deleted the personas those rows
named. Plan against the live file; do not go looking for 25 rows.

ADD in its place, in this order:

1. `## Routes` — a short preamble that (a) names the four routes, (b) states the selection rule:
   pick **exactly one** route before touching anything, and (c) states the refusal rule: if no route
   fits, **stop and ask** — do not improvise a fifth. Add one sentence stating that `research` is
   deliberately not a route: it terminates in a document, and `explorer` + `fan-out-synthesize` +
   `context-budget` already cover it (D-06 — do not add it for symmetry, and do not use the word as
   a `## Route:` heading anywhere).

2. `### Delegation packet` — the fields every subtask handed to a specialist must carry. Author six
   fields in our own words (this is ours, not a copy): **Objective** (one observable outcome) ·
   **Starting context** (exact paths, so the subagent never explores) · **Write scope** (the files it
   may modify, and only those) · **Repository evidence** (the computed facts from the route's
   evidence block) · **Stop condition** (the literal condition under which it halts) · **Return
   format** (the completion contract below).

3. `### Completion contract` — the six fields **verbatim** (D-05), inside a fenced ```text block,
   exactly as given in `<interfaces>` above, one per line, in that order, with those exact words
   ("Artifacts or changes", "Decisions and assumptions", "Risks or unresolved items"). Copy the
   *text*. Do NOT `@`-reference, import, link, or path-cite the vendored file in the authored prose
   (D-12) — a source citation belongs in the SUMMARY, not in a shipped product file.

4. Four sections, in this order, each headed exactly `## Route: small-change`, `## Route: bugfix`,
   `## Route: feature`, `## Route: contract-change`. Each section has these five subsections in this
   fixed order, so a weak model can pattern-match across routes:
   - **When to use** — one or two sentences with a concrete trigger.
   - **Steps** — a numbered list, each step naming the persona it delegates to and the backticked
     command that runs it. Use only commands from the 17-command list in `<interfaces>`.
   - **Repository evidence** — leave a placeholder line `TODO(Task 2)` for now; Task 2 fills these.
   - **Stop condition** — an explicit, checkable halt. Not "when done". Examples of the required
     shape: "Stop when the change exceeds the write scope you declared, or a second file outside it
     needs to change — re-route to `feature` rather than growing this one." Every route gets one.
   - **Next command** — a single literal backticked command, not a menu.

   `contract-change` is the phase's differentiator (D-07): it is the ONE route where this harness is
   not repo-agnostic. Its steps must be specific to a contract edge — locate the contract entry under
   `contracts/`, compute the affected set from the declared relationships, change the contract entry
   first, then the failing case, then the code, then `/contract-check`, then the golden pair. Do NOT
   water it into a generic "make a change" route; if `contract-change` reads like `small-change` with
   a different noun, the route is wrong.

5. **PROD-03 — exactly five operative sentences** (D-08), each on its own line and each led by the
   fixed literal token `**Discipline:**` so they are greppable as a set of five:
   - in `bugfix`: reproduce the defect as a failing test before changing any code;
   - in `feature`: settle the vocabulary first — name the concepts against the glossary and the
     contract ids before writing code;
   - in `contract-change`: contract entry first, then the failing case, then the code;
   - in `small-change`: keep it the smallest change that works and re-route rather than grow scope;
   - in `## Routes` (cross-cutting): red before green — nothing lands until a test that fails for the
     right reason exists.
   Five skills, five sentences, ~20 lines of prose in a file already being rewritten. Not five new
   artifacts.

**GEN-04 / GEN-05 phrasing rule (D-19 — this reddened three commits in Phase 45).**
`tools/harness_lint/tests/test_core_no_example_dep.py` scans every tracked file under `tools`,
`harness`, `libs` for the literal token `examples/` and for the prose tokens `dotnet-engineer`,
`dotnet-conventions`, `normalization-catalog`, `pipeline-patterns`, `libs/dotnet`, `equipment`,
`standard-log`, `correction-rules`, `wafer`, `설비`. `harness/agents/orchestrator.md` is inside that
scan. Refer to the reference instance as "the declared instance" or "the instance registered in
`project.toml`" — never with the `examples/` token, never by domain noun. Bare `.NET`, `parser`,
`converter` are explicitly allowed by the guard's own comment and already appear in the kept prose.

Do NOT cite `/flow` (Plan 02 authors it; it does not exist at this commit). Do NOT cite a
one-command form of the evidence block as a backticked slash-command — that command is v2.6 and
would red the Task 3 citation loop. Mention it, if at all, as unbackticked prose.
  </action>
  <verify>
    <automated>
test -f harness/agents/orchestrator.md &&
grep -c '^## Route: ' harness/agents/orchestrator.md | grep -qx 4 &&
for r in small-change bugfix feature contract-change; do grep -qx "## Route: $r" harness/agents/orchestrator.md || { echo "missing route $r"; exit 1; }; done &&
! grep -qi '^## Route: research' harness/agents/orchestrator.md &&
! grep -q '^## Routing decision table' harness/agents/orchestrator.md &&
[ "$(grep -c '^\*\*Discipline:\*\*' harness/agents/orchestrator.md)" = 5 ] &&
[ "$(grep -c '^\*\*Stop condition\*\*\|^### Stop condition\|Stop condition' harness/agents/orchestrator.md)" -ge 4 ] &&
python3 - <<'PY'
import re, sys, pathlib
body = pathlib.Path("harness/agents/orchestrator.md").read_text()
want = ["Outcome", "Artifacts or changes", "Verification",
        "Decisions and assumptions", "Risks or unresolved items", "Next command"]
# the six fields must appear as six CONSECUTIVE lines, in order, verbatim
lines = [ln.strip() for ln in body.splitlines()]
ok = any(lines[i:i+6] == want for i in range(len(lines)))
sys.exit(0 if ok else print("six-field completion contract not present verbatim/in order") or 1)
PY

! grep -q 'opencode-matt-workflows' harness/agents/orchestrator.md &&
! grep -q '`/flow`' harness/agents/orchestrator.md &&
uv run pytest tools/harness_lint -q
    </automated>
  </verify>
  <done>The 19-row table is gone; four `## Route:` headings exist and `research` is not one of them; the six-field contract is present verbatim; exactly five `**Discipline:**` sentences exist; no vendored path and no `/flow` citation appears; `harness_lint` (caps, commands, GEN-04/05) is green.</done>
</task>

<task type="auto">
  <name>Task 2: Fill each route's Repository evidence from live harness_config / contract_graph facts, then assert every citation resolves</name>
  <files>harness/agents/orchestrator.md</files>
  <action>
Replace each route's `TODO(Task 2)` placeholder with a real **Repository evidence** block (PROD-05,
D-11). This is the phase's differentiator and the single easiest thing to fumble into genericness:
the vendored flows are repo-agnostic; **these must cite facts only this harness can compute.**

Hard constraints:

- Cite **only** call sites that resolve today. The authorised surface is exactly:
  `tools.harness_config` → `components()`, `effective_relationships()`, `contract_graph_relationships()`,
  `languages()`, `language_bash_scopes()`, `pipeline()`, `load_project()`; and
  `tools.contract_graph` → `compile_graph()`, then `direct()` / `reverse()` / `transitive()`, each of
  which returns ids **and** connecting paths. Nothing else.
- **Neither package has a `__main__`.** `python -m tools.harness_config` and
  `python -m tools.contract_graph` would fail at runtime. Write every invocation as
  `uv run python -c "from tools.harness_config import components; ..."` form, or as a plain
  module+function citation in prose. Do not invent a CLI.
- Word each block so a future one-command form drops in **without a rewrite**: state the *question*
  the block answers first, then the calls that answer it today, then one sentence noting that a
  single-command form of this block is planned and these calls are the interface it preserves. Write
  that forward reference as unbackticked prose — a backticked slash-command would fail Task 3's
  citation loop.

Per-route content:

- **small-change** — which declared component owns the touched path, and which toolchain and bash
  scope that implies: `components()`, `languages()`, `language_bash_scopes()`.
- **bugfix** — the same ownership question, plus the reverse-dependency question: which contracts
  consume the thing being fixed, via `compile_graph()` then `reverse()`, so the failing test is
  written at the right level and the blast radius is known before the fix.
- **feature** — the declared pipeline shape and the upstream/downstream edges the new capability
  lands between: `pipeline()`, `components()`, `effective_relationships()`, plus `direct()`.
- **contract-change** — the full affected set with connecting paths: `compile_graph()` then
  `direct()` / `reverse()` / `transitive()` on the contract node, plus `effective_relationships()`
  for the declared edge and `components()` for the owning engineer on each side. State explicitly
  that a non-linear graph (branch, fan-in, or cycle) is why `transitive()` and its connecting paths
  are read rather than eyeballed. This block is the one that must not read generically.

Then assert mechanically (D-01, ROADMAP SC3 — *asserted mechanically, not by eye*). D-01's claim is
correct and already discharged by phases 43–45: at plan time the file carried **10 backticked
command citations** (`/add-language`, `/checkpoint`, `/contract-check`, `/fan-out-synthesize`,
`/lint`, `/new-contract-rule`, `/orient`, `/review`, `/test`, `/verify-work`) plus **2 module
citations** (`tools.harness_config`, `tools.contract_graph`) = the twelve D-01 counts, all resolving.
One precision note for the SUMMARY: D-01's *enumeration* also names `/component`, which is a live
command but is **not** actually cited in the file — the claim holds, the list is one name loose.
**Verify mechanically; do not redo the strip.** The verify block below is that assertion.
  </action>
  <verify>
    <automated>
! grep -q 'TODO(Task 2)' harness/agents/orchestrator.md &&
[ "$(grep -c 'Repository evidence' harness/agents/orchestrator.md)" -ge 4 ] &&
for c in $(grep -oE '`/[a-z][a-z0-9-]*`' harness/agents/orchestrator.md | tr -d '`/' | sort -u); do test -f "harness/commands/$c.md" || { echo "DANGLING /$c"; exit 1; }; done &&
for m in $(grep -oE 'tools\.[a-z_]+' harness/agents/orchestrator.md | sort -u); do test -d "tools/${m#tools.}" || { echo "DANGLING $m"; exit 1; }; done &&
! grep -qE 'python -m tools\.(harness_config|contract_graph)' harness/agents/orchestrator.md &&
grep -q 'transitive' harness/agents/orchestrator.md &&
uv run pytest tools/harness_lint tools/harness_config tools/contract_graph -q
    </automated>
  </verify>
  <done>All four routes carry a Repository evidence block built only from resolving `harness_config` / `contract_graph` calls; no `python -m` form of either package appears; every backticked `/command` resolves to `harness/commands/<name>.md` and every `tools.<module>` to a real package directory, proven by a loop.</done>
</task>

<task type="auto">
  <name>Task 3: Project to both runtime trees, regenerate the .ambr snapshot, commit green</name>
  <files>.opencode/agent/orchestrator.md, .claude/agents/orchestrator.md, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr</files>
  <action>
Source-first is now satisfied; project it (D-17).

1. `uv run python -m tools.harness_emit` — this rewrites both runtime persona files. It also
   re-splices the HARNESS-MANAGED block in `AGENTS.md` and `CLAUDE.md`; that block is a **pointer
   index of sorted names**, and this plan changes no name, so both should come back unchanged. If
   `git status` shows either as modified, include it in the pathspec — do not hand-revert it.
2. `uv run pytest tools/harness_emit -q --snapshot-update` — `test_emit_determinism.ambr` holds
   `test_projected_tree_matches_committed_snapshot`, which renders persona **bodies**, so the rewrite
   moves it. Regenerating in the same commit is the proven pattern from phases 43–45.
3. Full suite: `uv run pytest -q` → expect **876 passed / 7 snapshots** (baseline). No test count
   changes in this plan.
4. Emit-drift replica — the check CI actually runs (`.github/workflows/ci.yml:247`):
   `uv run python -m tools.harness_emit && git add -A && git diff --cached --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json`
   must exit 0. Reset the index afterwards (`git reset`) before committing with an explicit pathspec.
5. Commit — message BEFORE `--` (D-18); never `git add -A` as the commit path, never `git add .`,
   never `git commit -a`, never `git checkout <ref> -- .`:

   `git commit -m "feat(46): four product routes replace the orchestrator routing table (PROD-02, PROD-03, PROD-05)" -- harness/agents/orchestrator.md .opencode/agent/orchestrator.md .claude/agents/orchestrator.md tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`

   Add `AGENTS.md` / `CLAUDE.md` to that pathspec only if step 1 showed them modified.
6. `git status --porcelain` must be empty afterwards. A non-empty tree here means an emitter output
   was left out of the pathspec — the exact omission that reddened four Phase-44 commits.

This commit touches nothing under the reference instance, so no instance test leg is required
(D-20: `uv run pytest -q` collects `libs/python` + `tools` only). Do not add one for symmetry.
  </action>
  <verify>
    <automated>
uv run python -m tools.harness_emit &&
git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md &&
uv run pytest -q &&
uv run python -m tools.ruff_baseline &&
[ -z "$(git status --porcelain)" ]
    </automated>
  </verify>
  <done>Both runtime trees carry the rewritten persona byte-identically to a fresh emit; the `.ambr` snapshot is regenerated in the same commit; `uv run pytest -q` is green at 876; the emit-drift replica exits 0; the working tree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored prose → deployed agent behaviour | Route text is executed as instructions by a weak in-house model; an ambiguous stop condition is the failure mode, not a memory-safety bug. |
| vendored DEV reference → shipped product file | Text may be copied; a file dependency must never form. |
| source plane (`harness/`) → derived runtime trees | Only the emitter may write the derived side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | derived runtime trees | mitigate | Emit-drift replica in Task 3 verify; hand-edits to `.opencode/**` / `.claude/**` forbidden by D-17 and caught by `git diff --exit-code`. |
| T-46-02 | Information disclosure | vendored bundle coupling | mitigate | Task 1 forbids any `opencode-matt-workflows` path in authored prose; asserted by `! grep -q 'opencode-matt-workflows'`. |
| T-46-03 | Repudiation | dangling citations | mitigate | Task 2's resolution loop asserts every `/command` and `tools.<module>` mechanically rather than by eye (ROADMAP SC3). |
| T-46-04 | Elevation of privilege | persona permission block | accept | Frontmatter `permission` is kept byte-identical; `harness_lint/caps.py` validates it every run. |
| T-46-SC | Tampering | package installs | accept | This plan installs **zero** packages (net +0 dependencies per the phase constraint), so no legitimacy gate applies. |
</threat_model>

<verification>
- `grep -c '^## Route: ' harness/agents/orchestrator.md` → **4**; `research` is not among them.
- `grep -c '^\*\*Discipline:\*\*' harness/agents/orchestrator.md` → **5**.
- The six-field completion contract matches `WORKFLOW_CONTRACTS.md:39-46` word for word.
- The citation loop exits 0 for both commands and modules.
- `uv run pytest -q` → 876 passed / 7 snapshots; the ruff **ratchet** clean via `uv run python -m tools.ruff_baseline` (never a bare `ruff check .` — the repo carries 393 accepted findings; `ci.yml:192-217`).
- Emit-drift replica exits 0; working tree clean after the commit.
</verification>

<success_criteria>
ROADMAP SC1, SC2, SC3, SC4 and the PROD-05 half of SC7 are satisfied at this commit. SC5, SC6, the
count half of SC7 and SC8's whole-phase form are Plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/46-product-flow/46-01-SUMMARY.md` when done. Record: the measured line count
of the rewritten file, the exact set of backticked command citations after the rewrite, the D-01
enumeration correction (`/component` is live but uncited — the claim holds, the list is one name
loose), and the `git diff --shortstat` for this commit as the first input to D-24's whole-phase LOC
report.
</output>
