---
phase: 46-product-flow
plan: 03
type: execute
wave: 3
depends_on: [46-02]
files_modified:
  - .memory/state/activeContext.md
  - .memory/state/progress.md
autonomous: true
requirements: [PROD-02, PROD-03, PROD-04, PROD-05]
must_haves:
  truths:
    - "Locked decisions honored by this plan: D-10, D-13, D-20, D-22, D-23, D-24"
    - "Route, step and next command round-trip through .memory/state/activeContext.md via the existing /checkpoint writer and /orient reader — demonstrated by a run, not asserted from a read"
    - "No new state file, no new writer, no new reader was created anywhere in the phase"
    - "No file from docs/references/opencode-matt-workflows/ is imported, linked, or depended on by any shipped artifact"
    - "Every ROADMAP success criterion 1-8 is checked by a command whose output is recorded"
    - "Net surface change is +1 command and +0 gates, tools, contracts, skills, agents, hooks, or dependencies, proven by diffing the surface directories across the phase"
    - "Whole-phase LOC is reported from git diff --shortstat as a measured net addition"
    - "No mutation-proof table is owed: this phase adds prose and one command and adds no control"
  artifacts:
    - path: ".memory/state/activeContext.md"
      provides: "Route / step / next command recorded in the already-shipped state file"
      contains: "Route:"
    - path: ".memory/state/progress.md"
      provides: "Bounded last-N-done summary updated by the same existing writer"
  key_links:
    - from: "/checkpoint"
      to: ".memory/state/activeContext.md"
      via: "the existing writer — the three route fields, no new plumbing"
      pattern: "Route:|Step:|Next command:"
    - from: "/orient"
      to: ".memory/state/activeContext.md"
      via: "python -m tools.memory_regen.inject emits a POINTER to the file; the orchestrator reads it"
      pattern: "activeContext"
---

<objective>
Close the phase: demonstrate the route state round-trip through the artifacts that already ship
(D-10), then verify all eight ROADMAP success criteria mechanically and report the measured
whole-phase LOC (D-24).

This plan adds **no** capability. Its output is a state-plane commit and a set of recorded command
outputs. Nothing here enforces route adherence — that is deliberate and consistent with ADR-0012:
CI and the merge are the authority, and a route-compliance gate would be exactly the ceremony this
milestone removed (D-23). **No mutation-proof table is owed.**

Purpose: every phase from 40 to 45 found consumers its plan had not listed, and every one surfaced
from a **run** (D-21). This plan runs things rather than reading them.

Output: `.memory/state/` updated by the existing `/checkpoint` mechanism, and a verification record.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-product-flow/46-CONTEXT.md
@.planning/phases/46-product-flow/46-01-SUMMARY.md
@.planning/phases/46-product-flow/46-02-SUMMARY.md
@harness/commands/checkpoint.md
@harness/commands/orient.md
@.memory/state/activeContext.md

<interfaces>
<!-- Extracted at plan time. -->

`/orient`'s four regeneration steps, run verbatim:
```
uv run python -m tools.memory_regen.repo_map
uv run python -m tools.memory_regen.contracts_index
uv run python -m tools.memory_regen.pointer_index
uv run python -m tools.memory_regen.inject
```
`inject` emits a **pointer** to `.memory/state/activeContext.md` — never its body (`orient.md`
preamble; `activeContext.md`'s own DATA AUTHORITY banner says the same). The round-trip is:
`/checkpoint` writes the fields → `inject` surfaces the pointer → the orchestrator reads the file.
Do not overclaim an inlined body.

`/checkpoint`'s writer contract (`checkpoint.md` steps 1–2): quoted `updated: "YYYY-MM-DD"`
frontmatter on **both** state files; `activeContext.md` = what is in flight + what is next;
`progress.md` = `## Recently done (last 5)` (bounded, drop older — never append-only) + `## Remaining`;
then an explicit two-file `git add` and a `git commit` naming both paths. Never `git add -A`.

`.memory/derived/contracts-index.md` is **committed** and is regenerated by step 2 above; the
stale-derived CI gate diffs `docs/reference` and `.memory/derived/contracts-index.md`
(`ci.yml:283`). If regeneration moves it, it belongs in this commit's pathspec.

The three phase gate replicas:
- emit-drift: `uv run python -m tools.harness_emit` then `git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json`
- stale-derived: `uv run python -m tools.docs_sync` **and** `uv run python -m tools.memory_regen.contracts_index`,
  then `git diff --exit-code -- docs/reference .memory/derived/contracts-index.md`. CI regenerates
  with **both** (`ci.yml:279`); `docs_sync` is the only thing that regenerates `docs/reference`, so
  running only the `memory_regen` commands and then diffing `docs/reference` diffs a tree nobody
  regenerated — trivially clean, and vacuous on that half. The true gate including `docs_sync` exits
  0 today, so nothing is currently hidden; include it anyway so the replica can detect what it claims
  to guard.
- contract-drift: `uv run python -m tools.contract_drift.drift`
Lint gate is the RATCHET `uv run python -m tools.ruff_baseline`, never a bare `ruff check .`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demonstrate the route / step / next round-trip through the existing checkpoint → orient pair</name>
  <files>.memory/state/activeContext.md, .memory/state/progress.md</files>
  <action>
D-10 / ROADMAP SC6. Prove the round-trip by **running** it, using only what already ships.

1. Write the three route fields into `.memory/state/activeContext.md` under `## In flight`, exactly
   as `flow.md` instructs a driver to — for example:
   `- Route: small-change`, `- Step: 3 of 4 — verify`, `- Next command: /verify-work`.
   Also refresh the stale v2.1-era body: it still describes the MEM2 milestone and points at
   `/gsd:plan-phase 12`. Replace `## In flight` and `## Next` with the live v2.5 position, and set
   `updated: "YYYY-MM-DD"` (quoted, today's date) in the frontmatter of **both** state files.
2. Update `.memory/state/progress.md` per the same writer contract: `## Recently done (last 5)`
   bounded at five entries — **drop older entries, never append** — plus `## Remaining`.
3. Run `/orient`'s four regeneration commands verbatim (see `<interfaces>`). Capture `inject`'s
   output and confirm it carries a **pointer** naming `activeContext.md`. Then also run
   `uv run python -m tools.docs_sync` before the stale-derived diff — it is not one of `/orient`'s
   four, but it is the only regenerator of `docs/reference` and CI pairs it with
   `memory_regen.contracts_index` (`ci.yml:279`). Without it the `docs/reference` half of the diff
   is vacuous.
4. Read the three fields back out of `activeContext.md` and record them. That read-back is the
   round-trip's closing half: pointer surfaced by the existing reader, fields written by the existing
   writer, no new file and no new code on either side.
5. If step 3 moved `.memory/derived/contracts-index.md`, include it in the pathspec below (it is
   committed and gated by stale-derived); the rest of `.memory/derived/` is gitignored.
6. Commit — message BEFORE `--` (D-18), no `git add -A` / `git add .` / `git commit -a`:

   `git commit -m "chore(46): record the route/step/next state round-trip in the shipped state plane" -- .memory/state/activeContext.md .memory/state/progress.md`

**Create nothing.** No `.flow/state.md`, no new writer, no new reader, no schema for these three
fields. The whole point of D-10 is that the mechanism already exists.
  </action>
  <verify>
    <automated>
grep -q '^- Route: ' .memory/state/activeContext.md &&
grep -q '^- Step: ' .memory/state/activeContext.md &&
grep -q '^- Next command: ' .memory/state/activeContext.md &&
grep -qE '^updated: "[0-9]{4}-[0-9]{2}-[0-9]{2}"' .memory/state/activeContext.md &&
grep -qE '^updated: "[0-9]{4}-[0-9]{2}-[0-9]{2}"' .memory/state/progress.md &&
[ ! -e .flow ] &&
uv run python -m tools.memory_regen.repo_map &&
uv run python -m tools.memory_regen.contracts_index &&
uv run python -m tools.memory_regen.pointer_index &&
uv run python -m tools.memory_regen.inject | grep -q 'activeContext' &&
uv run python -m tools.docs_sync &&
git diff --exit-code -- docs/reference .memory/derived/contracts-index.md &&
uv run pytest -q &&
[ -z "$(git status --porcelain)" ]
    </automated>
  </verify>
  <done>`activeContext.md` carries `Route:`, `Step:` and `Next command:` lines written through the existing `/checkpoint` contract; `inject` surfaces a pointer naming that file; `.flow/` does not exist; the stale-derived replica exits 0; the suite is green and the tree is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all eight success criteria mechanically and report the measured whole-phase LOC</name>
  <files>.planning/phases/46-product-flow/46-03-SUMMARY.md</files>
  <action>
Run every check and record its literal output in the SUMMARY. Do not paraphrase a command you did
not run — a scratch-clone replay will falsify anything hand-waved.

**SC1–SC4 (orchestrator.md):**
- `grep -c '^## Route: ' harness/agents/orchestrator.md` → 4; the four names present, `research`
  absent as a heading.
- Six-field contract present verbatim (fixed-string match against the six lines in order).
- `grep -c '^## Routing decision table' harness/agents/orchestrator.md` → 0.
- The citation loop (both commands and modules) exits 0.
- `grep -c '^\*\*Discipline:\*\*' harness/agents/orchestrator.md` → 5.

**SC5 (surface):** `ls harness/commands/*.md | wc -l` → **18**.

**SC6 (state):** the three fields plus the pointer output from Task 1.

**SC7 (evidence + no vendored import):** each route's *Repository evidence* names only
`harness_config` / `contract_graph` calls that resolve. Then the isolation sweep — and **exclude the
vendored bundle from the sweep itself** (D-13: 79 files of third-party prose inside `docs/`, the
repo's largest false-positive source):
```
git grep -n 'opencode-matt-workflows' -- harness tools libs contracts .opencode .claude/agents .claude/commands .claude/skills README.md AGENTS.md
```
→ **zero hits**. Also confirm nothing under `docs/references/opencode-matt-workflows/` changed in
this phase: `git diff --stat ee5a41c..HEAD -- docs/references/opencode-matt-workflows` → empty.

**SC8 (green + net surface):**
- `uv run pytest -q` → zero failures (record the count; the phase tip is **881** passed / 7
  snapshots — 876 at `ee5a41c`, +5 parametrized ids added by `flow.md` in Plan 02: four in
  `test_commands.py`, one in `test_agent_referential_integrity.py`. Not 877).
- Instance leg (D-20 — root `testpaths` is `libs/python` + `tools`, so the instance is **not**
  collected by the root run): `uv run pytest examples/log-parser -q` → **31** passed. Run the whole
  instance directory, **not** `examples/log-parser/tests`: that path alone is **14** (measured), and
  the 31 is 14 (`tests`) + 17 (`golden_runner`), which CI runs as two separate steps
  (`ci.yml:168,170`). Scoping to `tests` silently skips the .NET golden-parity leg while still
  looking like a pass. Either form is acceptable so long as **both** legs run. No commit in this
  phase touches the instance, so this leg is a **confirmation of no regression**, not a requirement
  of any commit; run it once here.
- The three gate replicas (emit-drift, stale-derived, contract-drift) each exit 0, plus
  `uv run python -m tools.ruff_baseline` and `uv run pytest tools/ruff_baseline -q`.
- Net surface, measured by diffing the phase range rather than asserted:
  `git diff --name-status ee5a41c..HEAD -- harness/agents harness/skills harness/plugins harness/git-hooks contracts .github/workflows uv.lock pyproject.toml`
  → only **M** entries for `harness/agents/orchestrator.md`; **no A/D anywhere**, and `uv.lock`
  absent. `git diff --name-status ee5a41c..HEAD -- harness/commands` → exactly one **A**
  (`harness/commands/flow.md`). That pair is the `+1 command, +0 everything else` proof.

**D-24 (LOC):** `git diff --shortstat ee5a41c..HEAD` — report the measured whole-phase number and
state whether it is a net addition. Expect a small net **addition**: this is the milestone's only
additive phase, against seven that removed ~25k LOC.

**D-23:** state explicitly in the SUMMARY that no mutation-proof table is owed — this phase adds
prose and one command and adds no control, so there is no gate whose bypass could be demonstrated.

If any check fails, stop and report rather than patching around it.

This task writes only the SUMMARY; nothing to commit beyond it (the phase-doc commit is the
orchestrator's, not this plan's).
  </action>
  <verify>
    <automated>
[ "$(ls harness/commands/*.md | wc -l | tr -d ' ')" = 18 ] &&
[ "$(grep -c '^## Route: ' harness/agents/orchestrator.md)" = 4 ] &&
[ "$(grep -c '^\*\*Discipline:\*\*' harness/agents/orchestrator.md)" = 5 ] &&
! grep -q '^## Routing decision table' harness/agents/orchestrator.md &&
! git grep -q 'opencode-matt-workflows' -- harness tools libs contracts .opencode .claude/agents .claude/commands .claude/skills README.md AGENTS.md &&
[ "$(git diff --name-status ee5a41c..HEAD -- harness/commands | grep -c '^A')" = 1 ] &&
[ "$(git diff --name-status ee5a41c..HEAD -- harness/agents harness/skills harness/plugins harness/git-hooks contracts .github/workflows uv.lock | grep -cE '^(A|D)')" = 0 ] &&
uv run python -m tools.harness_emit &&
git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md &&
uv run python -m tools.contract_drift.drift &&
uv run python -m tools.ruff_baseline &&
uv run pytest -q &&
uv run pytest examples/log-parser -q
    </automated>
  </verify>
  <done>All eight success criteria have a recorded command and output; the vendored-isolation sweep returns zero hits with the bundle itself excluded; the surface diff proves exactly one added command and zero added or deleted gates, tools, contracts, skills, agents, hooks, or dependencies; the whole-phase `git diff --shortstat` is reported as a measured net addition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session state → next session's agent | `activeContext.md` is a hint, not authority; `contracts/` and `docs/adr/` win on conflict. |
| phase claims → merge authority | CI and the human merge are the authority (ADR-0012); a self-reported success criterion is not. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-09 | Information disclosure | `.memory/state/activeContext.md` | mitigate | The file's own banner forbids secrets, tokens, credentials and PII; the three route fields carry none. |
| T-46-10 | Spoofing | self-reported verification | mitigate | Every criterion is a recorded command output, replayable in a scratch clone; the surface claim is a `git diff --name-status` over the phase range, not a narrative. |
| T-46-11 | Tampering | derived plane | mitigate | The stale-derived replica diffs `docs/reference` and the committed `contracts-index.md` after regeneration. |
| T-46-12 | Repudiation | route adherence | accept | Nothing enforces it, deliberately (D-23, ADR-0012). A route-compliance gate is the ceremony this milestone removed. |
| T-46-SC | Tampering | package installs | accept | Zero packages installed across the whole phase; `uv.lock` absent from the phase-range diff is the check. |
</threat_model>

<verification>
- The three route fields are present in `activeContext.md` and `inject` surfaces a pointer to it.
- `.flow/` does not exist; no new writer or reader was added.
- Zero hits for `opencode-matt-workflows` outside the vendored bundle itself.
- `git diff --name-status ee5a41c..HEAD -- harness/commands` = exactly one `A`.
- `uv run pytest -q` green at **881**; instance leg `uv run pytest examples/log-parser -q` → 31
  passed (both legs: 14 `tests` + 17 `golden_runner`); emit-drift, stale-derived (with `docs_sync`),
  contract-drift and the ruff ratchet all exit 0.
- `git diff --shortstat ee5a41c..HEAD` reported as a measured net addition.
</verification>

<success_criteria>
ROADMAP SC6, SC7 and SC8 are satisfied here, and SC1–SC5 are re-asserted mechanically at the phase
tip. PROD-02, PROD-03, PROD-04 and PROD-05 are each traceable to a recorded command output.
</success_criteria>

<output>
Create `.planning/phases/46-product-flow/46-03-SUMMARY.md` when done. It carries the eight-criterion
verification record, the D-24 whole-phase LOC line, the D-23 statement that no mutation-proof table
is owed, and the per-commit greenness observations actually measured.
</output>
