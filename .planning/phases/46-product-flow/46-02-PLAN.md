---
phase: 46-product-flow
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - harness/commands/flow.md
  - .opencode/command/flow.md
  - .claude/commands/flow.md
  - tools/harness_emit/emit-manifest.json
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - AGENTS.md
  - README.md
autonomous: true
requirements: [PROD-04]
must_haves:
  truths:
    - "Locked decisions honored by this plan: D-09, D-10, D-14, D-15, D-16, D-17, D-18, D-20, D-21"
    - "harness/commands/flow.md exists and is the only command added; ls harness/commands/*.md returns 18"
    - "flow.md names the four routes, points at the orchestrator's route sections, and records route / step / next command in .memory/state/activeContext.md via the existing /checkpoint, read back via the existing /orient — no new file, writer, or reader"
    - "Every artifact that renders the live command set with a hardcoded expectation is repaired in the same commit that adds flow.md"
    - "test_coexist.py is updated at all four sites including the function NAME, which every prior phase forgot"
    - "emit-manifest.json gains exactly two rows and is in the commit pathspec"
    - "AGENTS.md's HARNESS-MANAGED command list and README.md's hand-maintained command list both gain flow"
    - "Net surface change is +1 command and +0 gates, tools, contracts, skills, agents, hooks, or dependencies"
  artifacts:
    - path: "harness/commands/flow.md"
      provides: "The product's named entry point — the one command PROD-04 authorises"
      contains: "agent: orchestrator"
    - path: "tools/harness_emit/emit-manifest.json"
      provides: "Ownership manifest, +2 rows (one per runtime tree)"
      contains: ".opencode/command/flow.md"
    - path: "tools/harness_emit/tests/test_coexist.py"
      provides: "Command-count coexistence gate, 17 → 18 at all four sites"
      contains: "test_all_18_commands_emit_to_both_trees"
  key_links:
    - from: "harness/commands/flow.md"
      to: "harness/agents/orchestrator.md"
      via: "agent: orchestrator frontmatter + route-section pointer"
      pattern: "agent: orchestrator"
    - from: "harness/commands/flow.md"
      to: ".memory/state/activeContext.md"
      via: "the existing /checkpoint writer and /orient reader — no new plumbing"
      pattern: "activeContext"
    - from: "harness/commands/flow.md"
      to: "tools/harness_emit/emit-manifest.json"
      via: "python -m tools.harness_emit, +2 owned paths"
      pattern: "command/flow.md"
---

<objective>
Add `harness/commands/flow.md` — **exactly one** new command (D-09), the deployed product's named
entry point — and repair, in the **same commit**, every committed artifact that renders the live
command set with a hardcoded expectation.

Phases 43–45 produced twenty-plus blockers of one class: a committed artifact rendering the live tree
with a hardcoded expectation, invalidated by a change, named by no plan (D-16). This phase reverses
the direction — an addition, not a deletion — but the class is identical. The enumeration below is
complete as of plan time and includes **two instances D-14 does not name**.

Purpose: the driver is the weakest model in the picture and has no GSD habit to fall back on. It
needs one command that tells it which of the four routes it is on and what the literal next step is.

Output: 18 source commands, both runtime trees projected, and five hardcoded renderers repaired
together.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-product-flow/46-CONTEXT.md
@.planning/phases/46-product-flow/46-01-SUMMARY.md
@harness/commands/orient.md
@harness/commands/checkpoint.md
@tools/harness_emit/tests/test_coexist.py

<interfaces>
<!-- Extracted at plan time. Do not go exploring; in particular do not go hunting for an
     EXPECTED_COMMANDS frozenset — it does not exist (D-15, confirmed three times since Phase 14). -->

Command frontmatter contract, enforced by `tools/harness_lint/tests/test_commands.py` (glob-driven —
it discovers `harness/commands/*.md` and covers a new file with **zero test edits**):
- frontmatter parses to a non-empty mapping;
- `description` is non-empty AND contains a routing trigger token — literally `use` or `when`
  (case-insensitive substring). A bare label fails.
- `agent` is present, a non-empty string, and a bare lowercase-alnum-plus-single-hyphen slug.
- `subtask`, if present, must be a **boolean**.
- `EXPECTED_GOLDEN_ADJACENT` (`test_commands.py:44`) = {build, test, lint, adr, checkpoint,
  component}. **`flow` must NOT be added to it** — that frozenset is the golden-adjacent six, not a
  command census.

`tools/harness_lint/caps.py` defines `EXPECTED_PERSONAS` and `EXPECTED_SKILLS` only. There is **no**
`EXPECTED_COMMANDS`, so a command addition does not trip the emitter's pre-write hard-fail (D-15).

`.memory/state/activeContext.md` — already installed (`tools/adoption_scan/destinations.py:151`,
`.memory/state/**/*` in `_CATEGORY_GLOBS`). Written by `/checkpoint` (`checkpoint.md` steps 1–2:
frontmatter `updated: "YYYY-MM-DD"` quoted, `## In flight`, `## Next`, then an explicit two-file
`git add` + `git commit`). Read side: `/orient` runs `python -m tools.memory_regen.inject`, whose
payload carries a **pointer** to `activeContext.md`, never its body — the orchestrator then reads the
file. Word the round-trip that way; do not claim the injector inlines the body.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author harness/commands/flow.md — the one authorised command</name>
  <files>harness/commands/flow.md</files>
  <action>
Author the source command only (D-17). Frontmatter, satisfying the lint contract above:

```
---
description: >-
  Use when starting or resuming any unit of product work — names the four routes
  (small-change, bugfix, feature, contract-change), picks one, and states the literal next step.
  Invoke when you do not know which route you are on.
agent: orchestrator
subtask: false
---
```

Body — short, imperative, written for a weak model with no GSD habit. Favour explicit stop
conditions and a literal next command over elegance:

1. **Pick a route.** A four-row table: route → one-line trigger → the persona that owns it. Point at
   `harness/agents/orchestrator.md`'s `## Route:` sections as the authority for the steps; do not
   restate them here. If none of the four fits, **stop and ask** — there is no fifth route, and
   `research` is deliberately not one (D-06).
2. **Record where you are.** Route · step · next command go in `.memory/state/activeContext.md`
   under `## In flight` / `## Next`, written by `/checkpoint` (D-10). State plainly: **no new state
   file, no new writer, no new reader.** Give the literal three lines to write, e.g.
   `- Route: contract-change`, `- Step: 3 of 5 — failing case`, `- Next command: /contract-check`.
3. **Resume.** `/orient` regenerates the derived plane and prints the pointer payload; the pointer
   names `activeContext.md`, and you read those three lines from it to know the route, the step, and
   the next command. Do not claim the injector inlines the body — it injects a pointer only.
4. **Finish.** Every handoff returns the six-field completion contract defined in the orchestrator's
   `### Completion contract`. Reference it; do not re-copy it.

Constraints:
- **No `!`-prefixed shell blocks.** `/checkpoint` and `/orient` own the execution; this command is a
  router. This also keeps `tools/adoption_scan/tests/test_install_completeness.py` (which regex-walks
  `harness/commands/**/*.md` for `python -m tools.X` and asserts each resolves) trivially satisfied.
- GEN-04/GEN-05 (D-19): `harness/commands/flow.md` is inside the scanned core set. Never write the
  literal token `examples/`, nor `equipment`, `wafer`, `설비`, `libs/dotnet`, `standard-log`,
  `correction-rules`, `normalization-catalog`, `pipeline-patterns`, `dotnet-engineer`,
  `dotnet-conventions`. Say "the declared instance".
- Add **nothing else**: no `.flow/state.md`, no router agent, no skill, no contract, no CI job, no
  hook, no dependency (D-09). Net **+1 command, +0** everything else.
- Do **not** add `flow` to `EXPECTED_GOLDEN_ADJACENT`, and do **not** create `EXPECTED_COMMANDS`.
  </action>
  <verify>
    <automated>
test -f harness/commands/flow.md &&
[ "$(ls harness/commands/*.md | wc -l | tr -d ' ')" = 18 ] &&
! grep -q '^!\|^\s*!`' harness/commands/flow.md &&
! grep -qE 'examples/|equipment|wafer|libs/dotnet|correction-rules|standard-log' harness/commands/flow.md &&
grep -q 'activeContext' harness/commands/flow.md &&
grep -q 'agent: orchestrator' harness/commands/flow.md &&
! grep -q 'EXPECTED_COMMANDS' tools/harness_lint/caps.py &&
! grep -q '"flow"' tools/harness_lint/tests/test_commands.py &&
uv run pytest tools/harness_lint -q
    </automated>
  </verify>
  <done>`harness/commands/flow.md` exists, is auto-covered by the glob-driven command lint with zero test edits there, names the four routes and the activeContext round-trip, carries no shell block, and `ls harness/commands/*.md` returns 18.</done>
</task>

<task type="auto">
  <name>Task 2: Repair every hardcoded renderer of the live command set, emit, and commit as one green change</name>
  <files>tools/harness_emit/tests/test_coexist.py, tools/harness_emit/emit-manifest.json, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, AGENTS.md, README.md, .opencode/command/flow.md, .claude/commands/flow.md</files>
  <action>
The complete enumeration as of plan time. All five land in the **same commit** as `flow.md`.

**(a) `tools/harness_emit/tests/test_coexist.py` — four sites, 17 → 18 (D-14).**
- `:40` — the function **NAME**: `test_all_17_commands_emit_to_both_trees` →
  `test_all_18_commands_emit_to_both_trees`. *This is the site every prior phase forgot.* A stale
  name still passes, so nothing reds — it just lies.
- `:41` — the first docstring line: "17 commands land in ..." → 18.
- `:73` — `assert len(opencode_cmds) == 17` and its f-string message → 18.
- `:74` — `assert len(claude_cmds) == 17` and its f-string message → 18.
- Append one sentence to the existing phase-history docstring paragraph (which currently ends at the
  Phase-44 19 → 17 line): Phase 46 adds `/flow`, the product's named entry point (PROD-04), taking it
  17 → 18.
- Also fix the **module docstring at `:3`**, which says "its 20 harness commands". It is already
  wrong (live is 17) — a sixth instance of the same class, stale since Phase 41. It asserts nothing,
  so it reds nothing; fix it to 18 while the file is open rather than leaving a known-false literal
  behind.

**(b) `tools/harness_emit/emit-manifest.json` — exactly +2 rows** (`.claude/commands/flow.md` and
`.opencode/command/flow.md`). It **self-generates** — do not hand-edit it — but it **must be in the
commit pathspec**, or the plan's own `git diff --exit-code` reds. This exact omission reddened four
Phase-44 commits.

**(c) `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`** — renders command
**bodies**, so regenerate with `--snapshot-update` inside this commit.

**(d) `AGENTS.md` HARNESS-MANAGED block (`:101-110`) — NOT named by D-14.** Its `**Commands**` line
is a sorted name list currently ending `..., review, test, verify-work`; it gains `flow` (after
`fan-out-synthesize`). It is emitter-spliced (`tools/harness_emit/generate.py:297`, Regime B) — do
not hand-edit; the emitter rewrites it. But `AGENTS.md` **is** in the emit-drift CI pathspec
(`ci.yml:247`), so it must be in the commit pathspec. Check `CLAUDE.md` too: its managed block is
pointer-only prose with no name list, so it should come back unchanged — if `git status` says
otherwise, include it rather than reverting it.

**(e) `README.md:119-122` — NOT named by D-14, and hand-maintained, not generated.** It spells out
the live command set as an explicit 17-name list ending "and `/verify-work`." Add `` `/flow`, ``
in sorted position (between `/docs-sync` and `/fan-out-synthesize`). Nothing asserts this list, which
is precisely why it rots.

**Checked and cleared — do NOT chase these:**
- `tools/harness_lint/caps.py` — no `EXPECTED_COMMANDS` (D-15).
- `tools/harness_lint/tests/test_commands.py` — glob-driven; zero edits (and `flow` must not join
  `EXPECTED_GOLDEN_ADJACENT`).
- `tools/adoption_scan/destinations.py` — `harness/commands/**/*` is a glob; zero edits.
- `tools/adoption_scan/tests/__snapshots__/test_snapshots.ambr` — renders a hand-listed
  `_FIXED_CATALOG` deliberately decoupled from the live repo (`test_snapshots.py:16-26`); unaffected.
- `tools/docs_sync/`, `tools/memory_regen/` snapshots — contracts/docs planes, no command census.

**Then:**
1. `uv run python -m tools.harness_emit` → writes `.opencode/command/flow.md`,
   `.claude/commands/flow.md`, the +2 manifest rows, and the `AGENTS.md` block.
2. `uv run pytest tools/harness_emit -q --snapshot-update`.
3. `uv run pytest -q` → expect **877 passed** (876 + 1: `test_commands.py` is parametrized per
   command file, so `flow.md` adds new parametrized ids; the renamed `test_coexist` function is a
   rename, not an addition). Treat the exact number as an observation to record, not a gate — the
   gate is zero failures.
4. `uv run python -m tools.ruff_baseline` exits 0 — that RATCHET is the lint gate (`ci.yml:198-217`),
   plus `uv run pytest tools/ruff_baseline -q` (27 tests). **Never run a bare `ruff check .`**: the
   repo carries 393 accepted findings and the ratchet, not a clean check, is the contract.
   `ruff format --check` is not a gate either.
5. Emit-drift replica must exit 0:
   `uv run python -m tools.harness_emit && git add -A && git diff --cached --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` — then `git reset`.
6. Commit, message BEFORE `--` (D-18), no `git add -A` / `git add .` / `git commit -a` /
   `git checkout <ref> -- .`:

   `git commit -m "feat(46): /flow — the product's named entry point (PROD-04)" -- harness/commands/flow.md .opencode/command/flow.md .claude/commands/flow.md tools/harness_emit/emit-manifest.json tools/harness_emit/tests/test_coexist.py tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr AGENTS.md README.md`

   Add `CLAUDE.md` only if step 1 modified it.
7. `git status --porcelain` must be empty. Non-empty = a renderer was left out.

This commit touches nothing under the reference instance, so no instance leg is required (D-20).
  </action>
  <verify>
    <automated>
grep -q 'def test_all_18_commands_emit_to_both_trees' tools/harness_emit/tests/test_coexist.py &&
! grep -q '17 commands\|== 17' tools/harness_emit/tests/test_coexist.py &&
grep -q '"\.opencode/command/flow\.md"' tools/harness_emit/emit-manifest.json &&
grep -q '"\.claude/commands/flow\.md"' tools/harness_emit/emit-manifest.json &&
grep -q 'fan-out-synthesize, flow, lint' AGENTS.md &&
grep -q '`/flow`' README.md &&
uv run python -m tools.harness_emit &&
git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md &&
uv run pytest -q &&
uv run python -m tools.ruff_baseline &&
[ -z "$(git status --porcelain)" ]
    </automated>
  </verify>
  <done>All four `test_coexist.py` sites read 18 including the function name, the module docstring's stale 20 is corrected, the manifest carries exactly two new rows, the `.ambr` is regenerated, `AGENTS.md`'s generated command list and `README.md`'s hand-maintained one both name `flow`, the full suite is green with zero failures, the emit-drift replica exits 0, and the working tree is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source command → both runtime trees | Only the emitter may write the derived side; a hand-edit is drift. |
| committed count literals → live command set | A hardcoded expectation that nothing re-derives is the D-16 defect class. |
| product command surface → phase constraint | The surface may not grow beyond the one command PROD-04 names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05 | Tampering | `emit-manifest.json` | mitigate | Self-generated, never hand-edited, but explicitly in the commit pathspec; `git status --porcelain` empty is the check. |
| T-46-06 | Repudiation | `test_coexist.py` function name | mitigate | The name is renamed, not just the assertions — a stale name passes silently and is how this class survives phases. |
| T-46-07 | Elevation of privilege | new command surface | mitigate | `flow.md` carries no `!` shell block and no new permission; `agent: orchestrator` inherits the existing `edit: ask` / `bash: ask` matrix. |
| T-46-08 | Spoofing | vendored bundle | mitigate | No file from `docs/references/opencode-matt-workflows/` is imported, linked, or depended on; that bundle is excluded from every sweep in this plan (D-13). |
| T-46-SC | Tampering | package installs | accept | Zero packages installed; `uv.lock` must be unchanged (net +0 dependencies). |
</threat_model>

<verification>
- `ls harness/commands/*.md | wc -l` → **18**.
- `git show --stat HEAD` names exactly the eight paths in the pathspec (plus `CLAUDE.md` only if the
  emitter moved it) — no more, no fewer.
- `grep -c flow tools/harness_emit/emit-manifest.json` → **2**.
- `git diff HEAD~1 -- uv.lock` empty; no new file under `contracts/`, `harness/skills/`,
  `harness/agents/`, `harness/git-hooks/`, or `.github/workflows/`.
- `uv run pytest -q` zero failures; `uv run python -m tools.ruff_baseline` exits 0.
</verification>

<success_criteria>
ROADMAP SC5 (the 18-count) and the `+1 command, +0 everything else` half of SC8 are satisfied at this
commit. SC6's round-trip and the whole-phase form of SC8 are Plan 03.
</success_criteria>

<output>
Create `.planning/phases/46-product-flow/46-02-SUMMARY.md` when done. Record: the enumerated renderer
list with which two were **not** named by D-14 (`AGENTS.md`'s generated block and `README.md:119-122`)
plus the sixth found in-file (`test_coexist.py:3`'s stale "20"), the exact post-run test count, the
manifest delta, and this commit's `git diff --shortstat` for D-24.
</output>
