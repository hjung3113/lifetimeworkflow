---
phase: 09-self-maintaining-derived-artifacts-curator-v2-0
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - tools/harness_lint/caps.py
  - harness/agents/curator.md
  - harness/commands/refresh-memory.md
  - harness/commands/verify-work.md
  - harness/skills/two-plane-memory/SKILL.md
  - tools/harness_lint/tests/test_agents.py
  - tools/harness_lint/tests/test_derived_freshness.py
  - tools/harness_emit/emit-manifest.json
  - .opencode/agent/curator.md
  - .opencode/command/refresh-memory.md
  - .opencode/command/verify-work.md
  - .claude/agents/curator.md
  - .claude/commands/refresh-memory.md
  - .claude/commands/verify-work.md
  - .claude/skills/two-plane-memory/SKILL.md
  - AGENTS.md
  - CLAUDE.md
  - opencode.json
autonomous: true
requirements: [MAINT-01, MAINT-03, MAINT-04]

must_haves:
  truths:
    - "A read-mostly `curator` persona exists as the single owner of derived freshness, writing derived paths only (D-04, D-05)"
    - "`/refresh-memory` regenerates the full derived set by invoking ONLY existing tools (no new derivation logic) (D-04, D-06)"
    - "`/verify-work` includes a derived-freshness step mirroring the CI stale-derived gate (D-10)"
    - "No new heavy on-write memory hook exists (memory regen is deferred to PR/CI + local /refresh-memory) (D-09)"
    - "curator + refresh-memory + verify-work round-trip the emitter to BOTH runtimes with no model identifier; GEN-04 stays green (D-11)"
  artifacts:
    - path: "harness/agents/curator.md"
      provides: "read-mostly curator persona (edit+bash allow, write deny, no model id, domain-neutral)"
      contains: "curator"
    - path: "harness/commands/refresh-memory.md"
      provides: "macro over repo_map + docs_sync + contracts_index + inject (agent: curator)"
      contains: "tools.memory_regen"
    - path: "tools/harness_lint/caps.py"
      provides: "EXPECTED_PERSONAS bumped 4→5 (curator admitted; READ_ONLY_PERSONAS unchanged)"
      contains: "curator"
    - path: "tools/harness_lint/tests/test_derived_freshness.py"
      provides: "body-invokes-only-tools scan + hook-posture (no memory regen on on-write path)"
      contains: "memory_regen"
    - path: "tools/harness_emit/emit-manifest.json"
      provides: "regenerated manifest listing curator + refresh-memory paths in both runtimes"
      contains: ".opencode/agent/curator.md"
  key_links:
    - from: "harness/agents/curator.md"
      to: "tools.memory_regen + tools.docs_sync"
      via: "invocation-only regeneration (D-06); constitution write denied by path_deny_globs"
      pattern: "tools\\.(memory_regen|docs_sync)"
    - from: "harness/commands/verify-work.md step 5"
      to: "docs/reference + .memory/derived/contracts-index.md"
      via: "regen + git add -A + git diff --cached --exit-code freshness check"
      pattern: "git diff --cached --exit-code"
    - from: "tools/harness_emit (re-emit)"
      to: ".opencode/ + .claude/ + emit-manifest.json"
      via: "python -m tools.harness_emit projects curator + commands to both runtimes"
      pattern: "curator"
---

<objective>
Author the entire NEW harness/ source surface for this phase — the `curator` persona, the `/refresh-memory`
command, the `/verify-work` freshness step, and the two-plane-memory committed-derived doc — bump
`EXPECTED_PERSONAS` to admit curator, add the boundary/hook-posture tests, then round-trip the Phase-7
emitter ONCE to both runtimes.

Purpose: This plan is the SOLE emitter-round-trip owner for Phase 9 (every harness/ source edit lives here so
the re-emit happens exactly once, avoiding `.opencode/`/`.claude/`/manifest conflicts). It delivers MAINT-01
(curator), MAINT-03 (hook posture — no new on-write memory hook), and MAINT-04 (/refresh-memory + /verify-work
freshness), all reuse-only per D-04/D-05/D-06/D-09/D-10/D-11.

Output: curator + refresh-memory emitted to both runtimes (no model id, GEN-04 green), verify-work freshness step,
caps bump, new tests, regenerated emit-manifest.

Note on scope: `files_modified` lists 18 paths, but 11 of them (`.opencode/**`, `.claude/**`, `AGENTS.md`,
`CLAUDE.md`, `opencode.json`, `emit-manifest.json`) are NOT hand-authored — they are produced by the single
`uv run python -m tools.harness_emit` invocation in Task 3 (do NOT hand-edit any emitted file, per the Phase-7
emit-drift precedent). Only 7 files are genuinely hand-authored across Tasks 1–2; the high count reflects the
consolidated sole-emitter-owner design, not shallow breadth.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-CONTEXT.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md
@.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md

<interfaces>
<!-- caps.py line 54: EXPECTED_PERSONAS = frozenset({"orchestrator","python-engineer","code-reviewer","explorer"}) -->
<!-- caps.py line 57: READ_ONLY_PERSONAS = frozenset({"code-reviewer","explorer"}) — DO NOT add curator here -->
<!-- Persona shape analog: harness/agents/python-engineer.md (edit+bash) + harness/agents/code-reviewer.md (read-mostly boundary prose, dual-representation frontmatter). -->
<!-- Command shape analog: harness/commands/orient.md (frontmatter agent+subtask; !`uv run python -m tools.*` steps). -->
<!-- test_agents.py assertions in force: test_expected_personas_present_no_sprawl (names == EXPECTED_PERSONAS), test_single_primary_carries_conductor_signal (only orchestrator is primary), test_description_is_routing_signal (line ~89), test_no_real_model_identifier (line ~120), test_read_only_personas_have_no_write_affordance (line ~130). -->
<!-- Emitter: iter_agents/iter_commands auto-discover harness/agents/*.md + harness/commands/*.md; project_command keeps agent+subtask for opencode, drops for Claude; validate.check_agent loud-fails on real model id. Entrypoint: python -m tools.harness_emit. -->
<!-- verify-work.md: presence-safe numbered `!`-shell steps 1-4; step 4 is the .NET-gated /golden step (mirror its nullglob/announce shape). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump EXPECTED_PERSONAS + author curator.md + persona-boundary tests</name>
  <files>tools/harness_lint/caps.py, harness/agents/curator.md, tools/harness_lint/tests/test_agents.py</files>
  <read_first>
    - tools/harness_lint/caps.py (EXPECTED_PERSONAS line 54, READ_ONLY_PERSONAS line 57, is_read_only line ~84)
    - harness/agents/python-engineer.md (edit+bash dual-representation frontmatter shape)
    - harness/agents/code-reviewer.md (read-mostly boundary prose lines ~32-35; description routing-signal voice)
    - harness/permission-matrix.json (`path_deny_globs`: contracts/**, docs/adr/**, golden/**, *.env — the global constitution deny)
    - tools/harness_lint/tests/test_agents.py (the assertions listed in <interfaces>)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md (§ curator.md + § caps.py + § test_agents.py deltas)
  </read_first>
  <action>
    (a) In `tools/harness_lint/caps.py`, add `"curator"` to `EXPECTED_PERSONAS` (→ 5 members) and update the
    "four enumerated" comment to "five", noting curator = derived-freshness owner. Leave `READ_ONLY_PERSONAS`
    UNCHANGED (curator writes derived → `is_read_only()` must return False).
    (b) Author `harness/agents/curator.md` on the python-engineer dual-representation shape: `name: curator`,
    `mode: subagent` (NOT primary — orchestrator stays sole primary), `description:` carrying a routing token
    (`Use when the derived plane needs refresh...`), `permission: { read: allow, edit: allow, bash: {"*": ask,
    "uv *": allow, "python -m *": allow}, write: deny }`, `tools: Read, Edit, Bash, Grep, Glob`. NO real `model:`
    key (omit, or only the `provider/explorer-tier` placeholder). Body states the D-05/D-06 boundary in prose:
    writes DERIVED paths only; regenerates ONLY by invoking `tools.memory_regen` + `tools.docs_sync`; never
    hand-edits a derived file; never writes `contracts/`, `docs/adr/`, `golden/`, or source (deny is global data,
    prose is advisory). Keep domain-neutral — no `examples/`, `equipment`, `standard-log`, `dotnet` tokens (GEN-04).
    (c) In `tools/harness_lint/tests/test_agents.py`, add assertions: `"curator" in EXPECTED_PERSONAS`,
    `"curator" not in READ_ONLY_PERSONAS`, curator frontmatter `mode == "subagent"` (single-primary stays 1), and
    that constitution paths (`contracts/**`, `docs/adr/**`, `golden/**`) are present in `permission-matrix.json`
    `path_deny_globs`.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_agents.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `caps.py` `EXPECTED_PERSONAS` contains `curator` (5 members); `READ_ONLY_PERSONAS` unchanged (2 members).
    - `harness/agents/curator.md` exists: `mode: subagent`, `edit: allow`, `write: deny`, no real model id, domain-neutral.
    - `test_agents.py` asserts curator NOT read-only + constitution paths in `path_deny_globs`; `uv run pytest tools/harness_lint/tests/test_agents.py -x` exits 0.
  </acceptance_criteria>
  <done>curator is admitted as the 5th persona, is read-mostly (edit+bash, write deny), carries no model id, and stays domain-neutral; the anti-sprawl + single-primary gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author /refresh-memory + /verify-work freshness step + two-plane doc + boundary/hook tests</name>
  <files>harness/commands/refresh-memory.md, harness/commands/verify-work.md, harness/skills/two-plane-memory/SKILL.md, tools/harness_lint/tests/test_derived_freshness.py</files>
  <read_first>
    - harness/commands/orient.md (exact analog: frontmatter agent+subtask; ordered `!`uv run python -m tools.*` steps)
    - harness/commands/verify-work.md (steps 1-4; the presence-safe .NET-gated /golden step 4 to mirror; intro prose "Run the four gates")
    - harness/skills/two-plane-memory/SKILL.md (plane-semantics prose — add a committed-derived sub-tier)
    - harness/plugins/*.ts and .claude/settings.json (the on-write hook groups — to prove NO memory_regen/docs_sync on Pre/PostToolUse write path, MAINT-03/D-09)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md (§ Pattern 3 refresh-memory, § Pattern 4 verify-work step 5, § Open Q2: refresh-memory DOES run repo-map locally)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md (§ refresh-memory.md + § verify-work.md + § two-plane SKILL deltas)
  </read_first>
  <action>
    (a) Author `harness/commands/refresh-memory.md` (analog: orient.md) with frontmatter `agent: curator`,
    `subtask: true`, a `use`-token description, and three ordered `!`-shell steps invoking ONLY existing modules:
    (1) session-derived `uv run python -m tools.memory_regen.repo_map` (gitignored, local convenience per Open Q2),
    (2) committed-derived `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`,
    (3) `uv run python -m tools.memory_regen.inject` to refresh the SessionStart payload. NO inline derivation logic.
    (b) Append step 5 to `harness/commands/verify-work.md`: `## 5. Derived freshness (mirror of the CI stale-derived
    gate)` running `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index; git add
    -A -- docs/reference .memory/derived/contracts-index.md; git diff --cached --exit-code -- docs/reference
    .memory/derived/contracts-index.md || { echo 'FAIL: derived plane stale — commit regenerated docs/reference +
    contracts-index'; exit 1; }`. Keep it presence-safe/announced like step 4 so a bare tree still exits 0. Update the
    intro prose "Run the four gates" → "five".
    (c) In `harness/skills/two-plane-memory/SKILL.md`, add a "committed-derived (machine-write + CI-verify)" sub-tier
    documenting why `contracts-index.md` lives under `.memory/derived/` yet is tracked (flipped in Plan 02), while
    repo-map stays gitignored. While editing, ALSO correct the pre-existing inaccurate sentence that reads as if
    `docs/reference/` is gitignored — clarify that `docs/reference/**` was already committed-derived pre-phase (tracked
    via `git ls-files`), so the committed-derived tier now names both `docs/reference/**` and `contracts-index.md`.
    Preserve the invariant: derived-never-hand-edited = machine-write + gate.
    (d) Add `tools/harness_lint/tests/test_derived_freshness.py` with two tests: (i) body-scan — `curator.md` +
    `refresh-memory.md` reference ONLY `tools.memory_regen`/`tools.docs_sync` module paths (no other derivation logic
    module), enforcing D-06; (ii) hook-posture — `.claude/settings.json` hook groups + `harness/plugins/*.ts` contain
    NO `memory_regen`/`docs_sync` invocation on any Pre/PostToolUse write path (MAINT-03/D-09).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_derived_freshness.py tools/harness_lint/tests/test_commands.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `harness/commands/refresh-memory.md`: `agent: curator`, 3 ordered steps invoking only `tools.memory_regen.*` + `tools.docs_sync`.
    - `harness/commands/verify-work.md` contains step 5 with `git diff --cached --exit-code` and the FAIL fix message; intro says "five".
    - `harness/skills/two-plane-memory/SKILL.md` documents the committed-derived sub-tier for contracts-index AND names `docs/reference/**` as already-committed-derived (no longer implies it is gitignored).
    - `test_derived_freshness.py` body-scan + hook-posture tests both pass; `uv run pytest tools/harness_lint/tests/test_derived_freshness.py tools/harness_lint/tests/test_commands.py -x` exits 0.
  </acceptance_criteria>
  <done>/refresh-memory and the /verify-work freshness step invoke only existing generators (D-06), no on-write memory hook is introduced (MAINT-03), and both are proven by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Round-trip the emitter to both runtimes + regenerate manifest</name>
  <files>tools/harness_emit/emit-manifest.json, .opencode/agent/curator.md, .opencode/command/refresh-memory.md, .opencode/command/verify-work.md, .claude/agents/curator.md, .claude/commands/refresh-memory.md, .claude/commands/verify-work.md, .claude/skills/two-plane-memory/SKILL.md, AGENTS.md, CLAUDE.md, opencode.json</files>
  <read_first>
    - tools/harness_emit/emit-manifest.json (current path list — regenerated, never hand-edited)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-PATTERNS.md (§ emit-manifest.json REGEN — the 4 new emitted paths + § Shared: Emitter round-trip, no model id)
    - .planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-RESEARCH.md (§ Pitfall P5 — model-id/example-token loud-fail; § Don't Hand-Roll — use python -m tools.harness_emit)
    - tools/harness_lint/tests/test_core_no_example_dep.py (GEN-04 guard that must stay green)
  </read_first>
  <action>
    Run `uv run python -m tools.harness_emit` to project the new/edited harness/ source to BOTH runtime trees.
    Do NOT hand-edit any emitted file or `emit-manifest.json`. Commit the regenerated `.opencode/` + `.claude/`
    trees, `emit-manifest.json`, and the AGENTS.md/CLAUDE.md/opencode.json managed blocks. The emitter loud-fails on
    a real model id or an over-cap description — if it raises, fix the SOURCE (curator.md/refresh-memory.md), never
    the output. Verify `emit-manifest.json` now lists `.opencode/agent/curator.md`, `.claude/agents/curator.md`,
    `.opencode/command/refresh-memory.md`, `.claude/commands/refresh-memory.md`. Confirm the emit is idempotent
    (re-emit → empty diff) and GEN-04 stays green.
  </action>
  <verify>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- .opencode .claude AGENTS.md CLAUDE.md opencode.json tools/harness_emit/emit-manifest.json && uv run pytest tools/harness_emit tools/harness_lint -x</automated>
  </verify>
  <acceptance_criteria>
    - `.opencode/agent/curator.md` AND `.claude/agents/curator.md` both exist and contain no real model identifier.
    - `emit-manifest.json` lists all four new emitted paths (curator + refresh-memory, both runtimes).
    - Re-running `uv run python -m tools.harness_emit` produces an empty `git diff --exit-code` over the emit path set (byte-identical, idempotent).
    - `uv run pytest tools/harness_emit tools/harness_lint -x` exits 0 (includes GEN-04 `test_core_no_example_dep.py` green).
  </acceptance_criteria>
  <done>curator + refresh-memory + verify-work round-trip to both runtimes with no model identifier, the manifest is regenerated, re-emit is byte-identical, and the core stays example-independent (D-11/GEN-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator persona → filesystem | curator has `edit`+`bash`; the risk is escalation to write constitution/golden/source |
| command `!`-shell → OS | `/refresh-memory` + `/verify-work` shell out; risk is arbitrary/injected commands |
| authored source → emitted artifacts | risk is a real model identifier or example/domain token leaking into both runtimes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Elevation of Privilege | curator write-boundary | mitigate | curator inherits the GLOBAL `path_deny_globs` (contracts/**, docs/adr/**, golden/**, *.env) enforced by the Phase-4 contract-guard hook — no per-persona glob needed; test asserts curator NOT in READ_ONLY_PERSONAS AND constitution paths in path_deny_globs; `write: deny` in frontmatter |
| T-9-03 | Tampering / EoP | refresh-memory + verify-work `!`-shell | mitigate | Commands invoke ONLY fixed `uv run python -m tools.{memory_regen.*,docs_sync,harness_emit}` module strings — NO variable/user/event interpolation; body-scan test (test_derived_freshness) enforces reference to only those modules (D-06) |
| T-9-04 | Information Disclosure | emitted curator/command | mitigate | Emitter `check_agent`/`check_opencode_config` loud-fail on any non-placeholder `model:` token; GEN-04 `test_core_no_example_dep.py` reds on any example/domain token; no `*.env`/secret referenced |
| T-9-05 | Repudiation | new on-write heavy hook | mitigate | MAINT-03/D-09: NO new on-write memory hook; hook-posture test asserts `.claude/settings.json` + `harness/plugins/*.ts` carry no `memory_regen`/`docs_sync` on the write path |
| T-9-03-SC | Tampering | package installs | accept | N/A — no npm/pip/cargo install this plan (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint tools/harness_emit -x` green (persona boundary, body-scan, hook-posture, GEN-04, emit determinism).
- `uv run python -m tools.harness_emit` idempotent (empty diff on re-emit).
- `.opencode/agent/curator.md` + `.claude/agents/curator.md` exist, no model id; manifest lists all 4 new paths.
</verification>

<success_criteria>
The curator persona (MAINT-01), /refresh-memory + /verify-work freshness (MAINT-04), and the no-new-on-write-hook
posture (MAINT-03) exist in harness/ source, are proven by tests, and round-trip the emitter to both runtimes with
no model id and GEN-04 green (D-11).
</success_criteria>

<output>
Create `.planning/phases/09-self-maintaining-derived-artifacts-curator-v2-0/09-03-SUMMARY.md` when done
</output>
