---
phase: 07-single-source-dual-runtime-emitter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/harness_emit/__init__.py
  - tools/harness_emit/__main__.py
  - tools/harness_emit/generate.py
  - tools/harness_emit/project_agent.py
  - tools/harness_emit/validate.py
  - tools/harness_emit/manifest.py
  - tools/harness_emit/pyproject.toml
  - tools/harness_emit/tests/conftest.py
  - tools/harness_emit/tests/test_emit_determinism.py
  - tools/harness_emit/tests/test_mapping.py
  - tools/harness_emit/tests/test_validators.py
  - tools/harness_emit/tests/test_manifest.py
  - tools/harness_emit/tests/__snapshots__/
  - tools/harness_lint/caps.py
  - tools/harness_lint/tests/test_agents.py
  - tools/harness_lint/tests/test_skills.py
  - .github/workflows/ci.yml
  - .opencode/agent/
  - .claude/agents/
autonomous: true
requirements: [EMIT-01, EMIT-02]

must_haves:
  truths:
    - "python -m tools.harness_emit writes the 4 harness agents to BOTH .opencode/agent/ and .claude/agents/"
    - "The opencode agent projection carries mode + permission; the Claude projection carries tools and NO permission block"
    - "read-only personas (code-reviewer, explorer) stay read-only in BOTH projections"
    - "An over-cap description or invalid permission key aborts the emit writing nothing (loud-fail, never truncate)"
    - "A second emit run reproduces every file byte-for-byte (drift gate is clean)"
    - "The manifest lists only harness-owned paths; no gsd-* file is ever written or pruned"
    - "CI job emit-drift re-emits and fails on any hand-edited generated-artifact diff, and is in gate.needs"
  artifacts:
    - path: "tools/harness_emit/generate.py"
      provides: "Emit spine: REPO_ROOT anchor, HarnessEmitError, _confine, deterministic write, main()"
      contains: "def _confine"
    - path: "tools/harness_emit/project_agent.py"
      provides: "Agent frontmatter projection to opencode + Claude shapes (D-04)"
    - path: "tools/harness_emit/validate.py"
      provides: "Loud-fail cap/shape gate importing tools.harness_lint.caps"
    - path: "tools/harness_emit/manifest.py"
      provides: "Ownership manifest: prune-then-write, gsd-* exclusion"
    - path: "tools/harness_lint/caps.py"
      provides: "Extracted shared cap constants + is_read_only, imported by validators and emitter"
    - path: ".github/workflows/ci.yml"
      provides: "emit-drift job + gate.needs entry"
      contains: "emit-drift"
  key_links:
    - from: "tools/harness_emit/project_agent.py"
      to: "tools.harness_lint.parse_frontmatter"
      via: "shared frontmatter reader (never re-slice fences)"
      pattern: "parse_frontmatter"
    - from: "tools/harness_emit/validate.py"
      to: "tools.harness_lint.caps"
      via: "import cap constants (single source of truth)"
      pattern: "from tools.harness_lint.caps import"
    - from: ".github/workflows/ci.yml gate"
      to: "emit-drift"
      via: "needs fan-in"
      pattern: "emit-drift"
---

## Phase Goal

> Derived from ROADMAP §Phase 7 Goal + locked decision D-05 (agent-first walking slice). The ROADMAP `**Goal:**` line is outcome-prose, not user-story form; this story is the planner's derivation for MVP framing.

**As a** harness maintainer, **I want to** compile the single runtime-neutral `harness/` source into both opencode and Claude Code runtime-native artifact trees with loud-fail validators and a CI re-emit-diff drift gate, **so that** the two runtimes stay byte-faithful to one source and no artifact is silently truncated or drifts.

<objective>
The agent-first walking skeleton (D-05): drive the 4 harness agents end-to-end through EVERY mechanic — frontmatter projection → per-runtime shape → loud-fail validators → ownership manifest → committed output → CI re-emit-diff drift gate — proving the whole pipeline on ONE artifact type before widening to commands/skills/plugins/config in later waves.

Purpose: Establish the emitter spine (`tools/harness_emit/`) and the drift gate so subsequent slices only add a projector branch, not new machinery. Almost every piece is a clone of an existing tested repo pattern (docs_sync determinism, harness_lint caps, contract_drift gate).

Output: `tools/harness_emit/` virtual uv member with the spine + agent projector + validator + manifest; the committed `.opencode/agent/**` + `.claude/agents/**` harness slice; the `emit-drift` CI job wired into `gate.needs`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-source-dual-runtime-emitter/07-CONTEXT.md
@.planning/phases/07-single-source-dual-runtime-emitter/07-RESEARCH.md
@.planning/phases/07-single-source-dual-runtime-emitter/07-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted this session; use directly, no exploration. -->

Shared frontmatter reader — `tools/harness_lint/frontmatter.py`:
  parse_frontmatter(md_text: str) -> tuple[dict, str]   # CRLF-safe, ruamel SAFE loader (V5). Public via `from tools.harness_lint import parse_frontmatter`.

Cap constants currently living inside test files (Task 1 EXTRACTS them to tools/harness_lint/caps.py):
  test_agents.py:  VALID_PERMISSION_KEYS (frozenset of 15), WRITE_AFFORDANCE_ALIAS, ALLOWED_PERMISSION_KEYS,
                   VALID_MODES = {"primary","subagent","all"}, EXPECTED_PERSONAS = {"orchestrator","python-engineer","code-reviewer","explorer"},
                   is_read_only(fm: dict) -> bool, placeholder model literal "provider/explorer-tier"
  test_skills.py:  _NAME_MAX=64, _DESC_MAX=1024, _BODY_WARN_LINES=500, _NAME_RE=^[a-z0-9]+(-[a-z0-9]+)*$,
                   _RESERVED_WORDS=("anthropic","claude"), _XML_CHARS=("<",">")

The 15 opencode permission keys (VERIFIED harness/permission-matrix.json top keys, minus _note/path_deny_globs):
  read, edit, bash, glob, grep, list, task, external_directory, todowrite, question, webfetch, websearch, lsp, skill, doom_loop

Determinism spine to clone — `tools/docs_sync/generate.py`:
  REPO_ROOT = Path(__file__).resolve().parents[2]
  class DocsSyncError(RuntimeError)                       # → clone as HarnessEmitError
  def _confine(path: Path, base: Path) -> Path            # lines 187-193, copy VERBATIM
  DERIVED_HEADER constant + first-line HTML comment; json.dumps(sort_keys=True); "\n".join(...).rstrip("\n") + "\n"
  def main(argv=None) -> int                              # prints "wrote <rel>" per file, returns 0

Virtual-member layout to clone — `tools/docs_sync/pyproject.toml` (requires-python >=3.11, dependencies = [], [tool.uv] package = false)
and `tools/docs_sync/tests/conftest.py` (parents[3] puts repo root on sys.path).

Authored agent frontmatter is DUAL-representation (one block carries both runtimes):
  name, description, mode (opencode), permission (opencode 15-key; bash is insertion-ordered last-wins),
  tools (Claude comma-list), optional model (placeholder tier only).

CI fan-in — `.github/workflows/ci.yml`: gate.needs currently = [setup, lang-tests, contract-check, drift, golden, core-suite].
Jobs use `uses: actions/checkout@v7.0.0`, `uses: astral-sh/setup-uv@v8.3.2`, `run: uv sync --all-packages`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 infra + extract shared caps + failing agent-projection tests (RED)</name>
  <read_first>
    - tools/docs_sync/pyproject.toml (virtual-member shape to clone verbatim)
    - tools/docs_sync/tests/conftest.py (parents[3] sys.path wiring)
    - tools/docs_sync/__init__.py and tools/docs_sync/__main__.py (import-light package + entrypoint)
    - tools/harness_lint/__init__.py (PEP 562 lazy re-export pattern to mirror for caps)
    - tools/harness_lint/tests/test_agents.py (VALID_PERMISSION_KEYS, VALID_MODES, EXPECTED_PERSONAS, is_read_only, model check — the constants to EXTRACT)
    - tools/harness_lint/tests/test_skills.py (_NAME_MAX/_DESC_MAX/_BODY_WARN_LINES/_NAME_RE/_RESERVED_WORDS/_XML_CHARS — the constants to EXTRACT)
    - tools/docs_sync/tests/test_docs_sync_determinism.py (syrupy .ambr + delete/regenerate idiom to clone)
  </read_first>
  <behavior>
    - test_mapping.py::test_opencode_agent_shape — opencode projection of python-engineer.md keeps name/description/mode/permission, has NO Claude-only leakage
    - test_mapping.py::test_claude_agent_shape — Claude projection keeps name/description/tools, has NO permission block and NO mode key
    - test_mapping.py::test_read_only_invariant_both — code-reviewer projects read-only in BOTH targets (is_read_only true on each)
    - test_emit_determinism.py::test_emit_twice_byte_identical — two emits into tmp_path produce identical sha256 per file
    - These import tools.harness_emit and MUST fail (red) at Task 1 because the emitter does not exist yet
  </behavior>
  <action>Create `tools/harness_emit/` as a virtual uv-workspace member by cloning `tools/docs_sync/pyproject.toml` verbatim (`requires-python = ">=3.11"`, `dependencies = []`, `[tool.uv] package = false`; rename project to `logparser-harness-emit`). Clone `tools/docs_sync/tests/conftest.py` (keep `parents[3]` repo-root sys.path insert). Add an import-light `__init__.py` (docstring only) and a `__main__.py` that does `from tools.harness_emit.generate import main` then `raise SystemExit(main())`. Create `tools/harness_emit/tests/__snapshots__/` (empty, for syrupy). EXTRACT the cap constants into a NEW `tools/harness_lint/caps.py`: move `VALID_PERMISSION_KEYS`, `WRITE_AFFORDANCE_ALIAS`, `ALLOWED_PERMISSION_KEYS`, `VALID_MODES`, `EXPECTED_PERSONAS`, `is_read_only`, and the placeholder-model literal `PLACEHOLDER_MODEL = "provider/explorer-tier"` from `test_agents.py`; move `_NAME_MAX=64`, `_DESC_MAX=1024`, `_BODY_WARN_LINES=500`, `_NAME_RE`, `_RESERVED_WORDS`, `_XML_CHARS`, and `EXPECTED_SKILLS` from `test_skills.py`. Re-point `test_agents.py` and `test_skills.py` to import from `tools.harness_lint.caps` (values UNCHANGED — the existing suite must stay green). Do NOT re-declare any cap in the emitter later; validate.py imports from caps.py. Write the failing tests `test_mapping.py` and `test_emit_determinism.py` per the behavior block, importing `from tools.harness_emit import ...`; they are RED now. No model identifiers anywhere.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint -x   # existing caps suite still green after extraction</automated>
    <automated>uv run pytest tools/harness_emit/tests/test_mapping.py -x 2>&1 | grep -qE 'ModuleNotFoundError|ImportError|AttributeError|failed'   # tests are RED (emitter absent)</automated>
  </verify>
  <acceptance_criteria>tools/harness_emit is a virtual member; tools/harness_lint/caps.py holds all extracted constants; test_agents.py/test_skills.py import from caps and pass; test_mapping.py + test_emit_determinism.py exist and FAIL because tools.harness_emit is not yet implemented.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit spine + agent projector + loud-fail validator + manifest → GREEN</name>
  <read_first>
    - tools/docs_sync/generate.py (REPO_ROOT parents[2], DocsSyncError, _confine lines 187-193, iter_*/write/main, DERIVED header, determinism)
    - tools/harness_lint/frontmatter.py (parse_frontmatter signature + CRLF/safe-loader behavior)
    - tools/harness_lint/caps.py (created Task 1 — the constants to import)
    - harness/agents/python-engineer.md and harness/agents/code-reviewer.md (dual-representation frontmatter to project)
    - harness/agents/orchestrator.md and harness/agents/explorer.md (mode: primary; read-only persona)
    - tools/contract_drift/drift.py (load_baseline / diff_manifests JSON set-diff idiom for manifest.py)
  </read_first>
  <behavior>
    - project_agent.to_opencode(fm) keeps name/description/mode/permission; drops tools if Claude-only; bash sub-keys in AUTHORED insertion order (not sorted)
    - project_agent.to_claude(fm) keeps name/description/tools; drops mode + permission; keeps model only if placeholder tier
    - validate.check_agent raises HarnessEmitError on: permission key outside the 15, mode outside {primary,subagent,all}, read-only persona gaining write/shell in EITHER projection, model != "provider/explorer-tier"
    - manifest.prune_then_write deletes prior-owned paths absent from the current set; never enumerates gsd-* paths
    - main() emits orchestrator/python-engineer/code-reviewer/explorer to .opencode/agent/<n>.md AND .claude/agents/<n>.md; re-run is byte-identical
  </behavior>
  <action>Create `tools/harness_emit/generate.py` cloning the docs_sync spine: `REPO_ROOT = Path(__file__).resolve().parents[2]`; `HARNESS_DIR = REPO_ROOT/"harness"`, `OPENCODE_DIR = REPO_ROOT/".opencode"`, `CLAUDE_DIR = REPO_ROOT/".claude"`; a `HarnessEmitError(RuntimeError)` mirroring `DocsSyncError`; copy `_confine` VERBATIM and call it before EVERY `write_text` (path-traversal mitigation — a `name: ../escape` must raise, not write). Discover agents with a sorted glob (`iter_agents` mirroring `iter_schemas`), read each via `parse_frontmatter` (NEVER re-slice fences). Re-serialize frontmatter from a FIXED ordered template per target (do NOT ruamel round-trip — reordering flaps the drift gate); emit with LF, no BOM, UTF-8, a DERIVED "generated by tools.harness_emit — do not hand-edit" first-line HTML comment, and `json.dumps(sort_keys=True)` for any JSON scalars — EXCEPT keep the `permission.bash` object in authored insertion order (semantic last-wins `*`-first, Pitfall 3). Create `project_agent.py` with `to_opencode`/`to_claude` per the behavior block and the Mapping Table in 07-RESEARCH. Create `validate.py` importing every cap from `tools.harness_lint.caps` (import `is_read_only`, `VALID_PERMISSION_KEYS`, `VALID_MODES`, `PLACEHOLDER_MODEL`); run it on the SOURCE and on BOTH projected outputs BEFORE any write; on a HARD failure raise `HarnessEmitError` and abort writing nothing (never `[:1024]`-slice, never `.truncate(`). Create `manifest.py` cloning contract_drift `load_baseline`/`diff_manifests`; implement prune-then-write to `tools/harness_emit/emit-manifest.json` (`json.dumps(sort_keys=True, indent=2)` + trailing LF); target globs MUST exclude `gsd-*`, `.claude/get-shit-done/**`, `.claude/hooks/**`, `.claude/commands/gsd/**`. Wire `main()` to emit the 4 agents to both trees + write the manifest. Make Task-1 tests GREEN.</action>
  <verify>
    <automated>uv run pytest tools/harness_emit/tests/test_mapping.py tools/harness_emit/tests/test_emit_determinism.py -x</automated>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- .opencode/agent .claude/agents   # idempotent re-emit clean</automated>
  </verify>
  <acceptance_criteria>test_mapping + test_emit_determinism pass; `.opencode/agent/{orchestrator,python-engineer,code-reviewer,explorer}.md` contain `mode` + a `permission` block and NO `tools:` leakage; `.claude/agents/<n>.md` contain `tools` and NO `permission` block; a second `python -m tools.harness_emit` yields an empty `git diff`; emit-manifest.json lists only the emitted agent paths.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Loud-fail + manifest tests, commit emitted agent surface, wire emit-drift CI gate</name>
  <read_first>
    - tools/docs_sync/tests/test_docs_sync_determinism.py (syrupy snapshot assertion idiom for the committed .ambr)
    - .github/workflows/ci.yml (the `drift` job structure lines ~125-135 and gate.needs line ~179 to mirror/extend)
    - tools/contract_drift/check.sh (optional local twin shape)
    - harness/agents/code-reviewer.md (read-only persona the negative tests must keep read-only)
    - tools/harness_emit/validate.py and manifest.py (created Task 2 — the units under test)
  </read_first>
  <action>Add `tools/harness_emit/tests/test_validators.py`: an over-cap (>1024) agent description, an invalid permission key, and a read-only persona mutated to gain `edit: allow` each cause `python -m tools.harness_emit` (into a tmp target) to raise `HarnessEmitError` and leave the target tree EMPTY (assert no files written — no truncation). Add `tools/harness_emit/tests/test_manifest.py`: the manifest covers every emitted agent path; a stale prior-manifest path is pruned on re-emit; a seeded `gsd-*` sibling in `.claude/agents/` is NEVER pruned or touched. Add a committed syrupy `.ambr` snapshot of the projected agent tree to `tools/harness_emit/tests/__snapshots__/` (determinism proof WITHOUT git diff, like docs_sync). Modify `.github/workflows/ci.yml`: add an `emit-drift` job mirroring the `drift` job (`actions/checkout@v7.0.0`, `astral-sh/setup-uv@v8.3.2`, `uv sync --all-packages`) that runs `uv run python -m tools.harness_emit` then `git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json` (full documented path set — later slices are already covered; unwritten paths stay clean); add `emit-drift` to `gate.needs`. Commit the emitted `.opencode/agent/**`, `.claude/agents/**` harness slice, and `emit-manifest.json` (they are tracked — NOT gitignored; `.gitignore` only excludes `.memory/derived/` + `.claude/settings.local.json`).</action>
  <verify>
    <automated>uv run pytest tools/harness_emit -x</automated>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); assert 'emit-drift' in d['jobs']; assert 'emit-drift' in d['jobs']['gate']['needs']"</automated>
    <automated>uv run pytest   # full core suite green (includes harness_lint caps consumers)</automated>
  </verify>
  <acceptance_criteria>test_validators + test_manifest pass; committed .ambr snapshot exists; ci.yml has an `emit-drift` job present in `gate.needs`; `.opencode/agent/**` + `.claude/agents/**` + emit-manifest.json are committed and re-emit produces a clean `git diff`; no emitted artifact matches `grep -rE 'claude-(opus|sonnet|haiku|fable)' .opencode .claude/agents`.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored `harness/` source → emitted trees | source `name`/frontmatter values become filesystem paths + artifact content |
| emitter → shared `.claude/` (GSD coexists) | writes must stay inside the harness lane, never touch gsd-* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | emitted filename from source `name` | mitigate | copy docs_sync `_confine` VERBATIM; call before every write; slug-validate names against `_NAME_RE`; a `../escape` name raises HarnessEmitError (test) |
| T-07-05 | Tampering | over-cap description silently truncated → corrupt artifact | mitigate | validate.py HARD-fails before write; test_validators asserts empty tmp tree (no truncation); no `[:1024]`/`.truncate(` in emitter |
| T-07-03 | Information disclosure | real model identifier leaks into emitted agent | mitigate | validate enforces `model == PLACEHOLDER_MODEL` on source AND both projections; done-gate greps emitted tree for `claude-(opus\|sonnet\|haiku\|fable)` |
| T-07-02 | Tampering / DoS-of-workflow | manifest prunes a gsd-* file | mitigate | manifest target globs exclude gsd-*/get-shit-done/hooks; test_manifest asserts a seeded gsd-* sibling survives |
| T-07-SC | Tampering | package installs at emit | accept | zero new deps this phase (dependencies = []); `uv sync --all-packages` must not mutate uv.lock — no slopcheck surface |
</threat_model>

<verification>
- `uv run pytest tools/harness_emit -x` green (mapping, determinism, validators, manifest)
- `uv run pytest tools/harness_lint -x` green (caps extraction did not change values)
- `uv run python -m tools.harness_emit && git diff --exit-code -- .opencode/agent .claude/agents` clean
- `emit-drift` present in ci.yml jobs and in gate.needs
</verification>

<success_criteria>
The 4 harness agents emit to BOTH runtime trees with correct per-runtime shape, read-only invariant preserved in both, cap violations abort writing nothing, re-emit is byte-identical, the manifest owns only harness paths, and CI's non-bypassable gate re-emits and diffs the surface. Every mechanic of the pipeline is exercised on the agents artifact type (D-05 walking slice).
</success_criteria>

<output>
Create `.planning/phases/07-single-source-dual-runtime-emitter/07-01-SUMMARY.md` when done.
</output>
