---
phase: 07-single-source-dual-runtime-emitter
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - tools/harness_emit/permissions.py
  - tools/harness_emit/generate.py
  - tools/harness_emit/validate.py
  - tools/harness_emit/tests/test_opencode_config.py
  - tools/harness_emit/tests/test_mapping.py
  - tools/harness_emit/tests/__snapshots__/
  - .opencode/plugin/
  - opencode.json
autonomous: true
requirements: [EMIT-01, EMIT-02]

must_haves:
  truths:
    - "The 5 harness .ts plugins are copied byte-for-byte to .opencode/plugin/ (never parsed, never executed)"
    - "permission-matrix.json projects into opencode.json's permission block as the full 15 opencode keys"
    - "The permission.bash object preserves authored insertion order (catch-all * FIRST, last-wins)"
    - "Emitted opencode.json validates against harness/opencode.config.schema.json; invalid aborts the emit"
    - "No emitted opencode.json or plugin carries a real model identifier"
  artifacts:
    - path: "tools/harness_emit/permissions.py"
      provides: "permission-matrix.json → opencode.json 15-key permission block"
      contains: "def "
    - path: "opencode.json"
      provides: "Emitter-owned wholesale opencode config with full 15-key permission block"
    - path: ".opencode/plugin/"
      provides: "5 verbatim-copied .ts plugins"
  key_links:
    - from: "tools/harness_emit/permissions.py"
      to: "harness/permission-matrix.json"
      via: "load_matrix (order-preserving json read)"
      pattern: "permission-matrix"
    - from: "tools/harness_emit/validate.py"
      to: "harness/opencode.config.schema.json"
      via: "jsonschema validation of emitted opencode.json"
      pattern: "opencode.config.schema"
---

## Phase Goal

**As a** harness maintainer, **I want to** compile the single runtime-neutral `harness/` source into both runtime artifact trees with loud-fail validators and a CI drift gate, **so that** the runtimes stay byte-faithful to one source. This plan widens to the opencode-only surfaces: plugins and the 15-key permission config.

<objective>
Complete the opencode primary target: verbatim-copy the 5 `.ts` plugins (D-01 — no Node toolchain, no execution at emit) and project `permission-matrix.json` into the emitter-owned wholesale `opencode.json` with the full 15-key permission block, validated against the vendored subset schema (loud-fail).

Purpose: This is the one genuine transform in the emitter (matrix → 15-key block) and the one Elevation-risk surface (untrusted `.ts` at emit — mitigated by copy-never-execute). Claude has no plugin/permission-file equivalent, so these are opencode-only.

Output: `permissions.py`, byte-copied `.opencode/plugin/*.ts`, emitter-owned root `opencode.json`, and schema validation wired into the loud-fail gate.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-single-source-dual-runtime-emitter/07-RESEARCH.md
@.planning/phases/07-single-source-dual-runtime-emitter/07-PATTERNS.md
@tools/harness_emit/generate.py
@tools/harness_emit/validate.py

<interfaces>
<!-- Established by Plans 01-02 + existing repo. -->
tools.harness_perms.resolver.load_matrix(path=...) -> dict   # json.loads, PRESERVES key order (last-wins depends on it)
harness/permission-matrix.json top keys: _note, read, edit, bash, glob, grep, list, task, external_directory,
  todowrite, question, webfetch, websearch, lsp, skill, doom_loop, path_deny_globs
The 15 opencode permission keys = the above MINUS _note and path_deny_globs (those are resolver-only data — strip them).
bash is an insertion-ordered object: catch-all "*" FIRST, specifics after (last-wins glob, Pitfall 3 / P3 — do NOT sort).
harness/opencode.json: authored config (model tiering placeholders, formatter, instructions, mcp, plugin) with a PARTIAL
  permission block (edit/bash/webfetch/external_directory) — the emitter OWNS opencode.json wholesale and writes the FULL 15-key block.
harness/opencode.config.schema.json: 48-line vendored subset schema; test_opencode_json.py validates against it with jsonschema.
harness/plugins/*.ts: 5 files (contract-guard, format-on-write, polyglot-lint, secret-scan, session-inject). Each carries an
  "AUTHORED-ONLY, EXECUTION DEFERRED" banner. COPIED byte-for-byte — never parsed, never executed at emit.
tools.harness_emit.generate: _confine, HarnessEmitError, deterministic write, main()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verbatim .ts plugin copy to .opencode/plugin/</name>
  <read_first>
    - harness/plugins/contract-guard.ts and harness/plugins/session-inject.ts (confirm the AUTHORED-ONLY banner + that these are source, not codegen input)
    - tools/harness_emit/generate.py (_confine + write path + main() to extend)
    - tools/harness_emit/manifest.py (add the plugin paths to the owned set)
    - .planning/phases/07-single-source-dual-runtime-emitter/07-RESEARCH.md §Common Pitfalls (Pitfall — never execute plugin .ts at emit)
  </read_first>
  <action>Extend `generate.py` to copy each `harness/plugins/*.ts` (5 files) byte-for-byte to `.opencode/plugin/<name>.ts` through a `_confine`d write target. Read as bytes and write as bytes — do NOT parse, transform, re-serialize, import, or execute the TypeScript (D-01; Elevation mitigation — the `.ts` is authored source consumed by opencode at runtime, deferred, not by the emitter). No DERIVED header is injected into `.ts` (it is verbatim source, not generated Markdown). Register the plugin paths in the manifest owned set. There is NO Claude target for plugins (Claude uses settings.json hooks). Do NOT emit a `.opencode/tool/` dir (no `harness/tool*` source exists — Open Question 3, omit).</action>
  <verify>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- .opencode/plugin</automated>
    <automated>for f in harness/plugins/*.ts; do cmp "$f" ".opencode/plugin/$(basename "$f")"; done   # byte-identical copy</automated>
  </verify>
  <acceptance_criteria>All 5 `.ts` plugins are byte-identical copies under `.opencode/plugin/`; no Claude plugin target exists; no `.opencode/tool/` dir; re-emit clean.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: permission-matrix → opencode.json 15-key block + schema loud-fail; commit opencode surface</name>
  <read_first>
    - harness/permission-matrix.json (15 keys + _note + path_deny_globs + insertion-ordered bash)
    - harness/opencode.json (authored config the emitter owns wholesale; partial permission block to replace)
    - harness/opencode.config.schema.json (the vendored subset schema to validate against)
    - tools/harness_perms/resolver.py (load_matrix — order-preserving reader to reuse)
    - tools/harness_lint/tests/test_opencode_json.py (jsonschema validation idiom to mirror in validate.py)
  </read_first>
  <behavior>
    - permissions.build_permission_block(matrix) returns the 15 opencode keys only (strips _note + path_deny_globs)
    - the bash sub-object preserves authored insertion order: "*" first, specifics after (assert order, not just membership)
    - the emitter writes root opencode.json = authored harness/opencode.json with its permission block REPLACED by the full 15-key block
    - validate raises HarnessEmitError if emitted opencode.json fails harness/opencode.config.schema.json
    - validate raises HarnessEmitError if any model value in opencode.json is not a placeholder tier token
  </behavior>
  <action>Create `tools/harness_emit/permissions.py` reusing `tools.harness_perms.resolver.load_matrix` (order-preserving) to read `harness/permission-matrix.json`; build the opencode `permission` block as the 15 valid keys ONLY — strip `_note` and `path_deny_globs` (resolver-only data). Keep the `bash` object in AUTHORED insertion order (`*` FIRST — sorting would break last-wins, Pitfall 3); everywhere else use `json.dumps(sort_keys=True)`. In `generate.py`, emit root `opencode.json` (Open Question 1 resolution — opencode's documented project-config location) by taking authored `harness/opencode.json` and REPLACING its partial permission block with the full 15-key block (emitter owns opencode.json wholesale via manifest — no GSD conflict). Add an `opencode.json` case to `validate.py` that validates the emitted config against `harness/opencode.config.schema.json` via `jsonschema` (mirror `test_opencode_json.py`) AND asserts every `model` value is a placeholder tier token only — HARD-fail via HarnessEmitError (no partial write). Add `tools/harness_emit/tests/test_opencode_config.py` asserting: 15 keys present, `_note`/`path_deny_globs` absent, bash `*`-first order preserved, schema-invalid config raises. Extend the `.ambr` snapshot. Commit emitted `opencode.json` + `.opencode/plugin/**` + updated manifest.</action>
  <verify>
    <automated>uv run pytest tools/harness_emit/tests/test_opencode_config.py -x</automated>
    <automated>uv run python -m tools.harness_emit && git diff --exit-code -- opencode.json .opencode</automated>
    <automated>python3 -c "import json,collections; d=json.load(open('opencode.json'),object_pairs_hook=collections.OrderedDict); b=list(d['permission']['bash']); assert b[0]=='*', b; assert '_note' not in d['permission'] and 'path_deny_globs' not in d['permission']"</automated>
  </verify>
  <acceptance_criteria>Root opencode.json carries the full 15-key permission block (no `_note`/`path_deny_globs`), bash `*`-first order preserved; a schema-invalid emitted config raises and writes nothing; no model identifier present; opencode + plugin surface committed; re-emit clean; `uv run pytest` full suite green.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/plugins/*.ts → .opencode/plugin | untrusted-shaped code crosses into the emitted tree |
| permission-matrix.json → opencode.json | access-control policy is projected; wrong shape weakens the runtime matrix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Elevation of privilege | plugin `.ts` executed at emit | mitigate | plugins are read+written as BYTES only — never parsed/imported/executed (D-01); execution is opencode-runtime, deferred; verified by `cmp` byte-identity, no interpreter invoked |
| T-07-06 | Tampering | permission block loses `*`-first last-wins order → over-broad allow | mitigate | reuse order-preserving load_matrix; keep bash authored insertion order; test asserts `b[0] == '*'` |
| T-07-03 | Information disclosure | real model id leaks via opencode.json model tiering | mitigate | validate asserts every `model` == placeholder tier; done-gate greps emitted config |
| T-07-07 | Tampering | malformed opencode.json emitted silently | mitigate | jsonschema validation against vendored subset schema HARD-fails before commit |
| T-07-SC | Tampering | package installs | accept | zero new deps (jsonschema already in lock); uv.lock unchanged |
</threat_model>

<verification>
- `uv run pytest tools/harness_emit -x` green
- 5 `.ts` plugins byte-identical under `.opencode/plugin/`
- opencode.json has 15-key permission block, `*`-first bash, no resolver-only keys
- `git diff --exit-code` clean after re-emit on opencode.json + .opencode
</verification>

<success_criteria>
The opencode primary target is complete: plugins verbatim-copied (never executed), permission-matrix projected to the full 15-key block with last-wins order intact, opencode.json schema-validated with loud-fail, no model identifiers — and re-emit byte-identical.
</success_criteria>

<output>
Create `.planning/phases/07-single-source-dual-runtime-emitter/07-03-SUMMARY.md` when done.
</output>
