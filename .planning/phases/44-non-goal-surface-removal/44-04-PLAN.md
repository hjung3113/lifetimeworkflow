---
phase: 44-non-goal-surface-removal
plan: 04
type: execute
wave: 4
depends_on: ["44-03"]
files_modified:
  - tools/adoption_scan/tests/test_install_completeness.py
  - harness/commands/golden.md
  - harness/commands/golden-approve.md
  - harness/skills/golden-testing/
  - harness/skills/golden-debug/
  - harness/agents/orchestrator.md
  - harness/agents/python-engineer.md
  - harness/agents/templates/engineer.md
  - harness/agents/templates/component-engineer.md
  - harness/commands/orient.md
  - harness/commands/verify-work.md
  - harness/commands/new-contract-rule.md
  - harness/skills/polyglot-boundary/SKILL.md
  - harness/skills/data-contracts/SKILL.md
  - harness/skills/two-plane-memory/SKILL.md
  - harness/skills/brownfield-adoption/SKILL.md
  - harness/skills/python-conventions/SKILL.md
  - tools/harness_lint/caps.py
  - tools/harness_lint/tests/test_commands.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - tools/harness_emit/emit-manifest.json
  - AGENTS.md
  - .opencode/
  - .claude/
  - opencode.json
autonomous: true
requirements: [CER-09]

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-14 (every live-tree-rendering test — caps.py EXPECTED_SKILLS, the .ambr bodies snapshot, the command-count literals AND the command-count function name, EXPECTED_GOLDEN_ADJACENT — repaired in the SAME commit as the deletion that invalidates it), D-15 (delete then add then commit then verify; the three adoption_scan reds are intra-commit only and no commit ENDS red), D-16 (git commit -m message BEFORE --; never git add -A / git add . / git commit -a / git checkout <ref> -- .), D-17 (source-first: edit harness/**, then python -m tools.harness_emit; never hand-edit .opencode/**, .claude/**, root opencode.json), D-18 (run things, don't read them — every claim here is backed by an executed command)."
    - "Every bare path argument passed to pytest in .github/workflows/*.yml resolves to a path that exists in this checkout, asserted by a test that reds when one does not"
    - "harness/commands/{golden,golden-approve}.md and harness/skills/{golden-testing,golden-debug}/ do not exist and no surviving harness file points at them"
    - "tools/golden_runner still resolves and the module-discovery floor still passes at 12 — this commit retires the command/skill surface only, not the package"
    - "17 commands and 8 skills emit to both runtime trees, and emit-manifest.json is pruned and committed in the same commit"
  artifacts:
    - path: "tools/adoption_scan/tests/test_install_completeness.py"
      provides: "A CI bare-path resolution assertion covering ci.yml's pytest path arguments, alongside the existing python -m tools.X module-ref coverage"
      contains: "workflows"
    - path: "tools/harness_lint/caps.py"
      provides: "EXPECTED_SKILLS reduced to 8"
      contains: "EXPECTED_SKILLS"
  key_links:
    - from: ".github/workflows/ci.yml pytest path arguments"
      to: "real directories in the checkout"
      via: "the new resolution assertion in test_install_completeness.py"
      pattern: "pytest"
    - from: "harness/skills/ directory set"
      to: "tools/harness_lint/caps.py EXPECTED_SKILLS"
      via: "emitter pre-write validation"
      pattern: "EXPECTED_SKILLS"
---

<objective>
Prepare CER-09's relocation by (a) closing the coverage gap that would let a mis-repointed CI path
land green, and (b) retiring the golden *command and skill* surface — which can be done while
`tools/golden_runner` is still in the core, and which shrinks the atomic relocation commit that
follows to something a single agent can hold.

Purpose: the relocation commit in plan 05 is the only commit in the phase that cannot be split — it
must move the package, fix the workspace, fix four path anchors, rewrite 11 imports and delete a
live gate component all at once. Everything that can legitimately land *before* it, should.
Output: two commits, each ending green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-non-goal-surface-removal/44-CONTEXT.md
@.planning/phases/44-non-goal-surface-removal/44-RESEARCH.md
@.planning/phases/44-non-goal-surface-removal/44-03-SUMMARY.md
</context>

<measured_anchors>
**Why Task 1 exists.** `tools/adoption_scan/tests/test_install_completeness.py`'s
`_discover_module_refs` (`:31-49`) globs `harness/commands/**/*.md`, `harness/skills/**/*.md` and
`.github/workflows/*.yml` for `python -m tools.X` style references, and
`test_every_referenced_tools_module_lands_in_applied_target` (`:116-120`) resolves each to a real
`.py`. It does **not** cover bare path arguments — which is exactly how `.github/workflows/ci.yml`
references the golden stack: `uv run pytest tools/golden_runner` at `:168` and
`… tools/golden_runner/tests/test_workspace_golden.py …` at `:320`. Nothing in the repo proves those
strings name real directories, so a mis-repointed CI path in plan 05 would land **green** locally and
fail only in CI. ROADMAP success criterion 5 demands both paths be repointed and YAML-resolved.

✅ **Held under full replay.** Task 1's CI-path assertion mutation-proves — repointing either string
at a non-existent directory reds it. Do not weaken it.

**Why Task 2 is safe before the move (all measured).**
- The only `tools.X` module reference anywhere under `harness/commands/{golden,golden-approve}.md`
  and `harness/skills/{golden-testing,golden-debug}/` is `tools.golden_runner` — verified by
  extraction. `tools.golden_runner` stays discoverable through `harness/commands/verify-work.md:44`
  and `harness/skills/python-conventions/SKILL.md:35`, both of which **stay in place until plan 05**.
  So the module-discovery floor stays at **12** across this commit. (Its slack is zero: the measured
  post-CER-08 value is exactly 12 against a floor of `>= 12`, and the trap holds in both directions.
  Do not remove the two surviving `tools.golden_runner` references here.)
- `tools/harness_lint/tests/test_commands.py:41-43` — `EXPECTED_GOLDEN_ADJACENT` is a frozenset of
  eight including `"golden"` and `"golden-approve"`, asserted at `:59` as
  `missing = EXPECTED_GOLDEN_ADJACENT - names`. Removing the two commands makes
  `test_golden_adjacent_commands_present` FAIL. Drop the two members; the `:41` comment and the `:57`
  docstring both say "eight" and become six.
- `tools/harness_lint/caps.py` `EXPECTED_SKILLS` loses `golden-testing` and `golden-debug`. It
  **hard-fails the emitter before it writes a byte** (`generate.py:362` → `validate.py:187`, exit 1,
  empty `git status` on the emitted trees), so it must change in this same commit. ✅ Replay
  confirmed `caps.py` is correctly paired with each skill deletion — keep that pairing.
- Counts entering this plan: **19 commands, 10 skills**. After Task 2: **17 commands, 8 skills**.
  `tools/harness_emit/tests/test_coexist.py` carries the command count in **four** places, not two:
  the `:70` and `:71` assertion literals, the `:41` docstring, and the **function NAME at `:40`**
  (`test_all_NN_commands_emit_to_both_trees`). The function name is the site most often missed —
  rename it with the count, or the test's name lies about what it asserts.
- Surviving `harness/**` pointers to sweep, from a live grep:
  `golden-debug` — `harness/agents/orchestrator.md`, `harness/agents/templates/engineer.md`,
  `harness/agents/templates/component-engineer.md`, `harness/commands/orient.md`,
  `harness/commands/verify-work.md`, `harness/skills/polyglot-boundary/SKILL.md`,
  `harness/skills/brownfield-adoption/SKILL.md`.
  `golden-testing` — `harness/skills/brownfield-adoption/SKILL.md`,
  `harness/skills/python-conventions/SKILL.md`.
  `/golden` and `/golden-approve` — `harness/agents/orchestrator.md`,
  `harness/agents/python-engineer.md`, `harness/agents/templates/engineer.md`,
  `harness/agents/templates/component-engineer.md`, `harness/commands/new-contract-rule.md`,
  `harness/commands/verify-work.md`, `harness/skills/polyglot-boundary/SKILL.md`,
  `harness/skills/data-contracts/SKILL.md`, `harness/skills/two-plane-memory/SKILL.md`.
  (`harness/commands/strangler-step.md` and `harness/skills/gate-model/` also appear in a HEAD-time
  grep; both were deleted by plan 01. Re-grep before sweeping — do not work from this list alone.)
- ⚠ **Do not remove `harness/commands/verify-work.md:44`'s golden loop or
  `harness/skills/python-conventions/SKILL.md:35`'s `python -m tools.golden_runner.runner` style
  example in this plan.** They are plan 05's, and removing them here drops the module floor to 11
  while the floor literal still says 12. Sweep only the *skill/command name* pointers.
- ⚠ **`harness/agents/templates/component-engineer.md:9` carries the `<id>.md` resolution token**,
  which plan 03 repointed `examples/log-parser/tests/test_pipeline_topology.py`'s
  `_CORE_RESOLUTION_DOCS` at. Sweep only the golden pointers out of this file; leave `:9` alone, or
  the instance's `examples/` leg reds.
- `harness/commands/verify-work.md:25`'s bare `ruff check .` is a **pre-existing** defect recorded as
  Phase 43 IN-05. Do not fix it here.
- Any `harness/**` edit invalidates `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`,
  which renders command/skill/agent **bodies**. Regenerate with `--snapshot-update` in this commit.
- 🔴 **`tools/harness_emit/emit-manifest.json` is a COMMITTED emitter output and belongs in Task 2's
  pathspec.** Task 2 removes four emitted artifacts, so the manifest self-prunes their rows from both
  trees on re-emit (`manifest.py:64-80`). Replaying this phase with the manifest outside the pathspec
  left ` M tools/harness_emit/emit-manifest.json` in `git status --porcelain` and made the task's own
  `git diff --exit-code` exit 1. "Self-prunes" means **do not hand-edit it**; it does **not** mean
  exclude it from the commit.
- `git rm` **already stages the deletion**. A subsequent `git add -- <deleted-path>` aborts with exit
  128 (`fatal: pathspec '<path>' did not match any files`). Add only the files you **modified**.
- 🔴 **THREE `adoption_scan` tests are red between `git rm` and `git commit`, not one** — same
  `git ls-files` + `stat()` root cause in every deletion commit of this phase:
  `test_dispositions.py::test_catalog_invariant_to_untracked_local_state`,
  `test_plan_classification.py::test_contract_candidate_matches_real_repo_schema_count`, and
  `test_scan_exclusions.py::test_ci_yml_false_positive_closed`. All three go green at the commit.
  Do not repair them.
- The ruff ratchet is at 84/84 with zero slack. After the sweep, re-check every file you edited for
  a now-unused import before committing.
- Verification block: as in `44-01-PLAN.md`.
</measured_anchors>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert every CI pytest path argument resolves to a real path</name>
  <files>tools/adoption_scan/tests/test_install_completeness.py</files>
  <behavior>
    - Test 1: for each `.github/workflows/*.yml`, every bare filesystem path argument passed to
      `pytest` in a `run:` step resolves to an existing path in this checkout. Green today:
      `ci.yml:168` names `tools/golden_runner` and `ci.yml:320` names
      `tools/golden_runner/tests/test_workspace_golden.py`, both of which exist.
    - Test 2 (mutation control): temporarily repoint one of those two strings at a non-existent
      directory and confirm the test RED. Then revert.
    - Non-vacuity: the discovered path set must be non-empty, backstopped by an explicit assertion,
      so a regex that stops matching cannot pass silently.
  </behavior>
  <action>
Wave-0 coverage gap. Landed before the relocation it protects, so it stands green on its own.

Add the assertion to the existing `tools/adoption_scan/tests/test_install_completeness.py` — the
module that already owns "does a reference in a workflow/command/skill resolve to something real".
This adds no gate, tool, contract or dependency: it is a test function inside an existing module
already executed by the existing `core-suite` CI job, so ROADMAP success criterion 8 is unaffected.

Resolve the workflow with `ruamel.yaml` (already a resolved transitive dependency at 0.19.1 — **do
not add PyYAML**), walk `jobs.*.steps[*].run`, and extract the arguments that follow a `pytest`
invocation. Skip tokens that begin with `-` (flags), tokens that look like flag values, and anything
containing a shell metacharacter or variable expansion; a path argument is a token that contains a
`/` or names an existing top-level entry. Assert each survivor exists relative to the repo root, and
report the workflow file, job and offending token in the failure message.

Keep the heuristic conservative and make it non-vacuous: assert the discovered set is non-empty and,
in the same assertion message, list what was found — a silently-empty matcher is the failure mode
this test exists to prevent, and it is the same class of defect as the `[pipeline]` vacuity in plan 03.

⚠ **GEN-04:** this is a core-plane file. Do not write the token `examples/` in the test body, its
docstring or its failure messages — `test_core_no_example_dep.py::test_core_has_no_example_dependency`
hard-fails on that token anywhere under `tools/`, `harness/`, `libs/`, prose included. Say "the
instance tree" if you need to refer to it.

Prove the mutation control before committing: edit one of the two `ci.yml` path strings to a
non-existent directory, run the file, observe RED, restore `ci.yml` with an explicit single-path
checkout (never `git checkout <ref> -- .`), re-run to observe GREEN. Record both outcomes.

Commit with the message before the pathspec separator (D-16), touching only the one test file.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_install_completeness.py -q && uv run pytest -q && uv run python -m tools.ruff_baseline && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q</automated>
  </verify>
  <done>A test in `test_install_completeness.py` resolves every CI pytest path argument against the checkout, is proved non-vacuous, and reds when a path is repointed at a non-existent directory (demonstrated and reverted); GEN-04 passes; the full suite is green; the commit contains exactly one file.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the /golden + /golden-approve commands and the golden-testing + golden-debug skills</name>
  <files>harness/commands/golden.md, harness/commands/golden-approve.md, harness/skills/golden-testing/, harness/skills/golden-debug/, harness/agents/orchestrator.md, harness/agents/python-engineer.md, harness/agents/templates/engineer.md, harness/agents/templates/component-engineer.md, harness/commands/orient.md, harness/commands/verify-work.md, harness/commands/new-contract-rule.md, harness/skills/polyglot-boundary/SKILL.md, harness/skills/data-contracts/SKILL.md, harness/skills/two-plane-memory/SKILL.md, harness/skills/brownfield-adoption/SKILL.md, harness/skills/python-conventions/SKILL.md, tools/harness_lint/caps.py, tools/harness_lint/tests/test_commands.py, tools/harness_emit/tests/test_coexist.py, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, tools/harness_emit/emit-manifest.json, AGENTS.md, .opencode/, .claude/, opencode.json</files>
  <action>
CER-09's command/skill half, separated from the package move so the relocation commit stays holdable.
One commit.

1. `git rm harness/commands/golden.md harness/commands/golden-approve.md` and
   `git rm -r harness/skills/golden-testing harness/skills/golden-debug` (the latter includes
   `references/canonicalization-axes.md`).
2. Re-grep for surviving pointers before sweeping — plan 01 and plan 03 already deleted several of
   the files a HEAD-time grep lists, so work from a fresh `git grep -ln`, not from the enumeration in
   `<measured_anchors>`. Excise the routing row, bullet or sentence that names `/golden`,
   `/golden-approve`, `golden-debug` or `golden-testing` from each surviving `harness/**` file. Excise
   minimally; the orchestrator's wholesale rewrite is Phase 46's PROD-02.
3. ⚠ **Preserve** `harness/commands/verify-work.md`'s `tools.golden_runner` loop and
   `harness/skills/python-conventions/SKILL.md`'s `python -m tools.golden_runner.runner` style
   example. They keep the module-discovery floor at 12, which has zero slack; plan 05 removes them
   together with the floor drop to 11. Sweeping the skill-name pointer out of `verify-work.md` is
   correct; sweeping the module invocation out of it is not, in this commit.
   ⚠ **Also preserve `harness/agents/templates/component-engineer.md:9`'s `<id>.md` token** — plan
   03 repointed the instance's `_CORE_RESOLUTION_DOCS` at that line, and removing it reds the
   `examples/` leg.
4. Repair the live-tree-rendering declarations in this same commit (D-14):
   - `tools/harness_lint/caps.py` — drop `golden-testing` and `golden-debug` from `EXPECTED_SKILLS`
     (10 to 8) and extend the narrative comment. **Before running the emitter.**
   - `tools/harness_lint/tests/test_commands.py` — drop `"golden"` and `"golden-approve"` from
     `EXPECTED_GOLDEN_ADJACENT` (eight to six) and correct the `:41` comment and `:57` docstring.
   - `tools/harness_emit/tests/test_coexist.py` — command count 19 to 17 at all four sites: the
     `:70` and `:71` literals, the `:41` docstring, and the **function name at `:40`**
     (`test_all_19_… → test_all_17_…`).
5. Re-emit (`uv run python -m tools.harness_emit`), then regenerate the emit snapshot
   (`uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update`). Include
   `AGENTS.md` in the pathspec — the emitter rewrites its HARNESS-MANAGED command/skill index.
   `emit-manifest.json` prunes the four deleted artifacts' rows from both trees by itself — **do not
   edit it, and do not leave it out of the commit.**
6. Before staging, re-check every file you edited for a now-unused import. The ruff ratchet is at
   84/84 with zero slack.
7. Stage the **modified** files only (`git rm` already staged the deletions; re-adding a removed path
   aborts with exit 128), commit with the message before `--` and an explicit pathspec listing every
   deleted path plus every modified path including `emit-manifest.json`, then verify with the full
   block, including `uv run pytest tools/adoption_scan/tests/test_install_completeness.py -q`
   explicitly: the module floor is the one thing in this commit with zero slack, and it must still be
   at 12. Run `git status --porcelain` after committing: it must be empty. The three `adoption_scan`
   tests named in `<measured_anchors>` are red between the `git rm` and the `git commit` and green
   after — three, not one.
8. Do **not** touch `.github/workflows/ci.yml` in this commit. The `golden` job still runs
   `uv run pytest tools/golden_runner`, which still exists; repointing is plan 05's, and Task 1's
   assertion now proves it lands correctly.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests -q && uv run python -m tools.harness_emit && git diff --exit-code && uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline && python3 tools/harness_lint/workspace_check.py</automated>
    <automated>uv run pytest tools/adoption_scan/tests/test_install_completeness.py tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_skills.py -q</automated>
    <automated>test -z "$(git status --porcelain)" && test ! -e harness/commands/golden.md && test ! -e harness/commands/golden-approve.md && test ! -e harness/skills/golden-testing && test ! -e harness/skills/golden-debug && ! git grep -n "golden-debug\|golden-testing" -- harness && python3 -c "import sys,glob; c=len(glob.glob('harness/commands/*.md')); s=len(glob.glob('harness/skills/*/')); print(c,s); sys.exit(0 if c==17 and s==8 else 1)"</automated>
  </verify>
  <done>The two golden commands and two golden skills are gone; no surviving `harness/**` file names them; `tools.golden_runner` is still referenced twice and the module floor still passes at 12; 17 commands and 8 skills emit to both trees; `emit-manifest.json` is pruned and committed; `git status --porcelain` is empty and the commit ends green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow strings → real filesystem paths | Unverified today; a mis-repointed path fails only in CI, after merge |
| `harness/**` source → emitted runtime trees | The emitter refuses to write on a declaration mismatch, so declarations must move with deletions |
| emitted artifact set → `emit-manifest.json` | The manifest is a committed derived record; omitting it from the pathspec leaves the commit dirty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-10 | Repudiation | `ci.yml`'s `golden` and `workspace` jobs could silently point at a moved directory and be claimed as passing gates | mitigate | Task 1's resolution assertion in `test_install_completeness.py`, mutation-proved by repointing a real path at a non-existent one |
| T-44-11 | Denial of Service | Removing the two surviving `tools.golden_runner` references here would drop the module-discovery floor below its `>= 12` literal and red the suite | mitigate | Explicit non-goal in Task 2 step 3; `test_install_completeness.py` asserted in the verify block |
| T-44-12 | Tampering | Hand-editing `.claude/**`, `.opencode/**`, `emit-manifest.json` or the `.ambr` rather than regenerating | mitigate | `python -m tools.harness_emit` plus `pytest --snapshot-update`; `git diff --exit-code` and empty `git status --porcelain` after re-emit |
| T-44-21 | Repudiation | `emit-manifest.json` left out of the pathspec leaves the commit dirty and its own `git diff --exit-code` red — a D-15 violation from a pathspec omission | mitigate | Manifest in `files_modified` and in Task 2's explicit pathspec; `git status --porcelain` asserted empty |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no dependency change in this plan; `ruamel.yaml` is an already-resolved transitive dependency and PyYAML is explicitly not added (SC-8). |
</threat_model>

<verification>
Both commits satisfy the full verification block and leave `git status --porcelain` empty. After
Task 2: `git grep -n "golden-debug\|golden-testing" -- harness` returns nothing;
`git grep -c "tools.golden_runner" -- harness` returns exactly the two preserved references.
</verification>

<success_criteria>
- A test proves every CI pytest path argument resolves, and reds when one does not.
- `harness/commands/{golden,golden-approve}.md` and `harness/skills/{golden-testing,golden-debug}/`
  do not exist and no surviving file invokes them (SC-1 discipline applied to CER-09's surface).
- 17 commands, 8 skills, module floor still 12; both commits end green with an empty
  `git status --porcelain` (SC-7).
- No new gate, tool, contract or dependency (SC-8).
</success_criteria>

<output>
Create `.planning/phases/44-non-goal-surface-removal/44-04-SUMMARY.md` when done. Record the Task 1
mutation result verbatim, the post-commit command/skill counts, the `emit-manifest.json` row-delta,
and the per-commit `git diff --shortstat`.
</output>
