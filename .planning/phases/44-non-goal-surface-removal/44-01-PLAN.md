---
phase: 44-non-goal-surface-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/memory_ui/
  - tools/strangler_guard/
  - harness/commands/strangler-step.md
  - harness/skills/gate-model/
  - harness/agents/orchestrator.md
  - harness/commands/orient.md
  - harness/commands/review.md
  - harness/commands/verify-work.md
  - harness/skills/brownfield-adoption/SKILL.md
  - harness/skills/two-plane-memory/SKILL.md
  - tools/harness_lint/caps.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - tools/harness_emit/emit-manifest.json
  - tools/contract_hash/hash.py
  - tools/contract_hash/tests/test_hash.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  - tools/adoption_scan/__init__.py
  - tools/adoption_scan/scan.py
  - tools/adoption_scan/tests/conftest.py
  - tools/adoption_scan/tests/test_scan_exclusions.py
  - contracts/harness/security/deny-domains.json
  - contracts/harness/security/deny-domains.schema.json
  - contracts/harness/task-control/gate-registry.json
  - contracts/.hashes/manifest.json
  - docs/reference/deny-domains.md
  - .memory/derived/contracts-index.md
  - AGENTS.md
  - .opencode/
  - .claude/
  - opencode.json
  - uv.lock
autonomous: true
requirements: [CER-08]

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-09 (deny-domains deletion self-clears the ledger_guard + tools.deny_domains.registry stale declarations — no separate task), D-10 (gate-registry.json plus its DATA_CONTRACT_PATHS entry plus the 5 hyphenated provenance docstrings), D-13 (tools/memory_ui has no external consumer — do not over-plan it), D-14 (every live-tree-rendering test repaired in the SAME commit as the deletion that invalidates it), D-15 (delete → git add → git commit -- pathspec → verify; the three intra-commit adoption_scan reds are intra-commit only and no commit ENDS red), D-16 (git commit -m message BEFORE --; never git add -A / git add . / git commit -a), D-17 (source-first: edit harness/**, then python -m tools.harness_emit; never hand-edit .opencode/**, .claude/**, root opencode.json), D-18 (run things, don't read them)."
    - "tools/memory_ui and tools/strangler_guard do not exist and no surviving file imports or invokes them"
    - "harness/commands/strangler-step.md and harness/skills/gate-model/ do not exist and no surviving harness/** file points at either"
    - "contracts/harness/security/ and contracts/harness/task-control/ do not exist; the hash manifest carries 6 entries and contract-drift exits 0"
    - "No hyphenated gate-registry.json provenance docstring survives in tools/adoption_scan/**"
    - "tools/adoption_scan's own secret redaction tests pass unchanged (D-08 proof carried into this plan's deletions)"
    - "Both commits end green under the full verification block; neither ends red"
  artifacts:
    - path: "tools/harness_lint/caps.py"
      provides: "EXPECTED_SKILLS frozenset reduced from 12 to 11 (loses gate-model) with its narrative comment corrected"
      contains: "EXPECTED_SKILLS"
    - path: "tools/contract_hash/hash.py"
      provides: "DATA_CONTRACT_PATHS retained as an empty seam with a comment explaining why it is empty"
      contains: "DATA_CONTRACT_PATHS"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined manifest, 9 entries down to 6"
    - path: "tools/docs_sync/tests/test_docs_sync_determinism.py"
      provides: "EXPECTED_PAGES frozenset of 6 (loses deny-domains)"
      contains: "EXPECTED_PAGES"
  key_links:
    - from: "harness/skills/ (12 dirs)"
      to: "tools/harness_lint/caps.py EXPECTED_SKILLS"
      via: "emitter pre-write validation (validate.check_skill_set)"
      pattern: "EXPECTED_SKILLS"
    - from: "contracts/harness/**"
      to: "contracts/.hashes/manifest.json"
      via: "python -m tools.contract_hash.hash"
      pattern: "contract_hash"
    - from: "harness/{agents,commands,skills}/**"
      to: "tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr"
      via: "syrupy snapshot of rendered bodies"
      pattern: "test_emit_determinism"
---

<objective>
Land the two CER-08 deletions that are structurally independent of everything else in the phase:
the two orphan tool packages (`tools/memory_ui`, `tools/strangler_guard`) with their command and
skill surface, and the two non-goal contracts (`deny-domains.*`, `gate-registry.json`) with their
`DATA_CONTRACT_PATHS` entries and the derived artifacts that render them.

Purpose: these are the largest, lowest-risk removals in the phase and they clear the `caps.py` /
`.ambr` / manifest machinery that every later plan also has to touch, so the later plans meet a
smaller surface.
Output: two commits, each ending green under the full verification block.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-non-goal-surface-removal/44-CONTEXT.md
@.planning/phases/44-non-goal-surface-removal/44-RESEARCH.md
@AGENTS.md
</context>

<measured_anchors>
Every number below was executed in a scratch clone by the researcher (44-RESEARCH.md), re-verified
against HEAD `1a0bad1` at plan time, or measured in a full ten-commit replay of this phase. Do not
re-derive them; do confirm them when a command disagrees.

- `harness/commands/*.md` live count: **21**. `harness/skills/*/` live count: **12**.
- `tools/harness_lint/caps.py:132-147` — `EXPECTED_SKILLS` frozenset of 12. `caps.py:122-131` is a
  narrative comment ending "The twelve entries below are the whole set."
- Emitter behavior: a skill-set mismatch raises `HarnessEmitError` at `generate.py:362` →
  `validate.py:187`, exit 1, **zero bytes written**. `caps.py` must therefore change in the same
  commit as any skill directory deletion.
- `tools/harness_emit/tests/test_coexist.py` carries the command count in **three** places, not two:
  the assertion literals at `:70` and `:71`, the `:41` docstring, **and the function NAME at `:40`
  (`test_all_21_commands_emit_to_both_trees`)**. The function name is the site most often missed —
  rename it with the count. The narrative lines at `:43-45` are phase history ("17 → 18 → 19 → 20");
  append to that history, do not rewrite it.
- `tools/contract_hash/hash.py:30-34` — `DATA_CONTRACT_PATHS` holds exactly the two paths being
  deleted; the tuple becomes empty.
- `tools/contract_hash/tests/test_hash.py:93-111` —
  `test_build_manifest_includes_ratified_data_contracts_and_detects_registry_mutation`, whose `:105`
  literal is `{"contracts/harness/task-control/gate-registry.json"}` and whose `:107-109` is its
  negative control. With zero data left there is nothing to narrow to.
- `tools/docs_sync/tests/test_docs_sync_determinism.py:29-39` — `EXPECTED_PAGES` frozenset of 7,
  compared with `==` at `:118`. Loses `"deny-domains"` → 6.
- Isolated contract-deletion run measured **6 failures**: 3 in `docs_sync`, 1 in `contract_hash`,
  1 in `memory_regen` contracts-index, plus the intra-commit `adoption_scan` reds below.
  Manifest 9 → 6; `contract-drift` exit 0 after rebaseline.
- The 5 hyphenated `gate-registry.json` provenance docstrings (D-10), exact list:
  `tools/adoption_scan/__init__.py:7`, `tools/adoption_scan/scan.py:11`,
  `tools/adoption_scan/scan.py:57`, `tools/adoption_scan/tests/conftest.py:50`,
  `tools/adoption_scan/tests/test_scan_exclusions.py:110`.
- `tools/adoption_scan/tests/test_scan_exclusions.py:110` carries historical prose about an
  "intended red state" for a fix that already landed. **Rewrite the prose; the test is green and
  stays.**
- 🔴 **THREE tests are red between `git rm` and `git commit`, not one.** All three read the index
  through `git ls-files` plus a `stat()` on the listed paths (`tools/adoption_scan/destinations.py:217`),
  so between the removal and the commit they see paths git still lists but the filesystem no longer
  has:
  - `tools/adoption_scan/tests/test_dispositions.py::test_catalog_invariant_to_untracked_local_state`
  - `tools/adoption_scan/tests/test_plan_classification.py::test_contract_candidate_matches_real_repo_schema_count`
  - `tools/adoption_scan/tests/test_scan_exclusions.py::test_ci_yml_false_positive_closed`

  All three go green the moment the deletion is committed (measured: 20 passed). This is D-15's
  documented intra-commit red. **Do not repair them** — an executor who expects one failure and sees
  three will otherwise start "fixing" a test that is already correct.
- `harness/skills/gate-model/SKILL.md:59` is the only surviving reference to `tools/strangler_guard`
  outside `harness/commands/strangler-step.md` — which is why the skill dies with the package.
- Surviving `harness/**` files that point at the `gate-model` skill and must be swept in the same
  commit: `harness/agents/orchestrator.md` (routing row near `:97`), `harness/commands/orient.md`,
  `harness/commands/review.md`, `harness/commands/verify-work.md`,
  `harness/skills/brownfield-adoption/SKILL.md`, `harness/skills/two-plane-memory/SKILL.md`.
- Root `AGENTS.md:100-109` is a **HARNESS-MANAGED block** that `tools.harness_emit` splices
  (`generate.py:230,297`); its `- **Commands**` and `- **Skills**` lines enumerate the live sets and
  are rewritten by every re-emit. `AGENTS.md` must be in each commit's pathspec. Everything outside
  those markers is Phase 45's and must not be touched here.
- 🔴 **`tools/harness_emit/emit-manifest.json` is a COMMITTED emitter output and belongs in the
  pathspec of every commit that adds or removes an emitted artifact.** It self-prunes correctly on
  re-emit (`manifest.py:64-80`) — measured at this commit: 4 deletions, the `strangler-step` and
  `gate-model` rows in both runtime trees. "Self-prunes" means **do not hand-edit it**; it does
  **not** mean exclude it from the commit. Replaying this phase with the manifest outside the
  pathspec left ` M tools/harness_emit/emit-manifest.json` in `git status --porcelain` and made this
  task's own `git diff --exit-code` exit 1. The ROADMAP scope line for `strangler_guard` already
  names its `emit-manifest.json` row as in scope.
- `git rm` **already stages the deletion**. A subsequent `git add -- <deleted-path>` aborts with
  exit 128 (`fatal: pathspec '<path>' did not match any files`). Add only the files you **modified**;
  never re-add a `git rm`'d path.
- The ruff ratchet is already rebaselined at 84/84 — no action, and no slack is owed to this phase.
  It hard-fails on any regression ("baseline may only SHRINK"), including a new `F401` from an
  orphaned import.

**Verification block used by every task in this phase** (D-19's single command is insufficient —
`testpaths = ["libs/python", "tools"]` means `uv run pytest -q` collects **zero** tests from
`examples/**`):

    uv run pytest -q
    uv run pytest examples/log-parser/tests -q
    uv run python -m tools.harness_emit && git diff --exit-code
    uv run python -m tools.contract_drift.drift
    uv run python -m tools.ruff_baseline
    python3 tools/harness_lint/workspace_check.py
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: Delete memory_ui + strangler_guard, /strangler-step, and the gate-model skill</name>
  <files>tools/memory_ui/, tools/strangler_guard/, harness/commands/strangler-step.md, harness/skills/gate-model/, harness/agents/orchestrator.md, harness/commands/orient.md, harness/commands/review.md, harness/commands/verify-work.md, harness/skills/brownfield-adoption/SKILL.md, harness/skills/two-plane-memory/SKILL.md, tools/harness_lint/caps.py, tools/harness_emit/tests/test_coexist.py, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, tools/harness_emit/emit-manifest.json, AGENTS.md, .opencode/, .claude/, opencode.json, uv.lock</files>
  <action>
Implements D-13 (memory_ui has no external consumer) and CER-08's `tools/strangler_guard` +
`/strangler-step` + skill `gate-model` clause, under D-14/D-15/D-16/D-17.

One commit. Do all of the following before committing:

1. `git rm -r tools/memory_ui tools/strangler_guard harness/commands/strangler-step.md harness/skills/gate-model`.
   Both tool packages are uv workspace members matched by the `tools/*` glob, so their removal
   requires a lock refresh — run `uv lock` then `uv sync --all-packages`. Skipping this produces
   spurious `ModuleNotFoundError` collection errors for `tree_sitter` / `networkx` (deps of the
   surviving `tools/memory_regen`) and sends you chasing the wrong bug.
2. Sweep every surviving `harness/**` pointer to the deleted artifacts so nothing points at a
   corpse mid-phase (CONTEXT canonical_refs is explicit about this even though the broader prose
   reconciliation is Phase 45's CER-11). The six files are listed in `<measured_anchors>`. Excise
   the routing row / bullet / sentence that names `gate-model` or `/strangler-step`; do not rewrite
   the surrounding sections and do not touch anything Phase 45 owns (root `AGENTS.md` outside the
   managed markers, `docs/**`, `.memory/derived/**` narrative).
   `harness/agents/templates/*.md` do not reference either artifact — confirm with a grep before
   assuming, but do not go looking for work.
3. Repair the live-tree-rendering declarations in this same commit (D-14):
   - `tools/harness_lint/caps.py` — remove `gate-model` from `EXPECTED_SKILLS` (12 → 11) and correct
     the narrative comment block above it, which currently says "The twelve entries below are the
     whole set." Add one sentence recording the Phase-44 / CER-08 removal, in the same style the
     block already uses for Phases 5.7, 8, 10 and 43. **This edit must precede the emitter run** —
     a skill-set mismatch hard-fails the emitter before it writes a byte.
   - `tools/harness_emit/tests/test_coexist.py` — command count 21 → 20 at **all four sites**: the
     `:70` and `:71` assertion literals, the `:41` docstring, and the **function name at `:40`**
     (`test_all_21_commands_emit_to_both_trees` → `test_all_20_commands_emit_to_both_trees`).
     Missing the function name leaves a test whose name lies about what it asserts. Append to the
     `:43-45` count history rather than rewriting it. Skill counts are asserted through
     `EXPECTED_SKILLS` set-equality in `tools/harness_lint/tests/test_skills.py:56`, which needs no
     edit once `caps.py` is right.
   - `tools/harness_lint/tests/test_commands.py` — inspect only. Its `:8` docstring mentions
     `/strangler-step` as historical narrative about commands added over time; that is not an
     invocation and stays. `EXPECTED_GOLDEN_ADJACENT` (`:41-43`) does not name `strangler-step`.
4. Re-emit: `uv run python -m tools.harness_emit`. This rewrites `.opencode/**`, `.claude/**`, root
   `opencode.json`, `tools/harness_emit/emit-manifest.json`, and the HARNESS-MANAGED block in
   `AGENTS.md` and `CLAUDE.md`. Never hand-edit any of them (D-17). `emit-manifest.json` prunes the
   two deleted artifacts' rows from both trees by itself — **do not edit it, and do not leave it out
   of the commit.** Expect 4 deleted rows.
5. Regenerate the emit snapshot: `uv run pytest tools/harness_emit/tests/test_emit_determinism.py
   --snapshot-update`. It renders command/skill/agent **bodies**, so every `harness/**` edit above
   invalidates it. It must be regenerated in this commit — there is no way to batch snapshot
   regeneration into a later commit without ending this one red.
6. Stage and commit. `git rm` has already staged the four deletions, so `git add` only the files you
   **modified** — re-adding a `git rm`'d path aborts with exit 128. The modified set is:
   `harness/agents/orchestrator.md`, `harness/commands/{orient,review,verify-work}.md`,
   `harness/skills/{brownfield-adoption,two-plane-memory}/SKILL.md`, `tools/harness_lint/caps.py`,
   `tools/harness_emit/tests/test_coexist.py`,
   `tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr`,
   `tools/harness_emit/emit-manifest.json`, `AGENTS.md`, `CLAUDE.md`, `.opencode/`, `.claude/`,
   `opencode.json`, `uv.lock`. Then commit with the message BEFORE the pathspec separator (D-16):
   `git commit -m "chore(44): delete memory_ui, strangler_guard, /strangler-step, gate-model" -- <explicit paths>`.
   The pathspec must include every deleted path **and** every modified path above, `emit-manifest.json`
   included. Never `git add -A`, `git add .`, `git commit -a`, or `git checkout <ref> -- .`.
7. Verify with the full block. The **three** `adoption_scan` tests named in `<measured_anchors>` are
   red between the `git rm` and the `git commit` and green after — that is D-15's documented
   intra-commit red, not a failure, and there are three of them, not one. If anything is still red
   after the commit, amend; the commit must not END red.
   Run `git status --porcelain` after committing: it must be empty. A stray
   ` M tools/harness_emit/emit-manifest.json` means the pathspec was short.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests -q && uv run python -m tools.harness_emit && git diff --exit-code && uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline && python3 tools/harness_lint/workspace_check.py</automated>
    <automated>test -z "$(git status --porcelain)" && test ! -e tools/memory_ui && test ! -e tools/strangler_guard && test ! -e harness/commands/strangler-step.md && test ! -e harness/skills/gate-model && ! git grep -n "strangler" -- harness tools ':(exclude)tools/harness_lint/tests/test_commands.py'</automated>
  </verify>
  <done>`tools/memory_ui`, `tools/strangler_guard`, `harness/commands/strangler-step.md` and `harness/skills/gate-model/` are gone; 20 commands and 11 skills emit to both trees; `emit-manifest.json` lost its 4 rows and is committed; no surviving `harness/**` file names `gate-model` or `/strangler-step`; `git status --porcelain` is empty and the full verification block is green at the commit.</done>
</task>

<task type="auto">
  <name>Task 2: Delete deny-domains + gate-registry contracts and rebaseline the manifest</name>
  <files>contracts/harness/security/deny-domains.json, contracts/harness/security/deny-domains.schema.json, contracts/harness/task-control/gate-registry.json, contracts/.hashes/manifest.json, tools/contract_hash/hash.py, tools/contract_hash/tests/test_hash.py, tools/docs_sync/tests/test_docs_sync_determinism.py, tools/docs_sync/tests/__snapshots__/test_docs_sync_determinism.ambr, docs/reference/deny-domains.md, .memory/derived/contracts-index.md, tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr, tools/adoption_scan/__init__.py, tools/adoption_scan/scan.py, tools/adoption_scan/tests/conftest.py, tools/adoption_scan/tests/test_scan_exclusions.py</files>
  <action>
Implements D-09 and D-10. One commit. This commit adds and removes **no** emitted artifact, so
`emit-manifest.json` does not change here — verified in replay.

1. `git rm contracts/harness/security/deny-domains.json contracts/harness/security/deny-domains.schema.json contracts/harness/task-control/gate-registry.json`. Both parent
   directories cease to exist — those files were their only contents. `contract_guard`'s PreToolUse
   hook matches `Write|Edit` only, never `Bash`, so `git rm` needs no `HARNESS_DEV_BYPASS`.
   D-09: this deletion self-clears the stale `tools.hooks.ledger_guard` declaration
   (`deny-domains.json:81,102`, a module deleted in Phase 41) and the never-existent
   `tools.deny_domains.registry` named in the schema description (`:5,77`). Do not open separate
   work for either.
2. `tools/contract_hash/hash.py:30-34` — remove both entries from `DATA_CONTRACT_PATHS`, leaving an
   empty tuple with a short comment saying it is a retained extension seam and that CER-08 removed
   its two entries (the requirement says "their `DATA_CONTRACT_PATHS` **entries**", not the
   mechanism).
3. **Delete** `tools/contract_hash/tests/test_hash.py:93-111`
   (`test_build_manifest_includes_ratified_data_contracts_and_detects_registry_mutation`) in its
   entirety, including its `:105` expected-set literal and its `:107-109` negative control. Phase 43
   already narrowed this test once; with zero data left there is nothing to narrow to, and a test
   whose subject is an empty tuple asserts nothing. After deleting it, check whether any import at
   the top of `test_hash.py` is now unused and remove it — the ruff ratchet hard-fails on a new
   `F401` and it is already at 84/84 with zero slack.
4. `tools/docs_sync/tests/test_docs_sync_determinism.py:29-39` — drop `"deny-domains"` from
   `EXPECTED_PAGES` (7 → 6). It is compared with `==` at `:118`; set equality stays strict.
5. Regenerate the derived artifacts in this same commit (D-14):
   - `docs_sync`'s prune removes `docs/reference/deny-domains.md` — let the tool do it, do not
     hand-delete. Then `uv run pytest tools/docs_sync/tests/test_docs_sync_determinism.py --snapshot-update`.
   - `uv run python -m tools.memory_regen.contracts_index` to regenerate
     `.memory/derived/contracts-index.md`, then
     `uv run pytest tools/memory_regen/tests/test_contracts_index.py --snapshot-update`.
   - Rebaseline the hash manifest with `uv run python -m tools.contract_hash.hash >
     contracts/.hashes/manifest.json` — 9 entries down to 6. Never recompute a SHA-256 by hand; the
     tool owns the RFC 8785 JCS canonicalization.
6. Rewrite the 5 hyphenated `gate-registry.json` provenance docstrings listed in
   `<measured_anchors>` (D-10). All 5 are prose. `test_scan_exclusions.py:110` additionally carries
   historical prose describing an "intended red state" for a fix that already landed — rewrite that
   sentence; the test itself is green and stays. Also sweep the underscore spelling: `git grep -n
   "gate.registry\|gate_registry" -- . ':(exclude).planning'` must return only legitimate history
   after this commit (ROADMAP success criterion 6).
7. Do **not** touch `tools/hooks/tests/test_contract_guard.py:330`. Measured: it does not break.
   `:326-333` is a list of synthetic path strings fed to `decide()`, and
   `"docs/reference/deny-domains.md"` is a **negative-control literal** asserting that path is not
   constitution plane. No filesystem access. Leave it.
8. Do **not** touch `tools/adoption_scan`'s `SECRET_PATH_GLOBS` (`scan.py:54`) or
   `SECRET_CONTENT_PATTERNS` (`:60`). D-08: they are a hand-copy, not a read of the deleted
   contract, and the proof of correctness is that all 20 adoption disposition + exclusion tests pass
   unchanged.
9. Stage the **modified** files only (`git rm` already staged the three deletions; re-adding a
   removed path aborts with exit 128), then
   `git commit -m "chore(44): delete deny-domains + gate-registry contracts" -- <explicit paths>`
   (message before `--`, D-16). Verify with the full block; amend if red. The same three
   intra-commit `adoption_scan` reds from `<measured_anchors>` apply here — three, not one.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests -q && uv run python -m tools.contract_drift.drift && uv run python -m tools.harness_emit && git diff --exit-code && uv run python -m tools.ruff_baseline</automated>
    <automated>test ! -e contracts/harness/security && test ! -e contracts/harness/task-control && test ! -e docs/reference/deny-domains.md && python3 -c "import json,sys; m=json.load(open('contracts/.hashes/manifest.json')); n=len(m.get('schemas', m)); print(n); sys.exit(0 if n==6 else 1)"</automated>
    <automated>! git grep -n "gate-registry" -- tools harness contracts</automated>
  </verify>
  <done>`contracts/harness/` holds no `deny-domains.*` and no `gate-registry.json`; the manifest carries 6 entries and `contract-drift` exits 0; `DATA_CONTRACT_PATHS` is an empty commented seam and its now-vacuous test is deleted with no orphaned import left behind; no hyphenated `gate-registry.json` provenance docstring survives in `tools harness contracts`; `adoption_scan`'s 20 redaction/exclusion tests pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → repo working tree | PreToolUse guards (`contract_guard`, `commit_gate`) decide every Write/Edit/Bash; a broken uv workspace disarms them |
| committed contract → derived manifest | `contract_hash` / `contract_drift` gate the constitution plane at CI |
| emitted artifact set → `emit-manifest.json` | The manifest is a committed derived record; leaving it out of a commit's pathspec leaves the tree dirty and the commit's own drift gate red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Denial of Service | `tools/memory_ui`, `tools/strangler_guard` are uv workspace members; removing them without a lock refresh breaks every `uv` invocation and the PreToolUse guards fail closed | mitigate | `uv lock && uv sync --all-packages` inside Task 1, before the commit; `python3 tools/harness_lint/workspace_check.py` in the verify block |
| T-44-02 | Tampering | Hand-editing `contracts/.hashes/manifest.json`, `emit-manifest.json` or a `.ambr` snapshot would forge the drift baseline | mitigate | Rebaseline only via `python -m tools.contract_hash.hash`, `python -m tools.harness_emit` and `pytest --snapshot-update`; `contract-drift` exit 0 and `git diff --exit-code` asserted in verify |
| T-44-03 | Information Disclosure | Deleting the `deny-domains` contract could be read as removing `adoption_scan`'s redaction | accept | D-08: `adoption_scan` owns its own inlined patterns; proof is its 20 tests passing unchanged. No control is removed here. |
| T-44-21 | Repudiation | `emit-manifest.json` left out of the pathspec leaves the commit dirty and its own `git diff --exit-code` red — a D-15 violation caused by pathspec omission rather than by a code defect | mitigate | Manifest in `files_modified` and in Task 1's explicit pathspec; `git status --porcelain` asserted empty in the verify block |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | This plan performs **no package installs**. `uv lock` / `uv sync --all-packages` re-resolve existing workspace members after a member is deleted; no new distribution enters the lock. RESEARCH.md records +0 dependencies (SC-8). |
</threat_model>

<verification>
Both commits must satisfy the full verification block and leave `git status --porcelain` empty.
Additionally, after Task 2:
`git grep -n "gate.registry\|gate_registry" -- . ':(exclude).planning'` returns only legitimate
history (ROADMAP SC-6). Note that `docs/adr/0012-…:114` names `gate-registry.json` permanently —
ADRs are append-only and must not be edited, which is why the Task 2 absence gate is scoped to
`tools harness contracts` and not to `docs`.
</verification>

<success_criteria>
- `tools/memory_ui`, `tools/strangler_guard`, `harness/commands/strangler-step.md`,
  `harness/skills/gate-model/` do not exist and no surviving file invokes or points at them (SC-1).
- `contracts/harness/` contains no `deny-domains.*` and no `gate-registry.json`;
  `DATA_CONTRACT_PATHS` retains only surviving entries (none); manifest rebaselined to 6;
  `contract-drift` exits 0 (SC-3).
- No Phase-42 hyphenated `gate-registry.json` provenance docstring survives in the core (SC-6).
- Two commits, each ending green with an empty `git status --porcelain`; `emit-drift`,
  `stale-derived`, ruff ratchet clean; `uv.lock` refreshed (SC-7).
</success_criteria>

<output>
Create `.planning/phases/44-non-goal-surface-removal/44-01-SUMMARY.md` when done. Record the measured
per-commit `git diff --shortstat` so plan 05 can total D-21 without re-deriving it, and record the
`emit-manifest.json` row-delta for each commit.
</output>
