---
phase: 44-non-goal-surface-removal
plan: 06
type: execute
wave: 6
depends_on: ["44-05"]
files_modified:
  - .planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md
autonomous: true
requirements: [CER-08, CER-09]

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-19 (the done-condition is asserted by running every gate — CER-08 paths gone with no surviving invoker, the stale-checkout drop asserted rather than read, contracts clean with a rebaselined manifest and contract-drift exit 0, the golden stack resolving under examples/log-parser with BOTH CI jobs repointed YAML-resolved, uv run pytest green, emit-drift / stale-derived / ruff ratchet clean, uv.lock refreshed), D-20 (no mutation-proof table is owed — this phase removes a gate and adds no control; the sole exception is the stale-checkout assertion, which is coverage of a retained behavior), D-21 (whole-phase LOC is REPORTED from git diff --shortstat, measured not estimated), D-18 (run things, don't read them — every line of the evidence table is a command and its output)."
    - "Every one of the ROADMAP's 8 success criteria has an executed command and its output recorded against it"
    - "SC-5's evidence command asserts the absence of GOLDEN/PARITY .NET resolution in the core, not the absence of the identifier resolve_dotnet — format_on_write::resolve_dotnet is deliberately retained"
    - "The accepted consequences and residuals of this phase are written down, not discovered by the next reader"
    - "No new gate, tool, contract or dependency entered the repo across the whole phase"
    - "All TEN commits are recorded with the gate result observed at each, and none ended red"
    - "SC-7's ratchet evidence is recorded as 'exits 0 with no rule class above baseline' plus the observed shrink (84 -> 77), NOT as a pinned 84/84 — the deletions remove E501 findings, so a falling count is the expected PASS and no --update rebaseline is performed"
  artifacts:
    - path: ".planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md"
      provides: "The phase evidence table, the measured net LOC, the ten-commit greenness table, and the eleven recorded consequences/residuals"
  key_links:
    - from: "ROADMAP Phase 44 success criteria 1-8"
      to: "executed command output"
      via: "the evidence table in this plan's summary"
      pattern: "success criteri"
---

<objective>
Close the phase: run every gate the done-condition names, record the evidence against each ROADMAP
success criterion, measure the net LOC, and write down the consequences this phase accepted so the
next reader does not have to rediscover them.

Purpose: D-19's done-condition is a list of commands, not a list of beliefs, and D-21 asks for a
measurement. Phase 43 needed four verification passes largely because its evidence was assembled
after the fact from memory.
Output: `44-06-SUMMARY.md` with an executed evidence table. **No code changes.**
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-non-goal-surface-removal/44-CONTEXT.md
@.planning/phases/44-non-goal-surface-removal/44-05-SUMMARY.md
</context>

<measured_anchors>
**The gate commands, resolved from `.github/workflows/ci.yml` rather than guessed:**

| CI job | Local equivalent |
|---|---|
| `emit-drift` | `uv run python -m tools.harness_emit` then `git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json tools/harness_emit/emit-manifest.json` |
| `stale-derived` | `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index` then `git diff --exit-code -- docs/reference .memory/derived/contracts-index.md` |
| `lint` | `uv run python -m tools.ruff_baseline` then `uv run pytest tools/ruff_baseline -q` |
| `contract-check` / `drift` | `uv run python -m tools.contract_drift.drift` |
| `core-suite` | `uv run pytest -q` |
| `golden` step 2 | `uv run pytest examples/log-parser/tests -q` |
| `workspace` | `uv run pytest examples/log-parser/golden_runner/tests/test_workspace_golden.py -q` |

- 🔴 **SC-7's ratchet criterion, CORRECTED. Do NOT require the line to read 84/84 — that is
  arithmetically impossible at phase end.** The baseline was rebaselined to **84** before this phase
  and stays there. But this phase deletes code, and the deletions remove `E501` findings, so the
  *found* count necessarily falls. Measured across the full ten-commit replay: **84 → 80 after the
  first commit, 77 from the sixth, and 77/84 at phase end** —
  `improved E501: baseline 84 -> found 77`, with every intermediate run printing
  `PASS — and findings went DOWN`.
  As originally written this criterion **reads a true green as a failure**: it would send the
  executor hunting a cleanup that was never skipped, or push a `--update` rebaseline that every
  other plan in this phase forbids.
  **The criterion is: `uv run python -m tools.ruff_baseline` exits 0 with NO rule class above its
  baseline.** Record the observed shrink (`84 → 77`) as evidence of the deletion, not as a defect.
  Do not pin the number, and do not `--update`.
  A genuine regression still fails: a rule class going ABOVE baseline. Four such regressions were
  found and pre-empted in the plans that cause them (`F401` ×3 in plan 03, `F401` ×1 in plan 05
  Task 1, `I001` ×3 in plan 05 Task 2) — if the tool reports any class above baseline, one of those
  cleanups was skipped.
- `tools/harness_emit/emit-manifest.json` is a committed emitter output that self-prunes on re-emit.
  Plans 01, 02, 03 and 04 each carry it in a commit pathspec. If `git status --porcelain` is not
  empty at closeout and the only entry is that file, a prior commit's pathspec was short — that is a
  D-15 violation to report, not to repair here.
- `gate.needs` must resolve to exactly **10** entries with `set(needs) - set(jobs)` empty. The
  existing `test_every_job_is_in_the_fan_in` (`test_ci_lint_gate.py:72-80`) is `declared <= fan_in`,
  subset-only, and would not catch a stale entry (Phase 43 IN-03) — so assert the count explicitly.
- D-21's measurement command, with the planning plane excluded:
  `git diff --shortstat 1a0bad1..HEAD -- . ':(exclude).planning'`. Expect a **smaller net deletion
  than Phase 43's −12,383**, because CER-09 moves rather than deletes.

**SC-5's evidence command — corrected. Do not assert `resolve_dotnet` is absent.**
`git grep -n "resolve_dotnet" -- tools harness libs` returns **3 hits after the full phase**, all in
`tools/hooks/format_on_write.py` (`:57` def, `:61` docstring, `:96` call). This is a **wording defect
in the criterion, not a missing deletion** — a human decision recorded here rather than rediscovered:
CER-09's ground is that .NET **golden/parity evidence** belongs in the instance, whereas
`format_on_write` resolves `dotnet` in order to run `dotnet format` — a language-toolchain concern
that is legitimate in a polyglot template core and has nothing to do with golden equivalence.
`format_on_write::resolve_dotnet` is **deliberately retained**. Assert the golden/parity surface
instead:

    test ! -e tools/golden_runner
    ! git grep -nE "^[[:space:]]*(from|import)[[:space:]]+tools\.golden_runner" -- tools harness libs
    ! git grep -n "run_golden_case\|GOLDEN_DIR\|run_identity_converter" -- tools harness libs

Verified against the post-phase tree: `tools/golden_runner` is gone, no core module imports it, and
the only pre-phase carriers of those golden symbols outside `tools/golden_runner` were
`tools/hooks/commit_gate.py` (removed by plan 05 Task 1) and `tools/strangler_guard/guard.py`
(removed by plan 01 Task 1) — so all three commands return clean.
The matching ROADMAP SC-5 wording correction is the developer's and is being made separately; **do
not edit `.planning/ROADMAP.md`**. Reference the corrected criterion in the evidence table and note
the retention.

**Consequences and residuals this phase must record — ELEVEN of them, all measured, none discovered
late:**

1. **Constitution-plane downgrade for 4 files.** `resolve_path(CONSTITUTION_GLOBS, 'golden/sample/meta.yaml')`
   returned `deny`; `resolve_path(CONSTITUTION_GLOBS, 'examples/log-parser/golden/repr-only/meta.yaml')`
   returns `allow`. `CONSTITUTION_GLOBS` (`tools/hooks/contract_guard.py:53`) is repo-root-anchored,
   so folding root `golden/` moves those 4 files from agent-write-**denied** to agent-write-**allowed**.
   Merge-time CODEOWNERS holds (`/examples/*/golden/  @hjung3113`); only the in-session gate is lost.
   **Accepted** rather than widening `CONSTITUTION_GLOBS`, which would be surface growth against
   success criterion 8.
2. **`/golden/` is now a stale CODEOWNERS entry, and ADR-0001:48 is contradicted.** ADR-0001 declares
   the constitution plane as `contracts/`, `golden/`, `docs/adr/`, `docs/glossary.md`, and
   `.github/CODEOWNERS`'s own header says it must not drift from that list. This is the Phase-43
   CR-03 class: an accepted ADR left describing a deleted structure. ADRs are append-only and
   supersede-don't-edit → **raise it in the PR; do not edit the ADR and do not edit CODEOWNERS here.**
   The repair belongs with Phase 45's ADR-0008 work or a human call at milestone close.
3. **A gate component disappeared without either requirement naming it.** `commit_gate`'s
   golden-parity component is gone (ROADMAP recorded scope extension). It is a **removal**, so it
   does not violate "+0 gates", but it should be stated plainly rather than left implicit.
4. **`secret_scan` has no replacement**, by design (D-06). Secret detection at the tool boundary
   stops existing; ADR-0012 already records it as a permanent residual caught at CI/PR review.
   Adopted repos lose a hook they may believe is protecting them.
5. **.NET SDK 10 is absent locally.** `require_dotnet` golden cases SKIP; the converter spawn path
   runs only in CI's `golden` job. Local verification cannot prove it — state it as a residual rather
   than claiming full local proof.
6. **`format_on_write::resolve_dotnet` is deliberately retained** (see the SC-5 note above). It is a
   `dotnet format` toolchain probe, not golden/parity evidence, and it is legitimate in a polyglot
   template core. A future reader grepping for `resolve_dotnet` and expecting zero hits is reading
   the pre-correction criterion.
7. **Deferred, unchanged, to Phase 45 (do not pre-empt):** `caps.py` narrative reconciliation beyond
   what each commit needed, `HARNESS_SIGNATURES` hygiene, `docs/reference/**`, root `AGENTS.md`
   outside the HARNESS-MANAGED markers (including `:8-9`'s "the true backstop" claim and the
   golden-path table at `:66-67`), `.memory/derived/**` narrative, the remaining ~17 prose/path
   spellings of `tools/golden_runner` (`README.md:119`, `tools/docs_sync/generate.py:14`,
   `tools/hooks/contract_guard.py:56`, `tools/hooks/format_on_write.py:36`, two `conftest.py:3`
   docstrings, 5 `pyproject.toml` comments), the stale prose in `docs/how-to/task-lifecycle.md` and
   the two `docs/explanation/` pages, `docs/glossary.md:20` + `docs/how-to/README.md:11` +
   `docs/how-to/approve-a-golden.md` (all still naming `/golden-approve`), and ADR-0008's
   supersession. (`README.md:58`'s `secret-scan`
  entry is NOT deferred — plan 02 removed it; see item 11.)
8. **Q3 is closed, with its answer.** `tools/memory_regen/tests/test_agents_md.py` was run at plan
   time in a fully-relocated scratch tree and re-confirmed in the full replay: **5 passed**. It does
   not red, because `:49` is an `any()` over three alternatives (two of which survive) and the
   monorepo-map check is a substring test on committed text, not a filesystem check. This is why
   Phase 44 leaves `AGENTS.md`'s non-managed prose to Phase 45.
9. **The six-commit sequence in RESEARCH.md was never replayed end-to-end by the researcher** (its
   own MEDIUM confidence). This phase executes a **ten-commit** variant: two commits per plan across
   plans 01–05. A ten-commit replay of the pre-revision plan set found six commits ending red; every
   one of those six causes is now pre-empted in the plan that causes it (`GOLDEN_DIR`,
   `_CORE_RESOLUTION_DOCS`, three ruff regressions, `emit-manifest.json` pathspec omission).
   Record the actual commit list with the per-commit gate result so a verification pass can replay it
   in a scratch clone.
10. **A gate lost coverage silently, and it is recorded rather than repaired.**
   `tools/harness_lint/tests/test_tests_are_isolatable.py`'s `_members_needing_wiring()` (`:86`)
   globs `tools/*/tests`, so once CER-09 relocates the golden runner out of the core tree the
   relocated package **permanently drops out of the isolatability gate**. Nothing breaks today —
   the relocated suite passes standalone (measured: 17 passed) — and the module docstring's now-stale
   citation of the scoped CI command was corrected in plan 05 Task 2 step 7, in the same commit as
   the move. Widening the glob to reach the instance tree would be a gate change against success
   criterion 8 **and** would put the instance path token in a core-plane file, which GEN-04
   hard-fails. **Accepted and written down** so the next reader does not discover it by finding an
   un-gated package. If a future phase wants the coverage back, it needs an instance-side gate, not
   a wider core glob.
11. **`README.md:58` was corrected in plan 02, `README.md:119` was not.** The front page's "Runtime
   hooks" row listed `secret-scan` as a shipped control; leaving it would have directly contradicted
   consequence 4, so plan 02 dropped it in the same commit as the deletion. `README.md:119`'s
   `tools/golden_runner` path spelling is a different residual and remains deferred to Phase 45
   (item 7 above).
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: Run the whole-phase gate sweep and build the success-criteria evidence table</name>
  <files>.planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md</files>
  <action>
Implements D-19 and D-18. **No code changes.** If any gate is red, stop and report — do not repair
here; a red gate means a prior plan's commit ended red, which is a D-15 violation that must be fixed
in the plan that caused it.

Run every command in the `<measured_anchors>` gate table and capture its exact output. Then build a
table with one row per ROADMAP Phase 44 success criterion (1 through 8), each row carrying the
command that proves it and the observed result:

1. The CER-08 paths do not exist and no surviving file invokes them — prove with
   `git grep` for each deleted name across **`tools harness libs examples .github contracts`**,
   plus `test -e` (and `git ls-files`, for the `__pycache__` residue case) for each path.
   🔴 **Scope note, so the row is honest rather than convenient.** `.planning/` and `docs/adr/**` are
   historical and append-only. `docs/**` beyond the ADRs is **deferred to Phase 45 (CER-11)** by
   consequence 7 and is therefore also outside this criterion — two live carriers remain and are
   named here rather than left for the reader to trip over: `docs/glossary.md:20` and
   `docs/how-to/README.md:11`, both naming `/golden-approve` (and `docs/how-to/approve-a-golden.md`
   itself). Record them in the SC-1 row as **known, deferred, named** — do not silently narrow the
   scope without saying so, and do not repair them here.
   The **instance** plane IS in scope and IS repaired: plan 04's sweep is what makes
   `git grep -n "golden-approve\|golden-debug\|golden-testing" -- harness examples` return nothing.
2. A stale checkout drops the `secret_scan` group on re-emit — prove by running
   `uv run pytest tools/harness_emit/tests/test_settings_merge.py -q`, and cite plan 02's recorded
   mutation result rather than re-running the mutation.
3. `contracts/harness/` clean, `DATA_CONTRACT_PATHS` reduced, manifest rebaselined,
   `contract-drift` exit 0. Note that the `gate-registry.json` absence gate is scoped to
   `tools harness contracts`, because `docs/adr/0012-…:114` names the file permanently and ADRs are
   append-only.
4. The `[pipeline]` slot and its consistency-gate assertions gone, no dangling or vacuous topology
   assertion — cite plan 03's recorded post-commit pass count for `test_pipeline_config.py`, and its
   recorded `examples/` leg result proving `test_pipeline_topology.py` passes with the repointed
   `_CORE_RESOLUTION_DOCS`.
5. The golden stack resolves under `examples/log-parser/`, the core carries no golden/parity .NET
   resolution, both CI jobs repointed YAML-resolved — run the ruamel assertion, plus the **three**
   corrected commands in `<measured_anchors>` under "SC-5's evidence command". **Do not run
   `git grep -n "resolve_dotnet" -- tools harness libs` as the criterion** — it returns 3 legitimate
   hits in `tools/hooks/format_on_write.py` and the criterion's original wording was a defect. Record
   the retention explicitly in the row's note.
6. No hyphenated `gate-registry.json` provenance docstring survives, and
   `git grep -n "gate.registry\|gate_registry" -- . ':(exclude).planning'` returns only legitimate
   history.
7. `uv run pytest -q` green, `emit-drift` / `stale-derived` / `contract-drift` / ruff ratchet clean,
   `uv.lock` refreshed. 🔴 **The ratchet criterion is "exits 0 with no rule class above baseline",
   NOT "the line reads 84/84"** — see `<measured_anchors>`. Record the observed line verbatim and
   the shrink alongside it (replay measured `84 → 77`, `improved E501: baseline 84 -> found 77`).
   A total below 84 is the expected consequence of deleting code and is a PASS. Do not `--update`,
   and do not report a green ratchet as a red one.
8. Net surface change is deletion or relocation only — **+0** gates, tools, contracts, dependencies.
   Prove: `git diff 1a0bad1..HEAD -- uv.lock pyproject.toml` shows only the one-line member/source
   move and no added distribution; no new file under `contracts/`; no new entry in
   `.claude/settings.json` hooks; no new `tools/` package; no new `harness/commands/` or
   `harness/skills/` entry.

Then record the **ten-commit** table: for each of the ten commits (two per plan across plans 01–05),
the SHA, the subject, and the gate result observed at that commit, taken from the five prior
SUMMARYs. State plainly whether every commit ended green. A pre-revision replay of this plan set had
six of ten commits ending red; the closeout is where that is confirmed fixed or found still broken.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests examples/log-parser/golden_runner -q && uv run python -m tools.harness_emit && git diff --exit-code -- .opencode opencode.json .claude/agents .claude/commands .claude/skills AGENTS.md CLAUDE.md .claude/settings.json tools/harness_emit/emit-manifest.json && uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git diff --exit-code -- docs/reference .memory/derived/contracts-index.md && uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline && uv run pytest tools/ruff_baseline -q && python3 tools/harness_lint/workspace_check.py</automated>
    <automated>test ! -e tools/golden_runner && ! git grep -nE "^[[:space:]]*(from|import)[[:space:]]+tools\.golden_runner" -- tools harness libs && ! git grep -n "run_golden_case\|GOLDEN_DIR\|run_identity_converter" -- tools harness libs</automated>
    <automated>test -z "$(git status --porcelain -- . ':(exclude).planning')"</automated>
  </verify>
  <done>Every gate in the table has been executed and its output recorded; all eight ROADMAP success criteria carry a command and an observed result, with SC-5 using the corrected golden/parity evidence commands and noting the deliberate `format_on_write::resolve_dotnet` retention; the ten-commit greenness table is recorded; the working tree is clean outside `.planning/`; no gate is red.</done>
</task>

<task type="auto">
  <name>Task 2: Measure the net LOC and record the phase's accepted consequences and residuals</name>
  <files>.planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md</files>
  <action>
Implements D-21 and D-20.

1. Measure, do not estimate: run `git diff --shortstat 1a0bad1..HEAD -- . ':(exclude).planning'` and
   record the verbatim output. Also record the per-plan `git diff --shortstat` figures the five prior
   SUMMARYs captured, so the total is auditable. Expect a **smaller net deletion than Phase 43's
   −12,383**, because CER-09 moves rather than deletes — if the number is larger, something was
   deleted that should have moved, and that is worth flagging rather than explaining away.
2. Write the **eleven** consequence/residual items from `<measured_anchors>` into the summary verbatim,
   each with its evidence. Items 1, 2 and 6 are the ones a reviewer must see: the constitution-plane
   downgrade for the four folded golden files; the fact that ADR-0001 and `.github/CODEOWNERS` now
   describe a `golden/` directory that no longer exists; and the deliberate retention of
   `format_on_write::resolve_dotnet` against a criterion that was worded to forbid it. **Raise item 2
   in the PR body. Do not edit the ADR and do not edit CODEOWNERS** — ADRs are append-only,
   supersede-don't-edit, and the repair belongs with Phase 45's ADR-0008 work or a human call at
   milestone close. **Do not edit `.planning/ROADMAP.md`** — the SC-5 wording correction is the
   developer's, made separately; reference it.
3. Record D-20 explicitly: **no mutation-proof table is owed** for this phase, because it removes a
   gate and adds no control. The single exception is the stale-checkout assertion in plan 02, which
   is coverage of a **retained** behavior, extended rather than newly invented — cite its two
   recorded mutation results (removing either tombstone from `RETIRED_SIGNATURES` turns the file
   red; `secret_scan` reds at `test_settings_merge.py:171`). Plan 04's CI-path resolution assertion
   carries its own recorded mutation result for the same reason.
4. Do not modify any file outside `.planning/`. Do not forge `GOLDEN_APPROVE_HUMAN`. Do not author
   `docs/.docs-review-ledger.toml`. No model identifiers in any commit message or code comment.
  </action>
  <verify>
    <automated>git diff --shortstat 1a0bad1..HEAD -- . ':(exclude).planning' && test -z "$(git status --porcelain -- . ':(exclude).planning')"</automated>
    <automated>grep -q "shortstat\|insertions\|deletions" .planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md && grep -qi "CONSTITUTION_GLOBS\|constitution" .planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md && grep -qi "ADR-0001" .planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md && grep -q "format_on_write" .planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md</automated>
  </verify>
  <done>The summary carries the verbatim whole-phase `git diff --shortstat`, the per-plan figures, all eleven recorded consequences and residuals including the `format_on_write::resolve_dotnet` retention and the isolatability-gate coverage loss, and an explicit D-20 statement with the mutation results cited; nothing outside `.planning/` was modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claimed gate result → executed command | A phase closeout assembled from memory is how Phase 43 needed four verification passes |
| written success criterion → the property it means to assert | A criterion whose command over-matches (SC-5 / `resolve_dotnet`) manufactures a false failure and invites an unjustified deletion |
| this phase → the reviewer at merge | Consequences not written down here become defects discovered downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-18 | Repudiation | A success criterion recorded as met without an executed command | mitigate | Every row of the evidence table carries the command and its verbatim output (D-18) |
| T-44-27 | Denial of Service | SC-5's original `resolve_dotnet` absence grep over-matches `format_on_write`, so an executor chasing green would delete a legitimate `dotnet format` toolchain probe from the core | mitigate | Criterion rewritten to assert the golden/parity surface specifically; the retention recorded as consequence 6; ROADMAP correction made by the developer, not here |
| T-44-19 | Elevation of Privilege | The four folded golden files are agent-writable in-session after this phase | accept | Recorded as an accepted consequence with the measured `resolve_path` results; merge-time CODEOWNERS holds; widening `CONSTITUTION_GLOBS` is refused as surface growth (SC-8) |
| T-44-20 | Repudiation | `.github/CODEOWNERS` and ADR-0001 now describe a deleted `golden/` directory | transfer | Raised in the PR for a human decision; ADRs are append-only and the repair is Phase 45's or a milestone-close call. Explicitly not fixed here. |
| T-44-30 | Repudiation | SC-7 written as "the ratchet line must read 84/84" reads a true green as a failure — the deletions remove `E501` findings, so the count necessarily falls (measured 84 → 77). An executor chasing the pinned number either hunts a cleanup that was never skipped or runs the `--update` rebaseline every other plan forbids | mitigate | Criterion restated as "exits 0 with no rule class above baseline"; the observed shrink recorded as evidence; `--update` explicitly forbidden |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | This plan runs read-only gates and writes one summary file. No installs, no dependency change; success criterion 8 is asserted, not assumed. |
</threat_model>

<verification>
All gate commands green; working tree clean outside `.planning/`; the summary contains the evidence
table with the corrected SC-5 commands, the measured shortstat, the ten-commit greenness table, and
the eleven recorded consequences.
</verification>

<success_criteria>
- All eight ROADMAP Phase 44 success criteria carry an executed command and its result (SC-1 to SC-8),
  with SC-5 asserted against the golden/parity surface rather than the bare `resolve_dotnet`
  identifier.
- The ten-commit table records a gate result per commit and states plainly whether every commit
  ended green.
- Whole-phase LOC is reported from `git diff --shortstat`, measured (D-21).
- D-20 is stated explicitly, with the sanctioned coverage extensions and their mutation results
  cited.
- SC-7's ratchet row records `exits 0, no rule class above baseline` with the observed shrink, not a
  pinned 84/84, and no `--update` was run.
- The accepted consequences — the constitution-plane downgrade, the stale CODEOWNERS/ADR-0001
  description, the `commit_gate` component removal, the unreplaced `secret_scan`, the retained
  `format_on_write::resolve_dotnet`, the isolatability-gate coverage loss, and the absent local
  .NET — are written down and, where they need a human, raised in the PR rather than repaired.
</success_criteria>

<output>
Create `.planning/phases/44-non-goal-surface-removal/44-06-SUMMARY.md` when done.
</output>
