---
phase: 44-non-goal-surface-removal
plan: 03
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - harness/commands/component.md
  - harness/commands/pipeline.md
  - harness/skills/pipeline-map/
  - harness/skills/brownfield-adoption/SKILL.md
  - harness/agents/orchestrator.md
  - harness/project.toml
  - tools/harness_lint/caps.py
  - tools/harness_lint/tests/test_conductor_graph_render.py
  - tools/harness_lint/tests/test_pipeline_config.py
  - tools/harness_lint/tests/test_orchestrator_topology.py
  - tools/harness_config/tests/test_loader.py
  - tools/harness_config/tests/test_topology_relationships.py
  - tools/workspace_config/tests/test_endpoints.py
  - examples/log-parser/tests/test_pipeline_topology.py
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - tools/harness_emit/emit-manifest.json
  - AGENTS.md
  - .opencode/
  - .claude/
  - opencode.json
autonomous: true
requirements: [CER-08]

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-11 (/component loses its SECOND \"Mandated order\" section and the component-binding guard that follows it — the first section, steps 1 to 3, survives as the ① mechanism), D-12 (the [pipeline] slot goes but [[components]] and the TOPO-02 [contract_graph.relationships] slot SURVIVE, and effective_relationships() must not break), D-14 (every live-tree-rendering test repaired in the same commit as the deletion), D-15/D-16/D-17/D-18 (commit discipline, message before --, source-first emit, run don't read)."
    - "The core [pipeline] DATA is gone from harness/project.toml but loader.pipeline() survives as a mechanism, so examples/log-parser/tests/test_pipeline_topology.py still collects AND passes — its _CORE_RESOLUTION_DOCS list is repointed in the same commit that deletes the two docs it names"
    - "No surviving harness_lint test reads the deleted slot and passes vacuously — the two edge tests whose loop body no longer executes are deleted, not left green"
    - "harness/commands/pipeline.md and harness/skills/pipeline-map/ do not exist and no surviving harness file points at either"
    - "effective_relationships() returns an empty list on the core default config without raising, and the synthetic-fixture lowering tests still cover the mechanism"
    - "No import is orphaned by the test deletions — the ruff ratchet exits 0 with no rule class above baseline and zero F401 regression (the absolute count falls across this phase; do not pin it)"
  artifacts:
    - path: "harness/commands/component.md"
      provides: "Only the first Mandated order section (structure, per-package AGENTS.md, test harness) plus its Guard"
      max_lines: 40
    - path: "harness/project.toml"
      provides: "No [pipeline] table; [[components]] and [contract_graph.relationships] intact"
    - path: "tools/harness_config/loader.py"
      provides: "pipeline() passthrough retained — the instance imports it"
      contains: "def pipeline"
    - path: "examples/log-parser/tests/test_pipeline_topology.py"
      provides: "_CORE_RESOLUTION_DOCS repointed at a surviving core doc that still declares the <id>.md convention"
      contains: "_CORE_RESOLUTION_DOCS"
  key_links:
    - from: "examples/log-parser/tests/test_pipeline_topology.py"
      to: "tools.harness_config.pipeline"
      via: "module-level import at collection time"
      pattern: "from tools.harness_config import"
    - from: "examples/log-parser/tests/test_pipeline_topology.py _CORE_RESOLUTION_DOCS"
      to: "harness/agents/templates/component-engineer.md"
      via: "read_text() plus an `<id>.md` substring assertion"
      pattern: "_CORE_RESOLUTION_DOCS"
    - from: "examples/log-parser/project.toml"
      to: "loader.pipeline()"
      via: "3 declared instance edges (parser to converter to scheduler to collector)"
      pattern: "\\[pipeline\\]"
---

<objective>
Remove `/component`'s topology-registration half, then remove the core `[pipeline]` DATA slot,
`/pipeline` and the `pipeline-map` skill — while keeping the `loader.pipeline()` mechanism alive,
because the log-parser instance legitimately declares three real pipeline edges and imports the
passthrough at collection time.

Purpose: this is the widest blast radius in the phase and the one whose failure mode is a **green**
test suite. Two `harness_lint` tests go vacuous rather than red when the slot disappears; leaving
them is exactly the "claimed control that does not exist" pattern this milestone was convened to
remove.
Output: two commits, each ending green under the full verification block **including the
`examples/` leg**.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-non-goal-surface-removal/44-CONTEXT.md
@.planning/phases/44-non-goal-surface-removal/44-RESEARCH.md
@.planning/phases/44-non-goal-surface-removal/44-02-SUMMARY.md
</context>

<measured_anchors>
**`/component` — D-11's line numbers, corrected by measurement.** `harness/commands/component.md`
is **78 lines**:

| Lines | Section | Fate |
|---|---|---|
| `:15-30` | "Mandated order (do not skip a step)" — structure, per-package AGENTS.md, test harness | **survives** (the ① mechanism CER-08 keeps) |
| `:31-36` | "Guard" | **survives** |
| `:37-67` | "Mandated order (keep the three in sync) — when the package maps to a topology component" | **goes** |
| `:68-78` | "Guard — component binding" | **goes** |

The correct span is `:37` to EOF (`:78`). D-11 said `:37-66` plus a guard at `:68`; the shape is
right, only the endpoints differ. Every `pipeline` / `pipeline-map` / `[pipeline]` reference in
`component.md` sits at `:39` or later, i.e. entirely inside the deleted half — which is why
`/component` is sequenced **before** the `[pipeline]` removal.

⚠ `component.md:51` — inside the deleted half — is one of only four files that carry the `<id>.md`
resolution token. After Task 1 the surviving carriers are `harness/commands/pipeline.md`,
`harness/skills/pipeline-map/SKILL.md` (both deleted by Task 2) and
**`harness/agents/templates/component-engineer.md:9`**, which survives the whole phase. That last
one is Task 2's repointing target — see the `_CORE_RESOLUTION_DOCS` item below.

**D-12's blast radius is INVERTED — measured in an isolated clone.** Removing the `[pipeline]` table
from `harness/project.toml` breaks **3 tests, none of them in `harness_lint`**:

- `tools/harness_config/tests/test_loader.py::test_pipeline_passthrough` (`:64-68`)
- `tools/harness_config/tests/test_topology_relationships.py::test_lowers_linear_default_to_single_relationship` (`:54-62`)
- `tools/workspace_config/tests/test_endpoints.py::test_core_pipeline_edges_stay_single_repo` (`:48-59`)

The eight `harness_lint` tests D-12 names go **vacuously green** instead: `test_pipeline_config.py:59`
and `:83` are `for edge in pipeline(cfg).get("edges", []):`, so with the slot gone the loop body never
executes and the assertions inside are never reached. Measured directly: 14 passed across the four
files after the slot removal. `test_pipeline_edges_are_well_formed` (`:50`) and
`test_edge_contracts_have_a_tracked_schema` (`:73`) must therefore be **deleted with the slot**,
because they pass — not because they fail.

**Three distinct changes, three different consequences:**

| Change | Breaks | Kind |
|---|---|---|
| Delete the `[pipeline]` table from `harness/project.toml:77-80` | the 3 tests above | runtime assertion |
| Delete `harness/commands/pipeline.md` | 3 tests in `test_conductor_graph_render.py` — it reads that file at `:29-30` | runtime `FileNotFoundError` |
| Delete `loader.pipeline()` + the `tools.harness_config` re-export | 🔴 **the instance, at collection** | collection error |

The third is the trap and it is **out of scope**. `examples/log-parser/tests/test_pipeline_topology.py:20`
imports `pipeline` from `tools.harness_config`, and `examples/log-parser/project.toml:67-71` declares
3 real edges. Removing the passthrough raises `ImportError: cannot import name 'pipeline'` and
interrupts collection — and `uv run pytest -q` **never sees it**, because `testpaths = ["libs/python",
"tools"]` collects zero tests from `examples/**`. It would land green and break only CI `golden`
step 2. **Keep `loader.pipeline()`.** This is symmetric with D-12's own carve-out for
`[[components]]` and `[contract_graph.relationships]`: the slot's *default data* is the non-goal, not
the reader.

🔴 **A SECOND instance-side trap in the same file, and it is not the import.**
`examples/log-parser/tests/test_pipeline_topology.py:111-114` defines a hardcoded path list:

    _CORE_RESOLUTION_DOCS = (
        _REPO_ROOT / "harness" / "commands" / "pipeline.md",
        _REPO_ROOT / "harness" / "skills" / "pipeline-map" / "SKILL.md",
    )

read at `:131-132` by `test_core_resolution_convention_matches_instance_agents`, which asserts
`"<id>.md" in doc.read_text()`. Task 2 deletes **both** named files, so the test raises
`FileNotFoundError` and the mandatory `examples/` leg exits 1 — measured in replay. The file
**collects** (the import survives); it does not **pass**. Repair it in Task 2's commit by repointing
the tuple at `harness/agents/templates/component-engineer.md`, the one surviving core doc that still
declares the `<id>.md` convention (`:9`). Keep the tuple non-empty and keep the assertion — an empty
tuple would make the second loop vacuous, which is the exact defect this plan exists to remove.

**`effective_relationships()` survives but returns empty.** Measured: with the `[pipeline]` table it
returns one relationship (`pipeline/greeting/source->sink`); without it, `[]`, no crash. TOPO-02's
seam is structurally intact as D-12 requires. `test_lowers_linear_default_to_single_relationship`
asserts the non-empty case against the **live** config; the same module's synthetic-fixture tests
(around `:74` and `:95-96`) construct their own `"pipeline"` dicts and keep working — they are the
real control for the lowering mechanism.

🔴 **Deleting those tests orphans imports and REGRESSES the ruff ratchet (`F401` 0 → 3), measured.**
The ratchet hard-fails on any rule class ABOVE baseline ("baseline may only SHRINK") — a smaller
total is a PASS — so this is a
red commit, not a warning:

| File | Import line | After the deletions |
|---|---|---|
| `tools/harness_lint/tests/test_pipeline_config.py` | `from tools.harness_config import components, languages, load_project, pipeline` | `pipeline` is unused — drop it from the list; the other three stay |
| `tools/workspace_config/tests/test_endpoints.py` | `from tools.harness_config import load_project, pipeline` | **both** names unused — delete the whole import line |

Fix them in the same commit as the deletions. Confirm with `uv run python -m tools.ruff_baseline`
before committing, not after.

**Surviving pointers to sweep** (nothing may point at a corpse mid-phase):
- `pipeline-map`: `harness/commands/component.md` (all inside the deleted half),
  `harness/skills/brownfield-adoption/SKILL.md`.
- `/pipeline`: `harness/agents/orchestrator.md` (`:85-88` routing rows, plus `:24`, `:59-65`,
  `:74-76`), `harness/commands/component.md` (deleted half), `harness/project.toml`.
- `tools/harness_lint/tests/test_orchestrator_topology.py:61` asserts `"/pipeline" in lowered`
  against the orchestrator routing body — it must be narrowed in the **same commit** that removes the
  routing rows, or the sweep reds.

**`tools/harness_lint/tests/test_conductor_graph_render.py`** reads `harness/commands/pipeline.md`
and `harness/skills/pipeline-map/SKILL.md` at `:29-30`. Its entire subject is those two deleted
artifacts — **delete the file**; there is nothing to narrow.

**`test_pipeline_config.py` survives as a file.** Its other two tests
(`test_component_languages_are_declared`, `test_component_ids_unique`) read `[[components]]`, which
survives. Keep the file, delete the two edge tests, and rename its module docstring off "PIPE-01
topology CONSISTENCY gate".

⚠ **GEN-04 trap when you write that docstring.** `tools/harness_lint/tests/test_core_no_example_dep.py::test_core_has_no_example_dependency`
scans every tracked file under `tools/`, `harness/`, `libs/` for the forbidden path token
`examples/` (`_PATH_TOKENS` at `:47` over `_CORE_ROOTS` at `:53`) and hard-fails on a hit — in
**prose as well as code**. Replay hit this twice by writing a rationale that named the example tree.
Phrase every docstring/comment you touch in a core file without the token: say "the instance
overlay" or "an instance's own tree", never `examples/` or `examples/log-parser`. The only
sanctioned instance-pointer file is `harness/project.toml`.

**Counts entering this plan:** 20 commands, 11 skills (plan 01 removed one of each). After Task 2:
**19 commands, 10 skills.** `tools/harness_lint/caps.py` `EXPECTED_SKILLS` must lose `pipeline-map`
in the same commit as the directory deletion — a skill-set mismatch hard-fails the emitter before it
writes a byte. `tools/harness_emit/tests/test_coexist.py` carries the command count in **four**
places: the `:70` and `:71` literals, the `:41` docstring, and the **function NAME at `:40`**
(`test_all_NN_commands_emit_to_both_trees`). The function name is the site most often missed.

🔴 **`tools/harness_emit/emit-manifest.json` is a COMMITTED emitter output and belongs in Task 2's
pathspec.** Task 2 removes two emitted artifacts, so the manifest self-prunes their rows on re-emit
(`manifest.py:64-80`). Replaying this phase with the manifest outside the pathspec left
` M tools/harness_emit/emit-manifest.json` in `git status --porcelain` and made the task's own
`git diff --exit-code` exit 1. "Self-prunes" means **do not hand-edit it**; it does **not** mean
exclude it from the commit. Task 1 changes no artifact set, so the manifest does not move there.

`git rm` **already stages the deletion**. A subsequent `git add -- <deleted-path>` aborts with exit
128 (`fatal: pathspec '<path>' did not match any files`). Add only the files you **modified**.

**Verification block:** as in `44-01-PLAN.md`. The `examples/` leg is **not optional** for Task 2 —
it is the only thing that proves the instance still collects **and passes**.
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: Remove /component's topology-registration half</name>
  <files>harness/commands/component.md, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, AGENTS.md, .opencode/, .claude/, opencode.json</files>
  <action>
Implements D-11. One commit, sequenced first so that the `[pipeline]` commit meets a smaller pointer
surface.

Delete `harness/commands/component.md:37` through EOF (`:78`) — the second "Mandated order" section
and the "Guard — component binding" block that follows it. Keep `:1-36` verbatim: the first Mandated
order section (structure, per-package `AGENTS.md`, test harness) and its Guard are the ① mechanism
CER-08 explicitly preserves. Do not rewrite the surviving half; if the file ends on a heading with no
body after the excision, trim trailing blank lines only.

Nothing else reads that section. `tools/harness_lint/tests/test_commands.py` checks that a command
named `component` is *present*, not what its body contains — confirm by running it rather than by
reading it (D-18).

Re-emit (`uv run python -m tools.harness_emit`) and regenerate the emit snapshot
(`uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update`) in this commit —
`component.md` is a rendered body. Include `AGENTS.md` in the pathspec (the emitter rewrites its
HARNESS-MANAGED block). This commit adds and removes no emitted artifact, so `emit-manifest.json`
does not change — verified in replay. Run `git status --porcelain` after committing anyway; if the
manifest did move, add it to the pathspec and amend.

Commit with the message before the pathspec separator (D-16), then verify with the full block.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests -q && uv run python -m tools.harness_emit && git diff --exit-code && uv run python -m tools.ruff_baseline</automated>
    <automated>test -z "$(git status --porcelain)" && python3 -c "import sys; n=sum(1 for _ in open('harness/commands/component.md')); print(n); sys.exit(0 if n <= 40 else 1)" && ! grep -qi "topology" harness/commands/component.md</automated>
  </verify>
  <done>`harness/commands/component.md` retains only the first Mandated order section and its Guard, carries no topology-registration content and no `/pipeline` or `pipeline-map` reference; both runtime trees re-emit clean; `git status --porcelain` is empty and the commit ends green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the core [pipeline] data, /pipeline and pipeline-map — keeping loader.pipeline()</name>
  <files>harness/project.toml, harness/commands/pipeline.md, harness/skills/pipeline-map/, harness/agents/orchestrator.md, harness/skills/brownfield-adoption/SKILL.md, tools/harness_lint/caps.py, tools/harness_lint/tests/test_conductor_graph_render.py, tools/harness_lint/tests/test_pipeline_config.py, tools/harness_lint/tests/test_orchestrator_topology.py, tools/harness_config/tests/test_loader.py, tools/harness_config/tests/test_topology_relationships.py, tools/workspace_config/tests/test_endpoints.py, examples/log-parser/tests/test_pipeline_topology.py, tools/harness_emit/tests/test_coexist.py, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, tools/harness_emit/emit-manifest.json, AGENTS.md, .opencode/, .claude/, opencode.json</files>
  <action>
Implements D-12 as corrected by measurement. One commit.

1. Delete the `[pipeline]` table from `harness/project.toml:77-80` (the `edges` array and its
   `# END PIPE-01 topology` fence as appropriate). **Do not touch `[[components]]` and do not touch
   the `[contract_graph.relationships]` TOPO-02 slot** — D-12 carves both out explicitly.
2. **Do not touch `tools/harness_config/loader.py`.** `pipeline()` stays. The instance imports it at
   collection time and declares three real edges of its own; removing it breaks
   `examples/log-parser/tests/test_pipeline_topology.py` at collection, and the core suite would
   never notice.
3. `git rm harness/commands/pipeline.md` and `git rm -r harness/skills/pipeline-map`.
4. **Repair the instance-side hardcoded path list in this same commit.** In
   `examples/log-parser/tests/test_pipeline_topology.py`, repoint `_CORE_RESOLUTION_DOCS`
   (`:111-114`) — which names both files step 3 just deleted — at
   `_REPO_ROOT / "harness" / "agents" / "templates" / "component-engineer.md"`, the one surviving
   core doc that declares the `<id>.md` convention (`:9`). Keep the tuple non-empty and keep the
   `"<id>.md" in text` assertion at `:131-132`; update the surrounding docstring/comment prose so it
   names the doc it now reads. Without this, `test_core_resolution_convention_matches_instance_agents`
   raises `FileNotFoundError` and the mandatory `examples/` leg exits 1. This file is in the
   instance plane, so GEN-04 does not constrain its prose (example→core is the allowed direction).
5. Sweep surviving pointers in the same commit: remove the `/pipeline` routing rows and the
   topology-trace step that names the deleted command from `harness/agents/orchestrator.md`, and the
   `pipeline-map` reference from `harness/skills/brownfield-adoption/SKILL.md`. Keep prose that
   refers to `[[components]]` or `effective_relationships()` — those mechanisms survive. Excise
   minimally; the orchestrator's broader rewrite is Phase 46's PROD-02 and is not this phase's work.
6. Repair the live-tree-rendering declarations in this same commit (D-14):
   - `tools/harness_lint/caps.py` — drop `pipeline-map` from `EXPECTED_SKILLS` (11 to 10) and extend
     the narrative comment. **Do this before running the emitter.**
   - `tools/harness_emit/tests/test_coexist.py` — command count 20 to 19 at all four sites: the
     `:70` and `:71` literals, the `:41` docstring, and the **function name at `:40`**
     (`test_all_20_… → test_all_19_…`).
   - `tools/harness_lint/tests/test_orchestrator_topology.py:61` — narrow or remove the
     `"/pipeline" in lowered` assertion so the routing-body sweep in step 5 does not red it. Keep the
     surrounding stage/component assertions if their subjects survive; run the file and let it tell
     you (D-18).
7. Handle the four test-side consequences, each measured:
   - **Delete** `tools/harness_lint/tests/test_conductor_graph_render.py` entirely. It reads the two
     deleted files at `:29-30`; its whole subject is gone.
   - **Delete** `test_pipeline_edges_are_well_formed` (`test_pipeline_config.py:50`) and
     `test_edge_contracts_have_a_tracked_schema` (`:73`). They iterate an empty list and pass while
     asserting nothing. Keep the file and its two `[[components]]` tests, and rename the module
     docstring off "PIPE-01 topology CONSISTENCY gate". ⚠ When you write that docstring, do **not**
     use the token `examples/` — GEN-04 hard-fails on it in any core-plane file, prose included.
     Say "the instance overlay" instead.
   - **Narrow** `test_loader.py::test_pipeline_passthrough` so it proves the passthrough *mechanism*
     against the now-empty core default (the function still resolves and returns a mapping) rather
     than asserting a single `greeting` edge.
   - **Delete** `test_endpoints.py::test_core_pipeline_edges_stay_single_repo`. Its first assertion is
     `assert core_edges, "core [pipeline] must declare at least one edge"` — its entire subject is the
     core default topology data that CER-08 removes. Leave the workspace-layer endpoint tests in the
     same file untouched.
   - **Delete** `test_topology_relationships.py::test_lowers_linear_default_to_single_relationship`
     (the live-config case). The synthetic-fixture lowering tests in the same module construct their
     own `"pipeline"` dicts and remain the real control for the mechanism — verify they still pass.
8. **Clean up the imports those deletions orphan, in this same commit** — measured `F401` 0 → 3, and
   the ruff ratchet hard-fails on it:
   - `tools/harness_lint/tests/test_pipeline_config.py` — drop `pipeline` from the
     `from tools.harness_config import …` line; `components`, `languages` and `load_project` are
     still used.
   - `tools/workspace_config/tests/test_endpoints.py` — delete the whole
     `from tools.harness_config import load_project, pipeline` line; both names go unused.
   Then run `uv run python -m tools.ruff_baseline` **before** committing and confirm it exits 0 with
   no rule class above baseline. Expect a total **below** 84 — the deletions remove `E501` findings
   and the tool prints `PASS — and findings went DOWN`. Do not `--update`. If any
   other file you touched reports a new finding, fix the import rather than the baseline — the
   baseline may only shrink.
9. Re-emit and regenerate the emit snapshot in this commit. `emit-manifest.json` prunes the
   `/pipeline` and `pipeline-map` rows from both trees by itself — **do not edit it, and do not
   leave it out of the commit.**
10. Stage the **modified** files only (`git rm` already staged the deletions; re-adding a removed
    path aborts with exit 128), commit with the message before `--` and an explicit pathspec listing
    every deleted path plus every modified path including `emit-manifest.json`, then verify with the
    full block. **The `examples/` leg is mandatory here** — it is the only thing that proves the
    instance still collects *and passes* with `loader.pipeline()` intact and
    `_CORE_RESOLUTION_DOCS` repointed. Run `git status --porcelain` after committing: it must be
    empty.
11. After the commit, run `uv run pytest tools/harness_lint/tests/test_pipeline_config.py -q` and read
    the count: it must reflect the two deletions, not the pre-deletion count. A file that reports the
    same number of passes as before the slot removal is the vacuity signature.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests -q && uv run python -m tools.harness_emit && git diff --exit-code && uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline && python3 tools/harness_lint/workspace_check.py</automated>
    <automated>test -z "$(git status --porcelain)" && test ! -e harness/commands/pipeline.md && test ! -e harness/skills/pipeline-map && test ! -e tools/harness_lint/tests/test_conductor_graph_render.py && grep -q "def pipeline" tools/harness_config/loader.py && ! grep -v '^#' harness/project.toml | grep -q '^\[pipeline\]'</automated>
    <automated>uv run pytest examples/log-parser/tests/test_pipeline_topology.py -q && uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q</automated>
    <automated>uv run python -c "from tools.harness_config import pipeline, load_project, effective_relationships; assert pipeline(load_project()).get('edges', []) == []; assert effective_relationships(load_project()) == []"</automated>
  </verify>
  <done>The core `[pipeline]` data, `/pipeline` and `pipeline-map` are gone; `loader.pipeline()` and the TOPO-02 relationships slot survive; `effective_relationships()` returns `[]` without raising; no surviving `harness_lint` test reads the deleted slot vacuously; `test_pipeline_topology.py` collects AND passes with `_CORE_RESOLUTION_DOCS` repointed; no orphaned import and the ruff ratchet exits 0 with no rule class above baseline; GEN-04 passes; `git status --porcelain` is empty and the commit ends green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core config slot → instance consumers | `examples/log-parser` imports `tools.harness_config.pipeline` at collection time and is invisible to `uv run pytest -q` |
| core doc → instance hardcoded path list | The instance reads named core docs by path; deleting one breaks it at runtime, not at import |
| declared gate → actual assertion | A gate whose loop body never executes reports green while checking nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07 | Repudiation | `test_pipeline_config.py:50,73` pass vacuously after the slot removal — the harness would claim a topology consistency gate it no longer runs | mitigate | Delete both edge tests with the slot; assert the file's post-commit pass count changed |
| T-44-08 | Denial of Service | Removing `loader.pipeline()` breaks the instance at collection, undetected by the core suite | mitigate | Keep the passthrough (explicit non-goal in this plan); run the `examples/` leg in every verify block; assert the symbol still imports |
| T-44-22 | Denial of Service | `_CORE_RESOLUTION_DOCS` names both deleted docs by path; the instance test collects but raises `FileNotFoundError` at run, reddening the mandatory `examples/` leg | mitigate | Repoint the tuple at `harness/agents/templates/component-engineer.md` in the same commit; assert `test_pipeline_topology.py` passes, not merely collects |
| T-44-23 | Denial of Service | The test deletions orphan `pipeline` / `load_project` imports; `F401` 0 → 3 hard-fails the zero-slack ruff ratchet and ends the commit red | mitigate | Explicit import cleanup step; `tools.ruff_baseline` run before committing, asserted in verify |
| T-44-09 | Tampering | Hand-editing the emitted trees, `emit-manifest.json` or the `.ambr` instead of regenerating | mitigate | `python -m tools.harness_emit` plus `pytest --snapshot-update`; `git diff --exit-code` and empty `git status --porcelain` after re-emit |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs and no dependency change in this plan (SC-8). |
</threat_model>

<verification>
Both commits satisfy the full verification block including the `examples/` leg, and leave
`git status --porcelain` empty. After Task 2: `git grep -n "/pipeline" -- harness` returns nothing,
and `git grep -n "pipeline-map" -- harness tools` returns **exactly one** line —
`tools/harness_lint/caps.py`'s Phase-8 narrative history line (`caps.py:125`, "Phase 8 (Pipeline
Topology) adds one topology-trace skill: pipeline-map"), which step 6 tells you to *extend*, not
delete. That line is retained phase history, the same class as `test_commands.py:8`'s historical
mention in plan 01. This criterion is **not** asserted in an `<automated>` block precisely because
it is not an absence claim; do not report it as "returns nothing", and do not delete the history
line to make it so.
</verification>

<success_criteria>
- The `[pipeline]` slot and its consistency-gate assertions are gone, and the surviving `harness_lint`
  suite has no dangling or vacuous topology assertion (SC-4).
- `harness/commands/pipeline.md` and `harness/skills/pipeline-map/` do not exist and no surviving
  file invokes them (SC-1).
- `/component` retains steps 1 to 3 as the ① mechanism.
- `[[components]]`, `[contract_graph.relationships]` and `loader.pipeline()` survive; the instance
  still collects **and passes**.
- Both commits end green; ruff ratchet exits 0 with no rule class above baseline, `emit-drift`,
  `contract-drift` clean (SC-7).
</success_criteria>

<output>
Create `.planning/phases/44-non-goal-surface-removal/44-03-SUMMARY.md` when done. Record the
post-commit pass count for `test_pipeline_config.py`, the `examples/` leg count, the
`emit-manifest.json` row-delta, and the per-commit `git diff --shortstat`.
</output>
