---
phase: 44-non-goal-surface-removal
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - tools/harness_emit/tests/test_settings_merge.py
  - tools/harness_emit/merge.py
  - tools/hooks/secret_scan.py
  - tools/hooks/tests/test_secret_scan.py
  - tools/hooks/tests/test_settings_coexist.py
  - tools/hooks/tests/test_contract_guard.py
  - tools/hooks/contract_guard.py
  - tools/hooks/_stdin.py
  - tools/hooks/__init__.py
  - tools/hooks/pyproject.toml
  - tools/harness_emit/tests/test_coexist.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - tools/harness_emit/emit-manifest.json
  - tools/harness_lint/workspace_check.py
  - tools/harness_lint/tests/test_workspace_member_completeness.py
  - tools/adoption_scan/scan.py
  - tools/adoption_scan/tests/conftest.py
  - harness/plugins/secret-scan.ts
  - harness/commands/review.md
  - README.md
  - AGENTS.md
  - .opencode/
  - .claude/
  - opencode.json
autonomous: true
requirements: [CER-08]

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-06 (secret_scan is a live PreToolUse hook, not a package — the hook module, its test, the .ts plugin, the HARNESS_SIGNATURES entry, the emitted hook-group literal and the review.md prose all go, with NO replacement hook, CI job or pre-commit entry), D-07 (merge.py's RETIRED_SIGNATURES gains \"tools.hooks.secret_scan\" and is NEVER cleared afterwards — append-only tombstone), D-08 (tools/adoption_scan keeps its own SECRET_PATH_GLOBS and SECRET_CONTENT_PATTERNS untouched; the proof is its redaction tests passing unchanged), D-14 (every live-tree-rendering test repaired in the same commit as the deletion), D-15 (delete then add then commit then verify; no commit ends red), D-16 (git commit -m message BEFORE --), D-17 (source-first emit, never hand-edit the emitted trees), D-18 (run things, don't read them)."
    - "A checkout still carrying the pre-44 .claude/settings.json drops the secret_scan hook group on re-emit"
    - "test_retired_signature_group_is_dropped_from_a_stale_checkout exercises EVERY entry of RETIRED_SIGNATURES, not entry [0] only"
    - "Deleting tools.hooks.secret_scan from RETIRED_SIGNATURES turns the suite RED (mutation-proved) — the pre-existing test did not do this"
    - "The emitted .claude/settings.json carries 6 PreToolUse slots and 4 PostToolUse slots and no secret_scan command"
    - "tools/adoption_scan's redaction tests pass with zero edits to its pattern data"
    - "No replacement for secret_scan exists anywhere — not a lighter hook, not a CI job, not a pre-commit entry"
    - "README.md's Runtime hooks row no longer advertises secret-scan as a shipped control"
  artifacts:
    - path: "tools/harness_emit/merge.py"
      provides: "RETIRED_SIGNATURES with two permanent tombstones; HARNESS_SIGNATURES without secret_scan; no secret_scan hook-group literal"
      contains: "RETIRED_SIGNATURES"
    - path: "tools/harness_emit/tests/test_settings_merge.py"
      provides: "Loop over every RETIRED_SIGNATURES entry plus explicit membership pins for resume_gate and secret_scan"
      contains: "RETIRED_SIGNATURES"
  key_links:
    - from: "tools/harness_emit/merge.py RETIRED_SIGNATURES"
      to: "a stale checkout's .claude/settings.json"
      via: "re-emit-driven group removal"
      pattern: "RETIRED_SIGNATURES"
    - from: "harness/plugins + merge.py hook groups"
      to: ".claude/settings.json PreToolUse slot count"
      via: "python -m tools.harness_emit"
      pattern: "PreToolUse"
---

<objective>
Remove `secret_scan` whole — hook module, test, TypeScript plugin, emitter signature, emitted hook
group and prose — with no replacement, and close the coverage hole that let Phase 43 ship a
`RETIRED_SIGNATURES` defect.

Purpose: this is the one deletion in the phase that can strand *other people's checkouts*. Getting
the tombstone right, and getting a test that actually fails when the tombstone is wrong, is the
whole point.
Output: two commits — a coverage-first commit that stands green on its own, then the removal.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-non-goal-surface-removal/44-CONTEXT.md
@.planning/phases/44-non-goal-surface-removal/44-RESEARCH.md
@.planning/phases/43-lifecycle-plane-removal/REVIEW.md
@.planning/phases/44-non-goal-surface-removal/44-01-SUMMARY.md
</context>

<measured_anchors>
- `tools/harness_emit/merge.py:88-93` — `HARNESS_SIGNATURES` tuple; `"tools.hooks.secret_scan"` is
  its third entry.
- `tools/harness_emit/merge.py:104-118` — the `RETIRED_SIGNATURES` docstring. It already states the
  permanence rule and cites Phase 43. **Append a Phase-44 line; do not rewrite it.**
- `tools/harness_emit/merge.py:111` — `RETIRED_SIGNATURES` currently holds exactly one entry,
  `tools.hooks.resume_gate`.
- `tools/harness_emit/merge.py:175-186` — the emitted hook group whose matcher is
  **`Read|Write|Edit`** (not `Write|Edit|Bash`) and whose command is the guard prefix plus
  `uv run python -m tools.hooks.secret_scan`.
- MUTATION-PROVED GAP. `tools/harness_emit/tests/test_settings_merge.py:131-132` reads
  `merge.RETIRED_SIGNATURES[0]` only. The researcher appended `tools.hooks.secret_scan`, went green,
  then deleted it from the tuple — the test **stayed green**. Converting the body to a `for` loop
  over the tuple also stays green on the same mutation, because a loop only exercises what is
  listed. **Two changes are required: the loop AND an explicit membership pin.** The pin is the only
  assertion that reds on the Phase-43 "clear the tuple afterwards" move.
- ✅ **Held under full replay.** The `RETIRED_SIGNATURES` repair is genuinely non-vacuous: dropping
  `secret_scan` from the tuple REDs at `test_settings_merge.py:171`. Do not weaken it.
- `tools/harness_emit/tests/test_settings_merge.py:106,152` compare **byte-for-byte against the live
  `.claude/settings.json`**. Before the re-emit, 3 of its 7 tests fail — so the re-emit belongs in
  the same commit as the `merge.py` edit.
- `tools/hooks/tests/test_settings_coexist.py` literals to repair, all in one commit: `:3` module
  docstring naming the four Phase-4 gates; `:83` the `_NEW_GATES` row for
  `tools.hooks.secret_scan` with matcher `Read|Write|Edit`; `:112` docstring "7 PreToolUse
  (4 GSD + 3 harness)"; `:114` the slot-count assertion and its message string. **Both the count and
  its message string** move 7 to 6 and "3 harness gates" to "2 harness gates". PostToolUse stays 4.
- `tools/harness_emit/tests/test_coexist.py:165` constructs a fixture hook group whose command is
  `tools.hooks.secret_scan`. Functional, not prose — run the test and repair what it reports (D-18);
  do not assume from reading which way it needs to go. The **command count** in the same file is
  Task 2 of plan 01's and plan 03's business, not this plan's; this commit adds and removes no
  command.
- Prose-only references to sweep (no test fires on any of them):
  `tools/harness_lint/workspace_check.py:22` and
  `tools/harness_lint/tests/test_workspace_member_completeness.py:13` — both list contract_guard,
  secret_scan, commit_gate; drop the middle one (Phase 43's WR-08 made the identical repair for
  `resume_gate` in these same two lines); `tools/hooks/pyproject.toml:4,11`;
  `tools/hooks/contract_guard.py:19,41`; `tools/hooks/_stdin.py:3`; `tools/hooks/__init__.py:4`;
  `tools/hooks/tests/test_contract_guard.py:11`; `tools/adoption_scan/scan.py` (the sentence saying
  its patterns are *not* the hook's); `tools/adoption_scan/tests/conftest.py:53`.
- `harness/commands/review.md:22-26` — the "2. Secret-scan posture" section, which states the hook
  *enforces* secret leakage on write. Remove the section; renumber what follows.
- 🔴 **`README.md:58` advertises a control that will no longer exist.** The "Runtime hooks" feature
  row reads ``contract-guard`, `polyglot-lint`, `format-on-write`, `secret-scan`, `commit-gate` —
  prose advice made enforceable.` Leaving `secret-scan` there directly undercuts this phase's own
  accepted consequence #4 ("`secret_scan` has no replacement, by design") by continuing to promise
  the removed control to every reader of the front page. **Drop the `secret-scan` item from the row;
  change nothing else in the row and nothing else in `README.md`.** `README.md:119`'s
  `tools/golden_runner` path spelling is a DIFFERENT residual, stays Phase 45's (CER-11), and must
  not be touched here.
- `docs/adr/0004`, `0010`, `0011`, `0012` name `secret_scan`. **Do not edit them.** ADRs are
  append-only, supersede-don't-edit, CODEOWNERS-gated; ADR-0012 already records `secret_scan` as a
  permanent residual caught at CI/PR review.
- 🔴 **`tools/harness_emit/emit-manifest.json` is a COMMITTED emitter output and belongs in Task 2's
  pathspec.** Deleting `harness/plugins/secret-scan.ts` removes an emitted artifact, so the manifest
  self-prunes its rows on re-emit (`manifest.py:64-80`). Replaying this phase with the manifest
  outside the pathspec left ` M tools/harness_emit/emit-manifest.json` in `git status --porcelain`
  and made this task's own `git diff --exit-code` exit 1. "Self-prunes" means **do not hand-edit
  it**; it does **not** mean exclude it from the commit.
- `git rm` **already stages the deletion**. A subsequent `git add -- <deleted-path>` aborts with
  exit 128 (`fatal: pathspec '<path>' did not match any files`). Add only the files you **modified**;
  never re-add a `git rm`'d path.
- 🔴 **THREE `adoption_scan` tests are red between `git rm` and `git commit`, not one** — same
  `git ls-files` + `stat()` root cause in every deletion commit of this phase:
  `test_dispositions.py::test_catalog_invariant_to_untracked_local_state`,
  `test_plan_classification.py::test_contract_candidate_matches_real_repo_schema_count`, and
  `test_scan_exclusions.py::test_ci_yml_false_positive_closed`. All three go green at the commit.
  Do not repair them.
- Measured post-removal emit: **83** artifacts emitted, exit 0; `.claude/settings.json` PreToolUse 6 /
  PostToolUse 4 / secret_scan absent; `test_settings_merge.py` 7 passed. (The plan previously said
  87; the replay measured 83. Report what the run prints — the count is informational, nothing
  asserts it.)
- The ruff ratchet hard-fails on any rule class going ABOVE its baseline; the absolute count falls
  across this phase and a smaller number is a PASS (see the ratchet rule in `44-01-PLAN.md`). After deleting
  `test_secret_scan.py` and the two `merge.py` functions, re-check every file you edited for an
  import that is now unused (`F401`) before committing.
- The verification block is the one in `44-01-PLAN.md` under `<measured_anchors>`.
</measured_anchors>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the RETIRED_SIGNATURES drop-test cover every tombstone, not entry [0]</name>
  <files>tools/harness_emit/tests/test_settings_merge.py</files>
  <behavior>
    - Test 1: the stale-checkout drop test reconstructs a stale settings.json group for EVERY entry
      of `merge.RETIRED_SIGNATURES` and asserts the re-emit drops each one. Today it does this for
      entry [0] only.
    - Test 2: an explicit membership assertion pins `tools.hooks.resume_gate` as present in
      `merge.RETIRED_SIGNATURES`, with a comment citing Phase 43 / CER-07. This is the assertion that
      reds when the tuple is cleared — the exact Phase-43 defect recorded as REVIEW.md CR-01.
    - Mutation control: removing `tools.hooks.resume_gate` from the tuple must turn this file RED.
      Demonstrate the red, then revert.
  </behavior>
  <action>
Wave-0 coverage gap, landed before the deletion it protects so it stands green on its own.

Refactor the drop test at `tools/harness_emit/tests/test_settings_merge.py:131-132`: extract the
existing single-signature body into a helper that takes a signature string, and call it from a loop
over `merge.RETIRED_SIGNATURES`. Keep the existing non-vacuity guard that asserts the tuple is
non-empty.

Then add the membership pin as a separate, named assertion — one line per tombstone, each carrying a
comment naming the phase and requirement that added it. Only `resume_gate` is pinned in this commit;
`secret_scan`'s pin lands in Task 2 in the same commit as its append, so this commit stays green.

Do not touch `merge.py`. Do not touch the emitted trees — this commit contains exactly one file.

Prove the mutation control before committing: delete the `resume_gate` entry from
`merge.RETIRED_SIGNATURES`, run the file, observe RED, restore `merge.py` with an explicit single
pathspec checkout (never `git checkout <ref> -- .`), re-run to observe GREEN. Record both outcomes in
the summary.

Commit with the message before the pathspec separator (D-16), touching only the one test file.

This adds no gate, tool, contract or dependency — it is assertions inside an existing test module
already run by the existing `core-suite` CI job, so ROADMAP success criterion 8 is unaffected.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_emit/tests/test_settings_merge.py -q && uv run pytest -q && uv run python -m tools.ruff_baseline</automated>
  </verify>
  <done>The drop-test loops over every `RETIRED_SIGNATURES` entry; a membership pin for `resume_gate` exists; deleting that entry from the tuple turns the file red (demonstrated and reverted); the full suite is green; the commit contains exactly one file.</done>
</task>

<task type="auto">
  <name>Task 2: Delete secret_scan whole and append its permanent tombstone</name>
  <files>tools/hooks/secret_scan.py, tools/hooks/tests/test_secret_scan.py, harness/plugins/secret-scan.ts, tools/harness_emit/merge.py, tools/harness_emit/tests/test_settings_merge.py, tools/hooks/tests/test_settings_coexist.py, tools/harness_emit/tests/test_coexist.py, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, tools/harness_emit/emit-manifest.json, tools/harness_lint/workspace_check.py, tools/harness_lint/tests/test_workspace_member_completeness.py, tools/hooks/pyproject.toml, tools/hooks/contract_guard.py, tools/hooks/_stdin.py, tools/hooks/__init__.py, tools/hooks/tests/test_contract_guard.py, tools/adoption_scan/scan.py, tools/adoption_scan/tests/conftest.py, harness/commands/review.md, README.md, AGENTS.md, .opencode/, .claude/, opencode.json</files>
  <action>
Implements D-06, D-07, D-08. One commit.

1. `git rm tools/hooks/secret_scan.py tools/hooks/tests/test_secret_scan.py harness/plugins/secret-scan.ts`.
   `tools/hooks` stays a workspace member (other gates live there), so no lock refresh should be
   needed — confirm with `python3 tools/harness_lint/workspace_check.py`, do not assume.
2. `tools/harness_emit/merge.py`: remove `tools.hooks.secret_scan` from `HARNESS_SIGNATURES`;
   remove the whole `Read|Write|Edit` hook group; **append** `tools.hooks.secret_scan` to
   `RETIRED_SIGNATURES` and append one sentence to the docstring above it recording Phase 44 /
   CER-08. Never clear or reorder existing entries (D-07). Removing a signature from
   `HARNESS_SIGNATURES` alone is what strands stale checkouts: the group falls through as an
   unmatched signature, is mistaken for GSD/human-owned, is kept verbatim forever, and then invokes a
   module that no longer exists — the guard exits non-zero and PreToolUse denies every
   Write/Edit/Bash with the repair locked behind the outage it caused.
3. `tools/harness_emit/tests/test_settings_merge.py`: add the `secret_scan` membership pin next to
   the `resume_gate` pin from Task 1. Before committing, mutate — delete `tools.hooks.secret_scan`
   from `RETIRED_SIGNATURES`, confirm the file goes RED (measured: it reds at `:171`), restore,
   confirm GREEN. Record both. This is the one coverage item D-20 sanctions, and it is coverage of a
   retained behavior, extended rather than newly invented.
4. Repair the live-tree-rendering literals in this same commit (D-14): all four sites in
   `tools/hooks/tests/test_settings_coexist.py` — count 7 to 6 **and** the message string
   "3 harness gates" to "2 harness gates". Then run `tools/harness_emit/tests/test_coexist.py` and
   repair whatever its fixture at `:165` reports. Do **not** change that file's command count or
   the `test_all_NN_commands_emit_to_both_trees` function name — this commit adds and removes no
   command.
5. Sweep the prose references listed in `<measured_anchors>`. For `tools/adoption_scan/scan.py`,
   adjust only the sentence that cross-references the hook — **do not touch `SECRET_PATH_GLOBS` or
   `SECRET_CONTENT_PATTERNS`** (D-08). Do not edit any `docs/adr/**` file.
6. Remove the "2. Secret-scan posture" section from `harness/commands/review.md` and renumber the
   sections after it. Then drop `secret-scan` from `README.md:58`'s "Runtime hooks" feature row —
   the front page must not keep advertising a control this commit deletes (see
   `<measured_anchors>`). `README.md` is not a derived artifact and not emitted; edit it directly,
   change nothing else in it, and leave `README.md:119` alone (Phase 45's CER-11).
7. Re-emit with `uv run python -m tools.harness_emit`, then regenerate the emit snapshot with
   `uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update`, both in this
   commit — `review.md` is a rendered body, so step 6 alone invalidates the snapshot. `AGENTS.md`'s
   HARNESS-MANAGED block is rewritten by the emitter; include `AGENTS.md` in the pathspec.
   `emit-manifest.json` prunes the `secret-scan` plugin rows by itself — **do not edit it, and do
   not leave it out of the commit.**
8. Before staging, re-check every file you edited for an import that is now unused. The ruff ratchet
   hard-fails on a new `F401` (a rule class above baseline); a lower total is not a failure.
9. Stage the **modified** files only — `git rm` already staged the three deletions and re-adding a
   removed path aborts with exit 128. Then commit with the message before `--` and an explicit
   pathspec that lists every deleted path **and** every modified path, `emit-manifest.json`
   included. Verify with the full block; amend if red. Run `git status --porcelain` after
   committing: it must be empty. The **three** `adoption_scan` tests named in `<measured_anchors>`
   are red between the `git rm` and the `git commit` and green after — D-15's documented
   intra-commit red, three of them, not one.
10. Confirm no replacement was introduced anywhere: no new hook module, no new CI job, no
   `.pre-commit-config.yaml` entry. D-06 is explicit that `secret_scan` goes with nothing in its place.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests -q && uv run python -m tools.harness_emit && git diff --exit-code && uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline && python3 tools/harness_lint/workspace_check.py</automated>
    <automated>test -z "$(git status --porcelain)" && test ! -e tools/hooks/secret_scan.py && test ! -e harness/plugins/secret-scan.ts && python3 -c "import json,sys; h=json.load(open('.claude/settings.json'))['hooks']; s=json.dumps(h); sys.exit(0 if len(h['PreToolUse'])==6 and len(h['PostToolUse'])==4 and 'secret_scan' not in s else 1)"</automated>
    <automated>uv run pytest tools/adoption_scan/tests -q</automated>
    <automated>! grep -n "secret-scan" README.md</automated>
  </verify>
  <done>`secret_scan` does not exist in any form; `RETIRED_SIGNATURES` carries two permanent tombstones and a test that reds if either is removed; the emitted settings.json has 6 PreToolUse / 4 PostToolUse slots; `emit-manifest.json` is pruned and committed; `adoption_scan`'s redaction tests pass with its pattern data untouched; no replacement control exists; `git status --porcelain` is empty and the commit ends green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitter → every checkout's `.claude/settings.json` | A merge that mis-classifies a retired group leaves other repos invoking a deleted module |
| agent → repo working tree | PreToolUse guards decide every Write/Edit/Bash; a guard that cannot run denies the tool |
| emitted artifact set → `emit-manifest.json` | The manifest is a committed derived record; omitting it from the pathspec leaves the commit dirty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Denial of Service | A stale checkout re-emits, keeps the orphaned `secret_scan` group verbatim, and runs a deleted module — PreToolUse then denies every write, with the repair locked behind the outage | mitigate | Append `tools.hooks.secret_scan` to `RETIRED_SIGNATURES` (never clear) **plus** the membership pin; the append alone is provably untested (mutation-proved, reds at `test_settings_merge.py:171`) |
| T-44-05 | Information Disclosure | Secret detection at the tool boundary ceases to exist | accept | ROADMAP accepted consequence; ADR-0012 records it as a permanent residual caught at CI/PR review. No replacement is permitted (D-06). |
| T-44-06 | Information Disclosure | Collateral damage to `tools/adoption_scan`'s own redaction while deleting the hook | mitigate | D-08: touch only prose in `scan.py`; assert all `tools/adoption_scan/tests` pass unchanged |
| T-44-21 | Repudiation | `emit-manifest.json` left out of the pathspec leaves the commit dirty and its own `git diff --exit-code` red — a D-15 violation from a pathspec omission | mitigate | Manifest in `files_modified` and in Task 2's explicit pathspec; `git status --porcelain` asserted empty |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | This plan performs no package installs and adds no dependency (SC-8). |
</threat_model>

<verification>
Both commits satisfy the full verification block and leave `git status --porcelain` empty.
Additionally, after Task 2 the emitted `.claude/settings.json` must contain no occurrence of the
string `secret_scan`, and
`git grep -rn "secret_scan\|secret-scan" -- tools harness libs examples .github` must return only
`tools/adoption_scan`'s retained-by-D-08 references (if any survive the prose sweep) and nothing that
names a live hook.
</verification>

<success_criteria>
- `tools/hooks/secret_scan.py` and `harness/plugins/secret-scan.ts` do not exist and no surviving
  file invokes them (SC-1), and no surviving file — `README.md` included — advertises the hook as a
  shipped control.
- A checkout still carrying the pre-44 `.claude/settings.json` drops the `secret_scan` group on
  re-emit, asserted by the extended drop test rather than by reading the merge (SC-2).
- Deleting either tombstone from `RETIRED_SIGNATURES` turns the suite red (mutation-demonstrated).
- Both commits end green with an empty `git status --porcelain`; ruff ratchet, `emit-drift`,
  `contract-drift` clean (SC-7).
</success_criteria>

<output>
Create `.planning/phases/44-non-goal-surface-removal/44-02-SUMMARY.md` when done. Record both
mutation results verbatim, the `emit-manifest.json` row-delta, and the per-commit
`git diff --shortstat`.
</output>
