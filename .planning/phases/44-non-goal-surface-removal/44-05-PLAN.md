---
phase: 44-non-goal-surface-removal
plan: 05
type: execute
wave: 5
depends_on: ["44-04"]
files_modified:
  - tools/hooks/commit_gate.py
  - tools/hooks/tests/test_commit_gate.py
  - tools/golden_runner/
  - examples/log-parser/golden_runner/
  - examples/log-parser/tests/conftest.py
  - examples/log-parser/tests/test_value_regression.py
  - examples/log-parser/tests/test_repr_only.py
  - examples/log-parser/tests/test_compare_recorded.py
  - examples/log-parser/golden/sample/
  - golden/
  - pyproject.toml
  - uv.lock
  - .github/workflows/ci.yml
  - harness/commands/verify-work.md
  - harness/skills/python-conventions/SKILL.md
  - tools/adoption_scan/tests/test_install_completeness.py
  - tools/harness_lint/tests/test_tests_are_isolatable.py
  - tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr
  - AGENTS.md
  - .opencode/
  - .claude/
  - opencode.json
autonomous: true
requirements: [CER-09]

must_haves:
  truths:
    - "Locked decisions honored by this plan: D-01 (examples/** is not a uv workspace member, so the move removes golden_runner from the workspace), D-02 (the four example importers already exist and import the core path today — CER-09 finishes a half-done migration), D-03 (relocate to examples/log-parser/golden_runner/, NOT examples/log-parser/tools/golden_runner/, so there is only one root for the tools.* namespace), D-04 (add the relocated package to [tool.uv.workspace] members as an explicit path entry — a DATA row in an existing key, not a mechanism, and the directory must carry its pyproject.toml in the same commit), D-05 (root golden/ folds into examples/log-parser/golden/, preserving history with git mv where the path allows), D-14 (every live-tree-rendering test repaired in the same commit as the change that invalidates it), D-15 (delete/move then add then commit then verify; no commit ENDS red), D-16 (git commit -m message BEFORE --), D-17 (source-first emit; never hand-edit the emitted trees), D-18 (run things, don't read them)."
    - "The golden stack resolves under examples/log-parser/ and both example suites pass under uv run — 14 + 17 = 31 passed"
    - "The core contains no golden/parity .NET resolution and no import of golden_runner"
    - "Both the golden CI job and the workspace CI job's test_workspace_golden.py path are repointed, YAML-resolved, and gate.needs is still exactly 10"
    - "runner.py's REPO_ROOT anchor is corrected so the T-06-02 path-confinement allowlist still roots at the repository root, AND GOLDEN_DIR is re-anchored at the instance so it does not resolve to the directory this same commit removes"
    - "tools/hooks/commit_gate.py keeps its drift and polyglot components and its GOLDEN_APPROVE_HUMAN drift-ratification path, and loses only the golden-parity component"
    - "No surviving line of commit_gate.py or its test module describes a golden component, and no surviving line makes a claim about a control this commit deletes — including the two comment lines (:52, :81) that a naive edit leaves behind, one of which becomes factually false"
    - "harness/commands/verify-work.md no longer describes itself as running five gates or the golden cases — the frontmatter description, the step-count sentence and the Notes block are corrected with the section that is deleted"
    - "test_tests_are_isolatable.py's docstring no longer cites a scoped CI command that this commit repoints"
    - "The ruff ratchet exits 0 with no rule class above baseline — no orphaned import from the commit_gate surgery, no import-order regression from the module-path rewrite. The absolute total is BELOW 84 by this point in the phase (measured 77) and that is a PASS, not a failure to chase"
  artifacts:
    - path: "examples/log-parser/golden_runner/pyproject.toml"
      provides: "The relocated workspace member, byte-identical to its pre-move form except stale comments"
      contains: "logparser-golden-runner"
    - path: "pyproject.toml"
      provides: "[tool.uv.workspace] members with an explicit examples/log-parser/golden_runner entry"
      contains: "examples/log-parser/golden_runner"
    - path: "tools/hooks/commit_gate.py"
      provides: "Two composed components (drift, polyglot); no golden import, no check_golden, no discover_golden_cases, no orphaned tempfile import"
  key_links:
    - from: "examples/log-parser/tests/conftest.py"
      to: "golden_runner.runner"
      via: "sys.path insert of the instance root plus the rewritten import"
      pattern: "from golden_runner"
    - from: ".github/workflows/ci.yml golden + workspace jobs"
      to: "examples/log-parser/golden_runner"
      via: "pytest path arguments, asserted by plan 04's resolution test"
      pattern: "examples/log-parser/golden_runner"
    - from: "examples/log-parser/golden_runner/runner.py REPO_ROOT"
      to: "the T-06-02 path-confinement allowlist"
      via: "parents[3] after the move"
      pattern: "parents\\[3\\]"
    - from: "examples/log-parser/golden_runner/runner.py GOLDEN_DIR"
      to: "examples/log-parser/golden/"
      via: "an instance-anchored parents[1], decoupled from REPO_ROOT"
      pattern: "parents\\[1\\]"
---

<objective>
Delete `commit_gate`'s golden-parity component, then relocate the golden stack to
`examples/log-parser/` in one atomic commit.

Purpose: this is the phase's long pole. The move, the workspace member edit, the lock refresh, four
path anchors, eleven import rewrites and a CI repoint all have to land together, and two of the
anchors fail **silently** rather than loudly if they are missed.
Output: two commits, each ending green under the full verification block **including the `examples/`
leg**.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-non-goal-surface-removal/44-CONTEXT.md
@.planning/phases/44-non-goal-surface-removal/44-RESEARCH.md
@.planning/phases/44-non-goal-surface-removal/44-04-SUMMARY.md
@docs/adr/0002-general-template-de-specialization.md
</context>

<measured_anchors>
### `commit_gate`'s golden component — the recorded scope extension

`tools/hooks/commit_gate.py:42` imports `GOLDEN_DIR`, `resolve_dotnet` and `run_golden_case` from
`tools.golden_runner.runner` **at module level**. After the move the core suite dies at collection —
the only collection error the entire phase produces. Repointing the import at the instance tree is
**hard-failed** by GEN-04: the researcher edited `commit_gate.py:42` to `sys.path`-insert the
instance root and
`tools/harness_lint/tests/test_core_no_example_dep.py::test_core_has_no_example_dependency`
failed on the forbidden path token (`_PATH_TOKENS` at `:47` over `_CORE_ROOTS` at `:53`). The only
sanctioned instance-pointer file is `harness/project.toml`. So the golden-parity component must be
**deleted**, which the ROADMAP records as an accepted scope extension grounded in ADR-0012.

Surface, by `file:line`:

| Item | Location |
|---|---|
| Import | `commit_gate.py:42` |
| `discover_golden_cases()` | `commit_gate.py:199-203` |
| `check_golden()` | `commit_gate.py:206-228` |
| Composition list (HOOK-03's three components) | `commit_gate.py:240` |
| Module docstring, component 3 | `commit_gate.py:14-17` |
| Docstring cross-refs ("polyglot and golden stay HARD") | `commit_gate.py:9,10,157` |
| 🔴 `APPROVAL_ENV` comment, scope clause | `commit_gate.py:52` |
| 🔴 `staged_files()` docstring, degradation claim | `commit_gate.py:81` |
| Test module docstring | `tools/hooks/tests/test_commit_gate.py:8,13` |
| Named golden tests | `test_dotnet_absent_skips_golden_not_fail` (`:117`), `test_golden_skip_does_not_suppress_drift` (`:130`) |

🔴 **The last two rows were MISSING from this table and are why the replay left this commit red.**
Both lines carry lowercase `golden`, both survive a plan-faithful edit, and this task's own second
verify assertion forbade any surviving lowercase `golden` — measured exit 1 on an otherwise correct
commit. Their exact current text and the replacement wording to use:

- `:52` — `# weakens polyglot (§4.3-4.6) or golden (equivalence). Empty/blank does NOT authorize.`
  (the tail of the `APPROVAL_ENV` comment at `:48-52`, which explains that the ratification token is
  drift-scoped). Replace the clause with one that names only surviving components, e.g.
  *"…it turns a contract-drift FAIL into a WARN+PASS and never weakens the polyglot §4.3-4.6
  component. Empty/blank does NOT authorize."*
- `:81` — `(e.g. not a repo) yields ``[]`` so the gate degrades to drift+golden rather than crashing.`
  This one is worse than a token collision: once the golden component is deleted the sentence is
  **factually false** — a stale claim about a control that no longer exists, which is precisely the
  defect class this milestone was convened to remove. Replace with the true statement, e.g.
  *"…yields ``[]`` so the gate degrades to the drift component alone rather than crashing."*

Step 3's mandated replacement prose will itself add further lowercase-`golden` lines (any honest
sentence about *what was removed* contains the word). That is why the verify assertion below is
scoped to the golden-parity **identifiers** rather than to the substring `golden`: an absence gate
that forbids a word no honest edit can avoid is unsatisfiable, and an executor who meets it has
written prose that hides what changed.

⚠ **All 15 `test_commit_gate.py` tests are coupled**, not the two named — they monkeypatch
module-level names on `commit_gate`, and a crude removal reddened all 15 (✅ re-confirmed in replay).
Budget the whole file.
⚠ **Preserve the `GOLDEN_APPROVE_HUMAN` drift-ratification path (`commit_gate.py:155-171`) and its
4 tests.** Despite the env-var name it is `contract_guard`'s precedent and is not golden-specific.
The env var is shared; the semantics are not.
🔴 **`check_golden()` is the only user of `tempfile` in `commit_gate.py`.** Removing it orphans the
`import tempfile` at `:31` — measured `F401` 0 → 1, which hard-fails the zero-slack ruff ratchet and
ends the commit red. Delete the import in the same commit.

### The relocation

- Root `pyproject.toml:34` is `members = ["libs/python", "tools/*"]` with
  `exclude = ["tools/bootstrap"]` — root-scoped globs only, so `examples/**` is not a member.
- `uv.lock:14,252-254` pins `logparser-golden-runner` at `source = { virtual = "tools/golden_runner" }`.
- ⚠ **The "uv is dead repo-wide after the `git mv`" hazard does NOT reproduce — the real failure mode
  is the opposite ordering.** Measured from a clean tree: after a bare
  `git mv tools/golden_runner examples/log-parser/golden_runner`, `uv lock` exits **0**
  ("Removed logparser-golden-runner v0.0.0"), `uv sync --all-packages` exits **0**, `commit_gate`
  runs, and `workspace_check.py` exits 0. uv self-heals by dropping the member, because the
  relocated directory matches no `members` glob. The hard-fail is reachable in the **other**
  direction: adding the explicit `members` entry **before** the directory exists at that path leaves
  uv resolving a declared member that is not there. **Therefore: keep the chained single-invocation
  ordering below** (move → members edit → lock → sync). It runs clean end-to-end (measured: exit 0,
  `uv.lock | 2 +-`) and it is the ordering that cannot enter the bad state. Do not reorder it to
  "edit members first"; do not split it across tool calls for no reason.
- `tools/golden_runner/pyproject.toml` needs **no change** — `uv sync --all-packages` succeeded with
  it byte-identical after the move. Only its `:10-11` comments naming
  `python -m tools.golden_runner.runner` are stale prose. Because it declares
  `[tool.uv] package = false`, workspace membership confers **no import resolution** — nothing is
  installed. Membership buys dependency resolution and `uv sync` participation, nothing more; imports
  come from `sys.path`.
- 🔴 **FOUR path anchors, not three. Two of the four fail silently.**

| File:line | Now | After | Why |
|---|---|---|---|
| `golden_runner/runner.py:29` (`REPO_ROOT`) | `parents[2]` | `parents[3]` | 🔴 **Security, silent.** `REPO_ROOT` feeds `_LIBS_PYTHON` (sys.path), the **T-06-02 path-confinement allowlist** (`runner.py:98`) and `run_workspace_golden_case`'s `member_root` resolution (`:283`, which resolves `workspace.toml` member paths relative to the repo root). Left at `parents[2]` it silently becomes the instance dir — a narrowed confinement root and broken member resolution, with **no import error**. |
| `golden_runner/runner.py:36` (`GOLDEN_DIR`) | `REPO_ROOT / "golden"` | `Path(__file__).resolve().parents[1] / "golden"` | 🔴 **Correctness, silent, and NOT fixed by the `REPO_ROOT` correction — it is made worse by it.** Once `REPO_ROOT` is the repo root, `GOLDEN_DIR` resolves to root `golden/`, the directory **this same commit deletes** under the D-05 fold. Measured with caches cleared: `parents[3]` alone → `1 failed, 16 passed` (`FileNotFoundError: <repo>/golden/sample/input/seed.tsv`); leaving `parents[2]` → `2 failed, 15 passed`. The researcher's "17 passed" was measured with the move but WITHOUT the `golden/` fold; this commit does both atomically. Re-anchor `GOLDEN_DIR` at the instance root, decoupled from `REPO_ROOT`. Verified: after this edit, `uv run pytest examples/log-parser/tests examples/log-parser/golden_runner -q` → **31 passed** (14 + 17). |
| `golden_runner/tests/conftest.py:27` | `parents[3]` | `parents[4]` | `repo_root` fixture |
| `golden_runner/tests/test_workspace_golden.py:33` | `parents[3]` | `parents[4]` | `_REPO_ROOT` |

- **Import rewrite `tools.golden_runner` → `golden_runner` in 11 files** (script-verified):
  `examples/log-parser/tests/{conftest,test_value_regression,test_repr_only,test_compare_recorded}.py`,
  `examples/log-parser/golden_runner/{runner,approve}.py`, and
  `examples/log-parser/golden_runner/tests/{conftest,test_approve_gate,test_sample_loop,test_identity_converter,test_workspace_golden}.py`.
- 🔴 **The mandated rewrite REGRESSES the ruff ratchet: `I001` 0 → 3, measured.** Shortening
  `tools.golden_runner…` to `golden_runner…` changes where the name sorts (and how ruff classifies
  the block), reordering the import blocks in
  `examples/log-parser/golden_runner/tests/{test_approve_gate,test_sample_loop,test_workspace_golden}.py`
  (three files — the earlier plan text named `test_identity_converter` as the third; the replay
  measured `test_workspace_golden`. The count of 3 is right either way and the tree-wide
  `--fix` in step 5 covers whichever three ruff reports, so do not hand-pick files).
  The ratchet hard-fails on a rule class ABOVE baseline ("baseline may only SHRINK") with zero slack owed to this
  phase. Fix it inside the same commit — see Task 2 step 5.
- **`sys.path` gains the instance root.** Both conftests currently insert `_REPO_ROOT` and
  `_LIBS_PYTHON`; they need the instance root as a third entry. Same `sys.path.insert` idiom already
  in use — not a new mechanism.
- **`golden/` fold:** no filename collision — root `golden/` holds `README.md` and `sample/`, while
  `examples/log-parser/golden/` holds `repr-only/` and `value-regression/`.
  `git mv golden/sample examples/log-parser/golden/sample` is clean; `git rm golden/README.md`
  (it describes the top-level-`golden/` convention that ceases to exist).
- **CI, ruamel-resolved (do NOT grep or sed — `gate.needs` is a flow sequence that sed mangles):**

| `ci.yml` | From | To |
|---|---|---|
| `:168` | `uv run pytest tools/golden_runner` | `uv run pytest examples/log-parser/golden_runner` |
| `:320` | `… tools/golden_runner/tests/test_workspace_golden.py …` | `… examples/log-parser/golden_runner/tests/test_workspace_golden.py …` |

  Plus the comment lines at `:155-156` and the step name at `:167` ("Golden — root identity
  (converter-agnostic)"), which names a step that no longer exists — per D-05 the two golden steps
  collapse into the example's one. The job and its `setup-dotnet` step stay.
  ✅ **`gate.needs` ruamel-resolves to 10 and is unchanged** because the job is repointed, not
  deleted — assert the count explicitly, since `test_every_job_is_in_the_fan_in`
  (`test_ci_lint_gate.py:72-80`) is `declared <= fan_in`, subset-only, and would not catch a stale
  entry (Phase 43 IN-03).
- **Module-discovery floor, measured in four states:** real tree 12; after CER-08 deletions 12 (zero
  slack); after CER-09 with the two `tools.golden_runner` refs left in place 12 but
  `test_every_referenced_tools_module_lands_in_applied_target` FAILS with
  `tools.golden_runner.runner does not resolve to any real .py file in this checkout`; after CER-09
  with both refs removed **11**, so the floor `>= 12` fails. ✅ Replay confirmed the trap holds in
  **both** directions with zero slack at exactly 12. **Both refs must go AND the floor must drop
  12 → 11 in the same commit.** The floor's own docstring (`:98-100`) licenses lowering: *"This is a
  vacuity guard, not a census — do not raise it back toward the live value."*
- ⚠ **GEN-04 trap when you write that floor rationale.**
  `tools/harness_lint/tests/test_core_no_example_dep.py::test_core_has_no_example_dependency` scans
  every tracked file under `tools/`, `harness/`, `libs/` for the forbidden path token `examples/`
  (`_PATH_TOKENS` at `:47`) and hard-fails on a hit — in **prose as well as code**. Replay hit this
  twice, writing a natural-sounding rationale that named the tree the runner moved to.
  `test_install_completeness.py` is a core-plane file. Phrase the Phase-44 / CER-09 sentence
  **without** the token, e.g. "CER-09 relocated the golden runner out of the core tree into the
  instance overlay, taking the discovered count to 11." Never write `examples/` or
  `examples/log-parser` in a core-plane file.
- `harness/commands/verify-work.md:44` is a `!`-prefixed golden loop (`cases=(golden/*/) … uv run
  python -m tools.golden_runner.runner "$c"`). After the `golden/` fold the glob matches nothing and
  the step prints `SKIP: no golden cases — no-op (exit 0)`: it would degrade **silently and greenly**
  into a permanently no-op claimed control. **Delete the block.** `verify-work.md:25`'s bare
  `ruff check .` is a pre-existing separate defect (Phase 43 IN-05) — do not fix it here.
- `harness/skills/python-conventions/SKILL.md:35` names `python -m tools.golden_runner.runner` as a
  *style example* of invoking tools by module path. Repoint it at a surviving core module.
- **Q3, CLOSED by execution and re-confirmed in replay (5 passed).** In a scratch clone with
  `tools/golden_runner` moved, `golden/sample` folded, `golden/README.md` removed and the `AGENTS.md`
  HARNESS-MANAGED block reduced to the post-44 command/skill sets,
  `pytest tools/memory_regen/tests/test_agents_md.py -q` → **5 passed**. It does **not** red. Two
  reasons: `:49` is an `any()` over three alternatives and both `uv run pytest` and
  `tools.contract_drift` survive in `AGENTS.md`'s golden-path table; and the monorepo-map member
  check is a **substring test on committed text**, not a filesystem check, satisfied by prose outside
  the managed block. **Therefore Phase 44 does not touch `AGENTS.md` outside the emitter-managed
  markers**, and the `AGENTS.md` golden-path-table reconciliation (`:66-67`) stays Phase 45's
  (CER-11).
- ⚠ **The post-move absence gate must be scoped to what this phase owns.** A blanket
  `! git grep -n "tools.golden_runner" -- . ':(exclude).planning' ':(exclude)docs/adr'` matches
  **17 files** after a correct relocation — `git grep`'s `.` is a regex wildcard, so it also matches
  the path spelling `tools/golden_runner` in prose. The survivors are all legitimate and all
  Phase 45's (CER-11): `AGENTS.md:66-67` (deferred by this plan's own Q3 finding), `README.md:119`,
  `tools/docs_sync/generate.py:14`, `tools/hooks/contract_guard.py:56`,
  `tools/hooks/format_on_write.py:36`, two `conftest.py:3` docstrings, and 5 `pyproject.toml`
  comments. **Gate on the import form instead** — that is exactly the 11-file rewrite this plan owns:

      ! git grep -nE "^[[:space:]]*(from|import)[[:space:]]+tools\.golden_runner" -- . ':(exclude).planning'

  Verified: it matches exactly the 12 import sites today (the 11 rewritten here plus
  `commit_gate.py:42`, removed by Task 1) and nothing else.
- 🔴 Root `pyproject.toml:38` is `testpaths = ["libs/python", "tools"]`; measured
  `uv run pytest --collect-only -q | grep -c "examples/"` → **0**. `uv run pytest -q` collects **zero**
  tests from the instance tree. D-19's per-commit gate is blind to all four golden importers and to
  `test_pipeline_topology.py`. The `examples/` leg is mandatory in every verify block in this plan.
- `git rm` **already stages the deletion**, and `git mv` **already stages both sides of the rename**.
  A subsequent `git add -- <removed-path>` aborts with exit 128
  (`fatal: pathspec '<path>' did not match any files`). Add only the files you **modified** after the
  move.
- This plan adds and removes no emitted artifact (it edits two rendered bodies), so
  `emit-manifest.json` does not change here — verified in replay. Run `git status --porcelain` after
  each commit anyway; if it moved, add it and amend.
- Verification block: as in `44-01-PLAN.md`, with the `examples/` leg extended to
  `uv run pytest examples/log-parser/tests examples/log-parser/golden_runner -q` after the move.
- .NET SDK 10 is **not available** in this environment. `require_dotnet`-gated golden cases SKIP
  cleanly and the 14 + 17 example tests still pass; the spawn path runs only in CI. State this as a
  residual rather than claiming full local proof.
</measured_anchors>

<tasks>

<task type="auto">
  <name>Task 1: Delete commit_gate's golden-parity component</name>
  <files>tools/hooks/commit_gate.py, tools/hooks/tests/test_commit_gate.py</files>
  <action>
Implements the ROADMAP's recorded scope extension. Sequenced **before** the move for a reason: it
removes the last core-side import of `golden_runner`, so the `Bash` PreToolUse guard survives the
directory move on its own merits.

1. Remove the golden component from `tools/hooks/commit_gate.py`: the module-level import at `:42`,
   `discover_golden_cases()` (`:199-203`), `check_golden()` (`:206-228`), and the third element of
   the composition list at `:240`. HOOK-03 then composes two components — drift and polyglot.
2. 🔴 **Delete the now-orphaned `import tempfile` at `:31`.** `check_golden()` is its only user;
   leaving it is a measured `F401` regression — a rule class going ABOVE baseline, which the ratchet
   hard-fails. (The ratchet's absolute total is already below 84 by this commit; that is expected and
   is a PASS. Never `--update`.) After editing, re-scan the file for any other import the
   removal orphaned, then run `uv run python -m tools.ruff_baseline` **before** committing.
3. Update the prose that describes three components: the module docstring's component 3
   (`:14-17`), the cross-references at `:9`, `:10` and `:157` ("polyglot and golden stay HARD"),
   🔴 **the `APPROVAL_ENV` comment tail at `:52`** and 🔴 **the `staged_files()` docstring at `:81`**.
   The last two were missing from an earlier version of the surface table and are the reason the
   replay left this commit red; `:81`'s "degrades to drift+golden" additionally becomes **factually
   false** once the component is gone. Use the replacement wording supplied in `<measured_anchors>`.
   Re-grep the file after editing (`grep -n "golden" tools/hooks/commit_gate.py`) and read every
   surviving hit: each one must be either a `GOLDEN_APPROVE_HUMAN` reference or an honest sentence
   about a surviving behavior. Nothing may describe a component this commit deletes.
   ⚠ GEN-04: `commit_gate.py` is a core-plane file — do not write the token `examples/` in any
   replacement prose. Say "the instance overlay" if you must refer to where the runner went.
4. ⚠ **Do not touch `commit_gate.py:155-171`, the `GOLDEN_APPROVE_HUMAN` drift-ratification path.**
   Despite the env-var name it is `contract_guard`'s precedent and is not golden-specific. Its 4
   tests must stay green and are the proof.
5. Repair `tools/hooks/tests/test_commit_gate.py`. All 15 tests are coupled through module-level
   monkeypatching, so run the file first and let it tell you what broke (D-18) rather than
   pre-judging from the two obviously-named golden tests. Delete
   `test_dotnet_absent_skips_golden_not_fail` and `test_golden_skip_does_not_suppress_drift`
   outright — their subject no longer exists. Rework the remaining tests' fixtures so they exercise
   the two surviving components. Update the module docstring at `:8` and `:13`, and drop any import
   the deletions orphan there too.
6. **Safety note for the executor:** `commit_gate` is the `Bash` PreToolUse guard. If you leave it in
   a state where it cannot import, every subsequent `Bash` call is denied. The `Edit`/`Write` tools
   are guarded by `contract_guard` instead, so you can still repair the file — but verify importability
   as the very first thing after editing:
   `uv run python -c "import tools.hooks.commit_gate"`.
7. Stage and commit with the message before `--` and an explicit pathspec, then verify with the full
   block. This commit does not move anything, so it must be green with `tools/golden_runner` still in
   place. `git status --porcelain` must be empty after the commit.
  </action>
  <verify>
    <automated>uv run python -c "import tools.hooks.commit_gate" && uv run pytest tools/hooks/tests/test_commit_gate.py -q && uv run pytest -q && uv run pytest examples/log-parser/tests -q && uv run python -m tools.ruff_baseline</automated>
    <automated>test -z "$(git status --porcelain)" && ! grep -niE "golden-parity|golden_runner|check_golden|discover_golden_cases|GOLDEN_DIR|run_golden_case" tools/hooks/commit_gate.py && ! grep -qn "^import tempfile" tools/hooks/commit_gate.py</automated>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q</automated>
  </verify>
  <done>`commit_gate` composes drift and polyglot only, imports nothing from `golden_runner`, carries no golden-parity identifier, carries no surviving comment that describes or claims the deleted component (`:52` and `:81` corrected, `:81`'s now-false degradation claim included), carries no orphaned `tempfile` import, retains the `GOLDEN_APPROVE_HUMAN` drift-ratification path with its 4 tests green, and `test_commit_gate.py` is fully repaired; the module imports; the ruff ratchet exits 0 with no rule class above baseline; the commit ends green with `tools/golden_runner` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the golden stack to examples/log-parser/ (atomic)</name>
  <files>tools/golden_runner/, examples/log-parser/golden_runner/, examples/log-parser/tests/conftest.py, examples/log-parser/tests/test_value_regression.py, examples/log-parser/tests/test_repr_only.py, examples/log-parser/tests/test_compare_recorded.py, examples/log-parser/golden/sample/, golden/, pyproject.toml, uv.lock, .github/workflows/ci.yml, harness/commands/verify-work.md, harness/skills/python-conventions/SKILL.md, tools/adoption_scan/tests/test_install_completeness.py, tools/harness_lint/tests/test_tests_are_isolatable.py, tools/harness_emit/tests/__snapshots__/test_emit_determinism.ambr, AGENTS.md, .opencode/, .claude/, opencode.json</files>
  <action>
Implements D-01 through D-05. One commit, and the move itself is one shell invocation.

1. **Step 1 is a SINGLE Bash call, and the ORDER is the point.** Chain the move, the `members` edit
   and the lock refresh with `&&` in one invocation, doing the TOML edit with an inline `python3`
   heredoc rather than the Edit tool:

     `git mv tools/golden_runner examples/log-parser/golden_runner` →
     edit root `pyproject.toml:34` so `members` reads
     `["libs/python", "tools/*", "examples/log-parser/golden_runner"]` →
     `uv lock --upgrade-package logparser-golden-runner` →
     `uv sync --all-packages`

   **Why this order, corrected by measurement:** the previously-feared "uv is dead repo-wide between
   the `git mv` and the lock refresh" hazard does **not** reproduce. After a bare `git mv`, `uv lock`
   exits 0 ("Removed logparser-golden-runner v0.0.0"), `uv sync --all-packages` exits 0, and every
   PreToolUse guard keeps working — uv self-heals by dropping a member that matches no `members`
   glob. The state that genuinely breaks resolution is the **opposite** ordering: declaring the
   explicit `members` entry **before** the directory exists at that path. This chain never enters
   that state, and it was measured end-to-end clean (exit 0, `uv.lock | 2 +-` — exactly one line).
   Keep it as one invocation, keep this order, and do **not** hand-edit `uv.lock`.
2. `git mv golden/sample examples/log-parser/golden/sample` and `git rm golden/README.md`. Root
   `golden/` ceases to exist. `git mv` preserves history where the path allows (D-05).
3. Correct **all four** path anchors in the table above. Two of them fail silently:
   - `runner.py:29` `REPO_ROOT` `parents[2] → parents[3]` — a **security** fix; it roots the T-06-02
     path-confinement allowlist and `workspace.toml` member resolution.
   - `runner.py:36` `GOLDEN_DIR` — change from `REPO_ROOT / "golden"` to
     `Path(__file__).resolve().parents[1] / "golden"`, anchored at the instance root and decoupled
     from `REPO_ROOT`. **This is not optional and it is not implied by the `REPO_ROOT` fix.** With
     `REPO_ROOT` corrected and `GOLDEN_DIR` left alone, `GOLDEN_DIR` points at root `golden/` — the
     directory step 2 just deleted — and the golden_runner suite reports `1 failed, 16 passed`
     (`FileNotFoundError … golden/sample/input/seed.tsv`). Add a one-line comment saying why this
     anchor is deliberately independent of `REPO_ROOT`.
   - `tests/conftest.py:27` and `tests/test_workspace_golden.py:33` — `parents[3] → parents[4]`.
   After editing, `grep -rn 'parents\[' ` the moved tree and re-derive every depth by hand — a wrong
   depth produces no import error, just a wrong root.
4. Rewrite `tools.golden_runner` → `golden_runner` across the 11 files listed above, and add the
   instance root as a third `sys.path` entry in both conftests
   (`examples/log-parser/tests/conftest.py` and
   `examples/log-parser/golden_runner/tests/conftest.py`). Do not invent a new import mechanism —
   this is the `sys.path.insert` idiom both files already use.
   `examples/log-parser/golden_runner/pyproject.toml` needs no functional change; its `:10-11`
   comments and `runner.py:296`'s CLI docstring name the old module path and are now wrong prose —
   update them (they are instance-plane files, so GEN-04 does not constrain them).
5. 🔴 **Fix the import-order regression the rewrite causes, in this same commit.** Shortening the
   module path reorders the import blocks in
   `examples/log-parser/golden_runner/tests/{test_approve_gate,test_sample_loop,test_identity_converter}.py`
   — measured `I001` 0 → 3, a rule class going ABOVE baseline, which the ratchet hard-fails
   ("baseline may only SHRINK"). Run `uv run ruff check --select I001 --fix` over the relocated tree
   and the four instance importers, then re-run `uv run python -m tools.ruff_baseline` **before**
   committing and confirm it exits 0 with no rule class above baseline. Expect a total **below** 84
   (measured 77 at this point in the phase) with `PASS — and findings went DOWN`; that is the
   expected outcome, not a skipped cleanup. If any rule class regressed, fix the code — never the
   baseline, and never `--update`.
6. Remove the whole golden **section** from `harness/commands/verify-work.md` — not just the `!`
   command line — and repoint `harness/skills/python-conventions/SKILL.md:35`'s module-path style
   example at a surviving core module.
   🔴 **The file describes itself in four more places, and deleting only the block at `:44` leaves
   three of them lying.** At HEAD they are: the frontmatter `description` at `:4` ("…contract
   validation + drift, and the golden cases"), the section heading and body at `:35-42`, the
   step-count sentence at `:21` ("Run the five gates in order"), and the Notes bullet at `:58`
   ("All five must be green before handoff"). After this deletion four steps remain, so: drop the
   golden clause from the `description`, renumber the surviving "5. Derived freshness" section to 4,
   and change both "five" occurrences to "four". Re-derive the exact line numbers before editing —
   plan 01 sweeps this file's `gate-model` reference and plan 04 sweeps its `golden-debug` /
   `/golden` skill-name pointers, so the numbering has already shifted twice by the time you get
   here. A `/verify-work` whose frontmatter still advertises golden cases is the same stale-claim
   defect as `commit_gate.py:81`. Both refs must go in **this** commit, together with step 8 — the two are trapped against
   each other in both directions (measured: the floor holds at exactly 12 with zero slack either
   way).
7. `tools/harness_lint/tests/test_tests_are_isolatable.py` — its module docstring cites
   ``pytest tools/golden_runner`` in the list of scoped CI commands that motivate the gate. This
   commit repoints that CI job, so the citation goes stale in the same commit that causes it (D-14).
   Drop that one item from the list; leave the rest of the docstring and every assertion alone.
   ⚠ GEN-04: core-plane file — do not write the replacement path token; just remove the item.
   ℹ **Record, do not repair, the coverage loss this move causes.** `_members_needing_wiring()`
   (`:86`) globs `tools/*/tests`, so the relocated package permanently drops out of the
   isolatability gate. Nothing breaks today — the relocated suite passes standalone (measured:
   17 passed) — and widening the glob would be surface growth against SC-8. Report it in this
   plan's SUMMARY so plan 06 can record it as a consequence rather than leave it unremarked.
8. `tools/adoption_scan/tests/test_install_completeness.py` — drop the module-discovery floor from
   `>= 12` to `>= 11` at `:103`, in the assertion **and** in both interpolated message strings, and
   extend the docstring at `:98-100` with the Phase-44 / CER-09 reason. The docstring already
   sanctions lowering and forbids raising.
   ⚠ **GEN-04 hard-fails on the token `examples/` anywhere under `tools/`, `harness/`, `libs/` —
   prose included, and this rationale is exactly the prose that trips it (hit twice in replay).**
   Write the sentence without the token, e.g. *"Floor lowered 12 → 11 in Phase 44 (CER-09): the
   golden runner was relocated out of the core tree into the instance overlay, taking the discovered
   count to 11."* Do not name the directory.
9. Repoint CI with `ruamel.yaml` (0.19.1, already resolved — do **not** add PyYAML, do **not** sed):
   the two `run:` strings, the two comment lines at `:155-156`, and the step name at `:167`. Then
   assert, by resolving the YAML, that `gate.needs` still has exactly **10** entries and that
   `set(gate.needs) - set(jobs)` is empty. Plan 04's resolution test now proves both new paths exist.
   🔴 **Configure the round-trip indentation before dumping, or the 3-string repoint rewrites the
   whole workflow.** A default `YAML()` round-trip de-indents every block sequence in `ci.yml`;
   measured on this file it produced `324 ++++----, 162 insertions(+), 162 deletions(-)` for a
   three-string change — a whole-file rewrite of a security-relevant workflow, against this phase's
   own "excise minimally" discipline and impossible to review. Set
   `yaml.indent(mapping=2, sequence=4, offset=2)` (and `yaml.preserve_quotes = True`) on the `YAML()`
   instance before `dump`. Measured with those settings: **5 insertions(+), 5 deletions(-)**.
   Assert the diff size before committing — `git diff --shortstat -- .github/workflows/ci.yml` must
   be single-digit on each side. If it is not, you dumped with the defaults; restore the file with an
   explicit single-path checkout and redo the edit.
10. Re-emit (`uv run python -m tools.harness_emit`) and regenerate the emit snapshot
   (`uv run pytest tools/harness_emit/tests/test_emit_determinism.py --snapshot-update`) — step 6
   edited two rendered bodies. Include `AGENTS.md` in the pathspec for its managed block; **do not
   touch `AGENTS.md` outside the HARNESS-MANAGED markers** (Q3 is closed: the golden-path table at
   `:66-67` can stay, and Phase 45 owns it).
11. Stage the **modified** files only — `git mv` already staged both sides of every rename and
    `git rm` already staged the deletion, so `git add -- <old-path>` aborts with exit 128. Then
    commit with the message before `--` and an explicit pathspec listing the moved paths, the deleted
    path and every modified path. Verify with the full block **plus**
    `uv run pytest examples/log-parser/tests examples/log-parser/golden_runner -q`. Expect
    **31 passed** (14 + 17). A bare `uv run pytest -q` proves nothing about this commit. Run
    `git status --porcelain` after committing: it must be empty.
12. Also run `python3 tools/harness_lint/workspace_check.py` and
    `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/harness_lint/tests/test_workspace_member_completeness.py -q`.
    `workspace_check.py` needs no code change for an explicit path entry: `unresolvable_members()`
    (`:113-122`) iterates `repo_root.glob(pattern)`, and a wildcard-free pattern returns the literal
    path when it exists, so an explicit member is checked exactly like a globbed one.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run pytest examples/log-parser/tests examples/log-parser/golden_runner -q && uv run python -m tools.harness_emit && git diff --exit-code && uv run python -m tools.contract_drift.drift && uv run python -m tools.ruff_baseline && python3 tools/harness_lint/workspace_check.py</automated>
    <automated>test -z "$(git status --porcelain)" && test ! -e tools/golden_runner && test ! -e golden && test -f examples/log-parser/golden_runner/pyproject.toml && test -d examples/log-parser/golden/sample && ! git grep -nE "^[[:space:]]*(from|import)[[:space:]]+tools\.golden_runner" -- . ':(exclude).planning'</automated>
    <automated>uv run python -c "
import sys, pathlib
sys.path.insert(0, 'examples/log-parser')
from golden_runner.runner import GOLDEN_DIR, REPO_ROOT
assert GOLDEN_DIR == pathlib.Path('examples/log-parser/golden').resolve(), GOLDEN_DIR
assert REPO_ROOT == pathlib.Path('.').resolve(), REPO_ROOT
"</automated>
    <automated>uv run python -c "
from ruamel.yaml import YAML
d = YAML().load(open('.github/workflows/ci.yml'))
jobs = list(d['jobs']); needs = d['jobs']['gate']['needs']
runs = ' '.join(s.get('run','') for j in d['jobs'].values() for s in j.get('steps',[]))
assert len(needs) == 10, needs
assert not (set(needs) - set(jobs))
assert 'tools/golden_runner' not in runs
assert 'examples/log-parser/golden_runner' in runs
"</automated>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/harness_lint/tests/test_workspace_member_completeness.py tools/adoption_scan/tests/test_install_completeness.py tools/memory_regen/tests/test_agents_md.py tools/harness_lint/tests/test_tests_are_isolatable.py -q</automated>
    <automated>git show --numstat HEAD -- .github/workflows/ci.yml | awk 'NF==3{found=1; if ($1+0 >= 20 || $2+0 >= 20) exit 1} END{exit !found}'</automated>
    <automated>! grep -n "the golden cases\|five gates\|All five must be green" harness/commands/verify-work.md</automated>
  </verify>
  <done>`tools/golden_runner` and root `golden/` no longer exist; the stack resolves under `examples/log-parser/` with **31 tests passing** (14 + 17) under `uv run`; `uv.lock` changed by one line and `uv sync --all-packages` succeeds; all four path anchors are corrected, `GOLDEN_DIR` included; the ruff ratchet exits 0 with no rule class above baseline and no `I001` regression; no `from tools.golden_runner` import survives anywhere; both CI jobs are repointed with `gate.needs` still at 10; the module floor is 11 and every discovered `tools.X` ref resolves; GEN-04 passes; `harness/commands/verify-work.md` describes four gates, not five, and no longer advertises the golden cases in its frontmatter; `ci.yml`'s diff is single-digit on each side; `git status --porcelain` is empty and the commit ends green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core (`tools/`, `harness/`, `libs/`) → instance | One-directional; GEN-04 hard-fails any core file referencing the instance path token, in code **or** prose |
| golden runner → filesystem | `runner.py` spawns converters and reads/writes cases under an allowlist rooted at `REPO_ROOT` |
| agent → repo working tree | Every PreToolUse guard runs through `uv`; a declared-but-absent workspace member disarms them all at once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-13 | Tampering | `golden_runner/runner.py:29` — left at `parents[2]`, `REPO_ROOT` silently becomes the instance dir, narrowing the T-06-02 path-confinement allowlist (`:98`) and breaking `workspace.toml` member resolution (`:283`), with no import error | mitigate | Correct to `parents[3]` in the move commit; re-derive every `parents[` depth in the moved tree by hand; asserted directly in verify |
| T-44-24 | Denial of Service | `golden_runner/runner.py:36` — `GOLDEN_DIR = REPO_ROOT / "golden"` resolves to the directory this same commit deletes under the D-05 fold. Fails at runtime with `FileNotFoundError`, not at import; measured `1 failed, 16 passed` | mitigate | Re-anchor at `Path(__file__).resolve().parents[1] / "golden"`, decoupled from `REPO_ROOT`; asserted directly in verify; the 31-passed count is the end-to-end proof |
| T-44-14 | Denial of Service | A workspace `members` entry declared for a path that does not yet exist leaves uv resolving an absent member; every PreToolUse guard then fails closed and the repair is locked behind the outage. `workspace_check.py` does not detect a stale lock. | mitigate | **Ordering, not atomicity, is the control:** move the directory FIRST, then add the `members` entry, then `uv lock --upgrade-package logparser-golden-runner`, then `uv sync --all-packages` — chained in a single Bash invocation. Measured clean end-to-end. (The inverse hazard — "a bare `git mv` kills uv" — was measured and does **not** reproduce: uv self-heals by dropping the unmatched member.) |
| T-44-25 | Repudiation | The mandated `tools.golden_runner` → `golden_runner` rewrite regresses the ruff ratchet (`I001` 0 → 3) and the `check_golden` removal orphans `tempfile` (`F401` 0 → 1); either ends its commit red on a gate that only shrinks | mitigate | Explicit cleanup steps inside the same commits (Task 1 step 2, Task 2 step 5); `tools.ruff_baseline` run before committing and asserted in verify |
| T-44-26 | Repudiation | GEN-04 hard-fails on the instance path token in **prose**, so writing the mandated floor/component rationale reds a core-plane file | mitigate | Explicit token-free phrasing supplied in Task 1 step 3 and Task 2 step 8; `test_core_no_example_dep.py` asserted in both verify blocks |
| T-44-15 | Elevation of Privilege | The 4 folded golden files move from `deny` to `allow` under `contract_guard`'s repo-root-anchored `CONSTITUTION_GLOBS` (`contract_guard.py:53`) — an agent could edit a `.verified` baseline in-session | accept | Measured and accepted. Merge-time CODEOWNERS holds (`/examples/*/golden/  @hjung3113`); only the in-session gate is lost, and the instance's existing golden tree already has this property. Widening `CONSTITUTION_GLOBS` would be surface growth against SC-8 and needs a human call. Record in the closeout. |
| T-44-16 | Repudiation | `verify-work.md:44`'s golden loop would degrade into a permanently no-op green step after the `golden/` fold — a claimed control that does nothing | mitigate | Delete the block rather than let the glob match nothing |
| T-44-17 | Repudiation | A mis-repointed CI path would land green locally | mitigate | Plan 04's resolution assertion, plus the explicit ruamel check on `gate.needs` count and the `run:` strings |
| T-44-28 | Repudiation | Surviving prose in `commit_gate.py` (`:52`, `:81`) and `verify-work.md` (`:4`, `:21`, `:58`) keeps describing a component this phase deletes — `:81`'s "degrades to drift+golden" becomes an outright false claim about a control that no longer exists | mitigate | Both files' self-description corrected in the same commit as the deletion (Task 1 step 3, Task 2 step 6); asserted by the scoped identifier grep and the `verify-work.md` phrase grep |
| T-44-29 | Tampering | A default `ruamel.yaml` round-trip rewrites all of `.github/workflows/ci.yml` (measured 162+/162−) for a 3-string repoint, making a security-relevant workflow unreviewable at PR | mitigate | `yaml.indent(mapping=2, sequence=4, offset=2)` + `preserve_quotes` mandated in Task 2 step 9; diff size asserted single-digit in verify |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs. `uv lock --upgrade-package` re-resolves an existing workspace member whose `dependencies` list is empty; the measured lock diff is one line and no new distribution enters the graph (SC-8, +0 dependencies). |
</threat_model>

<verification>
Both commits satisfy the full verification block with the `examples/` leg and leave
`git status --porcelain` empty. After Task 2 the import-form gate
(`! git grep -nE "^[[:space:]]*(from|import)[[:space:]]+tools\.golden_runner" -- . ':(exclude).planning'`)
returns nothing, the `GOLDEN_DIR` / `REPO_ROOT` assertion passes, and the ruamel assertion on
`ci.yml` passes.

**Deliberately out of scope:** the remaining ~17 prose/path spellings of `tools/golden_runner`
(`AGENTS.md:66-67`, `README.md:119`, `tools/docs_sync/generate.py:14`,
`tools/hooks/contract_guard.py:56`, `tools/hooks/format_on_write.py:36`, two `conftest.py:3`
docstrings, 5 `pyproject.toml` comments). They are Phase 45's CER-11 and this plan's own Q3 finding
already defers `AGENTS.md`. Do not widen the gate to cover them.

**Residual, to be stated rather than claimed away:** .NET SDK 10 is absent locally, so the
`require_dotnet` golden cases SKIP and the converter spawn path is exercised **only** in CI's
`golden` job. Local verification of Task 2 cannot prove it.
</verification>

<success_criteria>
- The golden stack resolves under `examples/log-parser/` with 31 tests passing; the core contains no
  golden/parity .NET resolution and no `golden_runner` import; both the `golden` job and the
  `workspace` job's `test_workspace_golden.py` path are repointed, YAML-resolved (SC-5).
- `commit_gate` retains drift, polyglot and the `GOLDEN_APPROVE_HUMAN` ratification path, and loses
  only the golden-parity component (ROADMAP recorded scope extension).
- `uv run pytest -q` green at both commits, `examples/` leg green, `emit-drift`, `stale-derived`,
  `contract-drift` clean, ruff ratchet exits 0 with no rule class above baseline, `uv.lock`
  refreshed (SC-7).
- No new gate, tool, contract or dependency (SC-8).
</success_criteria>

<output>
Create `.planning/phases/44-non-goal-surface-removal/44-05-SUMMARY.md` when done. Record the measured
`uv.lock` diff size, the two example suite counts (expect 14 and 17), the post-commit
`tools.ruff_baseline` line, the resolved `gate.needs` count, the `ci.yml`-only shortstat, the
per-commit `git diff --shortstat`, and the isolatability-gate coverage loss noted in Task 2 step 7
so plan 06 can record it as a consequence.
</output>
