---
phase: 05-despecialization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/hooks/commit_gate.py
  - tools/hooks/tests/test_commit_gate.py
autonomous: true
requirements: [GEN-01]

must_haves:
  truths:
    - "When contract-drift is present AND a non-empty GOLDEN_APPROVE_HUMAN token is set, the commit gate WARNs and PASSes the drift component (exit 0) instead of blocking."
    - "When contract-drift is present and the token is absent, the gate still BLOCKS (non-zero) — unchanged behavior."
    - "An empty / whitespace-only token does NOT bypass — drift still blocks (mirrors contract_guard Q1)."
    - "The token bypasses ONLY the drift component: a staged BOM/CRLF TSV still blocks (polyglot stays hard), and golden stays hard."
  artifacts:
    - path: "tools/hooks/commit_gate.py"
      provides: "D-05 approval path: check_drift honors GOLDEN_APPROVE_HUMAN (warn+pass on drift); polyglot/golden untouched"
      contains: "GOLDEN_APPROVE_HUMAN"
    - path: "tools/hooks/tests/test_commit_gate.py"
      provides: "extended 04-05 suite: token→warn/pass, absent→block, empty→block, token≠weaken polyglot"
      contains: "GOLDEN_APPROVE_HUMAN"
  key_links:
    - from: "tools/hooks/commit_gate.py"
      to: "os.environ[GOLDEN_APPROVE_HUMAN]"
      via: "_human_approved() mirrors contract_guard.py:91"
      pattern: "os.environ.get"
    - from: "tools/hooks/commit_gate.py::check_drift"
      to: "GateResult PASS-with-WARN"
      via: "drift present + approved -> PASS (blocked==False)"
      pattern: "check_drift"
---

## Phase Goal

**As a** developer or coding agent turning this log-parser harness into a reusable template, **I want to** relocate the semiconductor domain seed into `examples/log-parser/`, slot the language/toolchain assumption into config, and prove the core depends on no example, **so that** the repo can be cloned as a domain-neutral template while every contract/§4.3-4.6/gate invariant stays enforced.

> NOTE (planner): ROADMAP Phase 5 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived from the phase goal; ratify via `/gsd mvp-phase 5` if a canonical story is required.

<objective>
Add the **D-05 commit-gate approval path**: the `contract-drift` component of the commit gate honors a human-set `GOLDEN_APPROVE_HUMAN` token exactly as `contract_guard` already does — a non-empty token turns a drift FAIL into a logged WARN+PASS ("machines gate, humans ratify"), while an absent/empty token still blocks. This is the **first, standalone, core-only commit** of Phase 5: it edits only `tools/hooks/` (not the constitution plane), introduces no contract change, and therefore lands clean with NO token. Once merged, the intended domain-move commits (05-02, 05-03, 05-05) have a sanctioned landing path.

Purpose: close the template gap where `contract_guard` has an approval bypass but `commit_gate` does not, so an intended human-ratified contract change (the whole point of GEN-01) can be committed through the live gate rather than by bash-bypassing it. The bypass is scoped to **drift only** — polyglot (§4.3-4.6 wire hygiene) and golden (real equivalence regression) stay hard, per D-05 and D-01b Q2.
Output: an extended `check_drift` + the 04-05 test suite grown with the four approval cases.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-despecialization/05-CONTEXT.md
@.planning/phases/05-despecialization/05-RESEARCH.md
@CLAUDE.md
@tools/hooks/commit_gate.py
@tools/hooks/contract_guard.py
@tools/hooks/tests/test_commit_gate.py
@tools/contract_drift/drift.py

<interfaces>
<!-- The token precedent to mirror EXACTLY (do NOT invent a new flag) -->
From tools/hooks/contract_guard.py:
  APPROVAL_ENV = "GOLDEN_APPROVE_HUMAN"
  approved = bool((os.environ.get(APPROVAL_ENV) or "").strip())   # line 91 — empty/blank does NOT bypass

From tools/hooks/commit_gate.py (the surface to change — check_drift ONLY):
  def check_drift() -> GateResult          # run_gate(); FAIL on drift -> becomes WARN+PASS when approved
  def check_polyglot(files) -> GateResult  # UNTOUCHED (stays hard)
  def check_golden(cases=None) -> GateResult  # UNTOUCHED (stays hard)
  GateResult.blocked == (status == "FAIL")  # returning PASS => non-blocking, still logged

From tools/contract_drift/drift.py:
  run_gate(...) -> {"ok": bool, "drifted": [(rel, kind, cls), ...]}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the 04-05 commit-gate suite with the four D-05 approval cases (RED)</name>
  <files>tools/hooks/tests/test_commit_gate.py</files>
  <read_first>
    - tools/hooks/tests/test_commit_gate.py — the existing idiom (monkeypatch run_gate / resolve_dotnet / staged_files / lint_file on the module; monkeypatch.setenv/delenv for env).
    - tools/hooks/contract_guard.py:46,91 — the exact token check to mirror; the existing test_contract_guard empty-token case is the precedent for the empty-token assertion.
    - 05-RESEARCH.md §Q2 "Test extension (04-05)" — the four named cases and their expected outcomes.
    - tools/hooks/commit_gate.py — check_drift is the ONLY function changing; check_polyglot/check_golden stay hard.
  </read_first>
  <behavior>
    - test_drift_present_with_approval_warns_not_blocks: force drift (monkeypatch run_gate -> {"ok":False,"drifted":[("contracts/x.schema.json","schema","breaking")]}); monkeypatch.setenv("GOLDEN_APPROVE_HUMAN","yes"); assert commit_gate.main([]) == 0 AND the drift line reads WARN/"ratified"/"GOLDEN_APPROVE_HUMAN" (not FAIL).
    - test_drift_present_without_approval_still_blocks: same forced drift; monkeypatch.delenv("GOLDEN_APPROVE_HUMAN", raising=False); assert main([]) != 0 (guards the pre-existing block behavior).
    - test_empty_token_does_not_bypass_drift: same forced drift; setenv to "" then to "   "; assert main([]) != 0 for both (empty/blank never authorizes — mirrors contract_guard Q1).
    - test_approval_does_not_bypass_polyglot: setenv token AND stage a BOM/CRLF *.tsv (reuse the existing polyglot-violation setup: monkeypatch staged_files -> [that file], run_gate -> ok:True); assert main([]) != 0 — proving the token weakens drift ONLY, never polyglot (D-05 / T-05-01).
  </behavior>
  <action>Append the four cases above to test_commit_gate.py using the file's established monkeypatch idiom (patch names on the commit_gate module; use monkeypatch.setenv/delenv for the token; reuse the existing BOM/CRLF staged-file helper for the polyglot case). Do NOT modify the existing tests. Commit: `test(05-01): commit-gate drift approval-path cases (token warn/pass, absent/empty block, polyglot stays hard)`.</action>
  <verify>
    <automated>uv run pytest tools/hooks/tests/test_commit_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/hooks/tests/test_commit_gate.py -x -q` shows the new approval cases FAILING (check_drift does not yet honor the token) while the pre-existing cases still PASS.</acceptance_criteria>
  <done>Four new RED cases encode: token→warn/pass, absent→block, empty→block, token≠weaken-polyglot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the drift-only approval bypass to check_drift (GREEN)</name>
  <files>tools/hooks/commit_gate.py</files>
  <read_first>
    - tools/hooks/contract_guard.py:46,91 — copy the constant name and the exact `bool((os.environ.get(APPROVAL_ENV) or "").strip())` check; no new semantics.
    - tools/hooks/commit_gate.py::check_drift (137-143) — the only function to edit; the drifted-list formatting to reuse in the WARN message.
    - 05-RESEARCH.md §Q2 "D-05 fix — exact change surface" — the reference implementation (mirror it; do not extend the bypass to check_polyglot/check_golden).
  </read_first>
  <action>In `tools/hooks/commit_gate.py`: add `APPROVAL_ENV = "GOLDEN_APPROVE_HUMAN"` and `def _human_approved() -> bool: return bool((os.environ.get(APPROVAL_ENV) or "").strip())` (verbatim mirror of contract_guard.py:91 — `os` is already imported). In `check_drift`, when `run_gate()` reports drift AND `_human_approved()` is true, return `GateResult("contract-drift","PASS", f"drift present but GOLDEN_APPROVE_HUMAN set — WARN (ratified): {listed}")` instead of FAIL; when not approved, return the existing FAIL unchanged. Do NOT touch `check_polyglot`, `check_golden`, `run_composition`, `staged_files`, or the `--from-hook` path. Update the module docstring's drift bullet to note the drift component now honors the human ratification token (polyglot/golden stay hard). Commit: `feat(05-01): commit-gate drift honors GOLDEN_APPROVE_HUMAN (warn+pass); polyglot/golden stay hard`.</action>
  <verify>
    <automated>uv run pytest tools/hooks/tests/test_commit_gate.py tools/hooks/tests/test_contract_guard.py -q</automated>
  </verify>
  <acceptance_criteria>All commit-gate tests (old + four new) PASS; contract_guard tests still PASS. `uv run python -m tools.hooks.commit_gate; echo $?` on a clean tree still exits 0 and logs a golden-parity SKIP. Grep confirms `GOLDEN_APPROVE_HUMAN` appears in `commit_gate.py` only inside `_human_approved`/`check_drift` (not in `check_polyglot`/`check_golden`).</acceptance_criteria>
  <done>check_drift warns+passes on approved drift, still blocks unapproved/empty-token drift, and the token provably does not weaken polyglot; full commit-gate + contract-guard suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human process env -> commit_gate subprocess | `GOLDEN_APPROVE_HUMAN` may originate only from a human-set process env (agents are instructed never to fabricate it); the PreToolUse hook subprocess inherits it. |
| staged tree -> commit (VCS) | Last chance to block an unratified §4.3-4.6 / golden violation; the approval path must NOT open a hole for those. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | approval token weakens polyglot/golden | mitigate | Bypass confined to `check_drift`; `check_polyglot`/`check_golden` untouched; `test_approval_does_not_bypass_polyglot` proves a staged BOM/CRLF TSV still blocks even with the token set. |
| T-05-02 | Elevation of Privilege | agent self-approves a contract change (fabricated token) | mitigate | Token check is the verbatim contract_guard mirror; empty/blank does not authorize (`test_empty_token_does_not_bypass_drift`); token comes only from human-set env. |
| T-05-03 | Repudiation | drift silently bypassed with no record | mitigate | Bypass emits a WARN/"ratified" line (still logged via `run_composition`); the paired ADR (05-05) records the intended change in the append-only log. |
| T-05-SC | Tampering | dependency supply chain | accept | Zero new packages — edits one in-repo module + its test; `uv.lock` unchanged. |
</threat_model>

<verification>
- `uv run pytest tools/hooks/tests/test_commit_gate.py -x -q` green (old + four new cases).
- `uv run pytest` full suite green (no regression).
- Manual: `GOLDEN_APPROVE_HUMAN=yes` with a forced drift → gate exits 0 with a WARN line; unset → same drift exits non-zero.
- This commit itself lands clean with NO token (touches only `tools/hooks/`, no contract change).
</verification>

<success_criteria>
The commit-gate drift component honors the `GOLDEN_APPROVE_HUMAN` human-ratification token (warn+pass) while leaving polyglot and golden hard — the sanctioned landing path (D-05) for the Phase 5 domain-move commits, extending the 04-05 test suite.
</success_criteria>

<output>
Create `.planning/phases/05-despecialization/05-01-SUMMARY.md` when done.
</output>
