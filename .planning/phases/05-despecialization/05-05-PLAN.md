---
phase: 05-despecialization
plan: 05
type: execute
wave: 4
depends_on: [05-03, 05-04]
files_modified:
  - tools/harness_lint/tests/test_core_no_example_dep.py
  - docs/adr/0002-general-template-de-specialization.md
  - docs/adr/README.md
  - CLAUDE.md
  - AGENTS.md
  - examples/log-parser/AGENTS.md
  - examples/log-parser/README.md
  - docs/explanation/template-and-instances.md
autonomous: true
requirements: [GEN-04]

must_haves:
  truths:
    - "A guard test proves core->example single-direction dependency: nothing under tools/, harness/, libs/ imports or path-references examples/**."
    - "The full non-example pytest suite stays green after extraction."
    - "Root docs (CLAUDE.md, root AGENTS.md, docs/) describe the template + how to add an instance; log-parser specifics live in the example's own AGENTS.md/README."
    - "A new MADR ADR (0002) records the de-specialization (generic re-scope + domain demotion + language slot + gate approval path), complements (not supersedes) ADR-0001, and lands via the human approval path."
  artifacts:
    - path: "tools/harness_lint/tests/test_core_no_example_dep.py"
      provides: "GEN-04 core->example no-dependency guard (grep/scan of tools/ harness/ libs/)"
      contains: "examples"
    - path: "docs/adr/0002-general-template-de-specialization.md"
      provides: "MADR ADR: de-specialization decision, Status accepted, complements 0001"
      contains: "accepted"
    - path: "examples/log-parser/README.md"
      provides: "log-parser instance doc (specifics moved out of root docs)"
      contains: "log-parser"
  key_links:
    - from: "tools/harness_lint/tests/test_core_no_example_dep.py"
      to: "tools/ harness/ libs/ tracked files"
      via: "scan for examples/ path refs + examples imports; exempt harness/project.toml [instance] root"
      pattern: "examples/"
    - from: "docs/adr/0002-general-template-de-specialization.md"
      to: "docs/adr/README.md index"
      via: "append row (append-only MADR)"
      pattern: "0002"
    - from: "CLAUDE.md / AGENTS.md"
      to: "examples/log-parser/{AGENTS.md,README.md}"
      via: "root docs point to the instance for domain specifics"
      pattern: "examples/log-parser"
---

## Phase Goal

**As a** developer or coding agent turning this log-parser harness into a reusable template, **I want to** relocate the semiconductor domain seed into `examples/log-parser/`, slot the language/toolchain assumption into config, and prove the core depends on no example, **so that** the repo can be cloned as a domain-neutral template while every contract/§4.3-4.6/gate invariant stays enforced.

> NOTE (planner): ROADMAP Phase 5 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived; ratify via `/gsd mvp-phase 5` if a canonical story is required.

<objective>
Prove and record the de-specialization (GEN-04 + D-06): (1) a **core→example single-direction guard test** that fails if anything under `tools/`, `harness/`, or `libs/` imports or path-references `examples/**`; (2) recast the root docs (`CLAUDE.md`, root `AGENTS.md`, `docs/`) to describe the TEMPLATE + how to add an instance, moving log-parser specifics into the example's own `AGENTS.md`/`README.md`; (3) write the new MADR **ADR-0002** recording the de-specialization, complementing (not superseding) ADR-0001.

Purpose: lock the invariant that makes this a reusable template — the core cannot depend on any instance — and leave an auditable record of why the repo was re-scoped. This is the final Phase 5 plan; it runs after the move (05-03) so the guard test asserts the real post-move state and the docs describe reality.
Output: the guard test, recast root docs, the example instance docs, and ADR-0002 (+ its index row).

**Precondition (token):** the ADR write (`docs/adr/0002-*.md`) and the `docs/adr/README.md` index edit are constitution-plane Writes (`docs/adr/**`) → `contract_guard` DENIES them unless `GOLDEN_APPROVE_HUMAN` is set in the Claude Code process env (05-RESEARCH Q3: export before session or gitignored `.claude/settings.local.json` `env`, removed after — never commit the token, never bash-bypass). `CLAUDE.md`, `AGENTS.md`, `examples/**`, and the test are NOT constitution-plane and need no token. The commit lands through the live gate (05-01 approval path already merged).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-despecialization/05-CONTEXT.md
@.planning/phases/05-despecialization/05-RESEARCH.md
@CLAUDE.md
@AGENTS.md
@docs/adr/README.md
@docs/adr/0001-walking-skeleton-golden-core.md
@tools/harness_lint/tests/test_commands.py
@harness/project.toml

<interfaces>
<!-- Guard-test scope (RESEARCH Q6) -->
scan roots (core planes): tools/, harness/, libs/     # walk tracked files
FAIL on: a line containing an `examples/` path reference, OR a Python `import examples`/`from examples`
EXEMPT: harness/project.toml `[instance] root` (the single sanctioned instance-pointer datum;
        in this repo root="" so no examples/ ref exists — assert the ONLY allowed occurrence is there)
KEEP OUT of scan: contracts/sample, golden/sample (root generic instance, not examples/)
idiom to mirror: tools/harness_lint/tests/test_commands.py (parents[3] repo root, glob-driven, no runtime)

<!-- ADR shape (docs/adr/README.md convention: MADR 4.x, NNNN-kebab, append-only, immutable) -->
0002-general-template-de-specialization.md:
  Status: accepted ; Date: 2026-07-09 ; Supersedes: — ; (ADR-0001 is complemented, not superseded)
  Sections: Title, Status, Context and Problem Statement, Decision Drivers, Considered Options,
            Decision Outcome (generic re-scope + domain demotion to examples/log-parser + language
            config slot + commit-gate GOLDEN_APPROVE_HUMAN approval path), Consequences, Links
  README index: append a row `| 0002 | General Template De-specialization | accepted |`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core->example no-dependency guard test (GEN-04)</name>
  <files>tools/harness_lint/tests/test_core_no_example_dep.py</files>
  <read_first>
    - tools/harness_lint/tests/test_commands.py — the structural-scan idiom (repo-root via parents[3], walk files, assert properties, no subprocess) to mirror.
    - 05-RESEARCH.md §Q6 — the grep/import-scan shape, the harness/project.toml [instance] exemption, and keeping contracts/sample & golden/sample OUT of scope.
    - harness/project.toml — confirm `[instance] root=""` (so no examples/ reference exists to exempt in this repo).
  </read_first>
  <behavior>
    - Walk every tracked file under tools/, harness/, libs/ (use `git ls-files` for the tracked set, filtered to those three roots).
    - FAIL if any line contains an `examples/` path reference OR a Python `import examples` / `from examples` statement, with ONE sanctioned exception: the `[instance] root` value line in harness/project.toml (and only there).
    - Pass on the real post-move tree (root="" → no examples/ refs anywhere in core).
    - A synthetic core file containing `examples/log-parser/...` (created in a tmp fixture or asserted via a helper) would FAIL — prove the guard actually detects a violation.
  </behavior>
  <action>Create `tools/harness_lint/tests/test_core_no_example_dep.py` mirroring test_commands.py. Enumerate the tracked core files via `git ls-files tools harness libs` (subprocess.run([...], shell=False)); for each text file, scan lines for the token `examples/` and for `^\s*(from|import)\s+examples\b`. Assert zero hits except the single allowed `harness/project.toml` `[instance] root` occurrence (in this repo there are none, since root=""). Add a self-proving negative: feed a crafted string like `"x = 'examples/log-parser/foo'"` through the same scan helper and assert it is flagged (so the guard cannot silently no-op). Keep it a pure structural test (no imports of example modules).</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x -q</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x -q` passes on the real tree; the negative-control assertion proves the scan flags a synthetic `examples/` reference; `uv run pytest` full non-example suite green.</acceptance_criteria>
  <done>A guard test encodes and proves the core→example single-direction invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Recast root docs to template + write ADR-0002 (via the approval path)</name>
  <files>CLAUDE.md, AGENTS.md, examples/log-parser/AGENTS.md, examples/log-parser/README.md, docs/explanation/template-and-instances.md, docs/adr/0002-general-template-de-specialization.md, docs/adr/README.md</files>
  <read_first>
    - AGENTS.md — the "Monorepo map" section that currently names contracts/ as the semiconductor SSOT + libs/python as the normalization core; recast to describe the template (core planes) + examples/<instance>/ layout.
    - CLAUDE.md §Project / §Constraints — the log-parser identity to reframe as "the log-parser example instance of a general harness template."
    - docs/adr/0001-walking-skeleton-golden-core.md + docs/adr/README.md — MADR section shape + the append-only/immutable/complement-not-supersede convention.
    - 05-CONTEXT.md D-06 + 05-RESEARCH.md §Q3 (constitution-plane writes need the token; the ADR lands via the approval path).
  </read_first>
  <action>Recast root docs (NON-constitution — no token): in `AGENTS.md`, generalize the Monorepo map to the template shape — core planes (`contracts/` = the ACTIVE instance's contracts / generic default at root; `libs/` = the domain-neutral §4.3-4.6 normalize core that STAYS in core; `tools/`, `harness/` = the reusable engine) + `examples/<instance>/` for domain seeds — and point domain specifics to `examples/log-parser/`. In `CLAUDE.md`, reframe the project identity as a reusable harness template whose log-parser domain lives in `examples/log-parser/` (keep the stack pins; note the §4.3-4.6 table applies to the normalize core, unmoved). Move log-parser-specific prose into `examples/log-parser/AGENTS.md` (nearest-wins per-instance rules) and a new `examples/log-parser/README.md` (what the instance is, how it uses the core, .NET egress-deferred note). Add `docs/explanation/template-and-instances.md` (Diátaxis explanation) describing the template↔instance split + how to add an instance (add `examples/<name>/{contracts,golden,...}` + its own manifest + a project.toml language set). Then, via the approval path (token exported): write `docs/adr/0002-general-template-de-specialization.md` as MADR (Status accepted; Context = ADR-0002 re-scope from a log-parser-specific harness to a general template; Drivers; Considered Options; Decision Outcome = domain demotion to examples/log-parser + language config slot + commit-gate GOLDEN_APPROVE_HUMAN approval path + normalize-core-stays; Consequences; Links to ADR-0001 which it complements, not supersedes) and append its row to the `docs/adr/README.md` index. Commit through the live gate (token set; NO `--no-verify`): `docs(05-05): de-specialization guard + template docs recast + ADR-0002 (GEN-04)`.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint -x -q && test -f docs/adr/0002-general-template-de-specialization.md && grep -q "0002" docs/adr/README.md && grep -q "examples/log-parser" AGENTS.md && test -f examples/log-parser/README.md</automated>
  </verify>
  <acceptance_criteria>ADR-0002 exists with `Status: accepted` and links ADR-0001 as complemented (not superseded); the README index carries the 0002 row (append-only — 0001 row untouched). Root `AGENTS.md`/`CLAUDE.md` describe the template and point to `examples/log-parser/` for domain specifics; `examples/log-parser/{AGENTS.md,README.md}` carry the moved log-parser prose. The commit landed through the live gate without `--no-verify`. `uv run pytest` full suite green.</acceptance_criteria>
  <done>Root docs describe the template + instance-addition; log-parser specifics live in the example; ADR-0002 records the de-specialization via the human approval path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human token -> ADR/README write (docs/adr/**) | The ADR + index are constitution-plane; `contract_guard` gates them on the human-set `GOLDEN_APPROVE_HUMAN`. |
| core planes -> examples/ | The guard test is the enforced tamper-evidence of the one-directional dependency invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | a core file quietly grows an examples/ dependency later | mitigate | `test_core_no_example_dep` scans tools/harness/libs on every run; a new examples/ ref fails the suite; negative-control proves the scan is live. |
| T-05-14 | Repudiation | ADR silently edited / renumbered (violates append-only) | mitigate | New numbered ADR-0002 appended; ADR-0001 untouched; README index row appended, not rewritten (MADR immutable convention). |
| T-05-15 | Elevation of Privilege | agent fabricates the token to land the ADR | mitigate | Token is human-set process env only; `contract_guard` empty/blank check unchanged; commit lands through the live gate (no `--no-verify`). |
| T-05-SC | Tampering | dependency supply chain | accept | Zero new packages — one structural test + Markdown docs; `uv.lock` unchanged. |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint -x -q` green (guard + language-config + existing structural tests).
- `uv run pytest` full non-example suite green.
- ADR-0002 present, `Status: accepted`, README index appended (0001 row intact).
- Root docs describe the template; `examples/log-parser/{AGENTS.md,README.md}` hold domain specifics.
- Commit landed through the live gate (no `--no-verify`).
</verification>

<success_criteria>
A guard test proves core→example single-direction dependency with the non-example suite green (SC4), and the root docs describe the template + instance-addition with log-parser specifics moved into the example, recorded by ADR-0002 (SC5, D-06) — Phase 5 success criteria 4 and 5.
</success_criteria>

<output>
Create `.planning/phases/05-despecialization/05-05-SUMMARY.md` when done.
</output>
