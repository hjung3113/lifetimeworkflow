---
phase: 05-despecialization
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tools/golden_runner/runner.py
  - tools/golden_runner/tests/test_identity_converter.py
  - contracts/sample/greeting.schema.json
  - golden/sample/meta.yaml
  - golden/sample/input/seed.tsv
  - golden/sample/expected/baseline.verified.tsv
  - contracts/.hashes/manifest.json
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - tools/golden_runner/tests/test_sample_loop.py
autonomous: true
requirements: [GEN-02]

must_haves:
  truths:
    - "A domain-neutral generic contract (contracts/sample/greeting.schema.json) exists at repo root with zero semiconductor vocabulary."
    - "A generic golden case (golden/sample/*) runs the full contract->hash->drift->golden loop WITHOUT .NET, using a built-in language-agnostic identity converter, and PASSES via §4.3-4.6 normalization (BOM/CRLF/row-order neutralization) on byte-clean inputs."
    - "The golden runner is parametrized: an optional per-case converter selector (identity vs dotnet) and an optional golden_dir override, so the core engine no longer hardcodes the domain converter/root."
    - "After adding the sample, the root contract-hash manifest is rebaselined and the live contract-drift gate reads clean."
    - "The docs-sync determinism test's EXPECTED_PAGES includes the generic sample page."
  artifacts:
    - path: "contracts/sample/greeting.schema.json"
      provides: "GEN-02 domain-neutral Draft 2020-12 sample contract (name/greeting fields)"
      contains: "properties"
    - path: "golden/sample/meta.yaml"
      provides: "generic golden case declaring converter: identity (language-agnostic, no .NET)"
      contains: "identity"
    - path: "tools/golden_runner/runner.py"
      provides: "parametrized converter (identity built-in) + golden_dir override; §4.3-4.6 core reuse unchanged"
      contains: "identity"
    - path: "golden/sample/input/seed.tsv"
      provides: "byte-clean (LF, no BOM) generic seed; representation diff limited to what identity+normalize neutralizes"
      min_lines: 2
  key_links:
    - from: "golden/sample/meta.yaml"
      to: "tools/golden_runner/runner.py"
      via: "converter: identity selects the built-in generic converter"
      pattern: "identity"
    - from: "tools/golden_runner/runner.py"
      to: "libs/python/normalize/core.py"
      via: "normalize_tsv reuse (unchanged) neutralizes BOM/CRLF/row-order"
      pattern: "normalize_tsv"
    - from: "contracts/sample/greeting.schema.json"
      to: "contracts/.hashes/manifest.json"
      via: "contract_hash --write rebaseline includes the sample"
      pattern: "sample/greeting"
---

## Phase Goal

**As a** developer or coding agent turning this log-parser harness into a reusable template, **I want to** relocate the semiconductor domain seed into `examples/log-parser/`, slot the language/toolchain assumption into config, and prove the core depends on no example, **so that** the repo can be cloned as a domain-neutral template while every contract/§4.3-4.6/gate invariant stays enforced.

> NOTE (planner): ROADMAP Phase 5 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived; ratify via `/gsd mvp-phase 5` if a canonical story is required.

<objective>
Add the minimal **generic default instance** at repo root (GEN-02): a domain-neutral sample contract + a companion golden case that exercises the FULL contract→hash→drift→golden loop with zero semiconductor content and — crucially — WITHOUT .NET. To make the loop actually run in-env, parametrize the golden runner with a built-in **language-agnostic identity converter** (stdlib copy) selectable per-case via `meta.yaml`, and an optional `golden_dir` override so the engine no longer pins the domain golden root (RESEARCH Q1/Q7, D-01b Q3). This is the "core points at the generic instance, not a void" setup that the domain move (05-03) then relies on.

Purpose: prove the machinery runs on a blank domain — the template's clone-and-go state. The identity converter proves the §4.3-4.6 normalize path end-to-end (representation-only diff PASSes) without the domain .NET toy-converter.
Output: `contracts/sample/greeting.schema.json`, `golden/sample/**`, a parametrized `runner.py`, a rebaselined root manifest, and the loop tests.

**Precondition (token):** creating `contracts/sample/*` and `golden/sample/*` are constitution-plane Writes → `contract_guard` DENIES them unless a human has set `GOLDEN_APPROVE_HUMAN` in the Claude Code process env. Per 05-RESEARCH Q3: the human exports the token BEFORE launching the session (preferred), or adds it to a gitignored `.claude/settings.local.json` `env` block for the session and removes it after — NEVER commit the token, NEVER bash-bypass the gate. 05-01 (the commit-gate drift approval path) is already merged, so any residual drift is ratified; the manifest rebaseline in Task 2 keeps drift clean regardless.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-despecialization/05-CONTEXT.md
@.planning/phases/05-despecialization/05-RESEARCH.md
@CLAUDE.md
@tools/golden_runner/runner.py
@libs/python/normalize/core.py
@golden/repr-only/meta.yaml
@contracts/log-specs/standard-log.schema.json
@tools/contract_hash/hash.py
@tools/docs_sync/tests/test_docs_sync_determinism.py

<interfaces>
<!-- CRITICAL normalize semantics (verified libs/python/normalize/core.py) -->
normalize_tsv(raw: bytes) -> str        # ONLY R1 BOM-strip + R2 LF-fold + R8 ordinal row-sort.
                                        # It does NOT canonicalize decimals/timezones — the CONVERTER does that.
# => an IDENTITY converter (no canonicalization) can only make a case PASS if the seed->baseline
#    representation diff is limited to BOM / CRLF / ROW-ORDER. A decimal-locale or timezone diff
#    would NOT be neutralized (there is no converter to canonicalize it) and would FAIL.
# => the seed.tsv MUST be byte-clean (LF, no BOM) because it is an ADDED file (git status A, not R),
#    so commit_gate.check_polyglot -> lint_file(seed) runs R1/R2 on it. Use ROW-ORDER as the diff:
#    seed rows unsorted; baseline.verified.tsv = the ordinal-sorted lines of the seed. Both byte-clean.
#    normalize_tsv(identity-output) == normalize_tsv(baseline) -> PASS.

<!-- Golden runner surfaces to parametrize (tools/golden_runner/runner.py) -->
GOLDEN_DIR = REPO_ROOT / "golden"                       # add an optional golden_dir override
def case_dir(case) / seed_path / verified_path / received_path   # thread golden_dir through
def compare(output_bytes, case) -> GoldenResult         # calls verified_path(case) internally -> thread golden_dir
def run_golden_case(case, out_path, dotnet_exe=None, project=None) -> GoldenResult  # add converter + golden_dir
def run_converter(seed, out_path, dotnet_exe=None, project=None) -> int             # the .NET path (unchanged default)

<!-- Hasher (tools/contract_hash/hash.py) — already parametrized -->
build_manifest(contracts_dir=CONTRACTS_DIR) ; write_manifest()  # `python -m tools.contract_hash.hash --write` rebaselines root
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parametrize golden_runner — built-in identity converter + golden_dir override</name>
  <files>tools/golden_runner/runner.py, tools/golden_runner/tests/test_identity_converter.py</files>
  <read_first>
    - tools/golden_runner/runner.py — case_dir/seed_path/verified_path/received_path/compare/run_golden_case/run_converter; `_confine`; the `normalize_tsv` reuse in compare.
    - libs/python/normalize/core.py::normalize_tsv (78-88) — confirm it does ONLY BOM/CRLF/row-sort; the identity path depends on this.
    - golden/repr-only/meta.yaml — existing meta.yaml shape (YAML) to extend with an optional `converter:` key.
    - 05-RESEARCH.md §Q1 (GOLDEN_DIR/converter are the only genuinely domain-pinned roots) + §Q7 (parametrize a trivial converter so the loop runs without .NET).
  </read_first>
  <behavior>
    - identity converter: given seed bytes, write them verbatim to out_path (stdlib copy, zero canonicalization, zero domain vocabulary). No .NET, no subprocess spawn of dotnet.
    - run_golden_case(case, out, converter="identity"): uses the identity converter (never resolve_dotnet); converter defaults to the .NET path (backward-compatible — existing repr-only/value-regression tests unchanged).
    - golden_dir override: run_golden_case(case, out, golden_dir=<tmp>) resolves case_dir/seed/verified/received under <tmp> instead of REPO_ROOT/golden; default stays REPO_ROOT/golden.
    - A representation-only (row-order) seed vs its sorted baseline, run through the identity converter, PASSES (normalize_tsv sorts both sides equal). A genuinely different-value baseline FAILs and writes a .received (never touching .verified).
  </behavior>
  <action>Edit `tools/golden_runner/runner.py`: (1) add `golden_dir: Path | None = None` to `case_dir`, `seed_path`, `verified_path`, `received_path`, `compare`, and `run_golden_case`, defaulting to the module `GOLDEN_DIR` (thread it through so `compare`'s internal `verified_path(case)` honors the override). (2) Add a built-in generic converter `run_identity_converter(seed: Path, out_path: Path) -> None` that reads seed bytes and writes them to `_confine(out_path)` verbatim — pure stdlib, no `dotnet`, no domain vocabulary (this is the language-agnostic converter the template ships). (3) Give `run_golden_case` a `converter: str = "dotnet"` parameter: `"identity"` routes to `run_identity_converter` (skipping `resolve_dotnet`/`run_converter`), any other value keeps the existing .NET spawn. Keep the §4.3-4.6 `normalize_tsv` compare path UNCHANGED (D: normalization logic is move-only/untouched; this plan adds no cell normalization). Add `tools/golden_runner/tests/test_identity_converter.py` covering the four behaviors above using a tmp golden_dir (build a tiny row-order case in tmp — do NOT depend on the yet-unwritten golden/sample so this test is order-independent). Commit: `feat(05-02): golden_runner identity converter + golden_dir override (generic loop, no .NET)`.</action>
  <verify>
    <automated>uv run pytest tools/golden_runner/tests/test_identity_converter.py tools/golden_runner/tests/test_compare_recorded.py -x -q</automated>
  </verify>
  <acceptance_criteria>The identity-converter test passes (row-order case PASSES with converter="identity" and no .NET); the pre-existing `.NET`-free comparison test (`test_compare_recorded.py`) still passes; the .NET-gated tests (`test_repr_only`/`test_value_regression`) still SKIP cleanly when dotnet is absent (signatures backward-compatible).</acceptance_criteria>
  <done>The golden engine offers a language-agnostic identity converter + a golden_dir override, with the §4.3-4.6 normalize path untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Generic default instance — sample contract + golden case + rebaseline + docs-sync page</name>
  <files>contracts/sample/greeting.schema.json, golden/sample/meta.yaml, golden/sample/input/seed.tsv, golden/sample/expected/baseline.verified.tsv, contracts/.hashes/manifest.json, tools/docs_sync/tests/test_docs_sync_determinism.py, tools/golden_runner/tests/test_sample_loop.py</files>
  <read_first>
    - contracts/log-specs/standard-log.schema.json — a real Draft 2020-12 schema shape (`$schema`, `type`, `properties`, `required`) to mirror WITHOUT any domain vocabulary.
    - tools/docs_sync/tests/test_docs_sync_determinism.py:23-31 — EXPECTED_PAGES (the frozenset to add "greeting" to).
    - tools/contract_hash/hash.py — `--write` rebaselines the root manifest from the working tree.
    - golden/repr-only/{meta.yaml,input,expected} — the case directory layout to mirror.
    - Interfaces block above — the ROW-ORDER (not decimal/TZ) representation-diff constraint for an identity converter, and the byte-clean (LF/no-BOM) requirement for the ADDED seed.
  </read_first>
  <action>Write `contracts/sample/greeting.schema.json`: a trivial Draft 2020-12 schema, `properties: {name: {type: string}, greeting: {type: string, const: "hello"}}`, `required: ["name"]` — ZERO semiconductor vocabulary. Write `golden/sample/`: `meta.yaml` (mirror repr-only's fields; set `converter: identity` and `expected_result: PASS`, note the representation diff is ROW-ORDER only), `input/seed.tsv` (a byte-clean 2-column TSV — header + a few rows in UNSORTED order; LF newlines, NO BOM), and `expected/baseline.verified.tsv` (the SAME lines ordinal-sorted, byte-clean) so `normalize_tsv` (R8 row-sort) makes identity-output == baseline → PASS. Verify byte-cleanliness explicitly (`od -c golden/sample/input/seed.tsv | grep -qv '357 273 277'` for no-BOM; no `\r`). Add `tools/golden_runner/tests/test_sample_loop.py` asserting: (a) `run_golden_case("sample", <tmp out>, converter="identity")` PASSES with `received_path is None`; (b) `build_manifest(<repo>/contracts)` includes key `contracts/sample/greeting.schema.json`; (c) `run_gate()` (drift) is clean after rebaseline. Rebaseline the root manifest via Bash: `uv run python -m tools.contract_hash.hash --write` (this is a Bash step — NOT contract_guard-gated — so it needs no token). Add `"greeting"` to `EXPECTED_PAGES` in `test_docs_sync_determinism.py`. Commit (with the token exported per the precondition): `feat(05-02): generic default instance (greeting sample contract + identity golden case); rebaseline root manifest`.</action>
  <verify>
    <automated>uv run python -m tools.contract_hash.hash --write >/dev/null; uv run python -m tools.contract_drift.drift && uv run pytest tools/golden_runner/tests/test_sample_loop.py tools/docs_sync/tests/test_docs_sync_determinism.py -x -q</automated>
  </verify>
  <acceptance_criteria>`uv run python -m tools.contract_drift.drift` prints the clean/OK result (live manifest matches rebaselined baseline). `run_golden_case("sample", ..., converter="identity")` PASSES with no .received written. The docs-sync determinism test passes with the added `greeting` page. `golden/sample/input/seed.tsv` has no BOM and no CR (byte-clean). `uv run pytest` full suite green.</acceptance_criteria>
  <done>A domain-neutral generic instance exercises contract→hash→drift→golden end-to-end without .NET; the root manifest is rebaselined clean; the docs-sync page set includes the sample.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human token -> constitution-plane Write (contracts/sample, golden/sample) | The sample contract + golden fixtures are constitution-plane; `contract_guard` gates the Write on `GOLDEN_APPROVE_HUMAN` (human-set only). |
| added seed.tsv -> commit_gate polyglot | An ADDED (status A) seed is linted (R1/R2); it must be byte-clean or the commit blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | added generic seed trips polyglot (BOM/CRLF) | mitigate | seed.tsv authored byte-clean (LF, no BOM); verified via `od` in the acceptance check; representation diff limited to row-order (needs no BOM/CRLF). |
| T-05-07 | Tampering | generic golden falsely PASSes/FAILs (identity converter can't canonicalize decimals/TZ) | mitigate | Representation diff constrained to R8 row-order that `normalize_tsv` neutralizes; test asserts PASS + `received_path is None`; a value-diff variant would FAIL (documented). |
| T-05-08 | Elevation of Privilege | agent fabricates the approval token to land the constitution write | mitigate | Token is human-set process env only; `contract_guard` empty/blank check unchanged; ADR (05-05) records the intended change. |
| T-05-SC | Tampering | dependency supply chain | accept | Zero new packages — stdlib copy converter; reuses in-repo hasher/normalize; `uv.lock` unchanged. |
</threat_model>

<verification>
- `uv run pytest tools/golden_runner tools/docs_sync -x -q` green (identity + sample loop + determinism).
- `uv run python -m tools.contract_drift.drift` clean after `--write` rebaseline.
- `uv run pytest` full suite green.
- Byte-clean proof: `od -c golden/sample/input/seed.tsv` shows no `357 273 277` (BOM) and no `\r`.
</verification>

<success_criteria>
A minimal domain-neutral default instance at the repo root exercises the full contract→hash→drift→golden loop with zero semiconductor content and without .NET (language-agnostic identity converter), proving the machinery runs on a blank domain — Phase 5 success criterion 2.
</success_criteria>

<output>
Create `.planning/phases/05-despecialization/05-02-SUMMARY.md` when done.
</output>
