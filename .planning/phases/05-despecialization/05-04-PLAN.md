---
phase: 05-despecialization
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/project.toml
  - tools/harness_config/__init__.py
  - tools/harness_config/loader.py
  - tools/harness_config/pyproject.toml
  - tools/harness_config/tests/__init__.py
  - tools/harness_config/tests/test_loader.py
  - tools/harness_lint/tests/test_language_config.py
autonomous: true
requirements: [GEN-03]

must_haves:
  truths:
    - "A single project-config slot `harness/project.toml` declares the participating languages/toolchains (id + bash scope + test/format commands + persona + sdk bootstrap)."
    - "A thin loader parses that config with zero new dependencies (stdlib tomllib) and exposes the language list."
    - "A consistency test proves the permission-matrix `dotnet */uv */pytest *` allow-scopes and the engineer personas DERIVE from the config — any hardcoded value that diverges FAILS the test (config = SSOT)."
    - "The log-parser example supplies the concrete .NET 10 + Python(uv) values in that slot."
  artifacts:
    - path: "harness/project.toml"
      provides: "GEN-03 SSOT: [instance] root + [[languages]] dotnet/python (bash_scope, test, format, persona, sdk_bootstrap)"
      contains: "[[languages]]"
    - path: "tools/harness_config/loader.py"
      provides: "thin stdlib tomllib loader: load_project() -> parsed config; language_bash_scopes() helper"
      contains: "tomllib"
    - path: "tools/harness_lint/tests/test_language_config.py"
      provides: "GEN-03 consistency: permission-matrix scopes + personas derive from harness/project.toml"
      contains: "load_project"
  key_links:
    - from: "tools/harness_lint/tests/test_language_config.py"
      to: "harness/permission-matrix.json"
      via: "assert bash allow-scopes == languages[*].bash_scope (+ pytest)"
      pattern: "permission-matrix"
    - from: "tools/harness_config/loader.py"
      to: "harness/project.toml"
      via: "tomllib.load"
      pattern: "tomllib"
    - from: "tools/harness_lint/tests/test_language_config.py"
      to: "harness/agents/{dotnet,python}-engineer.md"
      via: "assert a persona file exists for each configured language"
      pattern: "persona"
---

## Phase Goal

**As a** developer or coding agent turning this log-parser harness into a reusable template, **I want to** relocate the semiconductor domain seed into `examples/log-parser/`, slot the language/toolchain assumption into config, and prove the core depends on no example, **so that** the repo can be cloned as a domain-neutral template while every contract/§4.3-4.6/gate invariant stays enforced.

> NOTE (planner): ROADMAP Phase 5 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived; ratify via `/gsd mvp-phase 5` if a canonical story is required.

<objective>
Turn the hardwired ".NET + Python" assumption into a single project-config slot `harness/project.toml` (GEN-03). Add a thin stdlib loader (`tools/harness_config/`, a new `tools/*` uv member) and — per D-03's "full codegen is overkill" — satisfy "derived, not hardcoded" with a **consistency test**: the permission-matrix's `dotnet */uv */pytest *` allow-scopes and the engineer personas must equal the set derived from the config. The config becomes the SSOT; any hardcoded value that diverges fails the test.

Purpose: languages become a slot a project fills, not a core assumption — the precondition for the Phase 6 config-derived CI matrix and for cloning the template to a non-.NET/Python domain. The log-parser example supplies the concrete .NET 10 + uv values in this slot (they are the active instance's values, not a core hardcode).
Output: `harness/project.toml` + a thin loader (uv member) + a consistency test wired into the existing `harness_lint` suite.

Precondition: `harness/` is NOT the constitution plane (constitution globs = `contracts/**`·`docs/adr/**`·`golden/**`), so these writes need NO `GOLDEN_APPROVE_HUMAN` token. Core-only, no domain move — lands clean.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-despecialization/05-CONTEXT.md
@.planning/phases/05-despecialization/05-RESEARCH.md
@CLAUDE.md
@harness/permission-matrix.json
@harness/agents/dotnet-engineer.md
@harness/agents/python-engineer.md
@harness/commands/test.md
@harness/commands/build.md
@harness/commands/lint.md
@tools/harness_perms/pyproject.toml
@tools/harness_lint/tests/test_commands.py

<interfaces>
<!-- Existing "config-as-data + thin consumer" precedent to mirror (RESEARCH Q5) -->
harness/permission-matrix.json  bash: {"*":"ask","dotnet *":"allow","uv *":"allow","pytest *":"allow","git push*":"ask","rm -rf*":"deny"}
  -> the LANGUAGE allow-scopes to reconcile against config are: "dotnet *", "uv *", "pytest *"
tools/harness_perms/resolver.py: load_matrix(path=...) -> dict   # reuse to read the matrix in the test
tools/harness_lint: parse_frontmatter(text) -> dict              # reuse if the test inspects persona frontmatter

<!-- Recommended harness/project.toml shape (RESEARCH Q5; TOML via stdlib tomllib, py3.11+ no dep) -->
[instance]
root = ""                       # "" = generic default instance at repo root; example overrides via its own config
[[languages]]
id = "dotnet"
bash_scope = "dotnet *"
test = "dotnet test"
format = "dotnet format"
sdk_bootstrap = "tools/bootstrap/install.sh"
persona = "harness/agents/dotnet-engineer.md"
[[languages]]
id = "python"
bash_scope = "uv *"             # note: pytest also carries its own "pytest *" allow-scope
test = "uv run pytest"
format = "ruff format"
persona = "harness/agents/python-engineer.md"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author harness/project.toml + the thin harness_config loader (uv member)</name>
  <files>harness/project.toml, tools/harness_config/__init__.py, tools/harness_config/loader.py, tools/harness_config/pyproject.toml, tools/harness_config/tests/__init__.py, tools/harness_config/tests/test_loader.py</files>
  <read_first>
    - tools/harness_perms/pyproject.toml — the exact shape of an existing `tools/*` uv member's pyproject (name, build config) to mirror for harness_config.
    - harness/permission-matrix.json — the language allow-scopes the config must reproduce ("dotnet *","uv *","pytest *").
    - harness/agents/dotnet-engineer.md, python-engineer.md — persona paths the config references.
    - pyproject.toml [tool.uv.workspace] members = ["libs/python","tools/*"] — tools/harness_config matches `tools/*` automatically once it has a pyproject.toml.
    - 05-RESEARCH.md §Q5 — schema + "config = SSOT, no full codegen" mandate.
  </read_first>
  <action>Create `harness/project.toml` per the Interfaces shape: `[instance] root=""` plus two `[[languages]]` tables (dotnet, python) carrying `id`, `bash_scope`, `test`, `format`, `sdk_bootstrap` (dotnet only), and `persona`. These are the log-parser example instance's values placed in the active-instance slot (reinterpreting the current hardcode as "the example's supplied values," per D-03). Create `tools/harness_config/` as a new uv member mirroring `tools/harness_perms/`: `pyproject.toml` (same build backend/naming convention), `__init__.py` re-exporting the loader API, `loader.py` with `load_project(path=<repo>/harness/project.toml) -> dict` using stdlib `tomllib` (open in binary mode; py3.11+ guaranteed by `requires-python >=3.11`) and a helper `language_bash_scopes(cfg) -> set[str]` returning the union of `languages[*].bash_scope` plus the implicit `"pytest *"` (Python's test-runner scope). Add `tools/harness_config/tests/test_loader.py`: assert `load_project()` returns two languages with ids {"dotnet","python"} and that each `persona` path exists on disk. NO new external packages; NO enforcement logic in the config (pure data, mirroring permission-matrix.json's "_note").</action>
  <verify>
    <automated>uv sync --all-packages >/dev/null 2>&1; uv run pytest tools/harness_config/tests/test_loader.py -x -q</automated>
  </verify>
  <acceptance_criteria>`uv sync --all-packages` resolves with the new member (no "member not found"); `uv run python -c "from tools.harness_config import load_project; print(sorted(l['id'] for l in load_project()['languages']))"` prints `['dotnet', 'python']`; the loader test passes.</acceptance_criteria>
  <done>`harness/project.toml` is the SSOT slot; a stdlib-only loader (uv member) parses it and exposes the language list + bash-scope helper.</done>
</task>

<task type="auto">
  <name>Task 2: Consistency test — permission-matrix scopes + personas derive from the config</name>
  <files>tools/harness_lint/tests/test_language_config.py</files>
  <read_first>
    - tools/harness_lint/tests/test_commands.py — the structural-test idiom (repo-root resolution via parents[3], glob-driven, no runtime) to mirror.
    - tools/harness_perms/resolver.py::load_matrix — reuse to load harness/permission-matrix.json in the test.
    - 05-RESEARCH.md §Q5 — the consistency-test approach (assert hardcoded == config) that satisfies "derived not hardcoded" without codegen.
  </read_first>
  <action>Create `tools/harness_lint/tests/test_language_config.py` mirroring test_commands.py's structural style. Load the real config via `tools.harness_config.load_project()` and the real matrix via `tools.harness_perms.load_matrix()`. Assertions: (1) the set of LANGUAGE allow-scopes in the matrix `bash` object (the keys mapping to "allow" that correspond to language toolchains — `dotnet *`, `uv *`, `pytest *`) EQUALS `language_bash_scopes(cfg)` — so a divergence (e.g. dropping `dotnet *` from the matrix or the config) fails the test; (2) for each configured language, its `persona` file exists under `harness/agents/`; (3) each language's `test` command string is non-empty. Keep it a pure structural test (no subprocess). This makes `harness/project.toml` authoritative: the hardcoded matrix/persona values must agree with it.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_language_config.py -x -q</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/harness_lint/tests/test_language_config.py -x -q` passes; manually editing `harness/project.toml` to remove the dotnet language (or the matrix to drop `dotnet *`) makes it FAIL — proving the config is the SSOT. `uv run pytest` full suite stays green.</acceptance_criteria>
  <done>A consistency test proves the permission-matrix language scopes and the engineer personas derive from `harness/project.toml`; GEN-03 satisfied at minimal cost (no codegen).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness/project.toml (data) -> loader | Pure config data; the loader adds NO enforcement logic (mirrors permission-matrix.json's data-only posture). |
| config vs hardcoded matrix/personas | The consistency test is the tamper-evidence: a silent divergence between the SSOT config and the hardcoded scopes fails CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | matrix scope silently diverges from the SSOT config | mitigate | `test_language_config` asserts the matrix language allow-scopes == config-derived set; divergence fails the suite. |
| T-05-05 | Spoofing | config references a non-existent persona | mitigate | Test asserts each configured `persona` file exists on disk. |
| T-05-SC | Tampering | dependency supply chain | accept | Zero new packages — stdlib `tomllib`; new uv member carries only in-repo code; `uv.lock` re-resolved with no external adds. |
</threat_model>

<verification>
- `uv run pytest tools/harness_config tools/harness_lint/tests/test_language_config.py -x -q` green.
- `uv run pytest` full suite green (new member collected, no regression).
- `uv sync --all-packages` resolves cleanly (P1 workspace-member sanity).
- Manual SSOT proof: drop a language from `harness/project.toml` → consistency test fails.
</verification>

<success_criteria>
Participating languages/toolchains are read from a single project-config slot (`harness/project.toml`); the permission-matrix `dotnet */uv */pytest *` scopes and the engineer personas provably derive from it, with the log-parser example supplying the .NET 10 + uv values — Phase 5 success criterion 3.
</success_criteria>

<output>
Create `.planning/phases/05-despecialization/05-04-SUMMARY.md` when done.
</output>
