# Phase 19 Implementation Plan

1. Define the runtime-neutral seven-axis core policy, lane cuts, promotion minima, and monotonic artifact/gate matrix in `harness/`.
2. Add an optional `harness/project.toml` risk-overlay slot plus schema and semantic validation that only permits escalation.
3. Implement a pure deterministic router and thin CLI in `tools/risk_router/`, including canonical decision JSON and policy hashes.
4. Author `/intake` once under `harness/commands/`, emit both runtime projections, and verify zero re-emit drift.
5. Cover Phase-2 acceptance criteria 1-10, then run router tests, the full tools suite, contract drift, and emit drift.

Scope stops before Phase 20 state transitions and Phase 21 evidence collection. No constitution-plane or example overlay files are changed.
