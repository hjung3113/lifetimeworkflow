---
phase: 39-decision-boundary-v2-5-a
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - .planning/STATE.md
  - .planning/phases/39-decision-boundary-v2-5-a/39-02-SUMMARY.md
autonomous: true
requirements: [CER-03]

must_haves:
  truths:
    - "STATE.md records RAT-4 as obsolete-by-deletion, citing ADR-0012, keyed on a unique v2.5 P39, ADR-0012 marker"
    - "STATE.md records RAT-5 as obsolete-by-deletion, citing ADR-0012, keyed on the same unique marker"
    - "STATE.md records the per-tool constitution-plane deny-spelling gap as obsolete-by-deletion, citing ADR-0012, keyed on the same unique marker"
    - "STATE.md marks v2.4's SEAL-05 as withdrawn (not deferred), citing ADR-0012, keyed on the same unique marker"
    - "Exactly 4 new rows carry the v2.5 P39, ADR-0012 marker — 3 obsolete-by-deletion + 1 withdrawn — not merely >= 3"
    - "The existing suite, contract-drift gate, and emitted trees stay exactly as green/red as they were before this phase — this plan makes nothing worse, and any new (non-pre-existing) failure triggers STOP-and-report, not repair"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "the four carried-item dispositions, appended to the existing Deferred Items table"
      contains: "obsolete-by-deletion"
  key_links:
    - from: ".planning/STATE.md"
      to: "docs/adr/0012-ci-and-merge-as-decision-authority.md"
      via: "prose citation of ADR-0012 in each new disposition row"
      pattern: "ADR-0012"
---

<objective>
Record the four carried-item dispositions this phase closes as decisions, not mechanisms: RAT-4,
RAT-5, and the per-tool constitution-plane deny-spelling gap as **obsolete-by-deletion**, and v2.4's
SEAL-05 ("portable ratification record") as **withdrawn** (never deferred) — per CER-03. Then run
the existing no-regression checks to confirm this phase changed nothing else.

Purpose: closes the milestone's Phase-39 bookkeeping so no later v2.5 phase re-litigates whether a
human-ratification mechanism is still owed for these three items.

Output: `.planning/STATE.md` gains four new rows in its existing `## Deferred Items` table (append
only — the existing RAT-4/RAT-5/security rows and the v2.4 SEAL-05 entry in
`.planning/milestones/v2.4-REQUIREMENTS.md` are NOT edited, per this repo's append-only-record
convention carried from the ADR practice into planning docs).

**Scope note:** `.planning/**` is the GSD-owned planning lane, not the constitution plane —
`tools/hooks/contract_guard.py`'s `CONSTITUTION_GLOBS` does not cover it, so this plan's writes are
normal agent Write/Edit with no `GOLDEN_APPROVE_HUMAN` checkpoint required. `tools.harness_emit`,
however, DOES write (`.claude/`, `.opencode/`) even though it is invoked here only as a
no-regression check — Task 2 treats it accordingly (baseline capture before running it, not a
blind "no diff assumed" check).

**Revision note (this plan was revised per cross-AI review; see 39-REVIEWS.md):** exact-count
verification keyed on a unique marker, dirty-worktree baselines for both `.planning/STATE.md` and
the emitted trees, and an explicit STOP-on-new-failure rule are corrections applied in this
revision — do not regress them in future edits to this plan.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-decision-boundary-v2-5-a/39-RESEARCH.md
@.planning/phases/39-decision-boundary-v2-5-a/39-VALIDATION.md
@.planning/phases/39-decision-boundary-v2-5-a/39-REVIEWS.md
@.planning/STATE.md
@.planning/milestones/v2.4-REQUIREMENTS.md

<interfaces>
<!-- Existing STATE.md rows this task must NOT edit, and the exact table shape to append to. -->

`.planning/STATE.md`'s `## Deferred Items` table (second occurrence, around line 302-315) uses this
shape — reuse it verbatim, do not invent a new column set:

```
| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
```

Rows this task must leave byte-identical (do not edit, only append new rows below them):
- `| ratification | **RAT-5 — DISCHARGED 2026-07-22 in outcome only.** ... | discharged (see caveats) | v2.3 close |`
- `| ratification | **RAT-4** — the Phase-28 constitution-plane schema write ... | open | v2.3 close |`
- `| security (harness-wide) | **Constitution-plane write-denies are spelled per-tool.** ... | open | v2.3 close |`

`.planning/milestones/v2.4-REQUIREMENTS.md:45-49,122-125` carries SEAL-05's original text and status
table — this task does NOT edit that milestone-archive file (it is closed/archived); the withdrawal
is recorded only in `.planning/STATE.md`, the live planning document.

**Unique verification marker:** every one of the four new rows must contain the literal substring
`v2.5 P39, ADR-0012` (in addition to the item-specific `CLOSED`/`WITHDRAWN` prose) so the automated
verify can key on this exact marker rather than the generic phrase `obsolete-by-deletion`, which
could otherwise match a typo'd or duplicated row and still look correct.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the four carried-item dispositions to STATE.md's Deferred Items table</name>
  <files>.planning/STATE.md</files>
  <read_first>
    .planning/STATE.md (the full `## Deferred Items` section, to find the exact insertion point
    below the existing rows and confirm none of them are edited), .planning/ROADMAP.md (Phase 39
    success criteria + CER-03 wording), .planning/milestones/v2.4-REQUIREMENTS.md (SEAL-05's
    original text, for accurate citation)
  </read_first>
  <action>
    Before editing, capture a dirty-worktree baseline: run `git status --porcelain -- .planning/STATE.md`.
    If this is non-empty (the file already has uncommitted changes before this task starts), STOP
    and report the existing diff — do not append on top of an unprovable baseline.
    With a clean baseline confirmed, append exactly four new rows to the existing `## Deferred
    Items` table in `.planning/STATE.md` (do not touch any existing row in that table or elsewhere
    in the file). Each row cites `docs/adr/0012-ci-and-merge-as-decision-authority.md` and today's
    date, and each row's text includes the literal marker `v2.5 P39, ADR-0012` (see `<interfaces>`)
    so it can be uniquely grepped. Use this content per row:
    (1) Category `ratification`, Item text starting `**RAT-4 — CLOSED 2026-07-26 (v2.5 P39,
    ADR-0012).**` explaining the disposition is obsolete-by-deletion because every human-ratification
    gate assumed reviewer != author while this repo has one owner, Status `obsolete-by-deletion`,
    Deferred At `v2.5 P39`. (2) Category `ratification`, Item starting `**RAT-5 — CLOSED 2026-07-26
    (v2.5 P39, ADR-0012).**`, same obsolete-by-deletion rationale, Status `obsolete-by-deletion`,
    Deferred At `v2.5 P39`. (3) Category `security (harness-wide)`, Item starting `**Per-tool
    constitution-plane deny-spelling gap — CLOSED 2026-07-26 (v2.5 P39, ADR-0012).**` referencing
    the original finding (`harness/permission-matrix.json:9` `"uv *": "allow"` bypassing the
    `Write|Edit`-only matcher), recording that ADR-0012 declares the bash surface a permanent
    residual by design rather than a gap to repair, Status `obsolete-by-deletion`, Deferred At
    `v2.5 P39`. (4) Category `ratification`, Item starting `**SEAL-05 — WITHDRAWN 2026-07-26 (v2.5
    P39, ADR-0012).**` stating the "portable ratification record" concept is withdrawn, not
    deferred, per CER-03 and `.planning/ROADMAP.md:339`, Status `withdrawn`, Deferred At `v2.5 P39`.
    After appending, compare the post-edit file against the recorded pre-edit baseline (e.g.
    `git diff --numstat .planning/STATE.md`) to confirm zero deleted lines — the append-only
    invariant must hold against the actual recorded baseline, not an assumption.
  </action>
  <verify>
    <automated>test "$(grep -c 'v2.5 P39, ADR-0012' .planning/STATE.md)" -eq 4; test "$(grep 'v2.5 P39, ADR-0012' .planning/STATE.md | grep -c 'obsolete-by-deletion')" -eq 3; test "$(grep 'v2.5 P39, ADR-0012' .planning/STATE.md | grep -c 'withdrawn')" -eq 1; test "$(git diff --numstat .planning/STATE.md | awk '{print $2}')" -eq 0</automated>
  </verify>
  <done>
    `.planning/STATE.md`'s Deferred Items table has exactly 4 new rows carrying the unique
    `v2.5 P39, ADR-0012` marker: 3 tagged `obsolete-by-deletion` (RAT-4, RAT-5, per-tool deny
    spelling) and 1 tagged `withdrawn` (SEAL-05); no existing row in the file was edited or deleted
    (confirmed against the pre-edit baseline, zero deleted lines).
  </done>
  <acceptance_criteria>
    - Pre-edit baseline (`git status --porcelain -- .planning/STATE.md`) was recorded before any
      write, and the task STOPPED instead of appending if the file was already dirty
    - `test "$(grep -c 'v2.5 P39, ADR-0012' .planning/STATE.md)" -eq 4` (exactly 4, not >= 3)
    - `test "$(grep 'v2.5 P39, ADR-0012' .planning/STATE.md | grep -c 'obsolete-by-deletion')" -eq 3`
    - `test "$(grep 'v2.5 P39, ADR-0012' .planning/STATE.md | grep -c 'withdrawn')" -eq 1`
    - `git diff --numstat .planning/STATE.md` shows 0 deleted lines, proving no existing row was
      edited or removed (append-only, machine-checkable against the recorded baseline)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the no-regression gate and confirm zero drift from this phase</name>
  <files>(none — read-only verification task; no file is modified)</files>
  <read_first>
    .planning/phases/39-decision-boundary-v2-5-a/39-VALIDATION.md (the exact commands for the
    no-regression gate), .planning/phases/39-decision-boundary-v2-5-a/39-RESEARCH.md (Pitfall 4 —
    `docs-guard` is already RED for an unrelated pre-existing reason and must not be "fixed" here;
    the confirmed pre-existing failing binding set is exactly `{task-control-cli-howto}`)
  </read_first>
  <action>
    Before running the emitter, capture a dirty-worktree baseline for the emitted trees: run
    `git status --porcelain -- .claude .opencode`. If this is non-empty (already dirty before this
    task starts, unrelated to this phase's doc-only edits), STOP and report — do not run the
    emitter against an already-dirty tree and do not treat a subsequent `git diff --exit-code` as
    proof of anything.
    With a clean baseline confirmed, run the full no-regression gate: `uv run pytest`,
    `uv run python -m tools.contract_drift.drift`, and `uv run python -m tools.harness_emit`
    followed by `git diff --exit-code .claude .opencode` to confirm zero emission drift relative to
    the recorded clean baseline (ADR/STATE.md edits must never affect emitted trees).
    **STOP-on-new-failure rule:** if `uv run pytest` or `uv run python -m tools.contract_drift.drift`
    fails for any reason NOT already documented as pre-existing in `39-RESEARCH.md`, STOP and
    report the exact failure — do not attempt to diagnose or repair it (repairing a gate/CI issue
    is a Phase 39 non-goal). Only a failure this plan can trace directly to a pre-existing,
    documented cause may be reported as "unchanged, not a regression."
    Separately run `uv run python -m tools.docs_guard` and capture its full output. Compare the
    complete set of failing binding names in this output against the pre-existing baseline
    documented in `39-RESEARCH.md` (exactly `{task-control-cli-howto}`) — not merely a substring
    presence-check for that one name. If the failing-binding set contains anything beyond
    `task-control-cli-howto`, STOP and report the new binding(s) as a new, undocumented failure — do
    NOT attempt to fix `docs-guard`, and do not report it as "unchanged" if the set differs. Record
    the exact pass/fail counts and the exact failing-binding set in this plan's SUMMARY so the
    phase-level VERIFICATION step can cite them directly.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- .claude .opencode)"; uv run pytest; uv run python -m tools.contract_drift.drift; uv run python -m tools.harness_emit; git diff --exit-code .claude .opencode</automated>
  </verify>
  <done>
    Pre-run baseline for `.claude`/`.opencode` was clean (or the task stopped and reported if not);
    `uv run pytest` and `uv run python -m tools.contract_drift.drift` exit 0; `git diff --exit-code
    .claude .opencode` after re-running the emitter exits 0 (no emission drift); `docs-guard`'s full
    failing-binding set is confirmed to be exactly `{task-control-cli-howto}` (not a superset), not
    newly broken by this phase; any failure outside this documented set was STOPPED and reported,
    not repaired or silently absorbed.
  </done>
  <acceptance_criteria>
    - `git status --porcelain -- .claude .opencode` is empty before the emitter runs (baseline is
      clean); if not, the task reports and stops rather than proceeding
    - `uv run pytest` exit code 0, or a STOP-and-report citing a documented pre-existing cause from
      39-RESEARCH.md — never a silent pass-through of an undocumented failure
    - `uv run python -m tools.contract_drift.drift` exit code 0, same STOP-and-report discipline
    - `git diff --exit-code .claude .opencode` exit code 0 after re-emit
    - `uv run python -m tools.docs_guard`'s full failing-binding set equals exactly
      `{task-control-cli-howto}` — any additional failing binding is reported as a new,
      undocumented failure, not summarized as "still red for the known reason"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| agent -> `.planning/STATE.md` | agent-writable planning-plane document; no constitution-plane gate applies |
| no-regression gate -> emitted trees | `tools.harness_emit` can write `.claude/`/`.opencode/`; a dirty pre-run baseline invalidates the "zero drift" proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|------------|-------------|------------------|
| T-39-04 | Repudiation | `.planning/STATE.md` Deferred Items table | mitigate | Append-only discipline: a pre-edit baseline (`git status --porcelain`) is captured, and `git diff --numstat` asserting zero deleted lines proves no existing row was edited, only new rows added |
| T-39-05 | Tampering (masking a real regression as "pre-existing") | `docs-guard` / `core-suite` / `contract-drift` output | mitigate | Task 2 records exact pass/fail counts and the exact `docs-guard` failing-binding SET (not a substring check) against the `39-RESEARCH.md` baseline; any failure outside the documented pre-existing set triggers an explicit STOP-and-report, never a silent "unchanged" summary |
| T-39-06 | Tampering (dirty-tree false negative/positive on emission drift) | `.claude/`, `.opencode/` | mitigate | A pre-run `git status --porcelain -- .claude .opencode` baseline is required to be clean before the emitter runs; a dirty baseline STOPS the task instead of producing an unprovable `git diff --exit-code` result |
</threat_model>

<verification>
- `test "$(grep -c 'v2.5 P39, ADR-0012' .planning/STATE.md)" -eq 4` (CER-03, ROADMAP Success
  Criterion 4) — exactly 4, keyed on the unique marker, not a `>= 3` presence check
- `uv run pytest && uv run python -m tools.contract_drift.drift` both exit 0, or a documented
  STOP-and-report citing 39-RESEARCH.md (ROADMAP Success Criterion 6)
- `git diff --exit-code .claude .opencode` after re-emit exits 0, run against a confirmed-clean
  pre-run baseline — no emission drift from this phase's doc-only edits
- `docs-guard`'s failing-binding set remains exactly `{task-control-cli-howto}` — not fixed, not
  worsened, and verified as a set comparison rather than a substring match
</verification>

<success_criteria>
1. `.planning/STATE.md` records a disposition for RAT-4, RAT-5, and the per-tool deny spelling as
   obsolete-by-deletion, and marks SEAL-05 withdrawn, not deferred, with exactly 4 new rows keyed on
   the `v2.5 P39, ADR-0012` marker (ROADMAP Success Criterion 4).
2. The existing suite and the contract-drift gate stay green; no contract, gate, or emitted
   artifact changed from this phase, proven against recorded pre-task baselines rather than assumed
   (ROADMAP Success Criterion 6).
</success_criteria>

<output>
Create `.planning/phases/39-decision-boundary-v2-5-a/39-02-SUMMARY.md` when done
</output>
