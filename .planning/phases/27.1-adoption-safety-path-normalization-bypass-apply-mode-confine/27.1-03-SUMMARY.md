---
phase: 27.1-adoption-safety-path-normalization-bypass-apply-mode-confine
plan: 03
subsystem: infra
tags: [json-schema, jsonschema, contract-drift, contracts-index, memory-regen, adoption-workflow]

# Dependency graph
requires:
  - phase: 27.1-01
    provides: "refuse_unsafe_destination runtime choke point (CR-01/CR-02/WR-01) — this plan adds the upstream schema-layer defense-in-depth constraint the runtime check complements"
  - phase: 27.1-02
    provides: "check_valid CLI-boundary approval gate (CR-03/WR-02/WR-04) — schema re-validation at apply time (WR-04) only matters once the schema itself constrains destination/path shape (this plan)"
provides:
  - "manifest.schema.json's dispositionRecord.destination, excludedDestinationRecord.destination, and evidenceRef.path all reject absolute paths and \"..\" path segments while still accepting a literal \"..\" substring inside a filename"
  - "rebaselined contracts/.hashes/manifest.json + regenerated .memory/derived/contracts-index.md AND tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr, landed together in one atomic, human-ratified commit"
  - "complete SC-5 seven-finding (plus one research-found) disposition table closing Phase 27.1"
affects: [27-VERIFICATION, brownfield-adoption-skill, adoption-apply-cli]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "constitution-plane schema pattern constraint as upstream defense-in-depth alongside (not instead of) a runtime confinement check"
    - "both derived-plane artifacts (.memory/derived/contracts-index.md AND the memory_regen .ambr snapshot) regenerated and committed together, never split across commits"

key-files:
  created:
    - tools/adoption_apply/tests/test_manifest_schema_conformance.py
  modified:
    - contracts/harness/adoption/manifest.schema.json
    - contracts/.hashes/manifest.json
    - .memory/derived/contracts-index.md
    - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr

key-decisions:
  - "Human ratified 2026-07-21 during /gsd:execute-phase 27.1, via explicit in-session confirmation of the schema diff, the hash rebaseline, the paired derived-plane regeneration, and the disposition table. HARNESS_DEV_BYPASS (already active in this dev session's .claude/settings.local.json, per Phase 26.1/26.2 precedent) enabled the mechanical writes only — it was never conflated with the human ratification."
  - "docs/reference/manifest.md regenerated with zero diff: docs_sync's property table renders only top-level schema properties, not $defs-level pattern constraints — a factual limitation of the doc renderer, not a bug requiring a fix in this plan's scope."
  - "harness/skills/brownfield-adoption/SKILL.md's Stage-5 claim (\"promotion ratifies those decisions, and only then is the batch safely applied to the target\") required no doc correction — 27.1-02's check_valid wiring already made the claim literally true before this plan started."

patterns-established:
  - "A constitution-plane edit's full ritual (schema edit -> hash rebaseline -> drift check -> BOTH derived artifacts regenerated) lands in one atomic commit, gated by a blocking human-verify checkpoint the agent never self-approves."

requirements-completed: [ADOPT-05, ADOPT-06]

# Metrics
duration: 25min
completed: 2026-07-21
---

# Phase 27.1 Plan 03: Schema Path-Shape Constraint + SC-5 Disposition Table Summary

**`manifest.schema.json`'s `destination`/`path` fields now reject absolute paths and `..` path segments at the schema layer (WR-03), rebaselined and derived-plane-regenerated in one human-ratified commit, closing out Phase 27.1's SC-4/SC-5 with a complete eight-item disposition table.**

## Performance

- **Duration:** ~25 min (RED commit to human ratification)
- **Tasks:** 2 auto tasks + 1 blocking human-verify checkpoint
- **Files modified:** 5 (1 created, 4 modified)

## Accomplishments

- Closed WR-03: `manifest.schema.json`'s `dispositionRecord.destination`, `excludedDestinationRecord.destination`, and `evidenceRef.path` all gain `"pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"` — rejecting absolute paths and `..` path segments while still accepting a literal `..` substring inside a filename (e.g. `foo/bar..json`), proven by a new data-based schema-conformance test suite.
- Rebaselined `contracts/.hashes/manifest.json` via `tools.contract_hash.hash --write` — exactly one entry changed (`manifest.schema.json`).
- Regenerated **both** derived-plane artifacts together in the same commit: `.memory/derived/contracts-index.md` and `tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr` — the exact Pitfall-2 companion-artifact coupling Phase 27's own 27-02 forgot, not repeated here.
- `uv run python -m tools.contract_drift.drift` clean (exit 0); `uv run python -m tools.harness_emit.generate && git diff --exit-code -- .opencode .claude` clean no-op (this phase touched no `harness/` source doc); `uv.lock` unchanged.
- Full workspace suite green: **1129 passed** (up from the 1119-passed baseline after 27.1-01/27.1-02; +10 new schema-conformance tests).
- Human-ratified the constitution-plane diff at a blocking checkpoint — the agent did not self-approve. The orchestrator independently re-verified the diff, the paired derived-artifact commit, a direct regex probe against a wider adversarial/legitimate set, and the drift/memory_regen gates before ratifying.
- Closed out Phase 27.1's SC-5 with the complete seven-finding (plus one research-found) disposition table below.

## Task Commits

Each task was committed atomically:

1. **Task 1: RED — schema-conformance test for the new `destination`/`path` pattern constraint** - `5b343af` (test)
2. **Task 2: GREEN — land the pattern constraint, rebaseline hash, verify drift, regenerate BOTH derived-plane artifacts** - `f6be4f1` (fix)
3. **Task 3: Checkpoint — human ratification** - approved 2026-07-21, no additional code/schema changes; ratified in-session (no separate commit)

**Plan metadata:** (this commit)

## Files Created/Modified

- `tools/adoption_apply/tests/test_manifest_schema_conformance.py` - new schema-validation test file mirroring `test_schema_conformance.py`'s `Draft202012Validator` loader idiom; asserts the pattern rejects the adversarial destination/path set and accepts the legitimate set across all three constrained fields
- `contracts/harness/adoption/manifest.schema.json` - added `pattern` to `evidenceRef.properties.path`, `dispositionRecord.properties.destination`, `excludedDestinationRecord.properties.destination`; nothing else changed
- `contracts/.hashes/manifest.json` - rebaselined via `tools.contract_hash.hash --write`; exactly one entry changed
- `.memory/derived/contracts-index.md` - regenerated; `manifest.schema.json` row's hash_prefix updated
- `tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr` - regenerated via `--snapshot-update`, moved in the SAME commit as the schema edit and the index regen

## Decisions Made

See `key-decisions` in frontmatter. Notably:
- Human ratification (2026-07-21) is recorded distinctly from the mechanical `HARNESS_DEV_BYPASS`-enabled write — the bypass never substitutes for or is conflated with ratification.
- No doc correction was needed for `brownfield-adoption/SKILL.md`'s Stage-5 claim; 27.1-02 already made it true.
- The `docs/reference/manifest.md` zero-diff outcome is a documented renderer limitation (top-level properties only), not a defect in this plan's scope.

## Deviations from Plan

None — plan executed exactly as written. One factual note recorded above (docs/reference/manifest.md regenerating with zero diff) was anticipated as a possible outcome in the plan's own risk framing and required no corrective action.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## SC-5: Seven-Finding Disposition Table (27-REVIEW.md)

| Finding | Disposition | Evidence |
|---------|-------------|----------|
| CR-01 (constitution refusal bypassed by `..`/case-variant destination) | fixed | `refuse_unsafe_destination` (27.1-01) resolves, confines, and case-insensitively classifies the destination before any write; `test_apply_disposition_refuses_hostile_destinations_end_to_end` proves it against the full `HOSTILE_DESTINATIONS` table |
| CR-02 (no destination confinement in apply mode) | fixed | `refuse_unsafe_destination` calls the existing `refuse_if_outside_root` for every apply-mode write, not only draft (27.1-01); `test_absolute_destination_refused_zero_writes`/`test_traversal_destination_refused_zero_writes` prove zero filesystem calls occur |
| CR-03 (`approval.check_valid` never consulted by apply) | fixed | `_cmd_apply` now calls `check_valid` as a hard precondition, refusing with exit 4 (27.1-02); `test_cli_apply_refuses_without_approval`/`test_cli_apply_refuses_on_stale_approval` prove it |
| WR-01 (unlocked `_apply_marker_merge` read-modify-write) | fixed | `fcntl.flock`-guarded critical section mirroring `batch.py::update_status` (27.1-01); `test_concurrent_marker_merge_does_not_lose_writes` proves it |
| WR-02 (`check_valid` raises instead of returning `False`) | fixed | `try/except (FileNotFoundError, OSError): return False` around the draft-hash recomputation (27.1-02); `test_valid_false_on_missing_draft_artifact` proves it |
| WR-03 (no path-shape constraint on `destination`/`path` in the schema) | fixed | `manifest.schema.json`'s `destination`/`path` fields gain the `pattern` constraint (this plan); `test_manifest_schema_rejects_unsafe_destination` proves it |
| WR-04 (`_cmd_apply` never re-validates the manifest against its schema) | fixed | `_cmd_apply` calls the existing `_validate("manifest", manifest)` helper before dispatching to `apply_manifest` (27.1-02); `test_cli_apply_refuses_on_malformed_manifest` proves it |

**Eighth item, recorded for completeness (not one of the seven required 27-REVIEW.md findings, found during this phase's research):** a symlink-read information-leak in `_apply_marker_merge` — reading through a symlinked marker-capable destination could silently splice foreign or constitution-plane content into the merge. Disposition: **fixed** in 27.1-01 via an `O_NOFOLLOW` read primitive (`_read_target_no_symlink`/`SymlinkRefusal`), proven by `test_marker_merge_refuses_symlink_read`. New scope beyond `27-REVIEW.md`'s literal findings, disposed of in the same phase since it shares the exact module/test files already being edited.

**Ratification record:** Human ratified 2026-07-21 during `/gsd:execute-phase 27.1`, via explicit in-session confirmation ("approved") of the schema diff, the single-entry hash rebaseline, the paired derived-plane regeneration (both `.memory/derived/contracts-index.md` and the `memory_regen` `.ambr` snapshot moved together in commit `f6be4f1`), and this disposition table. `HARNESS_DEV_BYPASS` (already active in this dev session) enabled the mechanical writes only and was never conflated with the ratification itself. The orchestrator independently re-verified `git show --stat f6be4f1` (exactly 4 files, one atomic commit), ran a direct regex probe against a wider adversarial/legitimate set (`/etc/passwd`, `../etc/passwd`, `a/../contracts/x.json`, `a/..`, `..` rejected; `contracts/x.json`, `src/widget.py`, `foo/bar..json`, `..hidden`, `a..b/c` accepted), and confirmed `tools.contract_drift.drift` exit 0 and `uv run pytest tools/memory_regen -q` → 84 passed — independently, not solely from this agent's summary.

## Next Phase Readiness

- Phase 27.1's SC-1..SC-5 are all satisfied: SC-1/SC-2 (27.1-01), SC-3 (27.1-02), SC-4/SC-5 (this plan).
- `manifest.schema.json` now provides upstream, schema-layer defense-in-depth alongside `apply.py`'s runtime confinement checks — a hand-edited or corrupted `manifest.json` between draft and apply is caught at the shape level even if the runtime checks were somehow bypassed.
- Full workspace suite green (1129 passed), contract-drift clean, both derived-plane artifacts current, emit round-trip a clean no-op — Phase 27.1 is ready to close.
- ADOPT-05/ADOPT-06 (already marked Complete under Phase 27 in REQUIREMENTS.md) are reaffirmed by this phase's hardening; no new requirement IDs introduced.

---
*Phase: 27.1-adoption-safety-path-normalization-bypass-apply-mode-confine*
*Completed: 2026-07-21*

## Self-Check: PASSED
