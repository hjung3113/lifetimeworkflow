---
phase: 27.1-adoption-safety-path-normalization-bypass-apply-mode-confine
plan: 02
type: execute
wave: 2
depends_on: ["27.1-01"]
files_modified:
  - tools/adoption_apply/approval.py
  - tools/adoption_apply/cli.py
  - tools/adoption_apply/tests/test_approval_invalidation.py
  - tools/adoption_apply/tests/test_cli.py
autonomous: true
requirements: [ADOPT-05, ADOPT-06]

must_haves:
  truths:
    - "SC-3: `apply` refuses (does not write) when a batch was never promoted, and when a prior approval is stale on any of `(draft_hash, task_revision, git_ref)`"
    - "SC-3: `brownfield-adoption/SKILL.md`'s claim that promotion gates the apply is TRUE after this plan lands — `_cmd_apply` actually consults `approval.check_valid` before doing any work"
    - "WR-02: `check_valid` returns `False` (never raises) when a batch's draft artifact is missing or the batch directory is incomplete"
    - "WR-04: `_cmd_apply` re-validates `manifest.json` against `manifest.schema.json` before use; a schema-invalid manifest is refused with a clean stderr message, not an unhandled traceback"
    - "Plan-checker BLOCKER closure: `_cmd_apply`'s except tuple also catches `apply_module.PathEscapeError` and `apply_module.SymlinkRefusal` (both introduced by 27.1-01) so a hostile-destination manifest — SC-2's exact trigger case — surfaces as the same clean, documented nonzero exit every other apply-time failure gets, never an unhandled traceback"
    - "D-02 upheld: `check_valid`'s `repo_root` in `_cmd_apply` is the harness's OWN checkout root (a new required `--repo-root` CLI flag, mirroring `promote`'s), not the brownfield `--target` being adopted into — this rationale is recorded in the SUMMARY"
  artifacts:
    - path: "tools/adoption_apply/cli.py"
      provides: "`apply` subparser gains a required `--repo-root` argument; `_cmd_apply` calls `approval.check_valid(args.task_dir, args.batch_id, args.repo_root)` before any work and returns exit 4 on failure; re-validates `manifest.json` via `_validate(\"manifest\", manifest)` before dispatching to `apply_manifest`, returning exit 1 on schema failure; the existing `except (apply_module.ConstitutionRefusal, apply_module.ConcurrentDriftError, apply_module.UnknownDispositionError)` tuple around `apply_manifest` is widened to also catch `apply_module.PathEscapeError` and `apply_module.SymlinkRefusal`, returning the same exit 1 with a clean stderr message (no new exit code introduced for this case)"
      contains: "check_valid(args.task_dir, args.batch_id, args.repo_root)"
    - path: "tools/adoption_apply/approval.py"
      provides: "`check_valid` catches `FileNotFoundError`/`OSError` from `_recompute_draft_hash` and returns `False` instead of propagating"
      contains: "except (FileNotFoundError, OSError)"
    - path: "tools/adoption_apply/tests/test_cli.py"
      provides: "New cases: apply refuses without approval (exit 4, zero files written), apply refuses on stale approval (exit 4), apply refuses on schema-invalid manifest (exit 1), apply refuses cleanly (exit 1, no traceback) on a hostile destination (absolute, `..`-traversal, and a symlinked marker-capable target) after a valid promotion; existing `test_cli_apply_end_to_end` updated to promote before applying (now required) and to pass `--repo-root`"
      contains: "def test_cli_apply_refuses_without_approval"
    - path: "tools/adoption_apply/tests/test_approval_invalidation.py"
      provides: "New case: `check_valid` on a batch missing a draft artifact returns `False`, does not raise"
      contains: "def test_valid_false_on_missing_draft_artifact"
  key_links:
    - from: "tools/adoption_apply/cli.py::_cmd_apply"
      to: "tools/adoption_apply/approval.py::check_valid"
      via: "direct function call, first statement of _cmd_apply, before manifest_path.is_file() check"
      pattern: "check_valid\\("
    - from: "tools/adoption_apply/cli.py::_cmd_apply"
      to: "tools/adoption_apply/cli.py::_validate"
      via: "reuses the existing _validate(name, document) helper _cmd_draft already calls"
      pattern: "_validate\\(\"manifest\""
    - from: "tools/adoption_apply/cli.py::_cmd_apply"
      to: "tools/adoption_apply/apply.py::PathEscapeError, tools/adoption_apply/apply.py::SymlinkRefusal"
      via: "widened except tuple around the existing apply_manifest call — both exception types are new in 27.1-01, this plan lands second (depends_on 27.1-01) specifically so the CLI learns about them in the same phase, closing the plan-checker BLOCKER"
      pattern: "PathEscapeError"
---

<objective>
Close CR-03 from `27-REVIEW.md`: nothing in `cli.py`'s `apply` subcommand ever consults
`approval.check_valid`, so a batch can be applied whether or not a human ever promoted it — directly
contradicting `brownfield-adoption/SKILL.md`'s documented claim that promotion gates the apply.
Wire `check_valid` into `_cmd_apply` as a hard precondition (mirroring `promote`'s existing exit-3
refusal idiom with a new, distinct exit code 4 per RESEARCH's Assumption A2). In the same module,
close WR-02 (`check_valid` raising instead of returning `False` on a missing draft artifact) and
WR-04 (`_cmd_apply` never re-validates `manifest.json` against its schema before use, unlike
`_cmd_draft`).

Also close a plan-checker BLOCKER found against this plan set: `_cmd_apply`'s except tuple
(`ConstitutionRefusal`, `ConcurrentDriftError`, `UnknownDispositionError`) never learns about
`PathEscapeError`/`SymlinkRefusal` — the two new exceptions 27.1-01 introduces. Without this fix,
running `python -m tools.adoption_apply apply` against a manifest with an absolute or
`..`-traversal destination (SC-2's exact trigger case) produces a raw, unhandled traceback instead
of the clean refusal every other failure mode already gets. This plan depends on 27.1-01 (moved to
a later wave) specifically so this fix — and its CLI-level regression test — can land honestly in
the plan that already owns `cli.py`, rather than leaving a cross-plan hazard in a shared wave.

Purpose: the human-ratification gate (`ADOPT-06`) that Plan 27-04 spent an entire plan building is
currently decorative on the apply path — an agent or bare CLI/CI caller can skip `promote` entirely.
Purpose (WR-04): a schema-invalid or tampered `manifest.json` currently surfaces as an unhandled
`KeyError` traceback rather than the clean refusal every other `cli.py` failure mode already gives.
Purpose (BLOCKER): 27.1-01's new destination-safety exceptions must be handled at every entry point
that can raise them, not just the module-level call sites already covered by 27.1-01's own tests.
Output: `_cmd_apply` refuses (exit 4) without a valid, exactly-matching approval; refuses (exit 1) on
a schema-invalid manifest OR a hostile destination; `check_valid` never raises.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-RESEARCH.md
@.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-VALIDATION.md
@.planning/phases/27-task-local-adoption-workflow-safe-application-v2-3-b/27-REVIEW.md
@harness/skills/brownfield-adoption/SKILL.md
@.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-01-PLAN.md

<interfaces>
<!-- Exact current signatures/bodies the executor needs — no codebase exploration required. -->

`tools/adoption_apply/approval.py:172-187` (`check_valid`, current body) reads: `path =
_approval_path(task_dir, batch_id)`; if not `path.is_file()` return `False`; else `stored =
json.loads(path.read_bytes())`; `batch_dir = _batch_dir(task_dir, batch_id)`; return the three-way
`and` of `stored["draft_hash"] == _recompute_draft_hash(batch_dir)`,
`stored["task_revision"] == _current_task_revision(task_dir)`,
`stored["git_ref"] == _current_git_ref(repo_root)`. `_recompute_draft_hash` (lines 57-63) calls
`path.read_bytes()` unconditionally for each of the 3 fixed `_DRAFT_FILES` names
(`inventory.json`, `plan.json`, `manifest.json`) — raises `FileNotFoundError` if any is missing.
This plan wraps the `_recompute_draft_hash(batch_dir)` call (or the whole boolean expression) in a
`try/except (FileNotFoundError, OSError): return False`.

`tools/adoption_apply/cli.py:138-177` (`_cmd_apply`, current body — the exact function this plan
edits): reads `manifest.json` via bare `json.loads(manifest_path.read_bytes())` with NO schema
validation and NO `approval.check_valid` call anywhere. The existing `try/except` around
`apply_manifest(...)` (lines 160-170) is EXACTLY:
`except (apply_module.ConstitutionRefusal, apply_module.ConcurrentDriftError,
apply_module.UnknownDispositionError) as exc: print(...); return 1`. 27.1-01 introduces
`apply_module.PathEscapeError` (raised by `refuse_unsafe_destination`/`refuse_if_outside_root`) and
`apply_module.SymlinkRefusal` (raised by `_apply_marker_merge`'s read side) — neither is in this
tuple, so both currently propagate uncaught out of `_cmd_apply`/`main`, producing a raw traceback
instead of a clean exit. This plan widens the tuple to
`(apply_module.ConstitutionRefusal, apply_module.ConcurrentDriftError,
apply_module.UnknownDispositionError, apply_module.PathEscapeError, apply_module.SymlinkRefusal)`,
keeping the same `return 1` and stderr-print behavior — no new exit code for this case; it joins the
existing exit-1 bucket alongside the other `apply_manifest`-raised faults. Compare against
`_cmd_draft` (lines 72-117), which DOES call `_validate(name, document)` (defined lines 57-65,
reusing `Draft202012Validator(schema).iter_errors(document)`) for all three draft artifacts before
writing.

`tools/adoption_apply/cli.py:180-199` (`_cmd_promote`, the exit-3 idiom to mirror for the NEW exit-4
refusal): catches `AdoptionApprovalRefused`, prints it, returns `3`.

`tools/adoption_apply/cli.py:214-218` (current `apply` subparser — the exact args to extend):
`apply_parser.add_argument("--task-dir", ...)`; `--batch-id`; `--target`. This plan adds
`apply_parser.add_argument("--repo-root", type=Path, required=True)` — mirroring `promote_parser`'s
own `--repo-root` (line 223), per D-02 (locked this session): `check_valid`'s `repo_root` binds to
the ADOPTING repo's own task-control state (the harness's own checkout, `_REPO_ROOT` semantics),
NEVER the brownfield `--target` tree being adopted into, because `git_ref`/`task_revision` are
harness-side concepts and a brownfield target may not even be a git repo.

`tools/adoption_apply/approval.py:172` — `check_valid(task_dir: Path, batch_id: str, repo_root:
Path) -> bool` — import this into `cli.py` (currently NOT imported there at all — `cli.py` imports
`AdoptionApprovalRefused` and `promote` from `approval.py`, but not `check_valid`).

`tools/adoption_apply/apply.py`'s `PathEscapeError` and `SymlinkRefusal` (both defined by 27.1-01,
which this plan depends on and executes after): `cli.py` already imports the module as
`apply_module` (`from tools.adoption_apply import apply as apply_module`), so both new exceptions
are reachable as `apply_module.PathEscapeError`/`apply_module.SymlinkRefusal` with no new import
statement needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — CLI-boundary approval-gate, schema-revalidation, and hostile-destination regression tests, plus the missing-draft-artifact WR-02 case</name>
  <files>
    tools/adoption_apply/tests/test_cli.py,
    tools/adoption_apply/tests/test_approval_invalidation.py
  </files>
  <read_first>
    tools/adoption_apply/cli.py (full — current `_cmd_apply`/`_cmd_promote`/`main`/argparse wiring),
    tools/adoption_apply/approval.py (full — `check_valid`/`_recompute_draft_hash`/`promote`),
    tools/adoption_apply/apply.py (full, POST-27.1-01 state — `refuse_unsafe_destination`,
      `PathEscapeError`, `SymlinkRefusal` all exist and are wired by the time this plan executes),
    tools/adoption_apply/tests/test_cli.py (full — existing fixtures `git_repo`/`task_dir`/
      `_write_state`/`_seed_batch_dir`/`decisions_path`/`synthetic_target`; existing
      `test_cli_apply_end_to_end` at the bottom, which currently applies WITHOUT promoting first —
      this test's behavior changes are this task's responsibility to update, not merely add
      alongside),
    tools/adoption_apply/tests/test_approval_invalidation.py (full — existing fixtures
      `git_repo`/`task_dir`/`_write_draft`/`_seed_batch`; the idiom `test_invalidated_on_*` uses for
      single-axis invalidation),
    27.1-RESEARCH.md "Pattern 3" (the CLI-boundary approval-gate exit-code idiom), "Assumption A2"
      (exit code 4 is unused/free), and "Open Question 2" (`--repo-root` flag decision, resolved by
      D-02 in this plan's `<interfaces>` block — do not re-litigate, just implement it)
  </read_first>
  <behavior>
    In `test_approval_invalidation.py`, add `test_valid_false_on_missing_draft_artifact`: seed a
    batch via `_seed_batch(task_dir, git_repo)`, promote it successfully (same pattern as
    `test_valid_when_nothing_changed`), then delete one of the three draft files (e.g.
    `batch._batch_dir(task_dir, batch_id) / "plan.json"`) via `Path.unlink()`; assert
    `check_valid(task_dir, batch_id, git_repo) is False` — NOT `pytest.raises(...)`. This is RED
    against the current `approval.py` (it currently raises `FileNotFoundError` instead).

    In `test_cli.py`, add `--repo-root` to every `main(["apply", ...])` invocation this task writes
    or updates (the `apply` subparser does not accept it yet — this is scaffolding needed for the
    new tests to even parse; adding the flag to the parser itself is Task 2's job, so until Task 2
    lands, any test that passes `--repo-root` to `apply` fails with an argparse "unrecognized
    arguments" error — this IS an acceptable RED state, structurally proving the flag is missing).

    Add `test_cli_apply_refuses_without_approval`: draft a batch (no promote step), call
    `main(["apply", "--task-dir", ..., "--batch-id", ..., "--target", str(apply_target),
    "--repo-root", str(git_repo)])`; assert `exit_code == 4`; assert the apply target directory
    contains none of the manifest's `create`-disposition destinations (zero-write proof at the CLI
    level, mirroring Plan 27.1-01's spy-based proof at the module level).

    Add `test_cli_apply_refuses_on_stale_approval`: draft, promote successfully (record the
    approval), then advance the task's `state.json` revision (a helper mirroring `_bump_revision`
    from `test_approval_invalidation.py`, or a second commit in `git_repo` moving `HEAD`), then call
    `apply` with the SAME `--repo-root`; assert `exit_code == 4`.

    Add `test_cli_apply_refuses_on_malformed_manifest`: manually construct a batch directory (via
    `batch.create_or_resume_batch` + hand-written `inventory.json`/`plan.json` + a `manifest.json`
    that is valid JSON but schema-invalid — e.g. omits the required `"excluded"` key, or contains a
    `dispositions[0].disposition` value outside the 6-value enum), promote it successfully (the
    approval hash binds to whatever bytes exist, valid or not — `check_valid` reports `True`), then
    call `apply`; assert `exit_code == 1` and that stderr mentions schema validation (not an
    unhandled traceback).

    Add `test_cli_apply_refuses_hostile_destination_cleanly`, parametrized over three cases: (1) an
    absolute destination built from a synthetic, guaranteed-nonexistent path under `tmp_path`
    (mirroring 27.1-01's `PATH_ESCAPE_DESTINATIONS` convention — never a literal system path like
    `/etc/passwd`); (2) a `..`-traversal destination (e.g. `"../outside-marker/widget.txt"`); (3) a
    symlinked marker-capable target (create a real file elsewhere under `tmp_path`, then a symlink at
    a marker-capable destination path pointing at it, matching 27.1-01's
    `test_marker_merge_refuses_symlink_read` fixture shape). For each case: draft a batch whose
    manifest contains a single disposition record using that hostile destination, promote it
    successfully (schema validation does not yet constrain destination shape — that lands in
    27.1-03 — so drafting/promoting a hostile-destination manifest succeeds at this point in the
    phase), then call `apply` with a valid `--repo-root`; assert `exit_code == 1` (the same bucket
    `ConstitutionRefusal`/`ConcurrentDriftError`/`UnknownDispositionError` already use); assert
    stderr contains a clean, documented refusal message; assert stderr/stdout do NOT contain the
    string `"Traceback"` (the structural proof this is a handled refusal, not an unhandled
    exception); assert the apply target directory contains none of the manifest's `create`/
    `marker-merge` destinations (zero-write proof). Pre-fix (before Task 2 widens the except tuple),
    this test is RED because `PathEscapeError`/`SymlinkRefusal` propagate uncaught out of `main()`,
    producing an actual Python traceback on stderr — the `"Traceback"` absence assertion is what
    fails, which is the honest RED reason for this specific plan-checker BLOCKER fix.

    Update the existing `test_cli_apply_end_to_end` to (1) add a `promote` call between `draft` and
    `apply` (approve/decisions/confirm exactly as `test_cli_promote_succeeds_with_full_human_signals`
    already does, reusing the `decisions_path` fixture and `monkeypatch.setenv(HUMAN_TOKEN_ENV,
    ...)`), and (2) pass `--repo-root str(task_dir.parent)` (the `git_repo` fixture's root — the
    harness/task's own repo, D-02) to the `apply` invocation. RATIONALE (record this verbatim in
    27.1-02-SUMMARY.md, per D-01): D-01's "no Phase 27 test may be weakened or deleted" targets the
    CR-01/CR-02 refusal-behavior tests (`test_refuses_before_mutation`,
    `test_refuses_bare_cli_invocation`, `test_non_constitution_destination_allowed`) — tests whose
    entire purpose is proving a refusal happens. `test_cli_apply_end_to_end` is a wiring/integration
    test proving the HAPPY PATH still works end-to-end; it is not one of D-01's protected refusal
    tests. Adding the promote step and `--repo-root` flag here is a REQUIRED STRENGTHENING forced by
    closing CR-03: once `_cmd_apply` hard-refuses an unpromoted apply (Task 2), this test would
    otherwise start failing with exit 4 — not because the test's own assertions weakened, but
    because the CLI's contract legitimately grew a new precondition. No existing assertion in this
    test is removed or loosened; only the promote step and the new required flag are added.
  </behavior>
  <action>
    Add the four new test functions to `test_cli.py` (`test_cli_apply_refuses_without_approval`,
    `test_cli_apply_refuses_on_stale_approval`, `test_cli_apply_refuses_on_malformed_manifest`,
    `test_cli_apply_refuses_hostile_destination_cleanly`) and update `test_cli_apply_end_to_end` in
    place to add the promote step, `--repo-root` flag, and the D-01 rationale comment (as a docstring
    or inline comment directly above the modified assertions, so the rationale travels with the code,
    not only the plan). Add `test_valid_false_on_missing_draft_artifact` to
    `test_approval_invalidation.py`. Every new/updated test calls the real `main()`/`check_valid`
    entry points — never a bypassed internal shortcut.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_approval_invalidation.py tools/adoption_apply/tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_approval_invalidation.py
    tools/adoption_apply/tests/test_cli.py -q` reports failures for every new/updated test (argparse
    "unrecognized arguments" for the `--repo-root`-passing tests, `FileNotFoundError` instead of a
    clean `False` for the missing-draft-artifact test, `exit_code == 0` instead of the expected
    refusal code for the no-approval/stale-approval/malformed-manifest tests, an actual
    `"Traceback"` string on stderr instead of a clean exit for the hostile-destination test) — this
    is the required RED state, and each failure is traceable to a documented cause, not an accident
    of platform filesystem state. All pre-existing tests in both files that this task did not
    intentionally modify still pass unmodified.
  </acceptance_criteria>
  <done>Both test files contain the full CR-03/WR-02/WR-04/BLOCKER regression suite; every new/updated assertion fails against the current shipped `approval.py`/`cli.py` for the expected, documented reason; the D-01 rationale for touching `test_cli_apply_end_to_end` is recorded in the test file itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — wire `check_valid` into `_cmd_apply`, add `--repo-root`, re-validate the manifest schema, widen the except tuple to cover `PathEscapeError`/`SymlinkRefusal`, fix `check_valid`'s missing-artifact case</name>
  <files>tools/adoption_apply/approval.py, tools/adoption_apply/cli.py</files>
  <read_first>
    tools/adoption_apply/approval.py (full, current state),
    tools/adoption_apply/cli.py (full, current state),
    Task 1's new/updated tests — the exact exit codes and stderr expectations they assert
  </read_first>
  <behavior>
    `approval.py::check_valid`: wrap the call to `_recompute_draft_hash(batch_dir)` (or the entire
    returned boolean expression) in a `try/except (FileNotFoundError, OSError): return False` —
    closes WR-02, "no partial credit — any mismatch (including a missing artifact) returns `False`,"
    matching the function's own existing docstring promise.

    `cli.py`: add `apply_parser.add_argument("--repo-root", type=Path, required=True)` to the
    `apply` subparser (mirrors `promote_parser`'s existing `--repo-root`). Import `check_valid` from
    `tools.adoption_apply.approval` alongside the existing `AdoptionApprovalRefused`/`promote`
    imports. In `_cmd_apply`, as the FIRST statement (before the current
    `manifest_path.is_file()` check): call `check_valid(args.task_dir, args.batch_id,
    args.repo_root)`; if `False`, print a clear refusal message to stderr naming the batch id and
    instructing the caller to run `promote` first, and `return 4`. After the existing
    `manifest_path.is_file()` guard and `manifest = json.loads(manifest_path.read_bytes())` line,
    add a schema-revalidation step reusing the module's existing `_validate("manifest", manifest)`
    helper (the same one `_cmd_draft` already calls for all three artifacts); on a non-`None` error,
    print a clear stderr message naming the validation error and `return 1`, before ever touching
    `payloads`/`block_bodies`/`apply_manifest`. Widen the existing
    `except (apply_module.ConstitutionRefusal, apply_module.ConcurrentDriftError,
    apply_module.UnknownDispositionError)` tuple around the `apply_manifest(...)` call to also
    include `apply_module.PathEscapeError` and `apply_module.SymlinkRefusal` — same `return 1`,
    same stderr-print behavior, no new exit code. This closes the plan-checker BLOCKER: a
    hostile-destination manifest (absolute, `..`-traversal, or a symlinked marker target) now
    surfaces as the same clean exit 1 every other `apply_manifest`-raised fault already gets.
  </behavior>
  <action>
    Implement the `approval.py::check_valid` fix, the `cli.py` `--repo-root` argument +
    `check_valid` precondition + manifest schema re-validation, and the widened except tuple exactly
    as specified in `<behavior>`. Do not change `_cmd_draft`, `_cmd_promote`, `promote_parser`, or
    `draft_parser`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_apply/tests/test_approval_invalidation.py tools/adoption_apply/tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_apply/tests/test_approval_invalidation.py
    tools/adoption_apply/tests/test_cli.py -q` is fully green, including
    `test_cli_apply_refuses_hostile_destination_cleanly`'s three parametrized cases (no `Traceback`
    string on stderr for any of them). `uv run pytest tools/adoption_apply -q` (the whole package)
    is green — no regression in `test_constitution_refusal.py`/`test_atomic_apply.py`/
    `test_batch_layout.py`/`test_fixtures.py`. `uv run pytest -q` (full workspace suite) reports no
    new failures relative to the 1096-passed baseline recorded in `27.1-VALIDATION.md`.
  </acceptance_criteria>
  <done>`_cmd_apply` refuses (exit 4) without a valid, current approval and refuses (exit 1) on a schema-invalid manifest OR a hostile destination (no unhandled traceback for the latter); `check_valid` never raises; `--repo-root` mirrors `promote`'s own flag with the D-02 rationale recorded in this plan and repeated in the SUMMARY; the full workspace suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent/bare-CLI/CI caller → `python -m tools.adoption_apply apply` | A caller with no Claude hook in the loop and, before this plan, no enforced requirement that a human ever reviewed the batch. |
| `manifest.json` (on-disk, possibly hand-edited/corrupted between draft and promote) → `_cmd_apply` | Untrusted structured input previously trusted verbatim with a bare `json.loads` and no schema check. |
| `manifest.json`'s hostile `destination` values → `_cmd_apply`'s exception handling | Even with 27.1-01's runtime refusal in place, the CLI boundary must translate a security-relevant exception into a clean, documented failure — not a raw traceback that could leak paths/stack internals to a caller. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-05 | Elevation of Privilege / Confused Deputy | `cli.py::_cmd_apply` | mitigate | `check_valid(args.task_dir, args.batch_id, args.repo_root)` is now a hard precondition, refusing with exit 4 when no valid, exactly-matching approval exists — closes CR-03. |
| T-27.1-06 | Denial of Service (crash, not security bypass) | `approval.py::check_valid` on an incomplete batch directory | mitigate | Wrapped in `try/except (FileNotFoundError, OSError): return False` — closes WR-02, matches the function's own documented "no partial credit" contract. |
| T-27.1-07 | Tampering | `cli.py::_cmd_apply` trusting an unrevalidated `manifest.json` | mitigate | Re-validates against `manifest.schema.json` via the existing `_validate` helper before any disposition is applied — closes WR-04. |
| T-27.1-11 | Information Disclosure (crash, not a bypass of 27.1-01's refusal) | `cli.py::_cmd_apply`'s exception handling around `apply_manifest` | mitigate | Except tuple widened to include `apply_module.PathEscapeError`/`apply_module.SymlinkRefusal`, converting an unhandled traceback into the same clean exit-1 refusal every other `apply_manifest` fault already gets — closes the plan-checker BLOCKER (write is already blocked by 27.1-01; this closes the CLI-level crash/leak surface). |
| T-27.1-SC | Tampering (supply chain) | N/A | accept | Zero new package installs — stdlib + existing `jsonschema` dependency only; no slopcheck required. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_apply -q` green.
- `uv run pytest -q` (full workspace suite) green, no regression vs. the 1096-passed baseline.
- `_cmd_apply` returns 4 for an unpromoted or stale-approval batch, 1 for a schema-invalid manifest, 1 for a hostile-destination manifest (clean message, no traceback), 0 for a valid promoted batch with a schema-valid, non-hostile manifest.
</verification>

<success_criteria>
- SC-3 (both halves: the approval-gate consultation, and the SKILL.md claim becoming literally true) is proven by data-based cases that failed against the shipped Phase 27 code before this plan's fix landed.
- WR-02 and WR-04 are fixed in the same modules, with regression coverage.
- The plan-checker BLOCKER (uncaught `PathEscapeError`/`SymlinkRefusal` at the CLI boundary) is fixed with CLI-level regression coverage, proven by an absent `"Traceback"` string on stderr.
- D-02 upheld: `check_valid`'s `repo_root` in `_cmd_apply` is the harness's own checkout root via a required `--repo-root` flag, not the brownfield target.
</success_criteria>

<output>
Create `.planning/phases/27.1-adoption-safety-path-normalization-bypass-apply-mode-confine/27.1-02-SUMMARY.md` when done.
</output>
