---
phase: 02-two-plane-memory-rules
plan: 05
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - AGENTS.md
  - libs/python/AGENTS.md
  - libs/dotnet/AGENTS.md
  - CLAUDE.md
  - tools/memory_regen/tests/test_agents_md.py
autonomous: true
requirements: [RULES-01, RULES-02]
user_setup: []

must_haves:
  truths:
    - "Root AGENTS.md carries the monorepo map, golden-path commands, contract-first rule, and the lazy-load rule."
    - "Per-package AGENTS.md (Python + .NET) resolve nearest-wins and RESTATE the non-negotiable invariants (not inherit-only), because runtime merge semantics differ (P11)."
    - "CLAUDE.md carries a pointer to AGENTS.md without disrupting the GSD-managed sections."
  artifacts:
    - path: "AGENTS.md"
      provides: "root rules: monorepo map, golden-path (Phase-1 tools), contract-first, lazy-load"
      contains: "contract-first"
    - path: "libs/python/AGENTS.md"
      provides: "Python-local rules (uv/pytest/ruff/pyright) + restated non-negotiables"
    - path: "libs/dotnet/AGENTS.md"
      provides: ".NET-local rules (dotnet scope) + restated non-negotiables"
    - path: "CLAUDE.md"
      provides: "pointer section to AGENTS.md (GSD-managed sections untouched)"
      contains: "AGENTS.md"
    - path: "tools/memory_regen/tests/test_agents_md.py"
      provides: "structural: all three AGENTS.md exist, per-package restate invariants, CLAUDE.md pointer present"
  key_links:
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "pointer section"
      pattern: "AGENTS\\.md"
    - from: "libs/python/AGENTS.md + libs/dotnet/AGENTS.md"
      to: "root non-negotiables"
      via: "restated per-package (P11 backstop)"
      pattern: "contract-first"
---

<objective>
Author the nearest-wins `AGENTS.md` rules layer: a root `AGENTS.md` (monorepo map, golden-path commands from Phase-1 tools, contract-first rule, lazy-load rule) plus per-package `AGENTS.md` for the Python and .NET trees that RESTATE the non-negotiable invariants (P11 backstop — merge semantics differ per runtime, so never inherit-only), plus a small `CLAUDE.md` pointer to `AGENTS.md`.

Purpose: Give agents language-local rules that resolve nearest-wins, with the constitution-first / polyglot-boundary invariants restated per-package AND carried by the injector (02-02) — prose is advisory; true enforcement is Phase-4 hooks.
Output: root + two per-package `AGENTS.md`, a `CLAUDE.md` pointer section, and a structural test.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-two-plane-memory-rules/02-CONTEXT.md
@.planning/phases/02-two-plane-memory-rules/02-RESEARCH.md
@CLAUDE.md
@contracts/README.md

<interfaces>
<!-- What-goes-where (RESEARCH §"AGENTS.md Nearest-Wins — What Goes Where", P11 backstop): -->
Root AGENTS.md: full monorepo map + golden-path commands (/golden, /test, contract-check via tools.contract_drift) + contract-first + polyglot §4-5 pointer + constitution-gated + lazy-load (the mechanism the injector implements).
Per-package (libs/python, libs/dotnet): language-local rules (python: uv/pytest/ruff/pyright; dotnet: `dotnet *` scope) AND RESTATE contract-first + §4-5 boundary + constitution-gated (never inherit-only — Codex replaces vs concat).
CLAUDE.md: ADD a small pointer section to AGENTS.md — do NOT restructure the large GSD-managed CLAUDE.md or touch its managed profile block.
Golden-path commands come from Phase-1 tools: tools.contract_drift (drift gate), tools.golden_runner (/golden), tools.contract_hash.
Per-package placement (A5): libs/python + libs/dotnet are the real code trees today (components/* are placeholders); either resolves nearest-wins.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root AGENTS.md + CLAUDE.md pointer</name>
  <files>AGENTS.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (§"AGENTS.md Nearest-Wins — What Goes Where" table, P11 backstop, Pitfall 5)
    - .planning/phases/02-two-plane-memory-rules/02-CONTEXT.md (nearest-wins + independent two-plane + provisional-first invariants)
    - CLAUDE.md (identify the GSD-managed sections + profile block to NOT disturb — add only a pointer section)
    - contracts/README.md (constitution-plane prose to align contract-first wording)
  </read_first>
  <action>
    Create root `AGENTS.md` per the what-goes-where table: (1) monorepo map — polyglot layout (parser/converter=.NET, scheduler/collector=Python; language boundary = process/file/DB only, never in-process object passing); (2) golden-path commands — how to run the drift gate (`python -m tools.contract_drift.drift` / check.sh), the golden runner (tools.golden_runner), the hash baseline (tools.contract_hash), and `uv run pytest`; (3) contract-first rule — "code that disagrees with the contract is wrong; contract changes carry a golden/contract-drift gate"; (4) constitution-gated rule — agents do not write contracts/, docs/adr/, golden/ (human/CODEOWNERS-owned; runtime enforcement is Phase-4); (5) lazy-load rule — do not preload full contracts; use the injected contracts-index/repo-map pointers and read a contract only when needed (this is the mechanism the 02-02 injector implements). Then ADD a small "## Agent Rules — see AGENTS.md" pointer section to `CLAUDE.md` (a few lines pointing agents to root + per-package AGENTS.md) WITHOUT editing the GSD-managed profile block or restructuring existing sections.
  </action>
  <acceptance_criteria>
    - `AGENTS.md` exists and contains: a monorepo map, at least one golden-path command reference (tools.contract_drift / tools.golden_runner / uv run pytest), the phrase "contract-first" (or equivalent stated rule), and a lazy-load rule.
    - `CLAUDE.md` contains a pointer section referencing `AGENTS.md`; the GSD-managed profile block is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'contract-first' AGENTS.md &amp;&amp; grep -q 'tools.contract_drift\|tools.golden_runner\|uv run pytest' AGENTS.md &amp;&amp; grep -q 'AGENTS.md' CLAUDE.md</automated>
  </verify>
  <done>Root AGENTS.md carries monorepo map + golden-path + contract-first + lazy-load; CLAUDE.md points to it.</done>
</task>

<task type="auto">
  <name>Task 2: Per-package AGENTS.md (Python + .NET) restating non-negotiables + structural test</name>
  <files>libs/python/AGENTS.md, libs/dotnet/AGENTS.md, tools/memory_regen/tests/test_agents_md.py</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (§what-goes-where table rows for per-package, Pitfall 5/P11, A5 placement)
    - AGENTS.md (root — Task 1 output, to reference/restate from)
    - libs/python/pyproject.toml + libs/dotnet layout (language-local command context)
  </read_first>
  <action>
    Create `libs/python/AGENTS.md`: Python-local rules (uv workspace: `uv run pytest`, `uv add`; ruff for lint+format; pyright for types; §4-5 normalization core lives here) AND RESTATE the non-negotiables — contract-first, the polyglot §4-5 boundary invariants (encoding/BOM/LF/decimal-InvariantCulture/timezone-UTC/TSV-escape/null-vs-empty), and constitution-gated (no agent writes to contracts/adr/golden). Create `libs/dotnet/AGENTS.md`: .NET-local rules (`dotnet *` scope; Verify.XunitV3 golden/approval; JsonSchema.Net validation; boundary = CLI-spawn/file/DB per A-model) AND RESTATE the SAME non-negotiables. Both must be self-sufficient (never rely on inheritance — P11: some runtimes replace rather than concat nested AGENTS.md). Write `tools/memory_regen/tests/test_agents_md.py`: assert all three AGENTS.md exist, assert each per-package file restates the contract-first + constitution-gated invariants (substring/heading checks), and assert CLAUDE.md contains the AGENTS.md pointer.
  </action>
  <acceptance_criteria>
    - `libs/python/AGENTS.md` and `libs/dotnet/AGENTS.md` exist; each restates contract-first + constitution-gated + §4-5 boundary invariants.
    - `uv run pytest tools/memory_regen/tests/test_agents_md.py -x` exits 0.
    - Test asserts the CLAUDE.md → AGENTS.md pointer and the per-package restatement (P11 backstop), not inheritance.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_agents_md.py -x -q</automated>
  </verify>
  <done>Per-package AGENTS.md resolve nearest-wins and restate non-negotiables; structural test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisory prose (AGENTS.md) → agent behavior | rules are advisory; a runtime may drop nested rules on merge (P11) |
| CLAUDE.md edit → GSD-managed config | a pointer section is added beside GSD-owned blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Repudiation (dropped invariants, P11) | per-package AGENTS.md | mitigate | Restate non-negotiables per-package (self-sufficient) AND carry them in the non-ignorable injector (02-02); true enforcement is Phase-4 hooks — prose is advisory by design |
| T-02-13 | Tampering (config disruption) | CLAUDE.md | mitigate | Add only a small pointer section; do NOT touch the GSD-managed profile block or restructure existing sections |
| T-02-SC | Tampering | supply chain | accept | No installs in this plan |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen/tests/test_agents_md.py -x` green.
- Root + per-package AGENTS.md exist; per-package files restate invariants; CLAUDE.md points to AGENTS.md.
- Full `uv run pytest` green.
</verification>

<success_criteria>
Root AGENTS.md (monorepo map, golden-path, contract-first, lazy-load) + per-package .NET/Python AGENTS.md resolve nearest-wins with non-negotiables restated per-package (P11), and a CLAUDE.md pointer exists. (ROADMAP Phase-2 criterion 3; RULES-01, RULES-02.)
</success_criteria>

<output>
Create `.planning/phases/02-two-plane-memory-rules/02-05-SUMMARY.md` when done.
</output>
