---
phase: 02-two-plane-memory-rules
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tools/memory_regen/contracts_index.py
  - tools/memory_regen/tests/test_contracts_index.py
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
autonomous: true
requirements: [MEM-03]
user_setup: []

must_haves:
  truths:
    - "contracts-index regenerates from contracts/ + drift status on demand — delete + rerun reproduces it byte-identically."
    - "The index reuses Phase-1 tools.contract_hash.build_manifest + tools.contract_drift.run_gate — no re-implemented hashing."
    - "Each contract row carries path, kind, hash prefix, and live drift status; the output is header-marked DERIVED."
  artifacts:
    - path: "tools/memory_regen/contracts_index.py"
      provides: "scan contracts/ + reuse drift modules → .memory/derived/contracts-index.md (deterministic)"
      exports: ["index_rows", "render", "main"]
      min_lines: 40
    - path: "tools/memory_regen/tests/test_contracts_index.py"
      provides: "determinism (generate-twice byte-identical) + drift-status-correct + syrupy snapshot"
  key_links:
    - from: "tools/memory_regen/contracts_index.py"
      to: "tools.contract_hash.build_manifest / tools.contract_drift.run_gate"
      via: "import + call (no re-hash)"
      pattern: "build_manifest|run_gate"
    - from: "tools/memory_regen/contracts_index.py"
      to: ".memory/derived/contracts-index.md"
      via: "deterministic write (sorted, no timestamp)"
      pattern: "contracts-index\\.md"
---

<objective>
Build the contracts-index generator: it scans `contracts/**/*.schema.json` and REUSES the Phase-1 hashing + drift modules to emit a deterministic `.memory/derived/contracts-index.md` (path, kind, hash prefix, live drift status). This is ~80% reuse — the only new logic is row assembly, rendering, and the determinism guarantee. It replaces the "(index pending)" stub the injector reads.

Purpose: Prove the derived plane regenerates from contracts (never hand-edited) — delete + rerun is byte-identical (success criterion 2), proven by snapshot/hash, NOT git-diff (the file is gitignored).
Output: `contracts_index.py` (entrypoint `python -m tools.memory_regen.contracts_index`) + determinism/drift tests + committed syrupy snapshot.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-two-plane-memory-rules/02-CONTEXT.md
@.planning/phases/02-two-plane-memory-rules/02-RESEARCH.md
@tools/contract_hash/hash.py
@tools/contract_drift/drift.py
@contracts/README.md

<interfaces>
From tools/contract_hash/hash.py:
  build_manifest(contracts_dir=CONTRACTS_DIR) -> {rel_path: sha256}   # rel keys are "contracts/..."
  CONTRACTS_DIR, REPO_ROOT (Path); glob confined to contracts/ subtree (symlink-defended)
From tools/contract_drift/drift.py:
  run_gate(contracts_dir=CONTRACTS_DIR, baseline_path=MANIFEST_PATH) -> {"ok": bool, "drifted": [(rel, kind, cls), ...]}
Output target: .memory/derived/contracts-index.md  (gitignored — created at generate time)
Owner: no machine-readable owner today (contracts/README.md: "owner: TBD") → emit TBD, never fabricate (A3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: contracts-index generator — scan + reuse drift modules + deterministic render</name>
  <files>tools/memory_regen/contracts_index.py, tools/memory_regen/tests/test_contracts_index.py, tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr</files>
  <read_first>
    - .planning/phases/02-two-plane-memory-rules/02-CONTEXT.md (D-04 no hand-edit/DERIVED marker, D-05 contracts-index scan, D-06 derive from contracts only)
    - .planning/phases/02-two-plane-memory-rules/02-RESEARCH.md (Pattern 3, §"contracts-index row assembly", Pitfall 1 float/timestamp, Pitfall 2 git-diff trap)
    - tools/contract_hash/hash.py (build_manifest — reuse; do NOT re-hash)
    - tools/contract_drift/drift.py (run_gate — reuse for drift column)
    - tools/memory_regen/tests/conftest.py (tmp_contracts_tree fixture from 02-01)
  </read_first>
  <behavior>
    - index_rows() returns rows sorted by relative path; each row = (rel_path, kind, hash_prefix[:12], drift_status) built from build_manifest() + run_gate().
    - kind is derived from the contracts/<top>/ segment (log-specs→log-spec, normalization→normalization, reference-data→reference-data, state→state, else other).
    - drift_status is "clean" when the schema is not in run_gate()["drifted"], else "drift:<kind>:<classification>".
    - render() output starts with a "DERIVED — do not hand-edit (tools/memory_regen/contracts_index.py)" header, contains one row per contract, NO timestamp, and NO raw float.
    - Generating twice yields byte-identical output (determinism); a committed syrupy snapshot pins the render over the real contracts tree.
    - A simulated drift (tmp contracts tree with a mutated schema) is reflected in the drift_status column.
  </behavior>
  <action>
    Implement tools/memory_regen/contracts_index.py per RESEARCH §"contracts-index row assembly": import build_manifest + run_gate; `index_rows()` maps each sorted manifest key to (rel, KIND[top], hash[:12], status); `render(rows)` emits the DERIVED header + a stable table (sorted rows, no timestamp, no float — Pitfall 1); `write()` writes `.memory/derived/repo-map.md`'s sibling `.memory/derived/contracts-index.md` (mkdir parents). Owner column emits TBD (A3 — do not fabricate). Add `main()` CLI (`python -m tools.memory_regen.contracts_index`) that regenerates the file. Write test_contracts_index.py: (a) determinism — call render twice (or generate → hash → delete → regenerate) and assert byte-identical, NOT via git-diff (the path is gitignored — Pitfall 2); (b) drift correctness — using the tmp_contracts_tree fixture with one mutated schema, assert the mutated contract's row shows a drift status while others are clean; (c) a syrupy snapshot of render() over the real contracts tree committed under __snapshots__/test_contracts_index.ambr as the determinism reference.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/memory_regen/tests/test_contracts_index.py -x` exits 0 (including the syrupy snapshot).
    - `uv run python -m tools.memory_regen.contracts_index` writes `.memory/derived/contracts-index.md` beginning with the DERIVED header.
    - Delete `.memory/derived/contracts-index.md`, rerun the generator, and `sha256sum` of the two outputs match (determinism; encoded as the pytest above, not a shell one-off).
    - Generated file contains no timestamp and no `"$schema"` full-body dump (index rows only).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/memory_regen/tests/test_contracts_index.py -x -q</automated>
  </verify>
  <done>contracts-index generator regenerates deterministically from contracts/ via reused Phase-1 modules; drift status correct; snapshot committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contracts/ (constitution) → derived index | one-way read: the generator reads contracts, never writes them |
| filesystem glob → generator | glob walks contracts/ subtree (symlink-defended in reused module) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering (path traversal) | contracts scan | mitigate | Reuse build_manifest() which confines the glob to the contracts/ subtree and skips symlinks escaping it (`root not in resolved.parents`); add no new unconfined walk |
| T-02-08 | Repudiation/Integrity (P12 derived rot) | contracts-index.md | mitigate | "DERIVED — do not hand-edit" header + regenerate-on-session + determinism test (generate-twice byte-identical) |
| T-02-09 | Integrity (drift laundering) | drift status column | mitigate | Reuse run_gate() (RFC-8785-exact) — no second hashing implementation that could disagree with the Phase-1 gate |
| T-02-SC | Tampering | supply chain | accept | No new installs; deps pinned/audited in 02-01 |
</threat_model>

<verification>
- `uv run pytest tools/memory_regen/tests/test_contracts_index.py -x` green.
- Generate → hash → delete → regenerate → identical hash.
- Full `uv run pytest` green (Phase-1 drift/golden suites unaffected).
</verification>

<success_criteria>
contracts-index regenerates from contracts/ on demand, delete+rerun byte-identical, drift status live via reused Phase-1 modules, DERIVED-marked. (ROADMAP Phase-2 criterion 2; MEM-03; D-04/D-05/D-06.)
</success_criteria>

<output>
Create `.planning/phases/02-two-plane-memory-rules/02-03-SUMMARY.md` when done.
</output>
