---
phase: 12-model-adr-doc-reframe-v2-1-a
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - docs/adr/0006-process-memory-channel-and-provenance-reframe.md
  - docs/adr/README.md
autonomous: false
requirements: [MEM2-06]

must_haves:
  truths:
    - "ADR-0006 records the memory-model change (4th PROCESS/agreements channel + provenance→data-authority reframe), append-only next-number after 0005"
    - "An agent Write to docs/adr/ WITHOUT the GOLDEN_APPROVE_HUMAN token is DENIED by contract-guard — the deny is the expected, verified behavior"
    - "ADR-0006 lands ONLY via the human-ratified path (human supplies the token or writes the file) with CODEOWNERS ratifying at merge"
    - "ADR-0006 LINKS to the reworded surfaces + the agreements shape; it never restates a project decision it merely records"
  artifacts:
    - path: "docs/adr/0006-process-memory-channel-and-provenance-reframe.md"
      provides: "The MADR record of the v2.1 memory-model change (mirrors ADR-0004/0005 shape)"
      contains: "Status:"
    - path: "docs/adr/README.md"
      provides: "Index row for 0006 (append-only, no rows removed)"
      contains: "0006"
  key_links:
    - from: "docs/adr/0006-*.md"
      to: ".memory/agreements/ + the 5 reworded surfaces"
      via: "Links section (link, not restate)"
      pattern: "agreements"
    - from: "docs/adr/README.md"
      to: "docs/adr/0006-*.md"
      via: "index row"
      pattern: "0006"
---

<objective>
Author ADR-0006 recording the v2.1 memory-model change (adding the 4th committed PROCESS/agreements
channel + scoping the "provisional/distrust" framing to data-authority) and land it via the
human-ratified constitution path (SC4 / the ADR-0006 authoring portion of MEM2-06).

Purpose: ratify the model + doc change that plans 12-01/12-02 made, as an append-only MADR — the
decision record the rest of the milestone (13→14→15) builds on.

Output: `docs/adr/0006-process-memory-channel-and-provenance-reframe.md` (Status: accepted) + a new
row in `docs/adr/README.md`. CRITICAL: `docs/adr/**` is a constitution-plane path; an agent Write is
CORRECTLY DENIED by `contract-guard` unless a human supplies `GOLDEN_APPROVE_HUMAN`. This plan is
`autonomous: false` — the agent PREPARES the content and REPRODUCES the deny as expected behavior;
a HUMAN supplies the token (or writes the file) and CODEOWNERS ratifies at merge. NEVER fabricate the
token, NEVER edit the contract-guard hook, NEVER route to a non-gated path (Pitfall 5 / T-12-01).

This is the emit portion's sibling ONLY for authoring — the emit round-trip of any surface is
Phase 15. No emitter run here.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-RESEARCH.md
@.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-VALIDATION.md
@docs/adr/README.md
@docs/adr/0005-golden-comparator-structural-only.md
@harness/commands/adr.md

<interfaces>
<!-- Deny mechanics, verified in 12-RESEARCH.md §SC4 (tools/hooks/contract_guard.py:42-98):
     CONSTITUTION_GLOBS includes "docs/adr/**". A Write/Edit to that path is DENIED unless env
     GOLDEN_APPROVE_HUMAN is a non-empty, non-blank string. Empty/blank does NOT bypass. The deny is
     the ratified design (ADR-0004). Precedent: ADR-0002/0003/0005 all landed via the human token gate. -->
<!-- Next number = highest docs/adr/NNNN-*.md + 1 = 0006 (0005 is latest). MADR sections mirror
     ADR-0005: title H1, "*MADR 4.x · plane: constitution ...*", Status/Date/Deciders/Supersedes/
     Superseded-by/Complements, Context and Problem Statement, Decision Drivers, Considered Options,
     Decision Outcome, Consequences, Links. Append-only: add a docs/adr/README.md index row, remove none. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft ADR-0006 content + prepare the index row (agent-side, pre-gate)</name>
  <files>.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md (durable scratch — the ONLY file Task 1 writes; NOT under docs/adr/ so contract-guard does not gate it), docs/adr/0006-process-memory-channel-and-provenance-reframe.md (final gated path — Task 1 must NOT write it), docs/adr/README.md (final gated path — Task 1 must NOT write it)</files>
  <read_first>
    - 12-RESEARCH.md §SC4 ("ADR-0006 content", "The legitimate authoring route", "Verify SC4") + the
      Security Domain table (T-12-01).
    - Format analog: `docs/adr/0005-golden-comparator-structural-only.md` (full MADR shape incl. the
      header block + `## Links`) and `docs/adr/README.md:28-36` (index table + "Do not remove rows").
    - `harness/commands/adr.md` (append-only convention: next-number, never edit an existing ADR).
    - The change being recorded: plans 12-01/12-02 SUMMARYs (the scaffolded agreements tier + the 5
      reworded surfaces).
  </read_first>
  <action>
    Confirm next number = 0006 (highest `docs/adr/NNNN-*.md` is 0005). Draft
    `docs/adr/0006-process-memory-channel-and-provenance-reframe.md` in the ADR-0005 MADR shape:
    H1 "# 6. Memory Model: PROCESS/Agreements Channel + Provenance→Data-Authority Reframe";
    the "*MADR 4.x · plane: constitution (human-owned, immutable, append-only)*" line; Status: accepted;
    Date: 2026-07-14; Deciders (operator-delegated, MEM2 kickoff Q1–Q6); Supersedes: —; Superseded by: —;
    Complements: the two-plane memory lineage (link ADR-0002 domain-neutrality where relevant).
    Content records: (a) adding a 4th COMMITTED, human-authored PROCESS tier `.memory/agreements/`
    (per-guideline `<slug>.md`, curated, NOT derived, NOT constitution — Q1 committed-but-writable),
    and (b) scoping the "provisional/distrust" framing to DATA-authority across the 5 surfaces.
    Decision Drivers / Considered Options / Decision Outcome / Consequences per MADR. `## Links`
    LINKS to (does NOT restate) `.memory/agreements/README.md` + `_TEMPLATE.md`, the 5 reworded
    surfaces, MEMORY-UPGRADE-PROPOSAL §7b/§7c, and the deferred Phase-13/14/15 work. Add a row
    `| [0006](0006-process-memory-channel-and-provenance-reframe.md) | ... | accepted |` to
    `docs/adr/README.md` — remove no existing row.
    Write the full drafted ADR body AND the drafted README index row into the durable scratch file
    `.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md` (this survives across
    sessions so Task 2's human landing does not depend on same-session memory). Do NOT write to
    `docs/adr/` in this task.
    STOP before landing the write to `docs/adr/` — that is the human-gated Task 2. If the executor's
    own Write to `docs/adr/` is denied by contract-guard, that DENY is the expected behavior; do not
    fabricate `GOLDEN_APPROVE_HUMAN`, do not edit the hook, do not write to a non-gated path. The
    scratch draft above IS the staged content for the human to land.
  </action>
  <acceptance_criteria>
    - Next-number correctness: `ls docs/adr/ | grep -E '^[0-9]{4}-' | sort | tail -1` shows `0005-*` as the prior latest (so 0006 is correct).
    - The drafted ADR content contains MADR sections: a Status line, "Context and Problem Statement", "Decision Outcome", and "## Links" (assert once the file lands in Task 2).
    - The drafted content LINKS to the agreements tier: it references `.memory/agreements/` (assert once landed).
    - No `GOLDEN_APPROVE_HUMAN` value is hardcoded anywhere in the plan, the ADR body, or the repo (`grep -rn 'GOLDEN_APPROVE_HUMAN=' docs/ .planning/phases/12-* ` returns no assignment with a literal value).
    - The durable scratch draft exists and carries the MADR shape: `test -f .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md` AND `grep -q "## Links" .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md` AND `grep -q ".memory/agreements/" .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md && grep -q "## Links" .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md && grep -q "\.memory/agreements/" .planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md && test "$(ls docs/adr/ | grep -E '^[0-9]{4}-' | sort | tail -1 | cut -c1-4)" = "0005" && ! grep -rn "GOLDEN_APPROVE_HUMAN=[^\"' ]" docs/ .planning/phases/12-model-adr-doc-reframe-v2-1-a/ 2>/dev/null</automated>
  </verify>
  <done>ADR-0006 content + the README index row are drafted into the durable scratch file `12-03-ADR-0006.draft.md` in the correct MADR shape with LINK-not-restate Links; the write is staged for the human-gated landing; no token fabricated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Land ADR-0006 via the human-ratified constitution path (deny is expected)</name>
  <files>docs/adr/0006-process-memory-channel-and-provenance-reframe.md, docs/adr/README.md</files>
  <read_first>
    - `.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md` (the durable staged
      ADR body + index row from Task 1 — this is the content the human lands; survives across sessions).
    - 12-RESEARCH.md §SC4 ("The deny is expected and correct", "The legitimate authoring route",
      "Verify SC4") + Pitfall 5.
    - `.planning/STATE.md:218` (Phase-12 ratification note) + `:153-157` (ADR-0002/0003/0005 landed via
      the live `GOLDEN_APPROVE_HUMAN` gate — precedent).
    - `tools/hooks/contract_guard.py:42-98` (deny mechanics) — READ ONLY, never edit.
  </read_first>
  <action>
    This task is a BLOCKING human checkpoint — the agent does NOT land the write. The agent's role is
    to (a) reproduce/confirm the contract-guard DENY on a tokenless Write to `docs/adr/` (expected
    behavior — do NOT bypass it), then PAUSE and hand off to the human, who supplies
    `GOLDEN_APPROVE_HUMAN` in their own shell (or writes the file directly) so the drafted ADR-0006 +
    the README index row land. The agent MUST NOT set the token, edit `tools/hooks/contract_guard.py`,
    use `--no-verify`, or write to a non-gated path. After the human lands the file, the agent
    re-verifies the acceptance criteria (file present, Status accepted, index row added with 0001–0005
    intact, Links reference the agreements tier, hook unmodified) and confirms CODEOWNERS routes the
    PR to the human owner at merge.
  </action>
  <what-built>
    The agent drafted ADR-0006 (Task 1). An agent Write to `docs/adr/0006-*.md` is CORRECTLY DENIED
    by contract-guard without a human-supplied `GOLDEN_APPROVE_HUMAN` token — this deny is the
    ratified design (ADR-0004), not a bug. The ADR must land via the human ratification act.
  </what-built>
  <how-to-verify>
    1. (Behavioral proof the gate holds) Confirm an agent Write to `docs/adr/` WITHOUT the token is
       denied — reproduce via the existing contract-guard test suite, e.g.
       `uv run pytest tools/hooks -k contract_guard -q` (the deny path is asserted there), OR observe
       the deny JSON when the executor attempted the Write in Task 1.
    2. (Human ratification — choose one) Copy the staged content from
       `.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-ADR-0006.draft.md`, then either: export
       the golden-approve-style token in your own shell
       (`export GOLDEN_APPROVE_HUMAN=<your ratification value>`) and let the write proceed;
       OR write/commit `docs/adr/0006-process-memory-channel-and-provenance-reframe.md` + the
       `docs/adr/README.md` index row yourself. The agent MUST NOT set the token.
    3. Confirm the landed file:
       `test -f docs/adr/0006-process-memory-channel-and-provenance-reframe.md` &&
       `grep -qi 'Status.*accepted' docs/adr/0006-*.md` &&
       `grep -q '0006' docs/adr/README.md` &&
       `grep -q 'agreements' docs/adr/0006-*.md` (Links to the tier, not a restatement).
    4. At merge, CODEOWNERS (`/docs/adr/ @hjung3113`) routes the PR to the human owner — the
       merge-time ratification backstop.
  </how-to-verify>
  <acceptance_criteria>
    - Deny reproduced: an agent/tokenless Write to `docs/adr/` is denied (contract-guard test suite green OR the deny JSON was observed) — the gate is intact and was NOT bypassed.
    - `test -f docs/adr/0006-process-memory-channel-and-provenance-reframe.md` exits 0.
    - `grep -qi 'accepted' docs/adr/0006-process-memory-channel-and-provenance-reframe.md` exits 0 (Status: accepted).
    - `grep -q '0006' docs/adr/README.md` exits 0 AND the pre-existing 0001–0005 rows are all still present (no rows removed — `for n in 0001 0002 0003 0004 0005; do grep -q "$n" docs/adr/README.md || exit 1; done`).
    - `grep -q 'agreements' docs/adr/0006-process-memory-channel-and-provenance-reframe.md` exits 0 (Links to the reworded/agreements surfaces).
    - `git diff --quiet tools/hooks/contract_guard.py` exits 0 (the hook was NOT edited to bypass — T-12-01).
    - No literal `GOLDEN_APPROVE_HUMAN` value is committed anywhere in the repo.
  </acceptance_criteria>
  <verify>
    <human-check>Human supplies GOLDEN_APPROVE_HUMAN (or writes the file); then: test -f docs/adr/0006-process-memory-channel-and-provenance-reframe.md && grep -qi accepted docs/adr/0006-*.md && grep -q 0006 docs/adr/README.md && grep -q agreements docs/adr/0006-*.md && git diff --quiet tools/hooks/contract_guard.py</human-check>
  </verify>
  <resume-signal>Type "approved" once ADR-0006 has landed via the token/human write and CODEOWNERS is set to ratify, or describe issues.</resume-signal>
  <done>ADR-0006 landed via the human-ratified path (agent Write denied as expected → human token/write), Status accepted, index row added with 0001–0005 intact, Links to the agreements tier, contract-guard hook unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → `docs/adr/**` (constitution plane) | Untrusted write attempt crosses the contract-guard gate. Deny unless a human supplies a non-empty `GOLDEN_APPROVE_HUMAN`. CODEOWNERS is the merge-time backstop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege / Repudiation | agent self-ratifies the ADR (fabricates `GOLDEN_APPROVE_HUMAN`, edits contract-guard, `--no-verify`, or writes to a non-gated path) | mitigate (BLOCKING) | Task 2 is `checkpoint:human-verify` gate=blocking-human. The deny is asserted as EXPECTED behavior; only a human supplies the token or writes the file; `git diff --quiet tools/hooks/contract_guard.py` proves the hook is unmodified; CODEOWNERS ratifies at merge; ADR is append-only (tamper-evident). |
| T-12-05 | Tampering | an existing ADR (0001–0005) is edited/removed instead of appending 0006 | mitigate | Acceptance asserts all 0001–0005 index rows still present and only 0006 is added (append-only, `/adr` convention). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (one markdown ADR + one index row; existing pytest suite only). |
</threat_model>

<verification>
- Behavioral: agent/tokenless Write to `docs/adr/` is denied (contract-guard suite green) — gate intact, not bypassed.
- `docs/adr/0006-*.md` exists, Status: accepted, MADR-shaped, Links to the agreements tier.
- `docs/adr/README.md` has a 0006 row; 0001–0005 rows all still present.
- `git diff --quiet tools/hooks/contract_guard.py` (hook unmodified); no committed `GOLDEN_APPROVE_HUMAN` value.
</verification>

<success_criteria>
- SC4: ADR-0006 records the memory-model change (append-only, next number after 0005), lands via the human-ratified path (agent Write correctly denied → human token/write + CODEOWNERS), and LINKS to (never restates) the reworded surfaces + agreements shape.
</success_criteria>

<output>
Create `.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-03-SUMMARY.md` when done. Record: the
deny was reproduced (gate intact), the human ratification method used (token vs direct write), and
the Phase-15 EMIT-DEFERRED reminder (no emitter run occurred in Phase 12).
</output>
