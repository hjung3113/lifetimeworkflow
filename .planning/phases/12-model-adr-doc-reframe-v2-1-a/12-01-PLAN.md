---
phase: 12-model-adr-doc-reframe-v2-1-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .memory/agreements/_TEMPLATE.md
  - .memory/agreements/README.md
  - .memory/README.md
  - harness/skills/two-plane-memory/SKILL.md
autonomous: true
requirements: [MEM2-01, MEM2-03]

must_haves:
  truths:
    - "A committed .memory/agreements/ tier exists (git tracks ≥1 file), documented with the per-guideline entry shape"
    - "The entry shape carries title + one-line rule + status(active/retired) + provenance stamp + added-date, and instructs LINK-to-ADR/Key-Decisions never restate (§7c)"
    - ".memory/README.md declares FOUR planes (agreements is committed + never-regenerated, a tier like state/)"
    - "The shared memory docs (.memory/README.md STATE section, two-plane-memory SKILL.md) read as data-authority, not epistemic distrust"
  artifacts:
    - path: ".memory/agreements/_TEMPLATE.md"
      provides: "The committed per-guideline entry-shape fixture (YAML frontmatter: status/added/provenance + body title/rule/Related-link)"
      contains: "provenance:"
    - path: ".memory/agreements/README.md"
      provides: "Tier doc: committed-not-derived posture + entry-shape spec + §7c link-never-restate rule"
      contains: "never restate"
    - path: ".memory/README.md"
      provides: "Four-plane table incl. PROCESS/agreements row + data-authority STATE section"
      contains: "agreements"
    - path: "harness/skills/two-plane-memory/SKILL.md"
      provides: "4th committed tier documented + data-authority reword (source edit; emit deferred to Phase 15)"
      contains: "agreements"
  key_links:
    - from: ".memory/agreements/_TEMPLATE.md"
      to: "docs/adr/ + PROJECT.md Key Decisions"
      via: "Related: link line"
      pattern: "Related:"
    - from: ".memory/README.md"
      to: ".memory/agreements/"
      via: "plane table row"
      pattern: "agreements"
---

<objective>
Scaffold the new committed, human-authored `.memory/agreements/` PROCESS memory tier (SC1/SC3) and
reframe the distrust prose to data-authority in the two SHARED memory-doc surfaces
(`.memory/README.md`, `harness/skills/two-plane-memory/SKILL.md`) that also carry the scaffold's
plane/tier documentation (part of SC2).

Purpose: establish the model + documentation foundation that the Phase-13 injector and Phase-14
`/agree` write-path build on — a per-guideline `<slug>.md` tier that is committed like `state/` (NOT
derived, never regenerated), with an entry shape that Phase-14's provenance lint can parse.

Output: `.memory/agreements/_TEMPLATE.md` + `.memory/agreements/README.md` (new committed tier),
a four-plane `.memory/README.md`, and a reworded/tier-extended two-plane-memory skill SOURCE.

Scope guard: this plan owns ONLY the two shared docs + the new agreements files. The three
non-shared reword surfaces (`.memory/state/activeContext.md`, `.memory/state/progress.md`,
`AGENTS.md`) belong to plan 12-02 (parallel, no file overlap). Do NOT touch `inject.py`,
`tools/harness_emit`, or any generated `.opencode/`/`.claude/` tree (Pitfall 1/2).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-RESEARCH.md
@.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-VALIDATION.md

<interfaces>
<!-- The committed-tier mechanism, verified in 12-RESEARCH.md §SC1. -->
<!-- .gitignore ONLY ignores `.memory/derived/*` (with a contracts-index negation) — so
     `.memory/agreements/**` is tracked BY DEFAULT. No .gitignore edit needed. Git does not track
     empty dirs, so ≥1 committed file (the _TEMPLATE + README) is what makes the tier exist. -->
<!-- Entry shape (12-RESEARCH.md §SC1 "Recommended entry shape"): YAML frontmatter carries
     `status` (active|retired), `added` (ISO date), `provenance` ("added because <user feedback>");
     the markdown body carries the title + one-line rule + a `Related:` LINK line. Frontmatter keys
     chosen to match the existing tools/harness_lint/frontmatter.py parser (eases Phase-14 lint). -->
<!-- Do NOT add a generator, a .gitignore entry, or a CODEOWNERS entry for agreements (Pitfall 3). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the committed .memory/agreements/ tier + entry-shape docs (SC1/SC3)</name>
  <files>.memory/agreements/_TEMPLATE.md, .memory/agreements/README.md</files>
  <read_first>
    - 12-RESEARCH.md §SC1 ("Recommended entry shape", "Where to document the shape", "Verify SC1")
      + §SC3 ("The rule to encode", the `Related:` precedent) + Pitfalls 3 & 4.
    - Analog for the committed-tier posture: `.memory/README.md:43-53` (STATE plane declaration) and
      `.gitignore:17-24` (only `derived/*` is ignored → agreements tracked by default).
    - Analog for the LINK-not-restate house style: `docs/adr/0005-*.md` `## Links` section +
      `.planning/PROJECT.md` `## Key Decisions` table.
  </read_first>
  <action>
    Create `.memory/agreements/_TEMPLATE.md` — the committed per-guideline entry-shape fixture. YAML
    frontmatter with EXACT keys `status:` (value `active`, comment `# active | retired`), `added:`
    (ISO date placeholder `YYYY-MM-DD`), and `provenance:` (value `"added because <verbatim user
    feedback>"`). Body: an H1 `# <Title — the working-agreement in a few words>`, a one-line rule
    (methodology/working-style ONLY), and a final `Related:` line linking to an ADR / PROJECT.md
    §Key Decisions with an inline comment "LINK, never restate a project decision (§7c)". Do NOT
    invent a real seed agreement — agreements come only from user feedback (machines gate, humans
    ratify); ship the template as the fixture that makes the dir git-tracked.
    Create `.memory/agreements/README.md` — the tier doc. State: (a) this is the PROCESS/agreements
    channel, a COMMITTED, human-authored tier LIKE `.memory/state/` — NOT derived (never regenerated,
    never written by `tools/memory_regen`, never collides with `.memory/derived/`) and NOT
    constitution (not path-denied); (b) the entry shape (the frontmatter keys + body fields above),
    one `<slug>.md` per guideline (Q3), retire via `status: retired` never deletion (Q5); (c) the
    §7c rule verbatim in intent: an entry is working-style/methodology ONLY — a project/architecture
    decision belongs in `docs/adr/` + PROJECT.md Key Decisions and is LINKED, never restated.
    Reference D-Q1 (committed-but-writable), MEM2-01, and note the write-path/`/agree` + provenance
    lint arrive in Phase 14 (out of scope here).
  </action>
  <acceptance_criteria>
    - `test -f .memory/agreements/_TEMPLATE.md` exits 0.
    - `test -f .memory/agreements/README.md` exits 0.
    - `git ls-files .memory/agreements/ | grep -q .` exits 0 (≥1 tracked file — the tier is committed).
    - `git check-ignore .memory/agreements/_TEMPLATE.md` exits non-zero (NOT gitignored).
    - `grep -qE '^status:' .memory/agreements/_TEMPLATE.md` and `grep -qE '^added:' .memory/agreements/_TEMPLATE.md` and `grep -qE '^provenance:' .memory/agreements/_TEMPLATE.md` all exit 0 (frontmatter keys present).
    - `grep -q 'Related:' .memory/agreements/_TEMPLATE.md` exits 0 (LINK line present).
    - `grep -qi 'never restate' .memory/agreements/README.md` exits 0 (§7c rule encoded).
    - `grep -riq 'agreements' tools/memory_regen/` exits non-zero (no generator references the tier — Pitfall 3).
    - `grep -q 'agreements' .github/CODEOWNERS 2>/dev/null` exits non-zero (not constitution-gated — Q1).
  </acceptance_criteria>
  <verify>
    <automated>test -f .memory/agreements/_TEMPLATE.md && git ls-files .memory/agreements/ | grep -q . && git check-ignore .memory/agreements/_TEMPLATE.md; test $? -ne 0 && grep -q 'Related:' .memory/agreements/_TEMPLATE.md && grep -qi 'never restate' .memory/agreements/README.md && ! grep -riq 'agreements' tools/memory_regen/</automated>
  </verify>
  <done>A committed `.memory/agreements/` tier exists with a documented, frontmatter-based entry shape and the §7c link-never-restate rule; no generator/CODEOWNERS/gitignore entry references it.</done>
</task>

<task type="auto">
  <name>Task 2: Four-plane README + data-authority reword of the two shared memory docs (SC1/SC2/SC3)</name>
  <files>.memory/README.md, harness/skills/two-plane-memory/SKILL.md</files>
  <read_first>
    - 12-RESEARCH.md §SC2 "Surface 1" (README L5-8 + L50-53 verbatim) and "Surface 4"
      (SKILL.md L31-33 verbatim + the EMIT NOTE) + §SC1 "Where to document the shape".
    - Current state: `.memory/README.md:9` ("## Three planes at a glance" table header) + `:11-15`
      (3-row table) + `:50-53` (STATE "provisional" sentence).
    - Current state: `harness/skills/two-plane-memory/SKILL.md:30-46` (the three sub-tier list;
      `:32` "is **provisional**:") + `:62` ("provisional-banner-first").
  </read_first>
  <action>
    In `.memory/README.md`: (1) retitle the table section "Three planes at a glance" → "Four planes
    at a glance" and reword the intro line "three-plane memory model" → "four-plane memory model";
    (2) add a fourth table row **PROCESS** — Location `.memory/agreements/`, Ownership "Human-authored
    via feedback (curated)", Tracked in git ✅ committed, Regenerated ❌ never; (3) reword the STATE
    section L50-53: remove the standalone epistemic "**provisional**" — reframe as data authority:
    keep "committed" + "pointer-only", and state that on a DATA conflict `contracts/` and `docs/adr/`
    are authoritative over `.memory/state/` (which artifact wins a contradiction — NOT a reason to
    distrust grounded work). Optionally add a short "(4) PROCESS plane — `.memory/agreements/`"
    subsection pointing to `.memory/agreements/README.md`.
    In `harness/skills/two-plane-memory/SKILL.md`: (1) reword L32 "It is **provisional**: contracts/
    and docs/adr/ always override it on conflict." → "On a **data** conflict, `contracts/` and
    `docs/adr/` are authoritative over it." (keep "Decisions do NOT live here — they belong in
    append-only ADRs."); (2) reword L62 "provisional-banner-first" → "data-authority-banner-first";
    (3) add the committed agreements tier to the plane map (a bullet: `.memory/agreements/` = committed,
    human-authored via feedback, curated, NOT regenerated — a peer of the committed-state tier).
    EMIT NOTE (record in the SUMMARY): SKILL.md is `harness/` SOURCE; its re-emit to
    `.opencode/skill/` + `.claude/skills/` is Phase 15 — do NOT edit the generated copies here.
  </action>
  <acceptance_criteria>
    - `grep -riqE 'provisional|hint, not truth|confirm before trusting' .memory/README.md harness/skills/two-plane-memory/SKILL.md` exits non-zero (all epistemic-distrust phrasing removed from these two files).
    - `grep -qi 'four plane' .memory/README.md` exits 0 (table retitled).
    - `grep -q 'agreements' .memory/README.md` exits 0 (PROCESS/agreements plane row added).
    - `grep -qi 'data conflict' harness/skills/two-plane-memory/SKILL.md` exits 0 (data-authority framing preserved).
    - `grep -q 'agreements' harness/skills/two-plane-memory/SKILL.md` exits 0 (4th committed tier documented).
    - `grep -qE 'contracts/.*docs/adr/|docs/adr/.*contracts/' .memory/README.md` exits 0 (the data-authority rule — contracts/+adr win a conflict — is STILL stated, meaning preserved).
    - `git diff --quiet tools/memory_regen/inject.py` exits 0 (inject.py untouched — Pitfall 1).
    - No find/replace target path under `.opencode/` or `.claude/` was edited (`git diff --name-only | grep -E '^\.(opencode|claude)/'` exits non-zero).
  </acceptance_criteria>
  <verify>
    <automated>! grep -riqE 'provisional|hint, not truth|confirm before trusting' .memory/README.md harness/skills/two-plane-memory/SKILL.md && grep -qi 'four plane' .memory/README.md && grep -q 'agreements' .memory/README.md && grep -qi 'data conflict' harness/skills/two-plane-memory/SKILL.md && git diff --quiet tools/memory_regen/inject.py && ! (git diff --name-only | grep -qE '^\.(opencode|claude)/')</automated>
  </verify>
  <done>`.memory/README.md` declares four planes incl. agreements; both shared docs read as data-authority (zero standalone "provisional"/"hint, not truth"); the contracts+ADR-win-on-conflict rule is preserved; inject.py and the generated trees are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → `.memory/agreements/` | Committed-but-writable tier (Q1); no path-deny. Frictionless capture by design; provenance-lint (Phase 14) is the guard, NOT this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | agent invents an unsolicited agreement | accept (this phase) | Shape carries a mandatory `provenance:` stamp NOW so Phase-14's lint can reject unstamped/auto-invented entries. No seed is invented here (template only). Enforcement is Phase 14, out of scope. |
| T-12-03 | Tampering | agent hand-edits a derived artifact by mis-classifying agreements | mitigate | Tier docs (`agreements/README.md` + `.memory/README.md` row + SKILL.md) state committed-not-derived explicitly; acceptance asserts `tools/memory_regen` never references the tier. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (docs + committed markdown only). |
</threat_model>

<verification>
- `test -f .memory/agreements/_TEMPLATE.md && test -f .memory/agreements/README.md`.
- `git ls-files .memory/agreements/ | grep .` returns ≥1 file; `git check-ignore` returns none.
- `grep -riE 'provisional|hint, not truth|confirm before trusting' .memory/README.md harness/skills/two-plane-memory/SKILL.md` returns nothing.
- `.memory/README.md` shows a four-plane table with an agreements row.
- `git diff --quiet tools/memory_regen/inject.py` (unchanged); no `.opencode/`/`.claude/` edits.
</verification>

<success_criteria>
- SC1: committed `.memory/agreements/` tier exists (≥1 tracked file), documented entry shape, four-plane README, explicitly committed-not-derived.
- SC2 (shared-doc portion): `.memory/README.md` + two-plane-memory SKILL.md reworded to data-authority; data-authority rule preserved.
- SC3: entry-shape template + tier README encode the §7c link-never-restate rule.
</success_criteria>

<output>
Create `.planning/phases/12-model-adr-doc-reframe-v2-1-a/12-01-SUMMARY.md` when done. Record the
Phase-15 EMIT-DEFERRED note for `harness/skills/two-plane-memory/SKILL.md` (source edited; re-emit
to both runtimes is Phase 15).
</output>
