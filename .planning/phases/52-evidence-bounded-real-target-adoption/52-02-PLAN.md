---
phase: 52-evidence-bounded-real-target-adoption
plan: 02
type: execute
wave: 3
# 52-01 ratifies the enum value this plan emits (contract-first). 52-04 asserts
# `git diff --quiet -- tools/adoption_scan/detect.py` and must therefore land BEFORE this plan
# touches detect.py; 52-03 shares wave 2's full-suite gate. Both are hard ordering constraints,
# not preferences.
depends_on: ["52-01", "52-03", "52-04"]
files_modified:
  - tools/adoption_scan/detect.py
  - tools/adoption_scan/scan.py
  - tools/adoption_scan/tests/conftest.py
  - tools/adoption_scan/tests/test_detect.py
  - tools/adoption_scan/tests/test_scan_exclusions.py
autonomous: true
requirements: [RTA-02, OBS-02]

must_haves:
  truths:
    - "When the scanned target root has a pnpm-workspace.yaml, the inventory's manifests and candidate_process_boundaries contain ONLY declared workspace members plus the root — a manifest outside the declared globs is not a member (OBS-D-01, D-07)."
    - "A non-member manifest is visible in the artifact as an excluded entry with reason non-workspace-member — excluded, never silently dropped (D-08 as shaped by D-20)."
    - "When no pnpm-workspace.yaml exists at the target root, discovery output is byte-identical to before this change (D-10) — the committed tmp_minirepo snapshot proves it."
    - "Workspace-glob expansion cannot reach outside the scanned target root; a glob that escapes confinement yields no members (V5 input validation, reusing scan.py's own _confined idiom)."
    - "Only pnpm is taught; no package.json `workspaces`, Cargo, or uv workspace support is added (D-09, NG-01)."
    - "`_dependencies_from_package_json` (detect.py:273) — the REFUTED OBS-03 site — is byte-unchanged by this plan; it is the only edit to detect.py in the phase and it must not touch that function."
  artifacts:
    - path: "tools/adoption_scan/detect.py"
      provides: "Pure parse_pnpm_workspace_globs(text) + workspace membership predicate over already-read text"
      contains: "pnpm-workspace.yaml"
    - path: "tools/adoption_scan/scan.py"
      provides: "build_inventory reads the workspace manifest once and excludes non-member manifests"
      contains: "non-workspace-member"
    - path: "tools/adoption_scan/tests/conftest.py"
      provides: "tmp_pnpm_workspace synthetic fixture (neutral widget/source/sink vocabulary)"
      contains: "tmp_pnpm_workspace"
  key_links:
    - from: "tools/adoption_scan/scan.py"
      to: "tools/adoption_scan/detect.py"
      via: "text passed in, never read from disk inside detect.py"
      pattern: "parse_pnpm_workspace_globs"
    - from: "tools/adoption_scan/scan.py"
      to: "contracts/harness/adoption/inventory.schema.json"
      via: "excluded entry reason value ratified by Plan 01"
      pattern: "non-workspace-member"
---

<objective>
Repair OBS-D-01 at the source: teach discovery the pnpm workspace manifest so the inventory's
member set is the target's declared globs, and record any manifest outside those globs as an
excluded entry with the `non-workspace-member` reason ratified by Plan 01.

Purpose: RTA-02 requires the inventory to contain exactly the real workspace members. Phase 51
observed a 6th, non-member manifest (`docs/design-prototype/package.json`) enumerated as a
candidate process boundary. D-07 forbids fixing this downstream in draft — the inventory itself
must be right.

Output: a pure pnpm-workspace parser + membership predicate in `detect.py`, the `scan.py` wiring
that reads the manifest text once, a synthetic pnpm fixture, and regression tests that go red if
either the scoping or the diagnostic disappears.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-evidence-bounded-real-target-adoption/52-CONTEXT.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-RESEARCH.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-VALIDATION.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-01-SUMMARY.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-04-SUMMARY.md
@AGENTS.md
@harness/skills/brownfield-adoption/SKILL.md
@harness/skills/python-conventions/SKILL.md
</context>

<parallel_execution_note>
This plan is ALONE in wave 3. Every full-suite red it sees is its own — there is no sibling to
attribute a failure to, and no assertion in this plan may be weakened to make one pass.

It is deliberately sequenced AFTER 52-04 because 52-04's Task 3 asserts
`git diff --quiet -- tools/adoption_scan/detect.py`. That fence is only meaningful while detect.py
is untouched, so 52-04 must observe a clean detect.py first; this plan is then the ONE plan in the
phase permitted to modify it, under the `_dependencies_from_package_json` pin below.
</parallel_execution_note>

<interfaces>
Ground truth for the real target (52-RESEARCH.md, read live 2026-07-31) — the shape the fixture must
reproduce, with neutral vocabulary:

```
packages:
  - "apps/*"
  - "packages/*"
```

Two bare glob entries, no `!` negation, no `**`. pnpm treats the workspace root as a member
implicitly. D-09/NG-01 forbid building negation or `**` support that no observed target needs.

Existing contracts this plan must respect:
- `detect.py` module docstring guarantees NO filesystem access — "detection can never diverge from
  what was actually hashed". The precedent for a text-in parser is `detect_dependencies(path, kind,
  text)` (`detect.py:360`) and `_dependencies_from_package_json(text)` (`detect.py:273`).
- **`_dependencies_from_package_json` (`detect.py:273`) is OFF LIMITS.** Phase 51 REFUTED OBS-03:
  that function discards version strings and resolves dependencies by name, which is precisely WHY
  `workspace:*` edges already resolve. 52-CONTEXT.md's "Do not 'fix' it" is a locked instruction.
  This plan is the only edit to `detect.py` in the phase, so it carries the pin that 52-04's
  worktree-level fence can no longer provide once this plan lands.
- `scan.py::build_inventory(target, *, max_bytes, _paths)` already has `target: Path` and already
  opens files during `classify_exclusions` — it is the correct (and only) place to read the
  workspace manifest text.
- `classify_exclusions(path, *, base, max_bytes)` returns `{"path","size","excluded"}` or `None`.
  Its existing security-first ordering (symlink-escape → vendored → generated → size cap → binary →
  generated marker → secret-path → source-dump → secret-content) must be preserved verbatim.
- `_confined(path, base_resolved)` (`scan.py:131`) is the containment idiom to reuse for glob
  expansion — never re-derive it.
- `detect.detect_manifests(included)` derives manifests from `included` only, and
  `detect.detect_candidate_process_boundaries(included)` derives one boundary per manifest
  directory. Excluding a non-member manifest from `included` therefore removes it from BOTH arrays
  with no change to either function.

**Deliberate deviation from D-07's literal wording, to be documented inline:** the pnpm workspace
manifest must NOT be added to `_MANIFEST_KIND_BY_NAME` (`detect.py:46`). That table drives
`detect_manifests` → `detect_candidate_process_boundaries`, so registering `pnpm-workspace.yaml`
there would emit a 6th manifest record and a duplicate root boundary — directly defeating RTA-02's
"exactly the five members". Teach the workspace manifest as its own module-level constant plus
parser instead; D-07's intent (scope membership at the source, in detect/scan, not in draft) is
honored exactly.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure pnpm-workspace parsing and membership predicate in detect.py</name>
  <files>tools/adoption_scan/detect.py, tools/adoption_scan/tests/test_detect.py</files>
  <behavior>
    - `parse_pnpm_workspace_globs('packages:\n  - "apps/*"\n  - "packages/*"\n')` returns `["apps/*", "packages/*"]` (quotes stripped, order preserved).
    - Bare (unquoted) and single-quoted entries parse identically to double-quoted ones.
    - Comment lines and blank lines inside the `packages:` block are ignored; a key that is not `packages:` ends the block.
    - Malformed / non-YAML / empty text returns `[]` and never raises — the degrade-per-file idiom, not a run-killer.
    - `is_workspace_member("apps/widget-app", ["apps/*", "packages/*"])` is True; `"docs/design-prototype"` is False; `"."` (the workspace root) is True regardless of globs; `"apps/widget-app/nested"` is False for `apps/*` (single-segment `*`).
    - A traversal-shaped glob (`"../outside/*"`) or an absolute glob (`"/etc/*"`) contributes NO members — membership is computed on repo-relative POSIX directories only.
  </behavior>
  <read_first>
    tools/adoption_scan/detect.py (module docstring's no-filesystem-access invariant; `_MANIFEST_KIND_BY_NAME` at :46; `_dependencies_from_package_json` at :273 and `detect_dependencies` at :360 — the exact pure-function-over-text shape to imitate, and the one function this plan must leave byte-identical),
    tools/adoption_scan/tests/test_detect.py (existing test style and naming),
    tools/adoption_scan/scan.py (`SECRET_PATH_GLOBS` / `SECRET_CONTENT_PATTERNS` — the documented "own the constant locally" idiom, `scan.py:52-71`),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-RESEARCH.md (§ Don't Hand-Roll: why a narrow `packages:`-block parser is the sanctioned choice over pulling `ruamel.yaml` into a module whose pyproject declares a zero-new-external-deps invariant)
  </read_first>
  <action>
    Before editing, confirm the OBS-03 pin holds on the pre-edit tree: extract
    `_dependencies_from_package_json` with `ast.get_source_segment` and check its SHA-256 equals
    `36f3253f152f5b0b7b475499a56bfe9f84128bb89ec8a7c72af5642dc12e76b6`. If it does NOT, stop and
    report — something already moved that function and the pin must be re-derived deliberately, not
    silently updated.

    Add to `detect.py`, stdlib-only, no filesystem access:
    - Module constant `PNPM_WORKSPACE_MANIFEST = "pnpm-workspace.yaml"`.
    - `parse_pnpm_workspace_globs(text: str) -> list[str]` — a narrow line-based reader of the top-level `packages:` block only: collect lines matching a leading `-` list item, strip surrounding matched single/double quotes, skip `#` comments and blanks, stop at the next top-level key. Wrap the whole body so any unexpected input returns `[]` rather than raising, mirroring `package_facts.py:176-182`'s degrade-not-crash posture. Document in the docstring that this is deliberately NOT a general YAML parser and that its scope is exactly pnpm's `packages:` list-of-glob-strings (52-RESEARCH.md "Don't Hand-Roll").
    - `is_workspace_member(directory: str, globs: list[str]) -> bool` — `directory` is a repo-relative POSIX dir (`"."` for the root). Return True for `"."`. Reject any glob that is absolute or contains a `..` segment (contributes nothing). Otherwise match with `PurePosixPath(directory).match(glob)` semantics via `fnmatch` on path segments such that `apps/*` matches `apps/widget-app` but not `apps/widget-app/nested`.
    - Carry an inline trace comment on each new symbol: `# OBS-D-01 (51-BASELINE-EVIDENCE.md) — purpose 2: pnpm workspace member scoping.` following the repo's existing `WR-02 (48-REVIEW.md)` citation convention.

    Do NOT add `pnpm-workspace.yaml` to `_MANIFEST_KIND_BY_NAME` — add a short comment there pointing at `PNPM_WORKSPACE_MANIFEST` and stating why (see `<interfaces>`).

    Do NOT touch `_dependencies_from_package_json` in any way — not a rename, not a reflow, not a
    comment. It is the REFUTED-OBS-03 site.

    Write the tests in `test_detect.py` first, one per bullet in `<behavior>`.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_detect.py -q -k "pnpm or workspace_member" || exit 1; grep -n "def parse_pnpm_workspace_globs" tools/adoption_scan/detect.py || exit 1; grep -q "ruamel" tools/adoption_scan/detect.py && exit 1; grep -q "open(\|read_text\|Path(" tools/adoption_scan/detect.py && exit 1; uv run python -c '
import ast, hashlib, sys
PIN = "36f3253f152f5b0b7b475499a56bfe9f84128bb89ec8a7c72af5642dc12e76b6"
text = open("tools/adoption_scan/detect.py").read()
seg = next((ast.get_source_segment(text, n) for n in ast.walk(ast.parse(text))
            if isinstance(n, ast.FunctionDef) and n.name == "_dependencies_from_package_json"), None)
if seg is None:
    sys.exit("OBS-03 pin FAILED: _dependencies_from_package_json no longer exists")
got = hashlib.sha256(seg.encode()).hexdigest()
if got != PIN:
    sys.exit("OBS-03 pin FAILED: expected " + PIN + " got " + got)
print("OBS-03 pin OK: " + got)
' || exit 1; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/adoption_scan/tests/test_detect.py -q` is green and includes at least one test per `<behavior>` bullet.
    - **OBS-03 pin (W-7):** the diff to `tools/adoption_scan/detect.py` touches NO line inside `_dependencies_from_package_json`. The `<verify>` block re-extracts that function with `ast.get_source_segment` and asserts its SHA-256 equals the plan-time pin `36f3253f152f5b0b7b475499a56bfe9f84128bb89ec8a7c72af5642dc12e76b6` (12 source lines, measured against the pre-plan tree). The digest is a literal in the plan, so the check is base-agnostic and stays meaningful before AND after commit. Quote the recomputed digest in the SUMMARY. Mutation check: renaming one local inside that function, or reflowing a single line of it, must red this check — verify by applying such a mutation in a scratch checkout, observing the failure, and reverting; record the observed message. This plan is the phase's only edit to `detect.py`, so 52-04's worktree-level `git diff --quiet -- tools/adoption_scan/detect.py` fence is void from here on — this pin replaces it.
    - `detect.py` still performs zero filesystem access: `grep -n "open(\|read_text\|read_bytes\|Path(" tools/adoption_scan/detect.py` returns no match (`PurePosixPath` is fine and is not `Path(`).
    - `detect.py` imports no third-party module (no `ruamel`, no `yaml`).
    - `_MANIFEST_KIND_BY_NAME` does NOT gain a `pnpm-workspace.yaml` key: `uv run python -c "from tools.adoption_scan import detect; assert 'pnpm-workspace.yaml' not in detect._MANIFEST_KIND_BY_NAME"` exits 0.
    - Negative controls exist and pass: the traversal glob test asserts `is_workspace_member` returns False for a `../`-prefixed glob, and the malformed-text test asserts `[]` rather than an exception. Deleting the traversal rejection must red the first; deleting the try/except must red the second.
  </acceptance_criteria>
  <done>detect.py exposes a pure, filesystem-free pnpm workspace parser and membership predicate, tested including hostile-input negative controls, with the REFUTED-OBS-03 function provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Synthetic pnpm workspace fixture (neutral vocabulary, second fixture — tmp_minirepo untouched)</name>
  <files>tools/adoption_scan/tests/conftest.py</files>
  <read_first>
    tools/adoption_scan/tests/conftest.py (the `tmp_minirepo` fixture's construction style, its D-06 "THE single synthetic mini-repo — no fixture proliferation" docstring, and its neutral widget/source/sink vocabulary rule),
    tools/adoption_scan/tests/test_snapshots.py (the committed syrupy snapshot rendered over `tmp_minirepo` — the artifact that must stay byte-identical to prove D-10),
    tools/harness_lint/tests/test_core_no_example_dep.py (the GEN-04 guard the new fixture must not trip),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-VALIDATION.md (Wave 0 fixture requirement)
  </read_first>
  <action>
    Add a SECOND fixture, `tmp_pnpm_workspace`, alongside `tmp_minirepo` — do not modify `tmp_minirepo` in any way. Amend the conftest docstring to record why the D-06 single-fixture rule gains exactly one exception here: `tmp_minirepo` must stay byte-identical so the committed snapshot proves D-10's no-workspace-manifest path is unchanged, which is impossible if the pnpm case is bolted onto it.

    `tmp_pnpm_workspace` materializes, under `tmp_path/"pnpm-workspace"`, with strictly neutral vocabulary (widget/source/sink — never FeedbackOps, `@fops`, frontend/backend/shared-as-real-names):
    - `pnpm-workspace.yaml` with exactly the two glob entries `"apps/*"` and `"packages/*"`;
    - root `package.json` (`{"name": "widget-workspace", "private": true, "scripts": {"lint": "...", "test": "...", "format": "..."}}`);
    - members `apps/widget-app/package.json`, `apps/widget-service/package.json`, `packages/widget-shared/package.json`, `packages/widget-ui/package.json` — four members plus the root, mirroring the real target's five;
    - `apps/widget-app/package.json` and `apps/widget-service/package.json` each declare `"dependencies": {"@widget/shared": "workspace:*"}` and `packages/widget-shared/package.json` is named `@widget/shared` (this makes the fixture reusable for the workspace-edge lock-in without depending on it);
    - ONE non-member manifest at `docs/design-prototype/package.json` — the neutral analogue of the Phase-51 symptom;
    - a `.gitignore` and one ordinary source file per member so the tree is not manifest-only.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests -q || exit 1; uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -q || exit 1; grep -v '^ *#' tools/adoption_scan/tests/conftest.py | grep -Eiq 'fops|feedbackops' && exit 1; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/adoption_scan/tests -q` is green (including `test_snapshots.py` WITHOUT `--snapshot-update` — `tmp_minirepo` was not touched).
    - `git diff -- tools/adoption_scan/tests/__snapshots__/` is empty.
    - `tmp_pnpm_workspace` yields 5 `package.json` files inside the declared globs (root + 4) and exactly 1 outside them, verified by a test that counts them rather than by inspection.
    - No FeedbackOps-derived identifier appears in conftest.py (`grep -Eiq 'fops|feedbackops'` over non-comment lines finds nothing) and `tools/harness_lint/tests/test_core_no_example_dep.py` passes (GEN-04).
  </acceptance_criteria>
  <done>A second, neutral synthetic pnpm fixture exists; the committed tmp_minirepo snapshot is provably unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire workspace scoping into build_inventory and record non-members as excluded</name>
  <files>tools/adoption_scan/scan.py, tools/adoption_scan/tests/test_scan_exclusions.py</files>
  <behavior>
    - `build_inventory(tmp_pnpm_workspace)`: `manifests` has exactly 5 entries and `candidate_process_boundaries` has exactly 5 distinct targets (`.`, `apps/widget-app`, `apps/widget-service`, `packages/widget-shared`, `packages/widget-ui`).
    - The same inventory's `excluded` array contains exactly one entry with `{"path": "docs/design-prototype/package.json", "excluded": "non-workspace-member"}`, and that path appears in NEITHER `included` NOR `manifests`.
    - `build_inventory(tmp_minirepo)` (no pnpm-workspace.yaml) is byte-identical to the pre-change output — no `non-workspace-member` entry appears anywhere (D-10).
    - Security precedence is preserved: a manifest that is BOTH outside the globs AND secret-path/vendored/size-capped keeps its original exclusion reason, not `non-workspace-member`.
    - A `pnpm-workspace.yaml` whose globs escape the target (e.g. `"../outside/*"`) produces no members from that glob and never reads or records a path outside the target root.
    - Non-manifest files under a non-member directory (e.g. `docs/design-prototype/README.md`) stay INCLUDED and hashed — only the manifest itself is scoped out.
  </behavior>
  <read_first>
    tools/adoption_scan/scan.py (`build_inventory` at :305, `classify_exclusions` at :205 with its documented security-first ordering, `_confined` at :131, `_dump` at :197),
    tools/adoption_scan/detect.py (the Task-1 `parse_pnpm_workspace_globs` / `is_workspace_member` signatures),
    contracts/harness/adoption/inventory.schema.json (the `excludedEntry` shape: path/size/excluded only — structurally never a sha256 or excerpt, D-10),
    tools/adoption_scan/tests/test_scan_exclusions.py (existing exclusion-test style),
    tools/adoption_scan/tests/test_schema_conformance.py (the conformance gate the new reason value must satisfy — this is where the D-20 enum value gets its REAL instance validation, since the schema has no sibling instance file for check-jsonschema),
    tools/adoption_scan/tests/test_inventory_determinism.py (the seeded-shuffle determinism proof the new branch must not break)
  </read_first>
  <action>
    In `build_inventory`, before the classification loop: if `(target / PNPM_WORKSPACE_MANIFEST).is_file()`, read its text once (UTF-8, errors tolerated) and compute `workspace_globs = detect.parse_pnpm_workspace_globs(text)`; otherwise `workspace_globs = None` — the D-10 additive branch. Never read the manifest inside `detect.py`.

    Inside the loop, keep `classify_exclusions(...)` FIRST and unchanged. Only when it returns `None` (the file would be included) AND `workspace_globs is not None` AND the candidate's basename is a recognized manifest name (reuse `detect._MANIFEST_KIND_BY_NAME` / the `.csproj` suffix rule via a small helper rather than retyping the table) AND `not detect.is_workspace_member(<the manifest's POSIX parent dir>, workspace_globs)`, append `{"path": rel, "size": <stat size>, "excluded": "non-workspace-member"}` to `excluded` and `continue`. This ordering is what preserves security precedence — state it in a comment.

    Re-validate confinement before treating any glob-derived path as a member by reusing `_confined` — a glob must never widen the walk beyond `target`.

    Carry the trace comment `# OBS-D-01 (51-BASELINE-EVIDENCE.md) — purpose 2; contract value ratified by 52-01 (D-20).` at the new branch.

    Write the `<behavior>` tests first in `test_scan_exclusions.py`, including the D-10 byte-identity assertion (compare `scan._dump(build_inventory(tmp_minirepo))` against the committed snapshot path or against a captured pre-change digest recorded in the test itself).
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan -q || exit 1; uv run pytest tools/adoption_scan/tests/test_schema_conformance.py tools/adoption_scan/tests/test_inventory_determinism.py tools/adoption_scan/tests/test_snapshots.py -q || exit 1; git diff --quiet -- tools/adoption_scan/tests/__snapshots__/ || exit 1; uv run pytest -q || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - Every `<behavior>` bullet has a test and all pass: `uv run pytest tools/adoption_scan -q` green.
    - The member-count test asserts the literal 5 targets by value (a set equality against the five expected directory strings), not merely `len(...) == 5` — a test that only counts would pass if scoping picked the wrong five.
    - The non-member test asserts BOTH that `docs/design-prototype/package.json` is in `excluded` with reason `non-workspace-member` AND that it is absent from `included` and `manifests`. Reverting the `scan.py` branch must red it.
    - The security-precedence test constructs a manifest that is also secret-path/vendored and asserts the ORIGINAL reason survives; swapping the branch order in `scan.py` must red it.
    - `git diff --quiet -- tools/adoption_scan/tests/__snapshots__/` succeeds and `test_snapshots.py` passes without `--snapshot-update` (D-10 proven, not asserted).
    - `uv run pytest -q` (full suite) is green, including `tools/adoption_scan/tests/test_schema_conformance.py` — the new reason validates against the Plan-01 schema. This is the phase's real instance-conformance evidence for the D-20 enum value; quote its node id in the SUMMARY.
  </acceptance_criteria>
  <done>Discovery scopes members by the target's own pnpm workspace declaration, records non-members visibly, leaves the no-workspace path byte-identical, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned target's `pnpm-workspace.yaml` → harness scan logic | Fully untrusted attacker-controllable text from a brownfield repo. |
| glob expansion → filesystem walk | A glob is a path expression; unconstrained it can escape the scanned root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-03 | Tampering | `parse_pnpm_workspace_globs` / `is_workspace_member` glob handling | high | mitigate | Reject absolute and `..`-containing globs in `is_workspace_member`; re-validate any glob-derived directory with `scan.py:131`'s existing `_confined` idiom before treating it as a member; Task 1 and Task 3 each carry an explicit traversal negative-control test. |
| T-52-04 | Denial of Service | Malformed/huge `pnpm-workspace.yaml` | medium | mitigate | The parser is line-based over already-bounded text, wrapped to return `[]` on any parse failure (`package_facts.py:176-182` degrade-per-file idiom) — a hostile manifest downgrades scoping to the D-10 unchanged path, never crashes the run. |
| T-52-05 | Information Disclosure | New exclusion reason leaking file content | low | mitigate | `excludedEntry` is structurally `{path, size, excluded}` with no sha256/excerpt property (D-10); the new branch reuses that exact record shape and the schema forbids anything more. |
| T-52-06 | Spoofing | A manifest slipping past a security exclusion by claiming non-membership | medium | mitigate | `classify_exclusions` runs FIRST and unchanged; the non-member branch only fires when it returned `None`. Task 3's security-precedence test is the enforcing check. |
| T-52-24 | Tampering | Regression of the REFUTED OBS-03 dependency resolution | medium | mitigate | Task 1's hunk-range + source-digest pin proves the diff touches no line of `_dependencies_from_package_json`; 52-04's lock-in test is the behavioral backstop. |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; `ruamel.yaml` is deliberately NOT adopted (52-RESEARCH.md § Standard Stack option (b)) so `uv.lock` is untouched. |
</threat_model>

<verification>
- `uv run pytest -q` fully green.
- `git diff --quiet -- tools/adoption_scan/tests/__snapshots__/` succeeds (D-10).
- `uv run python -m tools.contract_drift.drift` exits 0 (no contract touched by this plan).
- The `detect.py` diff touches no line of `_dependencies_from_package_json` (hunk-range check + source-digest equality).
- `grep -rn "workspaces\|Cargo workspace\|uv workspace" tools/adoption_scan/detect.py` finds no speculative non-pnpm support (D-09).
</verification>

<success_criteria>
Against a synthetic pnpm workspace, discovery inventories exactly the declared members plus the
root, records the one non-member manifest as `non-workspace-member`, and against a target with no
workspace manifest produces byte-identical output to before — each proven by a test that fails if
the corresponding behavior is reverted, with the REFUTED-OBS-03 function provably unmodified.
</success_criteria>

<output>
Create `.planning/phases/52-evidence-bounded-real-target-adoption/52-02-SUMMARY.md` when done.
</output>
