---
phase: 52-evidence-bounded-real-target-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py
  - contracts/harness/adoption/inventory.schema.json
  - contracts/.hashes/manifest.json
  - .memory/derived/contracts-index.md
  - docs/reference/inventory.md
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
autonomous: false
requirements: [RTA-02]

must_haves:
  truths:
    - "The inventory contract can express 'this manifest was found but is not a pnpm workspace member' before any code emits that value — contract entry first, never after (D-20)."
    - "The constitution-plane edit is performed by the human-authorized path, not silently by an agent write (contracts/** is CODEOWNERS-gated and contract_guard-denied)."
    - "The RFC 8785 schema-hash drift gate is green again only because the committed baseline was re-derived in the same change — a hash change with no paired golden update stays a gate failure (D-20)."
    - "The contract COUNT is unchanged at 6 — one enum value was added to an existing schema, no new schema file (NG-01)."
    - "Every derived artifact carrying the old schema hash (contracts-index, docs/reference, the contracts-index snapshot) is regenerated, so the derived plane is not left stale."
  artifacts:
    - path: "contracts/harness/adoption/inventory.schema.json"
      provides: "excludedEntry.excluded enum carrying the non-workspace-member reason"
      contains: "non-workspace-member"
    - path: "contracts/.hashes/manifest.json"
      provides: "Re-derived JCS SHA-256 baseline for the changed schema"
    - path: ".planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py"
      provides: "Idempotent, human-runnable off-plane applier for the constitution-plane edit"
  key_links:
    - from: "contracts/harness/adoption/inventory.schema.json"
      to: "contracts/.hashes/manifest.json"
      via: "tools.contract_hash.hash --write rebaseline"
      pattern: "inventory.schema.json"
    - from: "contracts/.hashes/manifest.json"
      to: ".memory/derived/contracts-index.md"
      via: "tools.memory_regen.contracts_index regeneration"
      pattern: "inventory.schema.json"
---

<objective>
Land the ONE contract change this phase is permitted: an additive `non-workspace-member` value on
`contracts/harness/adoption/inventory.schema.json`'s `excludedEntry.excluded` enum (D-20), through
the full contract-first chain — contract entry → `/contract-check` (check-jsonschema + RFC 8785
drift gate) → paired golden/baseline update — before any code can emit it.

Purpose: OBS-D-01's repair (Plan 02) records a non-member manifest as an excluded entry. The
schema is `additionalProperties: false` with an enumerated `excluded` list, so the emitter cannot
land first without turning every artifact-conformance test red. Contract before code, always.

Output: the extended schema, a re-derived hash baseline, regenerated derived artifacts, and an
idempotent off-plane script that performed the write under human authorization.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/52-evidence-bounded-real-target-adoption/52-CONTEXT.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-RESEARCH.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-VALIDATION.md
@AGENTS.md
@harness/commands/contract-check.md
@harness/skills/new-contract-rule/SKILL.md
@harness/skills/data-contracts/SKILL.md
</context>

<interfaces>
All commands run from `/Users/hyojung/Desktop/2026/lifetimeworkflow`.

The exact enum being extended (`contracts/harness/adoption/inventory.schema.json`, `$defs.excludedEntry.properties.excluded.enum`) currently holds, in this order:
`secret-path`, `secret-content`, `binary`, `vendored`, `generated`, `source-dump`, `size-capped`, `symlink-escape`.
The new value is the literal string `non-workspace-member`, appended AFTER `symlink-escape` (the list is semantically ordered, not sorted).

Baseline/gate/derived entry points, all already shipped — never re-implement any of them:
- rebaseline: `uv run python -m tools.contract_hash.hash --write`
- drift gate: `uv run python -m tools.contract_drift.drift` (exit 0 == live tree matches `contracts/.hashes/manifest.json`)
- instance validation: `uv run check-jsonschema --schemafile <schema> <sibling-instance>` (this schema has no sibling instance file; CI prints a visible SKIP — that is the expected, documented no-op)
- derived regeneration: `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index`

`contracts/**` is on the constitution plane: `tools/hooks/contract_guard.py` (`CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**", "docs/glossary.md"]`) denies agent Write/Edit there unless `GOLDEN_APPROVE_HUMAN` is non-empty, and an agent must never fabricate that token. The write therefore happens through a human-run script, never through an agent tool call.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the idempotent off-plane enum applier and prove it is a no-op today</name>
  <files>.planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py</files>
  <read_first>
    contracts/harness/adoption/inventory.schema.json (the exact enum list, the `additionalProperties: false` + `required` shape, and the `excluded` description text this task extends),
    tools/contract_hash/hash.py (the `--write` CLI + `MANIFEST_PATH`/`CONTRACTS_DIR` constants the script must reuse, never re-derive),
    tools/contract_drift/drift.py (module docstring: additive enum case == `non-breaking` classification — the class this change must fall into),
    tools/hooks/contract_guard.py (`CONSTITUTION_GLOBS`, `APPROVAL_ENV` — why this script exists at all),
    harness/skills/new-contract-rule/SKILL.md (the repo's own contract-change procedure)
  </read_first>
  <action>
    Create `apply-inventory-enum.py` as a stdlib-only script with two modes: `--check` (default, writes nothing) and `--write`.

    Behavior in both modes: load `contracts/harness/adoption/inventory.schema.json` with `json.load`; assert the current `$defs.excludedEntry.properties.excluded.enum` equals the exact 8-value list named in `<interfaces>` OR already ends with `non-workspace-member` (idempotence); refuse with exit 2 and a message naming the unexpected list if neither holds — never "repair" a surprise shape.

    In `--write` mode: append the literal `non-workspace-member` to that enum; extend that property's `description` string by appending the sentence `OBS-D-01 (51-BASELINE-EVIDENCE.md, purpose 2 / D-20): non-workspace-member records a manifest found by the walk that lies outside the target's declared pnpm workspace globs — excluded from the member set, never silently dropped.` (this is the D-19 trace line, carried in the contract itself); re-serialize with `json.dumps(..., indent=2, ensure_ascii=False)` plus a single trailing newline so the file's existing formatting is preserved byte-for-byte outside the two edited spots; then invoke the shipped rebaseline by importing and calling `tools.contract_hash.hash`'s `--write` entry point (or `subprocess`-free direct call) rather than re-implementing JCS hashing; print the old and new digest for `contracts/harness/adoption/inventory.schema.json`.

    Re-running `--write` after a successful run must change nothing and exit 0 (idempotent).

    Do NOT edit `contracts/harness/adoption/inventory.schema.json` from this task — Task 2 is the authorized write.
  </action>
  <verify>
    <automated>uv run python .planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py --check; test $? -eq 0 || exit 1; git diff --quiet -- contracts/ || exit 1; uv run python -m tools.contract_drift.drift || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `apply-inventory-enum.py --check` exits 0 and `git diff --quiet -- contracts/` still succeeds (the check mode provably wrote nothing).
    - The script contains no `open(..., "w")`/`write_text`/`write_bytes` call reachable from the `--check` branch: `uv run python - <<'PY'` style inspection or a `grep -n "write" script` review confirms every write is under the `--write` guard.
    - The script hard-codes the expected 8-value pre-state and exits 2 on any other list — verified by running it against a scratch copy of the schema whose enum has been mutated (e.g. one value renamed) and observing exit 2, not exit 0. This is the mutation check: a script that "succeeds" on an unexpected schema is worthless.
    - `uv run python -m tools.contract_drift.drift` exits 0 (tree still baseline-clean before Task 2).
  </acceptance_criteria>
  <done>The applier exists, is provably write-free in check mode, refuses an unexpected schema shape, and the contracts tree is untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Human runs the constitution-plane write</name>
  <read_first>
    .planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py (the exact edit it will make),
    contracts/harness/adoption/inventory.schema.json (the pre-write state being changed),
    tools/hooks/contract_guard.py (why this write needs human authorization)
  </read_first>
  <files>contracts/harness/adoption/inventory.schema.json, contracts/.hashes/manifest.json</files>
  <action>Pause and hand control to the human. Do not write to `contracts/**` from any agent tool call and never set or fabricate `GOLDEN_APPROVE_HUMAN` — present the command in `<how-to-verify>`, wait for the resume signal, then re-read the two files to confirm what landed.</action>
  <verify>
    <automated>git diff --name-only -- contracts/ | LC_ALL=C sort | tr '\n' ' ' | grep -Fq 'contracts/.hashes/manifest.json contracts/harness/adoption/inventory.schema.json' || exit 1; test "$(find contracts -name '*.schema.json' | wc -l | tr -d ' ')" = "6" || exit 1; grep -q '"non-workspace-member"' contracts/harness/adoption/inventory.schema.json || exit 1; grep -q '34a31944180f766e993ca46940c7352a37458027ffc89818c1e8267cbea54531' contracts/.hashes/manifest.json && exit 1; exit 0</automated>
  </verify>
  <what-built>An idempotent script that appends exactly one enum value (`non-workspace-member`) plus its OBS-D-01 trace sentence to `contracts/harness/adoption/inventory.schema.json`, then re-derives `contracts/.hashes/manifest.json` via the shipped `tools.contract_hash.hash --write`.</what-built>
  <how-to-verify>
    1. Review the pending change: `uv run python .planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py --check` (prints the intended edit, writes nothing).
    2. Apply it under human authorization, from `/Users/hyojung/Desktop/2026/lifetimeworkflow`:
       `GOLDEN_APPROVE_HUMAN=1 uv run python .planning/phases/52-evidence-bounded-real-target-adoption/scripts/apply-inventory-enum.py --write`
    3. Confirm the diff is exactly two files and no more: `git diff --stat -- contracts/` should show only `contracts/harness/adoption/inventory.schema.json` and `contracts/.hashes/manifest.json`.
    4. Confirm the contract COUNT is still 6: `find contracts -name '*.schema.json' | wc -l`.
  </how-to-verify>
  <resume-signal>Type "approved" once the script has been run and the diff shows exactly those two files, or describe what differed.</resume-signal>
  <acceptance_criteria>
    - `git diff --name-only -- contracts/` lists exactly `contracts/.hashes/manifest.json` and `contracts/harness/adoption/inventory.schema.json`.
    - `find contracts -name '*.schema.json' | wc -l` prints `6`.
    - The `inventory.schema.json` digest recorded in `contracts/.hashes/manifest.json` differs from the pre-change value `34a31944180f766e993ca46940c7352a37458027ffc89818c1e8267cbea54531` (a hash that did NOT move would mean the edit never landed).
  </acceptance_criteria>
  <done>The schema carries the new enum value and the hash baseline was re-derived in the same authorized action.</done>
</task>

<task type="auto">
  <name>Task 3: Close the gate chain and regenerate every derived artifact carrying the old hash</name>
  <files>.memory/derived/contracts-index.md, docs/reference/inventory.md, tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr</files>
  <read_first>
    contracts/harness/adoption/inventory.schema.json (post-write state),
    contracts/.hashes/manifest.json (the re-derived baseline),
    tools/memory_regen/contracts_index.py (the hash-prefix row format that just went stale),
    tools/memory_regen/tests/test_contracts_index.py (`test_render_matches_committed_snapshot` — the syrupy snapshot embedding the hash prefix),
    tools/docs_sync/generate.py (how `docs/reference/inventory.md` is derived from the schema),
    harness/commands/refresh-memory.md (the exact regeneration argv this repo ships)
  </read_first>
  <action>
    Run the shipped derived-plane regenerators, never hand-edit their outputs:
    `uv run python -m tools.docs_sync` and `uv run python -m tools.memory_regen.contracts_index`.

    The contracts-index syrupy snapshot embeds the first-12-hex hash prefix of every contract, so it
    is now stale by construction — update it with `uv run pytest tools/memory_regen/tests/test_contracts_index.py --snapshot-update` and then re-run that file WITHOUT the flag to confirm it passes on its own. Do the same for any docs_sync snapshot that reds.

    Then run the whole suite plus the drift gate. Do NOT touch `tools/adoption_scan/**` here — Plan 02 owns the emitter; this plan's suite run must show that adding the enum value alone changed no behavior.
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift || exit 1; grep -q '"non-workspace-member"' contracts/harness/adoption/inventory.schema.json || exit 1; test "$(find contracts -name '*.schema.json' | wc -l | tr -d ' ')" = "6" || exit 1; grep -v '^#' .memory/derived/contracts-index.md | grep -q '34a31944180f' && exit 1; uv run pytest -q || exit 1; git diff --quiet -- tools/adoption_scan/ || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -m tools.contract_drift.drift` exits 0 (drift gate green with the paired baseline).
    - `contracts/harness/adoption/inventory.schema.json` contains the literal `"non-workspace-member"` inside the `excludedEntry` enum, and `python -c "import json;json.load(open('contracts/harness/adoption/inventory.schema.json'))"` succeeds.
    - `.memory/derived/contracts-index.md` no longer contains the stale prefix `34a31944180f` and DOES contain the new prefix that matches the first 12 hex chars of the `inventory.schema.json` entry in `contracts/.hashes/manifest.json` (assert the two agree, not merely that the file changed).
    - `uv run pytest tools/memory_regen/tests/test_contracts_index.py -q` passes WITHOUT `--snapshot-update`.
    - `uv run pytest -q` is fully green; `git diff --quiet -- tools/adoption_scan/` succeeds (no emitter change smuggled in).
    - `find contracts -name '*.schema.json' | wc -l` is `6` (NG-01: contract count unchanged).
  </acceptance_criteria>
  <done>The contract-first chain is complete and green: schema extended, hash rebaselined, derived plane regenerated, whole suite passing, no code emitting the new value yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → constitution plane (`contracts/**`) | A CODEOWNERS-gated, hash-baselined plane an agent must not write unilaterally. |
| schema edit → drift/golden baselines | A schema change silently unpaired with its baseline would let later drift pass unnoticed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Elevation of Privilege | `contracts/harness/adoption/inventory.schema.json` write | high | mitigate | Write happens only via the Task-2 `checkpoint:human-action` under a human-supplied `GOLDEN_APPROVE_HUMAN`; the agent never sets or fabricates that token, and Task 1 is provably write-free. |
| T-52-02 | Tampering | Enum extension beyond the sanctioned single value | medium | mitigate | The applier asserts the exact 8-value pre-state, appends exactly one literal, and Task 2's acceptance limits the diff to two files verified by `git diff --name-only`. |
| T-52-03 | Repudiation | Hash change without a paired baseline/golden update | high | mitigate | Rebaseline is performed inside the same script invocation; Task 3 gates on `tools.contract_drift.drift` exit 0 plus a hash-prefix agreement assertion between the baseline and the derived contracts-index. |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages (52-RESEARCH.md: zero new packages in the dependency graph); the Package Legitimacy Gate has nothing to audit. |
</threat_model>

<verification>
- `uv run python -m tools.contract_drift.drift` exits 0.
- `uv run pytest -q` fully green.
- Contract file count is exactly 6.
- `git diff --name-only` for this plan touches no file under `tools/adoption_scan/`, `tools/adoption_apply/`, or `tools/harness_config/`.
</verification>

<success_criteria>
The inventory contract can express `non-workspace-member`, the drift gate and the whole test suite
are green with the baseline and derived plane re-derived in the same change, and no code emits the
value yet — Plan 02 is unblocked and contract-first order is provably intact.
</success_criteria>

<output>
Create `.planning/phases/52-evidence-bounded-real-target-adoption/52-01-SUMMARY.md` when done.
</output>
