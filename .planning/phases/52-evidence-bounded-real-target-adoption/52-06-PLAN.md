---
phase: 52-evidence-bounded-real-target-adoption
plan: 06
type: execute
wave: 5
depends_on: ["52-05"]
files_modified:
  - .planning/phases/52-evidence-bounded-real-target-adoption/evidence/downstream/
  - .planning/phases/52-evidence-bounded-real-target-adoption/evidence/isolation/
  - .planning/phases/52-evidence-bounded-real-target-adoption/evidence/disposal/
  - .planning/phases/52-evidence-bounded-real-target-adoption/52-ADOPTION-EVIDENCE.md
autonomous: true
requirements: [RTA-01, RTA-03, RTA-04, OBS-02]

must_haves:
  truths:
    - "Package facts and convention profiles are produced by calling the library functions with the WORKTREE passed explicitly, so a green command can never silently describe the harness checkout (D-05, D-12 carried from Phase-51 D-12)."
    - "The real packages/shared → apps/frontend and packages/shared → apps/backend runtime edges are recorded from the adopted target's own manifests (SC-3 / RTA-03)."
    - "Each adopted package resolves a nearest-wins convention profile whose lint AND test values are non-null for the JavaScript packages, read from the TARGET's emitted harness/project.toml (SC-4 / RTA-04)."
    - "The original develop checkout's after-proof is compared explicitly; any HEAD/index delta is attributed to third-party commit movement by reconstructing index digests from the target's commit trees, and recorded OUTSIDE the OBS-D namespace — never filed as an adoption defect and never silently absorbed (D-03)."
    - "The worktree is auto-disposed with a recorded exit code and no human checkpoint (D-04), and the phase record shows every observation terminating in a repair-plus-test or an evidence-backed confirmation — SC-5 admits no third outcome (D-19)."
  artifacts:
    - path: ".planning/phases/52-evidence-bounded-real-target-adoption/evidence/downstream/package-facts.json"
      provides: "Target-explicit package and dependency-edge facts"
    - path: ".planning/phases/52-evidence-bounded-real-target-adoption/evidence/downstream/conventions.json"
      provides: "Target-explicit per-package nearest-wins convention profiles including lint"
    - path: ".planning/phases/52-evidence-bounded-real-target-adoption/evidence/isolation/comparison.json"
      provides: "Explicit equality booleans for every D-03 proof dimension, before and after disposal"
    - path: ".planning/phases/52-evidence-bounded-real-target-adoption/52-ADOPTION-EVIDENCE.md"
      provides: "The phase record: reproducibility header, SC-1..SC-5 verdicts, and the OBS-D trace ledger"
      contains: "OBS-D-01"
  key_links:
    - from: "evidence/downstream/package-facts.json"
      to: "evidence/downstream/workspace-edge-comparison.json"
      via: "exact from/to/kind tuple comparison for the shared package's two consumers"
      pattern: "runtime"
    - from: "evidence/downstream/conventions.json"
      to: "evidence/downstream/convention-comparison.json"
      via: "per-package lint/test non-null verdict"
      pattern: "lint"
    - from: "evidence/isolation/before.index.sha256"
      to: "evidence/isolation/comparison.json"
      via: "byte comparison recorded as booleans plus a commit-tree drift attribution"
      pattern: "index_equal"
---

<objective>
Close the phase: run the two read-only downstream observations against the applied worktree
(package facts, convention profiles), decide SC-2/SC-3/SC-4 from them, complete the original
checkout's after-proof with third-party drift attributed rather than hidden, auto-dispose the
worktree, and write the phase record with the SC-5 trace ledger.

Purpose: SC-3 and SC-4's proof comes from reading the adopted target after apply; SC-1's proof is
completed only after the run; SC-5 requires every OBS-D observation to terminate in either a repair
with a regression test or a written evidence-backed confirmation.

Output: downstream observation captures and comparisons, the isolation after-proof plus disposal,
and `52-ADOPTION-EVIDENCE.md`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-evidence-bounded-real-target-adoption/52-CONTEXT.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-RESEARCH.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-VALIDATION.md
@.planning/phases/52-evidence-bounded-real-target-adoption/52-05-SUMMARY.md
@.planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md
@AGENTS.md
</context>

<interfaces>
Fixed paths (identical to Plan 05): harness cwd `/Users/hyojung/Desktop/2026/lifetimeworkflow`;
original checkout `/Users/hyojung/Desktop/2026/FeedbackOps` (read-only); worktree
`/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-52-adoption`; evidence root
`.planning/phases/52-evidence-bounded-real-target-adoption/evidence`.

Downstream observations are LIBRARY calls with the target passed explicitly — never a bare CLI that
would default to this checkout (the Phase-51 D-12 rule):
- package facts: `tools.memory_regen.package_facts.build_facts(repo_root=<WORKTREE>)`
- conventions: `tools.harness_config.loader.load_project(<WORKTREE>/harness/project.toml)` then
  `tools.harness_config.loader.conventions_for(pkg["dir"], cfg=<that cfg>, facts=<those facts>)`
  for every package in the facts.
Each observation records `*.argv.txt`, `*.cwd.txt`, `*.stderr.txt`, `*.exit-code.txt` beside its
JSON, exactly as Phase 51 did.

Expected real-target values (52-RESEARCH.md / ROADMAP SC-2..SC-4): five members — root,
`packages/ui`, `packages/shared`, `apps/frontend`, `apps/backend`; the two runtime edges from
`packages/shared` to `apps/frontend` and `apps/backend` (recorded in the facts' `from`/`to`
direction, i.e. each app depends on the shared package); and, post-repair, a non-null `lint` and
`test` on the JavaScript packages' profiles.

The convention profile key set after Plan 03 is:
`package`, `dir`, `language`, `test`, `format`, `lint`, `bash_scope`, `agents_md`, `is_default`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Target-explicit downstream observations and the SC-2/SC-3/SC-4 verdicts</name>
  <files>.planning/phases/52-evidence-bounded-real-target-adoption/evidence/downstream/</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md (OBS-D-02 and OBS-D-03 reproduction commands — the exact target-explicit invocation shapes to reuse),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-03-SUMMARY.md (the derived [[languages]] row and the profile key set now in play),
    tools/memory_regen/package_facts.py (`build_facts(repo_root=...)` return shape: `packages` and `edges`),
    tools/harness_config/loader.py (`load_project`, `conventions_for` — and the caveat that `_nearest_agents_md` walks the HARNESS root, so `agents_md` is not a target-side fact),
    .planning/phases/52-evidence-bounded-real-target-adoption/evidence/discover/member-comparison.json (the SC-2 verdict already computed in Plan 05)
  </read_first>
  <action>
    Run the two observations against the applied worktree with the target passed explicitly, writing
    `evidence/downstream/package-facts.json` and `evidence/downstream/conventions.json` plus their
    argv/cwd/stderr/exit-code sidecars. `conventions.json` must record, per package, the package id,
    dir, language and the full profile dict, plus the `config_source` path actually used — so a
    reader can see whether the TARGET's `harness/project.toml` or a fallback was read.

    Then write three comparison files, each carrying computed booleans and the literal values they
    were computed from:
    - `workspace-edge-comparison.json`: the exact `{from,to,kind}` records connecting the shared
      package to each of the two app packages, plus `both_runtime_edges_present`.
    - `convention-comparison.json`: per package, `lint`, `test`, and `lint_and_test_present`; plus a
      top-level `all_js_packages_have_lint_and_test`.
    - `downstream/member-comparison.json` (a NEW file; do not overwrite Plan 05's): restate the
      inventory's `members_equal_expected_five` verdict alongside `build_facts_package_ids`,
      `build_facts_count`, and `six_packages_one_non_member`. `build_facts` is an UNSCOPED path
      (`git ls-files`, no workspace globs — `package_facts.py:93-114`), so it is expected to report
      SIX with `docs/design-prototype` as the non-member. That divergence from the inventory is the
      honest result and must be reported as such; SC-2 is decided by the inventory, not by this
      second path.

    A false verdict is a result, not a trigger to change code in this plan — record it and say so.
  </action>
  <verify>
    <automated>E=.planning/phases/52-evidence-bounded-real-target-adoption/evidence/downstream; for f in package-facts conventions; do uv run python -m json.tool "$E/$f.json" >/dev/null || exit 1; test -s "$E/$f.argv.txt" || exit 1; grep -Eq '^[0-9]+$' "$E/$f.exit-code.txt" || exit 1; grep -Fq '/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-52-adoption' "$E/$f.argv.txt" || exit 1; done; uv run python -c "
import json,sys
w=json.load(open('$E/workspace-edge-comparison.json')); c=json.load(open('$E/convention-comparison.json'))
ok = isinstance(w.get('both_runtime_edges_present'),bool) and isinstance(c.get('all_js_packages_have_lint_and_test'),bool)
prof=json.load(open('$E/conventions.json'))
ok = ok and all('lint' in p['conventions'] for p in prof['packages'])
sys.exit(0 if ok else 1)" || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - Both observation argv captures contain the worktree path — a run that silently described the harness checkout is structurally impossible to pass off as target evidence.
    - `conventions.json` shows `config_source` pointing at `<WORKTREE>/harness/project.toml`, and EVERY package's profile contains the `lint` key (OBS-D-03's shape repair, observed on the real target).
    - `convention-comparison.json` reports `lint` and `test` values verbatim per package; the JavaScript packages' values are non-null strings. If any is null, the SUMMARY names which package and why — no rounding up to "SC-4 met".
    - `workspace-edge-comparison.json` contains the two literal edge records and `both_runtime_edges_present` computed from them (not asserted).
    - **`build_facts` corroboration, stated truthfully (W-5).** `build_facts` -> `discover_manifests` (`package_facts.py:93-114`) enumerates via `git ls-files` + `detect.detect_manifests` with NO workspace-glob scoping — Phase 52 deliberately adds none there (that would exceed D-07, which scopes membership in `scan.build_inventory`). On the real target it therefore reports **SIX** packages: the five workspace members PLUS `docs/design-prototype`. Record the expected value as `six_packages_one_non_member`: assert the derived id set equals the five members plus exactly that one non-member, and state in the SUMMARY that this is the EXPECTED behavior of an unscoped code path — not an SC-2 failure and not a Phase-52 defect. SC-2's deciding artifact is `evidence/discover/member-comparison.json` (the inventory); SC-3's two runtime edges are unaffected by the extra package. Do NOT manufacture a five-way agreement by filtering the sixth out after the fact.
  </acceptance_criteria>
  <done>SC-2, SC-3 and SC-4 each have a target-explicit, recomputable verdict backed by literal captured values.</done>
</task>

<task type="auto">
  <name>Task 2: Original-checkout after-proof with drift attribution, then auto-dispose the worktree</name>
  <files>.planning/phases/52-evidence-bounded-real-target-adoption/evidence/isolation/, .planning/phases/52-evidence-bounded-real-target-adoption/evidence/disposal/</files>
  <read_first>
    .planning/phases/52-evidence-bounded-real-target-adoption/evidence/isolation/ (the Plan-05 `before.*` captures this compares against),
    .planning/phases/51-real-target-observation-baseline/51-03-PLAN.md (the disposal argv, the final capture set, and the `post_disposal` comparison block shape),
    .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md (§ Isolation and external drift — the commit-tree index-digest reconstruction method for attributing HEAD/index deltas),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-CONTEXT.md (D-03 attribution rule, D-04 auto-disposal without a human checkpoint)
  </read_first>
  <action>
    Re-capture the three D-03 artifacts against the ORIGINAL checkout into `after.status.txt`,
    `after.head.txt`, `after.index.sha256`, `after.untracked-set.sha256`, plus
    `after.worktree-list.txt`. Write `evidence/isolation/comparison.json` with
    `status_equal`, `head_equal`, `index_equal`, `untracked_set_equal` as computed booleans.

    If `head_equal` or `index_equal` is false, attribute it: reconstruct the index digest from the
    target's own commit trees (`git -C <ORIG> ls-tree -r <sha>` piped through the same digest
    pipeline) for both the before and after HEADs, and write
    `evidence/isolation/external-drift.json` recording the two SHAs, the commits between them, the
    reconstructed digests, and an explicit statement that the delta is third-party `develop`
    movement, recorded OUTSIDE the OBS-D namespace. State the residual honestly: an equality proof
    over an empty untracked set has low discriminating power (as Phase 51 disclosed).

    Then auto-dispose (D-04, no human checkpoint), capturing the five sidecars into
    `evidence/disposal/`:
    `git -C /Users/hyojung/Desktop/2026/FeedbackOps worktree remove --force /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-52-adoption`.
    Use that exact single path — no glob, no `rm -rf`, no `prune`, no adjacent worktree operation.
    Afterwards capture `final.worktree-list.txt`, `final.original-status.txt`, `final.head.txt`,
    `final.index.sha256`, `final.untracked-set.sha256`, and extend `comparison.json` with a
    `post_disposal` block carrying the same four booleans.
  </action>
  <verify>
    <automated>E=.planning/phases/52-evidence-bounded-real-target-adoption/evidence; uv run python -m json.tool "$E/isolation/comparison.json" >/dev/null || exit 1; uv run python -c "
import json,sys
c=json.load(open('$E/isolation/comparison.json')); keys=('status_equal','head_equal','index_equal','untracked_set_equal')
p=c.get('post_disposal')
sys.exit(0 if all(isinstance(c.get(k),bool) for k in keys) and isinstance(p,dict) and all(isinstance(p.get(k),bool) for k in keys) else 1)" || exit 1; uv run python -c "
import json,sys
c=json.load(open('$E/isolation/comparison.json'))
import os
need = not (c['head_equal'] and c['index_equal'])
sys.exit(0 if (not need) or os.path.exists('$E/isolation/external-drift.json') else 1)" || exit 1; grep -Fq 'worktree remove --force /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-52-adoption' "$E/disposal/argv.txt" || exit 1; grep -Eq '^[0-9]+$' "$E/disposal/exit-code.txt" || exit 1; test ! -e /Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-52-adoption || exit 1; grep -Fq '/Users/hyojung/Desktop/2026/FeedbackOps-worktrees/v27-52-adoption' "$E/disposal/final.worktree-list.txt" && exit 1; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `comparison.json` carries all four booleans plus a `post_disposal` block with the same four, all computed from the captured files (a hand-written `true` is a falsification; the SUMMARY must quote the digests compared).
    - Whenever `head_equal` or `index_equal` is false, `external-drift.json` exists and names both SHAs, the intervening commits, and the reconstructed index digests proving the delta is commit movement, not a Phase-52 write. The attribution is recorded outside the OBS-D namespace.
    - `status_equal` is true and the original checkout's working tree is unmodified.
    - The disposal argv is the exact single-path `worktree remove --force` form; the worktree path no longer exists and is absent from `final.worktree-list.txt`.
    - `evidence/disposal/exit-code.txt` is recorded even if nonzero (a failed disposal is evidence and a blocker to report, not something to retry with a different command).
  </acceptance_criteria>
  <done>The original checkout is proven unmodified with any HEAD/index delta attributed to third-party drift, and the worktree is disposed with the outcome recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Write the phase record and the SC-5 trace ledger</name>
  <files>.planning/phases/52-evidence-bounded-real-target-adoption/52-ADOPTION-EVIDENCE.md</files>
  <read_first>
    .planning/phases/51-real-target-observation-baseline/51-BASELINE-EVIDENCE.md (the record's shape: Reproducibility header, isolation section, observation table, verdicts, disposal result — mirror it),
    .planning/phases/52-evidence-bounded-real-target-adoption/52-01-SUMMARY.md,
    .planning/phases/52-evidence-bounded-real-target-adoption/52-02-SUMMARY.md,
    .planning/phases/52-evidence-bounded-real-target-adoption/52-03-SUMMARY.md,
    .planning/phases/52-evidence-bounded-real-target-adoption/52-04-SUMMARY.md,
    .planning/phases/52-evidence-bounded-real-target-adoption/52-05-SUMMARY.md,
    .planning/phases/52-evidence-bounded-real-target-adoption/52-CONTEXT.md (D-19 and the purpose tags),
    .planning/ROADMAP.md (Phase 52 SC-1..SC-5, the exact wording each verdict answers)
  </read_first>
  <action>
    Write `52-ADOPTION-EVIDENCE.md` with:

    1. **Reproducibility** — harness SHA, target develop SHA as re-read at run time, worktree path,
       cwd, tool versions pointer, and the standing rule that nonzero/blocked outcomes are evidence.
    2. **Isolation and external drift** — the four comparison booleans, the attribution verdict, and
       the honest residual about the untracked-set digest's discriminating power.
    3. **Success-criterion verdicts** — one row per SC-1..SC-5 with the literal deciding artifact
       path and the deciding value quoted, not paraphrased.
    4. **OBS-D trace ledger (SC-5, D-19)** — one row per observation with: OBS-D id, purpose tag
       (①②③④), disposition (repaired / no-change-evidence-backed), the code change(s) by file, the
       terminating test(s) by node id, and the real-target evidence path. Every one of OBS-D-01,
       OBS-D-02, OBS-D-03, OBS-D-04 must appear with a non-empty terminating test — SC-5 admits no
       third outcome. Include the OBS-03 line restating it as REFUTED-and-locked-in, and state that
       no Phase-52 budget was spent on it.
    5. **Scope-fence statement** — what was deliberately NOT done: no target `.gitignore` write, no
       sidecar unlink, no non-pnpm workspace formats, no new contract/command/skill/CI job/gate, the
       contract count still 6, and the recorded-only Phase-51 secret-scan gap left untouched.

       This section MUST also carry three consequences that Phase 53 would otherwise rediscover as
       defects:
       - **`harness/project.toml` re-run classification (W-10).** The D-12 splice appends a
         target-derived `[[languages]]` row to the applied `harness/project.toml`, so the target's
         copy no longer matches any entry in `destinations.harness_proposed_hashes()`. A Phase-53
         managed re-run will classify that ONE destination as `conflict`, not as the observable
         no-op Phase 53's SC-2 anticipates. It is a recorded consequence of a locked decision
         (D-12), resolvable only by MONO-12 re-run/update semantics, which 52-CONTEXT.md defers to
         Phase 53. Quote 52-03's SUMMARY finding verbatim.
       - **The prior-run lock-sidecar report is provenance, not staleness (W-3).** Because D-15
         forbids unlinking the sidecar, the `pre_existed` predicate is true on every run after the
         first and cannot distinguish a normal re-run from a crash-interrupted one. Phase 53
         inherits that as a known, documented gap — never as a working staleness signal.
       - **`build_facts` is unscoped (W-5).** It reports six packages on the real target, including
         `docs/design-prototype`. Workspace scoping lives in `scan.build_inventory` only; extending
         it into `discover_manifests` was out of scope for D-07 and remains unbuilt.
    6. **Disposal result** — the exact argv, exit code, and the post-disposal comparison.

    Contain no model identifiers anywhere (NG/AGENTS.md rule).
  </action>
  <verify>
    <automated>F=.planning/phases/52-evidence-bounded-real-target-adoption/52-ADOPTION-EVIDENCE.md; test -s "$F" || exit 1; for id in OBS-D-01 OBS-D-02 OBS-D-03 OBS-D-04 OBS-03; do grep -q "$id" "$F" || exit 1; done; for sc in SC-1 SC-2 SC-3 SC-4 SC-5; do grep -q "$sc" "$F" || exit 1; done; sed -E 's/[.]?CLAUDE[.]md([.]lock)?//g; s/[.]?claude\/[A-Za-z0-9_.\/-]*//g' "$F" | grep -Eiq 'claude|gpt|opus|sonnet|codex' && exit 1; test "$(find contracts -name '*.schema.json' | wc -l | tr -d ' ')" = "6" || exit 1; uv run pytest -q || exit 1; uv run python -m tools.contract_drift.drift || exit 1</automated>
  </verify>
  <acceptance_criteria>
    - The trace ledger has a row for each of OBS-D-01..04 with a non-empty "terminating test" cell containing a runnable pytest node id (e.g. `tools/adoption_scan/tests/test_scan_exclusions.py::test_...`), and each cited node id actually exists: verify by running `uv run pytest <node id>` for every cited id and recording the results in the SUMMARY.
    - Each SC row quotes the literal deciding value from the named evidence file (a SHA, a boolean, an edge record, a command string) — a verdict with no quoted value is not acceptable.
    - The scope-fence section explicitly states the contract count is 6 and that command/skill/CI-job/gate counts did not grow (NG-01).
    - No model identifier appears anywhere in the file. **The check must strip the mandated artifact names before matching (B-2).** This same task requires the OBS-D-04 ledger row to name `.CLAUDE.md.lock` and `.claude/.settings.json.lock`, and requires quoting deciding values out of `worktree.changed-paths.json`, which lists `.claude/settings.json` (`MARKER_CAPABLE`, `destinations.py:101`). A bare case-insensitive `claude` match would therefore fail on content the task itself mandates — forcing the author to either omit required content or weaken the check. The `<verify>` block pipes the file through `sed -E 's/[.]?CLAUDE[.]md([.]lock)?//g; s/[.]?claude\/[A-Za-z0-9_.\/-]*//g'` first, so only genuine model identifiers can match. Confirm the guard still fires: append a scratch line containing a model identifier, observe the check exit 1, remove the line, and record that observation in the SUMMARY.
    - `uv run pytest -q` and `uv run python -m tools.contract_drift.drift` are both green at phase close.
  </acceptance_criteria>
  <done>The phase record exists with per-SC verdicts backed by quoted values and a complete OBS-D trace ledger in which every observation terminates in a test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downstream library calls → target vs harness checkout | A call defaulting to this repo would produce a green result describing the wrong tree. |
| disposal command → external repository | `worktree remove --force` is a destructive operation on someone's real repo. |
| phase record → future decisions | An overstated verdict silently sets the baseline for Phase 53. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-20 | Spoofing | Downstream observation silently describing the harness checkout | high | mitigate | Every observation passes `repo_root=`/`cfg=` explicitly; the automated verify greps each `*.argv.txt` for the worktree path and fails otherwise; `conventions.json` records the `config_source` actually read. |
| T-52-21 | Denial of Service / Tampering | `worktree remove --force` hitting the wrong path | high | mitigate | Exactly one absolute path, no glob, no `prune`, no `rm -rf`; the argv is grep-asserted verbatim; the post-disposal worktree list and the original checkout's status are both re-captured and compared. |
| T-52-22 | Repudiation | A HEAD/index delta absorbed silently or misfiled as an adoption defect | high | mitigate | D-03's attribution is mandatory whenever either boolean is false: `external-drift.json` must exist with reconstructed commit-tree digests, recorded outside the OBS-D namespace. The automated verify enforces the conditional existence. |
| T-52-23 | Repudiation | An SC verdict asserted without a deciding value | medium | mitigate | Each SC row must quote the literal value from the named artifact; the ledger's test cells must be runnable node ids, each executed to confirm. |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | No installs in this plan. |
</threat_model>

<verification>
- `evidence/downstream/*.json` all parse; both argv captures name the worktree.
- `evidence/isolation/comparison.json` carries the four booleans plus a `post_disposal` block; `external-drift.json` exists whenever a HEAD/index delta was observed.
- The worktree path no longer exists and is absent from the final worktree list.
- `52-ADOPTION-EVIDENCE.md` names OBS-D-01..04, OBS-03 and SC-1..SC-5, with no model identifiers.
- `uv run pytest -q` green and `uv run python -m tools.contract_drift.drift` exit 0.
</verification>

<success_criteria>
The adopted target's own facts prove the five members, the two shared-package runtime edges and
per-package lint+test convention profiles; the original checkout is proven unmodified with any
delta attributed to third-party drift; the worktree is disposed; and every Phase-51 observation
terminates in a repair-plus-regression-test or an evidence-backed confirmation with a runnable test
node id.
</success_criteria>

<output>
Create `.planning/phases/52-evidence-bounded-real-target-adoption/52-06-SUMMARY.md` when done.
</output>
