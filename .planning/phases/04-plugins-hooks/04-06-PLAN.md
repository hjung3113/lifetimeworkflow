---
phase: 04-plugins-hooks
plan: 06
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified:
  - .claude/settings.json
  - tools/hooks/tests/test_settings_coexist.py
  - harness/plugins/contract-guard.ts
  - harness/plugins/secret-scan.ts
  - harness/plugins/format-on-write.ts
  - harness/plugins/polyglot-lint.ts
autonomous: true
requirements: [HOOK-01, HOOK-02, HOOK-03, HOOK-04, POLY-01]

must_haves:
  truths:
    - "The four new gates fire as live Claude hooks: contract-guard + secret_scan (PreToolUse), format_on_write (PostToolUse), commit_gate (PreToolUse Bash)."
    - "Every pre-existing GSD hook command (SessionStart x4, PreToolUse x4, PostToolUse x3) is still present after the settings.json edit."
    - "Each opencode plugin stub is authored (mirroring session-inject.ts resume-note) and consumes the same `python -m tools.hooks.*` contract — execution deferred."
  artifacts:
    - path: ".claude/settings.json"
      provides: "appended PreToolUse/PostToolUse slots for the four gates (GSD slots preserved)"
      contains: "tools.hooks.contract_guard"
    - path: "tools/hooks/tests/test_settings_coexist.py"
      provides: "proof GSD hooks survive + new gates registered"
      contains: "gsd-"
    - path: "harness/plugins/contract-guard.ts"
      provides: "authored-only opencode tool.execute.before stub -> shared python gate"
      contains: "tool.execute.before"
  key_links:
    - from: ".claude/settings.json"
      to: "tools/hooks/contract_guard.py"
      via: "PreToolUse command uv run python -m tools.hooks.contract_guard"
      pattern: "tools.hooks.contract_guard"
    - from: "harness/plugins/contract-guard.ts"
      to: "tools/hooks/contract_guard.py"
      via: "same shell contract (authored, not run)"
      pattern: "tools.hooks.contract_guard"
---

## Phase Goal

**As a** developer or coding agent working in this polyglot harness, **I want to** have the Phase 1-3 contract, §4.3-4.6 boundary, and permission rules enforced by non-bypassable runtime hooks, **so that** a violation is blocked at write/commit time instead of relying on advisory prose.

> NOTE (planner): ROADMAP Phase 4 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived; ratify via `/gsd mvp-phase 4`.

<objective>
Wire the four Phase-4 gates into the live Claude runtime by APPENDING slots to `.claude/settings.json` (coexisting with the GSD hooks), prove coexistence with a test, and author (do not run) the matching opencode plugin stubs. This turns the unit-tested gates into non-bypassable runtime enforcement — closing the last mile of every Phase-4 success criterion.

Purpose: the gates are already unit-proven via crafted stdin (plans 01-05); this plan makes them FIRE in-session. The critical risk is silently clobbering GSD's own guards when editing the shared settings.json — so we append (never rewrite) and assert every pre-existing GSD hook command survives (Pitfall 1). opencode plugins are authored-only/deferred (D-01), mirroring session-inject.ts's resume-note banner and consuming the identical `python -m tools.hooks.*` contract.
Output: appended settings.json slots, a coexist test, and four authored opencode stubs.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-plugins-hooks/04-CONTEXT.md
@.planning/phases/04-plugins-hooks/04-RESEARCH.md
@CLAUDE.md
@.claude/settings.json
@harness/plugins/session-inject.ts
@tools/hooks/contract_guard.py
@tools/hooks/secret_scan.py
@tools/hooks/format_on_write.py
@tools/hooks/commit_gate.py

<interfaces>
<!-- Existing GSD hook commands that MUST survive (from .claude/settings.json) -->
SessionStart(4): gsd-check-update.js, gsd-session-state.sh, tools/bootstrap/install.sh, memory-inject.sh
PreToolUse(4): gsd-prompt-guard.js, gsd-read-guard.js, gsd-workflow-guard.js, gsd-validate-commit.sh
PostToolUse(3): gsd-context-monitor.js, gsd-read-injection-scanner.js, gsd-phase-boundary.sh
<!-- New slots to APPEND (04-RESEARCH.md settings.json append example) -->
PreToolUse += {matcher:"Write|Edit", cmd:"uv run python -m tools.hooks.contract_guard", timeout:10}
PreToolUse += {matcher:"Read|Write|Edit", cmd:"uv run python -m tools.hooks.secret_scan", timeout:10}
PreToolUse += {matcher:"Bash", cmd:"uv run python -m tools.hooks.commit_gate --from-hook", timeout:120}
PostToolUse += {matcher:"Write|Edit", cmd:"uv run python -m tools.hooks.format_on_write", timeout:30}
<!-- opencode stub shape: mirror harness/plugins/session-inject.ts resume-note banner + execFileSync argv (NOT shell); tool.execute.before (deny gates) / tool.execute.after (format) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coexist test first, then append settings.json slots</name>
  <files>tools/hooks/tests/test_settings_coexist.py, .claude/settings.json</files>
  <read_first>
    - .claude/settings.json (current arrays — 4 SessionStart, 4 PreToolUse, 3 PostToolUse; no Stop)
    - 04-RESEARCH.md Pattern 4 (coexist: append, never replace) + Pitfall 1 (dropping GSD guards) + settings.json append example
  </read_first>
  <action>FIRST write test_settings_coexist.py: parse `.claude/settings.json` with stdlib json and assert (a) every pre-existing GSD command substring is present in its array (gsd-check-update, gsd-session-state, bootstrap/install.sh, memory-inject, gsd-prompt-guard, gsd-read-guard, gsd-workflow-guard, gsd-validate-commit, gsd-context-monitor, gsd-read-injection-scanner, gsd-phase-boundary), and (b) each of the four new gate commands (tools.hooks.contract_guard, tools.hooks.secret_scan, tools.hooks.commit_gate, tools.hooks.format_on_write) is registered under the correct event array with the expected matcher. Run it RED (new gates not yet wired). THEN edit `.claude/settings.json` by APPENDING the four slots (three to PreToolUse, one to PostToolUse) exactly as in the interfaces block — do NOT rewrite existing objects, do NOT reorder. Re-run the test to GREEN. Keep the additions minimal (Phase 6 emitter will own generation).</action>
  <verify>
    <automated>uv run pytest tools/hooks/tests/test_settings_coexist.py -x -q</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/hooks/tests/test_settings_coexist.py -x -q` passes. `python -c "import json;d=json.load(open('.claude/settings.json'));print(len(d['hooks']['PreToolUse']),len(d['hooks']['PostToolUse']))"` prints `7 4` (4 GSD + 3 new PreToolUse; 3 GSD + 1 new PostToolUse). All eleven GSD command substrings still present.</acceptance_criteria>
  <done>Four gates wired as live Claude hooks; every GSD hook command preserved; coexist test green.</done>
</task>

<task type="auto">
  <name>Task 2: Author opencode plugin stubs (deferred, not run)</name>
  <files>harness/plugins/contract-guard.ts, harness/plugins/secret-scan.ts, harness/plugins/format-on-write.ts, harness/plugins/polyglot-lint.ts</files>
  <read_first>
    - harness/plugins/session-inject.ts (resume-note banner + execFileSync argv idiom + graceful-degrade pattern to mirror exactly)
    - 04-RESEARCH.md §opencode plugin shape + Assumption A1 (hook names MEDIUM confidence — re-verify at opencode wiring) + D-01 (authored-only/deferred)
  </read_first>
  <action>Author four `.ts` stubs mirroring session-inject.ts: each carries the RESUME-NOTE banner (AUTHORED-ONLY, EXECUTION DEFERRED — no opencode runtime; re-verify hook names A1), imports `execFileSync` from node:child_process, and shells the SAME contract as the Claude hook — `uv run python -m tools.hooks.contract_guard` / `secret_scan` / `format_on_write` and `python -m tools.polyglot_lint.lint` — via argv (NOT a shell string, command-injection defense). contract-guard + secret-scan use `tool.execute.before` (deny by throwing/returning a block); format-on-write + polyglot-lint use `tool.execute.after` (mutate/report). Do NOT wire them into any opencode.json and do NOT execute them (deferred). Export a default plugin function per session-inject.ts shape.</action>
  <verify>
    <automated>test -f harness/plugins/contract-guard.ts && test -f harness/plugins/secret-scan.ts && test -f harness/plugins/format-on-write.ts && test -f harness/plugins/polyglot-lint.ts && grep -l "EXECUTION DEFERRED" harness/plugins/contract-guard.ts</automated>
  </verify>
  <acceptance_criteria>All four stubs exist, each contains the EXECUTION DEFERRED resume-note and references its `tools.hooks.*` / `tools.polyglot_lint` python contract; none is wired into opencode.json.</acceptance_criteria>
  <done>Four authored opencode stubs share the single enforcement contract with the live Claude hooks; execution deferred per D-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-4 edit -> .claude/settings.json (shared live config) | A careless rewrite silently drops GSD's own guards (prompt/read/workflow/commit + context/phase-boundary), disabling enforcement. |
| authored opencode stub -> deferred runtime | The stub documents the contract but must never be executed here (no opencode runtime). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | GSD hooks clobbered on settings.json edit | mitigate | Append-only merge (never rewrite objects); test_settings_coexist asserts all eleven GSD command substrings survive AND the four new gates are registered (Pitfall 1). |
| T-04-18 | Repudiation | a new gate silently not wired (enforcement gap) | mitigate | Coexist test asserts each of the four gate commands is present under the correct event/matcher; written RED-first so absence fails. |
| T-04-19 | Elevation of Privilege | opencode stub command injection if later executed | mitigate | Stubs use `execFileSync(argv)` not shell strings (mirror session-inject.ts); authored-only until A1 hook names re-verified. |
| T-04-SC | Tampering | dependency supply chain | accept | Zero new packages — settings.json wiring + TS stubs reuse node (present) and the in-repo python gates; no install. |
</threat_model>

<verification>
- `uv run pytest tools/hooks/tests/test_settings_coexist.py -x -q` green.
- `uv run pytest` full suite green.
- Manual demo of the four success criteria end-to-end: editing contracts/ is blocked; a BOM/CRLF TSV fails the polyglot linter; an AWS-key write is blocked; the commit gate exits non-zero on drift — all via the now-live hooks, with GSD guards still firing.
</verification>

<success_criteria>
All four gates fire as live, coexisting Claude hooks (GSD guards preserved), and four authored opencode stubs share the single enforcement contract — completing the runtime-enforcement layer for HOOK-01/02/03/04 + POLY-01.
</success_criteria>

<output>
Create `.planning/phases/04-plugins-hooks/04-06-SUMMARY.md` when done.
</output>
