---
phase: 04-plugins-hooks
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/hooks/__init__.py
  - tools/hooks/_stdin.py
  - tools/hooks/pyproject.toml
  - tools/hooks/secret_scan.py
  - tools/hooks/tests/__init__.py
  - tools/hooks/tests/test_stdin.py
  - tools/hooks/tests/test_secret_scan.py
autonomous: true
requirements: [HOOK-02]

must_haves:
  truths:
    - "The shared stdin adapter parses a Claude PreToolUse JSON event into a typed record and emits a well-formed deny decision."
    - "A real secret (AWS access key or PEM private-key header) in write content is denied."
    - "A write/read to a *.env path is denied (via the resolver over a SECRET-specific glob subset [\"*.env\",\"**/*.env\"], reused not re-implemented)."
    - "A benign high-entropy fixture (tests/, golden/, libs/normalize-fixtures/) is allowed — no false positive."
    - "A write to the constitution plane (contracts/**, docs/adr/**, golden/**) is NOT denied by secret_scan — that is contract-guard's job (which honors the GOLDEN_APPROVE_HUMAN bypass); secret_scan must not shadow that approval path (composition invariant, see 04-06)."
  artifacts:
    - path: "tools/hooks/_stdin.py"
      provides: "parse Claude hook stdin -> dataclass; emit_deny/emit_allow decision JSON"
      contains: "def parse_event"
    - path: "tools/hooks/secret_scan.py"
      provides: "HOOK-02 PreToolUse gate: resolver path-deny + shape-anchored regex scan; main() exit 0 + JSON"
      contains: "def main"
    - path: "tools/hooks/pyproject.toml"
      provides: "uv workspace member (package=false), zero deps"
      contains: "package = false"
  key_links:
    - from: "tools/hooks/secret_scan.py"
      to: "tools/harness_perms/resolver.py"
      via: "resolve_path over matrix path_deny_globs"
      pattern: "resolve_path"
---

## Phase Goal

**As a** developer or coding agent working in this polyglot harness, **I want to** have the Phase 1-3 contract, §4.3-4.6 boundary, and permission rules enforced by non-bypassable runtime hooks, **so that** a violation is blocked at write/commit time instead of relying on advisory prose.

> NOTE (planner): ROADMAP Phase 4 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived; ratify via `/gsd mvp-phase 4`.

<objective>
Establish the shared `tools/hooks/` gate package (the thin stdin adapter every Phase-4 hook uses) AND the HOOK-02 secret-protection gate. The adapter maps Claude's hook stdin JSON to a typed record and back to a decision; secret_scan denies reads/writes of secret material via the reused resolver path-deny plus a shape-anchored regex set.

Purpose: (a) create the `tools/hooks` skeleton (`_stdin.py`, `__init__.py`, `pyproject.toml`) that plans 03/04/05 add gate modules into without touching each other's files; (b) deliver HOOK-02 (half of success criterion 3). Secret patterns are shape-anchored (not generic entropy) with a fixture allow-list so the repo's own high-entropy test data does not trip the gate (Pitfall 5 / Open Q2).
Output: a stdlib-only uv member with a tested stdin adapter and a secret gate proven to block a real secret and allow a benign fixture.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugins-hooks/04-CONTEXT.md
@.planning/phases/04-plugins-hooks/04-RESEARCH.md
@CLAUDE.md
@tools/harness_perms/resolver.py
@tools/harness_perms/__init__.py
@harness/permission-matrix.json
@tools/golden_runner/pyproject.toml
@.claude/hooks/gsd-read-guard.js

<interfaces>
<!-- Reused verbatim — DO NOT re-implement (D-02). From tools/harness_perms/resolver.py -->
From tools.harness_perms:
```python
def resolve_path(deny_globs: list[str], path: str) -> str   # "deny" if any glob matches else "allow"
def load_matrix(path=...) -> dict                            # matrix["path_deny_globs"] = ["contracts/**","docs/adr/**","golden/**","*.env","**/*.env"]
```
IMPORTANT (composition, 04-06): secret_scan MUST call `resolve_path` over a SECRET-SPECIFIC subset
`SECRET_PATH_GLOBS = ["*.env", "**/*.env"]` — NOT the full `path_deny_globs`. The constitution-plane
globs (`contracts/**`, `docs/adr/**`, `golden/**`) belong to contract-guard (04-03), which honors the
`GOLDEN_APPROVE_HUMAN` bypass. If secret_scan denied the full list, a constitution write WITH the
approval token would still be blocked by secret_scan (any-deny-wins aggregation) — killing the bypass.
Reuse the resolver (D-02), just feed it the secret subset.
Claude PreToolUse stdin (verified fields, 04-RESEARCH Pattern 2):
```jsonc
{ "session_id":"…", "hook_event_name":"PreToolUse", "cwd":"…",
  "tool_name":"Write|Edit|Read|Bash",
  "tool_input": { "file_path":"…", "content":"…", "command":"…" } }
```
PreToolUse deny shape (exit 0 + stdout JSON):
```jsonc
{ "hookSpecificOutput": { "hookEventName":"PreToolUse",
    "permissionDecision":"deny", "permissionDecisionReason":"…" } }
```
Exit-2 + stderr is the loud fallback. No decision -> print nothing, exit 0 (normal permission flow).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared stdin adapter + uv member skeleton, failing tests (RED then GREEN for adapter)</name>
  <files>tools/hooks/__init__.py, tools/hooks/_stdin.py, tools/hooks/pyproject.toml, tools/hooks/tests/__init__.py, tools/hooks/tests/test_stdin.py</files>
  <read_first>
    - .claude/hooks/gsd-read-guard.js (in-repo stdin-parse + block idiom; defensive try/except, exit-0-on-parse-error for advisory hooks)
    - 04-RESEARCH.md Pattern 2 + Pattern 3 (PreToolUse permissionDecision vs PostToolUse/Stop top-level decision), §Security Domain V5 (parse untrusted stdin defensively)
    - tools/harness_perms/__init__.py (PEP 562 lazy re-export) and tools/golden_runner/pyproject.toml (member template)
  </read_first>
  <behavior>
    - `parse_event(json_text)` returns a frozen record with tool_name, file_path, content, command, cwd, session_id; missing keys default to "" (never raises on absent optional fields).
    - malformed/empty stdin -> a sentinel record that maps to "no decision" (fail-safe for the adapter itself; individual gates decide fail-open vs fail-closed).
    - `emit_deny(reason)` returns the exact PreToolUse deny JSON dict; `emit_block(reason)` returns the top-level `{"decision":"block","reason":…}` dict (for Post/Stop gates in later plans).
  </behavior>
  <action>Create the `tools/hooks` uv member: `pyproject.toml` (`package=false`, `dependencies=[]`, copy golden_runner template), `__init__.py` (minimal namespace + lazy re-export of parse_event/emit_deny/emit_block per PEP 562). Implement `_stdin.py`: `parse_event(text:str)->Event` (frozen dataclass), `emit_deny(reason)->dict`, `emit_block(reason)->dict`, and a `read_stdin()` helper wrapping `sys.stdin.read()` in try/except. Write test_stdin.py asserting the behavior above (RED first, then implement to GREEN). Run `uv sync --all-packages`. Commit test then impl.</action>
  <verify>
    <automated>uv run pytest tools/hooks/tests/test_stdin.py -x -q</automated>
  </verify>
  <acceptance_criteria>`uv run pytest tools/hooks/tests/test_stdin.py -x -q` passes. `echo '{"tool_name":"Write","tool_input":{"file_path":"x"}}' | uv run python -c "import sys,json; from tools.hooks._stdin import parse_event; print(parse_event(sys.stdin.read()).file_path)"` prints `x`.</acceptance_criteria>
  <done>`tools/hooks` skeleton exists as a registered uv member; adapter round-trips a crafted event and emits both decision shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HOOK-02 secret_scan gate (RED->GREEN)</name>
  <files>tools/hooks/secret_scan.py, tools/hooks/tests/test_secret_scan.py</files>
  <read_first>
    - tools/harness_perms/resolver.py (resolve_path + load_matrix — reuse for *.env path-deny; DO NOT write a new glob matcher)
    - 04-RESEARCH.md Assumption A3 (shape-anchored pattern set) + Pitfall 5 (fixture false-positive) + §Security Domain (Information Disclosure row)
    - harness/permission-matrix.json (path_deny_globs already covers *.env / **/*.env)
  </read_first>
  <behavior>
    - Write content containing an AWS access key shape `AKIA[0-9A-Z]{16}` -> deny.
    - Write content containing a PEM header `-----BEGIN … PRIVATE KEY-----` -> deny.
    - Read or Write whose file_path matches `*.env` (resolve_path over the SECRET subset == "deny") -> deny.
    - A write to `contracts/x.schema.json` / `docs/adr/0002.md` / `golden/c/expected/x.tsv` with NO secret content -> allow (no decision) — secret_scan does NOT deny the constitution plane; contract-guard owns that gate + its bypass.
    - A benign high-entropy value under tests/ , golden/ , or libs/normalize-fixtures/ (allow-list) -> allow (no decision).
    - The GOLDEN_APPROVE_HUMAN token string appearing in a test file -> allow (not flagged).
    - main(): reads stdin via _stdin, emits deny JSON (exit 0) on a hit, prints nothing (exit 0) otherwise.
  </behavior>
  <action>Implement `secret_scan.py`: a module-level `SECRET_PATH_GLOBS = ["*.env", "**/*.env"]` (secret-specific subset — NOT the full `path_deny_globs`, see composition note in interfaces), a `PATTERNS` list of shape-anchored `re` patterns (AWS access key, PEM private-key header, and a conservative `(?i)(secret|token|api[_-]?key)\s*[:=]\s*\S{16,}` assignment shape) and an `ALLOWLIST_PREFIXES` for `tests/`, `golden/`, `libs/normalize-fixtures/`. `decide(file_path, content)` -> deny dict or None: deny if `resolve_path(SECRET_PATH_GLOBS, file_path) == "deny"` (path secret) OR any pattern matches content AND file_path not under an allow-listed prefix. `main()` calls _stdin.parse_event, applies decide, prints `emit_deny(...)` on hit. Reuse the resolver for the `*.env` path-deny (D-02) — never re-glob. Guard with `if __name__=="__main__": raise SystemExit(main())`. Write RED tests first (real secret blocked, benign fixture allowed, *.env blocked, AND a constitution-plane path with no secret content is NOT denied), then implement to GREEN.</action>
  <verify>
    <automated>uv run pytest tools/hooks/tests/test_secret_scan.py -x -q</automated>
  </verify>
  <acceptance_criteria>`echo '{"tool_name":"Write","tool_input":{"file_path":"src/x.py","content":"key=AKIAIOSFODNN7EXAMPLE"}}' | uv run python -m tools.hooks.secret_scan` prints `permissionDecision":"deny"` and exits 0. `echo '{"tool_name":"Write","tool_input":{"file_path":"tests/fixtures/blob.json","content":"AKIAIOSFODNN7EXAMPLE"}}' | uv run python -m tools.hooks.secret_scan` prints nothing (allow-listed). `echo '{"tool_name":"Write","tool_input":{"file_path":"contracts/x.schema.json","content":"{}"}}' | uv run python -m tools.hooks.secret_scan` prints nothing (constitution plane is NOT secret_scan's gate). `grep -q "path_deny_globs" tools/hooks/secret_scan.py; test $? -ne 0` — secret_scan does NOT read the full `path_deny_globs` (it uses SECRET_PATH_GLOBS); the resolver is still reused (grep `resolve_path`).</acceptance_criteria>
  <done>Real secret + *.env path denied; benign fixture allowed; resolver path-deny reused verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude runtime -> hook stdin | Untrusted JSON event (tool_name, file_path, content, command) crosses into the gate; must be parsed defensively. |
| edit/read target path + content -> secret gate | Secret material may be written to the repo or read out of a secret file (*.env / key). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | secret written into repo / read from *.env | mitigate | secret_scan PreToolUse deny: resolver path-deny on `*.env` + shape-anchored regex on content (AWS/PEM/assignment). |
| T-04-04 | Tampering | secret-scan false negative (missed real secret) / false positive (blocks own fixtures) | mitigate | Shape-anchored patterns (not generic entropy) + tests/golden/normalize-fixtures allow-list; unit test asserts both a true-secret block AND a benign-fixture pass (Pitfall 5). |
| T-04-05 | Denial of Service | malformed stdin crashes the gate -> tool proceeds unguarded | mitigate | `_stdin.read_stdin`/`parse_event` wrap parsing in try/except and default to a safe sentinel; malformed-input path unit-tested (V5). |
| T-04-SC | Tampering | dependency supply chain | accept | Zero new packages — stdlib `re`/`json` + reused resolver; `dependencies=[]`, `uv.lock` unchanged. |
</threat_model>

<verification>
- `uv run pytest tools/hooks/tests/test_stdin.py tools/hooks/tests/test_secret_scan.py -x -q` green.
- `uv run pytest` full suite green (no regression; new member registered).
- Manual demo: crafted-stdin AWS key write is denied; a tests/ fixture with the same string is allowed.
</verification>

<success_criteria>
Shared `tools/hooks` adapter exists and is tested; HOOK-02 secret gate blocks real secrets + *.env and passes benign fixtures — the secret half of success criterion 3.
</success_criteria>

<output>
Create `.planning/phases/04-plugins-hooks/04-02-SUMMARY.md` when done.
</output>
