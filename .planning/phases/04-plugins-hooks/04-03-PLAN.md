---
phase: 04-plugins-hooks
plan: 03
type: tdd
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - tools/hooks/contract_guard.py
  - tools/hooks/tests/test_contract_guard.py
autonomous: true
requirements: [HOOK-04]

must_haves:
  truths:
    - "A write to contracts/**, docs/adr/**, or golden/** without approval is denied (permissionDecision:deny)."
    - "The same write WITH a non-empty GOLDEN_APPROVE_HUMAN token present produces no access-control decision (bypass); an empty-string value does NOT bypass."
    - "A write to a normal source path (libs/python/foo.py) produces no decision (allowed)."
    - "An APPROVED constitution-plane write whose payload carries a BOM/CRLF/non-canonical TSV is still denied by the reused polyglot rules (the constitution plane must be byte-pristine even when access-approved)."
    - "A BOM/CRLF payload into a NON-constitution (allowed) path produces NO decision from contract-guard — general byte hygiene is format-on-write's job (04-04, PostToolUse auto-fix); contract-guard must not preempt it (composition invariant, see 04-06)."
  artifacts:
    - path: "tools/hooks/contract_guard.py"
      provides: "HOOK-04 PreToolUse gate: resolver path-deny + approval-token bypass + polyglot on-write; main() exit 0 + JSON"
      contains: "def main"
  key_links:
    - from: "tools/hooks/contract_guard.py"
      to: "tools/harness_perms/resolver.py"
      via: "resolve_path over matrix path_deny_globs"
      pattern: "resolve_path"
    - from: "tools/hooks/contract_guard.py"
      to: "tools/polyglot_lint/lint.py"
      via: "lint_bytes on the write payload (on-write TSV/encoding rules)"
      pattern: "lint_bytes"
---

## Phase Goal

**As a** developer or coding agent working in this polyglot harness, **I want to** have the Phase 1-3 contract, §4.3-4.6 boundary, and permission rules enforced by non-bypassable runtime hooks, **so that** a violation is blocked at write/commit time instead of relying on advisory prose.

> NOTE (planner): ROADMAP Phase 4 `**Goal:**` is a rationale sentence, not a user story. Provisional story derived; ratify via `/gsd mvp-phase 4`.

<objective>
Build the HOOK-04 contract-guard: a PreToolUse(Write|Edit) gate that denies writes to the constitution plane (contracts/ · docs/adr/ · golden/) unless a human-authorized approval token is present, and enforces §4.3-4.6 on-write encoding/TSV rules on the payload. This is Phase 4 success criterion 1.

Purpose: make "machines gate, humans ratify" a runtime fact rather than prose (Pitfall P8/P11). Reuses the 03-01 resolver path-deny (D-02, no new glob matcher) and the approval path is the presence of the existing `GOLDEN_APPROVE_HUMAN` token — agent-can't-fabricate (Open Q1 recommendation). On-write encoding/TSV enforcement reuses the POLY-01 `lint_bytes` engine from plan 04-01 (no second normalizer).
Output: a tested gate proving constitution writes are denied, the token bypasses, source paths pass, and a BOM/CRLF payload is flagged.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-plugins-hooks/04-CONTEXT.md
@.planning/phases/04-plugins-hooks/04-RESEARCH.md
@CLAUDE.md
@tools/harness_perms/resolver.py
@harness/permission-matrix.json
@tools/hooks/_stdin.py
@tools/polyglot_lint/lint.py
@tools/golden_runner/approve.py

<interfaces>
<!-- From plan 04-02 (tools/hooks/_stdin.py) -->
parse_event(text)->Event(tool_name,file_path,content,command,cwd,session_id); emit_deny(reason)->dict
<!-- From plan 04-01 (tools/polyglot_lint/lint.py) -->
lint_bytes(raw: bytes) -> list[Violation]   # R1-BOM / R2-CRLF etc.
<!-- Reused (D-02) tools.harness_perms -->
resolve_path(deny_globs, path)->"deny"|"allow"; load_matrix()["path_deny_globs"] covers contracts/**, docs/adr/**, golden/**, *.env, **/*.env
<!-- Symmetric split (W-1): contract-guard uses a CONSTITUTION-ONLY subset, NOT the full union.
     CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**", "golden/**"]   # NO *.env — that is secret_scan's domain
     This mirrors secret_scan's SECRET_PATH_GLOBS = ["*.env","**/*.env"] so the two gates' domains are
     provably non-overlapping, and a *.env write is never mislabeled "constitution plane" by contract-guard. -->
CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**", "golden/**"]
<!-- Approval token: reuse existing GOLDEN_APPROVE_HUMAN (see tools/golden_runner/approve.py) -->
APPROVAL_ENV = "GOLDEN_APPROVE_HUMAN"   # NON-EMPTY value in env == human-authorized session (agent must not fabricate)
# approved = bool((os.environ.get(APPROVAL_ENV) or "").strip())  -- empty string must NOT bypass (Q1 RESOLVED, 04-RESEARCH)
# Scope note (composition, 04-06): polyglot on-write deny fires ONLY on the constitution plane
# (resolve_path(path_deny_globs, fp)=="deny"). On allowed paths contract-guard returns None and
# leaves BOM/CRLF hygiene to format-on-write (PostToolUse). Do NOT deny BOM/CRLF on allowed paths here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing contract-guard tests (RED)</name>
  <files>tools/hooks/tests/test_contract_guard.py</files>
  <read_first>
    - tools/golden_runner/approve.py (the GOLDEN_APPROVE_HUMAN token precedent + refusal idiom this reuses)
    - 04-RESEARCH.md §"HOOK-04 contract-guard" code example + Open Q1 (approval path) + Pattern 2 (deny shape)
    - tools/hooks/_stdin.py (parse_event / emit_deny signatures)
    - harness/permission-matrix.json (path_deny_globs the resolver reads)
  </read_first>
  <behavior>
    - Write to `contracts/x.schema.json` with GOLDEN_APPROVE_HUMAN unset -> deny JSON (permissionDecision:"deny", reason names contracts/·adr/·golden/).
    - Write to `docs/adr/0002-foo.md` and `golden/case/expected/x.tsv` unset -> deny.
    - Same write with a non-empty `GOLDEN_APPROVE_HUMAN` set in env -> no access-control decision (bypass); the same write with `GOLDEN_APPROVE_HUMAN=""` (empty) -> still denied.
    - Write to `libs/python/foo.py` -> no decision (allowed source path).
    - APPROVED write to `contracts/x.schema.json` whose content carries a BOM/CRLF -> still denied (reason mentions the polyglot rule) — the constitution plane must be byte-pristine even when access-approved.
    - Write to an ALLOWED path (`libs/python/foo.py`) whose content bytes carry a BOM/CRLF -> NO decision from contract-guard (format-on-write handles it post-write).
  </behavior>
  <action>Author test_contract_guard.py driving `decide(...)`/`main()` with crafted events and toggling `GOLDEN_APPROVE_HUMAN` via monkeypatch of os.environ (test unset, non-empty, AND empty-string). Assert exact emitted JSON keys + that no-decision cases print nothing. Include the two composition cases: approved-constitution-with-BOM -> deny; allowed-path-with-BOM -> no decision. Tests must be RED (module absent). Commit: `test(04-03): failing HOOK-04 contract-guard tests`.</action>
  <verify>
    <automated>uv run pytest tools/hooks/tests/test_contract_guard.py -x -q 2>&1 | grep -qiE "error|failed|ModuleNotFoundError"</automated>
  </verify>
  <acceptance_criteria>Suite fails/errors (module absent); tests monkeypatch GOLDEN_APPROVE_HUMAN and assert both deny and bypass paths.</acceptance_criteria>
  <done>RED test file encodes deny / bypass / allow / on-write-violation cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement contract_guard.py (GREEN)</name>
  <files>tools/hooks/contract_guard.py</files>
  <read_first>
    - tools/harness_perms/resolver.py (resolve_path + load_matrix — reuse verbatim, D-02)
    - tools/polyglot_lint/lint.py (lint_bytes for on-write §4.3-4.6 enforcement)
    - tools/hooks/_stdin.py (parse_event, emit_deny) and 04-RESEARCH.md contract-guard code example
  </read_first>
  <action>Implement `decide(file_path, content, approved)` -> deny dict or None. Define a module-level `CONSTITUTION_GLOBS = ["contracts/**", "docs/adr/**", "golden/**"]` (constitution-only subset — NOT the full `path_deny_globs` union, which also carries `*.env`; symmetric with secret_scan's `SECRET_PATH_GLOBS` so a `.env` write is never mislabeled "constitution plane", W-1). Let `on_plane = resolve_path(CONSTITUTION_GLOBS, file_path) == "deny"`. If `on_plane` and NOT approved -> `emit_deny` with the constitution-plane reason (name /golden-approve + CODEOWNERS gate). If `on_plane` and approved but `content` bytes fail `lint_bytes` (BOM/CRLF/TSV) -> `emit_deny` naming the polyglot rule (the constitution plane must be byte-pristine even when access-approved, per D-04). If NOT `on_plane` -> return None (allowed source path; BOM/CRLF hygiene is format-on-write's PostToolUse job — do NOT deny here, or you preempt 04-04). `main()` reads stdin via _stdin.parse_event, computes `approved = bool((os.environ.get("GOLDEN_APPROVE_HUMAN") or "").strip())` (empty string does NOT bypass — Q1 RESOLVED), applies decide, prints the JSON on a hit (exit 0), nothing otherwise. Reuse resolver + lint_bytes — NO new glob matcher, NO second normalizer (D-02). Guard with `if __name__=="__main__": raise SystemExit(main())`.</action>
  <verify>
    <automated>uv run pytest tools/hooks/tests/test_contract_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>`echo '{"tool_name":"Write","tool_input":{"file_path":"contracts/x.schema.json","content":"{}"}}' | uv run python -m tools.hooks.contract_guard` prints `permissionDecision":"deny"` (exit 0). `GOLDEN_APPROVE_HUMAN=1 <same command>` prints nothing; `GOLDEN_APPROVE_HUMAN= <same command>` (empty) still prints deny. `echo '{"tool_name":"Write","tool_input":{"file_path":"libs/python/foo.py","content":"x=1\n"}}' | uv run python -m tools.hooks.contract_guard` prints nothing — including when the content carries a BOM/CRLF (allowed path -> format-on-write's job, not a deny here).</acceptance_criteria>
  <done>Constitution writes denied without token, bypass on token, source paths pass, BOM/CRLF payload flagged; resolver + polyglot reused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent write -> constitution plane | An agent attempts to edit contracts/ · docs/adr/ · golden/ (human-owned, CODEOWNERS-gated) without ratification. |
| write payload bytes -> repo | On-write §4.3-4.6 encoding/TSV rules must hold at the write boundary, not only at commit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Elevation of Privilege | agent self-approves a constitution/golden edit | mitigate | Default-deny via resolver path-deny; bypass ONLY on a NON-EMPTY `GOLDEN_APPROVE_HUMAN` value (human-only token agents are instructed never to fabricate; empty string does not bypass); unit-tested unset/non-empty/empty. Phase 5 CODEOWNERS is the hard CI gate. |
| T-04-07 | Tampering | non-canonical bytes slip into the constitution plane | mitigate | On the constitution plane (even when access-approved), `lint_bytes` runs on the payload; BOM/CRLF/TSV violation -> deny (reuses POLY-01, no second normalizer). Allowed-path byte hygiene is format-on-write's PostToolUse auto-fix (04-04) — contract-guard does not preempt it. |
| T-04-08 | Spoofing | wrong PreToolUse decision shape -> deny silently ignored | mitigate | Emit `hookSpecificOutput.permissionDecision:"deny"` (exit 0), asserted byte-for-byte in tests (Pitfall 4). |
| T-04-SC | Tampering | dependency supply chain | accept | Zero new packages — reuses resolver + polyglot_lint + stdlib; `uv.lock` unchanged. |
</threat_model>

<verification>
- `uv run pytest tools/hooks/tests/test_contract_guard.py -x -q` green.
- `uv run pytest` full suite green.
- Manual demo: crafted contracts/ write denied; with GOLDEN_APPROVE_HUMAN set it passes; libs/python write passes.
</verification>

<success_criteria>
HOOK-04 contract-guard denies unapproved constitution/golden writes, honors the human token bypass, and enforces on-write §4.3-4.6 rules — Phase 4 success criterion 1.
</success_criteria>

<output>
Create `.planning/phases/04-plugins-hooks/04-03-SUMMARY.md` when done.
</output>
