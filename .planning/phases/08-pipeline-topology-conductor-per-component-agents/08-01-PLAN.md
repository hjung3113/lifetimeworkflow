---
phase: 08-pipeline-topology-conductor-per-component-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/project.toml
  - tools/harness_config/loader.py
  - tools/harness_config/__init__.py
  - tools/harness_config/tests/test_loader.py
  - tools/harness_lint/tests/test_pipeline_config.py
autonomous: true
requirements: [PIPE-01]
must_haves:
  truths:
    - "harness/project.toml declares a generic [[components]] + [pipeline] topology default (source/sink/sample-record) with zero log-parser specifics"
    - "loader.components() and loader.pipeline() return the declared tables unchanged"
    - "The topology consistency gate fails loud when a component names an undeclared language or an edge is not well-formed"
    - "GEN-04 core→example guard stays green after the core topology default is added"
  artifacts:
    - path: "harness/project.toml"
      provides: "Generic pipeline-topology DATA slot ([[components]] + [pipeline])"
      contains: "[[components]]"
    - path: "tools/harness_config/loader.py"
      provides: "components() + pipeline() passthrough helpers"
      contains: "def components"
    - path: "tools/harness_config/__init__.py"
      provides: "Lazy re-export of components/pipeline"
      contains: "components"
    - path: "tools/harness_lint/tests/test_pipeline_config.py"
      provides: "GEN-03-style topology consistency gate over the generic default"
      contains: "def test_pipeline_edges_are_well_formed"
  key_links:
    - from: "tools/harness_lint/tests/test_pipeline_config.py"
      to: "tools.harness_config.components / pipeline"
      via: "import + consistency assertions"
      pattern: "from tools.harness_config import"
    - from: "tools/harness_config/loader.py"
      to: "harness/project.toml [[components]]"
      via: "cfg.get('components')"
      pattern: "cfg.get\\(\"components\""
---

<objective>
Establish the generic pipeline-topology DATA slot in the neutral core: add `[[components]]` + `[pipeline]`
tables to `harness/project.toml` (generic `source`/`sink`/`sample-record` default only), add `components()`
and `pipeline()` passthrough helpers to the loader (re-exported from the package), and add a GEN-03-style
consistency gate that fails loud on a malformed topology. This clones the proven `[[languages]]` →
`loader.languages()` → `test_language_config.py` triad verbatim.

Purpose: PIPE-01 — the core mechanism every later plan reads (conductor routing, /pipeline trace, instance overlay).
Output: topology slot + loader helpers + consistency gate, all domain-neutral (GEN-04 green).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-RESEARCH.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-PATTERNS.md

<interfaces>
<!-- Clone these exact idioms. Executor should not explore — the contracts are here. -->

harness/project.toml [[languages]] slot (the pure-DATA pattern to mirror):
  [[languages]] id="dotnet" bash_scope="dotnet *" ... persona="examples/log-parser/agents/dotnet-engineer.md"
  Header banner names its two consumers (loader.py + test_language_config.py).

tools/harness_config/loader.py languages() (the passthrough to mirror):
  def languages(cfg: dict | None = None) -> list[dict]:
      if cfg is None: cfg = load_project()
      return list(cfg.get("languages", []))
  load_project(path=_DEFAULT_PROJECT) already accepts a custom path arg.

tools/harness_config/__init__.py re-export (PEP 562 lazy __getattr__):
  __all__ = ["language_bash_scopes", "languages", "load_project"]  # add components, pipeline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generic topology slot + loader passthrough + re-export</name>
  <files>harness/project.toml, tools/harness_config/loader.py, tools/harness_config/__init__.py</files>
  <read_first>
    - harness/project.toml (the [[languages]] slot + the file-header consumer banner — clone the DATA-only posture and the inline `# matches ...` cross-reference comments)
    - tools/harness_config/loader.py (the `languages()` passthrough at lines 42-50 and `load_project` signature)
    - tools/harness_config/__init__.py (the `__all__` list + PEP 562 lazy `__getattr__` re-export)
  </read_first>
  <action>
    In `harness/project.toml`, append a GENERIC-DEFAULT pipeline-topology slot mirroring the `[[languages]]` idiom.
    Add two `[[components]]` tables — `id="source"` (stage=1, language="python", produces=["sample-record"], consumes=[])
    and `id="sink"` (stage=2, language="python", consumes=["sample-record"], produces=[]) — plus a `[pipeline]` table
    with `edges = [ { from = "source", to = "sink", contract = "sample-record" } ]`. Prepend a header-comment banner
    (mirroring the file's existing banner) naming the consumers `components()`/`pipeline()` in loader.py and
    `test_pipeline_config.py`. CRITICAL (GEN-04): use ONLY domain-neutral ids/labels — never `parser`/`converter`/
    `standard-log`/`equipment`; the concrete 4-component topology belongs in the instance overlay (Plan 04). Each
    `component.language` MUST match a declared `[[languages]].id` (`python` is valid). In `tools/harness_config/loader.py`,
    add `components(cfg=None) -> list[dict]` (returns `list(cfg.get("components", []))`) and `pipeline(cfg=None) -> dict`
    (returns `dict(cfg.get("pipeline", {}))`), both loading `load_project()` when `cfg is None` — pure passthrough,
    NO enforcement (that lives in the consistency gate). In `tools/harness_config/__init__.py`, add `"components"` and
    `"pipeline"` to `__all__` so the lazy `__getattr__` re-exports them.
  </action>
  <verify>
    <automated>uv run python -c "from tools.harness_config import components, pipeline, load_project; c=components(); assert {x['id'] for x in c}=={'source','sink'}, c; assert pipeline()['edges'][0]['contract']=='sample-record'"</automated>
  </verify>
  <acceptance_criteria>
    - `harness/project.toml` contains `[[components]]` and `[pipeline]` tables with ids exactly `{source, sink}` and one edge whose `contract == "sample-record"`.
    - `from tools.harness_config import components, pipeline` succeeds; `components()` returns 2 dicts, `pipeline()` returns a dict with a non-empty `edges` list.
    - `grep -v '^#' harness/project.toml` shows NO occurrence of `parser`, `converter`, `standard-log`, or `equipment` in the added tables (component *role* words absent from the generic default; domain contracts absent).
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` exits 0 (GEN-04 stays green).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Topology consistency gate + loader unit tests</name>
  <files>tools/harness_lint/tests/test_pipeline_config.py, tools/harness_config/tests/test_loader.py</files>
  <read_first>
    - tools/harness_lint/tests/test_language_config.py (the whole file — `_REPO_ROOT = parents[3]`, the `from tools.harness_config import ...` idiom, and the iterate-config/assert-agreement/fail-loud message shape)
    - tools/harness_config/tests/test_loader.py (the `test_load_project_returns_two_languages` unit-assert idiom; note `parents[3]` root anchoring is not needed here — it uses load_project directly)
  </read_first>
  <action>
    Create NEW `tools/harness_lint/tests/test_pipeline_config.py` cloning `test_language_config.py`. Import
    `components, pipeline, languages, load_project` from `tools.harness_config`. Add: `test_component_languages_are_declared`
    (every `c["language"]` in `{l["id"] for l in languages()}`, fail-loud message naming the offending component id);
    `test_pipeline_edges_are_well_formed` (for each `pipeline(cfg).get("edges", [])`: `edge["from"]`/`edge["to"]` in the
    component id map, `edge["contract"]` in the from-component's `produces` AND the to-component's `consumes`); and
    `test_component_ids_unique` (no duplicate `id`). These run against the GENERIC core default only — they must pass on
    `source`/`sink`/`sample-record` and must never reference `examples/`. Extend `tools/harness_config/tests/test_loader.py`
    with `test_components_passthrough` (asserts `{c["id"] for c in components()} == {"source","sink"}`) and
    `test_pipeline_passthrough` (asserts the single edge `contract == "sample-record"`).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_pipeline_config.py tools/harness_config/tests/test_loader.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_pipeline_config.py -x` exits 0.
    - `uv run pytest tools/harness_config/tests/test_loader.py -x` exits 0 with `test_components_passthrough` + `test_pipeline_passthrough` collected.
    - `test_pipeline_config.py` contains NO string `examples/` (would trip GEN-04 if it were a core file, but this IS a core file — must stay clean).
    - `uv run pytest tools/harness_lint tools/harness_config` exits 0 (no cross-artifact drift introduced).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| topology config → loader/gate | Untrusted (author-edited) TOML shape crosses into code that other agents route on |
| core plane → example plane | GEN-04 one-directional dependency; core must never reference an instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering/Spoofing (V5 input validation) | topology slot in harness/project.toml | mitigate | `test_pipeline_config.py` fails loud on undeclared language / malformed edge — malformed config never resolves silently |
| T-8-02 | Tampering (boundary erosion) | harness/project.toml + tools/* additions | mitigate | Generic-only default (source/sink/sample-record); `test_core_no_example_dep.py` stays green (verified in Task 1 acceptance) |
| T-8-SC | Supply chain | package installs | accept | N/A — this phase installs zero external packages (stdlib tomllib + pinned pytest only) |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint tools/harness_config` green (all structural gates + loader units).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` green (GEN-04).
- Generic default carries no domain contract tokens.
</verification>

<success_criteria>
PIPE-01 satisfied: generic topology slot + loader passthrough + consistency gate exist, all domain-neutral, GEN-04 green.
</success_criteria>

<output>
Create `.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-01-SUMMARY.md` when done.
</output>
