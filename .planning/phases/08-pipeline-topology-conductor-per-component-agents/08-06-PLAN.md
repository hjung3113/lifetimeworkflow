---
phase: 08-pipeline-topology-conductor-per-component-agents
plan: 06
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05]
autonomous: false
requirements: [PIPE-06]
files_modified:
  - docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md
  - tools/harness_lint/tests/test_agents.py
must_haves:
  truths:
    - "The GEN-04 core->example no-dependency guard and persona anti-sprawl gate are both green with the conductor + component-engineer template in place — EXPECTED_PERSONAS stays exactly 4 and the conductor is the evolved orchestrator (no new primary)"
    - "The topology-slot consistency gate, template anti-sprawl gate, skills gate (9), and command/referential-integrity gates all pass together as one closeout suite"
    - "The full core suite (uv run pytest) AND the instance leg (uv run pytest examples/log-parser/tests) are BOTH green — the Phase 7 emit surface is unaffected"
    - "ADR-0003 records the pipeline-topology-slot + instance-overlay decision on the constitution plane and lands via the human GOLDEN_APPROVE_HUMAN gate (contract-guard denies docs/adr/** without it)"
  artifacts:
    - path: "docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md"
      provides: "Constitution-plane record of the topology slot + instance-overlay (generic-default-in-core, concrete-topology-in-example) decision"
      contains: "plane: constitution"
    - path: "tools/harness_lint/tests/test_agents.py"
      provides: "Assertion that the conductor role signal is present on the single primary orchestrator (persona set unchanged at 4)"
      contains: "EXPECTED_PERSONAS"
  key_links:
    - from: "docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md"
      to: "harness/project.toml topology slot + examples/log-parser/project.toml overlay"
      via: "records the where-does-the-concrete-topology-live decision"
      pattern: "topology"
    - from: "tools/harness_lint/tests/test_agents.py"
      to: "harness/agents/orchestrator.md"
      via: "persona-set + conductor-signal assertions"
      pattern: "orchestrator"
---

<objective>
Close out the phase: prove the guards were extended, not just the features added. Run the full guard surface as one
suite — GEN-04 core->example no-dependency guard, persona anti-sprawl (EXPECTED_PERSONAS==4, conductor = evolved
orchestrator), the new template anti-sprawl gate, the topology-slot consistency gate, the skills(9) + command +
referential-integrity gates — and require BOTH the core suite and the instance leg green so the Phase 7 emit surface is
demonstrably unaffected. Then record the one architectural decision this phase locked (topology slot + instance-overlay)
as ADR-0003 on the constitution plane, landing via the human GOLDEN_APPROVE_HUMAN gate.

Purpose: PIPE-06 — guards/tests closeout + the constitution-plane record.
Output: ADR-0003 (human-ratified) + a green full-guard suite across both legs.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-RESEARCH.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-PATTERNS.md

<interfaces>
<!-- Clone these exact idioms. Executor should not explore — the contracts are here. -->

docs/adr/0002-general-template-de-specialization.md (the MADR shape to mirror):
  # 3. <Title>
  *MADR 4.x · plane: constitution (human-owned, immutable, append-only)*
  - **Status:** accepted
  - **Date:** 2026-07-10
  - **Deciders:** Phase 8 planning (Pipeline-Topology Conductor, PIPE-01..06)
  - **Supersedes:** — / **Superseded by:** — / **Complements:** [ADR-0002](0002-general-template-de-specialization.md)
  ## Context and Problem Statement / ## Decision Drivers / ## Considered Options / ## Decision Outcome / ## Consequences

harness/commands/adr.md — the scaffold command for appending an ADR in the mandated order (use it).

tools/harness_lint/tests/test_agents.py:53 EXPECTED_PERSONAS:
  frozenset({"orchestrator", "python-engineer", "code-reviewer", "explorer"})  # STAYS 4 — conductor IS orchestrator.

Constitution-plane gate (gate-model skill): contract-guard denies writes under docs/adr/** unless the human
GOLDEN_APPROVE_HUMAN token is set — the agent DRAFTS, the human RATIFIES.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-guard closeout suite — both legs green</name>
  <files>tools/harness_lint/tests/test_agents.py</files>
  <read_first>
    - tools/harness_lint/tests/test_agents.py (the `EXPECTED_PERSONAS` frozenset + the persona-shape assertions — add the conductor-signal assertion here, keeping the set at 4)
    - .planning/phases/08-pipeline-topology-conductor-per-component-agents/08-VALIDATION.md (the per-wave + phase-gate sampling commands — this task runs the phase-gate row)
  </read_first>
  <action>
    First, add ONE assertion to `tools/harness_lint/tests/test_agents.py` that the single primary `orchestrator` persona
    carries the conductor/topology signal (e.g. its body/description contains a stage/component/topology routing token),
    WITHOUT changing `EXPECTED_PERSONAS` (stays the 4-member frozenset — a second primary would fail the no-sprawl gate).
    This is the "persona anti-sprawl gate extended to the conductor" deliverable. Then run the whole guard surface as the
    phase gate: the topology consistency gate (`test_pipeline_config.py`), the template anti-sprawl gate
    (`test_agent_templates.py`), the persona gate (`test_agents.py`), the skills(9) gate (`test_skills.py`), the command +
    referential-integrity gates, and the GEN-04 (`test_core_no_example_dep.py`) + GEN-05 guards. Require the FULL core suite
    (`uv run pytest`) AND the instance leg (`uv run pytest examples/log-parser/tests`) both green — the instance leg is NOT in
    root `testpaths`, so it must be invoked explicitly. If any contract/schema file changed this phase, regenerate derived
    memory (`python -m tools.memory_regen.repo_map` / `contracts_index`) so the two-plane memory stays consistent; do NOT
    hand-edit derived files.
  </action>
  <verify>
    <automated>uv run pytest && uv run pytest examples/log-parser/tests -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` exits 0 (full core suite green; Phase 7 emit-surface tests unaffected).
    - `uv run pytest examples/log-parser/tests -x` exits 0 (instance leg green, incl. the Plan-04 topology gate).
    - `uv run pytest tools/harness_lint/tests/test_agents.py -x` exits 0 with the new conductor-signal assertion collected AND `EXPECTED_PERSONAS` still a 4-member set.
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py tools/harness_lint/tests/test_pipeline_config.py tools/harness_lint/tests/test_agent_templates.py tools/harness_lint/tests/test_skills.py -x` exits 0 (GEN-04 + all new/extended gates green together).
  </acceptance_criteria>
</task>

<task type="human">
  <name>Task 2: Record ADR-0003 (constitution plane — human GOLDEN_APPROVE gate)</name>
  <files>docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md</files>
  <read_first>
    - docs/adr/0002-general-template-de-specialization.md (the exact MADR section structure + constitution-plane banner to mirror)
    - harness/commands/adr.md (the mandated append-order scaffold for a new ADR)
    - .planning/phases/08-pipeline-topology-conductor-per-component-agents/08-RESEARCH.md (Open Question #1 — the decision + rationale this ADR records)
  </read_first>
  <action>
    Draft `docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md` using the `/adr` scaffold and mirroring ADR-0002's
    MADR structure. Record the decision: the pipeline-topology is a pure-DATA slot (`[[components]]` + `[pipeline]`) whose
    GENERIC default (source/sink/sample-record) lives in core `harness/project.toml`, while the CONCRETE per-instance topology
    (the log-parser 4-component parser->converter->scheduler->collector chain with domain edge contracts) lives in a separate
    `examples/log-parser/project.toml` overlay — because domain contract names are GEN-04-flagged tokens under core roots, so
    keeping them out of core is the only layout that holds GEN-04 green (state this as the primary Decision Driver). Note the
    consequence: the conductor is the evolved single-primary orchestrator (no new tier), and the instance topology test runs
    only in the example leg. Set `plane: constitution`, `Status: accepted`, `Deciders: Phase 8 planning`,
    `Complements: ADR-0002`. This is a constitution-plane write: contract-guard denies `docs/adr/**` without the human
    GOLDEN_APPROVE_HUMAN token — the agent DRAFTS, the human RATIFIES. Flag for human approval; do not attempt to self-approve.
  </action>
  <verify>
    <automated>test -f docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md &amp;&amp; grep -q "plane: constitution" docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/0003-pipeline-topology-slot-and-instance-overlay.md` exists with the MADR banner `plane: constitution` and Status/Date/Deciders header mirroring ADR-0002.
    - The ADR names the decision (generic-default-in-core, concrete-topology-in-example-overlay) and cites GEN-04 as the primary driver.
    - The ADR write was ratified through the human GOLDEN_APPROVE_HUMAN gate (not self-approved) — `docs/adr/README.md` index updated if the scaffold requires it.
    - The `contract-guard` / commit gate does not report an unratified constitution-plane change after approval.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent -> constitution plane (docs/adr) | Human-ratified only; an agent must not self-bless an immutable append-only record |
| core plane -> example plane | GEN-04 guard must remain green through the closeout, incl. the ADR text |
| authored personas -> primary tier | Only ONE primary persona may exist; the conductor must remain the evolved orchestrator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-03 | Repudiation / Elevation of Privilege | docs/adr/0003 (constitution write) | mitigate | contract-guard denies docs/adr/** without GOLDEN_APPROVE_HUMAN; Task 2 is `type="human"`, drafted-then-ratified, never self-approved |
| T-8-04 | Information Disclosure (policy) | new/edited agent `model:` fields across the phase | mitigate | `test_no_real_model_identifier` — only `provider/*-tier` placeholders; run as part of the full core suite |
| T-8-02 | Tampering (boundary erosion) | GEN-04 guard through closeout | mitigate | `test_core_no_example_dep.py` in the closeout suite; ADR text lives in docs/ (not a scanned core root) yet is checked for stray core-plane coupling |
| T-8-08 | Spoofing (second primary) | harness/agents/orchestrator.md | mitigate | `EXPECTED_PERSONAS` stays 4; a second `mode: primary` fails `test_expected_personas_present_no_sprawl` |
| T-8-SC | Supply chain | package installs | accept | N/A — zero external package installs this phase |
</threat_model>

<verification>
- `uv run pytest` green (full core suite; Phase 7 emit surface unaffected — success criterion 4).
- `uv run pytest examples/log-parser/tests -x` green (instance leg — the concrete 4-component demo).
- GEN-04 + persona + template + topology-consistency + skills(9) + command gates all green together.
- ADR-0003 present on the constitution plane, ratified via the human gate.
</verification>

<success_criteria>
PIPE-06 satisfied: GEN-04/GEN-05 + persona anti-sprawl (extended to the conductor) + template gate + topology-slot consistency gate all green; full core suite AND instance leg green; ADR-0003 recorded and human-ratified.
</success_criteria>

<output>
Create `.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-06-SUMMARY.md` when done.
</output>
