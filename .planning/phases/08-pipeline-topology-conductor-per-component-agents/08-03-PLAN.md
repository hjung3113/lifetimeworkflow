---
phase: 08-pipeline-topology-conductor-per-component-agents
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - harness/agents/templates/component-engineer.md
  - harness/commands/component.md
  - tools/harness_lint/tests/test_agent_templates.py
autonomous: true
requirements: [PIPE-03]
must_haves:
  truths:
    - "A neutral component-engineer template exists under harness/agents/templates/ and is NOT counted as a core persona"
    - "The /component command binds a per-component agent from the template and registers the component in the instance topology slot"
    - "A structural gate validates the templates/ dir contains exactly {engineer, component-engineer}, each subagent-mode with valid least-privilege perms and a routing-trigger description"
  artifacts:
    - path: "harness/agents/templates/component-engineer.md"
      provides: "Neutral fill-in-the-blanks per-component persona template"
      contains: "mode: subagent"
    - path: "harness/commands/component.md"
      provides: "Scaffold that derives a component agent + registers the topology component"
      contains: "component-engineer"
    - path: "tools/harness_lint/tests/test_agent_templates.py"
      provides: "Template anti-sprawl + shape gate (closes the unvalidated-templates gap)"
      contains: "EXPECTED_TEMPLATES"
  key_links:
    - from: "harness/commands/component.md"
      to: "harness/agents/templates/component-engineer.md"
      via: "copy + fill placeholders scaffold step"
      pattern: "component-engineer"
    - from: "tools/harness_lint/tests/test_agent_templates.py"
      to: "harness/agents/templates/*.md"
      via: "frozenset + parametrized shape asserts"
      pattern: "templates"
---

<objective>
Add the neutral `component-engineer` template under `harness/agents/templates/` (anti-sprawl-exempt like
`engineer.md`), extend `/component` to derive a per-component agent from it and register the component in the
instance topology slot, and add a NEW structural gate that validates the `templates/` directory (currently
validated by nothing — the `agents/*.md` glob is non-recursive). Clone the `engineer.md` + `/add-language`
+ `test_agents.py` frozenset idioms.

Purpose: PIPE-03 — the fill-in-the-blanks template + scaffold that Plan 04 instantiates into 4 concrete agents.
Output: component-engineer template + /component scaffold extension + template anti-sprawl gate.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-RESEARCH.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-PATTERNS.md

<interfaces>
harness/agents/templates/engineer.md (the template idiom to clone):
  Opens with an HTML-comment banner explaining it is NOT an active persona (non-recursive agents/*.md glob).
  frontmatter: name: <LANG>-engineer, mode: subagent, permission.bash {"*": ask, "<BASH_SCOPE>": allow}, tools: Read, Edit, Bash, Grep, Glob.
  Placeholders: <LANG> <TOOLCHAIN> <TEST_CMD> <BASH_SCOPE>.

test_agents.py reusable pieces (import these, do not re-derive):
  from tools.harness_lint import parse_frontmatter
  VALID_PERMISSION_KEYS, VALID_MODES frozensets; _AGENTS_DIR; test_description_is_routing_signal greps use/when.
  EXPECTED_PERSONAS = frozenset({"orchestrator","python-engineer","code-reviewer","explorer"})  # unaffected here.

harness/commands/add-language.md (order-enforcing scaffold idiom):
  "## Mandated order (keep the three in sync)" numbered steps + an "All three or none" Guard block.

harness/commands/component.md frontmatter: agent: orchestrator, subtask: true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the component-engineer template</name>
  <files>harness/agents/templates/component-engineer.md</files>
  <read_first>
    - harness/agents/templates/engineer.md (the whole file — clone the not-an-active-persona banner, frontmatter shape, placeholder slots, least-privilege bash, and the contract-first / §4.3–4.6 / golden-gate body)
    - examples/log-parser/agents/dotnet-engineer.md (an instantiated per-component-ish persona, for the description trigger + bash-allow shape a component agent will end up with)
  </read_first>
  <action>
    Create NEW `harness/agents/templates/component-engineer.md` cloning `engineer.md`. Keep the opening HTML-comment banner
    explaining it lives in `templates/` on purpose (non-recursive `agents/*.md` glob → NOT counted as a core persona), and
    that `/component` instantiates a COPY into the active instance's `agents/` dir. frontmatter: `name: <COMPONENT>-engineer`
    (or `name: <COMPONENT>`), `mode: subagent`, `permission` with `read: allow`, `edit: allow`, `bash: {"*": ask, "<BASH_SCOPE>": allow}`,
    `tools: Read, Edit, Bash, Grep, Glob`. Description MUST carry a `use`/`when` routing trigger keyed on the component's
    **stage** (e.g. "Use when a change to the `<COMPONENT>` stage is requested"). New placeholders: `<COMPONENT>`, `<STAGE>`,
    `<LANG>`/`<TOOLCHAIN>` (from the component's `language` ref → the `[[languages]]` toolchain), `<CONSUMES>`/`<PRODUCES>`
    (edge contracts). Body keeps the contract-first, §4.3–4.6 process/file/DB-only boundary, golden-gate (never self-bless),
    and derived-plane-not-hand-edited boilerplate; close with "Read this instance's per-package `AGENTS.md` before touching
    its subtree." Keep it fully domain-neutral — placeholders only, no `parser`/`standard-log`/`equipment` literals.
  </action>
  <verify>
    <automated>uv run python -c "from tools.harness_lint import parse_frontmatter; import pathlib; fm=parse_frontmatter(pathlib.Path('harness/agents/templates/component-engineer.md').read_text()); assert fm['mode']=='subagent', fm; assert 'bash' in fm['permission']"</automated>
  </verify>
  <acceptance_criteria>
    - `harness/agents/templates/component-engineer.md` exists, frontmatter `mode: subagent`, `permission.bash` has `"*": ask` + a scoped allow placeholder.
    - Description contains a `use`/`when` trigger and a `<STAGE>`/`<COMPONENT>` placeholder.
    - `grep -v '^#' harness/agents/templates/component-engineer.md` shows no domain contract tokens (`standard-log`, `equipment`, `libs/dotnet`) — GEN-04 clean.
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Template anti-sprawl + shape gate</name>
  <files>tools/harness_lint/tests/test_agent_templates.py</files>
  <read_first>
    - tools/harness_lint/tests/test_agents.py (the `EXPECTED_PERSONAS` frozenset + `test_expected_personas_present_no_sprawl` message idiom, the `parse_frontmatter` import, `VALID_PERMISSION_KEYS`/`VALID_MODES`, and the parametrized `test_mode_valid_when_present` / `test_permission_keys_are_valid_subset` / `test_description_is_routing_signal` checks)
    - harness/agents/templates/component-engineer.md (the file this gate validates — after Task 1)
  </read_first>
  <action>
    Create NEW `tools/harness_lint/tests/test_agent_templates.py` cloning `test_agents.py`'s frozenset + parametrized shape,
    but point `_TEMPLATES = _AGENTS_DIR / "templates"`. Define `EXPECTED_TEMPLATES = frozenset({"engineer", "component-engineer"})`.
    Add `test_templates_no_sprawl` asserting `{p.stem for p in _TEMPLATES.glob("*.md")} == set(EXPECTED_TEMPLATES)`. Reuse
    `parse_frontmatter`, `VALID_PERMISSION_KEYS`, `VALID_MODES` from the existing gate, and parametrize over the template
    files to assert each is `mode: subagent`, has permission keys ⊂ `VALID_PERMISSION_KEYS`, and has a routing-trigger
    (`use`/`when`) description. This closes the current gap where `templates/*.md` is validated by nothing.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_agent_templates.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_agent_templates.py -x` exits 0.
    - `EXPECTED_TEMPLATES` has exactly 2 members `{engineer, component-engineer}`; adding/removing a template file fails the gate.
    - `uv run pytest tools/harness_lint/tests/test_agents.py::test_expected_personas_present_no_sprawl -x` exits 0 (EXPECTED_PERSONAS still 4 — the template is NOT a persona).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Extend /component to bind a component agent + register the topology component</name>
  <files>harness/commands/component.md</files>
  <read_first>
    - harness/commands/component.md (its current frontmatter `agent: orchestrator`, `subtask: true`, and current scaffold body)
    - harness/commands/add-language.md (the "## Mandated order (keep the three in sync)" numbered-step idiom + the "All three or none" Guard block to clone)
  </read_first>
  <action>
    Edit `harness/commands/component.md`. Add a mandated-order section (cloning `add-language.md`) for the case where the new
    package maps to a declared topology **component**: (1) derive a `component-engineer` persona from
    `harness/agents/templates/component-engineer.md` into the active instance's own `agents/` dir, filling every `<PLACEHOLDER>`
    from the component's `[[components]]` entry (`id`/`stage`/`language`/`consumes`/`produces`); (2) register/verify the
    component in the instance's `[[components]]` + `[pipeline]` slot; (3) keep the instance topology consistency gate green.
    Add an "All three or none" Guard block (mirroring add-language.md) so a half-applied scaffold fails. Keep frontmatter
    `agent: orchestrator`, `subtask: true` unchanged (referential integrity — orchestrator is a real persona file). Keep the
    command body domain-neutral; the concrete instantiation happens in Plan 04 under `examples/`.
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_agent_referential_integrity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `harness/commands/component.md` frontmatter still resolves `agent: orchestrator` to `harness/agents/orchestrator.md`.
    - The command body references `component-engineer` template derivation + `[[components]]` registration + a Guard block.
    - `uv run pytest tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_agent_referential_integrity.py -x` exits 0 (glob-driven gates auto-cover the edit).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new agent bash scope → runtime privilege | An over-broad `bash` allow grants a component agent more than its toolchain |
| template placement → persona count | A template in the wrong dir becomes a 5th persona |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Elevation of Privilege (V4 least-priv) | component-engineer.md bash scope | mitigate | Template grants only `{"*": ask, "<BASH_SCOPE>": allow}`; the shape gate asserts permission keys ⊂ VALID_PERMISSION_KEYS and mode:subagent |
| T-8-05 | Spoofing (persona sprawl) | templates/ placement | mitigate | `test_templates_no_sprawl` pins EXPECTED_TEMPLATES to {engineer, component-engineer}; EXPECTED_PERSONAS stays 4 |
| T-8-04 | Info Disclosure (policy) | template model/frontmatter | mitigate | No real model identifier — placeholders only; existing model-identity gates stay green |
| T-8-SC | Supply chain | package installs | accept | N/A — zero package installs this phase |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint` green (template gate + command gates + referential integrity).
- Template is subagent-mode least-privilege; EXPECTED_PERSONAS unchanged (4).
</verification>

<success_criteria>
PIPE-03 satisfied: neutral component-engineer template (anti-sprawl-exempt) + /component scaffold binding + template validation gate.
</success_criteria>

<output>
Create `.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-03-SUMMARY.md` when done.
</output>
