---
phase: 08-pipeline-topology-conductor-per-component-agents
plan: 05
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - harness/skills/pipeline-map/SKILL.md
  - harness/commands/pipeline.md
  - tools/harness_lint/tests/test_skills.py
autonomous: true
requirements: [PIPE-05]
must_haves:
  truths:
    - "A pipeline-map skill exists under harness/skills/pipeline-map/ within Claude caps (name <=64, description <=1024, body <~500 lines) with a unique routing-trigger description"
    - "EXPECTED_SKILLS in test_skills.py is exactly 9 (adds pipeline-map) and the skill-set gate stays green"
    - "A /pipeline command renders the declared topology dataflow (stage->stage edges) and resolves each stage to its component agent by reading tools.harness_config — a deterministic render of loader output, no live runtime"
    - "The /pipeline command passes every command gate (frontmatter parses, agent resolves to a real persona, description is a routing signal, subtask is boolean)"
  artifacts:
    - path: "harness/skills/pipeline-map/SKILL.md"
      provides: "Topology-trace skill: how to read the pipeline slot, trace a request across stages, find the owning component agent"
      contains: "name: pipeline-map"
    - path: "harness/commands/pipeline.md"
      provides: "/pipeline command that renders the dataflow + resolves each stage to its component agent"
      contains: "agent: orchestrator"
    - path: "tools/harness_lint/tests/test_skills.py"
      provides: "EXPECTED_SKILLS bumped 8->9 (pipeline-map added)"
      contains: "pipeline-map"
  key_links:
    - from: "harness/commands/pipeline.md"
      to: "tools.harness_config components/pipeline"
      via: "render step reads the declared topology via the loader"
      pattern: "harness_config"
    - from: "tools/harness_lint/tests/test_skills.py"
      to: "harness/skills/pipeline-map/SKILL.md"
      via: "frozenset membership + shape/caps assertions"
      pattern: "pipeline-map"
---

<objective>
Make the pipeline model executable: add a `pipeline-map` core skill (clone an existing SKILL.md idiom, grow
`EXPECTED_SKILLS` 8->9) that teaches how to trace a request across the declared topology and find the owning
component agent, and a `/pipeline` command that deterministically renders the dataflow (stage->stage edges) from
`tools.harness_config` and resolves each stage to its component agent file.

Purpose: PIPE-05 — the human/agent-facing surface that turns the Plan-01 topology data + Plan-02 conductor routing
into a runnable trace. No live runtime — the "trace" is a deterministic render of loader output (RESEARCH Open Q#4).
Output: 1 new skill + 1 new command + the skill-set gate bumped to 9, all core-plane and domain-neutral (GEN-04 green).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-RESEARCH.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-PATTERNS.md

<interfaces>
<!-- Clone these exact idioms. Executor should not explore — the contracts are here. -->

harness/skills/gate-model/SKILL.md (skill frontmatter shape to mirror):
  ---
  name: <slug>
  description: >-
    Use when ... — <routing trigger>. Consult when ...
  ---
  # <slug>
  <body, progressive disclosure, <~500 lines>

tools/harness_lint/tests/test_skills.py EXPECTED_SKILLS (frozenset at line 50):
  currently 8 members {python-conventions, golden-testing, data-contracts, skill-creator,
  golden-debug, polyglot-boundary, gate-model, two-plane-memory} — add "pipeline-map" -> 9.
  Skill files are discovered by glob("*/SKILL.md"); the gate asserts names == set(EXPECTED_SKILLS).

harness/commands/component.md (command frontmatter shape to mirror):
  ---
  description: >- <routing signal>
  agent: orchestrator            # must resolve to a real persona
  subtask: true                  # boolean
  ---
  # /<name> — <title>
  <body>

Loader (from Plan 01) the command's render step reads:
  from tools.harness_config import components, pipeline, load_project
  # components() -> list[dict] with id/stage/language/consumes/produces
  # pipeline()   -> dict with edges [{from,to,contract}]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pipeline-map skill + EXPECTED_SKILLS bump</name>
  <files>harness/skills/pipeline-map/SKILL.md, tools/harness_lint/tests/test_skills.py</files>
  <read_first>
    - harness/skills/gate-model/SKILL.md (the whole file — clone its frontmatter shape: `name:`, `description: >-` routing trigger, progressive-disclosure body under the caps)
    - tools/harness_lint/tests/test_skills.py (the `EXPECTED_SKILLS` frozenset at line 50 + the caps/description-uniqueness assertions the new skill must satisfy)
  </read_first>
  <action>
    Create NEW `harness/skills/pipeline-map/SKILL.md`. Frontmatter `name: pipeline-map` (<=64 chars) and a
    `description: >-` that is a UNIQUE routing trigger — phrase it distinctly from every existing skill description
    (e.g. "Use when you need to trace a request across the pipeline dataflow, see which stage produces/consumes a
    contract, or find the component agent that owns a stage — reads the declared [[components]]/[pipeline] topology").
    Body (keep under ~150 lines, hard cap ~500): explain the topology slot fields (id/stage/language/consumes/produces),
    how edges chain stages, how to read them via `tools.harness_config` `components()`/`pipeline()`, and how each stage
    resolves to its owning component agent. Keep it domain-NEUTRAL (talk in terms of source/sink/generic stages, never
    parser/converter/standard-log) so the core skill trips no GEN-04 token. Then edit
    `tools/harness_lint/tests/test_skills.py`: add `"pipeline-map"` to the `EXPECTED_SKILLS` frozenset (8 -> 9).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_skills.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `harness/skills/pipeline-map/SKILL.md` exists; frontmatter `name: pipeline-map`; body line count < 500.
    - `EXPECTED_SKILLS` in `tools/harness_lint/tests/test_skills.py` contains `pipeline-map` and has exactly 9 members.
    - `uv run pytest tools/harness_lint/tests/test_skills.py -x` exits 0 (skill-set-no-sprawl + caps + unique-description gates green).
    - `grep -iE 'parser|converter|standard-log|equipment' harness/skills/pipeline-map/SKILL.md` returns no match (domain-neutral core skill).
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` exits 0 (GEN-04 stays green).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: /pipeline command (deterministic topology render)</name>
  <files>harness/commands/pipeline.md</files>
  <read_first>
    - harness/commands/component.md (the command frontmatter shape: `description:` routing signal, `agent: orchestrator`, `subtask:` boolean — clone it)
    - harness/commands/orient.md (a read-only render/report command body idiom — mirror how it structures numbered render steps)
    - .planning/phases/08-pipeline-topology-conductor-per-component-agents/08-01-PLAN.md (the loader API `components()`/`pipeline()` this command renders)
  </read_first>
  <action>
    Create NEW `harness/commands/pipeline.md`. Frontmatter: `description: >-` a routing signal keyed on "trace/visualize the
    pipeline dataflow and find the owning component agent"; `agent: orchestrator` (must resolve to the real primary persona);
    `subtask: true` (boolean). Body: a numbered, DETERMINISTIC render procedure that (1) loads the active topology via
    `from tools.harness_config import components, pipeline, load_project` — core reads the generic default; an instance passes
    its overlay path to `load_project(...)`; (2) prints each component as `stage N: <id> (<language>) consumes=[...] produces=[...]`;
    (3) prints the edge chain `from -> to (contract)` for every edge in `pipeline()["edges"]`; (4) resolves each stage to its
    owning component agent file and reports any stage whose agent file is missing. State explicitly there is NO live runtime —
    this is a render of declared config, not an execution. Keep the command core-plane neutral (reference source/sink generic
    ids in any example, never domain component names).
  </action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py tools/harness_lint/tests/test_agent_referential_integrity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `harness/commands/pipeline.md` exists with `agent: orchestrator` and a boolean `subtask:` field.
    - `uv run pytest tools/harness_lint/tests/test_commands.py -x` exits 0 (frontmatter parses, description is a routing signal, subtask is boolean, agent field well-formed).
    - `uv run pytest tools/harness_lint/tests/test_agent_referential_integrity.py -x` exits 0 (the command's `agent:` resolves to a real persona).
    - The render is machine-grounded: `uv run python -c "from tools.harness_config import components, pipeline; print([c['id'] for c in components()]); print(pipeline()['edges'])"` runs without error (the data the command renders is readable).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| new authored skill/command → routing surface | New markdown enters the agent routing surface other agents dispatch on |
| core plane → example plane | GEN-04 one-directional dependency; the new core skill/command must name no instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-02 | Tampering (boundary erosion) | harness/skills/pipeline-map + harness/commands/pipeline.md | mitigate | Core-plane artifacts stay domain-neutral (source/sink examples only); `test_core_no_example_dep.py` stays green (Task 1 acceptance) |
| T-8-05 | Spoofing (routing collision) | pipeline-map skill description | mitigate | Skill description is a UNIQUE routing trigger; `test_skills.py` unique-description gate enforces no collision with the other 8 skills |
| T-8-07 | Repudiation (dangling command target) | /pipeline `agent:` field | mitigate | `test_agent_referential_integrity.py` asserts `agent: orchestrator` resolves to a real persona |
| T-8-SC | Supply chain | package installs | accept | N/A — zero external package installs this phase |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint` green (skills + commands + referential-integrity gates).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` green (GEN-04, no example leak in the new core skill/command).
- `EXPECTED_SKILLS` set size is exactly 9.
</verification>

<success_criteria>
PIPE-05 satisfied: pipeline-map skill + /pipeline command make the pipeline model executable (deterministic topology render + component-agent resolution); skill-set gate green at 9; command gates green.
</success_criteria>

<output>
Create `.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-05-SUMMARY.md` when done.
</output>
