---
phase: 08-pipeline-topology-conductor-per-component-agents
plan: 04
type: execute
wave: 3
depends_on: [08-01, 08-03]
files_modified:
  - examples/log-parser/project.toml
  - examples/log-parser/agents/parser.md
  - examples/log-parser/agents/converter.md
  - examples/log-parser/agents/scheduler.md
  - examples/log-parser/agents/collector.md
  - examples/log-parser/tests/test_pipeline_topology.py
autonomous: true
requirements: [PIPE-04]
must_haves:
  truths:
    - "examples/log-parser/project.toml declares the concrete 4-component topology (parser→converter→scheduler→collector) with real edge contracts"
    - "Four instance component agents exist (parser/converter/scheduler/collector), each subagent-mode least-privilege bound to its stage"
    - "The instance topology gate (example leg) proves each component binds a real agent file + real language and edges are well-formed"
    - "The conductor can trace the full parser→converter→scheduler→collector flow from the instance overlay"
  artifacts:
    - path: "examples/log-parser/project.toml"
      provides: "Concrete 4-component instance topology overlay"
      contains: "[[components]]"
    - path: "examples/log-parser/agents/parser.md"
      provides: "Stage-1 parser component agent (subagent, least-privilege)"
      contains: "mode: subagent"
    - path: "examples/log-parser/tests/test_pipeline_topology.py"
      provides: "Instance topology + 4-agent structural gate (runs in the example leg)"
      contains: "def test_"
  key_links:
    - from: "examples/log-parser/tests/test_pipeline_topology.py"
      to: "examples/log-parser/project.toml + agents/*.md"
      via: "load_project(path=...) + agent-file existence asserts"
      pattern: "load_project"
    - from: "examples/log-parser/project.toml [[components]]"
      to: "examples/log-parser/agents/{parser,converter,scheduler,collector}.md"
      via: "each component binds a real agent file"
      pattern: "agents/"
---

<objective>
Demonstrate the mechanism end-to-end in the instance: add `examples/log-parser/project.toml` declaring the
concrete 4-component topology (parser stage1 → converter stage2 → scheduler stage3 → collector stage4 with real
edge contracts), author the 4 component agents (derived from the component-engineer template), and add an
instance topology gate that runs in the example leg. Domain contract names are allowed here — GEN-04 never scans
`examples/`.

Purpose: PIPE-04 — the concrete demonstration proving the neutral core mechanism works on a real 4-component pipeline.
Output: instance overlay + 4 component agents + instance topology gate (example test leg).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-RESEARCH.md
@.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-PATTERNS.md
@examples/log-parser/AGENTS.md

<interfaces>
Core slot shape to overlay (from Plan 01, harness/project.toml):
  [[components]] id / stage / language / produces[] / consumes[]  ;  [pipeline] edges=[{from,to,contract}]

loader signature (accepts a custom path — reuse, do NOT add a new signature):
  from tools.harness_config import components, pipeline, load_project
  load_project(path=<examples/log-parser/project.toml>)  # loader.py load_project(path=_DEFAULT_PROJECT)

examples/log-parser/agents/dotnet-engineer.md (per-instance persona idiom to clone):
  frontmatter name / description(use/when) / mode: subagent / permission.bash {"*": ask, "dotnet *": allow} / tools: Read, Edit, Bash, Grep, Glob
  body closes: "Read <package>/AGENTS.md before touching that package".

examples/log-parser/tests/conftest.py (example-leg root wiring):
  _REPO_ROOT = parents[3]  ; _EXAMPLE_ROOT = parents[1]  # examples/log-parser
  sys.path wiring already inserts repo root — reuse the conftest.

component-engineer template (from Plan 03): harness/agents/templates/component-engineer.md — fill <COMPONENT>/<STAGE>/<LANG>/<TOOLCHAIN>/<CONSUMES>/<PRODUCES>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instance topology overlay — concrete 4-component pipeline</name>
  <files>examples/log-parser/project.toml</files>
  <read_first>
    - harness/project.toml (the core generic slot shape from Plan 01 — clone the `[[components]]`/`[pipeline]` table shape; the instance overlay may use REAL domain contract names because it lives under examples/)
    - examples/log-parser/README.md (the parser→converter→scheduler→collector roles + which side is .NET vs Python)
    - examples/log-parser/AGENTS.md (the instance's language/toolchain split for the `language` refs)
  </read_first>
  <action>
    Create NEW `examples/log-parser/project.toml` declaring the concrete topology. Add four `[[components]]` tables:
    `parser` (stage=1, language="dotnet"), `converter` (stage=2, language="dotnet"), `scheduler` (stage=3, language="python"),
    `collector` (stage=4, language="python"), each with real `consumes`/`produces` edge-contract labels reflecting the
    log-parser dataflow (e.g. raw-log → `standard-log` → `equipment-progress` → collected records — use the instance's real
    contract names). Add a `[pipeline]` table with an `edges` list linking stage→stage (`parser→converter→scheduler→collector`),
    each edge's `contract` matching the upstream `produces` ∩ downstream `consumes`. This file MAY name domain contracts
    (`standard-log`/`equipment`) because GEN-04 never scans `examples/`. Do NOT change core `harness/project.toml`
    `[instance] root = ""` (Open Q#2 — `test_instance_root_is_generic_default` asserts `root == ""`); the example test reads
    this overlay path-locally. Each component's `language` MUST match a language the instance declares.
  </action>
  <verify>
    <automated>uv run python -c "from tools.harness_config import components, pipeline, load_project; cfg=load_project('examples/log-parser/project.toml'); ids=[c['id'] for c in components(cfg)]; assert ids==['parser','converter','scheduler','collector'], ids; assert len(pipeline(cfg)['edges'])==3"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/log-parser/project.toml` declares exactly 4 components with ids `[parser, converter, scheduler, collector]` and stages 1..4.
    - `[pipeline].edges` has 3 edges forming the linear parser→converter→scheduler→collector chain; each edge `contract` appears in the upstream `produces` AND downstream `consumes`.
    - Core `harness/project.toml` `[instance] root` is still `""` (unchanged).
    - `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` exits 0 (this file is under examples/, not scanned; confirm no core file was edited).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the 4 concrete component agents</name>
  <files>examples/log-parser/agents/parser.md, examples/log-parser/agents/converter.md, examples/log-parser/agents/scheduler.md, examples/log-parser/agents/collector.md</files>
  <read_first>
    - examples/log-parser/agents/dotnet-engineer.md (the whole file — clone the per-instance persona frontmatter: name / use-when description / mode: subagent / least-privilege bash allow / tools allowlist / AGENTS.md-first body line)
    - harness/agents/templates/component-engineer.md (the template to instantiate — fill <COMPONENT>/<STAGE>/<LANG>/<TOOLCHAIN>/<CONSUMES>/<PRODUCES> from Task 1's overlay)
  </read_first>
  <action>
    Create the 4 component agents by instantiating `component-engineer.md`, one per topology component. `parser.md` and
    `converter.md`: `name: parser`/`converter`, bash allow `"dotnet *"` (their `language` is dotnet). `scheduler.md` and
    `collector.md`: `name: scheduler`/`collector`, bash allow `"uv *"` (their `language` is python). Each: `mode: subagent`,
    `permission.bash {"*": ask, "<scope>": allow}`, `tools: Read, Edit, Bash, Grep, Glob`, a `use`/`when` description keyed
    on its **stage** and its `consumes`/`produces` edge contracts, and a body closing with "Read this package's `AGENTS.md`
    before touching it." These are instance-owned → invisible to core `EXPECTED_PERSONAS` (that gate globs core
    `harness/agents/` only). Keep least privilege: only the component's own toolchain scope is `allow`, everything else `ask`.
  </action>
  <verify>
    <automated>uv run python -c "from tools.harness_lint import parse_frontmatter; import pathlib; assert all(parse_frontmatter(pathlib.Path(f'examples/log-parser/agents/{n}.md').read_text())[0].get('mode')=='subagent' for n in ['parser','converter','scheduler','collector'])"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist under `examples/log-parser/agents/`; each frontmatter `mode: subagent`.
    - `parser.md`/`converter.md` bash allow-scope is `"dotnet *"`; `scheduler.md`/`collector.md` is `"uv *"`; each has `"*": ask` (least privilege).
    - Each description carries a `use`/`when` trigger referencing its stage.
    - Each agent `name` matches its topology `component.id` from Task 1.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Instance topology gate (example leg)</name>
  <files>examples/log-parser/tests/test_pipeline_topology.py</files>
  <read_first>
    - tools/harness_lint/tests/test_language_config.py (the consistency-gate body: iterate config, assert each persona file exists on disk, edges well-formed — clone the assertion shape)
    - examples/log-parser/tests/conftest.py (the example-leg root wiring: `_REPO_ROOT = parents[3]`, `_EXAMPLE_ROOT = parents[1]`, sys.path insertion — reuse it)
  </read_first>
  <action>
    Create NEW `examples/log-parser/tests/test_pipeline_topology.py`. Resolve `_EXAMPLE_ROOT = Path(__file__).resolve().parents[1]`,
    load the overlay directly via `load_project(_EXAMPLE_ROOT / "project.toml")` (reuse the existing path arg). Assert:
    `test_four_components_declared` (ids == `[parser, converter, scheduler, collector]`, stages 1..4 contiguous);
    `test_each_component_binds_a_real_agent` (a file `examples/log-parser/agents/<id>.md` exists for each component and parses
    to `mode: subagent` via `parse_frontmatter`); `test_component_languages_declared` (each `component.language` is one the
    instance uses); `test_pipeline_edges_well_formed` (mirror `test_pipeline_edges_are_well_formed` — contract ∈ upstream
    produces ∩ downstream consumes). This file MUST live under `examples/log-parser/tests/` (root `testpaths` excludes
    `examples/`, so it runs ONLY in the example leg; a core-plane copy would trip `test_core_no_example_dep.py`).
  </action>
  <verify>
    <automated>uv run pytest examples/log-parser/tests/test_pipeline_topology.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest examples/log-parser/tests/test_pipeline_topology.py -x` exits 0 with all 4 assertions collected.
    - `uv run pytest examples/log-parser/tests` exits 0 (whole example leg green, including pre-existing tests).
    - `uv run pytest` (root core suite) does NOT collect this file (it lives outside `testpaths`).
    - The instance trace is demonstrable: the gate proves every stage resolves to a real component agent file (conductor-traceable flow).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| instance agent bash scope → privilege | Each component agent must be scoped to only its toolchain |
| instance test → core suite | Instance topology test must be invisible to the core suite yet run in the example leg |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Elevation of Privilege (V4) | examples/log-parser/agents/*.md bash scopes | mitigate | Each component agent's bash allow is exactly its language toolchain (`dotnet *` or `uv *`), everything else `ask`; the instance gate asserts subagent-mode |
| T-8-02 | Tampering (boundary erosion) | domain contract names in overlay | mitigate | Domain names confined to `examples/` (GEN-04 never scans it); Task acceptance re-runs the GEN-04 guard to confirm no core file was touched |
| T-8-06 | Repudiation (invisible instance breakage) | example-leg test placement | mitigate | Gate placed under `examples/log-parser/tests/`; phase gate runs BOTH `uv run pytest` and `uv run pytest examples/log-parser/tests` |
| T-8-SC | Supply chain | package installs | accept | N/A — zero package installs this phase |
</threat_model>

<verification>
- `uv run pytest examples/log-parser/tests` green (instance leg, includes the new topology gate).
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x` green (no core→example leak introduced).
</verification>

<success_criteria>
PIPE-04 satisfied: instance overlay + 4 component agents + instance topology gate; conductor can trace the full 4-stage flow.
</success_criteria>

<output>
Create `.planning/phases/08-pipeline-topology-conductor-per-component-agents/08-04-SUMMARY.md` when done.
</output>
