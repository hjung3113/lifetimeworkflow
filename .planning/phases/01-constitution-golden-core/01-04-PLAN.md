---
phase: 01-constitution-golden-core
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - libs/normalize-spec.md
  - libs/normalize-fixtures/decimal_locale.json
  - libs/normalize-fixtures/bom_crlf.json
  - libs/normalize-fixtures/tz_iso8601.json
  - libs/normalize-fixtures/null_vs_empty.json
  - libs/python/normalize/__init__.py
  - libs/python/normalize/core.py
  - libs/python/normalize/tests/test_corpus_parity.py
  - libs/python/pyproject.toml
  - libs/dotnet/Normalize/Normalizer.cs
  - libs/dotnet/Normalize/Normalize.csproj
  - libs/dotnet/Normalize.Tests/CorpusParityTests.cs
  - libs/dotnet/Normalize.Tests/Normalize.Tests.csproj
autonomous: true
requirements: [CONTRACT-02]
user_setup: []

must_haves:
  truths:
    - "A language-neutral normalization spec defines the §4.3-4.6 rules as the single source of truth (rule is canonical, not one language)"
    - "The Python core and the .NET core both produce the identical canonical output for every entry in the shared fixture corpus"
    - "The shared (raw,canonical) corpus is consumed by BOTH test suites — any per-language drift fails the parity test"
    - "The core handles BOM strip, LF, InvariantCulture decimal, float tolerance, key/row sort, UTC/ISO-8601, and null-vs-empty"
  artifacts:
    - path: "libs/normalize-spec.md"
      provides: "Language-neutral §4.3-4.6 canonicalization rule spec (the canonical rule per D-04)"
    - path: "libs/normalize-fixtures/decimal_locale.json"
      provides: "Shared (raw,canonical) corpus entry — cross-validation drift catcher"
      contains: "canonical"
    - path: "libs/python/normalize/core.py"
      provides: "Python §4-5 thin impl: normalize_cell + normalize_tsv"
      min_lines: 30
    - path: "libs/dotnet/Normalize/Normalizer.cs"
      provides: ".NET §4-5 thin impl: InvariantCulture decimal + UTC datetime + no-BOM/LF"
      min_lines: 30
  key_links:
    - from: "libs/python/normalize/tests/test_corpus_parity.py"
      to: "libs/normalize-fixtures/*.json"
      via: "loads shared corpus"
      pattern: "normalize-fixtures"
    - from: "libs/dotnet/Normalize.Tests/CorpusParityTests.cs"
      to: "libs/normalize-fixtures/*.json"
      via: "loads the SAME shared corpus"
      pattern: "normalize-fixtures"
---

<objective>
Build the single shared canonicalizing comparator (CONTRACT-02) — the linchpin reused by both the golden runner (Plan 06) and the Phase-4 polyglot linter (POLY-01). Per D-04 the RULE is canonical: write a language-neutral spec, then two thin implementations (Python + .NET) cross-validated by ONE shared `(raw,canonical)` fixture corpus so any per-language drift fails a parity test. This is the §4.3-4.6 TSV comparator ONLY — it has nothing to do with RFC 8785/JCS (that is Plan 05, Python-only, on JSON contract text). Do not conflate the two canonicalizers.

Purpose: Representation differences (BOM/CRLF/decimal-locale/TZ) must be neutralized so golden equivalence is normalized, never byte-diff (P4). Built once, shared.
Output: language-neutral spec + shared corpus + Python core + .NET core, both green against the corpus.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-constitution-golden-core/01-CONTEXT.md
@.planning/phases/01-constitution-golden-core/01-RESEARCH.md
@CLAUDE.md
@contracts/normalization/format-conventions.schema.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Language-neutral normalization spec + shared fixture corpus</name>
  <files>libs/normalize-spec.md, libs/normalize-fixtures/decimal_locale.json, libs/normalize-fixtures/bom_crlf.json, libs/normalize-fixtures/tz_iso8601.json, libs/normalize-fixtures/null_vs_empty.json</files>
  <read_first>
    - /workspace/presentationformat/archive/parserimprove/uploads/integration_contracts_design.md §4.3 (encoding/BOM/LF/TSV-escape/null), §4.4 (timezone), §4.6 (decimal/culture)
    - CLAUDE.md (Golden-Equivalence Comparator Building Blocks table — the authoritative rule set)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Code Examples "Shared cross-validation fixture corpus" + §Normalization Comparator + D-04/D-05
  </read_first>
  <behavior>
    - decimal_locale: raw "1,5" and "1.5" both canonicalize to "1.5" (InvariantCulture, '.' separator, no thousands)
    - bom_crlf: raw bytes with UTF-8 BOM + CRLF canonicalize to no-BOM + LF
    - tz_iso8601: raw "2026-07-07 00:00:00+09:00" canonicalizes to "2026-07-06T15:00:00Z" (UTC ISO-8601)
    - null_vs_empty: an empty string and the agreed null_token canonicalize to DISTINCT tokens (§4.3 "" ≠ null)
  </behavior>
  <action>Write libs/normalize-spec.md defining the language-neutral canonicalization rules from §4.3-4.6 / CLAUDE.md building-blocks: UTF-8 with BOM stripped, LF newlines, InvariantCulture decimal with '.' separator and float tolerance for compare, deterministic key/row sort before diff, UTC ISO-8601 fixed-string datetimes, explicit null-token vs empty-string distinction, agreed TSV escape rule. Then author the shared corpus libs/normalize-fixtures/{decimal_locale,bom_crlf,tz_iso8601,null_vs_empty}.json where each entry carries raw (or base64 raw_bytes), the expected canonical output, and the cell kind — the exact structure both language test suites will load. These fixtures are the D-04 drift catcher. Use the null_token value from contracts/normalization/format-conventions.schema.json so the corpus and the materialized convention agree.</action>
  <verify>
    <automated>uv run python -c "import json,glob; fs=glob.glob('libs/normalize-fixtures/*.json'); assert len(fs)==4, fs; [json.load(open(f)) for f in fs]; print('ok')" && test -f libs/normalize-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - libs/normalize-spec.md documents all §4.3-4.6 rules
    - Four corpus JSON files exist and parse, each with raw + canonical + kind
    - The corpus null_token matches format-conventions.schema.json
  </acceptance_criteria>
  <done>Canonical rule spec + shared cross-validation corpus exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Python §4-5 core + corpus parity test</name>
  <files>libs/python/pyproject.toml, libs/python/normalize/__init__.py, libs/python/normalize/core.py, libs/python/normalize/tests/test_corpus_parity.py</files>
  <read_first>
    - libs/normalize-spec.md + libs/normalize-fixtures/*.json (just authored)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Code Examples "§4-5 TSV normalization — Python side" + §Don't Hand-Roll (use codecs/decimal/datetime/unicodedata, not float round-trip)
    - CLAUDE.md pinned stack (pytest 8.4.x)
  </read_first>
  <behavior>
    - normalize_cell("1,5", "decimal") == normalize_cell("1.5", "decimal") == "1.5"
    - normalize_tsv strips UTF-8 BOM (utf-8-sig) and joins on LF
    - datetime cells convert to UTC ISO-8601 fixed string
    - each corpus entry: normalize(raw) == entry.canonical
  </behavior>
  <action>Create libs/python/pyproject.toml (uv workspace member) and libs/python/normalize/{__init__.py,core.py}. Implement normalize_cell(value, kind, null_token) and normalize_tsv(raw_bytes) per libs/normalize-spec.md: decode utf-8-sig to strip BOM, replace CRLF→LF, split on tab, per-column normalize (decimal via decimal.Decimal + InvariantCulture-style fixed format, datetime via fromisoformat→astimezone(utc)→isoformat, explicit null-token→distinct sentinel), deterministic row sort before returning. Do NOT hand-roll number formatting or BOM detection — use stdlib decimal/codecs/datetime. Write libs/python/normalize/tests/test_corpus_parity.py that loads every libs/normalize-fixtures/*.json entry and asserts the Python core reproduces its canonical value.</action>
  <verify>
    <automated>uv run pytest libs/python/normalize/tests/test_corpus_parity.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest libs/python/normalize/tests/test_corpus_parity.py -x` exits 0
    - Every corpus entry's canonical value is reproduced by the Python core
  </acceptance_criteria>
  <done>Python §4-5 core green against the shared corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: .NET §4-5 core + corpus parity test (cross-validation)</name>
  <files>libs/dotnet/Normalize/Normalizer.cs, libs/dotnet/Normalize/Normalize.csproj, libs/dotnet/Normalize.Tests/CorpusParityTests.cs, libs/dotnet/Normalize.Tests/Normalize.Tests.csproj</files>
  <read_first>
    - libs/normalize-spec.md + libs/normalize-fixtures/*.json (the SAME corpus Python uses)
    - libs/python/normalize/core.py (parity target — .NET must produce identical canonical output)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Code Examples "§4-5 TSV normalization — .NET side" (CultureInfo.InvariantCulture, DateTimeStyles UTC, new UTF8Encoding(false)) + §Don't Hand-Roll
    - CLAUDE.md (.NET 10.0.100, xunit.v3 3.2.2)
  </read_first>
  <behavior>
    - NormDecimal("1,5") and NormDecimal("1.5") both yield "1.5" (InvariantCulture, no thousands)
    - NormDateTime yields "yyyy-MM-ddTHH:mm:ssZ" in UTC
    - output written with new UTF8Encoding(false) + explicit "\n" (no BOM, LF)
    - each corpus entry: Normalizer output == entry.canonical (identical to Python)
  </behavior>
  <action>Create libs/dotnet/Normalize/{Normalize.csproj,Normalizer.cs} targeting net10.0. Implement the §4.3-4.6 rules using System.Globalization.CultureInfo.InvariantCulture (decimal.Parse/ToString with '.' only, no thousands), DateTimeOffset.Parse with DateTimeStyles.AssumeUniversal|AdjustToUniversal → UtcDateTime → "yyyy-MM-ddTHH:mm:ssZ", and new UTF8Encoding(false) + explicit "\n" for output. Zero RFC 8785/JCS code — this is the TSV comparator only. Create libs/dotnet/Normalize.Tests/{Normalize.Tests.csproj,CorpusParityTests.cs} referencing xunit.v3 3.2.2 that loads the SAME libs/normalize-fixtures/*.json entries and asserts the .NET core reproduces each canonical value — identical output to Python proves cross-language parity (D-04). Requires .NET 10 from Plan 01's bootstrap.</action>
  <verify>
    <automated>bash tools/bootstrap/verify.sh && "$HOME/.dotnet/dotnet" test libs/dotnet/Normalize.Tests/Normalize.Tests.csproj</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test libs/dotnet/Normalize.Tests` passes all corpus parity tests
    - The .NET canonical output equals the Python canonical output for every corpus entry
    - No RFC 8785 / JCS reference anywhere in libs/dotnet
  </acceptance_criteria>
  <done>.NET §4-5 core produces byte-identical canonical output to Python across the shared corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw TSV bytes → normalizer | untrusted-representation input (BOM/CRLF/locale) canonicalized before compare |
| cross-language impls → shared corpus | the parity fixtures are the trust anchor preventing silent divergence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | per-language canonicalization drift | mitigate | Single shared (raw,canonical) corpus loaded by BOTH suites; divergence fails the parity test (D-04). |
| T-04-02 | Tampering | float/locale representation bugs | mitigate | Use stdlib decimal + InvariantCulture, never float round-trip or manual string munging (RESEARCH §Don't Hand-Roll, §4.6). |
| T-04-03 | Info Disclosure | corpus fixtures | accept | Corpus values are synthetic representation samples (1.5, dates) — no PII/secrets. |
</threat_model>

<verification>
- `uv run pytest libs/python/normalize/tests -x` exits 0.
- `dotnet test libs/dotnet/Normalize.Tests` passes.
- Both suites load the same libs/normalize-fixtures corpus and agree on every canonical value.
</verification>

<success_criteria>
A single canonicalizing comparator exists as a language-neutral rule with dual thin implementations cross-validated by a shared fixture corpus (CONTRACT-02) — the shared linchpin the golden runner and the Phase-4 linter reuse.
</success_criteria>

<output>
Create `.planning/phases/01-constitution-golden-core/01-04-SUMMARY.md` when done.
</output>
</content>
