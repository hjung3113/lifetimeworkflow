---
phase: 01-constitution-golden-core
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-04"]
files_modified:
  - components/toy-converter/ToyConverter.csproj
  - components/toy-converter/Program.cs
  - golden/repr-only/input/seed.tsv
  - golden/repr-only/expected/baseline.verified.tsv
  - golden/repr-only/meta.yaml
  - golden/value-regression/input/seed.tsv
  - golden/value-regression/expected/baseline.verified.tsv
  - golden/value-regression/meta.yaml
  - tools/golden_runner/__init__.py
  - tools/golden_runner/runner.py
  - tools/golden_runner/approve.py
  - tools/golden_runner/pyproject.toml
  - tools/golden_runner/tests/conftest.py
  - tools/golden_runner/tests/test_repr_only.py
  - tools/golden_runner/tests/test_value_regression.py
  - tools/golden_runner/tests/test_approve_gate.py
autonomous: true
requirements: [CONTRACT-03]
user_setup: []

must_haves:
  truths:
    - "The Python golden-runner spawns the .NET toy converter over the A-model CLI boundary (subprocess, shell=False), captures its output FILE, and normalizes both sides via the shared §4-5 core before diffing"
    - "A fixture differing only in BOM/CRLF/decimal-locale/TZ PASSES the golden runner (normalized equivalence, no byte-diff)"
    - "A fixture with a real value regression FAILS the golden runner"
    - "/golden-approve refuses to promote .received→.verified without an explicit human sign-off (no agent self-bless)"
    - "The golden fixtures live at TOP-LEVEL golden/repr-only/ and golden/value-regression/ (constitution-plane sibling of contracts/), keyed to golden/README.md"
  artifacts:
    - path: "components/toy-converter/Program.cs"
      provides: "Fixture-grade .NET converter: reads --in seed TSV → normalized --out TSV via libs/dotnet core; exit codes; path confinement"
      min_lines: 20
    - path: "tools/golden_runner/runner.py"
      provides: "spawn/capture/normalize/diff loop; writes .received on change, never auto-promotes; resolves dotnet via $DOTNET_ROOT/dotnet or $HOME/.dotnet/dotnet (not bare PATH)"
      min_lines: 40
    - path: "tools/golden_runner/approve.py"
      provides: "/golden-approve minimal: promote .received→.verified ONLY with explicit human flag + ADR reference"
      min_lines: 20
    - path: "golden/repr-only/expected/baseline.verified.tsv"
      provides: "approved 'legacy' baseline for the representation-only-diff case (top-level golden/)"
  key_links:
    - from: "tools/golden_runner/runner.py"
      to: "components/toy-converter (dotnet)"
      via: "subprocess.run([dotnet, dll, --in, --out], shell=False)"
      pattern: "subprocess.run"
    - from: "tools/golden_runner/runner.py"
      to: "libs/python/normalize/core.py"
      via: "normalize both sides before diff"
      pattern: "normalize"
---

<objective>
Close the walking-skeleton loop (CONTRACT-03): a Python golden-runner spawns the fixture-grade .NET toy converter over the A-model CLI boundary, captures the output file, normalizes BOTH the converter output and the approved `golden/` baseline through the shared §4-5 core (Plan 04), and diffs. Two fixtures are the phase gate — a representation-only-different case (BOM/CRLF/decimal-locale/TZ) must PASS via normalized equivalence, a real value-regression case must FAIL — proving no byte-diff false reds (P4). `/golden-approve` ships in minimal form: `.received`/`.verified` split with a refusal gate that no agent can self-bless (P9). The golden set lives at TOP-LEVEL `golden/` (constitution-plane sibling of `contracts/`, per 01-CONTEXT.md "Established Patterns"), keyed to `golden/README.md` (seeded in Plan 02).

Purpose: This is THE end-to-end slice — it exercises the real polyglot process boundary, the shared normalization core, and the golden human gate in one loop, the whole point of the walking skeleton (D-01/D-02/D-03).
Output: .NET toy converter + golden-runner + 2 fixtures + /golden-approve refusal gate, all green.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-constitution-golden-core/01-CONTEXT.md
@.planning/phases/01-constitution-golden-core/01-RESEARCH.md
@CLAUDE.md
@contracts/log-specs/standard-log.spec.yaml
@libs/normalize-spec.md
@libs/python/normalize/core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Golden fixtures + failing runner integration tests (RED)</name>
  <files>golden/repr-only/input/seed.tsv, golden/repr-only/expected/baseline.verified.tsv, golden/repr-only/meta.yaml, golden/value-regression/input/seed.tsv, golden/value-regression/expected/baseline.verified.tsv, golden/value-regression/meta.yaml, tools/golden_runner/tests/conftest.py, tools/golden_runner/tests/test_repr_only.py, tools/golden_runner/tests/test_value_regression.py</files>
  <read_first>
    - golden/README.md (top-level golden set structure: input/ expected/ meta.yaml; non-sensitive small samples — seeded in Plan 02) + contracts/log-specs/standard-log.spec.yaml (the 3-column datetime/id/decimal shape)
    - libs/normalize-spec.md + libs/normalize-fixtures/*.json (what representation differences the core neutralizes)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Walking-Skeleton Loop + §Architecture Diagram + Open Question 1 (3-column fixture touching TZ+decimal+BOM+LF) + §Pitfall 3 (byte-diff false reds)
    - .planning/phases/01-constitution-golden-core/01-CONTEXT.md <specifics> (the two-fixture success scenario)
  </read_first>
  <behavior>
    - repr-only case: input seed.tsv and expected baseline differ ONLY in BOM/CRLF/decimal-locale/TZ representation → after normalization the runner reports PASS
    - value-regression case: input produces a genuinely different value (e.g. a decimal that is not representationally equal) → runner reports FAIL
    - both tests are RED until the converter + runner exist (Tasks 2-3)
  </behavior>
  <action>Author the two golden cases under top-level golden/. repr-only/: input/seed.tsv carrying a UTF-8 BOM + CRLF + a locale/decimal + a +09:00 timestamp; expected/baseline.verified.tsv carrying the SAME logical values in canonical form (no BOM, LF, '.' decimal, UTC ISO-8601); meta.yaml listing the allowed representation-only diffs. value-regression/: input/seed.tsv whose logical value genuinely differs from expected/baseline.verified.tsv; meta.yaml. Keep all fixtures non-sensitive tiny samples (golden/README rule). Write tools/golden_runner/tests/conftest.py providing the subprocess/toy-converter fixtures using subprocess.run([list], shell=False) — and CRITICAL (project risk P5): resolve the dotnet executable EXPLICITLY via an env-var/absolute path (`os.environ.get("DOTNET_ROOT", os.path.expanduser("~/.dotnet")) + "/dotnet"`, falling back to `$HOME/.dotnet/dotnet`), NOT a bare PATH lookup (do NOT use shutil.which("dotnet") or a bare "dotnet"), because the bootstrap's PATH export does not persist across separate tool invocations in the ephemeral session. The fixture also provides a tmp --out path and locates the toy-converter project. Write test_repr_only.py (asserts PASS) and test_value_regression.py (asserts FAIL) — both currently RED. Requires pyproject.toml from Plan 01.</action>
  <verify>
    <automated>test -f golden/repr-only/input/seed.tsv && test -f golden/repr-only/expected/baseline.verified.tsv && test -f golden/value-regression/expected/baseline.verified.tsv && uv run python -c "import pathlib,base64; b=pathlib.Path('golden/repr-only/input/seed.tsv').read_bytes(); assert b[:3]==b'\xef\xbb\xbf' or b'\r\n' in b, 'repr-only input must carry BOM or CRLF'; print('fixtures ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Both golden cases exist under top-level golden/ with input/ + expected/*.verified.tsv + meta.yaml
    - repr-only input carries a real representation difference (BOM or CRLF) vs its canonical baseline
    - conftest.py + the two integration tests exist, use subprocess shell=False, and resolve dotnet via $DOTNET_ROOT/$HOME/.dotnet absolute path (no bare PATH lookup)
  </acceptance_criteria>
  <done>Two fixtures + failing runner tests scaffold the loop (RED).</done>
</task>

<task type="auto">
  <name>Task 2: .NET toy converter (A-model producer)</name>
  <files>components/toy-converter/ToyConverter.csproj, components/toy-converter/Program.cs</files>
  <read_first>
    - libs/dotnet/Normalize/Normalizer.cs (Plan 04 — the converter reuses this core; do NOT reimplement normalization)
    - .planning/phases/01-constitution-golden-core/01-CONTEXT.md D-02 (fixture-grade, NO real parser logic) + D-01 (reads seed TSV → emits normalized TSV)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Pattern 3 (subprocess + exit-code contract §4.5) + §Security Domain (path traversal in --in/--out → confine to workspace) + Open Question 2 (write to --out file)
    - CLAUDE.md (.NET 10.0.100)
  </read_first>
  <action>Create components/toy-converter/{ToyConverter.csproj,Program.cs} targeting net10.0 and referencing libs/dotnet/Normalize (project reference). Program.cs is a fixture-grade CLI: parse --in and --out args, read the seed TSV, run it through the shared Normalizer (no real parse/correction logic — D-02), and write the normalized result to --out using new UTF8Encoding(false) + explicit "\n". Exit-code contract per §4.5: 0 = success, non-zero = failure reason. Resolve and confine --in/--out to the workspace tmp/output area (reject traversal outside the repo). This is the "new" side of the A-model boundary; keep it minimal — it proves the plumbing, not domain behavior.</action>
  <verify>
    <automated>bash tools/bootstrap/verify.sh && "$HOME/.dotnet/dotnet" build components/toy-converter/ToyConverter.csproj && "$HOME/.dotnet/dotnet" run --project components/toy-converter/ToyConverter.csproj -- --in golden/repr-only/input/seed.tsv --out /tmp/toyconv_out.tsv && test -f /tmp/toyconv_out.tsv && uv run python -c "b=open('/tmp/toyconv_out.tsv','rb').read(); assert b[:3]!=b'\xef\xbb\xbf' and b'\r\n' not in b, 'output must be no-BOM LF'; print('converter ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build components/toy-converter` succeeds on .NET 10
    - Running with --in/--out writes a normalized (no-BOM, LF) output file and exits 0
    - The converter references the shared libs/dotnet Normalizer (no duplicated normalization logic)
  </acceptance_criteria>
  <done>The .NET toy converter emits normalized TSV over the A-model CLI boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Golden-runner loop + /golden-approve refusal gate (GREEN)</name>
  <files>tools/golden_runner/__init__.py, tools/golden_runner/runner.py, tools/golden_runner/approve.py, tools/golden_runner/pyproject.toml, tools/golden_runner/tests/test_approve_gate.py</files>
  <read_first>
    - tools/golden_runner/tests/{conftest.py,test_repr_only.py,test_value_regression.py} (Task 1 — the RED tests to turn GREEN)
    - components/toy-converter/Program.cs (Task 2 — the spawn target + exit-code contract)
    - libs/python/normalize/core.py (normalize both sides before diff)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Pattern 3 (subprocess shell=False) + §Pattern 4 (/golden-approve .received/.verified, Phase-1 minimal refusal) + §Pitfall 4 (agent self-bless P9)
    - .planning/phases/01-constitution-golden-core/01-CONTEXT.md D-03 (baseline = approved .verified file) + <specifics> (approve = refusal gate only, Phase 1)
    - .planning/phases/01-constitution-golden-core/01-VALIDATION.md §Manual-Only Verifications (refusal path automated; affirmative promotion manual)
  </read_first>
  <behavior>
    - runner spawns converter via subprocess.run([dotnet, ...], shell=False), captures --out file + exit code, normalizes new output AND the .verified baseline via the Python core, diffs
    - repr-only case → normalized diff empty → PASS
    - value-regression case → normalized diff non-empty → FAIL, runner writes a .received file (never overwrites .verified)
    - approve.py refuses to promote .received→.verified unless an explicit human-confirmation flag is passed AND an ADR reference is given; without them it raises/exits non-zero
  </behavior>
  <action>Create tools/golden_runner/{pyproject.toml,__init__.py,runner.py,approve.py}. runner.py: for a given golden case, resolve the dotnet executable EXPLICITLY via an env-var/absolute path (`os.environ.get("DOTNET_ROOT", os.path.expanduser("~/.dotnet")) + "/dotnet"`, falling back to `$HOME/.dotnet/dotnet`) — NOT a bare PATH lookup (do NOT use shutil.which("dotnet") or a bare "dotnet" string), because the bootstrap's PATH export does not persist across separate tool invocations (P5); mirror how 01-01 verify.sh and this plan's build/run commands hardcode `$HOME/.dotnet/dotnet`. Then spawn the toy converter with subprocess.run([dotnet_exe, "run", "--project", proj, "--", "--in", seed, "--out", tmp], shell=False), check exit code, read the --out file, normalize both it and the case's expected/*.verified.tsv via libs/python/normalize, diff normalized strings; on mismatch write expected/*.received.tsv (NEVER touch .verified) and return FAIL, else PASS. approve.py implements /golden-approve minimal form: promote a .received to .verified ONLY when an explicit human flag (e.g. --approve set by a human, not auto-passable by the agent) AND an --adr reference are present; otherwise refuse with a non-zero exit (P9 — machines gate, humans ratify). Make test_repr_only (PASS) and test_value_regression (FAIL) green, and add test_approve_gate.py asserting approve.py refuses promotion when the human flag/ADR is absent. Do NOT build hard CODEOWNERS/plugin enforcement — that is Phase 4/5.</action>
  <verify>
    <automated>uv run pytest tools/golden_runner/tests/test_repr_only.py tools/golden_runner/tests/test_value_regression.py tools/golden_runner/tests/test_approve_gate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tools/golden_runner/tests/test_repr_only.py -x` passes (repr-only PASSES via normalization)
    - `uv run pytest tools/golden_runner/tests/test_value_regression.py -x` passes (value-regression FAILS, no byte-diff false green)
    - `uv run pytest tools/golden_runner/tests/test_approve_gate.py -x` passes (approve refuses without human sign-off + ADR)
    - runner uses subprocess shell=False, resolves dotnet via $DOTNET_ROOT/$HOME/.dotnet absolute path (no bare PATH lookup), and never overwrites a .verified baseline
  </acceptance_criteria>
  <done>The full legacy↔new equivalence loop is green: repr-only PASS, value-regression FAIL, self-bless refused.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python runner → .NET CLI | A-model process boundary; args + exit code + output file cross here |
| golden input files → converter | untrusted TSV representation + user-supplied --in/--out paths |
| .received → .verified | the golden human-ratification gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering/Elevation | subprocess spawn of dotnet | mitigate | subprocess.run([list], shell=False) — never string+shell; dotnet resolved via $DOTNET_ROOT/$HOME/.dotnet absolute path (not PATH); args are repo-relative, validated (RESEARCH §Security). |
| T-06-02 | Tampering | path traversal via --in/--out | mitigate | Resolve + confine --in/--out to the workspace/tmp area in both the converter and runner. |
| T-06-03 | Repudiation/Tampering | agent self-blesses golden baseline (P9) | mitigate | .received/.verified split; approve.py refuses promotion without explicit human flag + ADR; runner never writes .verified. test_approve_gate automates the refusal. |
| T-06-04 | Info Disclosure | secrets/PII in golden fixtures | mitigate | Non-sensitive tiny samples only (golden/README rule); secret-scan hook is Phase 4. |
| T-06-05 | Tampering | byte-diff false reds (P4) | mitigate | Normalize both sides via the shared §4-5 core before diff; two-fixture demo is the acceptance proof. |
</threat_model>

<verification>
- Phase gate: `uv run pytest tools/golden_runner/tests -x` — repr-only PASS, value-regression FAIL, approve-gate refusal all green.
- Toy converter builds on .NET 10 and emits no-BOM/LF output over --in/--out.
- Runner spawns via subprocess shell=False, resolves dotnet via absolute $HOME/.dotnet path, and normalizes both sides before diffing.
</verification>

<success_criteria>
A legacy↔new fixture differing only in BOM/CRLF/decimal-locale/timezone PASSES the golden runner via normalized equivalence, a genuine value regression FAILS it (no byte-diff false reds), and `/golden-approve` refuses to update the baseline without a human sign-off (ROADMAP Phase 1 success criteria 3 + 4).
</success_criteria>

<output>
Create `.planning/phases/01-constitution-golden-core/01-06-SUMMARY.md` when done.
</output>
</content>
