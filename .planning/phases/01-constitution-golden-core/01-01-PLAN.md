---
phase: 01-constitution-golden-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - tools/bootstrap/install.sh
  - tools/bootstrap/verify.sh
  - tools/bootstrap/README.md
  - .claude/settings.json
autonomous: true
requirements: [BOOT-01, BOOT-02, BOOT-03]
user_setup: []

must_haves:
  truths:
    - "After bootstrap, dotnet --version reports a 10.x SDK inside the ephemeral container"
    - "The uv workspace resolves from a single root pyproject.toml with pinned tool versions"
    - "A second SessionStart run skips the .NET install (idempotent cache hit) instead of reinstalling"
    - "The two existing GSD SessionStart hook entries still fire alongside the new bootstrap entry"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv workspace root: members (libs/python, tools/*, components ignored), pinned deps (rfc8785, jsonschema) and dev tools (pytest 8.4.x, syrupy, ruff, pyright, check-jsonschema), pytest config"
      contains: "[tool.uv.workspace]"
    - path: "tools/bootstrap/install.sh"
      provides: "Idempotent dotnet-install --channel 10.0 + uv sync"
      min_lines: 20
    - path: "tools/bootstrap/verify.sh"
      provides: "Asserts dotnet 10 present + uv workspace resolves; exit 0 on success"
      min_lines: 10
    - path: ".claude/settings.json"
      provides: "New SessionStart array entry invoking tools/bootstrap/install.sh, appended after the 2 GSD entries"
      contains: "tools/bootstrap/install.sh"
    - path: ".gitignore"
      provides: "Ignores derived toolchain/build dirs (.dotnet/, .venv/, bin/, obj/)"
  key_links:
    - from: ".claude/settings.json"
      to: "tools/bootstrap/install.sh"
      via: "SessionStart command hook"
      pattern: "tools/bootstrap/install.sh"
    - from: "tools/bootstrap/install.sh"
      to: "$HOME/.dotnet/dotnet"
      via: "dotnet-install.sh --install-dir"
      pattern: "dotnet-install.sh"
---

<objective>
Bootstrap the ephemeral container's polyglot toolchain so every later plan can execute: install .NET 10 SDK idempotently, stand up the uv workspace with pinned Python tooling, and wire the whole thing into SessionStart so it self-heals each session without clobbering the existing GSD hooks. This is Wave 0 — nothing else in the phase can run a `dotnet` or `uv run` command until this lands.

Purpose: The env has NO .NET SDK (verified) and an outdated uv (0.8.17). BOOT gates CONTRACT-02/03/04 (all need `uv run` and/or `dotnet`).
Output: `pyproject.toml` workspace, `tools/bootstrap/{install,verify}.sh`, `.gitignore`, and a new SessionStart entry in `.claude/settings.json`.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-constitution-golden-core/01-CONTEXT.md
@.planning/phases/01-constitution-golden-core/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv workspace root + pinned Python tooling</name>
  <read_first>
    - CLAUDE.md (pinned versions table: uv 0.11.x, pytest 8.4.x, syrupy 5.2.0, ruff 0.15.x, pyright 1.1.409, check-jsonschema 0.37.4, jsonschema 4.26.0, rfc8785 0.1.4)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Standard Stack + §Version reconciliation notes (resolve A1: pin pytest 8.4.x) + §Wave 0 Gaps
  </read_first>
  <action>Create the uv workspace root pyproject.toml. Declare a [tool.uv.workspace] with members "libs/python" and "tools/*" (components/toy-converter is a .NET project, not a uv member). Add runtime deps rfc8785 (0.1.4) and jsonschema (4.26.0). Add dev deps pytest pinned >=8.4,<9 (per A1 — do NOT let it resolve to 9.1.1), syrupy 5.2.0, ruff ~=0.15, pyright 1.1.409, check-jsonschema 0.37.4. Add a [tool.pytest.ini_options] section with testpaths covering libs/python and tools, and a [tool.ruff] section. Then run `uv self update` to move 0.8.17→0.11.x, `uv add rfc8785 jsonschema`, and `uv add --dev pytest syrupy ruff pyright check-jsonschema`, producing uv.lock. Per D-09 use the exact pins from CLAUDE.md; treat forward-compatible patch bumps (pyright 1.1.411, ruff 0.15.20) as acceptable but pin the CLAUDE.md value.</action>
  <verify>
    <automated>uv sync --frozen && uv run python -c "import rfc8785, jsonschema" && uv run pytest --version</automated>
  </verify>
  <acceptance_criteria>
    - `uv --version` reports 0.11.x
    - `uv sync --frozen` exits 0 (lockfile is consistent)
    - `uv run pytest --version` prints a 8.4.x version (NOT 9.x)
    - `uv run python -c "import rfc8785, jsonschema"` exits 0
  </acceptance_criteria>
  <done>uv workspace resolves from one root pyproject.toml with all pinned tools installed and uv.lock committed.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent bootstrap + verify scripts + .gitignore</name>
  <read_first>
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Code Examples "Idempotent .NET bootstrap (bash, SessionStart)" + §Pitfall 5 (non-idempotency / cache-check) + §Runtime State Inventory (gitignore derived dirs)
    - .planning/phases/01-constitution-golden-core/01-CONTEXT.md D-08 (SessionStart idempotent auto-install), D-09
    - tools/bootstrap/install.sh (the file being created)
  </read_first>
  <action>Create tools/bootstrap/install.sh: set DOTNET_ROOT="$HOME/.dotnet" and export PATH; cache-check `test -x "$DOTNET_ROOT/dotnet" && "$DOTNET_ROOT/dotnet" --version | grep -q '^10\.'` → if true skip (fast/quiet path), else `curl -sSL https://dot.net/v1/dotnet-install.sh` and run it with `--channel 10.0 --install-dir "$DOTNET_ROOT"`. After the .NET branch, run `command -v uv >/dev/null && uv sync 2>/dev/null || true` to resolve the workspace idempotently. Keep the cached path silent so SessionStart stays fast (P5). Create tools/bootstrap/verify.sh: assert `"$HOME/.dotnet/dotnet" --version` starts with `10.` (grep '^10\.') and `uv sync --frozen` succeeds; exit non-zero with a clear message on failure. Create tools/bootstrap/README.md documenting the idempotent contract. Create/append .gitignore to exclude .dotnet/, .venv/, **/bin/, **/obj/, __pycache__/. Use no `shell=True`-style string concatenation anywhere; scripts take no untrusted args.</action>
  <verify>
    <automated>bash tools/bootstrap/install.sh && bash tools/bootstrap/verify.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash tools/bootstrap/install.sh` exits 0 and installs .NET 10 to $HOME/.dotnet on first run
    - `bash tools/bootstrap/verify.sh` exits 0 and `$HOME/.dotnet/dotnet --version` starts with `10.`
    - Running install.sh a SECOND time hits the cache branch (no re-download) and still exits 0
    - `.gitignore` contains .dotnet/, .venv/, bin/, obj/
  </acceptance_criteria>
  <done>Toolchain installs on first run, skips on cached runs, and verify.sh green-gates .NET 10 + uv.</done>
</task>

<task type="auto">
  <name>Task 3: Wire bootstrap into SessionStart (coexist with GSD)</name>
  <read_first>
    - .claude/settings.json (the file being modified — currently 2 GSD SessionStart entries: gsd-check-update.js, gsd-session-state.sh)
    - .planning/phases/01-constitution-golden-core/01-RESEARCH.md §Pitfall 5 (do NOT overwrite GSD hooks — append) + §Assumptions Log A3
    - .planning/phases/01-constitution-golden-core/01-CONTEXT.md D-08/D-09 (BOOT-03 same SessionStart path)
  </read_first>
  <action>Append a NEW object to the existing hooks.SessionStart array in .claude/settings.json whose hooks[0] is a command entry running `bash "$CLAUDE_PROJECT_DIR"/tools/bootstrap/install.sh`. Do NOT modify, reorder, or remove the two existing GSD entries (gsd-check-update.js and gsd-session-state.sh) — this is a pure array-append (A3). Preserve the exact JSON shape/indentation of the other hook groups. Do not touch PostToolUse or PreToolUse arrays.</action>
  <verify>
    <automated>uv run python -c "import json,sys; d=json.load(open('.claude/settings.json')); ss=d['hooks']['SessionStart']; cmds=[h['command'] for e in ss for h in e['hooks']]; assert any('tools/bootstrap/install.sh' in c for c in cmds), 'bootstrap entry missing'; assert any('gsd-check-update' in c for c in cmds) and any('gsd-session-state' in c for c in cmds), 'GSD entries lost'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/settings.json` parses as valid JSON
    - hooks.SessionStart contains an entry invoking tools/bootstrap/install.sh
    - Both GSD entries (gsd-check-update.js, gsd-session-state.sh) are still present and unmodified
  </acceptance_criteria>
  <done>Bootstrap runs at SessionStart alongside the untouched GSD hooks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → container | `dotnet-install.sh` + PyPI packages downloaded into the ephemeral env |
| SessionStart hook → shell | bootstrap script executed automatically each session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | rfc8785/jsonschema/pytest/ruff/pyright/check-jsonschema installs | mitigate | slopcheck already [OK] for all (RESEARCH §Package Legitimacy Audit); pin EXACT versions in pyproject.toml + uv.lock so resolution can't drift. No [ASSUMED]/[SUS] packages this plan → no blocking checkpoint needed. |
| T-01-01 | Tampering/Elevation | tools/bootstrap/install.sh | mitigate | Script takes no untrusted args; no string+shell concatenation; fetches dotnet-install over HTTPS from dot.net only. |
| T-01-02 | DoS | SessionStart re-install every session | mitigate | Idempotent cache-check (`dotnet --version | grep '^10\.'`) skips re-download (P5). |
| T-01-03 | Tampering | GSD SessionStart hooks clobbered | mitigate | Array-append only; verify test asserts both GSD entries survive (A3). |
</threat_model>

<verification>
- `bash tools/bootstrap/install.sh` (first run installs, second run cache-hits) then `bash tools/bootstrap/verify.sh` exits 0.
- `uv sync --frozen` exits 0; `uv run pytest --version` is 8.4.x.
- SessionStart JSON assertion passes (bootstrap present, GSD entries intact).
</verification>

<success_criteria>
.NET 10 SDK installs via dotnet-install.sh --channel 10.0 and the uv workspace resolves, both toolchains ready to execute a golden run in the ephemeral container (ROADMAP Phase 1 success criterion 5), wired idempotently into SessionStart without disturbing GSD.
</success_criteria>

<output>
Create `.planning/phases/01-constitution-golden-core/01-01-SUMMARY.md` when done.
</output>
