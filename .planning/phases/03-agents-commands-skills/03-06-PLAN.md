---
phase: 03-agents-commands-skills
plan: 06
type: execute
wave: 3
depends_on: [03-04]
files_modified:
  - tools/docs_sync/__init__.py
  - tools/docs_sync/generate.py
  - tools/docs_sync/pyproject.toml
  - tools/docs_sync/tests/__init__.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - harness/commands/docs-sync.md
  - docs/reference/standard-log.md
  - docs/reference/correction-rules.md
  - docs/reference/format-conventions.md
  - docs/reference/equipment-master.md
  - docs/reference/equipment-progress.md
autonomous: true
requirements: [CMD-08, DOCS-03]
must_haves:
  truths:
    - "/docs-sync regenerates docs/reference/ purely from contracts/ — no hand-written reference"
    - "Delete + regenerate is byte-identical (deterministic), proven by a committed syrupy snapshot (not git diff)"
    - "The generator writes ONLY under docs/reference/ (path-confined, no traversal)"
  artifacts:
    - path: "tools/docs_sync/generate.py"
      provides: "DOCS-03 contracts→reference generator (clone of contracts_index.py discipline)"
      exports: ["rows", "render", "write", "main"]
      min_lines: 40
    - path: "tools/docs_sync/tests/test_docs_sync_determinism.py"
      provides: "byte-identical determinism + reference-only confinement proof"
    - path: "harness/commands/docs-sync.md"
      provides: "CMD-08 command wrapping python -m tools.docs_sync"
      contains: "tools.docs_sync"
  key_links:
    - from: "tools/docs_sync/generate.py"
      to: "contracts/**/*.schema.json"
      via: "stdlib json read (same path as contract_hash)"
      pattern: "schema.json"
    - from: "tools/docs_sync/generate.py"
      to: "docs/reference/"
      via: "confined write of one .md per schema"
      pattern: "docs/reference"
---

<objective>
## Phase Goal

**As a** developer/agent in this monorepo, **I want to** regenerate the Diátaxis reference quadrant mechanically from the contract schemas via `/docs-sync`, **so that** reference docs never drift from the single source of truth and are never hand-authored (DOCS-03 anti-feature).

Build the runnable `tools/docs_sync/` generator (clone the `contracts_index.py` determinism discipline), author the `/docs-sync` command, and generate the actual `docs/reference/*.md` pages from the seed contracts. This is a migration-adjacent runnable deliverable, sequenced AFTER the golden-adjacent commands (D-05).

Purpose: proves the /docs-sync half of success criterion 4 (regenerates reference purely from contracts, deterministic).
Output: `tools/docs_sync/` uv member + `harness/commands/docs-sync.md` + generated reference pages.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agents-commands-skills/03-CONTEXT.md
@.planning/phases/03-agents-commands-skills/03-RESEARCH.md

<interfaces>
<!-- Generator contract to author (RESEARCH §"/docs-sync Generator" + §Code Examples): -->
rows(schema: dict) -> list[tuple]        # deterministic: sorted props, no timestamps/floats
render(name: str, schema: dict) -> str   # DERIVED header, stable markdown table
write(contracts="contracts", out="docs/reference") -> list[Path]  # one .md per *.schema.json, confined
main(argv=None) -> int                    # python -m tools.docs_sync

Clone target (determinism discipline): tools/memory_regen/contracts_index.py
  (rows→render→write→main, DERIVED header, no datetime, no float, byte-identical delete+regen)
Determinism proof pattern: tools/memory_regen/tests/test_contracts_index.py
  (render-twice byte-identical + generate→sha256→delete→regenerate→identical + committed syrupy snapshot)
Path-confinement idiom: tools/golden_runner/runner.py::_confine
Contract schemas carry title/description/properties{type,description}/required — rich enough for reference tables (A4 holds).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runnable docs_sync generator + determinism/confinement tests + generated reference</name>
  <files>tools/docs_sync/__init__.py, tools/docs_sync/generate.py, tools/docs_sync/pyproject.toml, tools/docs_sync/tests/__init__.py, tools/docs_sync/tests/test_docs_sync_determinism.py, docs/reference/standard-log.md, docs/reference/correction-rules.md, docs/reference/format-conventions.md, docs/reference/equipment-master.md, docs/reference/equipment-progress.md</files>
  <read_first>
    - tools/memory_regen/contracts_index.py (the rows→render→write→main + DERIVED-header + determinism structure to clone)
    - tools/memory_regen/tests/test_contracts_index.py (render-twice + delete/regenerate sha256 + syrupy snapshot pattern)
    - tools/golden_runner/runner.py §_confine (path confinement to repo)
    - contracts/log-specs/standard-log.schema.json + contracts/normalization/*.schema.json + contracts/reference-data/*.schema.json + contracts/state/*.schema.json (inputs) ; docs/reference/README.md (leave the README; it is not generated)
  </read_first>
  <behavior>
    - rows(schema) returns sorted (name, type, required, enum/const, description) tuples — deterministic, no timestamps/floats
    - render(name, schema) begins with the DERIVED "do not hand-edit — generated from contracts/ by tools.docs_sync" header and emits a stable property/column table + the §4-5 format-conventions block where applicable
    - write() emits exactly one docs/reference/<name>.md per contracts/**/*.schema.json (5 seed schemas → 5 pages), returns the written paths, and refuses/never writes outside docs/reference/ (path-confined)
    - render() called twice over the real contracts tree is byte-identical
    - generate → sha256 → delete → regenerate → identical sha256 (NOT git diff)
    - output matches a committed syrupy snapshot
    - a path-confinement test asserts write() targets stay under docs/reference/ (no traversal outside)
  </behavior>
  <action>
    Create tools/docs_sync/ as a virtual uv member (pyproject name logparser-docs-sync, requires-python >=3.11, `[tool.uv] package = false`, stdlib-only — read schemas via stdlib json exactly like contract_hash; zero new deps; syrupy/pytest come from the workspace env). generate.py implements rows/render/write/main cloning contracts_index.py: DERIVED header constant, sorted keys, no datetime.now(), no raw floats, one .md per schema keyed off the schema filename stem. render() produces title (from schema title/$id), description, a property/column table (name, type, required, enum/const, description) and, for format-conventions.schema.json, the §4.3–4.6 conventions block (BOM/LF/decimal/TZ/null). Confine every output path under docs/reference/ (mirror _confine); ONLY reference/ is generated — never touch tutorials/how-to/explanation, and leave docs/reference/README.md intact. tests/test_docs_sync_determinism.py encodes the behavior cases (render-twice byte-identical; generate→delete→regenerate sha256 identical using tmp_path; committed syrupy snapshot; path-confinement assertion that no write escapes docs/reference/). Then RUN `python -m tools.docs_sync` to materialize the 5 docs/reference/*.md pages and commit them. Run `uv sync --all-packages` to register the member.
  </action>
  <acceptance_criteria>
    - `uv sync --all-packages` succeeds; uv.lock unchanged (stdlib only)
    - `uv run pytest tools/docs_sync/tests/test_docs_sync_determinism.py -x -q` exits 0
    - `uv run python -m tools.docs_sync` writes exactly 5 files under docs/reference/ (one per seed schema), each starting with the DERIVED header
    - Deleting docs/reference/*.md (except README.md) and rerunning the generator reproduces byte-identical files (sha256 match)
    - No file is written outside docs/reference/
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/docs_sync/tests/test_docs_sync_determinism.py -x -q</automated>
  </verify>
  <done>docs_sync generates 5 DERIVED, deterministic reference pages purely from contracts, confined to docs/reference/, with byte-identical delete+regenerate proven by syrupy snapshot + sha256; `uv sync --all-packages` green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the /docs-sync command macro</name>
  <files>harness/commands/docs-sync.md</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Command Format" (frontmatter) and §"Command → Phase-1 tool map" (/docs-sync row)
    - harness/commands/golden.md (sibling macro shape authored in Plan 04)
    - tools/harness_lint/tests/test_commands.py (Plan 04 — glob validator this command must satisfy)
  </read_first>
  <action>
    Create harness/commands/docs-sync.md as a thin macro wrapping `python -m tools.docs_sync`. Frontmatter description = verb-first routing signal with a trigger token (e.g. "Use when contract schemas change to regenerate the derived reference docs"); agent = a persona that exists under harness/agents/ (e.g. python-engineer). Body documents that reference/ is DERIVED (do not hand-edit), that only docs/reference/ is regenerated, and that tutorials/how-to/explanation stay human-authored (DOCS-03 anti-feature). Ensure the frontmatter satisfies the Plan-04 glob validator (routing trigger + valid agent reference).
  </action>
  <acceptance_criteria>
    - harness/commands/docs-sync.md references tools.docs_sync and has a routing-trigger description
    - `uv run pytest tools/harness_lint/tests/test_commands.py -x -q` still passes (the new command is validated by the glob)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py -x -q</automated>
  </verify>
  <done>/docs-sync command authored, wrapping the runnable generator and documenting the derived-only invariant; passes the glob command validator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract schema → generated reference | untrusted schema content rendered into docs; output path could traverse |
| generator → docs/ tree | must write ONLY reference/, never tutorials/how-to/explanation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Tampering | write path traversal outside reference/ | mitigate | _confine-style confinement to docs/reference/; test asserts no write escapes (mirrors golden_runner._confine) |
| T-03-22 | Tampering | derived-doc rot (hand-edited reference) | mitigate | DERIVED "do not hand-edit" header + determinism test (delete+regen sha256 + syrupy); Phase-5 CI re-emit diff later (P12) |
| T-03-23 | Tampering | second hash/read impl disagreeing with the drift gate | mitigate | read schemas via stdlib json only (same path as contract_hash); do NOT re-implement hashing (P14) |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages (stdlib + workspace syrupy/pytest); `uv sync` must not mutate uv.lock |
</threat_model>

<verification>
- `uv run pytest tools/docs_sync -x -q` green (determinism + confinement).
- `uv run pytest tools/harness_lint/tests/test_commands.py -x -q` green (docs-sync command validated).
- `uv run pytest` full suite green.
- `uv run python -m tools.docs_sync` produces 5 DERIVED reference pages; delete+regenerate byte-identical.
</verification>

<success_criteria>
CMD-08 / DOCS-03 satisfied: /docs-sync regenerates docs/reference/ purely from contracts, deterministically (byte-identical delete+regenerate proven by syrupy), confined to reference/ only, with the command macro wrapping the runnable generator.
</success_criteria>

<output>
Create `.planning/phases/03-agents-commands-skills/03-06-SUMMARY.md` when done.
</output>
