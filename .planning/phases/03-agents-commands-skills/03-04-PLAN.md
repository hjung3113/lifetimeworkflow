---
phase: 03-agents-commands-skills
plan: 04
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - harness/commands/build.md
  - harness/commands/test.md
  - harness/commands/lint.md
  - harness/commands/golden.md
  - harness/commands/golden-approve.md
  - harness/commands/adr.md
  - harness/commands/checkpoint.md
  - harness/commands/component.md
  - tools/harness_lint/tests/test_commands.py
  - tools/harness_lint/tests/test_agent_referential_integrity.py
autonomous: true
requirements: [CMD-01, CMD-02, CMD-03, CMD-04, CMD-07, CMD-09]
must_haves:
  truths:
    - "Golden-adjacent commands (/build /test /lint /golden /golden-approve /adr /checkpoint /component) exist as thin macros wrapping the tested Phase-1 tool CLIs"
    - "/build and /test gate the dotnet path and skip+instruct gracefully when .NET is absent"
    - "/golden-approve wraps the already-coded human-gated refusal (no agent self-bless)"
    - "Command frontmatter is validated STRUCTURALLY per-plan; agent-file resolution is proven by a phase-level full-suite integration test (not a wave gate)"
  artifacts:
    - path: "harness/commands/golden.md"
      provides: "CMD-02 golden run + normalized diff wrapper"
      contains: "tools.golden_runner"
    - path: "harness/commands/golden-approve.md"
      provides: "CMD-03 human sign-off promotion wrapper"
      contains: "approve"
    - path: "tools/harness_lint/tests/test_commands.py"
      provides: "STRUCTURAL-ONLY validation of ALL command frontmatter (well-formed non-empty agent field + routing-trigger description + valid frontmatter; glob-based, covers Wave-3 commands too) — does NOT check agent-file existence"
    - path: "tools/harness_lint/tests/test_agent_referential_integrity.py"
      provides: "phase-level full-suite integration test: every command agent: resolves to a real harness/agents/*.md (runs green under full pytest AFTER wave 2 completes; NOT a per-plan wave gate)"
  key_links:
    - from: "harness/commands/golden.md"
      to: "tools/golden_runner/runner.py"
      via: "python -m tools.golden_runner.runner"
      pattern: "golden_runner"
    - from: "tools/harness_lint/tests/test_commands.py"
      to: "harness/commands"
      via: "glob harness/commands/*.md + parse_frontmatter"
      pattern: "parse_frontmatter"
    - from: "tools/harness_lint/tests/test_agent_referential_integrity.py"
      to: "harness/agents"
      via: "glob commands + resolve each agent: to harness/agents/<agent>.md (full-suite integration, post-wave-2)"
      pattern: "harness/agents"
---

<objective>
## Phase Goal

**As a** developer/agent in this monorepo, **I want to** drive the tested Phase-1 toolchain (build/test/lint/golden/approve/adr/checkpoint/component) through thin, discoverable slash-command macros, **so that** the golden-adjacent safety operations are one command away and land BEFORE any migration command (D-05 sequencing).

Author the 8 golden-adjacent commands as neutral `harness/commands/*.md` prompt macros wrapping the existing `tools/*` CLIs, plus a glob-based STRUCTURAL validator that covers every command (including the Wave-3 migration commands added later), plus a phase-level integration test that proves every command's `agent:` resolves to a real persona file once all of wave 2 exists.

Purpose: proves the golden-adjacent half of success criterion 3.
Output: 8 command markdown files + `test_commands.py` (structural gate) + `test_agent_referential_integrity.py` (full-suite integration).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agents-commands-skills/03-CONTEXT.md
@.planning/phases/03-agents-commands-skills/03-RESEARCH.md

<interfaces>
<!-- From Plan 02: -->
from tools.harness_lint.frontmatter import parse_frontmatter  # (dict, body)

Command → Phase-1 tool map (RESEARCH §"Command → Phase-1 tool map"):
  /build          dotnet build (gated by dotnet presence; skip+instruct if absent) + python noop
  /test           uv run pytest (+ dotnet test gated)
  /lint           ruff check + ruff format --check (+ dotnet format gated)
  /golden         python -m tools.golden_runner.runner <case>   (exit 0 PASS / 1 FAIL, normalized diff)
  /golden-approve python -m tools.golden_runner.approve --approve --adr <id>  (refusal already coded, exit 3)
  /adr            write docs/adr/NNNN-*.md (next number; never edit existing — append-only MADR)
  /checkpoint     write .memory/state/{activeContext,progress}.md; git commit (ephemeral persistence)
  /component      scaffold components/<n>/ or libs/* with per-package AGENTS.md + test harness (P11)
Claude commands use $ARGUMENTS / !`shell` / @file; opencode command frontmatter: description, agent, model, subtask.
Security: commands wrap subprocess([list], shell=False) CLIs — NEVER build shell strings from $ARGUMENTS.
Cross-wave note: the persona files referenced by `agent:` are authored by Plan 03 in the SAME wave — so the per-plan
  command gate (Task 2) is STRUCTURAL ONLY; agent-file resolution is a phase-level integration test (Task 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 8 golden-adjacent command macros</name>
  <files>harness/commands/build.md, harness/commands/test.md, harness/commands/lint.md, harness/commands/golden.md, harness/commands/golden-approve.md, harness/commands/adr.md, harness/commands/checkpoint.md, harness/commands/component.md</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Command Format (CMD-01..09)" and §"Command → Phase-1 tool map"
    - tools/golden_runner/runner.py (main entry `python -m tools.golden_runner.runner <case>`) and tools/golden_runner/approve.py (refusal shape, exit 3)
    - tools/memory_regen/ entrypoints (checkpoint regenerates derived plane) ; docs/adr/ (MADR numbering + append-only convention from adr/README.md)
  </read_first>
  <action>
    Create one markdown macro per command with neutral frontmatter (description = verb-first routing signal with a trigger token, P7; agent = the persona that should run it — e.g. golden/golden-approve → orchestrator or a reviewer, build/test/lint → the relevant engineer; subtask true where the command should not pollute primary context). Each body invokes the EXISTING tool CLI by module path — do NOT re-implement golden/hash/drift logic (Don't-Hand-Roll): /golden calls `python -m tools.golden_runner.runner $ARGUMENTS`; /golden-approve calls `python -m tools.golden_runner.approve` and documents that promotion refuses (exit 3) without human --approve + --adr + token (CMD-03, no agent self-bless); /test calls `uv run pytest`; /lint calls `ruff check` + `ruff format --check`; /build and /test wrap their dotnet path in a presence gate (check for the dotnet SDK via the Phase-1 bootstrap resolve path — $HOME/.dotnet — and SKIP with a clear instruction message when absent, D-05; never fail hard on missing .NET). /adr scaffolds the next-numbered docs/adr/NNNN-*.md from the MADR template and NEVER edits an existing ADR. /checkpoint writes .memory/state/{activeContext,progress}.md and git-commits for ephemeral-container persistence. /component scaffolds a new component/lib tree WITH a self-sufficient per-package AGENTS.md restating non-negotiables (P11) + a test harness, in the mandated order. All commands that accept arguments pass them to list-form subprocess CLIs — never interpolate $ARGUMENTS into a shell string (command-injection guard). Each command's `agent:` MUST name one of the five Plan-03 personas (orchestrator, dotnet-engineer, python-engineer, code-reviewer, explorer) so the phase-level integration test resolves it.
  </action>
  <acceptance_criteria>
    - Eight files exist under harness/commands/ each with non-empty description frontmatter
    - golden.md references tools.golden_runner.runner; golden-approve.md references tools.golden_runner.approve and documents the refusal/human-gate
    - build.md and test.md contain an explicit dotnet-absence skip/instruction path
    - adr.md documents append-only (never edit existing ADR)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pathlib; g=pathlib.Path('harness/commands/golden.md').read_text(); a=pathlib.Path('harness/commands/golden-approve.md').read_text(); b=pathlib.Path('harness/commands/build.md').read_text(); assert 'tools.golden_runner.runner' in g and 'tools.golden_runner.approve' in a and ('dotnet' in b.lower())"</automated>
  </verify>
  <done>All 8 golden-adjacent commands authored as thin macros over the tested tools, with the dotnet gate on build/test and the human-gate documented on golden-approve.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Glob-based STRUCTURAL command frontmatter validator (test_commands.py)</name>
  <files>tools/harness_lint/tests/test_commands.py</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Validation Architecture" CMD rows and §"Pitfall P7"
    - tools/harness_lint/frontmatter.py (Plan 02)
    - harness/commands/*.md (Task 1 outputs)
  </read_first>
  <behavior>
    - Globs harness/commands/*.md (so Wave-3 migration commands are auto-covered without editing this test)
    - For each command: valid frontmatter parses; description present, non-empty, and contains a routing trigger token
    - For each command: an `agent` field is present, is a non-empty string, and is well-formed (STRUCTURAL only — it does NOT check whether the referenced agent FILE exists; that cross-file resolution is the phase-level integration test in Task 3, because agents are authored by Plan 03 in the SAME wave)
    - Any `subtask` value, if present, is a boolean
    - The eight golden-adjacent command names are all present
  </behavior>
  <action>
    Create tools/harness_lint/tests/test_commands.py that globs harness/commands/*.md, parses each via parse_frontmatter, and asserts the STRUCTURAL behavior cases: valid frontmatter, a routing-trigger description, a well-formed non-empty `agent:` field, and a boolean `subtask` if present. Do NOT assert that the referenced agent FILE exists under harness/agents/ — agents are authored by Plan 03 in the SAME wave 2, so a file-existence check here would be a false cross-wave gate; agent-file resolution is deferred to the phase-level integration test (Task 3). Keep the check glob-driven so the migration commands added in Plans 06/07 are validated by the same test with no edits here (avoids cross-wave file overlap). Fail loud with the offending command filename.
  </action>
  <acceptance_criteria>
    - `uv run pytest tools/harness_lint/tests/test_commands.py -x -q` exits 0
    - Adding a command with a noun-phrase-only description would FAIL the routing-trigger assertion (invariant is real)
    - A command missing an `agent:` field FAILS the structural check; but a command referencing a not-yet-authored agent still PASSES test_commands (file existence is checked only by the Task-3 integration test)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py -x -q</automated>
  </verify>
  <done>test_commands.py glob-validates command frontmatter STRUCTURALLY (routing triggers + well-formed non-empty agent field + valid frontmatter) and passes for the 8 golden-adjacent commands — with agent FILE resolution deferred to the phase-level integration test.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-level agent-referential-integrity integration test (full-suite, NOT a wave-2 gate)</name>
  <files>tools/harness_lint/tests/test_agent_referential_integrity.py</files>
  <read_first>
    - tools/harness_lint/frontmatter.py (Plan 02 — parse_frontmatter)
    - harness/commands/*.md (this plan's + Plans 06/07 command outputs) ; harness/agents/*.md (authored by Plan 03, SAME wave — the reason this is a full-suite test, not a Task-2 gate)
  </read_first>
  <action>
    Create tools/harness_lint/tests/test_agent_referential_integrity.py: glob harness/commands/*.md, parse each via parse_frontmatter, and assert every non-empty `agent:` value resolves to a real harness/agents/<agent>.md file. This is the cross-file assertion moved OUT of Task 2 because the referenced personas are authored by Plan 03 in the SAME wave — it is intentionally a phase-level / full-suite integration test that runs green under `uv run pytest` only AFTER all of wave 2 exists, NOT a per-plan wave-2 gate. Fail loud with the offending command filename + the missing agent path. (Because it cannot be a wave-2 gate, its per-task verify only asserts the test file is syntactically importable; the assertion itself runs in the phase-level full-suite check below.)
  </action>
  <acceptance_criteria>
    - tools/harness_lint/tests/test_agent_referential_integrity.py exists and is importable (syntactically valid)
    - The test globs harness/commands/*.md and resolves each `agent:` to harness/agents/<agent>.md, failing loud on a dangling reference
    - It is documented as a full-suite integration test (green after wave 2), NOT wired as a Task-2 per-plan gate
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m py_compile tools/harness_lint/tests/test_agent_referential_integrity.py</automated>
  </verify>
  <done>The agent-referential-integrity cross-check exists as a full-suite integration test (validated in the phase-level `uv run pytest` after wave 2 completes), decoupled from Task 2's structural per-plan gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command $ARGUMENTS → wrapped CLI | user/agent-supplied args reach a subprocess |
| /golden-approve → golden baseline | promotion must require a human, never an agent self-bless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | command injection via $ARGUMENTS | mitigate | commands wrap list-form subprocess CLIs (golden_runner already shell=False); never build shell strings from args |
| T-03-14 | Repudiation/EoP | agent self-blessing a golden | mitigate | /golden-approve wraps tools.golden_runner.approve refusal (exit 3 without human --approve+--adr+token) — reused, not re-implemented |
| T-03-15 | Spoofing/routing | description-as-label | mitigate | test_commands asserts routing trigger token per command (P7) |
| T-03-16 | Tampering | dangling agent reference | mitigate | Task-2 structural gate asserts a well-formed non-empty `agent:` field per command; the phase-level integration test (test_agent_referential_integrity.py) asserts each `agent:` resolves to a real harness/agents file under the full `uv run pytest` suite after wave 2 |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_commands.py -x -q` green (structural command gate — the per-plan wave-2 gate).
- `uv run python -m py_compile tools/harness_lint/tests/test_agent_referential_integrity.py` green (integration test authored + importable).
- `uv run pytest` full suite green AFTER wave 2 completes — this is where test_agent_referential_integrity.py runs green (every command `agent:` resolves to a real harness/agents file authored by Plan 03) and Phase-1 golden_runner/approve tests stay green (CMD-03 gate untouched).
</verification>

<success_criteria>
CMD-01/02/03/04/07/09 satisfied: golden-adjacent commands exist as thin, validated macros over the tested tools; /build+/test gate .NET gracefully; /golden-approve preserves the human-only promotion gate; all command frontmatter carries routing triggers and a well-formed agent field (structural per-plan gate), with agent-file resolution enforced by a phase-level full-suite integration test.
</success_criteria>

<output>
Create `.planning/phases/03-agents-commands-skills/03-04-SUMMARY.md` when done.
</output>
</output>
