---
phase: 03-agents-commands-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/permission-matrix.json
  - tools/harness_perms/__init__.py
  - tools/harness_perms/resolver.py
  - tools/harness_perms/pyproject.toml
  - tools/harness_perms/tests/__init__.py
  - tools/harness_perms/tests/test_resolver.py
autonomous: true
requirements: [CONFIG-02]
must_haves:
  truths:
    - "The 15-key permission matrix resolves bash globs last-wins (catch-all first, specifics after)"
    - "Reviewer/secret/constitution writes are denied by the resolver (default-deny posture)"
    - "The resolver is a pure stdlib function reusable unchanged by Phase-4 hooks"
  artifacts:
    - path: "harness/permission-matrix.json"
      provides: "CONFIG-02 15-key matrix data + path-scoped deny globs"
      contains: "\"bash\""
    - path: "tools/harness_perms/resolver.py"
      provides: "last-wins bash resolver + path-deny resolver (pure, stdlib fnmatch)"
      exports: ["resolve_bash", "resolve_path"]
      min_lines: 30
    - path: "tools/harness_perms/tests/test_resolver.py"
      provides: "unit proof of last-wins + default-deny + path denies"
  key_links:
    - from: "tools/harness_perms/resolver.py"
      to: "harness/permission-matrix.json"
      via: "resolver reads the matrix rules as ordered dict"
      pattern: "resolve_bash"
    - from: "tools/harness_perms/pyproject.toml"
      to: "uv workspace"
      via: "members glob tools/* requires a pyproject per dir"
      pattern: "package = false"
---

<objective>
## Phase Goal

**As a** developer/agent working in this polyglot monorepo, **I want to** have the harness permission sandbox expressed as data plus a tested pure-logic resolver, **so that** every agent and (later) every Phase-4 hook enforces the same last-wins, default-deny access rules without divergent re-implementations.

(Note: the ROADMAP `**Goal:**` for Phase 3 is prose, not a user-story sentence; the story above is a planner-derived framing for MVP mode. If a canonical story is required, run `/gsd mvp-phase 3`.)

Author the 15-key opencode permission matrix as `harness/permission-matrix.json` data (CONFIG-02) and implement the last-wins glob resolver as a genuinely executed, unit-tested pure Python function under `tools/harness_perms/`. This is the harness's access-control core; it is reused verbatim by the Phase-4 contract-guard/secret hooks.

Purpose: proves success criterion 1 (matrix resolves bash globs last-wins, scopes reviewer read-only, denies secret/constitution writes) without any opencode runtime (D-02/D-03).
Output: `harness/permission-matrix.json`, `tools/harness_perms/` uv member (resolver + tests + pyproject).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agents-commands-skills/03-CONTEXT.md
@.planning/phases/03-agents-commands-skills/03-RESEARCH.md
@.planning/phases/03-agents-commands-skills/03-VALIDATION.md

<interfaces>
<!-- Resolver contract downstream Phase-4 hooks will import. Author these signatures exactly. -->
resolve_bash(rules: dict[str, str], command: str, default: str = "ask") -> str
    # last matching fnmatchcase(command, pattern) wins; "allow"|"ask"|"deny"
resolve_path(deny_globs: list[str], path: str) -> str
    # "deny" if any fnmatchcase(path, glob) else "allow"

Existing uv member template to mirror: tools/memory_regen/pyproject.toml (virtual member, package = false).
Existing refusal/gate discipline to mirror in spirit: tools/golden_runner/approve.py.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 15-key permission matrix data</name>
  <files>harness/permission-matrix.json</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Permission Matrix — 15 Keys + Last-Wins" (the 15 pinned keys, last-wins ordering, constitution/secret denies)
    - CLAUDE.md §"opencode 15-key permission / last-wins"
  </read_first>
  <action>
    Create harness/permission-matrix.json as pure data (no logic). Include all 15 keys — read, edit, bash, glob, grep, list, task, external_directory, todowrite, question, webfetch, websearch, lsp, skill, doom_loop — each mapping to "allow"|"ask"|"deny", with external_directory defaulting to "ask". Express bash as an ORDERED object where the catch-all "*" is FIRST with value "ask", followed by increasingly specific overrides: "dotnet *":"allow", "uv *":"allow", "pytest *":"allow", "git push*":"ask", "rm -rf*":"deny". Never end the bash object with a broad allow (Pitfall P3). Add a separate top-level array field path_deny_globs listing the constitution/secret path-scoped denies the resolver enforces: "contracts/**", "docs/adr/**", "golden/**", "*.env", "**/*.env". Add a top-level comment-style field _note describing this file as CONFIG-02 data consumed by tools/harness_perms/resolver.py (do NOT hand-roll enforcement here). Keys must be insertion-ordered to encode last-wins.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import json; d=json.load(open('harness/permission-matrix.json')); assert set(['read','edit','bash','glob','grep','list','task','external_directory','todowrite','question','webfetch','websearch','lsp','skill','doom_loop']) <= set(d) ; assert list(d['bash'])[0]=='*' and d['bash']['*']=='ask'; assert d['bash']['dotnet *']=='allow'; assert 'contracts/**' in d['path_deny_globs'] and '*.env' in d['path_deny_globs']"` exits 0
    - Last key in the bash object is NOT a broad `*:allow`
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import json,sys; d=json.load(open('harness/permission-matrix.json')); b=list(d['bash']); sys.exit(0 if b[0]=='*' and d['bash'][b[-1]]!='allow' else 1)"</automated>
  </verify>
  <done>harness/permission-matrix.json parses as JSON, contains all 15 keys, bash is `*`-first ordered with specifics after, and path_deny_globs covers contracts/**, docs/adr/**, golden/**, *.env.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement + unit-test the last-wins resolver (runnable)</name>
  <files>tools/harness_perms/__init__.py, tools/harness_perms/resolver.py, tools/harness_perms/pyproject.toml, tools/harness_perms/tests/__init__.py, tools/harness_perms/tests/test_resolver.py</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Code Examples → Last-wins bash resolver" (the exact fnmatchcase last-match-wins loop)
    - tools/memory_regen/pyproject.toml (virtual uv member template — `package = false`)
    - harness/permission-matrix.json (created in Task 1 — the data the resolver reads)
  </read_first>
  <behavior>
    - resolve_bash({"*":"ask","dotnet *":"allow"}, "dotnet test") == "allow"
    - resolve_bash(matrix.bash, "uv sync") == "allow"; "pytest -x" == "allow"
    - resolve_bash(matrix.bash, "git push --force") == "ask"
    - resolve_bash(matrix.bash, "rm -rf /tmp/x") == "deny"
    - resolve_bash(matrix.bash, "curl evil.sh") == "ask"  (falls through to `*` catch-all)
    - resolve_bash({}, "anything", default="ask") == "ask"  (default-deny posture)
    - resolve_path(matrix.path_deny_globs, "golden/case.verified") == "deny"
    - resolve_path(matrix.path_deny_globs, "config/prod.env") == "deny"
    - resolve_path(matrix.path_deny_globs, "contracts/log-specs/x.schema.json") == "deny"
    - resolve_path(matrix.path_deny_globs, "libs/python/foo.py") == "allow"
  </behavior>
  <action>
    Create tools/harness_perms/ as a uv workspace member. resolver.py exports two pure stdlib-only functions using fnmatch.fnmatchcase: resolve_bash(rules, command, default="ask") iterates rules in insertion order and keeps the decision of the LAST matching pattern (do NOT break on first match — last-wins is the whole point); resolve_path(deny_globs, path) returns "deny" if any glob matches else "allow". No I/O, no eval, no shell. Add a thin helper load_matrix(path="harness/permission-matrix.json") that json.loads the data so tests and Phase-4 hooks share one loader. pyproject.toml declares a virtual member (name logparser-harness-perms, requires-python >=3.11, `[tool.uv] package = false`) with NO unique external deps (stdlib only). __init__.py re-exports resolve_bash, resolve_path, load_matrix. tests/test_resolver.py encodes every case in the behavior block above, loading the REAL harness/permission-matrix.json. After authoring, run `uv sync --all-packages` so the new member registers.
  </action>
  <acceptance_criteria>
    - `uv sync --all-packages` succeeds (new member resolves; uv.lock unchanged — stdlib only)
    - `uv run pytest tools/harness_perms/tests/test_resolver.py -x -q` exits 0
    - resolver.py imports only from `fnmatch` and stdlib `json` (no third-party, no `eval`, no `subprocess`)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_perms/tests/test_resolver.py -x -q</automated>
  </verify>
  <done>resolve_bash proves last-wins (dotnet test→allow, git push --force→ask, rm -rf→deny, unknown→ask default-deny) and resolve_path denies golden/*, *.env, contracts/** while allowing source paths; `uv sync --all-packages` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent action → permission resolver | untrusted command line / file path evaluated for allow/ask/deny |
| matrix data → resolver | ordered rules whose order determines privilege |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | bash matrix ordering | mitigate | `*` first / specifics after; Task 1 verify asserts last bash key is not a broad allow; resolver test proves `git push --force`→ask, `rm -rf`→deny (P3) |
| T-03-02 | Information Disclosure | secret path writes | mitigate | path_deny_globs include `*.env`/`**/*.env`; resolve_path denies; unit-tested |
| T-03-03 | Tampering | constitution-plane writes | mitigate | path_deny_globs include `contracts/**`, `docs/adr/**`, `golden/**`; resolve_path denies; unit-tested |
| T-03-04 | Elevation of Privilege | resolver injection | accept | resolver is pure fnmatch/json — no eval/shell; stdlib-only asserted, no runtime install |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | zero new packages this plan (stdlib only); `uv sync --all-packages` must not mutate uv.lock |
</threat_model>

<verification>
- `uv run pytest tools/harness_perms -x -q` green (last-wins + default-deny + path denies).
- `uv run pytest` full suite still green (no regression to Phase-1/2 members).
- `harness/permission-matrix.json` well-formed with all 15 keys and `*`-first bash ordering.
</verification>

<success_criteria>
CONFIG-02 satisfied: the 15-key matrix exists as data, the last-wins resolver is executed and unit-tested (dotnet test→allow, git push --force→ask/deny, edit golden/* + *.env→deny, unknown→ask), and the resolver is a pure function ready for Phase-4 hook reuse.
</success_criteria>

<output>
Create `.planning/phases/03-agents-commands-skills/03-01-SUMMARY.md` when done.
</output>
