---
phase: 03-agents-commands-skills
plan: 07
type: execute
wave: 3
depends_on: [03-04]
files_modified:
  - harness/commands/new-normalization-rule.md
  - harness/commands/strangler-step.md
  - tools/strangler_guard/__init__.py
  - tools/strangler_guard/guard.py
  - tools/strangler_guard/pyproject.toml
  - tools/strangler_guard/tests/__init__.py
  - tools/strangler_guard/tests/test_refusal.py
autonomous: true
requirements: [CMD-05, CMD-06]
must_haves:
  truths:
    - "/strangler-step refuses (non-zero exit) when no captured legacy golden baseline exists for the target path"
    - "/strangler-step extracts one path only and requires /golden parity to pass"
    - "/new-normalization-rule enforces the contract → (input,expected) data case → code order via failing stubs"
  artifacts:
    - path: "tools/strangler_guard/guard.py"
      provides: "CMD-06 runnable baseline-refusal gate (mirrors approve.py refusal shape)"
      exports: ["require_baseline", "StranglerRefused", "main"]
    - path: "tools/strangler_guard/tests/test_refusal.py"
      provides: "proof the gate refuses without a baseline (non-zero exit)"
    - path: "harness/commands/new-normalization-rule.md"
      provides: "CMD-05 order-enforcing scaffold command"
      contains: "input"
  key_links:
    - from: "harness/commands/strangler-step.md"
      to: "tools/strangler_guard/guard.py"
      via: "command invokes python -m tools.strangler_guard before extraction"
      pattern: "strangler_guard"
    - from: "tools/strangler_guard/guard.py"
      to: "golden baseline (golden/)"
      via: "checks for a captured .verified baseline for the target path"
      pattern: "verified"
---

<objective>
## Phase Goal

**As a** developer/agent migrating legacy code in this monorepo, **I want to** strangler-extract exactly one path only behind a *trusted* golden net — refused outright when no baseline exists — and add normalization rules contract-first, **so that** migration can never silently regress (the whole reason migration commands are gated behind the golden safety net, D-05).

Author the two migration scaffolding commands (CMD-05, CMD-06) plus the runnable strangler baseline-refusal gate, sequenced AFTER the golden-adjacent commands (Wave 3, depends on Plan 04).

Purpose: proves the /strangler-step and /new-normalization-rule halves of success criteria 3 and 4.
Output: 2 migration command macros + `tools/strangler_guard/` uv member (refusal gate + test).
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agents-commands-skills/03-CONTEXT.md
@.planning/phases/03-agents-commands-skills/03-RESEARCH.md

<interfaces>
<!-- Refusal gate contract to author (RESEARCH §"Pattern 5: Refuse-without-baseline gate"): -->
class StranglerRefused(Exception): ...
require_baseline(target_path: str, golden_dir="golden") -> Path
    # returns the baseline path if a captured legacy .verified baseline exists for target_path;
    # else raises StranglerRefused (mirrors tools/golden_runner/approve.py::GoldenApprovalRefused)
main(argv=None) -> int   # exit 0 if baseline present, non-zero (e.g. 3) on refusal — python -m tools.strangler_guard

Mirror target (refusal shape + CLI exit code): tools/golden_runner/approve.py (GoldenApprovalRefused → exit 3)
Order-enforcing scaffold pattern: RESEARCH §"Pattern 4" (contract → (input,expected) data case → code stub that fails until filled)
Command glob validator this plan must satisfy: tools/harness_lint/tests/test_commands.py (Plan 04)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runnable strangler baseline-refusal gate + test</name>
  <files>tools/strangler_guard/__init__.py, tools/strangler_guard/guard.py, tools/strangler_guard/pyproject.toml, tools/strangler_guard/tests/__init__.py, tools/strangler_guard/tests/test_refusal.py</files>
  <read_first>
    - tools/golden_runner/approve.py (GoldenApprovalRefused exception + CLI exit-3 refusal shape to mirror)
    - tools/golden_runner/runner.py (how golden cases/baselines are named/located under golden/)
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Pattern 5" + §"Pitfall P10"
  </read_first>
  <behavior>
    - require_baseline("some/legacy/path") with NO captured baseline under golden/ raises StranglerRefused
    - require_baseline(target) WITH a captured .verified baseline returns its Path
    - main(["some/legacy/path"]) with no baseline returns a non-zero exit code (e.g. 3) and prints a REFUSED message
    - main([target]) with a baseline present returns 0
    - the gate never fabricates or creates a baseline (machines gate; golden/human provide the baseline)
  </behavior>
  <action>
    Create tools/strangler_guard/ as a virtual uv member (pyproject name logparser-strangler-guard, requires-python >=3.11, `[tool.uv] package = false`, stdlib-only). guard.py defines StranglerRefused(Exception), require_baseline(target_path, golden_dir="golden") that looks for a captured legacy baseline (a .verified file) associated with the target path and raises StranglerRefused with a clear "REFUSED: no captured legacy golden baseline for <path>" message when absent, and main(argv) that maps the exception to a non-zero exit code exactly like approve.py maps GoldenApprovalRefused to exit 3. Never create/fabricate a baseline. tests/test_refusal.py (using tmp_path + a monkeypatched/parameterized golden_dir) asserts: refusal raises + main returns non-zero when no baseline; returns the path + main returns 0 when a baseline file exists. Run `uv sync --all-packages` to register the member.
  </action>
  <acceptance_criteria>
    - `uv sync --all-packages` succeeds; uv.lock unchanged (stdlib only)
    - `uv run pytest tools/strangler_guard/tests/test_refusal.py -x -q` exits 0
    - `uv run python -m tools.strangler_guard nonexistent/path` exits non-zero and prints REFUSED
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/strangler_guard/tests/test_refusal.py -x -q</automated>
  </verify>
  <done>The strangler gate refuses (non-zero exit) without a captured legacy golden baseline and passes only when one exists, mirroring the approve.py refusal discipline; `uv sync --all-packages` green.</done>
</task>

<task type="auto">
  <name>Task 2: Author /strangler-step + /new-normalization-rule migration commands</name>
  <files>harness/commands/strangler-step.md, harness/commands/new-normalization-rule.md</files>
  <read_first>
    - .planning/phases/03-agents-commands-skills/03-RESEARCH.md §"Command → Phase-1 tool map" (/strangler-step, /new-normalization-rule rows) + §"Pattern 4" + §"Pattern 5"
    - tools/strangler_guard/guard.py (Task 1 — the gate the command invokes)
    - harness/commands/golden.md (Plan 04 sibling macro shape) ; tools/harness_lint/tests/test_commands.py (glob validator to satisfy)
    - contracts/normalization/correction-rules.schema.json (the contract /new-normalization-rule scaffolds against)
  </read_first>
  <action>
    Create harness/commands/strangler-step.md as a macro that FIRST invokes `python -m tools.strangler_guard $ARGUMENTS` and aborts (non-zero) when the gate refuses — extracts ONE path only — then requires `/golden` parity to pass before the step is accepted (D-05, success criterion 4). Frontmatter description = verb-first routing signal with a trigger token (e.g. "Use when migrating one legacy path behind the golden net"); agent = an existing persona. Body documents: refuses without a captured legacy baseline, single-path only, /golden parity mandatory, big-bang forbidden (anti-feature). Create harness/commands/new-normalization-rule.md as an order-enforcing scaffold macro: it scaffolds in the MANDATED order — (1) add the contract entry, (2) add a data-based (input, expected) case, (3) stub the code that FAILS until filled — so the order cannot be silently skipped (Pattern 4). Frontmatter with routing-trigger description + valid agent. Both commands must satisfy the Plan-04 glob validator (routing trigger + agent referential integrity).
  </action>
  <acceptance_criteria>
    - strangler-step.md invokes tools.strangler_guard and documents refuse-without-baseline + single-path + /golden parity
    - new-normalization-rule.md documents the contract → (input,expected) case → code order with a failing stub
    - `uv run pytest tools/harness_lint/tests/test_commands.py -x -q` passes (both new commands validated by the glob)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_commands.py -x -q</automated>
  </verify>
  <done>Both migration commands authored: /strangler-step gated on the runnable baseline refusal + single-path + /golden parity; /new-normalization-rule enforces contract→data-case→code order; both pass the glob validator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration request → strangler gate | a migration must not proceed without a trusted equivalence reference |
| command $ARGUMENTS → strangler_guard CLI | target path reaches a subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-24 | Repudiation/EoP | migration without a captured baseline (silent regression) | mitigate | require_baseline raises + main exits non-zero without a golden baseline; unit-tested (P10) |
| T-03-25 | Tampering | big-bang / multi-path extraction | mitigate | command documents single-path-only + mandatory /golden parity gate (anti-feature /rewrite-legacy forbidden) |
| T-03-26 | Tampering | skipping contract-first order | mitigate | /new-normalization-rule scaffolds contract → (input,expected) → failing code stub; order cannot be silently skipped (Pattern 4) |
| T-03-27 | Tampering | command injection via $ARGUMENTS | mitigate | strangler_guard invoked as a list-form subprocess CLI; never interpolate args into a shell string |
</threat_model>

<verification>
- `uv run pytest tools/strangler_guard -x -q` green (refusal gate).
- `uv run pytest tools/harness_lint/tests/test_commands.py -x -q` green (both migration commands validated).
- `uv run pytest` full suite green.
- `uv run python -m tools.strangler_guard nonexistent/path` exits non-zero (REFUSED).
</verification>

<success_criteria>
CMD-05 / CMD-06 satisfied: /strangler-step refuses without a captured legacy golden baseline (runnable, non-zero exit), extracts one path only, requires /golden parity; /new-normalization-rule enforces the contract → data-case → code order — both authored and validated after the golden-adjacent commands (D-05 sequencing).
</success_criteria>

<output>
Create `.planning/phases/03-agents-commands-skills/03-07-SUMMARY.md` when done.
</output>
