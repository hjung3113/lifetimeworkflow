---
phase: 05.5-authored-surface
plan: 02
type: execute
wave: 2
depends_on: [055-01]
files_modified:
  - harness/skills/data-contracts/SKILL.md
  - harness/commands/new-normalization-rule.md
  - harness/agents/orchestrator.md
  - harness/agents/explorer.md
  - libs/normalize-spec.md
  - tools/memory_regen/tests/test_agents_md.py
  - tools/contract_drift/drift.py
  - tools/contract_drift/tests/test_classify.py
  - tools/docs_sync/tests/test_docs_sync_determinism.py
  - examples/log-parser/AGENTS.md
  - examples/log-parser/README.md
autonomous: true
requirements: [GEN-05]

must_haves:
  truths:
    - "The surviving core authored assets data-contracts and new-normalization-rule carry no semiconductor domain examples — their bodies reference a domain-neutral instance (contracts/sample) or abstract placeholders, methodology/order logic unchanged."
    - "Core personas orchestrator and explorer no longer name the moved dotnet-engineer persona; libs/normalize-spec.md no longer names libs/dotnet."
    - "Every remaining core (tools/harness/libs) occurrence of a guard token (dotnet-engineer, dotnet-conventions, normalization-catalog, pipeline-patterns, libs/dotnet, equipment, standard-log, correction-rules) is removed or reworded, so the 055-03 guard will read 0."
    - "The example's own AGENTS.md and README record that examples/log-parser/ now owns the moved skills and persona."
    - "The non-example pytest suite stays green — no methodology/order/logic changed, only prose and fixture field names."
  artifacts:
    - path: "harness/commands/new-normalization-rule.md"
      provides: "Contract-first command with domain-neutral examples; three-step order intact"
      contains: "contracts/sample"
    - path: "harness/skills/data-contracts/SKILL.md"
      provides: "Contract-first methodology skill with genericized layout block"
      contains: "check-jsonschema"
    - path: "examples/log-parser/AGENTS.md"
      provides: "Instance doc updated to own the moved skills/persona"
      contains: "skills/"
  key_links:
    - from: "harness/commands/new-normalization-rule.md"
      to: "contracts/sample (generic default instance)"
      via: "domain example anchor swapped from correction-rules.catalog.yaml to the generic instance"
      pattern: "contracts/sample"
    - from: "harness/agents/orchestrator.md"
      to: "python-engineer (core) + instance-declared engineers via project.toml"
      via: "specialist list reworded to drop the moved dotnet-engineer"
      pattern: "python-engineer"
    - from: "libs/normalize-spec.md"
      to: "a language-side twin (generic)"
      via: "libs/dotnet reference reworded to language-neutral"
      pattern: "language-side"
---

## Phase Goal

**As a** developer or coding agent turning this log-parser harness into a reusable template, **I want to** purge semiconductor and moved-asset vocabulary from the surviving core authored surface and comments, **so that** the core planes carry only domain-neutral prose and the 055-03 guard can enforce that purity to 0.

<objective>
Sweep every remaining core (`tools/`, `harness/`, `libs/`) occurrence of a Phase 5.5 guard token — the exhaustive inventory RESEARCH enumerated in §Code Examples "Core token inventory" — by rewording prose and renaming arbitrary fixture fields, WITHOUT changing any methodology, ordering, or gate logic (D-02, D-05; RESEARCH Pattern 2 "genericize-body-keep-asset"). Then update the example's own docs to record its newly-owned skills/persona.

Two classes of edit:
1. **Authored-surface genericization** (`data-contracts`, `new-normalization-rule`, `orchestrator`, `explorer`, `libs/normalize-spec.md`) — reword domain examples to the generic `contracts/sample` instance / abstract placeholders; drop `dotnet-engineer` naming; keep the contract-first ORDER and drift-gate prose verbatim.
2. **Core comment/fixture sweep** (`memory_regen`, `contract_drift`, `docs_sync` test comments + `test_classify` fixture field names) — reword comments and rename arbitrary JSON-schema fixture fields off `equipment`/`standard-log`/`correction-rules`.

Purpose: leave the 055-03 guard nothing to flag. This plan is a precondition for the guard extension — the guard scans exactly these three core roots, so any un-swept token RED-flags it.
Output: swept core prose/fixtures + updated instance docs.

**Plane note:** all files are `harness/`, `tools/`, `libs/`, or `examples/` — NONE are constitution plane. No `GOLDEN_APPROVE_HUMAN` token required. (`CLAUDE.md` root docs are NOT edited here — RESEARCH verified the four moved slugs never appear in `CLAUDE.md`/root `AGENTS.md`; nothing to drop.)
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.5-authored-surface/055-CONTEXT.md
@.planning/phases/05.5-authored-surface/055-RESEARCH.md
@harness/skills/data-contracts/SKILL.md
@harness/commands/new-normalization-rule.md
@harness/agents/orchestrator.md
@harness/agents/explorer.md
@libs/normalize-spec.md
@examples/log-parser/AGENTS.md
@examples/log-parser/README.md

<interfaces>
<!-- Exhaustive core-token sweep list (RESEARCH Code Examples). REWORD = prose edit; RENAME = fixture field. -->

AUTHORED SURFACE (harness/ + libs/):
  harness/skills/data-contracts/SKILL.md — Layout block, lines ~20-24:
    log-specs/ standard-log.spec.yaml ; normalization/ correction-rules.catalog.yaml ;
    reference-data/ equipment-master.yaml ; state/ equipment-progress.yaml
    -> abstract dirs: <domain-spec>/ ...spec.yaml ; <rules>/ ...catalog.yaml ;
       <reference>/ ...master.yaml ; <state>/ ...progress.yaml  (or the generic contracts/sample dirs).
    KEEP verbatim: contract-first rule, Draft-2020-12, check-jsonschema, RFC-8785 schema-hash drift gate.
  harness/commands/new-normalization-rule.md — lines ~22-23:
    `correction_rules` / correction-rules.catalog.yaml / correction-rules.schema.json in contracts/normalization/
    -> the generic instance's contract: contracts/sample/<rule-catalog>.yaml + .schema.json (abstract rule with
       id + scope/target_field). KEEP verbatim: the three-step mandated order (contract -> (input,expected) data
       case -> failing stub), the failing-stub forcing function, and `agent: python-engineer` (core, unchanged).
  harness/agents/orchestrator.md — lines 6, 18, 23:
    line 6 specialist list "(dotnet-engineer, python-engineer, ...)" -> drop dotnet-engineer; keep python-engineer +
      "instance-declared engineers per project.toml".
    line 18 "polyglot log-parser monorepo" -> "polyglot monorepo" (cleanliness; log-parser is NOT a guard token).
    line 23 "dotnet-engineer — parser/converter changes on the .NET 10 side (`dotnet *`)" -> remove this specialist
      bullet OR reword to a generic "instance language engineer" line naming no moved asset.
  harness/agents/explorer.md — line 20:
    "more expensive specialist (dotnet-engineer, python-engineer)" -> "(python-engineer, or an instance-declared engineer)".
  libs/normalize-spec.md — line 7:
    "the .NET core (`libs/dotnet/Normalize`) are each a thin implementation" -> "a language-side twin ... is a thin
    implementation" (language-neutral; drop libs/dotnet + .NET-core naming).

CORE COMMENTS + FIXTURES (tools/):
  tools/memory_regen/tests/test_agents_md.py — comments lines 22, 43 naming `libs/dotnet` -> "the instance's
    language-side package" / "the instance's language package" (comments only; assertions unchanged).
  tools/contract_drift/drift.py — line 10 docstring "correction-rules.catalog.yaml" -> "the instance's change-policy
    catalog" (comment/docstring only; classification logic unchanged).
  tools/contract_drift/tests/test_classify.py — RENAME the arbitrary fixture field `equipment_id` -> a neutral name
    (e.g. `record_id`) at lines 23,26,58,65 (and the paired `equip_id` at 65 -> `rec_id`); reword the line-3 comment
    naming correction-rules.catalog.yaml -> "the change-policy catalog". Fields are arbitrary JSON-schema property
    names; the test asserts add/remove/RENAME *classification*, not the specific name (RESEARCH A3).
  tools/docs_sync/tests/test_docs_sync_determinism.py — comments lines 23, 129 naming standard-log/equipment/
    correction-rules -> neutral wording ("the relocated domain schemas" / "a schema that stays in the core tree").
    Assertions + EXPECTED_PAGES unchanged.

DELIBERATELY NOT SWEPT (legitimately general, per RESEARCH — leaving them is correct, they are NOT guard tokens):
  bare dotnet/.NET, parser, converter, normalize/normalization, log-parser/logparser, log-spec/log-specs.

INSTANCE DOCS (examples/ — record the newly-owned assets):
  examples/log-parser/AGENTS.md "Instance layout" block -> add `skills/` (normalization-catalog, pipeline-patterns,
    dotnet-conventions) and `agents/` (dotnet-engineer) to the tree.
  examples/log-parser/README.md -> note the instance now owns its domain skills + the .NET persona (moved in GEN-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Genericize the surviving core authored surface (skills, command, personas, normalize-spec)</name>
  <files>harness/skills/data-contracts/SKILL.md, harness/commands/new-normalization-rule.md, harness/agents/orchestrator.md, harness/agents/explorer.md, libs/normalize-spec.md</files>
  <read_first>
    - harness/skills/data-contracts/SKILL.md — the `## Layout` block (lines ~20-24) with the semiconductor dir names; the contract-first + drift-gate prose that MUST stay.
    - harness/commands/new-normalization-rule.md — lines 21-45: the three-step mandated order + failing-stub forcing function (KEEP verbatim), and lines 22-23 with the `correction-rules` example anchors to reword; `agent: python-engineer` stays.
    - harness/agents/orchestrator.md (lines 6, 18, 23) + harness/agents/explorer.md (line 20) — the `dotnet-engineer` specialist naming to drop.
    - libs/normalize-spec.md (line 7) — the `libs/dotnet/Normalize` reference to make language-neutral.
    - 055-RESEARCH.md Code Examples ("new-normalization-rule — what to reword", "data-contracts — semiconductor lines to genericize", Open Question 2) + the Interfaces block above; confirm `contracts/sample/greeting.schema.json` exists as the generic default (A2).
  </read_first>
  <behavior>
    - `data-contracts/SKILL.md` `## Layout` names abstract/generic dirs (`<domain-spec>/`, `<rules>/`, `<reference>/`, `<state>/`, or `contracts/sample`), zero semiconductor filenames; the contract-first rule, Draft-2020-12 shape, `check-jsonschema`, and RFC-8785 schema-hash drift-gate prose are byte-identical to before.
    - `new-normalization-rule.md` reworded to the generic instance's rule catalog; the three mandated steps and the failing-stub forcing function are byte-identical; `agent: python-engineer` unchanged.
    - `orchestrator.md` and `explorer.md` name no moved asset (`dotnet-engineer` absent); they still name `python-engineer` (core) and refer to instance-declared engineers via `project.toml`.
    - `libs/normalize-spec.md` names no `libs/dotnet`; the §4.3-4.6 rule content is intact.
    - No guard token (`dotnet-engineer`, `dotnet-conventions`, `normalization-catalog`, `pipeline-patterns`, `libs/dotnet`, `equipment`, `standard-log`, `correction-rules`) remains in any of these five files.
  </behavior>
  <action>Reword each file per the Interfaces "AUTHORED SURFACE" block. In `data-contracts/SKILL.md`, rewrite ONLY the `## Layout` dir/filename lines to abstract placeholders (or the generic `contracts/sample` dirs) — do not touch the methodology paragraphs. In `new-normalization-rule.md`, swap the `correction-rules.catalog.yaml`/`.schema.json`/`correction_rules` example anchors for the generic instance's rule catalog (`contracts/sample/<rule-catalog>.yaml` + `.schema.json`, an abstract rule with `id` + `scope`/`target_field`); keep the three-step order block and failing-stub language verbatim. In `orchestrator.md`, drop `dotnet-engineer` from the line-6 specialist list (keep `python-engineer` + a generic "instance-declared engineers per project.toml" phrase), change "polyglot log-parser monorepo" (line 18) to "polyglot monorepo", and remove/reword the line-23 `dotnet-engineer` specialist bullet to a generic instance-language-engineer line naming no moved asset. In `explorer.md` line 20, reword the specialist parenthetical to `(python-engineer, or an instance-declared engineer)`. In `libs/normalize-spec.md` line 7, reword `the .NET core (libs/dotnet/Normalize)` to "a language-side twin ... is a thin implementation of these rules". Change no logic, no order, no gate prose.</action>
  <verify>
    <automated>uv run pytest tools/harness_lint -x -q && ! git grep -nE 'dotnet-engineer|dotnet-conventions|normalization-catalog|pipeline-patterns|libs/dotnet|equipment|standard-log|correction-rules' -- harness/skills/data-contracts harness/commands/new-normalization-rule.md harness/agents/orchestrator.md harness/agents/explorer.md libs/normalize-spec.md</automated>
  </verify>
  <acceptance_criteria>The five authored files contain zero guard tokens (git grep empty); `data-contracts` keeps its contract-first/drift-gate prose and `new-normalization-rule` keeps its three-step order + failing-stub forcing function verbatim; `agent: python-engineer` unchanged; harness_lint structural tests still green.</acceptance_criteria>
  <done>The surviving core authored surface is domain-neutral — methodology and ordering intact, semiconductor and moved-asset vocabulary gone.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the core comment/fixture guard tokens in tools/ (memory_regen, contract_drift, docs_sync)</name>
  <files>tools/memory_regen/tests/test_agents_md.py, tools/contract_drift/drift.py, tools/contract_drift/tests/test_classify.py, tools/docs_sync/tests/test_docs_sync_determinism.py</files>
  <read_first>
    - tools/contract_drift/tests/test_classify.py — lines 23,26,58,65: the `equipment_id`/`equip_id` fixture fields (arbitrary JSON-schema property names) + the line-3 comment naming the change-policy catalog. The test asserts add/remove/RENAME classification, NOT the field name (RESEARCH A3).
    - tools/contract_drift/drift.py — line 10 docstring naming `correction-rules.catalog.yaml`.
    - tools/memory_regen/tests/test_agents_md.py — comment lines 22, 43 naming `libs/dotnet`.
    - tools/docs_sync/tests/test_docs_sync_determinism.py — comment lines 23, 129 naming `standard-log`/`equipment`/`correction-rules` (EXPECTED_PAGES + assertions must stay untouched).
    - 055-RESEARCH.md Code Examples "Core token inventory" (the per-token classification) + Pitfall 2 (do NOT flag/rename the general terms).
  </read_first>
  <behavior>
    - `test_classify.py` uses a neutral fixture field (e.g. `record_id`, with the rename-case counterpart `rec_id`) in place of `equipment_id`/`equip_id`; the add/remove/rename classification assertions pass unchanged.
    - Comments in `drift.py`, `test_agents_md.py`, `docs_sync` test no longer name `libs/dotnet`/`correction-rules`/`standard-log`/`equipment`; wording is neutral; no assertion, `EXPECTED_PAGES`, or logic changes.
    - After this task, `git grep` for the guard tokens across `tools/` (excluding the guard file itself, which is edited in 055-03) returns nothing.
  </behavior>
  <action>Rename the arbitrary fixture field in `tools/contract_drift/tests/test_classify.py`: `equipment_id` to `record_id` at lines 23, 26, 58, 65 and the paired renamed field `equip_id` to `rec_id` at BOTH occurrences — line 64 (the `required` list) AND line 65 (the property key), renamed together so the rename fixture stays coherent (checker W2); reword the line-3 comment's `correction-rules.catalog.yaml` to "the change-policy catalog". In `tools/contract_drift/drift.py`, reword the line-10 docstring `correction-rules.catalog.yaml` to "the instance's change-policy catalog". In `tools/memory_regen/tests/test_agents_md.py`, reword the `libs/dotnet` comments (lines 22, 43) to "the instance's language-side package". In `tools/docs_sync/tests/test_docs_sync_determinism.py`, reword the `standard-log`/`equipment`/`correction-rules` comment mentions (lines 23, 129) to neutral wording ("the relocated domain schemas" / "a schema that stays in the core tree"). Touch NO assertions, NO `EXPECTED_PAGES`, NO classification logic — comments and arbitrary fixture names only. Do NOT rename any general term (bare `dotnet`, `parser`, `converter`, `normalize`, `log-parser`).</action>
  <verify>
    <automated>uv run pytest tools/contract_drift tools/memory_regen tools/docs_sync -x -q && ! git grep -nE 'libs/dotnet|equipment|standard-log|correction-rules|normalization-catalog|pipeline-patterns|dotnet-engineer|dotnet-conventions' -- tools ':!tools/harness_lint/tests/test_core_no_example_dep.py'</automated>
  </verify>
  <acceptance_criteria>`git grep` for the guard tokens over `tools/` (excluding the guard file) is empty; the contract_drift/memory_regen/docs_sync suites pass unchanged (no assertion or EXPECTED_PAGES edits); the `test_classify` rename-case still classifies as a rename.</acceptance_criteria>
  <done>All core `tools/` comment/fixture occurrences of the guard tokens are removed — the guard (055-03) will read 0 across `tools/`.</done>
</task>

<task type="auto">
  <name>Task 3: Update the example's own docs to record the moved skills + persona</name>
  <files>examples/log-parser/AGENTS.md, examples/log-parser/README.md</files>
  <read_first>
    - examples/log-parser/AGENTS.md — the "Instance layout" code block (currently lists contracts/, components/, libs/dotnet/, golden/, tests/).
    - examples/log-parser/README.md — the "What it is" / layout prose.
    - 055-CONTEXT.md D-05 (docs reflect new locations) + RESEARCH Pattern 1 (root docs never named the moved slugs — nothing to drop there; the update lives in the instance docs).
  </read_first>
  <behavior>
    - `examples/log-parser/AGENTS.md` "Instance layout" tree includes `skills/` (with `normalization-catalog`, `pipeline-patterns`, `dotnet-conventions`) and `agents/` (with `dotnet-engineer`), each noted as instance-owned authored assets moved in GEN-05.
    - `examples/log-parser/README.md` notes the instance owns its domain skills + the .NET persona (relocated from core in Phase 5.5).
    - Both files keep the existing one-directional-dependency invariant statement (instance depends on core; core never on instance).
  </behavior>
  <action>In `examples/log-parser/AGENTS.md`, extend the "Instance layout" code block to add two entries: `skills/` — the instance's domain + .NET authored skills (`normalization-catalog`, `pipeline-patterns`, `dotnet-conventions`), and `agents/` — the instance's `dotnet-engineer` .NET persona; add a short line that these authored assets moved here in GEN-05 (Phase 5.5) because they are domain/instance-language, not core. In `examples/log-parser/README.md`, add a sentence to the layout/"how it uses the core" area noting the instance now owns its domain skills and its .NET language persona (relocated from the harness core in the authored-surface de-specialization). Do not restate core-owned assets as instance-owned; keep the dependency-direction invariant text.</action>
  <verify>
    <automated>grep -q 'normalization-catalog' examples/log-parser/AGENTS.md && grep -q 'dotnet-engineer' examples/log-parser/AGENTS.md && grep -qi 'persona\|skill' examples/log-parser/README.md && uv run pytest tools/harness_lint -x -q</automated>
  </verify>
  <acceptance_criteria>The example's `AGENTS.md` layout names the moved `skills/` (incl. `normalization-catalog`) and `agents/dotnet-engineer`; the README notes the instance owns its domain skills + .NET persona; harness_lint still green (instance docs are not scanned by the anti-sprawl pins).</acceptance_criteria>
  <done>The instance's own docs record the authored assets it now owns, completing D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core prose -> domain vocabulary | Reworded prose is the surface the 055-03 guard enforces to 0; a re-introduced token would RED the guard. |
| fixture field name -> classification logic | Renaming arbitrary JSON-schema property names must NOT change drift classification (asserted by the unchanged test bodies). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-055-04 | Tampering | a "genericization" that silently changes methodology/order/gate logic | mitigate | Only prose/dir-name/fixture-name edits; the contract-first order + drift-gate prose kept verbatim; the full non-example suite (which encodes the logic) stays green in verify. |
| T-055-05 | Spoofing | over-broad rename touches a general term and breaks a real assertion | mitigate | Rename limited to arbitrary fixture fields + comments; guard tokens are proper-noun/rare-vocab; general terms explicitly left alone (RESEARCH Pitfall 2); suites run in verify. |
| T-055-SC | Tampering | dependency supply chain | accept | Zero new packages — markdown/comment/fixture edits only; `uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest` full non-example suite green (no logic/order/assertion changed).
- `git grep` for the guard tokens across `tools/ harness/ libs/` (excluding the guard file) returns nothing — the 055-03 guard will read 0.
- `data-contracts` + `new-normalization-rule` retain their contract-first/order/drift-gate prose verbatim.
- `examples/log-parser/{AGENTS.md,README.md}` record the moved skills + `dotnet-engineer` persona.
</verification>

<success_criteria>
Success-criterion demo (2) from 055-CONTEXT: `data-contracts` and `new-normalization-rule` bodies carry zero semiconductor vocabulary; and — as the precondition for demo (3) — every core (`tools/harness/libs`) guard-token occurrence is removed or reworded so the extended GEN-04 guard (055-03) reads 0.
</success_criteria>

<output>
Create `.planning/phases/05.5-authored-surface/055-02-SUMMARY.md` when done.
</output>
