---
phase: 05.5-authored-surface
plan: 03
type: execute
wave: 3
depends_on: [055-01, 055-02]
files_modified:
  - tools/harness_lint/tests/test_core_no_example_dep.py
autonomous: true
requirements: [GEN-05]

must_haves:
  truths:
    - "The GEN-04 guard scans core (tools/harness/libs) for a narrow set of DOMAIN/LANGUAGE prose tokens (moved-asset proper nouns + libs/dotnet + rare semiconductor vocab) in addition to the SCOPE-A code-dependency tokens."
    - "The guard reads exactly 0 prose-token hits on the post-move + post-sweep tree — the non-example suite is green."
    - "A per-token negative control proves each new prose token is live (the scan cannot silently no-op) — including wafer and 설비 which are 0-occurrence anchors."
    - "The one sanctioned exemption is generalized: the harness/project.toml instance-pointer line (root AND persona) is not flagged even though dotnet.persona names examples/log-parser/agents/dotnet-engineer.md."
    - "The narrow token set deliberately EXCLUDES bare dotnet/.NET/parser/converter/normalize/log-parser to avoid over-flagging legitimately-general core text."
  artifacts:
    - path: "tools/harness_lint/tests/test_core_no_example_dep.py"
      provides: "GEN-04 guard extended to prose: _PROSE_TOKENS + per-token negative controls + generalized instance-pointer exemption + persona-pointer positive exemption test"
      contains: "_PROSE_TOKENS"
  key_links:
    - from: "tools/harness_lint/tests/test_core_no_example_dep.py"
      to: "tracked core files (tools/ harness/ libs/)"
      via: "_scan_lines flags any line containing a _PROSE_TOKENS entry, minus the project.toml pointer-line exemption"
      pattern: "_PROSE_TOKENS"
    - from: "the generalized _is_instance_root_line"
      to: "harness/project.toml root = and persona = lines"
      via: "exempt the sanctioned instance-data pointer lines (ADR-0002 (c))"
      pattern: "persona\\s*="
---

## Phase Goal

**As a** developer or coding agent maintaining this template, **I want to** extend the GEN-04 core→example guard from code-dependency tokens to a narrow set of DOMAIN/LANGUAGE prose tokens, **so that** the core planes are provably free of moved-asset and semiconductor vocabulary and can never silently regrow it.

<objective>
Extend the Phase-5 guard `tools/harness_lint/tests/test_core_no_example_dep.py` from SCOPE-A (code deps) to prose purity (D-04), using the narrow, over-reach-proof token set RESEARCH specified (§Pattern 3 "Guard Shape") — proper nouns of the moved assets + `libs/dotnet` + rare semiconductor vocab — plus one generalized exemption and a per-token negative control.

This plan is LAST in the phase: it depends on 055-01 (the move — so the moved-asset tokens vanish from core) AND 055-02 (the sweep — so every remaining core token is reworded). Running it before the sweep would RED the guard on un-swept refs (RESEARCH §Summary).

Concretely: add `_PROSE_TOKENS`; make `_scan_lines` flag them alongside the existing path/import tokens; generalize `_is_instance_root_line` from the `root =` line to also skip `persona =` lines in `harness/project.toml` (the one sanctioned place a core-plane file names an instance asset — ADR-0002 (c)); add one negative-control test per new token (asserting the scan flags a synthetic core line); add one positive-exemption test asserting the `persona = "examples/..."` pointer line is NOT flagged. Zero new packages.

**Plane note:** the only file is `tools/harness_lint/tests/test_core_no_example_dep.py` — not constitution plane. No `GOLDEN_APPROVE_HUMAN` token required.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.5-authored-surface/055-CONTEXT.md
@.planning/phases/05.5-authored-surface/055-RESEARCH.md
@tools/harness_lint/tests/test_core_no_example_dep.py
@harness/project.toml

<interfaces>
<!-- The extension shape (RESEARCH Pattern 3). Base = the existing Phase-5 guard, kept intact. -->

ADD, alongside the existing `_PATH_TOKENS = ("examples/", "components/toy-converter")`:
  # Prose domain/language tokens (GEN-05): moved-asset proper nouns + rare domain vocab.
  # NARROW on purpose — NO bare dotnet/.NET/parser/converter/normalize/log-parser (those are general, stay).
  _PROSE_TOKENS = (
      "dotnet-engineer", "dotnet-conventions", "normalization-catalog", "pipeline-patterns",
      "libs/dotnet", "equipment", "standard-log", "correction-rules", "wafer", "설비",
  )

SCAN: extend `_scan_lines` so a line hits if it contains any `_PATH_TOKENS` OR `_PROSE_TOKENS` entry,
      OR matches `_IMPORT_EXAMPLES` — minus the exempted instance-pointer lines. Scan roots unchanged
      (`git ls-files tools harness libs`), self-exclusion unchanged.

EXEMPTION (generalize the existing `_is_instance_root_line`):
  currently: rel_path == "harness/project.toml" AND line matches `^\s*root\s*=`
  ADD: also exempt `^\s*persona\s*=` lines in harness/project.toml.
  Rationale: project.toml is the ONE sanctioned instance-data file (ADR-0002 (c)); its dotnet.persona line
  legitimately contains BOTH `examples/` (SCOPE-A token) AND `dotnet-engineer` (new prose token). Exempting the
  whole pointer line covers both. (Rename the helper if clearer, e.g. `_is_instance_pointer_line`.)

NEGATIVE CONTROLS (one per NEW token — mirror the existing 3 negative-control tests):
  for each t in _PROSE_TOKENS: assert `_scan_lines("tools/fake_core_module.py", f"# see the {t} thing")` is non-empty.
  (wafer/설비 are 0-occurrence anchors — guaranteed-live proof.)

POSITIVE EXEMPTION TEST (mirror test_instance_root_pointer_is_exempt):
  test_instance_pointer_persona_is_exempt:
    assert _scan_lines("harness/project.toml", 'persona = "examples/log-parser/agents/dotnet-engineer.md"') == []

KEEP intact: the existing `test_core_has_no_example_dependency` (now also covers prose since _scan_lines is extended),
  the 3 SCOPE-A negative controls, `test_instance_root_pointer_is_exempt`, self-exclusion, docstring SCOPE note
  (update the docstring: SCOPE now includes the GEN-05 prose tier; the "deferred to GEN-05" caveat is removed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the GEN-04 guard to prose tokens + generalize the instance-pointer exemption</name>
  <files>tools/harness_lint/tests/test_core_no_example_dep.py</files>
  <read_first>
    - tools/harness_lint/tests/test_core_no_example_dep.py — the WHOLE existing guard: `_PATH_TOKENS`, `_IMPORT_EXAMPLES`, `_tracked_core_files` (git ls-files + self-exclusion), `_is_instance_root_line`, `_scan_lines`, `test_core_has_no_example_dependency`, the 3 negative-control tests, `test_instance_root_pointer_is_exempt`. The extension is additive — do not remove the SCOPE-A behavior.
    - harness/project.toml — confirm the `persona =` lines (dotnet block now points at `examples/log-parser/agents/dotnet-engineer.md` after 055-01) so the exemption regex matches the real line.
    - 055-RESEARCH.md Pattern 3 "Guard Shape" (token set, exemption, negative controls), Pitfall 2 (excluded general terms), Pitfall 3 (the project.toml persona line self-leak), and Code Examples "Core token inventory" (proof every real occurrence is gone after 055-01 + 055-02).
  </read_first>
  <behavior>
    - `_scan_lines` flags any tracked-core line containing a `_PROSE_TOKENS` entry (in addition to the existing path/import tokens), except the exempted `harness/project.toml` `root =` / `persona =` pointer lines.
    - On the real post-move + post-sweep tree, `test_core_has_no_example_dependency` passes — zero hits (proof the 055-02 sweep and 055-01 move removed every real occurrence).
    - One negative-control test per new token asserts a synthetic core line naming that token IS flagged (the scan cannot silently no-op); `wafer`/`설비` anchor the proof (0 real occurrences).
    - `test_instance_pointer_persona_is_exempt` asserts the real `persona = "examples/log-parser/agents/dotnet-engineer.md"` line is NOT flagged despite containing both an `examples/` and a `dotnet-engineer` token.
    - No bare `dotnet`/`.NET`/`parser`/`converter`/`normalize`/`log-parser` is in the token set — the guard does not flag `argparse` parsers, `golden_runner` "converter", or `logparser-*` package names.
  </behavior>
  <action>Edit `tools/harness_lint/tests/test_core_no_example_dep.py` additively. Add the `_PROSE_TOKENS` tuple exactly as in the Interfaces block. Extend `_scan_lines` so the hit condition also checks `any(tok in line for tok in _PROSE_TOKENS)`. Generalize `_is_instance_root_line` (optionally rename to `_is_instance_pointer_line`) so it returns True for `harness/project.toml` lines matching `^\s*root\s*=` OR `^\s*persona\s*=`; update its one call site in `_scan_lines`. Update the module docstring: the guard now enforces the GEN-05 prose tier (moved-asset proper nouns + `libs/dotnet` + rare domain vocab) in addition to SCOPE-A code deps; remove the "deferred to GEN-05" caveat and note the deliberately-excluded general terms. Add a parametrized negative-control test iterating `_PROSE_TOKENS`, each asserting `_scan_lines("tools/fake_core_module.py", f"# ref to {token}")` is non-empty. Add `test_instance_pointer_persona_is_exempt` asserting `_scan_lines(_INSTANCE_ROOT_FILE, 'persona = "examples/log-parser/agents/dotnet-engineer.md"')` is empty. Keep every existing test and the SCOPE-A tokens intact. Run the FULL non-example suite — the guard must be green on the real tree (dependency on 055-01 + 055-02 completing first).</action>
  <verify>
    <automated>uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>`test_core_no_example_dep.py` defines `_PROSE_TOKENS`, `_scan_lines` flags them, `_is_instance_root_line`/`_is_instance_pointer_line` exempts both `root =` and `persona =` in `harness/project.toml`; a negative-control passes per new token; `test_instance_pointer_persona_is_exempt` passes; `test_core_has_no_example_dependency` passes on the real tree (0 prose hits); the FULL non-example suite (`uv run pytest`) is green.</acceptance_criteria>
  <done>The GEN-04 guard is prose-aware: core planes carry 0 domain/moved-asset prose tokens, proven live by per-token negative controls, with the sole sanctioned project.toml instance-pointer exemption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core prose -> instance/domain vocabulary | The extended guard is the tamper-evidence that core planes never regrow a moved-asset or semiconductor prose token. |
| instance-data pointer -> guard | `harness/project.toml` `persona =`/`root =` is the ONE sanctioned line naming an instance asset; the generalized exemption keeps it from self-tripping the guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-055-06 | Tampering | a core file later regrows a domain/moved-asset prose token | mitigate | `_PROSE_TOKENS` scan on every run flags it; the full suite RED-flags on reintroduction. |
| T-055-07 | Tampering | the prose scan silently no-ops (dead token, bad membership check) | mitigate | One negative-control per token asserts a synthetic hit is flagged; `wafer`/`설비` (0 real occurrences) are guaranteed-live anchors. |
| T-055-08 | Denial of Service | over-broad token set flags legitimately-general core text and RED-flags the suite | mitigate | Narrow proper-noun/rare-vocab set; bare `dotnet`/`parser`/`converter`/`normalize`/`log-parser` deliberately excluded (RESEARCH Pitfall 2); full suite green in verify proves no over-reach. |
| T-055-09 | Elevation of Privilege | the project.toml pointer line self-leaks (examples/ + dotnet-engineer on one line) | mitigate | `_is_instance_pointer_line` generalized to `root =` + `persona =`; `test_instance_pointer_persona_is_exempt` proves the exemption. |
| T-055-SC | Tampering | dependency supply chain | accept | Zero new packages — one test file extended; `uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest tools/harness_lint/tests/test_core_no_example_dep.py -x -q` green: prose scan live (per-token negative controls), real tree 0 hits, persona-pointer exemption positive test passes.
- `uv run pytest` full non-example suite green — no over-reach RED-flag.
- The token set excludes bare `dotnet`/`.NET`/`parser`/`converter`/`normalize`/`log-parser` (inspect `_PROSE_TOKENS`).
- The guard depends on 055-01 (move) + 055-02 (sweep) having landed — it reads 0 only on that tree.
</verification>

<success_criteria>
Success-criterion demos (3) and (4) from 055-CONTEXT: the extended GEN-04 guard enforces 0 core-prose domain tokens with a per-token negative control (demo 3), and the non-example pytest suite is green with the harness_lint expected-lists updated (demo 4).
</success_criteria>

<output>
Create `.planning/phases/05.5-authored-surface/055-03-SUMMARY.md` when done.
</output>
