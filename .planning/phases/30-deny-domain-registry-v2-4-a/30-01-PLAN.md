---
phase: 30-deny-domain-registry-v2-4-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/harness/security/deny-domains.schema.json
  - contracts/harness/security/deny-domains.json
  - tools/contract_hash/hash.py
  - contracts/.hashes/manifest.json
  - docs/reference/deny-domains.md
  - .memory/derived/contracts-index.md
  - tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
autonomous: false
requirements: [SEAL-01]

must_haves:
  truths:
    - "`contracts/harness/security/deny-domains.json` declares EXACTLY THREE enforcing domains — `constitution`, `secret`, `review-ledger` — each with `id`, `owner_module`, `owner_constant`, `globs`, `bypasses`, `covered_tool_surfaces`, `uncovered_tool_surfaces`, `runtime_adapters`, `enforcement_sites`, `declared_by`"
    - "The three domains are NOT merged and CANNOT be: the schema forbids two records sharing an `owner_constant` and forbids duplicate `id`s (ADR-0010:163-175; REQUIREMENTS.md:105 lists merging as out of scope)"
    - "`bypasses` is `[\"GOLDEN_APPROVE_HUMAN\", \"HARNESS_DEV_BYPASS\"]` for `constitution` and the EMPTY ARRAY for BOTH `secret` and `review-ledger` — an empty array, never an omitted key, so 'no bypass' is an assertion rather than an absence (D-10)"
    - "Every domain's `uncovered_tool_surfaces` contains `\"Bash\"` — the SEAL-01 field that makes the gap visible as DATA (D-08). Phase 31 reads this field; phase 32 shrinks it"
    - "`non_enforcing_glob_sets` declares `tools/adoption_scan/scan.py:54`'s wider same-named `SECRET_PATH_GLOBS` and `tools/docs_guard/exclusions.py`'s `DERIVED_GLOBS` with an explicit `reason`, and a top-level note records that `commit_gate` enumerates no paths (D-06/D-07)"
    - "The declared `globs` are RESTATED, not imported — no hook imports this contract, and the instance carries a comment-bearing `_note` saying why (D-02). A registry the hooks read would make the phase-30 drift test a tautology"
    - "`tools/contract_hash/hash.py` `DATA_CONTRACT_PATHS` gains `harness/security/deny-domains.json`, so the RFC 8785 drift gate covers the instance the way it covers `gate-registry.json`"
    - "Schema + instance + `hash --write` + `docs_sync` + `contracts_index` + BOTH syrupy snapshots + the `hash.py` edit land in ONE atomic, human-ratified commit — a half-landed state reds `drift` (ci.yml:133) or `stale-derived` (ci.yml:227)"
    - "No model identifier, no timestamp, no reviewer/human-identity field anywhere in either contract file"
  artifacts:
    - path: "contracts/harness/security/deny-domains.schema.json"
      provides: "Draft 2020-12 SHAPE contract for the deny-domain registry — enums for bypass token, tool surface, and runtime; uniqueness of `id` and `owner_constant`"
      contains: "https://json-schema.org/draft/2020-12/schema"
    - path: "contracts/harness/security/deny-domains.json"
      provides: "the declaration itself — three enforcing domains plus the non-enforcing glob sets"
      contains: "review-ledger"
    - path: "tools/contract_hash/hash.py"
      provides: "the instance registered as a ratified data contract so drift covers it"
      contains: "harness/security/deny-domains.json"
    - path: "contracts/.hashes/manifest.json"
      provides: "rebaselined JCS SHA-256 entries for BOTH new files, written ONLY by `tools.contract_hash.hash --write`"
      contains: "deny-domains"
    - path: "docs/reference/deny-domains.md"
      provides: "the docs_sync-generated reference page (DERIVED header, never hand-edited)"
      contains: "DERIVED"
  key_links:
    - from: "contracts/harness/security/deny-domains.json"
      to: "contracts/harness/security/deny-domains.schema.json"
      via: "the repo's FIRST <name>.schema.json + sibling <name>.json pair — ci.yml:108-127 validates it, replacing that job's SKIP line"
      pattern: "deny-domains"
    - from: "contracts/harness/security/deny-domains.json"
      to: "tools/contract_hash/hash.py::DATA_CONTRACT_PATHS"
      via: "explicit data-contract registration, the gate-registry.json idiom (hash.py:29-32)"
      pattern: "deny-domains.json"
    - from: "contracts/harness/security/deny-domains.json"
      to: "tools/hooks/{contract_guard,secret_scan,ledger_guard}.py"
      via: "DECLARATION-of, never import-from — the drift test in plan 30-03 is the only link"
      pattern: "owner_constant"
---

<objective>
Land the ONE constitution-plane act Phase 30 pays for: the deny-domain registry as a schema +
instance pair, together with every derived artifact three CI gates observe, in a single atomic,
human-ratified commit.

Purpose: SEAL-01 requires the harness to declare each path-deny domain once, with its bypass
semantics and — the load-bearing part — the tool surfaces it covers and does not cover. This plan
authors the declaration. It changes no enforcement: the hooks are untouched (D-14), which is what
makes plan 30-03's comparison meaningful rather than circular.

Output: two contract files, one `DATA_CONTRACT_PATHS` line, the rebaselined hash manifest, the
generated reference page, the regenerated contracts-index, and both syrupy snapshots — staged as one
commit and presented for blocking human ratification.
</objective>

<execution_context>
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-deny-domain-registry-v2-4-a/30-CONTEXT.md
@.planning/phases/30-deny-domain-registry-v2-4-a/30-RESEARCH.md
@.planning/milestones/v2.3-phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-01-PLAN.md
@contracts/harness/task-control/gate-registry.json
@contracts/harness/adoption/manifest.schema.json
@tools/contract_hash/hash.py
@docs/adr/0010-human-docs-review-obligation-model.md

<interfaces>
<!-- VERIFIED against source in this session. No codebase exploration needed. -->

`tools/contract_hash/hash.py:24-32` — the registration point and the auto-globbed half:

    CONTRACTS_DIR = REPO_ROOT / "contracts"
    MANIFEST_PATH = CONTRACTS_DIR / ".hashes" / "manifest.json"
    SCHEMA_GLOB   = "**/*.schema.json"
    DATA_CONTRACT_PATHS = (
        Path("harness/task-control/transitions.json"),
        Path("harness/task-control/gate-registry.json"),
    )

The `.schema.json` half is picked up automatically. The `.json` INSTANCE is not — it must be added
to `DATA_CONTRACT_PATHS` or the drift gate never sees a change to the declaration.

VERIFIED NON-BREAKAGE: `tools/contract_hash/tests/test_hash.py:93-101` asserts the data-contract set
against a TMP contracts tree, and `hash.py:57` only counts entries that exist
(`if (root / rel).is_file()`). A third tuple entry pointing at a path that tmp tree never creates
leaves that assertion green. Do NOT edit that test.

`tools/docs_sync/generate.py:30-40` — one reference page per `*.schema.json`:

    REFERENCE_DIR  = REPO_ROOT / "docs" / "reference"
    _SCHEMA_SUFFIX = ".schema.json"
    DERIVED_HEADER = "DERIVED — do not hand-edit — generated from contracts/ by tools.docs_sync"

So the schema produces `docs/reference/deny-domains.md`. The `.json` instance produces no page.

`.github/workflows/ci.yml:108-127` — `contract-check` pairs `<base>.schema.json` with
`<base>.{yaml,yml,json}`. The repo has ZERO such pairs today, so the job prints its VISIBLE SKIP
(`ci.yml:121-123`). This commit creates the first pair — expect that SKIP line to disappear and a
real `check-jsonschema` line to appear. That is the intended effect, not a regression.

`tools/memory_regen/contracts_index.py:34-45` — `contracts/harness/...` has no `KIND` entry, so both
new rows render kind `other`, owner `TBD`. Correct — do NOT add a `KIND` entry to prettify it.

THE VALUES TO DECLARE — read from source this session, restate them EXACTLY:

    contract_guard.py:53   CONSTITUTION_GLOBS  = ["contracts/**", "docs/adr/**", "golden/**", "docs/glossary.md"]
    secret_scan.py:37      SECRET_PATH_GLOBS   = ["*.env", "**/*.env"]
    ledger_guard.py:48     REVIEW_LEDGER_GLOBS = ["docs/.docs-review-ledger.toml"]

    bypasses:  constitution -> GOLDEN_APPROVE_HUMAN (contract_guard.py:114) + HARNESS_DEV_BYPASS (:115)
               secret       -> [] (secret_scan.py:81-91 reads no env at all)
               review-ledger-> [] (ledger_guard.py:26-30, ADR-0010:163-166 — no token, by design)

    Claude matchers (tools/harness_emit/merge.py — the SOURCE, not .claude/settings.json):
               contract_guard :126-133  "Write|Edit"
               secret_scan    :135-143  "Read|Write|Edit"
               ledger_guard   :144-158  "Write|Edit"

    opencode adapter tool guards (each at :70):
               .opencode/plugin/contract-guard.ts  -> ["write", "edit"]
               .opencode/plugin/secret-scan.ts     -> ["read", "write", "edit"]
               .opencode/plugin/ledger-guard.ts    -> ["write", "edit"]

    enforcement_sites beyond the hook:
               constitution  -> tools/adoption_apply/apply.py:112 (ConstitutionRefusal)
               review-ledger -> tools/adoption_apply/apply.py:227 (ReviewLedgerRefusal, layer 2)
                                tools/docs_guard/ledger.py first_seen-unratified (layer 3, GREENNESS only)
               secret        -> (hook only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the deny-domain schema + instance, register the instance, regenerate every derived artifact</name>
  <files>
    contracts/harness/security/deny-domains.schema.json,
    contracts/harness/security/deny-domains.json,
    tools/contract_hash/hash.py,
    contracts/.hashes/manifest.json,
    docs/reference/deny-domains.md,
    .memory/derived/contracts-index.md,
    tools/memory_regen/tests/__snapshots__/test_contracts_index.ambr
  </files>
  <read_first>
    contracts/harness/adoption/manifest.schema.json (full — house style for a harness-plane schema),
    contracts/harness/task-control/gate-registry.json (the data-contract precedent),
    tools/hooks/{contract_guard,secret_scan,ledger_guard}.py (the values being declared — read them,
      do NOT copy them out of this plan),
    docs/adr/0010-human-docs-review-obligation-model.md:141-175,
    this plan's `<interfaces>` block
  </read_first>
  <action>
    **1a. The schema** — `contracts/harness/security/deny-domains.schema.json`, Draft 2020-12,
    `manifest.schema.json`'s style exactly: `$schema`, `$id` under the same host/path convention with
    `harness/security/`, `title`, `description`, `additionalProperties: false` at every object level,
    named `$defs` for the enums.

    Root object, `additionalProperties: false`, `required: ["version", "domains"]`:
      - `version`: const `"v1"`.
      - `domains`: array, `minItems: 1`, `uniqueItems: true`, items:
        - `required`: `["id","owner_module","owner_constant","globs","bypasses",
          "covered_tool_surfaces","uncovered_tool_surfaces","runtime_adapters","declared_by"]`,
          `additionalProperties: false`.
        - `id`: `$defs/domain_id` — `enum: ["constitution","secret","review-ledger"]`. A closed enum,
          deliberately: a NEW domain is an architectural act that needs a superseding ADR, not a
          registry row. Say that in the property `description`.
        - `owner_module` / `owner_constant`: strings, `minLength: 1`. Dotted module path and constant
          name, resolvable by `importlib` (plan 30-02 resolves them).
        - `globs`: array of strings, `minItems: 1`, `uniqueItems: true`.
        - `bypasses`: array, `uniqueItems: true`, `items.$ref` `$defs/bypass_token` —
          `enum: ["GOLDEN_APPROVE_HUMAN","HARNESS_DEV_BYPASS"]`. **`minItems` MUST NOT be set**: the
          empty array is the meaningful value for two of the three domains, and the key is REQUIRED
          so "no bypass" is asserted, never inferred from an omission. State that in the description.
        - `covered_tool_surfaces` / `uncovered_tool_surfaces`: arrays, `uniqueItems: true`,
          `items.$ref` `$defs/tool_surface` — `enum: ["Read","Write","Edit","Bash"]`. Both keys
          REQUIRED. The description states that `uncovered_tool_surfaces` is the SEAL-01 field
          recording what a domain does NOT gate, that it is not the complement of the covered set,
          and that shrinking it is phase 32's job.
        - `content_axis`: OPTIONAL object, `additionalProperties: false`, properties
          `kind` (string), `source` (string), `location` (string), `carried_debt` (string). Models a
          non-path deny axis (D-09).
        - `runtime_adapters`: array, `minItems: 1`, items `additionalProperties: false`,
          `required: ["runtime","path","surfaces"]`; `runtime` `$defs/runtime`
          — `enum: ["claude","opencode"]`; `path` string; `surfaces` array of strings.
        - `enforcement_sites`: OPTIONAL array of objects (`layer` int, `location` string,
          `covers` string) — the ADR-0010 layers beyond the hook.
        - `declared_by`: string — the ADR path that declares the domain's membership.
      - `non_enforcing_glob_sets`: array, items `required: ["location","constant","reason"]`,
        `additionalProperties: false`.
      - `_note`: string.

    Cross-record uniqueness of `id` and of `owner_constant` is NOT expressible in JSON Schema across
    array items beyond `uniqueItems` on whole objects — say so in the schema `description` and note
    that `tools.deny_domains.registry` (plan 30-02) enforces it, mirroring the D-16/A7 note in
    `contracts/harness/docs/doc-dependencies.schema.json`.

    FORBIDDEN anywhere in either file: any timestamp/date/`updated_at` property, any reviewer or
    human-identity property, any model identifier (in `$id`, `title`, or any `description`).

    **1b. The instance** — `contracts/harness/security/deny-domains.json`. Three `domains` records,
    values read from the hook modules (not from this plan). `uncovered_tool_surfaces` is `["Bash"]`
    for all three. `content_axis` on `constitution`
    (`{kind: "byte-hygiene", source: "tools.polyglot_lint.lint_bytes", location: "tools/hooks/contract_guard.py:94"}`)
    and on `secret`
    (`{kind: "regex-patterns", source: "hardcoded", location: "tools/hooks/secret_scan.py:44", carried_debt: "SEAL-04"}`);
    none on `review-ledger`. `non_enforcing_glob_sets` carries the two D-06 entries. `_note` states,
    in one paragraph: (i) that this file is a DECLARATION compared against the hooks, that the hooks
    remain the single source of truth, and that NO hook imports this file — because a registry the
    hooks read would make the drift gate a tautology (D-02); and (ii) that `tools/hooks/commit_gate.py`
    is deliberately absent because it enumerates no paths (D-07).

    **1c. Register the instance** — add `Path("harness/security/deny-domains.json")` to
    `DATA_CONTRACT_PATHS` at `tools/contract_hash/hash.py:29-32`, keeping the tuple's existing style.
    Do NOT edit `tools/contract_hash/tests/test_hash.py` — see `<interfaces>` for why it stays green.

    **1d. Run, in this STRICT order, inspecting each output before continuing:**

      1. `uv run python -m tools.contract_hash.hash --write`
         — confirm `git diff --stat contracts/.hashes/manifest.json` shows exactly TWO added entries
           (schema + instance) and NO existing entry's hash moved. If another moved, STOP.
      2. `uv run python -m tools.contract_drift.drift` — must exit 0.
      3. `uv run check-jsonschema --schemafile contracts/harness/security/deny-domains.schema.json contracts/harness/security/deny-domains.json`
         — must pass. This is the CI `contract-check` pairing, run locally first.
      4. `uv run python -m tools.docs_sync` — `docs/reference/deny-domains.md` appears; confirm it is
         a REAL new file whose first line is the docs_sync DERIVED header. Never hand-edit it.
      5. `uv run python -m tools.memory_regen.contracts_index` — two new rows, kind `other`, owner `TBD`.
      6. `uv run pytest tools/memory_regen/tests/test_contracts_index.py -q`; on a snapshot mismatch
         regenerate with `--snapshot-update` and re-run clean. Regenerate; never hand-edit `.ambr`.
      7. `uv run pytest tools/docs_sync tools/contract_hash tools/contract_drift -q` — green.

    Constitution-plane write path: writes under `contracts/**` are denied to a direct agent write by
    `contract_guard` unless a human-authorized `GOLDEN_APPROVE_HUMAN` is present. Land this through
    the SAME approved mechanism prior constitution commits used (27.1-03 / 28-01). Never fabricate the
    token; never use `HARNESS_DEV_BYPASS` to self-land. Task 2 is the ratification authority.

    Stage all seven files together. Do NOT commit — Task 2 gates the commit.
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift && uv run check-jsonschema --schemafile contracts/harness/security/deny-domains.schema.json contracts/harness/security/deny-domains.json && uv run pytest tools/memory_regen/tests/test_contracts_index.py tools/docs_sync tools/contract_hash -q</automated>
  </verify>
  <acceptance_criteria>
    All three commands above exit 0. `git status --porcelain` shows exactly the seven files in
    `files_modified` and nothing else — in particular no `tools/hooks/**` (D-14: this phase edits no
    hook) and no `tools/contract_hash/tests/**`. `git diff --cached contracts/.hashes/manifest.json`
    shows exactly two ADDED entries. `python -c "import json;d=json.load(open('contracts/harness/security/deny-domains.json'));ids=[x['id'] for x in d['domains']];assert ids==sorted(set(ids)) and len(ids)==3, ids;assert all('Bash' in x['uncovered_tool_surfaces'] for x in d['domains']);assert d['domains'][[i['id'] for i in d['domains']].index('review-ledger')]['bypasses']==[]"` exits 0.
    `grep -rniE 'claude|gpt|opus|sonnet|anthropic' contracts/harness/security/` returns nothing.
    `grep -rn "deny.domains" tools/hooks/` returns nothing (D-02: no hook reads the registry).
  </acceptance_criteria>
  <done>Both contract files are authored, the instance is registered as a data contract, the hash baseline is rebaselined by the tool, the reference page and contracts-index are regenerated, the snapshot matches, and all seven files are staged together awaiting ratification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human ratification — land the constitution-plane commit as one atomic unit</name>
  <files>contracts/harness/security/deny-domains.schema.json, contracts/harness/security/deny-domains.json</files>
  <read_first>
    Task 1's staged diff (`git diff --cached`),
    .planning/milestones/v2.3-phases/28-human-docs-registry-guard-derived-queue-v2-3-c/28-01-PLAN.md
      (Task 2 — the checkpoint this one mirrors in shape)
  </read_first>
  <action>
    Present Task 1's staged seven-file diff for blocking human review and STOP. The agent does not
    self-ratify: it neither flips an approval state nor commits until the human resume-signal
    arrives. On "ratified", land all seven files as ONE commit. On any correction request, return to
    Task 1 rather than amending the staged diff in place.
  </action>
  <what-built>
    A new constitution-plane contract pair, `contracts/harness/security/deny-domains.{schema.json,json}`,
    declaring the harness's three path-deny domains — `constitution`, `secret`, `review-ledger` —
    each with its owner module and constant, its glob set, its bypass semantics, the tool surfaces it
    covers, the tool surfaces it does NOT cover (every domain names `Bash`), its runtime adapters and
    its extra enforcement layers; plus the two non-enforcing glob sets that must not be mistaken for
    domains. Registered as a ratified data contract in `tools/contract_hash/hash.py`, with the
    rebaselined manifest, the generated reference page, the regenerated contracts-index, and the
    regenerated syrupy snapshot — all staged as ONE commit.
  </what-built>
  <how-to-verify>
    BLOCKING human checkpoint. The agent does NOT self-ratify. Present, in this order:

    1. `git diff --cached --stat` — exactly seven files, nothing else.
    2. The full text of the instance. Confirm against the live hooks, by eye:
       `contract_guard.py:53` == the `constitution` globs; `secret_scan.py:37` == the `secret` globs;
       `ledger_guard.py:48` == the `review-ledger` globs.
    3. Confirm `bypasses` is `[]` for BOTH `secret` and `review-ledger` and that the key is PRESENT
       (an omitted key would make "no bypass" an inference — ADR-0010:163-166 makes it a decision).
    4. Confirm every domain's `uncovered_tool_surfaces` contains `"Bash"` — this is the SEAL-01
       field, and phase 31's threat model reads it.
    5. Confirm the three domains are separate records with separate `owner_constant`s, and that the
       `_note` states the hooks remain the SSOT and no hook imports this file.
    6. Confirm no timestamp, reviewer identity, or model identifier appears in either file.
    7. Confirm the ratification path used: the standard human-authorized constitution write path,
       with NO fabricated `GOLDEN_APPROVE_HUMAN` and NO `HARNESS_DEV_BYPASS` self-landing.

    Then run, to confirm nothing half-landed:

        uv run python -m tools.contract_drift.drift
        uv run check-jsonschema --schemafile contracts/harness/security/deny-domains.schema.json contracts/harness/security/deny-domains.json
        uv run pytest tools/memory_regen tools/docs_sync tools/contract_hash tools/contract_drift -q
        git add -A -- docs/reference .memory/derived/contracts-index.md
        git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md
  </how-to-verify>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift && uv run pytest tools/memory_regen tools/docs_sync -q</automated>
  </verify>
  <resume-signal>Reply "ratified" to land the atomic commit, or describe the correction needed before ratification.</resume-signal>
  <done>The seven-file constitution-plane commit is human-ratified and landed as one atomic unit; `drift`, `contract-check`'s local equivalent, and the local `stale-derived` equivalent are all clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent session → `contracts/harness/security/**` | Constitution-plane write; denied to a direct agent write by `contract_guard` (`contract_guard.py:53,86-92`) unless human-authorized. Task 2 is the ratification authority. |
| `contract_hash --write` output → `contracts/.hashes/manifest.json` | Machine-computed baseline; never hand-edited. |
| `docs_sync` / `memory_regen` output → committed-derived plane | Machine-regenerated; CI's `stale-derived` job (`ci.yml:227`) is the non-bypassable verify side. |
| The registry as a claim → the hooks as the truth | One-directional by construction: no hook imports the registry (D-02). Plan 30-03's test is the only link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Elevation of Privilege (self-ratification) | `contracts/harness/security/**` | mitigate | Task 2 is a blocking human checkpoint; no fabricated `GOLDEN_APPROVE_HUMAN`, no `HARNESS_DEV_BYPASS` self-land. Verified aloud at step 7. |
| T-30-02 | Spoofing (false assurance) | a registry that reads as a control | mitigate | The `_note` and the schema `description` state that this file ENFORCES NOTHING — it is a declaration compared against the hooks. This is the ADR-0010:154-160 failure shape, and the phase exists to stop it, so it must not recreate it. |
| T-30-03 | Tampering (silent domain widening) | `domains[].id` | mitigate | `id` is a CLOSED enum of three values. Adding a fourth domain requires a schema change on the constitution plane, i.e. a human ratification — not a data edit. |
| T-30-04 | Repudiation | half-landed commit | mitigate | All seven artifacts staged and ratified together; the checkpoint re-runs `drift` + the local `stale-derived` equivalent before the commit lands. |
| T-30-05 | Information Disclosure | model identifier / human identity in a committed contract | mitigate | Explicit `grep -rniE` in the acceptance criteria; no timestamp, reviewer, or model-identifier property permitted. |
| T-30-06 | Tampering (circular truth) | a hook importing the registry | mitigate | `grep -rn "deny.domains" tools/hooks/` must return nothing, asserted in the acceptance criteria and re-asserted structurally by plan 30-03. |
| T-30-SC | Tampering (supply chain) | package installs | accept | Zero new external packages — existing `contract_hash` / `contract_drift` / `docs_sync` / `memory_regen` / `check-jsonschema` CLIs only. Package Legitimacy Gate: not applicable (30-RESEARCH §Environment availability). |
</threat_model>

<verification>
- `uv run python -m tools.contract_drift.drift` exits 0.
- `uv run check-jsonschema --schemafile contracts/harness/security/deny-domains.schema.json contracts/harness/security/deny-domains.json` exits 0.
- `uv run pytest tools/memory_regen tools/docs_sync tools/contract_hash tools/contract_drift -q` exits 0.
- `git add -A -- docs/reference .memory/derived/contracts-index.md && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md` is clean.
- `git status --porcelain tools/hooks tools/deny_domains tools/harness_lint` is empty.

Do NOT run the full suite as an in-flight gate — plans 30-02/30-03 create `tools/deny_domains/` and
the harness_lint test in later waves. The full-suite / emit / GEN-04 fan-in belongs to plan 30-04.
</verification>

<success_criteria>
- The deny-domain registry exists on the constitution plane as a schema + instance pair, declaring
  exactly three domains, each with globs, bypasses, covered and uncovered tool surfaces, runtime
  adapters and enforcement layers.
- The uncovered bash surface is present as DATA on every domain, not as prose.
- The two non-enforcing glob sets are declared as non-enforcing; `commit_gate` is explicitly absent.
- The instance is drift-gated (registered in `DATA_CONTRACT_PATHS`) and schema-validated by CI.
- The commit is human-ratified, not agent self-approved, and lands atomically.
- No hook was edited and no hook reads the registry.
</success_criteria>

<output>
Create `.planning/phases/30-deny-domain-registry-v2-4-a/30-01-SUMMARY.md` when done. It MUST contain
the verbatim `git diff --cached --stat` of the ratified commit, an explicit statement of which
human-ratification path was used, and the before/after of CI `contract-check`'s SKIP line.
</output>
