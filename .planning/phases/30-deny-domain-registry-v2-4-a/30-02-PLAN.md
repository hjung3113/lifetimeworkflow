---
phase: 30-deny-domain-registry-v2-4-a
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - tools/deny_domains/pyproject.toml
  - tools/deny_domains/__init__.py
  - tools/deny_domains/registry.py
  - tools/deny_domains/tests/__init__.py
  - tools/deny_domains/tests/conftest.py
  - tools/deny_domains/tests/test_registry.py
  - uv.lock
autonomous: true
requirements: [SEAL-01]

must_haves:
  truths:
    - "RED BEFORE GREEN: `tools/deny_domains/tests/test_registry.py` is authored and RUN FIRST against a missing `tools.deny_domains.registry`, and the run is recorded as a collection/import failure BEFORE `registry.py` exists — the /new-contract-rule order (contract entry → data case → failing code stub) with the contract entry already landed by plan 30-01"
    - "The (input, expected) cases are DATA — a table of synthetic registry documents paired with the exact expected rejection reason string — never a hand-rolled assertion per case"
    - "`load_registry(path=...)` takes an EXPLICIT path and defaults to the real contract, so tests are hermetic on tmp documents and an `examples/**` overlay stays possible later (the 28-D-14 seam)"
    - "The loader enforces the three cross-record rules JSON Schema cannot: `id` uniqueness, `owner_constant` uniqueness, and pairwise glob DISJOINTNESS across domains — the invariant `contract_guard.py:16-20` documents and ADR-0010:170-175 depends on"
    - "`resolve_owner(domain)` returns the LIVE constant via `importlib.import_module` + `getattr`, and raises a named error identifying the domain when the module or the attribute is gone — this is the seam plan 30-03's drift test drives"
    - "`matrix_reconciliation()` returns BOTH directions as sets: globs declared-but-not-in-matrix, and matrix `path_deny_globs` entries claimed by NO declared domain. The second is the ADR-0010:154-160 inert-data failure shape and must be a first-class return value, not a boolean"
    - "The loader IMPORTS nothing from `tools/hooks/**` at module scope beyond what `resolve_owner` imports dynamically — and NO hook imports this package (D-02, re-asserted here)"
    - "Zero new external packages: `importlib`, `json`, `pathlib`, `fnmatch` are stdlib; `resolve_path` is reused from `tools.harness_perms` — never a second glob matcher (D-02 of Phase 4)"
  artifacts:
    - path: "tools/deny_domains/registry.py"
      provides: "the loader: `load_registry`, `resolve_owner`, `matrix_reconciliation`, `DenyDomainError`, and the cross-record validation JSON Schema cannot express"
      contains: "importlib"
    - path: "tools/deny_domains/tests/test_registry.py"
      provides: "the data-based (input, expected) rejection table plus the disjointness and reconciliation cases"
      contains: "pytest.mark.parametrize"
    - path: "tools/deny_domains/pyproject.toml"
      provides: "virtual uv workspace member, package = false, zero dependencies"
      contains: "package = false"
  key_links:
    - from: "tools/deny_domains/registry.py"
      to: "contracts/harness/security/deny-domains.json"
      via: "load_registry(path=DEFAULT_REGISTRY_PATH) — read as DATA, validated against the sibling schema"
      pattern: "deny-domains"
    - from: "tools/deny_domains/registry.py"
      to: "tools/hooks/{contract_guard,secret_scan,ledger_guard}.py"
      via: "resolve_owner -> importlib.import_module(owner_module) + getattr(owner_constant) — dynamic, one direction only"
      pattern: "importlib.import_module"
    - from: "tools/deny_domains/registry.py"
      to: "harness/permission-matrix.json"
      via: "matrix_reconciliation via tools.harness_perms.load_matrix — the ONE loader (resolver.py:53)"
      pattern: "load_matrix"
---

<objective>
Build the loader that turns the landed declaration into something a gate can compare against — and
enforce, in code, the three cross-record rules the schema structurally cannot express.

Purpose: plan 30-03's drift test needs two things the contract alone does not give it: the LIVE
constant behind each declared `owner_module`/`owner_constant` pair, and a both-directions
reconciliation against `harness/permission-matrix.json`. Putting them in a standalone member rather
than in `harness_lint` keeps phases 31–33 able to consume the registry without a tool importing a
lint package (D-03).

Output: one virtual uv member, one loader module, one data-based rejection table. Zero production
behaviour change; no hook is touched.
</objective>

<execution_context>
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hyojung/orca/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-deny-domain-registry-v2-4-a/30-CONTEXT.md
@.planning/phases/30-deny-domain-registry-v2-4-a/30-RESEARCH.md
@.planning/phases/30-deny-domain-registry-v2-4-a/30-01-PLAN.md
@contracts/harness/security/deny-domains.json
@tools/harness_perms/resolver.py
@tools/harness_lint/pyproject.toml
@tools/harness_lint/tests/conftest.py
@tools/docs_guard/registry.py

<interfaces>
<!-- VERIFIED against source in this session. -->

`tools/harness_perms/resolver.py` — the ONE resolver and the ONE matrix loader. Reuse verbatim:

    def resolve_path(deny_globs: list[str], path: str) -> Decision   # "deny" | "allow"
    def load_matrix(path: str | Path = _DEFAULT_MATRIX) -> dict      # order-preserving

`harness/permission-matrix.json:27-35` — `path_deny_globs` is a flat list of 7 strings today,
exactly the concatenation of the three live constants (4 + 2 + 1). That equality is asserted NOWHERE
in the repo; `matrix_reconciliation()` is where it becomes assertable.

`tools/harness_lint/pyproject.toml` — the virtual-member template to clone: `package = false`,
`dependencies = []`, `requires-python = ">=3.11"`, and the explicit comment that the member must
never mutate `uv.lock` with a NEW external dep. Adding the member itself DOES change `uv.lock`'s
workspace member list — that is expected and is regenerated by `uv sync --all-packages`, never
hand-edited.

`tools/harness_lint/tests/conftest.py:19-22` — the sys.path idiom for a virtual member's tests
(`parents[3]` from `tests/`). Clone it; `tools` is a namespace package with no `__init__.py`.

`pyproject.toml:29-35` — `members = ["tools/*"]`, so `tools/deny_domains/` is picked up
automatically once it contains a `pyproject.toml`. No root edit needed.

`tools/docs_guard/registry.py` — the house style for "load a declaration, validate what the schema
cannot, raise a named error with an exact reason string". Follow its shape, including the
`DEFAULT_*_PATH` module constant + explicit-path parameter pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — author the (input, expected) rejection table against a loader that does not exist</name>
  <files>
    tools/deny_domains/tests/__init__.py,
    tools/deny_domains/tests/conftest.py,
    tools/deny_domains/tests/test_registry.py
  </files>
  <read_first>
    tools/harness_lint/tests/conftest.py (the sys.path idiom),
    tools/docs_guard/tests/ (any test module — the parametrized rejection-table style),
    contracts/harness/security/deny-domains.json (the landed shape)
  </read_first>
  <action>
    Author the tests FIRST. `tools/deny_domains/registry.py` must NOT exist yet.

    `conftest.py`: clone the harness_lint sys.path idiom (`parents[3]`), plus one fixture
    `valid_doc()` returning a minimal-but-schema-valid registry dict built PROGRAMMATICALLY from
    the real contract (`json.load` of `contracts/harness/security/deny-domains.json`, deep-copied) —
    never a hand-typed literal, so a later schema change cannot leave a stale fixture asserting a
    shape that no longer exists (the CR-03 lesson from 29-02).

    `test_registry.py`: ONE `pytest.mark.parametrize` table of `(mutation, expected_reason_fragment)`
    where `mutation` is a callable applied to a deep copy of `valid_doc()`. Minimum rows:

      | mutation | expected reason fragment |
      |---|---|
      | duplicate an `id` across two records | `duplicate domain id` |
      | give two records the same `owner_constant` | `duplicate owner_constant` |
      | add `docs/.docs-review-ledger.toml` to the `constitution` globs | `overlapping deny domains` |
      | add `*.env` to the `review-ledger` globs | `overlapping deny domains` |
      | point `owner_module` at a module that does not exist | `cannot resolve owner module` |
      | point `owner_constant` at an attribute that does not exist | `cannot resolve owner constant` |
      | drop the `bypasses` key entirely | `missing required field` |
      | set `bypasses` to `["NOT_A_TOKEN"]` | `unknown bypass token` |
      | empty `uncovered_tool_surfaces` on every domain | `uncovered_tool_surfaces` |

    The last row deserves a comment: an EMPTY uncovered set is not schema-invalid, and it is the
    exact false-assurance state phase 32 will be tempted to claim. The loader does not reject it —
    it is a legitimate future state — but the test pins today's expectation that all three domains
    name `Bash`, so a silent flip to "fully covered" cannot ride an unrelated commit. Write this row
    as an assertion about the REAL contract, not the mutated fixture.

    Plus three non-parametrized cases:
      - `test_load_registry_accepts_the_real_contract` — `load_registry()` with no argument loads
        `contracts/harness/security/deny-domains.json` and returns three domains.
      - `test_resolve_owner_returns_the_live_constant` — for each domain, `resolve_owner` returns an
        object that `is` the module attribute (identity, not equality) so a copy cannot pass.
      - `test_matrix_reconciliation_reports_both_directions` — on a synthetic matrix with one extra
        unowned glob, the returned `unowned_matrix_globs` set contains exactly that glob and
        `undeclared_in_matrix` is empty; and on a synthetic registry with one extra glob, the reverse.

    Run: `uv run pytest tools/deny_domains -q`. It MUST fail on import/collection
    (`ModuleNotFoundError: tools.deny_domains.registry`). Record the exact failure text — the SUMMARY
    must quote it. Do NOT proceed to Task 2 until that RED is captured.
  </action>
  <verify>
    <automated>! uv run pytest tools/deny_domains -q 2>&1 | tee /dev/stderr | grep -q "passed"</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/deny_domains -q` exits NON-ZERO and its output names
    `tools.deny_domains.registry` as unimportable. `ls tools/deny_domains/registry.py` fails. The
    parametrize table has at least nine rows and every `expected_reason_fragment` is a distinct
    string — two rows sharing a reason means the loader could conflate two defects.
  </acceptance_criteria>
  <done>The rejection table and the three behavioural cases exist and are RED for the stated reason: the loader is not written yet.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement the loader until the table passes, and no further</name>
  <files>
    tools/deny_domains/pyproject.toml,
    tools/deny_domains/__init__.py,
    tools/deny_domains/registry.py,
    uv.lock
  </files>
  <read_first>
    tools/harness_lint/pyproject.toml (member template),
    tools/docs_guard/registry.py (loader + named-error style),
    tools/harness_perms/resolver.py (resolve_path / load_matrix — reuse, never re-roll)
  </read_first>
  <action>
    `pyproject.toml`: clone the harness_lint template — name `logparser-deny-domains`,
    `version = "0.0.0"`, `requires-python = ">=3.11"`, `dependencies = []`, `[tool.uv] package = false`,
    with the same comment about never adding an external dep.

    `registry.py` — public surface, and NOTHING beyond what Task 1's table demands:

        DEFAULT_REGISTRY_PATH = REPO_ROOT / "contracts/harness/security/deny-domains.json"

        class DenyDomainError(ValueError): ...

        def load_registry(path=DEFAULT_REGISTRY_PATH) -> dict
            # json.load, then the cross-record rules JSON Schema cannot express:
            #   1. `id` unique across records                -> "duplicate domain id: <id>"
            #   2. `owner_constant` unique across records     -> "duplicate owner_constant: <name>"
            #   3. required keys present                      -> "missing required field: <key> on <id>"
            #   4. every bypass token in the known set        -> "unknown bypass token: <tok> on <id>"
            #   5. PAIRWISE DISJOINTNESS: for each ordered pair (A, B), no glob of A may
            #      `resolve_path`-deny any of B's globs treated as probe paths, and vice versa
            #                                                 -> "overlapping deny domains: <a> and <b> both claim <probe>"
            # Disjointness uses tools.harness_perms.resolve_path — never a second matcher.

        def resolve_owner(domain: dict) -> object
            # importlib.import_module(domain["owner_module"]) then getattr(..., owner_constant).
            # ModuleNotFoundError -> DenyDomainError("cannot resolve owner module ...")
            # AttributeError      -> DenyDomainError("cannot resolve owner constant ...")

        def matrix_reconciliation(registry=None, matrix_path=None) -> dict
            # {"undeclared_in_matrix": set[str], "unowned_matrix_globs": set[str]}
            # Reads path_deny_globs via tools.harness_perms.load_matrix (the ONE loader).
            # BOTH directions are returned as sets — never collapsed to a bool. `unowned_matrix_globs`
            # is the ADR-0010:154-160 inert-data shape and its own docstring must say so.

    Module docstring must state, in this order: (i) this package reads the registry as DATA and
    enforces NOTHING; (ii) the hooks remain the single source of truth and no hook imports this
    package — a registry the hooks read would make the phase-30 drift gate a tautology (D-02); and
    (iii) it exists as its own member, not inside `harness_lint`, because phases 31–33 consume it and
    a hook may never import a lint package (D-03).

    Then run `uv sync --all-packages` (regenerates `uv.lock`'s workspace member list — never
    hand-edit it) and `uv run pytest tools/deny_domains -q` until green. Stop when the table passes;
    do not add unrequested API.
  </action>
  <verify>
    <automated>uv run pytest tools/deny_domains -q && uv run ruff check tools/deny_domains && uv run ruff format --check tools/deny_domains</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/deny_domains -q` exits 0 with every parametrized row passing. `uv run ruff
    check tools/deny_domains` and `uv run ruff format --check tools/deny_domains` exit 0.
    `git diff uv.lock` shows ONLY the new workspace member (no new external package, no version
    bump) — if any dependency line moved, STOP and revert. `grep -rn "deny_domains" tools/hooks/`
    returns nothing. `grep -rn "fnmatch" tools/deny_domains/registry.py` returns nothing (the
    resolver is reused, not re-rolled).
  </acceptance_criteria>
  <done>The loader exists, the nine-row rejection table and the three behavioural cases are green, ruff is clean, and `uv.lock` moved only by the member addition.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the disjointness and reconciliation checks are load-bearing, by mutation</name>
  <files>tools/deny_domains/tests/test_registry.py</files>
  <read_first>
    tools/hooks/tests/test_contract_guard.py:352-381 (the delete-each-member mutation idiom),
    tools/adoption_apply/tests/test_constitution_refusal.py:528-576 (the existing partial
      disjointness/subset assertions this generalizes)
  </read_first>
  <action>
    Add two in-suite mutation proofs, following `test_contract_guard.py:352-381`'s restore-in-`finally`
    shape so a failing assertion cannot leak a mutated constant into a sibling test:

    1. **Disjointness is load-bearing.** For each ordered pair of declared domains, temporarily add
       the OTHER domain's first glob to this domain's glob list on a deep copy and assert
       `load_registry` raises `DenyDomainError` naming BOTH domain ids. A check that only tests one
       direction passes on an asymmetric overlap — assert both orders explicitly.

    2. **Reconciliation catches the inert-data shape.** Build a tmp matrix file whose
       `path_deny_globs` carries one glob no declared domain claims, and assert it appears in
       `unowned_matrix_globs`. Then remove one REAL glob from that tmp matrix and assert it appears
       in `undeclared_in_matrix`. Add a docstring naming ADR-0010:154-160: an unowned matrix row is a
       claimed control that does not exist, and it is the exact defect that made `ledger_guard`
       necessary.

    Both use tmp/deep-copied data — neither writes to `harness/permission-matrix.json` or to the real
    contract.
  </action>
  <verify>
    <automated>uv run pytest tools/deny_domains -q && uv run python -m tools.contract_drift.drift && git diff --exit-code -- harness/permission-matrix.json contracts/</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/deny_domains -q` exits 0. `git diff --exit-code -- harness/permission-matrix.json contracts/`
    is clean (no test mutated a real file). Deleting the disjointness check from `registry.py` makes
    at least two tests fail — demonstrate this locally and record the failing test ids in the
    SUMMARY, then restore.
  </acceptance_criteria>
  <done>Disjointness and both reconciliation directions are proven load-bearing by mutation, with the deletion proof recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry JSON (data) → loader | Read-only; the loader raises named errors and never repairs a malformed declaration. |
| Loader → hook modules | One direction only, dynamic (`importlib`). No hook imports this package — asserted in the acceptance criteria. |
| Tests → real repo files | Tests operate on deep copies and tmp files; `git diff --exit-code` over `contracts/` and the matrix is a verification step, not a hope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Spoofing (false assurance) | `matrix_reconciliation` collapsed to a bool | mitigate | Both directions returned as SETS. A boolean loses which glob is unowned, which is the only actionable part of the finding. |
| T-30-08 | Tampering (test leakage) | mutation proofs mutating module state | mitigate | `finally`-restore idiom cloned from `test_contract_guard.py:352-381`; deep copies for document mutations; `git diff --exit-code` in the verify step. |
| T-30-09 | Elevation of Privilege | a stale fixture pinning a shape the schema no longer has | mitigate | `valid_doc()` is derived from the real contract by `json.load` + deep copy, never a hand-typed literal (the 29-02 CR-03 lesson). |
| T-30-10 | Denial of Service (self-inflicted) | `uv.lock` churn from a new member | mitigate | `dependencies = []`, `package = false`; `git diff uv.lock` must show only the member entry, and a moved dependency line is a STOP condition. |
| T-30-SC | Tampering (supply chain) | package installs | accept | Zero new external packages — stdlib plus the existing `tools.harness_perms`. Package Legitimacy Gate: not applicable. |
</threat_model>

<verification>
- `uv run pytest tools/deny_domains -q` exits 0.
- `uv run ruff check tools/deny_domains && uv run ruff format --check tools/deny_domains` exit 0.
- `git diff uv.lock` shows only the new workspace member.
- `git diff --exit-code -- harness/permission-matrix.json contracts/ tools/hooks/` is clean.
- The Task 1 RED output is quoted verbatim in the SUMMARY.
</verification>

<success_criteria>
- The loader resolves each declared domain to its LIVE constant by identity, not by a copy.
- The three cross-record rules JSON Schema cannot express are enforced in code, each with its own
  distinct reason string.
- Matrix reconciliation reports both directions, and the unowned-glob direction is proven by a
  mutation that reproduces the ADR-0010 inert-data shape.
- The RED-before-GREEN order is recorded, not asserted.
- No hook was edited; no hook imports the new package.
</success_criteria>

<output>
Create `.planning/phases/30-deny-domain-registry-v2-4-a/30-02-SUMMARY.md` when done. It MUST contain
the verbatim Task 1 RED failure output, the Task 3 deletion-proof failing test ids, and the
`git diff --stat uv.lock`.
</output>
