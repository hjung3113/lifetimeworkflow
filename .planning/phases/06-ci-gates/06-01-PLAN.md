---
phase: 06-ci-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - harness/project.toml
  - tools/harness_config/loader.py
  - tools/harness_config/tests/test_matrix_emit.py
  - tools/harness_lint/tests/test_language_config.py
  - tools/harness_lint/tests/test_core_no_example_dep.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "harness/project.toml declares a per-language test_paths list for both the dotnet and python legs"
    - "languages() surfaces test_paths so the Phase-6 CI matrix emitter can fan out over configured test targets"
    - "the GEN-03 SSOT consistency gate (test_matrix_language_scopes_equal_config) still passes with the additive field — test_paths does not perturb language_bash_scopes()"
    - "the GEN-04 core-no-example-dep guard sanctions the test_paths = instance-pointer line (mirroring the existing persona = exemption) while staying live for genuine leaks on any other key"
    - "every declared test_paths entry points at a real path on disk"
  artifacts:
    - path: "harness/project.toml"
      provides: "per-language test_paths data slot (dotnet .csproj + python example tests dir)"
      contains: "test_paths"
    - path: "tools/harness_config/tests/test_matrix_emit.py"
      provides: "Wave-0 test asserting the matrix-JSON shape built from languages() incl. test_paths"
      min_lines: 20
    - path: "tools/harness_lint/tests/test_language_config.py"
      provides: "extended per-language assertion covering the new test_paths field"
      contains: "test_paths"
    - path: "tools/harness_lint/tests/test_core_no_example_dep.py"
      provides: "widened _INSTANCE_POINTER_LINE exemption covering test_paths + negative control proving the scan stays live"
      contains: "test_paths"
  key_links:
    - from: "tools/harness_config/loader.py"
      to: "harness/project.toml [[languages]].test_paths"
      via: "languages() passthrough (l.get('test_paths', []))"
      pattern: "test_paths"
    - from: "tools/harness_lint/tests/test_language_config.py"
      to: "language_bash_scopes()"
      via: "SSOT equality gate unchanged by additive field"
      pattern: "language_bash_scopes"
    - from: "harness/project.toml [[languages]].test_paths"
      to: "test_core_no_example_dep.py _INSTANCE_POINTER_LINE"
      via: "sanctioned instance-pointer exemption (root|persona|test_paths)"
      pattern: "root\\|persona\\|test_paths"
---

<objective>
Enabler-1: add a per-language `test_paths: list[str]` field to each `harness/project.toml [[languages]]` table so the Wave-2 CI matrix can locate the example's actual test targets (the example has 3 `.csproj` and NO `.sln`, so a bare `dotnet test` fails; example pytest lives at `examples/log-parser/tests`, OFF the root `testpaths`). The `tools/harness_config` loader is already forward-compatible (`languages()` is a raw passthrough), so this is a data addition plus test coverage — no loader signature change. Because the `test_paths` values point INTO `examples/` (and the dotnet leg's value also carries the `libs/dotnet` prose token), this plan ALSO widens the GEN-04 core-no-example-dep guard's sanctioned instance-pointer exemption to cover the `test_paths =` data-slot line — the exact precedent already set by the `persona =` line, which points at `examples/log-parser/agents/dotnet-engineer.md`.

Purpose: Without a config-declared `test_paths`, the Wave-2 matrix would have to hardcode paths, violating D-01 (config-derived, not hardcoded). This plan lands the SSOT data slot the matrix emitter consumes, and keeps the GEN-04 guard green BY CONSTRUCTION (not by regression).
Output: `test_paths` in both language tables, a Wave-0 matrix-shape test, an extended SSOT consistency gate proving the additive field is inert to `language_bash_scopes()`, and a widened GEN-04 exemption with a negative control proving the scan stays live for genuine leaks.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-gates/06-CONTEXT.md
@.planning/phases/06-ci-gates/06-PATTERNS.md

<interfaces>
<!-- Existing loader API the matrix emitter reuses — DO NOT change signatures. -->

From tools/harness_config/loader.py:
- load_project(path=_DEFAULT_PROJECT) -> dict
- languages(cfg=None) -> list[dict]        # raw passthrough of [[languages]] tables; test_paths flows through via l.get("test_paths", [])
- language_bash_scopes(cfg=None) -> set[str]  # MUST remain unchanged; test_paths is additive, not a bash_scope

From tools/harness_config/__init__.py (PEP-562 lazy re-export):
- from tools.harness_config import load_project, languages, language_bash_scopes

Existing [[languages]] tables in harness/project.toml (lines 21-34):
- dotnet: id, bash_scope="dotnet *", test="dotnet test", format, sdk_bootstrap, persona
- python: id, bash_scope="uv *", test="uv run pytest", format, persona

GEN-04 guard exemption (tools/harness_lint/tests/test_core_no_example_dep.py):
- _INSTANCE_POINTER_LINE = re.compile(r"\s*(root|persona)\s*=")  # line 82 — the sanctioned exemption
- _is_instance_pointer_line(rel_path, line) -> bool               # exempts matching lines ONLY in harness/project.toml
- _scan_lines(rel_path, text) -> list[(lineno, line)]             # flags examples/, components/toy-converter, libs/dotnet, prose, import examples
- Precedent: the persona = line ALREADY matches _INSTANCE_POINTER_LINE and legitimately carries BOTH an examples/ token AND a dotnet-engineer prose token (test_instance_pointer_persona_is_exempt, lines 187-193)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 — assert the matrix-JSON shape from languages() incl. test_paths</name>
  <files>tools/harness_config/tests/test_matrix_emit.py</files>
  <read_first>
    - tools/harness_config/tests/test_loader.py (exact analog — import + repo-root parents[3] idiom at lines 10-17; per-field assertion idiom at lines 20-37; exact-set style at lines 47-50)
    - tools/harness_config/__init__.py (PEP-562 lazy re-export — no conftest needed)
    - .planning/phases/06-ci-gates/06-PATTERNS.md (section "test_matrix_emit.py")
  </read_first>
  <behavior>
    - test: load_project() / languages() returns both dotnet and python legs (ids sorted == ["dotnet", "python"])
    - test: building the matrix include-list [{"id","test","test_paths"} for l in languages()] yields one leg per language, each carrying a non-empty "test" string and a "test_paths" that is a list[str]
    - test: every leg's test_paths is non-empty (both legs declare targets) — this is the assertion that goes RED until Task 2 adds the field
  </behavior>
  <action>Create `tools/harness_config/tests/test_matrix_emit.py`. Copy the import + `_REPO_ROOT = Path(__file__).resolve().parents[3]` bootstrap verbatim from `test_loader.py:10-17` (package PEP-562 re-export — no `sys.path` insert, no conftest). Add a test building the CI matrix include-list from `languages()` — one dict per leg with keys `id`, `test`, `test_paths` (using `l.get("test_paths", [])`) — and assert: (a) `sorted(leg["id"] for leg in include) == ["dotnet", "python"]`; (b) each leg's `test` is a non-empty stripped string; (c) each leg's `test_paths` is a `list` of `str`; (d) each leg's `test_paths` is non-empty. Assertion (d) is expected RED before Task 2 (fields not yet in project.toml). No subprocess, pure structural — mirror the `test_load_project_returns_two_languages` / `test_each_language_carries_required_fields` idiom.</action>
  <verify>
    <automated>uv run pytest tools/harness_config/tests/test_matrix_emit.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tools/harness_config/tests/test_matrix_emit.py` exists and imports `languages` from `tools.harness_config`
    - Before Task 2: the non-empty-test_paths assertion FAILS (RED) — confirming the test actually gates the new field
    - After Task 2: `uv run pytest tools/harness_config/tests/test_matrix_emit.py` exits 0
  </acceptance_criteria>
  <done>Test file exists, asserts the matrix include-list shape (id + test + list[str] test_paths, one leg per language), and is RED on the test_paths assertion until Task 2 lands the field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test_paths to both [[languages]] tables + note loader passthrough</name>
  <files>harness/project.toml, tools/harness_config/loader.py</files>
  <read_first>
    - harness/project.toml (lines 1-14 header declares PURE DATA; existing tables at lines 21-34; the persona = examples/... line at line 27 is the exemption precedent)
    - tools/harness_config/loader.py (languages() passthrough at lines 42-46 — already forward-compatible)
    - .planning/phases/06-ci-gates/06-PATTERNS.md (sections "harness/project.toml (MODIFY)" and "loader.py (MODIFY)")
  </read_first>
  <action>In `harness/project.toml`, add an additive `test_paths` array to each `[[languages]]` table, keeping the file data-only (no logic — respect the lines 1-14 header). For the `dotnet` table (lines 21-27): set `test_paths = ["examples/log-parser/libs/dotnet/Normalize.Tests/Normalize.Tests.csproj"]` with an inline `#` comment noting 3 `.csproj` + no `.sln` means bare `dotnet test` fails, so the explicit test-project path is required (RESEARCH Pitfall 2). For the `python` table (lines 29-34): set `test_paths = ["examples/log-parser/tests"]` with an inline `#` comment noting example pytest lives off the root `testpaths`. Then in `tools/harness_config/loader.py`, add ONE docstring line to `languages()` (lines 42-46) noting that legs MAY carry an optional `test_paths` list consumed by the Phase-6 CI matrix — do NOT change the signature or return type (the raw passthrough already surfaces the field). Do NOT add a dedicated accessor. NOTE ON ORDERING: adding these `examples/`-bearing lines (the dotnet leg's value ALSO carries the `libs/dotnet` prose token) trips the GEN-04 guard (`tools/harness_lint/tests/test_core_no_example_dep.py`) until Task 3 widens its sanctioned exemption — Task 2's verify is intentionally scoped to `tools/harness_config` to reflect this ordering; run the full suite only after Task 3.</action>
  <verify>
    <automated>uv run pytest tools/harness_config/tests/test_matrix_emit.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `harness/project.toml` contains `test_paths = ["examples/log-parser/libs/dotnet/Normalize.Tests/Normalize.Tests.csproj"]` under the dotnet table
    - `harness/project.toml` contains `test_paths = ["examples/log-parser/tests"]` under the python table
    - `languages()` signature is unchanged (still `languages(cfg: dict | None = None) -> list[dict]`)
    - `uv run pytest tools/harness_config` exits 0 (Task-1 test now GREEN)
  </acceptance_criteria>
  <done>Both language tables declare a real `test_paths` list; the loader passthrough surfaces it unchanged; Task-1 matrix-shape test passes. Full-suite green is deferred to Task 3 (guard exemption).</done>
</task>

<task type="auto">
  <name>Task 3: Extend SSOT gate for test_paths + widen GEN-04 guard exemption for the test_paths data slot</name>
  <files>tools/harness_lint/tests/test_language_config.py, tools/harness_lint/tests/test_core_no_example_dep.py</files>
  <read_first>
    - tools/harness_lint/tests/test_language_config.py (existing per-language idiom at lines 48-58; SSOT equality gate at lines 39-45; persona-exists .is_file() pattern at lines 48-52)
    - tools/harness_lint/tests/test_core_no_example_dep.py (the GEN-04 guard — _INSTANCE_POINTER_LINE at line 82; _is_instance_pointer_line at lines 106-109; _scan_lines at lines 112-126; persona-exemption precedent test at lines 187-193; negative-control idiom at lines 153-178)
    - .planning/phases/06-ci-gates/06-PATTERNS.md (section "test_language_config.py (MODIFY)")
  </read_first>
  <action>Two edits, both in `tools/harness_lint/tests/`.

  (A) In `test_language_config.py`, add `test_each_configured_language_has_test_paths()` mirroring the existing `for lang in languages()` idiom (lines 55-58). Assert each leg's `lang.get("test_paths", [])` is a `list` of `str` AND non-empty, and that every declared path resolves under `_REPO_ROOT` using `.exists()` (NOT `.is_file()` — a `.csproj` is a file but the pytest target `examples/log-parser/tests` is a directory; mirror the persona `.is_file()` check at lines 48-52 but use `.exists()`). Do NOT modify `test_matrix_language_scopes_equal_config` (lines 39-45) — it must still pass unchanged, proving the additive `test_paths` field does not perturb `language_bash_scopes()` (RESEARCH A4).

  (B) In `test_core_no_example_dep.py`, widen `_INSTANCE_POINTER_LINE` (line 82) from `r"\s*(root|persona)\s*="` to `r"\s*(root|persona|test_paths)\s*="` so a `test_paths =` data-slot line in `harness/project.toml` becomes a sanctioned instance-pointer exemption — the SAME precedent as the existing `persona =` line, which ALREADY points into `examples/log-parser/agents/dotnet-engineer.md` and carries BOTH an `examples/` SCOPE-A token AND a `dotnet-engineer` GEN-05 token on one exempt line (see `test_instance_pointer_persona_is_exempt`, lines 187-193). Update the `_INSTANCE_POINTER_LINE` comment (lines 81-82) and the `_is_instance_pointer_line` docstring (lines 106-109) to name `test_paths` alongside `root`/`persona` and cite it as the CI test-target data slot (ADR-0002 (c)). Then add TWO tests next to the existing exemption tests: (1) `test_instance_pointer_test_paths_is_exempt` calling `_scan_lines(_INSTANCE_ROOT_FILE, 'test_paths = ["examples/log-parser/libs/dotnet/Normalize.Tests/Normalize.Tests.csproj"]')` and asserting NO hits — proving the widened exemption covers the dotnet leg's line even though it carries BOTH an `examples/` token AND the `libs/dotnet` prose token; (2) `test_negative_control_flags_nonexempt_project_toml_leak` calling `_scan_lines(_INSTANCE_ROOT_FILE, 'sdk_bootstrap = "examples/leak/x.sh"')` and asserting hits ARE returned — proving the exemption is key-scoped to `root`/`persona`/`test_paths` ONLY and the scan stays LIVE for a genuine `examples/` leak on any other key in project.toml (tamper-evidence). Do NOT touch `_PATH_TOKENS`, `_PROSE_TOKENS`, `_IMPORT_EXAMPLES`, or the pre-existing negative controls (lines 153-178).</action>
  <verify>
    <automated>uv run pytest tools/harness_lint -x</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_each_configured_language_has_test_paths` exists and asserts `list[str]`, non-empty, and `(_REPO_ROOT / p).exists()` per declared path
    - `test_matrix_language_scopes_equal_config` still passes (SSOT equality unbroken by the additive field)
    - `_INSTANCE_POINTER_LINE` matches `test_paths =` (regex is `r"\s*(root|persona|test_paths)\s*="`)
    - `test_instance_pointer_test_paths_is_exempt` passes (dotnet leg's `test_paths` line — carrying `examples/` AND `libs/dotnet` — is exempt)
    - `test_negative_control_flags_nonexempt_project_toml_leak` passes (a non-pointer `examples/` line in project.toml still trips `_scan_lines` — scan stays live)
    - `test_core_has_no_example_dependency` passes against the real tree (project.toml's new `test_paths` lines are exempt; no other core file leaks)
    - Full non-example suite stays green: `uv run pytest` exits 0
  </acceptance_criteria>
  <done>The consistency gate verifies `test_paths` presence + on-disk existence, the SSOT `language_bash_scopes()` equality is untouched, and the GEN-04 guard sanctions the `test_paths =` data-slot line (with a negative control proving the scan is still live for genuine leaks). Full non-example suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo config → CI matrix | `harness/project.toml` values become CI matrix legs consumed downstream (Wave 2). Data must be repo-owned, not externally sourced. |
| core plane → instance | `harness/project.toml` is the ONE sanctioned core-plane file that may name an `examples/` asset (ADR-0002 (c)); the GEN-04 guard enforces that no OTHER core file — and no non-pointer line in project.toml — leaks an instance reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test_paths values in project.toml feeding the Wave-2 matrix | mitigate | Values are repo-owned data validated on-disk by `test_language_config.py` (`.exists()` under `_REPO_ROOT`); no path traversal / external input. GEN-04 core-no-example-dep guard remains clean BY CONSTRUCTION: Task 3 widens `_INSTANCE_POINTER_LINE` to sanction the `test_paths =` data-slot line (same precedent as the pre-existing `persona =` pointer that already names `examples/log-parser/agents/dotnet-engineer.md`), with a negative control proving the scan stays LIVE for a genuine `examples/` leak on any non-pointer key. |
| T-06-02 | Information Disclosure | loader passthrough | accept | Loader is pure I/O over a committed data file; no secrets, no PII. |
| T-06-10 | Tampering | over-broadened guard exemption (silent core→example leaks) | mitigate | The widened exemption is key-scoped (`root|persona|test_paths`) and file-scoped (`harness/project.toml` only); `test_negative_control_flags_nonexempt_project_toml_leak` proves a leak on any other key still trips `_scan_lines`, so the guard cannot silently no-op. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no new dependencies introduced. |
</threat_model>

<verification>
- `uv run pytest tools/harness_config tools/harness_lint` exits 0
- `uv run pytest` (full non-example suite) exits 0 — no regression from the additive field OR the widened guard exemption
- `harness/project.toml` remains data-only (no logic added)
- The GEN-04 guard flags a synthetic non-pointer `examples/` line in project.toml (negative control green) — exemption is not a blanket pass
</verification>

<success_criteria>
- Both `[[languages]]` tables declare a real, on-disk `test_paths` list (CI-01 config-derived matrix data slot)
- `languages()` surfaces `test_paths` with no signature change
- SSOT consistency gate still passes (additive field inert to `language_bash_scopes()`)
- GEN-04 guard sanctions the `test_paths =` pointer line (persona precedent) and stays live for genuine leaks
- Full non-example pytest suite green
</success_criteria>

<output>
Create `.planning/phases/06-ci-gates/06-01-SUMMARY.md` when done.
</output>
