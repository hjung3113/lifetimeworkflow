---
phase: 06-ci-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/contract_drift/drift.py
  - tools/contract_hash/hash.py
  - tools/contract_drift/tests/test_cli_flags.py
autonomous: true
requirements: [CI-01]
must_haves:
  truths:
    - "the contract-drift CLI accepts --contracts-dir and --baseline so CI can gate the EXAMPLE manifest, not only the root"
    - "the contract-hash CLI accepts --contracts-dir and --manifest so the example manifest can be (re)built/inspected symmetrically"
    - "a pristine example-contracts copy passes the gate (exit 0) and a mutated copy fails (exit 1) through the new CLI wrapper"
    - "the bare invocation (no flags) is unchanged for the root job — defaults stay CONTRACTS_DIR/MANIFEST_PATH"
  artifacts:
    - path: "tools/contract_drift/drift.py"
      provides: "argparse --contracts-dir/--baseline in main() routing to run_gate(contracts_dir, baseline_path)"
      contains: "--contracts-dir"
    - path: "tools/contract_hash/hash.py"
      provides: "argparse --contracts-dir/--manifest in main() threading through build_manifest/write_manifest"
      contains: "--contracts-dir"
    - path: "tools/contract_drift/tests/test_cli_flags.py"
      provides: "Wave-0 test driving main([...]) against the example manifest (pristine pass / mutated fail)"
      min_lines: 25
  key_links:
    - from: "tools/contract_drift/drift.py main()"
      to: "run_gate(contracts_dir, baseline_path)"
      via: "argparse args threaded into the already-parameterized gate"
      pattern: "run_gate\\(contracts_dir="
    - from: "tools/contract_drift/tests/test_cli_flags.py"
      to: "drift.main(['--contracts-dir', ..., '--baseline', ...])"
      via: "argv → exit-code contract"
      pattern: "main\\(\\["
---

<objective>
Enabler-2: add argparse `--contracts-dir` / `--baseline` to `tools/contract_drift/drift.py main()` and matching `--contracts-dir` / `--manifest` to `tools/contract_hash/hash.py main()`, so the Wave-2 CI `drift` job can gate BOTH the root manifest AND the example manifest (`examples/log-parser/contracts/.hashes/manifest.json`). The gate functions are ALREADY parameterized (`run_gate(contracts_dir, baseline_path)` at drift.py:133; `build_manifest(contracts_dir)` at hash.py:42; `write_manifest(manifest_path)` at hash.py:62) — only the `main()` argv wrappers are missing (drift.py:166 literally flags `# noqa … reserved for future flags`).

Purpose: Without these flags CI cannot target the example contracts tree by verbatim tool reuse (D-01 — no re-implementation), so example-schema drift goes undetected (RESEARCH Pitfall 3). The change is a ~10-line CLI wrapper per file; no gate/classification logic changes.
Output: two argparse-enabled CLIs (bare invocation unchanged) and a Wave-0 test proving the drift flags route to the example manifest and honor pristine-pass / mutated-fail.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ci-gates/06-CONTEXT.md
@.planning/phases/06-ci-gates/06-PATTERNS.md

<interfaces>
<!-- Already-parameterized targets the new flags thread into — DO NOT change these signatures. -->

From tools/contract_drift/drift.py:
- run_gate(contracts_dir=CONTRACTS_DIR, baseline_path=MANIFEST_PATH) -> dict   # returns {"ok": bool, "drifted": [...]}
- load_baseline(baseline_path=MANIFEST_PATH) -> dict[str, str]
- main(argv: list[str] | None = None) -> int    # currently ignores argv (drift.py:166, # noqa F841)
- CONTRACTS_DIR, MANIFEST_PATH imported from tools.contract_hash.hash (drift.py:22-27)

From tools/contract_hash/hash.py:
- build_manifest(contracts_dir=CONTRACTS_DIR) -> dict[str, str]
- write_manifest(manifest_path=MANIFEST_PATH) -> Path
- main(argv: list[str] | None = None) -> int    # currently only checks "--write" in argv (hash.py:71-79)
- REPO_ROOT, CONTRACTS_DIR, MANIFEST_PATH constants

CLI wrapper (unchanged): tools/contract_drift/check.sh:12 `exec uv run python -m tools.contract_drift.drift "$@"` — forwards argv, so new flags reach main() with no wrapper edit.
Example manifest target: examples/log-parser/contracts/.hashes/manifest.json (contracts dir: examples/log-parser/contracts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 — assert drift main() flags target the example manifest (pristine pass / mutated fail)</name>
  <read_first>
    - tools/contract_drift/tests/test_convention_mutation.py (exact analog — sys.path bootstrap + deferred imports w/ # noqa: E402 at lines 9-22; _copy_contracts(tmp_path) fixture + pristine-passes sanity at lines 27-38)
    - tools/contract_drift/drift.py (run_gate at lines 133-162; main at lines 165-183)
    - .planning/phases/06-ci-gates/06-PATTERNS.md (section "test_cli_flags.py")
  </read_first>
  <behavior>
    - test: drift.main(["--contracts-dir", <pristine example copy>, "--baseline", <example manifest copy>]) returns 0
    - test: after mutating a copied example schema, drift.main(["--contracts-dir", <mutated copy>, "--baseline", <example manifest copy>]) returns 1
    - test: drift.main([]) (no flags) still returns 0 against the pristine ROOT tree (defaults unchanged)
  </behavior>
  <action>Create `tools/contract_drift/tests/test_cli_flags.py`. Copy the `sys.path` bootstrap verbatim from `test_convention_mutation.py:9-22` (`_REPO_ROOT = Path(__file__).resolve().parents[3]` + insert-before-import + `# noqa: E402` on the deferred `from tools.contract_drift.drift import main, run_gate` and `from tools.contract_hash.hash import ...`). Build a tmp copy of the EXAMPLE contracts tree with `shutil.copytree(_REPO_ROOT / "examples/log-parser/contracts", tmp_path / "contracts")` (mirror the `_copy_contracts` helper). Add three tests driving the CLI wrapper by exit code: (1) pristine copy + its copied `.hashes/manifest.json` baseline → `main(["--contracts-dir", str(copied), "--baseline", str(copied_manifest)]) == 0`; (2) mutate one copied `.schema.json` (e.g. bump a numeric `const` or drop a `required` entry) then `main([...]) == 1`; (3) `main([]) == 0` against the untouched root defaults. Use `tmp_path` (pytest builtin) exactly as the analog does. NEVER mutate the committed baseline.</action>
  <verify>
    <automated>uv run pytest tools/contract_drift/tests/test_cli_flags.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tools/contract_drift/tests/test_cli_flags.py` exists, imports `main` from `tools.contract_drift.drift`, and uses `tmp_path` (never the committed baseline)
    - Before Task 2: the flag-routing tests FAIL (RED) — `main()` ignores argv so `--contracts-dir` has no effect
    - After Task 2: `uv run pytest tools/contract_drift/tests/test_cli_flags.py` exits 0 (pristine→0, mutated→1, no-flags→0)
  </acceptance_criteria>
  <done>Test file drives `drift.main([...])` against a copied example tree and asserts pristine-pass / mutated-fail / defaults-unchanged; RED until Task 2 wires argparse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add argparse --contracts-dir/--baseline to drift.py main()</name>
  <read_first>
    - tools/contract_drift/drift.py (main stub at lines 165-179 with the reserved-for-flags # noqa; run_gate params at lines 133-136)
    - tools/contract_hash/hash.py (sibling argv idiom at lines 71-79 — the pattern to extend into argparse)
    - .planning/phases/06-ci-gates/06-PATTERNS.md (section "drift.py (MODIFY)" + Shared Pattern "CLI main(argv) → exit-code")
  </read_first>
  <behavior>
    - main(["--contracts-dir", D, "--baseline", B]) calls run_gate(contracts_dir=D, baseline_path=B) and returns its ok→0/drift→1 mapping
    - main([]) calls run_gate() with defaults (CONTRACTS_DIR / MANIFEST_PATH) — output identical to today for the root job
  </behavior>
  <action>In `tools/contract_drift/drift.py main()` (lines 165-179), replace the reserved-argv stub with `argparse`. Keep the `argv = sys.argv[1:] if argv is None else argv` entry (remove the `# noqa: F841` now that argv is used). Add `--contracts-dir` (default `CONTRACTS_DIR`) and `--baseline` (default `MANIFEST_PATH`), parse `argv`, then call `run_gate(contracts_dir=args.contracts_dir, baseline_path=args.baseline)`. Preserve the existing OK/DRIFT print branches and the `return 0`/`return 1` mapping verbatim. Keep the `main(argv=None) -> int` signature and the `raise SystemExit(main())` guard (lines 182-183) so the test can call `main([...])` directly. Defaults MUST remain `CONTRACTS_DIR`/`MANIFEST_PATH` so the bare invocation (and `check.sh`) is byte-unchanged for the root job.</action>
  <verify>
    <automated>uv run pytest tools/contract_drift -x</automated>
  </verify>
  <acceptance_criteria>
    - `drift.py main()` contains an `argparse` parser with `--contracts-dir` and `--baseline`
    - `run_gate(contracts_dir=args.contracts_dir, baseline_path=args.baseline)` is the call site
    - `main(argv=None) -> int` signature and `raise SystemExit(main())` guard preserved
    - `uv run python -m tools.contract_drift.drift` (no flags) still prints the OK line and exits 0 on the clean root tree
    - `uv run pytest tools/contract_drift` exits 0 (Task-1 flag tests + existing convention-mutation tests all green)
  </acceptance_criteria>
  <done>Drift CLI accepts `--contracts-dir`/`--baseline`, routes them to the already-parameterized `run_gate`, keeps the bare-invocation default behavior, and turns Task-1 GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Add symmetric argparse --contracts-dir/--manifest to hash.py main()</name>
  <read_first>
    - tools/contract_hash/hash.py (existing --write argv idiom at lines 71-79; build_manifest param at line 42; write_manifest param at line 62)
    - tools/contract_drift/drift.py (Task-2 argparse for consistency)
    - .planning/phases/06-ci-gates/06-PATTERNS.md (section "hash.py (MODIFY)")
  </read_first>
  <action>In `tools/contract_hash/hash.py main()` (lines 71-79), extend the argv handling with `argparse` while preserving `--write`. Add `--contracts-dir` (default `CONTRACTS_DIR`) and `--manifest` (default `MANIFEST_PATH`). When `--write` is present, call `write_manifest(manifest_path=args.manifest)` (thread the manifest path through); otherwise print `json.dumps(build_manifest(contracts_dir=args.contracts_dir), indent=2, sort_keys=True)`. Keep the wrote-summary print and the `main(argv=None) -> int` + `raise SystemExit(main())` guard (lines 82-83). Note: `write_manifest` currently rebuilds over the real tree internally — thread `contracts_dir` into its `build_manifest` call if needed so `--write --contracts-dir examples/log-parser/contracts --manifest examples/log-parser/contracts/.hashes/manifest.json` rebuilds the example baseline; if that requires touching `write_manifest`'s body, keep its default-arg behavior identical for the no-flag path. Bare `python -m tools.contract_hash.hash` output MUST be unchanged.</action>
  <verify>
    <automated>uv run pytest tools/contract_hash -x</automated>
  </verify>
  <acceptance_criteria>
    - `hash.py main()` contains an `argparse` parser with `--contracts-dir` and `--manifest`, `--write` still honored
    - `uv run python -m tools.contract_hash.hash` (no flags) prints the root manifest JSON unchanged
    - `uv run python -m tools.contract_hash.hash --contracts-dir examples/log-parser/contracts` prints the EXAMPLE manifest JSON (different key set than root)
    - `uv run pytest tools/contract_hash tools/contract_drift` exits 0; full non-example suite `uv run pytest` exits 0
  </acceptance_criteria>
  <done>Hash CLI accepts `--contracts-dir`/`--manifest` symmetrically with drift, `--write` preserved, bare invocation unchanged; example manifest is inspectable/rebuildable via flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → gate functions | `--contracts-dir`/`--baseline` paths reach `run_gate`/`build_manifest`; in CI they are repo-owned literal paths, not PR-author input. |
| filesystem glob | `build_manifest` globs a caller-supplied `contracts_dir` — already confined to the subtree with symlink-escape defense (hash.py:52-56). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | `--contracts-dir` path traversal | mitigate | `build_manifest` already resolves + confines the glob to the subtree and skips symlink escapes (hash.py:52-56); the new flags do not relax this. |
| T-06-04 | Elevation | injected argv in CI | mitigate | Wave-2 CI passes only repo-owned literal paths in `run:` steps; no `${{ github.event.* }}` interpolation into these flags (enforced in Plan 03). |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no new dependencies introduced. |
</threat_model>

<verification>
- `uv run pytest tools/contract_drift tools/contract_hash` exits 0
- Bare `uv run python -m tools.contract_drift.drift` and `uv run python -m tools.contract_hash.hash` behave byte-identically to pre-change (root job unaffected)
- `uv run python -m tools.contract_drift.drift --contracts-dir examples/log-parser/contracts --baseline examples/log-parser/contracts/.hashes/manifest.json` exits 0 on the clean example tree
- Full non-example suite `uv run pytest` exits 0
</verification>

<success_criteria>
- Drift + hash CLIs accept `--contracts-dir` and `--baseline`/`--manifest`, threading to already-parameterized functions (D-01: no re-implementation)
- Example manifest is gateable/inspectable by verbatim tool reuse
- Bare invocations unchanged (root job untouched)
- Wave-0 flag test green; full non-example suite green
</success_criteria>

<output>
Create `.planning/phases/06-ci-gates/06-02-SUMMARY.md` when done.
</output>
