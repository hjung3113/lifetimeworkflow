---
phase: 26.1
slug: secret-pattern-precision-sc-4-false-positive-closure
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-20
---

# Phase 26.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `26.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.4.x (uv-managed) |
| **Config file** | repo-root `pyproject.toml` |
| **Quick run command** | `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py tools/evidence/tests/test_capture.py -x` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~10s quick / ~120s full |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py tools/evidence/tests/test_capture.py -x`
- **After every plan wave:** Run `uv run pytest -q`
- **Before `/gsd:verify-work`:** Full suite must be green AND `uv run python -m tools.contract_drift.drift` clean AND `checkpoint:human-verify` resume-signal received
- **Max feedback latency:** 15 seconds (quick run)

*Repo lesson carried from `26-01-SUMMARY.md`: a plan's own `<verify>` steps checking `contract_drift` + the `stale-derived` mirror is NOT equivalent to a green full test suite.*

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 26.1-01-xx | 01 | 0 | ADOPT-01 / SC-1 | — | Real `.github/workflows/ci.yml` false-positive line is not classified `secret-content` | unit (red→green) | `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k ci_yml -x` | ❌ W0 | ⬜ pending |
| 26.1-01-xx | 01 | 0 | ADOPT-01 / SC-2 | — | All 7 named secret shapes still matched (no weakening of detection) | unit, parametrized (7 cases) | `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k secret_shape -x` | ❌ W0 | ⬜ pending |
| 26.1-01-xx | 01 | 1 | ADOPT-01 / SC-1+SC-2 | — | Tightened `secret_patterns[1]` regex lands in `gate-registry.json`; both consumers pick it up as data | unit | `uv run pytest tools/adoption_scan/tests/ tools/evidence/tests/ -x` | ✅ | ⬜ pending |
| 26.1-01-xx | 01 | 1 | ADOPT-01 / SC-3 | — | Contract edit + hash rebaseline + derived-plane regen are atomic; drift clean | integration | `uv run python -m tools.contract_drift.drift` (exit 0) | ✅ | ⬜ pending |
| 26.1-01-xx | 01 | 2 | ADOPT-01 / SC-4 | — | Constitution-plane change human-ratified, not agent-self-approved | manual-only | `checkpoint:human-verify` blocking task | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tools/adoption_scan/tests/test_scan_exclusions.py` — new test proving SC-1 as a genuine red→green: the OLD pattern matches the real `.github/workflows/ci.yml` line, the NEW pattern does not. The existing `tmp_minirepo` synthetic fixture does **not** reproduce the bug, so a passing assertion against it is not proof.
- [ ] `tools/adoption_scan/tests/test_scan_exclusions.py` — new parametrized test (7 cases) proving SC-2: each unchanged named-shape pattern still matches a data-based positive literal. Literals MUST be built via string concatenation — this repo's own PreToolUse `secret_scan` hook refuses writes containing literal secret shapes (reproduced live during research).
- [ ] Optional belt-and-suspenders: extend `tmp_minirepo`'s `ci.yml` fixture in `conftest.py` with a comparably-shaped benign line so `test_no_spurious_exclusions` / `test_exclusion_reason` also exercise the fix at the synthetic level.
- [ ] Confirm whether `tools/adoption_scan/tests/test_snapshots.py` / its `.ambr` snapshot needs updating (RESEARCH Open Question 1).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Constitution-plane ratification of the `gate-registry.json` edit | ADOPT-01 / SC-4 | Machines gate, humans ratify — an agent cannot self-approve a constitution-plane change (gate-model, exit-3 refusal semantics) | `checkpoint:human-verify` blocking task per `26-01-PLAN.md` precedent: human reviews the tightened regex + the 7 positive cases + the SC-1 negative case, then issues the resume signal. CODEOWNERS review on the PR. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — both autonomous tasks in `26.1-01-PLAN.md` carry automated verify commands
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — 2/2 autonomous tasks verified
- [x] Wave 0 covers all MISSING references — Task 1 IS the Wave 0 test-authoring step, folded into the same single-wave plan
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-20 (plan-verification pass)

> Note: this sign-off covers *validation-strategy* compliance only. It is **not** the SC-4
> constitution-plane ratification — that remains the blocking `checkpoint:human-verify` task in
> `26.1-01-PLAN.md`, which only a human may release.
