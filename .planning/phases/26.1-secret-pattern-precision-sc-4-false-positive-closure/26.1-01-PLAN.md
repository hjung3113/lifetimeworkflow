---
phase: 26.1-secret-pattern-precision-sc-4-false-positive-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/adoption_scan/tests/test_scan_exclusions.py
  - tools/adoption_scan/tests/conftest.py
  - contracts/harness/task-control/gate-registry.json
  - contracts/.hashes/manifest.json
  - .memory/derived/contracts-index.md
autonomous: false
requirements: [ADOPT-01]

must_haves:
  truths:
    - "SC-1: scan._secret_pattern() does not match this repo's own .github/workflows/ci.yml; the file appears in the inventory rather than as excluded: \"secret-content\""
    - "SC-2: every one of the 7 unchanged named secret shapes (AWS key id, GitHub PAT prefixes, sk- keys, Slack tokens, PEM private-key header, JWT, Authorization: Bearer) is still matched — proven by one data-based positive test case per shape"
    - "SC-3: the gate-registry.json edit, contracts/.hashes/manifest.json rebaseline, and derived-plane regeneration (tools.docs_sync + tools.memory_regen.contracts_index) land in one commit; tools.contract_drift.drift is clean at that commit"
    - "SC-4: the constitution-plane change is human-ratified via a blocking checkpoint, not agent-self-approved"
    - "Red-green proof exists: the OLD pattern (?:api[_-]?key|secret|password|token)\\s*[:=]\\s*[^\\s]+ demonstrably matches the real ci.yml false-positive line, and the NEW pattern demonstrably does not — not just a post-fix assertion"
  artifacts:
    - path: "tools/adoption_scan/tests/test_scan_exclusions.py"
      provides: "test_ci_yml_false_positive_closed (SC-1 red-green proof) and test_secret_shape_still_matches (SC-2, 7-case parametrized proof)"
      contains: "def test_ci_yml_false_positive_closed"
    - path: "contracts/harness/task-control/gate-registry.json"
      provides: "secret_patterns[1] tightened to require a secret-shaped value (min-length 20 + 3-way charset diversity)"
      contains: "A-Z"
    - path: "contracts/.hashes/manifest.json"
      provides: "Rebaselined JCS SHA-256 manifest reflecting the gate-registry.json content change"
    - path: ".memory/derived/contracts-index.md"
      provides: "Regenerated contracts index whose gate-registry.json row hash_prefix reflects the new content"
      contains: "gate-registry.json"
  key_links:
    - from: "contracts/harness/task-control/gate-registry.json"
      to: "tools/adoption_scan/scan.py::_secret_pattern"
      via: "json.loads read of the secret_patterns array, no code change"
      pattern: "secret_patterns"
    - from: "contracts/harness/task-control/gate-registry.json"
      to: "tools/evidence/capture.py::_sensitive_pattern"
      via: "json.loads read of the same secret_patterns array (second consumer, unaffected by this plan's code but must be re-tested)"
      pattern: "secret_patterns"
---

<objective>
Close the SC-4 false-positive carried forward from Phase 26: tighten `secret_patterns[1]` in
`contracts/harness/task-control/gate-registry.json` — the generic
`(?:api[_-]?key|secret|password|token)\s*[:=]\s*[^\s]+` value matcher — so it requires a
secret-shaped value (minimum length 20 plus 3-way charset diversity) instead of matching any
non-space run, closing the false positive on this repo's own `.github/workflows/ci.yml` while
proving every one of the 7 other named secret shapes in the registry still matches.

Purpose: Phase 27 broadens secret classification to arbitrary brownfield targets; shipping that on
top of an over-matching pattern means silent, unreviewable over-exclusion of real files on user
repos. This plan closes the gap before Phase 27 builds on it.
Output: a red-green test pair proving the fix, the tightened contract, a rebaselined hash
manifest, a regenerated derived plane, and a human-ratified constitution-plane commit.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26.1-secret-pattern-precision-sc-4-false-positive-closure/26.1-RESEARCH.md
@.planning/phases/26.1-secret-pattern-precision-sc-4-false-positive-closure/26.1-PATTERNS.md
@.planning/phases/26.1-secret-pattern-precision-sc-4-false-positive-closure/26.1-VALIDATION.md
@.planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-01-PLAN.md
@tools/adoption_scan/scan.py
@tools/adoption_scan/tests/test_scan_exclusions.py
@tools/adoption_scan/tests/conftest.py
@contracts/harness/task-control/gate-registry.json
@tools/contract_hash/hash.py
@tools/contract_drift/drift.py
@tools/docs_sync/generate.py
@tools/memory_regen/contracts_index.py
@.github/CODEOWNERS
@.github/workflows/ci.yml

<interfaces>
<!-- Consumers this plan does NOT modify — both read secret_patterns as pure JSON data. No code
     change required in either. -->
`tools/adoption_scan/scan.py:109-113` — `_secret_pattern()`: reads
`_GATE_REGISTRY_PATH = _REPO_ROOT / "contracts" / "harness" / "task-control" / "gate-registry.json"`,
`json.loads` it, joins `registry["secret_patterns"]` into one `(?:p1|p2|...)` alternation via
`re.compile(..., re.IGNORECASE)`, cached with `functools.lru_cache(maxsize=1)`. Called from
`classify_exclusions()` (line ~262) against `text_prefix` (first 64 KiB of file content); on match
returns `{"path": rel, "size": size, "excluded": "secret-content"}`.

`tools/evidence/capture.py:94-100` — `_sensitive_pattern()`: same array, same alternation-join
idiom, no caching (re-reads per call), used by `_refuse_if_sensitive()`.

Ratification/regen entrypoints this plan invokes verbatim (no re-implementation, mirrors
`26-01-PLAN.md` Task 2/3 exactly):
`write_manifest(manifest_path=MANIFEST_PATH, contracts_dir=CONTRACTS_DIR) -> Path` in
`tools/contract_hash/hash.py`; CLI `python -m tools.contract_hash.hash --write`.
`run_gate(contracts_dir=CONTRACTS_DIR, baseline_path=MANIFEST_PATH) -> dict` in
`tools/contract_drift/drift.py`; CLI `python -m tools.contract_drift.drift` (exit 0 iff clean).
`python -m tools.docs_sync` (expect a NO-OP diff for this specific file — `iter_schemas()` globs
`*.schema.json` only, and `gate-registry.json` is a plain `.json` data contract, not a schema —
still run it, per the committed SC-3 procedure).
`python -m tools.memory_regen.contracts_index` (DOES change — `gate-registry.json` is one of the
two `DATA_CONTRACT_PATHS` entries in `tools/contract_hash/hash.py:30-33`, so its
`hash_prefix` row in `.memory/derived/contracts-index.md` updates).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the red-green SC-1 test and the SC-2 seven-shape regression test</name>
  <files>
    tools/adoption_scan/tests/test_scan_exclusions.py,
    tools/adoption_scan/tests/conftest.py
  </files>
  <read_first>
    tools/adoption_scan/tests/test_scan_exclusions.py (full — existing style: plain assert,
      parametrize shape, tmp_minirepo fixture usage, the existing
      test_derived_marker_does_not_false_positive_on_ordinary_prose /
      test_derived_marker_still_catches_real_generated_headers red/green pairing at lines 44-80,
      which this task's SC-1 test mirrors structurally),
    tools/adoption_scan/tests/conftest.py (full — the `(l)` CI-surface block that writes
      `.github/workflows/ci.yml` inside `tmp_minirepo`, and the `(b) secret-by-content`
      string-concatenation convention comment),
    tools/adoption_scan/scan.py lines 108-121 (`_secret_pattern()`) and lines 290+
      (`build_inventory()`, the function the SC-1 test calls),
    .github/workflows/ci.yml line 29 (the real false-positive source line — a short benign
      GITHUB_TOKEN comment; read it directly, do not reproduce it as a contiguous literal
      anywhere in test source — build it via string concatenation),
    26.1-PATTERNS.md "New test 1" and "New test 2" sections (exact test bodies to adapt),
    26.1-RESEARCH.md "Pitfall 1" (secret-literal write-time refusal — every SC-2 fixture literal
      MUST be built via concatenation, never a contiguous secret-shaped run) and "Pitfall 4"
      (why the existing test_no_spurious_exclusions does not currently prove SC-1)
  </read_first>
  <behavior>
    - test_ci_yml_false_positive_closed: reads the REAL repo's own `.github/workflows/ci.yml`
      (via the `repo_root` fixture already defined in conftest.py, not tmp_minirepo). It asserts
      TWO things: (1) the OLD pattern string
      `(?:api[_-]?key|secret|password|token)\s*[:=]\s*[^\s]+` (compiled inline in the test,
      case-insensitive) DOES match the file's text — this is the red-proof that the bug is real;
      (2) `scan.build_inventory(repo_root)`'s `excluded` list contains NO entry whose `path` is
      `.github/workflows/ci.yml` with `excluded == "secret-content"` — this is the fix proof,
      checked against the LIVE `_secret_pattern()` (reads the real, not-yet-edited registry at
      the moment this task runs, so this half of the assertion is expected to FAIL until Task 2
      lands — that is the intended red state for this task).
    - test_secret_shape_still_matches: a `@pytest.mark.parametrize`d test over the 7 unchanged
      named-shape literals from 26.1-PATTERNS.md's "New test 2" (AWS key-id shape, GitHub PAT
      prefix, `sk-` prefix, Slack `xoxb-` prefix, PEM begin-header, 3-segment JWT, Authorization
      Bearer header) — every literal built via string concatenation (`"AKIA" + "ABCDEFGHIJKLMNOP"`
      style), never a contiguous secret-shaped run in the source file. Each case asserts
      `scan._secret_pattern().search(fixture_value)` is truthy. This test is expected to PASS
      immediately (these 7 shapes are untouched by the coming edit) — it is the SC-2 regression
      guard, not a red-green pair.
    - Optional belt-and-suspenders (conftest.py): extend `tmp_minirepo`'s existing `ci.yml`
      fixture content (the `(l)` CI-surface block, currently
      `"name: test\non: [push]\njobs: {}\n"`) with one additional short comment line shaped like
      the real repo's false-positive (a `GITHUB_TOKEN:`-style comment with a short benign value,
      built via string concatenation per the file's own `(b)` convention), so
      `test_no_spurious_exclusions` also exercises the fix at the synthetic level once Task 2
      lands.
  </behavior>
  <action>
    Add `import re` to the top of `test_scan_exclusions.py` (currently imports `json`, `Path`,
    `pytest`, `scan` — no `re`). Add `test_ci_yml_false_positive_closed(repo_root: Path)` and the
    parametrized `test_secret_shape_still_matches(fixture_value: str)` as new module-level
    functions, following the read/behavior spec above. Do not modify any existing test function.
    In `conftest.py`, if implementing the optional belt-and-suspenders extension, change only the
    single `write_text` call in the `(l)` CI-surface block to append the additional comment line
    via string concatenation — leave every other fixture block untouched.
  </action>
  <verify>
    <automated>uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k "secret_shape" -x</automated>
  </verify>
  <acceptance_criteria>
    `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k secret_shape -x` exits 0
    (all 7 SC-2 cases pass against the CURRENT, unedited registry). `uv run pytest
    tools/adoption_scan/tests/test_scan_exclusions.py -k ci_yml -x` FAILS at this point (red state
    — proves the bug is real and the test genuinely exercises it, per Pitfall 4). Neither new test
    function's source contains a contiguous secret-shaped literal (grep the new test code for
    `AKIA[A-Z]` immediately followed by 16+ uppercase letters, or `sk-` immediately followed by
    16+ alnum chars, finds nothing — every literal is built via `+` concatenation).
  </acceptance_criteria>
  <done>Both new tests exist; the SC-2 seven-shape test is green against the unedited registry; the SC-1 test is red (expected), proving it genuinely reproduces the bug before Task 2's fix lands.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten secret_patterns[1], rebaseline hash, verify drift, regenerate derived plane</name>
  <files>
    contracts/harness/task-control/gate-registry.json,
    contracts/.hashes/manifest.json,
    .memory/derived/contracts-index.md
  </files>
  <read_first>
    contracts/harness/task-control/gate-registry.json (full — 20 lines; the target is index 1 of
      the 8-element secret_patterns array, currently
      `"(?:api[_-]?key|secret|password|token)\\s*[:=]\\s*[^\\s]+"`; indices 0 and 2-7 must stay
      byte-identical),
    tools/contract_hash/hash.py (`DATA_CONTRACT_PATHS`, `write_manifest`/`build_manifest` — confirms
      gate-registry.json is a tracked data contract, hashed via JCS canonicalization),
    tools/contract_drift/drift.py (`run_gate` — the exit-0-iff-clean CLI),
    tools/docs_sync/generate.py (`iter_schemas()` globs `*.schema.json` only — confirms this run
      is a documented no-op for docs/reference, per 26.1-RESEARCH.md Pitfall 3),
    tools/memory_regen/contracts_index.py (`index_rows`/`build_manifest` reuse — confirms this run
      DOES change the gate-registry.json row's hash_prefix),
    26.1-RESEARCH.md "Command Sequence" and "Pitfall 2" (drift is RED between edit and rebaseline
      — expected, not a bug) and "Pitfall 3" (docs_sync no-op for this file — expected)
  </read_first>
  <action>
    Edit ONLY `secret_patterns[1]` in `contracts/harness/task-control/gate-registry.json`,
    replacing the current value with the exact JSON string:
    `(?:api[_-]?key|secret|password|token)\s*[:=]\s*(?=[^\s]{20,})(?=[^\s]*[A-Z])(?=[^\s]*[a-z])(?=[^\s]*[0-9])[^\s]{20,}`
    (JSON-escaped form: backslashes doubled per standard JSON string escaping, matching the
    existing file's own escaping convention for the other 7 patterns). This requires the matched
    value to be at least 20 non-space characters containing at least one uppercase letter, one
    lowercase letter, and one digit (RESEARCH.md Candidate B — the recommended charset-diversity
    proxy for "secret-shaped", verified in research to reject the real ci.yml false positive and
    3 other benign shapes while still matching 28-char mixed-alnum secret literals). Leave indices
    0 and 2-7 of the array, and the entire `gates` object, byte-identical. Writing under
    `contracts/**` requires a human-authorized `GOLDEN_APPROVE_HUMAN` token or an equivalent
    human-present write — do not fabricate the token; if the write is denied, pause and request
    human authorization.

    Then run, in this strict order: `uv run python -m tools.contract_hash.hash --write`
    (rebaselines `contracts/.hashes/manifest.json` — only the gate-registry.json entry's hash
    changes); `uv run python -m tools.contract_drift.drift` (confirm exit 0); `uv run python -m
    tools.docs_sync` (confirm no diff under `docs/reference/` — expected no-op for this file);
    `uv run python -m tools.memory_regen.contracts_index` (confirm the gate-registry.json row in
    `.memory/derived/contracts-index.md` changes). Finally re-run both Task 1 tests plus the
    existing suite scoped to the two consumer modules: `uv run pytest
    tools/adoption_scan/tests/test_scan_exclusions.py tools/evidence/tests/test_capture.py -x` —
    the SC-1 test must now be GREEN (it was red after Task 1), and the SC-2 test must still be
    GREEN.
  </action>
  <verify>
    <automated>uv run python -m tools.contract_drift.drift && uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py tools/evidence/tests/test_capture.py -x</automated>
  </verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0. `uv run pytest
    tools/adoption_scan/tests/test_scan_exclusions.py -k "ci_yml or secret_shape" -x` exits 0 (SC-1
    now green, SC-2 still green — full red-to-green transition proven). `uv run pytest
    tools/evidence/tests/test_capture.py -x` exits 0 (second consumer unregressed). `git diff
    --stat contracts/.hashes/manifest.json` shows exactly one entry's hash changed
    (`contracts/harness/task-control/gate-registry.json`). `git diff --cached --exit-code --
    docs/reference` shows no diff (documented no-op). `.memory/derived/contracts-index.md`'s
    `gate-registry.json` row hash_prefix differs from its pre-edit value.
  </acceptance_criteria>
  <done>secret_patterns[1] is tightened to the min-20 + 3-charset-class pattern; drift is clean; both consumers' test suites pass; the derived plane is regenerated and ready to land in the same commit as the contract edit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human ratification of the constitution-plane secret_patterns edit</name>
  <files>contracts/harness/task-control/gate-registry.json, contracts/.hashes/manifest.json, .memory/derived/contracts-index.md</files>
  <read_first>
    .github/CODEOWNERS (the /contracts/ routing + advisory-enforcement + solo-owner
      self-approval caveats),
    .planning/phases/26-deterministic-brownfield-inventory-mapping-v2-3-b/26-01-PLAN.md lines
      275-309 (the checkpoint task this one mirrors verbatim in shape)
  </read_first>
  <action>
    BLOCKING human checkpoint — the agent does NOT self-ratify. Present for review: (1) the exact
    diff of `secret_patterns[1]` in `contracts/harness/task-control/gate-registry.json` (old value
    vs. the new min-20 + 3-charset-class value); (2) the SC-1 red-to-green proof (Task 1's ci_yml
    test was red before Task 2, green after); (3) the SC-2 seven-shape regression proof (all 7
    named shapes still match); (4) the rebaselined `contracts/.hashes/manifest.json` diff (exactly
    one changed entry). Then PAUSE. Ratify via CODEOWNERS PR review or an explicit human-supplied
    write — never a fabricated `GOLDEN_APPROVE_HUMAN` token or `HARNESS_DEV_BYPASS` used to
    self-land the ratification.
  </action>
  <what-built>
    A tightened `secret_patterns[1]` regex in the constitution-plane `gate-registry.json`, proven
    in both directions (SC-1 false positive closed, SC-2's 7 real secret shapes still caught),
    with a rebaselined hash manifest and a regenerated derived plane ready to commit atomically.
  </what-built>
  <how-to-verify>
    1. Confirm `uv run python -m tools.contract_drift.drift` reports clean (Task 2's automated
       check).
    2. Review the diff under `contracts/harness/task-control/gate-registry.json` and
       `contracts/.hashes/manifest.json` — this is the CODEOWNERS-gated constitution-plane change
       (`.github/CODEOWNERS` routes `/contracts/` to the repo owner) that must be ratified at PR
       merge.
    3. Confirm `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k "ci_yml or
       secret_shape" -x` and `uv run pytest tools/evidence/tests/test_capture.py -x` are both
       green.
    4. Confirm no agent self-approved this change (no fabricated `GOLDEN_APPROVE_HUMAN` token
       committed anywhere; if `HARNESS_DEV_BYPASS` was used for the session write, confirm it is
       not conflated with ratification, per the Phase-26 precedent).
  </how-to-verify>
  <acceptance_criteria>
    `uv run python -m tools.contract_drift.drift` exits 0; `git diff --stat
    contracts/.hashes/manifest.json` shows exactly one changed entry
    (`contracts/harness/task-control/gate-registry.json`); the full relevant test scope
    (`tools/adoption_scan/tests/test_scan_exclusions.py`, `tools/evidence/tests/test_capture.py`)
    is green.
  </acceptance_criteria>
  <verify>
    <human-check>Human ratifies; then: uv run python -m tools.contract_drift.drift</human-check>
  </verify>
  <resume-signal>Type "approved" once the constitution-plane diff has been reviewed, or describe required changes.</resume-signal>
  <done>The secret_patterns[1] edit, the rebaselined manifest, and the regenerated derived plane are human-ratified via the standard constitution-plane path (CODEOWNERS review), not agent self-approval — ready for the one atomic commit (SC-3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent session → `contracts/harness/task-control/gate-registry.json` | Constitution-plane write; denied by `contract_guard` unless human-authorized (`GOLDEN_APPROVE_HUMAN`) or dev-bypassed — never agent self-ratified. |
| Untrusted repo file content → `_secret_pattern()` / `_sensitive_pattern()` | The regex is applied to arbitrary, potentially attacker-influenced file content (a brownfield target repo in Phase 27's later scope) to decide inventory inclusion/exclusion and evidence-write refusal. |
| `contract_hash --write` output → `contracts/.hashes/manifest.json` | Machine-computed baseline; must never be hand-edited. |
| `docs_sync` / `contracts_index` output → committed-derived plane | Machine-regenerated; CI's `stale-derived` job is the non-bypassable verify side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.1-01 | Information Disclosure | Tightened `secret_patterns[1]` under-matching a REAL secret, allowing it to be classified `included` instead of `excluded: "secret-content"` (false negative) | mitigate | This is the primary counterweight risk to this phase's own fix: loosening the false-positive-prone catch-all is, by construction, a narrowing of exclusion. Mitigated by SC-2's 7-case positive-data test suite (Task 1/2) proving every NAMED secret shape (AWS key id, GitHub PAT, `sk-` keys, Slack tokens, PEM header, JWT, Authorization Bearer) is on its own dedicated alternation branch, unaffected by index-1's narrowing — verified as a distinct, separate regression test, not asserted by inspection. The generic pattern's own residual catch-all still requires 20+ chars with 3-way charset diversity, matching common secret-scanner heuristics (gitleaks-style generic rules), so ad hoc `key: value` secrets without a dedicated shape are still caught unless they are short or single-charset. |
| T-26.1-02 | Denial of Service (weak — data availability, not the DoS resource-exhaustion sense) | Over-broad `secret_patterns[1]` (the pre-fix state) silently dropping real, non-secret files from the adoption inventory | mitigate | This is the bug this phase closes. Mitigated by the SC-1 red-green test pair (Task 1: red proof against the unedited pattern; Task 2: green proof against the tightened pattern) run against the REAL repo's own `.github/workflows/ci.yml`, not just a synthetic fixture. |
| T-26.1-03 | Elevation of Privilege | `contracts/harness/task-control/gate-registry.json` write path | accept (out-of-band) | Unchanged existing controls: `contract_guard` PreToolUse deny + `GOLDEN_APPROVE_HUMAN` + CODEOWNERS `/contracts/`. This plan adds no new bypass; the Task 3 `checkpoint:human-verify` is the explicit ratification surface (SC-4). Residual risk (CODEOWNERS is advisory unless branch protection is enabled, solo-owner self-approval nuance) is pre-accepted per the Phase-26/24 precedent. |
| T-26.1-04 | Tampering | `contracts/.hashes/manifest.json` | mitigate | Rebaseline ONLY via `tools.contract_hash.hash --write` (Task 2) — never hand-edited. Verified by `git diff --stat` showing exactly one changed entry. |
| T-26.1-SC | Tampering (supply chain) | N/A | accept | Zero new package installs in this plan (regex-only JSON edit + existing `tools.contract_hash`/`tools.contract_drift`/`tools.docs_sync`/`tools.memory_regen` CLIs only); no slopcheck required. |
</threat_model>

<verification>
- `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k "ci_yml or secret_shape" -x` exits 0 (SC-1 green post-fix, SC-2 green throughout).
- `uv run pytest tools/evidence/tests/test_capture.py -x` exits 0 (second consumer unregressed).
- `uv run python -m tools.contract_drift.drift` exits 0.
- `uv run python -m tools.docs_sync && uv run python -m tools.memory_regen.contracts_index && git diff --cached --exit-code -- docs/reference .memory/derived/contracts-index.md` — committed-derived plane fresh (docs/reference no-op expected, contracts-index diff expected to already be staged/committed).
- `uv run pytest -q` (full suite) green before phase gate, per the repo's own carried lesson (26-01-SUMMARY.md) that per-task checks are not equivalent to a green full suite.
</verification>

<success_criteria>
- `scan._secret_pattern()` does not match this repo's own `.github/workflows/ci.yml`; the file appears in the inventory rather than as `excluded: "secret-content"` (SC-1), proven by a genuine red-green test pair, not a fixture that happened to already pass.
- All 7 unchanged named secret shapes in the registry still match, each proven by a dedicated positive data-based test case (SC-2).
- The `gate-registry.json` edit, `contracts/.hashes/manifest.json` rebaseline, and derived-plane regeneration land together with `tools.contract_drift.drift` clean (SC-3).
- The constitution-plane change is human-ratified via the blocking checkpoint, not agent-self-approved (SC-4).
</success_criteria>

<output>
Create `.planning/phases/26.1-secret-pattern-precision-sc-4-false-positive-closure/26.1-01-SUMMARY.md` when done
</output>
