---
phase: 26.1-secret-pattern-precision-sc-4-false-positive-closure
verified: 2026-07-20T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 26.1: Secret-Pattern Precision — SC-4 False-Positive Closure Verification Report

**Phase Goal:** Tighten `secret_patterns[1]` in `contracts/harness/task-control/gate-registry.json`
(the generic `(?:api[_-]?key|secret|password|token)\s*[:=]\s*[^\s]+` value matcher) to require a
secret-shaped value instead of any non-space run, closing the SC-4 false positive on this repo's
own `.github/workflows/ci.yml`, while proving every real secret shape currently caught stays
caught. Contract-first: the edit ships with a hash rebaseline, derived-plane regeneration, and
human ratification in one atomic commit.

**Verified:** 2026-07-20
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1: `scan._secret_pattern()` does not match `.github/workflows/ci.yml`; file appears in inventory, not excluded as `secret-content` | VERIFIED | Live re-derivation (not SUMMARY claim): compiled the OLD pattern string inline and confirmed it matches `TOKEN: gate` in the real file (`old match: True`, matched text `'TOKEN: gate'`); compiled the NEW (shipped) pattern and confirmed no match (`new match: False`). Ran `scan.build_inventory(Path('.'))` against the live, committed registry — `excluded` list contains zero entries for `ci.yml`. `uv run pytest ... -k ci_yml -x` passes. |
| 2 | SC-2: all 7 unchanged named secret shapes still matched, proven by data-based positive cases | VERIFIED | `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k secret_shape -v` — 7/7 parametrized cases pass (AWS AKIA id, `ghp_` GitHub PAT, `sk-` key, `xoxb-` Slack token, PEM `-----BEGIN...PRIVATE KEY-----` header, `eyJ...` JWT, `Authorization: Bearer ...`). Test literals built via string concatenation, no contiguous secret-shaped literal found in source (grepped test file, none). |
| 3 | SC-3: gate-registry.json edit + hash rebaseline + derived-plane regen land in one commit; drift clean | VERIFIED | `git show d6e9054 --stat` shows `contracts/harness/task-control/gate-registry.json`, `contracts/.hashes/manifest.json`, and `.memory/derived/contracts-index.md` all modified together in one commit, plus the collateral test/snapshot updates. `contracts/.hashes/manifest.json` diff shows exactly one changed entry (`gate-registry.json` hash `04a9d3bf...` → `1d212f2f...`). `uv run python -m tools.contract_drift.drift` run live → `contract-drift: OK` exit 0. `.memory/derived/contracts-index.md` row `hash_prefix` updated to `1d212f2f56d8` matching the new hash. |
| 4 | SC-4: constitution-plane change is human-ratified, not agent-self-approved | VERIFIED (documentary, cross-checked) | `26.1-01-PLAN.md` Task 3 is a `checkpoint:human-verify gate="blocking"` task (not `auto`), explicitly instructing "the agent does NOT self-ratify" and forbidding a fabricated `GOLDEN_APPROVE_HUMAN` token or using `HARNESS_DEV_BYPASS` to self-land ratification. `STATE.md:233` and `SUMMARY.md` both independently record the ratification as a human "approved"/"승인" reply, using the exact same phrasing convention as the two prior Phase 26 ratification events (`STATE.md:221`, `STATE.md:231`) that this repo has established for this pattern. Grepped all five phase commits (`2ef443d`, `d6e9054`, `90a4dcf`, `0c6a7cc`, `4d6b1fb`) for `GOLDEN_APPROVE_HUMAN` / self-approval tokens — none found; only prose references distinguishing the ratification from `HARNESS_DEV_BYPASS`. `.claude/settings.local.json` confirms `HARNESS_DEV_BYPASS=1` exists as a distinct, gitignored dev-write-permission setting, consistent with the plan's own warning not to conflate it with ratification. No code-path or commit makes the agent itself the ratifying party. |
| 5 | Red-green proof exists (not just a post-fix assertion) | VERIFIED | `test_ci_yml_false_positive_closed` asserts the OLD pattern (compiled inline in the test) matches the real file text FIRST, then asserts the live inventory excludes nothing for `ci.yml` — this is a genuine two-part red/green structure, not a fixture that happened to already pass. Independently re-derived the same red/green split outside the test suite via a standalone Python check (see truth #1 evidence). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tools/adoption_scan/tests/test_scan_exclusions.py` | `test_ci_yml_false_positive_closed` + `test_secret_shape_still_matches` (7-case) | VERIFIED | Both functions present, both pass live (`8 passed` for `-k "ci_yml or secret_shape"`). |
| `contracts/harness/task-control/gate-registry.json` | `secret_patterns[1]` tightened to min-20 + 3-way charset diversity | VERIFIED | Live file shows `(?:api[_-]?key|secret|password|token)\\s*[:=]\\s*(?=[^\\s]{20,})(?=[^\\s]*[A-Z])(?=[^\\s]*[a-z])(?=[^\\s]*[0-9])[^\\s]{20,}` at index 1; indices 0, 2-7, and `gates` unchanged (confirmed by reading the full file). |
| `contracts/.hashes/manifest.json` | Rebaselined hash for the new content | VERIFIED | Diff shows exactly one changed line, `drift` gate clean against this baseline. |
| `.memory/derived/contracts-index.md` | Regenerated row hash_prefix | VERIFIED | Row for `gate-registry.json` shows `1d212f2f56d8`, matching the new manifest hash prefix. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `contracts/harness/task-control/gate-registry.json` | `tools/adoption_scan/scan.py::_secret_pattern` | JSON read of `secret_patterns` array | VERIFIED | `scan.build_inventory()` executed live against the current repo picks up the new pattern (confirmed via the standalone `old match / new match` check and the passing `ci_yml` test). |
| `contracts/harness/task-control/gate-registry.json` | `tools/evidence/capture.py::_sensitive_pattern` | Same JSON array, second consumer | VERIFIED | `tools/evidence/tests/test_capture.py` fixture updated in `d6e9054` to a 20+ char mixed-charset value so it still satisfies the tightened pattern by design; full suite run confirms this file's tests pass (`1039 passed`, no failures in `tools/evidence`). |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| SC-1 old/new pattern behavior on real ci.yml | Standalone Python re-derivation of both regexes against `.github/workflows/ci.yml` | `old match: True` (`'TOKEN: gate'`), `new match: False` | PASS |
| SC-1 live inventory exclusion | `scan.build_inventory(Path('.'))`, filtered for `ci.yml` | `[]` (no exclusion entries) | PASS |
| SC-1/SC-2 targeted tests | `uv run pytest tools/adoption_scan/tests/test_scan_exclusions.py -k "secret_shape or ci_yml" -v` | 8 passed | PASS |
| SC-3 drift gate | `uv run python -m tools.contract_drift.drift` | `contract-drift: OK — live manifest matches the committed baseline.` exit 0 | PASS |
| Full regression suite | `uv run pytest -q` | 1039 passed | PASS |
| No fabricated ratification token | `git show` of all 5 phase commits, grep for `GOLDEN_APPROVE_HUMAN` | Only prose distinguishing ratification from `HARNESS_DEV_BYPASS`, no token usage | PASS |

Note: `uv run python -m tools.docs_sync` was also run live as part of SC-3 re-derivation; it correctly no-ops for `gate-registry.json` (data contract, not a `.schema.json`) as the plan predicted. It incidentally rewrote `docs/reference/*.md` (unrelated pre-existing docs-sync churn) and flipped an unrelated `.planning/config.json` flag (`_auto_chain_active`) as a side effect of running the tool — both were reverted with `git checkout` after inspection so as not to leave stray changes from this verification pass.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ADOPT-01 | 26.1-01-PLAN.md | Deterministic local-root inventory excludes secrets without over-excluding benign prose/config | SATISFIED | `REQUIREMENTS.md:78` already marks ADOPT-01 complete from Phase 26; this phase is a declared refinement (`requirements: [ADOPT-01]` in PLAN frontmatter) closing a false-positive carried forward from that phase. No orphaned requirements — ADOPT-01 is the only ID declared and it is accounted for. |

### Anti-Patterns Found

None. Grepped all files modified by this phase (`test_scan_exclusions.py`, `conftest.py`,
`gate-registry.json`, `test_capture.py`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` — zero matches.

### Human Verification Required

None — SC-4's human-ratification requirement was already discharged synchronously within the
phase's own blocking `checkpoint:human-verify` task during execution (per the launching agent's
account and cross-checked documentary trail in `STATE.md`/`SUMMARY.md`), not deferred to this
verification pass.

### Gaps Summary

No gaps. All 5 must-have truths independently re-derived from source/git/live execution (not
taken from SUMMARY.md claims): the red/green pattern behavior was recompiled and re-tested
standalone, the live inventory exclusion was recomputed, the full test suite (1039 tests) and the
contract-drift gate were re-run fresh, the one-commit atomicity of the SC-3 artifacts was
confirmed via `git show --stat` and diff inspection, and the SC-4 ratification claim was
cross-checked against the plan's blocking-checkpoint structure and the repo's established
ratification-documentation convention (STATE.md) with no fabricated-token evidence found.

---

*Verified: 2026-07-20*
*Verifier: Claude (gsd-verifier)*
