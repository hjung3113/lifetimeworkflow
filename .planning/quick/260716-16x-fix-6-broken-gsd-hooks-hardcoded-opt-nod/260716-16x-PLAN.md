---
quick_id: 260716-16x
type: execute
status: complete
completed: 2026-07-16
wave: 1
depends_on: []
files_modified:
  - .claude/settings.json
autonomous: true
requirements: [QUICK-260716-16x]

must_haves:
  truths:
    - "No GSD hook command hardcodes a bare /opt/node22/bin/node interpreter"
    - "Every node-based GSD hook resolves a real node on this macOS host"
    - "A GSD hook invoked end-to-end runs without 'no such file or directory'"
    - "The fix stays portable to the remote Linux container (no macOS-only path)"
    - ".claude/settings.json remains valid JSON with key order and indentation unchanged"
  artifacts:
    - path: ".claude/settings.json"
      provides: "6 GSD hook commands with portable interpreter resolution"
      contains: "command -v node"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/*.js"
      via: "shell command substitution resolving node"
      pattern: "command -v node \\|\\| echo /opt/node22/bin/node"
---

<objective>
Repair the 6 GSD hooks in `.claude/settings.json` that hardcode `/opt/node22/bin/node` — a path
baked in from the remote Linux dev container that does not exist on macOS. The hooks fail silently
(exit 0), so GSD's guard rails (workflow enforcement, read-before-write, prompt-injection scanning)
have been inert on this host.

Purpose: restore the guard rails without inverting the bug (a macOS-only path would break the
container). Mirror the precedent already established in `.claude/hooks/memory-inject.sh:18-20`:
prefer PATH `node`, fall back to `/opt/node22/bin/node`.

Output: `.claude/settings.json` with 6 command strings edited in place — nothing else touched.
</objective>

<execution_context>
@/home/user/lifetimeworkflow/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/lifetimeworkflow/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/settings.json

<precedent>
`.claude/hooks/memory-inject.sh:18-20` — the ONE node-touching hook that still works, and the
convention to mirror. Do NOT edit this file; it is the reference, not a target.

```sh
# node: existing hooks use /opt/node22; fall back to PATH `node`.
NODE=node
command -v node >/dev/null 2>&1 || NODE=/opt/node22/bin/node
```
</precedent>

<safety_note>
`_merge_settings_json` in `tools/harness_emit/generate.py:270-288` append-or-replaces ONLY
harness-signature hook groups and "never touch/reorder GSD hooks". These 6 are GSD hooks, so a
`harness_emit` re-emit will NOT revert this fix. That function also deliberately omits `sort_keys`
because the file is insertion-ordered (SessionStart-first) and a global sort flaps ~274 lines —
which is exactly why this plan forbids re-serializing the file.
</safety_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the hardcoded interpreter in all 6 GSD hook commands</name>
  <files>.claude/settings.json</files>
  <action>
Using the Edit tool (NEVER Write — a whole-file rewrite produces an unreviewable diff), replace the
bare interpreter token `"/opt/node22/bin/node"` with `"$(command -v node || echo /opt/node22/bin/node)"`
in exactly these 6 command strings. Hook commands run through a shell, so command substitution is
evaluated. In JSON the inner quotes stay escaped, so each edited command reads:

    "$(command -v node || echo /opt/node22/bin/node)" "$CLAUDE_PROJECT_DIR"/.claude/hooks/<name>.js

written in the file as `\"$(command -v node || echo /opt/node22/bin/node)\" \"$CLAUDE_PROJECT_DIR\"/.claude/hooks/<name>.js`.

The 6 targets (line numbers from the current file; each is a distinct `command` value):
  - L8   SessionStart `*`                                   → gsd-check-update.js
  - L43  PostToolUse `Bash|Edit|Write|MultiEdit|Agent|Task`  → gsd-context-monitor.js
  - L53  PostToolUse `Read`                                  → gsd-read-injection-scanner.js
  - L85  PreToolUse `Write|Edit`                             → gsd-prompt-guard.js
  - L95  PreToolUse `Write|Edit`                             → gsd-read-guard.js
  - L105 PreToolUse `Write|Edit`                             → gsd-workflow-guard.js

Because all 6 share an identical interpreter prefix, each Edit's `old_string` MUST include the
trailing script filename to stay unique. Do not touch: the `bash ...` hooks, the `uv run python ...`
hooks, `timeout` values, key order, or indentation. Do not add or remove any keys.
  </action>
  <verify>
    <automated>test "$(grep -c '"/opt/node22/bin/node\\"' .claude/settings.json)" -eq 0 && test "$(grep -c 'command -v node || echo /opt/node22/bin/node' .claude/settings.json)" -eq 6 && python3 -c "import json;json.load(open('.claude/settings.json'))" && git diff --numstat .claude/settings.json | awk '{exit !($1==6 && $2==6)}'</automated>
  </verify>
  <done>
Bare hardcoded-interpreter form gone (count 0); the fallback expression appears exactly 6 times;
file is still valid JSON; `git diff --numstat` shows exactly 6 insertions and 6 deletions — proving
only the 6 command strings changed and the file was not re-serialized.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the hooks actually execute (behavioral gate)</name>
  <files>.claude/settings.json</files>
  <action>
Task 1 only proves the string changed — a grep-only check would pass even if the hook still failed
to run. Prove execution behaviorally.

For each of the 6 hook scripts, extract the `command` from settings.json and resolve the interpreter
the same way the hook does, then assert it is a real node (`v22.x`). Then invoke at least one real
guard hook end-to-end over stdin with a representative Claude hook payload and assert its stderr
contains no `no such file or directory`.

Reference smoke invocation (already confirmed to exit 0 on this host):

    echo '{"tool_name":"Write","tool_input":{"file_path":"/tmp/x.txt","content":"y"},"cwd":"'"$PWD"'"}' \
      | "$(command -v node || echo /opt/node22/bin/node)" .claude/hooks/gsd-read-guard.js

A guard hook may legitimately exit non-zero when it decides to BLOCK — that is a working hook, not a
failure. Gate on the absence of the interpreter error, not on the exit code. Also confirm all 6
referenced `.js` files exist on disk, so a renamed script cannot masquerade as a fixed hook.

Report findings in the summary. Do NOT attempt to fix the red `tools/harness_emit` snapshot test —
that failure is inherited from Phase 12's deferred re-emit and belongs to Phase 15.
  </action>
  <verify>
    <automated>set -e; NODE="$(command -v node || echo /opt/node22/bin/node)"; "$NODE" --version | grep -q '^v22\.'; for f in gsd-check-update gsd-context-monitor gsd-read-injection-scanner gsd-prompt-guard gsd-read-guard gsd-workflow-guard; do test -f ".claude/hooks/$f.js"; done; OUT=$(echo "{\"tool_name\":\"Write\",\"tool_input\":{\"file_path\":\"/tmp/x.txt\",\"content\":\"y\"},\"cwd\":\"$PWD\"}" | "$NODE" .claude/hooks/gsd-read-guard.js 2>&1 || true); echo "$OUT" | grep -q 'no such file or directory' && exit 1; exit 0</automated>
  </verify>
  <done>
Resolved interpreter prints `v22.x`; all 6 hook scripts exist; a real guard hook runs end-to-end
without emitting `no such file or directory`. The guard rails are live on this host.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.json → shell | Hook command strings are evaluated by a shell before exec |
| host env → interpreter resolution | `PATH` decides which `node` binary runs the guard hooks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16x-01 | Elevation of Privilege | `$(command -v node ...)` in hook command | accept | Substitution contains only a literal, no user/tool-controlled input; `command -v` is a shell builtin. Same trust level as the existing `bash`/`uv run` hooks already in this file. |
| T-16x-02 | Tampering | `PATH`-resolved `node` | accept | An attacker who can prepend to `PATH` already controls the session; this is the same exposure as the `uv run python` hooks and the `memory-inject.sh` precedent. |
| T-16x-03 | Denial of Service | silent hook no-op (the bug itself) | mitigate | Task 2's behavioral gate asserts a hook actually executes — a string-only fix cannot pass. |
| T-16x-SC | Tampering | package installs | n/a | No packages installed; JSON config edit only. |
</threat_model>

<verification>
1. `grep -c '"/opt/node22/bin/node\"' .claude/settings.json` → `0` (bare form gone)
2. `python3 -c "import json; json.load(open('.claude/settings.json'))"` → exits 0
3. `git diff --numstat .claude/settings.json` → `6	6	.claude/settings.json` (no re-serialization)
4. `"$(command -v node || echo /opt/node22/bin/node)" --version` → `v22.x`
5. A real guard hook runs over stdin without `no such file or directory`
6. `git diff --stat` touches ONLY `.claude/settings.json`
</verification>

<success_criteria>
- All 6 GSD hooks resolve a working interpreter on macOS AND remain valid in the Linux container
- `.claude/settings.json` diff is exactly 6 changed lines; key order and indentation preserved
- No changes to `memory-inject.sh`, `harness/`, or `.planning/**-RESEARCH.md`
- The inherited `tools/harness_emit` snapshot failure is untouched (still 1 failed / 46 passed)
</success_criteria>

<output>
Create `.planning/quick/260716-16x-fix-6-broken-gsd-hooks-hardcoded-opt-nod/260716-16x-SUMMARY.md` when done
</output>
</content>
</invoke>
