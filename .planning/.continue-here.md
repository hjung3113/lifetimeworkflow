---
context: default
phase: none (milestone boundary — v2.0 opened)
milestone: v2.0 Long-Horizon
task: 0
total_tasks: 3
status: paused
last_updated: 2026-07-12T11:24:02.369Z
---

# Continue Here — Milestone v2.0 Long-Horizon (just opened)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `phases.clear` on continued numbering | The new-milestone workflow's Step 6 calls `gsd-sdk query phases.clear --confirm`. With continued numbering (9/10/11) and no archive target, this DELETES the executed phase 1–8 records that v2.0 builds on. | advisory | Only run `phases.clear` in the `--reset-phase-numbers` path with a valid `phase_archive_path`. When continuing numbering, skip it (done this session). |

<current_state>
Milestone **v2.0 Long-Horizon** is fully opened, committed, and pushed to `origin/claude/data-pipeline-harness-8aypct`. Git tree is clean. **No phase is in execution** — this is a milestone-boundary pause. v1.0 (phases 1–8) is complete; v2.0 adds phases 9/10/11 (numbering continued). Nothing is planned yet — next step is to plan Phase 9.
</current_state>

<completed_work>

This session opened v2.0 end-to-end:
- Confirmed v1.0 complete (8/8 phases, Phase-7 emitter verified 11/11) → the "open v2 only after Phase 7 ships" sequencing gate is satisfied.
- PROJECT.md: added `## Current Milestone: v2.0 Long-Horizon` + active MAINT/ECON/MREPO requirements + updated footer. (commit 3ae3640)
- STATE.md: reset to v2.0 `planning` via `gsd-sdk state.milestone-switch` (Accumulated Context preserved). (3ae3640)
- REQUIREMENTS.md: defined 11 v2.0 requirements (MAINT-01..04, ECON-01..03, MREPO-01..04); renamed old "v2 Requirements" backlog → "Future / Deferred" to remove the naming collision with milestone v2.0. (e2b3a6b)
- Removed consumed MILESTONE-CONTEXT.md. (324847c)
- Roadmapper appended phases 9/10/11 to ROADMAP.md, updated STATE.md + REQUIREMENTS.md traceability (11/11 mapped 1:1, v1.0 rows untouched). (81f8c61)
- Pushed the branch.
</completed_work>

<remaining_work>

- **Phase 9 (α) — Self-Maintaining Derived Artifacts + Curator** (MAINT-01..04): not started.
- **Phase 10 (β) — Context-Economy Fan-out/Synthesize** (ECON-01..03): not started.
- **Phase 11 (γ) — Multi-Repo Workspace** (MREPO-01..04): not started.
- Sequencing locked 9 → 10 → 11 (β is the reusable fan-out substrate γ builds on).
</remaining_work>

<decisions_made>

- **Skip research** for v2.0 — features are internal harness patterns; MILESTONE-CONTEXT already mapped reuse + open decisions.
- **Continue numbering (9/10/11)**, not reset — single continuous phase history.
- **Skipped `phases.clear`** deliberately — see anti-pattern above.
- **Two KEY DECISIONS deferred to plan time (by design):**
  - (α, Phase 9) which derived artifacts flip *gitignored-derived → committed-derived* for PR refresh (the committed set = what the stale-derived gate guards).
  - (γ, Phase 11) workspace model a/b/c — **lean b: workspace manifest** (raise the `project.toml` slot pattern one level).
</decisions_made>

<blockers>
- **Unverified commits**: SSH signing key at `/home/claude/.ssh/commit_signing_key.pub` is a 0-byte placeholder (no private key, `ssh-keygen` absent) — commits can't be signed in this container. Author identity is correct (`Claude <noreply@anthropic.com>`); GitHub will show "Unverified". Environment limitation, not an attribution error.
- **BOOT-01 (carried from v1.0)**: .NET 10 SDK install egress-denied. Does NOT block v2.0 planning/execution — phases 9/10/11 are harness/agent/CI infra with no .NET compile.
</blockers>

## Required Reading (in order)
1. `.planning/PROJECT.md` (`## Current Milestone: v2.0 Long-Horizon`) — goal, target features, non-negotiables, open decisions.
2. `.planning/REQUIREMENTS.md` (`## Milestone v2.0 Requirements — Long-Horizon`) — the 11 REQs + cross-cutting constraints.
3. `.planning/ROADMAP.md` (Phases 9/10/11) — goals, success criteria, plan-time KEY DECISIONs.
4. `.planning/STATE.md` — position + Accumulated Context (v1.0 decisions carried forward).

## Infrastructure State
- Git: clean, pushed to `origin/claude/data-pipeline-harness-8aypct`.
- No running services/watchers.
- Reuse-not-rebuild targets already in-repo: `tools/memory_regen`, `/docs-sync`, `orchestrator` persona, Phase-6 CI gates, Phase-7 emitter (`tools/harness_emit`), `harness/project.toml` slot pattern, `contract_drift`/`contract_hash`.

<context>
v2.0's north star: an agent (and human) carries multi-project work across many sessions and long calendar time while the project's own context (code graph, memory, docs, decisions) stays automatically fresh — machines maintain, humans ratify. Every v2.0 item REUSES existing machinery; do not rebuild. Per-phase non-negotiables: derived-never-hand-edited (machine-write + CI-verify is OK), machines-gate/humans-ratify for the constitution plane, GEN-04 core→example (γ: core→workspace-member) no-dependency stays green, every new agent/skill/hook round-trips the Phase-7 emitter to both runtimes (opencode primary, Claude secondary), no model identifiers in artifacts, prefer PR/CI enforcement over per-commit hooks for heavy regen.
</context>

<next_action>
Start with: `/clear` then `/gsd:discuss-phase 9` — gather context and surface the committed-derived vs session-derived KEY DECISION before planning. (Or `/gsd:plan-phase 9` to plan directly.)
</next_action>
