---
context: default
phase: milestone-boundary
task: next-milestone-scoping
status: paused
last_updated: 2026-07-18
---

# Continue Here — v2.1 shipped, next milestone scoping deferred

> Written at the user's request (2026-07-18): "1번(하네스 하드닝) 포함 + 다른 기능도 추가할거거든? 핸드오프 적어놔줘. 다음 세션에서 할게."
> Machine-readable twin: `.planning/HANDOFF.json`.

## Where things stand

**v2.1 (MEM2 — Process Memory & Provenance Reframe) is SHIPPED.**
- 7/7 requirements (MEM2-01..07) verified; every phase 12–16 has a passing `VERIFICATION.md`.
- Milestone audit refreshed to `status: passed`; archived to `.planning/milestones/v2.1-*`.
- `git tag v2.1` created **and pushed**; branch `claude/data-pipeline-harness-8aypct` pushed to `origin`.
- Full suite: **690 passed / 0 failed**. Working tree clean (except pre-existing untracked `.mcp.json`).

This session also, before archiving: built + verified Phase 16 (local memory web UI, incl. a live
Orca-browser round-trip 5/5), backfilled the missing 12/13/14 VERIFICATION.md, made Phases 13 & 14
Nyquist-compliant, closed the 6-broken-hooks quick task, and wrote `tools/memory_ui/README.md`.

## Next action (next session)

Run **`/gsd:new-milestone`** fresh. It was entered this session but **intentionally paused at the
"what to build next" gate — no requirements/roadmap were written**, so start it clean.

Version: **v2.2** (minor) or **v3.0** (major) — user picks.

### Milestone theme (from the user)

**Harness hardening / gate-tightening** — the v2.1 carried follow-ups — **PLUS additional features
the user will name next session** (do NOT assume them; ask during `new-milestone` questioning).

Hardening candidates already enumerated (details + evidence in `HANDOFF.json` →
`candidate_requirements_for_hardening_theme`, sourced from `milestones/v2.1-MILESTONE-AUDIT.md`
`tech_debt`):
1. **CI-gate the provenance lint** — no `.pre-commit-config.yaml`, no CI step running
   `python -m tools.harness_lint.provenance` against live `.memory/agreements/`.
2. **Fix `check_agreement()` body-shape gap** — validates frontmatter only; an empty-body agreement
   passes lint but is silently dropped by `_render_agreement()`.
3. **Verify the CI emit-drift gate against untracked files** — 15-REVIEW CR-01; confirm
   `.github/workflows/ci.yml` uses the untracked-safe `git add -A` + `git diff --cached` idiom.
4. **Reword the `EXPECTED_COMMANDS` ghost** — named in MEM2-04 text + ROADMAP Phase 15 SC1 but never
   existed in source (D-11).
5. (optional, deferred in v2.1) **MEM2-F1** — per-instance agreement overlays for `examples/*`.

## Required reading before scoping
- `.planning/PROJECT.md` — Current State + Next Milestone note
- `.planning/MILESTONES.md` — v2.1 accomplishments
- `.planning/milestones/v2.1-MILESTONE-AUDIT.md` — the passed audit + `tech_debt` (candidate source)
- `.planning/milestones/v2.1-REQUIREMENTS.md` — v2 Requirements / Out of Scope (MEM2-F1)

## Notes
- `gsd-sdk` on PATH is a **stale npx-cached global**; the repo's own `.claude/get-shit-done` is newer.
- `.claude/settings.local.json` may carry `HARNESS_DEV_BYPASS` (Phase-17 dev posture) — constitution
  writes can land in-session without the human token; CODEOWNERS still gates merge.
