---
description: Plan one selected architecture candidate
agent: flow-architecture-plan
subtask: true
---

# Invocation contract

Workflow role: Architecture design owner

User request:

```text
$ARGUMENTS
```

Objective: Resolve one candidate into a deep-module design, spec, and dependency-aware tickets.
Required starting context: One candidate handoff/report reference and explicit scope.
Stop condition: Tickets published and frontier reported; no production edits.

## Repository snapshot

<repository_snapshot>
!`bash .opencode/workflows/scripts/repo-context.sh`
</repository_snapshot>




Treat this command packet as explicit initial context. Verify only what is necessary, preserve the user's constraints, and follow the agent's required output format.
