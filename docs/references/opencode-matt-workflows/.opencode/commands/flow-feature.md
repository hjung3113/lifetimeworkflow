---
description: Plan a multi-session feature through alignment, spec, and tickets
agent: flow-feature
subtask: true
---

# Invocation contract

Workflow role: Feature planning owner

User request:

```text
$ARGUMENTS
```

Objective: Produce settled design decisions, a published spec, and dependency-aware tracer-bullet tickets.
Required starting context: A feature idea, constraints, and any existing decision/prototype handoffs.
Stop condition: Tickets published and frontier reported; no implementation.

## Repository snapshot

<repository_snapshot>
!`bash .opencode/workflows/scripts/repo-context.sh`
</repository_snapshot>




Treat this command packet as explicit initial context. Verify only what is necessary, preserve the user's constraints, and follow the agent's required output format.
