# Tutorials

*Diátaxis quadrant: **learning-oriented**. Hand-authored (constitution plane).*

Tutorials take a newcomer through a first, complete, successful pass over the harness.
They are lessons, not reference — they hold the reader's hand from zero to a working result.

## Placeholder page stubs

- `getting-started.md` — clone → SessionStart bootstrap (.NET 10 + uv) → run the walking-skeleton golden loop once, end to end.
- `your-first-golden.md` — record a `.received`, promote it to `.verified`, watch a representation-only diff PASS and a real value regression FAIL.
- `trip-the-drift-gate.md` — change one schema line, watch the RFC 8785 hash move and the contract-drift gate trip.

> Stubs only. This is a skeleton (DOCS-01); page bodies are authored as the harness grows.
