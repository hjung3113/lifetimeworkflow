{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://harness.local/contracts/harness/adoption/manifest.schema.json",
  "title": "Brownfield adoption disposition manifest",
  "description": "ADOPT-03 complete destination/disposition manifest for a brownfield target: exactly one of create/preserve/conflict/marker-merge/derived-regenerate/human-ratification-required assigned to every harness destination (D-03/D-04 total rule chain), plus a separate excluded[] array recording GSD-owned lanes by reason so \"excluded\" is distinguishable from \"missed\". Self-contained Draft 2020-12 — no cross-file $ref (D-11).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "target_ref",
    "dispositions",
    "excluded"
  ],
  "properties": {
    "target_ref": {
      "type": "string",
      "minLength": 1,
      "description": "Ties this manifest to the same scan as the inventory's target_ref (same shape: a git ref, or the literal \"unknown\")."
    },
    "dispositions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/dispositionRecord"
      }
    },
    "excluded": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/excludedDestinationRecord"
      }
    },
    "installed": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/installedRecord"
      },
      "description": "Optional (MONO-12): the installed-file records this manifest's dispositions were resolved against, copied verbatim from the target's .harness/adoption/installed.json. Absent on a first-ever adopt and on every Phase-52-era manifest — this array is never added to `required`, so older documents stay valid."
    }
  },
  "$defs": {
    "evidenceRef": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "sha256"
      ],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$",
          "description": "Repo-relative POSIX path pointing INTO an already-hashed inventory includedEntry."
        },
        "sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        },
        "size": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "dispositionEnum": {
      "type": "string",
      "enum": [
        "create",
        "preserve",
        "conflict",
        "marker-merge",
        "derived-regenerate",
        "human-ratification-required",
        "update"
      ],
      "description": "Exactly these 7 values, no more, no fewer: D-03/D-04's total rule chain plus MONO-12's `update` — fired when the target's current hash equals the recorded installed_sha256 AND the recomputed harness payload hash now differs (a harness-side move, not a target-side edit)."
    },
    "dispositionRecord": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "destination",
        "disposition"
      ],
      "properties": {
        "destination": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$",
          "description": "A repo-relative POSIX path in the TARGET tree."
        },
        "disposition": {
          "$ref": "#/$defs/dispositionEnum"
        },
        "reason": {
          "type": "string",
          "description": "Optional: which rule in the chain fired, e.g. constitution-plane / marker-capable:AGENTS.md / hash-equal / hash-differ / no-existing-target."
        },
        "evidence": {
          "type": "array",
          "minItems": 0,
          "items": {
            "$ref": "#/$defs/evidenceRef"
          },
          "description": "Optional; may be empty for path-only rules that never read content."
        }
      }
    },
    "excludedDestinationRecord": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "destination",
        "reason"
      ],
      "properties": {
        "destination": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"
        },
        "reason": {
          "type": "string",
          "minLength": 1,
          "description": "E.g. \"gsd-owned\" — GSD-owned lanes are recorded here as EXCLUDED, never as a dispositions[] entry, so a reviewer can tell \"excluded\" apart from \"missed\"."
        }
      }
    },
    "installedRecord": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "destination",
        "installed_sha256",
        "batch_id"
      ],
      "properties": {
        "destination": {
          "type": "string",
          "minLength": 1,
          "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+$",
          "description": "A repo-relative POSIX path in the TARGET tree that /adopt itself wrote."
        },
        "installed_sha256": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$",
          "description": "sha256 of the bytes /adopt actually wrote at this destination, AS WRITTEN (post-splice for harness/project.toml). Exactly one hash is stored: the source side is recomputed every run by destinations.harness_proposed_hashes(), so a second source_sha256 field would only go stale."
        },
        "batch_id": {
          "type": "string",
          "minLength": 1,
          "description": "The adoption batch that wrote these bytes."
        }
      }
    }
  }
}
